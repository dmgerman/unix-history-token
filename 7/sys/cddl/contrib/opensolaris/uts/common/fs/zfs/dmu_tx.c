begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * CDDL HEADER START  *  * The contents of this file are subject to the terms of the  * Common Development and Distribution License (the "License").  * You may not use this file except in compliance with the License.  *  * You can obtain a copy of the license at usr/src/OPENSOLARIS.LICENSE  * or http://www.opensolaris.org/os/licensing.  * See the License for the specific language governing permissions  * and limitations under the License.  *  * When distributing Covered Code, include this CDDL HEADER in each  * file and include the License file at usr/src/OPENSOLARIS.LICENSE.  * If applicable, add the following below this CDDL HEADER, with the  * fields enclosed by brackets "[]" replaced with your own identifying  * information: Portions Copyright [yyyy] [name of copyright owner]  *  * CDDL HEADER END  */
end_comment

begin_comment
comment|/*  * Copyright 2006 Sun Microsystems, Inc.  All rights reserved.  * Use is subject to license terms.  */
end_comment

begin_pragma
pragma|#
directive|pragma
name|ident
literal|"%Z%%M%	%I%	%E% SMI"
end_pragma

begin_include
include|#
directive|include
file|<sys/dmu.h>
end_include

begin_include
include|#
directive|include
file|<sys/dmu_impl.h>
end_include

begin_include
include|#
directive|include
file|<sys/dbuf.h>
end_include

begin_include
include|#
directive|include
file|<sys/dmu_tx.h>
end_include

begin_include
include|#
directive|include
file|<sys/dmu_objset.h>
end_include

begin_include
include|#
directive|include
file|<sys/dsl_dataset.h>
end_include

begin_comment
comment|/* for dsl_dataset_block_freeable() */
end_comment

begin_include
include|#
directive|include
file|<sys/dsl_dir.h>
end_include

begin_comment
comment|/* for dsl_dir_tempreserve_*() */
end_comment

begin_include
include|#
directive|include
file|<sys/dsl_pool.h>
end_include

begin_include
include|#
directive|include
file|<sys/zap_impl.h>
end_include

begin_comment
comment|/* for fzap_default_block_shift */
end_comment

begin_include
include|#
directive|include
file|<sys/spa.h>
end_include

begin_include
include|#
directive|include
file|<sys/zfs_context.h>
end_include

begin_typedef
typedef|typedef
name|void
function_decl|(
modifier|*
name|dmu_tx_hold_func_t
function_decl|)
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|struct
name|dnode
modifier|*
name|dn
parameter_list|,
name|uint64_t
name|arg1
parameter_list|,
name|uint64_t
name|arg2
parameter_list|)
function_decl|;
end_typedef

begin_function
name|dmu_tx_t
modifier|*
name|dmu_tx_create_dd
parameter_list|(
name|dsl_dir_t
modifier|*
name|dd
parameter_list|)
block|{
name|dmu_tx_t
modifier|*
name|tx
init|=
name|kmem_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
name|dmu_tx_t
argument_list|)
argument_list|,
name|KM_SLEEP
argument_list|)
decl_stmt|;
name|tx
operator|->
name|tx_dir
operator|=
name|dd
expr_stmt|;
if|if
condition|(
name|dd
condition|)
name|tx
operator|->
name|tx_pool
operator|=
name|dd
operator|->
name|dd_pool
expr_stmt|;
name|list_create
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
sizeof|sizeof
argument_list|(
name|dmu_tx_hold_t
argument_list|)
argument_list|,
name|offsetof
argument_list|(
name|dmu_tx_hold_t
argument_list|,
name|txh_node
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|ZFS_DEBUG
name|refcount_create
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|)
expr_stmt|;
name|refcount_create
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|tx
operator|)
return|;
block|}
end_function

begin_function
name|dmu_tx_t
modifier|*
name|dmu_tx_create
parameter_list|(
name|objset_t
modifier|*
name|os
parameter_list|)
block|{
name|dmu_tx_t
modifier|*
name|tx
init|=
name|dmu_tx_create_dd
argument_list|(
name|os
operator|->
name|os
operator|->
name|os_dsl_dataset
operator|->
name|ds_dir
argument_list|)
decl_stmt|;
name|tx
operator|->
name|tx_objset
operator|=
name|os
expr_stmt|;
name|tx
operator|->
name|tx_lastsnap_txg
operator|=
name|dsl_dataset_prev_snap_txg
argument_list|(
name|os
operator|->
name|os
operator|->
name|os_dsl_dataset
argument_list|)
expr_stmt|;
return|return
operator|(
name|tx
operator|)
return|;
block|}
end_function

begin_function
name|dmu_tx_t
modifier|*
name|dmu_tx_create_assigned
parameter_list|(
name|struct
name|dsl_pool
modifier|*
name|dp
parameter_list|,
name|uint64_t
name|txg
parameter_list|)
block|{
name|dmu_tx_t
modifier|*
name|tx
init|=
name|dmu_tx_create_dd
argument_list|(
name|NULL
argument_list|)
decl_stmt|;
name|ASSERT3U
argument_list|(
name|txg
argument_list|,
operator|<=
argument_list|,
name|dp
operator|->
name|dp_tx
operator|.
name|tx_open_txg
argument_list|)
expr_stmt|;
name|tx
operator|->
name|tx_pool
operator|=
name|dp
expr_stmt|;
name|tx
operator|->
name|tx_txg
operator|=
name|txg
expr_stmt|;
name|tx
operator|->
name|tx_anyobj
operator|=
name|TRUE
expr_stmt|;
return|return
operator|(
name|tx
operator|)
return|;
block|}
end_function

begin_function
name|int
name|dmu_tx_is_syncing
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
return|return
operator|(
name|tx
operator|->
name|tx_anyobj
operator|)
return|;
block|}
end_function

begin_function
name|int
name|dmu_tx_private_ok
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
return|return
operator|(
name|tx
operator|->
name|tx_anyobj
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|dmu_tx_hold_t
modifier|*
name|dmu_tx_hold_object_impl
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|objset_t
modifier|*
name|os
parameter_list|,
name|uint64_t
name|object
parameter_list|,
name|enum
name|dmu_tx_hold_type
name|type
parameter_list|,
name|uint64_t
name|arg1
parameter_list|,
name|uint64_t
name|arg2
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|dnode_t
modifier|*
name|dn
init|=
name|NULL
decl_stmt|;
name|int
name|err
decl_stmt|;
if|if
condition|(
name|object
operator|!=
name|DMU_NEW_OBJECT
condition|)
block|{
name|err
operator|=
name|dnode_hold
argument_list|(
name|os
operator|->
name|os
argument_list|,
name|object
argument_list|,
name|tx
argument_list|,
operator|&
name|dn
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
if|if
condition|(
name|err
operator|==
literal|0
operator|&&
name|tx
operator|->
name|tx_txg
operator|!=
literal|0
condition|)
block|{
name|mutex_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
comment|/* 			 * dn->dn_assigned_txg == tx->tx_txg doesn't pose a 			 * problem, but there's no way for it to happen (for 			 * now, at least). 			 */
name|ASSERT
argument_list|(
name|dn
operator|->
name|dn_assigned_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|dn
operator|->
name|dn_assigned_txg
operator|=
name|tx
operator|->
name|tx_txg
expr_stmt|;
operator|(
name|void
operator|)
name|refcount_add
argument_list|(
operator|&
name|dn
operator|->
name|dn_tx_holds
argument_list|,
name|tx
argument_list|)
expr_stmt|;
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
block|}
block|}
name|txh
operator|=
name|kmem_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
name|dmu_tx_hold_t
argument_list|)
argument_list|,
name|KM_SLEEP
argument_list|)
expr_stmt|;
name|txh
operator|->
name|txh_tx
operator|=
name|tx
expr_stmt|;
name|txh
operator|->
name|txh_dnode
operator|=
name|dn
expr_stmt|;
ifdef|#
directive|ifdef
name|ZFS_DEBUG
name|txh
operator|->
name|txh_type
operator|=
name|type
expr_stmt|;
name|txh
operator|->
name|txh_arg1
operator|=
name|arg1
expr_stmt|;
name|txh
operator|->
name|txh_arg2
operator|=
name|arg2
expr_stmt|;
endif|#
directive|endif
name|list_insert_tail
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
expr_stmt|;
return|return
operator|(
name|txh
operator|)
return|;
block|}
end_function

begin_function
name|void
name|dmu_tx_add_new_object
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|objset_t
modifier|*
name|os
parameter_list|,
name|uint64_t
name|object
parameter_list|)
block|{
comment|/* 	 * If we're syncing, they can manipulate any object anyhow, and 	 * the hold on the dnode_t can cause problems. 	 */
if|if
condition|(
operator|!
name|dmu_tx_is_syncing
argument_list|(
name|tx
argument_list|)
condition|)
block|{
operator|(
name|void
operator|)
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|os
argument_list|,
name|object
argument_list|,
name|THT_NEWOBJECT
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_function
specifier|static
name|int
name|dmu_tx_check_ioerr
parameter_list|(
name|zio_t
modifier|*
name|zio
parameter_list|,
name|dnode_t
modifier|*
name|dn
parameter_list|,
name|int
name|level
parameter_list|,
name|uint64_t
name|blkid
parameter_list|)
block|{
name|int
name|err
decl_stmt|;
name|dmu_buf_impl_t
modifier|*
name|db
decl_stmt|;
name|rw_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|,
name|RW_READER
argument_list|)
expr_stmt|;
name|db
operator|=
name|dbuf_hold_level
argument_list|(
name|dn
argument_list|,
name|level
argument_list|,
name|blkid
argument_list|,
name|FTAG
argument_list|)
expr_stmt|;
name|rw_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|db
operator|==
name|NULL
condition|)
return|return
operator|(
name|EIO
operator|)
return|;
name|err
operator|=
name|dbuf_read
argument_list|(
name|db
argument_list|,
name|zio
argument_list|,
name|DB_RF_CANFAIL
argument_list|)
expr_stmt|;
name|dbuf_rele
argument_list|(
name|db
argument_list|,
name|FTAG
argument_list|)
expr_stmt|;
return|return
operator|(
name|err
operator|)
return|;
block|}
end_function

begin_comment
comment|/* ARGSUSED */
end_comment

begin_function
specifier|static
name|void
name|dmu_tx_count_write
parameter_list|(
name|dmu_tx_hold_t
modifier|*
name|txh
parameter_list|,
name|uint64_t
name|off
parameter_list|,
name|uint64_t
name|len
parameter_list|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
name|uint64_t
name|start
decl_stmt|,
name|end
decl_stmt|,
name|i
decl_stmt|;
name|int
name|min_bs
decl_stmt|,
name|max_bs
decl_stmt|,
name|min_ibs
decl_stmt|,
name|max_ibs
decl_stmt|,
name|epbs
decl_stmt|,
name|bits
decl_stmt|;
name|int
name|err
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|len
operator|==
literal|0
condition|)
return|return;
name|min_bs
operator|=
name|SPA_MINBLOCKSHIFT
expr_stmt|;
name|max_bs
operator|=
name|SPA_MAXBLOCKSHIFT
expr_stmt|;
name|min_ibs
operator|=
name|DN_MIN_INDBLKSHIFT
expr_stmt|;
name|max_ibs
operator|=
name|DN_MAX_INDBLKSHIFT
expr_stmt|;
comment|/* 	 * For i/o error checking, read the first and last level-0 	 * blocks (if they are not aligned), and all the level-1 blocks. 	 */
if|if
condition|(
name|dn
condition|)
block|{
if|if
condition|(
name|dn
operator|->
name|dn_maxblkid
operator|==
literal|0
condition|)
block|{
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|NULL
argument_list|,
name|dn
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
goto|goto
name|out
goto|;
block|}
else|else
block|{
name|zio_t
modifier|*
name|zio
init|=
name|zio_root
argument_list|(
name|dn
operator|->
name|dn_objset
operator|->
name|os_spa
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|ZIO_FLAG_CANFAIL
argument_list|)
decl_stmt|;
comment|/* first level-0 block */
name|start
operator|=
name|off
operator|>>
name|dn
operator|->
name|dn_datablkshift
expr_stmt|;
if|if
condition|(
name|P2PHASE
argument_list|(
name|off
argument_list|,
name|dn
operator|->
name|dn_datablksz
argument_list|)
operator|||
name|len
operator|<
name|dn
operator|->
name|dn_datablksz
condition|)
block|{
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|zio
argument_list|,
name|dn
argument_list|,
literal|0
argument_list|,
name|start
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
goto|goto
name|out
goto|;
block|}
comment|/* last level-0 block */
name|end
operator|=
operator|(
name|off
operator|+
name|len
operator|-
literal|1
operator|)
operator|>>
name|dn
operator|->
name|dn_datablkshift
expr_stmt|;
if|if
condition|(
name|end
operator|!=
name|start
operator|&&
name|P2PHASE
argument_list|(
name|off
operator|+
name|len
argument_list|,
name|dn
operator|->
name|dn_datablksz
argument_list|)
condition|)
block|{
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|zio
argument_list|,
name|dn
argument_list|,
literal|0
argument_list|,
name|end
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
goto|goto
name|out
goto|;
block|}
comment|/* level-1 blocks */
if|if
condition|(
name|dn
operator|->
name|dn_nlevels
operator|>
literal|1
condition|)
block|{
name|start
operator|>>=
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
name|end
operator|>>=
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
for|for
control|(
name|i
operator|=
name|start
operator|+
literal|1
init|;
name|i
operator|<
name|end
condition|;
name|i
operator|++
control|)
block|{
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|zio
argument_list|,
name|dn
argument_list|,
literal|1
argument_list|,
name|i
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
goto|goto
name|out
goto|;
block|}
block|}
name|err
operator|=
name|zio_wait
argument_list|(
name|zio
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
goto|goto
name|out
goto|;
block|}
block|}
comment|/* 	 * If there's more than one block, the blocksize can't change, 	 * so we can make a more precise estimate.  Alternatively, 	 * if the dnode's ibs is larger than max_ibs, always use that. 	 * This ensures that if we reduce DN_MAX_INDBLKSHIFT, 	 * the code will still work correctly on existing pools. 	 */
if|if
condition|(
name|dn
operator|&&
operator|(
name|dn
operator|->
name|dn_maxblkid
operator|!=
literal|0
operator|||
name|dn
operator|->
name|dn_indblkshift
operator|>
name|max_ibs
operator|)
condition|)
block|{
name|min_ibs
operator|=
name|max_ibs
operator|=
name|dn
operator|->
name|dn_indblkshift
expr_stmt|;
if|if
condition|(
name|dn
operator|->
name|dn_datablkshift
operator|!=
literal|0
condition|)
name|min_bs
operator|=
name|max_bs
operator|=
name|dn
operator|->
name|dn_datablkshift
expr_stmt|;
block|}
comment|/* 	 * 'end' is the last thing we will access, not one past. 	 * This way we won't overflow when accessing the last byte. 	 */
name|start
operator|=
name|P2ALIGN
argument_list|(
name|off
argument_list|,
literal|1ULL
operator|<<
name|max_bs
argument_list|)
expr_stmt|;
name|end
operator|=
name|P2ROUNDUP
argument_list|(
name|off
operator|+
name|len
argument_list|,
literal|1ULL
operator|<<
name|max_bs
argument_list|)
operator|-
literal|1
expr_stmt|;
name|txh
operator|->
name|txh_space_towrite
operator|+=
name|end
operator|-
name|start
operator|+
literal|1
expr_stmt|;
name|start
operator|>>=
name|min_bs
expr_stmt|;
name|end
operator|>>=
name|min_bs
expr_stmt|;
name|epbs
operator|=
name|min_ibs
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
comment|/* 	 * The object contains at most 2^(64 - min_bs) blocks, 	 * and each indirect level maps 2^epbs. 	 */
for|for
control|(
name|bits
operator|=
literal|64
operator|-
name|min_bs
init|;
name|bits
operator|>=
literal|0
condition|;
name|bits
operator|-=
name|epbs
control|)
block|{
name|start
operator|>>=
name|epbs
expr_stmt|;
name|end
operator|>>=
name|epbs
expr_stmt|;
comment|/* 		 * If we increase the number of levels of indirection, 		 * we'll need new blkid=0 indirect blocks.  If start == 0, 		 * we're already accounting for that blocks; and if end == 0, 		 * we can't increase the number of levels beyond that. 		 */
if|if
condition|(
name|start
operator|!=
literal|0
operator|&&
name|end
operator|!=
literal|0
condition|)
name|txh
operator|->
name|txh_space_towrite
operator|+=
literal|1ULL
operator|<<
name|max_ibs
expr_stmt|;
name|txh
operator|->
name|txh_space_towrite
operator|+=
operator|(
name|end
operator|-
name|start
operator|+
literal|1
operator|)
operator|<<
name|max_ibs
expr_stmt|;
block|}
name|ASSERT
argument_list|(
name|txh
operator|->
name|txh_space_towrite
operator|<
literal|2
operator|*
name|DMU_MAX_ACCESS
argument_list|)
expr_stmt|;
name|out
label|:
if|if
condition|(
name|err
condition|)
name|txh
operator|->
name|txh_tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|void
name|dmu_tx_count_dnode
parameter_list|(
name|dmu_tx_hold_t
modifier|*
name|txh
parameter_list|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
name|dnode_t
modifier|*
name|mdn
init|=
name|txh
operator|->
name|txh_tx
operator|->
name|tx_objset
operator|->
name|os
operator|->
name|os_meta_dnode
decl_stmt|;
name|uint64_t
name|space
init|=
name|mdn
operator|->
name|dn_datablksz
operator|+
operator|(
operator|(
name|mdn
operator|->
name|dn_nlevels
operator|-
literal|1
operator|)
operator|<<
name|mdn
operator|->
name|dn_indblkshift
operator|)
decl_stmt|;
if|if
condition|(
name|dn
operator|&&
name|dn
operator|->
name|dn_dbuf
operator|->
name|db_blkptr
operator|&&
name|dsl_dataset_block_freeable
argument_list|(
name|dn
operator|->
name|dn_objset
operator|->
name|os_dsl_dataset
argument_list|,
name|dn
operator|->
name|dn_dbuf
operator|->
name|db_blkptr
operator|->
name|blk_birth
argument_list|)
condition|)
block|{
name|txh
operator|->
name|txh_space_tooverwrite
operator|+=
name|space
expr_stmt|;
block|}
else|else
block|{
name|txh
operator|->
name|txh_space_towrite
operator|+=
name|space
expr_stmt|;
block|}
block|}
end_function

begin_function
name|void
name|dmu_tx_hold_write
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|object
parameter_list|,
name|uint64_t
name|off
parameter_list|,
name|int
name|len
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|len
operator|<
name|DMU_MAX_ACCESS
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|len
operator|==
literal|0
operator|||
name|UINT64_MAX
operator|-
name|off
operator|>=
name|len
operator|-
literal|1
argument_list|)
expr_stmt|;
name|txh
operator|=
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|tx
operator|->
name|tx_objset
argument_list|,
name|object
argument_list|,
name|THT_WRITE
argument_list|,
name|off
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|txh
operator|==
name|NULL
condition|)
return|return;
name|dmu_tx_count_write
argument_list|(
name|txh
argument_list|,
name|off
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|dmu_tx_count_dnode
argument_list|(
name|txh
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|void
name|dmu_tx_count_free
parameter_list|(
name|dmu_tx_hold_t
modifier|*
name|txh
parameter_list|,
name|uint64_t
name|off
parameter_list|,
name|uint64_t
name|len
parameter_list|)
block|{
name|uint64_t
name|blkid
decl_stmt|,
name|nblks
decl_stmt|;
name|uint64_t
name|space
init|=
literal|0
decl_stmt|;
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
name|dsl_dataset_t
modifier|*
name|ds
init|=
name|dn
operator|->
name|dn_objset
operator|->
name|os_dsl_dataset
decl_stmt|;
name|spa_t
modifier|*
name|spa
init|=
name|txh
operator|->
name|txh_tx
operator|->
name|tx_pool
operator|->
name|dp_spa
decl_stmt|;
name|int
name|dirty
decl_stmt|;
comment|/* 	 * We don't need to use any locking to check for dirtyness 	 * because it's OK if we get stale data -- the dnode may become 	 * dirty immediately after our check anyway.  This is just a 	 * means to avoid the expensive count when we aren't sure we 	 * need it.  We need to be able to deal with a dirty dnode. 	 */
name|dirty
operator|=
name|list_link_active
argument_list|(
operator|&
name|dn
operator|->
name|dn_dirty_link
index|[
literal|0
index|]
argument_list|)
operator||
name|list_link_active
argument_list|(
operator|&
name|dn
operator|->
name|dn_dirty_link
index|[
literal|1
index|]
argument_list|)
operator||
name|list_link_active
argument_list|(
operator|&
name|dn
operator|->
name|dn_dirty_link
index|[
literal|2
index|]
argument_list|)
operator||
name|list_link_active
argument_list|(
operator|&
name|dn
operator|->
name|dn_dirty_link
index|[
literal|3
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dirty
operator|||
name|dn
operator|->
name|dn_assigned_txg
operator|||
name|dn
operator|->
name|dn_phys
operator|->
name|dn_nlevels
operator|==
literal|0
condition|)
return|return;
comment|/* 	 * the struct_rwlock protects us against dn_phys->dn_nlevels 	 * changing, in case (against all odds) we manage to dirty& 	 * sync out the changes after we check for being dirty. 	 * also, dbuf_hold_impl() wants us to have the struct_rwlock. 	 * 	 * It's fine to use dn_datablkshift rather than the dn_phys 	 * equivalent because if it is changing, maxblkid==0 and we will 	 * bail. 	 */
name|rw_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|,
name|RW_READER
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|->
name|dn_phys
operator|->
name|dn_maxblkid
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|off
operator|==
literal|0
operator|&&
name|len
operator|>=
name|dn
operator|->
name|dn_datablksz
condition|)
block|{
name|blkid
operator|=
literal|0
expr_stmt|;
name|nblks
operator|=
literal|1
expr_stmt|;
block|}
else|else
block|{
name|rw_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
else|else
block|{
name|blkid
operator|=
name|off
operator|>>
name|dn
operator|->
name|dn_datablkshift
expr_stmt|;
name|nblks
operator|=
operator|(
name|off
operator|+
name|len
operator|)
operator|>>
name|dn
operator|->
name|dn_datablkshift
expr_stmt|;
if|if
condition|(
name|blkid
operator|>=
name|dn
operator|->
name|dn_phys
operator|->
name|dn_maxblkid
condition|)
block|{
name|rw_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|blkid
operator|+
name|nblks
operator|>
name|dn
operator|->
name|dn_phys
operator|->
name|dn_maxblkid
condition|)
name|nblks
operator|=
name|dn
operator|->
name|dn_phys
operator|->
name|dn_maxblkid
operator|-
name|blkid
expr_stmt|;
comment|/* don't bother after 128,000 blocks */
name|nblks
operator|=
name|MIN
argument_list|(
name|nblks
argument_list|,
literal|128
operator|*
literal|1024
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|dn
operator|->
name|dn_phys
operator|->
name|dn_nlevels
operator|==
literal|1
condition|)
block|{
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|nblks
condition|;
name|i
operator|++
control|)
block|{
name|blkptr_t
modifier|*
name|bp
init|=
name|dn
operator|->
name|dn_phys
operator|->
name|dn_blkptr
decl_stmt|;
name|ASSERT3U
argument_list|(
name|blkid
operator|+
name|i
argument_list|,
operator|<
argument_list|,
name|dn
operator|->
name|dn_phys
operator|->
name|dn_nblkptr
argument_list|)
expr_stmt|;
name|bp
operator|+=
name|blkid
operator|+
name|i
expr_stmt|;
if|if
condition|(
name|dsl_dataset_block_freeable
argument_list|(
name|ds
argument_list|,
name|bp
operator|->
name|blk_birth
argument_list|)
condition|)
block|{
name|dprintf_bp
argument_list|(
name|bp
argument_list|,
literal|"can free old%s"
argument_list|,
literal|""
argument_list|)
expr_stmt|;
name|space
operator|+=
name|bp_get_dasize
argument_list|(
name|spa
argument_list|,
name|bp
argument_list|)
expr_stmt|;
block|}
block|}
name|nblks
operator|=
literal|0
expr_stmt|;
block|}
while|while
condition|(
name|nblks
condition|)
block|{
name|dmu_buf_impl_t
modifier|*
name|dbuf
decl_stmt|;
name|int
name|err
decl_stmt|,
name|epbs
decl_stmt|,
name|blkoff
decl_stmt|,
name|tochk
decl_stmt|;
name|epbs
operator|=
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
name|blkoff
operator|=
name|P2PHASE
argument_list|(
name|blkid
argument_list|,
literal|1
operator|<<
name|epbs
argument_list|)
expr_stmt|;
name|tochk
operator|=
name|MIN
argument_list|(
operator|(
literal|1
operator|<<
name|epbs
operator|)
operator|-
name|blkoff
argument_list|,
name|nblks
argument_list|)
expr_stmt|;
name|err
operator|=
name|dbuf_hold_impl
argument_list|(
name|dn
argument_list|,
literal|1
argument_list|,
name|blkid
operator|>>
name|epbs
argument_list|,
name|TRUE
argument_list|,
name|FTAG
argument_list|,
operator|&
name|dbuf
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|==
literal|0
condition|)
block|{
name|int
name|i
decl_stmt|;
name|blkptr_t
modifier|*
name|bp
decl_stmt|;
name|err
operator|=
name|dbuf_read
argument_list|(
name|dbuf
argument_list|,
name|NULL
argument_list|,
name|DB_RF_HAVESTRUCT
operator||
name|DB_RF_CANFAIL
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|!=
literal|0
condition|)
block|{
name|txh
operator|->
name|txh_tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
name|dbuf_rele
argument_list|(
name|dbuf
argument_list|,
name|FTAG
argument_list|)
expr_stmt|;
break|break;
block|}
name|bp
operator|=
name|dbuf
operator|->
name|db
operator|.
name|db_data
expr_stmt|;
name|bp
operator|+=
name|blkoff
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|tochk
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|dsl_dataset_block_freeable
argument_list|(
name|ds
argument_list|,
name|bp
index|[
name|i
index|]
operator|.
name|blk_birth
argument_list|)
condition|)
block|{
name|dprintf_bp
argument_list|(
operator|&
name|bp
index|[
name|i
index|]
argument_list|,
literal|"can free old%s"
argument_list|,
literal|""
argument_list|)
expr_stmt|;
name|space
operator|+=
name|bp_get_dasize
argument_list|(
name|spa
argument_list|,
operator|&
name|bp
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
block|}
name|dbuf_rele
argument_list|(
name|dbuf
argument_list|,
name|FTAG
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|err
operator|&&
name|err
operator|!=
name|ENOENT
condition|)
block|{
name|txh
operator|->
name|txh_tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
break|break;
block|}
name|blkid
operator|+=
name|tochk
expr_stmt|;
name|nblks
operator|-=
name|tochk
expr_stmt|;
block|}
name|rw_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_struct_rwlock
argument_list|)
expr_stmt|;
name|txh
operator|->
name|txh_space_tofree
operator|+=
name|space
expr_stmt|;
block|}
end_function

begin_function
name|void
name|dmu_tx_hold_free
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|object
parameter_list|,
name|uint64_t
name|off
parameter_list|,
name|uint64_t
name|len
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|dnode_t
modifier|*
name|dn
decl_stmt|;
name|uint64_t
name|start
decl_stmt|,
name|end
decl_stmt|,
name|i
decl_stmt|;
name|int
name|err
decl_stmt|,
name|shift
decl_stmt|;
name|zio_t
modifier|*
name|zio
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|txh
operator|=
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|tx
operator|->
name|tx_objset
argument_list|,
name|object
argument_list|,
name|THT_FREE
argument_list|,
name|off
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|txh
operator|==
name|NULL
condition|)
return|return;
name|dn
operator|=
name|txh
operator|->
name|txh_dnode
expr_stmt|;
comment|/* first block */
if|if
condition|(
name|off
operator|!=
literal|0
condition|)
name|dmu_tx_count_write
argument_list|(
name|txh
argument_list|,
name|off
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* last block */
if|if
condition|(
name|len
operator|!=
name|DMU_OBJECT_END
condition|)
name|dmu_tx_count_write
argument_list|(
name|txh
argument_list|,
name|off
operator|+
name|len
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|off
operator|>=
operator|(
name|dn
operator|->
name|dn_maxblkid
operator|+
literal|1
operator|)
operator|*
name|dn
operator|->
name|dn_datablksz
condition|)
return|return;
if|if
condition|(
name|len
operator|==
name|DMU_OBJECT_END
condition|)
name|len
operator|=
operator|(
name|dn
operator|->
name|dn_maxblkid
operator|+
literal|1
operator|)
operator|*
name|dn
operator|->
name|dn_datablksz
operator|-
name|off
expr_stmt|;
comment|/* 	 * For i/o error checking, read the first and last level-0 	 * blocks, and all the level-1 blocks.  The above count_write's 	 * will take care of the level-0 blocks. 	 */
if|if
condition|(
name|dn
operator|->
name|dn_nlevels
operator|>
literal|1
condition|)
block|{
name|shift
operator|=
name|dn
operator|->
name|dn_datablkshift
operator|+
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
name|start
operator|=
name|off
operator|>>
name|shift
expr_stmt|;
name|end
operator|=
name|dn
operator|->
name|dn_datablkshift
condition|?
operator|(
operator|(
name|off
operator|+
name|len
operator|)
operator|>>
name|shift
operator|)
else|:
literal|0
expr_stmt|;
name|zio
operator|=
name|zio_root
argument_list|(
name|tx
operator|->
name|tx_pool
operator|->
name|dp_spa
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|ZIO_FLAG_CANFAIL
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
name|start
init|;
name|i
operator|<=
name|end
condition|;
name|i
operator|++
control|)
block|{
name|uint64_t
name|ibyte
init|=
name|i
operator|<<
name|shift
decl_stmt|;
name|err
operator|=
name|dnode_next_offset
argument_list|(
name|dn
argument_list|,
name|FALSE
argument_list|,
operator|&
name|ibyte
argument_list|,
literal|2
argument_list|,
literal|1
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|i
operator|=
name|ibyte
operator|>>
name|shift
expr_stmt|;
if|if
condition|(
name|err
operator|==
name|ESRCH
condition|)
break|break;
if|if
condition|(
name|err
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return;
block|}
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|zio
argument_list|,
name|dn
argument_list|,
literal|1
argument_list|,
name|i
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return;
block|}
block|}
name|err
operator|=
name|zio_wait
argument_list|(
name|zio
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return;
block|}
block|}
name|dmu_tx_count_dnode
argument_list|(
name|txh
argument_list|)
expr_stmt|;
name|dmu_tx_count_free
argument_list|(
name|txh
argument_list|,
name|off
argument_list|,
name|len
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|dmu_tx_hold_zap
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|object
parameter_list|,
name|int
name|add
parameter_list|,
name|char
modifier|*
name|name
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|dnode_t
modifier|*
name|dn
decl_stmt|;
name|uint64_t
name|nblocks
decl_stmt|;
name|int
name|epbs
decl_stmt|,
name|err
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|txh
operator|=
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|tx
operator|->
name|tx_objset
argument_list|,
name|object
argument_list|,
name|THT_ZAP
argument_list|,
name|add
argument_list|,
operator|(
name|uintptr_t
operator|)
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|txh
operator|==
name|NULL
condition|)
return|return;
name|dn
operator|=
name|txh
operator|->
name|txh_dnode
expr_stmt|;
name|dmu_tx_count_dnode
argument_list|(
name|txh
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|==
name|NULL
condition|)
block|{
comment|/* 		 * We will be able to fit a new object's entries into one leaf 		 * block.  So there will be at most 2 blocks total, 		 * including the header block. 		 */
name|dmu_tx_count_write
argument_list|(
name|txh
argument_list|,
literal|0
argument_list|,
literal|2
operator|<<
name|fzap_default_block_shift
argument_list|)
expr_stmt|;
return|return;
block|}
name|ASSERT3P
argument_list|(
name|dmu_ot
index|[
name|dn
operator|->
name|dn_type
index|]
operator|.
name|ot_byteswap
argument_list|,
operator|==
argument_list|,
name|zap_byteswap
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|->
name|dn_maxblkid
operator|==
literal|0
operator|&&
operator|!
name|add
condition|)
block|{
comment|/* 		 * If there is only one block  (i.e. this is a micro-zap) 		 * and we are not adding anything, the accounting is simple. 		 */
name|err
operator|=
name|dmu_tx_check_ioerr
argument_list|(
name|NULL
argument_list|,
name|dn
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return;
block|}
comment|/* 		 * Use max block size here, since we don't know how much 		 * the size will change between now and the dbuf dirty call. 		 */
if|if
condition|(
name|dsl_dataset_block_freeable
argument_list|(
name|dn
operator|->
name|dn_objset
operator|->
name|os_dsl_dataset
argument_list|,
name|dn
operator|->
name|dn_phys
operator|->
name|dn_blkptr
index|[
literal|0
index|]
operator|.
name|blk_birth
argument_list|)
condition|)
name|txh
operator|->
name|txh_space_tooverwrite
operator|+=
name|SPA_MAXBLOCKSIZE
expr_stmt|;
else|else
name|txh
operator|->
name|txh_space_towrite
operator|+=
name|SPA_MAXBLOCKSIZE
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|dn
operator|->
name|dn_maxblkid
operator|>
literal|0
operator|&&
name|name
condition|)
block|{
comment|/* 		 * access the name in this fat-zap so that we'll check 		 * for i/o errors to the leaf blocks, etc. 		 */
name|err
operator|=
name|zap_lookup
argument_list|(
operator|&
name|dn
operator|->
name|dn_objset
operator|->
name|os
argument_list|,
name|dn
operator|->
name|dn_object
argument_list|,
name|name
argument_list|,
literal|8
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|==
name|EIO
condition|)
block|{
name|tx
operator|->
name|tx_err
operator|=
name|err
expr_stmt|;
return|return;
block|}
block|}
comment|/* 	 * 3 blocks overwritten: target leaf, ptrtbl block, header block 	 * 3 new blocks written if adding: new split leaf, 2 grown ptrtbl blocks 	 */
name|dmu_tx_count_write
argument_list|(
name|txh
argument_list|,
name|dn
operator|->
name|dn_maxblkid
operator|*
name|dn
operator|->
name|dn_datablksz
argument_list|,
operator|(
literal|3
operator|+
name|add
condition|?
literal|3
else|:
literal|0
operator|)
operator|<<
name|dn
operator|->
name|dn_datablkshift
argument_list|)
expr_stmt|;
comment|/* 	 * If the modified blocks are scattered to the four winds, 	 * we'll have to modify an indirect twig for each. 	 */
name|epbs
operator|=
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
expr_stmt|;
for|for
control|(
name|nblocks
operator|=
name|dn
operator|->
name|dn_maxblkid
operator|>>
name|epbs
init|;
name|nblocks
operator|!=
literal|0
condition|;
name|nblocks
operator|>>=
name|epbs
control|)
name|txh
operator|->
name|txh_space_towrite
operator|+=
literal|3
operator|<<
name|dn
operator|->
name|dn_indblkshift
expr_stmt|;
block|}
end_function

begin_function
name|void
name|dmu_tx_hold_bonus
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|object
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|txh
operator|=
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|tx
operator|->
name|tx_objset
argument_list|,
name|object
argument_list|,
name|THT_BONUS
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|txh
condition|)
name|dmu_tx_count_dnode
argument_list|(
name|txh
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|dmu_tx_hold_space
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|space
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|txh
operator|=
name|dmu_tx_hold_object_impl
argument_list|(
name|tx
argument_list|,
name|tx
operator|->
name|tx_objset
argument_list|,
name|DMU_NEW_OBJECT
argument_list|,
name|THT_SPACE
argument_list|,
name|space
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|txh
operator|->
name|txh_space_towrite
operator|+=
name|space
expr_stmt|;
block|}
end_function

begin_function
name|int
name|dmu_tx_holds
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|object
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|int
name|holds
init|=
literal|0
decl_stmt|;
comment|/* 	 * By asserting that the tx is assigned, we're counting the 	 * number of dn_tx_holds, which is the same as the number of 	 * dn_holds.  Otherwise, we'd be counting dn_holds, but 	 * dn_tx_holds could be 0. 	 */
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|!=
literal|0
argument_list|)
expr_stmt|;
comment|/* if (tx->tx_anyobj == TRUE) */
comment|/* return (0); */
for|for
control|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
init|;
name|txh
condition|;
name|txh
operator|=
name|list_next
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
control|)
block|{
if|if
condition|(
name|txh
operator|->
name|txh_dnode
operator|&&
name|txh
operator|->
name|txh_dnode
operator|->
name|dn_object
operator|==
name|object
condition|)
name|holds
operator|++
expr_stmt|;
block|}
return|return
operator|(
name|holds
operator|)
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|ZFS_DEBUG
end_ifdef

begin_function
name|void
name|dmu_tx_dirty_buf
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|dmu_buf_impl_t
modifier|*
name|db
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|int
name|match_object
init|=
name|FALSE
decl_stmt|,
name|match_offset
init|=
name|FALSE
decl_stmt|;
name|dnode_t
modifier|*
name|dn
init|=
name|db
operator|->
name|db_dnode
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|!=
literal|0
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_objset
operator|==
name|NULL
operator|||
name|dn
operator|->
name|dn_objset
operator|==
name|tx
operator|->
name|tx_objset
operator|->
name|os
argument_list|)
expr_stmt|;
name|ASSERT3U
argument_list|(
name|dn
operator|->
name|dn_object
argument_list|,
operator|==
argument_list|,
name|db
operator|->
name|db
operator|.
name|db_object
argument_list|)
expr_stmt|;
if|if
condition|(
name|tx
operator|->
name|tx_anyobj
condition|)
return|return;
comment|/* XXX No checking on the meta dnode for now */
if|if
condition|(
name|db
operator|->
name|db
operator|.
name|db_object
operator|==
name|DMU_META_DNODE_OBJECT
condition|)
return|return;
for|for
control|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
init|;
name|txh
condition|;
name|txh
operator|=
name|list_next
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
control|)
block|{
name|ASSERT
argument_list|(
name|dn
operator|==
name|NULL
operator|||
name|dn
operator|->
name|dn_assigned_txg
operator|==
name|tx
operator|->
name|tx_txg
argument_list|)
expr_stmt|;
if|if
condition|(
name|txh
operator|->
name|txh_dnode
operator|==
name|dn
operator|&&
name|txh
operator|->
name|txh_type
operator|!=
name|THT_NEWOBJECT
condition|)
name|match_object
operator|=
name|TRUE
expr_stmt|;
if|if
condition|(
name|txh
operator|->
name|txh_dnode
operator|==
name|NULL
operator|||
name|txh
operator|->
name|txh_dnode
operator|==
name|dn
condition|)
block|{
name|int
name|datablkshift
init|=
name|dn
operator|->
name|dn_datablkshift
condition|?
name|dn
operator|->
name|dn_datablkshift
else|:
name|SPA_MAXBLOCKSHIFT
decl_stmt|;
name|int
name|epbs
init|=
name|dn
operator|->
name|dn_indblkshift
operator|-
name|SPA_BLKPTRSHIFT
decl_stmt|;
name|int
name|shift
init|=
name|datablkshift
operator|+
name|epbs
operator|*
name|db
operator|->
name|db_level
decl_stmt|;
name|uint64_t
name|beginblk
init|=
name|shift
operator|>=
literal|64
condition|?
literal|0
else|:
operator|(
name|txh
operator|->
name|txh_arg1
operator|>>
name|shift
operator|)
decl_stmt|;
name|uint64_t
name|endblk
init|=
name|shift
operator|>=
literal|64
condition|?
literal|0
else|:
operator|(
operator|(
name|txh
operator|->
name|txh_arg1
operator|+
name|txh
operator|->
name|txh_arg2
operator|-
literal|1
operator|)
operator|>>
name|shift
operator|)
decl_stmt|;
name|uint64_t
name|blkid
init|=
name|db
operator|->
name|db_blkid
decl_stmt|;
comment|/* XXX txh_arg2 better not be zero... */
name|dprintf
argument_list|(
literal|"found txh type %x beginblk=%llx endblk=%llx\n"
argument_list|,
name|txh
operator|->
name|txh_type
argument_list|,
name|beginblk
argument_list|,
name|endblk
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|txh
operator|->
name|txh_type
condition|)
block|{
case|case
name|THT_WRITE
case|:
if|if
condition|(
name|blkid
operator|>=
name|beginblk
operator|&&
name|blkid
operator|<=
name|endblk
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
comment|/* 				 * We will let this hold work for the bonus 				 * buffer so that we don't need to hold it 				 * when creating a new object. 				 */
if|if
condition|(
name|blkid
operator|==
name|DB_BONUS_BLKID
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
comment|/* 				 * They might have to increase nlevels, 				 * thus dirtying the new TLIBs.  Or the 				 * might have to change the block size, 				 * thus dirying the new lvl=0 blk=0. 				 */
if|if
condition|(
name|blkid
operator|==
literal|0
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
break|break;
case|case
name|THT_FREE
case|:
if|if
condition|(
name|blkid
operator|==
name|beginblk
operator|&&
operator|(
name|txh
operator|->
name|txh_arg1
operator|!=
literal|0
operator|||
name|dn
operator|->
name|dn_maxblkid
operator|==
literal|0
operator|)
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
if|if
condition|(
name|blkid
operator|==
name|endblk
operator|&&
name|txh
operator|->
name|txh_arg2
operator|!=
name|DMU_OBJECT_END
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
break|break;
case|case
name|THT_BONUS
case|:
if|if
condition|(
name|blkid
operator|==
name|DB_BONUS_BLKID
condition|)
name|match_offset
operator|=
name|TRUE
expr_stmt|;
break|break;
case|case
name|THT_ZAP
case|:
name|match_offset
operator|=
name|TRUE
expr_stmt|;
break|break;
case|case
name|THT_NEWOBJECT
case|:
name|match_object
operator|=
name|TRUE
expr_stmt|;
break|break;
default|default:
name|ASSERT
argument_list|(
operator|!
literal|"bad txh_type"
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|match_object
operator|&&
name|match_offset
condition|)
return|return;
block|}
name|panic
argument_list|(
literal|"dirtying dbuf obj=%llx lvl=%u blkid=%llx but not tx_held\n"
argument_list|,
operator|(
name|u_longlong_t
operator|)
name|db
operator|->
name|db
operator|.
name|db_object
argument_list|,
name|db
operator|->
name|db_level
argument_list|,
operator|(
name|u_longlong_t
operator|)
name|db
operator|->
name|db_blkid
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|int
name|dmu_tx_try_assign
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|txg_how
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|uint64_t
name|lsize
decl_stmt|,
name|asize
decl_stmt|,
name|fsize
decl_stmt|,
name|towrite
decl_stmt|,
name|tofree
decl_stmt|,
name|tooverwrite
decl_stmt|;
name|ASSERT3U
argument_list|(
name|tx
operator|->
name|tx_txg
argument_list|,
operator|==
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|tx
operator|->
name|tx_err
condition|)
return|return
operator|(
name|tx
operator|->
name|tx_err
operator|)
return|;
name|tx
operator|->
name|tx_txg
operator|=
name|txg_hold_open
argument_list|(
name|tx
operator|->
name|tx_pool
argument_list|,
operator|&
name|tx
operator|->
name|tx_txgh
argument_list|)
expr_stmt|;
name|tx
operator|->
name|tx_needassign_txh
operator|=
name|NULL
expr_stmt|;
comment|/* 	 * NB: No error returns are allowed after txg_hold_open, but 	 * before processing the dnode holds, due to the 	 * dmu_tx_unassign() logic. 	 */
name|towrite
operator|=
name|tofree
operator|=
name|tooverwrite
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
init|;
name|txh
condition|;
name|txh
operator|=
name|list_next
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
control|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
if|if
condition|(
name|dn
operator|!=
name|NULL
condition|)
block|{
name|mutex_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|->
name|dn_assigned_txg
operator|==
name|tx
operator|->
name|tx_txg
operator|-
literal|1
condition|)
block|{
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|tx
operator|->
name|tx_needassign_txh
operator|=
name|txh
expr_stmt|;
return|return
operator|(
name|ERESTART
operator|)
return|;
block|}
if|if
condition|(
name|dn
operator|->
name|dn_assigned_txg
operator|==
literal|0
condition|)
name|dn
operator|->
name|dn_assigned_txg
operator|=
name|tx
operator|->
name|tx_txg
expr_stmt|;
name|ASSERT3U
argument_list|(
name|dn
operator|->
name|dn_assigned_txg
argument_list|,
operator|==
argument_list|,
name|tx
operator|->
name|tx_txg
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|refcount_add
argument_list|(
operator|&
name|dn
operator|->
name|dn_tx_holds
argument_list|,
name|tx
argument_list|)
expr_stmt|;
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
block|}
name|towrite
operator|+=
name|txh
operator|->
name|txh_space_towrite
expr_stmt|;
name|tofree
operator|+=
name|txh
operator|->
name|txh_space_tofree
expr_stmt|;
name|tooverwrite
operator|+=
name|txh
operator|->
name|txh_space_tooverwrite
expr_stmt|;
block|}
comment|/* 	 * NB: This check must be after we've held the dnodes, so that 	 * the dmu_tx_unassign() logic will work properly 	 */
if|if
condition|(
name|txg_how
operator|>=
name|TXG_INITIAL
operator|&&
name|txg_how
operator|!=
name|tx
operator|->
name|tx_txg
condition|)
return|return
operator|(
name|ERESTART
operator|)
return|;
comment|/* 	 * If a snapshot has been taken since we made our estimates, 	 * assume that we won't be able to free or overwrite anything. 	 */
if|if
condition|(
name|tx
operator|->
name|tx_objset
operator|&&
name|dsl_dataset_prev_snap_txg
argument_list|(
name|tx
operator|->
name|tx_objset
operator|->
name|os
operator|->
name|os_dsl_dataset
argument_list|)
operator|>
name|tx
operator|->
name|tx_lastsnap_txg
condition|)
block|{
name|towrite
operator|+=
name|tooverwrite
expr_stmt|;
name|tooverwrite
operator|=
name|tofree
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 	 * Convert logical size to worst-case allocated size. 	 */
name|fsize
operator|=
name|spa_get_asize
argument_list|(
name|tx
operator|->
name|tx_pool
operator|->
name|dp_spa
argument_list|,
name|tooverwrite
argument_list|)
operator|+
name|tofree
expr_stmt|;
name|lsize
operator|=
name|towrite
operator|+
name|tooverwrite
expr_stmt|;
name|asize
operator|=
name|spa_get_asize
argument_list|(
name|tx
operator|->
name|tx_pool
operator|->
name|dp_spa
argument_list|,
name|lsize
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|ZFS_DEBUG
name|tx
operator|->
name|tx_space_towrite
operator|=
name|asize
expr_stmt|;
name|tx
operator|->
name|tx_space_tofree
operator|=
name|tofree
expr_stmt|;
name|tx
operator|->
name|tx_space_tooverwrite
operator|=
name|tooverwrite
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|tx
operator|->
name|tx_dir
operator|&&
name|asize
operator|!=
literal|0
condition|)
block|{
name|int
name|err
init|=
name|dsl_dir_tempreserve_space
argument_list|(
name|tx
operator|->
name|tx_dir
argument_list|,
name|lsize
argument_list|,
name|asize
argument_list|,
name|fsize
argument_list|,
operator|&
name|tx
operator|->
name|tx_tempreserve_cookie
argument_list|,
name|tx
argument_list|)
decl_stmt|;
if|if
condition|(
name|err
condition|)
return|return
operator|(
name|err
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|dmu_tx_unassign
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
if|if
condition|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
condition|)
return|return;
name|txg_rele_to_quiesce
argument_list|(
operator|&
name|tx
operator|->
name|tx_txgh
argument_list|)
expr_stmt|;
for|for
control|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
init|;
name|txh
operator|!=
name|tx
operator|->
name|tx_needassign_txh
condition|;
name|txh
operator|=
name|list_next
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
control|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
if|if
condition|(
name|dn
operator|==
name|NULL
condition|)
continue|continue;
name|mutex_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|ASSERT3U
argument_list|(
name|dn
operator|->
name|dn_assigned_txg
argument_list|,
operator|==
argument_list|,
name|tx
operator|->
name|tx_txg
argument_list|)
expr_stmt|;
if|if
condition|(
name|refcount_remove
argument_list|(
operator|&
name|dn
operator|->
name|dn_tx_holds
argument_list|,
name|tx
argument_list|)
operator|==
literal|0
condition|)
block|{
name|dn
operator|->
name|dn_assigned_txg
operator|=
literal|0
expr_stmt|;
name|cv_broadcast
argument_list|(
operator|&
name|dn
operator|->
name|dn_notxholds
argument_list|)
expr_stmt|;
block|}
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
block|}
name|txg_rele_to_sync
argument_list|(
operator|&
name|tx
operator|->
name|tx_txgh
argument_list|)
expr_stmt|;
name|tx
operator|->
name|tx_lasttried_txg
operator|=
name|tx
operator|->
name|tx_txg
expr_stmt|;
name|tx
operator|->
name|tx_txg
operator|=
literal|0
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Assign tx to a transaction group.  txg_how can be one of:  *  * (1)	TXG_WAIT.  If the current open txg is full, waits until there's  *	a new one.  This should be used when you're not holding locks.  *	If will only fail if we're truly out of space (or over quota).  *  * (2)	TXG_NOWAIT.  If we can't assign into the current open txg without  *	blocking, returns immediately with ERESTART.  This should be used  *	whenever you're holding locks.  On an ERESTART error, the caller  *	should drop locks, do a dmu_tx_wait(tx), and try again.  *  * (3)	A specific txg.  Use this if you need to ensure that multiple  *	transactions all sync in the same txg.  Like TXG_NOWAIT, it  *	returns ERESTART if it can't assign you into the requested txg.  */
end_comment

begin_function
name|int
name|dmu_tx_assign
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|uint64_t
name|txg_how
parameter_list|)
block|{
name|int
name|err
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|txg_how
operator|!=
literal|0
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
operator|!
name|dsl_pool_sync_context
argument_list|(
name|tx
operator|->
name|tx_pool
argument_list|)
argument_list|)
expr_stmt|;
while|while
condition|(
operator|(
name|err
operator|=
name|dmu_tx_try_assign
argument_list|(
name|tx
argument_list|,
name|txg_how
argument_list|)
operator|)
operator|!=
literal|0
condition|)
block|{
name|dmu_tx_unassign
argument_list|(
name|tx
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|!=
name|ERESTART
operator|||
name|txg_how
operator|!=
name|TXG_WAIT
condition|)
return|return
operator|(
name|err
operator|)
return|;
name|dmu_tx_wait
argument_list|(
name|tx
argument_list|)
expr_stmt|;
block|}
name|txg_rele_to_quiesce
argument_list|(
operator|&
name|tx
operator|->
name|tx_txgh
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
name|void
name|dmu_tx_wait
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_lasttried_txg
operator|!=
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|tx
operator|->
name|tx_needassign_txh
condition|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|tx
operator|->
name|tx_needassign_txh
operator|->
name|txh_dnode
decl_stmt|;
name|mutex_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
while|while
condition|(
name|dn
operator|->
name|dn_assigned_txg
operator|==
name|tx
operator|->
name|tx_lasttried_txg
operator|-
literal|1
condition|)
name|cv_wait
argument_list|(
operator|&
name|dn
operator|->
name|dn_notxholds
argument_list|,
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|tx
operator|->
name|tx_needassign_txh
operator|=
name|NULL
expr_stmt|;
block|}
else|else
block|{
name|txg_wait_open
argument_list|(
name|tx
operator|->
name|tx_pool
argument_list|,
name|tx
operator|->
name|tx_lasttried_txg
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_function
name|void
name|dmu_tx_willuse_space
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|,
name|int64_t
name|delta
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|ZFS_DEBUG
if|if
condition|(
name|tx
operator|->
name|tx_dir
operator|==
name|NULL
operator|||
name|delta
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
name|delta
operator|>
literal|0
condition|)
block|{
name|ASSERT3U
argument_list|(
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|)
operator|+
name|delta
argument_list|,
operator|<=
argument_list|,
name|tx
operator|->
name|tx_space_towrite
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|refcount_add_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|,
name|delta
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|(
name|void
operator|)
name|refcount_add_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|,
operator|-
name|delta
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
end_function

begin_function
name|void
name|dmu_tx_commit
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|!=
literal|0
argument_list|)
expr_stmt|;
while|while
condition|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
condition|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
name|list_remove
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
expr_stmt|;
name|kmem_free
argument_list|(
name|txh
argument_list|,
sizeof|sizeof
argument_list|(
name|dmu_tx_hold_t
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|==
name|NULL
condition|)
continue|continue;
name|mutex_enter
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|ASSERT3U
argument_list|(
name|dn
operator|->
name|dn_assigned_txg
argument_list|,
operator|==
argument_list|,
name|tx
operator|->
name|tx_txg
argument_list|)
expr_stmt|;
if|if
condition|(
name|refcount_remove
argument_list|(
operator|&
name|dn
operator|->
name|dn_tx_holds
argument_list|,
name|tx
argument_list|)
operator|==
literal|0
condition|)
block|{
name|dn
operator|->
name|dn_assigned_txg
operator|=
literal|0
expr_stmt|;
name|cv_broadcast
argument_list|(
operator|&
name|dn
operator|->
name|dn_notxholds
argument_list|)
expr_stmt|;
block|}
name|mutex_exit
argument_list|(
operator|&
name|dn
operator|->
name|dn_mtx
argument_list|)
expr_stmt|;
name|dnode_rele
argument_list|(
name|dn
argument_list|,
name|tx
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tx
operator|->
name|tx_tempreserve_cookie
condition|)
name|dsl_dir_tempreserve_clear
argument_list|(
name|tx
operator|->
name|tx_tempreserve_cookie
argument_list|,
name|tx
argument_list|)
expr_stmt|;
if|if
condition|(
name|tx
operator|->
name|tx_anyobj
operator|==
name|FALSE
condition|)
name|txg_rele_to_sync
argument_list|(
operator|&
name|tx
operator|->
name|tx_txgh
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|ZFS_DEBUG
name|dprintf
argument_list|(
literal|"towrite=%llu written=%llu tofree=%llu freed=%llu\n"
argument_list|,
name|tx
operator|->
name|tx_space_towrite
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|)
argument_list|,
name|tx
operator|->
name|tx_space_tofree
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|)
argument_list|)
expr_stmt|;
name|refcount_destroy_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|)
argument_list|)
expr_stmt|;
name|refcount_destroy_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|kmem_free
argument_list|(
name|tx
argument_list|,
sizeof|sizeof
argument_list|(
name|dmu_tx_t
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|dmu_tx_abort
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
name|dmu_tx_hold_t
modifier|*
name|txh
decl_stmt|;
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|==
literal|0
argument_list|)
expr_stmt|;
while|while
condition|(
name|txh
operator|=
name|list_head
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|)
condition|)
block|{
name|dnode_t
modifier|*
name|dn
init|=
name|txh
operator|->
name|txh_dnode
decl_stmt|;
name|list_remove
argument_list|(
operator|&
name|tx
operator|->
name|tx_holds
argument_list|,
name|txh
argument_list|)
expr_stmt|;
name|kmem_free
argument_list|(
name|txh
argument_list|,
sizeof|sizeof
argument_list|(
name|dmu_tx_hold_t
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|dn
operator|!=
name|NULL
condition|)
name|dnode_rele
argument_list|(
name|dn
argument_list|,
name|tx
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|ZFS_DEBUG
name|refcount_destroy_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_written
argument_list|)
argument_list|)
expr_stmt|;
name|refcount_destroy_many
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|,
name|refcount_count
argument_list|(
operator|&
name|tx
operator|->
name|tx_space_freed
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|kmem_free
argument_list|(
name|tx
argument_list|,
sizeof|sizeof
argument_list|(
name|dmu_tx_t
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|uint64_t
name|dmu_tx_get_txg
parameter_list|(
name|dmu_tx_t
modifier|*
name|tx
parameter_list|)
block|{
name|ASSERT
argument_list|(
name|tx
operator|->
name|tx_txg
operator|!=
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
name|tx
operator|->
name|tx_txg
operator|)
return|;
block|}
end_function

end_unit

