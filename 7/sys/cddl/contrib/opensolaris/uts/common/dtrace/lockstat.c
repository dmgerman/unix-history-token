begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * CDDL HEADER START  *  * The contents of this file are subject to the terms of the  * Common Development and Distribution License (the "License").  * You may not use this file except in compliance with the License.  *  * You can obtain a copy of the license at usr/src/OPENSOLARIS.LICENSE  * or http://www.opensolaris.org/os/licensing.  * See the License for the specific language governing permissions  * and limitations under the License.  *  * When distributing Covered Code, include this CDDL HEADER in each  * file and include the License file at usr/src/OPENSOLARIS.LICENSE.  * If applicable, add the following below this CDDL HEADER, with the  * fields enclosed by brackets "[]" replaced with your own identifying  * information: Portions Copyright [yyyy] [name of copyright owner]  *  * CDDL HEADER END  */
end_comment

begin_comment
comment|/*  * Copyright 2008 Sun Microsystems, Inc.  All rights reserved.  * Use is subject to license terms.  */
end_comment

begin_pragma
pragma|#
directive|pragma
name|ident
literal|"%Z%%M%	%I%	%E% SMI"
end_pragma

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<sys/open.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<sys/conf.h>
end_include

begin_include
include|#
directive|include
file|<sys/modctl.h>
end_include

begin_include
include|#
directive|include
file|<sys/cmn_err.h>
end_include

begin_include
include|#
directive|include
file|<sys/bitmap.h>
end_include

begin_include
include|#
directive|include
file|<sys/debug.h>
end_include

begin_include
include|#
directive|include
file|<sys/kmem.h>
end_include

begin_include
include|#
directive|include
file|<sys/errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/sysmacros.h>
end_include

begin_include
include|#
directive|include
file|<sys/lockstat.h>
end_include

begin_include
include|#
directive|include
file|<sys/atomic.h>
end_include

begin_include
include|#
directive|include
file|<sys/dtrace.h>
end_include

begin_include
include|#
directive|include
file|<sys/ddi.h>
end_include

begin_include
include|#
directive|include
file|<sys/sunddi.h>
end_include

begin_typedef
typedef|typedef
struct|struct
name|lockstat_probe
block|{
specifier|const
name|char
modifier|*
name|lsp_func
decl_stmt|;
specifier|const
name|char
modifier|*
name|lsp_name
decl_stmt|;
name|int
name|lsp_probe
decl_stmt|;
name|dtrace_id_t
name|lsp_id
decl_stmt|;
block|}
name|lockstat_probe_t
typedef|;
end_typedef

begin_decl_stmt
name|lockstat_probe_t
name|lockstat_probes
index|[]
init|=
block|{
block|{
name|LS_MUTEX_ENTER
block|,
name|LSA_ACQUIRE
block|,
name|LS_MUTEX_ENTER_ACQUIRE
block|}
block|,
block|{
name|LS_MUTEX_ENTER
block|,
name|LSA_BLOCK
block|,
name|LS_MUTEX_ENTER_BLOCK
block|}
block|,
block|{
name|LS_MUTEX_ENTER
block|,
name|LSA_SPIN
block|,
name|LS_MUTEX_ENTER_SPIN
block|}
block|,
block|{
name|LS_MUTEX_EXIT
block|,
name|LSA_RELEASE
block|,
name|LS_MUTEX_EXIT_RELEASE
block|}
block|,
block|{
name|LS_MUTEX_DESTROY
block|,
name|LSA_RELEASE
block|,
name|LS_MUTEX_DESTROY_RELEASE
block|}
block|,
block|{
name|LS_MUTEX_TRYENTER
block|,
name|LSA_ACQUIRE
block|,
name|LS_MUTEX_TRYENTER_ACQUIRE
block|}
block|,
block|{
name|LS_LOCK_SET
block|,
name|LSS_ACQUIRE
block|,
name|LS_LOCK_SET_ACQUIRE
block|}
block|,
block|{
name|LS_LOCK_SET
block|,
name|LSS_SPIN
block|,
name|LS_LOCK_SET_SPIN
block|}
block|,
block|{
name|LS_LOCK_SET_SPL
block|,
name|LSS_ACQUIRE
block|,
name|LS_LOCK_SET_SPL_ACQUIRE
block|}
block|,
block|{
name|LS_LOCK_SET_SPL
block|,
name|LSS_SPIN
block|,
name|LS_LOCK_SET_SPL_SPIN
block|}
block|,
block|{
name|LS_LOCK_TRY
block|,
name|LSS_ACQUIRE
block|,
name|LS_LOCK_TRY_ACQUIRE
block|}
block|,
block|{
name|LS_LOCK_CLEAR
block|,
name|LSS_RELEASE
block|,
name|LS_LOCK_CLEAR_RELEASE
block|}
block|,
block|{
name|LS_LOCK_CLEAR_SPLX
block|,
name|LSS_RELEASE
block|,
name|LS_LOCK_CLEAR_SPLX_RELEASE
block|}
block|,
block|{
name|LS_CLOCK_UNLOCK
block|,
name|LSS_RELEASE
block|,
name|LS_CLOCK_UNLOCK_RELEASE
block|}
block|,
block|{
name|LS_RW_ENTER
block|,
name|LSR_ACQUIRE
block|,
name|LS_RW_ENTER_ACQUIRE
block|}
block|,
block|{
name|LS_RW_ENTER
block|,
name|LSR_BLOCK
block|,
name|LS_RW_ENTER_BLOCK
block|}
block|,
block|{
name|LS_RW_EXIT
block|,
name|LSR_RELEASE
block|,
name|LS_RW_EXIT_RELEASE
block|}
block|,
block|{
name|LS_RW_TRYENTER
block|,
name|LSR_ACQUIRE
block|,
name|LS_RW_TRYENTER_ACQUIRE
block|}
block|,
block|{
name|LS_RW_TRYUPGRADE
block|,
name|LSR_UPGRADE
block|,
name|LS_RW_TRYUPGRADE_UPGRADE
block|}
block|,
block|{
name|LS_RW_DOWNGRADE
block|,
name|LSR_DOWNGRADE
block|,
name|LS_RW_DOWNGRADE_DOWNGRADE
block|}
block|,
block|{
name|LS_THREAD_LOCK
block|,
name|LST_SPIN
block|,
name|LS_THREAD_LOCK_SPIN
block|}
block|,
block|{
name|LS_THREAD_LOCK_HIGH
block|,
name|LST_SPIN
block|,
name|LS_THREAD_LOCK_HIGH_SPIN
block|}
block|,
block|{
name|NULL
block|}
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|dev_info_t
modifier|*
name|lockstat_devi
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* saved in xxattach() for xxinfo() */
end_comment

begin_decl_stmt
specifier|static
name|kmutex_t
name|lockstat_test
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* for testing purposes only */
end_comment

begin_decl_stmt
specifier|static
name|dtrace_provider_id_t
name|lockstat_id
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
specifier|static
name|void
name|lockstat_enable
parameter_list|(
name|void
modifier|*
name|arg
parameter_list|,
name|dtrace_id_t
name|id
parameter_list|,
name|void
modifier|*
name|parg
parameter_list|)
block|{
name|lockstat_probe_t
modifier|*
name|probe
init|=
name|parg
decl_stmt|;
name|ASSERT
argument_list|(
operator|!
name|lockstat_probemap
index|[
name|probe
operator|->
name|lsp_probe
index|]
argument_list|)
expr_stmt|;
name|lockstat_probemap
index|[
name|probe
operator|->
name|lsp_probe
index|]
operator|=
name|id
expr_stmt|;
name|membar_producer
argument_list|()
expr_stmt|;
name|lockstat_hot_patch
argument_list|()
expr_stmt|;
name|membar_producer
argument_list|()
expr_stmt|;
comment|/* 	 * Immediately generate a record for the lockstat_test mutex 	 * to verify that the mutex hot-patch code worked as expected. 	 */
name|mutex_enter
argument_list|(
operator|&
name|lockstat_test
argument_list|)
expr_stmt|;
name|mutex_exit
argument_list|(
operator|&
name|lockstat_test
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
specifier|static
name|void
name|lockstat_disable
parameter_list|(
name|void
modifier|*
name|arg
parameter_list|,
name|dtrace_id_t
name|id
parameter_list|,
name|void
modifier|*
name|parg
parameter_list|)
block|{
name|lockstat_probe_t
modifier|*
name|probe
init|=
name|parg
decl_stmt|;
name|int
name|i
decl_stmt|;
name|ASSERT
argument_list|(
name|lockstat_probemap
index|[
name|probe
operator|->
name|lsp_probe
index|]
argument_list|)
expr_stmt|;
name|lockstat_probemap
index|[
name|probe
operator|->
name|lsp_probe
index|]
operator|=
literal|0
expr_stmt|;
name|lockstat_hot_patch
argument_list|()
expr_stmt|;
name|membar_producer
argument_list|()
expr_stmt|;
comment|/* 	 * See if we have any probes left enabled. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|LS_NPROBES
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|lockstat_probemap
index|[
name|i
index|]
condition|)
block|{
comment|/* 			 * This probe is still enabled.  We don't need to deal 			 * with waiting for all threads to be out of the 			 * lockstat critical sections; just return. 			 */
return|return;
block|}
block|}
comment|/* 	 * The delay() here isn't as cheesy as you might think.  We don't 	 * want to busy-loop in the kernel, so we have to give up the 	 * CPU between calls to lockstat_active_threads(); that much is 	 * obvious.  But the reason it's a do..while loop rather than a 	 * while loop is subtle.  The memory barrier above guarantees that 	 * no threads will enter the lockstat code from this point forward. 	 * However, another thread could already be executing lockstat code 	 * without our knowledge if the update to its t_lockstat field hasn't 	 * cleared its CPU's store buffer.  Delaying for one clock tick 	 * guarantees that either (1) the thread will have *ample* time to 	 * complete its work, or (2) the thread will be preempted, in which 	 * case it will have to grab and release a dispatcher lock, which 	 * will flush that CPU's store buffer.  Either way we're covered. 	 */
do|do
block|{
name|delay
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|lockstat_active_threads
argument_list|()
condition|)
do|;
block|}
end_function

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
specifier|static
name|int
name|lockstat_open
parameter_list|(
name|dev_t
modifier|*
name|devp
parameter_list|,
name|int
name|flag
parameter_list|,
name|int
name|otyp
parameter_list|,
name|cred_t
modifier|*
name|cred_p
parameter_list|)
block|{
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/* ARGSUSED */
end_comment

begin_function
specifier|static
name|int
name|lockstat_info
parameter_list|(
name|dev_info_t
modifier|*
name|dip
parameter_list|,
name|ddi_info_cmd_t
name|infocmd
parameter_list|,
name|void
modifier|*
name|arg
parameter_list|,
name|void
modifier|*
modifier|*
name|result
parameter_list|)
block|{
name|int
name|error
decl_stmt|;
switch|switch
condition|(
name|infocmd
condition|)
block|{
case|case
name|DDI_INFO_DEVT2DEVINFO
case|:
operator|*
name|result
operator|=
operator|(
name|void
operator|*
operator|)
name|lockstat_devi
expr_stmt|;
name|error
operator|=
name|DDI_SUCCESS
expr_stmt|;
break|break;
case|case
name|DDI_INFO_DEVT2INSTANCE
case|:
operator|*
name|result
operator|=
operator|(
name|void
operator|*
operator|)
literal|0
expr_stmt|;
name|error
operator|=
name|DDI_SUCCESS
expr_stmt|;
break|break;
default|default:
name|error
operator|=
name|DDI_FAILURE
expr_stmt|;
block|}
return|return
operator|(
name|error
operator|)
return|;
block|}
end_function

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
specifier|static
name|void
name|lockstat_provide
parameter_list|(
name|void
modifier|*
name|arg
parameter_list|,
specifier|const
name|dtrace_probedesc_t
modifier|*
name|desc
parameter_list|)
block|{
name|int
name|i
init|=
literal|0
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|lockstat_probes
index|[
name|i
index|]
operator|.
name|lsp_func
operator|!=
name|NULL
condition|;
name|i
operator|++
control|)
block|{
name|lockstat_probe_t
modifier|*
name|probe
init|=
operator|&
name|lockstat_probes
index|[
name|i
index|]
decl_stmt|;
if|if
condition|(
name|dtrace_probe_lookup
argument_list|(
name|lockstat_id
argument_list|,
literal|"genunix"
argument_list|,
name|probe
operator|->
name|lsp_func
argument_list|,
name|probe
operator|->
name|lsp_name
argument_list|)
operator|!=
literal|0
condition|)
continue|continue;
name|ASSERT
argument_list|(
operator|!
name|probe
operator|->
name|lsp_id
argument_list|)
expr_stmt|;
name|probe
operator|->
name|lsp_id
operator|=
name|dtrace_probe_create
argument_list|(
name|lockstat_id
argument_list|,
literal|"genunix"
argument_list|,
name|probe
operator|->
name|lsp_func
argument_list|,
name|probe
operator|->
name|lsp_name
argument_list|,
literal|1
argument_list|,
name|probe
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
specifier|static
name|void
name|lockstat_destroy
parameter_list|(
name|void
modifier|*
name|arg
parameter_list|,
name|dtrace_id_t
name|id
parameter_list|,
name|void
modifier|*
name|parg
parameter_list|)
block|{
name|lockstat_probe_t
modifier|*
name|probe
init|=
name|parg
decl_stmt|;
name|ASSERT
argument_list|(
operator|!
name|lockstat_probemap
index|[
name|probe
operator|->
name|lsp_probe
index|]
argument_list|)
expr_stmt|;
name|probe
operator|->
name|lsp_id
operator|=
literal|0
expr_stmt|;
block|}
end_function

begin_decl_stmt
specifier|static
name|dtrace_pattr_t
name|lockstat_attr
init|=
block|{
block|{
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_CLASS_COMMON
block|}
block|,
block|{
name|DTRACE_STABILITY_PRIVATE
block|,
name|DTRACE_STABILITY_PRIVATE
block|,
name|DTRACE_CLASS_UNKNOWN
block|}
block|,
block|{
name|DTRACE_STABILITY_PRIVATE
block|,
name|DTRACE_STABILITY_PRIVATE
block|,
name|DTRACE_CLASS_UNKNOWN
block|}
block|,
block|{
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_CLASS_COMMON
block|}
block|,
block|{
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_STABILITY_EVOLVING
block|,
name|DTRACE_CLASS_COMMON
block|}
block|, }
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|dtrace_pops_t
name|lockstat_pops
init|=
block|{
name|lockstat_provide
block|,
name|NULL
block|,
name|lockstat_enable
block|,
name|lockstat_disable
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|lockstat_destroy
block|}
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|int
name|lockstat_attach
parameter_list|(
name|dev_info_t
modifier|*
name|devi
parameter_list|,
name|ddi_attach_cmd_t
name|cmd
parameter_list|)
block|{
switch|switch
condition|(
name|cmd
condition|)
block|{
case|case
name|DDI_ATTACH
case|:
break|break;
case|case
name|DDI_RESUME
case|:
return|return
operator|(
name|DDI_SUCCESS
operator|)
return|;
default|default:
return|return
operator|(
name|DDI_FAILURE
operator|)
return|;
block|}
if|if
condition|(
name|ddi_create_minor_node
argument_list|(
name|devi
argument_list|,
literal|"lockstat"
argument_list|,
name|S_IFCHR
argument_list|,
literal|0
argument_list|,
name|DDI_PSEUDO
argument_list|,
literal|0
argument_list|)
operator|==
name|DDI_FAILURE
operator|||
name|dtrace_register
argument_list|(
literal|"lockstat"
argument_list|,
operator|&
name|lockstat_attr
argument_list|,
name|DTRACE_PRIV_KERNEL
argument_list|,
name|NULL
argument_list|,
operator|&
name|lockstat_pops
argument_list|,
name|NULL
argument_list|,
operator|&
name|lockstat_id
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|ddi_remove_minor_node
argument_list|(
name|devi
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
operator|(
name|DDI_FAILURE
operator|)
return|;
block|}
name|lockstat_probe
operator|=
name|dtrace_probe
expr_stmt|;
name|membar_producer
argument_list|()
expr_stmt|;
name|ddi_report_dev
argument_list|(
name|devi
argument_list|)
expr_stmt|;
name|lockstat_devi
operator|=
name|devi
expr_stmt|;
return|return
operator|(
name|DDI_SUCCESS
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|lockstat_detach
parameter_list|(
name|dev_info_t
modifier|*
name|devi
parameter_list|,
name|ddi_detach_cmd_t
name|cmd
parameter_list|)
block|{
switch|switch
condition|(
name|cmd
condition|)
block|{
case|case
name|DDI_DETACH
case|:
break|break;
case|case
name|DDI_SUSPEND
case|:
return|return
operator|(
name|DDI_SUCCESS
operator|)
return|;
default|default:
return|return
operator|(
name|DDI_FAILURE
operator|)
return|;
block|}
if|if
condition|(
name|dtrace_unregister
argument_list|(
name|lockstat_id
argument_list|)
operator|!=
literal|0
condition|)
return|return
operator|(
name|DDI_FAILURE
operator|)
return|;
name|ddi_remove_minor_node
argument_list|(
name|devi
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
operator|(
name|DDI_SUCCESS
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Configuration data structures  */
end_comment

begin_decl_stmt
specifier|static
name|struct
name|cb_ops
name|lockstat_cb_ops
init|=
block|{
name|lockstat_open
block|,
comment|/* open */
name|nodev
block|,
comment|/* close */
name|nulldev
block|,
comment|/* strategy */
name|nulldev
block|,
comment|/* print */
name|nodev
block|,
comment|/* dump */
name|nodev
block|,
comment|/* read */
name|nodev
block|,
comment|/* write */
name|nodev
block|,
comment|/* ioctl */
name|nodev
block|,
comment|/* devmap */
name|nodev
block|,
comment|/* mmap */
name|nodev
block|,
comment|/* segmap */
name|nochpoll
block|,
comment|/* poll */
name|ddi_prop_op
block|,
comment|/* cb_prop_op */
literal|0
block|,
comment|/* streamtab */
name|D_MP
operator||
name|D_NEW
comment|/* Driver compatibility flag */
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|struct
name|dev_ops
name|lockstat_ops
init|=
block|{
name|DEVO_REV
block|,
comment|/* devo_rev, */
literal|0
block|,
comment|/* refcnt */
name|lockstat_info
block|,
comment|/* getinfo */
name|nulldev
block|,
comment|/* identify */
name|nulldev
block|,
comment|/* probe */
name|lockstat_attach
block|,
comment|/* attach */
name|lockstat_detach
block|,
comment|/* detach */
name|nulldev
block|,
comment|/* reset */
operator|&
name|lockstat_cb_ops
block|,
comment|/* cb_ops */
name|NULL
block|,
comment|/* bus_ops */
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|struct
name|modldrv
name|modldrv
init|=
block|{
operator|&
name|mod_driverops
block|,
comment|/* Type of module.  This one is a driver */
literal|"Lock Statistics %I%"
block|,
comment|/* name of module */
operator|&
name|lockstat_ops
block|,
comment|/* driver ops */
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|struct
name|modlinkage
name|modlinkage
init|=
block|{
name|MODREV_1
block|,
operator|(
name|void
operator|*
operator|)
operator|&
name|modldrv
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|_init
parameter_list|(
name|void
parameter_list|)
block|{
return|return
operator|(
name|mod_install
argument_list|(
operator|&
name|modlinkage
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|int
name|_fini
parameter_list|(
name|void
parameter_list|)
block|{
return|return
operator|(
name|mod_remove
argument_list|(
operator|&
name|modlinkage
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|int
name|_info
parameter_list|(
name|struct
name|modinfo
modifier|*
name|modinfop
parameter_list|)
block|{
return|return
operator|(
name|mod_info
argument_list|(
operator|&
name|modlinkage
argument_list|,
name|modinfop
argument_list|)
operator|)
return|;
block|}
end_function

end_unit

