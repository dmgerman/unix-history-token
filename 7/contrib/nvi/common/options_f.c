begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)options_f.c	10.25 (Berkeley) 7/12/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_comment
comment|/*  * PUBLIC: int f_altwerase __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_altwerase
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
operator|!
operator|*
name|valp
condition|)
name|O_CLR
argument_list|(
name|sp
argument_list|,
name|O_TTYWERASE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_columns __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_columns
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
comment|/* Validate the number. */
if|if
condition|(
operator|*
name|valp
operator|<
name|MINIMUM_SCREEN_COLS
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"040|Screen columns too small, less than %d"
argument_list|,
name|MINIMUM_SCREEN_COLS
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * !!! 	 * It's not uncommon for allocation of huge chunks of memory to cause 	 * core dumps on various systems.  So, we prune out numbers that are 	 * "obviously" wrong.  Vi will not work correctly if it has the wrong 	 * number of lines/columns for the screen, but at least we don't drop 	 * core. 	 */
define|#
directive|define
name|MAXIMUM_SCREEN_COLS
value|500
if|if
condition|(
operator|*
name|valp
operator|>
name|MAXIMUM_SCREEN_COLS
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"041|Screen columns too large, greater than %d"
argument_list|,
name|MAXIMUM_SCREEN_COLS
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_lines __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_lines
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
comment|/* Validate the number. */
if|if
condition|(
operator|*
name|valp
operator|<
name|MINIMUM_SCREEN_ROWS
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"042|Screen lines too small, less than %d"
argument_list|,
name|MINIMUM_SCREEN_ROWS
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * !!! 	 * It's not uncommon for allocation of huge chunks of memory to cause 	 * core dumps on various systems.  So, we prune out numbers that are 	 * "obviously" wrong.  Vi will not work correctly if it has the wrong 	 * number of lines/columns for the screen, but at least we don't drop 	 * core. 	 */
define|#
directive|define
name|MAXIMUM_SCREEN_ROWS
value|500
if|if
condition|(
operator|*
name|valp
operator|>
name|MAXIMUM_SCREEN_ROWS
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"043|Screen lines too large, greater than %d"
argument_list|,
name|MAXIMUM_SCREEN_ROWS
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * Set the value, and the related scroll value.  If no window 	 * value set, set a new default window. 	 */
name|o_set
argument_list|(
name|sp
argument_list|,
name|O_LINES
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
operator|*
name|valp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|valp
operator|==
literal|1
condition|)
block|{
name|sp
operator|->
name|defscroll
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|==
name|O_D_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|||
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|>
operator|*
name|valp
condition|)
block|{
name|o_set
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|o_set
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|,
name|OS_DEF
argument_list|,
name|NULL
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
name|sp
operator|->
name|defscroll
operator|=
operator|(
operator|*
name|valp
operator|-
literal|1
operator|)
operator|/
literal|2
expr_stmt|;
if|if
condition|(
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|==
name|O_D_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|||
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|>
operator|*
name|valp
condition|)
block|{
name|o_set
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
operator|*
name|valp
operator|-
literal|1
argument_list|)
expr_stmt|;
name|o_set
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|,
name|OS_DEF
argument_list|,
name|NULL
argument_list|,
operator|*
name|valp
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_lisp __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_lisp
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"044|The lisp option is not implemented"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_msgcat __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_msgcat
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
operator|(
name|void
operator|)
name|msg_open
argument_list|(
name|sp
argument_list|,
name|str
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_paragraph __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_paragraph
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
name|strlen
argument_list|(
name|str
argument_list|)
operator|&
literal|1
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"048|The paragraph option must be in two character groups"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_print __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_print
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
comment|/* Reinitialize the key fast lookup table. */
name|v_key_ilookup
argument_list|(
name|sp
argument_list|)
expr_stmt|;
comment|/* Reformat the screen. */
name|F_SET
argument_list|(
name|sp
argument_list|,
name|SC_SCR_REFORMAT
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_readonly __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_readonly
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
comment|/* 	 * !!! 	 * See the comment in exf.c. 	 */
if|if
condition|(
operator|*
name|valp
condition|)
name|F_CLR
argument_list|(
name|sp
argument_list|,
name|SC_READONLY
argument_list|)
expr_stmt|;
else|else
name|F_SET
argument_list|(
name|sp
argument_list|,
name|SC_READONLY
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_recompile __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_recompile
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_RE_SEARCH
argument_list|)
condition|)
block|{
name|regfree
argument_list|(
operator|&
name|sp
operator|->
name|re_c
argument_list|)
expr_stmt|;
name|F_CLR
argument_list|(
name|sp
argument_list|,
name|SC_RE_SEARCH
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_RE_SUBST
argument_list|)
condition|)
block|{
name|regfree
argument_list|(
operator|&
name|sp
operator|->
name|subre_c
argument_list|)
expr_stmt|;
name|F_CLR
argument_list|(
name|sp
argument_list|,
name|SC_RE_SUBST
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_reformat __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_reformat
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
name|F_SET
argument_list|(
name|sp
argument_list|,
name|SC_SCR_REFORMAT
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_section __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_section
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
name|strlen
argument_list|(
name|str
argument_list|)
operator|&
literal|1
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"049|The section option must be in two character groups"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_ttywerase __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_ttywerase
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
operator|!
operator|*
name|valp
condition|)
name|O_CLR
argument_list|(
name|sp
argument_list|,
name|O_ALTWERASE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_w300 __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_w300
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
name|u_long
name|v
decl_stmt|;
comment|/* Historical behavior for w300 was< 1200. */
if|if
condition|(
name|sp
operator|->
name|gp
operator|->
name|scr_baud
argument_list|(
name|sp
argument_list|,
operator|&
name|v
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|v
operator|>=
literal|1200
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|f_window
argument_list|(
name|sp
argument_list|,
name|op
argument_list|,
name|str
argument_list|,
name|valp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_w1200 __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_w1200
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
name|u_long
name|v
decl_stmt|;
comment|/* Historical behavior for w1200 was == 1200. */
if|if
condition|(
name|sp
operator|->
name|gp
operator|->
name|scr_baud
argument_list|(
name|sp
argument_list|,
operator|&
name|v
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|v
operator|<
literal|1200
operator|||
name|v
operator|>
literal|4800
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|f_window
argument_list|(
name|sp
argument_list|,
name|op
argument_list|,
name|str
argument_list|,
name|valp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_w9600 __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_w9600
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
name|u_long
name|v
decl_stmt|;
comment|/* Historical behavior for w9600 was> 1200. */
if|if
condition|(
name|sp
operator|->
name|gp
operator|->
name|scr_baud
argument_list|(
name|sp
argument_list|,
operator|&
name|v
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|v
operator|<=
literal|4800
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|f_window
argument_list|(
name|sp
argument_list|,
name|op
argument_list|,
name|str
argument_list|,
name|valp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * PUBLIC: int f_window __P((SCR *, OPTION *, char *, u_long *));  */
end_comment

begin_function
name|int
name|f_window
parameter_list|(
name|sp
parameter_list|,
name|op
parameter_list|,
name|str
parameter_list|,
name|valp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|OPTION
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|str
decl_stmt|;
name|u_long
modifier|*
name|valp
decl_stmt|;
block|{
if|if
condition|(
operator|*
name|valp
operator|>=
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_LINES
argument_list|)
operator|-
literal|1
operator|&&
operator|(
operator|*
name|valp
operator|=
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_LINES
argument_list|)
operator|-
literal|1
operator|)
operator|==
literal|0
condition|)
operator|*
name|valp
operator|=
literal|1
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

