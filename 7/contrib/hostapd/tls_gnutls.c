begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * WPA Supplicant / SSL/TLS interface functions for openssl  * Copyright (c) 2004-2007, Jouni Malinen<j@w1.fi>  *  * This program is free software; you can redistribute it and/or modify  * it under the terms of the GNU General Public License version 2 as  * published by the Free Software Foundation.  *  * Alternatively, this software may be distributed under the terms of BSD  * license.  *  * See README and COPYING for more details.  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|<gnutls/gnutls.h>
end_include

begin_include
include|#
directive|include
file|<gnutls/x509.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|PKCS12_FUNCS
end_ifdef

begin_include
include|#
directive|include
file|<gnutls/pkcs12.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* PKCS12_FUNCS */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|CONFIG_GNUTLS_EXTRA
end_ifdef

begin_if
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|>=
literal|0x010302
end_if

begin_define
define|#
directive|define
name|GNUTLS_IA
end_define

begin_include
include|#
directive|include
file|<gnutls/extra.h>
end_include

begin_if
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|==
literal|0x010302
end_if

begin_comment
comment|/* This function is not included in the current gnutls/extra.h even though it  * should be, so define it here as a workaround for the time being. */
end_comment

begin_function_decl
name|int
name|gnutls_ia_verify_endphase
parameter_list|(
name|gnutls_session_t
name|session
parameter_list|,
name|char
modifier|*
name|checksum
parameter_list|)
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBGNUTLS_VERSION_NUMBER == 0x010302 */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBGNUTLS_VERSION_NUMBER>= 0x010302 */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* CONFIG_GNUTLS_EXTRA */
end_comment

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_include
include|#
directive|include
file|"tls.h"
end_include

begin_define
define|#
directive|define
name|TLS_RANDOM_SIZE
value|32
end_define

begin_define
define|#
directive|define
name|TLS_MASTER_SIZE
value|48
end_define

begin_if
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|<
literal|0x010302
end_if

begin_comment
comment|/* GnuTLS 1.3.2 added functions for using master secret. Older versions require  * use of internal structures to get the master_secret and  * {server,client}_random.  */
end_comment

begin_define
define|#
directive|define
name|GNUTLS_INTERNAL_STRUCTURE_HACK
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBGNUTLS_VERSION_NUMBER< 0x010302 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|GNUTLS_INTERNAL_STRUCTURE_HACK
end_ifdef

begin_comment
comment|/*  * It looks like gnutls does not provide access to client/server_random and  * master_key. This is somewhat unfortunate since these are needed for key  * derivation in EAP-{TLS,TTLS,PEAP,FAST}. Workaround for now is a horrible  * hack that copies the gnutls_session_int definition from gnutls_int.h so that  * we can get the needed information.  */
end_comment

begin_typedef
typedef|typedef
name|u8
name|uint8
typedef|;
end_typedef

begin_typedef
typedef|typedef
name|unsigned
name|char
name|opaque
typedef|;
end_typedef

begin_typedef
typedef|typedef
struct|struct
block|{
name|uint8
name|suite
index|[
literal|2
index|]
decl_stmt|;
block|}
name|cipher_suite_st
typedef|;
end_typedef

begin_typedef
typedef|typedef
struct|struct
block|{
name|gnutls_connection_end_t
name|entity
decl_stmt|;
name|gnutls_kx_algorithm_t
name|kx_algorithm
decl_stmt|;
name|gnutls_cipher_algorithm_t
name|read_bulk_cipher_algorithm
decl_stmt|;
name|gnutls_mac_algorithm_t
name|read_mac_algorithm
decl_stmt|;
name|gnutls_compression_method_t
name|read_compression_algorithm
decl_stmt|;
name|gnutls_cipher_algorithm_t
name|write_bulk_cipher_algorithm
decl_stmt|;
name|gnutls_mac_algorithm_t
name|write_mac_algorithm
decl_stmt|;
name|gnutls_compression_method_t
name|write_compression_algorithm
decl_stmt|;
name|cipher_suite_st
name|current_cipher_suite
decl_stmt|;
name|opaque
name|master_secret
index|[
name|TLS_MASTER_SIZE
index|]
decl_stmt|;
name|opaque
name|client_random
index|[
name|TLS_RANDOM_SIZE
index|]
decl_stmt|;
name|opaque
name|server_random
index|[
name|TLS_RANDOM_SIZE
index|]
decl_stmt|;
comment|/* followed by stuff we are not interested in */
block|}
name|security_parameters_st
typedef|;
end_typedef

begin_struct
struct|struct
name|gnutls_session_int
block|{
name|security_parameters_st
name|security_parameters
decl_stmt|;
comment|/* followed by things we are not interested in */
block|}
struct|;
end_struct

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBGNUTLS_VERSION_NUMBER< 0x010302 */
end_comment

begin_decl_stmt
specifier|static
name|int
name|tls_gnutls_ref_count
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|tls_global
block|{
comment|/* Data for session resumption */
name|void
modifier|*
name|session_data
decl_stmt|;
name|size_t
name|session_data_size
decl_stmt|;
name|int
name|server
decl_stmt|;
name|int
name|params_set
decl_stmt|;
name|gnutls_certificate_credentials_t
name|xcred
decl_stmt|;
block|}
struct|;
end_struct

begin_struct
struct|struct
name|tls_connection
block|{
name|gnutls_session
name|session
decl_stmt|;
name|char
modifier|*
name|subject_match
decl_stmt|,
modifier|*
name|altsubject_match
decl_stmt|;
name|int
name|read_alerts
decl_stmt|,
name|write_alerts
decl_stmt|,
name|failed
decl_stmt|;
name|u8
modifier|*
name|pre_shared_secret
decl_stmt|;
name|size_t
name|pre_shared_secret_len
decl_stmt|;
name|int
name|established
decl_stmt|;
name|int
name|verify_peer
decl_stmt|;
name|u8
modifier|*
name|push_buf
decl_stmt|,
modifier|*
name|pull_buf
decl_stmt|,
modifier|*
name|pull_buf_offset
decl_stmt|;
name|size_t
name|push_buf_len
decl_stmt|,
name|pull_buf_len
decl_stmt|;
name|int
name|params_set
decl_stmt|;
name|gnutls_certificate_credentials_t
name|xcred
decl_stmt|;
name|int
name|tls_ia
decl_stmt|;
name|int
name|final_phase_finished
decl_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_IA
name|gnutls_ia_server_credentials_t
name|iacred_srv
decl_stmt|;
name|gnutls_ia_client_credentials_t
name|iacred_cli
decl_stmt|;
comment|/* Session keys generated in the current phase for inner secret 	 * permutation before generating/verifying PhaseFinished. */
name|u8
modifier|*
name|session_keys
decl_stmt|;
name|size_t
name|session_keys_len
decl_stmt|;
name|u8
name|inner_secret
index|[
name|TLS_MASTER_SIZE
index|]
decl_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
block|}
struct|;
end_struct

begin_function
specifier|static
name|void
name|tls_log_func
parameter_list|(
name|int
name|level
parameter_list|,
specifier|const
name|char
modifier|*
name|msg
parameter_list|)
block|{
name|char
modifier|*
name|s
decl_stmt|,
modifier|*
name|pos
decl_stmt|;
if|if
condition|(
name|level
operator|==
literal|6
operator|||
name|level
operator|==
literal|7
condition|)
block|{
comment|/* These levels seem to be mostly I/O debug and msg dumps */
return|return;
block|}
name|s
operator|=
name|os_strdup
argument_list|(
name|msg
argument_list|)
expr_stmt|;
if|if
condition|(
name|s
operator|==
name|NULL
condition|)
return|return;
name|pos
operator|=
name|s
expr_stmt|;
while|while
condition|(
operator|*
name|pos
operator|!=
literal|'\0'
condition|)
block|{
if|if
condition|(
operator|*
name|pos
operator|==
literal|'\n'
condition|)
block|{
operator|*
name|pos
operator|=
literal|'\0'
expr_stmt|;
break|break;
block|}
name|pos
operator|++
expr_stmt|;
block|}
name|wpa_printf
argument_list|(
name|level
operator|>
literal|3
condition|?
name|MSG_MSGDUMP
else|:
name|MSG_DEBUG
argument_list|,
literal|"gnutls<%d> %s"
argument_list|,
name|level
argument_list|,
name|s
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_function

begin_decl_stmt
specifier|extern
name|int
name|wpa_debug_show_keys
decl_stmt|;
end_decl_stmt

begin_function
name|void
modifier|*
name|tls_init
parameter_list|(
specifier|const
name|struct
name|tls_config
modifier|*
name|conf
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
decl_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_INTERNAL_STRUCTURE_HACK
comment|/* Because of the horrible hack to get master_secret and client/server 	 * random, we need to make sure that the gnutls version is something 	 * that is expected to have same structure definition for the session 	 * data.. */
specifier|const
name|char
modifier|*
name|ver
decl_stmt|;
specifier|const
name|char
modifier|*
name|ok_ver
index|[]
init|=
block|{
literal|"1.2.3"
block|,
literal|"1.2.4"
block|,
literal|"1.2.5"
block|,
literal|"1.2.6"
block|,
literal|"1.2.9"
block|,
literal|"1.3.2"
block|,
name|NULL
block|}
decl_stmt|;
name|int
name|i
decl_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_INTERNAL_STRUCTURE_HACK */
name|global
operator|=
name|os_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|global
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|global
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|tls_gnutls_ref_count
operator|==
literal|0
operator|&&
name|gnutls_global_init
argument_list|()
operator|<
literal|0
condition|)
block|{
name|os_free
argument_list|(
name|global
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|tls_gnutls_ref_count
operator|++
expr_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_INTERNAL_STRUCTURE_HACK
name|ver
operator|=
name|gnutls_check_version
argument_list|(
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|ver
operator|==
name|NULL
condition|)
block|{
name|tls_deinit
argument_list|(
name|global
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - gnutls version %s"
argument_list|,
name|__func__
argument_list|,
name|ver
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|ok_ver
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|strcmp
argument_list|(
name|ok_ver
index|[
name|i
index|]
argument_list|,
name|ver
argument_list|)
operator|==
literal|0
condition|)
break|break;
block|}
if|if
condition|(
name|ok_ver
index|[
name|i
index|]
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"Untested gnutls version %s - this needs "
literal|"to be tested and enabled in tls_gnutls.c"
argument_list|,
name|ver
argument_list|)
expr_stmt|;
name|tls_deinit
argument_list|(
name|global
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
endif|#
directive|endif
comment|/* GNUTLS_INTERNAL_STRUCTURE_HACK */
name|gnutls_global_set_log_function
argument_list|(
name|tls_log_func
argument_list|)
expr_stmt|;
if|if
condition|(
name|wpa_debug_show_keys
condition|)
name|gnutls_global_set_log_level
argument_list|(
literal|11
argument_list|)
expr_stmt|;
return|return
name|global
return|;
block|}
end_function

begin_function
name|void
name|tls_deinit
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|ssl_ctx
decl_stmt|;
if|if
condition|(
name|global
condition|)
block|{
if|if
condition|(
name|global
operator|->
name|params_set
condition|)
name|gnutls_certificate_free_credentials
argument_list|(
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|global
operator|->
name|session_data
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|global
argument_list|)
expr_stmt|;
block|}
name|tls_gnutls_ref_count
operator|--
expr_stmt|;
if|if
condition|(
name|tls_gnutls_ref_count
operator|==
literal|0
condition|)
name|gnutls_global_deinit
argument_list|()
expr_stmt|;
block|}
end_function

begin_function
name|int
name|tls_get_errors
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
specifier|static
name|ssize_t
name|tls_pull_func
parameter_list|(
name|gnutls_transport_ptr
name|ptr
parameter_list|,
name|void
modifier|*
name|buf
parameter_list|,
name|size_t
name|len
parameter_list|)
block|{
name|struct
name|tls_connection
modifier|*
name|conn
init|=
operator|(
expr|struct
name|tls_connection
operator|*
operator|)
name|ptr
decl_stmt|;
name|u8
modifier|*
name|end
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|pull_buf
operator|==
name|NULL
condition|)
block|{
name|errno
operator|=
name|EWOULDBLOCK
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|end
operator|=
name|conn
operator|->
name|pull_buf
operator|+
name|conn
operator|->
name|pull_buf_len
expr_stmt|;
if|if
condition|(
call|(
name|size_t
call|)
argument_list|(
name|end
operator|-
name|conn
operator|->
name|pull_buf_offset
argument_list|)
operator|<
name|len
condition|)
name|len
operator|=
name|end
operator|-
name|conn
operator|->
name|pull_buf_offset
expr_stmt|;
name|os_memcpy
argument_list|(
name|buf
argument_list|,
name|conn
operator|->
name|pull_buf_offset
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|pull_buf_offset
operator|+=
name|len
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|pull_buf_offset
operator|==
name|end
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - pull_buf consumed"
argument_list|,
name|__func__
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|)
expr_stmt|;
name|conn
operator|->
name|pull_buf
operator|=
name|conn
operator|->
name|pull_buf_offset
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|pull_buf_len
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - %d bytes remaining in pull_buf"
argument_list|,
name|__func__
argument_list|,
name|end
operator|-
name|conn
operator|->
name|pull_buf_offset
argument_list|)
expr_stmt|;
block|}
return|return
name|len
return|;
block|}
end_function

begin_function
specifier|static
name|ssize_t
name|tls_push_func
parameter_list|(
name|gnutls_transport_ptr
name|ptr
parameter_list|,
specifier|const
name|void
modifier|*
name|buf
parameter_list|,
name|size_t
name|len
parameter_list|)
block|{
name|struct
name|tls_connection
modifier|*
name|conn
init|=
operator|(
expr|struct
name|tls_connection
operator|*
operator|)
name|ptr
decl_stmt|;
name|u8
modifier|*
name|nbuf
decl_stmt|;
name|nbuf
operator|=
name|os_realloc
argument_list|(
name|conn
operator|->
name|push_buf
argument_list|,
name|conn
operator|->
name|push_buf_len
operator|+
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|nbuf
operator|==
name|NULL
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|os_memcpy
argument_list|(
name|nbuf
operator|+
name|conn
operator|->
name|push_buf_len
argument_list|,
name|buf
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|push_buf
operator|=
name|nbuf
expr_stmt|;
name|conn
operator|->
name|push_buf_len
operator|+=
name|len
expr_stmt|;
return|return
name|len
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|tls_gnutls_init_session
parameter_list|(
name|struct
name|tls_global
modifier|*
name|global
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
specifier|const
name|int
name|cert_types
index|[
literal|2
index|]
init|=
block|{
name|GNUTLS_CRT_X509
block|,
literal|0
block|}
decl_stmt|;
specifier|const
name|int
name|protos
index|[
literal|2
index|]
init|=
block|{
name|GNUTLS_TLS1
block|,
literal|0
block|}
decl_stmt|;
name|int
name|ret
decl_stmt|;
name|ret
operator|=
name|gnutls_init
argument_list|(
operator|&
name|conn
operator|->
name|session
argument_list|,
name|global
operator|->
name|server
condition|?
name|GNUTLS_SERVER
else|:
name|GNUTLS_CLIENT
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to initialize new TLS "
literal|"connection: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|ret
operator|=
name|gnutls_set_default_priority
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
goto|goto
name|fail
goto|;
name|ret
operator|=
name|gnutls_certificate_type_set_priority
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|cert_types
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
goto|goto
name|fail
goto|;
name|ret
operator|=
name|gnutls_protocol_set_priority
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|protos
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
goto|goto
name|fail
goto|;
name|gnutls_transport_set_pull_function
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|tls_pull_func
argument_list|)
expr_stmt|;
name|gnutls_transport_set_push_function
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|tls_push_func
argument_list|)
expr_stmt|;
name|gnutls_transport_set_ptr
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|(
name|gnutls_transport_ptr
operator|)
name|conn
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
name|fail
label|:
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to setup new TLS connection: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|gnutls_deinit
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|struct
name|tls_connection
modifier|*
name|tls_connection_init
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|ssl_ctx
decl_stmt|;
name|struct
name|tls_connection
modifier|*
name|conn
decl_stmt|;
name|int
name|ret
decl_stmt|;
name|conn
operator|=
name|os_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|conn
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|tls_gnutls_init_session
argument_list|(
name|global
argument_list|,
name|conn
argument_list|)
condition|)
block|{
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|global
operator|->
name|params_set
condition|)
block|{
name|ret
operator|=
name|gnutls_credentials_set
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_CRD_CERTIFICATE
argument_list|,
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"Failed to configure "
literal|"credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
block|}
if|if
condition|(
name|gnutls_certificate_allocate_credentials
argument_list|(
operator|&
name|conn
operator|->
name|xcred
argument_list|)
condition|)
block|{
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
return|return
name|conn
return|;
block|}
end_function

begin_function
name|void
name|tls_connection_deinit
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return;
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|->
name|iacred_srv
condition|)
name|gnutls_ia_free_server_credentials
argument_list|(
name|conn
operator|->
name|iacred_srv
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|iacred_cli
condition|)
name|gnutls_ia_free_client_credentials
argument_list|(
name|conn
operator|->
name|iacred_cli
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|session_keys
condition|)
block|{
name|os_memset
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
literal|0
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* GNUTLS_IA */
name|gnutls_certificate_free_credentials
argument_list|(
name|conn
operator|->
name|xcred
argument_list|)
expr_stmt|;
name|gnutls_deinit
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|pre_shared_secret
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|subject_match
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|altsubject_match
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|push_buf
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|int
name|tls_connection_established
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
return|return
name|conn
condition|?
name|conn
operator|->
name|established
else|:
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_shutdown
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|ssl_ctx
decl_stmt|;
name|int
name|ret
decl_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
comment|/* Shutdown previous TLS connection without notifying the peer 	 * because the connection was already terminated in practice 	 * and "close notify" shutdown alert would confuse AS. */
name|gnutls_bye
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_SHUT_RDWR
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|push_buf
argument_list|)
expr_stmt|;
name|conn
operator|->
name|push_buf
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|push_buf_len
operator|=
literal|0
expr_stmt|;
name|conn
operator|->
name|established
operator|=
literal|0
expr_stmt|;
name|conn
operator|->
name|final_phase_finished
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|->
name|session_keys
condition|)
block|{
name|os_memset
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
literal|0
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|session_keys_len
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
name|gnutls_deinit
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
if|if
condition|(
name|tls_gnutls_init_session
argument_list|(
name|global
argument_list|,
name|conn
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"GnuTLS: Failed to preparare new session "
literal|"for session resumption use"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|ret
operator|=
name|gnutls_credentials_set
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_CRD_CERTIFICATE
argument_list|,
name|conn
operator|->
name|params_set
condition|?
name|conn
operator|->
name|xcred
else|:
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"GnuTLS: Failed to configure credentials "
literal|"for session resumption: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|global
operator|->
name|session_data
condition|)
block|{
name|ret
operator|=
name|gnutls_session_set_data
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|global
operator|->
name|session_data
argument_list|,
name|global
operator|->
name|session_data_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"GnuTLS: Failed to set session "
literal|"data: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
return|return
literal|0
return|;
block|}
end_function

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|static int tls_match_altsubject(X509 *cert, const char *match) { 	GENERAL_NAME *gen; 	char *field, *tmp; 	void *ext; 	int i, found = 0; 	size_t len;  	ext = X509_get_ext_d2i(cert, NID_subject_alt_name, NULL, NULL);  	for (i = 0; ext&& i< sk_GENERAL_NAME_num(ext); i++) { 		gen = sk_GENERAL_NAME_value(ext, i); 		switch (gen->type) { 		case GEN_EMAIL: 			field = "EMAIL"; 			break; 		case GEN_DNS: 			field = "DNS"; 			break; 		case GEN_URI: 			field = "URI"; 			break; 		default: 			field = NULL; 			wpa_printf(MSG_DEBUG, "TLS: altSubjectName: " 				   "unsupported type=%d", gen->type); 			break; 		}  		if (!field) 			continue;  		wpa_printf(MSG_DEBUG, "TLS: altSubjectName: %s:%s", 			   field, gen->d.ia5->data); 		len = os_strlen(field) + 1 + 			strlen((char *) gen->d.ia5->data) + 1; 		tmp = os_malloc(len); 		if (tmp == NULL) 			continue; 		snprintf(tmp, len, "%s:%s", field, gen->d.ia5->data); 		if (strstr(tmp, match)) 			found++; 		os_free(tmp); 	}  	return found; }
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|static int tls_verify_cb(int preverify_ok, X509_STORE_CTX *x509_ctx) { 	char buf[256]; 	X509 *err_cert; 	int err, depth; 	SSL *ssl; 	struct tls_connection *conn; 	char *match, *altmatch;  	err_cert = X509_STORE_CTX_get_current_cert(x509_ctx); 	err = X509_STORE_CTX_get_error(x509_ctx); 	depth = X509_STORE_CTX_get_error_depth(x509_ctx); 	ssl = X509_STORE_CTX_get_ex_data(x509_ctx, 					 SSL_get_ex_data_X509_STORE_CTX_idx()); 	X509_NAME_oneline(X509_get_subject_name(err_cert), buf, sizeof(buf));  	conn = SSL_get_app_data(ssl); 	match = conn ? conn->subject_match : NULL; 	altmatch = conn ? conn->altsubject_match : NULL;  	if (!preverify_ok) { 		wpa_printf(MSG_WARNING, "TLS: Certificate verification failed," 			   " error %d (%s) depth %d for '%s'", err, 			   X509_verify_cert_error_string(err), depth, buf); 	} else { 		wpa_printf(MSG_DEBUG, "TLS: tls_verify_cb - " 			   "preverify_ok=%d err=%d (%s) depth=%d buf='%s'", 			   preverify_ok, err, 			   X509_verify_cert_error_string(err), depth, buf); 		if (depth == 0&& match&& strstr(buf, match) == NULL) { 			wpa_printf(MSG_WARNING, "TLS: Subject '%s' did not " 				   "match with '%s'", buf, match); 			preverify_ok = 0; 		} else if (depth == 0&& altmatch&& 			   !tls_match_altsubject(err_cert, altmatch)) { 			wpa_printf(MSG_WARNING, "TLS: altSubjectName match " 				   "'%s' not found", altmatch); 			preverify_ok = 0; 		} 	}  	return preverify_ok; }
endif|#
directive|endif
end_endif

begin_function
name|int
name|tls_connection_set_params
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|)
block|{
name|int
name|ret
decl_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
name|params
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|os_free
argument_list|(
name|conn
operator|->
name|subject_match
argument_list|)
expr_stmt|;
name|conn
operator|->
name|subject_match
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|params
operator|->
name|subject_match
condition|)
block|{
name|conn
operator|->
name|subject_match
operator|=
name|os_strdup
argument_list|(
name|params
operator|->
name|subject_match
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|subject_match
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
block|}
name|os_free
argument_list|(
name|conn
operator|->
name|altsubject_match
argument_list|)
expr_stmt|;
name|conn
operator|->
name|altsubject_match
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|params
operator|->
name|altsubject_match
condition|)
block|{
name|conn
operator|->
name|altsubject_match
operator|=
name|os_strdup
argument_list|(
name|params
operator|->
name|altsubject_match
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|altsubject_match
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
block|}
comment|/* TODO: gnutls_certificate_set_verify_flags(xcred, flags);  	 * to force peer validation(?) */
if|if
condition|(
name|params
operator|->
name|ca_cert
condition|)
block|{
name|conn
operator|->
name|verify_peer
operator|=
literal|1
expr_stmt|;
name|ret
operator|=
name|gnutls_certificate_set_x509_trust_file
argument_list|(
name|conn
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|GNUTLS_X509_FMT_PEM
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read CA cert '%s' "
literal|"in PEM format: %s"
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|ret
operator|=
name|gnutls_certificate_set_x509_trust_file
argument_list|(
name|conn
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read CA cert "
literal|"'%s' in DER format: %s"
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
block|}
if|if
condition|(
name|params
operator|->
name|client_cert
operator|&&
name|params
operator|->
name|private_key
condition|)
block|{
comment|/* TODO: private_key_passwd? */
name|ret
operator|=
name|gnutls_certificate_set_x509_key_file
argument_list|(
name|conn
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_PEM
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read client cert/key "
literal|"in PEM format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|ret
operator|=
name|gnutls_certificate_set_x509_key_file
argument_list|(
name|conn
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read client "
literal|"cert/key in DER format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
block|}
block|}
elseif|else
if|if
condition|(
name|params
operator|->
name|private_key
condition|)
block|{
name|int
name|pkcs12_ok
init|=
literal|0
decl_stmt|;
ifdef|#
directive|ifdef
name|PKCS12_FUNCS
comment|/* Try to load in PKCS#12 format */
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|>=
literal|0x010302
name|ret
operator|=
name|gnutls_certificate_set_x509_simple_pkcs12_file
argument_list|(
name|conn
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|,
name|params
operator|->
name|private_key_passwd
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|!=
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to load private_key in "
literal|"PKCS#12 format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
else|else
name|pkcs12_ok
operator|=
literal|1
expr_stmt|;
endif|#
directive|endif
comment|/* LIBGNUTLS_VERSION_NUMBER>= 0x010302 */
endif|#
directive|endif
comment|/* PKCS12_FUNCS */
if|if
condition|(
operator|!
name|pkcs12_ok
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"GnuTLS: PKCS#12 support not "
literal|"included"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
name|conn
operator|->
name|tls_ia
operator|=
name|params
operator|->
name|tls_ia
expr_stmt|;
name|conn
operator|->
name|params_set
operator|=
literal|1
expr_stmt|;
name|ret
operator|=
name|gnutls_credentials_set
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_CRD_CERTIFICATE
argument_list|,
name|conn
operator|->
name|xcred
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"Failed to configure credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|->
name|iacred_cli
condition|)
name|gnutls_ia_free_client_credentials
argument_list|(
name|conn
operator|->
name|iacred_cli
argument_list|)
expr_stmt|;
name|ret
operator|=
name|gnutls_ia_allocate_client_credentials
argument_list|(
operator|&
name|conn
operator|->
name|iacred_cli
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to allocate IA credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|ret
operator|=
name|gnutls_credentials_set
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_CRD_IA
argument_list|,
name|conn
operator|->
name|iacred_cli
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to configure IA credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|gnutls_ia_free_client_credentials
argument_list|(
name|conn
operator|->
name|iacred_cli
argument_list|)
expr_stmt|;
name|conn
operator|->
name|iacred_cli
operator|=
name|NULL
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
endif|#
directive|endif
comment|/* GNUTLS_IE */
return|return
name|ret
return|;
block|}
end_function

begin_function
name|int
name|tls_global_set_params
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
specifier|const
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|tls_ctx
decl_stmt|;
name|int
name|ret
decl_stmt|;
comment|/* Currently, global parameters are only set when running in server 	 * mode. */
name|global
operator|->
name|server
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|global
operator|->
name|params_set
condition|)
block|{
name|gnutls_certificate_free_credentials
argument_list|(
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
name|global
operator|->
name|params_set
operator|=
literal|0
expr_stmt|;
block|}
name|ret
operator|=
name|gnutls_certificate_allocate_credentials
argument_list|(
operator|&
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to allocate global credentials "
literal|"%s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|params
operator|->
name|ca_cert
condition|)
block|{
name|ret
operator|=
name|gnutls_certificate_set_x509_trust_file
argument_list|(
name|global
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|GNUTLS_X509_FMT_PEM
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read CA cert '%s' "
literal|"in PEM format: %s"
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|ret
operator|=
name|gnutls_certificate_set_x509_trust_file
argument_list|(
name|global
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read CA cert "
literal|"'%s' in DER format: %s"
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
block|}
block|}
if|if
condition|(
name|params
operator|->
name|client_cert
operator|&&
name|params
operator|->
name|private_key
condition|)
block|{
comment|/* TODO: private_key_passwd? */
name|ret
operator|=
name|gnutls_certificate_set_x509_key_file
argument_list|(
name|global
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_PEM
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read client cert/key "
literal|"in PEM format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|ret
operator|=
name|gnutls_certificate_set_x509_key_file
argument_list|(
name|global
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to read client "
literal|"cert/key in DER format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
block|}
block|}
elseif|else
if|if
condition|(
name|params
operator|->
name|private_key
condition|)
block|{
name|int
name|pkcs12_ok
init|=
literal|0
decl_stmt|;
ifdef|#
directive|ifdef
name|PKCS12_FUNCS
comment|/* Try to load in PKCS#12 format */
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|>=
literal|0x010302
name|ret
operator|=
name|gnutls_certificate_set_x509_simple_pkcs12_file
argument_list|(
name|global
operator|->
name|xcred
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|,
name|params
operator|->
name|private_key_passwd
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|!=
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to load private_key in "
literal|"PKCS#12 format: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
else|else
name|pkcs12_ok
operator|=
literal|1
expr_stmt|;
endif|#
directive|endif
comment|/* LIBGNUTLS_VERSION_NUMBER>= 0x010302 */
endif|#
directive|endif
comment|/* PKCS12_FUNCS */
if|if
condition|(
operator|!
name|pkcs12_ok
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"GnuTLS: PKCS#12 support not "
literal|"included"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
block|}
name|global
operator|->
name|params_set
operator|=
literal|1
expr_stmt|;
return|return
literal|0
return|;
name|fail
label|:
name|gnutls_certificate_free_credentials
argument_list|(
name|global
operator|->
name|xcred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_global_set_verify
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|int
name|check_crl
parameter_list|)
block|{
comment|/* TODO */
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_verify
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|verify_peer
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
name|conn
operator|->
name|session
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|conn
operator|->
name|verify_peer
operator|=
name|verify_peer
expr_stmt|;
name|gnutls_certificate_server_set_request
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|verify_peer
condition|?
name|GNUTLS_CERT_REQUIRE
else|:
name|GNUTLS_CERT_REQUEST
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_keys
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|struct
name|tls_keys
modifier|*
name|keys
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|GNUTLS_INTERNAL_STRUCTURE_HACK
name|security_parameters_st
modifier|*
name|sec
decl_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_INTERNAL_STRUCTURE_HACK */
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
name|conn
operator|->
name|session
operator|==
name|NULL
operator|||
name|keys
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|os_memset
argument_list|(
name|keys
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|keys
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_INTERNAL_STRUCTURE_HACK
name|sec
operator|=
operator|&
name|conn
operator|->
name|session
operator|->
name|security_parameters
expr_stmt|;
name|keys
operator|->
name|master_key
operator|=
name|sec
operator|->
name|master_secret
expr_stmt|;
name|keys
operator|->
name|master_key_len
operator|=
name|TLS_MASTER_SIZE
expr_stmt|;
name|keys
operator|->
name|client_random
operator|=
name|sec
operator|->
name|client_random
expr_stmt|;
name|keys
operator|->
name|server_random
operator|=
name|sec
operator|->
name|server_random
expr_stmt|;
else|#
directive|else
comment|/* GNUTLS_INTERNAL_STRUCTURE_HACK */
name|keys
operator|->
name|client_random
operator|=
operator|(
name|u8
operator|*
operator|)
name|gnutls_session_get_client_random
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
name|keys
operator|->
name|server_random
operator|=
operator|(
name|u8
operator|*
operator|)
name|gnutls_session_get_server_random
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
comment|/* No access to master_secret */
endif|#
directive|endif
comment|/* GNUTLS_INTERNAL_STRUCTURE_HACK */
ifdef|#
directive|ifdef
name|GNUTLS_IA
name|gnutls_ia_extract_inner_secret
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|(
name|char
operator|*
operator|)
name|conn
operator|->
name|inner_secret
argument_list|)
expr_stmt|;
name|keys
operator|->
name|inner_secret
operator|=
name|conn
operator|->
name|inner_secret
expr_stmt|;
name|keys
operator|->
name|inner_secret_len
operator|=
name|TLS_MASTER_SIZE
expr_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
name|keys
operator|->
name|client_random_len
operator|=
name|TLS_RANDOM_SIZE
expr_stmt|;
name|keys
operator|->
name|server_random_len
operator|=
name|TLS_RANDOM_SIZE
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_prf
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|char
modifier|*
name|label
parameter_list|,
name|int
name|server_random_first
parameter_list|,
name|u8
modifier|*
name|out
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
if|#
directive|if
name|LIBGNUTLS_VERSION_NUMBER
operator|>=
literal|0x010302
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
name|conn
operator|->
name|session
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|gnutls_prf
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|os_strlen
argument_list|(
name|label
argument_list|)
argument_list|,
name|label
argument_list|,
name|server_random_first
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
name|out_len
argument_list|,
operator|(
name|char
operator|*
operator|)
name|out
argument_list|)
return|;
else|#
directive|else
comment|/* LIBGNUTLS_VERSION_NUMBER>= 0x010302 */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* LIBGNUTLS_VERSION_NUMBER>= 0x010302 */
block|}
end_function

begin_function
specifier|static
name|int
name|tls_connection_verify_peer
parameter_list|(
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
name|unsigned
name|int
name|status
decl_stmt|,
name|num_certs
decl_stmt|,
name|i
decl_stmt|;
name|struct
name|os_time
name|now
decl_stmt|;
specifier|const
name|gnutls_datum_t
modifier|*
name|certs
decl_stmt|;
name|gnutls_x509_crt_t
name|cert
decl_stmt|;
if|if
condition|(
name|gnutls_certificate_verify_peers2
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|&
name|status
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to verify peer "
literal|"certificate chain"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|conn
operator|->
name|verify_peer
operator|&&
operator|(
name|status
operator|&
name|GNUTLS_CERT_INVALID
operator|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Peer certificate not trusted"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|status
operator|&
name|GNUTLS_CERT_SIGNER_NOT_FOUND
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Peer certificate does not have a "
literal|"known issuer"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|status
operator|&
name|GNUTLS_CERT_REVOKED
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Peer certificate has been revoked"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|os_get_time
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
name|certs
operator|=
name|gnutls_certificate_get_peers
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|&
name|num_certs
argument_list|)
expr_stmt|;
if|if
condition|(
name|certs
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: No peer certificate chain "
literal|"received"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_certs
condition|;
name|i
operator|++
control|)
block|{
name|char
modifier|*
name|buf
decl_stmt|;
name|size_t
name|len
decl_stmt|;
if|if
condition|(
name|gnutls_x509_crt_init
argument_list|(
operator|&
name|cert
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Certificate initialization "
literal|"failed"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|gnutls_x509_crt_import
argument_list|(
name|cert
argument_list|,
operator|&
name|certs
index|[
name|i
index|]
argument_list|,
name|GNUTLS_X509_FMT_DER
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Could not parse peer "
literal|"certificate %d/%d"
argument_list|,
name|i
operator|+
literal|1
argument_list|,
name|num_certs
argument_list|)
expr_stmt|;
name|gnutls_x509_crt_deinit
argument_list|(
name|cert
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|gnutls_x509_crt_get_dn
argument_list|(
name|cert
argument_list|,
name|NULL
argument_list|,
operator|&
name|len
argument_list|)
expr_stmt|;
name|len
operator|++
expr_stmt|;
name|buf
operator|=
name|os_malloc
argument_list|(
name|len
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
condition|)
block|{
name|buf
index|[
literal|0
index|]
operator|=
name|buf
index|[
name|len
index|]
operator|=
literal|'\0'
expr_stmt|;
name|gnutls_x509_crt_get_dn
argument_list|(
name|cert
argument_list|,
name|buf
argument_list|,
operator|&
name|len
argument_list|)
expr_stmt|;
block|}
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Peer cert chain %d/%d: %s"
argument_list|,
name|i
operator|+
literal|1
argument_list|,
name|num_certs
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
literal|0
condition|)
block|{
comment|/* TODO: validate subject_match and altsubject_match */
block|}
name|os_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|gnutls_x509_crt_get_expiration_time
argument_list|(
name|cert
argument_list|)
operator|<
name|now
operator|.
name|sec
operator|||
name|gnutls_x509_crt_get_activation_time
argument_list|(
name|cert
argument_list|)
operator|>
name|now
operator|.
name|sec
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Peer certificate %d/%d is "
literal|"not valid at this time"
argument_list|,
name|i
operator|+
literal|1
argument_list|,
name|num_certs
argument_list|)
expr_stmt|;
name|gnutls_x509_crt_deinit
argument_list|(
name|cert
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|gnutls_x509_crt_deinit
argument_list|(
name|cert
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_function
name|u8
modifier|*
name|tls_connection_handshake
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|size_t
modifier|*
name|out_len
parameter_list|,
name|u8
modifier|*
modifier|*
name|appl_data
parameter_list|,
name|size_t
modifier|*
name|appl_data_len
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|ssl_ctx
decl_stmt|;
name|u8
modifier|*
name|out_data
decl_stmt|;
name|int
name|ret
decl_stmt|;
if|if
condition|(
name|appl_data
condition|)
operator|*
name|appl_data
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|in_data
operator|&&
name|in_len
condition|)
block|{
if|if
condition|(
name|conn
operator|->
name|pull_buf
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - %d bytes remaining in "
literal|"pull_buf"
argument_list|,
name|__func__
argument_list|,
name|conn
operator|->
name|pull_buf_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|pull_buf
operator|=
name|os_malloc
argument_list|(
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|pull_buf
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|os_memcpy
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|pull_buf_offset
operator|=
name|conn
operator|->
name|pull_buf
expr_stmt|;
name|conn
operator|->
name|pull_buf_len
operator|=
name|in_len
expr_stmt|;
block|}
name|ret
operator|=
name|gnutls_handshake
argument_list|(
name|conn
operator|->
name|session
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
switch|switch
condition|(
name|ret
condition|)
block|{
case|case
name|GNUTLS_E_AGAIN
case|:
if|if
condition|(
name|global
operator|->
name|server
operator|&&
name|conn
operator|->
name|established
operator|&&
name|conn
operator|->
name|push_buf
operator|==
name|NULL
condition|)
block|{
comment|/* Need to return something to trigger 				 * completion of EAP-TLS. */
name|conn
operator|->
name|push_buf
operator|=
name|os_malloc
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
break|break;
case|case
name|GNUTLS_E_FATAL_ALERT_RECEIVED
case|:
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - received fatal '%s' alert"
argument_list|,
name|__func__
argument_list|,
name|gnutls_alert_get_name
argument_list|(
name|gnutls_alert_get
argument_list|(
name|conn
operator|->
name|session
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
name|conn
operator|->
name|read_alerts
operator|++
expr_stmt|;
comment|/* continue */
default|default:
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - gnutls_handshake failed "
literal|"-> %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|conn
operator|->
name|failed
operator|++
expr_stmt|;
block|}
block|}
else|else
block|{
name|size_t
name|size
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|verify_peer
operator|&&
name|tls_connection_verify_peer
argument_list|(
name|conn
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Peer certificate chain "
literal|"failed validation"
argument_list|)
expr_stmt|;
name|conn
operator|->
name|failed
operator|++
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|conn
operator|->
name|tls_ia
operator|&&
operator|!
name|gnutls_ia_handshake_p
argument_list|(
name|conn
operator|->
name|session
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: No TLS/IA negotiation"
argument_list|)
expr_stmt|;
name|conn
operator|->
name|failed
operator|++
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|conn
operator|->
name|tls_ia
condition|)
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Start TLS/IA handshake"
argument_list|)
expr_stmt|;
else|else
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Handshake completed "
literal|"successfully"
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|established
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|push_buf
operator|==
name|NULL
condition|)
block|{
comment|/* Need to return something to get final TLS ACK. */
name|conn
operator|->
name|push_buf
operator|=
name|os_malloc
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|gnutls_session_get_data
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|NULL
argument_list|,
operator|&
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|global
operator|->
name|session_data
operator|==
name|NULL
operator|||
name|global
operator|->
name|session_data_size
operator|<
name|size
condition|)
block|{
name|os_free
argument_list|(
name|global
operator|->
name|session_data
argument_list|)
expr_stmt|;
name|global
operator|->
name|session_data
operator|=
name|os_malloc
argument_list|(
name|size
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|global
operator|->
name|session_data
condition|)
block|{
name|global
operator|->
name|session_data_size
operator|=
name|size
expr_stmt|;
name|gnutls_session_get_data
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|global
operator|->
name|session_data
argument_list|,
operator|&
name|global
operator|->
name|session_data_size
argument_list|)
expr_stmt|;
block|}
block|}
name|out_data
operator|=
name|conn
operator|->
name|push_buf
expr_stmt|;
operator|*
name|out_len
operator|=
name|conn
operator|->
name|push_buf_len
expr_stmt|;
name|conn
operator|->
name|push_buf
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|push_buf_len
operator|=
literal|0
expr_stmt|;
return|return
name|out_data
return|;
block|}
end_function

begin_function
name|u8
modifier|*
name|tls_connection_server_handshake
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|size_t
modifier|*
name|out_len
parameter_list|)
block|{
return|return
name|tls_connection_handshake
argument_list|(
name|ssl_ctx
argument_list|,
name|conn
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|,
name|out_len
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_encrypt
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|u8
modifier|*
name|out_data
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
name|ssize_t
name|res
decl_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|->
name|tls_ia
condition|)
name|res
operator|=
name|gnutls_ia_send
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|(
name|char
operator|*
operator|)
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
else|else
endif|#
directive|endif
comment|/* GNUTLS_IA */
name|res
operator|=
name|gnutls_record_send
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"%s: Encryption failed: %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|res
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|conn
operator|->
name|push_buf
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|conn
operator|->
name|push_buf_len
operator|<
name|out_len
condition|)
name|out_len
operator|=
name|conn
operator|->
name|push_buf_len
expr_stmt|;
name|os_memcpy
argument_list|(
name|out_data
argument_list|,
name|conn
operator|->
name|push_buf
argument_list|,
name|out_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|push_buf
argument_list|)
expr_stmt|;
name|conn
operator|->
name|push_buf
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|push_buf_len
operator|=
literal|0
expr_stmt|;
return|return
name|out_len
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_decrypt
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|u8
modifier|*
name|out_data
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
name|ssize_t
name|res
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|pull_buf
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - %d bytes remaining in "
literal|"pull_buf"
argument_list|,
name|__func__
argument_list|,
name|conn
operator|->
name|pull_buf_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|pull_buf
operator|=
name|os_malloc
argument_list|(
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|pull_buf
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|os_memcpy
argument_list|(
name|conn
operator|->
name|pull_buf
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|pull_buf_offset
operator|=
name|conn
operator|->
name|pull_buf
expr_stmt|;
name|conn
operator|->
name|pull_buf_len
operator|=
name|in_len
expr_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|->
name|tls_ia
condition|)
block|{
name|res
operator|=
name|gnutls_ia_recv
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|(
name|char
operator|*
operator|)
name|out_data
argument_list|,
name|out_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|out_len
operator|>=
literal|12
operator|&&
operator|(
name|res
operator|==
name|GNUTLS_E_WARNING_IA_IPHF_RECEIVED
operator|||
name|res
operator|==
name|GNUTLS_E_WARNING_IA_FPHF_RECEIVED
operator|)
condition|)
block|{
name|int
name|final
init|=
name|res
operator|==
name|GNUTLS_E_WARNING_IA_FPHF_RECEIVED
decl_stmt|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s: Received %sPhaseFinished"
argument_list|,
name|__func__
argument_list|,
name|final
condition|?
literal|"Final"
else|:
literal|"Intermediate"
argument_list|)
expr_stmt|;
name|res
operator|=
name|gnutls_ia_permute_inner_secret
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|,
operator|(
name|char
operator|*
operator|)
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|session_keys
condition|)
block|{
name|os_memset
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
literal|0
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|session_keys
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|session_keys_len
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|res
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s: Failed to permute "
literal|"inner secret: %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|res
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|res
operator|=
name|gnutls_ia_verify_endphase
argument_list|(
name|conn
operator|->
name|session
argument_list|,
operator|(
name|char
operator|*
operator|)
name|out_data
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s: Correct endphase "
literal|"checksum"
argument_list|,
name|__func__
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"%s: Endphase "
literal|"verification failed: %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|res
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|final
condition|)
name|conn
operator|->
name|final_phase_finished
operator|=
literal|1
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - gnutls_ia_recv failed: %d "
literal|"(%s)"
argument_list|,
name|__func__
argument_list|,
name|res
argument_list|,
name|gnutls_strerror
argument_list|(
name|res
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
name|res
return|;
block|}
endif|#
directive|endif
comment|/* GNUTLS_IA */
name|res
operator|=
name|gnutls_record_recv
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|out_data
argument_list|,
name|out_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s - gnutls_record_recv failed: %d "
literal|"(%s)"
argument_list|,
name|__func__
argument_list|,
name|res
argument_list|,
name|gnutls_strerror
argument_list|(
name|res
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
name|res
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_resumed
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
literal|0
return|;
return|return
name|gnutls_session_is_resumed
argument_list|(
name|conn
operator|->
name|session
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_master_key
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|key
parameter_list|,
name|size_t
name|key_len
parameter_list|)
block|{
comment|/* TODO */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_cipher_list
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|u8
modifier|*
name|ciphers
parameter_list|)
block|{
comment|/* TODO */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_get_cipher
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|buflen
parameter_list|)
block|{
comment|/* TODO */
name|buf
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_enable_workaround
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
comment|/* TODO: set SSL_OP_DONT_INSERT_EMPTY_FRAGMENTS */
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_client_hello_ext
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|ext_type
parameter_list|,
specifier|const
name|u8
modifier|*
name|data
parameter_list|,
name|size_t
name|data_len
parameter_list|)
block|{
comment|/* TODO */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_failed
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|conn
operator|->
name|failed
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_read_alerts
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|conn
operator|->
name|read_alerts
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_write_alerts
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|conn
operator|->
name|write_alerts
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_keyblock_size
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
comment|/* TODO */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|unsigned
name|int
name|tls_capabilities
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|)
block|{
name|unsigned
name|int
name|capa
init|=
literal|0
decl_stmt|;
ifdef|#
directive|ifdef
name|GNUTLS_IA
name|capa
operator||=
name|TLS_CAPABILITY_IA
expr_stmt|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
return|return
name|capa
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_ia
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|tls_ia
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|GNUTLS_IA
name|int
name|ret
decl_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|conn
operator|->
name|tls_ia
operator|=
name|tls_ia
expr_stmt|;
if|if
condition|(
operator|!
name|tls_ia
condition|)
return|return
literal|0
return|;
name|ret
operator|=
name|gnutls_ia_allocate_server_credentials
argument_list|(
operator|&
name|conn
operator|->
name|iacred_srv
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to allocate IA credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|ret
operator|=
name|gnutls_credentials_set
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|GNUTLS_CRD_IA
argument_list|,
name|conn
operator|->
name|iacred_srv
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"Failed to configure IA credentials: %s"
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|gnutls_ia_free_server_credentials
argument_list|(
name|conn
operator|->
name|iacred_srv
argument_list|)
expr_stmt|;
name|conn
operator|->
name|iacred_srv
operator|=
name|NULL
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
literal|0
return|;
else|#
directive|else
comment|/* GNUTLS_IA */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
block|}
end_function

begin_function
name|int
name|tls_connection_ia_send_phase_finished
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|final
parameter_list|,
name|u8
modifier|*
name|out_data
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|GNUTLS_IA
name|int
name|ret
decl_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
name|conn
operator|->
name|session
operator|==
name|NULL
operator|||
operator|!
name|conn
operator|->
name|tls_ia
condition|)
return|return
operator|-
literal|1
return|;
name|ret
operator|=
name|gnutls_ia_permute_inner_secret
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|,
operator|(
name|char
operator|*
operator|)
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|session_keys
condition|)
block|{
name|os_memset
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
literal|0
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|session_keys
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|session_keys_len
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s: Failed to permute inner secret: %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|ret
operator|=
name|gnutls_ia_endphase_send
argument_list|(
name|conn
operator|->
name|session
argument_list|,
name|final
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"%s: Failed to send endphase: %s"
argument_list|,
name|__func__
argument_list|,
name|gnutls_strerror
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|conn
operator|->
name|push_buf
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|conn
operator|->
name|push_buf_len
operator|<
name|out_len
condition|)
name|out_len
operator|=
name|conn
operator|->
name|push_buf_len
expr_stmt|;
name|os_memcpy
argument_list|(
name|out_data
argument_list|,
name|conn
operator|->
name|push_buf
argument_list|,
name|out_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|push_buf
argument_list|)
expr_stmt|;
name|conn
operator|->
name|push_buf
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|push_buf_len
operator|=
literal|0
expr_stmt|;
return|return
name|out_len
return|;
else|#
directive|else
comment|/* GNUTLS_IA */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
block|}
end_function

begin_function
name|int
name|tls_connection_ia_final_phase_finished
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|conn
operator|->
name|final_phase_finished
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_ia_permute_inner_secret
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|key
parameter_list|,
name|size_t
name|key_len
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|GNUTLS_IA
if|if
condition|(
name|conn
operator|==
name|NULL
operator|||
operator|!
name|conn
operator|->
name|tls_ia
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|conn
operator|->
name|session_keys
condition|)
block|{
name|os_memset
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
literal|0
argument_list|,
name|conn
operator|->
name|session_keys_len
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|)
expr_stmt|;
block|}
name|conn
operator|->
name|session_keys_len
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|key
condition|)
block|{
name|conn
operator|->
name|session_keys
operator|=
name|os_malloc
argument_list|(
name|key_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|session_keys
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|os_memcpy
argument_list|(
name|conn
operator|->
name|session_keys
argument_list|,
name|key
argument_list|,
name|key_len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|session_keys_len
operator|=
name|key_len
expr_stmt|;
block|}
else|else
block|{
name|conn
operator|->
name|session_keys
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|session_keys_len
operator|=
literal|0
expr_stmt|;
block|}
return|return
literal|0
return|;
else|#
directive|else
comment|/* GNUTLS_IA */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* GNUTLS_IA */
block|}
end_function

end_unit

