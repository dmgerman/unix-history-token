begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|// RUN: %clang_analyze_cc1 -std=c11 -analyzer-checker=alpha.clone.CloneChecker -verify %s
end_comment

begin_comment
comment|// expected-no-diagnostics
end_comment

begin_decl_stmt
name|int
name|global
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|foo1
parameter_list|()
block|{
if|if
condition|(
name|global
operator|>
literal|0
condition|)
return|return
literal|0
return|;
elseif|else
if|if
condition|(
name|global
operator|<
literal|0
condition|)
return|return
generic_selection|_Generic(
name|global
generic_selection|,
name|double
association|:
literal|1
association_list|,
name|float
association|:
literal|2
association_list|,
type|default
association|:
literal|3
generic_selection|)
return|;
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|// Different associated type (int instead of float)
end_comment

begin_function
name|int
name|foo2
parameter_list|()
block|{
if|if
condition|(
name|global
operator|>
literal|0
condition|)
return|return
literal|0
return|;
elseif|else
if|if
condition|(
name|global
operator|<
literal|0
condition|)
return|return
generic_selection|_Generic(
name|global
generic_selection|,
name|double
association|:
literal|1
association_list|,
name|int
association|:
literal|2
association_list|,
type|default
association|:
literal|4
generic_selection|)
return|;
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|// Different number of associated types.
end_comment

begin_function
name|int
name|foo3
parameter_list|()
block|{
if|if
condition|(
name|global
operator|>
literal|0
condition|)
return|return
literal|0
return|;
elseif|else
if|if
condition|(
name|global
operator|<
literal|0
condition|)
return|return
generic_selection|_Generic(
name|global
generic_selection|,
name|double
association|:
literal|1
association_list|,
type|default
association|:
literal|4
generic_selection|)
return|;
return|return
literal|1
return|;
block|}
end_function

end_unit

