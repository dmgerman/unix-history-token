begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* spool.c    See whether a filename is legal for the spool directory.  */
end_comment

begin_include
include|#
directive|include
file|"uucp.h"
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"uudefs.h"
end_include

begin_comment
comment|/* See whether a file is a spool file.  Spool file names are specially    crafted to hand around to other UUCP packages.  They always begin    with 'C', 'D' or 'X', and the second character is always a period.    The remaining characters may be any printable characters, since    they may include a grade set by another system.  */
end_comment

begin_function
name|boolean
name|fspool_file
parameter_list|(
name|zfile
parameter_list|)
specifier|const
name|char
modifier|*
name|zfile
decl_stmt|;
block|{
specifier|const
name|char
modifier|*
name|z
decl_stmt|;
if|if
condition|(
operator|*
name|zfile
operator|!=
literal|'C'
operator|&&
operator|*
name|zfile
operator|!=
literal|'D'
operator|&&
operator|*
name|zfile
operator|!=
literal|'X'
condition|)
return|return
name|FALSE
return|;
if|if
condition|(
name|zfile
index|[
literal|1
index|]
operator|!=
literal|'.'
condition|)
return|return
name|FALSE
return|;
for|for
control|(
name|z
operator|=
name|zfile
operator|+
literal|2
init|;
operator|*
name|z
operator|!=
literal|'\0'
condition|;
name|z
operator|++
control|)
if|if
condition|(
operator|*
name|z
operator|==
literal|'/'
operator|||
operator|!
name|isprint
argument_list|(
name|BUCHAR
argument_list|(
operator|*
name|z
argument_list|)
argument_list|)
operator|||
name|isspace
argument_list|(
name|BUCHAR
argument_list|(
operator|*
name|z
argument_list|)
argument_list|)
condition|)
return|return
name|FALSE
return|;
return|return
name|TRUE
return|;
block|}
end_function

end_unit

