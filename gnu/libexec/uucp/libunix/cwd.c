begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* cwd.c    Routines dealing with the current working directory.  */
end_comment

begin_include
include|#
directive|include
file|"uucp.h"
end_include

begin_include
include|#
directive|include
file|"uudefs.h"
end_include

begin_include
include|#
directive|include
file|"sysdep.h"
end_include

begin_include
include|#
directive|include
file|"system.h"
end_include

begin_comment
comment|/* See whether running this file through zsysdep_add_cwd would require    knowing the current working directory.  This is used to avoid    determining the cwd if it will not be needed.  */
end_comment

begin_function
name|boolean
name|fsysdep_needs_cwd
parameter_list|(
name|zfile
parameter_list|)
specifier|const
name|char
modifier|*
name|zfile
decl_stmt|;
block|{
return|return
operator|*
name|zfile
operator|!=
literal|'/'
operator|&&
operator|*
name|zfile
operator|!=
literal|'~'
return|;
block|}
end_function

begin_comment
comment|/* Expand a local file, putting relative pathnames in the current    working directory.  Note that ~/file is placed in the public    directory, rather than in the user's home directory.  This is    consistent with other UUCP packages.  */
end_comment

begin_function
name|char
modifier|*
name|zsysdep_local_file_cwd
parameter_list|(
name|zfile
parameter_list|,
name|zpubdir
parameter_list|,
name|pfbadname
parameter_list|)
specifier|const
name|char
modifier|*
name|zfile
decl_stmt|;
specifier|const
name|char
modifier|*
name|zpubdir
decl_stmt|;
name|boolean
modifier|*
name|pfbadname
decl_stmt|;
block|{
if|if
condition|(
name|pfbadname
operator|!=
name|NULL
condition|)
operator|*
name|pfbadname
operator|=
name|FALSE
expr_stmt|;
if|if
condition|(
operator|*
name|zfile
operator|==
literal|'/'
condition|)
return|return
name|zbufcpy
argument_list|(
name|zfile
argument_list|)
return|;
elseif|else
if|if
condition|(
operator|*
name|zfile
operator|==
literal|'~'
condition|)
return|return
name|zsysdep_local_file
argument_list|(
name|zfile
argument_list|,
name|zpubdir
argument_list|,
name|pfbadname
argument_list|)
return|;
else|else
return|return
name|zsysdep_add_cwd
argument_list|(
name|zfile
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* Add the current working directory to a remote file name.  */
end_comment

begin_function
name|char
modifier|*
name|zsysdep_add_cwd
parameter_list|(
name|zfile
parameter_list|)
specifier|const
name|char
modifier|*
name|zfile
decl_stmt|;
block|{
if|if
condition|(
operator|*
name|zfile
operator|==
literal|'/'
operator|||
operator|*
name|zfile
operator|==
literal|'~'
condition|)
return|return
name|zbufcpy
argument_list|(
name|zfile
argument_list|)
return|;
if|if
condition|(
name|zScwd
operator|==
name|NULL
condition|)
block|{
name|ulog
argument_list|(
name|LOG_ERROR
argument_list|,
literal|"Can't determine current directory"
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
return|return
name|zsysdep_in_dir
argument_list|(
name|zScwd
argument_list|,
name|zfile
argument_list|)
return|;
block|}
end_function

end_unit

