begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* rename.c    Rename a file to a new name (Unix specific implementation).  */
end_comment

begin_include
include|#
directive|include
file|"uucp.h"
end_include

begin_include
include|#
directive|include
file|"sysdep.h"
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_comment
comment|/* This implementation will not work on directories, but fortunately    we never want to rename directories.  */
end_comment

begin_function
name|int
name|rename
parameter_list|(
name|zfrom
parameter_list|,
name|zto
parameter_list|)
specifier|const
name|char
modifier|*
name|zfrom
decl_stmt|;
specifier|const
name|char
modifier|*
name|zto
decl_stmt|;
block|{
if|if
condition|(
name|link
argument_list|(
name|zfrom
argument_list|,
name|zto
argument_list|)
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|!=
name|EEXIST
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|unlink
argument_list|(
name|zto
argument_list|)
operator|<
literal|0
operator|||
name|link
argument_list|(
name|zfrom
argument_list|,
name|zto
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|-
literal|1
return|;
block|}
return|return
name|unlink
argument_list|(
name|zfrom
argument_list|)
return|;
block|}
end_function

end_unit

