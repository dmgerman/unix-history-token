begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* addbas.c    If we have a directory, add in a base name.  */
end_comment

begin_include
include|#
directive|include
file|"uucp.h"
end_include

begin_include
include|#
directive|include
file|"uudefs.h"
end_include

begin_include
include|#
directive|include
file|"sysdep.h"
end_include

begin_include
include|#
directive|include
file|"system.h"
end_include

begin_comment
comment|/* If we have a directory, add a base name.  */
end_comment

begin_function
name|char
modifier|*
name|zsysdep_add_base
parameter_list|(
name|zfile
parameter_list|,
name|zname
parameter_list|)
specifier|const
name|char
modifier|*
name|zfile
decl_stmt|;
specifier|const
name|char
modifier|*
name|zname
decl_stmt|;
block|{
name|size_t
name|clen
decl_stmt|;
specifier|const
name|char
modifier|*
name|zlook
decl_stmt|;
name|char
modifier|*
name|zfree
decl_stmt|;
name|char
modifier|*
name|zret
decl_stmt|;
if|#
directive|if
name|DEBUG
operator|>
literal|0
if|if
condition|(
operator|*
name|zfile
operator|!=
literal|'/'
condition|)
name|ulog
argument_list|(
name|LOG_FATAL
argument_list|,
literal|"zsysdep_add_base: %s: Can't happen"
argument_list|,
name|zfile
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|clen
operator|=
name|strlen
argument_list|(
name|zfile
argument_list|)
expr_stmt|;
if|if
condition|(
name|zfile
index|[
name|clen
operator|-
literal|1
index|]
operator|!=
literal|'/'
condition|)
block|{
if|if
condition|(
operator|!
name|fsysdep_directory
argument_list|(
name|zfile
argument_list|)
condition|)
return|return
name|zbufcpy
argument_list|(
name|zfile
argument_list|)
return|;
name|zfree
operator|=
name|NULL
expr_stmt|;
block|}
else|else
block|{
comment|/* Trim out the trailing '/'.  */
name|zfree
operator|=
name|zbufcpy
argument_list|(
name|zfile
argument_list|)
expr_stmt|;
name|zfree
index|[
name|clen
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|zfile
operator|=
name|zfree
expr_stmt|;
block|}
name|zlook
operator|=
name|strrchr
argument_list|(
name|zname
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|zlook
operator|!=
name|NULL
condition|)
name|zname
operator|=
name|zlook
operator|+
literal|1
expr_stmt|;
name|zret
operator|=
name|zsysdep_in_dir
argument_list|(
name|zfile
argument_list|,
name|zname
argument_list|)
expr_stmt|;
name|ubuffree
argument_list|(
name|zfree
argument_list|)
expr_stmt|;
return|return
name|zret
return|;
block|}
end_function

end_unit

