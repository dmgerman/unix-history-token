begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $FreeBSD$ */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|STDC_HEADERS
end_ifdef

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_else
else|#
directive|else
end_else

begin_function_decl
name|double
name|strtod
parameter_list|()
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"xstrtod.h"
end_include

begin_function
name|int
name|xstrtod
parameter_list|(
name|str
parameter_list|,
name|ptr
parameter_list|,
name|result
parameter_list|)
specifier|const
name|char
modifier|*
name|str
decl_stmt|;
specifier|const
name|char
modifier|*
modifier|*
name|ptr
decl_stmt|;
name|double
modifier|*
name|result
decl_stmt|;
block|{
name|double
name|val
decl_stmt|;
name|char
modifier|*
name|terminator
decl_stmt|;
name|int
name|fail
decl_stmt|;
name|fail
operator|=
literal|0
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|val
operator|=
name|strtod
argument_list|(
name|str
argument_list|,
operator|&
name|terminator
argument_list|)
expr_stmt|;
comment|/* Having a non-zero terminator is an error only when PTR is NULL. */
if|if
condition|(
name|terminator
operator|==
name|str
operator|||
operator|(
name|ptr
operator|==
name|NULL
operator|&&
operator|*
name|terminator
operator|!=
literal|'\0'
operator|)
condition|)
name|fail
operator|=
literal|1
expr_stmt|;
else|else
block|{
comment|/* Allow underflow (in which case strtod returns zero), 	 but flag overflow as an error. */
if|if
condition|(
name|val
operator|!=
literal|0.0
operator|&&
name|errno
operator|==
name|ERANGE
condition|)
name|fail
operator|=
literal|1
expr_stmt|;
block|}
if|if
condition|(
name|ptr
operator|!=
name|NULL
condition|)
operator|*
name|ptr
operator|=
name|terminator
expr_stmt|;
operator|*
name|result
operator|=
name|val
expr_stmt|;
return|return
name|fail
return|;
block|}
end_function

end_unit

