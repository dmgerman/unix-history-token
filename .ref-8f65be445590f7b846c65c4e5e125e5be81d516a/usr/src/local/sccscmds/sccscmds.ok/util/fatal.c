begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_decl_stmt
specifier|static
name|char
name|Sccsid
index|[]
init|=
literal|"@(#)fatal.c	1.4	%G%"
decl_stmt|;
end_decl_stmt

begin_include
include|#
directive|include
file|"../hdr/macros.h"
end_include

begin_include
include|#
directive|include
file|"../hdr/fatal.h"
end_include

begin_include
include|#
directive|include
file|<sys/syscall.h>
end_include

begin_define
define|#
directive|define
name|syswrite
parameter_list|(
name|a
parameter_list|,
name|b
parameter_list|,
name|c
parameter_list|)
value|syscall(SYS_write,a,b,c)
end_define

begin_comment
comment|/* 	General purpose error handler. 	Typically, low level subroutines which detect error conditions 	(an open or create routine, for example) return the 	value of calling fatal with an appropriate error message string. 	E.g.,	return(fatal("can't do it")); 	Higher level routines control the execution of fatal 	via the global word Fflags. 	The macros FSAVE and FRSTR in<fatal.h> can be used by higher 	level subroutines to save and restore the Fflags word.   	The argument to fatal is a pointer to an error message string. 	The action of this routine is driven completely from 	the "Fflags" global word (see<fatal.h>). 	The following discusses the interpretation of the various bits 	of Fflags.   	The FTLMSG bit controls the writing of the error 	message on file descriptor 2.  The message is preceded 	by the string "ERROR: ", unless the global character pointer 	"Ffile" is non-zero, in which case the message is preceded 	by the string "ERROR [<Ffile>]: ".  A newline is written 	after the user supplied message.   	If the FTLCLN bit is on, clean_up is called with an 	argument of 0 (see clean.c).   	If the FTLFUNC bit is on, the function pointed to by the global 	function pointer "Ffunc" is called with the user supplied 	error message pointer as argument. 	(This feature can be used to log error messages).   	The FTLACT bits determine how fatal should return. 	If the FTLJMP bit is on longjmp(Fjmp) is 	called (Fjmp is a global vector of n words, see 	setjmp, longjmp documentation).   	If the FTLEXIT bit is on the value of userexit(1) is 	passed as an argument to exit(II) 	(see userexit.c).   	If none of the FTLACT bits are on 	(the default value for Fflags is 0), the global word 	"Fvalue" (initialized to -1) is returned.   	If all fatal globals have their default values, fatal simply 	returns -1. */
end_comment

begin_decl_stmt
name|int
name|Fcnt
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|Fflags
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|Ffile
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|Fvalue
init|=
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_function_decl
name|int
function_decl|(
modifier|*
name|Ffunc
function_decl|)
parameter_list|()
function_decl|;
end_function_decl

begin_decl_stmt
name|jmp_buf
name|Fjmp
decl_stmt|;
end_decl_stmt

begin_macro
name|fatal
argument_list|(
argument|msg
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|msg
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|++
name|Fcnt
expr_stmt|;
if|if
condition|(
name|Fflags
operator|&
name|FTLMSG
condition|)
block|{
name|syswrite
argument_list|(
literal|2
argument_list|,
literal|"ERROR"
argument_list|,
literal|5
argument_list|)
expr_stmt|;
if|if
condition|(
name|Ffile
condition|)
block|{
name|syswrite
argument_list|(
literal|2
argument_list|,
literal|" ["
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|syswrite
argument_list|(
literal|2
argument_list|,
name|Ffile
argument_list|,
name|length
argument_list|(
name|Ffile
argument_list|)
argument_list|)
expr_stmt|;
name|syswrite
argument_list|(
literal|2
argument_list|,
literal|"]"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
name|syswrite
argument_list|(
literal|2
argument_list|,
literal|": "
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|syswrite
argument_list|(
literal|2
argument_list|,
name|msg
argument_list|,
name|length
argument_list|(
name|msg
argument_list|)
argument_list|)
expr_stmt|;
name|syswrite
argument_list|(
literal|2
argument_list|,
literal|"\n"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|Fflags
operator|&
name|FTLCLN
condition|)
name|clean_up
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|Fflags
operator|&
name|FTLFUNC
condition|)
call|(
modifier|*
name|Ffunc
call|)
argument_list|(
name|msg
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|Fflags
operator|&
name|FTLACT
condition|)
block|{
case|case
name|FTLJMP
case|:
name|longjmp
argument_list|(
name|Fjmp
argument_list|,
literal|1
argument_list|)
expr_stmt|;
case|case
name|FTLEXIT
case|:
name|exit
argument_list|(
name|userexit
argument_list|(
literal|1
argument_list|)
argument_list|)
expr_stmt|;
case|case
name|FTLRET
case|:
return|return
operator|(
name|Fvalue
operator|)
return|;
block|}
block|}
end_block

end_unit

