begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1988 University of Utah.  * Copyright (c) 1991 The Regents of the University of California.  * All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * the Systems Programming Group of the University of Utah Computer  * Science Department.  *  * %sccs.include.redist.c%  *  * from: Utah $Hdr: vm_unix.c 1.1 89/11/07$  *  *	@(#)vm_unix.c	7.1 (Berkeley) %G%  */
end_comment

begin_comment
comment|/*  * Traditional sbrk/grow interface to VM  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_param.h"
end_include

begin_include
include|#
directive|include
file|"machine/vmparam.h"
end_include

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|obreak
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|char
modifier|*
name|nsiz
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vm_offset_t
name|new
decl_stmt|,
name|old
decl_stmt|;
name|int
name|rv
decl_stmt|;
specifier|register
name|int
name|diff
decl_stmt|;
name|old
operator|=
operator|(
name|vm_offset_t
operator|)
name|u
operator|.
name|u_daddr
expr_stmt|;
name|new
operator|=
name|round_page
argument_list|(
name|uap
operator|->
name|nsiz
argument_list|)
expr_stmt|;
if|if
condition|(
call|(
name|int
call|)
argument_list|(
name|new
operator|-
name|old
argument_list|)
operator|>
name|u
operator|.
name|u_rlimit
index|[
name|RLIMIT_DATA
index|]
operator|.
name|rlim_cur
condition|)
return|return
operator|(
name|ENOMEM
operator|)
return|;
name|old
operator|=
name|round_page
argument_list|(
name|old
operator|+
name|ctob
argument_list|(
name|u
operator|.
name|u_dsize
argument_list|)
argument_list|)
expr_stmt|;
name|diff
operator|=
name|new
operator|-
name|old
expr_stmt|;
if|if
condition|(
name|diff
operator|>
literal|0
condition|)
block|{
name|rv
operator|=
name|vm_allocate
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
operator|&
name|old
argument_list|,
name|diff
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
name|uprintf
argument_list|(
literal|"sbrk: grow failed, return = %d\n"
argument_list|,
name|rv
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOMEM
operator|)
return|;
block|}
name|u
operator|.
name|u_dsize
operator|+=
name|btoc
argument_list|(
name|diff
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|diff
operator|<
literal|0
condition|)
block|{
name|diff
operator|=
operator|-
name|diff
expr_stmt|;
name|rv
operator|=
name|vm_deallocate
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
name|new
argument_list|,
name|diff
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
name|uprintf
argument_list|(
literal|"sbrk: shrink failed, return = %d\n"
argument_list|,
name|rv
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOMEM
operator|)
return|;
block|}
name|u
operator|.
name|u_dsize
operator|-=
name|btoc
argument_list|(
name|diff
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * grow the stack to include the SP  * true return if successful.  */
end_comment

begin_macro
name|grow
argument_list|(
argument|sp
argument_list|)
end_macro

begin_decl_stmt
name|unsigned
name|sp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|si
decl_stmt|;
comment|/* 	 * For user defined stacks (from sendsig). 	 */
if|if
condition|(
name|sp
operator|<
operator|(
name|unsigned
operator|)
name|u
operator|.
name|u_maxsaddr
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* 	 * For common case of already allocated (from trap). 	 */
if|if
condition|(
name|sp
operator|>=
name|USRSTACK
operator|-
name|ctob
argument_list|(
name|u
operator|.
name|u_ssize
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * Really need to check vs limit and increment stack size if ok. 	 */
name|si
operator|=
name|clrnd
argument_list|(
name|btoc
argument_list|(
name|USRSTACK
operator|-
name|sp
argument_list|)
operator|-
name|u
operator|.
name|u_ssize
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_ssize
operator|+
name|si
operator|>
name|btoc
argument_list|(
name|u
operator|.
name|u_rlimit
index|[
name|RLIMIT_STACK
index|]
operator|.
name|rlim_cur
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|u
operator|.
name|u_ssize
operator|+=
name|si
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|ovadvise
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|int
name|anom
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{  }
end_block

end_unit

