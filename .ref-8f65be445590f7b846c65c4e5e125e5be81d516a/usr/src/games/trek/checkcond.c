begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1980 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)checkcond.c	5.4 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|"trek.h"
end_include

begin_comment
comment|/* **  Check for Condition After a Move ** **	Various ship conditions are checked.  First we check **	to see if we have already lost the game, due to running **	out of life support reserves, running out of energy, **	or running out of crew members.  The check for running **	out of time is in events(). ** **	If we are in automatic override mode (Etc.nkling< 0), we **	don't want to do anything else, lest we call autover **	recursively. ** **	In the normal case, if there is a supernova, we call **	autover() to help us escape.  If after calling autover() **	we are still in the grips of a supernova, we get burnt **	up. ** **	If there are no Klingons in this quadrant, we nullify any **	distress calls which might exist. ** **	We then set the condition code, based on the energy level **	and battle conditions. */
end_comment

begin_macro
name|checkcond
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|,
name|j
decl_stmt|;
comment|/* see if we are still alive and well */
if|if
condition|(
name|Ship
operator|.
name|reserves
operator|<
literal|0.0
condition|)
name|lose
argument_list|(
name|L_NOLIFE
argument_list|)
expr_stmt|;
if|if
condition|(
name|Ship
operator|.
name|energy
operator|<=
literal|0
condition|)
name|lose
argument_list|(
name|L_NOENGY
argument_list|)
expr_stmt|;
if|if
condition|(
name|Ship
operator|.
name|crew
operator|<=
literal|0
condition|)
name|lose
argument_list|(
name|L_NOCREW
argument_list|)
expr_stmt|;
comment|/* if in auto override mode, ignore the rest */
if|if
condition|(
name|Etc
operator|.
name|nkling
operator|<
literal|0
condition|)
return|return;
comment|/* call in automatic override if appropriate */
if|if
condition|(
name|Quad
index|[
name|Ship
operator|.
name|quadx
index|]
index|[
name|Ship
operator|.
name|quady
index|]
operator|.
name|stars
operator|<
literal|0
condition|)
name|autover
argument_list|()
expr_stmt|;
if|if
condition|(
name|Quad
index|[
name|Ship
operator|.
name|quadx
index|]
index|[
name|Ship
operator|.
name|quady
index|]
operator|.
name|stars
operator|<
literal|0
condition|)
name|lose
argument_list|(
name|L_SNOVA
argument_list|)
expr_stmt|;
comment|/* nullify distress call if appropriate */
if|if
condition|(
name|Etc
operator|.
name|nkling
operator|<=
literal|0
condition|)
name|killd
argument_list|(
name|Ship
operator|.
name|quadx
argument_list|,
name|Ship
operator|.
name|quady
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* set condition code */
if|if
condition|(
name|Ship
operator|.
name|cond
operator|==
name|DOCKED
condition|)
return|return;
if|if
condition|(
name|Etc
operator|.
name|nkling
operator|>
literal|0
condition|)
block|{
name|Ship
operator|.
name|cond
operator|=
name|RED
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|Ship
operator|.
name|energy
operator|<
name|Param
operator|.
name|energylow
condition|)
block|{
name|Ship
operator|.
name|cond
operator|=
name|YELLOW
expr_stmt|;
return|return;
block|}
name|Ship
operator|.
name|cond
operator|=
name|GREEN
expr_stmt|;
return|return;
block|}
end_block

end_unit

