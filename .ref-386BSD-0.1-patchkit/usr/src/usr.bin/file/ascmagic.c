begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Ascii magic -- file types that we know based on keywords  * that can appear anywhere in the file.  *  * Copyright (c) Ian F. Darwin, 1987.  * Written by Ian F. Darwin.  *  * This software is not subject to any license of the American Telephone  * and Telegraph Company or of the Regents of the University of California.  *  * Permission is granted to anyone to use this software for any purpose on  * any computer system, and to alter it and redistribute it freely, subject  * to the following restrictions:  *  * 1. The author is not responsible for the consequences of use of this  *    software, no matter how awful, even if they arise from flaws in it.  *  * 2. The origin of this software must not be misrepresented, either by  *    explicit claim or by omission.  Since few users ever read sources,  *    credits must appear in the documentation.  *  * 3. Altered versions must be plainly marked as such, and must not be  *    misrepresented as being the original software.  Since few users  *    ever read sources, credits must appear in the documentation.  *  * 4. This notice may not be removed or altered.  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"file.h"
end_include

begin_include
include|#
directive|include
file|"names.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|moduleid
init|=
literal|"@(#)$Header: ascmagic.c,v 1.5 87/09/16 14:44:45 ian Exp $"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* lint */
end_comment

begin_decl_stmt
name|char
modifier|*
name|ckfmsg
init|=
literal|"write error on output"
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* an optimisation over plain strcmp() */
end_comment

begin_define
define|#
directive|define
name|STREQ
parameter_list|(
name|a
parameter_list|,
name|b
parameter_list|)
value|(*(a) == *(b)&& strcmp((a), (b)) == 0)
end_define

begin_expr_stmt
name|ascmagic
argument_list|(
name|buf
argument_list|)
specifier|register
name|char
operator|*
name|buf
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
name|char
modifier|*
name|s
decl_stmt|,
modifier|*
name|strtok
argument_list|()
decl_stmt|,
modifier|*
name|token
decl_stmt|;
specifier|register
name|struct
name|names
modifier|*
name|p
decl_stmt|;
specifier|extern
name|int
name|nbytes
decl_stmt|;
name|short
name|has_escapes
init|=
literal|0
decl_stmt|;
comment|/* these are easy, do them first */
comment|/* 	 * for troff, look for . + letter + letter; 	 * this must be done to disambiguate tar archives' ./file 	 * and other trash from real troff input. 	 */
if|if
condition|(
operator|*
name|buf
operator|==
literal|'.'
operator|&&
name|isascii
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|1
operator|)
argument_list|)
operator|&&
name|isalnum
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|1
operator|)
argument_list|)
operator|&&
name|isascii
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|2
operator|)
argument_list|)
operator|&&
name|isalnum
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|2
operator|)
argument_list|)
condition|)
block|{
name|ckfputs
argument_list|(
literal|"troff or preprocessor input text"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
if|if
condition|(
operator|(
operator|*
name|buf
operator|==
literal|'c'
operator|||
operator|*
name|buf
operator|==
literal|'C'
operator|)
operator|&&
name|isascii
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|1
operator|)
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
operator|(
name|buf
operator|+
literal|1
operator|)
argument_list|)
condition|)
block|{
name|ckfputs
argument_list|(
literal|"fortran program text"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
comment|/* look for tokens from names.h - this is expensive! */
name|s
operator|=
name|buf
expr_stmt|;
while|while
condition|(
operator|(
name|token
operator|=
name|strtok
argument_list|(
name|s
argument_list|,
literal|" \t\n\r\f"
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
name|s
operator|=
name|NULL
expr_stmt|;
comment|/* make strtok() keep on tokin' */
for|for
control|(
name|p
operator|=
name|names
init|;
name|p
operator|<
name|names
operator|+
name|NNAMES
condition|;
name|p
operator|++
control|)
block|{
if|if
condition|(
name|STREQ
argument_list|(
name|p
operator|->
name|name
argument_list|,
name|token
argument_list|)
condition|)
block|{
name|ckfputs
argument_list|(
name|types
index|[
name|p
operator|->
name|type
index|]
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
block|}
block|}
switch|switch
condition|(
name|is_tar
argument_list|(
name|buf
argument_list|)
condition|)
block|{
case|case
literal|1
case|:
name|ckfputs
argument_list|(
literal|"tar archive"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
case|case
literal|2
case|:
name|ckfputs
argument_list|(
literal|"POSIX tar archive"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|nbytes
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
operator|!
name|isascii
argument_list|(
operator|*
operator|(
name|buf
operator|+
name|i
operator|)
argument_list|)
condition|)
return|return
literal|0
return|;
comment|/* not all ascii */
if|if
condition|(
operator|*
operator|(
name|buf
operator|+
name|i
operator|)
operator|==
literal|'\033'
condition|)
comment|/* ascii ESCAPE */
name|has_escapes
operator|++
expr_stmt|;
block|}
comment|/* all else fails, but it is ascii... */
if|if
condition|(
name|has_escapes
condition|)
block|{
name|ckfputs
argument_list|(
literal|"ascii text (with escape sequences)"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ckfputs
argument_list|(
literal|"ascii text"
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
block|}
return|return
literal|1
return|;
block|}
end_block

end_unit

