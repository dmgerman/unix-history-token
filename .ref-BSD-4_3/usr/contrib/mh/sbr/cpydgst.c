begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* cpydgst.c - copy from one fd to another in encapsulating mode */
end_comment

begin_include
include|#
directive|include
file|"../h/mh.h"
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_comment
comment|/* All we want to do is to perform the substitution  	\n(-.*)\n	-->	\n- \1\n      we could use  	sed -e 's%^-%- -%'< ifile> ofile      to do this, but the routine below is faster than the pipe, fork, and     exec.  */
end_comment

begin_define
define|#
directive|define
name|S1
value|0
end_define

begin_define
define|#
directive|define
name|S2
value|1
end_define

begin_define
define|#
directive|define
name|output
parameter_list|(
name|c
parameter_list|)
value|if (bp>= dp) {flush(); *bp++ = c;} else *bp++ = c
end_define

begin_define
define|#
directive|define
name|flush
parameter_list|()
value|if ((j = bp - outbuf)&& write (out, outbuf, j) != j) \ 			adios (ofile, "error writing"); \ 		    else \ 			bp = outbuf
end_define

begin_comment
comment|/*
comment|*/
end_comment

begin_function
name|void
name|cpydgst
parameter_list|(
name|in
parameter_list|,
name|out
parameter_list|,
name|ifile
parameter_list|,
name|ofile
parameter_list|)
specifier|register
name|int
name|in
decl_stmt|,
name|out
decl_stmt|;
specifier|register
name|char
modifier|*
name|ifile
decl_stmt|,
decl|*
name|ofile
decl_stmt|;
end_function

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|,
name|state
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|ep
decl_stmt|;
name|char
name|buffer
index|[
name|BUFSIZ
index|]
decl_stmt|;
specifier|register
name|int
name|j
decl_stmt|;
specifier|register
name|char
modifier|*
name|bp
decl_stmt|,
modifier|*
name|dp
decl_stmt|;
name|char
name|outbuf
index|[
name|BUFSIZ
index|]
decl_stmt|;
name|dp
operator|=
operator|(
name|bp
operator|=
name|outbuf
operator|)
operator|+
sizeof|sizeof
name|outbuf
expr_stmt|;
for|for
control|(
name|state
operator|=
name|S1
init|;
operator|(
name|i
operator|=
name|read
argument_list|(
name|in
argument_list|,
name|buffer
argument_list|,
sizeof|sizeof
name|buffer
argument_list|)
operator|)
operator|>
literal|0
condition|;
control|)
for|for
control|(
name|ep
operator|=
operator|(
name|cp
operator|=
name|buffer
operator|)
operator|+
name|i
init|;
name|cp
operator|<
name|ep
condition|;
name|cp
operator|++
control|)
block|{
if|if
condition|(
operator|*
name|cp
operator|==
name|NULL
condition|)
continue|continue;
switch|switch
condition|(
name|state
condition|)
block|{
case|case
name|S1
case|:
if|if
condition|(
operator|*
name|cp
operator|==
literal|'-'
condition|)
block|{
name|output
argument_list|(
literal|'-'
argument_list|)
expr_stmt|;
name|output
argument_list|(
literal|' '
argument_list|)
expr_stmt|;
block|}
name|state
operator|=
name|S2
expr_stmt|;
comment|/* fall */
case|case
name|S2
case|:
name|output
argument_list|(
operator|*
name|cp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|cp
operator|==
literal|'\n'
condition|)
name|state
operator|=
name|S1
expr_stmt|;
break|break;
block|}
block|}
if|if
condition|(
name|i
operator|==
name|NOTOK
condition|)
name|adios
argument_list|(
name|ifile
argument_list|,
literal|"error reading"
argument_list|)
expr_stmt|;
name|flush
argument_list|()
expr_stmt|;
block|}
end_block

end_unit

