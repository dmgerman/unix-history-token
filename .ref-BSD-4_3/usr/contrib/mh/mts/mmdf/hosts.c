begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* hosts.c - use MMDF to get hostname information */
end_comment

begin_comment
comment|/* LINTLIBRARY */
end_comment

begin_include
include|#
directive|include
file|"../h/strings.h"
end_include

begin_include
include|#
directive|include
file|"util.h"
end_include

begin_include
include|#
directive|include
file|"mmdf.h"
end_include

begin_include
include|#
directive|include
file|"ch.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|MMDFII
end_ifdef

begin_include
include|#
directive|include
file|"dm.h"
end_include

begin_endif
endif|#
directive|endif
endif|MMDFII
end_endif

begin_include
include|#
directive|include
file|"../zotnet/mts.h"
end_include

begin_comment
comment|/*   * We really should be careful with the number of fd's that this routine  * opens:  the ch_seq ch_table.c module likes to keep 6 (yes, SIX) fds around  * to speed-up host lookups in the channel table.  Unfortunately, after all  * of them get opened, six are always open (ch_table may close one to open  * another).  The bottom line is that if repl calls post, then we get 12  * (yes, TWELVE) fds open, with only six usable.  *  * send will close all fds>= 3 prior to invoking post.  It would be nice  * if one could control ch_seq's use of fds for table lookups, but such is  * life.  *  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|MMDFII
end_ifndef

begin_function
name|char
modifier|*
name|OfficialName
parameter_list|(
name|name
parameter_list|)
specifier|register
name|char
modifier|*
name|name
decl_stmt|;
block|{
specifier|register
name|Chan
modifier|*
name|ch
decl_stmt|;
specifier|static
name|char
name|buffer
index|[
name|BUFSIZ
index|]
decl_stmt|;
return|return
operator|(
operator|(
name|ch
operator|=
name|ch_h2chan
argument_list|(
name|name
argument_list|,
name|buffer
argument_list|)
operator|)
operator|==
operator|(
name|Chan
operator|*
operator|)
operator|(
operator|-
literal|1
operator|)
condition|?
name|NULL
else|:
name|ch
operator|==
operator|(
name|Chan
operator|*
operator|)
name|NULL
condition|?
name|LocalName
argument_list|()
else|:
name|buffer
operator|)
return|;
block|}
end_function

begin_else
else|#
directive|else
else|MMDFII
end_else

begin_decl_stmt
specifier|extern
name|char
modifier|*
name|invo_name
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|short
name|ch_yloc
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* ok to intercept local names        */
end_comment

begin_decl_stmt
specifier|static
name|int
name|inited
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_function
name|char
modifier|*
name|OfficialName
parameter_list|(
name|name
parameter_list|)
specifier|register
name|char
modifier|*
name|name
decl_stmt|;
block|{
name|Dmn_route
name|route
decl_stmt|;
specifier|static
name|char
name|buffer
index|[
name|BUFSIZ
index|]
decl_stmt|;
if|if
condition|(
operator|!
name|inited
condition|)
block|{
name|mmdf_init
argument_list|(
name|invo_name
argument_list|)
expr_stmt|;
name|inited
operator|=
literal|1
expr_stmt|;
block|}
switch|switch
condition|(
name|dm_v2route
argument_list|(
name|name
argument_list|,
name|buffer
argument_list|,
operator|&
name|route
argument_list|)
condition|)
block|{
case|case
name|NOTOK
case|:
case|case
name|OK
case|:
return|return
operator|(
operator|(
name|ch_yloc
operator|&&
name|lexequ
argument_list|(
name|name
argument_list|,
name|LocalName
argument_list|()
argument_list|)
operator|)
condition|?
name|LocalName
argument_list|()
else|:
name|NULL
operator|)
return|;
default|default:
return|return
name|buffer
return|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
endif|MMDFII
end_endif

end_unit

