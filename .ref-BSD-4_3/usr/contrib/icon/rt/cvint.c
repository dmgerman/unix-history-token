begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_comment
comment|/*  * cvint - convert the value represented by d into an integer and write  *  the value into the location referenced by i.  cvint returns one of  *  T_INTEGER, T_LONGINT, and NULL depending on the outcome of the conversion.  */
end_comment

begin_expr_stmt
name|cvint
argument_list|(
name|d
argument_list|,
name|i
argument_list|)
specifier|register
expr|struct
name|descrip
operator|*
name|d
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|long
modifier|*
name|i
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|DclSave
name|union
name|numeric
name|result
decl_stmt|;
comment|/*     * Use cvnum to attempt the conversion into "result".     */
switch|switch
condition|(
name|cvnum
argument_list|(
name|d
argument_list|,
operator|&
name|result
argument_list|)
condition|)
block|{
case|case
name|T_LONGINT
case|:
comment|/*           * The value converted into an integer.  Assign the value to *i.           *  On systems with longs, distinguish between integer and long           *  integer results via the return value.           */
operator|*
name|i
operator|=
name|result
operator|.
name|integer
expr_stmt|;
ifdef|#
directive|ifdef
name|LONGS
if|if
condition|(
operator|*
name|i
operator|<
operator|(
name|long
operator|)
operator|(
name|int
operator|)
name|MINSHORT
operator|||
operator|*
name|i
operator|>
operator|(
name|long
operator|)
operator|(
name|int
operator|)
name|MAXSHORT
condition|)
return|return
operator|(
name|T_LONGINT
operator|)
return|;
endif|#
directive|endif
endif|LONGS
return|return
operator|(
name|T_INTEGER
operator|)
return|;
case|case
name|T_REAL
case|:
comment|/*           * The value converted into a real number.  If it's not in the           *  range of an integer, return a 0, otherwise convert the           *  real value into an integer.  As before, distinguish between           *  integers and long integers if necessary.           */
if|if
condition|(
name|result
operator|.
name|real
operator|>
name|MAXLONG
operator|||
name|result
operator|.
name|real
operator|<
name|MINLONG
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
operator|*
name|i
operator|=
operator|(
name|long
operator|)
name|result
operator|.
name|real
expr_stmt|;
ifdef|#
directive|ifdef
name|LONGS
if|if
condition|(
operator|*
name|i
operator|<
name|MINSHORT
operator|||
operator|*
name|i
operator|>
name|MAXSHORT
condition|)
return|return
operator|(
name|T_LONGINT
operator|)
return|;
endif|#
directive|endif
endif|LONGS
return|return
operator|(
name|T_INTEGER
operator|)
return|;
default|default:
return|return
operator|(
name|NULL
operator|)
return|;
block|}
block|}
end_block

end_unit

