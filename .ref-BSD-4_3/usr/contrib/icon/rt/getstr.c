begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_comment
comment|/*  * getstr - read a line into buf from file fd.  At most max characters  *  are read.  getstr returns the length of the line, not counting  *  the newline.  Note that if a file doesn't end in a newline, the  *  characters between the last newline and the end of the file  *  are never seen.  */
end_comment

begin_expr_stmt
name|getstr
argument_list|(
name|buf
argument_list|,
name|max
argument_list|,
name|fd
argument_list|)
specifier|register
name|char
operator|*
name|buf
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|max
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|FILE
modifier|*
name|fd
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|c
decl_stmt|,
name|l
decl_stmt|;
name|l
operator|=
literal|0
expr_stmt|;
while|while
condition|(
operator|(
name|c
operator|=
name|getc
argument_list|(
name|fd
argument_list|)
operator|)
operator|!=
literal|'\n'
condition|)
block|{
if|if
condition|(
name|c
operator|==
name|EOF
condition|)
return|return
operator|-
literal|1
return|;
operator|*
name|buf
operator|++
operator|=
name|c
expr_stmt|;
if|if
condition|(
operator|++
name|l
operator|>=
name|max
condition|)
break|break;
block|}
return|return
name|l
return|;
block|}
end_block

end_unit

