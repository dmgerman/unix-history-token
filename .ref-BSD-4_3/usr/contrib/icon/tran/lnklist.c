begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Routines for handling file linking.  */
end_comment

begin_include
include|#
directive|include
file|"itran.h"
end_include

begin_include
include|#
directive|include
file|"lfile.h"
end_include

begin_decl_stmt
name|struct
name|lfile
modifier|*
name|lfiles
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * alclfile allocates an lfile structure for the named file, fills  *  in the name and returns a pointer to it.  */
end_comment

begin_function
name|struct
name|lfile
modifier|*
name|alclfile
parameter_list|(
name|name
parameter_list|)
name|char
modifier|*
name|name
decl_stmt|;
block|{
name|struct
name|lfile
modifier|*
name|p
decl_stmt|;
name|char
modifier|*
name|np
decl_stmt|;
name|int
name|l
decl_stmt|;
specifier|extern
name|char
modifier|*
name|allocate
parameter_list|()
function_decl|;
name|p
operator|=
operator|(
expr|struct
name|lfile
operator|*
operator|)
name|allocate
argument_list|(
literal|1
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|lfile
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|p
condition|)
name|syserr
argument_list|(
literal|"not enough memory for file list"
argument_list|)
expr_stmt|;
name|p
operator|->
name|lf_link
operator|=
name|NULL
expr_stmt|;
name|l
operator|=
name|strlen
argument_list|(
name|name
argument_list|)
expr_stmt|;
name|np
operator|=
name|allocate
argument_list|(
literal|1
argument_list|,
operator|(
name|l
operator|+
literal|1
operator|+
sizeof|sizeof
argument_list|(
name|int
operator|*
argument_list|)
operator|)
operator|&
operator|~
operator|(
sizeof|sizeof
argument_list|(
name|int
operator|*
argument_list|)
operator|-
literal|1
operator|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|np
condition|)
name|syserr
argument_list|(
literal|"not enough memory for file list"
argument_list|)
expr_stmt|;
name|strncpy
argument_list|(
name|np
argument_list|,
name|name
argument_list|,
name|l
argument_list|)
expr_stmt|;
name|p
operator|->
name|lf_name
operator|=
name|np
expr_stmt|;
return|return
name|p
return|;
block|}
end_function

begin_comment
comment|/*  * dumplfiles - print the list of files to link.  Debugging only.  */
end_comment

begin_macro
name|dumplfiles
argument_list|()
end_macro

begin_block
block|{
name|struct
name|lfile
modifier|*
name|p
decl_stmt|,
modifier|*
name|lfls
decl_stmt|;
name|printf
argument_list|(
literal|"lfiles:\n"
argument_list|)
expr_stmt|;
name|lfls
operator|=
name|lfiles
expr_stmt|;
while|while
condition|(
name|p
operator|=
name|getlfile
argument_list|(
operator|&
name|lfls
argument_list|)
condition|)
name|printf
argument_list|(
literal|"'%s'\n"
argument_list|,
name|p
operator|->
name|lf_name
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * addlfile creates an lfile structure for the named file and add it to the  *  end of the list of files (lfiles) to generate link instructions for.  */
end_comment

begin_macro
name|addlfile
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|name
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|lfile
modifier|*
name|nlf
decl_stmt|,
modifier|*
name|p
decl_stmt|;
name|nlf
operator|=
name|alclfile
argument_list|(
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|lfiles
operator|==
name|NULL
condition|)
block|{
name|lfiles
operator|=
name|nlf
expr_stmt|;
block|}
else|else
block|{
name|p
operator|=
name|lfiles
expr_stmt|;
while|while
condition|(
name|p
operator|->
name|lf_link
operator|!=
name|NULL
condition|)
block|{
name|p
operator|=
name|p
operator|->
name|lf_link
expr_stmt|;
block|}
name|p
operator|->
name|lf_link
operator|=
name|nlf
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * getlfile returns a pointer (p) to the lfile structure pointed at by lptr  *  and moves lptr to the lfile structure that p points at.  In other words,  *  getlfile returns a pointer to the current (wrt. lptr) lfile and advances  *  lptr.  */
end_comment

begin_function
name|struct
name|lfile
modifier|*
name|getlfile
parameter_list|(
name|lptr
parameter_list|)
name|struct
name|lfile
modifier|*
modifier|*
name|lptr
decl_stmt|;
block|{
name|struct
name|lfile
modifier|*
name|p
decl_stmt|;
if|if
condition|(
operator|*
name|lptr
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
else|else
block|{
name|p
operator|=
operator|*
name|lptr
expr_stmt|;
operator|*
name|lptr
operator|=
name|p
operator|->
name|lf_link
expr_stmt|;
return|return
name|p
return|;
block|}
block|}
end_function

end_unit

