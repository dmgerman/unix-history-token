begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)ufs_bio.c	7.3 (Berkeley) 11/12/87  */
end_comment

begin_include
include|#
directive|include
file|"../machine/pte.h"
end_include

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"dir.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"buf.h"
end_include

begin_include
include|#
directive|include
file|"conf.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"seg.h"
end_include

begin_include
include|#
directive|include
file|"vm.h"
end_include

begin_include
include|#
directive|include
file|"trace.h"
end_include

begin_comment
comment|/*  * Read in (if necessary) the block and return a buffer pointer.  */
end_comment

begin_function
name|struct
name|buf
modifier|*
name|bread
parameter_list|(
name|dev
parameter_list|,
name|blkno
parameter_list|,
name|size
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|daddr_t
name|blkno
decl_stmt|;
name|int
name|size
decl_stmt|;
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
if|if
condition|(
name|size
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"bread: size 0"
argument_list|)
expr_stmt|;
name|bp
operator|=
name|getblk
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
condition|)
block|{
name|trace
argument_list|(
name|TR_BREADHIT
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|size
argument_list|)
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
name|bp
operator|->
name|b_flags
operator||=
name|B_READ
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|>
name|bp
operator|->
name|b_bufsize
condition|)
name|panic
argument_list|(
literal|"bread"
argument_list|)
expr_stmt|;
operator|(
operator|*
name|bdevsw
index|[
name|major
argument_list|(
name|dev
argument_list|)
index|]
operator|.
name|d_strategy
operator|)
operator|(
name|bp
operator|)
expr_stmt|;
name|trace
argument_list|(
name|TR_BREADMISS
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|size
argument_list|)
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_ru
operator|.
name|ru_inblock
operator|++
expr_stmt|;
comment|/* pay for read */
name|biowait
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Read in the block, like bread, but also start I/O on the  * read-ahead block (which is not allocated to the caller)  */
end_comment

begin_function
name|struct
name|buf
modifier|*
name|breada
parameter_list|(
name|dev
parameter_list|,
name|blkno
parameter_list|,
name|size
parameter_list|,
name|rablkno
parameter_list|,
name|rabsize
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|daddr_t
name|blkno
decl_stmt|;
name|int
name|size
decl_stmt|;
name|daddr_t
name|rablkno
decl_stmt|;
name|int
name|rabsize
decl_stmt|;
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|,
modifier|*
name|rabp
decl_stmt|;
name|bp
operator|=
name|NULL
expr_stmt|;
comment|/* 	 * If the block isn't in core, then allocate 	 * a buffer and initiate i/o (getblk checks 	 * for a cache hit). 	 */
if|if
condition|(
operator|!
name|incore
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|)
condition|)
block|{
name|bp
operator|=
name|getblk
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
operator|)
operator|==
literal|0
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_READ
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|>
name|bp
operator|->
name|b_bufsize
condition|)
name|panic
argument_list|(
literal|"breada"
argument_list|)
expr_stmt|;
operator|(
operator|*
name|bdevsw
index|[
name|major
argument_list|(
name|dev
argument_list|)
index|]
operator|.
name|d_strategy
operator|)
operator|(
name|bp
operator|)
expr_stmt|;
name|trace
argument_list|(
name|TR_BREADMISS
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|size
argument_list|)
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_ru
operator|.
name|ru_inblock
operator|++
expr_stmt|;
comment|/* pay for read */
block|}
else|else
name|trace
argument_list|(
name|TR_BREADHIT
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|size
argument_list|)
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * If there's a read-ahead block, start i/o 	 * on it also (as above). 	 */
if|if
condition|(
name|rablkno
operator|&&
operator|!
name|incore
argument_list|(
name|dev
argument_list|,
name|rablkno
argument_list|)
condition|)
block|{
name|rabp
operator|=
name|getblk
argument_list|(
name|dev
argument_list|,
name|rablkno
argument_list|,
name|rabsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|rabp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
condition|)
block|{
name|brelse
argument_list|(
name|rabp
argument_list|)
expr_stmt|;
name|trace
argument_list|(
name|TR_BREADHITRA
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|rabsize
argument_list|)
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|rabp
operator|->
name|b_flags
operator||=
name|B_READ
operator||
name|B_ASYNC
expr_stmt|;
if|if
condition|(
name|rabp
operator|->
name|b_bcount
operator|>
name|rabp
operator|->
name|b_bufsize
condition|)
name|panic
argument_list|(
literal|"breadrabp"
argument_list|)
expr_stmt|;
operator|(
operator|*
name|bdevsw
index|[
name|major
argument_list|(
name|dev
argument_list|)
index|]
operator|.
name|d_strategy
operator|)
operator|(
name|rabp
operator|)
expr_stmt|;
name|trace
argument_list|(
name|TR_BREADMISSRA
argument_list|,
name|pack
argument_list|(
name|dev
argument_list|,
name|rabsize
argument_list|)
argument_list|,
name|rablock
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_ru
operator|.
name|ru_inblock
operator|++
expr_stmt|;
comment|/* pay in advance */
block|}
block|}
comment|/* 	 * If block was in core, let bread get it. 	 * If block wasn't in core, then the read was started 	 * above, and just wait for it. 	 */
if|if
condition|(
name|bp
operator|==
name|NULL
condition|)
return|return
operator|(
name|bread
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|,
name|size
argument_list|)
operator|)
return|;
name|biowait
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Write the buffer, waiting for completion.  * Then release the buffer.  */
end_comment

begin_expr_stmt
name|bwrite
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|flag
expr_stmt|;
name|flag
operator|=
name|bp
operator|->
name|b_flags
expr_stmt|;
name|bp
operator|->
name|b_flags
operator|&=
operator|~
operator|(
name|B_READ
operator||
name|B_DONE
operator||
name|B_ERROR
operator||
name|B_DELWRI
operator|)
expr_stmt|;
if|if
condition|(
operator|(
name|flag
operator|&
name|B_DELWRI
operator|)
operator|==
literal|0
condition|)
name|u
operator|.
name|u_ru
operator|.
name|ru_oublock
operator|++
expr_stmt|;
comment|/* noone paid yet */
name|trace
argument_list|(
name|TR_BWRITE
argument_list|,
name|pack
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|,
name|bp
operator|->
name|b_bcount
argument_list|)
argument_list|,
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|>
name|bp
operator|->
name|b_bufsize
condition|)
name|panic
argument_list|(
literal|"bwrite"
argument_list|)
expr_stmt|;
operator|(
operator|*
name|bdevsw
index|[
name|major
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|)
index|]
operator|.
name|d_strategy
operator|)
operator|(
name|bp
operator|)
expr_stmt|;
comment|/* 	 * If the write was synchronous, then await i/o completion. 	 * If the write was "delayed", then we put the buffer on 	 * the q of blocks awaiting i/o completion status. 	 */
if|if
condition|(
operator|(
name|flag
operator|&
name|B_ASYNC
operator|)
operator|==
literal|0
condition|)
block|{
name|biowait
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|flag
operator|&
name|B_DELWRI
condition|)
name|bp
operator|->
name|b_flags
operator||=
name|B_AGE
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Release the buffer, marking it so that if it is grabbed  * for another purpose it will be written out before being  * given up (e.g. when writing a partial block where it is  * assumed that another write for the same block will soon follow).  * This can't be done for magtape, since writes must be done  * in the same order as requested.  */
end_comment

begin_expr_stmt
name|bdwrite
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DELWRI
operator|)
operator|==
literal|0
condition|)
name|u
operator|.
name|u_ru
operator|.
name|ru_oublock
operator|++
expr_stmt|;
comment|/* noone paid yet */
if|if
condition|(
name|bdevsw
index|[
name|major
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|)
index|]
operator|.
name|d_flags
operator|&
name|B_TAPE
condition|)
name|bawrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
else|else
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_DELWRI
operator||
name|B_DONE
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Release the buffer, start I/O on it, but don't wait for completion.  */
end_comment

begin_expr_stmt
name|bawrite
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_ASYNC
expr_stmt|;
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Release the buffer, with no I/O implied.  */
end_comment

begin_expr_stmt
name|brelse
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|flist
decl_stmt|;
specifier|register
name|s
expr_stmt|;
name|trace
argument_list|(
name|TR_BRELSE
argument_list|,
name|pack
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|,
name|bp
operator|->
name|b_bufsize
argument_list|)
argument_list|,
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
comment|/* 	 * If someone's waiting for the buffer, or 	 * is waiting for a buffer wake 'em up. 	 */
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_WANTED
condition|)
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bfreelist
index|[
literal|0
index|]
operator|.
name|b_flags
operator|&
name|B_WANTED
condition|)
block|{
name|bfreelist
index|[
literal|0
index|]
operator|.
name|b_flags
operator|&=
operator|~
name|B_WANTED
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|bfreelist
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_ERROR
condition|)
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_LOCKED
condition|)
name|bp
operator|->
name|b_flags
operator|&=
operator|~
name|B_ERROR
expr_stmt|;
comment|/* try again later */
else|else
name|bp
operator|->
name|b_dev
operator|=
name|NODEV
expr_stmt|;
comment|/* no assoc */
comment|/* 	 * Stick the buffer back on a free list. 	 */
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_bufsize
operator|<=
literal|0
condition|)
block|{
comment|/* block has no buffer ... put at front of unused buffer list */
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_EMPTY
index|]
expr_stmt|;
name|binsheadfree
argument_list|(
name|bp
argument_list|,
name|flist
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_ERROR
operator||
name|B_INVAL
operator|)
condition|)
block|{
comment|/* block has no info ... put at front of most free list */
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_AGE
index|]
expr_stmt|;
name|binsheadfree
argument_list|(
name|bp
argument_list|,
name|flist
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_LOCKED
condition|)
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_LOCKED
index|]
expr_stmt|;
elseif|else
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_AGE
condition|)
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_AGE
index|]
expr_stmt|;
else|else
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_LRU
index|]
expr_stmt|;
name|binstailfree
argument_list|(
name|bp
argument_list|,
name|flist
argument_list|)
expr_stmt|;
block|}
name|bp
operator|->
name|b_flags
operator|&=
operator|~
operator|(
name|B_WANTED
operator||
name|B_BUSY
operator||
name|B_ASYNC
operator||
name|B_AGE
operator|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * See if the block is associated with some buffer  * (mainly to avoid getting hung up on a wait in breada)  */
end_comment

begin_macro
name|incore
argument_list|(
argument|dev
argument_list|,
argument|blkno
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|daddr_t
name|blkno
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
specifier|register
name|struct
name|buf
modifier|*
name|dp
decl_stmt|;
name|dp
operator|=
name|BUFHASH
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
for|for
control|(
name|bp
operator|=
name|dp
operator|->
name|b_forw
init|;
name|bp
operator|!=
name|dp
condition|;
name|bp
operator|=
name|bp
operator|->
name|b_forw
control|)
if|if
condition|(
name|bp
operator|->
name|b_blkno
operator|==
name|blkno
operator|&&
name|bp
operator|->
name|b_dev
operator|==
name|dev
operator|&&
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_INVAL
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_function
name|struct
name|buf
modifier|*
name|baddr
parameter_list|(
name|dev
parameter_list|,
name|blkno
parameter_list|,
name|size
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|daddr_t
name|blkno
decl_stmt|;
name|int
name|size
decl_stmt|;
block|{
if|if
condition|(
name|incore
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|)
condition|)
return|return
operator|(
name|bread
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|,
name|size
argument_list|)
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Assign a buffer for the given block.  If the appropriate  * block is already associated, return it; otherwise search  * for the oldest non-busy buffer and reassign it.  *  * If we find the buffer, but it is dirty (marked DELWRI) and  * its size is changing, we must write it out first. When the  * buffer is shrinking, the write is done by brealloc to avoid  * losing the unwritten data. When the buffer is growing, the  * write is done by getblk, so that bread will not read stale  * disk data over the modified data in the buffer.  *  * We use splx here because this routine may be called  * on the interrupt stack during a dump, and we don't  * want to lower the ipl back to 0.  */
end_comment

begin_function
name|struct
name|buf
modifier|*
name|getblk
parameter_list|(
name|dev
parameter_list|,
name|blkno
parameter_list|,
name|size
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|daddr_t
name|blkno
decl_stmt|;
name|int
name|size
decl_stmt|;
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|,
modifier|*
name|dp
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|size
operator|>
name|MAXBSIZE
condition|)
name|panic
argument_list|(
literal|"getblk: size too big"
argument_list|)
expr_stmt|;
comment|/* 	 * To prevent overflow of 32-bit ints when converting block 	 * numbers to byte offsets, blknos> 2^32 / DEV_BSIZE are set 	 * to the maximum number that can be converted to a byte offset 	 * without overflow. This is historic code; what bug it fixed, 	 * or whether it is still a reasonable thing to do is open to 	 * dispute. mkm 9/85 	 */
if|if
condition|(
operator|(
name|unsigned
operator|)
name|blkno
operator|>=
literal|1
operator|<<
operator|(
sizeof|sizeof
argument_list|(
name|int
argument_list|)
operator|*
name|NBBY
operator|-
name|DEV_BSHIFT
operator|)
condition|)
name|blkno
operator|=
literal|1
operator|<<
operator|(
operator|(
sizeof|sizeof
argument_list|(
name|int
argument_list|)
operator|*
name|NBBY
operator|-
name|DEV_BSHIFT
operator|)
operator|+
literal|1
operator|)
expr_stmt|;
comment|/* 	 * Search the cache for the block.  If we hit, but 	 * the buffer is in use for i/o, then we wait until 	 * the i/o has completed. 	 */
name|dp
operator|=
name|BUFHASH
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
name|loop
label|:
for|for
control|(
name|bp
operator|=
name|dp
operator|->
name|b_forw
init|;
name|bp
operator|!=
name|dp
condition|;
name|bp
operator|=
name|bp
operator|->
name|b_forw
control|)
block|{
if|if
condition|(
name|bp
operator|->
name|b_blkno
operator|!=
name|blkno
operator|||
name|bp
operator|->
name|b_dev
operator|!=
name|dev
operator|||
name|bp
operator|->
name|b_flags
operator|&
name|B_INVAL
condition|)
continue|continue;
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_BUSY
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_WANTED
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|bp
argument_list|,
name|PRIBIO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|notavail
argument_list|(
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|!=
name|size
condition|)
block|{
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|<
name|size
operator|&&
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DELWRI
operator|)
condition|)
block|{
name|bp
operator|->
name|b_flags
operator|&=
operator|~
name|B_ASYNC
expr_stmt|;
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
if|if
condition|(
name|brealloc
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|loop
goto|;
block|}
if|if
condition|(
name|bp
operator|->
name|b_bcount
operator|!=
name|size
operator|&&
name|brealloc
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|loop
goto|;
name|bp
operator|->
name|b_flags
operator||=
name|B_CACHE
expr_stmt|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
if|if
condition|(
name|major
argument_list|(
name|dev
argument_list|)
operator|>=
name|nblkdev
condition|)
name|panic
argument_list|(
literal|"blkdev"
argument_list|)
expr_stmt|;
name|bp
operator|=
name|getnewbuf
argument_list|()
expr_stmt|;
name|bfree
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bremhash
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|binshash
argument_list|(
name|bp
argument_list|,
name|dp
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_dev
operator|=
name|dev
expr_stmt|;
name|bp
operator|->
name|b_blkno
operator|=
name|blkno
expr_stmt|;
name|bp
operator|->
name|b_error
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|brealloc
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|loop
goto|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * get an empty block,  * not assigned to any particular device  */
end_comment

begin_function
name|struct
name|buf
modifier|*
name|geteblk
parameter_list|(
name|size
parameter_list|)
name|int
name|size
decl_stmt|;
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|,
modifier|*
name|flist
decl_stmt|;
if|if
condition|(
name|size
operator|>
name|MAXBSIZE
condition|)
name|panic
argument_list|(
literal|"geteblk: size too big"
argument_list|)
expr_stmt|;
name|loop
label|:
name|bp
operator|=
name|getnewbuf
argument_list|()
expr_stmt|;
name|bp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
name|bfree
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bremhash
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|flist
operator|=
operator|&
name|bfreelist
index|[
name|BQ_AGE
index|]
expr_stmt|;
name|binshash
argument_list|(
name|bp
argument_list|,
name|flist
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_dev
operator|=
operator|(
name|dev_t
operator|)
name|NODEV
expr_stmt|;
name|bp
operator|->
name|b_error
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|brealloc
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|loop
goto|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Allocate space associated with a buffer.  * If can't get space, buffer is released  */
end_comment

begin_expr_stmt
name|brealloc
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|size
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|daddr_t
name|start
decl_stmt|,
name|last
decl_stmt|;
specifier|register
name|struct
name|buf
modifier|*
name|ep
decl_stmt|;
name|struct
name|buf
modifier|*
name|dp
decl_stmt|;
name|int
name|s
decl_stmt|;
comment|/* 	 * First need to make sure that all overlapping previous I/O 	 * is dispatched with. 	 */
if|if
condition|(
name|size
operator|==
name|bp
operator|->
name|b_bcount
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|size
operator|<
name|bp
operator|->
name|b_bcount
condition|)
block|{
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DELWRI
condition|)
block|{
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_LOCKED
condition|)
name|panic
argument_list|(
literal|"brealloc"
argument_list|)
expr_stmt|;
return|return
operator|(
name|allocbuf
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|)
return|;
block|}
name|bp
operator|->
name|b_flags
operator|&=
operator|~
name|B_DONE
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_dev
operator|==
name|NODEV
condition|)
return|return
operator|(
name|allocbuf
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|)
return|;
name|trace
argument_list|(
name|TR_BREALLOC
argument_list|,
name|pack
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|,
name|size
argument_list|)
argument_list|,
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
comment|/* 	 * Search cache for any buffers that overlap the one that we 	 * are trying to allocate. Overlapping buffers must be marked 	 * invalid, after being written out if they are dirty. (indicated 	 * by B_DELWRI) A disk block must be mapped by at most one buffer 	 * at any point in time. Care must be taken to avoid deadlocking 	 * when two buffer are trying to get the same set of disk blocks. 	 */
name|start
operator|=
name|bp
operator|->
name|b_blkno
expr_stmt|;
name|last
operator|=
name|start
operator|+
name|btodb
argument_list|(
name|size
argument_list|)
operator|-
literal|1
expr_stmt|;
name|dp
operator|=
name|BUFHASH
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|,
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
name|loop
label|:
for|for
control|(
name|ep
operator|=
name|dp
operator|->
name|b_forw
init|;
name|ep
operator|!=
name|dp
condition|;
name|ep
operator|=
name|ep
operator|->
name|b_forw
control|)
block|{
if|if
condition|(
name|ep
operator|==
name|bp
operator|||
name|ep
operator|->
name|b_dev
operator|!=
name|bp
operator|->
name|b_dev
operator|||
operator|(
name|ep
operator|->
name|b_flags
operator|&
name|B_INVAL
operator|)
condition|)
continue|continue;
comment|/* look for overlap */
if|if
condition|(
name|ep
operator|->
name|b_bcount
operator|==
literal|0
operator|||
name|ep
operator|->
name|b_blkno
operator|>
name|last
operator|||
name|ep
operator|->
name|b_blkno
operator|+
name|btodb
argument_list|(
name|ep
operator|->
name|b_bcount
argument_list|)
operator|<=
name|start
condition|)
continue|continue;
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
if|if
condition|(
name|ep
operator|->
name|b_flags
operator|&
name|B_BUSY
condition|)
block|{
name|ep
operator|->
name|b_flags
operator||=
name|B_WANTED
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|ep
argument_list|,
name|PRIBIO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|notavail
argument_list|(
name|ep
argument_list|)
expr_stmt|;
if|if
condition|(
name|ep
operator|->
name|b_flags
operator|&
name|B_DELWRI
condition|)
block|{
name|bwrite
argument_list|(
name|ep
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|ep
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
name|brelse
argument_list|(
name|ep
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|allocbuf
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Find a buffer which is available for use.  * Select something from a free list.  * Preference is to AGE list, then LRU list.  */
end_comment

begin_function
name|struct
name|buf
modifier|*
name|getnewbuf
parameter_list|()
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|,
modifier|*
name|dp
decl_stmt|;
name|int
name|s
decl_stmt|;
name|loop
label|:
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
for|for
control|(
name|dp
operator|=
operator|&
name|bfreelist
index|[
name|BQ_AGE
index|]
init|;
name|dp
operator|>
name|bfreelist
condition|;
name|dp
operator|--
control|)
if|if
condition|(
name|dp
operator|->
name|av_forw
operator|!=
name|dp
condition|)
break|break;
if|if
condition|(
name|dp
operator|==
name|bfreelist
condition|)
block|{
comment|/* no free blocks */
name|dp
operator|->
name|b_flags
operator||=
name|B_WANTED
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|dp
argument_list|,
name|PRIBIO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|bp
operator|=
name|dp
operator|->
name|av_forw
expr_stmt|;
name|notavail
argument_list|(
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DELWRI
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_ASYNC
expr_stmt|;
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|trace
argument_list|(
name|TR_BRELSE
argument_list|,
name|pack
argument_list|(
name|bp
operator|->
name|b_dev
argument_list|,
name|bp
operator|->
name|b_bufsize
argument_list|)
argument_list|,
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_flags
operator|=
name|B_BUSY
expr_stmt|;
return|return
operator|(
name|bp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Wait for I/O completion on the buffer; return errors  * to the user.  */
end_comment

begin_expr_stmt
name|biowait
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|int
name|s
decl_stmt|;
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
while|while
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DONE
operator|)
operator|==
literal|0
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|bp
argument_list|,
name|PRIBIO
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
operator|==
literal|0
condition|)
comment|/* XXX */
name|u
operator|.
name|u_error
operator|=
name|geterror
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Mark I/O complete on a buffer.  * If someone should be called, e.g. the pageout  * daemon, do so.  Otherwise, wake up anyone  * waiting for it.  */
end_comment

begin_expr_stmt
name|biodone
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DONE
condition|)
name|panic
argument_list|(
literal|"dup biodone"
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_flags
operator||=
name|B_DONE
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_CALL
condition|)
block|{
name|bp
operator|->
name|b_flags
operator|&=
operator|~
name|B_CALL
expr_stmt|;
call|(
modifier|*
name|bp
operator|->
name|b_iodone
call|)
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_ASYNC
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
else|else
block|{
name|bp
operator|->
name|b_flags
operator|&=
operator|~
name|B_WANTED
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|bp
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Insure that no part of a specified block is in an incore buffer. #ifdef SECSIZE  * "size" is given in device blocks (the units of b_blkno). #endif SECSIZE  */
end_comment

begin_macro
name|blkflush
argument_list|(
argument|dev
argument_list|,
argument|blkno
argument_list|,
argument|size
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|daddr_t
name|blkno
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|long
name|size
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|ep
decl_stmt|;
name|struct
name|buf
modifier|*
name|dp
decl_stmt|;
name|daddr_t
name|start
decl_stmt|,
name|last
decl_stmt|;
name|int
name|s
decl_stmt|;
name|start
operator|=
name|blkno
expr_stmt|;
name|last
operator|=
name|start
operator|+
name|btodb
argument_list|(
name|size
argument_list|)
operator|-
literal|1
expr_stmt|;
name|dp
operator|=
name|BUFHASH
argument_list|(
name|dev
argument_list|,
name|blkno
argument_list|)
expr_stmt|;
name|loop
label|:
for|for
control|(
name|ep
operator|=
name|dp
operator|->
name|b_forw
init|;
name|ep
operator|!=
name|dp
condition|;
name|ep
operator|=
name|ep
operator|->
name|b_forw
control|)
block|{
if|if
condition|(
name|ep
operator|->
name|b_dev
operator|!=
name|dev
operator|||
operator|(
name|ep
operator|->
name|b_flags
operator|&
name|B_INVAL
operator|)
condition|)
continue|continue;
comment|/* look for overlap */
if|if
condition|(
name|ep
operator|->
name|b_bcount
operator|==
literal|0
operator|||
name|ep
operator|->
name|b_blkno
operator|>
name|last
operator|||
name|ep
operator|->
name|b_blkno
operator|+
name|btodb
argument_list|(
name|ep
operator|->
name|b_bcount
argument_list|)
operator|<=
name|start
condition|)
continue|continue;
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
if|if
condition|(
name|ep
operator|->
name|b_flags
operator|&
name|B_BUSY
condition|)
block|{
name|ep
operator|->
name|b_flags
operator||=
name|B_WANTED
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|ep
argument_list|,
name|PRIBIO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
if|if
condition|(
name|ep
operator|->
name|b_flags
operator|&
name|B_DELWRI
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|notavail
argument_list|(
name|ep
argument_list|)
expr_stmt|;
name|bwrite
argument_list|(
name|ep
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Make sure all write-behind blocks  * on dev (or NODEV for all)  * are flushed out.  * (from umount and update)  */
end_comment

begin_macro
name|bflush
argument_list|(
argument|dev
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
specifier|register
name|struct
name|buf
modifier|*
name|flist
decl_stmt|;
name|int
name|s
decl_stmt|;
name|loop
label|:
name|s
operator|=
name|splbio
argument_list|()
expr_stmt|;
for|for
control|(
name|flist
operator|=
name|bfreelist
init|;
name|flist
operator|<
operator|&
name|bfreelist
index|[
name|BQ_EMPTY
index|]
condition|;
name|flist
operator|++
control|)
for|for
control|(
name|bp
operator|=
name|flist
operator|->
name|av_forw
init|;
name|bp
operator|!=
name|flist
condition|;
name|bp
operator|=
name|bp
operator|->
name|av_forw
control|)
block|{
if|if
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_DELWRI
operator|)
operator|==
literal|0
condition|)
continue|continue;
if|if
condition|(
name|dev
operator|==
name|NODEV
operator|||
name|dev
operator|==
name|bp
operator|->
name|b_dev
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_ASYNC
expr_stmt|;
name|notavail
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Pick up the device's error number and pass it to the user;  * if there is an error but the number is 0 set a generalized code.  */
end_comment

begin_expr_stmt
name|geterror
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|int
name|error
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_ERROR
condition|)
if|if
condition|(
operator|(
name|error
operator|=
name|bp
operator|->
name|b_error
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
name|EIO
operator|)
return|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Invalidate in core blocks belonging to closed or umounted filesystem  *  * This is not nicely done at all - the buffer ought to be removed from the  * hash chains& have its dev/blkno fields clobbered, but unfortunately we  * can't do that here, as it is quite possible that the block is still  * being used for i/o. Eventually, all disc drivers should be forced to  * have a close routine, which ought ensure that the queue is empty, then  * properly flush the queues. Until that happy day, this suffices for  * correctness.						... kre  */
end_comment

begin_macro
name|binval
argument_list|(
argument|dev
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
specifier|register
name|struct
name|bufhd
modifier|*
name|hp
decl_stmt|;
define|#
directive|define
name|dp
value|((struct buf *)hp)
for|for
control|(
name|hp
operator|=
name|bufhash
init|;
name|hp
operator|<
operator|&
name|bufhash
index|[
name|BUFHSZ
index|]
condition|;
name|hp
operator|++
control|)
for|for
control|(
name|bp
operator|=
name|dp
operator|->
name|b_forw
init|;
name|bp
operator|!=
name|dp
condition|;
name|bp
operator|=
name|bp
operator|->
name|b_forw
control|)
if|if
condition|(
name|bp
operator|->
name|b_dev
operator|==
name|dev
condition|)
name|bp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
block|}
end_block

end_unit

