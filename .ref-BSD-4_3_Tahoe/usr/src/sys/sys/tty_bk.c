begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)tty_bk.c	7.2 (Berkeley) 7/27/87  */
end_comment

begin_include
include|#
directive|include
file|"bk.h"
end_include

begin_if
if|#
directive|if
name|NBK
operator|>
literal|0
end_if

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"dir.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"ioctl.h"
end_include

begin_include
include|#
directive|include
file|"tty.h"
end_include

begin_include
include|#
directive|include
file|"file.h"
end_include

begin_include
include|#
directive|include
file|"buf.h"
end_include

begin_comment
comment|/*  * Line discipline for Berkeley network.  *  * This supplies single lines to a user level program  * with a minimum of fuss.  Lines are newline terminated.  *  * This discipline requires that tty device drivers call  * the line specific l_ioctl routine from their ioctl routines,  * assigning the result to cmd so that we can refuse most tty specific  * ioctls which are unsafe because we have ambushed the  * teletype input queues, overlaying them with other information.  */
end_comment

begin_comment
comment|/*  * Open as networked discipline.  Called when discipline changed  * with ioctl, this assigns a buffer to the line for input, and  * changing the interpretation of the information in the tty structure.  */
end_comment

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|bkopen
argument_list|(
argument|dev
argument_list|,
argument|tp
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|tty
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_line
operator|==
name|NETLDISC
condition|)
return|return
operator|(
name|EBUSY
operator|)
return|;
comment|/* sometimes the network opens /dev/tty */
name|bp
operator|=
name|geteblk
argument_list|(
literal|1024
argument_list|)
expr_stmt|;
name|ttyflush
argument_list|(
name|tp
argument_list|,
name|FREAD
operator||
name|FWRITE
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_bufp
operator|=
name|bp
expr_stmt|;
name|tp
operator|->
name|t_cp
operator|=
operator|(
name|char
operator|*
operator|)
name|bp
operator|->
name|b_un
operator|.
name|b_addr
expr_stmt|;
name|tp
operator|->
name|t_inbuf
operator|=
literal|0
expr_stmt|;
name|tp
operator|->
name|t_rec
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Break down... called when discipline changed or from device  * close routine.  */
end_comment

begin_expr_stmt
name|bkclose
argument_list|(
name|tp
argument_list|)
specifier|register
expr|struct
name|tty
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|s
decl_stmt|;
name|s
operator|=
name|spl5
argument_list|()
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|tp
operator|->
name|t_rawq
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_bufp
condition|)
block|{
name|brelse
argument_list|(
name|tp
operator|->
name|t_bufp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_bufp
operator|=
literal|0
expr_stmt|;
block|}
else|else
name|printf
argument_list|(
literal|"bkclose: no buf\n"
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_cp
operator|=
literal|0
expr_stmt|;
name|tp
operator|->
name|t_inbuf
operator|=
literal|0
expr_stmt|;
name|tp
operator|->
name|t_rec
operator|=
literal|0
expr_stmt|;
name|tp
operator|->
name|t_line
operator|=
literal|0
expr_stmt|;
comment|/* paranoid: avoid races */
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Read from a network line.  * Characters have been buffered in a system buffer and are  * now dumped back to the user in one fell swoop, and with a  * minimum of fuss.  Note that no input is accepted when a record  * is waiting.  Our clearing tp->t_rec here allows further input  * to accumulate.  */
end_comment

begin_expr_stmt
name|bkread
argument_list|(
name|tp
argument_list|,
name|uio
argument_list|)
specifier|register
expr|struct
name|tty
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|uio
modifier|*
name|uio
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|s
decl_stmt|;
name|int
name|error
decl_stmt|;
if|if
condition|(
operator|(
name|tp
operator|->
name|t_state
operator|&
name|TS_CARR_ON
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|s
operator|=
name|spl5
argument_list|()
expr_stmt|;
while|while
condition|(
name|tp
operator|->
name|t_rec
operator|==
literal|0
operator|&&
name|tp
operator|->
name|t_line
operator|==
name|NETLDISC
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|tp
operator|->
name|t_rawq
argument_list|,
name|TTIPRI
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_line
operator|!=
name|NETLDISC
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|error
operator|=
name|uiomove
argument_list|(
name|tp
operator|->
name|t_bufp
operator|->
name|b_un
operator|.
name|b_addr
argument_list|,
name|tp
operator|->
name|t_inbuf
argument_list|,
name|UIO_READ
argument_list|,
name|uio
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_cp
operator|=
operator|(
name|char
operator|*
operator|)
name|tp
operator|->
name|t_bufp
operator|->
name|b_un
operator|.
name|b_addr
expr_stmt|;
name|tp
operator|->
name|t_inbuf
operator|=
literal|0
expr_stmt|;
name|tp
operator|->
name|t_rec
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Low level character input routine.  * Stuff the character in the buffer, and wake up the top  * half after setting t_rec if this completes the record  * or if the buffer is (ick!) full.  *  * Thisis where the formatting should get done to allow  * 8 character data paths through escapes.  *  * This rutine should be expanded in-line in the receiver  * interrupt routine of the dh-11 to make it run as fast as possible.  */
end_comment

begin_expr_stmt
name|bkinput
argument_list|(
name|c
argument_list|,
name|tp
argument_list|)
specifier|register
name|c
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|tty
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|tp
operator|->
name|t_rec
condition|)
return|return;
operator|*
name|tp
operator|->
name|t_cp
operator|++
operator|=
name|c
expr_stmt|;
if|if
condition|(
operator|++
name|tp
operator|->
name|t_inbuf
operator|==
literal|1024
operator|||
name|c
operator|==
literal|'\n'
condition|)
block|{
name|tp
operator|->
name|t_rec
operator|=
literal|1
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|tp
operator|->
name|t_rawq
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * This routine is called whenever a ioctl is about to be performed  * and gets a chance to reject the ioctl.  We reject all teletype  * oriented ioctl's except those which set the discipline, and  * those which get parameters (gtty and get special characters).  */
end_comment

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|bkioctl
argument_list|(
argument|tp
argument_list|,
argument|cmd
argument_list|,
argument|data
argument_list|,
argument|flag
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tty
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|data
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
operator|(
name|cmd
operator|>>
literal|8
operator|)
operator|!=
literal|'t'
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
switch|switch
condition|(
name|cmd
condition|)
block|{
case|case
name|TIOCSETD
case|:
case|case
name|TIOCGETD
case|:
case|case
name|TIOCGETP
case|:
case|case
name|TIOCGETC
case|:
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
return|return
operator|(
name|ENOTTY
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

