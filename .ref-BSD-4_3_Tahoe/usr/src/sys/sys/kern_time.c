begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)kern_time.c	7.5 (Berkeley) 7/21/87  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"dir.h"
end_include

begin_comment
comment|/* XXX */
end_comment

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"kernel.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"../machine/reg.h"
end_include

begin_include
include|#
directive|include
file|"../machine/cpu.h"
end_include

begin_comment
comment|/*   * Time of day and interval timer support.  *  * These routines provide the kernel entry points to get and set  * the time-of-day and per-process interval timers.  Subroutines  * here provide support for adding and subtracting timeval structures  * and decrementing interval timers, optionally reloading the interval  * timers when they expire.  */
end_comment

begin_macro
name|gettimeofday
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|struct
name|timeval
modifier|*
name|tp
decl_stmt|;
name|struct
name|timezone
modifier|*
name|tzp
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|timeval
name|atv
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|tp
condition|)
block|{
name|microtime
argument_list|(
operator|&
name|atv
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|atv
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tp
argument_list|,
sizeof|sizeof
argument_list|(
name|atv
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|tzp
condition|)
name|u
operator|.
name|u_error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|tz
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tzp
argument_list|,
sizeof|sizeof
argument_list|(
name|tz
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|settimeofday
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|struct
name|timeval
modifier|*
name|tv
decl_stmt|;
name|struct
name|timezone
modifier|*
name|tzp
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|timeval
name|atv
decl_stmt|;
name|struct
name|timezone
name|atz
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|tv
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tv
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|atv
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|timeval
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
name|setthetime
argument_list|(
operator|&
name|atv
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|uap
operator|->
name|tzp
operator|&&
name|suser
argument_list|()
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tzp
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|atz
argument_list|,
sizeof|sizeof
argument_list|(
name|atz
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
operator|==
literal|0
condition|)
name|tz
operator|=
name|atz
expr_stmt|;
block|}
block|}
end_block

begin_macro
name|setthetime
argument_list|(
argument|tv
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|timeval
modifier|*
name|tv
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|s
decl_stmt|;
if|if
condition|(
operator|!
name|suser
argument_list|()
condition|)
return|return;
comment|/* WHAT DO WE DO ABOUT PENDING REAL-TIME TIMEOUTS??? */
name|boottime
operator|.
name|tv_sec
operator|+=
name|tv
operator|->
name|tv_sec
operator|-
name|time
operator|.
name|tv_sec
expr_stmt|;
name|s
operator|=
name|splhigh
argument_list|()
expr_stmt|;
name|time
operator|=
operator|*
name|tv
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|resettodr
argument_list|()
expr_stmt|;
block|}
end_block

begin_decl_stmt
specifier|extern
name|int
name|tickadj
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* "standard" clock skew, us./tick */
end_comment

begin_decl_stmt
name|int
name|tickdelta
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* current clock skew, us. per tick */
end_comment

begin_decl_stmt
name|long
name|timedelta
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* unapplied time correction, us. */
end_comment

begin_decl_stmt
name|long
name|bigadj
init|=
literal|1000000
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* use 10x skew above bigadj us. */
end_comment

begin_macro
name|adjtime
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|struct
name|timeval
modifier|*
name|delta
decl_stmt|;
name|struct
name|timeval
modifier|*
name|olddelta
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|timeval
name|atv
decl_stmt|,
name|oatv
decl_stmt|;
specifier|register
name|long
name|ndelta
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
operator|!
name|suser
argument_list|()
condition|)
return|return;
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|delta
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|atv
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|timeval
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
name|ndelta
operator|=
name|atv
operator|.
name|tv_sec
operator|*
literal|1000000
operator|+
name|atv
operator|.
name|tv_usec
expr_stmt|;
if|if
condition|(
name|timedelta
operator|==
literal|0
condition|)
if|if
condition|(
name|ndelta
operator|>
name|bigadj
condition|)
name|tickdelta
operator|=
literal|10
operator|*
name|tickadj
expr_stmt|;
else|else
name|tickdelta
operator|=
name|tickadj
expr_stmt|;
if|if
condition|(
name|ndelta
operator|%
name|tickdelta
condition|)
name|ndelta
operator|=
name|ndelta
operator|/
name|tickadj
operator|*
name|tickadj
expr_stmt|;
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|olddelta
condition|)
block|{
name|oatv
operator|.
name|tv_sec
operator|=
name|timedelta
operator|/
literal|1000000
expr_stmt|;
name|oatv
operator|.
name|tv_usec
operator|=
name|timedelta
operator|%
literal|1000000
expr_stmt|;
block|}
name|timedelta
operator|=
name|ndelta
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|olddelta
condition|)
operator|(
name|void
operator|)
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|oatv
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|olddelta
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|timeval
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Get value of an interval timer.  The process virtual and  * profiling virtual time timers are kept in the u. area, since  * they can be swapped out.  These are kept internally in the  * way they are specified externally: in time until they expire.  *  * The real time interval timer is kept in the process table slot  * for the process, and its value (it_value) is kept as an  * absolute time rather than as a delta, so that it is easy to keep  * periodic real-time signals from drifting.  *  * Virtual time timers are processed in the hardclock() routine of  * kern_clock.c.  The real time timer is processed by a timeout  * routine, called from the softclock() routine.  Since a callout  * may be delayed in real time due to interrupt processing in the system,  * it is possible for the real time timeout routine (realitexpire, given below),  * to be delayed in real time past when it is supposed to occur.  It  * does not suffice, therefore, to reload the real timer .it_value from the  * real time timers .it_interval.  Rather, we compute the next time in  * absolute time the timer should go off.  */
end_comment

begin_macro
name|getitimer
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|u_int
name|which
decl_stmt|;
name|struct
name|itimerval
modifier|*
name|itv
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|itimerval
name|aitv
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|which
operator|>
literal|2
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|which
operator|==
name|ITIMER_REAL
condition|)
block|{
comment|/* 		 * Convert from absoulte to relative time in .it_value 		 * part of real time timer.  If time for real time timer 		 * has passed return 0, else return difference between 		 * current time and time for the timer to go off. 		 */
name|aitv
operator|=
name|u
operator|.
name|u_procp
operator|->
name|p_realtimer
expr_stmt|;
if|if
condition|(
name|timerisset
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|)
condition|)
if|if
condition|(
name|timercmp
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|,
operator|&
name|time
argument_list|,
operator|<
argument_list|)
condition|)
name|timerclear
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|)
expr_stmt|;
else|else
name|timevalsub
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|,
operator|&
name|time
argument_list|)
expr_stmt|;
block|}
else|else
name|aitv
operator|=
name|u
operator|.
name|u_timer
index|[
name|uap
operator|->
name|which
index|]
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|aitv
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|itv
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|itimerval
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|setitimer
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|u_int
name|which
decl_stmt|;
name|struct
name|itimerval
modifier|*
name|itv
decl_stmt|,
modifier|*
name|oitv
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|itimerval
name|aitv
decl_stmt|,
modifier|*
name|aitvp
decl_stmt|;
name|int
name|s
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|u
operator|.
name|u_procp
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|which
operator|>
literal|2
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|aitvp
operator|=
name|uap
operator|->
name|itv
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|oitv
condition|)
block|{
name|uap
operator|->
name|itv
operator|=
name|uap
operator|->
name|oitv
expr_stmt|;
name|getitimer
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|aitvp
operator|==
literal|0
condition|)
return|return;
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|aitvp
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|aitv
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|itimerval
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
if|if
condition|(
name|itimerfix
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|)
operator|||
name|itimerfix
argument_list|(
operator|&
name|aitv
operator|.
name|it_interval
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|which
operator|==
name|ITIMER_REAL
condition|)
block|{
name|untimeout
argument_list|(
name|realitexpire
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
argument_list|)
expr_stmt|;
if|if
condition|(
name|timerisset
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|)
condition|)
block|{
name|timevaladd
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|,
operator|&
name|time
argument_list|)
expr_stmt|;
name|timeout
argument_list|(
name|realitexpire
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
argument_list|,
name|hzto
argument_list|(
operator|&
name|aitv
operator|.
name|it_value
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|p
operator|->
name|p_realtimer
operator|=
name|aitv
expr_stmt|;
block|}
else|else
name|u
operator|.
name|u_timer
index|[
name|uap
operator|->
name|which
index|]
operator|=
name|aitv
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Real interval timer expired:  * send process whose timer expired an alarm signal.  * If time is not set up to reload, then just return.  * Else compute next time timer should go off which is> current time.  * This is where delay in processing this timeout causes multiple  * SIGALRM calls to be compressed into one.  */
end_comment

begin_expr_stmt
name|realitexpire
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|int
name|s
decl_stmt|;
name|psignal
argument_list|(
name|p
argument_list|,
name|SIGALRM
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|timerisset
argument_list|(
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_interval
argument_list|)
condition|)
block|{
name|timerclear
argument_list|(
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_value
argument_list|)
expr_stmt|;
return|return;
block|}
for|for
control|(
init|;
condition|;
control|)
block|{
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
name|timevaladd
argument_list|(
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_value
argument_list|,
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_interval
argument_list|)
expr_stmt|;
if|if
condition|(
name|timercmp
argument_list|(
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_value
argument_list|,
operator|&
name|time
argument_list|,
operator|>
argument_list|)
condition|)
block|{
name|timeout
argument_list|(
name|realitexpire
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
argument_list|,
name|hzto
argument_list|(
operator|&
name|p
operator|->
name|p_realtimer
operator|.
name|it_value
argument_list|)
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Check that a proposed value to load into the .it_value or  * .it_interval part of an interval timer is acceptable, and  * fix it to have at least minimal value (i.e. if it is less  * than the resolution of the clock, round it up.)  */
end_comment

begin_macro
name|itimerfix
argument_list|(
argument|tv
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|timeval
modifier|*
name|tv
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|tv
operator|->
name|tv_sec
operator|<
literal|0
operator|||
name|tv
operator|->
name|tv_sec
operator|>
literal|100000000
operator|||
name|tv
operator|->
name|tv_usec
operator|<
literal|0
operator|||
name|tv
operator|->
name|tv_usec
operator|>=
literal|1000000
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
if|if
condition|(
name|tv
operator|->
name|tv_sec
operator|==
literal|0
operator|&&
name|tv
operator|->
name|tv_usec
operator|!=
literal|0
operator|&&
name|tv
operator|->
name|tv_usec
operator|<
name|tick
condition|)
name|tv
operator|->
name|tv_usec
operator|=
name|tick
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Decrement an interval timer by a specified number  * of microseconds, which must be less than a second,  * i.e.< 1000000.  If the timer expires, then reload  * it.  In this case, carry over (usec - old value) to  * reducint the value reloaded into the timer so that  * the timer does not drift.  This routine assumes  * that it is called in a context where the timers  * on which it is operating cannot change in value.  */
end_comment

begin_expr_stmt
name|itimerdecr
argument_list|(
name|itp
argument_list|,
name|usec
argument_list|)
specifier|register
expr|struct
name|itimerval
operator|*
name|itp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|usec
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|<
name|usec
condition|)
block|{
if|if
condition|(
name|itp
operator|->
name|it_value
operator|.
name|tv_sec
operator|==
literal|0
condition|)
block|{
comment|/* expired, and already in next interval */
name|usec
operator|-=
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
expr_stmt|;
goto|goto
name|expire
goto|;
block|}
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|+=
literal|1000000
expr_stmt|;
name|itp
operator|->
name|it_value
operator|.
name|tv_sec
operator|--
expr_stmt|;
block|}
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|-=
name|usec
expr_stmt|;
name|usec
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|timerisset
argument_list|(
operator|&
name|itp
operator|->
name|it_value
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* expired, exactly at end of interval */
name|expire
label|:
if|if
condition|(
name|timerisset
argument_list|(
operator|&
name|itp
operator|->
name|it_interval
argument_list|)
condition|)
block|{
name|itp
operator|->
name|it_value
operator|=
name|itp
operator|->
name|it_interval
expr_stmt|;
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|-=
name|usec
expr_stmt|;
if|if
condition|(
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|<
literal|0
condition|)
block|{
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|+=
literal|1000000
expr_stmt|;
name|itp
operator|->
name|it_value
operator|.
name|tv_sec
operator|--
expr_stmt|;
block|}
block|}
else|else
name|itp
operator|->
name|it_value
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
comment|/* sec is already 0 */
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Add and subtract routines for timevals.  * N.B.: subtract routine doesn't deal with  * results which are before the beginning,  * it just gets very confused in this case.  * Caveat emptor.  */
end_comment

begin_macro
name|timevaladd
argument_list|(
argument|t1
argument_list|,
argument|t2
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|timeval
modifier|*
name|t1
decl_stmt|,
modifier|*
name|t2
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|t1
operator|->
name|tv_sec
operator|+=
name|t2
operator|->
name|tv_sec
expr_stmt|;
name|t1
operator|->
name|tv_usec
operator|+=
name|t2
operator|->
name|tv_usec
expr_stmt|;
name|timevalfix
argument_list|(
name|t1
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|timevalsub
argument_list|(
argument|t1
argument_list|,
argument|t2
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|timeval
modifier|*
name|t1
decl_stmt|,
modifier|*
name|t2
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|t1
operator|->
name|tv_sec
operator|-=
name|t2
operator|->
name|tv_sec
expr_stmt|;
name|t1
operator|->
name|tv_usec
operator|-=
name|t2
operator|->
name|tv_usec
expr_stmt|;
name|timevalfix
argument_list|(
name|t1
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|timevalfix
argument_list|(
argument|t1
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|timeval
modifier|*
name|t1
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|t1
operator|->
name|tv_usec
operator|<
literal|0
condition|)
block|{
name|t1
operator|->
name|tv_sec
operator|--
expr_stmt|;
name|t1
operator|->
name|tv_usec
operator|+=
literal|1000000
expr_stmt|;
block|}
if|if
condition|(
name|t1
operator|->
name|tv_usec
operator|>=
literal|1000000
condition|)
block|{
name|t1
operator|->
name|tv_sec
operator|++
expr_stmt|;
name|t1
operator|->
name|tv_usec
operator|-=
literal|1000000
expr_stmt|;
block|}
block|}
end_block

end_unit

