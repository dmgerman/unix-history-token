begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)vm_text.c	7.3 (Berkeley) 3/25/87  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"map.h"
end_include

begin_include
include|#
directive|include
file|"dir.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"text.h"
end_include

begin_include
include|#
directive|include
file|"inode.h"
end_include

begin_include
include|#
directive|include
file|"buf.h"
end_include

begin_include
include|#
directive|include
file|"seg.h"
end_include

begin_include
include|#
directive|include
file|"vm.h"
end_include

begin_include
include|#
directive|include
file|"cmap.h"
end_include

begin_include
include|#
directive|include
file|"uio.h"
end_include

begin_include
include|#
directive|include
file|"exec.h"
end_include

begin_include
include|#
directive|include
file|"../machine/pte.h"
end_include

begin_include
include|#
directive|include
file|"../machine/cpu.h"
end_include

begin_define
define|#
directive|define
name|X_LOCK
parameter_list|(
name|xp
parameter_list|)
value|{ \ 	while ((xp)->x_flag& XLOCK) { \ 		(xp)->x_flag |= XWANT; \ 		sleep((caddr_t)(xp), PSWP); \ 	} \ 	(xp)->x_flag |= XLOCK; \ }
end_define

begin_define
define|#
directive|define
name|XUNLOCK
parameter_list|(
name|xp
parameter_list|)
value|{ \ 	if ((xp)->x_flag& XWANT) \ 		wakeup((caddr_t)(xp)); \ 	(xp)->x_flag&= ~(XLOCK|XWANT); \ }
end_define

begin_define
define|#
directive|define
name|FREE_AT_HEAD
parameter_list|(
name|xp
parameter_list|)
value|{ \ 	(xp)->x_forw = xhead; \ 	xhead = (xp); \ 	(xp)->x_back =&xhead; \ 	if (xtail ==&xhead) \ 		xtail =&(xp)->x_forw; \ 	else \ 		(xp)->x_forw->x_back =&(xp)->x_forw; \ }
end_define

begin_define
define|#
directive|define
name|FREE_AT_TAIL
parameter_list|(
name|xp
parameter_list|)
value|{ \ 	(xp)->x_back = xtail; \ 	*xtail = (xp); \ 	xtail =&(xp)->x_forw; \
comment|/* x_forw is NULL */
value|\ }
end_define

begin_define
define|#
directive|define
name|ALLOC
parameter_list|(
name|xp
parameter_list|)
value|{ \ 	*((xp)->x_back) = (xp)->x_forw; \ 	if ((xp)->x_forw) \ 		(xp)->x_forw->x_back = (xp)->x_back; \ 	else \ 		xtail = (xp)->x_back; \ 	(xp)->x_forw = NULL; \ 	(xp)->x_back = NULL; \ }
end_define

begin_comment
comment|/*  * We place free text table entries on a free list.  * All text images are treated as "sticky,"  * and are placed on the free list (as an LRU cache) when unused.  * They may be reclaimed from the free list until reused.  * Files marked sticky are locked into the table, and are never freed.  * For machines with limited swap space, this may result  * in filling up swap, and thus we allow a limit  * to be placed on the number of text images to cache.  * (In that case, really should change the algorithm  * for freeing a text when the cache is full;  * should free least-recently-used text rather than current one.)  */
end_comment

begin_decl_stmt
name|struct
name|text
modifier|*
name|xhead
decl_stmt|,
modifier|*
modifier|*
name|xtail
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* text table free list */
end_comment

begin_decl_stmt
name|int
name|xcache
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* number of "sticky" texts retained */
end_comment

begin_decl_stmt
name|int
name|maxtextcache
init|=
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* maximum number of "sticky" texts */
end_comment

begin_decl_stmt
name|struct
name|xstats
name|xstats
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* cache statistics */
end_comment

begin_comment
comment|/*  * initialize text table  */
end_comment

begin_macro
name|xinit
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
decl_stmt|;
name|xtail
operator|=
operator|&
name|xhead
expr_stmt|;
for|for
control|(
name|xp
operator|=
name|text
init|;
name|xp
operator|<
name|textNTEXT
condition|;
name|xp
operator|++
control|)
name|FREE_AT_TAIL
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|if
condition|(
name|maxtextcache
operator|==
operator|-
literal|1
condition|)
name|maxtextcache
operator|=
name|ntext
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * relinquish use of the shared text segment  * of a process.  */
end_comment

begin_macro
name|xfree
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
decl_stmt|;
if|if
condition|(
operator|(
name|xp
operator|=
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|)
operator|==
name|NULL
condition|)
return|return;
name|xstats
operator|.
name|free
operator|++
expr_stmt|;
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|--
name|xp
operator|->
name|x_count
operator|==
literal|0
operator|&&
operator|(
name|xp
operator|->
name|x_iptr
operator|->
name|i_mode
operator|&
name|ISVTX
operator|)
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|xcache
operator|>=
name|maxtextcache
operator|||
name|xp
operator|->
name|x_flag
operator|&
name|XTRC
operator|||
name|xp
operator|->
name|x_iptr
operator|->
name|i_nlink
operator|==
literal|0
condition|)
block|{
comment|/* XXX */
name|xp
operator|->
name|x_rssize
operator|-=
name|vmemfree
argument_list|(
name|tptopte
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
literal|0
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|u
operator|.
name|u_tsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_rssize
operator|!=
literal|0
condition|)
name|panic
argument_list|(
literal|"xfree rssize"
argument_list|)
expr_stmt|;
while|while
condition|(
name|xp
operator|->
name|x_poip
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|xp
operator|->
name|x_poip
argument_list|,
name|PSWP
operator|+
literal|1
argument_list|)
expr_stmt|;
name|xp
operator|->
name|x_flag
operator|&=
operator|~
name|XLOCK
expr_stmt|;
name|xuntext
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|FREE_AT_HEAD
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|xp
operator|->
name|x_flag
operator|&
name|XWRIT
condition|)
block|{
name|xstats
operator|.
name|free_cacheswap
operator|++
expr_stmt|;
name|xp
operator|->
name|x_flag
operator||=
name|XUNUSED
expr_stmt|;
block|}
name|xcache
operator|++
expr_stmt|;
name|xstats
operator|.
name|free_cache
operator|++
expr_stmt|;
name|xccdec
argument_list|(
name|xp
argument_list|,
name|u
operator|.
name|u_procp
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|tahoe
argument_list|)
name|xp
operator|->
name|x_ckey
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
name|FREE_AT_TAIL
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
if|#
directive|if
name|defined
argument_list|(
name|tahoe
argument_list|)
if|if
condition|(
name|xp
operator|->
name|x_count
operator|==
literal|0
condition|)
name|xp
operator|->
name|x_ckey
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
name|xccdec
argument_list|(
name|xp
argument_list|,
name|u
operator|.
name|u_procp
argument_list|)
expr_stmt|;
name|xstats
operator|.
name|free_inuse
operator|++
expr_stmt|;
block|}
name|xunlink
argument_list|(
name|u
operator|.
name|u_procp
argument_list|)
expr_stmt|;
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|=
name|NULL
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Attach to a shared text segment.  * If there is no shared text, just return.  * If there is, hook up to it:  * if it is not currently being used, it has to be read  * in from the inode (ip); the written bit is set to force it  * to be written out as appropriate.  * If it is being used, but is not currently in core,  * a swap has to be done to get it back.  */
end_comment

begin_macro
name|xalloc
argument_list|(
argument|ip
argument_list|,
argument|ep
argument_list|,
argument|pagi
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|exec
modifier|*
name|ep
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
decl_stmt|;
specifier|register
name|size_t
name|ts
decl_stmt|;
if|if
condition|(
name|ep
operator|->
name|a_text
operator|==
literal|0
condition|)
return|return;
name|xstats
operator|.
name|alloc
operator|++
expr_stmt|;
while|while
condition|(
operator|(
name|xp
operator|=
name|ip
operator|->
name|i_text
operator|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|xp
operator|->
name|x_flag
operator|&
name|XLOCK
condition|)
block|{
comment|/* 			 * Wait for text to be unlocked, 			 * then start over (may have changed state). 			 */
name|xwait
argument_list|(
name|xp
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_back
condition|)
block|{
name|xstats
operator|.
name|alloc_cachehit
operator|++
expr_stmt|;
name|ALLOC
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|xp
operator|->
name|x_flag
operator|&=
operator|~
name|XUNUSED
expr_stmt|;
name|xcache
operator|--
expr_stmt|;
block|}
else|else
name|xstats
operator|.
name|alloc_inuse
operator|++
expr_stmt|;
name|xp
operator|->
name|x_count
operator|++
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|=
name|xp
expr_stmt|;
name|xlink
argument_list|(
name|u
operator|.
name|u_procp
argument_list|)
expr_stmt|;
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|tahoe
argument_list|)
name|ckeyrelease
argument_list|(
name|u
operator|.
name|u_procp
operator|->
name|p_ckey
argument_list|)
expr_stmt|;
if|if
condition|(
name|ckey_cnt
index|[
name|xp
operator|->
name|x_ckey
index|]
condition|)
name|ckey_cnt
index|[
name|xp
operator|->
name|x_ckey
index|]
operator|++
expr_stmt|;
else|else
comment|/* dead key */
name|xp
operator|->
name|x_ckey
operator|=
name|getcodekey
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_ckey
operator|=
name|xp
operator|->
name|x_ckey
expr_stmt|;
endif|#
directive|endif
return|return;
block|}
name|xp
operator|=
name|xhead
expr_stmt|;
if|if
condition|(
name|xp
operator|==
name|NULL
condition|)
block|{
name|tablefull
argument_list|(
literal|"text"
argument_list|)
expr_stmt|;
name|psignal
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
name|SIGKILL
argument_list|)
expr_stmt|;
return|return;
block|}
name|ALLOC
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_iptr
condition|)
block|{
name|xstats
operator|.
name|alloc_cacheflush
operator|++
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_flag
operator|&
name|XUNUSED
condition|)
name|xstats
operator|.
name|alloc_unused
operator|++
expr_stmt|;
name|xuntext
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|xcache
operator|--
expr_stmt|;
block|}
name|xp
operator|->
name|x_flag
operator|=
name|XLOAD
operator||
name|XLOCK
expr_stmt|;
if|if
condition|(
name|pagi
condition|)
name|xp
operator|->
name|x_flag
operator||=
name|XPAGI
expr_stmt|;
name|ts
operator|=
name|clrnd
argument_list|(
name|btoc
argument_list|(
name|ep
operator|->
name|a_text
argument_list|)
argument_list|)
expr_stmt|;
name|xp
operator|->
name|x_size
operator|=
name|ts
expr_stmt|;
if|if
condition|(
name|vsxalloc
argument_list|(
name|xp
argument_list|)
operator|==
name|NULL
condition|)
block|{
name|swkill
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
literal|"xalloc: no swap space"
argument_list|)
expr_stmt|;
return|return;
block|}
name|xp
operator|->
name|x_count
operator|=
literal|1
expr_stmt|;
name|xp
operator|->
name|x_ccount
operator|=
literal|0
expr_stmt|;
name|xp
operator|->
name|x_rssize
operator|=
literal|0
expr_stmt|;
name|xp
operator|->
name|x_iptr
operator|=
name|ip
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|ITEXT
expr_stmt|;
name|ip
operator|->
name|i_text
operator|=
name|xp
expr_stmt|;
name|ip
operator|->
name|i_count
operator|++
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|=
name|xp
expr_stmt|;
name|xlink
argument_list|(
name|u
operator|.
name|u_procp
argument_list|)
expr_stmt|;
if|if
condition|(
name|pagi
operator|==
literal|0
condition|)
block|{
name|settprot
argument_list|(
name|RW
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_flag
operator||=
name|SKEEP
expr_stmt|;
operator|(
name|void
operator|)
name|rdwri
argument_list|(
name|UIO_READ
argument_list|,
name|ip
argument_list|,
operator|(
name|caddr_t
operator|)
name|ctob
argument_list|(
name|tptov
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
literal|0
argument_list|)
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|ep
operator|->
name|a_text
argument_list|,
operator|(
name|off_t
operator|)
sizeof|sizeof
argument_list|(
expr|struct
name|exec
argument_list|)
argument_list|,
literal|2
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_flag
operator|&=
operator|~
name|SKEEP
expr_stmt|;
block|}
name|settprot
argument_list|(
name|RO
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|tahoe
argument_list|)
name|ckeyrelease
argument_list|(
name|u
operator|.
name|u_procp
operator|->
name|p_ckey
argument_list|)
expr_stmt|;
name|xp
operator|->
name|x_ckey
operator|=
name|getcodekey
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_procp
operator|->
name|p_ckey
operator|=
name|xp
operator|->
name|x_ckey
expr_stmt|;
endif|#
directive|endif
name|xp
operator|->
name|x_flag
operator||=
name|XWRIT
expr_stmt|;
name|xp
operator|->
name|x_flag
operator|&=
operator|~
name|XLOAD
expr_stmt|;
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Lock and unlock a text segment from swapping  */
end_comment

begin_expr_stmt
name|xlock
argument_list|(
name|xp
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Wait for xp to be unlocked if it is currently locked.  */
end_comment

begin_expr_stmt
name|xwait
argument_list|(
name|xp
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|xunlock
argument_list|(
name|xp
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Decrement the in-core usage count of a shared text segment,  * which must be locked.  When the count drops to zero,  * free the core space.  */
end_comment

begin_expr_stmt
name|xccdec
argument_list|(
name|xp
argument_list|,
name|p
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
operator|--
name|xp
operator|->
name|x_ccount
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|xp
operator|->
name|x_flag
operator|&
name|XWRIT
condition|)
block|{
name|vsswap
argument_list|(
name|p
argument_list|,
name|tptopte
argument_list|(
name|p
argument_list|,
literal|0
argument_list|)
argument_list|,
name|CTEXT
argument_list|,
literal|0
argument_list|,
operator|(
name|int
operator|)
name|xp
operator|->
name|x_size
argument_list|,
operator|(
expr|struct
name|dmap
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_flag
operator|&
name|XPAGI
condition|)
operator|(
name|void
operator|)
name|swap
argument_list|(
name|p
argument_list|,
name|xp
operator|->
name|x_ptdaddr
argument_list|,
operator|(
name|caddr_t
operator|)
name|tptopte
argument_list|(
name|p
argument_list|,
literal|0
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|xp
operator|->
name|x_size
operator|*
sizeof|sizeof
argument_list|(
expr|struct
name|pte
argument_list|)
argument_list|,
name|B_WRITE
argument_list|,
name|B_PAGET
argument_list|,
name|swapdev
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|xp
operator|->
name|x_flag
operator|&=
operator|~
name|XWRIT
expr_stmt|;
block|}
else|else
name|xp
operator|->
name|x_rssize
operator|-=
name|vmemfree
argument_list|(
name|tptopte
argument_list|(
name|p
argument_list|,
literal|0
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|xp
operator|->
name|x_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_rssize
operator|!=
literal|0
condition|)
name|panic
argument_list|(
literal|"text rssize"
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Detach a process from the in-core text.  * External interface to xccdec, used when swapping out a process.  */
end_comment

begin_expr_stmt
name|xdetach
argument_list|(
name|xp
argument_list|,
name|p
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|xp
operator|&&
name|xp
operator|->
name|x_ccount
operator|!=
literal|0
condition|)
block|{
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
name|xccdec
argument_list|(
name|xp
argument_list|,
name|p
argument_list|)
expr_stmt|;
name|xunlink
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Free the swap image of all unused saved-text text segments  * which are from device dev (used by umount system call).  * If dev is NODEV, do all devices (used when rebooting).  */
end_comment

begin_expr_stmt
name|xumount
argument_list|(
name|dev
argument_list|)
specifier|register
name|dev_t
name|dev
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
decl_stmt|;
for|for
control|(
name|xp
operator|=
name|text
init|;
name|xp
operator|<
name|textNTEXT
condition|;
name|xp
operator|++
control|)
if|if
condition|(
name|xp
operator|->
name|x_iptr
operator|!=
name|NULL
operator|&&
operator|(
name|dev
operator|==
name|xp
operator|->
name|x_iptr
operator|->
name|i_dev
operator|||
name|dev
operator|==
name|NODEV
operator|)
operator|&&
operator|(
name|xp
operator|->
name|x_flag
operator|&
name|XLOCK
operator|)
operator|==
literal|0
condition|)
name|xuntext
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * remove a shared text segment from the text table, if possible.  */
end_comment

begin_expr_stmt
name|xrele
argument_list|(
name|ip
argument_list|)
specifier|register
expr|struct
name|inode
operator|*
name|ip
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ITEXT
condition|)
name|xuntext
argument_list|(
name|ip
operator|->
name|i_text
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * remove text image from the text table.  * the use count must be zero.  */
end_comment

begin_expr_stmt
name|xuntext
argument_list|(
name|xp
argument_list|)
specifier|register
expr|struct
name|text
operator|*
name|xp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|X_LOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
if|if
condition|(
name|xp
operator|->
name|x_count
operator|==
literal|0
condition|)
block|{
name|ip
operator|=
name|xp
operator|->
name|x_iptr
expr_stmt|;
name|xp
operator|->
name|x_iptr
operator|=
name|NULL
expr_stmt|;
name|vsxfree
argument_list|(
name|xp
argument_list|,
operator|(
name|long
operator|)
name|xp
operator|->
name|x_size
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_flag
operator|&=
operator|~
name|ITEXT
expr_stmt|;
name|ip
operator|->
name|i_text
operator|=
name|NULL
expr_stmt|;
name|irele
argument_list|(
name|ip
argument_list|)
expr_stmt|;
block|}
name|XUNLOCK
argument_list|(
name|xp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Add a process to those sharing a text segment by  * getting the page tables and then linking to x_caddr.  */
end_comment

begin_expr_stmt
name|xlink
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
init|=
name|p
operator|->
name|p_textp
decl_stmt|;
if|if
condition|(
name|xp
operator|==
literal|0
condition|)
return|return;
name|vinitpt
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_xlink
operator|=
name|xp
operator|->
name|x_caddr
expr_stmt|;
name|xp
operator|->
name|x_caddr
operator|=
name|p
expr_stmt|;
name|xp
operator|->
name|x_ccount
operator|++
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|xunlink
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
init|=
name|p
operator|->
name|p_textp
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|q
decl_stmt|;
if|if
condition|(
name|xp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
name|xp
operator|->
name|x_caddr
operator|==
name|p
condition|)
block|{
name|xp
operator|->
name|x_caddr
operator|=
name|p
operator|->
name|p_xlink
expr_stmt|;
name|p
operator|->
name|p_xlink
operator|=
literal|0
expr_stmt|;
return|return;
block|}
for|for
control|(
name|q
operator|=
name|xp
operator|->
name|x_caddr
init|;
name|q
operator|->
name|p_xlink
condition|;
name|q
operator|=
name|q
operator|->
name|p_xlink
control|)
if|if
condition|(
name|q
operator|->
name|p_xlink
operator|==
name|p
condition|)
block|{
name|q
operator|->
name|p_xlink
operator|=
name|p
operator|->
name|p_xlink
expr_stmt|;
name|p
operator|->
name|p_xlink
operator|=
literal|0
expr_stmt|;
return|return;
block|}
name|panic
argument_list|(
literal|"lost text"
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Replace p by q in a text incore linked list.  * Used by vfork(), internally.  */
end_comment

begin_macro
name|xrepl
argument_list|(
argument|p
argument_list|,
argument|q
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|,
modifier|*
name|q
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|text
modifier|*
name|xp
init|=
name|q
operator|->
name|p_textp
decl_stmt|;
if|if
condition|(
name|xp
operator|==
literal|0
condition|)
return|return;
name|xunlink
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|q
operator|->
name|p_xlink
operator|=
name|xp
operator|->
name|x_caddr
expr_stmt|;
name|xp
operator|->
name|x_caddr
operator|=
name|q
expr_stmt|;
block|}
end_block

end_unit

