begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)kern_sig.c	7.3 (Berkeley) 12/14/86  */
end_comment

begin_include
include|#
directive|include
file|"../machine/reg.h"
end_include

begin_include
include|#
directive|include
file|"../machine/pte.h"
end_include

begin_include
include|#
directive|include
file|"../machine/psl.h"
end_include

begin_include
include|#
directive|include
file|"../machine/mtpr.h"
end_include

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"dir.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"inode.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"timeb.h"
end_include

begin_include
include|#
directive|include
file|"times.h"
end_include

begin_include
include|#
directive|include
file|"buf.h"
end_include

begin_include
include|#
directive|include
file|"mount.h"
end_include

begin_include
include|#
directive|include
file|"text.h"
end_include

begin_include
include|#
directive|include
file|"seg.h"
end_include

begin_include
include|#
directive|include
file|"vm.h"
end_include

begin_include
include|#
directive|include
file|"acct.h"
end_include

begin_include
include|#
directive|include
file|"uio.h"
end_include

begin_include
include|#
directive|include
file|"kernel.h"
end_include

begin_define
define|#
directive|define
name|cantmask
value|(sigmask(SIGKILL)|sigmask(SIGCONT)|sigmask(SIGSTOP))
end_define

begin_define
define|#
directive|define
name|stopsigmask
value|(sigmask(SIGSTOP)|sigmask(SIGTSTP)| \ 			sigmask(SIGTTIN)|sigmask(SIGTTOU))
end_define

begin_comment
comment|/*  * Generalized interface signal handler.  */
end_comment

begin_macro
name|sigvec
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|signo
decl_stmt|;
name|struct
name|sigvec
modifier|*
name|nsv
decl_stmt|;
name|struct
name|sigvec
modifier|*
name|osv
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|sigvec
name|vec
decl_stmt|;
specifier|register
name|struct
name|sigvec
modifier|*
name|sv
decl_stmt|;
specifier|register
name|int
name|sig
decl_stmt|;
name|int
name|bit
decl_stmt|;
name|sig
operator|=
name|uap
operator|->
name|signo
expr_stmt|;
if|if
condition|(
name|sig
operator|<=
literal|0
operator|||
name|sig
operator|>=
name|NSIG
operator|||
name|sig
operator|==
name|SIGKILL
operator|||
name|sig
operator|==
name|SIGSTOP
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|sv
operator|=
operator|&
name|vec
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|osv
condition|)
block|{
name|sv
operator|->
name|sv_handler
operator|=
name|u
operator|.
name|u_signal
index|[
name|sig
index|]
expr_stmt|;
name|sv
operator|->
name|sv_mask
operator|=
name|u
operator|.
name|u_sigmask
index|[
name|sig
index|]
expr_stmt|;
name|bit
operator|=
name|sigmask
argument_list|(
name|sig
argument_list|)
expr_stmt|;
name|sv
operator|->
name|sv_flags
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|(
name|u
operator|.
name|u_sigonstack
operator|&
name|bit
operator|)
operator|!=
literal|0
condition|)
name|sv
operator|->
name|sv_flags
operator||=
name|SV_ONSTACK
expr_stmt|;
if|if
condition|(
operator|(
name|u
operator|.
name|u_sigintr
operator|&
name|bit
operator|)
operator|!=
literal|0
condition|)
name|sv
operator|->
name|sv_flags
operator||=
name|SV_INTERRUPT
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
name|sv
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|osv
argument_list|,
sizeof|sizeof
argument_list|(
name|vec
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|nsv
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|nsv
argument_list|,
operator|(
name|caddr_t
operator|)
name|sv
argument_list|,
sizeof|sizeof
argument_list|(
name|vec
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
if|if
condition|(
name|sig
operator|==
name|SIGCONT
operator|&&
name|sv
operator|->
name|sv_handler
operator|==
name|SIG_IGN
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|setsigvec
argument_list|(
name|sig
argument_list|,
name|sv
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_macro
name|setsigvec
argument_list|(
argument|sig
argument_list|,
argument|sv
argument_list|)
end_macro

begin_decl_stmt
name|int
name|sig
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|sigvec
modifier|*
name|sv
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
specifier|register
name|int
name|bit
decl_stmt|;
name|bit
operator|=
name|sigmask
argument_list|(
name|sig
argument_list|)
expr_stmt|;
name|p
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
comment|/* 	 * Change setting atomically. 	 */
operator|(
name|void
operator|)
name|splhigh
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_signal
index|[
name|sig
index|]
operator|=
name|sv
operator|->
name|sv_handler
expr_stmt|;
name|u
operator|.
name|u_sigmask
index|[
name|sig
index|]
operator|=
name|sv
operator|->
name|sv_mask
operator|&
operator|~
name|cantmask
expr_stmt|;
if|if
condition|(
name|sv
operator|->
name|sv_flags
operator|&
name|SV_INTERRUPT
condition|)
name|u
operator|.
name|u_sigintr
operator||=
name|bit
expr_stmt|;
else|else
name|u
operator|.
name|u_sigintr
operator|&=
operator|~
name|bit
expr_stmt|;
if|if
condition|(
name|sv
operator|->
name|sv_flags
operator|&
name|SV_ONSTACK
condition|)
name|u
operator|.
name|u_sigonstack
operator||=
name|bit
expr_stmt|;
else|else
name|u
operator|.
name|u_sigonstack
operator|&=
operator|~
name|bit
expr_stmt|;
if|if
condition|(
name|sv
operator|->
name|sv_handler
operator|==
name|SIG_IGN
condition|)
block|{
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|bit
expr_stmt|;
comment|/* never to be seen again */
name|p
operator|->
name|p_sigignore
operator||=
name|bit
expr_stmt|;
name|p
operator|->
name|p_sigcatch
operator|&=
operator|~
name|bit
expr_stmt|;
block|}
else|else
block|{
name|p
operator|->
name|p_sigignore
operator|&=
operator|~
name|bit
expr_stmt|;
if|if
condition|(
name|sv
operator|->
name|sv_handler
operator|==
name|SIG_DFL
condition|)
name|p
operator|->
name|p_sigcatch
operator|&=
operator|~
name|bit
expr_stmt|;
else|else
name|p
operator|->
name|p_sigcatch
operator||=
name|bit
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|spl0
argument_list|()
expr_stmt|;
block|}
end_block

begin_macro
name|sigblock
argument_list|()
end_macro

begin_block
block|{
struct|struct
name|a
block|{
name|int
name|mask
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|u
operator|.
name|u_procp
decl_stmt|;
operator|(
name|void
operator|)
name|splhigh
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_r
operator|.
name|r_val1
operator|=
name|p
operator|->
name|p_sigmask
expr_stmt|;
name|p
operator|->
name|p_sigmask
operator||=
name|uap
operator|->
name|mask
operator|&
operator|~
name|cantmask
expr_stmt|;
operator|(
name|void
operator|)
name|spl0
argument_list|()
expr_stmt|;
block|}
end_block

begin_macro
name|sigsetmask
argument_list|()
end_macro

begin_block
block|{
struct|struct
name|a
block|{
name|int
name|mask
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|u
operator|.
name|u_procp
decl_stmt|;
operator|(
name|void
operator|)
name|splhigh
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_r
operator|.
name|r_val1
operator|=
name|p
operator|->
name|p_sigmask
expr_stmt|;
name|p
operator|->
name|p_sigmask
operator|=
name|uap
operator|->
name|mask
operator|&
operator|~
name|cantmask
expr_stmt|;
operator|(
name|void
operator|)
name|spl0
argument_list|()
expr_stmt|;
block|}
end_block

begin_macro
name|sigpause
argument_list|()
end_macro

begin_block
block|{
struct|struct
name|a
block|{
name|int
name|mask
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|u
operator|.
name|u_procp
decl_stmt|;
comment|/* 	 * When returning from sigpause, we want 	 * the old mask to be restored after the 	 * signal handler has finished.  Thus, we 	 * save it here and mark the proc structure 	 * to indicate this (should be in u.). 	 */
name|u
operator|.
name|u_oldmask
operator|=
name|p
operator|->
name|p_sigmask
expr_stmt|;
name|p
operator|->
name|p_flag
operator||=
name|SOMASK
expr_stmt|;
name|p
operator|->
name|p_sigmask
operator|=
name|uap
operator|->
name|mask
operator|&
operator|~
name|cantmask
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|u
argument_list|,
name|PSLEP
argument_list|)
expr_stmt|;
comment|/*NOTREACHED*/
block|}
end_block

begin_undef
undef|#
directive|undef
name|cantmask
end_undef

begin_macro
name|sigstack
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|struct
name|sigstack
modifier|*
name|nss
decl_stmt|;
name|struct
name|sigstack
modifier|*
name|oss
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|sigstack
name|ss
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|oss
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|u
operator|.
name|u_sigstack
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|oss
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sigstack
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|nss
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|nss
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|ss
argument_list|,
sizeof|sizeof
argument_list|(
name|ss
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
operator|==
literal|0
condition|)
name|u
operator|.
name|u_sigstack
operator|=
name|ss
expr_stmt|;
block|}
block|}
end_block

begin_macro
name|kill
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|pid
decl_stmt|;
name|int
name|signo
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|signo
operator|<
literal|0
operator|||
name|uap
operator|->
name|signo
operator|>
name|NSIG
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|pid
operator|>
literal|0
condition|)
block|{
comment|/* kill single process */
name|p
operator|=
name|pfind
argument_list|(
name|uap
operator|->
name|pid
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ESRCH
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|u
operator|.
name|u_uid
operator|&&
name|u
operator|.
name|u_uid
operator|!=
name|p
operator|->
name|p_uid
condition|)
name|u
operator|.
name|u_error
operator|=
name|EPERM
expr_stmt|;
elseif|else
if|if
condition|(
name|uap
operator|->
name|signo
condition|)
name|psignal
argument_list|(
name|p
argument_list|,
name|uap
operator|->
name|signo
argument_list|)
expr_stmt|;
return|return;
block|}
switch|switch
condition|(
name|uap
operator|->
name|pid
condition|)
block|{
case|case
operator|-
literal|1
case|:
comment|/* broadcast signal */
name|u
operator|.
name|u_error
operator|=
name|killpg1
argument_list|(
name|uap
operator|->
name|signo
argument_list|,
literal|0
argument_list|,
literal|1
argument_list|)
expr_stmt|;
break|break;
case|case
literal|0
case|:
comment|/* signal own process group */
name|u
operator|.
name|u_error
operator|=
name|killpg1
argument_list|(
name|uap
operator|->
name|signo
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
break|break;
default|default:
comment|/* negative explicit process group */
name|u
operator|.
name|u_error
operator|=
name|killpg1
argument_list|(
name|uap
operator|->
name|signo
argument_list|,
operator|-
name|uap
operator|->
name|pid
argument_list|,
literal|0
argument_list|)
expr_stmt|;
break|break;
block|}
return|return;
block|}
end_block

begin_macro
name|killpg
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|pgrp
decl_stmt|;
name|int
name|signo
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
if|if
condition|(
name|uap
operator|->
name|signo
operator|<
literal|0
operator|||
name|uap
operator|->
name|signo
operator|>
name|NSIG
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|killpg1
argument_list|(
name|uap
operator|->
name|signo
argument_list|,
name|uap
operator|->
name|pgrp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/* KILL CODE SHOULDNT KNOW ABOUT PROCESS INTERNALS !?! */
end_comment

begin_macro
name|killpg1
argument_list|(
argument|signo
argument_list|,
argument|pgrp
argument_list|,
argument|all
argument_list|)
end_macro

begin_decl_stmt
name|int
name|signo
decl_stmt|,
name|pgrp
decl_stmt|,
name|all
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|int
name|f
decl_stmt|,
name|error
init|=
literal|0
decl_stmt|;
if|if
condition|(
operator|!
name|all
operator|&&
name|pgrp
operator|==
literal|0
condition|)
block|{
comment|/* 		 * Zero process id means send to my process group. 		 */
name|pgrp
operator|=
name|u
operator|.
name|u_procp
operator|->
name|p_pgrp
expr_stmt|;
if|if
condition|(
name|pgrp
operator|==
literal|0
condition|)
return|return
operator|(
name|ESRCH
operator|)
return|;
block|}
for|for
control|(
name|f
operator|=
literal|0
operator|,
name|p
operator|=
name|allproc
init|;
name|p
operator|!=
name|NULL
condition|;
name|p
operator|=
name|p
operator|->
name|p_nxt
control|)
block|{
if|if
condition|(
operator|(
name|p
operator|->
name|p_pgrp
operator|!=
name|pgrp
operator|&&
operator|!
name|all
operator|)
operator|||
name|p
operator|->
name|p_ppid
operator|==
literal|0
operator|||
operator|(
name|p
operator|->
name|p_flag
operator|&
name|SSYS
operator|)
operator|||
operator|(
name|all
operator|&&
name|p
operator|==
name|u
operator|.
name|u_procp
operator|)
condition|)
continue|continue;
if|if
condition|(
name|u
operator|.
name|u_uid
operator|!=
literal|0
operator|&&
name|u
operator|.
name|u_uid
operator|!=
name|p
operator|->
name|p_uid
operator|&&
operator|(
name|signo
operator|!=
name|SIGCONT
operator|||
operator|!
name|inferior
argument_list|(
name|p
argument_list|)
operator|)
condition|)
block|{
if|if
condition|(
operator|!
name|all
condition|)
name|error
operator|=
name|EPERM
expr_stmt|;
continue|continue;
block|}
name|f
operator|++
expr_stmt|;
if|if
condition|(
name|signo
condition|)
name|psignal
argument_list|(
name|p
argument_list|,
name|signo
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|error
condition|?
name|error
else|:
operator|(
name|f
operator|==
literal|0
condition|?
name|ESRCH
else|:
literal|0
operator|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Send the specified signal to  * all processes with 'pgrp' as  * process group.  */
end_comment

begin_expr_stmt
name|gsignal
argument_list|(
name|pgrp
argument_list|,
name|sig
argument_list|)
specifier|register
name|int
name|pgrp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|pgrp
operator|==
literal|0
condition|)
return|return;
for|for
control|(
name|p
operator|=
name|allproc
init|;
name|p
operator|!=
name|NULL
condition|;
name|p
operator|=
name|p
operator|->
name|p_nxt
control|)
if|if
condition|(
name|p
operator|->
name|p_pgrp
operator|==
name|pgrp
condition|)
name|psignal
argument_list|(
name|p
argument_list|,
name|sig
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Send the specified signal to  * the specified process.  */
end_comment

begin_expr_stmt
name|psignal
argument_list|(
name|p
argument_list|,
name|sig
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|sig
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|s
decl_stmt|;
specifier|register
name|int
function_decl|(
modifier|*
name|action
function_decl|)
parameter_list|()
function_decl|;
name|int
name|mask
decl_stmt|;
if|if
condition|(
operator|(
name|unsigned
operator|)
name|sig
operator|>=
name|NSIG
condition|)
return|return;
name|mask
operator|=
name|sigmask
argument_list|(
name|sig
argument_list|)
expr_stmt|;
comment|/* 	 * If proc is traced, always give parent a chance. 	 */
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
condition|)
name|action
operator|=
name|SIG_DFL
expr_stmt|;
else|else
block|{
comment|/* 		 * If the signal is being ignored, 		 * then we forget about it immediately. 		 */
if|if
condition|(
name|p
operator|->
name|p_sigignore
operator|&
name|mask
condition|)
return|return;
if|if
condition|(
name|p
operator|->
name|p_sigmask
operator|&
name|mask
condition|)
name|action
operator|=
name|SIG_HOLD
expr_stmt|;
elseif|else
if|if
condition|(
name|p
operator|->
name|p_sigcatch
operator|&
name|mask
condition|)
name|action
operator|=
name|SIG_CATCH
expr_stmt|;
else|else
name|action
operator|=
name|SIG_DFL
expr_stmt|;
block|}
if|if
condition|(
name|sig
condition|)
block|{
name|p
operator|->
name|p_sig
operator||=
name|mask
expr_stmt|;
switch|switch
condition|(
name|sig
condition|)
block|{
case|case
name|SIGTERM
case|:
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
operator|)
operator|||
name|action
operator|!=
name|SIG_DFL
condition|)
break|break;
comment|/* fall into ... */
case|case
name|SIGKILL
case|:
if|if
condition|(
name|p
operator|->
name|p_nice
operator|>
name|NZERO
condition|)
name|p
operator|->
name|p_nice
operator|=
name|NZERO
expr_stmt|;
break|break;
case|case
name|SIGCONT
case|:
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|stopsigmask
expr_stmt|;
break|break;
case|case
name|SIGSTOP
case|:
case|case
name|SIGTSTP
case|:
case|case
name|SIGTTIN
case|:
case|case
name|SIGTTOU
case|:
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|sigmask
argument_list|(
name|SIGCONT
argument_list|)
expr_stmt|;
break|break;
block|}
block|}
comment|/* 	 * Defer further processing for signals which are held. 	 */
if|if
condition|(
name|action
operator|==
name|SIG_HOLD
condition|)
return|return;
name|s
operator|=
name|splhigh
argument_list|()
expr_stmt|;
switch|switch
condition|(
name|p
operator|->
name|p_stat
condition|)
block|{
case|case
name|SSLEEP
case|:
comment|/* 		 * If process is sleeping at negative priority 		 * we can't interrupt the sleep... the signal will 		 * be noticed when the process returns through 		 * trap() or syscall(). 		 */
if|if
condition|(
name|p
operator|->
name|p_pri
operator|<=
name|PZERO
condition|)
goto|goto
name|out
goto|;
comment|/* 		 * Process is sleeping and traced... make it runnable 		 * so it can discover the signal in issig() and stop 		 * for the parent. 		 */
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
condition|)
goto|goto
name|run
goto|;
switch|switch
condition|(
name|sig
condition|)
block|{
case|case
name|SIGSTOP
case|:
case|case
name|SIGTSTP
case|:
case|case
name|SIGTTIN
case|:
case|case
name|SIGTTOU
case|:
comment|/* 			 * These are the signals which by default 			 * stop a process. 			 */
if|if
condition|(
name|action
operator|!=
name|SIG_DFL
condition|)
goto|goto
name|run
goto|;
comment|/* 			 * Don't clog system with children of init 			 * stopped from the keyboard. 			 */
if|if
condition|(
name|sig
operator|!=
name|SIGSTOP
operator|&&
name|p
operator|->
name|p_pptr
operator|==
operator|&
name|proc
index|[
literal|1
index|]
condition|)
block|{
name|psignal
argument_list|(
name|p
argument_list|,
name|SIGKILL
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|mask
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 			 * If a child in vfork(), stopping could 			 * cause deadlock. 			 */
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|SVFORK
condition|)
goto|goto
name|out
goto|;
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|mask
expr_stmt|;
name|p
operator|->
name|p_cursig
operator|=
name|sig
expr_stmt|;
name|psignal
argument_list|(
name|p
operator|->
name|p_pptr
argument_list|,
name|SIGCHLD
argument_list|)
expr_stmt|;
name|stop
argument_list|(
name|p
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
case|case
name|SIGIO
case|:
case|case
name|SIGURG
case|:
case|case
name|SIGCHLD
case|:
case|case
name|SIGWINCH
case|:
comment|/* 			 * These signals are special in that they 			 * don't get propogated... if the process 			 * isn't interested, forget it. 			 */
if|if
condition|(
name|action
operator|!=
name|SIG_DFL
condition|)
goto|goto
name|run
goto|;
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|mask
expr_stmt|;
comment|/* take it away */
goto|goto
name|out
goto|;
default|default:
comment|/* 			 * All other signals cause the process to run 			 */
goto|goto
name|run
goto|;
block|}
comment|/*NOTREACHED*/
case|case
name|SSTOP
case|:
comment|/* 		 * If traced process is already stopped, 		 * then no further action is necessary. 		 */
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
condition|)
goto|goto
name|out
goto|;
switch|switch
condition|(
name|sig
condition|)
block|{
case|case
name|SIGKILL
case|:
comment|/* 			 * Kill signal always sets processes running. 			 */
goto|goto
name|run
goto|;
case|case
name|SIGCONT
case|:
comment|/* 			 * If the process catches SIGCONT, let it handle 			 * the signal itself.  If it isn't waiting on 			 * an event, then it goes back to run state. 			 * Otherwise, process goes back to sleep state. 			 */
if|if
condition|(
name|action
operator|!=
name|SIG_DFL
operator|||
name|p
operator|->
name|p_wchan
operator|==
literal|0
condition|)
goto|goto
name|run
goto|;
name|p
operator|->
name|p_stat
operator|=
name|SSLEEP
expr_stmt|;
goto|goto
name|out
goto|;
case|case
name|SIGSTOP
case|:
case|case
name|SIGTSTP
case|:
case|case
name|SIGTTIN
case|:
case|case
name|SIGTTOU
case|:
comment|/* 			 * Already stopped, don't need to stop again. 			 * (If we did the shell could get confused.) 			 */
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|mask
expr_stmt|;
comment|/* take it away */
goto|goto
name|out
goto|;
default|default:
comment|/* 			 * If process is sleeping interruptibly, then 			 * unstick it so that when it is continued 			 * it can look at the signal. 			 * But don't setrun the process as its not to 			 * be unstopped by the signal alone. 			 */
if|if
condition|(
name|p
operator|->
name|p_wchan
operator|&&
name|p
operator|->
name|p_pri
operator|>
name|PZERO
condition|)
name|unsleep
argument_list|(
name|p
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/*NOTREACHED*/
default|default:
comment|/* 		 * SRUN, SIDL, SZOMB do nothing with the signal, 		 * other than kicking ourselves if we are running. 		 * It will either never be noticed, or noticed very soon. 		 */
if|if
condition|(
name|p
operator|==
name|u
operator|.
name|u_procp
operator|&&
operator|!
name|noproc
condition|)
name|aston
argument_list|()
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/*NOTREACHED*/
name|run
label|:
comment|/* 	 * Raise priority to at least PUSER. 	 */
if|if
condition|(
name|p
operator|->
name|p_pri
operator|>
name|PUSER
condition|)
name|p
operator|->
name|p_pri
operator|=
name|PUSER
expr_stmt|;
name|setrun
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|out
label|:
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Returns true if the current  * process has a signal to process.  * The signal to process is put in p_cursig.  * This is asked at least once each time a process enters the  * system (though this can usually be done without actually  * calling issig by checking the pending signal masks.)  * A signal does not do anything  * directly to a process; it sets  * a flag that asks the process to  * do something to itself.  */
end_comment

begin_macro
name|issig
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
specifier|register
name|int
name|sig
decl_stmt|;
name|int
name|sigbits
decl_stmt|,
name|mask
decl_stmt|;
name|p
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|sigbits
operator|=
name|p
operator|->
name|p_sig
operator|&
operator|~
name|p
operator|->
name|p_sigmask
expr_stmt|;
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
operator|)
operator|==
literal|0
condition|)
name|sigbits
operator|&=
operator|~
name|p
operator|->
name|p_sigignore
expr_stmt|;
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|SVFORK
condition|)
name|sigbits
operator|&=
operator|~
name|stopsigmask
expr_stmt|;
if|if
condition|(
name|sigbits
operator|==
literal|0
condition|)
break|break;
name|sig
operator|=
name|ffs
argument_list|(
operator|(
name|long
operator|)
name|sigbits
argument_list|)
expr_stmt|;
name|mask
operator|=
name|sigmask
argument_list|(
name|sig
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_sig
operator|&=
operator|~
name|mask
expr_stmt|;
comment|/* take the signal! */
name|p
operator|->
name|p_cursig
operator|=
name|sig
expr_stmt|;
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
operator|&&
operator|(
name|p
operator|->
name|p_flag
operator|&
name|SVFORK
operator|)
operator|==
literal|0
condition|)
block|{
comment|/* 			 * If traced, always stop, and stay 			 * stopped until released by the parent. 			 */
name|psignal
argument_list|(
name|p
operator|->
name|p_pptr
argument_list|,
name|SIGCHLD
argument_list|)
expr_stmt|;
do|do
block|{
name|stop
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|swtch
argument_list|()
expr_stmt|;
block|}
do|while
condition|(
operator|!
name|procxmt
argument_list|()
operator|&&
name|p
operator|->
name|p_flag
operator|&
name|STRC
condition|)
do|;
comment|/* 			 * If the traced bit got turned off, 			 * then put the signal taken above back into p_sig 			 * and go back up to the top to rescan signals. 			 * This ensures that p_sig* and u_signal are consistent. 			 */
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
operator|)
operator|==
literal|0
condition|)
block|{
name|p
operator|->
name|p_sig
operator||=
name|mask
expr_stmt|;
continue|continue;
block|}
comment|/* 			 * If parent wants us to take the signal, 			 * then it will leave it in p->p_cursig; 			 * otherwise we just look for signals again. 			 */
name|sig
operator|=
name|p
operator|->
name|p_cursig
expr_stmt|;
if|if
condition|(
name|sig
operator|==
literal|0
condition|)
continue|continue;
comment|/* 			 * If signal is being masked put it back 			 * into p_sig and look for other signals. 			 */
name|mask
operator|=
name|sigmask
argument_list|(
name|sig
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|->
name|p_sigmask
operator|&
name|mask
condition|)
block|{
name|p
operator|->
name|p_sig
operator||=
name|mask
expr_stmt|;
continue|continue;
block|}
block|}
switch|switch
condition|(
operator|(
name|int
operator|)
name|u
operator|.
name|u_signal
index|[
name|sig
index|]
condition|)
block|{
case|case
name|SIG_DFL
case|:
comment|/* 			 * Don't take default actions on system processes. 			 */
if|if
condition|(
name|p
operator|->
name|p_ppid
operator|==
literal|0
condition|)
break|break;
switch|switch
condition|(
name|sig
condition|)
block|{
case|case
name|SIGTSTP
case|:
case|case
name|SIGTTIN
case|:
case|case
name|SIGTTOU
case|:
comment|/* 				 * Children of init aren't allowed to stop 				 * on signals from the keyboard. 				 */
if|if
condition|(
name|p
operator|->
name|p_pptr
operator|==
operator|&
name|proc
index|[
literal|1
index|]
condition|)
block|{
name|psignal
argument_list|(
name|p
argument_list|,
name|SIGKILL
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* fall into ... */
case|case
name|SIGSTOP
case|:
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
condition|)
continue|continue;
name|psignal
argument_list|(
name|p
operator|->
name|p_pptr
argument_list|,
name|SIGCHLD
argument_list|)
expr_stmt|;
name|stop
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|swtch
argument_list|()
expr_stmt|;
continue|continue;
case|case
name|SIGCONT
case|:
case|case
name|SIGCHLD
case|:
case|case
name|SIGURG
case|:
case|case
name|SIGIO
case|:
case|case
name|SIGWINCH
case|:
comment|/* 				 * These signals are normally not 				 * sent if the action is the default. 				 */
continue|continue;
comment|/* == ignore */
default|default:
goto|goto
name|send
goto|;
block|}
comment|/*NOTREACHED*/
case|case
name|SIG_HOLD
case|:
case|case
name|SIG_IGN
case|:
comment|/* 			 * Masking above should prevent us 			 * ever trying to take action on a held 			 * or ignored signal, unless process is traced. 			 */
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|STRC
operator|)
operator|==
literal|0
condition|)
name|printf
argument_list|(
literal|"issig\n"
argument_list|)
expr_stmt|;
continue|continue;
default|default:
comment|/* 			 * This signal has an action, let 			 * psig process it. 			 */
goto|goto
name|send
goto|;
block|}
comment|/*NOTREACHED*/
block|}
comment|/* 	 * Didn't find a signal to send. 	 */
name|p
operator|->
name|p_cursig
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
name|send
label|:
comment|/* 	 * Let psig process the signal. 	 */
return|return
operator|(
name|sig
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Put the argument process into the stopped  * state and notify the parent via wakeup.  * Signals are handled elsewhere.  */
end_comment

begin_expr_stmt
name|stop
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|p
operator|->
name|p_stat
operator|=
name|SSTOP
expr_stmt|;
name|p
operator|->
name|p_flag
operator|&=
operator|~
name|SWTED
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|p
operator|->
name|p_pptr
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Perform the action specified by  * the current signal.  * The usual sequence is:  *	if (issig())  *		psig();  * The signal bit has already been cleared by issig,  * and the current signal number stored in p->p_cursig.  */
end_comment

begin_macro
name|psig
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|u
operator|.
name|u_procp
decl_stmt|;
specifier|register
name|int
name|sig
init|=
name|p
operator|->
name|p_cursig
decl_stmt|;
name|int
name|mask
init|=
name|sigmask
argument_list|(
name|sig
argument_list|)
decl_stmt|,
name|returnmask
decl_stmt|;
specifier|register
name|int
function_decl|(
modifier|*
name|action
function_decl|)
parameter_list|()
function_decl|;
if|if
condition|(
name|sig
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"psig"
argument_list|)
expr_stmt|;
name|action
operator|=
name|u
operator|.
name|u_signal
index|[
name|sig
index|]
expr_stmt|;
if|if
condition|(
name|action
operator|!=
name|SIG_DFL
condition|)
block|{
if|if
condition|(
name|action
operator|==
name|SIG_IGN
operator|||
operator|(
name|p
operator|->
name|p_sigmask
operator|&
name|mask
operator|)
condition|)
name|panic
argument_list|(
literal|"psig action"
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
literal|0
expr_stmt|;
comment|/* 		 * Set the new mask value and also defer further 		 * occurences of this signal (unless we're simulating 		 * the old signal facilities).  		 * 		 * Special case: user has done a sigpause.  Here the 		 * current mask is not of interest, but rather the 		 * mask from before the sigpause is what we want restored 		 * after the signal processing is completed. 		 */
operator|(
name|void
operator|)
name|splhigh
argument_list|()
expr_stmt|;
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|SOUSIG
condition|)
block|{
if|if
condition|(
name|sig
operator|!=
name|SIGILL
operator|&&
name|sig
operator|!=
name|SIGTRAP
condition|)
block|{
name|u
operator|.
name|u_signal
index|[
name|sig
index|]
operator|=
name|SIG_DFL
expr_stmt|;
name|p
operator|->
name|p_sigcatch
operator|&=
operator|~
name|mask
expr_stmt|;
block|}
name|mask
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
name|p
operator|->
name|p_flag
operator|&
name|SOMASK
condition|)
block|{
name|returnmask
operator|=
name|u
operator|.
name|u_oldmask
expr_stmt|;
name|p
operator|->
name|p_flag
operator|&=
operator|~
name|SOMASK
expr_stmt|;
block|}
else|else
name|returnmask
operator|=
name|p
operator|->
name|p_sigmask
expr_stmt|;
name|p
operator|->
name|p_sigmask
operator||=
name|u
operator|.
name|u_sigmask
index|[
name|sig
index|]
operator||
name|mask
expr_stmt|;
operator|(
name|void
operator|)
name|spl0
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_ru
operator|.
name|ru_nsignals
operator|++
expr_stmt|;
name|sendsig
argument_list|(
name|action
argument_list|,
name|sig
argument_list|,
name|returnmask
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_cursig
operator|=
literal|0
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_acflag
operator||=
name|AXSIG
expr_stmt|;
switch|switch
condition|(
name|sig
condition|)
block|{
case|case
name|SIGILL
case|:
case|case
name|SIGIOT
case|:
case|case
name|SIGBUS
case|:
case|case
name|SIGQUIT
case|:
case|case
name|SIGTRAP
case|:
case|case
name|SIGEMT
case|:
case|case
name|SIGFPE
case|:
case|case
name|SIGSEGV
case|:
case|case
name|SIGSYS
case|:
name|u
operator|.
name|u_arg
index|[
literal|0
index|]
operator|=
name|sig
expr_stmt|;
if|if
condition|(
name|core
argument_list|()
condition|)
name|sig
operator|+=
literal|0200
expr_stmt|;
block|}
name|exit
argument_list|(
name|sig
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Create a core image on the file "core"  * If you are looking for protection glitches,  * there are probably a wealth of them here  * when this occurs to a suid command.  *  * It writes UPAGES block of the  * user.h area followed by the entire  * data+stack segments.  */
end_comment

begin_macro
name|core
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
specifier|register
name|struct
name|nameidata
modifier|*
name|ndp
init|=
operator|&
name|u
operator|.
name|u_nd
decl_stmt|;
if|if
condition|(
name|u
operator|.
name|u_uid
operator|!=
name|u
operator|.
name|u_ruid
operator|||
name|u
operator|.
name|u_gid
operator|!=
name|u
operator|.
name|u_rgid
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|ctob
argument_list|(
name|UPAGES
operator|+
name|u
operator|.
name|u_dsize
operator|+
name|u
operator|.
name|u_ssize
argument_list|)
operator|>=
name|u
operator|.
name|u_rlimit
index|[
name|RLIMIT_CORE
index|]
operator|.
name|rlim_cur
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|&&
name|access
argument_list|(
name|u
operator|.
name|u_procp
operator|->
name|p_textp
operator|->
name|x_iptr
argument_list|,
name|IREAD
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|u
operator|.
name|u_error
operator|=
literal|0
expr_stmt|;
name|ndp
operator|->
name|ni_nameiop
operator|=
name|CREATE
operator||
name|FOLLOW
expr_stmt|;
name|ndp
operator|->
name|ni_segflg
operator|=
name|UIO_SYSSPACE
expr_stmt|;
name|ndp
operator|->
name|ni_dirp
operator|=
literal|"core"
expr_stmt|;
name|ip
operator|=
name|namei
argument_list|(
name|ndp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|ip
operator|=
name|maknode
argument_list|(
literal|0644
argument_list|,
name|ndp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|access
argument_list|(
name|ip
argument_list|,
name|IWRITE
argument_list|)
operator|||
operator|(
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
operator|)
operator|!=
name|IFREG
operator|||
name|ip
operator|->
name|i_nlink
operator|!=
literal|1
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
goto|goto
name|out
goto|;
block|}
ifdef|#
directive|ifdef
name|MMAP
block|{
specifier|register
name|int
name|fd
decl_stmt|;
comment|/* unmasp funky devices in the user's address space */
for|for
control|(
name|fd
operator|=
literal|0
init|;
name|fd
operator|<
name|u
operator|.
name|u_lastfile
condition|;
name|fd
operator|++
control|)
if|if
condition|(
name|u
operator|.
name|u_ofile
index|[
name|fd
index|]
operator|&&
operator|(
name|u
operator|.
name|u_pofile
index|[
name|fd
index|]
operator|&
name|UF_MAPPED
operator|)
condition|)
name|munmapfd
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
name|itrunc
argument_list|(
name|ip
argument_list|,
operator|(
name|u_long
operator|)
literal|0
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_acflag
operator||=
name|ACORE
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|rdwri
argument_list|(
name|UIO_WRITE
argument_list|,
name|ip
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|u
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|,
operator|(
name|off_t
operator|)
literal|0
argument_list|,
literal|1
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
operator|==
literal|0
condition|)
name|u
operator|.
name|u_error
operator|=
name|rdwri
argument_list|(
name|UIO_WRITE
argument_list|,
name|ip
argument_list|,
operator|(
name|caddr_t
operator|)
name|ctob
argument_list|(
name|dptov
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
literal|0
argument_list|)
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|ctob
argument_list|(
name|u
operator|.
name|u_dsize
argument_list|)
argument_list|,
operator|(
name|off_t
operator|)
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|,
literal|0
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
operator|==
literal|0
condition|)
name|u
operator|.
name|u_error
operator|=
name|rdwri
argument_list|(
name|UIO_WRITE
argument_list|,
name|ip
argument_list|,
operator|(
name|caddr_t
operator|)
name|ctob
argument_list|(
name|sptov
argument_list|(
name|u
operator|.
name|u_procp
argument_list|,
name|u
operator|.
name|u_ssize
operator|-
literal|1
argument_list|)
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|ctob
argument_list|(
name|u
operator|.
name|u_ssize
argument_list|)
argument_list|,
operator|(
name|off_t
operator|)
name|ctob
argument_list|(
name|UPAGES
argument_list|)
operator|+
name|ctob
argument_list|(
name|u
operator|.
name|u_dsize
argument_list|)
argument_list|,
literal|0
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
name|out
label|:
name|iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|u_error
operator|==
literal|0
operator|)
return|;
block|}
end_block

end_unit

