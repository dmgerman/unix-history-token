begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1983 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
name|char
name|copyright
index|[]
init|=
literal|"@(#) Copyright (c) 1983 Regents of the University of California.\n\  All rights reserved.\n"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|not lint
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)atrm.c	5.3 (Berkeley) 1/18/87"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|not lint
end_endif

begin_comment
comment|/*  *	synopsis: atrm [-f] [-i] [-] [[job #] [user] ...]  *  *  *	Remove files from the directory /usr/spool/at. These files  *	represent jobs to be run at a later date.  *  *	Author: Steve Wall  *		Computer Systems Research Group  *		University of California @ Berkeley  *  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/dir.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_define
define|#
directive|define
name|SUPERUSER
value|0
end_define

begin_comment
comment|/* is user super-user? */
end_comment

begin_define
define|#
directive|define
name|MAXENTRIES
value|1000
end_define

begin_comment
comment|/* max # of entries allowed */
end_comment

begin_define
define|#
directive|define
name|ATDIR
value|"/usr/spool/at"
end_define

begin_comment
comment|/* spooling area */
end_comment

begin_decl_stmt
name|int
name|user
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* person requesting removal */
end_comment

begin_decl_stmt
name|int
name|fflag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* suppress announcements? */
end_comment

begin_decl_stmt
name|int
name|iflag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* run interactively? */
end_comment

begin_function
name|main
parameter_list|(
name|argc
parameter_list|,
name|argv
parameter_list|)
name|int
name|argc
decl_stmt|;
name|char
modifier|*
modifier|*
name|argv
decl_stmt|;
block|{
specifier|register
name|int
name|i
decl_stmt|;
comment|/* for loop index */
name|int
name|isuname
decl_stmt|;
comment|/* is a command line argv a user name?*/
name|int
name|numjobs
decl_stmt|;
comment|/* # of jobs in spooling area */
name|int
name|usage
parameter_list|()
function_decl|;
comment|/* print usage info and exit */
name|int
name|allflag
init|=
literal|0
decl_stmt|;
comment|/* remove all jobs belonging to user? */
name|int
name|jobno
decl_stmt|;
name|int
name|jobexists
decl_stmt|;
comment|/* does a requested job exist? */
name|int
name|alphasort
parameter_list|()
function_decl|;
comment|/* sort jobs by date of execution */
name|int
name|filewanted
parameter_list|()
function_decl|;
comment|/* should a file be listed in queue? */
name|char
modifier|*
name|myname
decl_stmt|,
modifier|*
name|getname
argument_list|()
decl_stmt|;
comment|/* current user's name */
name|char
modifier|*
name|owner
decl_stmt|,
modifier|*
name|fowner
argument_list|()
decl_stmt|;
name|struct
name|stat
modifier|*
name|statptr
decl_stmt|;
comment|/* pointer to file stat structure */
name|struct
name|stat
modifier|*
name|stbuf
index|[
name|MAXENTRIES
index|]
decl_stmt|;
comment|/* array of pointers to stat structs */
name|struct
name|direct
modifier|*
modifier|*
name|namelist
decl_stmt|;
comment|/* names of jobs in spooling area */
comment|/* 	 * If job number, user name, or "-" is not specified, just print 	 * usage info and exit. 	 */
if|if
condition|(
name|argc
operator|<
literal|2
condition|)
name|usage
argument_list|()
expr_stmt|;
operator|--
name|argc
expr_stmt|;
operator|++
name|argv
expr_stmt|;
comment|/* 	 * Process command line flags. 	 * Special case the "-" option so that others may be grouped. 	 */
while|while
condition|(
name|argc
operator|>
literal|0
operator|&&
operator|*
operator|*
name|argv
operator|==
literal|'-'
condition|)
block|{
if|if
condition|(
operator|*
operator|(
operator|++
operator|(
operator|*
name|argv
operator|)
operator|)
operator|==
literal|'\0'
condition|)
block|{
operator|++
name|allflag
expr_stmt|;
block|}
else|else
while|while
condition|(
operator|*
operator|*
name|argv
condition|)
switch|switch
condition|(
operator|*
operator|(
operator|*
name|argv
operator|)
operator|++
condition|)
block|{
case|case
literal|'f'
case|:
operator|++
name|fflag
expr_stmt|;
break|break;
case|case
literal|'i'
case|:
operator|++
name|iflag
expr_stmt|;
break|break;
default|default:
name|usage
argument_list|()
expr_stmt|;
block|}
operator|++
name|argv
expr_stmt|;
operator|--
name|argc
expr_stmt|;
block|}
comment|/* 	 * If all jobs are to be removed and extra command line arguments  	 * are given, print usage info and exit. 	 */
if|if
condition|(
name|allflag
operator|&&
name|argc
condition|)
name|usage
argument_list|()
expr_stmt|;
comment|/* 	 * If only certain jobs are to be removed and no job #'s or user 	 * names are specified, print usage info and exit. 	 */
if|if
condition|(
operator|!
name|allflag
operator|&&
operator|!
name|argc
condition|)
name|usage
argument_list|()
expr_stmt|;
comment|/* 	 * If interactive removal and quiet removal are requested, override 	 * quiet removal and run interactively. 	 */
if|if
condition|(
name|iflag
operator|&&
name|fflag
condition|)
name|fflag
operator|=
literal|0
expr_stmt|;
comment|/*  	 * Move to spooling area and get user id of person requesting removal. 	 */
if|if
condition|(
name|chdir
argument_list|(
name|ATDIR
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|perror
argument_list|(
name|ATDIR
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|user
operator|=
name|getuid
argument_list|()
expr_stmt|;
name|myname
operator|=
name|getname
argument_list|(
name|user
argument_list|)
expr_stmt|;
comment|/* 	 * Get a list of the files in the spooling area. 	 */
if|if
condition|(
operator|(
name|numjobs
operator|=
name|scandir
argument_list|(
literal|"."
argument_list|,
operator|&
name|namelist
argument_list|,
name|filewanted
argument_list|,
name|alphasort
argument_list|)
operator|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
name|ATDIR
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Build an array of pointers to the file stats for all jobs in 	 * the spooling area. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|numjobs
condition|;
operator|++
name|i
control|)
block|{
name|statptr
operator|=
operator|(
expr|struct
name|stat
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|stat
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|statptr
operator|==
name|NULL
condition|)
block|{
name|perror
argument_list|(
literal|"malloc"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|stat
argument_list|(
name|namelist
index|[
name|i
index|]
operator|->
name|d_name
argument_list|,
name|statptr
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"stat"
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|stbuf
index|[
name|i
index|]
operator|=
name|statptr
expr_stmt|;
block|}
comment|/* 	 * If all jobs belonging to the user are to be removed, compare 	 * the user's id to the owner of the file. If they match, remove 	 * the file. If the user is the super-user, don't bother comparing 	 * the id's. After all files are removed, exit (status 0). 	 */
if|if
condition|(
name|allflag
condition|)
block|{
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|numjobs
condition|;
operator|++
name|i
control|)
block|{
name|owner
operator|=
name|fowner
argument_list|(
name|namelist
index|[
name|i
index|]
operator|->
name|d_name
argument_list|)
expr_stmt|;
if|if
condition|(
name|isowner
argument_list|(
name|myname
argument_list|,
name|owner
argument_list|)
condition|)
name|removentry
argument_list|(
name|namelist
index|[
name|i
index|]
operator|->
name|d_name
argument_list|,
operator|(
name|int
operator|)
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * If only certain jobs are to be removed, interpret each command 	 * line argument. A check is done to see if it is a user's name or 	 * a job number (inode #). If it's a user's name, compare the argument 	 * to the files owner. If it's a job number, compare the argument to 	 * the inode number of the file. In either case, if a match occurs, 	 * try to remove the file. (The function "isusername" scans the 	 * argument to see if it is all digits which we will assume means  	 * that it's a job number (a fairly safe assumption?). This is done 	 * because we have to determine whether we are dealing with a user 	 * name or a job number. By assuming that only arguments that are 	 * all digits is a job number, we allow users to have digits in 	 * their login name i.e. "johndoe2"). 	 */
while|while
condition|(
name|argc
operator|--
condition|)
block|{
name|jobexists
operator|=
literal|0
expr_stmt|;
name|isuname
operator|=
name|isusername
argument_list|(
operator|*
name|argv
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|isuname
condition|)
name|jobno
operator|=
name|atoi
argument_list|(
operator|*
name|argv
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|numjobs
condition|;
operator|++
name|i
control|)
block|{
comment|/* if the inode number is 0, this entry was removed */
if|if
condition|(
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
operator|==
literal|0
condition|)
continue|continue;
name|owner
operator|=
name|fowner
argument_list|(
name|namelist
index|[
name|i
index|]
operator|->
name|d_name
argument_list|)
expr_stmt|;
comment|/*  			 * if argv is a username, compare it to 			 * the owner of the file...... 			 * otherwise, we assume that the argv is a job # and 			 * thus compare argv to the inode (job #) of the file. 			 */
if|if
condition|(
name|isuname
condition|)
block|{
if|if
condition|(
name|strcmp
argument_list|(
operator|*
name|argv
argument_list|,
name|owner
argument_list|)
condition|)
continue|continue;
block|}
else|else
block|{
if|if
condition|(
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
operator|!=
name|jobno
condition|)
continue|continue;
block|}
operator|++
name|jobexists
expr_stmt|;
comment|/* 			 * if the entry is removed, don't 			 * try to remove it again later. 			 */
if|if
condition|(
name|user
operator|==
name|SUPERUSER
operator|||
name|isowner
argument_list|(
name|myname
argument_list|,
name|owner
argument_list|)
condition|)
block|{
name|removentry
argument_list|(
name|namelist
index|[
name|i
index|]
operator|->
name|d_name
argument_list|,
operator|(
name|int
operator|)
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
argument_list|,
name|owner
argument_list|)
expr_stmt|;
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
operator|=
literal|0
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|fflag
condition|)
name|printf
argument_list|(
literal|"%6d: permission denied\n"
argument_list|,
name|stbuf
index|[
name|i
index|]
operator|->
name|st_ino
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|isuname
condition|)
break|break;
block|}
comment|/* 		 * If a requested argument doesn't exist, print a message. 		 */
if|if
condition|(
operator|!
name|jobexists
operator|&&
operator|!
name|fflag
operator|&&
operator|!
name|isuname
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%6s: no such job number\n"
argument_list|,
operator|*
name|argv
argument_list|)
expr_stmt|;
block|}
operator|++
name|argv
expr_stmt|;
block|}
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Print usage info and exit.  */
end_comment

begin_macro
name|usage
argument_list|()
end_macro

begin_block
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"usage: atrm [-f] [-i] [-] [[job #] [user] ...]\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Do we want to include a file in the queue? (used by "scandir") We are looking  * for files with following syntax: yy.ddd.hhhh. so the test is made to see if   * the file name has three dots in it. This test will suffice since the only  * other files in /usr/spool/at don't have any dots in their name.  */
end_comment

begin_macro
name|filewanted
argument_list|(
argument|direntry
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|direct
modifier|*
name|direntry
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|numdot
init|=
literal|0
decl_stmt|;
comment|/* number of dots in a filename */
name|char
modifier|*
name|filename
decl_stmt|;
comment|/* filename we are looking at */
name|filename
operator|=
name|direntry
operator|->
name|d_name
expr_stmt|;
while|while
condition|(
operator|*
name|filename
condition|)
name|numdot
operator|+=
operator|(
operator|*
operator|(
name|filename
operator|++
operator|)
operator|==
literal|'.'
operator|)
expr_stmt|;
return|return
operator|(
name|numdot
operator|==
literal|3
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Is a command line argument a username? As noted above we will assume   * that an argument that is all digits means that it's a job number, not  * a user's name. We choose to determine whether an argument is a user name  * in this manner because then it's ok for someone to have digits in their   * user name.  */
end_comment

begin_macro
name|isusername
argument_list|(
argument|string
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|string
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|ptr
decl_stmt|;
comment|/* pointer used for scanning string */
name|ptr
operator|=
name|string
expr_stmt|;
while|while
condition|(
name|isdigit
argument_list|(
operator|*
name|ptr
argument_list|)
condition|)
operator|++
name|ptr
expr_stmt|;
return|return
operator|(
operator|(
operator|*
name|ptr
operator|==
literal|'\0'
operator|)
condition|?
literal|0
else|:
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Remove an entry from the queue. The access of the file is checked for  * write permission (since all jobs are mode 644). If access is granted,  * unlink the file. If the fflag (suppress announcements) is not set,  * print the job number that we are removing and the result of the access  * check (either "permission denied" or "removed"). If we are running   * interactively (iflag), prompt the user before we unlink the file. If   * the super-user is removing jobs, inform him/her who owns each file before   * it is removed.  */
end_comment

begin_macro
name|removentry
argument_list|(
argument|filename
argument_list|,
argument|inode
argument_list|,
argument|owner
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|filename
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|inode
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|owner
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
operator|!
name|fflag
condition|)
name|printf
argument_list|(
literal|"%6d: "
argument_list|,
name|inode
argument_list|)
expr_stmt|;
if|if
condition|(
name|iflag
condition|)
block|{
if|if
condition|(
name|user
operator|==
name|SUPERUSER
operator|&&
name|owner
condition|)
name|printf
argument_list|(
literal|"\t(owned by %s) "
argument_list|,
name|owner
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"remove? "
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|yes
argument_list|()
condition|)
return|return;
block|}
if|if
condition|(
name|unlink
argument_list|(
name|filename
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
name|filename
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
operator|!
name|fflag
operator|&&
operator|!
name|iflag
condition|)
name|printf
argument_list|(
literal|"removed\n"
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * See if "name" owns job owned by "jobname".  */
end_comment

begin_macro
name|isowner
argument_list|(
argument|name
argument_list|,
argument|jobname
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|name
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|jobname
decl_stmt|;
end_decl_stmt

begin_block
block|{
return|return
operator|(
name|strcmp
argument_list|(
name|name
argument_list|,
name|jobname
argument_list|)
operator|==
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Return the owner of the job. This is stored on the first line of the  * spoolfile. If we run into trouble getting the name, we'll just return "???".  */
end_comment

begin_function
name|char
modifier|*
name|fowner
parameter_list|(
name|file
parameter_list|)
name|char
modifier|*
name|file
decl_stmt|;
block|{
specifier|static
name|char
name|owner
index|[
literal|128
index|]
decl_stmt|;
comment|/* the owner */
name|FILE
modifier|*
name|infile
decl_stmt|;
comment|/* I/O stream to spoolfile */
comment|/* 	 * Open the job file and grab the first line. 	 */
if|if
condition|(
operator|(
name|infile
operator|=
name|fopen
argument_list|(
name|file
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|perror
argument_list|(
name|file
argument_list|)
expr_stmt|;
return|return
operator|(
literal|"???"
operator|)
return|;
block|}
if|if
condition|(
name|fscanf
argument_list|(
name|infile
argument_list|,
literal|"# owner: %127s%*[^\n]\n"
argument_list|,
name|owner
argument_list|)
operator|!=
literal|1
condition|)
block|{
name|fclose
argument_list|(
name|infile
argument_list|)
expr_stmt|;
return|return
operator|(
literal|"???"
operator|)
return|;
block|}
name|fclose
argument_list|(
name|infile
argument_list|)
expr_stmt|;
return|return
operator|(
name|owner
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Get answer to interactive prompts, eating all characters beyond the first  * one. If a 'y' is typed, return 1.  */
end_comment

begin_macro
name|yes
argument_list|()
end_macro

begin_block
block|{
name|char
name|ch
decl_stmt|;
comment|/* dummy variable */
name|char
name|ch1
decl_stmt|;
comment|/* dummy variable */
name|ch
operator|=
name|ch1
operator|=
name|getchar
argument_list|()
expr_stmt|;
while|while
condition|(
name|ch1
operator|!=
literal|'\n'
operator|&&
name|ch1
operator|!=
name|EOF
condition|)
name|ch1
operator|=
name|getchar
argument_list|()
expr_stmt|;
if|if
condition|(
name|isupper
argument_list|(
name|ch
argument_list|)
condition|)
name|ch
operator|=
name|tolower
argument_list|(
name|ch
argument_list|)
expr_stmt|;
return|return
operator|(
name|ch
operator|==
literal|'y'
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Get the uid of a person using his/her login name. Return -1 if no  * such account name exists.  */
end_comment

begin_macro
name|getid
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|name
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|passwd
modifier|*
name|pwdinfo
decl_stmt|;
comment|/* password info structure */
if|if
condition|(
operator|(
name|pwdinfo
operator|=
name|getpwnam
argument_list|(
name|name
argument_list|)
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
return|return
operator|(
name|pwdinfo
operator|->
name|pw_uid
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Get the full login name of a person using his/her user id.  */
end_comment

begin_function
name|char
modifier|*
name|getname
parameter_list|(
name|uid
parameter_list|)
name|int
name|uid
decl_stmt|;
block|{
name|struct
name|passwd
modifier|*
name|pwdinfo
decl_stmt|;
comment|/* password info structure */
name|char
modifier|*
name|logname
decl_stmt|,
modifier|*
name|getlogin
argument_list|()
decl_stmt|;
name|logname
operator|=
name|getlogin
argument_list|()
expr_stmt|;
if|if
condition|(
name|logname
operator|==
name|NULL
operator|||
operator|(
name|pwdinfo
operator|=
name|getpwnam
argument_list|(
name|logname
argument_list|)
operator|)
operator|==
name|NULL
operator|||
name|pwdinfo
operator|->
name|pw_uid
operator|!=
name|uid
condition|)
name|pwdinfo
operator|=
name|getpwuid
argument_list|(
name|uid
argument_list|)
expr_stmt|;
if|if
condition|(
name|pwdinfo
operator|==
literal|0
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"no name for uid %d?\n"
argument_list|,
name|uid
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|pwdinfo
operator|->
name|pw_name
operator|)
return|;
block|}
end_function

end_unit

