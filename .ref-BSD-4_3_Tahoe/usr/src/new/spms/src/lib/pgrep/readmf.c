begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $Header$ */
end_comment

begin_comment
comment|/*  * Author: Peter J. Nicklin  */
end_comment

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"macro.h"
end_include

begin_include
include|#
directive|include
file|"null.h"
end_include

begin_include
include|#
directive|include
file|"pgrep.h"
end_include

begin_include
include|#
directive|include
file|"slist.h"
end_include

begin_include
include|#
directive|include
file|"yesno.h"
end_include

begin_define
define|#
directive|define
name|DEPENDMARK
value|"###"
end_define

begin_decl_stmt
specifier|static
name|char
name|Iobuf
index|[
name|BUFSIZ
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* I/O line buffer */
end_comment

begin_decl_stmt
specifier|static
name|short
name|Continue
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* does the line continue? */
end_comment

begin_comment
comment|/*  * dependmark() returns 1 if the current input line begins with a string  * marking the beginning of generated dependency lines, otherwise zero.  */
end_comment

begin_macro
name|dependmark
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
name|EQUAL
argument_list|(
name|Iobuf
argument_list|,
name|DEPENDMARK
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * findmacro() searchs a line for a macro definition. Returns the name,  * or null if not found.  */
end_comment

begin_function
name|char
modifier|*
name|findmacro
parameter_list|(
name|macroname
parameter_list|)
name|char
modifier|*
name|macroname
decl_stmt|;
comment|/* macro name receiving buffer */
block|{
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
comment|/* buffer pointer */
specifier|register
name|char
modifier|*
name|mp
decl_stmt|;
comment|/* macro name pointer */
name|bp
operator|=
name|Iobuf
expr_stmt|;
name|mp
operator|=
name|macroname
expr_stmt|;
while|while
condition|(
operator|*
name|bp
operator|==
literal|' '
condition|)
name|bp
operator|++
expr_stmt|;
if|if
condition|(
operator|!
name|isalnum
argument_list|(
operator|*
name|bp
argument_list|)
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
while|while
condition|(
name|isalnum
argument_list|(
operator|*
name|bp
argument_list|)
condition|)
operator|*
name|mp
operator|++
operator|=
operator|*
name|bp
operator|++
expr_stmt|;
operator|*
name|mp
operator|=
literal|'\0'
expr_stmt|;
while|while
condition|(
operator|*
name|bp
operator|==
literal|' '
operator|||
operator|*
name|bp
operator|==
literal|'\t'
condition|)
name|bp
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|bp
operator|!=
literal|'='
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
return|return
operator|(
name|macroname
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * getlin() stores a line from input stream in Iobuf. The string is terminated  * by a newline character which is replaced by a null character. getlin()  * returns Iobuf, or null pointer upon end of file.  */
end_comment

begin_function
name|char
modifier|*
name|getlin
parameter_list|(
name|stream
parameter_list|)
specifier|register
name|FILE
modifier|*
name|stream
decl_stmt|;
comment|/* input stream */
block|{
specifier|register
name|int
name|c
decl_stmt|;
comment|/* current character */
specifier|register
name|char
modifier|*
name|iop
decl_stmt|;
comment|/* Iobuf pointer */
name|iop
operator|=
name|Iobuf
expr_stmt|;
while|while
condition|(
operator|(
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
operator|)
operator|!=
literal|'\n'
operator|&&
name|c
operator|!=
name|EOF
condition|)
operator|*
name|iop
operator|++
operator|=
name|c
expr_stmt|;
if|if
condition|(
name|c
operator|==
name|EOF
operator|&&
name|iop
operator|==
name|Iobuf
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
if|if
condition|(
name|iop
operator|!=
name|Iobuf
operator|&&
name|iop
index|[
operator|-
literal|1
index|]
operator|==
literal|'\\'
condition|)
block|{
name|iop
index|[
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|Continue
operator|=
name|YES
expr_stmt|;
block|}
else|else
block|{
name|iop
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
name|Continue
operator|=
name|NO
expr_stmt|;
block|}
return|return
operator|(
name|Iobuf
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * getmacro() loads the body of a macro definition into slist and returns  * a pointer to slist. If the macro definition continues on more than  * one line further lines are fetched from the input stream. Returns NO  * if out of memory, otherwise YES.  */
end_comment

begin_expr_stmt
name|getmacro
argument_list|(
name|slist
argument_list|,
name|stream
argument_list|)
specifier|register
name|SLIST
operator|*
name|slist
expr_stmt|;
end_expr_stmt

begin_comment
comment|/* singly-linked list */
end_comment

begin_decl_stmt
specifier|register
name|FILE
modifier|*
name|stream
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* input stream */
end_comment

begin_block
block|{
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
comment|/* buffer pointer */
name|char
name|mdefbuf
index|[
name|MACRODEFSIZE
index|]
decl_stmt|;
comment|/* macro definition buffer */
name|char
modifier|*
name|getlin
parameter_list|()
function_decl|;
comment|/* get a line from input stream */
name|char
modifier|*
name|gettoken
parameter_list|()
function_decl|;
comment|/* get next token */
name|char
modifier|*
name|slappend
parameter_list|()
function_decl|;
comment|/* append key */
name|bp
operator|=
name|Iobuf
expr_stmt|;
while|while
condition|(
operator|*
name|bp
operator|++
operator|!=
literal|'='
condition|)
continue|continue;
while|while
condition|(
operator|(
name|bp
operator|=
name|gettoken
argument_list|(
name|mdefbuf
argument_list|,
name|bp
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
if|if
condition|(
name|slappend
argument_list|(
name|mdefbuf
argument_list|,
name|slist
argument_list|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|NO
operator|)
return|;
while|while
condition|(
name|Continue
operator|==
name|YES
condition|)
block|{
if|if
condition|(
name|getlin
argument_list|(
name|stream
argument_list|)
operator|==
name|NULL
condition|)
break|break;
name|bp
operator|=
name|Iobuf
expr_stmt|;
while|while
condition|(
operator|(
name|bp
operator|=
name|gettoken
argument_list|(
name|mdefbuf
argument_list|,
name|bp
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
if|if
condition|(
name|slappend
argument_list|(
name|mdefbuf
argument_list|,
name|slist
argument_list|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|NO
operator|)
return|;
block|}
return|return
operator|(
name|YES
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * gettoken() copies the next token from token buffer to token. Returns a  * pointer to the first character after the token, or null upon reaching  * the end of the token buffer.  */
end_comment

begin_function
name|char
modifier|*
name|gettoken
parameter_list|(
name|token
parameter_list|,
name|tp
parameter_list|)
specifier|register
name|char
modifier|*
name|token
decl_stmt|;
comment|/* receiving token */
specifier|register
name|char
modifier|*
name|tp
decl_stmt|;
comment|/* token buffer pointer */
block|{
while|while
condition|(
name|isspace
argument_list|(
operator|*
name|tp
argument_list|)
operator|&&
operator|*
name|tp
operator|!=
literal|'\0'
condition|)
name|tp
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|tp
operator|==
literal|'\0'
condition|)
block|{
operator|*
name|token
operator|=
literal|'\0'
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
while|while
condition|(
operator|!
name|isspace
argument_list|(
operator|*
name|tp
argument_list|)
operator|&&
operator|*
name|tp
operator|!=
literal|'\0'
condition|)
operator|*
name|token
operator|++
operator|=
operator|*
name|tp
operator|++
expr_stmt|;
operator|*
name|token
operator|=
literal|'\0'
expr_stmt|;
return|return
operator|(
name|tp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * readmf() reads a makefile and loads file names from HEADERS and SOURCE  * macros into a singly-linked list. Returns NO on error, otherwise YES.  */
end_comment

begin_macro
name|readmf
argument_list|(
argument|mfname
argument_list|,
argument|filelist
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|mfname
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* name of makefile */
end_comment

begin_decl_stmt
name|SLIST
modifier|*
name|filelist
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* file name list */
end_comment

begin_block
block|{
name|char
modifier|*
name|findmacro
parameter_list|()
function_decl|;
comment|/* is the line a macro definition? */
name|char
modifier|*
name|getlin
parameter_list|()
function_decl|;
comment|/* get a line from input stream */
name|char
name|macroname
index|[
name|MACRONAMSIZE
index|]
decl_stmt|;
comment|/* macro name buffer */
name|FILE
modifier|*
name|fopen
parameter_list|()
function_decl|;
comment|/* open file */
name|FILE
modifier|*
name|fp
decl_stmt|;
comment|/* file pointer */
name|int
name|dependmark
parameter_list|()
function_decl|;
comment|/* generated dependency line? */
name|int
name|getmacro
parameter_list|()
function_decl|;
comment|/* get macro def from input stream */
name|SLIST
modifier|*
name|headlist
decl_stmt|;
comment|/* header file name list */
name|SLIST
modifier|*
name|slinit
parameter_list|()
function_decl|;
comment|/* initialize list */
name|SLIST
modifier|*
name|srclist
decl_stmt|;
comment|/* source file name list */
name|void
name|slsplice
parameter_list|()
function_decl|;
comment|/* splice lists together */
name|headlist
operator|=
name|slinit
argument_list|()
expr_stmt|;
name|srclist
operator|=
name|slinit
argument_list|()
expr_stmt|;
if|if
condition|(
operator|(
name|fp
operator|=
name|fopen
argument_list|(
name|mfname
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|pperror
argument_list|(
name|mfname
argument_list|)
expr_stmt|;
return|return
operator|(
name|NO
operator|)
return|;
block|}
while|while
condition|(
name|getlin
argument_list|(
name|fp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|dependmark
argument_list|()
condition|)
break|break;
if|if
condition|(
name|findmacro
argument_list|(
name|macroname
argument_list|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|EQUAL
argument_list|(
name|macroname
argument_list|,
name|MHEADERS
argument_list|)
condition|)
block|{
if|if
condition|(
name|getmacro
argument_list|(
name|headlist
argument_list|,
name|fp
argument_list|)
operator|==
name|NO
condition|)
return|return
operator|(
name|NO
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|EQUAL
argument_list|(
name|macroname
argument_list|,
name|MSOURCE
argument_list|)
condition|)
block|{
if|if
condition|(
name|getmacro
argument_list|(
name|srclist
argument_list|,
name|fp
argument_list|)
operator|==
name|NO
condition|)
return|return
operator|(
name|NO
operator|)
return|;
block|}
block|}
block|}
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
comment|/* splice header files to file list */
name|slsplice
argument_list|(
name|filelist
argument_list|,
name|headlist
argument_list|)
expr_stmt|;
comment|/* splice source files to file list */
name|slsplice
argument_list|(
name|filelist
argument_list|,
name|srclist
argument_list|)
expr_stmt|;
return|return
operator|(
name|YES
operator|)
return|;
block|}
end_block

end_unit

