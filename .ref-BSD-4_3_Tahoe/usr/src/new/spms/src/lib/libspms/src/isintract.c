begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $Header$ */
end_comment

begin_comment
comment|/*  * Author: Peter J. Nicklin  */
end_comment

begin_comment
comment|/*  * isintract() determines whether a process is a foreground job, and  * standard input and output streams are connected to a terminal.  * Returns integer YES if it is, otherwise NO.  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_include
include|#
directive|include
file|"system.h"
end_include

begin_include
include|#
directive|include
file|"yesno.h"
end_include

begin_macro
name|isintract
argument_list|()
end_macro

begin_block
block|{
name|int
name|intract
init|=
name|NO
decl_stmt|;
comment|/* is interactive? */
ifdef|#
directive|ifdef
name|V4BSD
name|int
name|isatty
parameter_list|()
function_decl|;
comment|/* is stream a terminal? */
name|int
name|tpgrp
decl_stmt|;
comment|/* terminal process group */
name|int
function_decl|(
modifier|*
name|tstpstat
function_decl|)
parameter_list|()
function_decl|;
comment|/* stop signal status */
name|int
function_decl|(
modifier|*
name|ttinstat
function_decl|)
parameter_list|()
function_decl|;
comment|/* background read status */
name|int
function_decl|(
modifier|*
name|ttoustat
function_decl|)
parameter_list|()
function_decl|;
comment|/* background write status */
name|tstpstat
operator|=
name|signal
argument_list|(
name|SIGTSTP
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|ttinstat
operator|=
name|signal
argument_list|(
name|SIGTTIN
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|ttoustat
operator|=
name|signal
argument_list|(
name|SIGTTOU
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
if|if
condition|(
name|isatty
argument_list|(
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|)
operator|&&
name|isatty
argument_list|(
name|fileno
argument_list|(
name|stdout
argument_list|)
argument_list|)
condition|)
block|{
name|ioctl
argument_list|(
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|,
name|TIOCGPGRP
argument_list|,
operator|&
name|tpgrp
argument_list|)
expr_stmt|;
if|if
condition|(
name|tpgrp
operator|==
name|getpgrp
argument_list|(
literal|0
argument_list|)
condition|)
name|intract
operator|=
name|YES
expr_stmt|;
block|}
name|signal
argument_list|(
name|SIGTSTP
argument_list|,
name|tstpstat
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTIN
argument_list|,
name|ttinstat
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTOU
argument_list|,
name|ttoustat
argument_list|)
expr_stmt|;
else|#
directive|else
name|int
name|isatty
parameter_list|()
function_decl|;
comment|/* is stream a terminal? */
if|if
condition|(
name|isatty
argument_list|(
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|)
operator|&&
name|isatty
argument_list|(
name|fileno
argument_list|(
name|stdout
argument_list|)
argument_list|)
condition|)
name|intract
operator|=
name|YES
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|intract
operator|)
return|;
block|}
end_block

end_unit

