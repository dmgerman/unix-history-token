begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $Header$ */
end_comment

begin_comment
comment|/*  * Author: Peter J. Nicklin  */
end_comment

begin_comment
comment|/*  * slsprepend() saves a null-terminated key + string somewhere and inserts a  * pointer to the key at the head of list slslist. Returns a pointer to  * the somewhere, or a null pointer if out of memory.  */
end_comment

begin_include
include|#
directive|include
file|"macro.h"
end_include

begin_include
include|#
directive|include
file|"null.h"
end_include

begin_include
include|#
directive|include
file|"slslist.h"
end_include

begin_function
name|char
modifier|*
name|slsprepend
parameter_list|(
name|key
parameter_list|,
name|string
parameter_list|,
name|slslist
parameter_list|)
name|char
modifier|*
name|key
decl_stmt|;
comment|/* key string */
name|char
modifier|*
name|string
decl_stmt|;
comment|/* non-key string */
name|SLSLIST
modifier|*
name|slslist
decl_stmt|;
comment|/* pointer to list head block */
block|{
name|char
modifier|*
name|malloc
parameter_list|()
function_decl|;
comment|/* memory allocator */
name|char
modifier|*
name|strcpy
parameter_list|()
function_decl|;
comment|/* string copy */
name|int
name|strlen
parameter_list|()
function_decl|;
comment|/* string length */
name|SLSBLK
modifier|*
name|slsbptr
decl_stmt|;
comment|/* pointer to list block */
name|unsigned
name|int
name|klen
decl_stmt|;
comment|/* key length */
name|unsigned
name|int
name|slen
decl_stmt|;
comment|/* string length */
if|if
condition|(
name|slslist
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|klen
operator|=
name|strlen
argument_list|(
name|key
argument_list|)
expr_stmt|;
name|slen
operator|=
name|strlen
argument_list|(
name|string
argument_list|)
expr_stmt|;
name|slslist
operator|->
name|maxkey
operator|=
name|MAX
argument_list|(
name|slslist
operator|->
name|maxkey
argument_list|,
name|klen
argument_list|)
expr_stmt|;
name|slslist
operator|->
name|maxstr
operator|=
name|MAX
argument_list|(
name|slslist
operator|->
name|maxstr
argument_list|,
name|slen
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|slsbptr
operator|=
operator|(
name|SLSBLK
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
name|SLSBLK
argument_list|)
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|(
name|slsbptr
operator|->
name|key
operator|=
name|malloc
argument_list|(
name|klen
operator|+
literal|1
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|(
name|slsbptr
operator|->
name|string
operator|=
name|malloc
argument_list|(
name|slen
operator|+
literal|1
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|warn
argument_list|(
literal|"out of memory"
argument_list|)
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
name|strcpy
argument_list|(
name|slsbptr
operator|->
name|key
argument_list|,
name|key
argument_list|)
expr_stmt|;
name|strcpy
argument_list|(
name|slsbptr
operator|->
name|string
argument_list|,
name|string
argument_list|)
expr_stmt|;
if|if
condition|(
name|slslist
operator|->
name|head
operator|==
name|NULL
condition|)
block|{
name|slsbptr
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
name|slslist
operator|->
name|head
operator|=
name|slslist
operator|->
name|tail
operator|=
name|slsbptr
expr_stmt|;
block|}
else|else
block|{
name|slsbptr
operator|->
name|next
operator|=
name|slslist
operator|->
name|head
expr_stmt|;
name|slslist
operator|->
name|head
operator|=
name|slsbptr
expr_stmt|;
block|}
name|slslist
operator|->
name|nk
operator|++
expr_stmt|;
return|return
operator|(
name|slsbptr
operator|->
name|key
operator|)
return|;
block|}
end_function

end_unit

