begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|sccsid
init|=
literal|"@(#)misc.c	1.20	(Berkeley) 10/15/87"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"../common/conf.h"
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_comment
comment|/*  * open_valid_art -- determine if a given article name is valid;  *		if it is, return a file pointer to the open article,  *		along with a unique id of the article.  *  *	Parameters:	"artname" is a string containing the  *			name of the article.  *			"id" is space for us to put the article  *			id in.  *  *	Returns:	File pointer to the open article if the  *			article is valid; NULL otherwise  *  *	Side effects:	None.  */
end_comment

begin_function
name|FILE
modifier|*
name|open_valid_art
parameter_list|(
name|artname
parameter_list|,
name|id
parameter_list|)
name|char
modifier|*
name|artname
decl_stmt|;
name|char
modifier|*
name|id
decl_stmt|;
block|{
specifier|static
name|int
name|crnt_art_num
decl_stmt|;
specifier|static
name|char
name|crnt_art_id
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|struct
name|stat
name|statbuf
decl_stmt|;
if|if
condition|(
name|art_fp
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|crnt_art_num
operator|==
name|atoi
argument_list|(
name|artname
argument_list|)
condition|)
block|{
if|if
condition|(
name|fseek
argument_list|(
name|art_fp
argument_list|,
operator|(
name|long
operator|)
literal|0
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
name|close_crnt
argument_list|()
expr_stmt|;
else|else
block|{
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|id
argument_list|,
name|crnt_art_id
argument_list|)
expr_stmt|;
return|return
operator|(
name|art_fp
operator|)
return|;
block|}
block|}
else|else
name|close_crnt
argument_list|()
expr_stmt|;
block|}
name|art_fp
operator|=
name|fopen
argument_list|(
name|artname
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|art_fp
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|fd
operator|=
name|fileno
argument_list|(
name|art_fp
argument_list|)
expr_stmt|;
if|if
condition|(
name|fstat
argument_list|(
name|fd
argument_list|,
operator|&
name|statbuf
argument_list|)
operator|<
literal|0
condition|)
block|{
name|close_crnt
argument_list|()
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
if|if
condition|(
operator|(
name|statbuf
operator|.
name|st_mode
operator|&
name|S_IFREG
operator|)
operator|!=
name|S_IFREG
condition|)
block|{
name|close_crnt
argument_list|()
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
name|get_id
argument_list|(
name|art_fp
argument_list|,
name|id
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|crnt_art_id
argument_list|,
name|id
argument_list|)
expr_stmt|;
name|crnt_art_num
operator|=
name|atoi
argument_list|(
name|artname
argument_list|)
expr_stmt|;
return|return
operator|(
name|art_fp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * gethistent -- return the path name of an article if it's  * in the history file.  *  *	Parameters:	"msg_id" is the message ID of the  *			article, enclosed in<>'s.  *  *	Returns:	A char pointer to a static data area  *			containing the full pathname of the  *			article, or NULL if the message-id is not  *			in thef history file.  *  *	Side effects:	opens dbm database  *			(only once, keeps it open after that).  *			Converts "msg_id" to lower case.  */
end_comment

begin_function
name|char
modifier|*
name|gethistent
parameter_list|(
name|msg_id
parameter_list|)
name|char
modifier|*
name|msg_id
decl_stmt|;
block|{
name|char
name|line
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
name|char
modifier|*
name|tmp
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
name|long
name|ltmp
decl_stmt|;
specifier|static
name|char
name|path
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
ifdef|#
directive|ifdef
name|DBM
specifier|static
name|int
name|dbopen
init|=
literal|0
decl_stmt|;
name|datum
name|fetch
parameter_list|()
function_decl|;
else|#
directive|else
specifier|static
name|DBM
modifier|*
name|db
init|=
name|NULL
decl_stmt|;
comment|/* History file, dbm version */
endif|#
directive|endif
specifier|static
name|FILE
modifier|*
name|hfp
init|=
name|NULL
decl_stmt|;
comment|/* history file, text version */
name|datum
name|key
decl_stmt|,
name|content
decl_stmt|;
ifdef|#
directive|ifdef
name|DBM
if|if
condition|(
operator|!
name|dbopen
condition|)
block|{
if|if
condition|(
name|dbminit
argument_list|(
name|historyfile
argument_list|)
operator|<
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"openartbyid: dbminit %s: %m"
argument_list|,
name|historyfile
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|SYSLOG
return|return
operator|(
name|NULL
operator|)
return|;
block|}
else|else
name|dbopen
operator|=
literal|1
expr_stmt|;
block|}
else|#
directive|else
comment|/* ndbm */
if|if
condition|(
name|db
operator|==
name|NULL
condition|)
block|{
name|db
operator|=
name|dbm_open
argument_list|(
name|historyfile
argument_list|,
name|O_RDONLY
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|db
operator|==
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"openartbyid: dbm_open %s: %m"
argument_list|,
name|historyfile
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|SYSLOG
return|return
operator|(
name|NULL
operator|)
return|;
block|}
block|}
endif|#
directive|endif
endif|DBM
for|for
control|(
name|cp
operator|=
name|msg_id
init|;
operator|*
name|cp
operator|!=
literal|'\0'
condition|;
operator|++
name|cp
control|)
if|if
condition|(
name|isupper
argument_list|(
operator|*
name|cp
argument_list|)
condition|)
operator|*
name|cp
operator|=
name|tolower
argument_list|(
operator|*
name|cp
argument_list|)
expr_stmt|;
name|key
operator|.
name|dptr
operator|=
name|msg_id
expr_stmt|;
name|key
operator|.
name|dsize
operator|=
name|strlen
argument_list|(
name|msg_id
argument_list|)
operator|+
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|DBM
name|content
operator|=
name|fetch
argument_list|(
name|key
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* ndbm */
name|content
operator|=
name|dbm_fetch
argument_list|(
name|db
argument_list|,
name|key
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DBM
if|if
condition|(
name|content
operator|.
name|dptr
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
if|if
condition|(
name|hfp
operator|==
name|NULL
condition|)
block|{
name|hfp
operator|=
name|fopen
argument_list|(
name|historyfile
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|hfp
operator|==
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"message: fopen %s: %m"
argument_list|,
name|historyfile
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|SYSLOG
return|return
operator|(
name|NULL
operator|)
return|;
block|}
block|}
name|bcopy
argument_list|(
name|content
operator|.
name|dptr
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|ltmp
argument_list|,
sizeof|sizeof
argument_list|(
name|long
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|fseek
argument_list|(
name|hfp
argument_list|,
name|ltmp
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"message: fseek: %m"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|SYSLOG
return|return
operator|(
name|NULL
operator|)
return|;
block|}
operator|(
name|void
operator|)
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
argument_list|,
name|hfp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|line
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
name|cp
operator|=
name|index
argument_list|(
name|line
argument_list|,
literal|'\t'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|!=
name|NULL
condition|)
name|cp
operator|=
name|index
argument_list|(
name|cp
operator|+
literal|1
argument_list|,
literal|'\t'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"message: malformed line in history file at %ld bytes, id %s"
argument_list|,
name|ltmp
argument_list|,
name|msg_id
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|SYSLOG
return|return
operator|(
name|NULL
operator|)
return|;
block|}
name|tmp
operator|=
name|cp
operator|+
literal|1
expr_stmt|;
if|if
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|tmp
argument_list|,
literal|' '
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
while|while
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|tmp
argument_list|,
literal|'.'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'/'
expr_stmt|;
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|path
argument_list|,
name|spooldir
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|path
argument_list|,
literal|"/"
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|path
argument_list|,
name|tmp
argument_list|)
expr_stmt|;
return|return
operator|(
name|path
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * openartbyid -- open an article by message-id.  *  *	Arguments:	"msg_id" is the message-id of the article  *			to open.  *  *	Returns:	File pointer to opened article, or NULL if  *			the article was not in the history file or  *			could not be opened.  *  *	Side effects:	Opens article.  */
end_comment

begin_function
name|FILE
modifier|*
name|openartbyid
parameter_list|(
name|msg_id
parameter_list|)
name|char
modifier|*
name|msg_id
decl_stmt|;
block|{
name|char
modifier|*
name|path
decl_stmt|;
name|path
operator|=
name|gethistent
argument_list|(
name|msg_id
argument_list|)
expr_stmt|;
if|if
condition|(
name|path
operator|!=
name|NULL
condition|)
return|return
operator|(
name|fopen
argument_list|(
name|path
argument_list|,
literal|"r"
argument_list|)
operator|)
return|;
else|else
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * check_ngperm -- check to see if they're allowed to see this  * article by matching Newsgroups: and Distribution: line.  *  *	Parameters:	"fp" is the file pointer of this article.  *  *	Returns:	0 if they're not allowed to see it.  *			1 if they are.  *  *	Side effects:	None.  */
end_comment

begin_expr_stmt
name|check_ngperm
argument_list|(
name|fp
argument_list|)
specifier|register
name|FILE
operator|*
name|fp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|char
name|buf
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
specifier|static
name|char
modifier|*
modifier|*
name|ngarray
decl_stmt|;
name|int
name|ngcount
decl_stmt|;
if|if
condition|(
name|ngpermcount
operator|==
literal|0
condition|)
return|return
operator|(
literal|1
operator|)
return|;
while|while
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|fp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|buf
index|[
literal|0
index|]
operator|==
literal|'\n'
condition|)
comment|/* End of header */
break|break;
if|if
condition|(
name|buf
index|[
literal|0
index|]
operator|!=
literal|'N'
operator|&&
name|buf
index|[
literal|0
index|]
operator|!=
literal|'n'
condition|)
continue|continue;
name|cp
operator|=
name|index
argument_list|(
name|buf
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
name|cp
operator|=
name|index
argument_list|(
name|buf
argument_list|,
literal|':'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
name|NULL
condition|)
continue|continue;
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|streql
argument_list|(
name|buf
argument_list|,
literal|"newsgroups"
argument_list|)
condition|)
block|{
name|ngcount
operator|=
name|get_nglist
argument_list|(
operator|&
name|ngarray
argument_list|,
name|cp
operator|+
literal|2
argument_list|)
expr_stmt|;
break|break;
block|}
block|}
operator|(
name|void
operator|)
name|rewind
argument_list|(
name|fp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ngcount
operator|==
literal|0
condition|)
comment|/* Either no newgroups or null entry */
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
name|ngmatch
argument_list|(
name|strneql
argument_list|,
name|ALLBUT
argument_list|,
name|ngpermlist
argument_list|,
name|ngpermcount
argument_list|,
name|ngarray
argument_list|,
name|ngcount
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * spew -- spew out the contents of a file to stdout, doing  * the necessary cr-lf additions at the end.  Finish with  * a "." on a line by itself, and an fflush(stdout).  *  *	Parameters:	"how" tells what part of the file we  *			want spewed:  *				ARTICLE   The entire thing.  *				HEAD	  Just the first part.  *				BODY	  Just the second part.  *			"fp" is the open file to spew from.  *  *	Returns:	Nothing.  *  *	Side effects:	Changes current position in file.  */
end_comment

begin_macro
name|spew
argument_list|(
argument|fp
argument_list|,
argument|how
argument_list|)
end_macro

begin_decl_stmt
name|FILE
modifier|*
name|fp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|how
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|line
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
ifdef|#
directive|ifdef
name|LOG
operator|++
name|arts_acsd
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|how
operator|==
name|STAT
condition|)
block|{
operator|(
name|void
operator|)
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
return|return;
block|}
while|while
condition|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
operator|-
literal|6
argument_list|,
name|fp
argument_list|)
operator|!=
name|NULL
operator|&&
operator|*
name|line
operator|!=
literal|'\n'
condition|)
block|{
if|if
condition|(
name|how
operator|==
name|BODY
condition|)
comment|/* We need to skip this anyway */
continue|continue;
name|cp
operator|=
name|index
argument_list|(
name|line
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
operator|*
name|line
operator|==
literal|'.'
condition|)
name|putchar
argument_list|(
literal|'.'
argument_list|)
expr_stmt|;
name|putline
argument_list|(
name|line
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
name|NULL
condition|)
block|{
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
operator|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
operator|-
literal|6
argument_list|,
name|fp
argument_list|)
operator|==
name|NULL
operator|)
operator|||
operator|(
name|index
argument_list|(
name|line
argument_list|,
literal|'\n'
argument_list|)
operator|!=
name|NULL
operator|)
condition|)
break|break;
block|}
block|}
block|}
if|if
condition|(
name|how
operator|==
name|HEAD
condition|)
block|{
name|putchar
argument_list|(
literal|'.'
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\r'
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\n'
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
return|return;
block|}
elseif|else
if|if
condition|(
name|how
operator|==
name|ARTICLE
condition|)
block|{
name|putchar
argument_list|(
literal|'\r'
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\n'
argument_list|)
expr_stmt|;
block|}
while|while
condition|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
operator|-
literal|6
argument_list|,
name|fp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
name|cp
operator|=
name|index
argument_list|(
name|line
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
operator|*
name|line
operator|==
literal|'.'
condition|)
name|putchar
argument_list|(
literal|'.'
argument_list|)
expr_stmt|;
name|putline
argument_list|(
name|line
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
name|NULL
condition|)
block|{
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
operator|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
operator|-
literal|6
argument_list|,
name|fp
argument_list|)
operator|==
name|NULL
operator|)
operator|||
operator|(
name|index
argument_list|(
name|line
argument_list|,
literal|'\n'
argument_list|)
operator|!=
name|NULL
operator|)
condition|)
break|break;
block|}
block|}
block|}
name|putchar
argument_list|(
literal|'.'
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\r'
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\n'
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * get_id -- get the message id of the current article.  *  *	Parameters:	"art_fp" is a pointer to the open file.  *			"id" is space for the message ID.  *  *	Returns:	Nothing.  *  *	Side effects:	Seeks and rewinds on "art_fp".  *			Changes space pointed to by "id".  */
end_comment

begin_expr_stmt
name|get_id
argument_list|(
name|art_fp
argument_list|,
name|id
argument_list|)
specifier|register
name|FILE
operator|*
name|art_fp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|char
modifier|*
name|id
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|line
index|[
name|MAX_STRLEN
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
while|while
condition|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
argument_list|,
name|art_fp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
operator|*
name|line
operator|==
literal|'\n'
condition|)
break|break;
if|if
condition|(
operator|*
name|line
operator|==
literal|'M'
operator|||
operator|*
name|line
operator|==
literal|'m'
condition|)
block|{
comment|/* "Message-ID" */
if|if
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|line
argument_list|,
literal|' '
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|streql
argument_list|(
name|line
argument_list|,
literal|"Message-ID:"
argument_list|)
condition|)
block|{
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|id
argument_list|,
name|cp
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|id
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
operator|(
name|void
operator|)
name|rewind
argument_list|(
name|art_fp
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
block|}
block|}
operator|(
name|void
operator|)
name|rewind
argument_list|(
name|art_fp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|id
argument_list|,
literal|"<0>"
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * close_crnt -- close the current article file pointer, if it's  *	open.  *  *	Parameters:	None.  *  *	Returns:	Nothing.  *  *	Side effects:	Closes "art_fp" if it's open; sets "art_fp" to NULL.  */
end_comment

begin_macro
name|close_crnt
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
name|art_fp
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|art_fp
argument_list|)
expr_stmt|;
name|art_fp
operator|=
name|NULL
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * findart -- find an article number in the article array.  *  *	Parameters:	"artname" is a string containing  *			the name of the article.  *  *	Returns:	An index into "art_array",  *			or -1 if "artname" isn't in "art_array".  *			  *	Side effects:	None.  *  *	Improvement:	Replace this linear search with a binary one.  */
end_comment

begin_macro
name|findart
argument_list|(
argument|artname
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|artname
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|,
name|artnum
decl_stmt|;
name|artnum
operator|=
name|atoi
argument_list|(
name|artname
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_arts
condition|;
operator|++
name|i
control|)
if|if
condition|(
name|art_array
index|[
name|i
index|]
operator|==
name|artnum
condition|)
return|return
operator|(
name|i
operator|)
return|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * get_distlist -- return a nicely set up array of distribution groups  * along with a count, when given an NNTP-spec distribution list  * in the form<dist1,dist2,...,distn>.  *  *	Parameters:		"array" is storage for our array,  *				set to point at some static data.  *				"list" is the NNTP distribution list.  *  *	Returns:		Number of distributions found.  *				-1 on error.  *  *	Side effects:		Changes static data area.  */
end_comment

begin_macro
name|get_distlist
argument_list|(
argument|array
argument_list|,
argument|list
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
modifier|*
modifier|*
name|array
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|list
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|cp
decl_stmt|;
name|int
name|distcount
decl_stmt|;
specifier|static
name|char
modifier|*
modifier|*
name|dist_list
init|=
operator|(
name|char
operator|*
operator|*
operator|)
name|NULL
decl_stmt|;
if|if
condition|(
name|list
index|[
literal|0
index|]
operator|!=
literal|'<'
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|cp
operator|=
name|index
argument_list|(
name|list
operator|+
literal|1
argument_list|,
literal|'>'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
else|else
return|return
operator|(
operator|-
literal|1
operator|)
return|;
for|for
control|(
name|cp
operator|=
name|list
operator|+
literal|1
init|;
operator|*
name|cp
operator|!=
literal|'\0'
condition|;
operator|++
name|cp
control|)
if|if
condition|(
operator|*
name|cp
operator|==
literal|','
condition|)
operator|*
name|cp
operator|=
literal|' '
expr_stmt|;
name|distcount
operator|=
name|parsit
argument_list|(
name|list
operator|+
literal|1
argument_list|,
operator|&
name|dist_list
argument_list|)
expr_stmt|;
operator|*
name|array
operator|=
name|dist_list
expr_stmt|;
return|return
operator|(
name|distcount
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * streql -- determine if two strings are equal, ignoring case.  *  *	Parameters:	"a" and "b" are the pointers  *			to characters to be compared.  *  *	Returns:	1 if the strings are equal, 0 otherwise.  *  *	Side effects:	None.  */
end_comment

begin_expr_stmt
name|streql
argument_list|(
name|a
argument_list|,
name|b
argument_list|)
specifier|register
name|char
operator|*
name|a
operator|,
operator|*
name|b
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|char
name|lower
parameter_list|()
function_decl|;
while|while
condition|(
name|lower
argument_list|(
operator|*
name|a
argument_list|)
operator|==
name|lower
argument_list|(
operator|*
name|b
argument_list|)
condition|)
block|{
if|if
condition|(
operator|*
name|a
operator|==
literal|'\0'
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|a
operator|++
expr_stmt|;
name|b
operator|++
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * lower -- convert a character to lower case, if it's upper case.  *  *	Parameters:	"c" is the character to be  *			converted.  *  *	Returns:	"c" if the character is not  *			upper case, otherwise the lower  *			case eqivalent of "c".  *  *	Side effects:	None.  */
end_comment

begin_function
name|char
name|lower
parameter_list|(
name|c
parameter_list|)
specifier|register
name|char
name|c
decl_stmt|;
block|{
if|if
condition|(
name|isascii
argument_list|(
name|c
argument_list|)
operator|&&
name|isupper
argument_list|(
name|c
argument_list|)
condition|)
name|c
operator|=
name|c
operator|-
literal|'A'
operator|+
literal|'a'
expr_stmt|;
return|return
operator|(
name|c
operator|)
return|;
block|}
end_function

end_unit

