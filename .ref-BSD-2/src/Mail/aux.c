begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Copyright (c) 1979 Regents of the University of California */
end_comment

begin_empty
empty|#
end_empty

begin_include
include|#
directive|include
file|"rcv.h"
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<sgtty.h>
end_include

begin_comment
comment|/*  * Mail -- a mail program  *  * Auxiliary functions.  */
end_comment

begin_comment
comment|/*  * Return a pointer to a dynamic copy of the argument.  */
end_comment

begin_function
name|char
modifier|*
name|savestr
parameter_list|(
name|str
parameter_list|)
name|char
modifier|*
name|str
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|cp2
decl_stmt|,
modifier|*
name|top
decl_stmt|;
for|for
control|(
name|cp
operator|=
name|str
init|;
operator|*
name|cp
condition|;
name|cp
operator|++
control|)
empty_stmt|;
name|top
operator|=
name|salloc
argument_list|(
name|cp
operator|-
name|str
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|top
operator|==
name|NOSTR
condition|)
return|return
operator|(
name|NOSTR
operator|)
return|;
for|for
control|(
name|cp
operator|=
name|str
operator|,
name|cp2
operator|=
name|top
init|;
operator|*
name|cp
condition|;
name|cp
operator|++
control|)
operator|*
name|cp2
operator|++
operator|=
operator|*
name|cp
expr_stmt|;
operator|*
name|cp2
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|top
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Copy the name from the passed header line into the passed  * name buffer.  Null pad the name buffer.  */
end_comment

begin_macro
name|copyname
argument_list|(
argument|linebuf
argument_list|,
argument|nbuf
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|linebuf
decl_stmt|,
modifier|*
name|nbuf
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|cp2
decl_stmt|;
for|for
control|(
name|cp
operator|=
name|linebuf
operator|+
literal|5
operator|,
name|cp2
operator|=
name|nbuf
init|;
operator|*
name|cp
operator|!=
literal|' '
operator|&&
name|cp2
operator|-
name|nbuf
operator|<
literal|8
condition|;
name|cp
operator|++
control|)
operator|*
name|cp2
operator|++
operator|=
operator|*
name|cp
expr_stmt|;
while|while
condition|(
name|cp2
operator|-
name|nbuf
operator|<
literal|8
condition|)
operator|*
name|cp2
operator|++
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Announce a fatal error and die.  */
end_comment

begin_macro
name|panic
argument_list|(
argument|str
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|str
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|prs
argument_list|(
literal|"panic: "
argument_list|)
expr_stmt|;
name|prs
argument_list|(
name|str
argument_list|)
expr_stmt|;
name|prs
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Catch stdio errors and report them more nicely.  */
end_comment

begin_macro
name|_error
argument_list|(
argument|str
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|str
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|prs
argument_list|(
literal|"Stdio Error: "
argument_list|)
expr_stmt|;
name|prs
argument_list|(
name|str
argument_list|)
expr_stmt|;
name|prs
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
name|abort
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Print a string on diagnostic output.  */
end_comment

begin_macro
name|prs
argument_list|(
argument|str
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|str
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|s
decl_stmt|;
for|for
control|(
name|s
operator|=
name|str
init|;
operator|*
name|s
condition|;
name|s
operator|++
control|)
empty_stmt|;
name|write
argument_list|(
literal|2
argument_list|,
name|str
argument_list|,
name|s
operator|-
name|str
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Touch the named message by setting its MTOUCH flag.  * Touched messages have the effect of not being sent  * back to the system mailbox on exit.  */
end_comment

begin_macro
name|touch
argument_list|(
argument|mesg
argument_list|)
end_macro

begin_block
block|{
if|if
condition|(
name|mesg
operator|>=
literal|1
operator|&&
name|mesg
operator|<=
name|msgCount
condition|)
name|message
index|[
name|mesg
operator|-
literal|1
index|]
operator|.
name|m_flag
operator||=
name|MTOUCH
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Test to see if the passed file name is a directory.  * Return true if it is.  */
end_comment

begin_macro
name|isdir
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
name|name
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|stat
name|sbuf
decl_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|name
argument_list|,
operator|&
name|sbuf
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
operator|(
name|sbuf
operator|.
name|st_mode
operator|&
name|S_IFMT
operator|)
operator|==
name|S_IFDIR
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Compute the size in characters of the passed message  */
end_comment

begin_function
name|unsigned
name|int
name|msize
parameter_list|(
name|messp
parameter_list|)
name|struct
name|message
modifier|*
name|messp
decl_stmt|;
block|{
specifier|register
name|struct
name|message
modifier|*
name|mp
decl_stmt|;
name|mp
operator|=
name|messp
expr_stmt|;
return|return
operator|(
name|mp
operator|->
name|m_size
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Count the number of arguments in the given string raw list.  */
end_comment

begin_macro
name|argcount
argument_list|(
argument|argv
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
modifier|*
name|argv
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
modifier|*
name|ap
decl_stmt|;
for|for
control|(
name|ap
operator|=
name|argv
init|;
operator|*
name|ap
operator|!=
name|NOSTR
condition|;
name|ap
operator|++
control|)
empty_stmt|;
return|return
operator|(
name|ap
operator|-
name|argv
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Given a file address, determine the  * block number it represents.  */
end_comment

begin_macro
name|blockof
argument_list|(
argument|off
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|off
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|off_t
name|a
decl_stmt|;
name|a
operator|=
name|off
operator|>>
literal|9
expr_stmt|;
name|a
operator|&=
literal|077777
expr_stmt|;
return|return
operator|(
operator|(
name|int
operator|)
name|a
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Take a file address, and determine  * its offset in the current block.  */
end_comment

begin_macro
name|offsetof
argument_list|(
argument|off
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|off
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|off_t
name|a
decl_stmt|;
name|a
operator|=
name|off
operator|&
literal|0777
expr_stmt|;
return|return
operator|(
operator|(
name|int
operator|)
name|a
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Determine if the passed file is actually a tty, via a call to  * gtty.  This is not totally reliable, but . . .  */
end_comment

begin_macro
name|isatty
argument_list|(
argument|f
argument_list|)
end_macro

begin_block
block|{
name|struct
name|sgttyb
name|buf
decl_stmt|;
if|if
condition|(
name|gtty
argument_list|(
name|f
argument_list|,
operator|&
name|buf
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Return the desired header line from the passed message  * pointer (or NOSTR if the desired header field is not available.  */
end_comment

begin_function
name|char
modifier|*
name|hfield
parameter_list|(
name|field
parameter_list|,
name|mp
parameter_list|)
name|char
name|field
index|[]
decl_stmt|;
name|struct
name|message
modifier|*
name|mp
decl_stmt|;
block|{
name|FILE
modifier|*
name|ibuf
decl_stmt|;
name|char
name|linebuf
index|[
name|LINESIZE
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|cp2
decl_stmt|;
name|int
name|hfc
decl_stmt|;
name|ibuf
operator|=
name|setinput
argument_list|(
name|mp
argument_list|)
expr_stmt|;
name|hfc
operator|=
literal|0
expr_stmt|;
while|while
condition|(
name|readline
argument_list|(
name|ibuf
argument_list|,
name|linebuf
argument_list|)
operator|>
literal|0
operator|&&
name|hfc
operator|<
name|HDRFIELDS
condition|)
block|{
if|if
condition|(
name|equal
argument_list|(
name|linebuf
argument_list|,
literal|""
argument_list|)
condition|)
return|return
operator|(
name|NOSTR
operator|)
return|;
name|cp
operator|=
name|linebuf
expr_stmt|;
name|cp2
operator|=
name|field
expr_stmt|;
while|while
condition|(
name|raise
argument_list|(
operator|*
name|cp
operator|++
argument_list|)
operator|==
name|raise
argument_list|(
operator|*
name|cp2
operator|++
argument_list|)
condition|)
empty_stmt|;
if|if
condition|(
operator|*
operator|--
name|cp
operator|==
literal|':'
operator|&&
operator|*
operator|--
name|cp2
operator|==
literal|'\0'
condition|)
block|{
name|cp
operator|++
expr_stmt|;
while|while
condition|(
name|any
argument_list|(
operator|*
name|cp
argument_list|,
literal|" \t"
argument_list|)
condition|)
name|cp
operator|++
expr_stmt|;
return|return
operator|(
name|savestr
argument_list|(
name|cp
argument_list|)
operator|)
return|;
block|}
name|hfc
operator|++
expr_stmt|;
block|}
return|return
operator|(
name|NOSTR
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * The following code deals with input stacking to do source  * commands.  All but the current file pointer are saved on  * the stack.  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|ssp
init|=
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Top of file stack */
end_comment

begin_decl_stmt
specifier|static
name|FILE
modifier|*
name|sstack
index|[
name|_NFILE
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Saved input files */
end_comment

begin_comment
comment|/*  * Pushdown current input file and switch to a new one.  * Set the global flag "sourcing" so that others will realize  * that they are no longer reading from a tty (in all probability).  */
end_comment

begin_macro
name|source
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
name|name
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|FILE
modifier|*
name|fi
decl_stmt|;
if|if
condition|(
operator|(
name|fi
operator|=
name|fopen
argument_list|(
name|name
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|perror
argument_list|(
name|name
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
if|if
condition|(
name|ssp
operator|>=
name|_NFILE
operator|-
literal|2
condition|)
block|{
name|printf
argument_list|(
literal|"Too much \"sourcing\" going on.\n"
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|fi
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|sstack
index|[
operator|++
name|ssp
index|]
operator|=
name|input
expr_stmt|;
name|input
operator|=
name|fi
expr_stmt|;
name|sourcing
operator|++
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Source a file, but do nothing if the file cannot be opened.  */
end_comment

begin_macro
name|source1
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
name|name
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|f
decl_stmt|;
if|if
condition|(
operator|(
name|f
operator|=
name|open
argument_list|(
name|name
argument_list|,
literal|0
argument_list|)
operator|)
operator|<
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|close
argument_list|(
name|f
argument_list|)
expr_stmt|;
name|source
argument_list|(
name|name
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Pop the current input back to the previous level.  * Update the "sourcing" flag as appropriate.  */
end_comment

begin_macro
name|unstack
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
name|ssp
operator|<
literal|0
condition|)
block|{
name|printf
argument_list|(
literal|"\"Source\" stack over-pop.\n"
argument_list|)
expr_stmt|;
name|sourcing
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|fclose
argument_list|(
name|input
argument_list|)
expr_stmt|;
name|input
operator|=
name|sstack
index|[
name|ssp
operator|--
index|]
expr_stmt|;
if|if
condition|(
name|ssp
operator|<
literal|0
condition|)
name|sourcing
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Touch the indicated file.  * This is nifty for the shell.  */
end_comment

begin_macro
name|alter
argument_list|(
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
name|name
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|pid
decl_stmt|,
name|f
decl_stmt|;
name|char
name|w
decl_stmt|;
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|!=
literal|0
condition|)
return|return;
name|clrbuf
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
name|clrbuf
argument_list|(
name|stderr
argument_list|)
expr_stmt|;
name|clrbuf
argument_list|(
name|stdin
argument_list|)
expr_stmt|;
name|sleep
argument_list|(
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|f
operator|=
name|open
argument_list|(
name|name
argument_list|,
literal|0
argument_list|)
operator|)
operator|<
literal|0
condition|)
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
name|read
argument_list|(
name|f
argument_list|,
operator|&
name|w
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Examine the passed line buffer and  * return true if it is all blanks and tabs.  */
end_comment

begin_macro
name|blankline
argument_list|(
argument|linebuf
argument_list|)
end_macro

begin_decl_stmt
name|char
name|linebuf
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
for|for
control|(
name|cp
operator|=
name|linebuf
init|;
operator|*
name|cp
condition|;
name|cp
operator|++
control|)
if|if
condition|(
operator|!
name|any
argument_list|(
operator|*
name|cp
argument_list|,
literal|" \t"
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Fetch the sender's name from the passed message.  */
end_comment

begin_function
name|char
modifier|*
name|nameof
parameter_list|(
name|mp
parameter_list|)
specifier|register
name|struct
name|message
modifier|*
name|mp
decl_stmt|;
block|{
specifier|static
name|char
name|namebuf
index|[
name|NAMESIZE
index|]
decl_stmt|;
name|char
name|linebuf
index|[
name|LINESIZE
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|cp2
decl_stmt|;
specifier|register
name|FILE
modifier|*
name|ibuf
decl_stmt|;
name|ibuf
operator|=
name|setinput
argument_list|(
name|mp
argument_list|)
expr_stmt|;
name|copy
argument_list|(
literal|""
argument_list|,
name|namebuf
argument_list|)
expr_stmt|;
if|if
condition|(
name|readline
argument_list|(
name|ibuf
argument_list|,
name|linebuf
argument_list|)
operator|<=
literal|0
condition|)
return|return
operator|(
name|namebuf
operator|)
return|;
for|for
control|(
name|cp
operator|=
name|linebuf
init|;
operator|*
name|cp
operator|!=
literal|' '
condition|;
name|cp
operator|++
control|)
empty_stmt|;
while|while
condition|(
name|any
argument_list|(
operator|*
name|cp
argument_list|,
literal|" \t"
argument_list|)
condition|)
name|cp
operator|++
expr_stmt|;
for|for
control|(
name|cp2
operator|=
name|namebuf
init|;
operator|*
name|cp
operator|&&
operator|!
name|any
argument_list|(
operator|*
name|cp
argument_list|,
literal|" \t"
argument_list|)
operator|&&
name|cp2
operator|-
name|namebuf
operator|<
name|NAMESIZE
operator|-
literal|1
condition|;
operator|*
name|cp2
operator|++
operator|=
operator|*
name|cp
operator|++
control|)
empty_stmt|;
operator|*
name|cp2
operator|=
literal|'\0'
expr_stmt|;
return|return
operator|(
name|namebuf
operator|)
return|;
block|}
end_function

end_unit

