begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * refclock_goes - clock driver for the Kinemetrics Truetime GOES  *	Receiver Version 3.0C - tested plain, with CLKLDISC  *	Developement work being done:  * 	- Properly handle varying satellite positions (more acurately)  *	- Integrate GPSTM and/or OMEGA and/or TRAK and/or ??? drivers  */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|REFCLOCK
argument_list|)
operator|&&
name|defined
argument_list|(
name|GOES
argument_list|)
end_if

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_refclock.h"
end_include

begin_include
include|#
directive|include
file|"ntp_unixtime.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_comment
comment|/*  * Support for Kinemetrics Truetime 468-DC GOES Receiver  *   OM-DC OMEGA and GPS-TM/TMD support in progress...  *  * Most of this code is originally from refclock_wwvb.c with thanks.  * It has been so mangled that wwvb is not a recognizable ancestor.  *  * Timcode format: ADDD:HH:MM:SSQCL  *	A - control A  *	Q Quality indication: indicates possible error of  *	C - Carriage return  *	L - Line feed  *  * Quality codes indicate possible error of  *   468-DC GOES Receiver:  *   GPS-TM/TMD Receiver:  *       ?     +/- 500 milliseconds	#     +/- 50 milliseconds  *       *     +/- 5 milliseconds	.     +/- 1 millisecond  *     space   less than 1 millisecond  *   OM-DC OMEGA Receiver:  *>>+- 5 seconds  *       ?>+/- 500 milliseconds    #>+/- 50 milliseconds  *       *>+/- 5 milliseconds      .>+/- 1 millisecond  *      A-H    less than 1 millisecond.  Character indicates which station  *             is being received as follows:  *             A = Norway, B = Liberia, C = Hawaii, D = North Dakota,  *             E = La Reunion, F = Argentina, G = Australia, H = Japan.  *  * The carriage return start bit begins on 0 seconds and extends to 1  * bit time  *  * Notes on 468-DC and OMEGA receiver:  *  * Send the clock a 'R' or 'C' and once per second a timestamp will  * appear.  Send a 'P' to get the satellite position once.  *  * Notes on the 468-DC receiver:  *  * Unless you live on 125 degrees west longitude, you can't  * set your clock propagation delay settings correctly and still use  * automatic mode. The manual says to use a compromise when setting the  * switches. This results in significant errors. The solution; use fudge  * time1 and time2 to incorporate corrections. If your clock is set for  * 50 and it should be 58 for using the west and 46 for using the east,  * use the line  *  * fudge 127.127.5.0 time1 +0.008 time2 -0.004  *  * This corrects the 4 milliseconds advance and 8 milliseconds retard  * needed. The software will ask the clock which satellite it sees.  *  * Ntp.conf parameters:  * time1 - offset applied to samples when reading WEST satellite (default = 0)  * time2 - offset applied to samples when reading EAST satellite (default = 0)  * val1  - stratum to assign to this clock (default = 0)  * val2  - refid assigned to this clock (default = "GOES", see below)  * flag1 - will silence the clock side of xntpd, just reading the clock  *         without trying to write to it.  (default = 0)  * flag2 - not assigned  * flag3 - enable ppsclock streams module  * flag4 - not assigned  *  */
end_comment

begin_comment
comment|/*  * Definitions  */
end_comment

begin_define
define|#
directive|define
name|DEVICE
value|"/dev/goes%d"
end_define

begin_define
define|#
directive|define
name|SPEED232
value|B9600
end_define

begin_comment
comment|/* 9600 baud */
end_comment

begin_comment
comment|/*  * Radio interface parameters  */
end_comment

begin_define
define|#
directive|define
name|MAXDISPERSE
value|(FP_SECOND>>1)
end_define

begin_comment
comment|/* max error for synchronized clock (0.5 s as an u_fp) */
end_comment

begin_define
define|#
directive|define
name|PRECISION
value|(-10)
end_define

begin_comment
comment|/* precision assumed (about 1 ms) */
end_comment

begin_define
define|#
directive|define
name|REFID
value|"GOES"
end_define

begin_comment
comment|/* reference id */
end_comment

begin_define
define|#
directive|define
name|DESCRIPTION
value|"TrueTime GPS/GOES Receivers"
end_define

begin_comment
comment|/* WRU */
end_comment

begin_define
define|#
directive|define
name|NSAMPLES
value|3
end_define

begin_comment
comment|/* stages of median filter */
end_comment

begin_comment
comment|/*  * Tags which station (satellite) we see  */
end_comment

begin_define
define|#
directive|define
name|GOES_WEST
value|0
end_define

begin_comment
comment|/* Default to WEST satellite and apply time1 */
end_comment

begin_define
define|#
directive|define
name|GOES_EAST
value|1
end_define

begin_comment
comment|/* until you discover otherwise */
end_comment

begin_comment
comment|/*  * used by the state machine  */
end_comment

begin_enum
enum|enum
name|goes_event
block|{
name|e_Init
block|,
name|e_F18
block|,
name|e_F50
block|,
name|e_F51
block|,
name|e_TS
block|}
enum|;
end_enum

begin_comment
comment|/*  * Imported from the timer module  */
end_comment

begin_decl_stmt
specifier|extern
name|u_long
name|current_time
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Imported from ntpd module  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|debug
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* global debug flag */
end_comment

begin_comment
comment|/*  * unit control structure  */
end_comment

begin_struct
struct|struct
name|goesunit
block|{
name|int
name|pollcnt
decl_stmt|;
comment|/* poll message counter */
name|u_short
name|station
decl_stmt|;
comment|/* which station we are on */
name|u_short
name|polled
decl_stmt|;
comment|/* Hand in a time sample? */
enum|enum
block|{
name|Base
block|,
name|Start
block|,
name|F18
block|,
name|F50
block|,
name|F51
block|,
name|F08
block|}
name|State
enum|;
comment|/* State machine */
block|}
struct|;
end_struct

begin_comment
comment|/*  * Function prototypes  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|goes_start
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_shutdown
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_receive
name|P
argument_list|(
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_poll
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_send
name|P
argument_list|(
operator|(
expr|struct
name|peer
operator|*
operator|,
name|char
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_initstate
name|P
argument_list|(
operator|(
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|goes_doevent
name|P
argument_list|(
operator|(
expr|struct
name|peer
operator|*
operator|,
expr|enum
name|goes_event
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Transfer vector  */
end_comment

begin_decl_stmt
name|struct
name|refclock
name|refclock_goes
init|=
block|{
name|goes_start
block|,
comment|/* start up driver */
name|goes_shutdown
block|,
comment|/* shut down driver */
name|goes_poll
block|,
comment|/* transmit poll message */
name|noentry
block|,
comment|/* not used (old goes_control) */
name|noentry
block|,
comment|/* initialize driver (not used) */
name|noentry
block|,
comment|/* not used (old goes_buginfo) */
name|NOFLAGS
comment|/* not used */
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * goes_start - open the devices and initialize data for processing  */
end_comment

begin_function
specifier|static
name|int
name|goes_start
parameter_list|(
name|unit
parameter_list|,
name|peer
parameter_list|)
name|int
name|unit
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
specifier|register
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|char
name|device
index|[
literal|20
index|]
decl_stmt|;
comment|/* 	 * Open serial port 	 */
operator|(
name|void
operator|)
name|sprintf
argument_list|(
name|device
argument_list|,
name|DEVICE
argument_list|,
name|unit
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|fd
operator|=
name|refclock_open
argument_list|(
name|device
argument_list|,
name|SPEED232
argument_list|,
name|LDISC_CLK
argument_list|)
operator|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* 	 * Allocate and initialize unit structure 	 */
if|if
condition|(
operator|!
operator|(
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|emalloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|goesunit
argument_list|)
argument_list|)
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|memset
argument_list|(
operator|(
name|char
operator|*
operator|)
name|up
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|goesunit
argument_list|)
argument_list|)
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|clock_recv
operator|=
name|goes_receive
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|srcclock
operator|=
operator|(
name|caddr_t
operator|)
name|peer
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|datalen
operator|=
literal|0
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|fd
operator|=
name|fd
expr_stmt|;
if|if
condition|(
operator|!
name|io_addclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|pp
operator|->
name|unitptr
operator|=
operator|(
name|caddr_t
operator|)
name|up
expr_stmt|;
comment|/* 	 * Initialize miscellaneous variables 	 */
name|peer
operator|->
name|precision
operator|=
name|PRECISION
expr_stmt|;
name|pp
operator|->
name|clockdesc
operator|=
name|DESCRIPTION
expr_stmt|;
name|memcpy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|pp
operator|->
name|refid
argument_list|,
name|REFID
argument_list|,
literal|4
argument_list|)
expr_stmt|;
name|up
operator|->
name|pollcnt
operator|=
literal|2
expr_stmt|;
comment|/*	goes_initstate(peer);*/
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * goes_shutdown - shut down the clock  */
end_comment

begin_function
specifier|static
name|void
name|goes_shutdown
parameter_list|(
name|unit
parameter_list|,
name|peer
parameter_list|)
name|int
name|unit
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
specifier|register
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|io_closeclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * goes_receive - receive data from the serial interface on a  * Kinimetrics clock  */
end_comment

begin_function
specifier|static
name|void
name|goes_receive
parameter_list|(
name|rbufp
parameter_list|)
name|struct
name|recvbuf
modifier|*
name|rbufp
decl_stmt|;
block|{
specifier|register
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|l_fp
name|tmp_l_fp
decl_stmt|;
name|u_short
name|new_station
decl_stmt|;
name|char
name|sync
decl_stmt|,
name|c1
decl_stmt|,
name|c2
decl_stmt|;
name|int
name|i
decl_stmt|;
name|int
name|lat
decl_stmt|,
name|lon
decl_stmt|,
name|off
decl_stmt|;
comment|/* GOES Satellite position */
comment|/* 	 * Get the clock this applies to and pointers to the data 	 */
name|peer
operator|=
operator|(
expr|struct
name|peer
operator|*
operator|)
name|rbufp
operator|->
name|recv_srcclock
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
comment|/* 	 * Read clock output.  Automatically handles STREAMS, CLKLDISC 	 */
name|pp
operator|->
name|lencode
operator|=
name|refclock_gtlin
argument_list|(
name|rbufp
argument_list|,
name|pp
operator|->
name|lastcode
argument_list|,
name|BMAX
argument_list|,
operator|&
name|pp
operator|->
name|lastrec
argument_list|)
expr_stmt|;
comment|/* 	 * There is a case where<cr><lf> generates 2 timestamps 	 */
if|if
condition|(
name|pp
operator|->
name|lencode
operator|==
literal|0
condition|)
return|return;
name|up
operator|->
name|pollcnt
operator|=
literal|2
expr_stmt|;
name|record_clock_stats
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|,
name|pp
operator|->
name|lastcode
argument_list|)
expr_stmt|;
comment|/* 	 * We get down to business, check the timecode format and decode 	 * its contents. This code decodes a multitude of different 	 * clock messages. Timecodes are processed if needed. All replies 	 * will be run through the state machine to tweak driver options 	 * and program the clock. 	 */
comment|/* 	 * Timecode: "nnnnn+nnn-nnn" 	 */
if|if
condition|(
name|sscanf
argument_list|(
name|pp
operator|->
name|lastcode
argument_list|,
literal|"%5d%c%3d%c%3d"
argument_list|,
operator|&
name|lon
argument_list|,
operator|&
name|c1
argument_list|,
operator|&
name|lat
argument_list|,
operator|&
name|c2
argument_list|,
operator|&
name|off
argument_list|)
operator|==
literal|5
operator|&&
operator|(
name|c1
operator|==
literal|'+'
operator|||
name|c1
operator|==
literal|'-'
operator|)
operator|&&
operator|(
name|c2
operator|==
literal|'+'
operator|||
name|c2
operator|==
literal|'-'
operator|)
condition|)
block|{
comment|/* 		 * This is less than perfect.  Call the (satellite) 		 * either EAST or WEST and adjust slop accodingly 		 * Perfectionists would recalcuted the exact delay 		 * and adjust accordingly... 		 */
if|if
condition|(
name|lon
operator|>
literal|7000
operator|&&
name|lon
operator|<
literal|14000
condition|)
block|{
if|if
condition|(
name|lon
operator|<
literal|10000
condition|)
name|new_station
operator|=
name|GOES_EAST
expr_stmt|;
else|else
name|new_station
operator|=
name|GOES_WEST
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
if|if
condition|(
name|new_station
operator|==
name|GOES_EAST
condition|)
name|printf
argument_list|(
literal|"goes: station EAST\n"
argument_list|)
expr_stmt|;
if|if
condition|(
name|new_station
operator|==
name|GOES_WEST
condition|)
name|printf
argument_list|(
literal|"goes: station WEST\n"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|new_station
operator|!=
name|up
operator|->
name|station
condition|)
block|{
name|tmp_l_fp
operator|=
name|pp
operator|->
name|fudgetime1
expr_stmt|;
name|pp
operator|->
name|fudgetime1
operator|=
name|pp
operator|->
name|fudgetime2
expr_stmt|;
name|pp
operator|->
name|fudgetime2
operator|=
name|tmp_l_fp
expr_stmt|;
name|up
operator|->
name|station
operator|=
name|new_station
expr_stmt|;
block|}
block|}
else|else
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADREPLY
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"goes: station UNKNONW\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 		 * Switch back to on-second time codes and return. 		 */
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"C"
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Timecode: "Fnn" 	 */
if|if
condition|(
name|sscanf
argument_list|(
name|pp
operator|->
name|lastcode
argument_list|,
literal|"F%2d"
argument_list|,
operator|&
name|i
argument_list|)
operator|==
literal|1
operator|&&
name|i
operator|>
literal|0
operator|&&
name|i
operator|<
literal|80
condition|)
block|{
name|enum
name|goes_event
name|event
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|i
operator|==
literal|50
condition|)
name|event
operator|=
name|e_F50
expr_stmt|;
if|if
condition|(
name|i
operator|==
literal|51
condition|)
name|event
operator|=
name|e_F51
expr_stmt|;
if|if
condition|(
name|i
operator|==
literal|50
operator|||
name|i
operator|==
literal|51
condition|)
block|{
name|goes_doevent
argument_list|(
name|peer
argument_list|,
name|event
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
comment|/* 	 * Timecode:" TRUETIME Mk III" 	 */
if|if
condition|(
name|strcmp
argument_list|(
name|pp
operator|->
name|lastcode
argument_list|,
literal|" TRUETIME Mk III"
argument_list|)
operator|==
literal|0
condition|)
block|{
name|enum
name|goes_event
name|event
decl_stmt|;
name|event
operator|=
name|e_F18
expr_stmt|;
name|goes_doevent
argument_list|(
name|peer
argument_list|,
name|event
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/*  	 * Timecode: "ddd:hh:mm:ssQ"  	 */
if|if
condition|(
name|sscanf
argument_list|(
name|pp
operator|->
name|lastcode
argument_list|,
literal|"%3d:%2d:%2d:%2d%c"
argument_list|,
operator|&
name|pp
operator|->
name|day
argument_list|,
operator|&
name|pp
operator|->
name|hour
argument_list|,
operator|&
name|pp
operator|->
name|minute
argument_list|,
operator|&
name|pp
operator|->
name|second
argument_list|,
operator|&
name|sync
argument_list|)
operator|==
literal|5
condition|)
block|{
comment|/* 		 * Adjust the synchronize indicator according to timecode 		 */
if|if
condition|(
name|sync
operator|!=
literal|' '
operator|&&
name|sync
operator|!=
literal|'.'
operator|&&
name|sync
operator|!=
literal|'*'
condition|)
name|pp
operator|->
name|leap
operator|=
name|LEAP_NOTINSYNC
expr_stmt|;
else|else
block|{
name|pp
operator|->
name|leap
operator|=
literal|0
expr_stmt|;
name|pp
operator|->
name|lasttime
operator|=
name|current_time
expr_stmt|;
block|}
comment|/* goes_doevent(peer, e_TS); */
comment|/* 		 * The clock will blurt a timecode every second but we only 		 * want one when polled.  If we havn't been polled, bail out. 		 */
if|if
condition|(
operator|!
name|up
operator|->
name|polled
condition|)
return|return;
comment|/* 		 * After each poll, check the station (satellite) 		 */
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"P"
argument_list|)
expr_stmt|;
comment|/* 		 * Process the new sample in the median filter and determine the 		 * reference clock offset and dispersion. We use lastrec as both 		 * the reference time and receive time in order to avoid being 		 * cute, like setting the reference time later than the receive 		 * time, which may cause a paranoid protocol module to chuck out 		 * the data. 	 	 */
if|if
condition|(
operator|!
name|refclock_process
argument_list|(
name|pp
argument_list|,
name|NSAMPLES
argument_list|,
name|NSAMPLES
argument_list|)
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADTIME
argument_list|)
expr_stmt|;
return|return;
block|}
name|refclock_receive
argument_list|(
name|peer
argument_list|,
operator|&
name|pp
operator|->
name|offset
argument_list|,
literal|0
argument_list|,
name|pp
operator|->
name|dispersion
argument_list|,
operator|&
name|pp
operator|->
name|lastrec
argument_list|,
operator|&
name|pp
operator|->
name|lastrec
argument_list|,
name|pp
operator|->
name|leap
argument_list|)
expr_stmt|;
comment|/* 		 * We have succedded in answering the poll. 		 * Turn off the flag and return 		 */
name|up
operator|->
name|polled
operator|=
literal|0
expr_stmt|;
return|return;
block|}
comment|/* 	 * No match to known timecodes, report failure and return 	 */
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADREPLY
argument_list|)
expr_stmt|;
return|return;
block|}
end_function

begin_comment
comment|/*  * goes_send - time to send the clock a signal to cough up a time sample  */
end_comment

begin_function
specifier|static
name|void
name|goes_send
parameter_list|(
name|peer
parameter_list|,
name|cmd
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|char
modifier|*
name|cmd
decl_stmt|;
block|{
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
specifier|register
name|int
name|len
init|=
name|strlen
argument_list|(
name|cmd
argument_list|)
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
if|if
condition|(
operator|!
name|pp
operator|->
name|sloppyclockflag
operator|&
name|CLK_FLAG1
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"goes: Send '%s'\n"
argument_list|,
name|cmd
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
name|cmd
argument_list|,
name|len
argument_list|)
operator|!=
name|len
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_FAULT
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|pp
operator|->
name|polls
operator|++
expr_stmt|;
block|}
block|}
block|}
end_function

begin_comment
comment|/*  * state machine for initializing the clock  */
end_comment

begin_function
specifier|static
name|void
name|goes_doevent
parameter_list|(
name|peer
parameter_list|,
name|event
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|enum
name|goes_event
name|event
decl_stmt|;
block|{
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
name|printf
argument_list|(
literal|"goes_doevent: %d\n"
argument_list|,
operator|(
name|int
operator|)
name|event
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|event
operator|==
name|e_TS
operator|&&
name|up
operator|->
name|State
operator|!=
name|F51
operator|&&
name|up
operator|->
name|State
operator|!=
name|F08
condition|)
block|{
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"\03\r"
argument_list|)
expr_stmt|;
block|}
switch|switch
condition|(
name|event
condition|)
block|{
case|case
name|e_Init
case|:
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"F18\r"
argument_list|)
expr_stmt|;
name|up
operator|->
name|State
operator|=
name|Start
expr_stmt|;
break|break;
case|case
name|e_F18
case|:
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"F50\r"
argument_list|)
expr_stmt|;
name|up
operator|->
name|State
operator|=
name|F18
expr_stmt|;
break|break;
case|case
name|e_F50
case|:
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"F51\r"
argument_list|)
expr_stmt|;
name|up
operator|->
name|State
operator|=
name|F50
expr_stmt|;
break|break;
case|case
name|e_F51
case|:
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"F08\r"
argument_list|)
expr_stmt|;
name|up
operator|->
name|State
operator|=
name|F51
expr_stmt|;
break|break;
case|case
name|e_TS
case|:
comment|/* nothing to send - we like this mode */
name|up
operator|->
name|State
operator|=
name|F08
expr_stmt|;
break|break;
block|}
block|}
end_function

begin_function
specifier|static
name|void
name|goes_initstate
parameter_list|(
name|peer
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|up
operator|->
name|State
operator|=
name|Base
expr_stmt|;
comment|/* just in case */
name|goes_doevent
argument_list|(
name|peer
argument_list|,
name|e_Init
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * goes_poll - called by the transmit procedure  */
end_comment

begin_function
specifier|static
name|void
name|goes_poll
parameter_list|(
name|unit
parameter_list|,
name|peer
parameter_list|)
name|int
name|unit
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
name|struct
name|goesunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
comment|/* 	 * You don't need to poll this clock.  It puts out timecodes 	 * once per second.  If asked for a timestamp, take note. 	 * The next time a timecode comes in, it will be fed back. 	 */
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|goesunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
if|if
condition|(
name|up
operator|->
name|pollcnt
operator|==
literal|0
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_TIMEOUT
argument_list|)
expr_stmt|;
name|goes_send
argument_list|(
name|peer
argument_list|,
literal|"C"
argument_list|)
expr_stmt|;
block|}
else|else
name|up
operator|->
name|pollcnt
operator|--
expr_stmt|;
comment|/* 	 * polled every 64 seconds. Ask goes_receive to hand in a 	 * timestamp. 	 */
name|up
operator|->
name|polled
operator|=
literal|1
expr_stmt|;
name|pp
operator|->
name|polls
operator|++
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

