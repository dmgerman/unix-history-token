begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * ntpd.c - main program for the fixed point NTP daemon  *  * $FreeBSD$  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/signal.h>
end_include

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_HPUX
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_include
include|#
directive|include
file|<sys/rtprio.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_SVR4
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_UNIXWARE1
argument_list|)
end_if

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|(
name|defined
argument_list|(
name|SYS_SOLARIS
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|bsd
argument_list|)
operator|)
operator|||
name|defined
argument_list|(
name|__svr4__
argument_list|)
end_if

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_select.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|LOCK_PROCESS
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_SOLARIS
end_ifdef

begin_include
include|#
directive|include
file|<sys/mman.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Signals we catch for debugging.  If not debugging we ignore them.  */
end_comment

begin_define
define|#
directive|define
name|MOREDEBUGSIG
value|SIGUSR1
end_define

begin_define
define|#
directive|define
name|LESSDEBUGSIG
value|SIGUSR2
end_define

begin_comment
comment|/*  * Signals which terminate us gracefully.  */
end_comment

begin_define
define|#
directive|define
name|SIGDIE1
value|SIGHUP
end_define

begin_define
define|#
directive|define
name|SIGDIE2
value|SIGINT
end_define

begin_define
define|#
directive|define
name|SIGDIE3
value|SIGQUIT
end_define

begin_define
define|#
directive|define
name|SIGDIE4
value|SIGTERM
end_define

begin_comment
comment|/*  * Scheduling priority we run at  */
end_comment

begin_define
define|#
directive|define
name|NTPD_PRIO
value|(-12)
end_define

begin_comment
comment|/*  * Debugging flag  */
end_comment

begin_decl_stmt
name|int
name|debug
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Initializing flag.  All async routines watch this and only do their  * thing when it is clear.  */
end_comment

begin_decl_stmt
name|int
name|initializing
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Version declaration  */
end_comment

begin_decl_stmt
specifier|extern
name|char
modifier|*
name|Version
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Alarm flag.  Imported from timer module  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|alarm_flag
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|SYS_386BSD
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|SYS_BSDI
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|SYS_44BSD
argument_list|)
end_if

begin_comment
comment|/*  * We put this here, since the argument profile is syscall-specific  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|syscall
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|timeval
operator|*
operator|,
expr|struct
name|timeval
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !SYS_386BSD */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SIGDIE1
end_ifdef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|finish
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SIGDIE1 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|moredebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|lessdebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DEBUG */
end_comment

begin_comment
comment|/*  * Main program.  Initialize us, disconnect us from the tty if necessary,  * and loop waiting for I/O and/or timer expiries.  */
end_comment

begin_function
name|int
name|main
parameter_list|(
name|argc
parameter_list|,
name|argv
parameter_list|)
name|int
name|argc
decl_stmt|;
name|char
modifier|*
name|argv
index|[]
decl_stmt|;
block|{
name|char
modifier|*
name|cp
decl_stmt|;
name|int
name|was_alarmed
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuflist
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuf
decl_stmt|;
name|initializing
operator|=
literal|1
expr_stmt|;
comment|/* mark that we are initializing */
name|debug
operator|=
literal|0
expr_stmt|;
comment|/* no debugging by default */
name|getstartup
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
comment|/* startup configuration, may set debug */
ifndef|#
directive|ifndef
name|NODETACH
comment|/* 	 * Detach us from the terminal.  May need an #ifndef GIZMO. 	 */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|!
name|debug
condition|)
block|{
endif|#
directive|endif
comment|/* DEBUG */
undef|#
directive|undef
name|BSD19906
if|#
directive|if
name|defined
argument_list|(
name|BSD
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|sun
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|SYS_SINIXM
argument_list|)
if|#
directive|if
operator|(
name|BSD
operator|>=
literal|199006
operator|&&
operator|!
name|defined
argument_list|(
name|i386
argument_list|)
operator|)
define|#
directive|define
name|BSD19906
endif|#
directive|endif
comment|/* BSD... */
endif|#
directive|endif
comment|/* BSD sun */
if|#
directive|if
name|defined
argument_list|(
name|BSD19906
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_44BSD
argument_list|)
name|daemon
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* BSD19906 */
if|if
condition|(
name|fork
argument_list|()
condition|)
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|{
name|u_long
name|s
decl_stmt|;
name|int
name|max_fd
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|NTP_POSIX_SOURCE
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|SYS_386BSD
argument_list|)
name|max_fd
operator|=
name|sysconf
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NTP_POSIX_SOURCE */
name|max_fd
operator|=
name|getdtablesize
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* NTP_POSIX_SOURCE */
for|for
control|(
name|s
operator|=
literal|0
init|;
name|s
operator|<
name|max_fd
condition|;
name|s
operator|++
control|)
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|2
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|NTP_POSIX_SOURCE
if|#
directive|if
name|defined
argument_list|(
name|SOLARIS
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_PTX
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_AUX3
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_AIX
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_ULTRIX
argument_list|)
operator|(
name|void
operator|)
name|setsid
argument_list|()
expr_stmt|;
else|#
directive|else
operator|(
name|void
operator|)
name|setpgid
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
else|#
directive|else
comment|/* NTP_POSIX_SOURCE */
ifdef|#
directive|ifdef
name|HAVE_ATT_SETPGRP
operator|(
name|void
operator|)
name|setpgrp
argument_list|()
expr_stmt|;
else|#
directive|else
comment|/* HAVE_ATT_SETPGRP */
operator|(
name|void
operator|)
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_ATT_SETPGRP */
if|#
directive|if
name|defined
argument_list|(
name|SYS_HPUX
argument_list|)
if|if
condition|(
name|fork
argument_list|()
condition|)
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* SYS_HPUX */
ifdef|#
directive|ifdef
name|SYS_DOMAINOS
comment|/*  * This breaks... the program fails to listen to any packets coming  * in on the UDP socket.  So how do you break terminal affiliation?  */
else|#
directive|else
comment|/* SYS_DOMAINOS */
block|{
name|int
name|fid
decl_stmt|;
name|fid
operator|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
if|if
condition|(
name|fid
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|ioctl
argument_list|(
name|fid
argument_list|,
operator|(
name|u_long
operator|)
name|TIOCNOTTY
argument_list|,
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|fid
argument_list|)
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SYS_DOMAINOS */
endif|#
directive|endif
comment|/* SYS_HPUX */
endif|#
directive|endif
comment|/* NTP_POSIX_SOURCE */
block|}
endif|#
directive|endif
comment|/* BSD19906 */
ifdef|#
directive|ifdef
name|DEBUG
block|}
endif|#
directive|endif
comment|/* DEBUG */
endif|#
directive|endif
comment|/* NODETACH */
comment|/* 	 * Logging.  This may actually work on the gizmo board.  Find a name 	 * to log with by using the basename of argv[0] 	 */
name|cp
operator|=
name|strrchr
argument_list|(
name|argv
index|[
literal|0
index|]
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
literal|0
condition|)
name|cp
operator|=
name|argv
index|[
literal|0
index|]
expr_stmt|;
else|else
name|cp
operator|++
expr_stmt|;
ifndef|#
directive|ifndef
name|LOG_DAEMON
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
argument_list|)
expr_stmt|;
else|#
directive|else
ifndef|#
directive|ifndef
name|LOG_NTP
define|#
directive|define
name|LOG_NTP
value|LOG_DAEMON
endif|#
directive|endif
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
operator||
name|LOG_NDELAY
argument_list|,
name|LOG_NTP
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
else|else
endif|#
directive|endif
comment|/* DEBUG */
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
comment|/* @@@ was INFO */
endif|#
directive|endif
comment|/* LOG_DAEMON */
name|syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
name|Version
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|SYS_HPUX
argument_list|)
comment|/* 	 * Lock text into ram, set real time priority 	 */
if|if
condition|(
name|plock
argument_list|(
name|TXTLOCK
argument_list|)
operator|<
literal|0
condition|)
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock() error: %m"
argument_list|)
expr_stmt|;
if|if
condition|(
name|rtprio
argument_list|(
literal|0
argument_list|,
literal|120
argument_list|)
operator|<
literal|0
condition|)
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"rtprio() error: %m"
argument_list|)
expr_stmt|;
else|#
directive|else
if|#
directive|if
name|defined
argument_list|(
name|LOCK_PROCESS
argument_list|)
if|#
directive|if
name|defined
argument_list|(
name|MCL_CURRENT
argument_list|)
operator|&&
name|defined
argument_list|(
name|MCL_FUTURE
argument_list|)
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|mlockall
argument_list|(
name|MCL_CURRENT
operator||
name|MCL_FUTURE
argument_list|)
operator|<
literal|0
condition|)
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"mlockall(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
if|#
directive|if
name|defined
argument_list|(
name|PROCLOCK
argument_list|)
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|plock
argument_list|(
name|PROCLOCK
argument_list|)
operator|<
literal|0
condition|)
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock(): %m"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|NTPD_PRIO
argument_list|)
operator|&&
name|NTPD_PRIO
operator|!=
literal|0
comment|/* 	 * Set the priority. 	 */
ifdef|#
directive|ifdef
name|HAVE_ATT_NICE
name|nice
argument_list|(
name|NTPD_PRIO
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_ATT_NICE */
ifdef|#
directive|ifdef
name|HAVE_BSD_NICE
operator|(
name|void
operator|)
name|setpriority
argument_list|(
name|PRIO_PROCESS
argument_list|,
literal|0
argument_list|,
name|NTPD_PRIO
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_BSD_NICE */
endif|#
directive|endif
comment|/* !PROCLOCK || !LOCK_PROCESS */
endif|#
directive|endif
comment|/* SYS_HPUX */
comment|/* 	 * Set up signals we pay attention to locally. 	 */
ifdef|#
directive|ifdef
name|SIGDIE1
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE1
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE1 */
ifdef|#
directive|ifdef
name|SIGDIE2
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE2
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE2 */
ifdef|#
directive|ifdef
name|SIGDIE3
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE3
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE3 */
ifdef|#
directive|ifdef
name|SIGDIE4
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE4
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE4 */
ifdef|#
directive|ifdef
name|DEBUG
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|moredebug
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|lessdebug
argument_list|)
expr_stmt|;
else|#
directive|else
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* DEBUG */
comment|/* 	 * Set up signals we should never pay attention to. 	 */
ifdef|#
directive|ifdef
name|SIGPIPE
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGPIPE
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGPIPE */
comment|/* 	 * Call the init_ routines to initialize the data structures. 	 * Note that init_systime() may run a protocol to get a crude 	 * estimate of the time as an NTP client when running on the 	 * gizmo board.  It is important that this be run before 	 * init_subs() since the latter uses the time of day to seed 	 * the random number generator.  That is not the only 	 * dependency between these, either, be real careful about 	 * reordering. 	 */
name|init_auth
argument_list|()
expr_stmt|;
name|init_util
argument_list|()
expr_stmt|;
name|init_restrict
argument_list|()
expr_stmt|;
name|init_mon
argument_list|()
expr_stmt|;
name|init_systime
argument_list|()
expr_stmt|;
name|init_timer
argument_list|()
expr_stmt|;
name|init_lib
argument_list|()
expr_stmt|;
name|init_random
argument_list|()
expr_stmt|;
name|init_request
argument_list|()
expr_stmt|;
name|init_control
argument_list|()
expr_stmt|;
name|init_leap
argument_list|()
expr_stmt|;
name|init_peer
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|REFCLOCK
name|init_refclock
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|init_proto
argument_list|()
expr_stmt|;
name|init_io
argument_list|()
expr_stmt|;
name|init_loopfilter
argument_list|()
expr_stmt|;
name|mon_start
argument_list|(
name|MON_ON
argument_list|)
expr_stmt|;
comment|/* monitor on by default now      */
comment|/* turn off in config if unwanted */
comment|/* 	 * Get configuration.  This (including argument list parsing) is 	 * done in a separate module since this will definitely be different 	 * for the gizmo board. 	 */
name|getconfig
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|initializing
operator|=
literal|0
expr_stmt|;
comment|/* 	 * Report that we're up to any trappers 	 */
name|report_event
argument_list|(
name|EVNT_SYSRESTART
argument_list|,
operator|(
expr|struct
name|peer
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Use select() on all on all input fd's for unlimited 	 * time.  select() will terminate on SIGALARM or on the 	 * reception of input.  Using select() means we can't do 	 * robust signal handling and we get a potential race 	 * between checking for alarms and doing the select(). 	 * Mostly harmless, I think. 	 */
name|was_alarmed
operator|=
literal|0
expr_stmt|;
name|rbuflist
operator|=
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
ifndef|#
directive|ifndef
name|HAVE_SIGNALED_IO
specifier|extern
name|fd_set
name|activefds
decl_stmt|;
specifier|extern
name|int
name|maxactivefd
decl_stmt|;
name|fd_set
name|rdfdes
decl_stmt|;
name|int
name|nfound
decl_stmt|;
else|#
directive|else
name|block_io_and_alarm
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|rbuflist
operator|=
name|getrecvbufs
argument_list|()
expr_stmt|;
comment|/* get received buffers */
if|if
condition|(
name|alarm_flag
condition|)
block|{
comment|/* alarmed? */
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|was_alarmed
operator|&&
name|rbuflist
operator|==
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
condition|)
block|{
comment|/* 			 * Nothing to do.  Wait for something. 			 */
ifndef|#
directive|ifndef
name|HAVE_SIGNALED_IO
name|rdfdes
operator|=
name|activefds
expr_stmt|;
name|nfound
operator|=
name|select
argument_list|(
name|maxactivefd
operator|+
literal|1
argument_list|,
operator|&
name|rdfdes
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|nfound
operator|>
literal|0
condition|)
block|{
name|l_fp
name|ts
decl_stmt|;
name|get_systime
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|input_handler
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|nfound
operator|==
operator|-
literal|1
operator|&&
name|errno
operator|!=
name|EINTR
condition|)
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"select() error: %m"
argument_list|)
expr_stmt|;
else|#
directive|else
name|wait_for_signal
argument_list|()
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|alarm_flag
condition|)
block|{
comment|/* alarmed? */
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
name|rbuflist
operator|=
name|getrecvbufs
argument_list|()
expr_stmt|;
comment|/* get received buffers */
block|}
ifdef|#
directive|ifdef
name|HAVE_SIGNALED_IO
name|unblock_io_and_alarm
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * Out here, signals are unblocked.  Call timer routine 		 * to process expiry. 		 */
if|if
condition|(
name|was_alarmed
condition|)
block|{
name|timer
argument_list|()
expr_stmt|;
name|was_alarmed
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 		 * Call the data procedure to handle each received 		 * packet. 		 */
while|while
condition|(
name|rbuflist
operator|!=
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
condition|)
block|{
name|rbuf
operator|=
name|rbuflist
expr_stmt|;
name|rbuflist
operator|=
name|rbuf
operator|->
name|next
expr_stmt|;
call|(
name|rbuf
operator|->
name|receiver
call|)
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
name|freerecvbuf
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
block|}
comment|/* 		 * Go around again 		 */
block|}
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|SIGDIE1
end_ifdef

begin_comment
comment|/*  * finish - exit gracefully  */
end_comment

begin_function
specifier|static
name|RETSIGTYPE
name|finish
parameter_list|(
name|sig
parameter_list|)
name|int
name|sig
decl_stmt|;
block|{
comment|/* 	 * Log any useful info before exiting. 	 */
ifdef|#
directive|ifdef
name|notdef
name|log_exit_stats
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SIGDIE1 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_comment
comment|/*  * moredebug - increase debugging verbosity  */
end_comment

begin_function
specifier|static
name|RETSIGTYPE
name|moredebug
parameter_list|(
name|sig
parameter_list|)
name|int
name|sig
decl_stmt|;
block|{
if|if
condition|(
name|debug
operator|<
literal|255
condition|)
block|{
name|debug
operator|++
expr_stmt|;
name|syslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug raised to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * lessdebug - decrease debugging verbosity  */
end_comment

begin_function
specifier|static
name|RETSIGTYPE
name|lessdebug
parameter_list|(
name|sig
parameter_list|)
name|int
name|sig
decl_stmt|;
block|{
if|if
condition|(
name|debug
operator|>
literal|0
condition|)
block|{
name|debug
operator|--
expr_stmt|;
name|syslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug lowered to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DEBUG */
end_comment

end_unit

