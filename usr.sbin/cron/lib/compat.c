begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Copyright 1988,1990,1993,1994 by Paul Vixie  * All rights reserved  *  * Distribute freely, except: don't remove my name from the source or  * documentation (don't take credit for my work), mark your changes (don't  * get me blamed for your possible bugs), don't alter or remove this  * notice.  May be sold if buildable source is provided to buyer.  No  * warrantee of any kind, express or implied, is included with this  * software; use at your own risk, responsibility for damages (if any) to  * anyone resulting from the use of this software rests entirely with the  * user.  *  * Send bug reports, bug fixes, enhancements, requests, flames, etc., and  * I'll try to keep a version up to date.  I can be reached as follows:  * Paul Vixie<paul@vix.com>          uunet!decwrl!vixie!paul  */
end_comment

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|lint
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|LINT
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|char
name|rcsid
index|[]
init|=
literal|"$Id: compat.c,v 1.2 1995/05/30 03:47:09 rgrimes Exp $"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* vix 30dec93 [broke this out of misc.c - see RCS log for history]  * vix 15jan87 [added TIOCNOTTY, thanks csg@pyramid]  */
end_comment

begin_include
include|#
directive|include
file|"cron.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_GETDTABLESIZE
end_ifdef

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|NEED_SETSID
argument_list|)
operator|&&
name|defined
argument_list|(
name|BSD
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_comment
comment|/* the code does not depend on any of vfork's  * side-effects; it just uses it as a quick  * fork-and-exec.  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_VFORK
end_ifdef

begin_function
name|PID_T
name|vfork
parameter_list|()
block|{
return|return
operator|(
name|fork
argument_list|()
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_STRDUP
end_ifdef

begin_function
name|char
modifier|*
name|strdup
parameter_list|(
name|str
parameter_list|)
name|char
modifier|*
name|str
decl_stmt|;
block|{
name|char
modifier|*
name|temp
decl_stmt|;
if|if
condition|(
operator|(
name|temp
operator|=
name|malloc
argument_list|(
name|strlen
argument_list|(
name|str
argument_list|)
operator|+
literal|1
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
name|NULL
return|;
block|}
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|temp
argument_list|,
name|str
argument_list|)
expr_stmt|;
return|return
name|temp
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_STRERROR
end_ifdef

begin_function
name|char
modifier|*
name|strerror
parameter_list|(
name|error
parameter_list|)
name|int
name|error
decl_stmt|;
block|{
specifier|extern
name|char
modifier|*
name|sys_errlist
index|[]
decl_stmt|;
specifier|extern
name|int
name|sys_nerr
decl_stmt|;
specifier|static
name|char
name|buf
index|[
literal|32
index|]
decl_stmt|;
if|if
condition|(
operator|(
name|error
operator|<=
name|sys_nerr
operator|)
operator|&&
operator|(
name|error
operator|>
literal|0
operator|)
condition|)
block|{
return|return
name|sys_errlist
index|[
name|error
index|]
return|;
block|}
name|sprintf
argument_list|(
name|buf
argument_list|,
literal|"Unknown error: %d"
argument_list|,
name|error
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_STRCASECMP
end_ifdef

begin_function
name|int
name|strcasecmp
parameter_list|(
name|left
parameter_list|,
name|right
parameter_list|)
name|char
modifier|*
name|left
decl_stmt|;
name|char
modifier|*
name|right
decl_stmt|;
block|{
while|while
condition|(
operator|*
name|left
operator|&&
operator|(
name|MkLower
argument_list|(
operator|*
name|left
argument_list|)
operator|==
name|MkLower
argument_list|(
operator|*
name|right
argument_list|)
operator|)
condition|)
block|{
name|left
operator|++
expr_stmt|;
name|right
operator|++
expr_stmt|;
block|}
return|return
name|MkLower
argument_list|(
operator|*
name|left
argument_list|)
operator|-
name|MkLower
argument_list|(
operator|*
name|right
argument_list|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_SETSID
end_ifdef

begin_function
name|int
name|setsid
parameter_list|()
block|{
name|int
name|newpgrp
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|BSD
argument_list|)
name|int
name|fd
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|POSIX
argument_list|)
name|newpgrp
operator|=
name|setpgid
argument_list|(
operator|(
name|pid_t
operator|)
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
else|#
directive|else
name|newpgrp
operator|=
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|(
name|fd
operator|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|2
argument_list|)
operator|)
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|ioctl
argument_list|(
name|fd
argument_list|,
name|TIOCNOTTY
argument_list|,
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
comment|/*BSD*/
name|newpgrp
operator|=
name|setpgrp
argument_list|()
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|STDIN
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/dev/null"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|STDOUT
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/dev/null"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|STDERR
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/dev/null"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/*BSD*/
return|return
name|newpgrp
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*NEED_SETSID*/
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_GETDTABLESIZE
end_ifdef

begin_function
name|int
name|getdtablesize
parameter_list|()
block|{
ifdef|#
directive|ifdef
name|_SC_OPEN_MAX
return|return
name|sysconf
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
return|;
else|#
directive|else
return|return
name|_POSIX_OPEN_MAX
return|;
endif|#
directive|endif
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_FLOCK
end_ifdef

begin_comment
comment|/* The following flock() emulation snarfed intact *) from the HP-UX  * "BSD to HP-UX porting tricks" maintained by  * system@alchemy.chem.utoronto.ca (System Admin (Mike Peterson))  * from the version "last updated: 11-Jan-1993"  * Snarfage done by Jarkko Hietaniemi<Jarkko.Hietaniemi@hut.fi>  * *) well, almost, had to K&R the function entry, HPUX "cc"  * does not grok ANSI function prototypes */
end_comment

begin_comment
comment|/*  * flock (fd, operation)  *  * This routine performs some file locking like the BSD 'flock'  * on the object described by the int file descriptor 'fd',  * which must already be open.  *  * The operations that are available are:  *  * LOCK_SH  -  get a shared lock.  * LOCK_EX  -  get an exclusive lock.  * LOCK_NB  -  don't block (must be ORed with LOCK_SH or LOCK_EX).  * LOCK_UN  -  release a lock.  *  * Return value: 0 if lock successful, -1 if failed.  *  * Note that whether the locks are enforced or advisory is  * controlled by the presence or absence of the SETGID bit on  * the executable.  *  * Note that there is no difference between shared and exclusive  * locks, since the 'lockf' system call in SYSV doesn't make any  * distinction.  *  * The file "<sys/file.h>" should be modified to contain the definitions  * of the available operations, which must be added manually (see below  * for the values).  */
end_comment

begin_comment
comment|/* this code has been reformatted by vixie */
end_comment

begin_function
name|int
name|flock
parameter_list|(
name|fd
parameter_list|,
name|operation
parameter_list|)
name|int
name|fd
decl_stmt|;
name|int
name|operation
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
switch|switch
condition|(
name|operation
condition|)
block|{
case|case
name|LOCK_SH
case|:
comment|/* get a shared lock */
case|case
name|LOCK_EX
case|:
comment|/* get an exclusive lock */
name|i
operator|=
name|lockf
argument_list|(
name|fd
argument_list|,
name|F_LOCK
argument_list|,
literal|0
argument_list|)
expr_stmt|;
break|break;
case|case
name|LOCK_SH
operator||
name|LOCK_NB
case|:
comment|/* get a non-blocking shared lock */
case|case
name|LOCK_EX
operator||
name|LOCK_NB
case|:
comment|/* get a non-blocking exclusive lock */
name|i
operator|=
name|lockf
argument_list|(
name|fd
argument_list|,
name|F_TLOCK
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
operator|-
literal|1
condition|)
if|if
condition|(
operator|(
name|errno
operator|==
name|EAGAIN
operator|)
operator|||
operator|(
name|errno
operator|==
name|EACCES
operator|)
condition|)
name|errno
operator|=
name|EWOULDBLOCK
expr_stmt|;
break|break;
case|case
name|LOCK_UN
case|:
comment|/* unlock */
name|i
operator|=
name|lockf
argument_list|(
name|fd
argument_list|,
name|F_ULOCK
argument_list|,
literal|0
argument_list|)
expr_stmt|;
break|break;
default|default:
comment|/* can't decipher operation */
name|i
operator|=
operator|-
literal|1
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
break|break;
block|}
return|return
operator|(
name|i
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*NEED_FLOCK*/
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NEED_SETENV
end_ifdef

begin_function
name|int
name|setenv
parameter_list|(
name|name
parameter_list|,
name|value
parameter_list|,
name|overwrite
parameter_list|)
name|char
modifier|*
name|name
decl_stmt|,
decl|*
name|value
decl_stmt|;
end_function

begin_decl_stmt
name|int
name|overwrite
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|tmp
decl_stmt|;
if|if
condition|(
name|overwrite
operator|&&
name|getenv
argument_list|(
name|name
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
operator|!
operator|(
name|tmp
operator|=
name|malloc
argument_list|(
name|strlen
argument_list|(
name|name
argument_list|)
operator|+
name|strlen
argument_list|(
name|value
argument_list|)
operator|+
literal|2
argument_list|)
operator|)
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|sprintf
argument_list|(
name|tmp
argument_list|,
literal|"%s=%s"
argument_list|,
name|name
argument_list|,
name|value
argument_list|)
expr_stmt|;
return|return
name|putenv
argument_list|(
name|tmp
argument_list|)
return|;
comment|/* intentionally orphan 'tmp' storage */
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

