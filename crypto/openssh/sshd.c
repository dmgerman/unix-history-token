begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Created: Fri Mar 17 17:09:28 1995 ylo  * This program is the ssh daemon.  It listens for connections from clients, and  * performs authentication, executes use commands or shell, and forwards  * information to/from the application to the user client over an encrypted  * connection.  This can also handle forwarding of X11, TCP/IP, and authentication  * agent connections.  *   * $FreeBSD$  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$OpenBSD: sshd.c,v 1.88 2000/02/15 16:52:57 markus Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|"xmalloc.h"
end_include

begin_include
include|#
directive|include
file|"rsa.h"
end_include

begin_include
include|#
directive|include
file|"ssh.h"
end_include

begin_include
include|#
directive|include
file|"pty.h"
end_include

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"buffer.h"
end_include

begin_include
include|#
directive|include
file|"cipher.h"
end_include

begin_include
include|#
directive|include
file|"mpaux.h"
end_include

begin_include
include|#
directive|include
file|"servconf.h"
end_include

begin_include
include|#
directive|include
file|"uidswap.h"
end_include

begin_include
include|#
directive|include
file|"compat.h"
end_include

begin_include
include|#
directive|include
file|<poll.h>
end_include

begin_include
include|#
directive|include
file|<time.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|LIBWRAP
end_ifdef

begin_include
include|#
directive|include
file|<tcpd.h>
end_include

begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_decl_stmt
name|int
name|allow_severity
init|=
name|LOG_INFO
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|deny_severity
init|=
name|LOG_WARNING
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBWRAP */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|__FreeBSD__
end_ifdef

begin_include
include|#
directive|include
file|<libutil.h>
end_include

begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_define
define|#
directive|define
name|LOGIN_CAP
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __FreeBSD__ */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|LOGIN_CAP
end_ifdef

begin_include
include|#
directive|include
file|<login_cap.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LOGIN_CAP */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|O_NOCTTY
end_ifndef

begin_define
define|#
directive|define
name|O_NOCTTY
value|0
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* Local Xauthority file. */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|xauthfile
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Server configuration options. */
end_comment

begin_decl_stmt
name|ServerOptions
name|options
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Name of the server configuration file. */
end_comment

begin_decl_stmt
name|char
modifier|*
name|config_file_name
init|=
name|SERVER_CONFIG_FILE
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*   * Flag indicating whether IPv4 or IPv6.  This can be set on the command line.  * Default value is AF_UNSPEC means both IPv4 and IPv6.  */
end_comment

begin_decl_stmt
name|int
name|IPv4or6
init|=
name|AF_UNSPEC
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Debug mode flag.  This can be set on the command line.  If debug  * mode is enabled, extra debugging output will be sent to the system  * log, the daemon will not go to background, and will exit after processing  * the first connection.  */
end_comment

begin_decl_stmt
name|int
name|debug_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Flag indicating that the daemon is being started from inetd. */
end_comment

begin_decl_stmt
name|int
name|inetd_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* debug goes to stderr unless inetd_flag is set */
end_comment

begin_decl_stmt
name|int
name|log_stderr
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* argv[0] without path. */
end_comment

begin_decl_stmt
name|char
modifier|*
name|av0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Saved arguments to main(). */
end_comment

begin_decl_stmt
name|char
modifier|*
modifier|*
name|saved_argv
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * The sockets that the server is listening; this is used in the SIGHUP  * signal handler.  */
end_comment

begin_define
define|#
directive|define
name|MAX_LISTEN_SOCKS
value|16
end_define

begin_decl_stmt
name|int
name|listen_socks
index|[
name|MAX_LISTEN_SOCKS
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|num_listen_socks
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * the client's version string, passed by sshd2 in compat mode. if != NULL,  * sshd will skip the version-number exchange  */
end_comment

begin_decl_stmt
name|char
modifier|*
name|client_version_string
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Flags set in auth-rsa from authorized_keys flags.  These are set in auth-rsa.c. */
end_comment

begin_decl_stmt
name|int
name|no_port_forwarding_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|no_agent_forwarding_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|no_x11_forwarding_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|no_pty_flag
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* RSA authentication "command=" option. */
end_comment

begin_decl_stmt
name|char
modifier|*
name|forced_command
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* RSA authentication "environment=" options. */
end_comment

begin_decl_stmt
name|struct
name|envstring
modifier|*
name|custom_environment
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Session id for the current session. */
end_comment

begin_decl_stmt
name|unsigned
name|char
name|session_id
index|[
literal|16
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Any really sensitive data in the application is contained in this  * structure. The idea is that this structure could be locked into memory so  * that the pages do not get written into swap.  However, there are some  * problems. The private key contains BIGNUMs, and we do not (in principle)  * have access to the internals of them, and locking just the structure is  * not very useful.  Currently, memory locking is not implemented.  */
end_comment

begin_struct
struct|struct
block|{
name|RSA
modifier|*
name|private_key
decl_stmt|;
comment|/* Private part of server key. */
name|RSA
modifier|*
name|host_key
decl_stmt|;
comment|/* Private part of host key. */
block|}
name|sensitive_data
struct|;
end_struct

begin_comment
comment|/*  * Flag indicating whether the current session key has been used.  This flag  * is set whenever the key is used, and cleared when the key is regenerated.  */
end_comment

begin_decl_stmt
name|int
name|key_used
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* This is set to true when SIGHUP is received. */
end_comment

begin_decl_stmt
name|int
name|received_sighup
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Public side of the server key.  This value is regenerated regularly with    the private key. */
end_comment

begin_decl_stmt
name|RSA
modifier|*
name|public_key
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* These are used to implement connections_per_period. */
end_comment

begin_struct
struct|struct
name|magic_connection
block|{
name|struct
name|timeval
name|connections_begin
decl_stmt|;
name|unsigned
name|int
name|connections_this_period
decl_stmt|;
block|}
modifier|*
name|magic_connections
struct|;
end_struct

begin_comment
comment|/* Magic number, too!  TODO: this doesn't have to be static. */
end_comment

begin_decl_stmt
specifier|const
name|size_t
name|MAGIC_CONNECTIONS_SIZE
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|__inline
name|int
name|magic_hash
parameter_list|(
name|struct
name|sockaddr_storage
modifier|*
name|sa
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_expr_stmt
specifier|static
name|__inline
expr|struct
name|timeval
name|timevaldiff
argument_list|(
argument|struct timeval *tv1
argument_list|,
argument|struct timeval *tv2
argument_list|)
block|{ 	struct
name|timeval
name|diff
block|;
name|int
name|carry
block|;
name|carry
operator|=
name|tv1
operator|->
name|tv_usec
operator|>
name|tv2
operator|->
name|tv_usec
block|;
name|diff
operator|.
name|tv_sec
operator|=
name|tv2
operator|->
name|tv_sec
operator|-
name|tv1
operator|->
name|tv_sec
operator|-
operator|(
name|carry
condition|?
literal|0
else|:
literal|1
operator|)
block|;
name|diff
operator|.
name|tv_usec
operator|=
name|tv2
operator|->
name|tv_usec
operator|-
name|tv1
operator|->
name|tv_usec
operator|+
operator|(
name|carry
condition|?
literal|1000000
else|:
literal|0
operator|)
block|;
return|return
name|diff
return|;
block|}
end_expr_stmt

begin_comment
comment|/* Prototypes for various functions defined later in this file. */
end_comment

begin_function_decl
name|void
name|do_ssh_kex
parameter_list|()
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_authentication
parameter_list|()
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_authloop
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_fake_authloop
parameter_list|(
name|char
modifier|*
name|user
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_authenticated
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_exec_pty
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|int
name|ptyfd
parameter_list|,
name|int
name|ttyfd
parameter_list|,
specifier|const
name|char
modifier|*
name|ttyname
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|term
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_exec_no_pty
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|void
name|do_child
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|term
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|,
specifier|const
name|char
modifier|*
name|ttyname
parameter_list|)
function_decl|;
end_function_decl

begin_comment
comment|/*  * Remove local Xauthority file.  */
end_comment

begin_function
name|void
name|xauthfile_cleanup_proc
parameter_list|(
name|void
modifier|*
name|ignore
parameter_list|)
block|{
name|debug
argument_list|(
literal|"xauthfile_cleanup_proc called"
argument_list|)
expr_stmt|;
if|if
condition|(
name|xauthfile
operator|!=
name|NULL
condition|)
block|{
name|char
modifier|*
name|p
decl_stmt|;
name|unlink
argument_list|(
name|xauthfile
argument_list|)
expr_stmt|;
name|p
operator|=
name|strrchr
argument_list|(
name|xauthfile
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
block|{
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
name|rmdir
argument_list|(
name|xauthfile
argument_list|)
expr_stmt|;
block|}
name|xfree
argument_list|(
name|xauthfile
argument_list|)
expr_stmt|;
name|xauthfile
operator|=
name|NULL
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * Close all listening sockets  */
end_comment

begin_function
name|void
name|close_listen_socks
parameter_list|(
name|void
parameter_list|)
block|{
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_listen_socks
condition|;
name|i
operator|++
control|)
name|close
argument_list|(
name|listen_socks
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|num_listen_socks
operator|=
operator|-
literal|1
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Signal handler for SIGHUP.  Sshd execs itself when it receives SIGHUP;  * the effect is to reread the configuration file (and to regenerate  * the server key).  */
end_comment

begin_function
name|void
name|sighup_handler
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|received_sighup
operator|=
literal|1
expr_stmt|;
name|signal
argument_list|(
name|SIGHUP
argument_list|,
name|sighup_handler
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Called from the main program after receiving SIGHUP.  * Restarts the server.  */
end_comment

begin_function
name|void
name|sighup_restart
parameter_list|()
block|{
name|log
argument_list|(
literal|"Received SIGHUP; restarting."
argument_list|)
expr_stmt|;
name|close_listen_socks
argument_list|()
expr_stmt|;
name|execv
argument_list|(
name|saved_argv
index|[
literal|0
index|]
argument_list|,
name|saved_argv
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"RESTART FAILED: av0='%s', error: %s."
argument_list|,
name|av0
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Generic signal handler for terminating signals in the master daemon.  * These close the listen socket; not closing it seems to cause "Address  * already in use" problems on some machines, which is inconvenient.  */
end_comment

begin_function
name|void
name|sigterm_handler
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|log
argument_list|(
literal|"Received signal %d; terminating."
argument_list|,
name|sig
argument_list|)
expr_stmt|;
name|close_listen_socks
argument_list|()
expr_stmt|;
name|exit
argument_list|(
literal|255
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * SIGCHLD handler.  This is called whenever a child dies.  This will then  * reap any zombies left by exited c.  */
end_comment

begin_function
name|void
name|main_sigchld_handler
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|save_errno
init|=
name|errno
decl_stmt|;
name|int
name|status
decl_stmt|;
while|while
condition|(
name|waitpid
argument_list|(
operator|-
literal|1
argument_list|,
operator|&
name|status
argument_list|,
name|WNOHANG
argument_list|)
operator|>
literal|0
condition|)
empty_stmt|;
name|signal
argument_list|(
name|SIGCHLD
argument_list|,
name|main_sigchld_handler
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Signal handler for the alarm after the login grace period has expired.  */
end_comment

begin_function
name|void
name|grace_alarm_handler
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
comment|/* Close the connection. */
name|packet_close
argument_list|()
expr_stmt|;
comment|/* Log error and exit. */
name|fatal
argument_list|(
literal|"Timeout before authentication for %s."
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * convert ssh auth msg type into description  */
end_comment

begin_function
name|char
modifier|*
name|get_authname
parameter_list|(
name|int
name|type
parameter_list|)
block|{
specifier|static
name|char
name|buf
index|[
literal|1024
index|]
decl_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|SSH_CMSG_AUTH_PASSWORD
case|:
return|return
literal|"password"
return|;
case|case
name|SSH_CMSG_AUTH_RSA
case|:
return|return
literal|"rsa"
return|;
case|case
name|SSH_CMSG_AUTH_RHOSTS_RSA
case|:
return|return
literal|"rhosts-rsa"
return|;
case|case
name|SSH_CMSG_AUTH_RHOSTS
case|:
return|return
literal|"rhosts"
return|;
ifdef|#
directive|ifdef
name|KRB4
case|case
name|SSH_CMSG_AUTH_KERBEROS
case|:
return|return
literal|"kerberos"
return|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|SKEY
case|case
name|SSH_CMSG_AUTH_TIS_RESPONSE
case|:
return|return
literal|"s/key"
return|;
endif|#
directive|endif
block|}
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"bad-auth-msg-%d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_comment
comment|/*  * Signal handler for the key regeneration alarm.  Note that this  * alarm only occurs in the daemon waiting for connections, and it does not  * do anything with the private key or random state before forking.  * Thus there should be no concurrency control/asynchronous execution  * problems.  */
end_comment

begin_function
name|void
name|key_regeneration_alarm
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|save_errno
init|=
name|errno
decl_stmt|;
comment|/* Check if we should generate a new key. */
if|if
condition|(
name|key_used
condition|)
block|{
comment|/* This should really be done in the background. */
name|log
argument_list|(
literal|"Generating new %d bit RSA key."
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
if|if
condition|(
name|sensitive_data
operator|.
name|private_key
operator|!=
name|NULL
condition|)
name|RSA_free
argument_list|(
name|sensitive_data
operator|.
name|private_key
argument_list|)
expr_stmt|;
name|sensitive_data
operator|.
name|private_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
if|if
condition|(
name|public_key
operator|!=
name|NULL
condition|)
name|RSA_free
argument_list|(
name|public_key
argument_list|)
expr_stmt|;
name|public_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|rsa_generate_key
argument_list|(
name|sensitive_data
operator|.
name|private_key
argument_list|,
name|public_key
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
name|arc4random_stir
argument_list|()
expr_stmt|;
name|key_used
operator|=
literal|0
expr_stmt|;
name|log
argument_list|(
literal|"RSA key generation complete."
argument_list|)
expr_stmt|;
block|}
comment|/* Reschedule the alarm. */
name|signal
argument_list|(
name|SIGALRM
argument_list|,
name|key_regeneration_alarm
argument_list|)
expr_stmt|;
name|alarm
argument_list|(
name|options
operator|.
name|key_regeneration_time
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Main program for the daemon.  */
end_comment

begin_function
name|int
name|main
parameter_list|(
name|int
name|ac
parameter_list|,
name|char
modifier|*
modifier|*
name|av
parameter_list|)
block|{
specifier|extern
name|char
modifier|*
name|optarg
decl_stmt|;
specifier|extern
name|int
name|optind
decl_stmt|;
name|int
name|opt
decl_stmt|,
name|sock_in
init|=
literal|0
decl_stmt|,
name|sock_out
init|=
literal|0
decl_stmt|,
name|newsock
decl_stmt|,
name|i
decl_stmt|,
name|fdsetsz
decl_stmt|,
name|pid
decl_stmt|,
name|on
init|=
literal|1
decl_stmt|;
name|socklen_t
name|fromlen
decl_stmt|;
name|int
name|connections_per_period_exceeded
init|=
literal|0
decl_stmt|;
name|int
name|remote_major
decl_stmt|,
name|remote_minor
decl_stmt|;
name|int
name|silentrsa
init|=
literal|0
decl_stmt|;
name|fd_set
modifier|*
name|fdset
decl_stmt|;
name|struct
name|sockaddr_storage
name|from
decl_stmt|;
name|char
name|buf
index|[
literal|100
index|]
decl_stmt|;
comment|/* Must not be larger than remote_version. */
name|char
name|remote_version
index|[
literal|100
index|]
decl_stmt|;
comment|/* Must be at least as big as buf. */
specifier|const
name|char
modifier|*
name|remote_ip
decl_stmt|;
name|int
name|remote_port
decl_stmt|;
name|char
modifier|*
name|comment
decl_stmt|;
name|FILE
modifier|*
name|f
decl_stmt|;
name|struct
name|linger
name|linger
decl_stmt|;
name|struct
name|addrinfo
modifier|*
name|ai
decl_stmt|;
name|char
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|,
name|strport
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
name|int
name|listen_sock
decl_stmt|,
name|maxfd
decl_stmt|;
comment|/* Save argv[0]. */
name|saved_argv
operator|=
name|av
expr_stmt|;
if|if
condition|(
name|strchr
argument_list|(
name|av
index|[
literal|0
index|]
argument_list|,
literal|'/'
argument_list|)
condition|)
name|av0
operator|=
name|strrchr
argument_list|(
name|av
index|[
literal|0
index|]
argument_list|,
literal|'/'
argument_list|)
operator|+
literal|1
expr_stmt|;
else|else
name|av0
operator|=
name|av
index|[
literal|0
index|]
expr_stmt|;
comment|/* Initialize configuration options to their default values. */
name|initialize_server_options
argument_list|(
operator|&
name|options
argument_list|)
expr_stmt|;
comment|/* Parse command-line arguments. */
while|while
condition|(
operator|(
name|opt
operator|=
name|getopt
argument_list|(
name|ac
argument_list|,
name|av
argument_list|,
literal|"f:p:b:k:h:g:V:diqQ46"
argument_list|)
operator|)
operator|!=
name|EOF
condition|)
block|{
switch|switch
condition|(
name|opt
condition|)
block|{
case|case
literal|'4'
case|:
name|IPv4or6
operator|=
name|AF_INET
expr_stmt|;
break|break;
case|case
literal|'6'
case|:
name|IPv4or6
operator|=
name|AF_INET6
expr_stmt|;
break|break;
case|case
literal|'f'
case|:
name|config_file_name
operator|=
name|optarg
expr_stmt|;
break|break;
case|case
literal|'d'
case|:
name|debug_flag
operator|=
literal|1
expr_stmt|;
name|options
operator|.
name|log_level
operator|=
name|SYSLOG_LEVEL_DEBUG
expr_stmt|;
break|break;
case|case
literal|'i'
case|:
name|inetd_flag
operator|=
literal|1
expr_stmt|;
break|break;
case|case
literal|'Q'
case|:
name|silentrsa
operator|=
literal|1
expr_stmt|;
break|break;
case|case
literal|'q'
case|:
name|options
operator|.
name|log_level
operator|=
name|SYSLOG_LEVEL_QUIET
expr_stmt|;
break|break;
case|case
literal|'b'
case|:
name|options
operator|.
name|server_key_bits
operator|=
name|atoi
argument_list|(
name|optarg
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'p'
case|:
name|options
operator|.
name|ports_from_cmdline
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|num_ports
operator|>=
name|MAX_PORTS
condition|)
name|fatal
argument_list|(
literal|"too many ports.\n"
argument_list|)
expr_stmt|;
name|options
operator|.
name|ports
index|[
name|options
operator|.
name|num_ports
operator|++
index|]
operator|=
name|atoi
argument_list|(
name|optarg
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'g'
case|:
name|options
operator|.
name|login_grace_time
operator|=
name|atoi
argument_list|(
name|optarg
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'k'
case|:
name|options
operator|.
name|key_regeneration_time
operator|=
name|atoi
argument_list|(
name|optarg
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'h'
case|:
name|options
operator|.
name|host_key_file
operator|=
name|optarg
expr_stmt|;
break|break;
case|case
literal|'V'
case|:
name|client_version_string
operator|=
name|optarg
expr_stmt|;
comment|/* only makes sense with inetd_flag, i.e. no listen() */
name|inetd_flag
operator|=
literal|1
expr_stmt|;
break|break;
case|case
literal|'?'
case|:
default|default:
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"sshd version %s\n"
argument_list|,
name|SSH_VERSION
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Usage: %s [options]\n"
argument_list|,
name|av0
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Options:\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -f file    Configuration file (default %s)\n"
argument_list|,
name|SERVER_CONFIG_FILE
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -d         Debugging mode\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -i         Started from inetd\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -q         Quiet (no logging)\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -p port    Listen on the specified port (default: 22)\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -k seconds Regenerate server key every this many seconds (default: 3600)\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -g seconds Grace period for authentication (default: 300)\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -b bits    Size of server RSA key (default: 768 bits)\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -h file    File from which to read host key (default: %s)\n"
argument_list|,
name|HOST_KEY_FILE
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -4         Use IPv4 only\n"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  -6         Use IPv6 only\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* 	 * Force logging to stderr until we have loaded the private host 	 * key (unless started from inetd) 	 */
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
operator|==
operator|-
literal|1
condition|?
name|SYSLOG_LEVEL_INFO
else|:
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
operator|==
operator|-
literal|1
condition|?
name|SYSLOG_FACILITY_AUTH
else|:
name|options
operator|.
name|log_facility
argument_list|,
operator|!
name|inetd_flag
argument_list|)
expr_stmt|;
comment|/* check if RSA support exists */
if|if
condition|(
name|rsa_alive
argument_list|()
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|silentrsa
operator|==
literal|0
condition|)
name|printf
argument_list|(
literal|"sshd: no RSA support in libssl and libcrypto -- exiting.  See ssl(8)\n"
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"no RSA support in libssl and libcrypto -- exiting.  See ssl(8)"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
comment|/* Read server configuration options from the configuration file. */
name|read_server_config
argument_list|(
operator|&
name|options
argument_list|,
name|config_file_name
argument_list|)
expr_stmt|;
comment|/* Fill in default values for those options not explicitly set. */
name|fill_default_server_options
argument_list|(
operator|&
name|options
argument_list|)
expr_stmt|;
comment|/* Check certain values for sanity. */
if|if
condition|(
name|options
operator|.
name|server_key_bits
operator|<
literal|512
operator|||
name|options
operator|.
name|server_key_bits
operator|>
literal|32768
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Bad server key size.\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
comment|/* Check that there are no remaining arguments. */
if|if
condition|(
name|optind
operator|<
name|ac
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Extra argument %s.\n"
argument_list|,
name|av
index|[
name|optind
index|]
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|debug
argument_list|(
literal|"sshd version %.100s"
argument_list|,
name|SSH_VERSION
argument_list|)
expr_stmt|;
name|sensitive_data
operator|.
name|host_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
comment|/* Load the host key.  It must have empty passphrase. */
if|if
condition|(
operator|!
name|load_private_key
argument_list|(
name|options
operator|.
name|host_key_file
argument_list|,
literal|""
argument_list|,
name|sensitive_data
operator|.
name|host_key
argument_list|,
operator|&
name|comment
argument_list|)
condition|)
block|{
name|error
argument_list|(
literal|"Could not load host key: %.200s: %.100s"
argument_list|,
name|options
operator|.
name|host_key_file
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|xfree
argument_list|(
name|comment
argument_list|)
expr_stmt|;
comment|/* Initialize the log (it is reinitialized below in case we 	   forked). */
if|if
condition|(
name|debug_flag
operator|&&
operator|!
name|inetd_flag
condition|)
name|log_stderr
operator|=
literal|1
expr_stmt|;
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
argument_list|,
name|log_stderr
argument_list|)
expr_stmt|;
comment|/* If not in debugging mode, and not started from inetd, 	   disconnect from the controlling terminal, and fork.  The 	   original process exits. */
if|if
condition|(
operator|!
name|debug_flag
operator|&&
operator|!
name|inetd_flag
condition|)
block|{
ifdef|#
directive|ifdef
name|TIOCNOTTY
name|int
name|fd
decl_stmt|;
endif|#
directive|endif
comment|/* TIOCNOTTY */
if|if
condition|(
name|daemon
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
name|fatal
argument_list|(
literal|"daemon() failed: %.200s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Disconnect from the controlling tty. */
ifdef|#
directive|ifdef
name|TIOCNOTTY
name|fd
operator|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
name|O_RDWR
operator||
name|O_NOCTTY
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|ioctl
argument_list|(
name|fd
argument_list|,
name|TIOCNOTTY
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* TIOCNOTTY */
block|}
comment|/* Reinitialize the log (because of the fork above). */
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
argument_list|,
name|log_stderr
argument_list|)
expr_stmt|;
comment|/* Check that server and host key lengths differ sufficiently. 	   This is necessary to make double encryption work with rsaref. 	   Oh, I hate software patents. I dont know if this can go? Niels */
if|if
condition|(
name|options
operator|.
name|server_key_bits
operator|>
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|-
name|SSH_KEY_BITS_RESERVED
operator|&&
name|options
operator|.
name|server_key_bits
operator|<
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
condition|)
block|{
name|options
operator|.
name|server_key_bits
operator|=
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
expr_stmt|;
name|debug
argument_list|(
literal|"Forcing server key to %d bits to make it differ from host key."
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
block|}
comment|/* Do not display messages to stdout in RSA code. */
name|rsa_set_verbose
argument_list|(
literal|0
argument_list|)
expr_stmt|;
comment|/* Initialize the random number generator. */
name|arc4random_stir
argument_list|()
expr_stmt|;
comment|/* Chdir to the root directory so that the current disk can be 	   unmounted if desired. */
name|chdir
argument_list|(
literal|"/"
argument_list|)
expr_stmt|;
comment|/* Close connection cleanly after attack. */
name|cipher_attack_detected
operator|=
name|packet_disconnect
expr_stmt|;
comment|/* Start listening for a socket, unless started from inetd. */
if|if
condition|(
name|inetd_flag
condition|)
block|{
name|int
name|s1
decl_stmt|,
name|s2
decl_stmt|;
name|s1
operator|=
name|dup
argument_list|(
literal|0
argument_list|)
expr_stmt|;
comment|/* Make sure descriptors 0, 1, and 2 are in use. */
name|s2
operator|=
name|dup
argument_list|(
name|s1
argument_list|)
expr_stmt|;
name|sock_in
operator|=
name|dup
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|sock_out
operator|=
name|dup
argument_list|(
literal|1
argument_list|)
expr_stmt|;
comment|/* We intentionally do not close the descriptors 0, 1, and 2 		   as our code for setting the descriptors won\'t work 		   if ttyfd happens to be one of those. */
name|debug
argument_list|(
literal|"inetd sockets after dupping: %d, %d"
argument_list|,
name|sock_in
argument_list|,
name|sock_out
argument_list|)
expr_stmt|;
name|public_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|sensitive_data
operator|.
name|private_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|log
argument_list|(
literal|"Generating %d bit RSA key."
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
name|rsa_generate_key
argument_list|(
name|sensitive_data
operator|.
name|private_key
argument_list|,
name|public_key
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
name|arc4random_stir
argument_list|()
expr_stmt|;
name|log
argument_list|(
literal|"RSA key generation complete."
argument_list|)
expr_stmt|;
block|}
else|else
block|{
for|for
control|(
name|ai
operator|=
name|options
operator|.
name|listen_addrs
init|;
name|ai
condition|;
name|ai
operator|=
name|ai
operator|->
name|ai_next
control|)
block|{
if|if
condition|(
name|ai
operator|->
name|ai_family
operator|!=
name|AF_INET
operator|&&
name|ai
operator|->
name|ai_family
operator|!=
name|AF_INET6
condition|)
continue|continue;
if|if
condition|(
name|num_listen_socks
operator|>=
name|MAX_LISTEN_SOCKS
condition|)
name|fatal
argument_list|(
literal|"Too many listen sockets. "
literal|"Enlarge MAX_LISTEN_SOCKS"
argument_list|)
expr_stmt|;
if|if
condition|(
name|getnameinfo
argument_list|(
name|ai
operator|->
name|ai_addr
argument_list|,
name|ai
operator|->
name|ai_addrlen
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|strport
argument_list|,
sizeof|sizeof
argument_list|(
name|strport
argument_list|)
argument_list|,
name|NI_NUMERICHOST
operator||
name|NI_NUMERICSERV
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"getnameinfo failed"
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Create socket for listening. */
name|listen_sock
operator|=
name|socket
argument_list|(
name|ai
operator|->
name|ai_family
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|listen_sock
operator|<
literal|0
condition|)
block|{
comment|/* kernel may not support ipv6 */
name|verbose
argument_list|(
literal|"socket: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|fcntl
argument_list|(
name|listen_sock
argument_list|,
name|F_SETFL
argument_list|,
name|O_NONBLOCK
argument_list|)
operator|<
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"listen_sock O_NONBLOCK: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|listen_sock
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* 			 * Set socket options.  We try to make the port 			 * reusable and have it close as fast as possible 			 * without waiting in unnecessary wait states on 			 * close. 			 */
name|setsockopt
argument_list|(
name|listen_sock
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
name|linger
operator|.
name|l_onoff
operator|=
literal|1
expr_stmt|;
name|linger
operator|.
name|l_linger
operator|=
literal|5
expr_stmt|;
name|setsockopt
argument_list|(
name|listen_sock
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_LINGER
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|linger
argument_list|,
sizeof|sizeof
argument_list|(
name|linger
argument_list|)
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Bind to port %s on %s."
argument_list|,
name|strport
argument_list|,
name|ntop
argument_list|)
expr_stmt|;
comment|/* Bind the socket to the desired port. */
if|if
condition|(
name|bind
argument_list|(
name|listen_sock
argument_list|,
name|ai
operator|->
name|ai_addr
argument_list|,
name|ai
operator|->
name|ai_addrlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"Bind to port %s on %s failed: %.200s."
argument_list|,
name|strport
argument_list|,
name|ntop
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|listen_sock
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|listen_socks
index|[
name|num_listen_socks
index|]
operator|=
name|listen_sock
expr_stmt|;
name|num_listen_socks
operator|++
expr_stmt|;
comment|/* Start listening on the port. */
name|log
argument_list|(
literal|"Server listening on %s port %s."
argument_list|,
name|ntop
argument_list|,
name|strport
argument_list|)
expr_stmt|;
if|if
condition|(
name|listen
argument_list|(
name|listen_sock
argument_list|,
literal|5
argument_list|)
operator|<
literal|0
condition|)
name|fatal
argument_list|(
literal|"listen: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|freeaddrinfo
argument_list|(
name|options
operator|.
name|listen_addrs
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|num_listen_socks
condition|)
name|fatal
argument_list|(
literal|"Cannot bind any address."
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|debug_flag
condition|)
block|{
comment|/* 			 * Record our pid in /etc/sshd_pid to make it easier 			 * to kill the correct sshd.  We don\'t want to do 			 * this before the bind above because the bind will 			 * fail if there already is a daemon, and this will 			 * overwrite any old pid in the file. 			 */
name|f
operator|=
name|fopen
argument_list|(
name|SSH_DAEMON_PID_FILE
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
condition|)
block|{
name|fprintf
argument_list|(
name|f
argument_list|,
literal|"%u\n"
argument_list|,
operator|(
name|unsigned
name|int
operator|)
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
block|}
name|public_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|sensitive_data
operator|.
name|private_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|log
argument_list|(
literal|"Generating %d bit RSA key."
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
name|rsa_generate_key
argument_list|(
name|sensitive_data
operator|.
name|private_key
argument_list|,
name|public_key
argument_list|,
name|options
operator|.
name|server_key_bits
argument_list|)
expr_stmt|;
name|arc4random_stir
argument_list|()
expr_stmt|;
name|log
argument_list|(
literal|"RSA key generation complete."
argument_list|)
expr_stmt|;
comment|/* Schedule server key regeneration alarm. */
name|signal
argument_list|(
name|SIGALRM
argument_list|,
name|key_regeneration_alarm
argument_list|)
expr_stmt|;
name|alarm
argument_list|(
name|options
operator|.
name|key_regeneration_time
argument_list|)
expr_stmt|;
comment|/* Arrange to restart on SIGHUP.  The handler needs listen_sock. */
name|signal
argument_list|(
name|SIGHUP
argument_list|,
name|sighup_handler
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTERM
argument_list|,
name|sigterm_handler
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGQUIT
argument_list|,
name|sigterm_handler
argument_list|)
expr_stmt|;
comment|/* Arrange SIGCHLD to be caught. */
name|signal
argument_list|(
name|SIGCHLD
argument_list|,
name|main_sigchld_handler
argument_list|)
expr_stmt|;
comment|/* setup fd set for listen */
name|maxfd
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_listen_socks
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|listen_socks
index|[
name|i
index|]
operator|>
name|maxfd
condition|)
name|maxfd
operator|=
name|listen_socks
index|[
name|i
index|]
expr_stmt|;
name|fdsetsz
operator|=
name|howmany
argument_list|(
name|maxfd
argument_list|,
name|NFDBITS
argument_list|)
operator|*
sizeof|sizeof
argument_list|(
name|fd_mask
argument_list|)
expr_stmt|;
name|fdset
operator|=
operator|(
name|fd_set
operator|*
operator|)
name|xmalloc
argument_list|(
name|fdsetsz
argument_list|)
expr_stmt|;
comment|/* Initialize the magic_connections table.  It's magical! */
name|magic_connections
operator|=
name|calloc
argument_list|(
name|MAGIC_CONNECTIONS_SIZE
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|magic_connection
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|magic_connections
operator|==
name|NULL
condition|)
name|fatal
argument_list|(
literal|"calloc: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 		 * Stay listening for connections until the system crashes or 		 * the daemon is killed with a signal. 		 */
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|received_sighup
condition|)
name|sighup_restart
argument_list|()
expr_stmt|;
comment|/* Wait in select until there is a connection. */
name|memset
argument_list|(
name|fdset
argument_list|,
literal|0
argument_list|,
name|fdsetsz
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_listen_socks
condition|;
name|i
operator|++
control|)
name|FD_SET
argument_list|(
name|listen_socks
index|[
name|i
index|]
argument_list|,
name|fdset
argument_list|)
expr_stmt|;
if|if
condition|(
name|select
argument_list|(
name|maxfd
operator|+
literal|1
argument_list|,
name|fdset
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|!=
name|EINTR
condition|)
name|error
argument_list|(
literal|"select: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
continue|continue;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_listen_socks
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
operator|!
name|FD_ISSET
argument_list|(
name|listen_socks
index|[
name|i
index|]
argument_list|,
name|fdset
argument_list|)
condition|)
continue|continue;
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|newsock
operator|=
name|accept
argument_list|(
name|listen_socks
index|[
name|i
index|]
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
expr_stmt|;
if|if
condition|(
name|newsock
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|!=
name|EINTR
operator|&&
name|errno
operator|!=
name|EWOULDBLOCK
condition|)
name|error
argument_list|(
literal|"accept: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|fcntl
argument_list|(
name|newsock
argument_list|,
name|F_SETFL
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"newsock del O_NONBLOCK: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|options
operator|.
name|connections_per_period
operator|!=
literal|0
condition|)
block|{
name|struct
name|timeval
name|diff
decl_stmt|,
name|connections_end
decl_stmt|;
name|struct
name|magic_connection
modifier|*
name|mc
decl_stmt|;
operator|(
name|void
operator|)
name|gettimeofday
argument_list|(
operator|&
name|connections_end
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|mc
operator|=
operator|&
name|magic_connections
index|[
name|magic_hash
argument_list|(
operator|&
name|from
argument_list|)
index|]
expr_stmt|;
name|diff
operator|=
name|timevaldiff
argument_list|(
operator|&
name|mc
operator|->
name|connections_begin
argument_list|,
operator|&
name|connections_end
argument_list|)
expr_stmt|;
if|if
condition|(
name|diff
operator|.
name|tv_sec
operator|>=
name|options
operator|.
name|connections_period
condition|)
block|{
comment|/* 					 * Slide the window forward only after completely 					 * leaving it. 					 */
name|mc
operator|->
name|connections_begin
operator|=
name|connections_end
expr_stmt|;
name|mc
operator|->
name|connections_this_period
operator|=
literal|1
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
operator|++
name|mc
operator|->
name|connections_this_period
operator|>
name|options
operator|.
name|connections_per_period
condition|)
name|connections_per_period_exceeded
operator|=
literal|1
expr_stmt|;
block|}
block|}
comment|/* 			 * Got connection.  Fork a child to handle it unless 			 * we are in debugging mode or the maximum number of 			 * connections per period has been exceeded. 			 */
if|if
condition|(
name|debug_flag
condition|)
block|{
comment|/* 				 * In debugging mode.  Close the listening 				 * socket, and start processing the 				 * connection without forking. 				 */
name|debug
argument_list|(
literal|"Server will not fork when running in debugging mode."
argument_list|)
expr_stmt|;
name|close_listen_socks
argument_list|()
expr_stmt|;
name|sock_in
operator|=
name|newsock
expr_stmt|;
name|sock_out
operator|=
name|newsock
expr_stmt|;
name|pid
operator|=
name|getpid
argument_list|()
expr_stmt|;
break|break;
block|}
elseif|else
if|if
condition|(
name|connections_per_period_exceeded
condition|)
block|{
name|log
argument_list|(
literal|"Connection rate limit of %u/%us has been exceeded; "
literal|"dropping connection from %s."
argument_list|,
name|options
operator|.
name|connections_per_period
argument_list|,
name|options
operator|.
name|connections_period
argument_list|,
name|ntop
argument_list|)
expr_stmt|;
name|connections_per_period_exceeded
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
comment|/* 				 * Normal production daemon.  Fork, and have 				 * the child process the connection. The 				 * parent continues listening. 				 */
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|==
literal|0
condition|)
block|{
comment|/* 					 * Child.  Close the listening socket, and start using the 					 * accepted socket.  Reinitialize logging (since our pid has 					 * changed).  We break out of the loop to handle the connection. 					 */
name|close_listen_socks
argument_list|()
expr_stmt|;
name|sock_in
operator|=
name|newsock
expr_stmt|;
name|sock_out
operator|=
name|newsock
expr_stmt|;
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
argument_list|,
name|log_stderr
argument_list|)
expr_stmt|;
break|break;
block|}
block|}
comment|/* Parent.  Stay in the loop. */
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"fork: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
else|else
name|debug
argument_list|(
literal|"Forked child %d."
argument_list|,
name|pid
argument_list|)
expr_stmt|;
comment|/* Mark that the key has been used (it was "given" to the child). */
name|key_used
operator|=
literal|1
expr_stmt|;
name|arc4random_stir
argument_list|()
expr_stmt|;
comment|/* Close the new socket (the child is now taking care of it). */
name|close
argument_list|(
name|newsock
argument_list|)
expr_stmt|;
block|}
comment|/* for (i = 0; i< num_listen_socks; i++) */
comment|/* child process check (or debug mode) */
if|if
condition|(
name|num_listen_socks
operator|<
literal|0
condition|)
break|break;
block|}
block|}
comment|/* This is the child processing a new connection. */
comment|/* 	 * Disable the key regeneration alarm.  We will not regenerate the 	 * key since we are no longer in a position to give it to anyone. We 	 * will not restart on SIGHUP since it no longer makes sense. 	 */
name|alarm
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGALRM
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGHUP
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTERM
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGQUIT
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGCHLD
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
comment|/* 	 * Set socket options for the connection.  We want the socket to 	 * close as fast as possible without waiting for anything.  If the 	 * connection is not a socket, these will do nothing. 	 */
comment|/* setsockopt(sock_in, SOL_SOCKET, SO_REUSEADDR, (void *)&on, sizeof(on)); */
name|linger
operator|.
name|l_onoff
operator|=
literal|1
expr_stmt|;
name|linger
operator|.
name|l_linger
operator|=
literal|5
expr_stmt|;
name|setsockopt
argument_list|(
name|sock_in
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_LINGER
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|linger
argument_list|,
sizeof|sizeof
argument_list|(
name|linger
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * Register our connection.  This turns encryption off because we do 	 * not have a key. 	 */
name|packet_set_connection
argument_list|(
name|sock_in
argument_list|,
name|sock_out
argument_list|)
expr_stmt|;
name|remote_port
operator|=
name|get_remote_port
argument_list|()
expr_stmt|;
name|remote_ip
operator|=
name|get_remote_ipaddr
argument_list|()
expr_stmt|;
comment|/* Check whether logins are denied from this host. */
ifdef|#
directive|ifdef
name|LIBWRAP
comment|/* XXX LIBWRAP noes not know about IPv6 */
block|{
name|struct
name|request_info
name|req
decl_stmt|;
name|request_init
argument_list|(
operator|&
name|req
argument_list|,
name|RQ_DAEMON
argument_list|,
name|av0
argument_list|,
name|RQ_FILE
argument_list|,
name|sock_in
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|fromhost
argument_list|(
operator|&
name|req
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|hosts_access
argument_list|(
operator|&
name|req
argument_list|)
condition|)
block|{
name|close
argument_list|(
name|sock_in
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock_out
argument_list|)
expr_stmt|;
name|refuse
argument_list|(
operator|&
name|req
argument_list|)
expr_stmt|;
block|}
comment|/*XXX IPv6 verbose("Connection from %.500s port %d", eval_client(&req), remote_port); */
block|}
endif|#
directive|endif
comment|/* LIBWRAP */
comment|/* Log the connection. */
name|verbose
argument_list|(
literal|"Connection from %.500s port %d"
argument_list|,
name|remote_ip
argument_list|,
name|remote_port
argument_list|)
expr_stmt|;
comment|/* 	 * We don\'t want to listen forever unless the other side 	 * successfully authenticates itself.  So we set up an alarm which is 	 * cleared after successful authentication.  A limit of zero 	 * indicates no limit. Note that we don\'t set the alarm in debugging 	 * mode; it is just annoying to have the server exit just when you 	 * are about to discover the bug. 	 */
name|signal
argument_list|(
name|SIGALRM
argument_list|,
name|grace_alarm_handler
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|debug_flag
condition|)
name|alarm
argument_list|(
name|options
operator|.
name|login_grace_time
argument_list|)
expr_stmt|;
if|if
condition|(
name|client_version_string
operator|!=
name|NULL
condition|)
block|{
comment|/* we are exec'ed by sshd2, so skip exchange of protocol version */
name|strlcpy
argument_list|(
name|buf
argument_list|,
name|client_version_string
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* Send our protocol version identification. */
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"SSH-%d.%d-%.100s\n"
argument_list|,
name|PROTOCOL_MAJOR
argument_list|,
name|PROTOCOL_MINOR
argument_list|,
name|SSH_VERSION
argument_list|)
expr_stmt|;
if|if
condition|(
name|atomicio
argument_list|(
name|write
argument_list|,
name|sock_out
argument_list|,
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
operator|!=
name|strlen
argument_list|(
name|buf
argument_list|)
condition|)
block|{
name|log
argument_list|(
literal|"Could not write ident string to %s."
argument_list|,
name|remote_ip
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
comment|/* Read other side\'s version identification. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|read
argument_list|(
name|sock_in
argument_list|,
operator|&
name|buf
index|[
name|i
index|]
argument_list|,
literal|1
argument_list|)
operator|!=
literal|1
condition|)
block|{
name|log
argument_list|(
literal|"Did not receive ident string from %s."
argument_list|,
name|remote_ip
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|buf
index|[
name|i
index|]
operator|==
literal|'\r'
condition|)
block|{
name|buf
index|[
name|i
index|]
operator|=
literal|'\n'
expr_stmt|;
name|buf
index|[
name|i
operator|+
literal|1
index|]
operator|=
literal|0
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|buf
index|[
name|i
index|]
operator|==
literal|'\n'
condition|)
block|{
comment|/* buf[i] == '\n' */
name|buf
index|[
name|i
operator|+
literal|1
index|]
operator|=
literal|0
expr_stmt|;
break|break;
block|}
block|}
name|buf
index|[
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 	 * Check that the versions match.  In future this might accept 	 * several versions and set appropriate flags to handle them. 	 */
if|if
condition|(
name|sscanf
argument_list|(
name|buf
argument_list|,
literal|"SSH-%d.%d-%[^\n]\n"
argument_list|,
operator|&
name|remote_major
argument_list|,
operator|&
name|remote_minor
argument_list|,
name|remote_version
argument_list|)
operator|!=
literal|3
condition|)
block|{
name|char
modifier|*
name|s
init|=
literal|"Protocol mismatch.\n"
decl_stmt|;
operator|(
name|void
operator|)
name|atomicio
argument_list|(
name|write
argument_list|,
name|sock_out
argument_list|,
name|s
argument_list|,
name|strlen
argument_list|(
name|s
argument_list|)
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock_in
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock_out
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"Bad protocol version identification '%.100s' from %s"
argument_list|,
name|buf
argument_list|,
name|remote_ip
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
name|debug
argument_list|(
literal|"Client protocol version %d.%d; client software version %.100s"
argument_list|,
name|remote_major
argument_list|,
name|remote_minor
argument_list|,
name|remote_version
argument_list|)
expr_stmt|;
if|if
condition|(
name|remote_major
operator|!=
name|PROTOCOL_MAJOR
condition|)
block|{
name|char
modifier|*
name|s
init|=
literal|"Protocol major versions differ.\n"
decl_stmt|;
operator|(
name|void
operator|)
name|atomicio
argument_list|(
name|write
argument_list|,
name|sock_out
argument_list|,
name|s
argument_list|,
name|strlen
argument_list|(
name|s
argument_list|)
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock_in
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock_out
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"Protocol major versions differ for %s: %d vs. %d"
argument_list|,
name|remote_ip
argument_list|,
name|PROTOCOL_MAJOR
argument_list|,
name|remote_major
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
comment|/* Check that the client has sufficiently high software version. */
if|if
condition|(
name|remote_major
operator|==
literal|1
operator|&&
name|remote_minor
operator|<
literal|3
condition|)
name|packet_disconnect
argument_list|(
literal|"Your ssh version is too old and is no longer supported.  Please install a newer version."
argument_list|)
expr_stmt|;
if|if
condition|(
name|remote_major
operator|==
literal|1
operator|&&
name|remote_minor
operator|==
literal|3
condition|)
block|{
comment|/* note that this disables agent-forwarding */
name|enable_compat13
argument_list|()
expr_stmt|;
block|}
comment|/* 	 * Check that the connection comes from a privileged port.  Rhosts- 	 * and Rhosts-RSA-Authentication only make sense from priviledged 	 * programs.  Of course, if the intruder has root access on his local 	 * machine, he can connect from any port.  So do not use these 	 * authentication methods from machines that you do not trust. 	 */
if|if
condition|(
name|remote_port
operator|>=
name|IPPORT_RESERVED
operator|||
name|remote_port
operator|<
name|IPPORT_RESERVED
operator|/
literal|2
condition|)
block|{
name|options
operator|.
name|rhosts_authentication
operator|=
literal|0
expr_stmt|;
name|options
operator|.
name|rhosts_rsa_authentication
operator|=
literal|0
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|KRB4
if|if
condition|(
operator|!
name|packet_connection_is_ipv4
argument_list|()
operator|&&
name|options
operator|.
name|kerberos_authentication
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos Authentication disabled, only available for IPv4."
argument_list|)
expr_stmt|;
name|options
operator|.
name|kerberos_authentication
operator|=
literal|0
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* KRB4 */
name|packet_set_nonblocking
argument_list|()
expr_stmt|;
comment|/* perform the key exchange */
name|do_ssh_kex
argument_list|()
expr_stmt|;
comment|/* authenticate user and start session */
name|do_authentication
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|KRB4
comment|/* Cleanup user's ticket cache file. */
if|if
condition|(
name|options
operator|.
name|kerberos_ticket_cleanup
condition|)
operator|(
name|void
operator|)
name|dest_tkt
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* KRB4 */
comment|/* Cleanup user's local Xauthority file. */
if|if
condition|(
name|xauthfile
condition|)
name|xauthfile_cleanup_proc
argument_list|(
name|NULL
argument_list|)
expr_stmt|;
comment|/* The connection has been terminated. */
name|verbose
argument_list|(
literal|"Closing connection to %.100s"
argument_list|,
name|remote_ip
argument_list|)
expr_stmt|;
name|packet_close
argument_list|()
expr_stmt|;
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * SSH1 key exchange  */
end_comment

begin_function
name|void
name|do_ssh_kex
parameter_list|()
block|{
name|int
name|i
decl_stmt|,
name|len
decl_stmt|;
name|int
name|plen
decl_stmt|,
name|slen
decl_stmt|;
name|BIGNUM
modifier|*
name|session_key_int
decl_stmt|;
name|unsigned
name|char
name|session_key
index|[
name|SSH_SESSION_KEY_LENGTH
index|]
decl_stmt|;
name|unsigned
name|char
name|cookie
index|[
literal|8
index|]
decl_stmt|;
name|unsigned
name|int
name|cipher_type
decl_stmt|,
name|auth_mask
decl_stmt|,
name|protocol_flags
decl_stmt|;
name|u_int32_t
name|rand
init|=
literal|0
decl_stmt|;
comment|/* 	 * Generate check bytes that the client must send back in the user 	 * packet in order for it to be accepted; this is used to defy ip 	 * spoofing attacks.  Note that this only works against somebody 	 * doing IP spoofing from a remote machine; any machine on the local 	 * network can still see outgoing packets and catch the random 	 * cookie.  This only affects rhosts authentication, and this is one 	 * of the reasons why it is inherently insecure. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|8
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|i
operator|%
literal|4
operator|==
literal|0
condition|)
name|rand
operator|=
name|arc4random
argument_list|()
expr_stmt|;
name|cookie
index|[
name|i
index|]
operator|=
name|rand
operator|&
literal|0xff
expr_stmt|;
name|rand
operator|>>=
literal|8
expr_stmt|;
block|}
comment|/* 	 * Send our public key.  We include in the packet 64 bits of random 	 * data that must be matched in the reply in order to prevent IP 	 * spoofing. 	 */
name|packet_start
argument_list|(
name|SSH_SMSG_PUBLIC_KEY
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|8
condition|;
name|i
operator|++
control|)
name|packet_put_char
argument_list|(
name|cookie
index|[
name|i
index|]
argument_list|)
expr_stmt|;
comment|/* Store our public server RSA key. */
name|packet_put_int
argument_list|(
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|public_key
operator|->
name|e
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
expr_stmt|;
comment|/* Store our public host RSA key. */
name|packet_put_int
argument_list|(
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|e
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
expr_stmt|;
comment|/* Put protocol flags. */
name|packet_put_int
argument_list|(
name|SSH_PROTOFLAG_HOST_IN_FWD_OPEN
argument_list|)
expr_stmt|;
comment|/* Declare which ciphers we support. */
name|packet_put_int
argument_list|(
name|cipher_mask
argument_list|()
argument_list|)
expr_stmt|;
comment|/* Declare supported authentication types. */
name|auth_mask
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|rhosts_authentication
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_RHOSTS
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|rhosts_rsa_authentication
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_RHOSTS_RSA
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|rsa_authentication
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_RSA
expr_stmt|;
ifdef|#
directive|ifdef
name|KRB4
if|if
condition|(
name|options
operator|.
name|kerberos_authentication
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_KERBEROS
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|AFS
if|if
condition|(
name|options
operator|.
name|kerberos_tgt_passing
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_PASS_KERBEROS_TGT
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|afs_token_passing
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_PASS_AFS_TOKEN
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|SKEY
if|if
condition|(
name|options
operator|.
name|skey_authentication
operator|==
literal|1
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_TIS
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|options
operator|.
name|password_authentication
condition|)
name|auth_mask
operator||=
literal|1
operator|<<
name|SSH_AUTH_PASSWORD
expr_stmt|;
name|packet_put_int
argument_list|(
name|auth_mask
argument_list|)
expr_stmt|;
comment|/* Send the packet and wait for it to be sent. */
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|debug
argument_list|(
literal|"Sent %d bit public key and %d bit host key."
argument_list|,
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Read clients reply (cipher type and session key). */
name|packet_read_expect
argument_list|(
operator|&
name|plen
argument_list|,
name|SSH_CMSG_SESSION_KEY
argument_list|)
expr_stmt|;
comment|/* Get cipher type and check whether we accept this. */
name|cipher_type
operator|=
name|packet_get_char
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|cipher_mask
argument_list|()
operator|&
operator|(
literal|1
operator|<<
name|cipher_type
operator|)
operator|)
condition|)
name|packet_disconnect
argument_list|(
literal|"Warning: client selects unsupported cipher."
argument_list|)
expr_stmt|;
comment|/* Get check bytes from the packet.  These must match those we 	   sent earlier with the public key packet. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|8
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|cookie
index|[
name|i
index|]
operator|!=
name|packet_get_char
argument_list|()
condition|)
name|packet_disconnect
argument_list|(
literal|"IP Spoofing check bytes do not match."
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Encryption type: %.200s"
argument_list|,
name|cipher_name
argument_list|(
name|cipher_type
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Get the encrypted integer. */
name|session_key_int
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|session_key_int
argument_list|,
operator|&
name|slen
argument_list|)
expr_stmt|;
name|protocol_flags
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|packet_set_protocol_flags
argument_list|(
name|protocol_flags
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|1
operator|+
literal|8
operator|+
name|slen
operator|+
literal|4
argument_list|,
name|SSH_CMSG_SESSION_KEY
argument_list|)
expr_stmt|;
comment|/* 	 * Decrypt it using our private server key and private host key (key 	 * with larger modulus first). 	 */
if|if
condition|(
name|BN_cmp
argument_list|(
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|,
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|>
literal|0
condition|)
block|{
comment|/* Private key has bigger modulus. */
if|if
condition|(
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|)
operator|<
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
condition|)
block|{
name|fatal
argument_list|(
literal|"do_connection: %s: private_key %d< host_key %d + SSH_KEY_BITS_RESERVED %d"
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
argument_list|,
name|SSH_KEY_BITS_RESERVED
argument_list|)
expr_stmt|;
block|}
name|rsa_private_decrypt
argument_list|(
name|session_key_int
argument_list|,
name|session_key_int
argument_list|,
name|sensitive_data
operator|.
name|private_key
argument_list|)
expr_stmt|;
name|rsa_private_decrypt
argument_list|(
name|session_key_int
argument_list|,
name|session_key_int
argument_list|,
name|sensitive_data
operator|.
name|host_key
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* Host key has bigger modulus (or they are equal). */
if|if
condition|(
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
operator|<
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
condition|)
block|{
name|fatal
argument_list|(
literal|"do_connection: %s: host_key %d< private_key %d + SSH_KEY_BITS_RESERVED %d"
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|)
argument_list|,
name|SSH_KEY_BITS_RESERVED
argument_list|)
expr_stmt|;
block|}
name|rsa_private_decrypt
argument_list|(
name|session_key_int
argument_list|,
name|session_key_int
argument_list|,
name|sensitive_data
operator|.
name|host_key
argument_list|)
expr_stmt|;
name|rsa_private_decrypt
argument_list|(
name|session_key_int
argument_list|,
name|session_key_int
argument_list|,
name|sensitive_data
operator|.
name|private_key
argument_list|)
expr_stmt|;
block|}
name|compute_session_id
argument_list|(
name|session_id
argument_list|,
name|cookie
argument_list|,
name|sensitive_data
operator|.
name|host_key
operator|->
name|n
argument_list|,
name|sensitive_data
operator|.
name|private_key
operator|->
name|n
argument_list|)
expr_stmt|;
comment|/* Destroy the private and public keys.  They will no longer be needed. */
name|RSA_free
argument_list|(
name|public_key
argument_list|)
expr_stmt|;
name|RSA_free
argument_list|(
name|sensitive_data
operator|.
name|private_key
argument_list|)
expr_stmt|;
name|RSA_free
argument_list|(
name|sensitive_data
operator|.
name|host_key
argument_list|)
expr_stmt|;
comment|/* 	 * Extract session key from the decrypted integer.  The key is in the 	 * least significant 256 bits of the integer; the first byte of the 	 * key is in the highest bits. 	 */
name|BN_mask_bits
argument_list|(
name|session_key_int
argument_list|,
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
operator|*
literal|8
argument_list|)
expr_stmt|;
name|len
operator|=
name|BN_num_bytes
argument_list|(
name|session_key_int
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|<
literal|0
operator|||
name|len
operator|>
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
condition|)
name|fatal
argument_list|(
literal|"do_connection: bad len from %s: session_key_int %d> sizeof(session_key) %d"
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|len
argument_list|,
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|session_key
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
argument_list|)
expr_stmt|;
name|BN_bn2bin
argument_list|(
name|session_key_int
argument_list|,
name|session_key
operator|+
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
operator|-
name|len
argument_list|)
expr_stmt|;
comment|/* Destroy the decrypted integer.  It is no longer needed. */
name|BN_clear_free
argument_list|(
name|session_key_int
argument_list|)
expr_stmt|;
comment|/* Xor the first 16 bytes of the session key with the session id. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|16
condition|;
name|i
operator|++
control|)
name|session_key
index|[
name|i
index|]
operator|^=
name|session_id
index|[
name|i
index|]
expr_stmt|;
comment|/* Set the session key.  From this on all communications will be encrypted. */
name|packet_set_encryption_key
argument_list|(
name|session_key
argument_list|,
name|SSH_SESSION_KEY_LENGTH
argument_list|,
name|cipher_type
argument_list|)
expr_stmt|;
comment|/* Destroy our copy of the session key.  It is no longer needed. */
name|memset
argument_list|(
name|session_key
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Received session key; encryption turned on."
argument_list|)
expr_stmt|;
comment|/* Send an acknowledgement packet.  Note that this packet is sent encrypted. */
name|packet_start
argument_list|(
name|SSH_SMSG_SUCCESS
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Check if the user is allowed to log in via ssh. If user is listed in  * DenyUsers or user's primary group is listed in DenyGroups, false will  * be returned. If AllowUsers isn't empty and user isn't listed there, or  * if AllowGroups isn't empty and user isn't listed there, false will be  * returned.   * If the user's shell is not executable, false will be returned.  * Otherwise true is returned.   */
end_comment

begin_function
specifier|static
name|int
name|allowed_user
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|)
block|{
name|struct
name|stat
name|st
decl_stmt|;
name|struct
name|group
modifier|*
name|grp
decl_stmt|;
name|int
name|i
decl_stmt|;
comment|/* Shouldn't be called if pw is NULL, but better safe than sorry... */
if|if
condition|(
operator|!
name|pw
condition|)
return|return
literal|0
return|;
comment|/* deny if shell does not exists or is not executable */
if|if
condition|(
name|stat
argument_list|(
name|pw
operator|->
name|pw_shell
argument_list|,
operator|&
name|st
argument_list|)
operator|!=
literal|0
condition|)
return|return
literal|0
return|;
if|if
condition|(
operator|!
operator|(
operator|(
name|st
operator|.
name|st_mode
operator|&
name|S_IFREG
operator|)
operator|&&
operator|(
name|st
operator|.
name|st_mode
operator|&
operator|(
name|S_IXOTH
operator||
name|S_IXUSR
operator||
name|S_IXGRP
operator|)
operator|)
operator|)
condition|)
return|return
literal|0
return|;
comment|/* Return false if user is listed in DenyUsers */
if|if
condition|(
name|options
operator|.
name|num_deny_users
operator|>
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|pw
operator|->
name|pw_name
condition|)
return|return
literal|0
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|num_deny_users
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|match_pattern
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|,
name|options
operator|.
name|deny_users
index|[
name|i
index|]
argument_list|)
condition|)
return|return
literal|0
return|;
block|}
comment|/* Return false if AllowUsers isn't empty and user isn't listed there */
if|if
condition|(
name|options
operator|.
name|num_allow_users
operator|>
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|pw
operator|->
name|pw_name
condition|)
return|return
literal|0
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|num_allow_users
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|match_pattern
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|,
name|options
operator|.
name|allow_users
index|[
name|i
index|]
argument_list|)
condition|)
break|break;
comment|/* i< options.num_allow_users iff we break for loop */
if|if
condition|(
name|i
operator|>=
name|options
operator|.
name|num_allow_users
condition|)
return|return
literal|0
return|;
block|}
comment|/* Get the primary group name if we need it. Return false if it fails */
if|if
condition|(
name|options
operator|.
name|num_deny_groups
operator|>
literal|0
operator|||
name|options
operator|.
name|num_allow_groups
operator|>
literal|0
condition|)
block|{
name|grp
operator|=
name|getgrgid
argument_list|(
name|pw
operator|->
name|pw_gid
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|grp
condition|)
return|return
literal|0
return|;
comment|/* Return false if user's group is listed in DenyGroups */
if|if
condition|(
name|options
operator|.
name|num_deny_groups
operator|>
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|grp
operator|->
name|gr_name
condition|)
return|return
literal|0
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|num_deny_groups
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|match_pattern
argument_list|(
name|grp
operator|->
name|gr_name
argument_list|,
name|options
operator|.
name|deny_groups
index|[
name|i
index|]
argument_list|)
condition|)
return|return
literal|0
return|;
block|}
comment|/* 		 * Return false if AllowGroups isn't empty and user's group 		 * isn't listed there 		 */
if|if
condition|(
name|options
operator|.
name|num_allow_groups
operator|>
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|grp
operator|->
name|gr_name
condition|)
return|return
literal|0
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|num_allow_groups
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|match_pattern
argument_list|(
name|grp
operator|->
name|gr_name
argument_list|,
name|options
operator|.
name|allow_groups
index|[
name|i
index|]
argument_list|)
condition|)
break|break;
comment|/* i< options.num_allow_groups iff we break for 			   loop */
if|if
condition|(
name|i
operator|>=
name|options
operator|.
name|num_allow_groups
condition|)
return|return
literal|0
return|;
block|}
block|}
comment|/* Fail if the account's expiration time has passed. */
if|if
condition|(
name|pw
operator|->
name|pw_expire
operator|!=
literal|0
condition|)
block|{
name|struct
name|timeval
name|tv
decl_stmt|;
operator|(
name|void
operator|)
name|gettimeofday
argument_list|(
operator|&
name|tv
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|tv
operator|.
name|tv_sec
operator|>=
name|pw
operator|->
name|pw_expire
condition|)
return|return
literal|0
return|;
block|}
comment|/* We found no reason not to let this user try to log on... */
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|/*  * Performs authentication of an incoming connection.  Session key has already  * been exchanged and encryption is enabled.  */
end_comment

begin_function
name|void
name|do_authentication
parameter_list|()
block|{
name|struct
name|passwd
modifier|*
name|pw
decl_stmt|,
name|pwcopy
decl_stmt|;
name|int
name|plen
decl_stmt|,
name|ulen
decl_stmt|;
name|char
modifier|*
name|user
decl_stmt|;
comment|/* Get the name of the user that we wish to log in as. */
name|packet_read_expect
argument_list|(
operator|&
name|plen
argument_list|,
name|SSH_CMSG_USER
argument_list|)
expr_stmt|;
comment|/* Get the user name. */
name|user
operator|=
name|packet_get_string
argument_list|(
operator|&
name|ulen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
operator|(
literal|4
operator|+
name|ulen
operator|)
argument_list|,
name|SSH_CMSG_USER
argument_list|)
expr_stmt|;
name|setproctitle
argument_list|(
literal|"%s"
argument_list|,
name|user
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|AFS
comment|/* If machine has AFS, set process authentication group. */
if|if
condition|(
name|k_hasafs
argument_list|()
condition|)
block|{
name|k_setpag
argument_list|()
expr_stmt|;
name|k_unlog
argument_list|()
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* AFS */
comment|/* Verify that the user is a valid user. */
name|pw
operator|=
name|getpwnam
argument_list|(
name|user
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pw
operator|||
operator|!
name|allowed_user
argument_list|(
name|pw
argument_list|)
condition|)
name|do_fake_authloop
argument_list|(
name|user
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|user
argument_list|)
expr_stmt|;
comment|/* Take a copy of the returned structure. */
name|memset
argument_list|(
operator|&
name|pwcopy
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|pwcopy
argument_list|)
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_name
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_passwd
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_passwd
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_uid
operator|=
name|pw
operator|->
name|pw_uid
expr_stmt|;
name|pwcopy
operator|.
name|pw_gid
operator|=
name|pw
operator|->
name|pw_gid
expr_stmt|;
name|pwcopy
operator|.
name|pw_dir
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_shell
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_shell
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_class
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_class
argument_list|)
expr_stmt|;
name|pwcopy
operator|.
name|pw_expire
operator|=
name|pw
operator|->
name|pw_expire
expr_stmt|;
name|pwcopy
operator|.
name|pw_change
operator|=
name|pw
operator|->
name|pw_change
expr_stmt|;
name|pw
operator|=
operator|&
name|pwcopy
expr_stmt|;
comment|/* 	 * If we are not running as root, the user must have the same uid as 	 * the server. 	 */
if|if
condition|(
name|getuid
argument_list|()
operator|!=
literal|0
operator|&&
name|pw
operator|->
name|pw_uid
operator|!=
name|getuid
argument_list|()
condition|)
name|packet_disconnect
argument_list|(
literal|"Cannot change user when server not running as root."
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Attempting authentication for %.100s."
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
comment|/* If the user has no password, accept authentication immediately. */
if|if
condition|(
name|options
operator|.
name|password_authentication
operator|&&
ifdef|#
directive|ifdef
name|KRB4
operator|(
operator|!
name|options
operator|.
name|kerberos_authentication
operator|||
name|options
operator|.
name|kerberos_or_local_passwd
operator|)
operator|&&
endif|#
directive|endif
comment|/* KRB4 */
name|auth_password
argument_list|(
name|pw
argument_list|,
literal|""
argument_list|)
condition|)
block|{
comment|/* Authentication with empty password succeeded. */
name|log
argument_list|(
literal|"Login for user %s from %.100s, accepted without authentication."
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* Loop until the user has been authenticated or the 		   connection is closed, do_authloop() returns only if 		   authentication is successfull */
name|do_authloop
argument_list|(
name|pw
argument_list|)
expr_stmt|;
block|}
comment|/* Check if the user is logging in as root and root logins are disallowed. */
if|if
condition|(
name|pw
operator|->
name|pw_uid
operator|==
literal|0
operator|&&
operator|!
name|options
operator|.
name|permit_root_login
condition|)
block|{
if|if
condition|(
name|forced_command
condition|)
name|log
argument_list|(
literal|"Root login accepted for forced command."
argument_list|)
expr_stmt|;
else|else
name|packet_disconnect
argument_list|(
literal|"ROOT LOGIN REFUSED FROM %.200s"
argument_list|,
name|get_canonical_hostname
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|/* The user has been authenticated and accepted. */
name|packet_start
argument_list|(
name|SSH_SMSG_SUCCESS
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Perform session preparation. */
name|do_authenticated
argument_list|(
name|pw
argument_list|)
expr_stmt|;
block|}
end_function

begin_define
define|#
directive|define
name|AUTH_FAIL_MAX
value|6
end_define

begin_define
define|#
directive|define
name|AUTH_FAIL_LOG
value|(AUTH_FAIL_MAX/2)
end_define

begin_define
define|#
directive|define
name|AUTH_FAIL_MSG
value|"Too many authentication failures for %.100s"
end_define

begin_comment
comment|/*  * read packets and try to authenticate local user *pw.  * return if authentication is successfull  */
end_comment

begin_function
name|void
name|do_authloop
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|)
block|{
name|int
name|attempt
init|=
literal|0
decl_stmt|;
name|unsigned
name|int
name|bits
decl_stmt|;
name|BIGNUM
modifier|*
name|client_host_key_e
decl_stmt|,
modifier|*
name|client_host_key_n
decl_stmt|;
name|BIGNUM
modifier|*
name|n
decl_stmt|;
name|char
modifier|*
name|client_user
decl_stmt|,
modifier|*
name|password
decl_stmt|;
name|char
name|user
index|[
literal|1024
index|]
decl_stmt|;
name|int
name|plen
decl_stmt|,
name|dlen
decl_stmt|,
name|nlen
decl_stmt|,
name|ulen
decl_stmt|,
name|elen
decl_stmt|;
name|int
name|type
init|=
literal|0
decl_stmt|;
name|void
function_decl|(
modifier|*
name|authlog
function_decl|)
parameter_list|(
specifier|const
name|char
modifier|*
name|fmt
parameter_list|,
modifier|...
parameter_list|)
init|=
name|verbose
function_decl|;
comment|/* Indicate that authentication is needed. */
name|packet_start
argument_list|(
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
for|for
control|(
name|attempt
operator|=
literal|1
init|;
condition|;
name|attempt
operator|++
control|)
block|{
name|int
name|authenticated
init|=
literal|0
decl_stmt|;
name|strlcpy
argument_list|(
name|user
argument_list|,
literal|""
argument_list|,
sizeof|sizeof
name|user
argument_list|)
expr_stmt|;
comment|/* Get a packet from the client. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* Process the packet. */
switch|switch
condition|(
name|type
condition|)
block|{
ifdef|#
directive|ifdef
name|AFS
case|case
name|SSH_CMSG_HAVE_KERBEROS_TGT
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|kerberos_tgt_passing
condition|)
block|{
comment|/* packet_get_all(); */
name|verbose
argument_list|(
literal|"Kerberos tgt passing disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
else|else
block|{
comment|/* Accept Kerberos tgt. */
name|char
modifier|*
name|tgt
init|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
decl_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|auth_kerberos_tgt
argument_list|(
name|pw
argument_list|,
name|tgt
argument_list|)
condition|)
name|verbose
argument_list|(
literal|"Kerberos tgt REFUSED for %s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|tgt
argument_list|)
expr_stmt|;
block|}
continue|continue;
case|case
name|SSH_CMSG_HAVE_AFS_TOKEN
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|afs_token_passing
operator|||
operator|!
name|k_hasafs
argument_list|()
condition|)
block|{
comment|/* packet_get_all(); */
name|verbose
argument_list|(
literal|"AFS token passing disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
else|else
block|{
comment|/* Accept AFS token. */
name|char
modifier|*
name|token_string
init|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
decl_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|auth_afs_token
argument_list|(
name|pw
argument_list|,
name|token_string
argument_list|)
condition|)
name|verbose
argument_list|(
literal|"AFS token REFUSED for %s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|token_string
argument_list|)
expr_stmt|;
block|}
continue|continue;
endif|#
directive|endif
comment|/* AFS */
ifdef|#
directive|ifdef
name|KRB4
case|case
name|SSH_CMSG_AUTH_KERBEROS
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|kerberos_authentication
condition|)
block|{
comment|/* packet_get_all(); */
name|verbose
argument_list|(
literal|"Kerberos authentication disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
else|else
block|{
comment|/* Try Kerberos v4 authentication. */
name|KTEXT_ST
name|auth
decl_stmt|;
name|char
modifier|*
name|tkt_user
init|=
name|NULL
decl_stmt|;
name|char
modifier|*
name|kdata
init|=
name|packet_get_string
argument_list|(
operator|(
name|unsigned
name|int
operator|*
operator|)
operator|&
name|auth
operator|.
name|length
argument_list|)
decl_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|auth
operator|.
name|length
argument_list|,
name|type
argument_list|)
expr_stmt|;
if|if
condition|(
name|auth
operator|.
name|length
operator|<
name|MAX_KTXT_LEN
condition|)
name|memcpy
argument_list|(
name|auth
operator|.
name|dat
argument_list|,
name|kdata
argument_list|,
name|auth
operator|.
name|length
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|kdata
argument_list|)
expr_stmt|;
name|authenticated
operator|=
name|auth_krb4
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|,
operator|&
name|auth
argument_list|,
operator|&
name|tkt_user
argument_list|)
expr_stmt|;
if|if
condition|(
name|authenticated
condition|)
block|{
name|snprintf
argument_list|(
name|user
argument_list|,
sizeof|sizeof
name|user
argument_list|,
literal|" tktuser %s"
argument_list|,
name|tkt_user
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|tkt_user
argument_list|)
expr_stmt|;
block|}
block|}
break|break;
endif|#
directive|endif
comment|/* KRB4 */
case|case
name|SSH_CMSG_AUTH_RHOSTS
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|rhosts_authentication
condition|)
block|{
name|verbose
argument_list|(
literal|"Rhosts authentication disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* 			 * Get client user name.  Note that we just have to 			 * trust the client; this is one reason why rhosts 			 * authentication is insecure. (Another is 			 * IP-spoofing on a local network.) 			 */
name|client_user
operator|=
name|packet_get_string
argument_list|(
operator|&
name|ulen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|ulen
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Try to authenticate using /etc/hosts.equiv and 			   .rhosts. */
name|authenticated
operator|=
name|auth_rhosts
argument_list|(
name|pw
argument_list|,
name|client_user
argument_list|)
expr_stmt|;
name|snprintf
argument_list|(
name|user
argument_list|,
sizeof|sizeof
name|user
argument_list|,
literal|" ruser %s"
argument_list|,
name|client_user
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|client_user
argument_list|)
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_AUTH_RHOSTS_RSA
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|rhosts_rsa_authentication
condition|)
block|{
name|verbose
argument_list|(
literal|"Rhosts with RSA authentication disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* 			 * Get client user name.  Note that we just have to 			 * trust the client; root on the client machine can 			 * claim to be any user. 			 */
name|client_user
operator|=
name|packet_get_string
argument_list|(
operator|&
name|ulen
argument_list|)
expr_stmt|;
comment|/* Get the client host key. */
name|client_host_key_e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|client_host_key_n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|bits
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|client_host_key_e
argument_list|,
operator|&
name|elen
argument_list|)
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|client_host_key_n
argument_list|,
operator|&
name|nlen
argument_list|)
expr_stmt|;
if|if
condition|(
name|bits
operator|!=
name|BN_num_bits
argument_list|(
name|client_host_key_n
argument_list|)
condition|)
name|error
argument_list|(
literal|"Warning: keysize mismatch for client_host_key: "
literal|"actual %d, announced %d"
argument_list|,
name|BN_num_bits
argument_list|(
name|client_host_key_n
argument_list|)
argument_list|,
name|bits
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
operator|(
literal|4
operator|+
name|ulen
operator|)
operator|+
literal|4
operator|+
name|elen
operator|+
name|nlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|authenticated
operator|=
name|auth_rhosts_rsa
argument_list|(
name|pw
argument_list|,
name|client_user
argument_list|,
name|client_host_key_e
argument_list|,
name|client_host_key_n
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|client_host_key_e
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|client_host_key_n
argument_list|)
expr_stmt|;
name|snprintf
argument_list|(
name|user
argument_list|,
sizeof|sizeof
name|user
argument_list|,
literal|" ruser %s"
argument_list|,
name|client_user
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|client_user
argument_list|)
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_AUTH_RSA
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|rsa_authentication
condition|)
block|{
name|verbose
argument_list|(
literal|"RSA authentication disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* RSA authentication requested. */
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|n
argument_list|,
operator|&
name|nlen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
name|nlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|authenticated
operator|=
name|auth_rsa
argument_list|(
name|pw
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|n
argument_list|)
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_AUTH_PASSWORD
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|password_authentication
condition|)
block|{
name|verbose
argument_list|(
literal|"Password authentication disabled."
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* 			 * Read user password.  It is in plain text, but was 			 * transmitted over the encrypted channel so it is 			 * not visible to an outside observer. 			 */
name|password
operator|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Try authentication with the password. */
name|authenticated
operator|=
name|auth_password
argument_list|(
name|pw
argument_list|,
name|password
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|password
argument_list|,
literal|0
argument_list|,
name|strlen
argument_list|(
name|password
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|password
argument_list|)
expr_stmt|;
break|break;
ifdef|#
directive|ifdef
name|SKEY
case|case
name|SSH_CMSG_AUTH_TIS
case|:
name|debug
argument_list|(
literal|"rcvd SSH_CMSG_AUTH_TIS"
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|skey_authentication
operator|==
literal|1
condition|)
block|{
name|char
modifier|*
name|skeyinfo
init|=
name|skey_keyinfo
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
decl_stmt|;
if|if
condition|(
name|skeyinfo
operator|==
name|NULL
condition|)
block|{
name|debug
argument_list|(
literal|"generating fake skeyinfo for %.100s."
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|skeyinfo
operator|=
name|skey_fake_keyinfo
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|skeyinfo
operator|!=
name|NULL
condition|)
block|{
comment|/* we send our s/key- in tis-challenge messages */
name|debug
argument_list|(
literal|"sending challenge '%s'"
argument_list|,
name|skeyinfo
argument_list|)
expr_stmt|;
name|packet_start
argument_list|(
name|SSH_SMSG_AUTH_TIS_CHALLENGE
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|skeyinfo
argument_list|,
name|strlen
argument_list|(
name|skeyinfo
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
continue|continue;
block|}
block|}
break|break;
case|case
name|SSH_CMSG_AUTH_TIS_RESPONSE
case|:
name|debug
argument_list|(
literal|"rcvd SSH_CMSG_AUTH_TIS_RESPONSE"
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|skey_authentication
operator|==
literal|1
condition|)
block|{
name|char
modifier|*
name|response
init|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
decl_stmt|;
name|debug
argument_list|(
literal|"skey response == '%s'"
argument_list|,
name|response
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|authenticated
operator|=
operator|(
name|skey_haskey
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
operator|==
literal|0
operator|&&
name|skey_passcheck
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|,
name|response
argument_list|)
operator|!=
operator|-
literal|1
operator|)
expr_stmt|;
name|xfree
argument_list|(
name|response
argument_list|)
expr_stmt|;
block|}
break|break;
else|#
directive|else
case|case
name|SSH_CMSG_AUTH_TIS
case|:
comment|/* TIS Authentication is unsupported */
name|log
argument_list|(
literal|"TIS authentication unsupported."
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
default|default:
comment|/* 			 * Any unknown messages will be ignored (and failure 			 * returned) during authentication. 			 */
name|log
argument_list|(
literal|"Unknown message during authentication: type %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* Raise logging level */
if|if
condition|(
name|authenticated
operator|||
name|attempt
operator|==
name|AUTH_FAIL_LOG
operator|||
name|type
operator|==
name|SSH_CMSG_AUTH_PASSWORD
condition|)
name|authlog
operator|=
name|log
expr_stmt|;
name|authlog
argument_list|(
literal|"%s %s for %.200s from %.200s port %d%s"
argument_list|,
name|authenticated
condition|?
literal|"Accepted"
else|:
literal|"Failed"
argument_list|,
name|get_authname
argument_list|(
name|type
argument_list|)
argument_list|,
name|pw
operator|->
name|pw_uid
operator|==
literal|0
condition|?
literal|"ROOT"
else|:
name|pw
operator|->
name|pw_name
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|get_remote_port
argument_list|()
argument_list|,
name|user
argument_list|)
expr_stmt|;
if|if
condition|(
name|authenticated
condition|)
return|return;
if|if
condition|(
name|attempt
operator|>
name|AUTH_FAIL_MAX
condition|)
name|packet_disconnect
argument_list|(
name|AUTH_FAIL_MSG
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
comment|/* Send a message indicating that the authentication attempt failed. */
name|packet_start
argument_list|(
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * The user does not exist or access is denied,  * but fake indication that authentication is needed.  */
end_comment

begin_function
name|void
name|do_fake_authloop
parameter_list|(
name|char
modifier|*
name|user
parameter_list|)
block|{
name|int
name|attempt
init|=
literal|0
decl_stmt|;
name|log
argument_list|(
literal|"Faking authloop for illegal user %.200s from %.200s port %d"
argument_list|,
name|user
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|get_remote_port
argument_list|()
argument_list|)
expr_stmt|;
comment|/* Indicate that authentication is needed. */
name|packet_start
argument_list|(
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* 	 * Keep reading packets, and always respond with a failure.  This is 	 * to avoid disclosing whether such a user really exists. 	 */
for|for
control|(
name|attempt
operator|=
literal|1
init|;
condition|;
name|attempt
operator|++
control|)
block|{
comment|/* Read a packet.  This will not return if the client disconnects. */
name|int
name|plen
decl_stmt|;
name|int
name|type
init|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
decl_stmt|;
ifdef|#
directive|ifdef
name|SKEY
name|int
name|dlen
decl_stmt|;
name|char
modifier|*
name|password
decl_stmt|,
modifier|*
name|skeyinfo
decl_stmt|;
comment|/* Try to send a fake s/key challenge. */
if|if
condition|(
name|options
operator|.
name|skey_authentication
operator|==
literal|1
operator|&&
operator|(
name|skeyinfo
operator|=
name|skey_fake_keyinfo
argument_list|(
name|user
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
name|password
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_CMSG_AUTH_TIS
condition|)
block|{
name|packet_start
argument_list|(
name|SSH_SMSG_AUTH_TIS_CHALLENGE
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|skeyinfo
argument_list|,
name|strlen
argument_list|(
name|skeyinfo
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
continue|continue;
block|}
elseif|else
if|if
condition|(
name|type
operator|==
name|SSH_CMSG_AUTH_PASSWORD
operator|&&
name|options
operator|.
name|password_authentication
operator|&&
operator|(
name|password
operator|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
operator|)
operator|!=
name|NULL
operator|&&
name|dlen
operator|==
literal|5
operator|&&
name|strncasecmp
argument_list|(
name|password
argument_list|,
literal|"s/key"
argument_list|,
literal|5
argument_list|)
operator|==
literal|0
condition|)
block|{
name|packet_send_debug
argument_list|(
name|skeyinfo
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|password
operator|!=
name|NULL
condition|)
name|xfree
argument_list|(
name|password
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|attempt
operator|>
name|AUTH_FAIL_MAX
condition|)
name|packet_disconnect
argument_list|(
name|AUTH_FAIL_MSG
argument_list|,
name|user
argument_list|)
expr_stmt|;
comment|/* 		 * Send failure.  This should be indistinguishable from a 		 * failed authentication. 		 */
name|packet_start
argument_list|(
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
block|}
comment|/* NOTREACHED */
name|abort
argument_list|()
expr_stmt|;
block|}
end_function

begin_struct
struct|struct
name|pty_cleanup_context
block|{
specifier|const
name|char
modifier|*
name|ttyname
decl_stmt|;
name|int
name|pid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/*  * Function to perform cleanup if we get aborted abnormally (e.g., due to a  * dropped connection).  */
end_comment

begin_function
name|void
name|pty_cleanup_proc
parameter_list|(
name|void
modifier|*
name|context
parameter_list|)
block|{
name|struct
name|pty_cleanup_context
modifier|*
name|cu
init|=
name|context
decl_stmt|;
name|debug
argument_list|(
literal|"pty_cleanup_proc called"
argument_list|)
expr_stmt|;
comment|/* Record that the user has logged out. */
name|record_logout
argument_list|(
name|cu
operator|->
name|pid
argument_list|,
name|cu
operator|->
name|ttyname
argument_list|)
expr_stmt|;
comment|/* Release the pseudo-tty. */
name|pty_release
argument_list|(
name|cu
operator|->
name|ttyname
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* simple cleanup: chown tty slave back to root */
end_comment

begin_function
specifier|static
name|void
name|pty_release_proc
parameter_list|(
name|void
modifier|*
name|tty
parameter_list|)
block|{
name|char
modifier|*
name|ttyname
init|=
name|tty
decl_stmt|;
name|pty_release
argument_list|(
name|ttyname
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Prepares for an interactive session.  This is called after the user has  * been successfully authenticated.  During this message exchange, pseudo  * terminals are allocated, X11, TCP/IP, and authentication agent forwardings  * are requested, etc.  */
end_comment

begin_function
name|void
name|do_authenticated
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|)
block|{
name|int
name|type
decl_stmt|;
name|int
name|compression_level
init|=
literal|0
decl_stmt|,
name|enable_compression_after_reply
init|=
literal|0
decl_stmt|;
name|int
name|have_pty
init|=
literal|0
decl_stmt|,
name|ptyfd
init|=
operator|-
literal|1
decl_stmt|,
name|ttyfd
init|=
operator|-
literal|1
decl_stmt|;
name|int
name|row
decl_stmt|,
name|col
decl_stmt|,
name|xpixel
decl_stmt|,
name|ypixel
decl_stmt|,
name|screen
decl_stmt|;
name|char
name|ttyname
index|[
literal|64
index|]
decl_stmt|;
name|char
modifier|*
name|command
decl_stmt|,
modifier|*
name|term
init|=
name|NULL
decl_stmt|,
modifier|*
name|display
init|=
name|NULL
decl_stmt|,
modifier|*
name|proto
init|=
name|NULL
decl_stmt|,
modifier|*
name|data
init|=
name|NULL
decl_stmt|;
name|int
name|n_bytes
decl_stmt|;
comment|/* 	 * Cancel the alarm we set to limit the time taken for 	 * authentication. 	 */
name|alarm
argument_list|(
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Inform the channel mechanism that we are the server side and that 	 * the client may request to connect to any port at all. (The user 	 * could do it anyway, and we wouldn\'t know what is permitted except 	 * by the client telling us, so we can equally well trust the client 	 * not to request anything bogus.) 	 */
if|if
condition|(
operator|!
name|no_port_forwarding_flag
condition|)
name|channel_permit_all_opens
argument_list|()
expr_stmt|;
comment|/* 	 * We stay in this loop until the client requests to execute a shell 	 * or a command. 	 */
while|while
condition|(
literal|1
condition|)
block|{
name|int
name|plen
decl_stmt|,
name|dlen
decl_stmt|;
comment|/* Get a packet from the client. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* Process the packet. */
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|SSH_CMSG_REQUEST_COMPRESSION
case|:
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|compression_level
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
if|if
condition|(
name|compression_level
operator|<
literal|1
operator|||
name|compression_level
operator|>
literal|9
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Received illegal compression level %d."
argument_list|,
name|compression_level
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
comment|/* Enable compression after we have responded with SUCCESS. */
name|enable_compression_after_reply
operator|=
literal|1
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_REQUEST_PTY
case|:
if|if
condition|(
name|no_pty_flag
condition|)
block|{
name|debug
argument_list|(
literal|"Allocating a pty not permitted for this authentication."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
if|if
condition|(
name|have_pty
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: you already have a pty."
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Allocating pty."
argument_list|)
expr_stmt|;
comment|/* Allocate a pty and open it. */
if|if
condition|(
operator|!
name|pty_allocate
argument_list|(
operator|&
name|ptyfd
argument_list|,
operator|&
name|ttyfd
argument_list|,
name|ttyname
argument_list|,
sizeof|sizeof
argument_list|(
name|ttyname
argument_list|)
argument_list|)
condition|)
block|{
name|error
argument_list|(
literal|"Failed to allocate pty."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|fatal_add_cleanup
argument_list|(
name|pty_release_proc
argument_list|,
operator|(
name|void
operator|*
operator|)
name|ttyname
argument_list|)
expr_stmt|;
name|pty_setowner
argument_list|(
name|pw
argument_list|,
name|ttyname
argument_list|)
expr_stmt|;
comment|/* Get TERM from the packet.  Note that the value may be of arbitrary length. */
name|term
operator|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|dlen
argument_list|,
name|strlen
argument_list|(
name|term
argument_list|)
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Remaining bytes */
name|n_bytes
operator|=
name|plen
operator|-
operator|(
literal|4
operator|+
name|dlen
operator|+
literal|4
operator|*
literal|4
operator|)
expr_stmt|;
if|if
condition|(
name|strcmp
argument_list|(
name|term
argument_list|,
literal|""
argument_list|)
operator|==
literal|0
condition|)
block|{
name|xfree
argument_list|(
name|term
argument_list|)
expr_stmt|;
name|term
operator|=
name|NULL
expr_stmt|;
block|}
comment|/* Get window size from the packet. */
name|row
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|col
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|xpixel
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|ypixel
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|pty_change_window_size
argument_list|(
name|ptyfd
argument_list|,
name|row
argument_list|,
name|col
argument_list|,
name|xpixel
argument_list|,
name|ypixel
argument_list|)
expr_stmt|;
comment|/* Get tty modes from the packet. */
name|tty_parse_modes
argument_list|(
name|ttyfd
argument_list|,
operator|&
name|n_bytes
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
operator|+
literal|4
operator|*
literal|4
operator|+
name|n_bytes
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Indicate that we now have a pty. */
name|have_pty
operator|=
literal|1
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_X11_REQUEST_FORWARDING
case|:
if|if
condition|(
operator|!
name|options
operator|.
name|x11_forwarding
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"X11 forwarding disabled in server configuration file."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
ifdef|#
directive|ifdef
name|XAUTH_PATH
if|if
condition|(
name|no_x11_forwarding_flag
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"X11 forwarding not permitted for this authentication."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|debug
argument_list|(
literal|"Received request for X11 forwarding with auth spoofing."
argument_list|)
expr_stmt|;
if|if
condition|(
name|display
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: X11 display already set."
argument_list|)
expr_stmt|;
block|{
name|int
name|proto_len
decl_stmt|,
name|data_len
decl_stmt|;
name|proto
operator|=
name|packet_get_string
argument_list|(
operator|&
name|proto_len
argument_list|)
expr_stmt|;
name|data
operator|=
name|packet_get_string
argument_list|(
operator|&
name|data_len
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|proto_len
operator|+
literal|4
operator|+
name|data_len
operator|+
literal|4
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|packet_get_protocol_flags
argument_list|()
operator|&
name|SSH_PROTOFLAG_SCREEN_NUMBER
condition|)
name|screen
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
else|else
name|screen
operator|=
literal|0
expr_stmt|;
name|display
operator|=
name|x11_create_display_inet
argument_list|(
name|screen
argument_list|,
name|options
operator|.
name|x11_display_offset
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|display
condition|)
goto|goto
name|fail
goto|;
comment|/* Setup to always have a local .Xauthority. */
name|xauthfile
operator|=
name|xmalloc
argument_list|(
name|MAXPATHLEN
argument_list|)
expr_stmt|;
name|strlcpy
argument_list|(
name|xauthfile
argument_list|,
literal|"/tmp/ssh-XXXXXXXX"
argument_list|,
name|MAXPATHLEN
argument_list|)
expr_stmt|;
name|temporarily_use_uid
argument_list|(
name|pw
operator|->
name|pw_uid
argument_list|)
expr_stmt|;
if|if
condition|(
name|mkdtemp
argument_list|(
name|xauthfile
argument_list|)
operator|==
name|NULL
condition|)
block|{
name|restore_uid
argument_list|()
expr_stmt|;
name|error
argument_list|(
literal|"private X11 dir: mkdtemp %s failed: %s"
argument_list|,
name|xauthfile
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|xauthfile
argument_list|)
expr_stmt|;
name|xauthfile
operator|=
name|NULL
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|restore_uid
argument_list|()
expr_stmt|;
name|strlcat
argument_list|(
name|xauthfile
argument_list|,
literal|"/cookies"
argument_list|,
name|MAXPATHLEN
argument_list|)
expr_stmt|;
name|fatal_add_cleanup
argument_list|(
name|xauthfile_cleanup_proc
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
break|break;
else|#
directive|else
comment|/* XAUTH_PATH */
name|packet_send_debug
argument_list|(
literal|"No xauth program; cannot forward with spoofing."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
endif|#
directive|endif
comment|/* XAUTH_PATH */
case|case
name|SSH_CMSG_AGENT_REQUEST_FORWARDING
case|:
if|if
condition|(
name|no_agent_forwarding_flag
operator|||
name|compat13
condition|)
block|{
name|debug
argument_list|(
literal|"Authentication agent forwarding not permitted for this authentication."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|debug
argument_list|(
literal|"Received authentication agent forwarding request."
argument_list|)
expr_stmt|;
name|auth_input_request_forwarding
argument_list|(
name|pw
argument_list|)
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_PORT_FORWARD_REQUEST
case|:
if|if
condition|(
name|no_port_forwarding_flag
condition|)
block|{
name|debug
argument_list|(
literal|"Port forwarding not permitted for this authentication."
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|debug
argument_list|(
literal|"Received TCP/IP port forwarding request."
argument_list|)
expr_stmt|;
name|channel_input_port_forward_request
argument_list|(
name|pw
operator|->
name|pw_uid
operator|==
literal|0
argument_list|)
expr_stmt|;
break|break;
case|case
name|SSH_CMSG_MAX_PACKET_SIZE
case|:
if|if
condition|(
name|packet_set_maxsize
argument_list|(
name|packet_get_int
argument_list|()
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|fail
goto|;
break|break;
case|case
name|SSH_CMSG_EXEC_SHELL
case|:
comment|/* Set interactive/non-interactive mode. */
name|packet_set_interactive
argument_list|(
name|have_pty
operator|||
name|display
operator|!=
name|NULL
argument_list|,
name|options
operator|.
name|keepalives
argument_list|)
expr_stmt|;
if|if
condition|(
name|forced_command
operator|!=
name|NULL
condition|)
goto|goto
name|do_forced_command
goto|;
name|debug
argument_list|(
literal|"Forking shell."
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|0
argument_list|,
name|type
argument_list|)
expr_stmt|;
if|if
condition|(
name|have_pty
condition|)
name|do_exec_pty
argument_list|(
name|NULL
argument_list|,
name|ptyfd
argument_list|,
name|ttyfd
argument_list|,
name|ttyname
argument_list|,
name|pw
argument_list|,
name|term
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
else|else
name|do_exec_no_pty
argument_list|(
name|NULL
argument_list|,
name|pw
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
return|return;
case|case
name|SSH_CMSG_EXEC_CMD
case|:
comment|/* Set interactive/non-interactive mode. */
name|packet_set_interactive
argument_list|(
name|have_pty
operator|||
name|display
operator|!=
name|NULL
argument_list|,
name|options
operator|.
name|keepalives
argument_list|)
expr_stmt|;
if|if
condition|(
name|forced_command
operator|!=
name|NULL
condition|)
goto|goto
name|do_forced_command
goto|;
comment|/* Get command from the packet. */
block|{
name|int
name|dlen
decl_stmt|;
name|command
operator|=
name|packet_get_string
argument_list|(
operator|&
name|dlen
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Executing command '%.500s'"
argument_list|,
name|command
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|dlen
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|have_pty
condition|)
name|do_exec_pty
argument_list|(
name|command
argument_list|,
name|ptyfd
argument_list|,
name|ttyfd
argument_list|,
name|ttyname
argument_list|,
name|pw
argument_list|,
name|term
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
else|else
name|do_exec_no_pty
argument_list|(
name|command
argument_list|,
name|pw
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|command
argument_list|)
expr_stmt|;
return|return;
default|default:
comment|/* 			 * Any unknown messages in this phase are ignored, 			 * and a failure message is returned. 			 */
name|log
argument_list|(
literal|"Unknown packet type received after authentication: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
comment|/* The request was successfully processed. */
name|packet_start
argument_list|(
name|SSH_SMSG_SUCCESS
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Enable compression now that we have replied if appropriate. */
if|if
condition|(
name|enable_compression_after_reply
condition|)
block|{
name|enable_compression_after_reply
operator|=
literal|0
expr_stmt|;
name|packet_start_compression
argument_list|(
name|compression_level
argument_list|)
expr_stmt|;
block|}
continue|continue;
name|fail
label|:
comment|/* The request failed. */
name|packet_start
argument_list|(
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
continue|continue;
name|do_forced_command
label|:
comment|/* 		 * There is a forced command specified for this login. 		 * Execute it. 		 */
name|debug
argument_list|(
literal|"Executing forced command: %.900s"
argument_list|,
name|forced_command
argument_list|)
expr_stmt|;
if|if
condition|(
name|have_pty
condition|)
name|do_exec_pty
argument_list|(
name|forced_command
argument_list|,
name|ptyfd
argument_list|,
name|ttyfd
argument_list|,
name|ttyname
argument_list|,
name|pw
argument_list|,
name|term
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
else|else
name|do_exec_no_pty
argument_list|(
name|forced_command
argument_list|,
name|pw
argument_list|,
name|display
argument_list|,
name|proto
argument_list|,
name|data
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
end_function

begin_comment
comment|/*  * This is called to fork and execute a command when we have no tty.  This  * will call do_child from the child, and server_loop from the parent after  * setting up file descriptors and such.  */
end_comment

begin_function
name|void
name|do_exec_no_pty
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|)
block|{
name|int
name|pid
decl_stmt|;
ifdef|#
directive|ifdef
name|USE_PIPES
name|int
name|pin
index|[
literal|2
index|]
decl_stmt|,
name|pout
index|[
literal|2
index|]
decl_stmt|,
name|perr
index|[
literal|2
index|]
decl_stmt|;
comment|/* Allocate pipes for communicating with the program. */
if|if
condition|(
name|pipe
argument_list|(
name|pin
argument_list|)
operator|<
literal|0
operator|||
name|pipe
argument_list|(
name|pout
argument_list|)
operator|<
literal|0
operator|||
name|pipe
argument_list|(
name|perr
argument_list|)
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"Could not create pipes: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* USE_PIPES */
name|int
name|inout
index|[
literal|2
index|]
decl_stmt|,
name|err
index|[
literal|2
index|]
decl_stmt|;
comment|/* Uses socket pairs to communicate with the program. */
if|if
condition|(
name|socketpair
argument_list|(
name|AF_UNIX
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|,
name|inout
argument_list|)
operator|<
literal|0
operator|||
name|socketpair
argument_list|(
name|AF_UNIX
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|,
name|err
argument_list|)
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"Could not create socket pairs: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* USE_PIPES */
name|setproctitle
argument_list|(
literal|"%s@notty"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
comment|/* Fork the child. */
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|==
literal|0
condition|)
block|{
comment|/* Child.  Reinitialize the log since the pid has changed. */
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
argument_list|,
name|log_stderr
argument_list|)
expr_stmt|;
comment|/* 		 * Create a new session and process group since the 4.4BSD 		 * setlogin() affects the entire process group. 		 */
if|if
condition|(
name|setsid
argument_list|()
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"setsid failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|USE_PIPES
comment|/* 		 * Redirect stdin.  We close the parent side of the socket 		 * pair, and make the child side the standard input. 		 */
name|close
argument_list|(
name|pin
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"dup2 stdin"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* Redirect stdout. */
name|close
argument_list|(
name|pout
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"dup2 stdout"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* Redirect stderr. */
name|close
argument_list|(
name|perr
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|perr
index|[
literal|1
index|]
argument_list|,
literal|2
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"dup2 stderr"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|perr
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* USE_PIPES */
comment|/* 		 * Redirect stdin, stdout, and stderr.  Stdin and stdout will 		 * use the same socket, as some programs (particularly rdist) 		 * seem to depend on it. 		 */
name|close
argument_list|(
name|inout
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|err
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|inout
index|[
literal|0
index|]
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
comment|/* stdin */
name|perror
argument_list|(
literal|"dup2 stdin"
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|inout
index|[
literal|0
index|]
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
condition|)
comment|/* stdout.  Note: same socket as stdin. */
name|perror
argument_list|(
literal|"dup2 stdout"
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|err
index|[
literal|0
index|]
argument_list|,
literal|2
argument_list|)
operator|<
literal|0
condition|)
comment|/* stderr */
name|perror
argument_list|(
literal|"dup2 stderr"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* USE_PIPES */
comment|/* Do processing for the child (exec command etc). */
name|do_child
argument_list|(
name|command
argument_list|,
name|pw
argument_list|,
name|NULL
argument_list|,
name|display
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"fork failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|USE_PIPES
comment|/* We are the parent.  Close the child sides of the pipes. */
name|close
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|perr
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* Enter the interactive session. */
name|server_loop
argument_list|(
name|pid
argument_list|,
name|pin
index|[
literal|1
index|]
argument_list|,
name|pout
index|[
literal|0
index|]
argument_list|,
name|perr
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* server_loop has closed pin[1], pout[1], and perr[1]. */
else|#
directive|else
comment|/* USE_PIPES */
comment|/* We are the parent.  Close the child sides of the socket pairs. */
name|close
argument_list|(
name|inout
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|err
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* 	 * Enter the interactive session.  Note: server_loop must be able to 	 * handle the case that fdin and fdout are the same. 	 */
name|server_loop
argument_list|(
name|pid
argument_list|,
name|inout
index|[
literal|1
index|]
argument_list|,
name|inout
index|[
literal|1
index|]
argument_list|,
name|err
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* server_loop has closed inout[1] and err[1]. */
endif|#
directive|endif
comment|/* USE_PIPES */
block|}
end_function

begin_comment
comment|/*  * This is called to fork and execute a command when we have a tty.  This  * will call do_child from the child, and server_loop from the parent after  * setting up file descriptors, controlling tty, updating wtmp, utmp,  * lastlog, and other such operations.  */
end_comment

begin_function
name|void
name|do_exec_pty
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|int
name|ptyfd
parameter_list|,
name|int
name|ttyfd
parameter_list|,
specifier|const
name|char
modifier|*
name|ttyname
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|term
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|)
block|{
name|int
name|pid
decl_stmt|,
name|fdout
decl_stmt|;
name|int
name|ptymaster
decl_stmt|;
specifier|const
name|char
modifier|*
name|hostname
decl_stmt|;
name|time_t
name|last_login_time
decl_stmt|;
name|char
name|buf
index|[
literal|100
index|]
decl_stmt|,
modifier|*
name|time_string
decl_stmt|;
name|FILE
modifier|*
name|f
decl_stmt|;
name|char
name|line
index|[
literal|256
index|]
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
name|int
name|quiet_login
decl_stmt|;
name|struct
name|sockaddr_storage
name|from
decl_stmt|;
name|socklen_t
name|fromlen
decl_stmt|;
name|struct
name|pty_cleanup_context
name|cleanup_context
decl_stmt|;
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|login_cap_t
modifier|*
name|lc
decl_stmt|;
name|char
modifier|*
name|fname
decl_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
comment|/* Get remote host name. */
name|hostname
operator|=
name|get_canonical_hostname
argument_list|()
expr_stmt|;
comment|/* 	 * Get the time when the user last logged in.  Buf will be set to 	 * contain the hostname the last login was from. 	 */
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
name|last_login_time
operator|=
name|get_last_login_time
argument_list|(
name|pw
operator|->
name|pw_uid
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|setproctitle
argument_list|(
literal|"%s@%s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|strrchr
argument_list|(
name|ttyname
argument_list|,
literal|'/'
argument_list|)
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* Fork the child. */
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|==
literal|0
condition|)
block|{
name|pid
operator|=
name|getpid
argument_list|()
expr_stmt|;
comment|/* Child.  Reinitialize the log because the pid has 		   changed. */
name|log_init
argument_list|(
name|av0
argument_list|,
name|options
operator|.
name|log_level
argument_list|,
name|options
operator|.
name|log_facility
argument_list|,
name|log_stderr
argument_list|)
expr_stmt|;
comment|/* Close the master side of the pseudo tty. */
name|close
argument_list|(
name|ptyfd
argument_list|)
expr_stmt|;
comment|/* Make the pseudo tty our controlling tty. */
name|pty_make_controlling_tty
argument_list|(
operator|&
name|ttyfd
argument_list|,
name|ttyname
argument_list|)
expr_stmt|;
comment|/* Redirect stdin from the pseudo tty. */
if|if
condition|(
name|dup2
argument_list|(
name|ttyfd
argument_list|,
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"dup2 stdin failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Redirect stdout to the pseudo tty. */
if|if
condition|(
name|dup2
argument_list|(
name|ttyfd
argument_list|,
name|fileno
argument_list|(
name|stdout
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"dup2 stdin failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Redirect stderr to the pseudo tty. */
if|if
condition|(
name|dup2
argument_list|(
name|ttyfd
argument_list|,
name|fileno
argument_list|(
name|stderr
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"dup2 stdin failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Close the extra descriptor for the pseudo tty. */
name|close
argument_list|(
name|ttyfd
argument_list|)
expr_stmt|;
comment|/* 		 * Get IP address of client.  This is needed because we want 		 * to record where the user logged in from.  If the 		 * connection is not a socket, let the ip address be 0.0.0.0. 		 */
name|memset
argument_list|(
operator|&
name|from
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|from
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|packet_get_connection_in
argument_list|()
operator|==
name|packet_get_connection_out
argument_list|()
condition|)
block|{
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
expr_stmt|;
if|if
condition|(
name|getpeername
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"getpeername: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
block|}
comment|/* Record that there was a login on that terminal. */
name|record_login
argument_list|(
name|pid
argument_list|,
name|ttyname
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|pw
operator|->
name|pw_uid
argument_list|,
name|hostname
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|)
expr_stmt|;
comment|/* Check if .hushlogin exists. */
name|snprintf
argument_list|(
name|line
argument_list|,
sizeof|sizeof
name|line
argument_list|,
literal|"%.200s/.hushlogin"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
name|quiet_login
operator|=
name|stat
argument_list|(
name|line
argument_list|,
operator|&
name|st
argument_list|)
operator|>=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|lc
operator|=
name|login_getpwclass
argument_list|(
name|pw
argument_list|)
expr_stmt|;
if|if
condition|(
name|lc
operator|==
name|NULL
condition|)
name|lc
operator|=
name|login_getclassbyname
argument_list|(
name|NULL
argument_list|,
name|pw
argument_list|)
expr_stmt|;
name|quiet_login
operator|=
name|login_getcapbool
argument_list|(
name|lc
argument_list|,
literal|"hushlogin"
argument_list|,
name|quiet_login
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
comment|/* 		 * If the user has logged in before, display the time of last 		 * login. However, don't display anything extra if a command 		 * has been specified (so that ssh can be used to execute 		 * commands on a remote machine without users knowing they 		 * are going to another machine). Login(1) will do this for 		 * us as well, so check if login(1) is used 		 */
if|if
condition|(
name|command
operator|==
name|NULL
operator|&&
name|last_login_time
operator|!=
literal|0
operator|&&
operator|!
name|quiet_login
operator|&&
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
comment|/* Convert the date to a string. */
name|time_string
operator|=
name|ctime
argument_list|(
operator|&
name|last_login_time
argument_list|)
expr_stmt|;
comment|/* Remove the trailing newline. */
if|if
condition|(
name|strchr
argument_list|(
name|time_string
argument_list|,
literal|'\n'
argument_list|)
condition|)
operator|*
name|strchr
argument_list|(
name|time_string
argument_list|,
literal|'\n'
argument_list|)
operator|=
literal|0
expr_stmt|;
comment|/* Display the last login time.  Host if displayed 			   if known. */
if|if
condition|(
name|strcmp
argument_list|(
name|buf
argument_list|,
literal|""
argument_list|)
operator|==
literal|0
condition|)
name|printf
argument_list|(
literal|"Last login: %s\r\n"
argument_list|,
name|time_string
argument_list|)
expr_stmt|;
else|else
name|printf
argument_list|(
literal|"Last login: %s from %s\r\n"
argument_list|,
name|time_string
argument_list|,
name|buf
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|LOGIN_CAP
if|if
condition|(
name|command
operator|==
name|NULL
operator|&&
operator|!
name|quiet_login
operator|&&
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
name|fname
operator|=
name|login_getcapstr
argument_list|(
name|lc
argument_list|,
literal|"copyright"
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|fname
operator|!=
name|NULL
operator|&&
operator|(
name|f
operator|=
name|fopen
argument_list|(
name|fname
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
while|while
condition|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
argument_list|,
name|f
argument_list|)
operator|!=
name|NULL
condition|)
name|fputs
argument_list|(
name|line
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
else|else
operator|(
name|void
operator|)
name|printf
argument_list|(
literal|"%s\n\t%s %s\n"
argument_list|,
literal|"Copyright (c) 1980, 1983, 1986, 1988, 1990, 1991, 1993, 1994"
argument_list|,
literal|"The Regents of the University of California. "
argument_list|,
literal|"All rights reserved."
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* LOGIN_CAP */
comment|/* 		 * Print /etc/motd unless a command was specified or printing 		 * it was disabled in server options or login(1) will be 		 * used.  Note that some machines appear to print it in 		 * /etc/profile or similar. 		 */
if|if
condition|(
name|command
operator|==
name|NULL
operator|&&
name|options
operator|.
name|print_motd
operator|&&
operator|!
name|quiet_login
operator|&&
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|fname
operator|=
name|login_getcapstr
argument_list|(
name|lc
argument_list|,
literal|"welcome"
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|login_close
argument_list|(
name|lc
argument_list|)
expr_stmt|;
if|if
condition|(
name|fname
operator|==
name|NULL
operator|||
operator|(
name|f
operator|=
name|fopen
argument_list|(
name|fname
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
name|f
operator|=
name|fopen
argument_list|(
literal|"/etc/motd"
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* LOGIN_CAP */
name|f
operator|=
name|fopen
argument_list|(
literal|"/etc/motd"
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
comment|/* Print /etc/motd if it exists. */
if|if
condition|(
name|f
condition|)
block|{
while|while
condition|(
name|fgets
argument_list|(
name|line
argument_list|,
sizeof|sizeof
argument_list|(
name|line
argument_list|)
argument_list|,
name|f
argument_list|)
condition|)
name|fputs
argument_list|(
name|line
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* Do common processing for the child, such as execing the command. */
name|do_child
argument_list|(
name|command
argument_list|,
name|pw
argument_list|,
name|term
argument_list|,
name|display
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|,
name|ttyname
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"fork failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Parent.  Close the slave side of the pseudo tty. */
name|close
argument_list|(
name|ttyfd
argument_list|)
expr_stmt|;
comment|/* 	 * Add a cleanup function to clear the utmp entry and record logout 	 * time in case we call fatal() (e.g., the connection gets closed). 	 */
name|cleanup_context
operator|.
name|pid
operator|=
name|pid
expr_stmt|;
name|cleanup_context
operator|.
name|ttyname
operator|=
name|ttyname
expr_stmt|;
name|fatal_add_cleanup
argument_list|(
name|pty_cleanup_proc
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|cleanup_context
argument_list|)
expr_stmt|;
name|fatal_remove_cleanup
argument_list|(
name|pty_release_proc
argument_list|,
operator|(
name|void
operator|*
operator|)
name|ttyname
argument_list|)
expr_stmt|;
comment|/* 	 * Create another descriptor of the pty master side for use as the 	 * standard input.  We could use the original descriptor, but this 	 * simplifies code in server_loop.  The descriptor is bidirectional. 	 */
name|fdout
operator|=
name|dup
argument_list|(
name|ptyfd
argument_list|)
expr_stmt|;
if|if
condition|(
name|fdout
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"dup #1 failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* we keep a reference to the pty master */
name|ptymaster
operator|=
name|dup
argument_list|(
name|ptyfd
argument_list|)
expr_stmt|;
if|if
condition|(
name|ptymaster
operator|<
literal|0
condition|)
name|packet_disconnect
argument_list|(
literal|"dup #2 failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Enter interactive session. */
name|server_loop
argument_list|(
name|pid
argument_list|,
name|ptyfd
argument_list|,
name|fdout
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
comment|/* server_loop _has_ closed ptyfd and fdout. */
comment|/* Cancel the cleanup function. */
name|fatal_remove_cleanup
argument_list|(
name|pty_cleanup_proc
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|cleanup_context
argument_list|)
expr_stmt|;
comment|/* Record that the user has logged out. */
name|record_logout
argument_list|(
name|pid
argument_list|,
name|ttyname
argument_list|)
expr_stmt|;
comment|/* Release the pseudo-tty. */
name|pty_release
argument_list|(
name|ttyname
argument_list|)
expr_stmt|;
comment|/* 	 * Close the server side of the socket pairs.  We must do this after 	 * the pty cleanup, so that another process doesn't get this pty 	 * while we're still cleaning up. 	 */
if|if
condition|(
name|close
argument_list|(
name|ptymaster
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"close(ptymaster): %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Sets the value of the given variable in the environment.  If the variable  * already exists, its value is overriden.  */
end_comment

begin_function
name|void
name|child_set_env
parameter_list|(
name|char
modifier|*
modifier|*
modifier|*
name|envp
parameter_list|,
name|unsigned
name|int
modifier|*
name|envsizep
parameter_list|,
specifier|const
name|char
modifier|*
name|name
parameter_list|,
specifier|const
name|char
modifier|*
name|value
parameter_list|)
block|{
name|unsigned
name|int
name|i
decl_stmt|,
name|namelen
decl_stmt|;
name|char
modifier|*
modifier|*
name|env
decl_stmt|;
comment|/* 	 * Find the slot where the value should be stored.  If the variable 	 * already exists, we reuse the slot; otherwise we append a new slot 	 * at the end of the array, expanding if necessary. 	 */
name|env
operator|=
operator|*
name|envp
expr_stmt|;
name|namelen
operator|=
name|strlen
argument_list|(
name|name
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|env
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|strncmp
argument_list|(
name|env
index|[
name|i
index|]
argument_list|,
name|name
argument_list|,
name|namelen
argument_list|)
operator|==
literal|0
operator|&&
name|env
index|[
name|i
index|]
index|[
name|namelen
index|]
operator|==
literal|'='
condition|)
break|break;
if|if
condition|(
name|env
index|[
name|i
index|]
condition|)
block|{
comment|/* Reuse the slot. */
name|xfree
argument_list|(
name|env
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* New variable.  Expand if necessary. */
if|if
condition|(
name|i
operator|>=
operator|(
operator|*
name|envsizep
operator|)
operator|-
literal|1
condition|)
block|{
operator|(
operator|*
name|envsizep
operator|)
operator|+=
literal|50
expr_stmt|;
name|env
operator|=
operator|(
operator|*
name|envp
operator|)
operator|=
name|xrealloc
argument_list|(
name|env
argument_list|,
operator|(
operator|*
name|envsizep
operator|)
operator|*
sizeof|sizeof
argument_list|(
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
block|}
comment|/* Need to set the NULL pointer at end of array beyond the new slot. */
name|env
index|[
name|i
operator|+
literal|1
index|]
operator|=
name|NULL
expr_stmt|;
block|}
comment|/* Allocate space and format the variable in the appropriate slot. */
name|env
index|[
name|i
index|]
operator|=
name|xmalloc
argument_list|(
name|strlen
argument_list|(
name|name
argument_list|)
operator|+
literal|1
operator|+
name|strlen
argument_list|(
name|value
argument_list|)
operator|+
literal|1
argument_list|)
expr_stmt|;
name|snprintf
argument_list|(
name|env
index|[
name|i
index|]
argument_list|,
name|strlen
argument_list|(
name|name
argument_list|)
operator|+
literal|1
operator|+
name|strlen
argument_list|(
name|value
argument_list|)
operator|+
literal|1
argument_list|,
literal|"%s=%s"
argument_list|,
name|name
argument_list|,
name|value
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Reads environment variables from the given file and adds/overrides them  * into the environment.  If the file does not exist, this does nothing.  * Otherwise, it must consist of empty lines, comments (line starts with '#')  * and assignments of the form name=value.  No other forms are allowed.  */
end_comment

begin_function
name|void
name|read_environment_file
parameter_list|(
name|char
modifier|*
modifier|*
modifier|*
name|env
parameter_list|,
name|unsigned
name|int
modifier|*
name|envsize
parameter_list|,
specifier|const
name|char
modifier|*
name|filename
parameter_list|)
block|{
name|FILE
modifier|*
name|f
decl_stmt|;
name|char
name|buf
index|[
literal|4096
index|]
decl_stmt|;
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|value
decl_stmt|;
name|f
operator|=
name|fopen
argument_list|(
name|filename
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|f
condition|)
return|return;
while|while
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|f
argument_list|)
condition|)
block|{
for|for
control|(
name|cp
operator|=
name|buf
init|;
operator|*
name|cp
operator|==
literal|' '
operator|||
operator|*
name|cp
operator|==
literal|'\t'
condition|;
name|cp
operator|++
control|)
empty_stmt|;
if|if
condition|(
operator|!
operator|*
name|cp
operator|||
operator|*
name|cp
operator|==
literal|'#'
operator|||
operator|*
name|cp
operator|==
literal|'\n'
condition|)
continue|continue;
if|if
condition|(
name|strchr
argument_list|(
name|cp
argument_list|,
literal|'\n'
argument_list|)
condition|)
operator|*
name|strchr
argument_list|(
name|cp
argument_list|,
literal|'\n'
argument_list|)
operator|=
literal|'\0'
expr_stmt|;
name|value
operator|=
name|strchr
argument_list|(
name|cp
argument_list|,
literal|'='
argument_list|)
expr_stmt|;
if|if
condition|(
name|value
operator|==
name|NULL
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Bad line in %.100s: %.200s\n"
argument_list|,
name|filename
argument_list|,
name|buf
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Replace the equals sign by nul, and advance value to the value string. */
operator|*
name|value
operator|=
literal|'\0'
expr_stmt|;
name|value
operator|++
expr_stmt|;
name|child_set_env
argument_list|(
name|env
argument_list|,
name|envsize
argument_list|,
name|cp
argument_list|,
name|value
argument_list|)
expr_stmt|;
block|}
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Performs common processing for the child, such as setting up the  * environment, closing extra file descriptors, setting the user and group  * ids, and executing the command or shell.  */
end_comment

begin_function
name|void
name|do_child
parameter_list|(
specifier|const
name|char
modifier|*
name|command
parameter_list|,
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|term
parameter_list|,
specifier|const
name|char
modifier|*
name|display
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_proto
parameter_list|,
specifier|const
name|char
modifier|*
name|auth_data
parameter_list|,
specifier|const
name|char
modifier|*
name|ttyname
parameter_list|)
block|{
name|char
modifier|*
name|shell
decl_stmt|;
specifier|const
name|char
modifier|*
name|cp
init|=
name|NULL
decl_stmt|;
name|char
name|buf
index|[
literal|256
index|]
decl_stmt|;
name|FILE
modifier|*
name|f
decl_stmt|;
name|unsigned
name|int
name|envsize
decl_stmt|,
name|i
decl_stmt|;
name|char
modifier|*
modifier|*
name|env
decl_stmt|;
specifier|extern
name|char
modifier|*
modifier|*
name|environ
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
name|char
modifier|*
name|argv
index|[
literal|10
index|]
decl_stmt|;
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|login_cap_t
modifier|*
name|lc
decl_stmt|;
name|lc
operator|=
name|login_getpwclass
argument_list|(
name|pw
argument_list|)
expr_stmt|;
if|if
condition|(
name|lc
operator|==
name|NULL
condition|)
name|lc
operator|=
name|login_getclassbyname
argument_list|(
name|NULL
argument_list|,
name|pw
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
name|f
operator|=
name|fopen
argument_list|(
literal|"/etc/nologin"
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|__FreeBSD__
if|if
condition|(
name|f
operator|==
name|NULL
condition|)
name|f
operator|=
name|fopen
argument_list|(
literal|"/var/run/nologin"
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* __FreeBSD__ */
if|if
condition|(
name|f
condition|)
block|{
comment|/* /etc/nologin exists.  Print its contents and exit. */
ifdef|#
directive|ifdef
name|LOGIN_CAP
comment|/* On FreeBSD, etc., allow overriding nologin via login.conf. */
if|if
condition|(
operator|!
name|login_getcapbool
argument_list|(
name|lc
argument_list|,
literal|"ignorenologin"
argument_list|,
literal|0
argument_list|)
condition|)
block|{
else|#
directive|else
comment|/* LOGIN_CAP */
if|if
condition|(
literal|1
condition|)
block|{
endif|#
directive|endif
comment|/* LOGIN_CAP */
while|while
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|f
argument_list|)
condition|)
name|fputs
argument_list|(
name|buf
argument_list|,
name|stderr
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
if|if
condition|(
name|pw
operator|->
name|pw_uid
operator|!=
literal|0
condition|)
name|exit
argument_list|(
literal|254
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* Set login name in the kernel. */
if|if
condition|(
name|setlogin
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"setlogin failed: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Set uid, gid, and groups. */
comment|/* Login(1) does this as well, and it needs uid 0 for the "-h" 	   switch, so we let login(1) to this for us. */
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
ifdef|#
directive|ifdef
name|LOGIN_CAP
if|if
condition|(
name|setclasscontext
argument_list|(
name|pw
operator|->
name|pw_class
argument_list|,
name|LOGIN_SETPRIORITY
operator||
name|LOGIN_SETRESOURCES
operator||
name|LOGIN_SETUMASK
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|perror
argument_list|(
literal|"setclasscontext"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* LOGIN_CAP */
if|if
condition|(
name|getuid
argument_list|()
operator|==
literal|0
operator|||
name|geteuid
argument_list|()
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|setgid
argument_list|(
name|pw
operator|->
name|pw_gid
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"setgid"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
comment|/* Initialize the group list. */
if|if
condition|(
name|initgroups
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|,
name|pw
operator|->
name|pw_gid
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"initgroups"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|endgrent
argument_list|()
expr_stmt|;
comment|/* Permanently switch to the desired uid. */
name|permanently_set_uid
argument_list|(
name|pw
operator|->
name|pw_uid
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|getuid
argument_list|()
operator|!=
name|pw
operator|->
name|pw_uid
operator|||
name|geteuid
argument_list|()
operator|!=
name|pw
operator|->
name|pw_uid
condition|)
name|fatal
argument_list|(
literal|"Failed to set uids to %d."
argument_list|,
operator|(
name|int
operator|)
name|pw
operator|->
name|pw_uid
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Get the shell from the password data.  An empty shell field is 	 * legal, and means /bin/sh. 	 */
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|shell
operator|=
name|pw
operator|->
name|pw_shell
expr_stmt|;
name|shell
operator|=
name|login_getcapstr
argument_list|(
name|lc
argument_list|,
literal|"shell"
argument_list|,
name|shell
argument_list|,
name|shell
argument_list|)
expr_stmt|;
if|if
condition|(
name|shell
index|[
literal|0
index|]
operator|==
literal|'\0'
condition|)
name|shell
operator|=
name|_PATH_BSHELL
expr_stmt|;
else|#
directive|else
comment|/* LOGIN_CAP */
name|shell
operator|=
operator|(
name|pw
operator|->
name|pw_shell
index|[
literal|0
index|]
operator|==
literal|'\0'
operator|)
condition|?
name|_PATH_BSHELL
else|:
name|pw
operator|->
name|pw_shell
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
ifdef|#
directive|ifdef
name|AFS
comment|/* Try to get AFS tokens for the local cell. */
if|if
condition|(
name|k_hasafs
argument_list|()
condition|)
block|{
name|char
name|cell
index|[
literal|64
index|]
decl_stmt|;
if|if
condition|(
name|k_afs_cell_of_file
argument_list|(
name|pw
operator|->
name|pw_dir
argument_list|,
name|cell
argument_list|,
sizeof|sizeof
argument_list|(
name|cell
argument_list|)
argument_list|)
operator|==
literal|0
condition|)
name|krb_afslog
argument_list|(
name|cell
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|krb_afslog
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* AFS */
comment|/* Initialize the environment. */
name|envsize
operator|=
literal|100
expr_stmt|;
name|env
operator|=
name|xmalloc
argument_list|(
name|envsize
operator|*
sizeof|sizeof
argument_list|(
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
name|env
index|[
literal|0
index|]
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
comment|/* Set basic environment. */
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"USER"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"LOGNAME"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"HOME"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"PATH"
argument_list|,
name|login_getpath
argument_list|(
name|lc
argument_list|,
literal|"path"
argument_list|,
name|_PATH_STDPATH
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* LOGIN_CAP */
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"PATH"
argument_list|,
name|_PATH_STDPATH
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"%.200s/%.50s"
argument_list|,
name|_PATH_MAILDIR
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"MAIL"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
comment|/* Normal systems set SHELL by default. */
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"SHELL"
argument_list|,
name|shell
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|getenv
argument_list|(
literal|"TZ"
argument_list|)
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"TZ"
argument_list|,
name|getenv
argument_list|(
literal|"TZ"
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Set custom environment options from RSA authentication. */
while|while
condition|(
name|custom_environment
condition|)
block|{
name|struct
name|envstring
modifier|*
name|ce
init|=
name|custom_environment
decl_stmt|;
name|char
modifier|*
name|s
init|=
name|ce
operator|->
name|s
decl_stmt|;
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|s
index|[
name|i
index|]
operator|!=
literal|'='
operator|&&
name|s
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
empty_stmt|;
if|if
condition|(
name|s
index|[
name|i
index|]
operator|==
literal|'='
condition|)
block|{
name|s
index|[
name|i
index|]
operator|=
literal|0
expr_stmt|;
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
name|s
argument_list|,
name|s
operator|+
name|i
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
name|custom_environment
operator|=
name|ce
operator|->
name|next
expr_stmt|;
name|xfree
argument_list|(
name|ce
operator|->
name|s
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|ce
argument_list|)
expr_stmt|;
block|}
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"%.50s %d %d"
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
name|get_remote_port
argument_list|()
argument_list|,
name|get_local_port
argument_list|()
argument_list|)
expr_stmt|;
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"SSH_CLIENT"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|ttyname
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"SSH_TTY"
argument_list|,
name|ttyname
argument_list|)
expr_stmt|;
if|if
condition|(
name|term
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"TERM"
argument_list|,
name|term
argument_list|)
expr_stmt|;
if|if
condition|(
name|display
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"DISPLAY"
argument_list|,
name|display
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|KRB4
block|{
specifier|extern
name|char
modifier|*
name|ticket
decl_stmt|;
if|if
condition|(
name|ticket
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"KRBTKFILE"
argument_list|,
name|ticket
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* KRB4 */
if|if
condition|(
name|xauthfile
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
literal|"XAUTHORITY"
argument_list|,
name|xauthfile
argument_list|)
expr_stmt|;
if|if
condition|(
name|auth_get_socket_name
argument_list|()
operator|!=
name|NULL
condition|)
name|child_set_env
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
name|SSH_AUTHSOCKET_ENV_NAME
argument_list|,
name|auth_get_socket_name
argument_list|()
argument_list|)
expr_stmt|;
comment|/* read $HOME/.ssh/environment. */
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"%.200s/.ssh/environment"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
name|read_environment_file
argument_list|(
operator|&
name|env
argument_list|,
operator|&
name|envsize
argument_list|,
name|buf
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|debug_flag
condition|)
block|{
comment|/* dump the environment */
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Environment:\n"
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|env
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"  %.200s\n"
argument_list|,
name|env
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Close the connection descriptors; note that this is the child, and 	 * the server will still have the socket open, and it is important 	 * that we do not shutdown it.  Note that the descriptors cannot be 	 * closed before building the environment, as we call 	 * get_remote_ipaddr there. 	 */
if|if
condition|(
name|packet_get_connection_in
argument_list|()
operator|==
name|packet_get_connection_out
argument_list|()
condition|)
name|close
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|)
expr_stmt|;
else|else
block|{
name|close
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|packet_get_connection_out
argument_list|()
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Close all descriptors related to channels.  They will still remain 	 * open in the parent. 	 */
comment|/* XXX better use close-on-exec? -markus */
name|channel_close_all
argument_list|()
expr_stmt|;
comment|/* 	 * Close any extra file descriptors.  Note that there may still be 	 * descriptors left by system functions.  They will be closed later. 	 */
name|endpwent
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|LOGIN_CAP
name|login_close
argument_list|(
name|lc
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* LOGIN_CAP */
comment|/* 	 * Close any extra open file descriptors so that we don\'t have them 	 * hanging around in clients.  Note that we want to do this after 	 * initgroups, because at least on Solaris 2.3 it leaves file 	 * descriptors open. 	 */
for|for
control|(
name|i
operator|=
literal|3
init|;
name|i
operator|<
name|getdtablesize
argument_list|()
condition|;
name|i
operator|++
control|)
name|close
argument_list|(
name|i
argument_list|)
expr_stmt|;
comment|/* Change current directory to the user\'s home directory. */
if|if
condition|(
name|chdir
argument_list|(
name|pw
operator|->
name|pw_dir
argument_list|)
operator|<
literal|0
condition|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Could not chdir to home directory %s: %s\n"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * Must take new environment into use so that .ssh/rc, /etc/sshrc and 	 * xauth are run in the proper environment. 	 */
name|environ
operator|=
name|env
expr_stmt|;
comment|/* 	 * Run $HOME/.ssh/rc, /etc/sshrc, or xauth (whichever is found first 	 * in this order). 	 */
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
ifdef|#
directive|ifdef
name|__FreeBSD__
comment|/* 		 * If the password change time is set and has passed, give the 		 * user a password expiry notice and chance to change it. 		 */
if|if
condition|(
name|pw
operator|->
name|pw_change
operator|!=
literal|0
condition|)
block|{
name|struct
name|timeval
name|tv
decl_stmt|;
operator|(
name|void
operator|)
name|gettimeofday
argument_list|(
operator|&
name|tv
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|tv
operator|.
name|tv_sec
operator|>=
name|pw
operator|->
name|pw_change
condition|)
block|{
operator|(
name|void
operator|)
name|printf
argument_list|(
literal|"Sorry -- your password has expired.\n"
argument_list|)
expr_stmt|;
name|syslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"%s Password expired - forcing change"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
if|if
condition|(
name|system
argument_list|(
literal|"/usr/bin/passwd"
argument_list|)
operator|!=
literal|0
condition|)
name|perror
argument_list|(
literal|"/usr/bin/passwd"
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* __FreeBSD__ */
if|if
condition|(
name|stat
argument_list|(
name|SSH_USER_RC
argument_list|,
operator|&
name|st
argument_list|)
operator|>=
literal|0
condition|)
block|{
if|if
condition|(
name|debug_flag
condition|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Running /bin/sh %s\n"
argument_list|,
name|SSH_USER_RC
argument_list|)
expr_stmt|;
name|f
operator|=
name|popen
argument_list|(
literal|"/bin/sh "
name|SSH_USER_RC
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
condition|)
block|{
if|if
condition|(
name|auth_proto
operator|!=
name|NULL
operator|&&
name|auth_data
operator|!=
name|NULL
condition|)
name|fprintf
argument_list|(
name|f
argument_list|,
literal|"%s %s\n"
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|)
expr_stmt|;
name|pclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
else|else
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Could not run %s\n"
argument_list|,
name|SSH_USER_RC
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|stat
argument_list|(
name|SSH_SYSTEM_RC
argument_list|,
operator|&
name|st
argument_list|)
operator|>=
literal|0
condition|)
block|{
if|if
condition|(
name|debug_flag
condition|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Running /bin/sh %s\n"
argument_list|,
name|SSH_SYSTEM_RC
argument_list|)
expr_stmt|;
name|f
operator|=
name|popen
argument_list|(
literal|"/bin/sh "
name|SSH_SYSTEM_RC
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
condition|)
block|{
if|if
condition|(
name|auth_proto
operator|!=
name|NULL
operator|&&
name|auth_data
operator|!=
name|NULL
condition|)
name|fprintf
argument_list|(
name|f
argument_list|,
literal|"%s %s\n"
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|)
expr_stmt|;
name|pclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
else|else
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Could not run %s\n"
argument_list|,
name|SSH_SYSTEM_RC
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|XAUTH_PATH
else|else
block|{
comment|/* Add authority data to .Xauthority if appropriate. */
if|if
condition|(
name|auth_proto
operator|!=
name|NULL
operator|&&
name|auth_data
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|debug_flag
condition|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Running %.100s add %.100s %.100s %.100s\n"
argument_list|,
name|XAUTH_PATH
argument_list|,
name|display
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|)
expr_stmt|;
name|f
operator|=
name|popen
argument_list|(
name|XAUTH_PATH
literal|" -q -"
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
condition|)
block|{
name|fprintf
argument_list|(
name|f
argument_list|,
literal|"add %s %s %s\n"
argument_list|,
name|display
argument_list|,
name|auth_proto
argument_list|,
name|auth_data
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
else|else
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Could not run %s -q -\n"
argument_list|,
name|XAUTH_PATH
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* XAUTH_PATH */
comment|/* Get the last component of the shell name. */
name|cp
operator|=
name|strrchr
argument_list|(
name|shell
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
condition|)
name|cp
operator|++
expr_stmt|;
else|else
name|cp
operator|=
name|shell
expr_stmt|;
block|}
comment|/* 	 * If we have no command, execute the shell.  In this case, the shell 	 * name to be passed in argv[0] is preceded by '-' to indicate that 	 * this is a login shell. 	 */
if|if
condition|(
operator|!
name|command
condition|)
block|{
if|if
condition|(
operator|!
name|options
operator|.
name|use_login
condition|)
block|{
name|char
name|buf
index|[
literal|256
index|]
decl_stmt|;
comment|/* 			 * Check for mail if we have a tty and it was enabled 			 * in server options. 			 */
if|if
condition|(
name|ttyname
operator|&&
name|options
operator|.
name|check_mail
condition|)
block|{
name|char
modifier|*
name|mailbox
decl_stmt|;
name|struct
name|stat
name|mailstat
decl_stmt|;
name|mailbox
operator|=
name|getenv
argument_list|(
literal|"MAIL"
argument_list|)
expr_stmt|;
if|if
condition|(
name|mailbox
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|stat
argument_list|(
name|mailbox
argument_list|,
operator|&
name|mailstat
argument_list|)
operator|!=
literal|0
operator|||
name|mailstat
operator|.
name|st_size
operator|==
literal|0
condition|)
name|printf
argument_list|(
literal|"No mail.\n"
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|mailstat
operator|.
name|st_mtime
operator|<
name|mailstat
operator|.
name|st_atime
condition|)
name|printf
argument_list|(
literal|"You have mail.\n"
argument_list|)
expr_stmt|;
else|else
name|printf
argument_list|(
literal|"You have new mail.\n"
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* Start the shell.  Set initial character to '-'. */
name|buf
index|[
literal|0
index|]
operator|=
literal|'-'
expr_stmt|;
name|strncpy
argument_list|(
name|buf
operator|+
literal|1
argument_list|,
name|cp
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
argument_list|)
expr_stmt|;
name|buf
index|[
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|0
expr_stmt|;
comment|/* Execute the shell. */
name|argv
index|[
literal|0
index|]
operator|=
name|buf
expr_stmt|;
name|argv
index|[
literal|1
index|]
operator|=
name|NULL
expr_stmt|;
name|execve
argument_list|(
name|shell
argument_list|,
name|argv
argument_list|,
name|env
argument_list|)
expr_stmt|;
comment|/* Executing the shell failed. */
name|perror
argument_list|(
name|shell
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* Launch login(1). */
name|execl
argument_list|(
literal|"/usr/bin/login"
argument_list|,
literal|"login"
argument_list|,
literal|"-h"
argument_list|,
name|get_remote_ipaddr
argument_list|()
argument_list|,
literal|"-p"
argument_list|,
literal|"-f"
argument_list|,
literal|"--"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* Login couldn't be executed, die. */
name|perror
argument_list|(
literal|"login"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* 	 * Execute the command using the user's shell.  This uses the -c 	 * option to execute the command. 	 */
name|argv
index|[
literal|0
index|]
operator|=
operator|(
name|char
operator|*
operator|)
name|cp
expr_stmt|;
name|argv
index|[
literal|1
index|]
operator|=
literal|"-c"
expr_stmt|;
name|argv
index|[
literal|2
index|]
operator|=
operator|(
name|char
operator|*
operator|)
name|command
expr_stmt|;
name|argv
index|[
literal|3
index|]
operator|=
name|NULL
expr_stmt|;
name|execve
argument_list|(
name|shell
argument_list|,
name|argv
argument_list|,
name|env
argument_list|)
expr_stmt|;
name|perror
argument_list|(
name|shell
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

