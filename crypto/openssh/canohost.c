begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $OpenBSD: canohost.c,v 1.73 2016/03/07 19:02:43 djm Exp $ */
end_comment

begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Functions for returning the canonical host name of the remote site.  *  * As far as I am concerned, the code I have written for this software  * can be used freely for any purpose.  Any derived versions of this  * software must be clearly marked as such, and if the derived work is  * incompatible with the protocol description in the RFC file, it must be  * called by a name other than "ssh" or "Secure Shell".  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<sys/un.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<arpa/inet.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<netdb.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<stdarg.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|"xmalloc.h"
end_include

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"log.h"
end_include

begin_include
include|#
directive|include
file|"canohost.h"
end_include

begin_include
include|#
directive|include
file|"misc.h"
end_include

begin_function
name|void
name|ipv64_normalise_mapped
parameter_list|(
name|struct
name|sockaddr_storage
modifier|*
name|addr
parameter_list|,
name|socklen_t
modifier|*
name|len
parameter_list|)
block|{
name|struct
name|sockaddr_in6
modifier|*
name|a6
init|=
operator|(
expr|struct
name|sockaddr_in6
operator|*
operator|)
name|addr
decl_stmt|;
name|struct
name|sockaddr_in
modifier|*
name|a4
init|=
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|addr
decl_stmt|;
name|struct
name|in_addr
name|inaddr
decl_stmt|;
name|u_int16_t
name|port
decl_stmt|;
if|if
condition|(
name|addr
operator|->
name|ss_family
operator|!=
name|AF_INET6
operator|||
operator|!
name|IN6_IS_ADDR_V4MAPPED
argument_list|(
operator|&
name|a6
operator|->
name|sin6_addr
argument_list|)
condition|)
return|return;
name|debug3
argument_list|(
literal|"Normalising mapped IPv4 in IPv6 address"
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|inaddr
argument_list|,
operator|(
operator|(
name|char
operator|*
operator|)
operator|&
name|a6
operator|->
name|sin6_addr
operator|)
operator|+
literal|12
argument_list|,
sizeof|sizeof
argument_list|(
name|inaddr
argument_list|)
argument_list|)
expr_stmt|;
name|port
operator|=
name|a6
operator|->
name|sin6_port
expr_stmt|;
name|memset
argument_list|(
name|a4
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|a4
argument_list|)
argument_list|)
expr_stmt|;
name|a4
operator|->
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
operator|*
name|len
operator|=
sizeof|sizeof
argument_list|(
operator|*
name|a4
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|a4
operator|->
name|sin_addr
argument_list|,
operator|&
name|inaddr
argument_list|,
sizeof|sizeof
argument_list|(
name|inaddr
argument_list|)
argument_list|)
expr_stmt|;
name|a4
operator|->
name|sin_port
operator|=
name|port
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Returns the local/remote IP-address/hostname of socket as a string.  * The returned string must be freed.  */
end_comment

begin_function
specifier|static
name|char
modifier|*
name|get_socket_address
parameter_list|(
name|int
name|sock
parameter_list|,
name|int
name|remote
parameter_list|,
name|int
name|flags
parameter_list|)
block|{
name|struct
name|sockaddr_storage
name|addr
decl_stmt|;
name|socklen_t
name|addrlen
decl_stmt|;
name|char
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|;
name|int
name|r
decl_stmt|;
comment|/* Get IP address of client. */
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|addr
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|remote
condition|)
block|{
if|if
condition|(
name|getpeername
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
operator|&
name|addrlen
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|NULL
return|;
block|}
else|else
block|{
if|if
condition|(
name|getsockname
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
operator|&
name|addrlen
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|NULL
return|;
block|}
comment|/* Work around Linux IPv6 weirdness */
if|if
condition|(
name|addr
operator|.
name|ss_family
operator|==
name|AF_INET6
condition|)
block|{
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in6
argument_list|)
expr_stmt|;
name|ipv64_normalise_mapped
argument_list|(
operator|&
name|addr
argument_list|,
operator|&
name|addrlen
argument_list|)
expr_stmt|;
block|}
switch|switch
condition|(
name|addr
operator|.
name|ss_family
condition|)
block|{
case|case
name|AF_INET
case|:
case|case
name|AF_INET6
case|:
comment|/* Get the address in ascii. */
if|if
condition|(
operator|(
name|r
operator|=
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
name|addrlen
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|flags
argument_list|)
operator|)
operator|!=
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"%s: getnameinfo %d failed: %s"
argument_list|,
name|__func__
argument_list|,
name|flags
argument_list|,
name|ssh_gai_strerror
argument_list|(
name|r
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
return|return
name|xstrdup
argument_list|(
name|ntop
argument_list|)
return|;
case|case
name|AF_UNIX
case|:
comment|/* Get the Unix domain socket path. */
return|return
name|xstrdup
argument_list|(
operator|(
operator|(
expr|struct
name|sockaddr_un
operator|*
operator|)
operator|&
name|addr
operator|)
operator|->
name|sun_path
argument_list|)
return|;
default|default:
comment|/* We can't look up remote Unix domain sockets. */
return|return
name|NULL
return|;
block|}
block|}
end_function

begin_function
name|char
modifier|*
name|get_peer_ipaddr
parameter_list|(
name|int
name|sock
parameter_list|)
block|{
name|char
modifier|*
name|p
decl_stmt|;
if|if
condition|(
operator|(
name|p
operator|=
name|get_socket_address
argument_list|(
name|sock
argument_list|,
literal|1
argument_list|,
name|NI_NUMERICHOST
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
return|return
name|p
return|;
return|return
name|xstrdup
argument_list|(
literal|"UNKNOWN"
argument_list|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_local_ipaddr
parameter_list|(
name|int
name|sock
parameter_list|)
block|{
name|char
modifier|*
name|p
decl_stmt|;
if|if
condition|(
operator|(
name|p
operator|=
name|get_socket_address
argument_list|(
name|sock
argument_list|,
literal|0
argument_list|,
name|NI_NUMERICHOST
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
return|return
name|p
return|;
return|return
name|xstrdup
argument_list|(
literal|"UNKNOWN"
argument_list|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_local_name
parameter_list|(
name|int
name|fd
parameter_list|)
block|{
name|char
modifier|*
name|host
decl_stmt|,
name|myname
index|[
name|NI_MAXHOST
index|]
decl_stmt|;
comment|/* Assume we were passed a socket */
if|if
condition|(
operator|(
name|host
operator|=
name|get_socket_address
argument_list|(
name|fd
argument_list|,
literal|0
argument_list|,
name|NI_NAMEREQD
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
return|return
name|host
return|;
comment|/* Handle the case where we were passed a pipe */
if|if
condition|(
name|gethostname
argument_list|(
name|myname
argument_list|,
sizeof|sizeof
argument_list|(
name|myname
argument_list|)
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|verbose
argument_list|(
literal|"%s: gethostname: %s"
argument_list|,
name|__func__
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|host
operator|=
name|xstrdup
argument_list|(
literal|"UNKNOWN"
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|host
operator|=
name|xstrdup
argument_list|(
name|myname
argument_list|)
expr_stmt|;
block|}
return|return
name|host
return|;
block|}
end_function

begin_comment
comment|/* Returns the local/remote port for the socket. */
end_comment

begin_function
specifier|static
name|int
name|get_sock_port
parameter_list|(
name|int
name|sock
parameter_list|,
name|int
name|local
parameter_list|)
block|{
name|struct
name|sockaddr_storage
name|from
decl_stmt|;
name|socklen_t
name|fromlen
decl_stmt|;
name|char
name|strport
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
name|int
name|r
decl_stmt|;
comment|/* Get IP address of client. */
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|from
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|from
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|local
condition|)
block|{
if|if
condition|(
name|getsockname
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"getsockname failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
block|}
else|else
block|{
if|if
condition|(
name|getpeername
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"getpeername failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
comment|/* Work around Linux IPv6 weirdness */
if|if
condition|(
name|from
operator|.
name|ss_family
operator|==
name|AF_INET6
condition|)
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in6
argument_list|)
expr_stmt|;
comment|/* Non-inet sockets don't have a port number. */
if|if
condition|(
name|from
operator|.
name|ss_family
operator|!=
name|AF_INET
operator|&&
name|from
operator|.
name|ss_family
operator|!=
name|AF_INET6
condition|)
return|return
literal|0
return|;
comment|/* Return port number. */
if|if
condition|(
operator|(
name|r
operator|=
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
name|fromlen
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|strport
argument_list|,
sizeof|sizeof
argument_list|(
name|strport
argument_list|)
argument_list|,
name|NI_NUMERICSERV
argument_list|)
operator|)
operator|!=
literal|0
condition|)
name|fatal
argument_list|(
literal|"%s: getnameinfo NI_NUMERICSERV failed: %s"
argument_list|,
name|__func__
argument_list|,
name|ssh_gai_strerror
argument_list|(
name|r
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|atoi
argument_list|(
name|strport
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|get_peer_port
parameter_list|(
name|int
name|sock
parameter_list|)
block|{
return|return
name|get_sock_port
argument_list|(
name|sock
argument_list|,
literal|0
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|get_local_port
parameter_list|(
name|int
name|sock
parameter_list|)
block|{
return|return
name|get_sock_port
argument_list|(
name|sock
argument_list|,
literal|1
argument_list|)
return|;
block|}
end_function

end_unit

