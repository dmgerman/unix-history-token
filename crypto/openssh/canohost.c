begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Functions for returning the canonical host name of the remote site.  *  * As far as I am concerned, the code I have written for this software  * can be used freely for any purpose.  Any derived versions of this  * software must be clearly marked as such, and if the derived work is  * incompatible with the protocol description in the RFC file, it must be  * called by a name other than "ssh" or "Secure Shell".  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$OpenBSD: canohost.c,v 1.26 2001/04/18 14:15:00 markus Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$FreeBSD$"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"xmalloc.h"
end_include

begin_include
include|#
directive|include
file|"log.h"
end_include

begin_include
include|#
directive|include
file|"canohost.h"
end_include

begin_function_decl
name|void
name|check_ip_options
parameter_list|(
name|int
name|socket
parameter_list|,
name|char
modifier|*
name|ipaddr
parameter_list|)
function_decl|;
end_function_decl

begin_comment
comment|/*  * Return the canonical name of the host at the other end of the socket. The  * caller should free the returned string with xfree.  */
end_comment

begin_function
name|char
modifier|*
name|get_remote_hostname
parameter_list|(
name|int
name|socket
parameter_list|,
name|int
name|reverse_mapping_check
parameter_list|)
block|{
name|struct
name|sockaddr_storage
name|from
decl_stmt|;
name|int
name|i
decl_stmt|;
name|socklen_t
name|fromlen
decl_stmt|;
name|struct
name|addrinfo
name|hints
decl_stmt|,
modifier|*
name|ai
decl_stmt|,
modifier|*
name|aitop
decl_stmt|;
name|char
name|name
index|[
name|NI_MAXHOST
index|]
decl_stmt|,
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|,
name|ntop2
index|[
name|NI_MAXHOST
index|]
decl_stmt|;
comment|/* Get IP address of client. */
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|from
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|from
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|getpeername
argument_list|(
name|socket
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"getpeername failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
name|from
operator|.
name|ss_family
operator|==
name|AF_INET
condition|)
name|check_ip_options
argument_list|(
name|socket
argument_list|,
name|ntop
argument_list|)
expr_stmt|;
if|if
condition|(
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
name|fromlen
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|NI_NUMERICHOST
argument_list|)
operator|!=
literal|0
condition|)
name|fatal
argument_list|(
literal|"get_remote_hostname: getnameinfo NI_NUMERICHOST failed"
argument_list|)
expr_stmt|;
name|debug3
argument_list|(
literal|"Trying to reverse map address %.100s."
argument_list|,
name|ntop
argument_list|)
expr_stmt|;
comment|/* Map the IP address to a host name. */
if|if
condition|(
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
name|fromlen
argument_list|,
name|name
argument_list|,
sizeof|sizeof
argument_list|(
name|name
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|NI_NAMEREQD
argument_list|)
operator|!=
literal|0
condition|)
block|{
comment|/* Host name not found.  Use ip address. */
name|log
argument_list|(
literal|"Could not reverse map address %.100s."
argument_list|,
name|ntop
argument_list|)
expr_stmt|;
return|return
name|xstrdup
argument_list|(
name|ntop
argument_list|)
return|;
block|}
comment|/* Got host name. */
name|name
index|[
sizeof|sizeof
argument_list|(
name|name
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/* 	 * Convert it to all lowercase (which is expected by the rest 	 * of this software). 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|name
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|isupper
argument_list|(
name|name
index|[
name|i
index|]
argument_list|)
condition|)
name|name
index|[
name|i
index|]
operator|=
name|tolower
argument_list|(
name|name
index|[
name|i
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|reverse_mapping_check
condition|)
return|return
name|xstrdup
argument_list|(
name|name
argument_list|)
return|;
comment|/* 	 * Map it back to an IP address and check that the given 	 * address actually is an address of this host.  This is 	 * necessary because anyone with access to a name server can 	 * define arbitrary names for an IP address. Mapping from 	 * name to IP address can be trusted better (but can still be 	 * fooled if the intruder has access to the name server of 	 * the domain). 	 */
name|memset
argument_list|(
operator|&
name|hints
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|hints
argument_list|)
argument_list|)
expr_stmt|;
name|hints
operator|.
name|ai_family
operator|=
name|from
operator|.
name|ss_family
expr_stmt|;
name|hints
operator|.
name|ai_socktype
operator|=
name|SOCK_STREAM
expr_stmt|;
if|if
condition|(
name|getaddrinfo
argument_list|(
name|name
argument_list|,
name|NULL
argument_list|,
operator|&
name|hints
argument_list|,
operator|&
name|aitop
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|log
argument_list|(
literal|"reverse mapping checking getaddrinfo for %.700s "
literal|"failed - POSSIBLE BREAKIN ATTEMPT!"
argument_list|,
name|name
argument_list|)
expr_stmt|;
return|return
name|xstrdup
argument_list|(
name|ntop
argument_list|)
return|;
block|}
comment|/* Look for the address from the list of addresses. */
for|for
control|(
name|ai
operator|=
name|aitop
init|;
name|ai
condition|;
name|ai
operator|=
name|ai
operator|->
name|ai_next
control|)
block|{
if|if
condition|(
name|getnameinfo
argument_list|(
name|ai
operator|->
name|ai_addr
argument_list|,
name|ai
operator|->
name|ai_addrlen
argument_list|,
name|ntop2
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop2
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|NI_NUMERICHOST
argument_list|)
operator|==
literal|0
operator|&&
operator|(
name|strcmp
argument_list|(
name|ntop
argument_list|,
name|ntop2
argument_list|)
operator|==
literal|0
operator|)
condition|)
break|break;
block|}
name|freeaddrinfo
argument_list|(
name|aitop
argument_list|)
expr_stmt|;
comment|/* If we reached the end of the list, the address was not there. */
if|if
condition|(
operator|!
name|ai
condition|)
block|{
comment|/* Address not found for the host name. */
name|log
argument_list|(
literal|"Address %.100s maps to %.600s, but this does not "
literal|"map back to the address - POSSIBLE BREAKIN ATTEMPT!"
argument_list|,
name|ntop
argument_list|,
name|name
argument_list|)
expr_stmt|;
return|return
name|xstrdup
argument_list|(
name|ntop
argument_list|)
return|;
block|}
return|return
name|xstrdup
argument_list|(
name|name
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * If IP options are supported, make sure there are none (log and  * disconnect them if any are found).  Basically we are worried about  * source routing; it can be used to pretend you are somebody  * (ip-address) you are not. That itself may be "almost acceptable"  * under certain circumstances, but rhosts autentication is useless  * if source routing is accepted. Notice also that if we just dropped  * source routing here, the other side could use IP spoofing to do  * rest of the interaction and could still bypass security.  So we  * exit here if we detect any IP options.  */
end_comment

begin_comment
comment|/* IPv4 only */
end_comment

begin_function
name|void
name|check_ip_options
parameter_list|(
name|int
name|socket
parameter_list|,
name|char
modifier|*
name|ipaddr
parameter_list|)
block|{
name|u_char
name|options
index|[
literal|200
index|]
decl_stmt|;
name|char
name|text
index|[
sizeof|sizeof
argument_list|(
name|options
argument_list|)
operator|*
literal|3
operator|+
literal|1
index|]
decl_stmt|;
name|socklen_t
name|option_size
decl_stmt|;
name|int
name|i
decl_stmt|,
name|ipproto
decl_stmt|;
name|struct
name|protoent
modifier|*
name|ip
decl_stmt|;
if|if
condition|(
operator|(
name|ip
operator|=
name|getprotobyname
argument_list|(
literal|"ip"
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
name|ipproto
operator|=
name|ip
operator|->
name|p_proto
expr_stmt|;
else|else
name|ipproto
operator|=
name|IPPROTO_IP
expr_stmt|;
name|option_size
operator|=
sizeof|sizeof
argument_list|(
name|options
argument_list|)
expr_stmt|;
if|if
condition|(
name|getsockopt
argument_list|(
name|socket
argument_list|,
name|ipproto
argument_list|,
name|IP_OPTIONS
argument_list|,
operator|(
name|void
operator|*
operator|)
name|options
argument_list|,
operator|&
name|option_size
argument_list|)
operator|>=
literal|0
operator|&&
name|option_size
operator|!=
literal|0
condition|)
block|{
name|text
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|option_size
condition|;
name|i
operator|++
control|)
name|snprintf
argument_list|(
name|text
operator|+
name|i
operator|*
literal|3
argument_list|,
sizeof|sizeof
argument_list|(
name|text
argument_list|)
operator|-
name|i
operator|*
literal|3
argument_list|,
literal|" %2.2x"
argument_list|,
name|options
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"Connection from %.100s with IP options:%.800s"
argument_list|,
name|ipaddr
argument_list|,
name|text
argument_list|)
expr_stmt|;
name|packet_disconnect
argument_list|(
literal|"Connection from %.100s with IP options:%.800s"
argument_list|,
name|ipaddr
argument_list|,
name|text
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * Return the canonical name of the host in the other side of the current  * connection.  The host name is cached, so it is efficient to call this  * several times.  */
end_comment

begin_function
specifier|const
name|char
modifier|*
name|get_canonical_hostname
parameter_list|(
name|int
name|reverse_mapping_check
parameter_list|)
block|{
specifier|static
name|char
modifier|*
name|canonical_host_name
init|=
name|NULL
decl_stmt|;
specifier|static
name|int
name|reverse_mapping_checked
init|=
literal|0
decl_stmt|;
comment|/* Check if we have previously retrieved name with same option. */
if|if
condition|(
name|canonical_host_name
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|reverse_mapping_checked
operator|!=
name|reverse_mapping_check
condition|)
name|xfree
argument_list|(
name|canonical_host_name
argument_list|)
expr_stmt|;
else|else
return|return
name|canonical_host_name
return|;
block|}
comment|/* Get the real hostname if socket; otherwise return UNKNOWN. */
if|if
condition|(
name|packet_connection_is_on_socket
argument_list|()
condition|)
name|canonical_host_name
operator|=
name|get_remote_hostname
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|,
name|reverse_mapping_check
argument_list|)
expr_stmt|;
else|else
name|canonical_host_name
operator|=
name|xstrdup
argument_list|(
literal|"UNKNOWN"
argument_list|)
expr_stmt|;
name|reverse_mapping_checked
operator|=
name|reverse_mapping_check
expr_stmt|;
return|return
name|canonical_host_name
return|;
block|}
end_function

begin_comment
comment|/*  * Returns the remote IP-address of socket as a string.  The returned  * string must be freed.  */
end_comment

begin_function
name|char
modifier|*
name|get_socket_address
parameter_list|(
name|int
name|socket
parameter_list|,
name|int
name|remote
parameter_list|,
name|int
name|flags
parameter_list|)
block|{
name|struct
name|sockaddr_storage
name|addr
decl_stmt|;
name|socklen_t
name|addrlen
decl_stmt|;
name|char
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|;
comment|/* Get IP address of client. */
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|addr
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|remote
condition|)
block|{
if|if
condition|(
name|getpeername
argument_list|(
name|socket
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
operator|&
name|addrlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"get_socket_ipaddr: getpeername failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
block|}
else|else
block|{
if|if
condition|(
name|getsockname
argument_list|(
name|socket
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
operator|&
name|addrlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"get_socket_ipaddr: getsockname failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
block|}
comment|/* Get the address in ascii. */
if|if
condition|(
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
name|addrlen
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|flags
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"get_socket_ipaddr: getnameinfo %d failed"
argument_list|,
name|flags
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
return|return
name|xstrdup
argument_list|(
name|ntop
argument_list|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_peer_ipaddr
parameter_list|(
name|int
name|socket
parameter_list|)
block|{
return|return
name|get_socket_address
argument_list|(
name|socket
argument_list|,
literal|1
argument_list|,
name|NI_NUMERICHOST
argument_list|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_local_ipaddr
parameter_list|(
name|int
name|socket
parameter_list|)
block|{
return|return
name|get_socket_address
argument_list|(
name|socket
argument_list|,
literal|0
argument_list|,
name|NI_NUMERICHOST
argument_list|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_local_name
parameter_list|(
name|int
name|socket
parameter_list|)
block|{
return|return
name|get_socket_address
argument_list|(
name|socket
argument_list|,
literal|0
argument_list|,
name|NI_NAMEREQD
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * Returns the IP-address of the remote host as a string.  The returned  * string must not be freed.  */
end_comment

begin_function
specifier|const
name|char
modifier|*
name|get_remote_ipaddr
parameter_list|()
block|{
specifier|static
name|char
modifier|*
name|canonical_host_ip
init|=
name|NULL
decl_stmt|;
comment|/* Check whether we have cached the ipaddr. */
if|if
condition|(
name|canonical_host_ip
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|packet_connection_is_on_socket
argument_list|()
condition|)
block|{
name|canonical_host_ip
operator|=
name|get_peer_ipaddr
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|)
expr_stmt|;
if|if
condition|(
name|canonical_host_ip
operator|==
name|NULL
condition|)
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
else|else
block|{
comment|/* If not on socket, return UNKNOWN. */
name|canonical_host_ip
operator|=
name|xstrdup
argument_list|(
literal|"UNKNOWN"
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|canonical_host_ip
return|;
block|}
end_function

begin_function
specifier|const
name|char
modifier|*
name|get_remote_name_or_ip
parameter_list|(
name|u_int
name|utmp_len
parameter_list|,
name|int
name|reverse_mapping_check
parameter_list|)
block|{
specifier|static
specifier|const
name|char
modifier|*
name|remote
init|=
literal|""
decl_stmt|;
if|if
condition|(
name|utmp_len
operator|>
literal|0
condition|)
name|remote
operator|=
name|get_canonical_hostname
argument_list|(
name|reverse_mapping_check
argument_list|)
expr_stmt|;
if|if
condition|(
name|utmp_len
operator|==
literal|0
operator|||
name|strlen
argument_list|(
name|remote
argument_list|)
operator|>
name|utmp_len
condition|)
name|remote
operator|=
name|get_remote_ipaddr
argument_list|()
expr_stmt|;
return|return
name|remote
return|;
block|}
end_function

begin_comment
comment|/* Returns the local/remote port for the socket. */
end_comment

begin_function
name|int
name|get_sock_port
parameter_list|(
name|int
name|sock
parameter_list|,
name|int
name|local
parameter_list|)
block|{
name|struct
name|sockaddr_storage
name|from
decl_stmt|;
name|socklen_t
name|fromlen
decl_stmt|;
name|char
name|strport
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
comment|/* Get IP address of client. */
name|fromlen
operator|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|from
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|from
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|local
condition|)
block|{
if|if
condition|(
name|getsockname
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"getsockname failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
block|}
else|else
block|{
if|if
condition|(
name|getpeername
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
operator|&
name|fromlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"getpeername failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
block|}
comment|/* Return port number. */
if|if
condition|(
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|from
argument_list|,
name|fromlen
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|strport
argument_list|,
sizeof|sizeof
argument_list|(
name|strport
argument_list|)
argument_list|,
name|NI_NUMERICSERV
argument_list|)
operator|!=
literal|0
condition|)
name|fatal
argument_list|(
literal|"get_sock_port: getnameinfo NI_NUMERICSERV failed"
argument_list|)
expr_stmt|;
return|return
name|atoi
argument_list|(
name|strport
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* Returns remote/local port number for the current connection. */
end_comment

begin_function
name|int
name|get_port
parameter_list|(
name|int
name|local
parameter_list|)
block|{
comment|/* 	 * If the connection is not a socket, return 65535.  This is 	 * intentionally chosen to be an unprivileged port number. 	 */
if|if
condition|(
operator|!
name|packet_connection_is_on_socket
argument_list|()
condition|)
return|return
literal|65535
return|;
comment|/* Get socket and return the port number. */
return|return
name|get_sock_port
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|,
name|local
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|get_peer_port
parameter_list|(
name|int
name|sock
parameter_list|)
block|{
return|return
name|get_sock_port
argument_list|(
name|sock
argument_list|,
literal|0
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|get_remote_port
parameter_list|()
block|{
return|return
name|get_port
argument_list|(
literal|0
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|get_local_port
parameter_list|()
block|{
return|return
name|get_port
argument_list|(
literal|1
argument_list|)
return|;
block|}
end_function

end_unit

