begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Encoding and decoding of terminal modes in a portable way.  * Much of the format is defined in ttymodes.h; it is included multiple times  * into this file with the appropriate macro definitions to generate the  * suitable code.  *  * As far as I am concerned, the code I have written for this software  * can be used freely for any purpose.  Any derived versions of this  * software must be clearly marked as such, and if the derived work is  * incompatible with the protocol description in the RFC file, it must be  * called by a name other than "ssh" or "Secure Shell".  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$OpenBSD: ttymodes.c,v 1.8 2000/09/07 20:27:55 deraadt Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"ssh.h"
end_include

begin_define
define|#
directive|define
name|TTY_OP_END
value|0
end_define

begin_define
define|#
directive|define
name|TTY_OP_ISPEED
value|192
end_define

begin_comment
comment|/* int follows */
end_comment

begin_define
define|#
directive|define
name|TTY_OP_OSPEED
value|193
end_define

begin_comment
comment|/* int follows */
end_comment

begin_comment
comment|/*  * Converts POSIX speed_t to a baud rate.  The values of the  * constants for speed_t are not themselves portable.  */
end_comment

begin_function
specifier|static
name|int
name|speed_to_baud
parameter_list|(
name|speed_t
name|speed
parameter_list|)
block|{
switch|switch
condition|(
name|speed
condition|)
block|{
case|case
name|B0
case|:
return|return
literal|0
return|;
case|case
name|B50
case|:
return|return
literal|50
return|;
case|case
name|B75
case|:
return|return
literal|75
return|;
case|case
name|B110
case|:
return|return
literal|110
return|;
case|case
name|B134
case|:
return|return
literal|134
return|;
case|case
name|B150
case|:
return|return
literal|150
return|;
case|case
name|B200
case|:
return|return
literal|200
return|;
case|case
name|B300
case|:
return|return
literal|300
return|;
case|case
name|B600
case|:
return|return
literal|600
return|;
case|case
name|B1200
case|:
return|return
literal|1200
return|;
case|case
name|B1800
case|:
return|return
literal|1800
return|;
case|case
name|B2400
case|:
return|return
literal|2400
return|;
case|case
name|B4800
case|:
return|return
literal|4800
return|;
case|case
name|B9600
case|:
return|return
literal|9600
return|;
ifdef|#
directive|ifdef
name|B19200
case|case
name|B19200
case|:
return|return
literal|19200
return|;
else|#
directive|else
comment|/* B19200 */
ifdef|#
directive|ifdef
name|EXTA
case|case
name|EXTA
case|:
return|return
literal|19200
return|;
endif|#
directive|endif
comment|/* EXTA */
endif|#
directive|endif
comment|/* B19200 */
ifdef|#
directive|ifdef
name|B38400
case|case
name|B38400
case|:
return|return
literal|38400
return|;
else|#
directive|else
comment|/* B38400 */
ifdef|#
directive|ifdef
name|EXTB
case|case
name|EXTB
case|:
return|return
literal|38400
return|;
endif|#
directive|endif
comment|/* EXTB */
endif|#
directive|endif
comment|/* B38400 */
ifdef|#
directive|ifdef
name|B7200
case|case
name|B7200
case|:
return|return
literal|7200
return|;
endif|#
directive|endif
comment|/* B7200 */
ifdef|#
directive|ifdef
name|B14400
case|case
name|B14400
case|:
return|return
literal|14400
return|;
endif|#
directive|endif
comment|/* B14400 */
ifdef|#
directive|ifdef
name|B28800
case|case
name|B28800
case|:
return|return
literal|28800
return|;
endif|#
directive|endif
comment|/* B28800 */
ifdef|#
directive|ifdef
name|B57600
case|case
name|B57600
case|:
return|return
literal|57600
return|;
endif|#
directive|endif
comment|/* B57600 */
ifdef|#
directive|ifdef
name|B76800
case|case
name|B76800
case|:
return|return
literal|76800
return|;
endif|#
directive|endif
comment|/* B76800 */
ifdef|#
directive|ifdef
name|B115200
case|case
name|B115200
case|:
return|return
literal|115200
return|;
endif|#
directive|endif
comment|/* B115200 */
ifdef|#
directive|ifdef
name|B230400
case|case
name|B230400
case|:
return|return
literal|230400
return|;
endif|#
directive|endif
comment|/* B230400 */
default|default:
return|return
literal|9600
return|;
block|}
block|}
end_function

begin_comment
comment|/*  * Converts a numeric baud rate to a POSIX speed_t.  */
end_comment

begin_function
specifier|static
name|speed_t
name|baud_to_speed
parameter_list|(
name|int
name|baud
parameter_list|)
block|{
switch|switch
condition|(
name|baud
condition|)
block|{
case|case
literal|0
case|:
return|return
name|B0
return|;
case|case
literal|50
case|:
return|return
name|B50
return|;
case|case
literal|75
case|:
return|return
name|B75
return|;
case|case
literal|110
case|:
return|return
name|B110
return|;
case|case
literal|134
case|:
return|return
name|B134
return|;
case|case
literal|150
case|:
return|return
name|B150
return|;
case|case
literal|200
case|:
return|return
name|B200
return|;
case|case
literal|300
case|:
return|return
name|B300
return|;
case|case
literal|600
case|:
return|return
name|B600
return|;
case|case
literal|1200
case|:
return|return
name|B1200
return|;
case|case
literal|1800
case|:
return|return
name|B1800
return|;
case|case
literal|2400
case|:
return|return
name|B2400
return|;
case|case
literal|4800
case|:
return|return
name|B4800
return|;
case|case
literal|9600
case|:
return|return
name|B9600
return|;
ifdef|#
directive|ifdef
name|B19200
case|case
literal|19200
case|:
return|return
name|B19200
return|;
else|#
directive|else
comment|/* B19200 */
ifdef|#
directive|ifdef
name|EXTA
case|case
literal|19200
case|:
return|return
name|EXTA
return|;
endif|#
directive|endif
comment|/* EXTA */
endif|#
directive|endif
comment|/* B19200 */
ifdef|#
directive|ifdef
name|B38400
case|case
literal|38400
case|:
return|return
name|B38400
return|;
else|#
directive|else
comment|/* B38400 */
ifdef|#
directive|ifdef
name|EXTB
case|case
literal|38400
case|:
return|return
name|EXTB
return|;
endif|#
directive|endif
comment|/* EXTB */
endif|#
directive|endif
comment|/* B38400 */
ifdef|#
directive|ifdef
name|B7200
case|case
literal|7200
case|:
return|return
name|B7200
return|;
endif|#
directive|endif
comment|/* B7200 */
ifdef|#
directive|ifdef
name|B14400
case|case
literal|14400
case|:
return|return
name|B14400
return|;
endif|#
directive|endif
comment|/* B14400 */
ifdef|#
directive|ifdef
name|B28800
case|case
literal|28800
case|:
return|return
name|B28800
return|;
endif|#
directive|endif
comment|/* B28800 */
ifdef|#
directive|ifdef
name|B57600
case|case
literal|57600
case|:
return|return
name|B57600
return|;
endif|#
directive|endif
comment|/* B57600 */
ifdef|#
directive|ifdef
name|B76800
case|case
literal|76800
case|:
return|return
name|B76800
return|;
endif|#
directive|endif
comment|/* B76800 */
ifdef|#
directive|ifdef
name|B115200
case|case
literal|115200
case|:
return|return
name|B115200
return|;
endif|#
directive|endif
comment|/* B115200 */
ifdef|#
directive|ifdef
name|B230400
case|case
literal|230400
case|:
return|return
name|B230400
return|;
endif|#
directive|endif
comment|/* B230400 */
default|default:
return|return
name|B9600
return|;
block|}
block|}
end_function

begin_comment
comment|/*  * Encodes terminal modes for the terminal referenced by fd  * in a portable manner, and appends the modes to a packet  * being constructed.  */
end_comment

begin_function
name|void
name|tty_make_modes
parameter_list|(
name|int
name|fd
parameter_list|)
block|{
name|struct
name|termios
name|tio
decl_stmt|;
name|int
name|baud
decl_stmt|;
if|if
condition|(
name|tcgetattr
argument_list|(
name|fd
argument_list|,
operator|&
name|tio
argument_list|)
operator|<
literal|0
condition|)
block|{
name|packet_put_char
argument_list|(
name|TTY_OP_END
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"tcgetattr: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* Store input and output baud rates. */
name|baud
operator|=
name|speed_to_baud
argument_list|(
name|cfgetospeed
argument_list|(
operator|&
name|tio
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_char
argument_list|(
name|TTY_OP_OSPEED
argument_list|)
expr_stmt|;
name|packet_put_int
argument_list|(
name|baud
argument_list|)
expr_stmt|;
name|baud
operator|=
name|speed_to_baud
argument_list|(
name|cfgetispeed
argument_list|(
operator|&
name|tio
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_char
argument_list|(
name|TTY_OP_ISPEED
argument_list|)
expr_stmt|;
name|packet_put_int
argument_list|(
name|baud
argument_list|)
expr_stmt|;
comment|/* Store values of mode flags. */
define|#
directive|define
name|TTYCHAR
parameter_list|(
name|NAME
parameter_list|,
name|OP
parameter_list|)
define|\
value|packet_put_char(OP); packet_put_char(tio.c_cc[NAME]);
define|#
directive|define
name|TTYMODE
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
define|\
value|packet_put_char(OP); packet_put_char((tio.FIELD& NAME) != 0);
define|#
directive|define
name|SGTTYCHAR
parameter_list|(
name|NAME
parameter_list|,
name|OP
parameter_list|)
define|#
directive|define
name|SGTTYMODE
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
define|#
directive|define
name|SGTTYMODEN
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
include|#
directive|include
file|"ttymodes.h"
undef|#
directive|undef
name|TTYCHAR
undef|#
directive|undef
name|TTYMODE
undef|#
directive|undef
name|SGTTYCHAR
undef|#
directive|undef
name|SGTTYMODE
undef|#
directive|undef
name|SGTTYMODEN
comment|/* Mark end of mode data. */
name|packet_put_char
argument_list|(
name|TTY_OP_END
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Decodes terminal modes for the terminal referenced by fd in a portable  * manner from a packet being read.  */
end_comment

begin_function
name|void
name|tty_parse_modes
parameter_list|(
name|int
name|fd
parameter_list|,
name|int
modifier|*
name|n_bytes_ptr
parameter_list|)
block|{
name|struct
name|termios
name|tio
decl_stmt|;
name|int
name|opcode
decl_stmt|,
name|baud
decl_stmt|;
name|int
name|n_bytes
init|=
literal|0
decl_stmt|;
name|int
name|failure
init|=
literal|0
decl_stmt|;
comment|/* 	 * Get old attributes for the terminal.  We will modify these 	 * flags. I am hoping that if there are any machine-specific 	 * modes, they will initially have reasonable values. 	 */
if|if
condition|(
name|tcgetattr
argument_list|(
name|fd
argument_list|,
operator|&
name|tio
argument_list|)
operator|<
literal|0
condition|)
name|failure
operator|=
operator|-
literal|1
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|n_bytes
operator|+=
literal|1
expr_stmt|;
name|opcode
operator|=
name|packet_get_char
argument_list|()
expr_stmt|;
switch|switch
condition|(
name|opcode
condition|)
block|{
case|case
name|TTY_OP_END
case|:
goto|goto
name|set
goto|;
case|case
name|TTY_OP_ISPEED
case|:
name|n_bytes
operator|+=
literal|4
expr_stmt|;
name|baud
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
if|if
condition|(
name|failure
operator|!=
operator|-
literal|1
operator|&&
name|cfsetispeed
argument_list|(
operator|&
name|tio
argument_list|,
name|baud_to_speed
argument_list|(
name|baud
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"cfsetispeed failed for %d"
argument_list|,
name|baud
argument_list|)
expr_stmt|;
break|break;
case|case
name|TTY_OP_OSPEED
case|:
name|n_bytes
operator|+=
literal|4
expr_stmt|;
name|baud
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
if|if
condition|(
name|failure
operator|!=
operator|-
literal|1
operator|&&
name|cfsetospeed
argument_list|(
operator|&
name|tio
argument_list|,
name|baud_to_speed
argument_list|(
name|baud
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"cfsetospeed failed for %d"
argument_list|,
name|baud
argument_list|)
expr_stmt|;
break|break;
define|#
directive|define
name|TTYCHAR
parameter_list|(
name|NAME
parameter_list|,
name|OP
parameter_list|)
define|\
value|case OP:					\ 	  n_bytes += 1;					\ 	  tio.c_cc[NAME] = packet_get_char();		\ 	  break;
define|#
directive|define
name|TTYMODE
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
define|\
value|case OP:					\ 	  n_bytes += 1;					\ 	  if (packet_get_char())			\ 	    tio.FIELD |= NAME;				\ 	  else						\ 	    tio.FIELD&= ~NAME;				\ 	  break;
define|#
directive|define
name|SGTTYCHAR
parameter_list|(
name|NAME
parameter_list|,
name|OP
parameter_list|)
define|#
directive|define
name|SGTTYMODE
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
define|#
directive|define
name|SGTTYMODEN
parameter_list|(
name|NAME
parameter_list|,
name|FIELD
parameter_list|,
name|OP
parameter_list|)
include|#
directive|include
file|"ttymodes.h"
undef|#
directive|undef
name|TTYCHAR
undef|#
directive|undef
name|TTYMODE
undef|#
directive|undef
name|SGTTYCHAR
undef|#
directive|undef
name|SGTTYMODE
undef|#
directive|undef
name|SGTTYMODEN
default|default:
name|debug
argument_list|(
literal|"Ignoring unsupported tty mode opcode %d (0x%x)"
argument_list|,
name|opcode
argument_list|,
name|opcode
argument_list|)
expr_stmt|;
comment|/* 			 * Opcodes 0 to 127 are defined to have 			 * a one-byte argument. 			 */
if|if
condition|(
name|opcode
operator|>=
literal|0
operator|&&
name|opcode
operator|<
literal|128
condition|)
block|{
name|n_bytes
operator|+=
literal|1
expr_stmt|;
operator|(
name|void
operator|)
name|packet_get_char
argument_list|()
expr_stmt|;
break|break;
block|}
else|else
block|{
comment|/* 				 * Opcodes 128 to 159 are defined to have 				 * an integer argument. 				 */
if|if
condition|(
name|opcode
operator|>=
literal|128
operator|&&
name|opcode
operator|<
literal|160
condition|)
block|{
name|n_bytes
operator|+=
literal|4
expr_stmt|;
operator|(
name|void
operator|)
name|packet_get_int
argument_list|()
expr_stmt|;
break|break;
block|}
block|}
comment|/* 			 * It is a truly undefined opcode (160 to 255). 			 * We have no idea about its arguments.  So we 			 * must stop parsing.  Note that some data may be 			 * left in the packet; hopefully there is nothing 			 * more coming after the mode data. 			 */
name|log
argument_list|(
literal|"parse_tty_modes: unknown opcode %d"
argument_list|,
name|opcode
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
literal|0
argument_list|,
literal|1
argument_list|,
name|SSH_CMSG_REQUEST_PTY
argument_list|)
expr_stmt|;
goto|goto
name|set
goto|;
block|}
block|}
name|set
label|:
if|if
condition|(
operator|*
name|n_bytes_ptr
operator|!=
name|n_bytes
condition|)
block|{
operator|*
name|n_bytes_ptr
operator|=
name|n_bytes
expr_stmt|;
return|return;
comment|/* Don't process bytes passed */
block|}
if|if
condition|(
name|failure
operator|==
operator|-
literal|1
condition|)
return|return;
comment|/* Packet parsed ok but tty stuff failed */
comment|/* Set the new modes for the terminal. */
if|if
condition|(
name|tcsetattr
argument_list|(
name|fd
argument_list|,
name|TCSANOW
argument_list|,
operator|&
name|tio
argument_list|)
operator|<
literal|0
condition|)
name|log
argument_list|(
literal|"Setting tty modes failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
end_function

end_unit

