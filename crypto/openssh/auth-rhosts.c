begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Rhosts authentication.  This file contains code to check whether to admit  * the login based on rhosts authentication.  This file also processes  * /etc/hosts.equiv.  *  * As far as I am concerned, the code I have written for this software  * can be used freely for any purpose.  Any derived versions of this  * software must be clearly marked as such, and if the derived work is  * incompatible with the protocol description in the RFC file, it must be  * called by a name other than "ssh" or "Secure Shell".  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$OpenBSD: auth-rhosts.c,v 1.23 2001/04/12 19:15:24 markus Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"xmalloc.h"
end_include

begin_include
include|#
directive|include
file|"uidswap.h"
end_include

begin_include
include|#
directive|include
file|"pathnames.h"
end_include

begin_include
include|#
directive|include
file|"log.h"
end_include

begin_include
include|#
directive|include
file|"servconf.h"
end_include

begin_include
include|#
directive|include
file|"canohost.h"
end_include

begin_include
include|#
directive|include
file|"auth.h"
end_include

begin_comment
comment|/* import */
end_comment

begin_decl_stmt
specifier|extern
name|ServerOptions
name|options
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * This function processes an rhosts-style file (.rhosts, .shosts, or  * /etc/hosts.equiv).  This returns true if authentication can be granted  * based on the file, and returns zero otherwise.  */
end_comment

begin_function
name|int
name|check_rhosts_file
parameter_list|(
specifier|const
name|char
modifier|*
name|filename
parameter_list|,
specifier|const
name|char
modifier|*
name|hostname
parameter_list|,
specifier|const
name|char
modifier|*
name|ipaddr
parameter_list|,
specifier|const
name|char
modifier|*
name|client_user
parameter_list|,
specifier|const
name|char
modifier|*
name|server_user
parameter_list|)
block|{
name|FILE
modifier|*
name|f
decl_stmt|;
name|char
name|buf
index|[
literal|1024
index|]
decl_stmt|;
comment|/* Must not be larger than host, user, dummy below. */
comment|/* Open the .rhosts file, deny if unreadable */
name|f
operator|=
name|fopen
argument_list|(
name|filename
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|f
condition|)
return|return
literal|0
return|;
while|while
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|f
argument_list|)
condition|)
block|{
comment|/* All three must be at least as big as buf to avoid overflows. */
name|char
name|hostbuf
index|[
literal|1024
index|]
decl_stmt|,
name|userbuf
index|[
literal|1024
index|]
decl_stmt|,
name|dummy
index|[
literal|1024
index|]
decl_stmt|,
modifier|*
name|host
decl_stmt|,
modifier|*
name|user
decl_stmt|,
modifier|*
name|cp
decl_stmt|;
name|int
name|negated
decl_stmt|;
for|for
control|(
name|cp
operator|=
name|buf
init|;
operator|*
name|cp
operator|==
literal|' '
operator|||
operator|*
name|cp
operator|==
literal|'\t'
condition|;
name|cp
operator|++
control|)
empty_stmt|;
if|if
condition|(
operator|*
name|cp
operator|==
literal|'#'
operator|||
operator|*
name|cp
operator|==
literal|'\n'
operator|||
operator|!
operator|*
name|cp
condition|)
continue|continue;
comment|/* 		 * NO_PLUS is supported at least on OSF/1.  We skip it (we 		 * don't ever support the plus syntax). 		 */
if|if
condition|(
name|strncmp
argument_list|(
name|cp
argument_list|,
literal|"NO_PLUS"
argument_list|,
literal|7
argument_list|)
operator|==
literal|0
condition|)
continue|continue;
comment|/* 		 * This should be safe because each buffer is as big as the 		 * whole string, and thus cannot be overwritten. 		 */
switch|switch
condition|(
name|sscanf
argument_list|(
name|buf
argument_list|,
literal|"%s %s %s"
argument_list|,
name|hostbuf
argument_list|,
name|userbuf
argument_list|,
name|dummy
argument_list|)
condition|)
block|{
case|case
literal|0
case|:
name|packet_send_debug
argument_list|(
literal|"Found empty line in %.100s."
argument_list|,
name|filename
argument_list|)
expr_stmt|;
continue|continue;
case|case
literal|1
case|:
comment|/* Host name only. */
name|strlcpy
argument_list|(
name|userbuf
argument_list|,
name|server_user
argument_list|,
sizeof|sizeof
argument_list|(
name|userbuf
argument_list|)
argument_list|)
expr_stmt|;
break|break;
case|case
literal|2
case|:
comment|/* Got both host and user name. */
break|break;
case|case
literal|3
case|:
name|packet_send_debug
argument_list|(
literal|"Found garbage in %.100s."
argument_list|,
name|filename
argument_list|)
expr_stmt|;
continue|continue;
default|default:
comment|/* Weird... */
continue|continue;
block|}
name|host
operator|=
name|hostbuf
expr_stmt|;
name|user
operator|=
name|userbuf
expr_stmt|;
name|negated
operator|=
literal|0
expr_stmt|;
comment|/* Process negated host names, or positive netgroups. */
if|if
condition|(
name|host
index|[
literal|0
index|]
operator|==
literal|'-'
condition|)
block|{
name|negated
operator|=
literal|1
expr_stmt|;
name|host
operator|++
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|host
index|[
literal|0
index|]
operator|==
literal|'+'
condition|)
name|host
operator|++
expr_stmt|;
if|if
condition|(
name|user
index|[
literal|0
index|]
operator|==
literal|'-'
condition|)
block|{
name|negated
operator|=
literal|1
expr_stmt|;
name|user
operator|++
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|user
index|[
literal|0
index|]
operator|==
literal|'+'
condition|)
name|user
operator|++
expr_stmt|;
comment|/* Check for empty host/user names (particularly '+'). */
if|if
condition|(
operator|!
name|host
index|[
literal|0
index|]
operator|||
operator|!
name|user
index|[
literal|0
index|]
condition|)
block|{
comment|/* We come here if either was '+' or '-'. */
name|packet_send_debug
argument_list|(
literal|"Ignoring wild host/user names in %.100s."
argument_list|,
name|filename
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Verify that host name matches. */
if|if
condition|(
name|host
index|[
literal|0
index|]
operator|==
literal|'@'
condition|)
block|{
if|if
condition|(
operator|!
name|innetgr
argument_list|(
name|host
operator|+
literal|1
argument_list|,
name|hostname
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
operator|&&
operator|!
name|innetgr
argument_list|(
name|host
operator|+
literal|1
argument_list|,
name|ipaddr
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
condition|)
continue|continue;
block|}
elseif|else
if|if
condition|(
name|strcasecmp
argument_list|(
name|host
argument_list|,
name|hostname
argument_list|)
operator|&&
name|strcmp
argument_list|(
name|host
argument_list|,
name|ipaddr
argument_list|)
operator|!=
literal|0
condition|)
continue|continue;
comment|/* Different hostname. */
comment|/* Verify that user name matches. */
if|if
condition|(
name|user
index|[
literal|0
index|]
operator|==
literal|'@'
condition|)
block|{
if|if
condition|(
operator|!
name|innetgr
argument_list|(
name|user
operator|+
literal|1
argument_list|,
name|NULL
argument_list|,
name|client_user
argument_list|,
name|NULL
argument_list|)
condition|)
continue|continue;
block|}
elseif|else
if|if
condition|(
name|strcmp
argument_list|(
name|user
argument_list|,
name|client_user
argument_list|)
operator|!=
literal|0
condition|)
continue|continue;
comment|/* Different username. */
comment|/* Found the user and host. */
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
comment|/* If the entry was negated, deny access. */
if|if
condition|(
name|negated
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Matched negative entry in %.100s."
argument_list|,
name|filename
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Accept authentication. */
return|return
literal|1
return|;
block|}
comment|/* Authentication using this file denied. */
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Tries to authenticate the user using the .shosts or .rhosts file. Returns  * true if authentication succeeds.  If ignore_rhosts is true, only  * /etc/hosts.equiv will be considered (.rhosts and .shosts are ignored).  */
end_comment

begin_function
name|int
name|auth_rhosts
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|client_user
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|hostname
decl_stmt|,
modifier|*
name|ipaddr
decl_stmt|;
name|int
name|ret
decl_stmt|;
name|hostname
operator|=
name|get_canonical_hostname
argument_list|(
name|options
operator|.
name|reverse_mapping_check
argument_list|)
expr_stmt|;
name|ipaddr
operator|=
name|get_remote_ipaddr
argument_list|()
expr_stmt|;
name|ret
operator|=
name|auth_rhosts2
argument_list|(
name|pw
argument_list|,
name|client_user
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_function
name|int
name|auth_rhosts2
parameter_list|(
name|struct
name|passwd
modifier|*
name|pw
parameter_list|,
specifier|const
name|char
modifier|*
name|client_user
parameter_list|,
specifier|const
name|char
modifier|*
name|hostname
parameter_list|,
specifier|const
name|char
modifier|*
name|ipaddr
parameter_list|)
block|{
name|char
name|buf
index|[
literal|1024
index|]
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
specifier|static
specifier|const
name|char
modifier|*
name|rhosts_files
index|[]
init|=
block|{
literal|".shosts"
block|,
literal|".rhosts"
block|,
name|NULL
block|}
decl_stmt|;
name|u_int
name|rhosts_file_index
decl_stmt|;
name|debug2
argument_list|(
literal|"auth_rhosts2: clientuser %s hostname %s ipaddr %s"
argument_list|,
name|client_user
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|)
expr_stmt|;
comment|/* no user given */
if|if
condition|(
name|pw
operator|==
name|NULL
condition|)
return|return
literal|0
return|;
comment|/* Switch to the user's uid. */
name|temporarily_use_uid
argument_list|(
name|pw
argument_list|)
expr_stmt|;
comment|/* 	 * Quick check: if the user has no .shosts or .rhosts files, return 	 * failure immediately without doing costly lookups from name 	 * servers. 	 */
for|for
control|(
name|rhosts_file_index
operator|=
literal|0
init|;
name|rhosts_files
index|[
name|rhosts_file_index
index|]
condition|;
name|rhosts_file_index
operator|++
control|)
block|{
comment|/* Check users .rhosts or .shosts. */
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"%.500s/%.100s"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|,
name|rhosts_files
index|[
name|rhosts_file_index
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|buf
argument_list|,
operator|&
name|st
argument_list|)
operator|>=
literal|0
condition|)
break|break;
block|}
comment|/* Switch back to privileged uid. */
name|restore_uid
argument_list|()
expr_stmt|;
comment|/* Deny if The user has no .shosts or .rhosts file and there are no system-wide files. */
if|if
condition|(
operator|!
name|rhosts_files
index|[
name|rhosts_file_index
index|]
operator|&&
name|stat
argument_list|(
name|_PATH_RHOSTS_EQUIV
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
operator|&&
name|stat
argument_list|(
name|_PATH_SSH_HOSTS_EQUIV
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
return|return
literal|0
return|;
comment|/* If not logging in as superuser, try /etc/hosts.equiv and shosts.equiv. */
if|if
condition|(
name|pw
operator|->
name|pw_uid
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|check_rhosts_file
argument_list|(
name|_PATH_RHOSTS_EQUIV
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|,
name|client_user
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Accepted for %.100s [%.100s] by /etc/hosts.equiv."
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
if|if
condition|(
name|check_rhosts_file
argument_list|(
name|_PATH_SSH_HOSTS_EQUIV
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|,
name|client_user
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Accepted for %.100s [%.100s] by %.100s."
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|,
name|_PATH_SSH_HOSTS_EQUIV
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
block|}
comment|/* 	 * Check that the home directory is owned by root or the user, and is 	 * not group or world writable. 	 */
if|if
condition|(
name|stat
argument_list|(
name|pw
operator|->
name|pw_dir
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
block|{
name|log
argument_list|(
literal|"Rhosts authentication refused for %.100s: no home directory %.200s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
name|packet_send_debug
argument_list|(
literal|"Rhosts authentication refused for %.100s: no home directory %.200s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
name|options
operator|.
name|strict_modes
operator|&&
operator|(
operator|(
name|st
operator|.
name|st_uid
operator|!=
literal|0
operator|&&
name|st
operator|.
name|st_uid
operator|!=
name|pw
operator|->
name|pw_uid
operator|)
operator|||
operator|(
name|st
operator|.
name|st_mode
operator|&
literal|022
operator|)
operator|!=
literal|0
operator|)
condition|)
block|{
name|log
argument_list|(
literal|"Rhosts authentication refused for %.100s: bad ownership or modes for home directory."
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|packet_send_debug
argument_list|(
literal|"Rhosts authentication refused for %.100s: bad ownership or modes for home directory."
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Temporarily use the user's uid. */
name|temporarily_use_uid
argument_list|(
name|pw
argument_list|)
expr_stmt|;
comment|/* Check all .rhosts files (currently .shosts and .rhosts). */
for|for
control|(
name|rhosts_file_index
operator|=
literal|0
init|;
name|rhosts_files
index|[
name|rhosts_file_index
index|]
condition|;
name|rhosts_file_index
operator|++
control|)
block|{
comment|/* Check users .rhosts or .shosts. */
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"%.500s/%.100s"
argument_list|,
name|pw
operator|->
name|pw_dir
argument_list|,
name|rhosts_files
index|[
name|rhosts_file_index
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|buf
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
continue|continue;
comment|/* 		 * Make sure that the file is either owned by the user or by 		 * root, and make sure it is not writable by anyone but the 		 * owner.  This is to help avoid novices accidentally 		 * allowing access to their account by anyone. 		 */
if|if
condition|(
name|options
operator|.
name|strict_modes
operator|&&
operator|(
operator|(
name|st
operator|.
name|st_uid
operator|!=
literal|0
operator|&&
name|st
operator|.
name|st_uid
operator|!=
name|pw
operator|->
name|pw_uid
operator|)
operator|||
operator|(
name|st
operator|.
name|st_mode
operator|&
literal|022
operator|)
operator|!=
literal|0
operator|)
condition|)
block|{
name|log
argument_list|(
literal|"Rhosts authentication refused for %.100s: bad modes for %.200s"
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|,
name|buf
argument_list|)
expr_stmt|;
name|packet_send_debug
argument_list|(
literal|"Bad file modes for %.200s"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Check if we have been configured to ignore .rhosts and .shosts files. */
if|if
condition|(
name|options
operator|.
name|ignore_rhosts
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Server has been configured to ignore %.100s."
argument_list|,
name|rhosts_files
index|[
name|rhosts_file_index
index|]
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Check if authentication is permitted by the file. */
if|if
condition|(
name|check_rhosts_file
argument_list|(
name|buf
argument_list|,
name|hostname
argument_list|,
name|ipaddr
argument_list|,
name|client_user
argument_list|,
name|pw
operator|->
name|pw_name
argument_list|)
condition|)
block|{
name|packet_send_debug
argument_list|(
literal|"Accepted by %.100s."
argument_list|,
name|rhosts_files
index|[
name|rhosts_file_index
index|]
argument_list|)
expr_stmt|;
comment|/* Restore the privileged uid. */
name|restore_uid
argument_list|()
expr_stmt|;
return|return
literal|1
return|;
block|}
block|}
comment|/* Restore the privileged uid. */
name|restore_uid
argument_list|()
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

end_unit

