begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Author: Tatu Ylonen<ylo@cs.hut.fi>  * Copyright (c) 1995 Tatu Ylonen<ylo@cs.hut.fi>, Espoo, Finland  *                    All rights reserved  * Created: Sat Mar 18 22:15:47 1995 ylo  * Code to connect to a remote host, and to perform the client side of the  * login (authentication) dialog.  *   * $FreeBSD$  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$OpenBSD: sshconnect.c,v 1.56 2000/02/18 08:50:33 markus Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|<openssl/bn.h>
end_include

begin_include
include|#
directive|include
file|"xmalloc.h"
end_include

begin_include
include|#
directive|include
file|"rsa.h"
end_include

begin_include
include|#
directive|include
file|"ssh.h"
end_include

begin_include
include|#
directive|include
file|"packet.h"
end_include

begin_include
include|#
directive|include
file|"authfd.h"
end_include

begin_include
include|#
directive|include
file|"cipher.h"
end_include

begin_include
include|#
directive|include
file|"mpaux.h"
end_include

begin_include
include|#
directive|include
file|"uidswap.h"
end_include

begin_include
include|#
directive|include
file|"compat.h"
end_include

begin_include
include|#
directive|include
file|"readconf.h"
end_include

begin_include
include|#
directive|include
file|"fingerprint.h"
end_include

begin_include
include|#
directive|include
file|<openssl/md5.h>
end_include

begin_comment
comment|/* Session id for the current session. */
end_comment

begin_decl_stmt
name|unsigned
name|char
name|session_id
index|[
literal|16
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* authentications supported by server */
end_comment

begin_decl_stmt
name|unsigned
name|int
name|supported_authentications
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|Options
name|options
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|char
modifier|*
name|__progname
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Connect to the given ssh server using a proxy command.  */
end_comment

begin_function
name|int
name|ssh_proxy_connect
parameter_list|(
specifier|const
name|char
modifier|*
name|host
parameter_list|,
name|u_short
name|port
parameter_list|,
name|uid_t
name|original_real_uid
parameter_list|,
specifier|const
name|char
modifier|*
name|proxy_command
parameter_list|)
block|{
name|Buffer
name|command
decl_stmt|;
specifier|const
name|char
modifier|*
name|cp
decl_stmt|;
name|char
modifier|*
name|command_string
decl_stmt|;
name|int
name|pin
index|[
literal|2
index|]
decl_stmt|,
name|pout
index|[
literal|2
index|]
decl_stmt|;
name|int
name|pid
decl_stmt|;
name|char
name|strport
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
comment|/* Convert the port number into a string. */
name|snprintf
argument_list|(
name|strport
argument_list|,
sizeof|sizeof
name|strport
argument_list|,
literal|"%hu"
argument_list|,
name|port
argument_list|)
expr_stmt|;
comment|/* Build the final command string in the buffer by making the 	   appropriate substitutions to the given proxy command. */
name|buffer_init
argument_list|(
operator|&
name|command
argument_list|)
expr_stmt|;
for|for
control|(
name|cp
operator|=
name|proxy_command
init|;
operator|*
name|cp
condition|;
name|cp
operator|++
control|)
block|{
if|if
condition|(
name|cp
index|[
literal|0
index|]
operator|==
literal|'%'
operator|&&
name|cp
index|[
literal|1
index|]
operator|==
literal|'%'
condition|)
block|{
name|buffer_append
argument_list|(
operator|&
name|command
argument_list|,
literal|"%"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|cp
operator|++
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|cp
index|[
literal|0
index|]
operator|==
literal|'%'
operator|&&
name|cp
index|[
literal|1
index|]
operator|==
literal|'h'
condition|)
block|{
name|buffer_append
argument_list|(
operator|&
name|command
argument_list|,
name|host
argument_list|,
name|strlen
argument_list|(
name|host
argument_list|)
argument_list|)
expr_stmt|;
name|cp
operator|++
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|cp
index|[
literal|0
index|]
operator|==
literal|'%'
operator|&&
name|cp
index|[
literal|1
index|]
operator|==
literal|'p'
condition|)
block|{
name|buffer_append
argument_list|(
operator|&
name|command
argument_list|,
name|strport
argument_list|,
name|strlen
argument_list|(
name|strport
argument_list|)
argument_list|)
expr_stmt|;
name|cp
operator|++
expr_stmt|;
continue|continue;
block|}
name|buffer_append
argument_list|(
operator|&
name|command
argument_list|,
name|cp
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
name|buffer_append
argument_list|(
operator|&
name|command
argument_list|,
literal|"\0"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* Get the final command string. */
name|command_string
operator|=
name|buffer_ptr
argument_list|(
operator|&
name|command
argument_list|)
expr_stmt|;
comment|/* Create pipes for communicating with the proxy. */
if|if
condition|(
name|pipe
argument_list|(
name|pin
argument_list|)
operator|<
literal|0
operator|||
name|pipe
argument_list|(
name|pout
argument_list|)
operator|<
literal|0
condition|)
name|fatal
argument_list|(
literal|"Could not create pipes to communicate with the proxy: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Executing proxy command: %.500s"
argument_list|,
name|command_string
argument_list|)
expr_stmt|;
comment|/* Fork and execute the proxy command. */
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|==
literal|0
condition|)
block|{
name|char
modifier|*
name|argv
index|[
literal|10
index|]
decl_stmt|;
comment|/* Child.  Permanently give up superuser privileges. */
name|permanently_set_uid
argument_list|(
name|original_real_uid
argument_list|)
expr_stmt|;
comment|/* Redirect stdin and stdout. */
name|close
argument_list|(
name|pin
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|pin
index|[
literal|0
index|]
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|dup2
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"dup2 stdin"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
block|}
name|close
argument_list|(
name|pout
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|dup2
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"dup2 stdout"
argument_list|)
expr_stmt|;
comment|/* Cannot be 1 because pin allocated two descriptors. */
name|close
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* Stderr is left as it is so that error messages get 		   printed on the user's terminal. */
name|argv
index|[
literal|0
index|]
operator|=
literal|"/bin/sh"
expr_stmt|;
name|argv
index|[
literal|1
index|]
operator|=
literal|"-c"
expr_stmt|;
name|argv
index|[
literal|2
index|]
operator|=
name|command_string
expr_stmt|;
name|argv
index|[
literal|3
index|]
operator|=
name|NULL
expr_stmt|;
comment|/* Execute the proxy command.  Note that we gave up any 		   extra privileges above. */
name|execv
argument_list|(
literal|"/bin/sh"
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|perror
argument_list|(
literal|"/bin/sh"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
comment|/* Parent. */
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
name|fatal
argument_list|(
literal|"fork failed: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Close child side of the descriptors. */
name|close
argument_list|(
name|pin
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|pout
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* Free the command name. */
name|buffer_free
argument_list|(
operator|&
name|command
argument_list|)
expr_stmt|;
comment|/* Set the connection file descriptors. */
name|packet_set_connection
argument_list|(
name|pout
index|[
literal|0
index|]
argument_list|,
name|pin
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|/*  * Creates a (possibly privileged) socket for use as the ssh connection.  */
end_comment

begin_function
name|int
name|ssh_create_socket
parameter_list|(
name|uid_t
name|original_real_uid
parameter_list|,
name|int
name|privileged
parameter_list|,
name|int
name|family
parameter_list|)
block|{
name|int
name|sock
decl_stmt|;
comment|/* 	 * If we are running as root and want to connect to a privileged 	 * port, bind our own socket to a privileged port. 	 */
if|if
condition|(
name|privileged
condition|)
block|{
name|int
name|p
init|=
name|IPPORT_RESERVED
operator|-
literal|1
decl_stmt|;
name|sock
operator|=
name|rresvport_af
argument_list|(
operator|&
name|p
argument_list|,
name|family
argument_list|)
expr_stmt|;
if|if
condition|(
name|sock
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"rresvport: af=%d %.100s"
argument_list|,
name|family
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
else|else
name|debug
argument_list|(
literal|"Allocated local port %d."
argument_list|,
name|p
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* 		 * Just create an ordinary socket on arbitrary port.  We use 		 * the user's uid to create the socket. 		 */
name|temporarily_use_uid
argument_list|(
name|original_real_uid
argument_list|)
expr_stmt|;
name|sock
operator|=
name|socket
argument_list|(
name|family
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|sock
operator|<
literal|0
condition|)
name|error
argument_list|(
literal|"socket: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|restore_uid
argument_list|()
expr_stmt|;
block|}
return|return
name|sock
return|;
block|}
end_function

begin_comment
comment|/*  * Opens a TCP/IP connection to the remote server on the given host.  * The address of the remote host will be returned in hostaddr.  * If port is 0, the default port will be used.  If anonymous is zero,  * a privileged port will be allocated to make the connection.  * This requires super-user privileges if anonymous is false.  * Connection_attempts specifies the maximum number of tries (one per  * second).  If proxy_command is non-NULL, it specifies the command (with %h  * and %p substituted for host and port, respectively) to use to contact  * the daemon.  */
end_comment

begin_function
name|int
name|ssh_connect
parameter_list|(
specifier|const
name|char
modifier|*
name|host
parameter_list|,
name|struct
name|sockaddr_storage
modifier|*
name|hostaddr
parameter_list|,
name|u_short
name|port
parameter_list|,
name|int
name|connection_attempts
parameter_list|,
name|int
name|anonymous
parameter_list|,
name|uid_t
name|original_real_uid
parameter_list|,
specifier|const
name|char
modifier|*
name|proxy_command
parameter_list|)
block|{
name|int
name|sock
init|=
operator|-
literal|1
decl_stmt|,
name|attempt
decl_stmt|;
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|struct
name|addrinfo
name|hints
decl_stmt|,
modifier|*
name|ai
decl_stmt|,
modifier|*
name|aitop
decl_stmt|;
name|char
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|,
name|strport
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
name|int
name|gaierr
decl_stmt|;
name|struct
name|linger
name|linger
decl_stmt|;
name|debug
argument_list|(
literal|"ssh_connect: getuid %d geteuid %d anon %d"
argument_list|,
operator|(
name|int
operator|)
name|getuid
argument_list|()
argument_list|,
operator|(
name|int
operator|)
name|geteuid
argument_list|()
argument_list|,
name|anonymous
argument_list|)
expr_stmt|;
comment|/* Get default port if port has not been set. */
if|if
condition|(
name|port
operator|==
literal|0
condition|)
block|{
name|sp
operator|=
name|getservbyname
argument_list|(
name|SSH_SERVICE_NAME
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
condition|)
name|port
operator|=
name|ntohs
argument_list|(
name|sp
operator|->
name|s_port
argument_list|)
expr_stmt|;
else|else
name|port
operator|=
name|SSH_DEFAULT_PORT
expr_stmt|;
block|}
comment|/* If a proxy command is given, connect using it. */
if|if
condition|(
name|proxy_command
operator|!=
name|NULL
condition|)
return|return
name|ssh_proxy_connect
argument_list|(
name|host
argument_list|,
name|port
argument_list|,
name|original_real_uid
argument_list|,
name|proxy_command
argument_list|)
return|;
comment|/* No proxy command. */
name|memset
argument_list|(
operator|&
name|hints
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|hints
argument_list|)
argument_list|)
expr_stmt|;
name|hints
operator|.
name|ai_family
operator|=
name|IPv4or6
expr_stmt|;
name|hints
operator|.
name|ai_socktype
operator|=
name|SOCK_STREAM
expr_stmt|;
name|snprintf
argument_list|(
name|strport
argument_list|,
sizeof|sizeof
name|strport
argument_list|,
literal|"%d"
argument_list|,
name|port
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|gaierr
operator|=
name|getaddrinfo
argument_list|(
name|host
argument_list|,
name|strport
argument_list|,
operator|&
name|hints
argument_list|,
operator|&
name|aitop
argument_list|)
operator|)
operator|!=
literal|0
condition|)
name|fatal
argument_list|(
literal|"%s: %.100s: %s"
argument_list|,
name|__progname
argument_list|,
name|host
argument_list|,
name|gai_strerror
argument_list|(
name|gaierr
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * Try to connect several times.  On some machines, the first time 	 * will sometimes fail.  In general socket code appears to behave 	 * quite magically on many machines. 	 */
for|for
control|(
name|attempt
operator|=
literal|0
init|;
name|attempt
operator|<
name|connection_attempts
condition|;
name|attempt
operator|++
control|)
block|{
if|if
condition|(
name|attempt
operator|>
literal|0
condition|)
name|debug
argument_list|(
literal|"Trying again..."
argument_list|)
expr_stmt|;
comment|/* Loop through addresses for this host, and try each one in  		   sequence until the connection succeeds. */
for|for
control|(
name|ai
operator|=
name|aitop
init|;
name|ai
condition|;
name|ai
operator|=
name|ai
operator|->
name|ai_next
control|)
block|{
if|if
condition|(
name|ai
operator|->
name|ai_family
operator|!=
name|AF_INET
operator|&&
name|ai
operator|->
name|ai_family
operator|!=
name|AF_INET6
condition|)
continue|continue;
if|if
condition|(
name|getnameinfo
argument_list|(
name|ai
operator|->
name|ai_addr
argument_list|,
name|ai
operator|->
name|ai_addrlen
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|strport
argument_list|,
sizeof|sizeof
argument_list|(
name|strport
argument_list|)
argument_list|,
name|NI_NUMERICHOST
operator||
name|NI_NUMERICSERV
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|error
argument_list|(
literal|"ssh_connect: getnameinfo failed"
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|debug
argument_list|(
literal|"Connecting to %.200s [%.100s] port %s."
argument_list|,
name|host
argument_list|,
name|ntop
argument_list|,
name|strport
argument_list|)
expr_stmt|;
comment|/* Create a socket for connecting. */
name|sock
operator|=
name|ssh_create_socket
argument_list|(
name|original_real_uid
argument_list|,
operator|!
name|anonymous
operator|&&
name|geteuid
argument_list|()
operator|==
literal|0
operator|&&
name|port
operator|<
name|IPPORT_RESERVED
argument_list|,
name|ai
operator|->
name|ai_family
argument_list|)
expr_stmt|;
if|if
condition|(
name|sock
operator|<
literal|0
condition|)
continue|continue;
comment|/* Connect to the host.  We use the user's uid in the 			 * hope that it will help with tcp_wrappers showing 			 * the remote uid as root. 			 */
name|temporarily_use_uid
argument_list|(
name|original_real_uid
argument_list|)
expr_stmt|;
if|if
condition|(
name|connect
argument_list|(
name|sock
argument_list|,
name|ai
operator|->
name|ai_addr
argument_list|,
name|ai
operator|->
name|ai_addrlen
argument_list|)
operator|>=
literal|0
condition|)
block|{
comment|/* Successful connection. */
name|memcpy
argument_list|(
name|hostaddr
argument_list|,
name|ai
operator|->
name|ai_addr
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|hostaddr
argument_list|)
argument_list|)
expr_stmt|;
name|restore_uid
argument_list|()
expr_stmt|;
break|break;
block|}
else|else
block|{
name|debug
argument_list|(
literal|"connect: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|restore_uid
argument_list|()
expr_stmt|;
comment|/* 				 * Close the failed socket; there appear to 				 * be some problems when reusing a socket for 				 * which connect() has already returned an 				 * error. 				 */
name|shutdown
argument_list|(
name|sock
argument_list|,
name|SHUT_RDWR
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|sock
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|ai
condition|)
break|break;
comment|/* Successful connection. */
comment|/* Sleep a moment before retrying. */
name|sleep
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|freeaddrinfo
argument_list|(
name|aitop
argument_list|)
expr_stmt|;
comment|/* Return failure if we didn't get a successful connection. */
if|if
condition|(
name|attempt
operator|>=
name|connection_attempts
condition|)
return|return
literal|0
return|;
name|debug
argument_list|(
literal|"Connection established."
argument_list|)
expr_stmt|;
comment|/* 	 * Set socket options.  We would like the socket to disappear as soon 	 * as it has been closed for whatever reason. 	 */
comment|/* setsockopt(sock, SOL_SOCKET, SO_REUSEADDR, (void *)&on, sizeof(on)); */
name|linger
operator|.
name|l_onoff
operator|=
literal|1
expr_stmt|;
name|linger
operator|.
name|l_linger
operator|=
literal|5
expr_stmt|;
name|setsockopt
argument_list|(
name|sock
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_LINGER
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|linger
argument_list|,
sizeof|sizeof
argument_list|(
name|linger
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Set the connection. */
name|packet_set_connection
argument_list|(
name|sock
argument_list|,
name|sock
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|/*  * Checks if the user has an authentication agent, and if so, tries to  * authenticate using the agent.  */
end_comment

begin_function
name|int
name|try_agent_authentication
parameter_list|()
block|{
name|int
name|status
decl_stmt|,
name|type
decl_stmt|;
name|char
modifier|*
name|comment
decl_stmt|;
name|AuthenticationConnection
modifier|*
name|auth
decl_stmt|;
name|unsigned
name|char
name|response
index|[
literal|16
index|]
decl_stmt|;
name|unsigned
name|int
name|i
decl_stmt|;
name|BIGNUM
modifier|*
name|e
decl_stmt|,
modifier|*
name|n
decl_stmt|,
modifier|*
name|challenge
decl_stmt|;
comment|/* Get connection to the agent. */
name|auth
operator|=
name|ssh_get_authentication_connection
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
name|auth
condition|)
return|return
literal|0
return|;
name|e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|challenge
operator|=
name|BN_new
argument_list|()
expr_stmt|;
comment|/* Loop through identities served by the agent. */
for|for
control|(
name|status
operator|=
name|ssh_get_first_identity
argument_list|(
name|auth
argument_list|,
name|e
argument_list|,
name|n
argument_list|,
operator|&
name|comment
argument_list|)
init|;
name|status
condition|;
name|status
operator|=
name|ssh_get_next_identity
argument_list|(
name|auth
argument_list|,
name|e
argument_list|,
name|n
argument_list|,
operator|&
name|comment
argument_list|)
control|)
block|{
name|int
name|plen
decl_stmt|,
name|clen
decl_stmt|;
comment|/* Try this identity. */
name|debug
argument_list|(
literal|"Trying RSA authentication via agent with '%.100s'"
argument_list|,
name|comment
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|comment
argument_list|)
expr_stmt|;
comment|/* Tell the server that we are willing to authenticate using this key. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RSA
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|n
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Wait for server's response. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* The server sends failure if it doesn\'t like our key or 		   does not support RSA authentication. */
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_FAILURE
condition|)
block|{
name|debug
argument_list|(
literal|"Server refused our key."
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* Otherwise it should have sent a challenge. */
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_AUTH_RSA_CHALLENGE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error during RSA authentication: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|challenge
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
name|clen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Received RSA challenge from server."
argument_list|)
expr_stmt|;
comment|/* Ask the agent to decrypt the challenge. */
if|if
condition|(
operator|!
name|ssh_decrypt_challenge
argument_list|(
name|auth
argument_list|,
name|e
argument_list|,
name|n
argument_list|,
name|challenge
argument_list|,
name|session_id
argument_list|,
literal|1
argument_list|,
name|response
argument_list|)
condition|)
block|{
comment|/* The agent failed to authenticate this identifier although it 			   advertised it supports this.  Just return a wrong value. */
name|log
argument_list|(
literal|"Authentication agent failed to decrypt challenge."
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|response
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|response
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|debug
argument_list|(
literal|"Sending response to RSA challenge."
argument_list|)
expr_stmt|;
comment|/* Send the decrypted challenge back to the server. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RSA_RESPONSE
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|16
condition|;
name|i
operator|++
control|)
name|packet_put_char
argument_list|(
name|response
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Wait for response from the server. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* The server returns success if it accepted the authentication. */
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"RSA authentication accepted by server."
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|e
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|n
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
comment|/* Otherwise it should return failure. */
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error waiting RSA auth response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
name|BN_clear_free
argument_list|(
name|e
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|n
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"RSA authentication using agent refused."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Computes the proper response to a RSA challenge, and sends the response to  * the server.  */
end_comment

begin_function
name|void
name|respond_to_rsa_challenge
parameter_list|(
name|BIGNUM
modifier|*
name|challenge
parameter_list|,
name|RSA
modifier|*
name|prv
parameter_list|)
block|{
name|unsigned
name|char
name|buf
index|[
literal|32
index|]
decl_stmt|,
name|response
index|[
literal|16
index|]
decl_stmt|;
name|MD5_CTX
name|md
decl_stmt|;
name|int
name|i
decl_stmt|,
name|len
decl_stmt|;
comment|/* Decrypt the challenge using the private key. */
name|rsa_private_decrypt
argument_list|(
name|challenge
argument_list|,
name|challenge
argument_list|,
name|prv
argument_list|)
expr_stmt|;
comment|/* Compute the response. */
comment|/* The response is MD5 of decrypted challenge plus session id. */
name|len
operator|=
name|BN_num_bytes
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|<=
literal|0
operator|||
name|len
operator|>
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
condition|)
name|packet_disconnect
argument_list|(
literal|"respond_to_rsa_challenge: bad challenge length %d"
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|buf
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|BN_bn2bin
argument_list|(
name|challenge
argument_list|,
name|buf
operator|+
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
name|len
argument_list|)
expr_stmt|;
name|MD5_Init
argument_list|(
operator|&
name|md
argument_list|)
expr_stmt|;
name|MD5_Update
argument_list|(
operator|&
name|md
argument_list|,
name|buf
argument_list|,
literal|32
argument_list|)
expr_stmt|;
name|MD5_Update
argument_list|(
operator|&
name|md
argument_list|,
name|session_id
argument_list|,
literal|16
argument_list|)
expr_stmt|;
name|MD5_Final
argument_list|(
name|response
argument_list|,
operator|&
name|md
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Sending response to host key RSA challenge."
argument_list|)
expr_stmt|;
comment|/* Send the response back to the server. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RSA_RESPONSE
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|16
condition|;
name|i
operator|++
control|)
name|packet_put_char
argument_list|(
name|response
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|memset
argument_list|(
name|buf
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|response
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|response
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|md
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|md
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Checks if the user has authentication file, and if so, tries to authenticate  * the user using it.  */
end_comment

begin_function
name|int
name|try_rsa_authentication
parameter_list|(
specifier|const
name|char
modifier|*
name|authfile
parameter_list|)
block|{
name|BIGNUM
modifier|*
name|challenge
decl_stmt|;
name|RSA
modifier|*
name|private_key
decl_stmt|;
name|RSA
modifier|*
name|public_key
decl_stmt|;
name|char
modifier|*
name|passphrase
decl_stmt|,
modifier|*
name|comment
decl_stmt|;
name|int
name|type
decl_stmt|,
name|i
decl_stmt|;
name|int
name|plen
decl_stmt|,
name|clen
decl_stmt|;
comment|/* Try to load identification for the authentication key. */
name|public_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
if|if
condition|(
operator|!
name|load_public_key
argument_list|(
name|authfile
argument_list|,
name|public_key
argument_list|,
operator|&
name|comment
argument_list|)
condition|)
block|{
name|RSA_free
argument_list|(
name|public_key
argument_list|)
expr_stmt|;
comment|/* Could not load it.  Fail. */
return|return
literal|0
return|;
block|}
name|debug
argument_list|(
literal|"Trying RSA authentication with key '%.100s'"
argument_list|,
name|comment
argument_list|)
expr_stmt|;
comment|/* Tell the server that we are willing to authenticate using this key. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RSA
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* We no longer need the public key. */
name|RSA_free
argument_list|(
name|public_key
argument_list|)
expr_stmt|;
comment|/* Wait for server's response. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* 	 * The server responds with failure if it doesn\'t like our key or 	 * doesn\'t support RSA authentication. 	 */
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_FAILURE
condition|)
block|{
name|debug
argument_list|(
literal|"Server refused our key."
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|comment
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Otherwise, the server should respond with a challenge. */
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_AUTH_RSA_CHALLENGE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error during RSA authentication: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Get the challenge from the packet. */
name|challenge
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|challenge
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
name|clen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Received RSA challenge from server."
argument_list|)
expr_stmt|;
name|private_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
comment|/* 	 * Load the private key.  Try first with empty passphrase; if it 	 * fails, ask for a passphrase. 	 */
if|if
condition|(
operator|!
name|load_private_key
argument_list|(
name|authfile
argument_list|,
literal|""
argument_list|,
name|private_key
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
name|char
name|buf
index|[
literal|300
index|]
decl_stmt|;
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"Enter passphrase for RSA key '%.100s': "
argument_list|,
name|comment
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|options
operator|.
name|batch_mode
condition|)
name|passphrase
operator|=
name|read_passphrase
argument_list|(
name|buf
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|else
block|{
name|debug
argument_list|(
literal|"Will not query passphrase for %.100s in batch mode."
argument_list|,
name|comment
argument_list|)
expr_stmt|;
name|passphrase
operator|=
name|xstrdup
argument_list|(
literal|""
argument_list|)
expr_stmt|;
block|}
comment|/* Load the authentication file using the pasphrase. */
if|if
condition|(
operator|!
name|load_private_key
argument_list|(
name|authfile
argument_list|,
name|passphrase
argument_list|,
name|private_key
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
name|memset
argument_list|(
name|passphrase
argument_list|,
literal|0
argument_list|,
name|strlen
argument_list|(
name|passphrase
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|passphrase
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"Bad passphrase."
argument_list|)
expr_stmt|;
comment|/* Send a dummy response packet to avoid protocol error. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RSA_RESPONSE
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|16
condition|;
name|i
operator|++
control|)
name|packet_put_char
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Expect the server to reject it... */
name|packet_read_expect
argument_list|(
operator|&
name|plen
argument_list|,
name|SSH_SMSG_FAILURE
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|comment
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Destroy the passphrase. */
name|memset
argument_list|(
name|passphrase
argument_list|,
literal|0
argument_list|,
name|strlen
argument_list|(
name|passphrase
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|passphrase
argument_list|)
expr_stmt|;
block|}
comment|/* We no longer need the comment. */
name|xfree
argument_list|(
name|comment
argument_list|)
expr_stmt|;
comment|/* Compute and send a response to the challenge. */
name|respond_to_rsa_challenge
argument_list|(
name|challenge
argument_list|,
name|private_key
argument_list|)
expr_stmt|;
comment|/* Destroy the private key. */
name|RSA_free
argument_list|(
name|private_key
argument_list|)
expr_stmt|;
comment|/* We no longer need the challenge. */
name|BN_clear_free
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
comment|/* Wait for response from the server. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"RSA authentication accepted by server."
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error waiting RSA auth response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"RSA authentication refused."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Tries to authenticate the user using combined rhosts or /etc/hosts.equiv  * authentication and RSA host authentication.  */
end_comment

begin_function
name|int
name|try_rhosts_rsa_authentication
parameter_list|(
specifier|const
name|char
modifier|*
name|local_user
parameter_list|,
name|RSA
modifier|*
name|host_key
parameter_list|)
block|{
name|int
name|type
decl_stmt|;
name|BIGNUM
modifier|*
name|challenge
decl_stmt|;
name|int
name|plen
decl_stmt|,
name|clen
decl_stmt|;
name|debug
argument_list|(
literal|"Trying rhosts or /etc/hosts.equiv with RSA host authentication."
argument_list|)
expr_stmt|;
comment|/* Tell the server that we are willing to authenticate using this key. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RHOSTS_RSA
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|local_user
argument_list|,
name|strlen
argument_list|(
name|local_user
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_int
argument_list|(
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|host_key
operator|->
name|e
argument_list|)
expr_stmt|;
name|packet_put_bignum
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Wait for server's response. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
comment|/* The server responds with failure if it doesn't admit our 	   .rhosts authentication or doesn't know our host key. */
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_FAILURE
condition|)
block|{
name|debug
argument_list|(
literal|"Server refused our rhosts authentication or host key."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Otherwise, the server should respond with a challenge. */
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_AUTH_RSA_CHALLENGE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error during RSA authentication: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* Get the challenge from the packet. */
name|challenge
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|challenge
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
name|clen
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Received RSA challenge for host key from server."
argument_list|)
expr_stmt|;
comment|/* Compute a response to the challenge. */
name|respond_to_rsa_challenge
argument_list|(
name|challenge
argument_list|,
name|host_key
argument_list|)
expr_stmt|;
comment|/* We no longer need the challenge. */
name|BN_clear_free
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
comment|/* Wait for response from the server. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"Rhosts or /etc/hosts.equiv with RSA host authentication accepted by server."
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error waiting RSA auth response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Rhosts or /etc/hosts.equiv with RSA host authentication refused."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|KRB4
end_ifdef

begin_function
name|int
name|try_krb4_authentication
parameter_list|()
block|{
name|KTEXT_ST
name|auth
decl_stmt|;
comment|/* Kerberos data */
name|char
modifier|*
name|reply
decl_stmt|;
name|char
name|inst
index|[
name|INST_SZ
index|]
decl_stmt|;
name|char
modifier|*
name|realm
decl_stmt|;
name|CREDENTIALS
name|cred
decl_stmt|;
name|int
name|r
decl_stmt|,
name|type
decl_stmt|,
name|plen
decl_stmt|;
name|Key_schedule
name|schedule
decl_stmt|;
name|u_long
name|checksum
decl_stmt|,
name|cksum
decl_stmt|;
name|MSG_DAT
name|msg_data
decl_stmt|;
name|struct
name|sockaddr_in
name|local
decl_stmt|,
name|foreign
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
comment|/* Don't do anything if we don't have any tickets. */
if|if
condition|(
name|stat
argument_list|(
name|tkt_string
argument_list|()
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
return|return
literal|0
return|;
name|strncpy
argument_list|(
name|inst
argument_list|,
operator|(
name|char
operator|*
operator|)
name|krb_get_phost
argument_list|(
name|get_canonical_hostname
argument_list|()
argument_list|)
argument_list|,
name|INST_SZ
argument_list|)
expr_stmt|;
name|realm
operator|=
operator|(
name|char
operator|*
operator|)
name|krb_realmofhost
argument_list|(
name|get_canonical_hostname
argument_list|()
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|realm
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4: no realm for %s"
argument_list|,
name|get_canonical_hostname
argument_list|()
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* This can really be anything. */
name|checksum
operator|=
operator|(
name|u_long
operator|)
name|getpid
argument_list|()
expr_stmt|;
name|r
operator|=
name|krb_mk_req
argument_list|(
operator|&
name|auth
argument_list|,
name|KRB4_SERVICE_NAME
argument_list|,
name|inst
argument_list|,
name|realm
argument_list|,
name|checksum
argument_list|)
expr_stmt|;
if|if
condition|(
name|r
operator|!=
name|KSUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 krb_mk_req failed: %s"
argument_list|,
name|krb_err_txt
index|[
name|r
index|]
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* Get session key to decrypt the server's reply with. */
name|r
operator|=
name|krb_get_cred
argument_list|(
name|KRB4_SERVICE_NAME
argument_list|,
name|inst
argument_list|,
name|realm
argument_list|,
operator|&
name|cred
argument_list|)
expr_stmt|;
if|if
condition|(
name|r
operator|!=
name|KSUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"get_cred failed: %s"
argument_list|,
name|krb_err_txt
index|[
name|r
index|]
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|des_key_sched
argument_list|(
operator|(
name|des_cblock
operator|*
operator|)
name|cred
operator|.
name|session
argument_list|,
name|schedule
argument_list|)
expr_stmt|;
comment|/* Send authentication info to server. */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_KRB4
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
operator|(
name|char
operator|*
operator|)
name|auth
operator|.
name|dat
argument_list|,
name|auth
operator|.
name|length
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Zero the buffer. */
operator|(
name|void
operator|)
name|memset
argument_list|(
name|auth
operator|.
name|dat
argument_list|,
literal|0
argument_list|,
name|MAX_KTXT_LEN
argument_list|)
expr_stmt|;
name|r
operator|=
sizeof|sizeof
argument_list|(
name|local
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|local
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|local
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|getsockname
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|local
argument_list|,
operator|&
name|r
argument_list|)
operator|<
literal|0
condition|)
name|debug
argument_list|(
literal|"getsockname failed: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|r
operator|=
sizeof|sizeof
argument_list|(
name|foreign
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|foreign
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|foreign
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|getpeername
argument_list|(
name|packet_get_connection_in
argument_list|()
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|foreign
argument_list|,
operator|&
name|r
argument_list|)
operator|<
literal|0
condition|)
block|{
name|debug
argument_list|(
literal|"getpeername failed: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|fatal_cleanup
argument_list|()
expr_stmt|;
block|}
comment|/* Get server reply. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|SSH_SMSG_FAILURE
case|:
comment|/* Should really be SSH_SMSG_AUTH_KRB4_FAILURE */
name|debug
argument_list|(
literal|"Kerberos V4 authentication failed."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
break|break;
case|case
name|SSH_SMSG_AUTH_KRB4_RESPONSE
case|:
comment|/* SSH_SMSG_AUTH_KRB4_SUCCESS */
name|debug
argument_list|(
literal|"Kerberos V4 authentication accepted."
argument_list|)
expr_stmt|;
comment|/* Get server's response. */
name|reply
operator|=
name|packet_get_string
argument_list|(
operator|(
name|unsigned
name|int
operator|*
operator|)
operator|&
name|auth
operator|.
name|length
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|auth
operator|.
name|dat
argument_list|,
name|reply
argument_list|,
name|auth
operator|.
name|length
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|reply
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|plen
argument_list|,
literal|4
operator|+
name|auth
operator|.
name|length
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* 		 * If his response isn't properly encrypted with the session 		 * key, and the decrypted checksum fails to match, he's 		 * bogus. Bail out. 		 */
name|r
operator|=
name|krb_rd_priv
argument_list|(
name|auth
operator|.
name|dat
argument_list|,
name|auth
operator|.
name|length
argument_list|,
name|schedule
argument_list|,
operator|&
name|cred
operator|.
name|session
argument_list|,
operator|&
name|foreign
argument_list|,
operator|&
name|local
argument_list|,
operator|&
name|msg_data
argument_list|)
expr_stmt|;
if|if
condition|(
name|r
operator|!=
name|KSUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 krb_rd_priv failed: %s"
argument_list|,
name|krb_err_txt
index|[
name|r
index|]
argument_list|)
expr_stmt|;
name|packet_disconnect
argument_list|(
literal|"Kerberos V4 challenge failed!"
argument_list|)
expr_stmt|;
block|}
comment|/* Fetch the (incremented) checksum that we supplied in the request. */
operator|(
name|void
operator|)
name|memcpy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|cksum
argument_list|,
operator|(
name|char
operator|*
operator|)
name|msg_data
operator|.
name|app_data
argument_list|,
sizeof|sizeof
argument_list|(
name|cksum
argument_list|)
argument_list|)
expr_stmt|;
name|cksum
operator|=
name|ntohl
argument_list|(
name|cksum
argument_list|)
expr_stmt|;
comment|/* If it matches, we're golden. */
if|if
condition|(
name|cksum
operator|==
name|checksum
operator|+
literal|1
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 challenge successful."
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
else|else
name|packet_disconnect
argument_list|(
literal|"Kerberos V4 challenge failed!"
argument_list|)
expr_stmt|;
break|break;
default|default:
name|packet_disconnect
argument_list|(
literal|"Protocol error on Kerberos V4 response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* KRB4 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|AFS
end_ifdef

begin_function
name|int
name|send_krb4_tgt
parameter_list|()
block|{
name|CREDENTIALS
modifier|*
name|creds
decl_stmt|;
name|char
name|pname
index|[
name|ANAME_SZ
index|]
decl_stmt|,
name|pinst
index|[
name|INST_SZ
index|]
decl_stmt|,
name|prealm
index|[
name|REALM_SZ
index|]
decl_stmt|;
name|int
name|r
decl_stmt|,
name|type
decl_stmt|,
name|plen
decl_stmt|;
name|unsigned
name|char
name|buffer
index|[
literal|8192
index|]
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
comment|/* Don't do anything if we don't have any tickets. */
if|if
condition|(
name|stat
argument_list|(
name|tkt_string
argument_list|()
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
return|return
literal|0
return|;
name|creds
operator|=
name|xmalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|creds
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|r
operator|=
name|krb_get_tf_fullname
argument_list|(
name|TKT_FILE
argument_list|,
name|pname
argument_list|,
name|pinst
argument_list|,
name|prealm
argument_list|)
operator|)
operator|!=
name|KSUCCESS
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 tf_fullname failed: %s"
argument_list|,
name|krb_err_txt
index|[
name|r
index|]
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
operator|(
name|r
operator|=
name|krb_get_cred
argument_list|(
literal|"krbtgt"
argument_list|,
name|prealm
argument_list|,
name|prealm
argument_list|,
name|creds
argument_list|)
operator|)
operator|!=
name|GC_OK
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 get_cred failed: %s"
argument_list|,
name|krb_err_txt
index|[
name|r
index|]
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
name|time
argument_list|(
literal|0
argument_list|)
operator|>
name|krb_life_to_time
argument_list|(
name|creds
operator|->
name|issue_date
argument_list|,
name|creds
operator|->
name|lifetime
argument_list|)
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V4 ticket expired: %s"
argument_list|,
name|TKT_FILE
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|creds_to_radix
argument_list|(
name|creds
argument_list|,
name|buffer
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|creds
argument_list|)
expr_stmt|;
name|packet_start
argument_list|(
name|SSH_CMSG_HAVE_KRB4_TGT
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
operator|(
name|char
operator|*
operator|)
name|buffer
argument_list|,
name|strlen
argument_list|(
name|buffer
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_FAILURE
condition|)
name|debug
argument_list|(
literal|"Kerberos TGT for realm %s rejected."
argument_list|,
name|prealm
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_SUCCESS
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error on Kerberos TGT response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_function
name|void
name|send_afs_tokens
parameter_list|(
name|void
parameter_list|)
block|{
name|CREDENTIALS
name|creds
decl_stmt|;
name|struct
name|ViceIoctl
name|parms
decl_stmt|;
name|struct
name|ClearToken
name|ct
decl_stmt|;
name|int
name|i
decl_stmt|,
name|type
decl_stmt|,
name|len
decl_stmt|,
name|plen
decl_stmt|;
name|char
name|buf
index|[
literal|2048
index|]
decl_stmt|,
modifier|*
name|p
decl_stmt|,
modifier|*
name|server_cell
decl_stmt|;
name|unsigned
name|char
name|buffer
index|[
literal|8192
index|]
decl_stmt|;
comment|/* Move over ktc_GetToken, here's something leaner. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|100
condition|;
name|i
operator|++
control|)
block|{
comment|/* just in case */
name|parms
operator|.
name|in
operator|=
operator|(
name|char
operator|*
operator|)
operator|&
name|i
expr_stmt|;
name|parms
operator|.
name|in_size
operator|=
sizeof|sizeof
argument_list|(
name|i
argument_list|)
expr_stmt|;
name|parms
operator|.
name|out
operator|=
name|buf
expr_stmt|;
name|parms
operator|.
name|out_size
operator|=
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|k_pioctl
argument_list|(
literal|0
argument_list|,
name|VIOCGETTOK
argument_list|,
operator|&
name|parms
argument_list|,
literal|0
argument_list|)
operator|!=
literal|0
condition|)
break|break;
name|p
operator|=
name|buf
expr_stmt|;
comment|/* Get secret token. */
name|memcpy
argument_list|(
operator|&
name|creds
operator|.
name|ticket_st
operator|.
name|length
argument_list|,
name|p
argument_list|,
expr|sizeof
operator|(
name|unsigned
name|int
operator|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|creds
operator|.
name|ticket_st
operator|.
name|length
operator|>
name|MAX_KTXT_LEN
condition|)
break|break;
name|p
operator|+=
expr|sizeof
operator|(
name|unsigned
name|int
operator|)
expr_stmt|;
name|memcpy
argument_list|(
name|creds
operator|.
name|ticket_st
operator|.
name|dat
argument_list|,
name|p
argument_list|,
name|creds
operator|.
name|ticket_st
operator|.
name|length
argument_list|)
expr_stmt|;
name|p
operator|+=
name|creds
operator|.
name|ticket_st
operator|.
name|length
expr_stmt|;
comment|/* Get clear token. */
name|memcpy
argument_list|(
operator|&
name|len
argument_list|,
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|len
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|!=
sizeof|sizeof
argument_list|(
expr|struct
name|ClearToken
argument_list|)
condition|)
break|break;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|len
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|ct
argument_list|,
name|p
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|p
operator|+=
name|len
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|len
argument_list|)
expr_stmt|;
comment|/* primary flag */
name|server_cell
operator|=
name|p
expr_stmt|;
comment|/* Flesh out our credentials. */
name|strlcpy
argument_list|(
name|creds
operator|.
name|service
argument_list|,
literal|"afs"
argument_list|,
sizeof|sizeof
name|creds
operator|.
name|service
argument_list|)
expr_stmt|;
name|creds
operator|.
name|instance
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
name|strlcpy
argument_list|(
name|creds
operator|.
name|realm
argument_list|,
name|server_cell
argument_list|,
name|REALM_SZ
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|creds
operator|.
name|session
argument_list|,
name|ct
operator|.
name|HandShakeKey
argument_list|,
name|DES_KEY_SZ
argument_list|)
expr_stmt|;
name|creds
operator|.
name|issue_date
operator|=
name|ct
operator|.
name|BeginTimestamp
expr_stmt|;
name|creds
operator|.
name|lifetime
operator|=
name|krb_time_to_life
argument_list|(
name|creds
operator|.
name|issue_date
argument_list|,
name|ct
operator|.
name|EndTimestamp
argument_list|)
expr_stmt|;
name|creds
operator|.
name|kvno
operator|=
name|ct
operator|.
name|AuthHandle
expr_stmt|;
name|snprintf
argument_list|(
name|creds
operator|.
name|pname
argument_list|,
sizeof|sizeof
argument_list|(
name|creds
operator|.
name|pname
argument_list|)
argument_list|,
literal|"AFS ID %d"
argument_list|,
name|ct
operator|.
name|ViceId
argument_list|)
expr_stmt|;
name|creds
operator|.
name|pinst
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/* Encode token, ship it off. */
if|if
condition|(
operator|!
name|creds_to_radix
argument_list|(
operator|&
name|creds
argument_list|,
name|buffer
argument_list|)
condition|)
break|break;
name|packet_start
argument_list|(
name|SSH_CMSG_HAVE_AFS_TOKEN
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
operator|(
name|char
operator|*
operator|)
name|buffer
argument_list|,
name|strlen
argument_list|(
name|buffer
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* Roger, Roger. Clearance, Clarence. What's your vector, 		   Victor? */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|plen
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_FAILURE
condition|)
name|debug
argument_list|(
literal|"AFS token for cell %s rejected."
argument_list|,
name|server_cell
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_SUCCESS
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error on AFS token response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* AFS */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|KRB5
end_ifdef

begin_function
name|int
name|try_krb5_authentication
parameter_list|(
name|krb5_context
modifier|*
name|context
parameter_list|,
name|krb5_auth_context
modifier|*
name|auth_context
parameter_list|)
block|{
name|krb5_error_code
name|problem
decl_stmt|;
specifier|const
name|char
modifier|*
name|tkfile
decl_stmt|;
name|struct
name|stat
name|buf
decl_stmt|;
name|krb5_ccache
name|ccache
init|=
name|NULL
decl_stmt|;
name|krb5_creds
name|req_creds
decl_stmt|;
name|krb5_creds
modifier|*
name|new_creds
init|=
name|NULL
decl_stmt|;
specifier|const
name|char
modifier|*
name|remotehost
decl_stmt|;
name|krb5_data
name|ap
decl_stmt|;
name|int
name|type
decl_stmt|,
name|payload_len
decl_stmt|;
name|krb5_ap_rep_enc_part
modifier|*
name|reply
init|=
name|NULL
decl_stmt|;
name|int
name|ret
decl_stmt|;
name|memset
argument_list|(
operator|&
name|ap
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|ap
argument_list|)
argument_list|)
expr_stmt|;
name|problem
operator|=
name|krb5_init_context
argument_list|(
name|context
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|tkfile
operator|=
name|krb5_cc_default_name
argument_list|(
operator|*
name|context
argument_list|)
expr_stmt|;
if|if
condition|(
name|strncmp
argument_list|(
name|tkfile
argument_list|,
literal|"FILE:"
argument_list|,
literal|5
argument_list|)
operator|==
literal|0
condition|)
name|tkfile
operator|+=
literal|5
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|tkfile
argument_list|,
operator|&
name|buf
argument_list|)
operator|==
literal|0
operator|&&
name|getuid
argument_list|()
operator|!=
name|buf
operator|.
name|st_uid
condition|)
block|{
name|debug
argument_list|(
literal|"Kerberos V5: could not get default ccache (permission denied)."
argument_list|)
expr_stmt|;
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|problem
operator|=
name|krb5_cc_default
argument_list|(
operator|*
name|context
argument_list|,
operator|&
name|ccache
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|memset
argument_list|(
operator|&
name|req_creds
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|req_creds
argument_list|)
argument_list|)
expr_stmt|;
name|remotehost
operator|=
name|get_canonical_hostname
argument_list|()
expr_stmt|;
name|problem
operator|=
name|krb5_sname_to_principal
argument_list|(
operator|*
name|context
argument_list|,
name|remotehost
argument_list|,
literal|"host"
argument_list|,
name|KRB5_NT_SRV_HST
argument_list|,
operator|&
name|req_creds
operator|.
name|server
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|problem
operator|=
name|krb5_cc_get_principal
argument_list|(
operator|*
name|context
argument_list|,
name|ccache
argument_list|,
operator|&
name|req_creds
operator|.
name|client
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/* creds.session.keytype=ETYPE_DES_CBC_CRC; */
name|problem
operator|=
name|krb5_get_credentials
argument_list|(
operator|*
name|context
argument_list|,
literal|0
argument_list|,
name|ccache
argument_list|,
operator|&
name|req_creds
argument_list|,
operator|&
name|new_creds
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|problem
operator|=
name|krb5_auth_con_init
argument_list|(
operator|*
name|context
argument_list|,
name|auth_context
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/* krb5_auth_con_setflags(ssh_context, auth_context,                                   KRB5_AUTH_CONTEXT_RET_TIME);  	*/
name|problem
operator|=
name|krb5_mk_req_extended
argument_list|(
operator|*
name|context
argument_list|,
name|auth_context
argument_list|,
name|AP_OPTS_MUTUAL_REQUIRED
comment|/*| AP_OPTS_USE_SUBKEY*/
argument_list|,
name|NULL
argument_list|,
name|new_creds
argument_list|,
operator|&
name|ap
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_KRB5
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
operator|(
name|char
operator|*
operator|)
name|ap
operator|.
name|data
argument_list|,
name|ap
operator|.
name|length
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|xfree
argument_list|(
name|ap
operator|.
name|data
argument_list|)
expr_stmt|;
name|ap
operator|.
name|length
operator|=
literal|0
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|SSH_SMSG_FAILURE
case|:
comment|/* Should really be SSH_SMSG_AUTH_KRB5_FAILURE */
name|debug
argument_list|(
literal|"Kerberos V5 authentication failed."
argument_list|)
expr_stmt|;
name|ret
operator|=
literal|0
expr_stmt|;
break|break;
case|case
name|SSH_SMSG_AUTH_KRB5_RESPONSE
case|:
comment|/* SSH_SMSG_AUTH_KRB5_SUCCESS */
name|debug
argument_list|(
literal|"Kerberos V5 authentication accepted."
argument_list|)
expr_stmt|;
comment|/* Get server's response. */
name|ap
operator|.
name|data
operator|=
name|packet_get_string
argument_list|(
operator|(
name|unsigned
name|int
operator|*
operator|)
operator|&
name|ap
operator|.
name|length
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|payload_len
argument_list|,
literal|4
operator|+
name|ap
operator|.
name|length
argument_list|,
name|type
argument_list|)
expr_stmt|;
comment|/* XXX je to dobre? */
name|problem
operator|=
name|krb5_rd_rep
argument_list|(
operator|*
name|context
argument_list|,
operator|*
name|auth_context
argument_list|,
operator|&
name|ap
argument_list|,
operator|&
name|reply
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
name|ret
operator|=
literal|0
expr_stmt|;
block|}
name|ret
operator|=
literal|1
expr_stmt|;
break|break;
default|default:
name|packet_disconnect
argument_list|(
literal|"Protocol error on Kerberos V5 response: %d"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|ret
operator|=
literal|0
expr_stmt|;
break|break;
block|}
name|out
label|:
if|if
condition|(
name|req_creds
operator|.
name|server
operator|!=
name|NULL
condition|)
name|krb5_free_principal
argument_list|(
operator|*
name|context
argument_list|,
name|req_creds
operator|.
name|server
argument_list|)
expr_stmt|;
if|if
condition|(
name|req_creds
operator|.
name|client
operator|!=
name|NULL
condition|)
name|krb5_free_principal
argument_list|(
operator|*
name|context
argument_list|,
name|req_creds
operator|.
name|client
argument_list|)
expr_stmt|;
if|if
condition|(
name|new_creds
operator|!=
name|NULL
condition|)
name|krb5_free_creds
argument_list|(
operator|*
name|context
argument_list|,
name|new_creds
argument_list|)
expr_stmt|;
if|if
condition|(
name|ccache
operator|!=
name|NULL
condition|)
name|krb5_cc_close
argument_list|(
operator|*
name|context
argument_list|,
name|ccache
argument_list|)
expr_stmt|;
if|if
condition|(
name|reply
operator|!=
name|NULL
condition|)
name|krb5_free_ap_rep_enc_part
argument_list|(
operator|*
name|context
argument_list|,
name|reply
argument_list|)
expr_stmt|;
if|if
condition|(
name|ap
operator|.
name|length
operator|>
literal|0
condition|)
name|krb5_data_free
argument_list|(
operator|&
name|ap
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_function
name|void
name|send_krb5_tgt
parameter_list|(
name|krb5_context
name|context
parameter_list|,
name|krb5_auth_context
name|auth_context
parameter_list|)
block|{
name|int
name|fd
decl_stmt|;
name|int
name|type
decl_stmt|,
name|payload_len
decl_stmt|;
name|krb5_error_code
name|problem
decl_stmt|;
name|krb5_data
name|outbuf
decl_stmt|;
name|krb5_ccache
name|ccache
init|=
name|NULL
decl_stmt|;
name|krb5_creds
name|creds
decl_stmt|;
name|krb5_kdc_flags
name|flags
decl_stmt|;
specifier|const
name|char
modifier|*
name|remotehost
init|=
name|get_canonical_hostname
argument_list|()
decl_stmt|;
name|memset
argument_list|(
operator|&
name|creds
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|creds
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|outbuf
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|outbuf
argument_list|)
argument_list|)
expr_stmt|;
name|fd
operator|=
name|packet_get_connection_in
argument_list|()
expr_stmt|;
name|problem
operator|=
name|krb5_auth_con_setaddrs_from_fd
argument_list|(
name|context
argument_list|,
name|auth_context
argument_list|,
operator|&
name|fd
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
goto|goto
name|out
goto|;
block|}
if|#
directive|if
literal|0
block|tkfile = krb5_cc_default_name(context);   if (strncmp(tkfile, "FILE:", 5) == 0)      tkfile += 5;      if (stat(tkfile,&buf) == 0&& getuid() != buf.st_uid) {      debug("Kerberos V5: could not get default ccache (permission denied).");      goto out;   }
endif|#
directive|endif
name|problem
operator|=
name|krb5_cc_default
argument_list|(
name|context
argument_list|,
operator|&
name|ccache
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
goto|goto
name|out
goto|;
block|}
name|problem
operator|=
name|krb5_cc_get_principal
argument_list|(
name|context
argument_list|,
name|ccache
argument_list|,
operator|&
name|creds
operator|.
name|client
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
goto|goto
name|out
goto|;
block|}
name|problem
operator|=
name|krb5_build_principal
argument_list|(
name|context
argument_list|,
operator|&
name|creds
operator|.
name|server
argument_list|,
name|strlen
argument_list|(
name|creds
operator|.
name|client
operator|->
name|realm
argument_list|)
argument_list|,
name|creds
operator|.
name|client
operator|->
name|realm
argument_list|,
literal|"krbtgt"
argument_list|,
name|creds
operator|.
name|client
operator|->
name|realm
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
goto|goto
name|out
goto|;
block|}
name|creds
operator|.
name|times
operator|.
name|endtime
operator|=
literal|0
expr_stmt|;
name|flags
operator|.
name|i
operator|=
literal|0
expr_stmt|;
name|flags
operator|.
name|b
operator|.
name|forwarded
operator|=
literal|1
expr_stmt|;
name|flags
operator|.
name|b
operator|.
name|forwardable
operator|=
name|krb5_config_get_bool
argument_list|(
name|context
argument_list|,
name|NULL
argument_list|,
literal|"libdefaults"
argument_list|,
literal|"forwardable"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|problem
operator|=
name|krb5_get_forwarded_creds
argument_list|(
name|context
argument_list|,
name|auth_context
argument_list|,
name|ccache
argument_list|,
name|flags
operator|.
name|i
argument_list|,
name|remotehost
argument_list|,
operator|&
name|creds
argument_list|,
operator|&
name|outbuf
argument_list|)
expr_stmt|;
if|if
condition|(
name|problem
condition|)
block|{
goto|goto
name|out
goto|;
block|}
name|packet_start
argument_list|(
name|SSH_CMSG_HAVE_KRB5_TGT
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
operator|(
name|char
operator|*
operator|)
name|outbuf
operator|.
name|data
argument_list|,
name|outbuf
operator|.
name|length
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|SSH_SMSG_SUCCESS
case|:
break|break;
case|case
name|SSH_SMSG_FAILURE
case|:
break|break;
default|default:
break|break;
block|}
name|out
label|:
if|if
condition|(
name|creds
operator|.
name|client
condition|)
name|krb5_free_principal
argument_list|(
name|context
argument_list|,
name|creds
operator|.
name|client
argument_list|)
expr_stmt|;
if|if
condition|(
name|creds
operator|.
name|server
condition|)
name|krb5_free_principal
argument_list|(
name|context
argument_list|,
name|creds
operator|.
name|server
argument_list|)
expr_stmt|;
if|if
condition|(
name|ccache
condition|)
name|krb5_cc_close
argument_list|(
name|context
argument_list|,
name|ccache
argument_list|)
expr_stmt|;
if|if
condition|(
name|outbuf
operator|.
name|data
condition|)
name|xfree
argument_list|(
name|outbuf
operator|.
name|data
argument_list|)
expr_stmt|;
return|return;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* KRB5 */
end_comment

begin_comment
comment|/*  * Tries to authenticate with any string-based challenge/response system.  * Note that the client code is not tied to s/key or TIS.  */
end_comment

begin_function
name|int
name|try_skey_authentication
parameter_list|()
block|{
name|int
name|type
decl_stmt|,
name|i
decl_stmt|,
name|payload_len
decl_stmt|;
name|char
modifier|*
name|challenge
decl_stmt|,
modifier|*
name|response
decl_stmt|;
name|debug
argument_list|(
literal|"Doing skey authentication."
argument_list|)
expr_stmt|;
comment|/* request a challenge */
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_TIS
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
operator|&&
name|type
operator|!=
name|SSH_SMSG_AUTH_TIS_CHALLENGE
condition|)
block|{
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response "
literal|"to skey-auth"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_AUTH_TIS_CHALLENGE
condition|)
block|{
name|debug
argument_list|(
literal|"No challenge for skey authentication."
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|challenge
operator|=
name|packet_get_string
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NONE
condition|)
name|log
argument_list|(
literal|"WARNING: Encryption is disabled! "
literal|"Reponse will be transmitted in clear text."
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s\n"
argument_list|,
name|challenge
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|challenge
argument_list|)
expr_stmt|;
name|fflush
argument_list|(
name|stderr
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|number_of_password_prompts
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|i
operator|!=
literal|0
condition|)
name|error
argument_list|(
literal|"Permission denied, please try again."
argument_list|)
expr_stmt|;
name|response
operator|=
name|read_passphrase
argument_list|(
literal|"Response: "
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_TIS_RESPONSE
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|response
argument_list|,
name|strlen
argument_list|(
name|response
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|response
argument_list|,
literal|0
argument_list|,
name|strlen
argument_list|(
name|response
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|response
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
return|return
literal|1
return|;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response "
literal|"to skey-auth-reponse"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
comment|/* failure */
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Tries to authenticate with plain passwd authentication.  */
end_comment

begin_function
name|int
name|try_password_authentication
parameter_list|(
name|char
modifier|*
name|prompt
parameter_list|)
block|{
name|int
name|type
decl_stmt|,
name|i
decl_stmt|,
name|payload_len
decl_stmt|;
name|char
modifier|*
name|password
decl_stmt|;
name|debug
argument_list|(
literal|"Doing password authentication."
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NONE
condition|)
name|log
argument_list|(
literal|"WARNING: Encryption is disabled! Password will be transmitted in clear text."
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|number_of_password_prompts
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|i
operator|!=
literal|0
condition|)
name|error
argument_list|(
literal|"Permission denied, please try again."
argument_list|)
expr_stmt|;
name|password
operator|=
name|read_passphrase
argument_list|(
name|prompt
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_PASSWORD
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|password
argument_list|,
name|strlen
argument_list|(
name|password
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
name|password
argument_list|,
literal|0
argument_list|,
name|strlen
argument_list|(
name|password
argument_list|)
argument_list|)
expr_stmt|;
name|xfree
argument_list|(
name|password
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
return|return
literal|1
return|;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response to passwd auth"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
comment|/* failure */
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Waits for the server identification string, and sends our own  * identification string.  */
end_comment

begin_function
name|void
name|ssh_exchange_identification
parameter_list|()
block|{
name|char
name|buf
index|[
literal|256
index|]
decl_stmt|,
name|remote_version
index|[
literal|256
index|]
decl_stmt|;
comment|/* must be same size! */
name|int
name|remote_major
decl_stmt|,
name|remote_minor
decl_stmt|,
name|i
decl_stmt|;
name|int
name|connection_in
init|=
name|packet_get_connection_in
argument_list|()
decl_stmt|;
name|int
name|connection_out
init|=
name|packet_get_connection_out
argument_list|()
decl_stmt|;
comment|/* Read other side\'s version identification. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
condition|;
name|i
operator|++
control|)
block|{
name|int
name|len
init|=
name|read
argument_list|(
name|connection_in
argument_list|,
operator|&
name|buf
index|[
name|i
index|]
argument_list|,
literal|1
argument_list|)
decl_stmt|;
if|if
condition|(
name|len
operator|<
literal|0
condition|)
name|fatal
argument_list|(
literal|"ssh_exchange_identification: read: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|!=
literal|1
condition|)
name|fatal
argument_list|(
literal|"ssh_exchange_identification: Connection closed by remote host"
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
index|[
name|i
index|]
operator|==
literal|'\r'
condition|)
block|{
name|buf
index|[
name|i
index|]
operator|=
literal|'\n'
expr_stmt|;
name|buf
index|[
name|i
operator|+
literal|1
index|]
operator|=
literal|0
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|buf
index|[
name|i
index|]
operator|==
literal|'\n'
condition|)
block|{
name|buf
index|[
name|i
operator|+
literal|1
index|]
operator|=
literal|0
expr_stmt|;
break|break;
block|}
block|}
name|buf
index|[
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|0
expr_stmt|;
comment|/* 	 * Check that the versions match.  In future this might accept 	 * several versions and set appropriate flags to handle them. 	 */
if|if
condition|(
name|sscanf
argument_list|(
name|buf
argument_list|,
literal|"SSH-%d.%d-%[^\n]\n"
argument_list|,
operator|&
name|remote_major
argument_list|,
operator|&
name|remote_minor
argument_list|,
name|remote_version
argument_list|)
operator|!=
literal|3
condition|)
name|fatal
argument_list|(
literal|"Bad remote protocol version identification: '%.100s'"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Remote protocol version %d.%d, remote software version %.100s"
argument_list|,
name|remote_major
argument_list|,
name|remote_minor
argument_list|,
name|remote_version
argument_list|)
expr_stmt|;
comment|/* Check if the remote protocol version is too old. */
if|if
condition|(
name|remote_major
operator|==
literal|1
operator|&&
name|remote_minor
operator|<
literal|3
condition|)
name|fatal
argument_list|(
literal|"Remote machine has too old SSH software version."
argument_list|)
expr_stmt|;
comment|/* We speak 1.3, too. */
if|if
condition|(
name|remote_major
operator|==
literal|1
operator|&&
name|remote_minor
operator|==
literal|3
condition|)
block|{
name|enable_compat13
argument_list|()
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|forward_agent
condition|)
block|{
name|log
argument_list|(
literal|"Agent forwarding disabled for protocol 1.3"
argument_list|)
expr_stmt|;
name|options
operator|.
name|forward_agent
operator|=
literal|0
expr_stmt|;
block|}
block|}
if|#
directive|if
literal|0
comment|/* 	 * Removed for now, to permit compatibility with latter versions. The 	 * server will reject our version and disconnect if it doesn't 	 * support it. 	 */
block|if (remote_major != PROTOCOL_MAJOR) 		fatal("Protocol major versions differ: %d vs. %d", 		      PROTOCOL_MAJOR, remote_major);
endif|#
directive|endif
comment|/* Send our own protocol version identification. */
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
literal|"SSH-%d.%d-%.100s\n"
argument_list|,
name|PROTOCOL_MAJOR
argument_list|,
name|PROTOCOL_MINOR
argument_list|,
name|SSH_VERSION
argument_list|)
expr_stmt|;
if|if
condition|(
name|atomicio
argument_list|(
name|write
argument_list|,
name|connection_out
argument_list|,
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
operator|!=
name|strlen
argument_list|(
name|buf
argument_list|)
condition|)
name|fatal
argument_list|(
literal|"write: %.100s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_decl_stmt
name|int
name|ssh_cipher_default
init|=
name|SSH_CIPHER_3DES
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|read_yes_or_no
parameter_list|(
specifier|const
name|char
modifier|*
name|prompt
parameter_list|,
name|int
name|defval
parameter_list|)
block|{
name|char
name|buf
index|[
literal|1024
index|]
decl_stmt|;
name|FILE
modifier|*
name|f
decl_stmt|;
name|int
name|retval
init|=
operator|-
literal|1
decl_stmt|;
if|if
condition|(
name|isatty
argument_list|(
literal|0
argument_list|)
condition|)
name|f
operator|=
name|stdin
expr_stmt|;
else|else
name|f
operator|=
name|fopen
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|"rw"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
operator|==
name|NULL
condition|)
return|return
literal|0
return|;
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
while|while
condition|(
literal|1
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s"
argument_list|,
name|prompt
argument_list|)
expr_stmt|;
if|if
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|f
argument_list|)
operator|==
name|NULL
condition|)
block|{
comment|/* Print a newline (the prompt probably didn\'t have one). */
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"\n"
argument_list|)
expr_stmt|;
name|strlcpy
argument_list|(
name|buf
argument_list|,
literal|"no"
argument_list|,
sizeof|sizeof
name|buf
argument_list|)
expr_stmt|;
block|}
comment|/* Remove newline from response. */
if|if
condition|(
name|strchr
argument_list|(
name|buf
argument_list|,
literal|'\n'
argument_list|)
condition|)
operator|*
name|strchr
argument_list|(
name|buf
argument_list|,
literal|'\n'
argument_list|)
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|buf
index|[
literal|0
index|]
operator|==
literal|0
condition|)
name|retval
operator|=
name|defval
expr_stmt|;
if|if
condition|(
name|strcmp
argument_list|(
name|buf
argument_list|,
literal|"yes"
argument_list|)
operator|==
literal|0
condition|)
name|retval
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|strcmp
argument_list|(
name|buf
argument_list|,
literal|"no"
argument_list|)
operator|==
literal|0
condition|)
name|retval
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|retval
operator|!=
operator|-
literal|1
condition|)
block|{
if|if
condition|(
name|f
operator|!=
name|stdin
condition|)
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
return|return
name|retval
return|;
block|}
block|}
block|}
end_function

begin_comment
comment|/*  * check whether the supplied host key is valid, return only if ok.  */
end_comment

begin_function
name|void
name|check_host_key
parameter_list|(
name|char
modifier|*
name|host
parameter_list|,
name|struct
name|sockaddr
modifier|*
name|hostaddr
parameter_list|,
name|RSA
modifier|*
name|host_key
parameter_list|)
block|{
name|RSA
modifier|*
name|file_key
decl_stmt|;
name|char
modifier|*
name|ip
init|=
name|NULL
decl_stmt|;
name|char
name|hostline
index|[
literal|1000
index|]
decl_stmt|,
modifier|*
name|hostp
decl_stmt|;
name|HostStatus
name|host_status
decl_stmt|;
name|HostStatus
name|ip_status
decl_stmt|;
name|int
name|local
init|=
literal|0
decl_stmt|,
name|host_ip_differ
init|=
literal|0
decl_stmt|;
name|char
name|ntop
index|[
name|NI_MAXHOST
index|]
decl_stmt|;
comment|/* 	 * Force accepting of the host key for loopback/localhost. The 	 * problem is that if the home directory is NFS-mounted to multiple 	 * machines, localhost will refer to a different machine in each of 	 * them, and the user will get bogus HOST_CHANGED warnings.  This 	 * essentially disables host authentication for localhost; however, 	 * this is probably not a real problem. 	 */
switch|switch
condition|(
name|hostaddr
operator|->
name|sa_family
condition|)
block|{
case|case
name|AF_INET
case|:
name|local
operator|=
operator|(
name|ntohl
argument_list|(
operator|(
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|hostaddr
operator|)
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
operator|>>
literal|24
operator|)
operator|==
name|IN_LOOPBACKNET
expr_stmt|;
break|break;
case|case
name|AF_INET6
case|:
name|local
operator|=
name|IN6_IS_ADDR_LOOPBACK
argument_list|(
operator|&
operator|(
operator|(
operator|(
expr|struct
name|sockaddr_in6
operator|*
operator|)
name|hostaddr
operator|)
operator|->
name|sin6_addr
operator|)
argument_list|)
expr_stmt|;
break|break;
default|default:
name|local
operator|=
literal|0
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|local
condition|)
block|{
name|debug
argument_list|(
literal|"Forcing accepting of host key for loopback/localhost."
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Turn off check_host_ip for proxy connects, since 	 * we don't have the remote ip-address 	 */
if|if
condition|(
name|options
operator|.
name|proxy_command
operator|!=
name|NULL
operator|&&
name|options
operator|.
name|check_host_ip
condition|)
name|options
operator|.
name|check_host_ip
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|check_host_ip
condition|)
block|{
if|if
condition|(
name|getnameinfo
argument_list|(
name|hostaddr
argument_list|,
name|hostaddr
operator|->
name|sa_len
argument_list|,
name|ntop
argument_list|,
sizeof|sizeof
argument_list|(
name|ntop
argument_list|)
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|NI_NUMERICHOST
argument_list|)
operator|!=
literal|0
condition|)
name|fatal
argument_list|(
literal|"check_host_key: getnameinfo failed"
argument_list|)
expr_stmt|;
name|ip
operator|=
name|xstrdup
argument_list|(
name|ntop
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Store the host key from the known host file in here so that we can 	 * compare it with the key for the IP address. 	 */
name|file_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|file_key
operator|->
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|file_key
operator|->
name|e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
comment|/* 	 * Check if the host key is present in the user\'s list of known 	 * hosts or in the systemwide list. 	 */
name|host_status
operator|=
name|check_host_in_hostfile
argument_list|(
name|options
operator|.
name|user_hostfile
argument_list|,
name|host
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|,
name|file_key
operator|->
name|e
argument_list|,
name|file_key
operator|->
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|host_status
operator|==
name|HOST_NEW
condition|)
name|host_status
operator|=
name|check_host_in_hostfile
argument_list|(
name|options
operator|.
name|system_hostfile
argument_list|,
name|host
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|,
name|file_key
operator|->
name|e
argument_list|,
name|file_key
operator|->
name|n
argument_list|)
expr_stmt|;
comment|/* 	 * Also perform check for the ip address, skip the check if we are 	 * localhost or the hostname was an ip address to begin with 	 */
if|if
condition|(
name|options
operator|.
name|check_host_ip
operator|&&
operator|!
name|local
operator|&&
name|strcmp
argument_list|(
name|host
argument_list|,
name|ip
argument_list|)
condition|)
block|{
name|RSA
modifier|*
name|ip_key
init|=
name|RSA_new
argument_list|()
decl_stmt|;
name|ip_key
operator|->
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|ip_key
operator|->
name|e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|ip_status
operator|=
name|check_host_in_hostfile
argument_list|(
name|options
operator|.
name|user_hostfile
argument_list|,
name|ip
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|,
name|ip_key
operator|->
name|e
argument_list|,
name|ip_key
operator|->
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip_status
operator|==
name|HOST_NEW
condition|)
name|ip_status
operator|=
name|check_host_in_hostfile
argument_list|(
name|options
operator|.
name|system_hostfile
argument_list|,
name|ip
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|,
name|ip_key
operator|->
name|e
argument_list|,
name|ip_key
operator|->
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|host_status
operator|==
name|HOST_CHANGED
operator|&&
operator|(
name|ip_status
operator|!=
name|HOST_CHANGED
operator|||
operator|(
name|BN_cmp
argument_list|(
name|ip_key
operator|->
name|e
argument_list|,
name|file_key
operator|->
name|e
argument_list|)
operator|||
name|BN_cmp
argument_list|(
name|ip_key
operator|->
name|n
argument_list|,
name|file_key
operator|->
name|n
argument_list|)
operator|)
operator|)
condition|)
name|host_ip_differ
operator|=
literal|1
expr_stmt|;
name|RSA_free
argument_list|(
name|ip_key
argument_list|)
expr_stmt|;
block|}
else|else
name|ip_status
operator|=
name|host_status
expr_stmt|;
name|RSA_free
argument_list|(
name|file_key
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|host_status
condition|)
block|{
case|case
name|HOST_OK
case|:
comment|/* The host is known and the key matches. */
name|debug
argument_list|(
literal|"Host '%.200s' is known and matches the host key."
argument_list|,
name|host
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|check_host_ip
condition|)
block|{
if|if
condition|(
name|ip_status
operator|==
name|HOST_NEW
condition|)
block|{
if|if
condition|(
operator|!
name|add_host_to_hostfile
argument_list|(
name|options
operator|.
name|user_hostfile
argument_list|,
name|ip
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|)
condition|)
name|log
argument_list|(
literal|"Failed to add the host key for IP address '%.30s' to the list of known hosts (%.30s)."
argument_list|,
name|ip
argument_list|,
name|options
operator|.
name|user_hostfile
argument_list|)
expr_stmt|;
else|else
name|log
argument_list|(
literal|"Warning: Permanently added host key for IP address '%.30s' to the list of known hosts."
argument_list|,
name|ip
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|ip_status
operator|!=
name|HOST_OK
condition|)
name|log
argument_list|(
literal|"Warning: the host key for '%.200s' differs from the key for the IP address '%.30s'"
argument_list|,
name|host
argument_list|,
name|ip
argument_list|)
expr_stmt|;
block|}
break|break;
case|case
name|HOST_NEW
case|:
comment|/* The host is new. */
if|if
condition|(
name|options
operator|.
name|strict_host_key_checking
operator|==
literal|1
condition|)
block|{
comment|/* User has requested strict host key checking.  We will not add the host key 			   automatically.  The only alternative left is to abort. */
name|fatal
argument_list|(
literal|"No host key is known for %.200s and you have requested strict checking."
argument_list|,
name|host
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|options
operator|.
name|strict_host_key_checking
operator|==
literal|2
condition|)
block|{
comment|/* The default */
name|char
name|prompt
index|[
literal|1024
index|]
decl_stmt|;
name|char
modifier|*
name|fp
init|=
name|fingerprint
argument_list|(
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|)
decl_stmt|;
name|snprintf
argument_list|(
name|prompt
argument_list|,
sizeof|sizeof
argument_list|(
name|prompt
argument_list|)
argument_list|,
literal|"The authenticity of host '%.200s' can't be established.\n"
literal|"Key fingerprint is %d %s.\n"
literal|"Are you sure you want to continue connecting (yes/no)? "
argument_list|,
name|host
argument_list|,
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
argument_list|,
name|fp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|read_yes_or_no
argument_list|(
name|prompt
argument_list|,
operator|-
literal|1
argument_list|)
condition|)
name|fatal
argument_list|(
literal|"Aborted by user!\n"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|options
operator|.
name|check_host_ip
operator|&&
name|ip_status
operator|==
name|HOST_NEW
operator|&&
name|strcmp
argument_list|(
name|host
argument_list|,
name|ip
argument_list|)
condition|)
block|{
name|snprintf
argument_list|(
name|hostline
argument_list|,
sizeof|sizeof
argument_list|(
name|hostline
argument_list|)
argument_list|,
literal|"%s,%s"
argument_list|,
name|host
argument_list|,
name|ip
argument_list|)
expr_stmt|;
name|hostp
operator|=
name|hostline
expr_stmt|;
block|}
else|else
name|hostp
operator|=
name|host
expr_stmt|;
comment|/* If not in strict mode, add the key automatically to the local known_hosts file. */
if|if
condition|(
operator|!
name|add_host_to_hostfile
argument_list|(
name|options
operator|.
name|user_hostfile
argument_list|,
name|hostp
argument_list|,
name|host_key
operator|->
name|e
argument_list|,
name|host_key
operator|->
name|n
argument_list|)
condition|)
name|log
argument_list|(
literal|"Failed to add the host to the list of known hosts (%.500s)."
argument_list|,
name|options
operator|.
name|user_hostfile
argument_list|)
expr_stmt|;
else|else
name|log
argument_list|(
literal|"Warning: Permanently added '%.200s' to the list of known hosts."
argument_list|,
name|hostp
argument_list|)
expr_stmt|;
break|break;
case|case
name|HOST_CHANGED
case|:
if|if
condition|(
name|options
operator|.
name|check_host_ip
operator|&&
name|host_ip_differ
condition|)
block|{
name|char
modifier|*
name|msg
decl_stmt|;
if|if
condition|(
name|ip_status
operator|==
name|HOST_NEW
condition|)
name|msg
operator|=
literal|"is unknown"
expr_stmt|;
elseif|else
if|if
condition|(
name|ip_status
operator|==
name|HOST_OK
condition|)
name|msg
operator|=
literal|"is unchanged"
expr_stmt|;
else|else
name|msg
operator|=
literal|"has a different value"
expr_stmt|;
name|error
argument_list|(
literal|"@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"@       WARNING: POSSIBLE DNS SPOOFING DETECTED!          @"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"The host key for %s has changed,"
argument_list|,
name|host
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"and the key for the according IP address %s"
argument_list|,
name|ip
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"%s. This could either mean that"
argument_list|,
name|msg
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"DNS SPOOFING is happening or the IP address for the host"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"and its host key have changed at the same time"
argument_list|)
expr_stmt|;
block|}
comment|/* The host key has changed. */
name|error
argument_list|(
literal|"@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"@    WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!     @"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"IT IS POSSIBLE THAT SOMEONE IS DOING SOMETHING NASTY!"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"Someone could be eavesdropping on you right now (man-in-the-middle attack)!"
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"It is also possible that the host key has just been changed."
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"Please contact your system administrator."
argument_list|)
expr_stmt|;
name|error
argument_list|(
literal|"Add correct host key in %.100s to get rid of this message."
argument_list|,
name|options
operator|.
name|user_hostfile
argument_list|)
expr_stmt|;
comment|/* 		 * If strict host key checking is in use, the user will have 		 * to edit the key manually and we can only abort. 		 */
if|if
condition|(
name|options
operator|.
name|strict_host_key_checking
condition|)
name|fatal
argument_list|(
literal|"Host key for %.200s has changed and you have requested strict checking."
argument_list|,
name|host
argument_list|)
expr_stmt|;
comment|/* 		 * If strict host key checking has not been requested, allow 		 * the connection but without password authentication or 		 * agent forwarding. 		 */
if|if
condition|(
name|options
operator|.
name|password_authentication
condition|)
block|{
name|error
argument_list|(
literal|"Password authentication is disabled to avoid trojan horses."
argument_list|)
expr_stmt|;
name|options
operator|.
name|password_authentication
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
name|options
operator|.
name|forward_agent
condition|)
block|{
name|error
argument_list|(
literal|"Agent forwarding is disabled to avoid trojan horses."
argument_list|)
expr_stmt|;
name|options
operator|.
name|forward_agent
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 		 * XXX Should permit the user to change to use the new id. 		 * This could be done by converting the host key to an 		 * identifying sentence, tell that the host identifies itself 		 * by that sentence, and ask the user if he/she whishes to 		 * accept the authentication. 		 */
break|break;
block|}
if|if
condition|(
name|options
operator|.
name|check_host_ip
condition|)
name|xfree
argument_list|(
name|ip
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * SSH1 key exchange  */
end_comment

begin_function
name|void
name|ssh_kex
parameter_list|(
name|char
modifier|*
name|host
parameter_list|,
name|struct
name|sockaddr
modifier|*
name|hostaddr
parameter_list|)
block|{
name|int
name|i
decl_stmt|;
name|BIGNUM
modifier|*
name|key
decl_stmt|;
name|RSA
modifier|*
name|host_key
decl_stmt|;
name|RSA
modifier|*
name|public_key
decl_stmt|;
name|int
name|bits
decl_stmt|,
name|rbits
decl_stmt|;
name|unsigned
name|char
name|session_key
index|[
name|SSH_SESSION_KEY_LENGTH
index|]
decl_stmt|;
name|unsigned
name|char
name|cookie
index|[
literal|8
index|]
decl_stmt|;
name|unsigned
name|int
name|supported_ciphers
decl_stmt|;
name|unsigned
name|int
name|server_flags
decl_stmt|,
name|client_flags
decl_stmt|;
name|int
name|payload_len
decl_stmt|,
name|clen
decl_stmt|,
name|sum_len
init|=
literal|0
decl_stmt|;
name|u_int32_t
name|rand
init|=
literal|0
decl_stmt|;
name|debug
argument_list|(
literal|"Waiting for server public key."
argument_list|)
expr_stmt|;
comment|/* Wait for a public key packet from the server. */
name|packet_read_expect
argument_list|(
operator|&
name|payload_len
argument_list|,
name|SSH_SMSG_PUBLIC_KEY
argument_list|)
expr_stmt|;
comment|/* Get cookie from the packet. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|8
condition|;
name|i
operator|++
control|)
name|cookie
index|[
name|i
index|]
operator|=
name|packet_get_char
argument_list|()
expr_stmt|;
comment|/* Get the public key. */
name|public_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|bits
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
comment|/* bits */
name|public_key
operator|->
name|e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|public_key
operator|->
name|e
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|sum_len
operator|+=
name|clen
expr_stmt|;
name|public_key
operator|->
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|public_key
operator|->
name|n
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|sum_len
operator|+=
name|clen
expr_stmt|;
name|rbits
operator|=
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|bits
operator|!=
name|rbits
condition|)
block|{
name|log
argument_list|(
literal|"Warning: Server lies about size of server public key: "
literal|"actual size is %d bits vs. announced %d."
argument_list|,
name|rbits
argument_list|,
name|bits
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"Warning: This may be due to an old implementation of ssh."
argument_list|)
expr_stmt|;
block|}
comment|/* Get the host key. */
name|host_key
operator|=
name|RSA_new
argument_list|()
expr_stmt|;
name|bits
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
comment|/* bits */
name|host_key
operator|->
name|e
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|host_key
operator|->
name|e
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|sum_len
operator|+=
name|clen
expr_stmt|;
name|host_key
operator|->
name|n
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|packet_get_bignum
argument_list|(
name|host_key
operator|->
name|n
argument_list|,
operator|&
name|clen
argument_list|)
expr_stmt|;
name|sum_len
operator|+=
name|clen
expr_stmt|;
name|rbits
operator|=
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|bits
operator|!=
name|rbits
condition|)
block|{
name|log
argument_list|(
literal|"Warning: Server lies about size of server host key: "
literal|"actual size is %d bits vs. announced %d."
argument_list|,
name|rbits
argument_list|,
name|bits
argument_list|)
expr_stmt|;
name|log
argument_list|(
literal|"Warning: This may be due to an old implementation of ssh."
argument_list|)
expr_stmt|;
block|}
comment|/* Get protocol flags. */
name|server_flags
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|packet_set_protocol_flags
argument_list|(
name|server_flags
argument_list|)
expr_stmt|;
name|supported_ciphers
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|supported_authentications
operator|=
name|packet_get_int
argument_list|()
expr_stmt|;
name|debug
argument_list|(
literal|"Received server public key (%d bits) and host key (%d bits)."
argument_list|,
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|packet_integrity_check
argument_list|(
name|payload_len
argument_list|,
literal|8
operator|+
literal|4
operator|+
name|sum_len
operator|+
literal|0
operator|+
literal|4
operator|+
literal|0
operator|+
literal|0
operator|+
literal|4
operator|+
literal|4
operator|+
literal|4
argument_list|,
name|SSH_SMSG_PUBLIC_KEY
argument_list|)
expr_stmt|;
name|check_host_key
argument_list|(
name|host
argument_list|,
name|hostaddr
argument_list|,
name|host_key
argument_list|)
expr_stmt|;
name|client_flags
operator|=
name|SSH_PROTOFLAG_SCREEN_NUMBER
operator||
name|SSH_PROTOFLAG_HOST_IN_FWD_OPEN
expr_stmt|;
name|compute_session_id
argument_list|(
name|session_id
argument_list|,
name|cookie
argument_list|,
name|host_key
operator|->
name|n
argument_list|,
name|public_key
operator|->
name|n
argument_list|)
expr_stmt|;
comment|/* Generate a session key. */
name|arc4random_stir
argument_list|()
expr_stmt|;
comment|/* 	 * Generate an encryption key for the session.   The key is a 256 bit 	 * random number, interpreted as a 32-byte key, with the least 	 * significant 8 bits being the first byte of the key. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|32
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|i
operator|%
literal|4
operator|==
literal|0
condition|)
name|rand
operator|=
name|arc4random
argument_list|()
expr_stmt|;
name|session_key
index|[
name|i
index|]
operator|=
name|rand
operator|&
literal|0xff
expr_stmt|;
name|rand
operator|>>=
literal|8
expr_stmt|;
block|}
comment|/* 	 * According to the protocol spec, the first byte of the session key 	 * is the highest byte of the integer.  The session key is xored with 	 * the first 16 bytes of the session id. 	 */
name|key
operator|=
name|BN_new
argument_list|()
expr_stmt|;
name|BN_set_word
argument_list|(
name|key
argument_list|,
literal|0
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|SSH_SESSION_KEY_LENGTH
condition|;
name|i
operator|++
control|)
block|{
name|BN_lshift
argument_list|(
name|key
argument_list|,
name|key
argument_list|,
literal|8
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|<
literal|16
condition|)
name|BN_add_word
argument_list|(
name|key
argument_list|,
name|session_key
index|[
name|i
index|]
operator|^
name|session_id
index|[
name|i
index|]
argument_list|)
expr_stmt|;
else|else
name|BN_add_word
argument_list|(
name|key
argument_list|,
name|session_key
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Encrypt the integer using the public key and host key of the 	 * server (key with smaller modulus first). 	 */
if|if
condition|(
name|BN_cmp
argument_list|(
name|public_key
operator|->
name|n
argument_list|,
name|host_key
operator|->
name|n
argument_list|)
operator|<
literal|0
condition|)
block|{
comment|/* Public key has smaller modulus. */
if|if
condition|(
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
operator|<
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
condition|)
block|{
name|fatal
argument_list|(
literal|"respond_to_rsa_challenge: host_key %d< public_key %d + "
literal|"SSH_KEY_BITS_RESERVED %d"
argument_list|,
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
argument_list|,
name|SSH_KEY_BITS_RESERVED
argument_list|)
expr_stmt|;
block|}
name|rsa_public_encrypt
argument_list|(
name|key
argument_list|,
name|key
argument_list|,
name|public_key
argument_list|)
expr_stmt|;
name|rsa_public_encrypt
argument_list|(
name|key
argument_list|,
name|key
argument_list|,
name|host_key
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* Host key has smaller modulus (or they are equal). */
if|if
condition|(
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
operator|<
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
operator|+
name|SSH_KEY_BITS_RESERVED
condition|)
block|{
name|fatal
argument_list|(
literal|"respond_to_rsa_challenge: public_key %d< host_key %d + "
literal|"SSH_KEY_BITS_RESERVED %d"
argument_list|,
name|BN_num_bits
argument_list|(
name|public_key
operator|->
name|n
argument_list|)
argument_list|,
name|BN_num_bits
argument_list|(
name|host_key
operator|->
name|n
argument_list|)
argument_list|,
name|SSH_KEY_BITS_RESERVED
argument_list|)
expr_stmt|;
block|}
name|rsa_public_encrypt
argument_list|(
name|key
argument_list|,
name|key
argument_list|,
name|host_key
argument_list|)
expr_stmt|;
name|rsa_public_encrypt
argument_list|(
name|key
argument_list|,
name|key
argument_list|,
name|public_key
argument_list|)
expr_stmt|;
block|}
comment|/* Destroy the public keys since we no longer need them. */
name|RSA_free
argument_list|(
name|public_key
argument_list|)
expr_stmt|;
name|RSA_free
argument_list|(
name|host_key
argument_list|)
expr_stmt|;
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NOT_SET
condition|)
block|{
if|if
condition|(
name|cipher_mask
argument_list|()
operator|&
name|supported_ciphers
operator|&
operator|(
literal|1
operator|<<
name|ssh_cipher_default
operator|)
condition|)
name|options
operator|.
name|cipher
operator|=
name|ssh_cipher_default
expr_stmt|;
else|else
block|{
name|debug
argument_list|(
literal|"Cipher %s not supported, using %.100s instead."
argument_list|,
name|cipher_name
argument_list|(
name|ssh_cipher_default
argument_list|)
argument_list|,
name|cipher_name
argument_list|(
name|SSH_FALLBACK_CIPHER
argument_list|)
argument_list|)
expr_stmt|;
name|options
operator|.
name|cipher
operator|=
name|SSH_FALLBACK_CIPHER
expr_stmt|;
block|}
block|}
comment|/* Check that the selected cipher is supported. */
if|if
condition|(
operator|!
operator|(
name|supported_ciphers
operator|&
operator|(
literal|1
operator|<<
name|options
operator|.
name|cipher
operator|)
operator|)
condition|)
name|fatal
argument_list|(
literal|"Selected cipher type %.100s not supported by server."
argument_list|,
name|cipher_name
argument_list|(
name|options
operator|.
name|cipher
argument_list|)
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Encryption type: %.100s"
argument_list|,
name|cipher_name
argument_list|(
name|options
operator|.
name|cipher
argument_list|)
argument_list|)
expr_stmt|;
comment|/* Send the encrypted session key to the server. */
name|packet_start
argument_list|(
name|SSH_CMSG_SESSION_KEY
argument_list|)
expr_stmt|;
name|packet_put_char
argument_list|(
name|options
operator|.
name|cipher
argument_list|)
expr_stmt|;
comment|/* Send the cookie back to the server. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|8
condition|;
name|i
operator|++
control|)
name|packet_put_char
argument_list|(
name|cookie
index|[
name|i
index|]
argument_list|)
expr_stmt|;
comment|/* Send and destroy the encrypted encryption key integer. */
name|packet_put_bignum
argument_list|(
name|key
argument_list|)
expr_stmt|;
name|BN_clear_free
argument_list|(
name|key
argument_list|)
expr_stmt|;
comment|/* Send protocol flags. */
name|packet_put_int
argument_list|(
name|client_flags
argument_list|)
expr_stmt|;
comment|/* Send the packet now. */
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
name|debug
argument_list|(
literal|"Sent encrypted session key."
argument_list|)
expr_stmt|;
comment|/* Set the encryption key. */
name|packet_set_encryption_key
argument_list|(
name|session_key
argument_list|,
name|SSH_SESSION_KEY_LENGTH
argument_list|,
name|options
operator|.
name|cipher
argument_list|)
expr_stmt|;
comment|/* We will no longer need the session key here.  Destroy any extra copies. */
name|memset
argument_list|(
name|session_key
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|session_key
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * Expect a success message from the server.  Note that this message 	 * will be received in encrypted form. 	 */
name|packet_read_expect
argument_list|(
operator|&
name|payload_len
argument_list|,
name|SSH_SMSG_SUCCESS
argument_list|)
expr_stmt|;
name|debug
argument_list|(
literal|"Received encrypted confirmation."
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Authenticate user  */
end_comment

begin_function
name|void
name|ssh_userauth
parameter_list|(
name|int
name|host_key_valid
parameter_list|,
name|RSA
modifier|*
name|own_host_key
parameter_list|,
name|uid_t
name|original_real_uid
parameter_list|,
name|char
modifier|*
name|host
parameter_list|)
block|{
name|int
name|i
decl_stmt|,
name|type
decl_stmt|;
name|int
name|payload_len
decl_stmt|;
name|struct
name|passwd
modifier|*
name|pw
decl_stmt|;
specifier|const
name|char
modifier|*
name|server_user
decl_stmt|,
modifier|*
name|local_user
decl_stmt|;
comment|/* Get local user name.  Use it as server user if no user name was given. */
name|pw
operator|=
name|getpwuid
argument_list|(
name|original_real_uid
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pw
condition|)
name|fatal
argument_list|(
literal|"User id %d not found from user database."
argument_list|,
name|original_real_uid
argument_list|)
expr_stmt|;
name|local_user
operator|=
name|xstrdup
argument_list|(
name|pw
operator|->
name|pw_name
argument_list|)
expr_stmt|;
name|server_user
operator|=
name|options
operator|.
name|user
condition|?
name|options
operator|.
name|user
else|:
name|local_user
expr_stmt|;
comment|/* Send the name of the user to log in as on the server. */
name|packet_start
argument_list|(
name|SSH_CMSG_USER
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|server_user
argument_list|,
name|strlen
argument_list|(
name|server_user
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* 	 * The server should respond with success if no authentication is 	 * needed (the user has no password).  Otherwise the server responds 	 * with failure. 	 */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
comment|/* check whether the connection was accepted without authentication. */
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
return|return;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response to SSH_CMSG_USER"
argument_list|,
name|type
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|AFS
comment|/* Try Kerberos tgt passing if the server supports it. */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_PASS_KRB4_TGT
operator|)
operator|)
operator|&&
name|options
operator|.
name|krb4_tgt_passing
condition|)
block|{
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NONE
condition|)
name|log
argument_list|(
literal|"WARNING: Encryption is disabled! Ticket will be transmitted in the clear!"
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|send_krb4_tgt
argument_list|()
expr_stmt|;
block|}
comment|/* Try AFS token passing if the server supports it. */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_PASS_AFS_TOKEN
operator|)
operator|)
operator|&&
name|options
operator|.
name|afs_token_passing
operator|&&
name|k_hasafs
argument_list|()
condition|)
block|{
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NONE
condition|)
name|log
argument_list|(
literal|"WARNING: Encryption is disabled! Token will be transmitted in the clear!"
argument_list|)
expr_stmt|;
name|send_afs_tokens
argument_list|()
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* AFS */
ifdef|#
directive|ifdef
name|KRB4
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_KRB4
operator|)
operator|)
operator|&&
name|options
operator|.
name|krb4_authentication
condition|)
block|{
name|debug
argument_list|(
literal|"Trying Kerberos authentication."
argument_list|)
expr_stmt|;
if|if
condition|(
name|try_krb4_authentication
argument_list|()
condition|)
block|{
comment|/* The server should respond with success or failure. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
return|return;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response to Kerberos auth"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* KRB4 */
ifdef|#
directive|ifdef
name|KRB5
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_KRB5
operator|)
operator|)
operator|&&
name|options
operator|.
name|krb5_authentication
condition|)
block|{
name|krb5_context
name|ssh_context
init|=
name|NULL
decl_stmt|;
name|krb5_auth_context
name|auth_context
init|=
name|NULL
decl_stmt|;
name|debug
argument_list|(
literal|"Trying Kerberos V5 authentication."
argument_list|)
expr_stmt|;
if|if
condition|(
name|try_krb5_authentication
argument_list|(
operator|&
name|ssh_context
argument_list|,
operator|&
name|auth_context
argument_list|)
condition|)
block|{
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
block|{
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_PASS_KRB5_TGT
operator|)
operator|)
operator|&&
name|options
operator|.
name|krb5_tgt_passing
condition|)
block|{
if|if
condition|(
name|options
operator|.
name|cipher
operator|==
name|SSH_CIPHER_NONE
condition|)
name|log
argument_list|(
literal|"WARNING: Encryption is disabled! Ticket will be transmitted in the clear!"
argument_list|)
expr_stmt|;
name|send_krb5_tgt
argument_list|(
name|ssh_context
argument_list|,
name|auth_context
argument_list|)
expr_stmt|;
block|}
name|krb5_auth_con_free
argument_list|(
name|ssh_context
argument_list|,
name|auth_context
argument_list|)
expr_stmt|;
name|krb5_free_context
argument_list|(
name|ssh_context
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response to Kerberos5 auth"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* KRB5 */
comment|/* 	 * Use rhosts authentication if running in privileged socket and we 	 * do not wish to remain anonymous. 	 */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_RHOSTS
operator|)
operator|)
operator|&&
name|options
operator|.
name|rhosts_authentication
condition|)
block|{
name|debug
argument_list|(
literal|"Trying rhosts authentication."
argument_list|)
expr_stmt|;
name|packet_start
argument_list|(
name|SSH_CMSG_AUTH_RHOSTS
argument_list|)
expr_stmt|;
name|packet_put_string
argument_list|(
name|local_user
argument_list|,
name|strlen
argument_list|(
name|local_user
argument_list|)
argument_list|)
expr_stmt|;
name|packet_send
argument_list|()
expr_stmt|;
name|packet_write_wait
argument_list|()
expr_stmt|;
comment|/* The server should respond with success or failure. */
name|type
operator|=
name|packet_read
argument_list|(
operator|&
name|payload_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|SSH_SMSG_SUCCESS
condition|)
return|return;
if|if
condition|(
name|type
operator|!=
name|SSH_SMSG_FAILURE
condition|)
name|packet_disconnect
argument_list|(
literal|"Protocol error: got %d in response to rhosts auth"
argument_list|,
name|type
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Try .rhosts or /etc/hosts.equiv authentication with RSA host 	 * authentication. 	 */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_RHOSTS_RSA
operator|)
operator|)
operator|&&
name|options
operator|.
name|rhosts_rsa_authentication
operator|&&
name|host_key_valid
condition|)
block|{
if|if
condition|(
name|try_rhosts_rsa_authentication
argument_list|(
name|local_user
argument_list|,
name|own_host_key
argument_list|)
condition|)
return|return;
block|}
comment|/* Try RSA authentication if the server supports it. */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_RSA
operator|)
operator|)
operator|&&
name|options
operator|.
name|rsa_authentication
condition|)
block|{
comment|/* 		 * Try RSA authentication using the authentication agent. The 		 * agent is tried first because no passphrase is needed for 		 * it, whereas identity files may require passphrases. 		 */
if|if
condition|(
name|try_agent_authentication
argument_list|()
condition|)
return|return;
comment|/* Try RSA authentication for each identity. */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|options
operator|.
name|num_identity_files
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|try_rsa_authentication
argument_list|(
name|options
operator|.
name|identity_files
index|[
name|i
index|]
argument_list|)
condition|)
return|return;
block|}
comment|/* Try skey authentication if the server supports it. */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_TIS
operator|)
operator|)
operator|&&
name|options
operator|.
name|skey_authentication
operator|&&
operator|!
name|options
operator|.
name|batch_mode
condition|)
block|{
if|if
condition|(
name|try_skey_authentication
argument_list|()
condition|)
return|return;
block|}
comment|/* Try password authentication if the server supports it. */
if|if
condition|(
operator|(
name|supported_authentications
operator|&
operator|(
literal|1
operator|<<
name|SSH_AUTH_PASSWORD
operator|)
operator|)
operator|&&
name|options
operator|.
name|password_authentication
operator|&&
operator|!
name|options
operator|.
name|batch_mode
condition|)
block|{
name|char
name|prompt
index|[
literal|80
index|]
decl_stmt|;
name|snprintf
argument_list|(
name|prompt
argument_list|,
sizeof|sizeof
argument_list|(
name|prompt
argument_list|)
argument_list|,
literal|"%.30s@%.40s's password: "
argument_list|,
name|server_user
argument_list|,
name|host
argument_list|)
expr_stmt|;
if|if
condition|(
name|try_password_authentication
argument_list|(
name|prompt
argument_list|)
condition|)
return|return;
block|}
comment|/* All authentication methods have failed.  Exit with an error message. */
name|fatal
argument_list|(
literal|"Permission denied."
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
end_function

begin_comment
comment|/*  * Starts a dialog with the server, and authenticates the current user on the  * server.  This does not need any extra privileges.  The basic connection  * to the server must already have been established before this is called.  * If login fails, this function prints an error and never returns.  * This function does not require super-user privileges.  */
end_comment

begin_function
name|void
name|ssh_login
parameter_list|(
name|int
name|host_key_valid
parameter_list|,
name|RSA
modifier|*
name|own_host_key
parameter_list|,
specifier|const
name|char
modifier|*
name|orighost
parameter_list|,
name|struct
name|sockaddr
modifier|*
name|hostaddr
parameter_list|,
name|uid_t
name|original_real_uid
parameter_list|)
block|{
name|char
modifier|*
name|host
decl_stmt|,
modifier|*
name|cp
decl_stmt|;
comment|/* Convert the user-supplied hostname into all lowercase. */
name|host
operator|=
name|xstrdup
argument_list|(
name|orighost
argument_list|)
expr_stmt|;
for|for
control|(
name|cp
operator|=
name|host
init|;
operator|*
name|cp
condition|;
name|cp
operator|++
control|)
if|if
condition|(
name|isupper
argument_list|(
operator|*
name|cp
argument_list|)
condition|)
operator|*
name|cp
operator|=
name|tolower
argument_list|(
operator|*
name|cp
argument_list|)
expr_stmt|;
comment|/* Exchange protocol version identification strings with the server. */
name|ssh_exchange_identification
argument_list|()
expr_stmt|;
comment|/* Put the connection into non-blocking mode. */
name|packet_set_nonblocking
argument_list|()
expr_stmt|;
name|supported_authentications
operator|=
literal|0
expr_stmt|;
comment|/* key exchange */
name|ssh_kex
argument_list|(
name|host
argument_list|,
name|hostaddr
argument_list|)
expr_stmt|;
if|if
condition|(
name|supported_authentications
operator|==
literal|0
condition|)
name|fatal
argument_list|(
literal|"supported_authentications == 0."
argument_list|)
expr_stmt|;
comment|/* authenticate user */
name|ssh_userauth
argument_list|(
name|host_key_valid
argument_list|,
name|own_host_key
argument_list|,
name|original_real_uid
argument_list|,
name|host
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

