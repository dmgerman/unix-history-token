begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * fake library for ssh  *  * This file includes getnameinfo().  * These funtions are defined in rfc2133.  *  * But these functions are not implemented correctly. The minimum subset  * is implemented for ssh use only. For exapmle, this routine assumes  * that ai_family is AF_INET. Don't use it for another purpose.  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|"ssh.h"
end_include

begin_expr_stmt
name|RCSID
argument_list|(
literal|"$Id: fake-getnameinfo.c,v 1.2 2001/02/09 01:55:36 djm Exp $"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_ifndef
ifndef|#
directive|ifndef
name|HAVE_GETNAMEINFO
end_ifndef

begin_function
name|int
name|getnameinfo
parameter_list|(
specifier|const
name|struct
name|sockaddr
modifier|*
name|sa
parameter_list|,
name|size_t
name|salen
parameter_list|,
name|char
modifier|*
name|host
parameter_list|,
name|size_t
name|hostlen
parameter_list|,
name|char
modifier|*
name|serv
parameter_list|,
name|size_t
name|servlen
parameter_list|,
name|int
name|flags
parameter_list|)
block|{
name|struct
name|sockaddr_in
modifier|*
name|sin
init|=
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|sa
decl_stmt|;
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|char
name|tmpserv
index|[
literal|16
index|]
decl_stmt|;
if|if
condition|(
name|serv
condition|)
block|{
name|snprintf
argument_list|(
name|tmpserv
argument_list|,
sizeof|sizeof
argument_list|(
name|tmpserv
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|ntohs
argument_list|(
name|sin
operator|->
name|sin_port
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|tmpserv
argument_list|)
operator|>=
name|servlen
condition|)
return|return
name|EAI_MEMORY
return|;
else|else
name|strcpy
argument_list|(
name|serv
argument_list|,
name|tmpserv
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|host
condition|)
block|{
if|if
condition|(
name|flags
operator|&
name|NI_NUMERICHOST
condition|)
block|{
if|if
condition|(
name|strlen
argument_list|(
name|inet_ntoa
argument_list|(
name|sin
operator|->
name|sin_addr
argument_list|)
argument_list|)
operator|>=
name|hostlen
condition|)
return|return
name|EAI_MEMORY
return|;
name|strcpy
argument_list|(
name|host
argument_list|,
name|inet_ntoa
argument_list|(
name|sin
operator|->
name|sin_addr
argument_list|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
else|else
block|{
name|hp
operator|=
name|gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sin
operator|->
name|sin_addr
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|in_addr
argument_list|)
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
return|return
name|EAI_NODATA
return|;
if|if
condition|(
name|strlen
argument_list|(
name|hp
operator|->
name|h_name
argument_list|)
operator|>=
name|hostlen
condition|)
return|return
name|EAI_MEMORY
return|;
name|strcpy
argument_list|(
name|host
argument_list|,
name|hp
operator|->
name|h_name
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
block|}
return|return
literal|0
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !HAVE_GETNAMEINFO */
end_comment

end_unit

