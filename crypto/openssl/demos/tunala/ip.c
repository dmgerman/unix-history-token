begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"tunala.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|NO_IP
end_ifndef

begin_define
define|#
directive|define
name|IP_LISTENER_BACKLOG
value|511
end_define

begin_comment
comment|/* So if it gets masked by 256 or some other 				   such value it'll still be respectable */
end_comment

begin_comment
comment|/* Any IP-related initialisations. For now, this means blocking SIGPIPE */
end_comment

begin_function
name|int
name|ip_initialise
parameter_list|(
name|void
parameter_list|)
block|{
name|struct
name|sigaction
name|sa
decl_stmt|;
name|sa
operator|.
name|sa_handler
operator|=
name|SIG_IGN
expr_stmt|;
name|sa
operator|.
name|sa_flags
operator|=
literal|0
expr_stmt|;
name|sigemptyset
argument_list|(
operator|&
name|sa
operator|.
name|sa_mask
argument_list|)
expr_stmt|;
if|if
condition|(
name|sigaction
argument_list|(
name|SIGPIPE
argument_list|,
operator|&
name|sa
argument_list|,
name|NULL
argument_list|)
operator|!=
literal|0
condition|)
return|return
literal|0
return|;
return|return
literal|1
return|;
block|}
end_function

begin_function
name|int
name|ip_create_listener_split
parameter_list|(
specifier|const
name|char
modifier|*
name|ip
parameter_list|,
name|unsigned
name|short
name|port
parameter_list|)
block|{
name|struct
name|sockaddr_in
name|in_addr
decl_stmt|;
name|int
name|fd
init|=
operator|-
literal|1
decl_stmt|;
name|int
name|reuseVal
init|=
literal|1
decl_stmt|;
comment|/* Create the socket */
if|if
condition|(
operator|(
name|fd
operator|=
name|socket
argument_list|(
name|PF_INET
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
operator|)
operator|==
operator|-
literal|1
condition|)
goto|goto
name|err
goto|;
comment|/* Set the SO_REUSEADDR flag - servers act weird without it */
if|if
condition|(
name|setsockopt
argument_list|(
name|fd
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|(
operator|&
name|reuseVal
operator|)
argument_list|,
sizeof|sizeof
argument_list|(
name|reuseVal
argument_list|)
argument_list|)
operator|!=
literal|0
condition|)
goto|goto
name|err
goto|;
comment|/* Prepare the listen address stuff */
name|in_addr
operator|.
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|in_addr
operator|.
name|sin_addr
operator|.
name|s_addr
argument_list|,
name|ip
argument_list|,
literal|4
argument_list|)
expr_stmt|;
name|in_addr
operator|.
name|sin_port
operator|=
name|htons
argument_list|(
name|port
argument_list|)
expr_stmt|;
comment|/* Bind to the required port/address/interface */
if|if
condition|(
name|bind
argument_list|(
name|fd
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|in_addr
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
argument_list|)
operator|!=
literal|0
condition|)
goto|goto
name|err
goto|;
comment|/* Start "listening" */
if|if
condition|(
name|listen
argument_list|(
name|fd
argument_list|,
name|IP_LISTENER_BACKLOG
argument_list|)
operator|!=
literal|0
condition|)
goto|goto
name|err
goto|;
return|return
name|fd
return|;
name|err
label|:
if|if
condition|(
name|fd
operator|!=
operator|-
literal|1
condition|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|ip_create_connection_split
parameter_list|(
specifier|const
name|char
modifier|*
name|ip
parameter_list|,
name|unsigned
name|short
name|port
parameter_list|)
block|{
name|struct
name|sockaddr_in
name|in_addr
decl_stmt|;
name|int
name|flags
decl_stmt|,
name|fd
init|=
operator|-
literal|1
decl_stmt|;
comment|/* Create the socket */
if|if
condition|(
operator|(
name|fd
operator|=
name|socket
argument_list|(
name|PF_INET
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
operator|)
operator|==
operator|-
literal|1
condition|)
goto|goto
name|err
goto|;
comment|/* Make it non-blocking */
if|if
condition|(
operator|(
operator|(
name|flags
operator|=
name|fcntl
argument_list|(
name|fd
argument_list|,
name|F_GETFL
argument_list|,
literal|0
argument_list|)
operator|)
operator|<
literal|0
operator|)
operator|||
operator|(
name|fcntl
argument_list|(
name|fd
argument_list|,
name|F_SETFL
argument_list|,
name|flags
operator||
name|O_NONBLOCK
argument_list|)
operator|<
literal|0
operator|)
condition|)
goto|goto
name|err
goto|;
comment|/* Prepare the connection address stuff */
name|in_addr
operator|.
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|in_addr
operator|.
name|sin_addr
operator|.
name|s_addr
argument_list|,
name|ip
argument_list|,
literal|4
argument_list|)
expr_stmt|;
name|in_addr
operator|.
name|sin_port
operator|=
name|htons
argument_list|(
name|port
argument_list|)
expr_stmt|;
comment|/* Start a connect (non-blocking, in all likelihood) */
if|if
condition|(
operator|(
name|connect
argument_list|(
name|fd
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|in_addr
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
argument_list|)
operator|!=
literal|0
operator|)
operator|&&
operator|(
name|errno
operator|!=
name|EINPROGRESS
operator|)
condition|)
goto|goto
name|err
goto|;
return|return
name|fd
return|;
name|err
label|:
if|if
condition|(
name|fd
operator|!=
operator|-
literal|1
condition|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_decl_stmt
specifier|static
name|char
name|all_local_ip
index|[]
init|=
block|{
literal|0x00
block|,
literal|0x00
block|,
literal|0x00
block|,
literal|0x00
block|}
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|ip_parse_address
parameter_list|(
specifier|const
name|char
modifier|*
name|address
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|parsed_ip
parameter_list|,
name|unsigned
name|short
modifier|*
name|parsed_port
parameter_list|,
name|int
name|accept_all_ip
parameter_list|)
block|{
name|char
name|buf
index|[
literal|256
index|]
decl_stmt|;
name|struct
name|hostent
modifier|*
name|lookup
decl_stmt|;
name|unsigned
name|long
name|port
decl_stmt|;
specifier|const
name|char
modifier|*
name|ptr
init|=
name|strstr
argument_list|(
name|address
argument_list|,
literal|":"
argument_list|)
decl_stmt|;
specifier|const
name|char
modifier|*
name|ip
init|=
name|all_local_ip
decl_stmt|;
if|if
condition|(
operator|!
name|ptr
condition|)
block|{
comment|/* We assume we're listening on all local interfaces and have 		 * only specified a port. */
if|if
condition|(
operator|!
name|accept_all_ip
condition|)
return|return
literal|0
return|;
name|ptr
operator|=
name|address
expr_stmt|;
goto|goto
name|determine_port
goto|;
block|}
if|if
condition|(
operator|(
name|ptr
operator|-
name|address
operator|)
operator|>
literal|255
condition|)
return|return
literal|0
return|;
name|memset
argument_list|(
name|buf
argument_list|,
literal|0
argument_list|,
literal|256
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|buf
argument_list|,
name|address
argument_list|,
name|ptr
operator|-
name|address
argument_list|)
expr_stmt|;
name|ptr
operator|++
expr_stmt|;
if|if
condition|(
operator|(
name|lookup
operator|=
name|gethostbyname
argument_list|(
name|buf
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
comment|/* Spit a message to differentiate between lookup failures and 		 * bad strings. */
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"hostname lookup for '%s' failed\n"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|ip
operator|=
name|lookup
operator|->
name|h_addr_list
index|[
literal|0
index|]
expr_stmt|;
name|determine_port
label|:
if|if
condition|(
name|strlen
argument_list|(
name|ptr
argument_list|)
operator|<
literal|1
condition|)
return|return
literal|0
return|;
if|if
condition|(
operator|!
name|int_strtoul
argument_list|(
name|ptr
argument_list|,
operator|&
name|port
argument_list|)
operator|||
operator|(
name|port
operator|>
literal|65535
operator|)
condition|)
return|return
literal|0
return|;
operator|*
name|parsed_ip
operator|=
name|ip
expr_stmt|;
operator|*
name|parsed_port
operator|=
operator|(
name|unsigned
name|short
operator|)
name|port
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_function
name|int
name|ip_create_listener
parameter_list|(
specifier|const
name|char
modifier|*
name|address
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|ip
decl_stmt|;
name|unsigned
name|short
name|port
decl_stmt|;
if|if
condition|(
operator|!
name|ip_parse_address
argument_list|(
name|address
argument_list|,
operator|&
name|ip
argument_list|,
operator|&
name|port
argument_list|,
literal|1
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|ip_create_listener_split
argument_list|(
name|ip
argument_list|,
name|port
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|ip_create_connection
parameter_list|(
specifier|const
name|char
modifier|*
name|address
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|ip
decl_stmt|;
name|unsigned
name|short
name|port
decl_stmt|;
if|if
condition|(
operator|!
name|ip_parse_address
argument_list|(
name|address
argument_list|,
operator|&
name|ip
argument_list|,
operator|&
name|port
argument_list|,
literal|0
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|ip_create_connection_split
argument_list|(
name|ip
argument_list|,
name|port
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|ip_accept_connection
parameter_list|(
name|int
name|listen_fd
parameter_list|)
block|{
return|return
name|accept
argument_list|(
name|listen_fd
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !defined(NO_IP) */
end_comment

end_unit

