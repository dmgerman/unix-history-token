begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  *  Copyright (c) 1999-2004 Sendmail, Inc. and its suppliers.  *	All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_include
include|#
directive|include
file|<sm/gen.h>
end_include

begin_macro
name|SM_RCSID
argument_list|(
literal|"@(#)$Id: comm.c,v 8.67 2006/11/02 17:54:44 ca Exp $"
argument_list|)
end_macro

begin_include
include|#
directive|include
file|"libmilter.h"
end_include

begin_include
include|#
directive|include
file|<sm/errstring.h>
end_include

begin_include
include|#
directive|include
file|<sys/uio.h>
end_include

begin_decl_stmt
specifier|static
name|ssize_t
name|retry_writev
name|__P
argument_list|(
operator|(
name|socket_t
operator|,
expr|struct
name|iovec
operator|*
operator|,
name|int
operator|,
expr|struct
name|timeval
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|size_t
name|Maxdatasize
init|=
name|MILTER_MAX_DATA_SIZE
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
name|_FFR_MAXDATASIZE
end_if

begin_comment
comment|/* **  SMFI_SETMAXDATASIZE -- set limit for milter data read/write. ** **	Parameters: **		sz -- new limit. ** **	Returns: **		old limit */
end_comment

begin_function
name|size_t
name|smfi_setmaxdatasize
parameter_list|(
name|sz
parameter_list|)
name|size_t
name|sz
decl_stmt|;
block|{
name|size_t
name|old
decl_stmt|;
name|old
operator|=
name|Maxdatasize
expr_stmt|;
name|Maxdatasize
operator|=
name|sz
expr_stmt|;
return|return
name|old
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _FFR_MAXDATASIZE */
end_comment

begin_comment
comment|/* **  MI_RD_CMD -- read a command ** **	Parameters: **		sd -- socket descriptor **		timeout -- maximum time to wait **		cmd -- single character command read from sd **		rlen -- pointer to length of result **		name -- name of milter ** **	Returns: **		buffer with rest of command **		(malloc()ed here, should be free()d) **		hack: encode error in cmd */
end_comment

begin_function
name|char
modifier|*
name|mi_rd_cmd
parameter_list|(
name|sd
parameter_list|,
name|timeout
parameter_list|,
name|cmd
parameter_list|,
name|rlen
parameter_list|,
name|name
parameter_list|)
name|socket_t
name|sd
decl_stmt|;
name|struct
name|timeval
modifier|*
name|timeout
decl_stmt|;
name|char
modifier|*
name|cmd
decl_stmt|;
name|size_t
modifier|*
name|rlen
decl_stmt|;
name|char
modifier|*
name|name
decl_stmt|;
block|{
name|ssize_t
name|len
decl_stmt|;
name|mi_int32
name|expl
decl_stmt|;
name|ssize_t
name|i
decl_stmt|;
name|FD_RD_VAR
argument_list|(
name|rds
argument_list|,
name|excs
argument_list|)
expr_stmt|;
name|int
name|ret
decl_stmt|;
name|int
name|save_errno
decl_stmt|;
name|char
modifier|*
name|buf
decl_stmt|;
name|char
name|data
index|[
name|MILTER_LEN_BYTES
operator|+
literal|1
index|]
decl_stmt|;
operator|*
name|cmd
operator|=
literal|'\0'
expr_stmt|;
operator|*
name|rlen
operator|=
literal|0
expr_stmt|;
name|i
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|FD_RD_INIT
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|)
expr_stmt|;
name|ret
operator|=
name|FD_RD_READY
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|,
name|timeout
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
literal|0
condition|)
break|break;
elseif|else
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EINTR
condition|)
continue|continue;
break|break;
block|}
if|if
condition|(
name|FD_IS_RD_EXC
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|)
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_SELECT
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|len
operator|=
name|MI_SOCK_READ
argument_list|(
name|sd
argument_list|,
name|data
operator|+
name|i
argument_list|,
sizeof|sizeof
name|data
operator|-
name|i
argument_list|)
expr_stmt|;
if|if
condition|(
name|MI_SOCK_READ_FAIL
argument_list|(
name|len
argument_list|)
condition|)
block|{
name|smi_log
argument_list|(
name|SMI_LOG_ERR
argument_list|,
literal|"%s, mi_rd_cmd: read returned %d: %s"
argument_list|,
name|name
argument_list|,
operator|(
name|int
operator|)
name|len
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|cmd
operator|=
name|SMFIC_RECVERR
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|len
operator|==
literal|0
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_EOF
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|len
operator|>=
operator|(
name|ssize_t
operator|)
sizeof|sizeof
name|data
operator|-
name|i
condition|)
break|break;
name|i
operator|+=
name|len
expr_stmt|;
block|}
if|if
condition|(
name|ret
operator|==
literal|0
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_TIMEOUT
expr_stmt|;
return|return
name|NULL
return|;
block|}
elseif|else
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|smi_log
argument_list|(
name|SMI_LOG_ERR
argument_list|,
literal|"%s: mi_rd_cmd: select returned %d: %s"
argument_list|,
name|name
argument_list|,
name|ret
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|cmd
operator|=
name|SMFIC_RECVERR
expr_stmt|;
return|return
name|NULL
return|;
block|}
operator|*
name|cmd
operator|=
name|data
index|[
name|MILTER_LEN_BYTES
index|]
expr_stmt|;
name|data
index|[
name|MILTER_LEN_BYTES
index|]
operator|=
literal|'\0'
expr_stmt|;
operator|(
name|void
operator|)
name|memcpy
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|&
name|expl
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
operator|(
name|data
index|[
literal|0
index|]
operator|)
argument_list|,
name|MILTER_LEN_BYTES
argument_list|)
expr_stmt|;
name|expl
operator|=
name|ntohl
argument_list|(
name|expl
argument_list|)
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|expl
operator|<=
literal|0
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|expl
operator|>
name|Maxdatasize
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_TOOBIG
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|#
directive|if
name|_FFR_ADD_NULL
name|buf
operator|=
name|malloc
argument_list|(
name|expl
operator|+
literal|1
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* _FFR_ADD_NULL */
name|buf
operator|=
name|malloc
argument_list|(
name|expl
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* _FFR_ADD_NULL */
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_MALLOC
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|i
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|FD_RD_INIT
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|)
expr_stmt|;
name|ret
operator|=
name|FD_RD_READY
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|,
name|timeout
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
literal|0
condition|)
break|break;
elseif|else
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EINTR
condition|)
continue|continue;
break|break;
block|}
if|if
condition|(
name|FD_IS_RD_EXC
argument_list|(
name|sd
argument_list|,
name|rds
argument_list|,
name|excs
argument_list|)
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_SELECT
expr_stmt|;
name|free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|len
operator|=
name|MI_SOCK_READ
argument_list|(
name|sd
argument_list|,
name|buf
operator|+
name|i
argument_list|,
name|expl
operator|-
name|i
argument_list|)
expr_stmt|;
if|if
condition|(
name|MI_SOCK_READ_FAIL
argument_list|(
name|len
argument_list|)
condition|)
block|{
name|smi_log
argument_list|(
name|SMI_LOG_ERR
argument_list|,
literal|"%s: mi_rd_cmd: read returned %d: %s"
argument_list|,
name|name
argument_list|,
operator|(
name|int
operator|)
name|len
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|ret
operator|=
operator|-
literal|1
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|len
operator|==
literal|0
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_EOF
expr_stmt|;
name|free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|len
operator|>
name|expl
operator|-
name|i
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_RECVERR
expr_stmt|;
name|free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|len
operator|>=
name|expl
operator|-
name|i
condition|)
block|{
operator|*
name|rlen
operator|=
name|expl
expr_stmt|;
if|#
directive|if
name|_FFR_ADD_NULL
comment|/* makes life simpler for common string routines */
name|buf
index|[
name|expl
index|]
operator|=
literal|'\0'
expr_stmt|;
endif|#
directive|endif
comment|/* _FFR_ADD_NULL */
return|return
name|buf
return|;
block|}
name|i
operator|+=
name|len
expr_stmt|;
block|}
name|save_errno
operator|=
name|errno
expr_stmt|;
name|free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
comment|/* select returned 0 (timeout) or< 0 (error) */
if|if
condition|(
name|ret
operator|==
literal|0
condition|)
block|{
operator|*
name|cmd
operator|=
name|SMFIC_TIMEOUT
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|ret
operator|<
literal|0
condition|)
block|{
name|smi_log
argument_list|(
name|SMI_LOG_ERR
argument_list|,
literal|"%s: mi_rd_cmd: select returned %d: %s"
argument_list|,
name|name
argument_list|,
name|ret
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|cmd
operator|=
name|SMFIC_RECVERR
expr_stmt|;
return|return
name|NULL
return|;
block|}
operator|*
name|cmd
operator|=
name|SMFIC_UNKNERR
expr_stmt|;
return|return
name|NULL
return|;
block|}
end_function

begin_comment
comment|/* **  RETRY_WRITEV -- Keep calling the writev() system call **	until all the data is written out or an error occurs. ** **	Parameters: **		fd -- socket descriptor **		iov -- io vector **		iovcnt -- number of elements in io vector **			must NOT exceed UIO_MAXIOV. **		timeout -- maximum time to wait ** **	Returns: **		success: number of bytes written **		otherwise: MI_FAILURE */
end_comment

begin_function
specifier|static
name|ssize_t
name|retry_writev
parameter_list|(
name|fd
parameter_list|,
name|iov
parameter_list|,
name|iovcnt
parameter_list|,
name|timeout
parameter_list|)
name|socket_t
name|fd
decl_stmt|;
name|struct
name|iovec
modifier|*
name|iov
decl_stmt|;
name|int
name|iovcnt
decl_stmt|;
name|struct
name|timeval
modifier|*
name|timeout
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
name|ssize_t
name|n
decl_stmt|,
name|written
decl_stmt|;
name|FD_WR_VAR
argument_list|(
name|wrs
argument_list|)
expr_stmt|;
name|written
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
while|while
condition|(
name|iovcnt
operator|>
literal|0
operator|&&
name|iov
index|[
literal|0
index|]
operator|.
name|iov_len
operator|==
literal|0
condition|)
block|{
name|iov
operator|++
expr_stmt|;
name|iovcnt
operator|--
expr_stmt|;
block|}
if|if
condition|(
name|iovcnt
operator|<=
literal|0
condition|)
return|return
name|written
return|;
comment|/* 		**  We don't care much about the timeout here, 		**  it's very long anyway; correct solution would be 		**  to take the time before the loop and reduce the 		**  timeout after each invocation. 		**  FD_SETSIZE is checked when socket is created. 		*/
name|FD_WR_INIT
argument_list|(
name|fd
argument_list|,
name|wrs
argument_list|)
expr_stmt|;
name|i
operator|=
name|FD_WR_READY
argument_list|(
name|fd
argument_list|,
name|wrs
argument_list|,
name|timeout
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
literal|0
condition|)
return|return
name|MI_FAILURE
return|;
if|if
condition|(
name|i
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EINTR
operator|||
name|errno
operator|==
name|EAGAIN
condition|)
continue|continue;
return|return
name|MI_FAILURE
return|;
block|}
name|n
operator|=
name|writev
argument_list|(
name|fd
argument_list|,
name|iov
argument_list|,
name|iovcnt
argument_list|)
expr_stmt|;
if|if
condition|(
name|n
operator|==
operator|-
literal|1
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EINTR
operator|||
name|errno
operator|==
name|EAGAIN
condition|)
continue|continue;
return|return
name|MI_FAILURE
return|;
block|}
name|written
operator|+=
name|n
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|iovcnt
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|iov
index|[
name|i
index|]
operator|.
name|iov_len
operator|>
operator|(
name|unsigned
name|int
operator|)
name|n
condition|)
block|{
name|iov
index|[
name|i
index|]
operator|.
name|iov_base
operator|=
operator|(
name|char
operator|*
operator|)
name|iov
index|[
name|i
index|]
operator|.
name|iov_base
operator|+
name|n
expr_stmt|;
name|iov
index|[
name|i
index|]
operator|.
name|iov_len
operator|-=
operator|(
name|unsigned
name|int
operator|)
name|n
expr_stmt|;
break|break;
block|}
name|n
operator|-=
operator|(
name|int
operator|)
name|iov
index|[
name|i
index|]
operator|.
name|iov_len
expr_stmt|;
name|iov
index|[
name|i
index|]
operator|.
name|iov_len
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|==
name|iovcnt
condition|)
return|return
name|written
return|;
block|}
block|}
end_function

begin_comment
comment|/* **  MI_WR_CMD -- write a cmd to sd ** **	Parameters: **		sd -- socket descriptor **		timeout -- maximum time to wait **		cmd -- single character command to write **		buf -- buffer with further data **		len -- length of buffer (without cmd!) ** **	Returns: **		MI_SUCCESS/MI_FAILURE */
end_comment

begin_function
name|int
name|mi_wr_cmd
parameter_list|(
name|sd
parameter_list|,
name|timeout
parameter_list|,
name|cmd
parameter_list|,
name|buf
parameter_list|,
name|len
parameter_list|)
name|socket_t
name|sd
decl_stmt|;
name|struct
name|timeval
modifier|*
name|timeout
decl_stmt|;
name|int
name|cmd
decl_stmt|;
name|char
modifier|*
name|buf
decl_stmt|;
name|size_t
name|len
decl_stmt|;
block|{
name|size_t
name|sl
decl_stmt|,
name|i
decl_stmt|;
name|ssize_t
name|l
decl_stmt|;
name|mi_int32
name|nl
decl_stmt|;
name|int
name|iovcnt
decl_stmt|;
name|struct
name|iovec
name|iov
index|[
literal|2
index|]
decl_stmt|;
name|char
name|data
index|[
name|MILTER_LEN_BYTES
operator|+
literal|1
index|]
decl_stmt|;
if|if
condition|(
name|len
operator|>
name|Maxdatasize
operator|||
operator|(
name|len
operator|>
literal|0
operator|&&
name|buf
operator|==
name|NULL
operator|)
condition|)
return|return
name|MI_FAILURE
return|;
name|nl
operator|=
name|htonl
argument_list|(
name|len
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* add 1 for the cmd char */
operator|(
name|void
operator|)
name|memcpy
argument_list|(
name|data
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|nl
argument_list|,
name|MILTER_LEN_BYTES
argument_list|)
expr_stmt|;
name|data
index|[
name|MILTER_LEN_BYTES
index|]
operator|=
operator|(
name|char
operator|)
name|cmd
expr_stmt|;
name|i
operator|=
literal|0
expr_stmt|;
name|sl
operator|=
name|MILTER_LEN_BYTES
operator|+
literal|1
expr_stmt|;
comment|/* set up the vector for the size / command */
name|iov
index|[
literal|0
index|]
operator|.
name|iov_base
operator|=
operator|(
name|void
operator|*
operator|)
name|data
expr_stmt|;
name|iov
index|[
literal|0
index|]
operator|.
name|iov_len
operator|=
name|sl
expr_stmt|;
name|iovcnt
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|len
operator|>=
literal|0
operator|&&
name|buf
operator|!=
name|NULL
condition|)
block|{
name|iov
index|[
literal|1
index|]
operator|.
name|iov_base
operator|=
operator|(
name|void
operator|*
operator|)
name|buf
expr_stmt|;
name|iov
index|[
literal|1
index|]
operator|.
name|iov_len
operator|=
name|len
expr_stmt|;
name|iovcnt
operator|=
literal|2
expr_stmt|;
block|}
name|l
operator|=
name|retry_writev
argument_list|(
name|sd
argument_list|,
name|iov
argument_list|,
name|iovcnt
argument_list|,
name|timeout
argument_list|)
expr_stmt|;
if|if
condition|(
name|l
operator|==
name|MI_FAILURE
condition|)
return|return
name|MI_FAILURE
return|;
return|return
name|MI_SUCCESS
return|;
block|}
end_function

end_unit

