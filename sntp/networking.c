begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<config.h>
end_include

begin_include
include|#
directive|include
file|"networking.h"
end_include

begin_include
include|#
directive|include
file|"ntp_debug.h"
end_include

begin_comment
comment|/* Send a packet */
end_comment

begin_function
name|int
name|sendpkt
parameter_list|(
name|SOCKET
name|rsock
parameter_list|,
name|sockaddr_u
modifier|*
name|dest
parameter_list|,
name|struct
name|pkt
modifier|*
name|pkt
parameter_list|,
name|int
name|len
parameter_list|)
block|{
name|int
name|cc
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
operator|>
literal|2
condition|)
block|{
name|printf
argument_list|(
literal|"sntp sendpkt: Packet data:\n"
argument_list|)
expr_stmt|;
name|pkt_output
argument_list|(
name|pkt
argument_list|,
name|len
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
name|TRACE
argument_list|(
literal|1
argument_list|,
operator|(
literal|"sntp sendpkt: Sending packet to %s ...\n"
operator|,
name|sptoa
argument_list|(
name|dest
argument_list|)
operator|)
argument_list|)
expr_stmt|;
name|cc
operator|=
name|sendto
argument_list|(
name|rsock
argument_list|,
operator|(
name|void
operator|*
operator|)
name|pkt
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
operator|&
name|dest
operator|->
name|sa
argument_list|,
name|SOCKLEN
argument_list|(
name|dest
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|cc
operator|==
name|SOCKET_ERROR
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Send to %s failed, %m"
argument_list|,
name|sptoa
argument_list|(
name|dest
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|TRACE
argument_list|(
literal|1
argument_list|,
operator|(
literal|"Packet sent.\n"
operator|)
argument_list|)
expr_stmt|;
return|return
name|TRUE
return|;
block|}
end_function

begin_comment
comment|/* Receive raw data */
end_comment

begin_function
name|int
name|recvdata
parameter_list|(
name|SOCKET
name|rsock
parameter_list|,
name|sockaddr_u
modifier|*
name|sender
parameter_list|,
name|void
modifier|*
name|rdata
parameter_list|,
name|int
name|rdata_length
parameter_list|)
block|{
name|GETSOCKNAME_SOCKLEN_TYPE
name|slen
decl_stmt|;
name|int
name|recvc
decl_stmt|;
name|slen
operator|=
sizeof|sizeof
argument_list|(
operator|*
name|sender
argument_list|)
expr_stmt|;
name|recvc
operator|=
name|recvfrom
argument_list|(
name|rsock
argument_list|,
name|rdata
argument_list|,
name|rdata_length
argument_list|,
literal|0
argument_list|,
operator|&
name|sender
operator|->
name|sa
argument_list|,
operator|&
name|slen
argument_list|)
expr_stmt|;
if|if
condition|(
name|recvc
operator|<
literal|0
condition|)
return|return
name|recvc
return|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
operator|>
literal|2
condition|)
block|{
name|printf
argument_list|(
literal|"Received %d bytes from %s:\n"
argument_list|,
name|recvc
argument_list|,
name|sptoa
argument_list|(
name|sender
argument_list|)
argument_list|)
expr_stmt|;
name|pkt_output
argument_list|(
operator|(
expr|struct
name|pkt
operator|*
operator|)
name|rdata
argument_list|,
name|recvc
argument_list|,
name|stdout
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
return|return
name|recvc
return|;
block|}
end_function

begin_comment
comment|/* Parsing from a short 'struct pkt' directly is bound to create  * coverity warnings. These are hard to avoid, as the formal declaration  * does not reflect the true layout in the presence of autokey extension  * fields. Parsing and skipping the extension fields of a received packet  * until there's only the MAC left is better done in this separate  * function.  */
end_comment

begin_function
specifier|static
name|void
modifier|*
name|skip_efields
parameter_list|(
name|u_int32
modifier|*
name|head
parameter_list|,
comment|/* head of extension chain 	*/
name|u_int32
modifier|*
name|tail
comment|/* tail/end of extension chain	*/
parameter_list|)
block|{
name|u_int
name|nlen
decl_stmt|;
comment|/* next extension length */
while|while
condition|(
operator|(
name|tail
operator|-
name|head
operator|)
operator|>
literal|6
condition|)
block|{
name|nlen
operator|=
name|ntohl
argument_list|(
operator|*
name|head
operator|++
argument_list|)
operator|&
literal|0xffff
expr_stmt|;
name|nlen
operator|=
operator|(
name|nlen
operator|+
literal|3
operator|)
operator|>>
literal|2
expr_stmt|;
if|if
condition|(
name|nlen
operator|>
call|(
name|u_int
call|)
argument_list|(
name|tail
operator|-
name|head
argument_list|)
operator|||
name|nlen
operator|<
literal|4
condition|)
return|return
name|NULL
return|;
comment|/* Blooper! Inconsistent! */
name|head
operator|+=
name|nlen
expr_stmt|;
block|}
return|return
name|head
return|;
block|}
end_function

begin_comment
comment|/* ** Check if it's data for us and whether it's useable or not. ** ** If not, return a failure code so we can delete this server from our list ** and continue with another one. */
end_comment

begin_function
name|int
name|process_pkt
parameter_list|(
name|struct
name|pkt
modifier|*
name|rpkt
parameter_list|,
name|sockaddr_u
modifier|*
name|sender
parameter_list|,
name|int
name|pkt_len
parameter_list|,
name|int
name|mode
parameter_list|,
name|struct
name|pkt
modifier|*
name|spkt
parameter_list|,
specifier|const
name|char
modifier|*
name|func_name
parameter_list|)
block|{
name|u_int
name|key_id
decl_stmt|;
name|struct
name|key
modifier|*
name|pkt_key
decl_stmt|;
name|int
name|is_authentic
decl_stmt|;
name|int
name|mac_size
decl_stmt|;
name|u_int
name|exten_len
decl_stmt|;
name|u_int32
modifier|*
name|exten_end
decl_stmt|;
name|u_int32
modifier|*
name|packet_end
decl_stmt|;
name|l_fp
name|sent_xmt
decl_stmt|;
name|l_fp
name|resp_org
decl_stmt|;
name|key_id
operator|=
literal|0
expr_stmt|;
name|pkt_key
operator|=
name|NULL
expr_stmt|;
name|is_authentic
operator|=
operator|(
name|HAVE_OPT
argument_list|(
name|AUTHENTICATION
argument_list|)
operator|)
condition|?
literal|0
else|:
operator|-
literal|1
expr_stmt|;
comment|/* 	 * Parse the extension field if present. We figure out whether 	 * an extension field is present by measuring the MAC size. If 	 * the number of words following the packet header is 0, no MAC 	 * is present and the packet is not authenticated. If 1, the 	 * packet is a crypto-NAK; if 3, the packet is authenticated 	 * with DES; if 5, the packet is authenticated with MD5; if 6, 	 * the packet is authenticated with SHA. If 2 or 4, the packet 	 * is a runt and discarded forthwith. If greater than 6, an 	 * extension field is present, so we subtract the length of the 	 * field and go around again. 	 */
if|if
condition|(
name|pkt_len
operator|<
operator|(
name|int
operator|)
name|LEN_PKT_NOMAC
operator|||
operator|(
name|pkt_len
operator|&
literal|3
operator|)
operator|!=
literal|0
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: Incredible packet length: %d.  Discarding."
argument_list|,
name|func_name
argument_list|,
name|pkt_len
argument_list|)
expr_stmt|;
return|return
name|PACKET_UNUSEABLE
return|;
block|}
comment|/* Note: pkt_len must be a multiple of 4 at this point! */
name|packet_end
operator|=
operator|(
name|u_int32
operator|*
operator|)
operator|(
operator|(
name|char
operator|*
operator|)
name|rpkt
operator|+
name|pkt_len
operator|)
expr_stmt|;
name|exten_end
operator|=
name|skip_efields
argument_list|(
name|rpkt
operator|->
name|exten
argument_list|,
name|packet_end
argument_list|)
expr_stmt|;
if|if
condition|(
name|NULL
operator|==
name|exten_end
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: Missing extension field.  Discarding."
argument_list|,
name|func_name
argument_list|)
expr_stmt|;
return|return
name|PACKET_UNUSEABLE
return|;
block|}
comment|/* get size of MAC in cells; can be zero */
name|exten_len
operator|=
call|(
name|u_int
call|)
argument_list|(
name|packet_end
operator|-
name|exten_end
argument_list|)
expr_stmt|;
comment|/* deduce action required from remaining length */
switch|switch
condition|(
name|exten_len
condition|)
block|{
case|case
literal|0
case|:
comment|/* no MAC at all */
break|break;
case|case
literal|1
case|:
comment|/* crypto NAK */
name|key_id
operator|=
name|ntohl
argument_list|(
operator|*
name|exten_end
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"Crypto NAK = 0x%08x\n"
argument_list|,
name|key_id
argument_list|)
expr_stmt|;
break|break;
case|case
literal|3
case|:
comment|/* key ID + 3DES MAC -- unsupported! */
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: Key ID + 3DES MAC is unsupported.  Discarding."
argument_list|,
name|func_name
argument_list|)
expr_stmt|;
return|return
name|PACKET_UNUSEABLE
return|;
case|case
literal|5
case|:
comment|/* key ID + MD5 MAC */
case|case
literal|6
case|:
comment|/* key ID + SHA MAC */
comment|/* 		** Look for the key used by the server in the specified 		** keyfile and if existent, fetch it or else leave the 		** pointer untouched 		*/
name|key_id
operator|=
name|ntohl
argument_list|(
operator|*
name|exten_end
argument_list|)
expr_stmt|;
name|get_key
argument_list|(
name|key_id
argument_list|,
operator|&
name|pkt_key
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pkt_key
condition|)
block|{
name|printf
argument_list|(
literal|"unrecognized key ID = 0x%08x\n"
argument_list|,
name|key_id
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* 		** Seems like we've got a key with matching keyid. 		** 		** Generate a md5sum of the packet with the key from our 		** keyfile and compare those md5sums. 		*/
name|mac_size
operator|=
name|exten_len
operator|<<
literal|2
expr_stmt|;
if|if
condition|(
operator|!
name|auth_md5
argument_list|(
operator|(
name|char
operator|*
operator|)
name|rpkt
argument_list|,
name|pkt_len
operator|-
name|mac_size
argument_list|,
name|mac_size
operator|-
literal|4
argument_list|,
name|pkt_key
argument_list|)
condition|)
block|{
name|is_authentic
operator|=
name|FALSE
expr_stmt|;
break|break;
block|}
comment|/* Yay! Things worked out! */
name|is_authentic
operator|=
name|TRUE
expr_stmt|;
name|TRACE
argument_list|(
literal|1
argument_list|,
operator|(
literal|"sntp %s: packet from %s authenticated using key id %d.\n"
operator|,
name|func_name
operator|,
name|stoa
argument_list|(
name|sender
argument_list|)
operator|,
name|key_id
operator|)
argument_list|)
expr_stmt|;
break|break;
default|default:
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: Unexpected extension length: %d.  Discarding."
argument_list|,
name|func_name
argument_list|,
name|exten_len
argument_list|)
expr_stmt|;
return|return
name|PACKET_UNUSEABLE
return|;
block|}
switch|switch
condition|(
name|is_authentic
condition|)
block|{
case|case
operator|-
literal|1
case|:
comment|/* unknown */
break|break;
case|case
literal|0
case|:
comment|/* not authentic */
return|return
name|SERVER_AUTH_FAIL
return|;
break|break;
case|case
literal|1
case|:
comment|/* authentic */
break|break;
default|default:
comment|/* error */
break|break;
block|}
comment|/* Check for server's ntp version */
if|if
condition|(
name|PKT_VERSION
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
operator|<
name|NTP_OLDVERSION
operator|||
name|PKT_VERSION
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
operator|>
name|NTP_VERSION
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: Packet shows wrong version (%d)"
argument_list|,
name|func_name
argument_list|,
name|PKT_VERSION
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|SERVER_UNUSEABLE
return|;
block|}
comment|/* We want a server to sync with */
if|if
condition|(
name|PKT_MODE
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
operator|!=
name|mode
operator|&&
name|PKT_MODE
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
operator|!=
name|MODE_PASSIVE
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: mode %d stratum %d"
argument_list|,
name|func_name
argument_list|,
name|PKT_MODE
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
argument_list|,
name|rpkt
operator|->
name|stratum
argument_list|)
expr_stmt|;
return|return
name|SERVER_UNUSEABLE
return|;
block|}
comment|/* Stratum is unspecified (0) check what's going on */
if|if
condition|(
name|STRATUM_PKT_UNSPEC
operator|==
name|rpkt
operator|->
name|stratum
condition|)
block|{
name|char
modifier|*
name|ref_char
decl_stmt|;
name|TRACE
argument_list|(
literal|1
argument_list|,
operator|(
literal|"%s: Stratum unspecified, going to check for KOD (stratum: %d)\n"
operator|,
name|func_name
operator|,
name|rpkt
operator|->
name|stratum
operator|)
argument_list|)
expr_stmt|;
name|ref_char
operator|=
operator|(
name|char
operator|*
operator|)
operator|&
name|rpkt
operator|->
name|refid
expr_stmt|;
name|TRACE
argument_list|(
literal|1
argument_list|,
operator|(
literal|"%s: Packet refid: %c%c%c%c\n"
operator|,
name|func_name
operator|,
name|ref_char
index|[
literal|0
index|]
operator|,
name|ref_char
index|[
literal|1
index|]
operator|,
name|ref_char
index|[
literal|2
index|]
operator|,
name|ref_char
index|[
literal|3
index|]
operator|)
argument_list|)
expr_stmt|;
comment|/* If it's a KOD packet we'll just use the KOD information */
if|if
condition|(
name|ref_char
index|[
literal|0
index|]
operator|!=
literal|'X'
condition|)
block|{
if|if
condition|(
name|strncmp
argument_list|(
name|ref_char
argument_list|,
literal|"DENY"
argument_list|,
literal|4
argument_list|)
operator|==
literal|0
condition|)
return|return
name|KOD_DEMOBILIZE
return|;
if|if
condition|(
name|strncmp
argument_list|(
name|ref_char
argument_list|,
literal|"RSTR"
argument_list|,
literal|4
argument_list|)
operator|==
literal|0
condition|)
return|return
name|KOD_DEMOBILIZE
return|;
if|if
condition|(
name|strncmp
argument_list|(
name|ref_char
argument_list|,
literal|"RATE"
argument_list|,
literal|4
argument_list|)
operator|==
literal|0
condition|)
return|return
name|KOD_RATE
return|;
comment|/* 			** There are other interesting kiss codes which 			** might be interesting for authentication. 			*/
block|}
block|}
comment|/* If the server is not synced it's not really useable for us */
if|if
condition|(
name|LEAP_NOTINSYNC
operator|==
name|PKT_LEAP
argument_list|(
name|rpkt
operator|->
name|li_vn_mode
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s: %s not in sync, skipping this server"
argument_list|,
name|func_name
argument_list|,
name|stoa
argument_list|(
name|sender
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|SERVER_UNUSEABLE
return|;
block|}
comment|/* 	 * Decode the org timestamp and make sure we're getting a response 	 * to our last request, but only if we're not in broadcast mode. 	 */
if|if
condition|(
name|MODE_BROADCAST
operator|==
name|mode
condition|)
return|return
name|pkt_len
return|;
if|if
condition|(
operator|!
name|L_ISEQU
argument_list|(
operator|&
name|rpkt
operator|->
name|org
argument_list|,
operator|&
name|spkt
operator|->
name|xmt
argument_list|)
condition|)
block|{
name|NTOHL_FP
argument_list|(
operator|&
name|rpkt
operator|->
name|org
argument_list|,
operator|&
name|resp_org
argument_list|)
expr_stmt|;
name|NTOHL_FP
argument_list|(
operator|&
name|spkt
operator|->
name|xmt
argument_list|,
operator|&
name|sent_xmt
argument_list|)
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s response org expected to match sent xmt"
argument_list|,
name|stoa
argument_list|(
name|sender
argument_list|)
argument_list|)
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"resp org: %s"
argument_list|,
name|prettydate
argument_list|(
operator|&
name|resp_org
argument_list|)
argument_list|)
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"sent xmt: %s"
argument_list|,
name|prettydate
argument_list|(
operator|&
name|sent_xmt
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|PACKET_UNUSEABLE
return|;
block|}
return|return
name|pkt_len
return|;
block|}
end_function

end_unit

