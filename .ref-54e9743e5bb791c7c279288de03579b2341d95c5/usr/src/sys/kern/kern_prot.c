begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986, 1989, 1990, 1991, 1993  *	The Regents of the University of California.  All rights reserved.  * (c) UNIX System Laboratories, Inc.  * All or some portions of this file are derived from material licensed  * to the University of California by American Telephone and Telegraph  * Co. or Unix System Laboratories, Inc. and are reproduced herein with  * the permission of UNIX System Laboratories, Inc.  *  * %sccs.include.redist.c%  *  *	@(#)kern_prot.c	8.6 (Berkeley) %G%  */
end_comment

begin_comment
comment|/*  * System calls related to processes and protection  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/acct.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/ucred.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/timeb.h>
end_include

begin_include
include|#
directive|include
file|<sys/times.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_struct
struct|struct
name|args
block|{
name|int
name|dummy
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getpid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_pid
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|COMPAT_43
argument_list|)
operator|||
name|defined
argument_list|(
name|COMPAT_SUNOS
argument_list|)
name|retval
index|[
literal|1
index|]
operator|=
name|p
operator|->
name|p_pptr
operator|->
name|p_pid
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getppid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_pptr
operator|->
name|p_pid
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* Get process group ID; note that POSIX getpgrp takes no parameter */
end_comment

begin_macro
name|getpgrp
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_pgrp
operator|->
name|pg_id
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getuid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_cred
operator|->
name|p_ruid
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|COMPAT_43
argument_list|)
operator|||
name|defined
argument_list|(
name|COMPAT_SUNOS
argument_list|)
name|retval
index|[
literal|1
index|]
operator|=
name|p
operator|->
name|p_ucred
operator|->
name|cr_uid
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|geteuid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_ucred
operator|->
name|cr_uid
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getgid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_cred
operator|->
name|p_rgid
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|COMPAT_43
argument_list|)
operator|||
name|defined
argument_list|(
name|COMPAT_SUNOS
argument_list|)
name|retval
index|[
literal|1
index|]
operator|=
name|p
operator|->
name|p_ucred
operator|->
name|cr_groups
index|[
literal|0
index|]
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Get effective group ID.  The "egid" is groups[0], and could be obtained  * via getgroups.  This syscall exists because it is somewhat painful to do  * correctly in a library function.  */
end_comment

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getegid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|p
operator|->
name|p_ucred
operator|->
name|cr_groups
index|[
literal|0
index|]
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|getgroups_args
block|{
name|u_int
name|gidsetsize
decl_stmt|;
name|gid_t
modifier|*
name|gidset
decl_stmt|;
block|}
struct|;
end_struct

begin_macro
name|getgroups
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|getgroups_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|u_int
name|ngrp
decl_stmt|;
name|int
name|error
decl_stmt|;
if|if
condition|(
operator|(
name|ngrp
operator|=
name|uap
operator|->
name|gidsetsize
operator|)
operator|==
literal|0
condition|)
block|{
operator|*
name|retval
operator|=
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_ngroups
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|ngrp
operator|<
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_ngroups
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
name|ngrp
operator|=
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_ngroups
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_groups
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|gidset
argument_list|,
name|ngrp
operator|*
sizeof|sizeof
argument_list|(
name|gid_t
argument_list|)
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
operator|*
name|retval
operator|=
name|ngrp
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_expr_stmt
name|setsid
argument_list|(
name|p
argument_list|,
name|uap
argument_list|,
name|retval
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|p
operator|->
name|p_pgid
operator|==
name|p
operator|->
name|p_pid
operator|||
name|pgfind
argument_list|(
name|p
operator|->
name|p_pid
argument_list|)
condition|)
block|{
return|return
operator|(
name|EPERM
operator|)
return|;
block|}
else|else
block|{
operator|(
name|void
operator|)
name|enterpgrp
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|*
name|retval
operator|=
name|p
operator|->
name|p_pid
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
block|}
end_block

begin_comment
comment|/*  * set process group (setpgid/old setpgrp)  *  * caller does setpgid(targpid, targpgid)  *  * pid must be caller or child of caller (ESRCH)  * if a child  *	pid must be in same session (EPERM)  *	pid can't have done an exec (EACCES)  * if pgid != pid  * 	there must exist some pid in same session having pgid (EPERM)  * pid must not be session leader (EPERM)  */
end_comment

begin_struct
struct|struct
name|setpgid_args
block|{
name|int
name|pid
decl_stmt|;
comment|/* target process id */
name|int
name|pgid
decl_stmt|;
comment|/* target pgrp id */
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setpgid
argument_list|(
argument|curp
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|curp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|setpgid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|targp
decl_stmt|;
comment|/* target process */
specifier|register
name|struct
name|pgrp
modifier|*
name|pgrp
decl_stmt|;
comment|/* target pgrp */
if|if
condition|(
name|uap
operator|->
name|pid
operator|!=
literal|0
operator|&&
name|uap
operator|->
name|pid
operator|!=
name|curp
operator|->
name|p_pid
condition|)
block|{
if|if
condition|(
operator|(
name|targp
operator|=
name|pfind
argument_list|(
name|uap
operator|->
name|pid
argument_list|)
operator|)
operator|==
literal|0
operator|||
operator|!
name|inferior
argument_list|(
name|targp
argument_list|)
condition|)
return|return
operator|(
name|ESRCH
operator|)
return|;
if|if
condition|(
name|targp
operator|->
name|p_session
operator|!=
name|curp
operator|->
name|p_session
condition|)
return|return
operator|(
name|EPERM
operator|)
return|;
if|if
condition|(
name|targp
operator|->
name|p_flag
operator|&
name|P_EXEC
condition|)
return|return
operator|(
name|EACCES
operator|)
return|;
block|}
else|else
name|targp
operator|=
name|curp
expr_stmt|;
if|if
condition|(
name|SESS_LEADER
argument_list|(
name|targp
argument_list|)
condition|)
return|return
operator|(
name|EPERM
operator|)
return|;
if|if
condition|(
name|uap
operator|->
name|pgid
operator|==
literal|0
condition|)
name|uap
operator|->
name|pgid
operator|=
name|targp
operator|->
name|p_pid
expr_stmt|;
elseif|else
if|if
condition|(
name|uap
operator|->
name|pgid
operator|!=
name|targp
operator|->
name|p_pid
condition|)
if|if
condition|(
operator|(
name|pgrp
operator|=
name|pgfind
argument_list|(
name|uap
operator|->
name|pgid
argument_list|)
operator|)
operator|==
literal|0
operator|||
name|pgrp
operator|->
name|pg_session
operator|!=
name|curp
operator|->
name|p_session
condition|)
return|return
operator|(
name|EPERM
operator|)
return|;
return|return
operator|(
name|enterpgrp
argument_list|(
name|targp
argument_list|,
name|uap
operator|->
name|pgid
argument_list|,
literal|0
argument_list|)
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|setuid_args
block|{
name|uid_t
name|uid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setuid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|setuid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|uid_t
name|uid
decl_stmt|;
name|int
name|error
decl_stmt|;
name|uid
operator|=
name|uap
operator|->
name|uid
expr_stmt|;
if|if
condition|(
name|uid
operator|!=
name|pc
operator|->
name|p_ruid
operator|&&
operator|(
name|error
operator|=
name|suser
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
operator|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
comment|/* 	 * Everything's okay, do it. 	 * Transfer proc count to new user. 	 * Copy credentials so other references do not see our changes. 	 */
operator|(
name|void
operator|)
name|chgproccnt
argument_list|(
name|pc
operator|->
name|p_ruid
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|chgproccnt
argument_list|(
name|uid
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|pc
operator|->
name|pc_ucred
operator|=
name|crcopy
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|)
expr_stmt|;
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_uid
operator|=
name|uid
expr_stmt|;
name|pc
operator|->
name|p_ruid
operator|=
name|uid
expr_stmt|;
name|pc
operator|->
name|p_svuid
operator|=
name|uid
expr_stmt|;
name|p
operator|->
name|p_flag
operator||=
name|P_SUGID
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|seteuid_args
block|{
name|uid_t
name|euid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|seteuid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|seteuid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|uid_t
name|euid
decl_stmt|;
name|int
name|error
decl_stmt|;
name|euid
operator|=
name|uap
operator|->
name|euid
expr_stmt|;
if|if
condition|(
name|euid
operator|!=
name|pc
operator|->
name|p_ruid
operator|&&
name|euid
operator|!=
name|pc
operator|->
name|p_svuid
operator|&&
operator|(
name|error
operator|=
name|suser
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
operator|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
comment|/* 	 * Everything's okay, do it.  Copy credentials so other references do 	 * not see our changes. 	 */
name|pc
operator|->
name|pc_ucred
operator|=
name|crcopy
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|)
expr_stmt|;
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_uid
operator|=
name|euid
expr_stmt|;
name|p
operator|->
name|p_flag
operator||=
name|P_SUGID
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|setgid_args
block|{
name|gid_t
name|gid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setgid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|setgid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|gid_t
name|gid
decl_stmt|;
name|int
name|error
decl_stmt|;
name|gid
operator|=
name|uap
operator|->
name|gid
expr_stmt|;
if|if
condition|(
name|gid
operator|!=
name|pc
operator|->
name|p_rgid
operator|&&
operator|(
name|error
operator|=
name|suser
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
operator|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|pc
operator|->
name|pc_ucred
operator|=
name|crcopy
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|)
expr_stmt|;
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_groups
index|[
literal|0
index|]
operator|=
name|gid
expr_stmt|;
name|pc
operator|->
name|p_rgid
operator|=
name|gid
expr_stmt|;
name|pc
operator|->
name|p_svgid
operator|=
name|gid
expr_stmt|;
comment|/* ??? */
name|p
operator|->
name|p_flag
operator||=
name|P_SUGID
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|setegid_args
block|{
name|gid_t
name|egid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setegid
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|setegid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|gid_t
name|egid
decl_stmt|;
name|int
name|error
decl_stmt|;
name|egid
operator|=
name|uap
operator|->
name|egid
expr_stmt|;
if|if
condition|(
name|egid
operator|!=
name|pc
operator|->
name|p_rgid
operator|&&
name|egid
operator|!=
name|pc
operator|->
name|p_svgid
operator|&&
operator|(
name|error
operator|=
name|suser
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
operator|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|pc
operator|->
name|pc_ucred
operator|=
name|crcopy
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|)
expr_stmt|;
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_groups
index|[
literal|0
index|]
operator|=
name|egid
expr_stmt|;
name|p
operator|->
name|p_flag
operator||=
name|P_SUGID
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|setgroups_args
block|{
name|u_int
name|gidsetsize
decl_stmt|;
name|gid_t
modifier|*
name|gidset
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setgroups
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|setgroups_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
specifier|register
name|u_int
name|ngrp
decl_stmt|;
name|int
name|error
decl_stmt|;
if|if
condition|(
name|error
operator|=
name|suser
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
if|if
condition|(
operator|(
name|ngrp
operator|=
name|uap
operator|->
name|gidsetsize
operator|)
operator|>
name|NGROUPS
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
name|pc
operator|->
name|pc_ucred
operator|=
name|crcopy
argument_list|(
name|pc
operator|->
name|pc_ucred
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|gidset
argument_list|,
operator|(
name|caddr_t
operator|)
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_groups
argument_list|,
name|ngrp
operator|*
sizeof|sizeof
argument_list|(
name|gid_t
argument_list|)
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|pc
operator|->
name|pc_ucred
operator|->
name|cr_ngroups
operator|=
name|ngrp
expr_stmt|;
name|p
operator|->
name|p_flag
operator||=
name|P_SUGID
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_if
if|#
directive|if
name|defined
argument_list|(
name|COMPAT_43
argument_list|)
operator|||
name|defined
argument_list|(
name|COMPAT_SUNOS
argument_list|)
end_if

begin_struct
struct|struct
name|setreuid_args
block|{
name|int
name|ruid
decl_stmt|;
name|int
name|euid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_expr_stmt
name|osetreuid
argument_list|(
name|p
argument_list|,
name|uap
argument_list|,
name|retval
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|setreuid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
name|struct
name|seteuid_args
name|args
decl_stmt|;
comment|/* 	 * we assume that the intent of setting ruid is to be able to get 	 * back ruid priviledge. So we make sure that we will be able to 	 * do so, but do not actually set the ruid. 	 */
if|if
condition|(
name|uap
operator|->
name|ruid
operator|!=
operator|(
name|uid_t
operator|)
operator|-
literal|1
operator|&&
name|uap
operator|->
name|ruid
operator|!=
name|pc
operator|->
name|p_ruid
operator|&&
name|uap
operator|->
name|ruid
operator|!=
name|pc
operator|->
name|p_svuid
condition|)
return|return
operator|(
name|EPERM
operator|)
return|;
if|if
condition|(
name|uap
operator|->
name|euid
operator|==
operator|(
name|uid_t
operator|)
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|args
operator|.
name|euid
operator|=
name|uap
operator|->
name|euid
expr_stmt|;
return|return
operator|(
name|seteuid
argument_list|(
name|p
argument_list|,
operator|&
name|args
argument_list|,
name|retval
argument_list|)
operator|)
return|;
block|}
end_block

begin_struct
struct|struct
name|setregid_args
block|{
name|int
name|rgid
decl_stmt|;
name|int
name|egid
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_expr_stmt
name|osetregid
argument_list|(
name|p
argument_list|,
name|uap
argument_list|,
name|retval
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|setregid_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pcred
modifier|*
name|pc
init|=
name|p
operator|->
name|p_cred
decl_stmt|;
name|struct
name|setegid_args
name|args
decl_stmt|;
comment|/* 	 * we assume that the intent of setting rgid is to be able to get 	 * back rgid priviledge. So we make sure that we will be able to 	 * do so, but do not actually set the rgid. 	 */
if|if
condition|(
name|uap
operator|->
name|rgid
operator|!=
operator|(
name|gid_t
operator|)
operator|-
literal|1
operator|&&
name|uap
operator|->
name|rgid
operator|!=
name|pc
operator|->
name|p_rgid
operator|&&
name|uap
operator|->
name|rgid
operator|!=
name|pc
operator|->
name|p_svgid
condition|)
return|return
operator|(
name|EPERM
operator|)
return|;
if|if
condition|(
name|uap
operator|->
name|egid
operator|==
operator|(
name|gid_t
operator|)
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|args
operator|.
name|egid
operator|=
name|uap
operator|->
name|egid
expr_stmt|;
return|return
operator|(
name|setegid
argument_list|(
name|p
argument_list|,
operator|&
name|args
argument_list|,
name|retval
argument_list|)
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(COMPAT_43) || defined(COMPAT_SUNOS) */
end_comment

begin_comment
comment|/*  * Check if gid is a member of the group set.  */
end_comment

begin_macro
name|groupmember
argument_list|(
argument|gid
argument_list|,
argument|cred
argument_list|)
end_macro

begin_decl_stmt
name|gid_t
name|gid
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|ucred
modifier|*
name|cred
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|gid_t
modifier|*
name|gp
decl_stmt|;
name|gid_t
modifier|*
name|egp
decl_stmt|;
name|egp
operator|=
operator|&
operator|(
name|cred
operator|->
name|cr_groups
index|[
name|cred
operator|->
name|cr_ngroups
index|]
operator|)
expr_stmt|;
for|for
control|(
name|gp
operator|=
name|cred
operator|->
name|cr_groups
init|;
name|gp
operator|<
name|egp
condition|;
name|gp
operator|++
control|)
if|if
condition|(
operator|*
name|gp
operator|==
name|gid
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Test whether the specified credentials imply "super-user"  * privilege; if so, and we have accounting info, set the flag  * indicating use of super-powers.  * Returns 0 or error.  */
end_comment

begin_macro
name|suser
argument_list|(
argument|cred
argument_list|,
argument|acflag
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|ucred
modifier|*
name|cred
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|short
modifier|*
name|acflag
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|cred
operator|->
name|cr_uid
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|acflag
condition|)
operator|*
name|acflag
operator||=
name|ASU
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
return|return
operator|(
name|EPERM
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Allocate a zeroed cred structure.  */
end_comment

begin_function
name|struct
name|ucred
modifier|*
name|crget
parameter_list|()
block|{
specifier|register
name|struct
name|ucred
modifier|*
name|cr
decl_stmt|;
name|MALLOC
argument_list|(
name|cr
argument_list|,
expr|struct
name|ucred
operator|*
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|cr
argument_list|)
argument_list|,
name|M_CRED
argument_list|,
name|M_WAITOK
argument_list|)
expr_stmt|;
name|bzero
argument_list|(
operator|(
name|caddr_t
operator|)
name|cr
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|cr
argument_list|)
argument_list|)
expr_stmt|;
name|cr
operator|->
name|cr_ref
operator|=
literal|1
expr_stmt|;
return|return
operator|(
name|cr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Free a cred structure.  * Throws away space when ref count gets to 0.  */
end_comment

begin_macro
name|crfree
argument_list|(
argument|cr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|ucred
modifier|*
name|cr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|s
decl_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
comment|/* ??? */
if|if
condition|(
operator|--
name|cr
operator|->
name|cr_ref
operator|==
literal|0
condition|)
name|FREE
argument_list|(
operator|(
name|caddr_t
operator|)
name|cr
argument_list|,
name|M_CRED
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Copy cred structure to a new one and free the old one.  */
end_comment

begin_function
name|struct
name|ucred
modifier|*
name|crcopy
parameter_list|(
name|cr
parameter_list|)
name|struct
name|ucred
modifier|*
name|cr
decl_stmt|;
block|{
name|struct
name|ucred
modifier|*
name|newcr
decl_stmt|;
if|if
condition|(
name|cr
operator|->
name|cr_ref
operator|==
literal|1
condition|)
return|return
operator|(
name|cr
operator|)
return|;
name|newcr
operator|=
name|crget
argument_list|()
expr_stmt|;
operator|*
name|newcr
operator|=
operator|*
name|cr
expr_stmt|;
name|crfree
argument_list|(
name|cr
argument_list|)
expr_stmt|;
name|newcr
operator|->
name|cr_ref
operator|=
literal|1
expr_stmt|;
return|return
operator|(
name|newcr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Dup cred struct to a new held one.  */
end_comment

begin_function
name|struct
name|ucred
modifier|*
name|crdup
parameter_list|(
name|cr
parameter_list|)
name|struct
name|ucred
modifier|*
name|cr
decl_stmt|;
block|{
name|struct
name|ucred
modifier|*
name|newcr
decl_stmt|;
name|newcr
operator|=
name|crget
argument_list|()
expr_stmt|;
operator|*
name|newcr
operator|=
operator|*
name|cr
expr_stmt|;
name|newcr
operator|->
name|cr_ref
operator|=
literal|1
expr_stmt|;
return|return
operator|(
name|newcr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Get login name, if available.  */
end_comment

begin_struct
struct|struct
name|getlogin_args
block|{
name|char
modifier|*
name|namebuf
decl_stmt|;
name|u_int
name|namelen
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getlogin
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|getlogin_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|uap
operator|->
name|namelen
operator|>
sizeof|sizeof
argument_list|(
name|p
operator|->
name|p_pgrp
operator|->
name|pg_session
operator|->
name|s_login
argument_list|)
condition|)
name|uap
operator|->
name|namelen
operator|=
sizeof|sizeof
argument_list|(
name|p
operator|->
name|p_pgrp
operator|->
name|pg_session
operator|->
name|s_login
argument_list|)
expr_stmt|;
return|return
operator|(
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
name|p
operator|->
name|p_pgrp
operator|->
name|pg_session
operator|->
name|s_login
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|namebuf
argument_list|,
name|uap
operator|->
name|namelen
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Set login name.  */
end_comment

begin_struct
struct|struct
name|setlogin_args
block|{
name|char
modifier|*
name|namebuf
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|setlogin
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|setlogin_args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|error
decl_stmt|;
if|if
condition|(
name|error
operator|=
name|suser
argument_list|(
name|p
operator|->
name|p_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|error
operator|=
name|copyinstr
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|namebuf
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
operator|->
name|p_pgrp
operator|->
name|pg_session
operator|->
name|s_login
argument_list|,
sizeof|sizeof
argument_list|(
name|p
operator|->
name|p_pgrp
operator|->
name|pg_session
operator|->
name|s_login
argument_list|)
operator|-
literal|1
argument_list|,
operator|(
name|u_int
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
name|ENAMETOOLONG
condition|)
name|error
operator|=
name|EINVAL
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

end_unit

