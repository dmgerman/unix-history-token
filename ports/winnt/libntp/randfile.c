begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Make sure that there is a good source of random characters  * so that OpenSSL can work properly and securely.  */
end_comment

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_include
include|#
directive|include
file|<wincrypt.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_function_decl
name|unsigned
name|int
name|getrandom_chars
parameter_list|(
name|int
name|desired
parameter_list|,
name|unsigned
name|char
modifier|*
name|buf
parameter_list|,
name|int
name|lenbuf
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|BOOL
name|create_random_file
parameter_list|(
name|char
modifier|*
name|filename
parameter_list|)
function_decl|;
end_function_decl

begin_function
name|BOOL
name|init_randfile
parameter_list|()
block|{
name|FILE
modifier|*
name|rf
decl_stmt|;
name|char
modifier|*
name|randfile
decl_stmt|;
name|char
modifier|*
name|homedir
decl_stmt|;
name|char
name|tmp
index|[
literal|256
index|]
decl_stmt|;
comment|/* See if the environmental variable RANDFILE is defined 	 * and the file exists 	 */
name|randfile
operator|=
name|getenv
argument_list|(
literal|"RANDFILE"
argument_list|)
expr_stmt|;
if|if
condition|(
name|randfile
operator|!=
name|NULL
condition|)
block|{
name|rf
operator|=
name|fopen
argument_list|(
name|randfile
argument_list|,
literal|"rb"
argument_list|)
expr_stmt|;
if|if
condition|(
name|rf
operator|!=
name|NULL
condition|)
block|{
name|fclose
argument_list|(
name|rf
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
else|else
block|{
comment|/* The environmental variable exists but not the file */
return|return
operator|(
name|create_random_file
argument_list|(
name|randfile
argument_list|)
operator|)
return|;
block|}
block|}
comment|/* 	 * If the RANDFILE environmental variable does not exist, 	 * see if the HOME enviromental variable exists and 	 * a .rnd file is in there. 	 */
name|homedir
operator|=
name|getenv
argument_list|(
literal|"HOME"
argument_list|)
expr_stmt|;
if|if
condition|(
name|homedir
operator|!=
name|NULL
operator|&&
operator|(
name|strlen
argument_list|(
name|homedir
argument_list|)
operator|+
literal|5
comment|/* \.rnd */
operator|)
operator|<
sizeof|sizeof
argument_list|(
name|tmp
argument_list|)
condition|)
block|{
name|snprintf
argument_list|(
name|tmp
argument_list|,
sizeof|sizeof
argument_list|(
name|tmp
argument_list|)
argument_list|,
literal|"%s\\.rnd"
argument_list|,
name|homedir
argument_list|)
expr_stmt|;
name|rf
operator|=
name|fopen
argument_list|(
name|tmp
argument_list|,
literal|"rb"
argument_list|)
expr_stmt|;
if|if
condition|(
name|rf
operator|!=
name|NULL
condition|)
block|{
name|fclose
argument_list|(
name|rf
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
else|else
block|{
comment|/* The HOME environmental variable exists but not the file */
return|return
operator|(
name|create_random_file
argument_list|(
name|tmp
argument_list|)
operator|)
return|;
block|}
block|}
comment|/* 	 * Final try. Look for it on the C:\ directory 	 * NOTE: This is a really bad place for it security-wise 	 * However, OpenSSL looks for it there if it can't find it elsewhere 	 */
name|rf
operator|=
name|fopen
argument_list|(
literal|"C:\\.rnd"
argument_list|,
literal|"rb"
argument_list|)
expr_stmt|;
if|if
condition|(
name|rf
operator|!=
name|NULL
condition|)
block|{
name|fclose
argument_list|(
name|rf
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
comment|/* The file does not exist */
return|return
operator|(
name|create_random_file
argument_list|(
literal|"C:\\.rnd"
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Routine to create the random file with 1024 random characters  */
end_comment

begin_function
name|BOOL
name|create_random_file
parameter_list|(
name|char
modifier|*
name|filename
parameter_list|)
block|{
name|FILE
modifier|*
name|rf
decl_stmt|;
name|int
name|nchars
decl_stmt|;
name|unsigned
name|char
name|buf
index|[
literal|1025
index|]
decl_stmt|;
name|nchars
operator|=
name|getrandom_chars
argument_list|(
literal|1024
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|rf
operator|=
name|fopen
argument_list|(
name|filename
argument_list|,
literal|"wb"
argument_list|)
expr_stmt|;
if|if
condition|(
name|rf
operator|==
name|NULL
condition|)
return|return
operator|(
name|FALSE
operator|)
return|;
name|fwrite
argument_list|(
name|buf
argument_list|,
expr|sizeof
operator|(
name|unsigned
name|char
operator|)
argument_list|,
name|nchars
argument_list|,
name|rf
argument_list|)
expr_stmt|;
name|fclose
argument_list|(
name|rf
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
end_function

begin_function
name|unsigned
name|int
name|getrandom_chars
parameter_list|(
name|int
name|desired
parameter_list|,
name|unsigned
name|char
modifier|*
name|buf
parameter_list|,
name|int
name|lenbuf
parameter_list|)
block|{
name|HCRYPTPROV
name|hcryptprov
decl_stmt|;
name|BOOL
name|err
decl_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
operator|||
name|lenbuf
operator|<=
literal|0
operator|||
name|desired
operator|>
name|lenbuf
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* 	 * The first time we just try to acquire the context 	 */
name|err
operator|=
name|CryptAcquireContext
argument_list|(
operator|&
name|hcryptprov
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|PROV_RSA_FULL
argument_list|,
name|CRYPT_VERIFYCONTEXT
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|err
condition|)
block|{
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
operator|!
name|CryptGenRandom
argument_list|(
name|hcryptprov
argument_list|,
name|desired
argument_list|,
name|buf
argument_list|)
condition|)
block|{
name|CryptReleaseContext
argument_list|(
name|hcryptprov
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|CryptReleaseContext
argument_list|(
name|hcryptprov
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
name|desired
operator|)
return|;
block|}
end_function

end_unit

