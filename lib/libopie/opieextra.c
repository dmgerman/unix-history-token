begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * This file contains routines modified from OpenBSD. Parts are contributed  * by Todd Miller<millert@openbsd.org>, Theo De Raadt<deraadt@openbsd.org>  * and possibly others.  */
end_comment

begin_include
include|#
directive|include
file|<sys/cdefs.h>
end_include

begin_expr_stmt
name|__FBSDID
argument_list|(
literal|"$FreeBSD$"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<opie.h>
end_include

begin_comment
comment|/*  * opie_haopie()  *  * Returns: 1 user doesnt exist, -1 file error, 0 user exists.  *  */
end_comment

begin_function
name|int
name|opie_haskey
parameter_list|(
name|username
parameter_list|)
name|char
modifier|*
name|username
decl_stmt|;
block|{
name|struct
name|opie
name|opie
decl_stmt|;
return|return
name|opielookup
argument_list|(
operator|&
name|opie
argument_list|,
name|username
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * opie_keyinfo()  *  * Returns the current sequence number and  * seed for the passed user.  *  */
end_comment

begin_function
name|char
modifier|*
name|opie_keyinfo
parameter_list|(
name|username
parameter_list|)
name|char
modifier|*
name|username
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
specifier|static
name|char
name|str
index|[
name|OPIE_CHALLENGE_MAX
index|]
decl_stmt|;
name|struct
name|opie
name|opie
decl_stmt|;
name|i
operator|=
name|opiechallenge
argument_list|(
operator|&
name|opie
argument_list|,
name|username
argument_list|,
name|str
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|str
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * opie_passverify()  *  * Check to see if answer is the correct one to the current  * challenge.  *  * Returns: 0 success, -1 failure  *  */
end_comment

begin_function
name|int
name|opie_passverify
parameter_list|(
name|username
parameter_list|,
name|passwd
parameter_list|)
name|char
modifier|*
name|username
decl_stmt|;
name|char
modifier|*
name|passwd
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
name|struct
name|opie
name|opie
decl_stmt|;
name|i
operator|=
name|opielookup
argument_list|(
operator|&
name|opie
argument_list|,
name|username
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
operator|-
literal|1
operator|||
name|i
operator|==
literal|1
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
if|if
condition|(
name|opieverify
argument_list|(
operator|&
name|opie
argument_list|,
name|passwd
argument_list|)
operator|==
literal|0
condition|)
return|return
operator|(
name|opie
operator|.
name|opie_n
operator|)
return|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
end_function

begin_define
define|#
directive|define
name|OPIE_HASH_DEFAULT
value|1
end_define

begin_comment
comment|/* Current hash type (index into opie_hash_types array) */
end_comment

begin_decl_stmt
specifier|static
name|int
name|opie_hash_type
init|=
name|OPIE_HASH_DEFAULT
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|opie_algorithm_table
block|{
specifier|const
name|char
modifier|*
name|name
decl_stmt|;
block|}
struct|;
end_struct

begin_decl_stmt
specifier|static
name|struct
name|opie_algorithm_table
name|opie_algorithm_table
index|[]
init|=
block|{
literal|"md4"
block|,
literal|"md5"
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Get current hash type */
end_comment

begin_function
specifier|const
name|char
modifier|*
name|opie_get_algorithm
parameter_list|()
block|{
return|return
operator|(
name|opie_algorithm_table
index|[
name|opie_hash_type
index|]
operator|.
name|name
operator|)
return|;
block|}
end_function

end_unit

