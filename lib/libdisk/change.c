begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * ----------------------------------------------------------------------------  * "THE BEER-WARE LICENSE" (Revision 42):  *<phk@login.dknet.dk> wrote this file.  As long as you retain this notice you  * can do whatever you want with this stuff. If we meet some day, and you think  * this stuff is worth it, you can buy me a beer in return.   Poul-Henning Kamp  * ----------------------------------------------------------------------------  *  * $Id: change.c,v 1.10 1995/12/07 10:33:18 peter Exp $  *  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|<fcntl.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<err.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|"libdisk.h"
end_include

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|struct disk * Set_Phys_Geom(struct disk *disk, u_long cyl, u_long hd, u_long sect) { 	struct disk *d = Int_Open_Disk(disk->name,cyl*hd*sect); 	d->real_cyl = cyl; 	d->real_hd = hd; 	d->real_sect = sect; 	d->bios_cyl = disk->bios_cyl; 	d->bios_hd = disk->bios_hd; 	d->bios_sect = disk->bios_sect; 	d->flags = disk->flags; 	Free_Disk(disk); 	return d; }
endif|#
directive|endif
end_endif

begin_function
name|void
name|Set_Bios_Geom
parameter_list|(
name|struct
name|disk
modifier|*
name|disk
parameter_list|,
name|u_long
name|cyl
parameter_list|,
name|u_long
name|hd
parameter_list|,
name|u_long
name|sect
parameter_list|)
block|{
name|disk
operator|->
name|bios_cyl
operator|=
name|cyl
expr_stmt|;
name|disk
operator|->
name|bios_hd
operator|=
name|hd
expr_stmt|;
name|disk
operator|->
name|bios_sect
operator|=
name|sect
expr_stmt|;
name|Bios_Limit_Chunk
argument_list|(
name|disk
operator|->
name|chunks
argument_list|,
literal|1024
operator|*
name|hd
operator|*
name|sect
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|All_FreeBSD
parameter_list|(
name|struct
name|disk
modifier|*
name|d
parameter_list|,
name|int
name|force_all
parameter_list|)
block|{
name|struct
name|chunk
modifier|*
name|c
decl_stmt|;
name|again
label|:
for|for
control|(
name|c
operator|=
name|d
operator|->
name|chunks
operator|->
name|part
init|;
name|c
condition|;
name|c
operator|=
name|c
operator|->
name|next
control|)
if|if
condition|(
name|c
operator|->
name|type
operator|!=
name|unused
condition|)
block|{
name|Delete_Chunk
argument_list|(
name|d
argument_list|,
name|c
argument_list|)
expr_stmt|;
goto|goto
name|again
goto|;
block|}
name|c
operator|=
name|d
operator|->
name|chunks
expr_stmt|;
name|Create_Chunk
argument_list|(
name|d
argument_list|,
name|c
operator|->
name|offset
argument_list|,
name|c
operator|->
name|size
argument_list|,
name|freebsd
argument_list|,
literal|0xa5
argument_list|,
name|force_all
condition|?
name|CHUNK_FORCE_ALL
else|:
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

