begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<login_cap.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_function
name|int
name|login_progok
parameter_list|(
name|uid_t
name|uid
parameter_list|,
specifier|const
name|char
modifier|*
name|prog
parameter_list|)
block|{
name|login_cap_t
modifier|*
name|lc
decl_stmt|;
specifier|const
name|struct
name|passwd
modifier|*
name|pwd
decl_stmt|;
name|char
modifier|*
modifier|*
name|data
decl_stmt|;
name|pwd
operator|=
name|getpwuid
argument_list|(
name|uid
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pwd
condition|)
return|return
literal|0
return|;
comment|/* How did that happen ? - we can't run */
name|lc
operator|=
name|login_getpwclass
argument_list|(
name|pwd
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|lc
condition|)
return|return
literal|1
return|;
comment|/* We're missing login.conf ? - we can run */
name|data
operator|=
name|login_getcaplist
argument_list|(
name|lc
argument_list|,
literal|"prog.allow"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
condition|)
for|for
control|(
init|;
operator|*
name|data
condition|;
name|data
operator|++
control|)
if|if
condition|(
operator|!
name|strcmp
argument_list|(
operator|*
name|data
argument_list|,
name|prog
argument_list|)
condition|)
block|{
name|login_close
argument_list|(
name|lc
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
comment|/* We're in prog.allow - we can run */
block|}
name|data
operator|=
name|login_getcaplist
argument_list|(
name|lc
argument_list|,
literal|"prog.deny"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
condition|)
for|for
control|(
init|;
operator|*
name|data
condition|;
name|data
operator|++
control|)
if|if
condition|(
operator|!
name|strcmp
argument_list|(
operator|*
name|data
argument_list|,
name|prog
argument_list|)
condition|)
block|{
name|login_close
argument_list|(
name|lc
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
comment|/* We're in prog.deny - we can't run */
block|}
name|login_close
argument_list|(
name|lc
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
comment|/* We're not mentioned anywhere - we can run */
block|}
end_function

end_unit

