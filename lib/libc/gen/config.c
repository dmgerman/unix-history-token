begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * ----------------------------------------------------------------------------  * "THE BEER-WARE LICENSE" (Revision 42):  *<phk@login.dknet.dk> wrote this file.  As long as you retain this notice you  * can do whatever you want with this stuff. If we meet some day, and you think  * this stuff is worth it, you can buy me a beer in return.   Poul-Henning Kamp  * ----------------------------------------------------------------------------  *  * $FreeBSD$  *  */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|LIBC_SCCS
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|lint
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|char
name|rcsid
index|[]
init|=
literal|"$FreeBSD$"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBC_SCCS and not lint */
end_comment

begin_comment
comment|/*  * This file contains four procedures used to read config-files.  *  * char * config_open(const char *filename,int contlines)  *	Will open the named file, read it into a private malloc'ed area,  *	and close the file again.  *	All lines where the first !isspace() char is '#' are deleted.  *	If contlines are non-zero lines where the first char is isspace()  *	will be joined to the preceeding line.  *	In case of trouble the name of the offending system call will be  *	returned.  On success NULL is returned.  *  * void   config_close()  *	This will free the internal malloc'ed area.  *  * char * config_next()  *	This will return a pointer to the next entry in the area.  NULL is  *	returned at "end of file".  The return value is '\0' terminated, and  *	can be modified, but the contents must be copied somewhere else for  *	permanent use.  *  * char * config_skip(char **p)  *	This will pick out the next word from the string.  The return-value  *	points to the word found, and *p is advanced past the word.  NULL is  *	returned at "end of string".  *  * Many programs have a n*100 bytes config-file and N*1000 bytes of source  * to read it.  Doing pointer-aerobics on files that small is a waste of  * time, and bashing around with getchar/ungetc isn't much better.  These  * routines implement a simple algorithm and syntax.  *  * config_skip consider a contiguous string of !isspace() chars a word.  *  * 13nov1994 Poul-Henning Kamp  phk@login.dknet.dk  */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<fcntl.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_decl_stmt
specifier|static
name|char
modifier|*
name|file_buf
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|char
modifier|*
name|ptr
decl_stmt|;
end_decl_stmt

begin_function
name|char
modifier|*
name|config_open
parameter_list|(
specifier|const
name|char
modifier|*
name|filename
parameter_list|,
name|int
name|contlines
parameter_list|)
block|{
name|int
name|fd
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|,
modifier|*
name|q
decl_stmt|;
if|if
condition|(
operator|(
name|fd
operator|=
name|open
argument_list|(
name|filename
argument_list|,
name|O_RDONLY
argument_list|)
operator|)
operator|<
literal|0
condition|)
return|return
literal|"open"
return|;
if|if
condition|(
name|fstat
argument_list|(
name|fd
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
block|{
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
literal|"fstat"
return|;
block|}
if|if
condition|(
name|file_buf
condition|)
name|free
argument_list|(
name|file_buf
argument_list|)
expr_stmt|;
name|file_buf
operator|=
name|malloc
argument_list|(
name|st
operator|.
name|st_size
operator|+
literal|2
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|file_buf
condition|)
block|{
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
literal|"malloc"
return|;
block|}
if|if
condition|(
name|st
operator|.
name|st_size
operator|!=
name|read
argument_list|(
name|fd
argument_list|,
name|file_buf
argument_list|,
name|st
operator|.
name|st_size
argument_list|)
condition|)
block|{
name|free
argument_list|(
name|file_buf
argument_list|)
expr_stmt|;
name|file_buf
operator|=
operator|(
name|char
operator|*
operator|)
literal|0
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
literal|"read"
return|;
block|}
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
name|file_buf
index|[
name|st
operator|.
name|st_size
index|]
operator|=
literal|'\n'
expr_stmt|;
name|file_buf
index|[
name|st
operator|.
name|st_size
operator|+
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/*          * /^[ \t]*#[^\n]*$/d 	 * 	 * Delete all lines where the first !isspace() char is '#'          */
name|ptr
operator|=
name|file_buf
expr_stmt|;
for|for
control|(
name|p
operator|=
name|ptr
init|;
operator|*
name|p
condition|;
control|)
block|{
for|for
control|(
name|q
operator|=
name|p
init|;
operator|*
name|q
operator|!=
literal|'\n'
operator|&&
name|isspace
argument_list|(
operator|*
name|q
argument_list|)
condition|;
name|q
operator|++
control|)
continue|continue;
if|if
condition|(
operator|*
name|q
operator|==
literal|'#'
condition|)
block|{
name|p
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
condition|)
name|p
operator|++
expr_stmt|;
block|}
else|else
block|{
name|q
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
name|q
operator|++
expr_stmt|;
name|memcpy
argument_list|(
name|ptr
argument_list|,
name|p
argument_list|,
name|q
operator|-
name|p
argument_list|)
expr_stmt|;
name|ptr
operator|+=
name|q
operator|-
name|p
expr_stmt|;
name|p
operator|=
name|q
expr_stmt|;
block|}
block|}
operator|*
name|ptr
operator|=
literal|'\0'
expr_stmt|;
name|ptr
operator|=
name|file_buf
expr_stmt|;
if|if
condition|(
operator|!
name|contlines
condition|)
return|return
literal|0
return|;
comment|/* Join all lines starting with a isspace() char to the preceeding 	 * line */
for|for
control|(
name|p
operator|=
name|ptr
init|;
operator|*
name|p
condition|;
control|)
block|{
name|q
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|isspace
argument_list|(
operator|*
operator|(
name|q
operator|+
literal|1
operator|)
argument_list|)
condition|)
operator|*
name|q
operator|=
literal|' '
expr_stmt|;
name|p
operator|=
name|q
operator|+
literal|1
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_function
name|void
name|config_close
parameter_list|(
name|void
parameter_list|)
block|{
if|if
condition|(
name|file_buf
condition|)
name|free
argument_list|(
name|file_buf
argument_list|)
expr_stmt|;
name|ptr
operator|=
name|file_buf
operator|=
literal|0
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Get next entry.  config_open did all the weird stuff, so just return  * the next line.  */
end_comment

begin_function
name|char
modifier|*
name|config_next
parameter_list|(
name|void
parameter_list|)
block|{
name|char
modifier|*
name|p
decl_stmt|;
comment|/* We might be done already ! */
if|if
condition|(
operator|!
name|ptr
operator|||
operator|!
operator|*
name|ptr
condition|)
return|return
literal|0
return|;
while|while
condition|(
name|isspace
argument_list|(
operator|*
name|ptr
argument_list|)
condition|)
name|ptr
operator|++
expr_stmt|;
name|p
operator|=
name|ptr
expr_stmt|;
name|ptr
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|'\n'
argument_list|)
expr_stmt|;
if|if
condition|(
name|ptr
condition|)
block|{
operator|*
name|ptr
operator|=
literal|'\0'
expr_stmt|;
name|ptr
operator|++
expr_stmt|;
block|}
return|return
name|p
return|;
block|}
end_function

begin_comment
comment|/*  * Return next word  */
end_comment

begin_function
name|char
modifier|*
name|config_skip
parameter_list|(
name|char
modifier|*
modifier|*
name|p
parameter_list|)
block|{
name|char
modifier|*
name|q
decl_stmt|,
modifier|*
name|r
decl_stmt|;
if|if
condition|(
operator|!
operator|*
name|p
operator|||
operator|!
operator|*
operator|*
name|p
condition|)
return|return
literal|0
return|;
for|for
control|(
name|q
operator|=
operator|*
name|p
init|;
name|isspace
argument_list|(
operator|*
name|q
argument_list|)
condition|;
name|q
operator|++
control|)
empty_stmt|;
if|if
condition|(
operator|!
operator|*
name|q
condition|)
return|return
literal|0
return|;
for|for
control|(
name|r
operator|=
name|q
init|;
operator|*
name|r
operator|&&
operator|!
name|isspace
argument_list|(
operator|*
name|r
argument_list|)
condition|;
name|r
operator|++
control|)
empty_stmt|;
if|if
condition|(
operator|*
name|r
condition|)
operator|*
name|r
operator|++
operator|=
literal|'\0'
expr_stmt|;
operator|*
name|p
operator|=
name|r
expr_stmt|;
return|return
name|q
return|;
block|}
end_function

end_unit

