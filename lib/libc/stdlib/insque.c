begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Initial implementation:  * Copyright (c) 2002 Robert Drehmel  * All rights reserved.  *  * As long as the above copyright statement and this notice remain  * unchanged, you can do what ever you want with this file.   */
end_comment

begin_include
include|#
directive|include
file|<sys/cdefs.h>
end_include

begin_expr_stmt
name|__FBSDID
argument_list|(
literal|"$FreeBSD$"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_define
define|#
directive|define
name|_SEARCH_PRIVATE
end_define

begin_include
include|#
directive|include
file|<search.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_comment
comment|/* for NULL */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_function
name|void
name|insque
parameter_list|(
name|void
modifier|*
name|element
parameter_list|,
name|void
modifier|*
name|pred
parameter_list|)
block|{
name|struct
name|que_elem
modifier|*
name|prev
decl_stmt|,
modifier|*
name|next
decl_stmt|,
modifier|*
name|elem
decl_stmt|;
name|elem
operator|=
operator|(
expr|struct
name|que_elem
operator|*
operator|)
name|element
expr_stmt|;
name|prev
operator|=
operator|(
expr|struct
name|que_elem
operator|*
operator|)
name|pred
expr_stmt|;
if|if
condition|(
name|prev
operator|==
name|NULL
condition|)
block|{
name|elem
operator|->
name|prev
operator|=
name|elem
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
return|return;
block|}
name|next
operator|=
name|prev
operator|->
name|next
expr_stmt|;
if|if
condition|(
name|next
operator|!=
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|next
operator|->
name|prev
operator|!=
name|prev
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"insque: Inconsistency detected:"
literal|" next(%p)->prev(%p) != prev(%p)\n"
argument_list|,
name|next
argument_list|,
name|next
operator|->
name|prev
argument_list|,
name|prev
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
name|next
operator|->
name|prev
operator|=
name|elem
expr_stmt|;
block|}
name|prev
operator|->
name|next
operator|=
name|elem
expr_stmt|;
name|elem
operator|->
name|prev
operator|=
name|prev
expr_stmt|;
name|elem
operator|->
name|next
operator|=
name|next
expr_stmt|;
block|}
end_function

end_unit

