begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	$NetBSD: ldexp.c,v 1.1 1995/02/10 17:50:24 cgd Exp $	*/
end_comment

begin_comment
comment|/* $FreeBSD$ */
end_comment

begin_comment
comment|/*  * Copyright (c) 1994, 1995 Carnegie-Mellon University.  * All rights reserved.  *  * Author: Chris G. Demetriou  *   * Permission to use, copy, modify and distribute this software and  * its documentation is hereby granted, provided that both the copyright  * notice and this permission notice appear in all copies of the  * software, derivative works or modified versions, and any portions  * thereof, and that both notices appear in supporting documentation.  *   * CARNEGIE MELLON ALLOWS FREE USE OF THIS SOFTWARE IN ITS "AS IS"   * CONDITION.  CARNEGIE MELLON DISCLAIMS ANY LIABILITY OF ANY KIND   * FOR ANY DAMAGES WHATSOEVER RESULTING FROM THE USE OF THIS SOFTWARE.  *   * Carnegie Mellon requests users of this software to return to  *  *  Software Distribution Coordinator  or  Software.Distribution@CS.CMU.EDU  *  School of Computer Science  *  Carnegie Mellon University  *  Pittsburgh PA 15213-3890  *  * any improvements or extensions that they make and grant Carnegie the  * rights to redistribute these changes.  */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<machine/ieee.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<math.h>
end_include

begin_comment
comment|/*  * double ldexp(double val, int exp)  * returns: val * (2**exp)  */
end_comment

begin_function
name|double
name|ldexp
parameter_list|(
name|val
parameter_list|,
name|exp
parameter_list|)
name|double
name|val
decl_stmt|;
name|int
name|exp
decl_stmt|;
block|{
name|int
name|oldexp
decl_stmt|,
name|newexp
decl_stmt|,
name|mulexp
decl_stmt|;
union|union
name|doub
block|{
name|double
name|v
decl_stmt|;
name|struct
name|ieee_double
name|s
decl_stmt|;
block|}
name|u
union|,
name|mul
union|;
comment|/* 	 * If input is zero, or no change, just return input. 	 * Likewise, if input is Inf or NaN, just return it. 	 */
name|u
operator|.
name|v
operator|=
name|val
expr_stmt|;
name|oldexp
operator|=
name|u
operator|.
name|s
operator|.
name|dbl_exp
expr_stmt|;
if|if
condition|(
name|val
operator|==
literal|0
operator|||
name|exp
operator|==
literal|0
operator|||
name|oldexp
operator|==
name|DBL_EXP_INFNAN
condition|)
return|return
operator|(
name|val
operator|)
return|;
comment|/* 	 * Compute new exponent and check for over/under flow. 	 * Underflow, unfortunately, could mean switching to denormal. 	 * If result out of range, set ERANGE and return 0 if too small 	 * or Inf if too big, with the same sign as the input value. 	 */
name|newexp
operator|=
name|oldexp
operator|+
name|exp
expr_stmt|;
if|if
condition|(
name|newexp
operator|>=
name|DBL_EXP_INFNAN
condition|)
block|{
comment|/* u.s.dbl_sign = val< 0; -- already set */
name|u
operator|.
name|s
operator|.
name|dbl_exp
operator|=
name|DBL_EXP_INFNAN
expr_stmt|;
name|u
operator|.
name|s
operator|.
name|dbl_frach
operator|=
name|u
operator|.
name|s
operator|.
name|dbl_fracl
operator|=
literal|0
expr_stmt|;
name|errno
operator|=
name|ERANGE
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|v
operator|)
return|;
comment|/* Inf */
block|}
if|if
condition|(
name|newexp
operator|<=
literal|0
condition|)
block|{
comment|/* 		 * The output number is either a denormal or underflows 		 * (see comments in machine/ieee.h). 		 */
if|if
condition|(
name|newexp
operator|<=
operator|-
name|DBL_FRACBITS
condition|)
block|{
comment|/* u.s.dbl_sign = val< 0; -- already set */
name|u
operator|.
name|s
operator|.
name|dbl_exp
operator|=
literal|0
expr_stmt|;
name|u
operator|.
name|s
operator|.
name|dbl_frach
operator|=
name|u
operator|.
name|s
operator|.
name|dbl_fracl
operator|=
literal|0
expr_stmt|;
name|errno
operator|=
name|ERANGE
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|v
operator|)
return|;
comment|/* zero */
block|}
comment|/* 		 * We are going to produce a denorm.  Our `exp' argument 		 * might be as small as -2097, and we cannot compute 		 * 2^-2097, so we may have to do this as many as three 		 * steps (not just two, as for positive `exp's below). 		 */
name|mul
operator|.
name|v
operator|=
literal|0
expr_stmt|;
while|while
condition|(
name|exp
operator|<=
operator|-
name|DBL_EXP_BIAS
condition|)
block|{
name|mul
operator|.
name|s
operator|.
name|dbl_exp
operator|=
literal|1
expr_stmt|;
name|val
operator|*=
name|mul
operator|.
name|v
expr_stmt|;
name|exp
operator|+=
name|DBL_EXP_BIAS
operator|-
literal|1
expr_stmt|;
block|}
name|mul
operator|.
name|s
operator|.
name|dbl_exp
operator|=
name|exp
operator|+
name|DBL_EXP_BIAS
expr_stmt|;
name|val
operator|*=
name|mul
operator|.
name|v
expr_stmt|;
return|return
operator|(
name|val
operator|)
return|;
block|}
comment|/* 	 * Newexp is positive. 	 * 	 * If oldexp is zero, we are starting with a denorm, and simply 	 * adjusting the exponent will produce bogus answers.  We need 	 * to fix that first. 	 */
if|if
condition|(
name|oldexp
operator|==
literal|0
condition|)
block|{
comment|/* 		 * Multiply by 2^mulexp to make the number normalizable. 		 * We cannot multiply by more than 2^1023, but `exp' 		 * argument might be as large as 2046.  A single 		 * adjustment, however, will normalize the number even 		 * for huge `exp's, and then we can use exponent 		 * arithmetic just as for normal `double's. 		 */
name|mulexp
operator|=
name|exp
operator|<=
name|DBL_EXP_BIAS
condition|?
name|exp
else|:
name|DBL_EXP_BIAS
expr_stmt|;
name|mul
operator|.
name|v
operator|=
literal|0
expr_stmt|;
name|mul
operator|.
name|s
operator|.
name|dbl_exp
operator|=
name|mulexp
operator|+
name|DBL_EXP_BIAS
expr_stmt|;
name|val
operator|*=
name|mul
operator|.
name|v
expr_stmt|;
if|if
condition|(
name|mulexp
operator|==
name|exp
condition|)
return|return
operator|(
name|val
operator|)
return|;
name|u
operator|.
name|v
operator|=
name|val
expr_stmt|;
name|newexp
operator|-=
name|mulexp
expr_stmt|;
block|}
comment|/* 	 * Both oldexp and newexp are positive; just replace the 	 * old exponent with the new one. 	 */
name|u
operator|.
name|s
operator|.
name|dbl_exp
operator|=
name|newexp
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|v
operator|)
return|;
block|}
end_function

end_unit

