begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* This work is copyrighted. See COPYRIGHT.OLD& COPYRIGHT.NEW for   * *  details. If they are missing then this copy is in violation of    * *  the copyright conditions.                                        */
end_comment

begin_comment
comment|/* **	lib_tstp.c ** **	The routine tstp(). ** */
end_comment

begin_include
include|#
directive|include
file|"curses.priv.h"
end_include

begin_include
include|#
directive|include
file|"terminfo.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|SVR4_ACTION
end_ifdef

begin_define
define|#
directive|define
name|_POSIX_SOURCE
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_function
name|void
name|tstp
parameter_list|(
name|int
name|dummy
parameter_list|)
block|{
name|sigaction_t
name|act
decl_stmt|,
name|oact
decl_stmt|;
name|sigset_t
name|mask
decl_stmt|,
name|omask
decl_stmt|;
name|T
argument_list|(
operator|(
literal|"tstp() called"
operator|)
argument_list|)
expr_stmt|;
comment|/* 	 * The user may have changed the prog_mode tty bits, so save them. 	 */
name|def_prog_mode
argument_list|()
expr_stmt|;
comment|/* 	 * Block window change and timer signals.  The latter 	 * is because applications use timers to decide when 	 * to repaint the screen. 	 */
operator|(
name|void
operator|)
name|sigemptyset
argument_list|(
operator|&
name|mask
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigaddset
argument_list|(
operator|&
name|mask
argument_list|,
name|SIGALRM
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SIGWINCH
operator|(
name|void
operator|)
name|sigaddset
argument_list|(
operator|&
name|mask
argument_list|,
name|SIGWINCH
argument_list|)
expr_stmt|;
endif|#
directive|endif
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_BLOCK
argument_list|,
operator|&
name|mask
argument_list|,
operator|&
name|omask
argument_list|)
expr_stmt|;
name|endwin
argument_list|()
expr_stmt|;
name|sigemptyset
argument_list|(
operator|&
name|mask
argument_list|)
expr_stmt|;
name|sigaddset
argument_list|(
operator|&
name|mask
argument_list|,
name|SIGTSTP
argument_list|)
expr_stmt|;
name|sigprocmask
argument_list|(
name|SIG_UNBLOCK
argument_list|,
operator|&
name|mask
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|act
operator|.
name|sa_handler
operator|=
name|SIG_DFL
expr_stmt|;
name|sigemptyset
argument_list|(
operator|&
name|act
operator|.
name|sa_mask
argument_list|)
expr_stmt|;
name|act
operator|.
name|sa_flags
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|SA_RESTART
name|act
operator|.
name|sa_flags
operator||=
name|SA_RESTART
expr_stmt|;
endif|#
directive|endif
name|sigaction
argument_list|(
name|SIGTSTP
argument_list|,
operator|&
name|act
argument_list|,
operator|&
name|oact
argument_list|)
expr_stmt|;
name|kill
argument_list|(
name|getpid
argument_list|()
argument_list|,
name|SIGTSTP
argument_list|)
expr_stmt|;
name|T
argument_list|(
operator|(
literal|"SIGCONT received"
operator|)
argument_list|)
expr_stmt|;
name|sigaction
argument_list|(
name|SIGTSTP
argument_list|,
operator|&
name|oact
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|flushinp
argument_list|()
expr_stmt|;
comment|/* 	 * If the user modified the tty state while suspended, he wants 	 * those changes to stick.  So save the new "default" terminal state. 	 */
name|def_shell_mode
argument_list|()
expr_stmt|;
comment|/* 	 * This relies on the fact that doupdate() will restore the 	 * program-mode tty state, and issue enter_ca_mode if need be. 	 */
name|doupdate
argument_list|()
expr_stmt|;
comment|/* Reset the signals. */
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_SETMASK
argument_list|,
operator|&
name|omask
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

