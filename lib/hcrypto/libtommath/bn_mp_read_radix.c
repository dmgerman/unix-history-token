begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<tommath.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|BN_MP_READ_RADIX_C
end_ifdef

begin_comment
comment|/* LibTomMath, multiple-precision integer library -- Tom St Denis  *  * LibTomMath is a library that provides multiple-precision  * integer arithmetic as well as number theoretic functionality.  *  * The library was designed directly after the MPI library by  * Michael Fromberger but has been written from scratch with  * additional optimizations in place.  *  * The library is free for all purposes without any express  * guarantee it works.  *  * Tom St Denis, tomstdenis@gmail.com, http://libtom.org  */
end_comment

begin_comment
comment|/* read a string [ASCII] in a given radix */
end_comment

begin_function
name|int
name|mp_read_radix
parameter_list|(
name|mp_int
modifier|*
name|a
parameter_list|,
specifier|const
name|char
modifier|*
name|str
parameter_list|,
name|int
name|radix
parameter_list|)
block|{
name|int
name|y
decl_stmt|,
name|res
decl_stmt|,
name|neg
decl_stmt|;
name|char
name|ch
decl_stmt|;
comment|/* zero the digit bignum */
name|mp_zero
argument_list|(
name|a
argument_list|)
expr_stmt|;
comment|/* make sure the radix is ok */
if|if
condition|(
name|radix
operator|<
literal|2
operator|||
name|radix
operator|>
literal|64
condition|)
block|{
return|return
name|MP_VAL
return|;
block|}
comment|/* if the leading digit is a    * minus set the sign to negative.    */
if|if
condition|(
operator|*
name|str
operator|==
literal|'-'
condition|)
block|{
operator|++
name|str
expr_stmt|;
name|neg
operator|=
name|MP_NEG
expr_stmt|;
block|}
else|else
block|{
name|neg
operator|=
name|MP_ZPOS
expr_stmt|;
block|}
comment|/* set the integer to the default of zero */
name|mp_zero
argument_list|(
name|a
argument_list|)
expr_stmt|;
comment|/* process each digit of the string */
while|while
condition|(
operator|*
name|str
condition|)
block|{
comment|/* if the radix< 36 the conversion is case insensitive      * this allows numbers like 1AB and 1ab to represent the same  value      * [e.g. in hex]      */
name|ch
operator|=
call|(
name|char
call|)
argument_list|(
operator|(
name|radix
operator|<
literal|36
operator|)
condition|?
name|toupper
argument_list|(
operator|*
name|str
argument_list|)
else|:
operator|*
name|str
argument_list|)
expr_stmt|;
for|for
control|(
name|y
operator|=
literal|0
init|;
name|y
operator|<
literal|64
condition|;
name|y
operator|++
control|)
block|{
if|if
condition|(
name|ch
operator|==
name|mp_s_rmap
index|[
name|y
index|]
condition|)
block|{
break|break;
block|}
block|}
comment|/* if the char was found in the map      * and is less than the given radix add it      * to the number, otherwise exit the loop.      */
if|if
condition|(
name|y
operator|<
name|radix
condition|)
block|{
if|if
condition|(
operator|(
name|res
operator|=
name|mp_mul_d
argument_list|(
name|a
argument_list|,
operator|(
name|mp_digit
operator|)
name|radix
argument_list|,
name|a
argument_list|)
operator|)
operator|!=
name|MP_OKAY
condition|)
block|{
return|return
name|res
return|;
block|}
if|if
condition|(
operator|(
name|res
operator|=
name|mp_add_d
argument_list|(
name|a
argument_list|,
operator|(
name|mp_digit
operator|)
name|y
argument_list|,
name|a
argument_list|)
operator|)
operator|!=
name|MP_OKAY
condition|)
block|{
return|return
name|res
return|;
block|}
block|}
else|else
block|{
break|break;
block|}
operator|++
name|str
expr_stmt|;
block|}
comment|/* set the sign only if a != 0 */
if|if
condition|(
name|mp_iszero
argument_list|(
name|a
argument_list|)
operator|!=
literal|1
condition|)
block|{
name|a
operator|->
name|sign
operator|=
name|neg
expr_stmt|;
block|}
return|return
name|MP_OKAY
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* $Source: /cvs/libtom/libtommath/bn_mp_read_radix.c,v $ */
end_comment

begin_comment
comment|/* $Revision: 1.5 $ */
end_comment

begin_comment
comment|/* $Date: 2006/12/28 01:25:13 $ */
end_comment

end_unit

