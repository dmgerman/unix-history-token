begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|///////////////////////////////////////////////////////////////////////////////
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|/// \file       util.c
end_comment

begin_comment
comment|/// \brief      Miscellaneous utility functions
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|//  Author:     Lasse Collin
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|//  This file has been put into the public domain.
end_comment

begin_comment
comment|//  You can do whatever you want with this file.
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|///////////////////////////////////////////////////////////////////////////////
end_comment

begin_include
include|#
directive|include
file|"private.h"
end_include

begin_include
include|#
directive|include
file|<stdarg.h>
end_include

begin_comment
comment|/// Buffers for uint64_to_str() and uint64_to_nicestr()
end_comment

begin_decl_stmt
specifier|static
name|char
name|bufs
index|[
literal|4
index|]
index|[
literal|128
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/// Thousand separator support in uint64_to_str() and uint64_to_nicestr()
end_comment

begin_enum
specifier|static
enum|enum
block|{
name|UNKNOWN
block|,
name|WORKS
block|,
name|BROKEN
block|}
name|thousand
init|=
name|UNKNOWN
enum|;
end_enum

begin_function
specifier|extern
name|void
modifier|*
name|xrealloc
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
name|assert
argument_list|(
name|size
operator|>
literal|0
argument_list|)
expr_stmt|;
comment|// Save ptr so that we can free it if realloc fails.
comment|// The point is that message_fatal ends up calling stdio functions
comment|// which in some libc implementations might allocate memory from
comment|// the heap. Freeing ptr improves the chances that there's free
comment|// memory for stdio functions if they need it.
name|void
modifier|*
name|p
init|=
name|ptr
decl_stmt|;
name|ptr
operator|=
name|realloc
argument_list|(
name|ptr
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|ptr
operator|==
name|NULL
condition|)
block|{
specifier|const
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
name|free
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|message_fatal
argument_list|(
literal|"%s"
argument_list|,
name|strerror
argument_list|(
name|saved_errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
name|ptr
return|;
block|}
end_function

begin_function
specifier|extern
name|char
modifier|*
name|xstrdup
parameter_list|(
specifier|const
name|char
modifier|*
name|src
parameter_list|)
block|{
name|assert
argument_list|(
name|src
operator|!=
name|NULL
argument_list|)
expr_stmt|;
specifier|const
name|size_t
name|size
init|=
name|strlen
argument_list|(
name|src
argument_list|)
operator|+
literal|1
decl_stmt|;
name|char
modifier|*
name|dest
init|=
name|xmalloc
argument_list|(
name|size
argument_list|)
decl_stmt|;
return|return
name|memcpy
argument_list|(
name|dest
argument_list|,
name|src
argument_list|,
name|size
argument_list|)
return|;
block|}
end_function

begin_function
specifier|extern
name|uint64_t
name|str_to_uint64
parameter_list|(
specifier|const
name|char
modifier|*
name|name
parameter_list|,
specifier|const
name|char
modifier|*
name|value
parameter_list|,
name|uint64_t
name|min
parameter_list|,
name|uint64_t
name|max
parameter_list|)
block|{
name|uint64_t
name|result
init|=
literal|0
decl_stmt|;
comment|// Skip blanks.
while|while
condition|(
operator|*
name|value
operator|==
literal|' '
operator|||
operator|*
name|value
operator|==
literal|'\t'
condition|)
operator|++
name|value
expr_stmt|;
comment|// Accept special value "max". Supporting "min" doesn't seem useful.
if|if
condition|(
name|strcmp
argument_list|(
name|value
argument_list|,
literal|"max"
argument_list|)
operator|==
literal|0
condition|)
return|return
name|max
return|;
if|if
condition|(
operator|*
name|value
operator|<
literal|'0'
operator|||
operator|*
name|value
operator|>
literal|'9'
condition|)
name|message_fatal
argument_list|(
name|_
argument_list|(
literal|"%s: Value is not a non-negative "
literal|"decimal integer"
argument_list|)
argument_list|,
name|value
argument_list|)
expr_stmt|;
do|do
block|{
comment|// Don't overflow.
if|if
condition|(
name|result
operator|>
name|UINT64_MAX
operator|/
literal|10
condition|)
goto|goto
name|error
goto|;
name|result
operator|*=
literal|10
expr_stmt|;
comment|// Another overflow check
specifier|const
name|uint32_t
name|add
init|=
operator|*
name|value
operator|-
literal|'0'
decl_stmt|;
if|if
condition|(
name|UINT64_MAX
operator|-
name|add
operator|<
name|result
condition|)
goto|goto
name|error
goto|;
name|result
operator|+=
name|add
expr_stmt|;
operator|++
name|value
expr_stmt|;
block|}
do|while
condition|(
operator|*
name|value
operator|>=
literal|'0'
operator|&&
operator|*
name|value
operator|<=
literal|'9'
condition|)
do|;
if|if
condition|(
operator|*
name|value
operator|!=
literal|'\0'
condition|)
block|{
comment|// Look for suffix. Originally this supported both base-2
comment|// and base-10, but since there seems to be little need
comment|// for base-10 in this program, treat everything as base-2
comment|// and also be more relaxed about the case of the first
comment|// letter of the suffix.
name|uint64_t
name|multiplier
init|=
literal|0
decl_stmt|;
if|if
condition|(
operator|*
name|value
operator|==
literal|'k'
operator|||
operator|*
name|value
operator|==
literal|'K'
condition|)
name|multiplier
operator|=
name|UINT64_C
argument_list|(
literal|1
argument_list|)
operator|<<
literal|10
expr_stmt|;
elseif|else
if|if
condition|(
operator|*
name|value
operator|==
literal|'m'
operator|||
operator|*
name|value
operator|==
literal|'M'
condition|)
name|multiplier
operator|=
name|UINT64_C
argument_list|(
literal|1
argument_list|)
operator|<<
literal|20
expr_stmt|;
elseif|else
if|if
condition|(
operator|*
name|value
operator|==
literal|'g'
operator|||
operator|*
name|value
operator|==
literal|'G'
condition|)
name|multiplier
operator|=
name|UINT64_C
argument_list|(
literal|1
argument_list|)
operator|<<
literal|30
expr_stmt|;
operator|++
name|value
expr_stmt|;
comment|// Allow also e.g. Ki, KiB, and KB.
if|if
condition|(
operator|*
name|value
operator|!=
literal|'\0'
operator|&&
name|strcmp
argument_list|(
name|value
argument_list|,
literal|"i"
argument_list|)
operator|!=
literal|0
operator|&&
name|strcmp
argument_list|(
name|value
argument_list|,
literal|"iB"
argument_list|)
operator|!=
literal|0
operator|&&
name|strcmp
argument_list|(
name|value
argument_list|,
literal|"B"
argument_list|)
operator|!=
literal|0
condition|)
name|multiplier
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|multiplier
operator|==
literal|0
condition|)
block|{
name|message
argument_list|(
name|V_ERROR
argument_list|,
name|_
argument_list|(
literal|"%s: Invalid multiplier suffix"
argument_list|)
argument_list|,
name|value
operator|-
literal|1
argument_list|)
expr_stmt|;
name|message_fatal
argument_list|(
name|_
argument_list|(
literal|"Valid suffixes are `KiB' (2^10), "
literal|"`MiB' (2^20), and `GiB' (2^30)."
argument_list|)
argument_list|)
expr_stmt|;
block|}
comment|// Don't overflow here either.
if|if
condition|(
name|result
operator|>
name|UINT64_MAX
operator|/
name|multiplier
condition|)
goto|goto
name|error
goto|;
name|result
operator|*=
name|multiplier
expr_stmt|;
block|}
if|if
condition|(
name|result
operator|<
name|min
operator|||
name|result
operator|>
name|max
condition|)
goto|goto
name|error
goto|;
return|return
name|result
return|;
name|error
label|:
name|message_fatal
argument_list|(
name|_
argument_list|(
literal|"Value of the option `%s' must be in the range "
literal|"[%"
name|PRIu64
literal|", %"
name|PRIu64
literal|"]"
argument_list|)
argument_list|,
name|name
argument_list|,
name|min
argument_list|,
name|max
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
specifier|extern
name|uint64_t
name|round_up_to_mib
parameter_list|(
name|uint64_t
name|n
parameter_list|)
block|{
return|return
operator|(
name|n
operator|>>
literal|20
operator|)
operator|+
operator|(
operator|(
name|n
operator|&
operator|(
operator|(
name|UINT32_C
argument_list|(
literal|1
argument_list|)
operator|<<
literal|20
operator|)
operator|-
literal|1
operator|)
operator|)
operator|!=
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/// Check if thousand separator is supported. Run-time checking is easiest,
end_comment

begin_comment
comment|/// because it seems to be sometimes lacking even on POSIXish system.
end_comment

begin_function
specifier|static
name|void
name|check_thousand_sep
parameter_list|(
name|uint32_t
name|slot
parameter_list|)
block|{
if|if
condition|(
name|thousand
operator|==
name|UNKNOWN
condition|)
block|{
name|bufs
index|[
name|slot
index|]
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
name|snprintf
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|)
argument_list|,
literal|"%'u"
argument_list|,
literal|1U
argument_list|)
expr_stmt|;
name|thousand
operator|=
name|bufs
index|[
name|slot
index|]
index|[
literal|0
index|]
operator|==
literal|'1'
condition|?
name|WORKS
else|:
name|BROKEN
expr_stmt|;
block|}
return|return;
block|}
end_function

begin_function
specifier|extern
specifier|const
name|char
modifier|*
name|uint64_to_str
parameter_list|(
name|uint64_t
name|value
parameter_list|,
name|uint32_t
name|slot
parameter_list|)
block|{
name|assert
argument_list|(
name|slot
operator|<
name|ARRAY_SIZE
argument_list|(
name|bufs
argument_list|)
argument_list|)
expr_stmt|;
name|check_thousand_sep
argument_list|(
name|slot
argument_list|)
expr_stmt|;
if|if
condition|(
name|thousand
operator|==
name|WORKS
condition|)
name|snprintf
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|)
argument_list|,
literal|"%'"
name|PRIu64
argument_list|,
name|value
argument_list|)
expr_stmt|;
else|else
name|snprintf
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|)
argument_list|,
literal|"%"
name|PRIu64
argument_list|,
name|value
argument_list|)
expr_stmt|;
return|return
name|bufs
index|[
name|slot
index|]
return|;
block|}
end_function

begin_function
specifier|extern
specifier|const
name|char
modifier|*
name|uint64_to_nicestr
parameter_list|(
name|uint64_t
name|value
parameter_list|,
name|enum
name|nicestr_unit
name|unit_min
parameter_list|,
name|enum
name|nicestr_unit
name|unit_max
parameter_list|,
name|bool
name|always_also_bytes
parameter_list|,
name|uint32_t
name|slot
parameter_list|)
block|{
name|assert
argument_list|(
name|unit_min
operator|<=
name|unit_max
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|unit_max
operator|<=
name|NICESTR_TIB
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|slot
operator|<
name|ARRAY_SIZE
argument_list|(
name|bufs
argument_list|)
argument_list|)
expr_stmt|;
name|check_thousand_sep
argument_list|(
name|slot
argument_list|)
expr_stmt|;
name|enum
name|nicestr_unit
name|unit
init|=
name|NICESTR_B
decl_stmt|;
name|char
modifier|*
name|pos
init|=
name|bufs
index|[
name|slot
index|]
decl_stmt|;
name|size_t
name|left
init|=
sizeof|sizeof
argument_list|(
name|bufs
index|[
name|slot
index|]
argument_list|)
decl_stmt|;
if|if
condition|(
operator|(
name|unit_min
operator|==
name|NICESTR_B
operator|&&
name|value
operator|<
literal|10000
operator|)
operator|||
name|unit_max
operator|==
name|NICESTR_B
condition|)
block|{
comment|// The value is shown as bytes.
if|if
condition|(
name|thousand
operator|==
name|WORKS
condition|)
name|my_snprintf
argument_list|(
operator|&
name|pos
argument_list|,
operator|&
name|left
argument_list|,
literal|"%'u"
argument_list|,
operator|(
name|unsigned
name|int
operator|)
name|value
argument_list|)
expr_stmt|;
else|else
name|my_snprintf
argument_list|(
operator|&
name|pos
argument_list|,
operator|&
name|left
argument_list|,
literal|"%u"
argument_list|,
operator|(
name|unsigned
name|int
operator|)
name|value
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|// Scale the value to a nicer unit. Unless unit_min and
comment|// unit_max limit us, we will show at most five significant
comment|// digits with one decimal place.
name|double
name|d
init|=
call|(
name|double
call|)
argument_list|(
name|value
argument_list|)
decl_stmt|;
do|do
block|{
name|d
operator|/=
literal|1024.0
expr_stmt|;
operator|++
name|unit
expr_stmt|;
block|}
do|while
condition|(
name|unit
operator|<
name|unit_min
operator|||
operator|(
name|d
operator|>
literal|9999.9
operator|&&
name|unit
operator|<
name|unit_max
operator|)
condition|)
do|;
if|if
condition|(
name|thousand
operator|==
name|WORKS
condition|)
name|my_snprintf
argument_list|(
operator|&
name|pos
argument_list|,
operator|&
name|left
argument_list|,
literal|"%'.1f"
argument_list|,
name|d
argument_list|)
expr_stmt|;
else|else
name|my_snprintf
argument_list|(
operator|&
name|pos
argument_list|,
operator|&
name|left
argument_list|,
literal|"%.1f"
argument_list|,
name|d
argument_list|)
expr_stmt|;
block|}
specifier|static
specifier|const
name|char
name|suffix
index|[
literal|5
index|]
index|[
literal|4
index|]
init|=
block|{
literal|"B"
block|,
literal|"KiB"
block|,
literal|"MiB"
block|,
literal|"GiB"
block|,
literal|"TiB"
block|}
decl_stmt|;
name|my_snprintf
argument_list|(
operator|&
name|pos
argument_list|,
operator|&
name|left
argument_list|,
literal|" %s"
argument_list|,
name|suffix
index|[
name|unit
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|always_also_bytes
operator|&&
name|value
operator|>=
literal|10000
condition|)
block|{
if|if
condition|(
name|thousand
operator|==
name|WORKS
condition|)
name|snprintf
argument_list|(
name|pos
argument_list|,
name|left
argument_list|,
literal|" (%'"
name|PRIu64
literal|" B)"
argument_list|,
name|value
argument_list|)
expr_stmt|;
else|else
name|snprintf
argument_list|(
name|pos
argument_list|,
name|left
argument_list|,
literal|" (%"
name|PRIu64
literal|" B)"
argument_list|,
name|value
argument_list|)
expr_stmt|;
block|}
return|return
name|bufs
index|[
name|slot
index|]
return|;
block|}
end_function

begin_function
specifier|extern
name|void
name|my_snprintf
parameter_list|(
name|char
modifier|*
modifier|*
name|pos
parameter_list|,
name|size_t
modifier|*
name|left
parameter_list|,
specifier|const
name|char
modifier|*
name|fmt
parameter_list|,
modifier|...
parameter_list|)
block|{
name|va_list
name|ap
decl_stmt|;
name|va_start
argument_list|(
name|ap
argument_list|,
name|fmt
argument_list|)
expr_stmt|;
specifier|const
name|int
name|len
init|=
name|vsnprintf
argument_list|(
operator|*
name|pos
argument_list|,
operator|*
name|left
argument_list|,
name|fmt
argument_list|,
name|ap
argument_list|)
decl_stmt|;
name|va_end
argument_list|(
name|ap
argument_list|)
expr_stmt|;
comment|// If an error occurred, we want the caller to think that the whole
comment|// buffer was used. This way no more data will be written to the
comment|// buffer. We don't need better error handling here, although it
comment|// is possible that the result looks garbage on the terminal if
comment|// e.g. an UTF-8 character gets split. That shouldn't (easily)
comment|// happen though, because the buffers used have some extra room.
if|if
condition|(
name|len
operator|<
literal|0
operator|||
call|(
name|size_t
call|)
argument_list|(
name|len
argument_list|)
operator|>=
operator|*
name|left
condition|)
block|{
operator|*
name|left
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
operator|*
name|pos
operator|+=
name|len
expr_stmt|;
operator|*
name|left
operator|-=
name|len
expr_stmt|;
block|}
return|return;
block|}
end_function

begin_function
specifier|extern
name|bool
name|is_empty_filename
parameter_list|(
specifier|const
name|char
modifier|*
name|filename
parameter_list|)
block|{
if|if
condition|(
name|filename
index|[
literal|0
index|]
operator|==
literal|'\0'
condition|)
block|{
name|message_error
argument_list|(
name|_
argument_list|(
literal|"Empty filename, skipping"
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|true
return|;
block|}
return|return
name|false
return|;
block|}
end_function

begin_function
specifier|extern
name|bool
name|is_tty_stdin
parameter_list|(
name|void
parameter_list|)
block|{
specifier|const
name|bool
name|ret
init|=
name|isatty
argument_list|(
name|STDIN_FILENO
argument_list|)
decl_stmt|;
if|if
condition|(
name|ret
condition|)
name|message_error
argument_list|(
name|_
argument_list|(
literal|"Compressed data cannot be read from "
literal|"a terminal"
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_function
specifier|extern
name|bool
name|is_tty_stdout
parameter_list|(
name|void
parameter_list|)
block|{
specifier|const
name|bool
name|ret
init|=
name|isatty
argument_list|(
name|STDOUT_FILENO
argument_list|)
decl_stmt|;
if|if
condition|(
name|ret
condition|)
name|message_error
argument_list|(
name|_
argument_list|(
literal|"Compressed data cannot be written to "
literal|"a terminal"
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

end_unit

