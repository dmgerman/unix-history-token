begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr_arch_threadproc.h"
end_include

begin_macro
name|APR_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_proc_detach
argument_list|(
argument|int daemonize
argument_list|)
end_macro

begin_block
block|{
if|if
condition|(
name|chdir
argument_list|(
literal|"/"
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
return|return
name|errno
return|;
block|}
if|#
directive|if
operator|!
name|defined
argument_list|(
name|MPE
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|OS2
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|TPF
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|BEOS
argument_list|)
comment|/* Don't detach for MPE because child processes can't survive the death of      * the parent. */
if|if
condition|(
name|daemonize
condition|)
block|{
name|int
name|x
decl_stmt|;
if|if
condition|(
operator|(
name|x
operator|=
name|fork
argument_list|()
operator|)
operator|>
literal|0
condition|)
block|{
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|x
operator|==
operator|-
literal|1
condition|)
block|{
name|perror
argument_list|(
literal|"fork"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"unable to fork new process\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
comment|/* we can't do anything here, so just exit. */
block|}
comment|/* RAISE_SIGSTOP(DETACH); */
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|HAVE_SETSID
comment|/* A setsid() failure is not fatal if we didn't just fork().      * The calling process may be the process group leader, in      * which case setsid() will fail with EPERM.      */
if|if
condition|(
name|setsid
argument_list|()
operator|==
operator|-
literal|1
operator|&&
name|daemonize
condition|)
block|{
return|return
name|errno
return|;
block|}
elif|#
directive|elif
name|defined
argument_list|(
name|NEXT
argument_list|)
operator|||
name|defined
argument_list|(
name|NEWSOS
argument_list|)
if|if
condition|(
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
return|return
name|errno
return|;
block|}
elif|#
directive|elif
name|defined
argument_list|(
name|OS2
argument_list|)
operator|||
name|defined
argument_list|(
name|TPF
argument_list|)
operator|||
name|defined
argument_list|(
name|MPE
argument_list|)
comment|/* do nothing */
else|#
directive|else
if|if
condition|(
name|setpgid
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
return|return
name|errno
return|;
block|}
endif|#
directive|endif
comment|/* close out the standard file descriptors */
if|if
condition|(
name|freopen
argument_list|(
literal|"/dev/null"
argument_list|,
literal|"r"
argument_list|,
name|stdin
argument_list|)
operator|==
name|NULL
condition|)
block|{
return|return
name|errno
return|;
comment|/* continue anyhow -- note we can't close out descriptor 0 because we          * have nothing to replace it with, and if we didn't have a descriptor          * 0 the next file would be created with that value ... leading to          * havoc.          */
block|}
if|if
condition|(
name|freopen
argument_list|(
literal|"/dev/null"
argument_list|,
literal|"w"
argument_list|,
name|stdout
argument_list|)
operator|==
name|NULL
condition|)
block|{
return|return
name|errno
return|;
block|}
comment|/* We are going to reopen this again in a little while to the error       * log file, but better to do it twice and suffer a small performance       * hit for consistancy than not reopen it here.       */
if|if
condition|(
name|freopen
argument_list|(
literal|"/dev/null"
argument_list|,
literal|"w"
argument_list|,
name|stderr
argument_list|)
operator|==
name|NULL
condition|)
block|{
return|return
name|errno
return|;
block|}
return|return
name|APR_SUCCESS
return|;
block|}
end_block

begin_if
if|#
directive|if
operator|(
operator|!
name|HAVE_WAITPID
operator|)
end_if

begin_comment
comment|/* From ikluft@amdahl.com  * this is not ideal but it works for SVR3 variants  * Modified by dwd@bell-labs.com to call wait3 instead of wait because  *   apache started to use the WNOHANG option.  */
end_comment

begin_function
name|int
name|waitpid
parameter_list|(
name|pid_t
name|pid
parameter_list|,
name|int
modifier|*
name|statusp
parameter_list|,
name|int
name|options
parameter_list|)
block|{
name|int
name|tmp_pid
decl_stmt|;
if|if
condition|(
name|kill
argument_list|(
name|pid
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|errno
operator|=
name|ECHILD
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
while|while
condition|(
operator|(
operator|(
name|tmp_pid
operator|=
name|wait3
argument_list|(
name|statusp
argument_list|,
name|options
argument_list|,
literal|0
argument_list|)
operator|)
operator|!=
name|pid
operator|)
operator|&&
operator|(
name|tmp_pid
operator|!=
operator|-
literal|1
operator|)
operator|&&
operator|(
name|tmp_pid
operator|!=
literal|0
operator|)
operator|&&
operator|(
name|pid
operator|!=
operator|-
literal|1
operator|)
condition|)
empty_stmt|;
return|return
name|tmp_pid
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

