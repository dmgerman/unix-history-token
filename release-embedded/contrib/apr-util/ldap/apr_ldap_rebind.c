begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_comment
comment|/*  apr_ldap_rebind.c -- LDAP rebind callbacks for referrals  *  *  The LDAP SDK allows a callback to be set to enable rebinding  *  for referral processing.  *  */
end_comment

begin_include
include|#
directive|include
file|"apr.h"
end_include

begin_include
include|#
directive|include
file|"apu.h"
end_include

begin_include
include|#
directive|include
file|"apu_config.h"
end_include

begin_if
if|#
directive|if
name|APU_DSO_BUILD
end_if

begin_define
define|#
directive|define
name|APU_DSO_LDAP_BUILD
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"apr_ldap.h"
end_include

begin_include
include|#
directive|include
file|"apr_errno.h"
end_include

begin_include
include|#
directive|include
file|"apr_strings.h"
end_include

begin_include
include|#
directive|include
file|"apr_ldap_rebind.h"
end_include

begin_include
include|#
directive|include
file|"stdio.h"
end_include

begin_if
if|#
directive|if
name|APR_HAS_LDAP
end_if

begin_comment
comment|/* Used to store information about connections for use in the referral rebind callback. */
end_comment

begin_struct
struct|struct
name|apr_ldap_rebind_entry
block|{
name|apr_pool_t
modifier|*
name|pool
decl_stmt|;
name|LDAP
modifier|*
name|index
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindDN
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindPW
decl_stmt|;
name|struct
name|apr_ldap_rebind_entry
modifier|*
name|next
decl_stmt|;
block|}
struct|;
end_struct

begin_typedef
typedef|typedef
name|struct
name|apr_ldap_rebind_entry
name|apr_ldap_rebind_entry_t
typedef|;
end_typedef

begin_ifdef
ifdef|#
directive|ifdef
name|NETWARE
end_ifdef

begin_include
include|#
directive|include
file|"apr_private.h"
end_include

begin_define
define|#
directive|define
name|get_apd
value|APP_DATA* apd = (APP_DATA*)get_app_data(gLibId);
end_define

begin_define
define|#
directive|define
name|apr_ldap_xref_lock
value|((apr_thread_mutex_t *)(apd->gs_ldap_xref_lock))
end_define

begin_define
define|#
directive|define
name|xref_head
value|((apr_ldap_rebind_entry_t *)(apd->gs_xref_head))
end_define

begin_else
else|#
directive|else
end_else

begin_if
if|#
directive|if
name|APR_HAS_THREADS
end_if

begin_decl_stmt
specifier|static
name|apr_thread_mutex_t
modifier|*
name|apr_ldap_xref_lock
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
specifier|static
name|apr_ldap_rebind_entry_t
modifier|*
name|xref_head
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_function_decl
specifier|static
name|int
name|apr_ldap_rebind_set_callback
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|static
name|apr_status_t
name|apr_ldap_rebind_remove_helper
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
function_decl|;
end_function_decl

begin_function
specifier|static
name|apr_status_t
name|apr_ldap_pool_cleanup_set_null
parameter_list|(
name|void
modifier|*
name|data_
parameter_list|)
block|{
name|void
modifier|*
modifier|*
name|ptr
init|=
operator|(
name|void
operator|*
operator|*
operator|)
name|data_
decl_stmt|;
operator|*
name|ptr
operator|=
name|NULL
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_comment
comment|/* APR utility routine used to create the xref_lock. */
end_comment

begin_macro
name|APU_DECLARE_LDAP
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_ldap_rebind_init
argument_list|(
argument|apr_pool_t *pool
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|retcode
init|=
name|APR_SUCCESS
decl_stmt|;
ifdef|#
directive|ifdef
name|NETWARE
name|get_apd
endif|#
directive|endif
if|#
directive|if
name|APR_HAS_THREADS
comment|/* run after apr_thread_mutex_create cleanup */
name|apr_pool_cleanup_register
argument_list|(
name|pool
argument_list|,
operator|&
name|apr_ldap_xref_lock
argument_list|,
name|apr_ldap_pool_cleanup_set_null
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
decl_stmt|;
if|if
condition|(
name|apr_ldap_xref_lock
operator|==
name|NULL
condition|)
block|{
name|retcode
operator|=
name|apr_thread_mutex_create
argument_list|(
operator|&
name|apr_ldap_xref_lock
argument_list|,
name|APR_THREAD_MUTEX_DEFAULT
argument_list|,
name|pool
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
return|return
operator|(
name|retcode
operator|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE_LDAP
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_ldap_rebind_add
argument_list|(
argument|apr_pool_t *pool
argument_list|,
argument|LDAP *ld
argument_list|,
argument|const char *bindDN
argument_list|,
argument|const char *bindPW
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|retcode
init|=
name|APR_SUCCESS
decl_stmt|;
name|apr_ldap_rebind_entry_t
modifier|*
name|new_xref
decl_stmt|;
ifdef|#
directive|ifdef
name|NETWARE
name|get_apd
endif|#
directive|endif
name|new_xref
init|=
operator|(
name|apr_ldap_rebind_entry_t
operator|*
operator|)
name|apr_pcalloc
argument_list|(
name|pool
argument_list|,
sizeof|sizeof
argument_list|(
name|apr_ldap_rebind_entry_t
argument_list|)
argument_list|)
decl_stmt|;
if|if
condition|(
name|new_xref
condition|)
block|{
name|new_xref
operator|->
name|pool
operator|=
name|pool
expr_stmt|;
name|new_xref
operator|->
name|index
operator|=
name|ld
expr_stmt|;
if|if
condition|(
name|bindDN
condition|)
block|{
name|new_xref
operator|->
name|bindDN
operator|=
name|apr_pstrdup
argument_list|(
name|pool
argument_list|,
name|bindDN
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bindPW
condition|)
block|{
name|new_xref
operator|->
name|bindPW
operator|=
name|apr_pstrdup
argument_list|(
name|pool
argument_list|,
name|bindPW
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
name|APR_HAS_THREADS
name|retcode
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
expr_stmt|;
if|if
condition|(
name|retcode
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|retcode
return|;
block|}
endif|#
directive|endif
name|new_xref
operator|->
name|next
operator|=
name|xref_head
expr_stmt|;
name|xref_head
operator|=
name|new_xref
expr_stmt|;
if|#
directive|if
name|APR_HAS_THREADS
name|retcode
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
expr_stmt|;
if|if
condition|(
name|retcode
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|retcode
return|;
block|}
endif|#
directive|endif
block|}
else|else
block|{
return|return
operator|(
name|APR_ENOMEM
operator|)
return|;
block|}
name|retcode
operator|=
name|apr_ldap_rebind_set_callback
argument_list|(
name|ld
argument_list|)
expr_stmt|;
if|if
condition|(
name|APR_SUCCESS
operator|!=
name|retcode
condition|)
block|{
name|apr_ldap_rebind_remove
argument_list|(
name|ld
argument_list|)
expr_stmt|;
return|return
name|retcode
return|;
block|}
name|apr_pool_cleanup_register
argument_list|(
name|pool
argument_list|,
name|ld
argument_list|,
name|apr_ldap_rebind_remove_helper
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
expr_stmt|;
return|return
operator|(
name|APR_SUCCESS
operator|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE_LDAP
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_ldap_rebind_remove
argument_list|(
argument|LDAP *ld
argument_list|)
end_macro

begin_block
block|{
name|apr_ldap_rebind_entry_t
modifier|*
name|tmp_xref
decl_stmt|,
modifier|*
name|prev
init|=
name|NULL
decl_stmt|;
name|apr_status_t
name|retcode
init|=
literal|0
decl_stmt|;
ifdef|#
directive|ifdef
name|NETWARE
name|get_apd
endif|#
directive|endif
if|#
directive|if
name|APR_HAS_THREADS
name|retcode
init|=
name|apr_thread_mutex_lock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
decl_stmt|;
if|if
condition|(
name|retcode
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|retcode
return|;
block|}
endif|#
directive|endif
name|tmp_xref
operator|=
name|xref_head
expr_stmt|;
while|while
condition|(
operator|(
name|tmp_xref
operator|)
operator|&&
operator|(
name|tmp_xref
operator|->
name|index
operator|!=
name|ld
operator|)
condition|)
block|{
name|prev
operator|=
name|tmp_xref
expr_stmt|;
name|tmp_xref
operator|=
name|tmp_xref
operator|->
name|next
expr_stmt|;
block|}
if|if
condition|(
name|tmp_xref
condition|)
block|{
if|if
condition|(
name|tmp_xref
operator|==
name|xref_head
condition|)
block|{
name|xref_head
operator|=
name|xref_head
operator|->
name|next
expr_stmt|;
block|}
else|else
block|{
name|prev
operator|->
name|next
operator|=
name|tmp_xref
operator|->
name|next
expr_stmt|;
block|}
comment|/* tmp_xref and its contents were pool allocated so they don't need to be freed here. */
comment|/* remove the cleanup, just in case this was done manually */
name|apr_pool_cleanup_kill
argument_list|(
name|tmp_xref
operator|->
name|pool
argument_list|,
name|tmp_xref
operator|->
name|index
argument_list|,
name|apr_ldap_rebind_remove_helper
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
name|APR_HAS_THREADS
name|retcode
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
expr_stmt|;
if|if
condition|(
name|retcode
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|retcode
return|;
block|}
endif|#
directive|endif
return|return
name|APR_SUCCESS
return|;
block|}
end_block

begin_function
specifier|static
name|apr_status_t
name|apr_ldap_rebind_remove_helper
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
block|{
name|LDAP
modifier|*
name|ld
init|=
operator|(
name|LDAP
operator|*
operator|)
name|data
decl_stmt|;
name|apr_ldap_rebind_remove
argument_list|(
name|ld
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_if
if|#
directive|if
name|APR_HAS_TIVOLI_LDAPSDK
operator|||
name|APR_HAS_OPENLDAP_LDAPSDK
operator|||
name|APR_HAS_NOVELL_LDAPSDK
end_if

begin_function
specifier|static
name|apr_ldap_rebind_entry_t
modifier|*
name|apr_ldap_rebind_lookup
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
block|{
name|apr_ldap_rebind_entry_t
modifier|*
name|tmp_xref
decl_stmt|,
modifier|*
name|match
init|=
name|NULL
decl_stmt|;
ifdef|#
directive|ifdef
name|NETWARE
name|get_apd
endif|#
directive|endif
if|#
directive|if
name|APR_HAS_THREADS
name|apr_thread_mutex_lock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
decl_stmt|;
endif|#
directive|endif
name|tmp_xref
operator|=
name|xref_head
expr_stmt|;
while|while
condition|(
name|tmp_xref
condition|)
block|{
if|if
condition|(
name|tmp_xref
operator|->
name|index
operator|==
name|ld
condition|)
block|{
name|match
operator|=
name|tmp_xref
expr_stmt|;
name|tmp_xref
operator|=
name|NULL
expr_stmt|;
block|}
else|else
block|{
name|tmp_xref
operator|=
name|tmp_xref
operator|->
name|next
expr_stmt|;
block|}
block|}
if|#
directive|if
name|APR_HAS_THREADS
name|apr_thread_mutex_unlock
argument_list|(
name|apr_ldap_xref_lock
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|match
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|APR_HAS_TIVOLI_LDAPSDK
end_if

begin_comment
comment|/* LDAP_rebindproc() Tivoli LDAP style  *     Rebind callback function. Called when chasing referrals. See API docs.  * ON ENTRY:  *     ld       Pointer to an LDAP control structure. (input only)  *     binddnp  Pointer to an Application DName used for binding (in *or* out)  *     passwdp  Pointer to the password associated with the DName (in *or* out)  *     methodp  Pointer to the Auth method (output only)  *     freeit   Flag to indicate if this is a lookup or a free request (input only)  */
end_comment

begin_function
specifier|static
name|int
name|LDAP_rebindproc
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|,
name|char
modifier|*
modifier|*
name|binddnp
parameter_list|,
name|char
modifier|*
modifier|*
name|passwdp
parameter_list|,
name|int
modifier|*
name|methodp
parameter_list|,
name|int
name|freeit
parameter_list|)
block|{
if|if
condition|(
operator|!
name|freeit
condition|)
block|{
name|apr_ldap_rebind_entry_t
modifier|*
name|my_conn
decl_stmt|;
operator|*
name|methodp
operator|=
name|LDAP_AUTH_SIMPLE
expr_stmt|;
name|my_conn
operator|=
name|apr_ldap_rebind_lookup
argument_list|(
name|ld
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|my_conn
operator|)
operator|&&
operator|(
name|my_conn
operator|->
name|bindDN
operator|!=
name|NULL
operator|)
condition|)
block|{
operator|*
name|binddnp
operator|=
name|strdup
argument_list|(
name|my_conn
operator|->
name|bindDN
argument_list|)
expr_stmt|;
operator|*
name|passwdp
operator|=
name|strdup
argument_list|(
name|my_conn
operator|->
name|bindPW
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|*
name|binddnp
operator|=
name|NULL
expr_stmt|;
operator|*
name|passwdp
operator|=
name|NULL
expr_stmt|;
block|}
block|}
else|else
block|{
if|if
condition|(
operator|*
name|binddnp
condition|)
block|{
name|free
argument_list|(
operator|*
name|binddnp
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|passwdp
condition|)
block|{
name|free
argument_list|(
operator|*
name|passwdp
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|LDAP_SUCCESS
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|apr_ldap_rebind_set_callback
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
block|{
name|ldap_set_rebind_proc
argument_list|(
name|ld
argument_list|,
operator|(
name|LDAPRebindProc
operator|)
name|LDAP_rebindproc
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_elif
elif|#
directive|elif
name|APR_HAS_OPENLDAP_LDAPSDK
end_elif

begin_comment
comment|/* LDAP_rebindproc() openLDAP V3 style  * ON ENTRY:  *     ld       Pointer to an LDAP control structure. (input only)  *     url      Unused in this routine  *     request  Unused in this routine  *     msgid    Unused in this routine  *     params   Unused in this routine  *  *     or  *  *     ld       Pointer to an LDAP control structure. (input only)  *     url      Unused in this routine  *     request  Unused in this routine  *     msgid    Unused in this routine  */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|LDAP_SET_REBIND_PROC_THREE
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|int
name|LDAP_rebindproc
argument_list|(
name|LDAP
operator|*
name|ld
argument_list|,
name|LDAP_CONST
name|char
operator|*
name|url
argument_list|,
name|ber_tag_t
name|request
argument_list|,
name|ber_int_t
name|msgid
argument_list|,
name|void
operator|*
name|params
argument_list|)
else|#
directive|else
decl|static
name|int
name|LDAP_rebindproc
argument_list|(
name|LDAP
operator|*
name|ld
argument_list|,
name|LDAP_CONST
name|char
operator|*
name|url
argument_list|,
name|int
name|request
argument_list|,
name|ber_int_t
name|msgid
argument_list|)
endif|#
directive|endif
block|{
name|apr_ldap_rebind_entry_t
modifier|*
name|my_conn
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindDN
init|=
name|NULL
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindPW
init|=
name|NULL
decl_stmt|;
name|my_conn
operator|=
name|apr_ldap_rebind_lookup
argument_list|(
name|ld
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|my_conn
operator|)
operator|&&
operator|(
name|my_conn
operator|->
name|bindDN
operator|!=
name|NULL
operator|)
condition|)
block|{
name|bindDN
operator|=
name|my_conn
operator|->
name|bindDN
expr_stmt|;
name|bindPW
operator|=
name|my_conn
operator|->
name|bindPW
expr_stmt|;
block|}
return|return
operator|(
name|ldap_bind_s
argument_list|(
name|ld
argument_list|,
name|bindDN
argument_list|,
name|bindPW
argument_list|,
name|LDAP_AUTH_SIMPLE
argument_list|)
operator|)
return|;
block|}
end_decl_stmt

begin_function
specifier|static
name|int
name|apr_ldap_rebind_set_callback
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|LDAP_SET_REBIND_PROC_THREE
argument_list|)
name|ldap_set_rebind_proc
argument_list|(
name|ld
argument_list|,
name|LDAP_rebindproc
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
name|ldap_set_rebind_proc
argument_list|(
name|ld
argument_list|,
name|LDAP_rebindproc
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_elif
elif|#
directive|elif
name|APR_HAS_NOVELL_LDAPSDK
end_elif

begin_comment
comment|/* LDAP_rebindproc() openLDAP V3 style  * ON ENTRY:  *     ld       Pointer to an LDAP control structure. (input only)  *     url      Unused in this routine  *     request  Unused in this routine  *     msgid    Unused in this routine  */
end_comment

begin_function
specifier|static
name|int
name|LDAP_rebindproc
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|,
name|LDAP_CONST
name|char
modifier|*
name|url
parameter_list|,
name|int
name|request
parameter_list|,
name|ber_int_t
name|msgid
parameter_list|)
block|{
name|apr_ldap_rebind_entry_t
modifier|*
name|my_conn
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindDN
init|=
name|NULL
decl_stmt|;
specifier|const
name|char
modifier|*
name|bindPW
init|=
name|NULL
decl_stmt|;
name|my_conn
operator|=
name|apr_ldap_rebind_lookup
argument_list|(
name|ld
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|my_conn
operator|)
operator|&&
operator|(
name|my_conn
operator|->
name|bindDN
operator|!=
name|NULL
operator|)
condition|)
block|{
name|bindDN
operator|=
name|my_conn
operator|->
name|bindDN
expr_stmt|;
name|bindPW
operator|=
name|my_conn
operator|->
name|bindPW
expr_stmt|;
block|}
return|return
operator|(
name|ldap_bind_s
argument_list|(
name|ld
argument_list|,
name|bindDN
argument_list|,
name|bindPW
argument_list|,
name|LDAP_AUTH_SIMPLE
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|apr_ldap_rebind_set_callback
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
block|{
name|ldap_set_rebind_proc
argument_list|(
name|ld
argument_list|,
name|LDAP_rebindproc
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* Implementation not recognised */
end_comment

begin_function
specifier|static
name|int
name|apr_ldap_rebind_set_callback
parameter_list|(
name|LDAP
modifier|*
name|ld
parameter_list|)
block|{
return|return
name|APR_ENOTIMPL
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* APR_HAS_LDAP */
end_comment

end_unit

