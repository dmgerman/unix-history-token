begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr_buckets.h"
end_include

begin_define
define|#
directive|define
name|APR_WANT_MEMFUNC
end_define

begin_include
include|#
directive|include
file|"apr_want.h"
end_include

begin_function
specifier|static
name|apr_status_t
name|pool_bucket_cleanup
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
block|{
name|apr_bucket_pool
modifier|*
name|p
init|=
name|data
decl_stmt|;
comment|/*      * If the pool gets cleaned up, we have to copy the data out      * of the pool and onto the heap.  But the apr_buckets out there      * that point to this pool bucket need to be notified such that      * they can morph themselves into a regular heap bucket the next      * time they try to read.  To avoid having to manipulate      * reference counts and b->data pointers, the apr_bucket_pool      * actually _contains_ an apr_bucket_heap as its first element,      * so the two share their apr_bucket_refcount member, and you      * can typecast a pool bucket struct to make it look like a      * regular old heap bucket struct.      */
name|p
operator|->
name|heap
operator|.
name|base
operator|=
name|apr_bucket_alloc
argument_list|(
name|p
operator|->
name|heap
operator|.
name|alloc_len
argument_list|,
name|p
operator|->
name|list
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|p
operator|->
name|heap
operator|.
name|base
argument_list|,
name|p
operator|->
name|base
argument_list|,
name|p
operator|->
name|heap
operator|.
name|alloc_len
argument_list|)
expr_stmt|;
name|p
operator|->
name|base
operator|=
name|NULL
expr_stmt|;
name|p
operator|->
name|pool
operator|=
name|NULL
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_function
specifier|static
name|apr_status_t
name|pool_bucket_read
parameter_list|(
name|apr_bucket
modifier|*
name|b
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|str
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|,
name|apr_read_type_e
name|block
parameter_list|)
block|{
name|apr_bucket_pool
modifier|*
name|p
init|=
name|b
operator|->
name|data
decl_stmt|;
specifier|const
name|char
modifier|*
name|base
init|=
name|p
operator|->
name|base
decl_stmt|;
if|if
condition|(
name|p
operator|->
name|pool
operator|==
name|NULL
condition|)
block|{
comment|/*          * pool has been cleaned up... masquerade as a heap bucket from now          * on. subsequent bucket operations will use the heap bucket code.          */
name|b
operator|->
name|type
operator|=
operator|&
name|apr_bucket_type_heap
expr_stmt|;
name|base
operator|=
name|p
operator|->
name|heap
operator|.
name|base
expr_stmt|;
block|}
operator|*
name|str
operator|=
name|base
operator|+
name|b
operator|->
name|start
expr_stmt|;
operator|*
name|len
operator|=
name|b
operator|->
name|length
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|pool_bucket_destroy
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
block|{
name|apr_bucket_pool
modifier|*
name|p
init|=
name|data
decl_stmt|;
comment|/* If the pool is cleaned up before the last reference goes      * away, the data is really now on the heap; heap_destroy() takes      * over.  free() in heap_destroy() thinks it's freeing      * an apr_bucket_heap, when in reality it's freeing the whole      * apr_bucket_pool for us.      */
if|if
condition|(
name|p
operator|->
name|pool
condition|)
block|{
comment|/* the shared resource is still in the pool          * because the pool has not been cleaned up yet          */
if|if
condition|(
name|apr_bucket_shared_destroy
argument_list|(
name|p
argument_list|)
condition|)
block|{
name|apr_pool_cleanup_kill
argument_list|(
name|p
operator|->
name|pool
argument_list|,
name|p
argument_list|,
name|pool_bucket_cleanup
argument_list|)
expr_stmt|;
name|apr_bucket_free
argument_list|(
name|p
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
comment|/* the shared resource is no longer in the pool, it's          * on the heap, but this reference still thinks it's a pool          * bucket.  we should just go ahead and pass control to          * heap_destroy() for it since it doesn't know any better.          */
name|apr_bucket_type_heap
operator|.
name|destroy
argument_list|(
name|p
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_pool_make
argument_list|(
argument|apr_bucket *b
argument_list|,
argument|const char *buf
argument_list|,
argument|apr_size_t length
argument_list|,
argument|apr_pool_t *pool
argument_list|)
end_macro

begin_block
block|{
name|apr_bucket_pool
modifier|*
name|p
decl_stmt|;
name|p
operator|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|p
argument_list|)
argument_list|,
name|b
operator|->
name|list
argument_list|)
expr_stmt|;
comment|/* XXX: we lose the const qualifier here which indicates      * there's something screwy with the API...      */
comment|/* XXX: why is this?  buf is const, p->base is const... what's      * the problem?  --jcw */
name|p
operator|->
name|base
operator|=
operator|(
name|char
operator|*
operator|)
name|buf
expr_stmt|;
name|p
operator|->
name|pool
operator|=
name|pool
expr_stmt|;
name|p
operator|->
name|list
operator|=
name|b
operator|->
name|list
expr_stmt|;
name|b
operator|=
name|apr_bucket_shared_make
argument_list|(
name|b
argument_list|,
name|p
argument_list|,
literal|0
argument_list|,
name|length
argument_list|)
expr_stmt|;
name|b
operator|->
name|type
operator|=
operator|&
name|apr_bucket_type_pool
expr_stmt|;
comment|/* pre-initialize heap bucket member */
name|p
operator|->
name|heap
operator|.
name|alloc_len
operator|=
name|length
expr_stmt|;
name|p
operator|->
name|heap
operator|.
name|base
operator|=
name|NULL
expr_stmt|;
name|p
operator|->
name|heap
operator|.
name|free_func
operator|=
name|apr_bucket_free
expr_stmt|;
name|apr_pool_cleanup_register
argument_list|(
name|p
operator|->
name|pool
argument_list|,
name|p
argument_list|,
name|pool_bucket_cleanup
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
expr_stmt|;
return|return
name|b
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_pool_create
argument_list|(
argument|const char *buf
argument_list|,
argument|apr_size_t length
argument_list|,
argument|apr_pool_t *pool
argument_list|,
argument|apr_bucket_alloc_t *list
argument_list|)
end_macro

begin_block
block|{
name|apr_bucket
modifier|*
name|b
init|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|b
argument_list|)
argument_list|,
name|list
argument_list|)
decl_stmt|;
name|APR_BUCKET_INIT
argument_list|(
name|b
argument_list|)
expr_stmt|;
name|b
operator|->
name|free
operator|=
name|apr_bucket_free
expr_stmt|;
name|b
operator|->
name|list
operator|=
name|list
expr_stmt|;
return|return
name|apr_bucket_pool_make
argument_list|(
name|b
argument_list|,
name|buf
argument_list|,
name|length
argument_list|,
name|pool
argument_list|)
return|;
block|}
end_block

begin_decl_stmt
name|APU_DECLARE_DATA
specifier|const
name|apr_bucket_type_t
name|apr_bucket_type_pool
init|=
block|{
literal|"POOL"
block|,
literal|5
block|,
name|APR_BUCKET_DATA
block|,
name|pool_bucket_destroy
block|,
name|pool_bucket_read
block|,
name|apr_bucket_setaside_noop
block|,
comment|/* don't need to setaside thanks to the cleanup*/
name|apr_bucket_shared_split
block|,
name|apr_bucket_shared_copy
block|}
decl_stmt|;
end_decl_stmt

end_unit

