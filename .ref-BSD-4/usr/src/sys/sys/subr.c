begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	subr.c	4.1	11/9/80	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/conf.h"
end_include

begin_include
include|#
directive|include
file|"../h/inode.h"
end_include

begin_include
include|#
directive|include
file|"../h/dir.h"
end_include

begin_include
include|#
directive|include
file|"../h/user.h"
end_include

begin_include
include|#
directive|include
file|"../h/buf.h"
end_include

begin_include
include|#
directive|include
file|"../h/proc.h"
end_include

begin_comment
comment|/*  * Bmap defines the structure of file system storage  * by returning the physical block number on a device given the  * inode and the logical block number in a file.  * When convenient, it also leaves the physical  * block number of the next block of the file in rablock  * for use in read-ahead.  */
end_comment

begin_function
name|daddr_t
name|bmap
parameter_list|(
name|ip
parameter_list|,
name|bn
parameter_list|,
name|rwflg
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|daddr_t
name|bn
decl_stmt|;
block|{
specifier|register
name|i
expr_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|,
modifier|*
name|nbp
decl_stmt|;
name|int
name|j
decl_stmt|,
name|sh
decl_stmt|;
name|daddr_t
name|nb
decl_stmt|,
modifier|*
name|bap
decl_stmt|;
name|dev_t
name|dev
decl_stmt|;
if|if
condition|(
name|bn
operator|<
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFBIG
expr_stmt|;
return|return
operator|(
operator|(
name|daddr_t
operator|)
literal|0
operator|)
return|;
block|}
name|dev
operator|=
name|ip
operator|->
name|i_dev
expr_stmt|;
name|rablock
operator|=
literal|0
expr_stmt|;
comment|/* 	 * blocks 0..NADDR-4 are direct blocks 	 */
if|if
condition|(
name|bn
operator|<
name|NADDR
operator|-
literal|3
condition|)
block|{
name|i
operator|=
name|bn
expr_stmt|;
name|nb
operator|=
name|ip
operator|->
name|i_un
operator|.
name|i_addr
index|[
name|i
index|]
expr_stmt|;
if|if
condition|(
name|nb
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|rwflg
operator|==
name|B_READ
operator|||
operator|(
name|bp
operator|=
name|alloc
argument_list|(
name|dev
argument_list|)
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
operator|(
name|daddr_t
operator|)
operator|-
literal|1
operator|)
return|;
name|nb
operator|=
name|dbtofsb
argument_list|(
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
operator|)
operator|==
name|IFDIR
condition|)
comment|/* 				 * Write directory blocks synchronously 				 * so they never appear with garbage in 				 * them on the disk. 				 */
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
else|else
name|bdwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_un
operator|.
name|i_addr
index|[
name|i
index|]
operator|=
name|nb
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|IUPD
operator||
name|ICHG
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|<
name|NADDR
operator|-
literal|4
condition|)
name|rablock
operator|=
name|ip
operator|->
name|i_un
operator|.
name|i_addr
index|[
name|i
operator|+
literal|1
index|]
expr_stmt|;
return|return
operator|(
name|nb
operator|)
return|;
block|}
comment|/* 	 * addresses NADDR-3, NADDR-2, and NADDR-1 	 * have single, double, triple indirect blocks. 	 * the first step is to determine 	 * how many levels of indirection. 	 */
name|sh
operator|=
literal|0
expr_stmt|;
name|nb
operator|=
literal|1
expr_stmt|;
name|bn
operator|-=
name|NADDR
operator|-
literal|3
expr_stmt|;
for|for
control|(
name|j
operator|=
literal|3
init|;
name|j
operator|>
literal|0
condition|;
name|j
operator|--
control|)
block|{
name|sh
operator|+=
name|NSHIFT
expr_stmt|;
name|nb
operator|<<=
name|NSHIFT
expr_stmt|;
if|if
condition|(
name|bn
operator|<
name|nb
condition|)
break|break;
name|bn
operator|-=
name|nb
expr_stmt|;
block|}
if|if
condition|(
name|j
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFBIG
expr_stmt|;
return|return
operator|(
operator|(
name|daddr_t
operator|)
literal|0
operator|)
return|;
block|}
comment|/* 	 * fetch the first indirect block 	 */
name|nb
operator|=
name|ip
operator|->
name|i_un
operator|.
name|i_addr
index|[
name|NADDR
operator|-
name|j
index|]
expr_stmt|;
if|if
condition|(
name|nb
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|rwflg
operator|==
name|B_READ
operator|||
operator|(
name|bp
operator|=
name|alloc
argument_list|(
name|dev
argument_list|)
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
operator|(
name|daddr_t
operator|)
operator|-
literal|1
operator|)
return|;
name|nb
operator|=
name|dbtofsb
argument_list|(
name|bp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
comment|/* 		 * Write synchronously so that indirect blocks 		 * never point at garbage. 		 */
name|bwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_un
operator|.
name|i_addr
index|[
name|NADDR
operator|-
name|j
index|]
operator|=
name|nb
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|IUPD
operator||
name|ICHG
expr_stmt|;
block|}
comment|/* 	 * fetch through the indirect blocks 	 */
for|for
control|(
init|;
name|j
operator|<=
literal|3
condition|;
name|j
operator|++
control|)
block|{
name|bp
operator|=
name|bread
argument_list|(
name|dev
argument_list|,
name|nb
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
name|B_ERROR
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|daddr_t
operator|)
literal|0
operator|)
return|;
block|}
name|bap
operator|=
name|bp
operator|->
name|b_un
operator|.
name|b_daddr
expr_stmt|;
name|sh
operator|-=
name|NSHIFT
expr_stmt|;
name|i
operator|=
operator|(
name|bn
operator|>>
name|sh
operator|)
operator|&
name|NMASK
expr_stmt|;
name|nb
operator|=
name|bap
index|[
name|i
index|]
expr_stmt|;
if|if
condition|(
name|nb
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|rwflg
operator|==
name|B_READ
operator|||
operator|(
name|nbp
operator|=
name|alloc
argument_list|(
name|dev
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|daddr_t
operator|)
operator|-
literal|1
operator|)
return|;
block|}
name|nb
operator|=
name|dbtofsb
argument_list|(
name|nbp
operator|->
name|b_blkno
argument_list|)
expr_stmt|;
if|if
condition|(
name|j
operator|<
literal|3
operator|||
operator|(
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
operator|)
operator|==
name|IFDIR
condition|)
comment|/* 				 * Write synchronously so indirect blocks 				 * never point at garbage and blocks 				 * in directories never contain garbage. 				 */
name|bwrite
argument_list|(
name|nbp
argument_list|)
expr_stmt|;
else|else
name|bdwrite
argument_list|(
name|nbp
argument_list|)
expr_stmt|;
name|bap
index|[
name|i
index|]
operator|=
name|nb
expr_stmt|;
name|bdwrite
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
else|else
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * calculate read-ahead. 	 */
if|if
condition|(
name|i
operator|<
name|NINDIR
operator|-
literal|1
condition|)
name|rablock
operator|=
name|bap
index|[
name|i
operator|+
literal|1
index|]
expr_stmt|;
return|return
operator|(
name|nb
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Pass back  c  to the user at his location u_base;  * update u_base, u_count, and u_offset.  Return -1  * on the last character of the user's read.  * u_base is in the user address space unless u_segflg is set.  */
end_comment

begin_expr_stmt
name|passc
argument_list|(
name|c
argument_list|)
specifier|register
name|c
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|id
expr_stmt|;
if|if
condition|(
operator|(
name|id
operator|=
name|u
operator|.
name|u_segflg
operator|)
operator|==
literal|1
condition|)
operator|*
name|u
operator|.
name|u_base
operator|=
name|c
expr_stmt|;
elseif|else
if|if
condition|(
name|id
condition|?
name|suibyte
argument_list|(
name|u
operator|.
name|u_base
argument_list|,
name|c
argument_list|)
else|:
name|subyte
argument_list|(
name|u
operator|.
name|u_base
argument_list|,
name|c
argument_list|)
operator|<
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
name|u
operator|.
name|u_count
operator|--
expr_stmt|;
name|u
operator|.
name|u_offset
operator|++
expr_stmt|;
name|u
operator|.
name|u_base
operator|++
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|u_count
operator|==
literal|0
condition|?
operator|-
literal|1
else|:
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Pick up and return the next character from the user's  * write call at location u_base;  * update u_base, u_count, and u_offset.  Return -1  * when u_count is exhausted.  u_base is in the user's  * address space unless u_segflg is set.  */
end_comment

begin_comment
comment|/* cpass() { 	register c, id;  	if(u.u_count == 0) 		return(-1); 	if((id = u.u_segflg) == 1) 		c = *u.u_base; 	else 		if((c = id==0?fubyte(u.u_base):fuibyte(u.u_base))< 0) { 			u.u_error = EFAULT; 			return(-1); 		} 	u.u_count--; 	u.u_offset++; 	u.u_base++; 	return(c&0377); } */
end_comment

begin_comment
comment|/*  * Routine which sets a user error; placed in  * illegal entries in the bdevsw and cdevsw tables.  */
end_comment

begin_macro
name|nodev
argument_list|()
end_macro

begin_block
block|{
name|u
operator|.
name|u_error
operator|=
name|ENODEV
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Null routine; placed in insignificant entries  * in the bdevsw and cdevsw tables.  */
end_comment

begin_macro
name|nulldev
argument_list|()
end_macro

begin_block
block|{  }
end_block

begin_macro
name|imin
argument_list|(
argument|a
argument_list|,
argument|b
argument_list|)
end_macro

begin_block
block|{
return|return
operator|(
name|a
operator|<
name|b
condition|?
name|a
else|:
name|b
operator|)
return|;
block|}
end_block

begin_macro
name|imax
argument_list|(
argument|a
argument_list|,
argument|b
argument_list|)
end_macro

begin_block
block|{
return|return
operator|(
name|a
operator|>
name|b
condition|?
name|a
else|:
name|b
operator|)
return|;
block|}
end_block

begin_function
name|struct
name|proc
modifier|*
name|pfind
parameter_list|(
name|pid
parameter_list|)
name|int
name|pid
decl_stmt|;
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
for|for
control|(
name|p
operator|=
operator|&
name|proc
index|[
name|pidhash
index|[
name|PIDHASH
argument_list|(
name|pid
argument_list|)
index|]
index|]
init|;
name|p
operator|!=
operator|&
name|proc
index|[
literal|0
index|]
condition|;
name|p
operator|=
operator|&
name|proc
index|[
name|p
operator|->
name|p_idhash
index|]
control|)
if|if
condition|(
name|p
operator|->
name|p_pid
operator|==
name|pid
condition|)
return|return
operator|(
name|p
operator|)
return|;
return|return
operator|(
operator|(
expr|struct
name|proc
operator|*
operator|)
literal|0
operator|)
return|;
block|}
end_function

end_unit

