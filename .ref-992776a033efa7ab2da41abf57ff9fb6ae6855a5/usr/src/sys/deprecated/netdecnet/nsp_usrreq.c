begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	nsp_usrreq.c	1.5	82/12/18	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../netdecnet/decnet.h"
end_include

begin_include
include|#
directive|include
file|"../netdecnet/dn_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/if.h"
end_include

begin_include
include|#
directive|include
file|"../netdecnet/nsp.h"
end_include

begin_include
include|#
directive|include
file|"../netdecnet/nsp_var.h"
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_comment
comment|/*  * NSP protocol interface to socket abstraction.  */
end_comment

begin_function_decl
name|struct
name|nspcb
modifier|*
name|nsp_newnspcb
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * Process an NSP user request for NSP np.  If this is a send request  * then m is the mbuf chain of send data.  If this is a timer expiration  * (called from the software clock routine), then timertype tells which timer.  */
end_comment

begin_macro
name|nsp_usrreq
argument_list|(
argument|so
argument_list|,
argument|req
argument_list|,
argument|m
argument_list|,
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|req
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|nspcb
modifier|*
name|np
init|=
name|sotonspcb
argument_list|(
name|so
argument_list|)
decl_stmt|;
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
name|int
name|ostate
decl_stmt|;
name|COUNT
argument_list|(
name|NSP_USRREQ
argument_list|)
expr_stmt|;
comment|/* 	 * When an NSP is attached to a socket, then there will be 	 * a (struct nspcb) pointed at by the socket. 	 * The normal sequence of events is: 	 *	PRU_ATTACH		creating these structures 	 *	PRU_CONNECT		connecting to a remote peer 	 *	(PRU_SEND|PRU_RCVD)*	exchanging data 	 *	PRU_DISCONNECT		disconnecting from remote peer 	 *	PRU_DETACH		deleting the structures 	 * With the operations from PRU_CONNECT through PRU_DISCONNECT 	 * possible repeated several times. 	 * 	 * MULTIPLE CONNECTS ARE NOT YET IMPLEMENTED. 	 */
if|if
condition|(
name|np
operator|==
literal|0
operator|&&
name|req
operator|!=
name|PRU_ATTACH
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* XXX */
block|}
if|if
condition|(
name|np
condition|)
block|{
name|ostate
operator|=
name|np
operator|->
name|n_state
expr_stmt|;
block|}
switch|switch
condition|(
name|req
condition|)
block|{
comment|/* 	 * NSP attaches to socket via PRU_ATTACH, reserving space 	 * and NSP control block. 	 **** If the socket is to receive connections, 	 **** then the LISTEN state is entered. 	 */
case|case
name|PRU_ATTACH
case|:
if|if
condition|(
name|np
condition|)
block|{
name|error
operator|=
name|EISCONN
expr_stmt|;
break|break;
block|}
name|error
operator|=
name|nsp_attach
argument_list|(
name|so
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
break|break;
name|np
operator|=
name|sotonspcb
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * PRU_DETACH detaches the NSP protocol from the socket. 	 * If the protocol state is non-embryonic, then can't 	 * do this directly: have to initiate a PRU_DISCONNECT, 	 * which may finish later; embryonic nspcb's can just 	 * be discarded here. 	 */
case|case
name|PRU_DETACH
case|:
if|if
condition|(
name|np
operator|->
name|n_state
operator|!=
name|NS_O
operator|&&
name|np
operator|->
name|n_state
operator|!=
name|NS_CL
operator|&&
name|np
operator|->
name|n_state
operator|!=
name|NS_LI
condition|)
name|nsp_disconnect
argument_list|(
name|np
argument_list|,
operator|<
name|reason
operator|>
argument_list|)
expr_stmt|;
else|else
block|{
name|nsp_close
argument_list|(
name|np
argument_list|)
expr_stmt|;
name|np
operator|=
literal|0
expr_stmt|;
block|}
break|break;
comment|/* 	 * Initiate connection to peer. 	 * Enter CI state, and mark socket as connecting. 	 **** Start keep-alive timer, and seed output sequence space. 	 **** Send initial segment on connection. 	 */
case|case
name|PRU_CONNECT
case|:
name|error
operator|=
name|dn_pcbconnect
argument_list|(
name|np
argument_list|,
operator|(
expr|struct
name|sockaddr_dn
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
break|break;
name|soisconnecting
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|nsp_connect
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Initiate disconnect from peer. 	 * If connection never passed embryonic stage, just drop; 	 * else if don't need to let data drain, then can just drop anyways, 	 * else have to begin NSP shutdown process: mark socket disconnecting, 	 * drain unread data, state switch to reflect user close, and 	 * send segment (e.g. DI) to peer.  Socket will be really disconnected 	 * when peer sends DC to ack our DI. 	 * 	 * SHOULD IMPLEMENT LATER PRU_CONNECT VIA REALLOC NSPCB. 	 */
case|case
name|PRU_DISCONNECT
case|:
name|nsp_disconnect
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Accept a connection.  Essentially all the work is 	 * done at higher levels; just return the address 	 * of the peer, storing through addr. 	 */
case|case
name|PRU_ACCEPT
case|:
name|dn_pcbconnaddr
argument_list|(
name|np
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
break|break;
comment|/*** BEGIN NOT MODIFIED FOR NSP ***/
comment|/* 	 * Mark the connection as being incapable of further output. 	 */
case|case
name|PRU_SHUTDOWN
case|:
name|socantsendmore
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|nsp_usrclosed
argument_list|(
name|np
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|nsp_output
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * After a receive, possibly send window update to peer. 	 */
case|case
name|PRU_RCVD
case|:
operator|(
name|void
operator|)
name|nsp_output
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
comment|/*** END NOT MODIFIED FOR NSP ***/
comment|/* 	 * Do a send by putting data in output queue and 	 * calling output processor. 	 */
case|case
name|PRU_SEND
case|:
name|sbpappend
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
name|m
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|nsp_output
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
comment|/*** BEGIN NOT MODIFIED FOR NSP ***/
comment|/* 	 * Abort the NSP. 	 */
case|case
name|PRU_ABORT
case|:
name|nsp_drop
argument_list|(
name|np
argument_list|,
name|ECONNABORTED
argument_list|)
expr_stmt|;
break|break;
comment|/* SOME AS YET UNIMPLEMENTED HOOKS */
case|case
name|PRU_CONTROL
case|:
name|error
operator|=
name|EOPNOTSUPP
expr_stmt|;
break|break;
case|case
name|PRU_SENSE
case|:
name|error
operator|=
name|EOPNOTSUPP
expr_stmt|;
break|break;
comment|/* END UNIMPLEMENTED HOOKS */
case|case
name|PRU_RCVOOB
case|:
if|if
condition|(
name|so
operator|->
name|so_oobmark
operator|==
literal|0
operator|&&
operator|(
name|so
operator|->
name|so_state
operator|&
name|SS_RCVATMARK
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|EINVAL
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|(
name|np
operator|->
name|n_flags
operator|&
name|NSP_RCVINTR
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|EWOULDBLOCK
expr_stmt|;
break|break;
block|}
comment|/* RETURN THE DATA */
break|break;
case|case
name|PRU_SENDOOB
case|:
comment|/* 		if interrupt data present return error (can't queue) 		if len> 16 return error 		put in xmt mbuf 		mark interrupt data available 		call nsp_output 		*/
break|break;
comment|/* 	 * NSP slow timer went off; going through this 	 * routine for tracing's sake. 	 */
case|case
name|PRU_SLOWTIMO
case|:
name|nsp_timers
argument_list|(
name|np
argument_list|,
operator|(
name|int
operator|)
name|addr
argument_list|)
expr_stmt|;
name|req
operator||=
operator|(
name|int
operator|)
name|addr
operator|<<
literal|8
expr_stmt|;
comment|/* for debug's sake */
break|break;
comment|/*** END NOT MODIFIED FOR NSP ***/
default|default:
name|panic
argument_list|(
literal|"nsp_usrreq"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|np
operator|&&
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_DEBUG
operator|)
condition|)
name|nsp_trace
argument_list|(
name|NA_USER
argument_list|,
name|ostate
argument_list|,
name|np
argument_list|,
operator|(
expr|struct
name|XXXXXXXX
operator|*
operator|)
literal|0
argument_list|,
name|req
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Attach NSP protocol to socket, allocating NSP control block,  * bufer space, and entering LISTEN state if to accept connections.  */
end_comment

begin_macro
name|nsp_attach
argument_list|(
argument|so
argument_list|,
argument|sa
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr
modifier|*
name|sa
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|nspcb
modifier|*
name|np
decl_stmt|;
name|struct
name|sockaddr_dn
modifier|*
name|sdn
init|=
operator|(
expr|struct
name|sockaddr_dn
operator|*
operator|)
name|sa
decl_stmt|;
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|int
name|error
decl_stmt|;
if|if
condition|(
name|sdn
condition|)
block|{
if|if
condition|(
name|sdn
operator|->
name|sdn_family
operator|!=
name|AF_DECNET
condition|)
return|return
operator|(
name|EAFNOSUPPORT
operator|)
return|;
comment|/* the user has specified a sockaddr with a socreate. 		all this can do is allow the user to specify an object 		type or other info if he is going to wait for a connection. 		figure this out later. */
block|}
else|else
block|{
comment|/* nothing specified, will expect a connect request soon */
block|}
name|m
operator|=
name|m_getclr
argument_list|(
name|MT_CANTWAIT
argument_list|,
name|MT_PCB
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOBUFS
operator|)
return|;
if|if
condition|(
name|sbreserve
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
literal|1024
argument_list|)
operator|==
literal|0
condition|)
block|{
name|bad
label|:
operator|(
name|void
operator|)
name|m_free
argument_list|(
name|m
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOBUFS
operator|)
return|;
block|}
if|if
condition|(
name|sbreserve
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|,
literal|1024
argument_list|)
operator|==
literal|0
condition|)
block|{
name|sbrelease
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|)
expr_stmt|;
goto|goto
name|bad
goto|;
block|}
name|np
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|nspcb
operator|*
argument_list|)
expr_stmt|;
name|np
operator|->
name|n_head
operator|=
operator|&
name|ncb
expr_stmt|;
name|insque
argument_list|(
name|np
argument_list|,
operator|&
name|ncb
argument_list|)
expr_stmt|;
name|sp
operator|->
name|so_pcb
operator|=
operator|(
name|caddr_t
operator|)
name|np
expr_stmt|;
name|sdn
operator|=
operator|(
expr|struct
name|sockaddr_dn
operator|*
operator|)
operator|&
name|so
operator|->
name|so_addr
expr_stmt|;
name|sdn
operator|->
name|sdn_family
operator|==
name|AF_DECNET
expr_stmt|;
name|sdn
operator|->
name|sdn_addr
operator|=
name|WHAT
name|ELSE
name|NEEDS
name|TO
name|BE
name|FILLED
name|IN
name|HERE
condition|?
if|if
condition|(
name|so
operator|->
name|so_options
operator|&
name|SO_ACCEPTCONN
condition|)
block|{
name|np
operator|->
name|n_state
operator|=
name|NS_LI
expr_stmt|;
block|}
else|else
name|np
operator|->
name|n_state
operator|=
name|NS_O
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*** BEGIN NOT MODIFIED FOR NSP ***/
end_comment

begin_comment
comment|/*  * Initiate (or continue) disconnect.  * If embryonic state, just send reset (once).  * If not in ``let data drain'' option, just drop.  * Otherwise (hard), mark socket disconnecting and drop  * current input data; switch states based on user close, and  * send segment to peer (with FIN).  */
end_comment

begin_macro
name|nsp_disconnect
argument_list|(
argument|np
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|nspcb
modifier|*
name|np
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|socket
modifier|*
name|so
init|=
name|np
operator|->
name|n_socket
decl_stmt|;
if|if
condition|(
name|np
operator|->
name|n_state
operator|<
name|NSPS_ESTABLISHED
condition|)
name|nsp_close
argument_list|(
name|np
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|so
operator|->
name|so_linger
operator|==
literal|0
condition|)
name|nsp_drop
argument_list|(
name|np
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|else
block|{
name|soisdisconnecting
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|sbflush
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|)
expr_stmt|;
name|nsp_usrclosed
argument_list|(
name|np
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|nsp_output
argument_list|(
name|np
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * User issued close, and wish to trail through shutdown states:  * if never received SYN, just forget it.  If got a SYN from peer,  * but haven't sent FIN, then go to FIN_WAIT_1 state to send peer a FIN.  * If already got a FIN from peer, then almost done; go to LAST_ACK  * state.  In all other cases, have already sent FIN to peer (e.g.  * after PRU_SHUTDOWN), and just have to play tedious game waiting  * for peer to send FIN or not respond to keep-alives, etc.  */
end_comment

begin_macro
name|nsp_usrclosed
argument_list|(
argument|np
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|nspcb
modifier|*
name|np
decl_stmt|;
end_decl_stmt

begin_block
block|{
switch|switch
condition|(
name|np
operator|->
name|n_state
condition|)
block|{
case|case
name|NSPS_LISTEN
case|:
case|case
name|NSPS_SYN_SENT
case|:
name|np
operator|->
name|n_state
operator|=
name|NSPS_CLOSED
expr_stmt|;
name|nsp_close
argument_list|(
name|np
argument_list|)
expr_stmt|;
break|break;
case|case
name|NSPS_SYN_RECEIVED
case|:
case|case
name|NSPS_ESTABLISHED
case|:
name|np
operator|->
name|n_state
operator|=
name|NSPS_FIN_WAIT_1
expr_stmt|;
break|break;
case|case
name|NSPS_CLOSE_WAIT
case|:
name|np
operator|->
name|n_state
operator|=
name|NSPS_LAST_ACK
expr_stmt|;
break|break;
block|}
block|}
end_block

end_unit

