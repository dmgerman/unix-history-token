begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1992 Eric P. Allman.  * Copyright (c) 1992 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)map.c	6.11 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|"sendmail.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|DBM_MAP
end_ifdef

begin_include
include|#
directive|include
file|<ndbm.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HASH_MAP
argument_list|)
operator|||
name|defined
argument_list|(
name|BTREE_MAP
argument_list|)
end_if

begin_include
include|#
directive|include
file|<db.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NIS_MAP
end_ifdef

begin_include
include|#
directive|include
file|<rpcsvc/ypclnt.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|DBM_MAP
end_ifdef

begin_comment
comment|/* **  DBM_MAP_INIT -- DBM-style map initialization ** **	Parameters: **		map -- the pointer to the actual map **		mapname -- the name of the map (for error messages) **		args -- a pointer to the config file line arguments ** **	Returns: **		TRUE -- if it could successfully open the map. **		FALSE -- otherwise. ** **	Side Effects: **		Gives an error if it can't open the map. */
end_comment

begin_function
name|bool
name|dbm_map_init
parameter_list|(
name|map
parameter_list|,
name|mapname
parameter_list|,
name|args
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|mapname
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
block|{
name|DBM
modifier|*
name|dbm
decl_stmt|;
name|map_parseargs
argument_list|(
name|map
argument_list|,
operator|&
name|args
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_file
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"No file name for DBM map %s"
argument_list|,
name|mapname
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|dbm
operator|=
name|dbm_open
argument_list|(
name|map
operator|->
name|map_file
argument_list|,
name|O_RDONLY
argument_list|,
literal|0644
argument_list|)
expr_stmt|;
if|if
condition|(
name|dbm
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_OPTIONAL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|syserr
argument_list|(
literal|"Cannot open DBM database %s"
argument_list|,
name|map
operator|->
name|map_file
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|map
operator|->
name|map_db
operator|=
operator|(
name|void
operator|*
operator|)
name|dbm
expr_stmt|;
return|return
name|TRUE
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  DBM_MAP_LOOKUP -- look up a datum in a DBM-type map ** **	Parameters: **		map -- the map to look up in. **		buf -- a pointer to to the buffer containing the key. **			This is a null terminated string. **		bufsiz -- the size of buf -- note that this is in general **			larger that strlen(buf), and buf can be changed **			in place if desired. **		av -- arguments from the config file (can be interpolated **			into the final result). **		statp -- pointer to status word (out-parameter). ** **	Returns: **		A pointer to the rewritten result. **		NULL if not found in the map. */
end_comment

begin_function
name|char
modifier|*
name|dbm_map_lookup
parameter_list|(
name|map
parameter_list|,
name|buf
parameter_list|,
name|bufsiz
parameter_list|,
name|av
parameter_list|,
name|statp
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
name|buf
index|[]
decl_stmt|;
name|int
name|bufsiz
decl_stmt|;
name|char
modifier|*
modifier|*
name|av
decl_stmt|;
name|int
modifier|*
name|statp
decl_stmt|;
block|{
name|datum
name|key
decl_stmt|,
name|val
decl_stmt|;
name|key
operator|.
name|dptr
operator|=
name|buf
expr_stmt|;
name|key
operator|.
name|dsize
operator|=
name|strlen
argument_list|(
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_NOFOLDCASE
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
for|for
control|(
name|p
operator|=
name|buf
init|;
operator|*
name|p
operator|!=
literal|'\0'
condition|;
name|p
operator|++
control|)
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isupper
argument_list|(
operator|*
name|p
argument_list|)
condition|)
operator|*
name|p
operator|=
name|tolower
argument_list|(
operator|*
name|p
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|MF_INCLNULL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|key
operator|.
name|dsize
operator|++
expr_stmt|;
operator|(
name|void
operator|)
name|lockfile
argument_list|(
name|dbm_dirfno
argument_list|(
name|map
operator|->
name|map_db
argument_list|)
argument_list|,
name|map
operator|->
name|map_file
argument_list|,
name|LOCK_SH
argument_list|)
expr_stmt|;
name|val
operator|=
name|dbm_fetch
argument_list|(
name|map
operator|->
name|map_db
argument_list|,
name|key
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|lockfile
argument_list|(
name|dbm_dirfno
argument_list|(
name|map
operator|->
name|map_db
argument_list|)
argument_list|,
name|map
operator|->
name|map_file
argument_list|,
name|LOCK_UN
argument_list|)
expr_stmt|;
if|if
condition|(
name|val
operator|.
name|dptr
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_MATCHONLY
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|map_rewrite
argument_list|(
name|val
operator|.
name|dptr
argument_list|,
name|val
operator|.
name|dsize
argument_list|,
name|buf
argument_list|,
name|bufsiz
argument_list|,
name|av
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DBM_MAP */
end_comment

begin_escape
end_escape

begin_ifdef
ifdef|#
directive|ifdef
name|BTREE_MAP
end_ifdef

begin_comment
comment|/* **  BTREE_MAP_INIT -- BTREE-style map initialization ** **	Parameters: **		map -- the pointer to the actual map **		mapname -- the name of the map (for error messages) **		args -- a pointer to the config file line arguments ** **	Returns: **		TRUE -- if it could successfully open the map. **		FALSE -- otherwise. ** **	Side Effects: **		Gives an error if it can't open the map. */
end_comment

begin_function
name|bool
name|bt_map_init
parameter_list|(
name|map
parameter_list|,
name|mapname
parameter_list|,
name|args
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|mapname
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
block|{
name|DB
modifier|*
name|db
decl_stmt|;
name|map_parseargs
argument_list|(
name|map
argument_list|,
operator|&
name|args
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_file
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"No file name for BTREE map %s"
argument_list|,
name|mapname
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|db
operator|=
name|dbopen
argument_list|(
name|map
operator|->
name|map_file
argument_list|,
name|O_RDONLY
argument_list|,
literal|0644
argument_list|,
name|DB_BTREE
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|db
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_OPTIONAL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|syserr
argument_list|(
literal|"Cannot open BTREE database %s"
argument_list|,
name|map
operator|->
name|map_file
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|map
operator|->
name|map_db
operator|=
operator|(
name|void
operator|*
operator|)
name|db
expr_stmt|;
return|return
name|TRUE
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* BTREE_MAP */
end_comment

begin_escape
end_escape

begin_ifdef
ifdef|#
directive|ifdef
name|HASH_MAP
end_ifdef

begin_comment
comment|/* **  HASH_MAP_INIT -- HASH-style map initialization ** **	Parameters: **		map -- the pointer to the actual map **		mapname -- the name of the map (for error messages) **		args -- a pointer to the config file line arguments ** **	Returns: **		TRUE -- if it could successfully open the map. **		FALSE -- otherwise. ** **	Side Effects: **		Gives an error if it can't open the map. */
end_comment

begin_function
name|bool
name|hash_map_init
parameter_list|(
name|map
parameter_list|,
name|mapname
parameter_list|,
name|args
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|mapname
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
block|{
name|DB
modifier|*
name|db
decl_stmt|;
name|map_parseargs
argument_list|(
name|map
argument_list|,
operator|&
name|args
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_file
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"No file name for HASH map %s"
argument_list|,
name|mapname
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|db
operator|=
name|dbopen
argument_list|(
name|map
operator|->
name|map_file
argument_list|,
name|O_RDONLY
argument_list|,
literal|0644
argument_list|,
name|DB_HASH
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|db
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_OPTIONAL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|syserr
argument_list|(
literal|"Cannot open HASH database %s"
argument_list|,
name|map
operator|->
name|map_file
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
name|map
operator|->
name|map_db
operator|=
operator|(
name|void
operator|*
operator|)
name|db
expr_stmt|;
return|return
name|TRUE
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HASH_MAP */
end_comment

begin_escape
end_escape

begin_if
if|#
directive|if
name|defined
argument_list|(
name|BTREE_MAP
argument_list|)
operator|||
name|defined
argument_list|(
name|HASH_MAP
argument_list|)
end_if

begin_comment
comment|/* **  DB_MAP_LOOKUP -- look up a datum in a BTREE- or HASH-type map ** **	Parameters: **		map -- the map to look up in. **		buf -- a pointer to to the buffer containing the key. **			This is a null terminated string. **		bufsiz -- the size of buf -- note that this is in general **			larger that strlen(buf), and buf can be changed **			in place if desired. **		av -- arguments from the config file (can be interpolated **			into the final result). **		statp -- pointer to status word (out-parameter). ** **	Returns: **		A pointer to the rewritten result. **		NULL if not found in the map. */
end_comment

begin_function
name|char
modifier|*
name|db_map_lookup
parameter_list|(
name|map
parameter_list|,
name|buf
parameter_list|,
name|bufsiz
parameter_list|,
name|av
parameter_list|,
name|statp
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
name|buf
index|[]
decl_stmt|;
name|int
name|bufsiz
decl_stmt|;
name|char
modifier|*
modifier|*
name|av
decl_stmt|;
name|int
modifier|*
name|statp
decl_stmt|;
block|{
name|DBT
name|key
decl_stmt|,
name|val
decl_stmt|;
name|key
operator|.
name|data
operator|=
name|buf
expr_stmt|;
name|key
operator|.
name|size
operator|=
name|strlen
argument_list|(
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_NOFOLDCASE
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
for|for
control|(
name|p
operator|=
name|buf
init|;
operator|*
name|p
operator|!=
literal|'\0'
condition|;
name|p
operator|++
control|)
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isupper
argument_list|(
operator|*
name|p
argument_list|)
condition|)
operator|*
name|p
operator|=
name|tolower
argument_list|(
operator|*
name|p
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|MF_INCLNULL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|key
operator|.
name|size
operator|++
expr_stmt|;
if|if
condition|(
operator|(
operator|(
name|DB
operator|*
operator|)
name|map
operator|->
name|map_db
operator|)
operator|->
name|get
argument_list|(
operator|(
name|DB
operator|*
operator|)
name|map
operator|->
name|map_db
argument_list|,
operator|&
name|key
argument_list|,
operator|&
name|val
argument_list|,
literal|0
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_MATCHONLY
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|map_rewrite
argument_list|(
name|val
operator|.
name|data
argument_list|,
name|val
operator|.
name|size
argument_list|,
name|buf
argument_list|,
name|bufsiz
argument_list|,
name|av
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* BTREE_MAP || HASH_MAP */
end_comment

begin_escape
end_escape

begin_comment
comment|/* **  MAP_PARSEARGS -- parse config line arguments for database lookup ** **	Parameters: **		map -- the map being initialized. **		pp -- an indirect pointer to the config line.  It will **			be replaced with a pointer to the next field **			on the line. ** **	Returns: **		none ** **	Side Effects: **		null terminates the filename; stores it in map */
end_comment

begin_macro
name|map_parseargs
argument_list|(
argument|map
argument_list|,
argument|pp
argument_list|)
end_macro

begin_decl_stmt
name|MAP
modifier|*
name|map
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
modifier|*
name|pp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|p
init|=
operator|*
name|pp
decl_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
while|while
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'-'
condition|)
break|break;
switch|switch
condition|(
operator|*
operator|++
name|p
condition|)
block|{
case|case
literal|'N'
case|:
name|map
operator|->
name|map_flags
operator||=
name|MF_INCLNULL
expr_stmt|;
break|break;
case|case
literal|'o'
case|:
name|map
operator|->
name|map_flags
operator||=
name|MF_OPTIONAL
expr_stmt|;
break|break;
case|case
literal|'f'
case|:
name|map
operator|->
name|map_flags
operator||=
name|MF_NOFOLDCASE
expr_stmt|;
break|break;
case|case
literal|'m'
case|:
name|map
operator|->
name|map_flags
operator||=
name|MF_MATCHONLY
expr_stmt|;
break|break;
case|case
literal|'a'
case|:
name|map
operator|->
name|map_app
operator|=
operator|++
name|p
expr_stmt|;
break|break;
case|case
literal|'d'
case|:
name|map
operator|->
name|map_domain
operator|=
operator|++
name|p
expr_stmt|;
break|break;
block|}
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
operator|&&
operator|!
operator|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
operator|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
operator|*
name|p
operator|++
operator|=
literal|'\0'
expr_stmt|;
block|}
if|if
condition|(
name|map
operator|->
name|map_app
operator|!=
name|NULL
condition|)
name|map
operator|->
name|map_app
operator|=
name|newstr
argument_list|(
name|map
operator|->
name|map_app
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_domain
operator|!=
name|NULL
condition|)
name|map
operator|->
name|map_domain
operator|=
name|newstr
argument_list|(
name|map
operator|->
name|map_domain
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
block|{
name|map
operator|->
name|map_file
operator|=
name|p
expr_stmt|;
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
operator|&&
operator|!
operator|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
operator|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
operator|*
name|p
operator|++
operator|=
literal|'\0'
expr_stmt|;
name|map
operator|->
name|map_file
operator|=
name|newstr
argument_list|(
name|map
operator|->
name|map_file
argument_list|)
expr_stmt|;
block|}
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
operator|&&
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
operator|*
name|pp
operator|=
name|p
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
name|map
operator|->
name|map_rebuild
operator|=
name|newstr
argument_list|(
name|p
argument_list|)
expr_stmt|;
block|}
end_block

begin_escape
end_escape

begin_ifdef
ifdef|#
directive|ifdef
name|NIS_MAP
end_ifdef

begin_comment
comment|/* **  NIS_MAP_INIT -- initialize DBM map ** **	Parameters: **		map -- the pointer to the actual map. **		mapname -- the name of the map (for error messages). **		args -- a pointer to the config file line arguments. ** **	Returns: **		TRUE -- if it could successfully open the map. **		FALSE -- otherwise. ** **	Side Effects: **		Prints an error if it can't open the map. */
end_comment

begin_function
name|bool
name|nis_map_init
parameter_list|(
name|map
parameter_list|,
name|mapname
parameter_list|,
name|args
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|mapname
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
block|{
name|int
name|yperr
decl_stmt|;
name|char
modifier|*
name|master
decl_stmt|;
comment|/* parse arguments */
name|map_parseargs
argument_list|(
name|map
argument_list|,
operator|&
name|args
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_file
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"No NIS map name for map %s"
argument_list|,
name|mapname
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
if|if
condition|(
name|map
operator|->
name|map_domain
operator|==
name|NULL
condition|)
name|yp_get_default_domain
argument_list|(
operator|&
name|map
operator|->
name|map_domain
argument_list|)
expr_stmt|;
comment|/* check to see if this map actually exists */
name|yperr
operator|=
name|yp_master
argument_list|(
name|map
operator|->
name|map_domain
argument_list|,
name|map
operator|->
name|map_file
argument_list|,
operator|&
name|master
argument_list|)
expr_stmt|;
if|if
condition|(
name|yperr
operator|==
literal|0
condition|)
return|return
name|TRUE
return|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_OPTIONAL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|syserr
argument_list|(
literal|"Cannot bind to domain %s: %s"
argument_list|,
name|map
operator|->
name|map_domain
argument_list|,
name|yperr_string
argument_list|(
name|yperr
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|FALSE
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  NIS_MAP_LOOKUP -- look up a datum in a NIS map ** **	Parameters: **		map -- the map to look up in. **		buf -- a pointer to to the buffer containing the key. **			This is a null terminated string. **		bufsiz -- the size of buf -- note that this is in general **			larger that strlen(buf), and buf can be changed **			in place if desired. **		av -- arguments from the config file (can be interpolated **			into the final result). **		statp -- pointer to status word (out-parameter). ** **	Returns: **		A pointer to the rewritten result. **		NULL if not found in the map. */
end_comment

begin_function
name|char
modifier|*
name|nis_map_lookup
parameter_list|(
name|map
parameter_list|,
name|buf
parameter_list|,
name|bufsiz
parameter_list|,
name|av
parameter_list|,
name|statp
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
name|buf
index|[]
decl_stmt|;
name|int
name|bufsiz
decl_stmt|;
name|char
modifier|*
modifier|*
name|av
decl_stmt|;
name|int
modifier|*
name|statp
decl_stmt|;
block|{
name|char
modifier|*
name|vp
decl_stmt|;
specifier|auto
name|int
name|vsize
decl_stmt|;
name|int
name|buflen
decl_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_NOFOLDCASE
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
for|for
control|(
name|p
operator|=
name|buf
init|;
operator|*
name|p
operator|!=
literal|'\0'
condition|;
name|p
operator|++
control|)
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isupper
argument_list|(
operator|*
name|p
argument_list|)
condition|)
operator|*
name|p
operator|=
name|tolower
argument_list|(
operator|*
name|p
argument_list|)
expr_stmt|;
block|}
name|buflen
operator|=
name|strlen
argument_list|(
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|MF_INCLNULL
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|buflen
operator|++
expr_stmt|;
if|if
condition|(
name|yp_match
argument_list|(
name|map
operator|->
name|map_domain
argument_list|,
name|map
operator|->
name|map_file
argument_list|,
name|buf
argument_list|,
name|buflen
argument_list|,
operator|&
name|vp
argument_list|,
operator|&
name|vsize
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MF_MATCHONLY
argument_list|,
name|map
operator|->
name|map_flags
argument_list|)
condition|)
name|map_rewrite
argument_list|(
name|vp
argument_list|,
name|vsize
argument_list|,
name|buf
argument_list|,
name|bufsiz
argument_list|,
name|av
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NIS_MAP */
end_comment

begin_escape
end_escape

begin_comment
comment|/* **  MAP_REWRITE -- rewrite a database key, interpolating %n indications. ** **	Parameters: **		s -- the string to rewrite, NOT necessarily null terminated. **		slen -- the length of s. **		buf -- the place to write it. **		buflen -- the length of buf. **		av -- arguments to interpolate into buf. ** **	Returns: **		none. ** **	Side Effects: **		none. */
end_comment

begin_expr_stmt
name|map_rewrite
argument_list|(
name|s
argument_list|,
name|slen
argument_list|,
name|buf
argument_list|,
name|buflen
argument_list|,
name|av
argument_list|)
specifier|register
name|char
operator|*
name|s
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|slen
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|buf
index|[]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|buflen
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
modifier|*
name|av
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
name|char
modifier|*
name|buflim
decl_stmt|;
specifier|register
name|char
name|c
decl_stmt|;
name|char
modifier|*
modifier|*
name|avp
decl_stmt|;
specifier|register
name|char
modifier|*
name|ap
decl_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|23
argument_list|,
literal|1
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"map_rewrite(%.*s), av =\n"
argument_list|,
name|slen
argument_list|,
name|s
argument_list|)
expr_stmt|;
for|for
control|(
name|avp
operator|=
name|av
init|;
operator|*
name|avp
operator|!=
name|NULL
condition|;
name|avp
operator|++
control|)
name|printf
argument_list|(
literal|"\t%s\n"
argument_list|,
operator|*
name|avp
argument_list|)
expr_stmt|;
block|}
name|bp
operator|=
name|buf
expr_stmt|;
name|buflim
operator|=
operator|&
name|buf
index|[
name|buflen
operator|-
literal|2
index|]
expr_stmt|;
while|while
condition|(
operator|--
name|slen
operator|>=
literal|0
operator|&&
operator|(
name|c
operator|=
operator|*
name|s
operator|++
operator|)
operator|!=
literal|'\0'
condition|)
block|{
if|if
condition|(
name|c
operator|!=
literal|'%'
condition|)
block|{
name|pushc
label|:
if|if
condition|(
name|bp
operator|<
name|buflim
condition|)
operator|*
name|bp
operator|++
operator|=
name|c
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
operator|--
name|slen
operator|<
literal|0
operator|||
operator|(
name|c
operator|=
operator|*
name|s
operator|++
operator|)
operator|==
literal|'\0'
condition|)
name|c
operator|=
literal|'%'
expr_stmt|;
if|if
condition|(
name|c
operator|==
literal|'%'
condition|)
goto|goto
name|pushc
goto|;
if|if
condition|(
operator|!
operator|(
name|isascii
argument_list|(
name|c
argument_list|)
operator|&&
name|isdigit
argument_list|(
name|c
argument_list|)
operator|)
condition|)
block|{
operator|*
name|bp
operator|++
operator|=
literal|'%'
expr_stmt|;
goto|goto
name|pushc
goto|;
block|}
name|c
operator|-=
literal|'0'
expr_stmt|;
for|for
control|(
name|avp
operator|=
name|av
init|;
operator|--
name|c
operator|>=
literal|0
operator|&&
operator|*
name|avp
operator|!=
name|NULL
condition|;
name|avp
operator|++
control|)
continue|continue;
if|if
condition|(
operator|*
name|avp
operator|==
name|NULL
condition|)
continue|continue;
comment|/* transliterate argument into output string */
for|for
control|(
name|ap
operator|=
operator|*
name|avp
init|;
operator|(
name|c
operator|=
operator|*
name|ap
operator|++
operator|)
operator|!=
literal|'\0'
condition|;
control|)
block|{
if|if
condition|(
name|bp
operator|<
name|buflim
condition|)
operator|*
name|bp
operator|++
operator|=
name|c
expr_stmt|;
block|}
block|}
operator|*
name|bp
operator|++
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|23
argument_list|,
literal|1
argument_list|)
condition|)
name|printf
argument_list|(
literal|"map_rewrite => %s\n"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

