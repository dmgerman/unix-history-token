begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|"dlvrmail.h"
end_include

begin_decl_stmt
specifier|static
name|char
name|SccsId
index|[]
init|=
literal|"@(#)conf.c	1.5	%G%"
decl_stmt|;
end_decl_stmt

begin_include
include|#
directive|include
file|<whoami.h>
end_include

begin_comment
comment|/* **  CONF.C -- Delivermail Configuration Tables. ** **	Defines the configuration of this installation. ** **	The first table describes available mailers.  This is **	just a list of argument vectors, with the following **	codes embedded: **		$u -- insert the user name. **		$h -- insert the host name. **		$f -- insert the from person name. **		$c -- insert the hop count. **	This stuff is interpreted in buildmail.  There are two **	important conventions here: entry zero must be the **	local mailer& entry one must be the shell. ** **	The second table gives a list of special characters.  This **	table is scanned linearly by parse() until an entry is **	found using one of the magic characters.  Other fields **	give more information on how to handle it. ** **	Defined Constants: **		M_* -- indices into Mailer, used only in this module. ** **	Defines: **		Mailer -- the mailer descriptor table. **		ParseTab -- the parse table. ** **	Notes: **		Ingres 11/70 version. ** **	History: **		3/5/80 -- Generalized to use<whoami.h>. **		12/26/79 -- written for Ingres 11/70. */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|ING70
end_ifdef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"i"
block|,
literal|"ingres"
block|,
literal|"ing70"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|ArpaLocal
value|NULL
end_define

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"Ing70"
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|HASARPA
end_define

begin_define
define|#
directive|define
name|V6
end_define

begin_endif
endif|#
directive|endif
endif|ING70
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|INGVAX
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"j"
block|,
literal|"ingvax"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"IngVax"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|INGVAX
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|CSVAX
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"v"
block|,
literal|"csvax"
block|,
literal|"vax"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|char
modifier|*
name|UucpLocal
index|[]
init|=
block|{
literal|"ucbvax"
block|,
literal|"ernie"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"CSVAX"
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|HASUUCP
end_define

begin_define
define|#
directive|define
name|NETV6MAIL
end_define

begin_endif
endif|#
directive|endif
endif|CSVAX
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|CORY
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"y"
block|,
literal|"cory"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"Cory"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|CORY
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|IMAGE
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"m"
block|,
literal|"image"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"Image"
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|V6
end_define

begin_endif
endif|#
directive|endif
endif|IMAGE
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|ESVAX
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"o"
block|,
literal|"esvax"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"ESVAX"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|ESVAX
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|EECS40
end_ifdef

begin_comment
comment|/* untested */
end_comment

begin_decl_stmt
specifier|static
name|char
modifier|*
name|BerkLocal
index|[]
init|=
block|{
literal|"z"
block|,
literal|"eecs40"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|MyLocNam
init|=
literal|"EECS40"
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|V6
end_define

begin_endif
endif|#
directive|endif
endif|EECS40
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|HASARPA
end_ifndef

begin_define
define|#
directive|define
name|ArpaLocal
value|NULL
end_define

begin_endif
endif|#
directive|endif
endif|HASARPA
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|HASUUCP
end_ifndef

begin_define
define|#
directive|define
name|UucpLocal
value|NULL
end_define

begin_endif
endif|#
directive|endif
endif|HASUUCP
end_endif

begin_decl_stmt
name|struct
name|mailer
name|Mailer
index|[]
init|=
block|{
comment|/* local mail -- must be #0 */
block|{
ifdef|#
directive|ifdef
name|NETV6MAIL
literal|"/usr/net/bin/v6mail"
block|,
else|#
directive|else
literal|"/bin/mail"
block|,
endif|#
directive|endif
name|M_ROPT
operator||
name|M_NOHOST
operator||
name|M_STRIPQ
block|,
name|EX_NOUSER
block|,
name|NULL
block|,
block|{
literal|"...local%mail"
block|,
literal|"-d"
block|,
literal|"$u"
block|,
name|NULL
block|}
block|}
block|,
comment|/* pipes through programs -- must be #1 */
block|{
literal|"/bin/csh"
block|,
name|M_HDR
operator||
name|M_NOHOST
block|,
name|EX_UNAVAIL
block|,
name|NULL
block|,
block|{
literal|"...prog%mail"
block|,
literal|"-fc"
block|,
literal|"$u"
block|,
name|NULL
block|}
block|}
block|,
comment|/* local berkeley mail */
block|{
literal|"/usr/net/bin/sendberkmail"
block|,
name|M_FOPT
operator||
name|M_HDR
operator||
name|M_STRIPQ
block|,
name|EX_UNAVAIL
block|,
name|BerkLocal
block|,
block|{
literal|"...berk%mail"
block|,
literal|"-m"
block|,
literal|"$h"
block|,
literal|"-t"
block|,
literal|"$u"
block|,
literal|"-h"
block|,
literal|"$c"
block|,
name|NULL
block|}
block|}
block|,
comment|/* arpanet mail */
block|{
literal|"/usr/lib/mailers/arpa"
block|,
name|M_STRIPQ
block|,
literal|0
block|,
name|ArpaLocal
block|,
block|{
literal|"...arpa%mail"
block|,
literal|"$f"
block|,
literal|"$h"
block|,
literal|"$u"
block|,
name|NULL
block|}
block|}
block|,
comment|/* uucp mail (cheat& use Bell's v7 mail) */
block|{
literal|"/bin/mail"
block|,
name|M_ROPT
operator||
name|M_NOHOST
operator||
name|M_STRIPQ
block|,
name|EX_NOUSER
block|,
name|UucpLocal
block|,
ifdef|#
directive|ifdef
name|DUMBMAIL
block|{
literal|"...uucp%mail"
block|,
literal|"$h!$u"
block|,
name|NULL
block|}
else|#
directive|else
block|{
literal|"...uucp%mail"
block|,
literal|"-d"
block|,
literal|"$h!$u"
block|,
name|NULL
block|}
endif|#
directive|endif
endif|DUMBMAIL
block|}
block|, }
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|M_LOCAL
value|0
end_define

begin_define
define|#
directive|define
name|M_BERK
value|2
end_define

begin_define
define|#
directive|define
name|M_ARPA
value|3
end_define

begin_define
define|#
directive|define
name|M_UUCP
value|4
end_define

begin_decl_stmt
name|struct
name|parsetab
name|ParseTab
index|[]
init|=
block|{
literal|':'
block|,
name|M_BERK
block|,
name|P_ONE
block|,
name|NULL
block|,
ifdef|#
directive|ifdef
name|HASARPA
literal|'@'
block|,
name|M_ARPA
block|,
name|P_HLAST
operator||
name|P_USR_UPPER
block|,
name|NULL
block|,
else|#
directive|else
literal|'@'
block|,
name|M_BERK
block|,
name|P_HLAST
operator||
name|P_USR_UPPER
operator||
name|P_MOVE
block|,
literal|"ing70"
block|,
endif|#
directive|endif
endif|HASARPA
literal|'^'
block|,
operator|-
literal|1
block|,
name|P_MAP
block|,
literal|"!"
block|,
ifdef|#
directive|ifdef
name|HASUUCP
literal|'!'
block|,
name|M_UUCP
block|,
literal|0
block|,
name|NULL
block|,
else|#
directive|else
literal|'!'
block|,
name|M_BERK
block|,
name|P_MOVE
block|,
literal|"csvax"
block|,
endif|#
directive|endif
endif|HASUUCP
literal|'.'
block|,
operator|-
literal|1
block|,
name|P_MAP
operator||
name|P_ONE
block|,
literal|":"
block|,
literal|'\0'
block|,
name|M_LOCAL
block|,
name|P_MOVE
block|,
literal|""
block|, }
decl_stmt|;
end_decl_stmt

begin_escape
end_escape

begin_comment
comment|/* **  GETNAME -- Get the current users login name. ** **	This is in config.c because it is somewhat machine dependent. **	Examine it carefully for your installation. ** **	Algorithm: **		See if the person is logged in.  If so, return **			the name s/he is logged in as. **		Look up the user id in /etc/passwd.  If found, **			return that name. **		Return NULL. ** **	Parameters: **		none ** **	Returns: **		The login name of this user. **		NULL if this person is noone. ** **	Side Effects: **		none ** **	Called By: **		main */
end_comment

begin_function
name|char
modifier|*
name|getname
parameter_list|()
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|struct
name|passwd
modifier|*
name|w
decl_stmt|;
specifier|extern
name|char
modifier|*
name|getlogin
parameter_list|()
function_decl|;
specifier|extern
name|struct
name|passwd
modifier|*
name|getpwuid
parameter_list|()
function_decl|;
specifier|static
name|char
name|namebuf
index|[
literal|9
index|]
decl_stmt|;
name|p
operator|=
name|getlogin
argument_list|()
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
operator|&&
name|p
index|[
literal|0
index|]
operator|!=
literal|'\0'
condition|)
return|return
operator|(
name|p
operator|)
return|;
ifdef|#
directive|ifdef
name|V6
name|w
operator|=
name|getpwuid
argument_list|(
name|getuid
argument_list|()
operator|&
literal|0377
argument_list|)
expr_stmt|;
else|#
directive|else
name|w
operator|=
name|getpwuid
argument_list|(
name|getuid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|V6
if|if
condition|(
name|w
operator|!=
name|NULL
condition|)
block|{
name|strcpy
argument_list|(
name|namebuf
argument_list|,
name|w
operator|->
name|pw_name
argument_list|)
expr_stmt|;
return|return
operator|(
name|namebuf
operator|)
return|;
block|}
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

begin_escape
end_escape

begin_ifdef
ifdef|#
directive|ifdef
name|V6
end_ifdef

begin_comment
comment|/* **  TTYPATH -- Get the path of the user's tty -- Version 6 version. ** **	Returns the pathname of the user's tty.  Returns NULL if **	the user is not logged in or if s/he has write permission **	denied. ** **	Parameters: **		none ** **	Returns: **		pathname of the user's tty. **		NULL if not logged in or write permission denied. ** **	Side Effects: **		none. ** **	WARNING: **		Return value is in a local buffer. ** **	Called By: **		savemail */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_function
name|char
modifier|*
name|ttypath
parameter_list|()
block|{
name|struct
name|stat
name|stbuf
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
specifier|static
name|char
name|pathn
index|[]
init|=
literal|"/dev/ttyx"
decl_stmt|;
specifier|extern
name|int
name|errno
decl_stmt|;
comment|/* compute the pathname of the controlling tty */
if|if
condition|(
operator|(
name|i
operator|=
name|ttyn
argument_list|(
literal|2
argument_list|)
operator|)
operator|==
literal|'x'
operator|&&
operator|(
name|i
operator|=
name|ttyn
argument_list|(
literal|1
argument_list|)
operator|)
operator|==
literal|'x'
operator|&&
operator|(
name|i
operator|=
name|ttyn
argument_list|(
literal|0
argument_list|)
operator|)
operator|==
literal|'x'
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
name|pathn
index|[
literal|8
index|]
operator|=
name|i
expr_stmt|;
comment|/* see if we have write permission */
if|if
condition|(
name|stat
argument_list|(
name|pathn
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|<
literal|0
operator|||
operator|!
name|flagset
argument_list|(
literal|02
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* see if the user is logged in */
if|if
condition|(
name|getlogin
argument_list|()
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
comment|/* looks good */
return|return
operator|(
name|pathn
operator|)
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  FDOPEN -- Open a stdio file given an open file descriptor. ** **	This is included here because it is standard in v7, but we **	need it in v6. ** **	Algorithm: **		Open /dev/null to create a descriptor. **		Close that descriptor. **		Copy the existing fd into the descriptor. ** **	Parameters: **		fd -- the open file descriptor. **		type -- "r", "w", or whatever. ** **	Returns: **		The file descriptor it creates. ** **	Side Effects: **		none ** **	Called By: **		deliver ** **	Notes: **		The mode of fd must match "type". */
end_comment

begin_function
name|FILE
modifier|*
name|fdopen
parameter_list|(
name|fd
parameter_list|,
name|type
parameter_list|)
name|int
name|fd
decl_stmt|;
name|char
modifier|*
name|type
decl_stmt|;
block|{
specifier|register
name|FILE
modifier|*
name|f
decl_stmt|;
name|f
operator|=
name|fopen
argument_list|(
literal|"/dev/null"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|fileno
argument_list|(
name|f
argument_list|)
argument_list|)
expr_stmt|;
name|fileno
argument_list|(
name|f
argument_list|)
operator|=
name|fd
expr_stmt|;
return|return
operator|(
name|f
operator|)
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  INDEX -- Return pointer to character in string ** **	For V7 compatibility. ** **	Parameters: **		s -- a string to scan. **		c -- a character to look for. ** **	Returns: **		If c is in s, returns the address of the first **			instance of c in s. **		NULL if c is not in s. ** **	Side Effects: **		none. */
end_comment

begin_expr_stmt
name|index
argument_list|(
name|s
argument_list|,
name|c
argument_list|)
specifier|register
name|char
operator|*
name|s
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|char
name|c
decl_stmt|;
end_decl_stmt

begin_block
block|{
while|while
condition|(
operator|*
name|s
operator|!=
literal|'\0'
condition|)
block|{
if|if
condition|(
operator|*
name|s
operator|++
operator|==
name|c
condition|)
return|return
operator|(
operator|--
name|s
operator|)
return|;
block|}
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
endif|V6
end_endif

begin_escape
end_escape

begin_ifndef
ifndef|#
directive|ifndef
name|V6
end_ifndef

begin_comment
comment|/* **  TTYPATH -- Get the path of the user's tty -- Version 7 version. ** **	Returns the pathname of the user's tty.  Returns NULL if **	the user is not logged in or if s/he has write permission **	denied. ** **	Parameters: **		none ** **	Returns: **		pathname of the user's tty. **		NULL if not logged in or write permission denied. ** **	Side Effects: **		none. ** **	WARNING: **		Return value is in a local buffer. ** **	Called By: **		savemail */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_function
name|char
modifier|*
name|ttypath
parameter_list|()
block|{
name|struct
name|stat
name|stbuf
decl_stmt|;
specifier|register
name|char
modifier|*
name|pathn
decl_stmt|;
specifier|extern
name|int
name|errno
decl_stmt|;
specifier|extern
name|char
modifier|*
name|ttyname
parameter_list|()
function_decl|;
comment|/* compute the pathname of the controlling tty */
if|if
condition|(
operator|(
name|pathn
operator|=
name|ttyname
argument_list|(
literal|2
argument_list|)
operator|)
operator|==
name|NULL
operator|&&
operator|(
name|pathn
operator|=
name|ttyname
argument_list|(
literal|1
argument_list|)
operator|)
operator|==
name|NULL
operator|&&
operator|(
name|pathn
operator|=
name|ttyname
argument_list|(
literal|0
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* see if we have write permission */
if|if
condition|(
name|stat
argument_list|(
name|pathn
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|<
literal|0
operator|||
operator|!
name|flagset
argument_list|(
literal|02
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* see if the user is logged in */
if|if
condition|(
name|getlogin
argument_list|()
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
comment|/* looks good */
return|return
operator|(
name|pathn
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
endif|V6
end_endif

end_unit

