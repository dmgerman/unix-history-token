begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|"sendmail.h"
end_include

begin_decl_stmt
specifier|static
name|char
name|SccsId
index|[]
init|=
literal|"@(#)alias.c	3.9	%G%"
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  ALIAS -- Compute aliases. ** **	Scans the file AliasFile for a set of aliases. **	If found, it arranges to deliver to them.  Uses libdbm **	database if -DDBM. ** **	Parameters: **		a -- address to alias. ** **	Returns: **		none ** **	Side Effects: **		Aliases found are expanded. ** **	Defined Constants: **		MAXRCRSN -- the maximum recursion depth. ** **	Files: **		AliasFile -- the mail aliases.  The format is **			a series of lines of the form: **				alias:name1,name2,name3,... **			where 'alias' expands to all of **			'name[i]'.  Continuations begin with **			space or tab. **		AliasFile.pag, AliasFile.dir: libdbm version **			of alias file.  Keys are aliases, datums **			(data?) are name1,name2, ... ** **	Notes: **		If NoAlias (the "-n" flag) is set, no aliasing is **			done. ** **	Deficiencies: **		It should complain about names that are aliased to **			nothing. **		It is unsophisticated about line overflows. */
end_comment

begin_define
define|#
directive|define
name|MAXRCRSN
value|10
end_define

begin_ifdef
ifdef|#
directive|ifdef
name|DBM
end_ifdef

begin_typedef
typedef|typedef
struct|struct
block|{
name|char
modifier|*
name|dptr
decl_stmt|;
name|int
name|dsize
decl_stmt|;
block|}
name|datum
typedef|;
end_typedef

begin_decl_stmt
name|datum
name|lhs
decl_stmt|,
name|rhs
decl_stmt|;
end_decl_stmt

begin_function_decl
specifier|extern
name|datum
name|fetch
parameter_list|()
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
endif|DBM
end_endif

begin_expr_stmt
name|alias
argument_list|(
name|a
argument_list|)
specifier|register
name|ADDRESS
operator|*
name|a
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|ADDRESS
modifier|*
name|q
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|extern
name|char
modifier|*
name|AliasFile
decl_stmt|;
ifndef|#
directive|ifndef
name|DBM
name|FILE
modifier|*
name|af
decl_stmt|;
name|char
name|line
index|[
name|MAXLINE
operator|+
literal|1
index|]
decl_stmt|;
name|bool
name|didalias
decl_stmt|;
name|bool
name|gotmatch
decl_stmt|;
specifier|auto
name|ADDRESS
name|al
decl_stmt|;
endif|#
directive|endif
endif|DBM
if|if
condition|(
name|NoAlias
condition|)
return|return;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|Debug
condition|)
name|printf
argument_list|(
literal|"--- alias ---\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DBM
name|dbminit
argument_list|(
name|AliasFile
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DBM
comment|/* 	**  Scan send queue for local mailer. 	**	We only have to do this once, since anything we alias 	**	to is being put at the end of the queue we are 	**	scanning or another queue.  This is because we only 	**	scan the local mailer queue. 	*/
for|for
control|(
name|q
operator|=
name|Mailer
index|[
name|M_LOCAL
index|]
operator|->
name|m_sendq
init|;
name|q
operator|!=
name|NULL
condition|;
name|q
operator|=
name|q
operator|->
name|q_next
control|)
block|{
name|To
operator|=
name|q
operator|->
name|q_paddr
expr_stmt|;
comment|/* don't realias already aliased names */
if|if
condition|(
name|bitset
argument_list|(
name|QDONTSEND
argument_list|,
name|q
operator|->
name|q_flags
argument_list|)
condition|)
continue|continue;
ifdef|#
directive|ifdef
name|DBM
comment|/* create a key for fetch */
name|lhs
operator|.
name|dptr
operator|=
name|q
operator|->
name|q_user
expr_stmt|;
name|lhs
operator|.
name|dsize
operator|=
name|strlen
argument_list|(
name|q
operator|->
name|q_user
argument_list|)
operator|+
literal|1
expr_stmt|;
name|rhs
operator|=
name|fetch
argument_list|(
name|lhs
argument_list|)
expr_stmt|;
comment|/* find this alias? */
name|p
operator|=
name|rhs
operator|.
name|dptr
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
continue|continue;
else|#
directive|else
else|DBM
name|s
operator|=
name|stab
argument_list|(
name|q
operator|->
name|q_user
argument_list|,
name|ST_ALIAS
argument_list|,
name|ST_FIND
argument_list|)
expr_stmt|;
if|if
condition|(
name|s
operator|==
name|NULL
condition|)
continue|continue;
name|p
operator|=
name|s
operator|->
name|s_alias
expr_stmt|;
endif|#
directive|endif
endif|DBM
comment|/* 		**  Match on Alias. 		**	Deliver to the target list. 		**	Remove the alias from the send queue 		**	  and put it on the Alias queue. 		*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|Debug
condition|)
name|printf
argument_list|(
literal|"%s (%s, %s) aliased to %s\n"
argument_list|,
name|q
operator|->
name|q_paddr
argument_list|,
name|q
operator|->
name|q_host
argument_list|,
name|q
operator|->
name|q_user
argument_list|,
name|p
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|Verbose
condition|)
name|message
argument_list|(
literal|"050"
argument_list|,
literal|"aliased to %s"
argument_list|,
name|p
argument_list|)
expr_stmt|;
name|q
operator|->
name|q_flags
operator||=
name|QDONTSEND
expr_stmt|;
name|sendto
argument_list|(
name|p
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/* **  FORWARD -- Try to forward mail ** **	This is similar but not identical to aliasing. ** **	Currently it is undefined, until the protocol for userinfo **	databases is finalized. ** **	Parameters: **		user -- the name of the user who's mail we **			would like to forward to. ** **	Returns: **		TRUE -- we have forwarded it somewhere. **		FALSE -- not forwarded; go ahead& deliver. ** **	Side Effects: **		New names are added to send queues. */
end_comment

begin_function
name|bool
name|forward
parameter_list|(
name|user
parameter_list|)
name|ADDRESS
modifier|*
name|user
decl_stmt|;
block|{
name|char
name|buf
index|[
literal|60
index|]
decl_stmt|;
specifier|register
name|FILE
modifier|*
name|fp
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|user
operator|->
name|q_mailer
operator|!=
name|M_LOCAL
operator|||
name|bitset
argument_list|(
name|QBADADDR
argument_list|,
name|user
operator|->
name|q_flags
argument_list|)
condition|)
return|return
operator|(
name|FALSE
operator|)
return|;
comment|/* good address -- look for .forward file in home */
operator|(
name|void
operator|)
name|expand
argument_list|(
literal|"$z/.forward"
argument_list|,
name|buf
argument_list|,
operator|&
name|buf
index|[
sizeof|sizeof
name|buf
operator|-
literal|1
index|]
argument_list|)
expr_stmt|;
name|fp
operator|=
name|fopen
argument_list|(
name|buf
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
name|NULL
condition|)
return|return
operator|(
name|FALSE
operator|)
return|;
comment|/* we do have an address to forward to -- do it */
operator|(
name|void
operator|)
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
name|buf
argument_list|,
name|fp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|p
operator|=
name|index
argument_list|(
name|buf
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
index|[
literal|0
index|]
operator|==
literal|'\0'
condition|)
return|return
operator|(
name|FALSE
operator|)
return|;
if|if
condition|(
name|Verbose
condition|)
name|message
argument_list|(
literal|"050"
argument_list|,
literal|"forwarded to %s"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
name|sendto
argument_list|(
name|buf
argument_list|,
literal|1
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
end_function

end_unit

