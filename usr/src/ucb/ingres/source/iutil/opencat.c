begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<aux.h>
end_include

begin_include
include|#
directive|include
file|<opsys.h>
end_include

begin_include
include|#
directive|include
file|<access.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)opencat.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  OPENCATALOG -- open system catalog ** **	This routine opens a system catalog into a predetermined **	cache.  If the catalog is already open, it is not reopened. ** **	The 'Desxx' struct defines which relations may be opened **	in this manner and is defined in .../source/aux.h. ** **	The relation should not be closed after use (except in **	special cases); however, it should be noclose'd after use **	if the number of tuples in the catalog may have changed. ** **	The Desxx structure has an alias field which **	is the address of the 'Admin' structure cache which holds **	the relation descriptor.  Thus, an openr need never actually **	occur. ** **	The actual desxx structure definition is in the file **	 **		catalog_desc.c ** **	which defines which relations can be cached and if any **	alias descriptors exist for the relations. That file **	can be redefined to include various caching. ** ** **	Parameters: **		name -- the name of the relation to open.  It must **			match one of the names in the Desxx **			structure. **		mode -- just like 'mode' to openr.  If zero, it **			is opened read-only; if two, it is opened **			read/write.  In fact, the catalog is always **			opened read/write, but the flags are set **			right for concurrency to think that you are **			using it as you have declared. ** **	Returns: **		none ** **	Side Effects: **		A relation is (may be) opened. ** **	Trace Flags: **		none */
end_comment

begin_macro
name|opencatalog
argument_list|(
argument|name
argument_list|,
argument|mode
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|name
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|mode
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|i
decl_stmt|;
specifier|register
name|DESC
modifier|*
name|d
decl_stmt|;
specifier|register
name|char
modifier|*
name|n
decl_stmt|;
specifier|register
name|struct
name|desxx
modifier|*
name|p
decl_stmt|;
specifier|extern
name|struct
name|desxx
name|Desxx
index|[]
decl_stmt|;
specifier|extern
name|long
name|CmOfiles
decl_stmt|;
name|n
operator|=
name|name
expr_stmt|;
comment|/* find out which descriptor it is */
for|for
control|(
name|p
operator|=
name|Desxx
init|;
name|p
operator|->
name|cach_relname
condition|;
name|p
operator|++
control|)
if|if
condition|(
name|sequal
argument_list|(
name|n
argument_list|,
name|p
operator|->
name|cach_relname
argument_list|)
condition|)
break|break;
if|if
condition|(
operator|!
name|p
operator|->
name|cach_relname
condition|)
name|syserr
argument_list|(
literal|"opencatalog: (%s)"
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|d
operator|=
name|p
operator|->
name|cach_desc
expr_stmt|;
comment|/* if it's already open, just return */
if|if
condition|(
name|d
operator|->
name|relopn
condition|)
block|{
name|clearkeys
argument_list|(
name|d
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* not open, open it */
if|if
condition|(
name|p
operator|->
name|cach_alias
condition|)
block|{
name|acc_init
argument_list|()
expr_stmt|;
name|bmove
argument_list|(
operator|(
name|char
operator|*
operator|)
name|p
operator|->
name|cach_alias
argument_list|,
operator|(
name|char
operator|*
operator|)
name|d
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|d
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
operator|(
name|i
operator|=
name|openr
argument_list|(
name|d
argument_list|,
literal|2
argument_list|,
name|n
argument_list|)
operator|)
operator|!=
literal|0
condition|)
name|syserr
argument_list|(
literal|"opencatalog: openr(%s) %d"
argument_list|,
name|n
argument_list|,
name|i
argument_list|)
expr_stmt|;
block|}
comment|/* mark it as an open file */
name|CmOfiles
operator||=
literal|1
operator|<<
name|d
operator|->
name|relfp
expr_stmt|;
block|}
comment|/* determine the mode to mark it as for concurrency */
switch|switch
condition|(
name|mode
condition|)
block|{
case|case
literal|0
case|:
comment|/* read only */
name|d
operator|->
name|relopn
operator|=
name|abs
argument_list|(
name|d
operator|->
name|relopn
argument_list|)
expr_stmt|;
break|break;
case|case
literal|2
case|:
comment|/* read-write */
name|d
operator|->
name|relopn
operator|=
operator|-
name|abs
argument_list|(
name|d
operator|->
name|relopn
argument_list|)
expr_stmt|;
break|break;
default|default:
name|syserr
argument_list|(
literal|"opencatalog(%s): mode %d"
argument_list|,
name|n
argument_list|,
name|mode
argument_list|)
expr_stmt|;
block|}
return|return;
block|}
end_block

end_unit

