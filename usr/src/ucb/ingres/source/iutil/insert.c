begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<access.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)insert.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **	INSERT - add a new tuple to a relation ** **	Insert puts a given tuple into a relation in **	the "correct" position. ** **	If insert is called with checkdups == TRUE then **	the tuple will not be inserted if it is a duplicate **	of some already existing tuple. If the relation is a **	heap then checkdups is made false. ** **	Tid will be set to the tuple id where the **	tuple is placed. ** **	returns: **<0  fatal eror **		0   success **		1   tuple was a duplicate */
end_comment

begin_expr_stmt
name|insert
argument_list|(
name|d
argument_list|,
name|tid
argument_list|,
name|tuple
argument_list|,
name|checkdups
argument_list|)
specifier|register
name|DESC
operator|*
name|d
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|TID
modifier|*
name|tid
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|tuple
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|bool
name|checkdups
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
name|int
name|need
decl_stmt|;
ifdef|#
directive|ifdef
name|xATR1
if|if
condition|(
name|tTf
argument_list|(
literal|24
argument_list|,
literal|0
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"insert:%.14s,"
argument_list|,
name|d
operator|->
name|reldum
operator|.
name|relid
argument_list|)
expr_stmt|;
name|dumptid
argument_list|(
name|tid
argument_list|)
expr_stmt|;
name|printup
argument_list|(
name|d
argument_list|,
name|tuple
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* determine how much space is needed for tuple */
name|need
operator|=
name|canonical
argument_list|(
name|d
argument_list|,
name|tuple
argument_list|)
expr_stmt|;
comment|/* find the "best" page to place tuple */
if|if
condition|(
name|i
operator|=
name|findbest
argument_list|(
name|d
argument_list|,
name|tid
argument_list|,
name|tuple
argument_list|,
name|need
argument_list|,
name|checkdups
argument_list|)
condition|)
return|return
operator|(
name|i
operator|)
return|;
comment|/* put tuple in position "tid" */
name|put_tuple
argument_list|(
name|tid
argument_list|,
name|Acctuple
argument_list|,
name|need
argument_list|)
expr_stmt|;
name|d
operator|->
name|reladds
operator|++
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

