begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<access.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)getequal.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **	getequal - get the first tuple equal to the provided key **	 **	GETEQUAL is used to do a keyed retrieval of a single **	tuple in cases where the calling program knows the key to **	be unique.  SETKEY must be called first to set all desired **	domain values.  GETEQUAL will return the first tuple with **	equality on all of the specified domains. **	The tuple is returned in TUPLE. **	 **	function values: **	 **<0 fatal error **		 0  success **		 1  no match ** **	Trace Flags: **		23.8-15 */
end_comment

begin_expr_stmt
name|getequal
argument_list|(
name|d
argument_list|,
name|key
argument_list|,
name|tuple
argument_list|,
name|tid
argument_list|)
specifier|register
name|DESC
operator|*
name|d
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|char
name|key
index|[
name|MAXTUP
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|tuple
index|[
name|MAXTUP
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|TID
modifier|*
name|tid
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|TID
name|limtid
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
ifdef|#
directive|ifdef
name|xATR1
if|if
condition|(
name|tTf
argument_list|(
literal|23
argument_list|,
literal|8
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"getequal: %.14s,"
argument_list|,
name|d
operator|->
name|reldum
operator|.
name|relid
argument_list|)
expr_stmt|;
name|printdesc
argument_list|(
name|d
argument_list|)
expr_stmt|;
name|printup
argument_list|(
name|d
argument_list|,
name|key
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|i
operator|=
name|find
argument_list|(
name|d
argument_list|,
name|EXACTKEY
argument_list|,
name|tid
argument_list|,
operator|&
name|limtid
argument_list|,
name|key
argument_list|)
condition|)
return|return
operator|(
name|i
operator|)
return|;
while|while
condition|(
operator|(
name|i
operator|=
name|get
argument_list|(
name|d
argument_list|,
name|tid
argument_list|,
operator|&
name|limtid
argument_list|,
name|tuple
argument_list|,
name|TRUE
argument_list|)
operator|)
operator|==
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|kcompare
argument_list|(
name|d
argument_list|,
name|key
argument_list|,
name|tuple
argument_list|)
condition|)
block|{
ifdef|#
directive|ifdef
name|xATR2
if|if
condition|(
name|tTf
argument_list|(
literal|23
argument_list|,
literal|9
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"getequal: "
argument_list|)
expr_stmt|;
name|dumptid
argument_list|(
name|tid
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"getequal: "
argument_list|)
expr_stmt|;
name|printup
argument_list|(
name|d
argument_list|,
name|tuple
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
return|return
operator|(
literal|0
operator|)
return|;
block|}
block|}
ifdef|#
directive|ifdef
name|xATR2
if|if
condition|(
name|tTf
argument_list|(
literal|23
argument_list|,
literal|10
argument_list|)
condition|)
name|printf
argument_list|(
literal|"getequal: %d\n"
argument_list|,
name|i
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|i
operator|)
return|;
block|}
end_block

end_unit

