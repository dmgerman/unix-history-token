begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"constants.h"
end_include

begin_include
include|#
directive|include
file|"globals.h"
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)comment.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  COMMENT.C -- routine to scan comments inside an equel statement ** **	Uses the endcmnt token code to find the endcmnt **	terminal string, then reads the input until it sees this  **	terminal (must be<= 2 characters), returning EOF_TOK and **	giving an error diagnostic if end-of-file is encountered. ** **	Parameters: **		none ** **	Returns: **		CONTINUE -- valid comment **		EOF_TOK -- EOF in comment ** **	Side Effects: **		deletes comments from within an equel statement */
end_comment

begin_macro
name|comment
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|,
name|l
decl_stmt|;
specifier|register
name|struct
name|optab
modifier|*
name|op
decl_stmt|;
name|char
name|buf
index|[
literal|3
index|]
decl_stmt|;
comment|/* find end of comment operator */
for|for
control|(
name|op
operator|=
name|Optab
init|;
name|op
operator|->
name|op_term
condition|;
name|op
operator|++
control|)
if|if
condition|(
name|op
operator|->
name|op_token
operator|==
name|Tokens
operator|.
name|sp_endcmnt
condition|)
break|break;
if|if
condition|(
operator|!
name|op
operator|->
name|op_term
condition|)
name|syserr
argument_list|(
literal|"no end of comment operator in the parse tables"
argument_list|)
expr_stmt|;
comment|/* scan for the end of comment */
name|l
operator|=
name|length
argument_list|(
name|op
operator|->
name|op_term
argument_list|)
expr_stmt|;
if|if
condition|(
name|l
operator|>
sizeof|sizeof
name|buf
operator|-
literal|1
condition|)
name|syserr
argument_list|(
literal|"comment : buf too short for endcmnt %s %d"
argument_list|,
name|op
operator|->
name|op_term
argument_list|,
name|l
argument_list|)
expr_stmt|;
comment|/* fill buffer to length of endmnt terminal */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|l
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
operator|(
name|buf
index|[
name|i
index|]
operator|=
name|getch
argument_list|()
operator|)
operator|==
name|EOF_TOK
condition|)
block|{
name|nontermcom
label|:
comment|/* non-terminated comment */
name|yysemerr
argument_list|(
literal|"premature EOF encountered in comment"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
name|EOF_TOK
operator|)
return|;
block|}
block|}
comment|/* shift on input until endcmnt */
while|while
condition|(
operator|!
name|bequal
argument_list|(
name|buf
argument_list|,
name|op
operator|->
name|op_term
argument_list|,
name|l
argument_list|)
condition|)
block|{
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|l
operator|-
literal|1
condition|;
name|i
operator|++
control|)
name|buf
index|[
name|i
index|]
operator|=
name|buf
index|[
name|i
operator|+
literal|1
index|]
expr_stmt|;
if|if
condition|(
operator|(
name|buf
index|[
name|l
operator|-
literal|1
index|]
operator|=
name|getch
argument_list|()
operator|)
operator|==
name|EOF_TOK
condition|)
goto|goto
name|nontermcom
goto|;
block|}
return|return
operator|(
name|CONTINUE
operator|)
return|;
block|}
end_block

end_unit

