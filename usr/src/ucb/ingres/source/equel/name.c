begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"constants.h"
end_include

begin_include
include|#
directive|include
file|"globals.h"
end_include

begin_include
include|#
directive|include
file|"y.tab.h"
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)name.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  NAME -- Process an identifier or keyword token. ** **	Name gets the identifier that follows in the std. **	input, and checks if it is a keyword. **	An identifier is defined as a sequence of **	MAXNAME or fewer alphanumerics, starting with an **	alphabetic character. ** **	Parameters: **		chr - the first character of the identifier ** **	Returns: **		Tokens.sp_name - for a user-defined name **		Tokens.sp_struct_var -- if the name is declared  **			a structurw variable **		other - lexical codes for keys ** **	Side Effects: **		Adds a token to the symbol space. **		yylval is set to the new node in the space. **		If the identifier is a keyword, sets Opcode to **		op_code from tokens.y. */
end_comment

begin_macro
name|name
argument_list|(
argument|chr
argument_list|)
end_macro

begin_decl_stmt
name|char
name|chr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|lval
decl_stmt|;
specifier|register
name|i
expr_stmt|;
name|char
name|wbuf
index|[
name|MAXNAME
operator|+
literal|1
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|cp
decl_stmt|;
specifier|register
name|char
name|c
decl_stmt|;
name|struct
name|optab
modifier|*
name|op
decl_stmt|;
name|struct
name|optab
modifier|*
name|getkey
parameter_list|()
function_decl|;
name|struct
name|cvar
modifier|*
name|getcvar
parameter_list|()
function_decl|;
name|c
operator|=
name|chr
expr_stmt|;
name|cp
operator|=
name|wbuf
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<=
name|MAXNAME
condition|;
name|i
operator|++
control|)
block|{
name|lval
operator|=
name|Cmap
index|[
name|c
index|]
expr_stmt|;
if|if
condition|(
name|i
operator|<
name|MAXNAME
operator|&&
operator|(
name|lval
operator|==
name|ALPHA
operator|||
name|lval
operator|==
name|NUMBR
operator|)
condition|)
block|{
operator|*
name|cp
operator|++
operator|=
name|c
expr_stmt|;
name|c
operator|=
name|getch
argument_list|()
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|lval
operator|==
name|ALPHA
operator|||
name|lval
operator|==
name|NUMBR
condition|)
block|{
comment|/* {i == MAXNAME&& "c is legal"&&  			 *  cp ==&wbuf [MAXNAME]}  			 */
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
name|yysemerr
argument_list|(
literal|"name too long"
argument_list|,
name|wbuf
argument_list|)
expr_stmt|;
comment|/* chomp to end of identifier */
do|do
block|{
name|c
operator|=
name|getch
argument_list|()
expr_stmt|;
name|lval
operator|=
name|Cmap
index|[
name|c
index|]
expr_stmt|;
block|}
do|while
condition|(
name|lval
operator|==
name|ALPHA
operator|||
name|lval
operator|==
name|NUMBR
condition|)
do|;
name|backup
argument_list|(
name|c
argument_list|)
expr_stmt|;
comment|/* take first MAXNAME characters as IDENTIFIER  			 * (non-key) 			 */
name|yylval
operator|.
name|u_dn
operator|=
name|addsym
argument_list|(
name|salloc
argument_list|(
name|wbuf
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|Tokens
operator|.
name|sp_name
operator|)
return|;
block|}
else|else
block|{
comment|/* {cp<=&wbuf [MAXNAME]&& i<= MAXNAME 			 *&& "c is not part of id"} 			 */
name|backup
argument_list|(
name|c
argument_list|)
expr_stmt|;
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
name|i
operator|=
literal|0
expr_stmt|;
break|break;
block|}
block|}
name|op
operator|=
name|getkey
argument_list|(
name|wbuf
argument_list|)
expr_stmt|;
comment|/* Is it a keyword ? */
if|if
condition|(
name|op
condition|)
block|{
name|yylval
operator|.
name|u_dn
operator|=
name|addsym
argument_list|(
name|op
operator|->
name|op_term
argument_list|)
expr_stmt|;
name|Opcode
operator|=
name|op
operator|->
name|op_code
expr_stmt|;
return|return
operator|(
name|op
operator|->
name|op_token
operator|)
return|;
block|}
comment|/* user-defined name */
name|yylval
operator|.
name|u_dn
operator|=
name|addsym
argument_list|(
name|salloc
argument_list|(
name|wbuf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|getcvar
argument_list|(
name|wbuf
argument_list|)
operator|->
name|c_type
operator|==
name|opSTRUCT
condition|)
return|return
operator|(
name|Tokens
operator|.
name|sp_struct_var
operator|)
return|;
return|return
operator|(
name|Tokens
operator|.
name|sp_name
operator|)
return|;
block|}
end_block

end_unit

