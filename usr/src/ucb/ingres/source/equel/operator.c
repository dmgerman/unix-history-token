begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"constants.h"
end_include

begin_include
include|#
directive|include
file|"globals.h"
end_include

begin_include
include|#
directive|include
file|"y.tab.h"
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)operator.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  OPERATOR -- process a token starting with an operator ** **	Processes operators, strings, comments, and  **	floating constants without a leading 0. ** **	Parameters: **		chr - first character of token {Cmap [chr] == OPATR} ** **	Returns: **		NUMBER or STRING token, or operator token. **		CONTINUE on error. ** **	Side Effects: **		Adds a node to the Symbol space, and returns adress **		in "yylval". **		Opcode is set to the opcode of the operator. **		May backup a character. */
end_comment

begin_macro
name|operator
argument_list|(
argument|chr
argument_list|)
end_macro

begin_decl_stmt
name|char
name|chr
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|optab
modifier|*
name|op
decl_stmt|;
name|char
name|opbuf
index|[
literal|3
index|]
decl_stmt|;
name|opbuf
index|[
literal|0
index|]
operator|=
name|chr
expr_stmt|;
name|opbuf
index|[
literal|1
index|]
operator|=
name|getch
argument_list|()
expr_stmt|;
name|opbuf
index|[
literal|2
index|]
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|opbuf
index|[
literal|0
index|]
operator|==
literal|'.'
operator|&&
name|Cmap
index|[
name|opbuf
index|[
literal|1
index|]
index|]
operator|==
name|NUMBR
condition|)
block|{
comment|/* floating mantissa w/o leading 0 */
name|backup
argument_list|(
name|opbuf
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
return|return
operator|(
name|number
argument_list|(
name|opbuf
index|[
literal|0
index|]
argument_list|)
operator|)
return|;
block|}
if|if
condition|(
name|Cmap
index|[
name|opbuf
index|[
literal|1
index|]
index|]
operator|!=
name|OPATR
condition|)
block|{
name|backup
argument_list|(
name|opbuf
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|opbuf
index|[
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
block|}
comment|/* operator has been reduced to its smallest  	 * possible length, now try to find it in the 	 * operator table [tokens.y] 	 */
for|for
control|(
init|;
condition|;
control|)
block|{
for|for
control|(
name|op
operator|=
name|Optab
init|;
name|op
operator|->
name|op_term
condition|;
name|op
operator|++
control|)
if|if
condition|(
name|sequal
argument_list|(
name|op
operator|->
name|op_term
argument_list|,
name|opbuf
argument_list|)
condition|)
break|break;
if|if
condition|(
operator|!
name|op
operator|->
name|op_term
operator|&&
name|opbuf
index|[
literal|1
index|]
condition|)
block|{
comment|/* reduce a 2 char operator to 1 char, 			 * and re-search 			 */
name|backup
argument_list|(
name|opbuf
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|opbuf
index|[
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
continue|continue;
block|}
break|break;
block|}
if|if
condition|(
name|op
operator|->
name|op_term
condition|)
block|{
comment|/* string quotes ? */
if|if
condition|(
name|op
operator|->
name|op_token
operator|==
name|Tokens
operator|.
name|sp_quote
condition|)
return|return
operator|(
name|string
argument_list|(
name|op
argument_list|)
operator|)
return|;
comment|/* comment indicator ? */
if|if
condition|(
name|op
operator|->
name|op_token
operator|==
name|Tokens
operator|.
name|sp_bgncmnt
condition|)
return|return
operator|(
name|comment
argument_list|()
operator|)
return|;
comment|/* {sequal(opbuf, op->op_term)} */
name|Opcode
operator|=
name|op
operator|->
name|op_code
expr_stmt|;
name|yylval
operator|.
name|u_dn
operator|=
name|addsym
argument_list|(
name|op
operator|->
name|op_term
argument_list|)
expr_stmt|;
return|return
operator|(
name|op
operator|->
name|op_token
operator|)
return|;
block|}
name|yysemerr
argument_list|(
literal|"bad operator"
argument_list|,
name|opbuf
argument_list|)
expr_stmt|;
comment|/* operator not found, skip token and try again */
return|return
operator|(
name|CONTINUE
operator|)
return|;
block|}
end_block

end_unit

