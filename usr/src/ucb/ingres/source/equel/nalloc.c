begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)nalloc.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **	NALLOC -- **		Dynamic allocation routine which ** 		merely calls alloc(III), returning  **		0 if no core and a pointer otherwise. ** */
end_comment

begin_function
name|char
modifier|*
name|nalloc
parameter_list|(
name|s
parameter_list|)
name|int
name|s
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|PDP11
name|unsigned
name|size
decl_stmt|;
name|size
operator|=
name|s
expr_stmt|;
name|size
operator|=
name|alloc
argument_list|(
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|size
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|size
operator|)
return|;
else|#
directive|else
specifier|extern
name|char
modifier|*
name|malloc
parameter_list|()
function_decl|;
return|return
operator|(
name|malloc
argument_list|(
name|s
argument_list|)
operator|)
return|;
endif|#
directive|endif
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **	SALLOC -- allocate **		place for string and initialize it, **		return string or 0 if no core. ** */
end_comment

begin_function
name|char
modifier|*
name|salloc
parameter_list|(
name|s
parameter_list|)
name|char
modifier|*
name|s
decl_stmt|;
block|{
specifier|register
name|unsigned
name|i
decl_stmt|;
specifier|register
name|char
modifier|*
name|string
decl_stmt|;
name|char
modifier|*
name|malloc
parameter_list|()
function_decl|;
name|string
operator|=
name|s
expr_stmt|;
ifdef|#
directive|ifdef
name|PDP11
name|i
operator|=
name|alloc
argument_list|(
name|length
argument_list|(
name|string
argument_list|)
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
else|#
directive|else
name|i
operator|=
operator|(
name|unsigned
operator|)
name|malloc
argument_list|(
name|length
argument_list|(
name|string
argument_list|)
operator|+
literal|1
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|i
condition|)
name|smove
argument_list|(
name|string
argument_list|,
name|i
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|char
operator|*
operator|)
name|i
operator|)
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **	XFREE -- Free possibly dynamic storage **		checking if its in the heap first. ** **		0 - freed **		1 - not in heap ** */
end_comment

begin_macro
name|xfree
argument_list|(
argument|cp
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|cp
decl_stmt|;
end_decl_stmt

begin_block
block|{
extern|extern 			end;
comment|/* break (II) */
specifier|register
name|char
modifier|*
name|lcp
decl_stmt|,
modifier|*
name|lend
decl_stmt|,
modifier|*
name|lacp
decl_stmt|;
name|lcp
operator|=
name|cp
expr_stmt|;
name|lacp
operator|=
operator|(
name|char
operator|*
operator|)
operator|&
name|cp
expr_stmt|;
name|lend
operator|=
operator|(
name|char
operator|*
operator|)
operator|&
name|end
expr_stmt|;
if|if
condition|(
name|lcp
operator|>=
name|lend
operator|&&
name|lcp
operator|<
name|lacp
condition|)
comment|/* make sure its in heap */
block|{
name|free
argument_list|(
name|lcp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

end_unit

