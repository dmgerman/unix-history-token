begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<tree.h>
end_include

begin_include
include|#
directive|include
file|<symbol.h>
end_include

begin_include
include|#
directive|include
file|"qrymod.h"
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)tree.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  TREE -- create new tree node. ** **	This is a stripped down version of the same thing in the **	parser. ** **	It only knows about lengths of zero and two. ** **	Parameters: **		lptr -- the left pointer. **		rptr -- the right pointer. **		typ -- the node type. **		len -- the node length. **		value -- the node value. ** **	Returns: **		A pointer to the created node. ** **	Side Effects: **		Space is taken from Qbuf. */
end_comment

begin_function
name|QTREE
modifier|*
name|tree
parameter_list|(
name|lptr
parameter_list|,
name|rptr
parameter_list|,
name|typ
parameter_list|,
name|len
parameter_list|,
name|value
parameter_list|)
name|QTREE
modifier|*
name|lptr
decl_stmt|;
name|QTREE
modifier|*
name|rptr
decl_stmt|;
name|char
name|typ
decl_stmt|;
name|int
name|len
decl_stmt|;
name|int
name|value
decl_stmt|;
block|{
specifier|register
name|QTREE
modifier|*
name|tptr
decl_stmt|;
specifier|extern
name|char
modifier|*
name|need
parameter_list|()
function_decl|;
specifier|register
name|int
name|l
decl_stmt|;
name|l
operator|=
name|len
expr_stmt|;
name|tptr
operator|=
operator|(
name|QTREE
operator|*
operator|)
name|need
argument_list|(
name|Qbuf
argument_list|,
name|l
operator|+
name|QT_HDR_SIZ
argument_list|)
expr_stmt|;
name|tptr
operator|->
name|left
operator|=
name|lptr
expr_stmt|;
name|tptr
operator|->
name|right
operator|=
name|rptr
expr_stmt|;
name|tptr
operator|->
name|sym
operator|.
name|type
operator|=
name|typ
expr_stmt|;
name|tptr
operator|->
name|sym
operator|.
name|len
operator|=
name|l
expr_stmt|;
if|if
condition|(
name|l
operator|>
literal|0
condition|)
name|tptr
operator|->
name|sym
operator|.
name|value
operator|.
name|sym_data
operator|.
name|i2type
operator|=
name|value
expr_stmt|;
return|return
operator|(
name|tptr
operator|)
return|;
block|}
end_function

end_unit

