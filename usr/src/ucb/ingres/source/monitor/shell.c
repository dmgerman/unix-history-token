begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"monitor.h"
end_include

begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<aux.h>
end_include

begin_include
include|#
directive|include
file|<opsys.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)shell.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  SHELL -- call unix shell ** **	The UNIX shell is called.  Shell() first tries to call an **	alternate shell defined by the macro {shell}, and if it fails **	calls /bin/sh. ** **	If an argument is supplied, it is a shell file which is **	supplied to the shell. ** **	Trace Flags: **		34 */
end_comment

begin_macro
name|shell
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|char
modifier|*
name|shellfile
decl_stmt|;
specifier|extern
name|char
modifier|*
name|getfilenm
parameter_list|()
function_decl|;
specifier|extern
name|char
modifier|*
name|macro
parameter_list|()
function_decl|;
name|shellfile
operator|=
name|getfilenm
argument_list|()
expr_stmt|;
if|if
condition|(
operator|*
name|shellfile
operator|==
literal|0
condition|)
name|shellfile
operator|=
literal|0
expr_stmt|;
name|fclose
argument_list|(
name|Qryiop
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|Xwaitpid
operator|=
name|fork
argument_list|()
operator|)
operator|==
operator|-
literal|1
condition|)
name|syserr
argument_list|(
literal|"shell: fork"
argument_list|)
expr_stmt|;
if|if
condition|(
name|Xwaitpid
operator|==
literal|0
condition|)
block|{
name|setuid
argument_list|(
name|getuid
argument_list|()
argument_list|)
expr_stmt|;
ifndef|#
directive|ifndef
name|xB_UNIX
name|setgid
argument_list|(
name|getgid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
for|for
control|(
name|i
operator|=
literal|3
init|;
name|i
operator|<
name|NOFILE
condition|;
name|i
operator|++
control|)
name|close
argument_list|(
name|i
argument_list|)
expr_stmt|;
name|p
operator|=
name|macro
argument_list|(
literal|"{shell}"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|xMTR3
name|tTfp
argument_list|(
literal|34
argument_list|,
literal|0
argument_list|,
literal|"{shell} = '%o'\n"
argument_list|,
name|p
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|p
operator|!=
literal|0
condition|)
block|{
name|execl
argument_list|(
name|p
argument_list|,
name|p
argument_list|,
name|shellfile
argument_list|,
name|Qbname
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"Cannot call %s; using /bin/sh\n"
argument_list|,
name|p
argument_list|)
expr_stmt|;
block|}
name|execl
argument_list|(
literal|"/bin/sh"
argument_list|,
literal|"sh"
argument_list|,
name|shellfile
argument_list|,
name|Qbname
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|syserr
argument_list|(
literal|"shell: exec"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|Nodayfile
operator|>=
literal|0
condition|)
name|printf
argument_list|(
literal|">>shell\n"
argument_list|)
expr_stmt|;
comment|/* wait for shell to complete */
name|xwait
argument_list|()
expr_stmt|;
block|}
end_block

end_unit

