begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)atof.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  ATOF -- ASCII TO FLOATING CONVERSION ** **	Converts the string 'str' to floating point and stores the **	result into the cell pointed to by 'val'. ** **	The syntax which it accepts is pretty much what you would **	expect.  Basically, it is: **		{<sp>} [+|-] {<sp>} {<digit>} [.{digit}] {<sp>} [<exp>] **	where<exp> is "e" or "E" followed by an integer,<sp> is a **	space character,<digit> is zero through nine, [] is zero or **	one, and {} is zero or more. ** **	Parameters: **		str -- string to convert. **		val -- pointer to place to put the result (which **			must be type double). ** **	Returns: **		zero -- ok. **		-1 -- syntax error. **		+1 -- overflow (someday). ** **	Side Effects: **		clobbers *val. */
end_comment

begin_macro
name|atof
argument_list|(
argument|str
argument_list|,
argument|val
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|str
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|double
modifier|*
name|val
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
name|double
name|v
decl_stmt|;
specifier|extern
name|double
name|pow
parameter_list|()
function_decl|;
name|double
name|fact
decl_stmt|;
name|int
name|minus
decl_stmt|;
specifier|register
name|char
name|c
decl_stmt|;
name|int
name|expon
decl_stmt|;
specifier|register
name|int
name|gotmant
decl_stmt|;
name|v
operator|=
literal|0.0
expr_stmt|;
name|p
operator|=
name|str
expr_stmt|;
name|minus
operator|=
literal|0
expr_stmt|;
comment|/* skip leading blanks */
while|while
condition|(
name|c
operator|=
operator|*
name|p
condition|)
block|{
if|if
condition|(
name|c
operator|!=
literal|' '
condition|)
break|break;
name|p
operator|++
expr_stmt|;
block|}
comment|/* handle possible sign */
switch|switch
condition|(
name|c
condition|)
block|{
case|case
literal|'-'
case|:
name|minus
operator|++
expr_stmt|;
case|case
literal|'+'
case|:
name|p
operator|++
expr_stmt|;
block|}
comment|/* skip blanks after sign */
while|while
condition|(
name|c
operator|=
operator|*
name|p
condition|)
block|{
if|if
condition|(
name|c
operator|!=
literal|' '
condition|)
break|break;
name|p
operator|++
expr_stmt|;
block|}
comment|/* start collecting the number to the decimal point */
name|gotmant
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|c
operator|=
operator|*
name|p
expr_stmt|;
if|if
condition|(
name|c
operator|<
literal|'0'
operator|||
name|c
operator|>
literal|'9'
condition|)
break|break;
name|v
operator|=
name|v
operator|*
literal|10.0
operator|+
operator|(
name|c
operator|-
literal|'0'
operator|)
expr_stmt|;
name|gotmant
operator|++
expr_stmt|;
name|p
operator|++
expr_stmt|;
block|}
comment|/* check for fractional part */
if|if
condition|(
name|c
operator|==
literal|'.'
condition|)
block|{
name|fact
operator|=
literal|1.0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|c
operator|=
operator|*
operator|++
name|p
expr_stmt|;
if|if
condition|(
name|c
operator|<
literal|'0'
operator|||
name|c
operator|>
literal|'9'
condition|)
break|break;
name|fact
operator|*=
literal|0.1
expr_stmt|;
name|v
operator|+=
operator|(
name|c
operator|-
literal|'0'
operator|)
operator|*
name|fact
expr_stmt|;
name|gotmant
operator|++
expr_stmt|;
block|}
block|}
comment|/* skip blanks before possible exponent */
while|while
condition|(
name|c
operator|=
operator|*
name|p
condition|)
block|{
if|if
condition|(
name|c
operator|!=
literal|' '
condition|)
break|break;
name|p
operator|++
expr_stmt|;
block|}
comment|/* test for exponent */
if|if
condition|(
name|c
operator|==
literal|'e'
operator|||
name|c
operator|==
literal|'E'
condition|)
block|{
name|p
operator|++
expr_stmt|;
if|if
condition|(
name|atoi
argument_list|(
name|p
argument_list|,
operator|&
name|expon
argument_list|)
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
if|if
condition|(
operator|!
name|gotmant
condition|)
name|v
operator|=
literal|1.0
expr_stmt|;
name|fact
operator|=
name|expon
expr_stmt|;
name|v
operator|*=
name|pow
argument_list|(
literal|10.0
argument_list|,
name|fact
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* if no exponent, then nothing */
if|if
condition|(
name|c
operator|!=
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
comment|/* store the result and exit */
if|if
condition|(
name|minus
condition|)
name|v
operator|=
operator|-
name|v
expr_stmt|;
operator|*
name|val
operator|=
name|v
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

