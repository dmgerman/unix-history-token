begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"ctlmod.h"
end_include

begin_include
include|#
directive|include
file|"pipes.h"
end_include

begin_include
include|#
directive|include
file|<resp.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)do_seq.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  DO_SEQ -- do sequence of states ** **	The sequence of states indicated by the start state **	ppb->pb_st is executed. ** **	Recursion and multiple processes are implemented in **	this routine.  For each iteration through the top **	loop, the next state to call is checked.  If the state **	is local (in this process), the state is executed and **	the next state is computed.  If the state is remote, **	(contained in another process), we write out the **	current call on a pipe which will (eventually) get **	to the target process.  In this case (and the system **	reset case) the next state is unknown.  This causes **	this routine to read the input pipe for a command. **	The command read is then executed -- notice that it **	does not matter if this command is part of this level **	of call (a sibling of the 'call'd process) or a lower **	level (a descendent), since 'call' saved the state. ** **	If there are no more states in the chain after execution **	of a local state, then a response block is generated. **	The 'readinput' call can also generate one of these. **	If the return is local (to this process), it is just **	returned.  Otherwise, it sends it off to the process **	currently blocked for this state sequence to complete, **	and the PB_UNKNOWN state is reentered. ** **	Parameters: **		ppb -- a pointer to the pipe block which **			(a) describes the state to call and **			(b) provides an I/O area. ** **	Returns: **		none ** **	Side Effects: **		Lots.  The side effects of all the states. **		Leaves 'Resp' set to the response of the last **			state in the chain. ** **	Called By: **		call **		main ** **	Trace Flags: **		2.0 - 2.7 */
end_comment

begin_expr_stmt
name|do_seq
argument_list|(
name|ppb
argument_list|)
specifier|register
name|pb_t
operator|*
name|ppb
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
comment|/* 	**  Top Loop. 	**	Iterate until we have a response block intended 	**	for this process (and hence this state, since 	**	state invocations are properly nested inside 	**	process invocations). 	**	We also insure that we can always get at the 	**	current pipe block. 	** 	**	We return from the setjmp with non-zero value if 	**	we get a fatal error. 	*/
name|Ctx
operator|.
name|ctx_ppb
operator|=
name|ppb
expr_stmt|;
if|if
condition|(
name|setjmp
argument_list|(
name|Ctx
operator|.
name|ctx_jbuf
argument_list|)
operator|!=
literal|0
condition|)
return|return;
for|for
control|(
init|;
condition|;
control|)
block|{
ifdef|#
directive|ifdef
name|xCTR1
if|if
condition|(
name|tTf
argument_list|(
literal|2
argument_list|,
literal|0
argument_list|)
condition|)
name|lprintf
argument_list|(
literal|"do_seq: state %d\n"
argument_list|,
name|ppb
operator|->
name|pb_st
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* take cases */
switch|switch
condition|(
name|ppb
operator|->
name|pb_st
condition|)
block|{
case|case
name|PB_UNKNOWN
case|:
comment|/* 			**  Read the input and get a state, since we 			**  don't have one already.  This will set 			**  Parmc& Parmv and change *ppb.  The 			**  state it found will be processed next 			**  time through the loop. 			*/
name|readinput
argument_list|(
name|ppb
argument_list|)
expr_stmt|;
break|break;
case|case
name|PB_NONE
case|:
comment|/* 			**  The 'no next state' case, i.e., a response. 			**	This only happens if the response is 			**	for this process, so we just return. 			*/
ifdef|#
directive|ifdef
name|xCTR1
if|if
condition|(
name|tTf
argument_list|(
literal|2
argument_list|,
literal|1
argument_list|)
condition|)
name|lprintf
argument_list|(
literal|"do_seq: exit\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return;
default|default:
comment|/* 			** The state is known, let do_st do all 			** the dirty work.  Hand it the global 			** Parmc& Parmv to pass to the function. 			** Do_st returns the next state to execute. 			*/
name|ppb
operator|->
name|pb_st
operator|=
name|i
operator|=
name|do_st
argument_list|(
name|ppb
argument_list|,
name|Ctx
operator|.
name|ctx_pc
argument_list|,
name|Ctx
operator|.
name|ctx_pv
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|==
name|PB_NONE
condition|)
block|{
ifdef|#
directive|ifdef
name|xCM_DEBUG
if|if
condition|(
name|ppb
operator|->
name|pb_resp
operator|==
name|PB_UNKNOWN
condition|)
name|syserr
argument_list|(
literal|"do_seq: pb_resp"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|ppb
operator|->
name|pb_proc
operator|=
name|ppb
operator|->
name|pb_resp
expr_stmt|;
if|if
condition|(
name|ppb
operator|->
name|pb_proc
operator|!=
name|Cm
operator|.
name|cm_myproc
condition|)
block|{
comment|/* write to correct process */
name|pb_prime
argument_list|(
name|ppb
argument_list|,
name|PB_RESP
argument_list|)
expr_stmt|;
name|send_off
argument_list|(
name|ppb
argument_list|,
literal|0
argument_list|,
operator|(
name|PARM
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
name|pb_flush
argument_list|(
name|ppb
argument_list|)
expr_stmt|;
comment|/* next state is unknown */
name|ppb
operator|->
name|pb_st
operator|=
name|PB_UNKNOWN
expr_stmt|;
block|}
block|}
break|break;
block|}
block|}
block|}
end_block

end_unit

