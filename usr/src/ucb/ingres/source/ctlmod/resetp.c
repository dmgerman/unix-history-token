begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"ctlmod.h"
end_include

begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<tree.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)resetp.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  RESETP -- reset parameters from call. ** **	This must be called after an initp-setp-call sequence to **	reclaim the memory gathered by the initp.  It can also be **	used in an initp-setp sequence to clean up if an error is **	found. ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		Restores the old context. **		Reclaims space from Qbuf. ** **	Trace Flags: **		4.4 - 4.7 */
end_comment

begin_macro
name|resetp
argument_list|()
end_macro

begin_block
block|{
specifier|extern
name|char
modifier|*
name|Proc_name
decl_stmt|;
ifdef|#
directive|ifdef
name|xCTR1
if|if
condition|(
name|tTf
argument_list|(
literal|4
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"resetp: ctx_new=%d _init=%d _link=%x _pmark=%d _cmark=%d\n"
argument_list|,
name|Ctx
operator|.
name|ctx_new
argument_list|,
name|Ctx
operator|.
name|ctx_init
argument_list|,
name|Ctx
operator|.
name|ctx_link
argument_list|,
name|Ctx
operator|.
name|ctx_pmark
argument_list|,
name|Ctx
operator|.
name|ctx_cmark
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* don't do anything if no matching "initp" call */
if|if
condition|(
operator|!
name|Ctx
operator|.
name|ctx_new
condition|)
return|return;
comment|/* 	**	The 'freebuf' to ctx_cmark will also deallocate 	**	any parameters, since ctx_pmark>= ctx_cmark 	**	always. 	*/
if|if
condition|(
name|Ctx
operator|.
name|ctx_link
operator|!=
name|NULL
condition|)
name|bmove
argument_list|(
operator|(
name|char
operator|*
operator|)
name|Ctx
operator|.
name|ctx_link
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|Ctx
argument_list|,
name|Ctx
operator|.
name|ctx_size
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|xCM_DEBUG
if|if
condition|(
name|Ctx
operator|.
name|ctx_pmark
operator|<
name|Ctx
operator|.
name|ctx_cmark
condition|)
name|syserr
argument_list|(
literal|"resetp: pmark %d cmark %d"
argument_list|,
name|Ctx
operator|.
name|ctx_pmark
argument_list|,
name|Ctx
operator|.
name|ctx_cmark
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|freebuf
argument_list|(
name|Qbuf
argument_list|,
name|Ctx
operator|.
name|ctx_cmark
argument_list|)
expr_stmt|;
name|tT
operator|=
name|Ctx
operator|.
name|ctx_tvect
expr_stmt|;
name|Proc_name
operator|=
name|Ctx
operator|.
name|ctx_name
expr_stmt|;
ifdef|#
directive|ifdef
name|xCTR1
if|if
condition|(
name|tTf
argument_list|(
literal|4
argument_list|,
literal|5
argument_list|)
condition|)
name|lprintf
argument_list|(
literal|"resetp: free %d\n"
argument_list|,
name|Ctx
operator|.
name|ctx_cmark
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	**	If there was a Qt associated with this context, 	**	restore it also.  If not, but there was a Qt 	**	associated with the context we just deallocated, 	**	set Qt to be 'unallocated'. 	*/
if|if
condition|(
name|Ctx
operator|.
name|ctx_qt
operator|!=
name|NULL
condition|)
block|{
comment|/* first free any descriptors */
name|clrrange
argument_list|()
expr_stmt|;
name|bmove
argument_list|(
operator|(
name|char
operator|*
operator|)
name|Ctx
operator|.
name|ctx_qt
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|Qt
argument_list|,
sizeof|sizeof
name|Qt
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|Ctx
operator|.
name|ctx_qt
argument_list|)
expr_stmt|;
name|Ctx
operator|.
name|ctx_qt
operator|=
name|NULL
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|Qt
operator|.
name|qt_ctx
operator|==
name|NULL
condition|)
name|Qt
operator|.
name|qt_active
operator|=
literal|0
expr_stmt|;
block|}
end_block

end_unit

