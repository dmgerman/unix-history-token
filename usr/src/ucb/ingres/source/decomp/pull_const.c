begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<symbol.h>
end_include

begin_include
include|#
directive|include
file|<aux.h>
end_include

begin_include
include|#
directive|include
file|<tree.h>
end_include

begin_include
include|#
directive|include
file|"globs.h"
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)pull_const.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* ** PULL_CONST - Detach and execute all constant clauses in the qualification. ** **	Pull_const examines the root tree for any constant clauses. **	If none are present then it returns TRUE. If there are any **	constant clauses, then they are removed, executed and if **	TRUE then pull_const returns TRUE and other wise it returns **	FALSE. ** **	This routine is not necessary to decomposition but rather **	can be called as an optimization when constant clauses are **	expected. Note that without this routine, constant clauses **	would only be examined at the bottom level of decomposition. **	Thus a multivar query which was true except for a constant clause **	would look at the required cross-product space before returning. */
end_comment

begin_macro
name|pull_const
argument_list|(
argument|root
argument_list|,
argument|buf
argument_list|)
end_macro

begin_decl_stmt
name|QTREE
modifier|*
name|root
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|buf
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|QTREE
modifier|*
name|r
decl_stmt|,
modifier|*
name|q
decl_stmt|,
modifier|*
name|s
decl_stmt|;
name|QTREE
modifier|*
name|makroot
parameter_list|()
function_decl|;
name|s
operator|=
operator|(
name|QTREE
operator|*
operator|)
name|NULL
expr_stmt|;
for|for
control|(
name|r
operator|=
name|root
init|;
name|r
operator|->
name|right
operator|->
name|sym
operator|.
name|type
operator|!=
name|QLEND
condition|;
control|)
block|{
name|q
operator|=
name|r
expr_stmt|;
name|r
operator|=
name|r
operator|->
name|right
expr_stmt|;
comment|/* r is now the AND node */
if|if
condition|(
name|r
operator|->
name|sym
operator|.
name|value
operator|.
name|sym_root
operator|.
name|lvarc
operator|==
literal|0
condition|)
block|{
comment|/* we have a constant clause */
if|if
condition|(
name|s
operator|==
literal|0
condition|)
name|s
operator|=
name|makroot
argument_list|(
name|buf
argument_list|)
expr_stmt|;
comment|/* remove AND from root tree */
name|q
operator|->
name|right
operator|=
name|r
operator|->
name|right
expr_stmt|;
comment|/* put node into constant tree */
name|r
operator|->
name|right
operator|=
name|s
operator|->
name|right
expr_stmt|;
name|s
operator|->
name|right
operator|=
name|r
expr_stmt|;
comment|/* fix up var counts (just for good form!) */
name|r
operator|->
name|sym
operator|.
name|value
operator|.
name|sym_root
operator|.
name|rvarm
operator|=
name|r
operator|->
name|sym
operator|.
name|value
operator|.
name|sym_root
operator|.
name|tvarc
operator|=
literal|0
expr_stmt|;
name|r
operator|=
name|q
expr_stmt|;
block|}
block|}
if|if
condition|(
name|s
condition|)
block|{
comment|/* run the constant query */
return|return
operator|(
name|execsq1
argument_list|(
name|s
argument_list|,
operator|-
literal|1
argument_list|,
name|NORESULT
argument_list|)
operator|)
return|;
block|}
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
end_block

end_unit

