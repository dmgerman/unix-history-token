begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Copyright (c) 1982 Regents of the University of California */
end_comment

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)step.c 1.2 2/11/82"
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Continue execution up to the next source line.  *  * We call "nextaddr" from the machine module to figure out  * what the object address is that corresponds to the next source line.  * If nextaddr returns -1, then the end of the program has been reached.  *  * There are two ways to define the next source line depending on what  * is desired when a procedure or function call is encountered.  Step  * stops at the beginning of the procedure or call; next skips over it.  */
end_comment

begin_include
include|#
directive|include
file|"defs.h"
end_include

begin_include
include|#
directive|include
file|"process.h"
end_include

begin_include
include|#
directive|include
file|"machine.h"
end_include

begin_include
include|#
directive|include
file|"breakpoint.h"
end_include

begin_include
include|#
directive|include
file|"source.h"
end_include

begin_include
include|#
directive|include
file|"mappings.h"
end_include

begin_include
include|#
directive|include
file|"process.rep"
end_include

begin_if
if|#
directive|if
operator|(
name|isvax
operator|)
end_if

begin_include
include|#
directive|include
file|"machine/vaxops.h"
end_include

begin_function_decl
name|LOCAL
name|ADDRESS
name|getcall
parameter_list|()
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Stepc is what is called when the step command is given.  * It has to play with the "isstopped" information.  */
end_comment

begin_macro
name|stepc
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
operator|!
name|isstopped
condition|)
block|{
name|error
argument_list|(
literal|"can't continue execution"
argument_list|)
expr_stmt|;
block|}
name|isstopped
operator|=
name|FALSE
expr_stmt|;
name|dostep
argument_list|(
name|FALSE
argument_list|)
expr_stmt|;
name|isstopped
operator|=
name|TRUE
expr_stmt|;
block|}
end_block

begin_macro
name|next
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
operator|!
name|isstopped
condition|)
block|{
name|error
argument_list|(
literal|"can't continue execution"
argument_list|)
expr_stmt|;
block|}
name|isstopped
operator|=
name|FALSE
expr_stmt|;
name|dostep
argument_list|(
name|TRUE
argument_list|)
expr_stmt|;
name|isstopped
operator|=
name|TRUE
expr_stmt|;
block|}
end_block

begin_macro
name|step
argument_list|()
end_macro

begin_block
block|{
name|dostep
argument_list|(
name|FALSE
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Resume execution up to the given address.  It is assumed that  * no breakpoints exist between the current address and the one  * we're stepping to.  This saves us from setting all the breakpoints.  */
end_comment

begin_macro
name|stepto
argument_list|(
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|ADDRESS
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|setbp
argument_list|(
name|addr
argument_list|)
expr_stmt|;
name|resume
argument_list|()
expr_stmt|;
name|unsetbp
argument_list|(
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|isbperr
argument_list|()
condition|)
block|{
name|printstatus
argument_list|()
expr_stmt|;
block|}
block|}
end_block

begin_function
name|LOCAL
name|dostep
parameter_list|(
name|isnext
parameter_list|)
name|BOOLEAN
name|isnext
decl_stmt|;
block|{
specifier|register
name|ADDRESS
name|addr
decl_stmt|;
specifier|register
name|LINENO
name|line
decl_stmt|;
name|char
modifier|*
name|filename
decl_stmt|;
name|addr
operator|=
name|pc
expr_stmt|;
do|do
block|{
if|#
directive|if
operator|(
name|isvaxpx
operator|)
name|addr
operator|=
name|nextaddr
argument_list|(
name|addr
argument_list|,
name|isnext
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
name|isnext
operator|&&
operator|(
name|addr
operator|=
name|getcall
argument_list|(
name|addr
argument_list|)
operator|)
operator|!=
literal|0
condition|)
block|{
name|stepto
argument_list|(
name|addr
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|pstep
argument_list|(
name|process
argument_list|)
expr_stmt|;
name|addr
operator|=
name|process
operator|->
name|pc
expr_stmt|;
name|pc
operator|=
name|process
operator|->
name|pc
expr_stmt|;
name|errnum
operator|=
name|process
operator|->
name|signo
expr_stmt|;
if|if
condition|(
operator|!
name|isbperr
argument_list|()
condition|)
block|{
name|printstatus
argument_list|()
expr_stmt|;
block|}
block|}
endif|#
directive|endif
name|line
operator|=
name|linelookup
argument_list|(
name|addr
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|line
operator|==
literal|0
operator|&&
operator|!
name|ss_instructions
condition|)
do|;
name|stepto
argument_list|(
name|addr
argument_list|)
expr_stmt|;
name|curline
operator|=
name|line
expr_stmt|;
block|}
end_function

begin_if
if|#
directive|if
operator|(
name|isvax
operator|)
end_if

begin_comment
comment|/*  * If the current address contains a call instruction, return the  * address of the instruction where control will return.  *  * This function is intentionally dependent on a particular type  * of calling sequence.  */
end_comment

begin_function
name|LOCAL
name|ADDRESS
name|getcall
parameter_list|(
name|addr
parameter_list|)
name|ADDRESS
name|addr
decl_stmt|;
block|{
name|VAXOP
name|op
decl_stmt|;
name|iread
argument_list|(
operator|&
name|op
argument_list|,
name|addr
argument_list|,
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|op
operator|==
name|O_CALLS
condition|)
block|{
return|return
operator|(
name|addr
operator|+
literal|7
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
literal|0
operator|)
return|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

