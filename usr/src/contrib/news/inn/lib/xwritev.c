begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  $Revision: 1.3 $ ** */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/uio.h>
end_include

begin_include
include|#
directive|include
file|"clibrary.h"
end_include

begin_comment
comment|/* **  Do a writev, return -1 on error; 0 if okay.  Handling the return value **  of writev is a pain.  It should return the number of iov's it fully **  wrote out, and update the fields in all of them to contain the new **  startpoints. */
end_comment

begin_function
name|int
name|xwritev
parameter_list|(
name|fd
parameter_list|,
name|vp
parameter_list|,
name|vpcount
parameter_list|)
name|int
name|fd
decl_stmt|;
name|struct
name|iovec
modifier|*
name|vp
decl_stmt|;
specifier|register
name|int
name|vpcount
decl_stmt|;
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|long
name|left
decl_stmt|;
comment|/* Get the total bytecount. */
for|for
control|(
name|left
operator|=
literal|0
operator|,
name|i
operator|=
name|vpcount
init|;
operator|--
name|i
operator|>=
literal|0
condition|;
control|)
name|left
operator|+=
name|vp
index|[
name|i
index|]
operator|.
name|iov_len
expr_stmt|;
while|while
condition|(
name|vpcount
condition|)
block|{
if|if
condition|(
operator|(
name|i
operator|=
name|writev
argument_list|(
name|fd
argument_list|,
name|vp
argument_list|,
operator|(
name|SIZE_T
operator|)
name|vpcount
argument_list|)
operator|)
operator|<
literal|0
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
operator|(
name|left
operator|-=
name|i
operator|)
operator|<=
literal|0
condition|)
break|break;
for|for
control|(
init|;
name|i
operator|>=
name|vp
operator|->
name|iov_len
condition|;
name|vp
operator|++
operator|,
name|vpcount
operator|--
control|)
name|i
operator|-=
name|vp
operator|->
name|iov_len
expr_stmt|;
name|vp
operator|->
name|iov_base
operator|+=
name|i
expr_stmt|;
name|vp
operator|->
name|iov_len
operator|-=
name|i
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

end_unit

