begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  $Revision: 1.8 $ ** */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<netdb.h>
end_include

begin_include
include|#
directive|include
file|"configdata.h"
end_include

begin_include
include|#
directive|include
file|"paths.h"
end_include

begin_include
include|#
directive|include
file|"clibrary.h"
end_include

begin_include
include|#
directive|include
file|"libinn.h"
end_include

begin_comment
comment|/* **  Get the fully-qualified domain name for this host. */
end_comment

begin_function
name|char
modifier|*
name|GetFQDN
parameter_list|()
block|{
specifier|static
name|char
name|buff
index|[
name|SMBUF
index|]
decl_stmt|;
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|char
name|temp
index|[
name|SMBUF
operator|+
literal|2
index|]
decl_stmt|;
comment|/* Return any old results. */
if|if
condition|(
name|buff
index|[
literal|0
index|]
condition|)
return|return
name|buff
return|;
comment|/* Try gethostname. */
if|if
condition|(
name|gethostname
argument_list|(
name|buff
argument_list|,
operator|(
name|int
operator|)
sizeof|sizeof
name|buff
argument_list|)
operator|<
literal|0
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|strchr
argument_list|(
name|buff
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
condition|)
return|return
name|buff
return|;
comment|/* See if DNS (or /etc/hosts) gives us a full domain name. */
if|if
condition|(
operator|(
name|hp
operator|=
name|gethostbyname
argument_list|(
name|buff
argument_list|)
operator|)
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|strchr
argument_list|(
name|hp
operator|->
name|h_name
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
condition|)
block|{
comment|/* Try to force DNS lookup if NIS/whatever gets in the way. */
operator|(
name|void
operator|)
name|strncpy
argument_list|(
name|temp
argument_list|,
name|buff
argument_list|,
sizeof|sizeof
name|buff
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|temp
argument_list|,
literal|"."
argument_list|)
expr_stmt|;
name|hp
operator|=
name|gethostbyname
argument_list|(
name|temp
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|hp
operator|!=
name|NULL
operator|&&
name|strchr
argument_list|(
name|hp
operator|->
name|h_name
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|strlen
argument_list|(
name|hp
operator|->
name|h_name
argument_list|)
operator|<
sizeof|sizeof
name|buff
operator|-
literal|1
condition|)
return|return
name|strcpy
argument_list|(
name|buff
argument_list|,
name|hp
operator|->
name|h_name
argument_list|)
return|;
comment|/* Doesn't fit; make sure we don't return bad data next time. */
name|buff
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return
name|hp
operator|->
name|h_name
return|;
block|}
comment|/* Get the domain name config parameter and append it. */
if|if
condition|(
operator|(
name|p
operator|=
name|GetFileConfigValue
argument_list|(
name|_CONF_DOMAIN
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|*
name|p
operator|==
literal|'\0'
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|strlen
argument_list|(
name|buff
argument_list|)
operator|+
literal|1
operator|+
name|strlen
argument_list|(
name|p
argument_list|)
operator|>
sizeof|sizeof
name|buff
operator|-
literal|1
condition|)
comment|/* Doesn't fit. */
return|return
name|NULL
return|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|buff
argument_list|,
literal|"."
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|buff
argument_list|,
name|p
argument_list|)
expr_stmt|;
return|return
name|buff
return|;
block|}
end_function

end_unit

