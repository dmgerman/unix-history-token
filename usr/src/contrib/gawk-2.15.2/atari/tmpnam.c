begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* tmpnam.c : return a temporary file name */
end_comment

begin_comment
comment|/* written by Eric R. Smith and placed in the public domain */
end_comment

begin_comment
comment|/**  *  - modified for gawk needs - pattern /$$XXXXXX from the original  *    code creates names which are hard to remove when somethig  *    goes wrong  *  - retuned name can be passed outside via system(); other programs  *    may not dig '/' as a path separator  *  - somehow more frugal in a memory use  *    (mj - October 1990)  **/
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_function_decl
specifier|extern
name|char
modifier|*
name|getenv
parameter_list|(
specifier|const
name|char
modifier|*
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|extern
name|char
modifier|*
name|mktemp
parameter_list|(
name|char
modifier|*
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|char
modifier|*
name|tempnam
parameter_list|(
specifier|const
name|char
modifier|*
name|path
parameter_list|,
specifier|const
name|char
modifier|*
name|base
parameter_list|)
function_decl|;
end_function_decl

begin_decl_stmt
specifier|static
name|char
name|pattern
index|[]
init|=
literal|"\\gwkXXXXX"
decl_stmt|;
end_decl_stmt

begin_function
name|char
modifier|*
name|tmpnam
parameter_list|(
name|buf
parameter_list|)
name|char
modifier|*
name|buf
decl_stmt|;
block|{
name|char
modifier|*
name|tmpdir
decl_stmt|;
if|if
condition|(
operator|!
operator|(
name|tmpdir
operator|=
name|getenv
argument_list|(
literal|"TEMP"
argument_list|)
operator|)
operator|&&
operator|!
operator|(
name|tmpdir
operator|=
name|getenv
argument_list|(
literal|"TMPDIR"
argument_list|)
operator|)
condition|)
name|tmpdir
operator|=
literal|"."
expr_stmt|;
if|if
condition|(
operator|!
name|buf
condition|)
block|{
name|size_t
name|blen
decl_stmt|;
name|blen
operator|=
name|strlen
argument_list|(
name|tmpdir
argument_list|)
operator|+
sizeof|sizeof
argument_list|(
name|pattern
argument_list|)
expr_stmt|;
if|if
condition|(
name|NULL
operator|==
operator|(
name|buf
operator|=
name|malloc
argument_list|(
name|blen
argument_list|)
operator|)
condition|)
return|return
name|NULL
return|;
block|}
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|strcpy
argument_list|(
name|buf
argument_list|,
name|tmpdir
argument_list|)
argument_list|,
name|pattern
argument_list|)
expr_stmt|;
return|return
operator|(
name|mktemp
argument_list|(
name|buf
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/* used by gawk_popen() */
end_comment

begin_function
name|char
modifier|*
name|tempnam
parameter_list|(
name|path
parameter_list|,
name|base
parameter_list|)
specifier|const
name|char
modifier|*
name|path
decl_stmt|,
decl|*
name|base
decl_stmt|;
end_function

begin_comment
comment|/* ignored */
end_comment

begin_block
block|{
return|return
name|tmpnam
argument_list|(
name|NULL
argument_list|)
return|;
block|}
end_block

end_unit

