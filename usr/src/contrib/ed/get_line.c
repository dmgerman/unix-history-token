begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * Rodney Ruddock of the University of Guelph.  *  * %sccs.include.redist.c%  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)get_line.c	8.1 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<regex.h>
end_include

begin_include
include|#
directive|include
file|<setjmp.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|DBI
end_ifdef

begin_include
include|#
directive|include
file|<db.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ed.h"
end_include

begin_include
include|#
directive|include
file|"extern.h"
end_include

begin_comment
comment|/*  * Get the specified line from the buffer. Note that in the buffer  * we stored lengths of text, not strings (NULL terminated, except  * under MEMORY). So we want to put a terminating NULL in for  * whatever command is going to be handling the line.  */
end_comment

begin_comment
comment|/* these variables are here (instead of main and ed.h) because they  * are only used with the buffer when run under STDIO. STDIO is a  * resource pig with most of the OS's I've tested this with. The  * use of these variables improves performance up to 100% in several  * cases. The piggyness is thus: fseek causes the current STDIO buf  * to be flushed out and a new one read in...even when it is not necessary!  * Read 512 (or 1024) when you don't have to for almost every access  * and you'll slow down too. So these variable are used to control unneeded  * fseeks.  * I've been told the newer BSD STDIO has fixed this, but don't  * currently have a copy.  */
end_comment

begin_decl_stmt
name|int
name|file_loc
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Get a particular line of length len from ed's buffer and place it in  * 'text', the standard repository for the "current" line.  */
end_comment

begin_function
name|void
name|get_line
parameter_list|(
name|loc
parameter_list|,
name|len
parameter_list|)
ifdef|#
directive|ifdef
name|STDIO
name|long
name|loc
decl_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DBI
name|recno_t
name|loc
decl_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|MEMORY
name|char
modifier|*
name|loc
decl_stmt|;
endif|#
directive|endif
name|int
name|len
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DBI
name|DBT
name|db_key
decl_stmt|,
name|db_data
decl_stmt|;
endif|#
directive|endif
name|int
name|l_jmp_flag
decl_stmt|;
if|if
condition|(
name|l_jmp_flag
operator|=
name|setjmp
argument_list|(
name|ctrl_position2
argument_list|)
condition|)
block|{
name|text
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return;
block|}
name|sigspecial2
operator|=
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|STDIO
if|if
condition|(
name|loc
operator|==
literal|0L
condition|)
block|{
name|text
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|file_loc
operator|!=
name|loc
condition|)
name|fseek
argument_list|(
name|fhtmp
argument_list|,
name|loc
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|file_seek
operator|=
literal|1
expr_stmt|;
name|file_loc
operator|=
name|loc
operator|+
name|fread
argument_list|(
name|text
argument_list|,
sizeof|sizeof
argument_list|(
name|char
argument_list|)
argument_list|,
name|len
argument_list|,
name|fhtmp
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DBI
if|if
condition|(
name|loc
operator|==
operator|(
name|recno_t
operator|)
literal|0
condition|)
block|{
name|text
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return;
block|}
operator|(
name|db_key
operator|.
name|data
operator|)
operator|=
operator|&
name|loc
expr_stmt|;
operator|(
name|db_key
operator|.
name|size
operator|)
operator|=
sizeof|sizeof
argument_list|(
name|recno_t
argument_list|)
expr_stmt|;
call|(
name|dbhtmp
operator|->
name|get
call|)
argument_list|(
name|dbhtmp
argument_list|,
operator|&
name|db_key
argument_list|,
operator|&
name|db_data
argument_list|,
operator|(
name|u_int
operator|)
literal|0
argument_list|)
expr_stmt|;
name|strcpy
argument_list|(
name|text
argument_list|,
name|db_data
operator|.
name|data
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|MEMORY
if|if
condition|(
name|loc
operator|==
operator|(
name|char
operator|*
operator|)
name|NULL
condition|)
block|{
name|text
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return;
block|}
name|memmove
argument_list|(
name|text
argument_list|,
name|loc
argument_list|,
name|len
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|text
index|[
name|len
index|]
operator|=
literal|'\0'
expr_stmt|;
name|sigspecial2
operator|=
literal|0
expr_stmt|;
block|}
end_function

end_unit

