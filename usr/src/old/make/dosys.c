begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_decl_stmt
specifier|static
name|char
modifier|*
name|sccsid
init|=
literal|"@(#)dosys.c	4.9 (Berkeley) %G%; #include "
name|defs
literal|" #include<signal.h>  dosys(comstring,nohalt) register char *comstring; int nohalt; { register int status;  if(metas(comstring)) 	status = doshell(comstring,nohalt); else	status = doexec(comstring);  return(status); }    metas(s)   /* Are there are any  Shell meta-characters? */ register char *s; { register char c;  while( (funny[c = *s++]& META) == 0 ) 	; return( c ); }
literal|doshell(comstring,nohalt) char *comstring; int nohalt; { #ifdef SHELLENV char *getenv(), *rindex(); char *shellcom = getenv("
name|SHELL
literal|"); char *shellstr; #endif if((waitpid = vfork()) == 0) 	{ 	enbint(SIG_DFL); 	doclose();  #ifdef SHELLENV 	if (shellcom == 0) shellcom = SHELLCOM; 	shellstr = rindex(shellcom, '/') + 1; 	execl(shellcom, shellstr, (nohalt ? "
operator|-
name|c
literal|" : "
operator|-
name|ce
literal|"), comstring, 0); #else 	execl(SHELLCOM, "
name|sh
literal|", (nohalt ? "
operator|-
name|c
literal|" : "
operator|-
name|ce
literal|"), comstring, 0); #endif 	fatal("
name|Couldn
literal|'t load Shell"); 	}  return( await() ); }     int intrupt();  await() { int status; register int pid;  enbint(SIG_IGN); while( (pid = wait(&status)) != waitpid) 	if(pid == -1) 		fatal("bad wait code"); waitpid = 0; enbint(intrupt); return(status); }  /*  * Close open directory files before exec'
name|ing
operator|*
operator|/
name|doclose
argument_list|()
block|{
specifier|register
expr|struct
name|dirhdr
operator|*
name|od
block|;
for|for
control|(
name|od
operator|=
name|firstod
init|;
name|od
condition|;
name|od
operator|=
name|od
operator|->
name|nxtopendir
control|)
if|if
condition|(
name|od
operator|->
name|dirfc
operator|!=
name|NULL
condition|)
comment|/* 		 * vfork kludge... 		 * we cannot call closedir since this will modify 		 * the parents data space; just call close directly. 		 */
name|close
argument_list|(
name|od
operator|->
name|dirfc
operator|->
name|dd_fd
argument_list|)
expr_stmt|;
block|}
end_decl_stmt

begin_escape
end_escape

begin_define
define|#
directive|define
name|MAXARGV
value|400
end_define

begin_expr_stmt
name|doexec
argument_list|(
name|str
argument_list|)
specifier|register
name|char
operator|*
name|str
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|char
modifier|*
name|t
decl_stmt|;
name|char
modifier|*
name|argv
index|[
name|MAXARGV
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
modifier|*
name|p
decl_stmt|;
while|while
condition|(
operator|*
name|str
operator|==
literal|' '
operator|||
operator|*
name|str
operator|==
literal|'\t'
condition|)
operator|++
name|str
expr_stmt|;
if|if
condition|(
operator|*
name|str
operator|==
literal|'\0'
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
comment|/* no command */
name|p
operator|=
name|argv
expr_stmt|;
for|for
control|(
name|t
operator|=
name|str
init|;
operator|*
name|t
condition|;
control|)
block|{
if|if
condition|(
name|p
operator|>=
name|argv
operator|+
name|MAXARGV
condition|)
name|fatal1
argument_list|(
literal|"%s: Too many arguments."
argument_list|,
name|str
argument_list|)
expr_stmt|;
operator|*
name|p
operator|++
operator|=
name|t
expr_stmt|;
while|while
condition|(
operator|*
name|t
operator|!=
literal|' '
operator|&&
operator|*
name|t
operator|!=
literal|'\t'
operator|&&
operator|*
name|t
operator|!=
literal|'\0'
condition|)
operator|++
name|t
expr_stmt|;
if|if
condition|(
operator|*
name|t
condition|)
for|for
control|(
operator|*
name|t
operator|++
operator|=
literal|'\0'
init|;
operator|*
name|t
operator|==
literal|' '
operator|||
operator|*
name|t
operator|==
literal|'\t'
condition|;
operator|++
name|t
control|)
empty_stmt|;
block|}
operator|*
name|p
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|(
name|waitpid
operator|=
name|vfork
argument_list|()
operator|)
operator|==
literal|0
condition|)
block|{
name|enbint
argument_list|(
name|SIG_DFL
argument_list|)
expr_stmt|;
name|doclose
argument_list|()
expr_stmt|;
name|enbint
argument_list|(
name|intrupt
argument_list|)
expr_stmt|;
name|execvp
argument_list|(
name|str
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|fatal1
argument_list|(
literal|"Cannot load %s"
argument_list|,
name|str
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|await
argument_list|()
operator|)
return|;
block|}
end_block

begin_escape
end_escape

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_macro
name|touch
argument_list|(
argument|force
argument_list|,
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|int
name|force
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|name
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|stat
name|stbuff
decl_stmt|;
name|char
name|junk
index|[
literal|1
index|]
decl_stmt|;
name|int
name|fd
decl_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|name
argument_list|,
operator|&
name|stbuff
argument_list|)
operator|<
literal|0
condition|)
if|if
condition|(
name|force
condition|)
goto|goto
name|create
goto|;
else|else
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"touch: file %s does not exist.\n"
argument_list|,
name|name
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|stbuff
operator|.
name|st_size
operator|==
literal|0
condition|)
goto|goto
name|create
goto|;
if|if
condition|(
operator|(
name|fd
operator|=
name|open
argument_list|(
name|name
argument_list|,
literal|2
argument_list|)
operator|)
operator|<
literal|0
condition|)
goto|goto
name|bad
goto|;
if|if
condition|(
name|read
argument_list|(
name|fd
argument_list|,
name|junk
argument_list|,
literal|1
argument_list|)
operator|<
literal|1
condition|)
block|{
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
goto|goto
name|bad
goto|;
block|}
name|lseek
argument_list|(
name|fd
argument_list|,
literal|0L
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|write
argument_list|(
name|fd
argument_list|,
name|junk
argument_list|,
literal|1
argument_list|)
operator|<
literal|1
condition|)
block|{
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
goto|goto
name|bad
goto|;
block|}
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return;
name|bad
label|:
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Cannot touch %s\n"
argument_list|,
name|name
argument_list|)
expr_stmt|;
return|return;
name|create
label|:
if|if
condition|(
operator|(
name|fd
operator|=
name|creat
argument_list|(
name|name
argument_list|,
literal|0666
argument_list|)
operator|)
operator|<
literal|0
condition|)
goto|goto
name|bad
goto|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

