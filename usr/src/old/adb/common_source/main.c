begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)main.c	5.4 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * adb - main command loop and error/interrupt handling  */
end_comment

begin_include
include|#
directive|include
file|"defs.h"
end_include

begin_include
include|#
directive|include
file|<setjmp.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<sys/uio.h>
end_include

begin_include
include|#
directive|include
file|"pathnames.h"
end_include

begin_decl_stmt
specifier|extern
name|char
name|NOEOR
index|[]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* "newline expected" */
end_comment

begin_comment
comment|/*  * executing is set by command.c whenever we are running a subprocess.  */
end_comment

begin_decl_stmt
name|int
name|executing
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|infile
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* XXX */
end_comment

begin_decl_stmt
name|char
modifier|*
name|Ipath
init|=
name|_PATH_SCRIPTS
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* XXX */
end_comment

begin_decl_stmt
specifier|static
name|int
name|xargc
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* remembers argc for getfile() */
end_comment

begin_decl_stmt
specifier|static
name|int
name|reading
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* set whenever reading input */
end_comment

begin_decl_stmt
specifier|static
name|jmp_buf
name|mainloop
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* label for error jumps */
end_comment

begin_function_decl
name|void
name|fault
parameter_list|()
function_decl|;
end_function_decl

begin_function_decl
name|off_t
name|lseek
parameter_list|()
function_decl|;
end_function_decl

begin_function
name|main
parameter_list|(
name|argc
parameter_list|,
name|argv
parameter_list|)
specifier|register
name|int
name|argc
decl_stmt|;
specifier|register
name|char
modifier|*
modifier|*
name|argv
decl_stmt|;
block|{
name|int
name|waserr
decl_stmt|;
name|char
name|line
index|[
name|LINELEN
index|]
decl_stmt|;
name|radix
operator|=
literal|16
expr_stmt|;
comment|/* default radix is hex */
name|maxoff
operator|=
name|MAXOFF
expr_stmt|;
name|maxcol
operator|=
name|MAXCOL
expr_stmt|;
comment|/* 	 * Set up machine dependent code (e.g., instruction decoding tables), 	 * then look at arguments, and open the object and core files; 	 * finally, set up signal handlers.  Alas, cannot really use getopt. 	 */
name|mch_init
argument_list|()
expr_stmt|;
name|symfile
operator|.
name|name
operator|=
literal|"a.out"
expr_stmt|;
name|corefile
operator|.
name|name
operator|=
literal|"core"
expr_stmt|;
while|while
condition|(
name|argc
operator|>
literal|1
operator|&&
name|argv
index|[
literal|1
index|]
index|[
literal|0
index|]
operator|==
literal|'-'
condition|)
block|{
specifier|register
name|char
modifier|*
name|p
init|=
name|argv
index|[
literal|1
index|]
operator|+
literal|1
decl_stmt|;
if|if
condition|(
operator|*
name|p
operator|==
literal|'w'
operator|&&
name|p
index|[
literal|1
index|]
operator|==
literal|0
condition|)
block|{
name|wtflag
operator|=
literal|2
expr_stmt|;
comment|/* suitable for open() */
name|argc
operator|--
operator|,
name|argv
operator|++
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|*
name|p
operator|==
literal|'k'
operator|&&
name|p
index|[
literal|1
index|]
operator|==
literal|0
condition|)
block|{
name|kernel
operator|=
literal|1
expr_stmt|;
name|argc
operator|--
operator|,
name|argv
operator|++
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|*
name|p
operator|==
literal|'I'
condition|)
block|{
name|Ipath
operator|=
name|argv
index|[
literal|1
index|]
operator|+
literal|2
expr_stmt|;
name|argc
operator|--
operator|,
name|argv
operator|++
expr_stmt|;
block|}
else|else
break|break;
block|}
if|if
condition|(
name|argc
operator|>
literal|1
condition|)
name|symfile
operator|.
name|name
operator|=
name|argv
index|[
literal|1
index|]
expr_stmt|;
if|if
condition|(
name|argc
operator|>
literal|2
condition|)
name|corefile
operator|.
name|name
operator|=
name|argv
index|[
literal|2
index|]
expr_stmt|;
name|xargc
operator|=
name|argc
expr_stmt|;
comment|/* remember for getfile() */
name|setsym
argument_list|()
expr_stmt|;
name|setcore
argument_list|()
expr_stmt|;
if|if
condition|(
operator|(
name|sigint
operator|=
name|signal
argument_list|(
name|SIGINT
argument_list|,
name|SIG_IGN
argument_list|)
operator|)
operator|!=
name|SIG_IGN
condition|)
block|{
name|intcatch
operator|=
name|fault
expr_stmt|;
operator|(
name|void
operator|)
name|signal
argument_list|(
name|SIGINT
argument_list|,
name|fault
argument_list|)
expr_stmt|;
block|}
name|sigquit
operator|=
name|signal
argument_list|(
name|SIGQUIT
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
comment|/* 	 * Errors jump back to the main loop here. 	 * If the error occurred while the process was running, 	 * we need to remove any breakpoints. 	 */
operator|(
name|void
operator|)
name|setjmp
argument_list|(
name|mainloop
argument_list|)
expr_stmt|;
if|if
condition|(
name|executing
condition|)
block|{
name|executing
operator|=
literal|0
expr_stmt|;
name|delbp
argument_list|()
expr_stmt|;
block|}
comment|/* 	 * Main loop: 	 *	flush pending output, and print any error message(s); 	 *	read a line; if end of file, close current input and 	 *	continue, unless input == stdin; otherwise, perform 	 *	the command(s) on the line and make sure that that 	 *	consumed the whole line. 	 */
for|for
control|(
init|;
condition|;
control|)
block|{
name|flushbuf
argument_list|()
expr_stmt|;
if|if
condition|(
name|errflag
condition|)
block|{
name|adbprintf
argument_list|(
literal|"%s\n"
argument_list|,
name|errflag
argument_list|)
expr_stmt|;
name|waserr
operator|=
literal|1
expr_stmt|;
name|errflag
operator|=
name|NULL
expr_stmt|;
block|}
if|if
condition|(
name|mkfault
condition|)
block|{
name|mkfault
operator|=
literal|0
expr_stmt|;
name|prints
argument_list|(
literal|"\nadb\n"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|readline
argument_list|(
name|line
argument_list|,
sizeof|sizeof
name|line
argument_list|)
condition|)
block|{
if|if
condition|(
name|infile
operator|==
literal|0
condition|)
name|done
argument_list|(
name|waserr
argument_list|)
expr_stmt|;
name|iclose
argument_list|(
operator|-
literal|1
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|waserr
operator|=
literal|0
expr_stmt|;
name|command
argument_list|(
name|line
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
comment|/* lp&& */
name|lastc
operator|!=
literal|'\n'
condition|)
name|errflag
operator|=
name|NOEOR
expr_stmt|;
block|}
block|}
block|}
end_function

begin_comment
comment|/*  * Exit with optional error status.  */
end_comment

begin_macro
name|done
argument_list|(
argument|err
argument_list|)
end_macro

begin_decl_stmt
name|int
name|err
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|endpcs
argument_list|()
expr_stmt|;
name|exit
argument_list|(
name|err
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Open the a.out (1) or core (2) file.  If the name was given,  * rather than defaulted, and we were asked to open for writing,  * create the file if necessary.  */
end_comment

begin_macro
name|getfile
argument_list|(
argument|which
argument_list|)
end_macro

begin_decl_stmt
name|int
name|which
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|fname
decl_stmt|;
name|int
name|flags
decl_stmt|,
name|fd
decl_stmt|;
name|char
modifier|*
name|strerror
parameter_list|()
function_decl|;
switch|switch
condition|(
name|which
condition|)
block|{
case|case
literal|1
case|:
name|fname
operator|=
name|symfile
operator|.
name|name
expr_stmt|;
break|break;
case|case
literal|2
case|:
name|fname
operator|=
name|corefile
operator|.
name|name
expr_stmt|;
break|break;
default|default:
name|panic
argument_list|(
literal|"getfile"
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
if|if
condition|(
name|fname
index|[
literal|0
index|]
operator|==
literal|'-'
operator|&&
name|fname
index|[
literal|1
index|]
operator|==
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
if|if
condition|(
operator|(
name|flags
operator|=
name|wtflag
operator|)
operator|!=
literal|0
operator|&&
name|xargc
operator|>
name|which
condition|)
name|flags
operator||=
name|O_CREAT
expr_stmt|;
if|if
condition|(
operator|(
name|fd
operator|=
name|open
argument_list|(
name|fname
argument_list|,
name|flags
argument_list|,
literal|0666
argument_list|)
operator|)
operator|<
literal|0
operator|&&
name|xargc
operator|>
name|which
condition|)
name|adbprintf
argument_list|(
literal|"cannot open `%s': %s\n"
argument_list|,
name|fname
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|fd
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Input routines  */
end_comment

begin_comment
comment|/*  * Read a character, skipping white space.  */
end_comment

begin_macro
name|rdc
argument_list|()
end_macro

begin_block
block|{
while|while
condition|(
operator|*
name|lp
operator|==
literal|' '
operator|||
operator|*
name|lp
operator|==
literal|'\t'
condition|)
name|lp
operator|++
expr_stmt|;
return|return
operator|(
name|readchar
argument_list|()
operator|)
return|;
block|}
end_block

begin_ifndef
ifndef|#
directive|ifndef
name|readchar
end_ifndef

begin_comment
comment|/*  * Read a character, incrementing the pointer if not at end.  */
end_comment

begin_macro
name|readchar
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
operator|(
name|lastc
operator|=
operator|*
name|lp
operator|)
operator|!=
literal|0
condition|)
name|lp
operator|++
expr_stmt|;
return|return
operator|(
name|lastc
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Read a line.  Return -1 at end of file.  * Alas, cannot read more than one character at a time here (except  * possibly on tty devices; must think about that later).  */
end_comment

begin_expr_stmt
specifier|static
name|readline
argument_list|(
name|p
argument_list|,
name|n
argument_list|)
specifier|register
name|char
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|n
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|n
operator|--
expr_stmt|;
comment|/* for \0 */
name|reading
operator|++
expr_stmt|;
do|do
block|{
if|if
condition|(
operator|--
name|n
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|read
argument_list|(
name|infile
argument_list|,
name|p
argument_list|,
literal|1
argument_list|)
operator|!=
literal|1
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
else|else
operator|*
name|p
operator|=
literal|'\n'
expr_stmt|;
block|}
do|while
condition|(
operator|*
name|p
operator|++
operator|!=
literal|'\n'
condition|)
do|;
name|reading
operator|=
literal|0
expr_stmt|;
operator|*
name|p
operator|=
literal|0
expr_stmt|;
comment|/* can we perhaps eliminate this? */
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Return the next non-white non-end-of-line character.  */
end_comment

begin_macro
name|nextchar
argument_list|()
end_macro

begin_block
block|{
name|int
name|c
init|=
name|rdc
argument_list|()
decl_stmt|;
if|if
condition|(
name|eol
argument_list|(
name|c
argument_list|)
condition|)
block|{
name|unreadc
argument_list|()
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
return|return
operator|(
name|c
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Error handlers  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|checkerr
end_ifndef

begin_comment
comment|/*  * If there has been an error or a fault, take the error.  */
end_comment

begin_macro
name|checkerr
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
name|errflag
operator|||
name|mkfault
condition|)
name|error
argument_list|(
name|errflag
argument_list|)
expr_stmt|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * An error occurred.  Save the message for later printing,  * close open files, and reset to main command loop.  */
end_comment

begin_macro
name|error
argument_list|(
argument|which
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|which
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|errflag
operator|=
name|which
expr_stmt|;
name|iclose
argument_list|(
literal|0
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|oclose
argument_list|()
expr_stmt|;
name|longjmp
argument_list|(
name|mainloop
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
end_block

begin_comment
comment|/*  * An interrupt occurred.  Seek to the end of the current input file.  * If we were reading commands, jump back to the main loop.  */
end_comment

begin_comment
comment|/* ARGSUSED */
end_comment

begin_function
name|void
name|fault
parameter_list|(
name|sig
parameter_list|)
name|int
name|sig
decl_stmt|;
block|{
comment|/* (void) signal(sig, fault); */
comment|/* unnecessary */
operator|(
name|void
operator|)
name|lseek
argument_list|(
name|infile
argument_list|,
literal|0L
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|mkfault
operator|++
expr_stmt|;
if|if
condition|(
name|reading
condition|)
block|{
name|reading
operator|=
literal|0
expr_stmt|;
name|error
argument_list|(
operator|(
name|char
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * Panic announces an internally detected error.  */
end_comment

begin_macro
name|panic
argument_list|(
argument|s
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|s
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|static
name|char
name|p
index|[]
init|=
literal|"panic: \n"
decl_stmt|;
specifier|static
name|struct
name|iovec
name|iov
index|[
literal|3
index|]
init|=
block|{
block|{
name|p
block|,
literal|7
block|}
block|,
block|{
literal|0
block|}
block|,
block|{
name|p
operator|+
literal|7
block|,
literal|1
block|}
block|}
decl_stmt|;
name|iov
index|[
literal|1
index|]
operator|.
name|iov_base
operator|=
name|s
expr_stmt|;
name|iov
index|[
literal|1
index|]
operator|.
name|iov_len
operator|=
name|strlen
argument_list|(
name|s
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|writev
argument_list|(
literal|2
argument_list|,
name|iov
argument_list|,
literal|3
argument_list|)
expr_stmt|;
name|abort
argument_list|()
expr_stmt|;
comment|/* beware, overwrites current core file! */
comment|/*	exit(1); */
block|}
end_block

end_unit

