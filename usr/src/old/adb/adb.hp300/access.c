begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)access.c	4.8 10/13/84"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Adb: access data in file/process address space.  *  * The routines in this file access referenced data using  * the maps to access files, ptrace to access subprocesses,  * or the system page tables when debugging the kernel,  * to translate virtual to physical addresses.  */
end_comment

begin_include
include|#
directive|include
file|"defs.h"
end_include

begin_decl_stmt
name|MAP
name|txtmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|MAP
name|datmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|INT
name|wtflag
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|STRING
name|errflg
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|errno
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|INT
name|pid
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Primitives: put a value in a space, get a value from a space  * and get a word or byte not returning if an error occurred.  */
end_comment

begin_macro
name|put
argument_list|(
argument|addr
argument_list|,
argument|space
argument_list|,
argument|value
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|(
name|void
operator|)
name|access
argument_list|(
name|WT
argument_list|,
name|addr
argument_list|,
name|space
argument_list|,
name|value
argument_list|)
expr_stmt|;
block|}
end_block

begin_if
if|#
directive|if
name|vax
operator|||
name|pdp11
end_if

begin_function
name|u_int
name|get
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
return|return
operator|(
name|access
argument_list|(
name|RD
argument_list|,
name|addr
argument_list|,
name|space
argument_list|,
literal|0
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|u_int
name|lchkget
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
name|u_int
name|w
init|=
name|get
argument_list|(
name|addr
argument_list|,
name|space
argument_list|)
decl_stmt|;
name|chkerr
argument_list|()
expr_stmt|;
return|return
operator|(
name|w
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|mc68000
end_ifdef

begin_comment
comment|/*  * 68000 Unices don't like odd addresses.  */
end_comment

begin_function
name|u_int
name|get
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
name|u_int
name|data
decl_stmt|;
if|if
condition|(
name|addr
operator|&
literal|1
condition|)
block|{
name|data
operator|=
name|access
argument_list|(
name|RD
argument_list|,
name|addr
operator|-
literal|1
argument_list|,
name|space
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|data
operator|>>
literal|8
operator|)
operator|&
literal|0xffff
operator|)
return|;
block|}
name|data
operator|=
name|access
argument_list|(
name|RD
argument_list|,
name|addr
argument_list|,
name|space
argument_list|,
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|data
operator|>>
literal|16
operator|)
operator|&
literal|0xffff
operator|)
return|;
block|}
end_function

begin_function
name|u_int
name|lget
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
if|if
condition|(
name|addr
operator|&
literal|1
condition|)
block|{
name|u_int
name|data
init|=
name|get
argument_list|(
name|addr
argument_list|,
name|space
argument_list|)
decl_stmt|;
return|return
operator|(
name|get
argument_list|(
name|addr
operator|+
literal|2
argument_list|,
name|space
argument_list|)
operator||
operator|(
name|data
operator|<<
literal|16
operator|)
operator|)
return|;
block|}
return|return
operator|(
name|access
argument_list|(
name|RD
argument_list|,
name|addr
argument_list|,
name|space
argument_list|,
literal|0
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|u_int
name|lchkget
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
name|u_int
name|w
init|=
name|lget
argument_list|(
name|addr
argument_list|,
name|space
argument_list|)
decl_stmt|;
name|chkerr
argument_list|()
expr_stmt|;
return|return
operator|(
name|w
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|!
name|pdp11
operator|&&
operator|!
name|vax
operator|&&
operator|!
name|mc68000
end_if

begin_expr_stmt
name|help
operator|!
endif|#
directive|endif
name|u_int
name|chkget
argument_list|(
argument|addr
argument_list|,
argument|space
argument_list|)
name|off_t
name|addr
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|u_int
name|w
init|=
name|get
argument_list|(
name|addr
argument_list|,
name|space
argument_list|)
decl_stmt|;
name|chkerr
argument_list|()
expr_stmt|;
return|return
operator|(
name|w
operator|)
return|;
block|}
end_block

begin_function
name|u_int
name|bchkget
parameter_list|(
name|addr
parameter_list|,
name|space
parameter_list|)
name|off_t
name|addr
decl_stmt|;
block|{
return|return
operator|(
name|lobyte
argument_list|(
name|chkget
argument_list|(
name|addr
argument_list|,
name|space
argument_list|)
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Read/write according to mode at address addr in i/d space.  * Value is quantity to be written, if write.  *  * This routine decides whether to get the data from the subprocess  * address space with ptrace, or to get it from the files being  * debugged.    *  * When the kernel is being debugged with the -k flag we interpret  * the system page tables for data space, mapping p0 and p1 addresses  * relative to the ``current'' process (as specified by its p_addr in  *<p) and mapping system space addresses through the system page tables.  */
end_comment

begin_macro
name|access
argument_list|(
argument|mode
argument_list|,
argument|addr
argument_list|,
argument|space
argument_list|,
argument|value
argument_list|)
end_macro

begin_decl_stmt
name|int
name|mode
decl_stmt|,
name|space
decl_stmt|,
name|value
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|off_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|rd
init|=
name|mode
operator|==
name|RD
decl_stmt|;
name|int
name|file
decl_stmt|,
name|w
decl_stmt|;
if|if
condition|(
name|space
operator|==
name|NSP
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|pid
condition|)
block|{
name|int
name|pmode
init|=
operator|(
name|space
operator|&
name|DSP
condition|?
operator|(
name|rd
condition|?
name|PT_READ_D
else|:
name|PT_WRITE_D
operator|)
else|:
operator|(
name|rd
condition|?
name|PT_READ_I
else|:
name|PT_WRITE_I
operator|)
operator|)
decl_stmt|;
name|w
operator|=
name|ptrace
argument_list|(
name|pmode
argument_list|,
name|pid
argument_list|,
name|addr
argument_list|,
name|value
argument_list|)
expr_stmt|;
if|if
condition|(
name|errno
condition|)
name|rwerr
argument_list|(
name|space
argument_list|)
expr_stmt|;
return|return
operator|(
name|w
operator|)
return|;
block|}
name|w
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|mode
operator|==
name|WT
operator|&&
name|wtflag
operator|==
literal|0
condition|)
name|error
argument_list|(
literal|"not in write mode"
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|chkmap
argument_list|(
operator|&
name|addr
argument_list|,
name|space
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|file
operator|=
name|mapptr
argument_list|(
name|space
argument_list|)
operator|->
name|ufd
expr_stmt|;
if|if
condition|(
name|kernel
operator|&&
name|space
operator|==
name|DSP
condition|)
block|{
name|addr
operator|=
name|vtophys
argument_list|(
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|addr
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|physrw
argument_list|(
name|file
argument_list|,
name|addr
argument_list|,
name|rd
condition|?
operator|&
name|w
else|:
operator|&
name|value
argument_list|,
name|rd
argument_list|)
operator|<
literal|0
condition|)
name|rwerr
argument_list|(
name|space
argument_list|)
expr_stmt|;
return|return
operator|(
name|w
operator|)
return|;
block|}
end_block

begin_ifdef
ifdef|#
directive|ifdef
name|vax
end_ifdef

begin_comment
comment|/*  * When looking at kernel data space through /dev/mem or  * with a core file, do virtual memory mapping.  */
end_comment

begin_macro
name|vtophys
argument_list|(
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|oldaddr
init|=
name|addr
decl_stmt|;
name|int
name|v
decl_stmt|;
name|struct
name|pte
name|pte
decl_stmt|;
name|addr
operator|&=
operator|~
literal|0xc0000000
expr_stmt|;
name|v
operator|=
name|btop
argument_list|(
name|addr
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|oldaddr
operator|&
literal|0xc0000000
condition|)
block|{
case|case
literal|0xc0000000
case|:
case|case
literal|0x80000000
case|:
comment|/* 		 * In system space get system pte.  If 		 * valid or reclaimable then physical address 		 * is combination of its page number and the page 		 * offset of the original address. 		 */
if|if
condition|(
name|v
operator|>=
name|slr
condition|)
goto|goto
name|oor
goto|;
name|addr
operator|=
operator|(
call|(
name|long
call|)
argument_list|(
name|sbr
operator|+
name|v
argument_list|)
operator|)
operator|&
operator|~
literal|0x80000000
expr_stmt|;
goto|goto
name|simple
goto|;
case|case
literal|0x40000000
case|:
comment|/* 		 * In p1 space must not be in shadow region. 		 */
if|if
condition|(
name|v
operator|<
name|pcb
operator|.
name|pcb_p1lr
condition|)
goto|goto
name|oor
goto|;
name|addr
operator|=
name|pcb
operator|.
name|pcb_p1br
operator|+
name|v
expr_stmt|;
break|break;
case|case
literal|0x00000000
case|:
comment|/* 		 * In p0 space must not be off end of region. 		 */
if|if
condition|(
name|v
operator|>=
name|pcb
operator|.
name|pcb_p0lr
condition|)
goto|goto
name|oor
goto|;
name|addr
operator|=
name|pcb
operator|.
name|pcb_p0br
operator|+
name|v
expr_stmt|;
break|break;
name|oor
label|:
name|errflg
operator|=
literal|"address out of segment"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
comment|/* 	 * For p0/p1 address, user-level page table should 	 * be in kernel vm.  Do second-level indirect by recursing. 	 */
if|if
condition|(
operator|(
name|addr
operator|&
literal|0x80000000
operator|)
operator|==
literal|0
condition|)
block|{
name|errflg
operator|=
literal|"bad p0br or p1br in pcb"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
name|addr
operator|=
name|vtophys
argument_list|(
name|addr
argument_list|)
expr_stmt|;
name|simple
label|:
comment|/* 	 * Addr is now address of the pte of the page we 	 * are interested in; get the pte and paste up the 	 * physical address. 	 */
if|if
condition|(
name|physrw
argument_list|(
name|fcor
argument_list|,
name|addr
argument_list|,
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
condition|)
block|{
name|errflg
operator|=
literal|"page table botch"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
comment|/* SHOULD CHECK NOT I/O ADDRESS; NEED CPU TYPE! */
if|if
condition|(
name|pte
operator|.
name|pg_v
operator|==
literal|0
operator|&&
operator|(
name|pte
operator|.
name|pg_fod
operator|||
name|pte
operator|.
name|pg_pfnum
operator|==
literal|0
operator|)
condition|)
block|{
name|errflg
operator|=
literal|"page not valid/reclaimable"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
return|return
operator|(
name|ptob
argument_list|(
name|pte
operator|.
name|pg_pfnum
argument_list|)
operator|+
operator|(
name|oldaddr
operator|&
name|PGOFSET
operator|)
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|hp300
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|NEWVM
end_ifdef

begin_ifndef
ifndef|#
directive|ifndef
name|btop
end_ifndef

begin_define
define|#
directive|define
name|btop
value|hp300_btop
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|ptob
end_ifndef

begin_define
define|#
directive|define
name|ptob
value|hp300_ptob
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|int
name|is68040
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * When looking at kernel data space through /dev/mem or  * with a core file, do virtual memory mapping.  */
end_comment

begin_macro
name|vtophys
argument_list|(
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|int
name|v
decl_stmt|;
name|struct
name|pte
name|pte
decl_stmt|;
name|int
name|oldaddr
init|=
name|addr
decl_stmt|;
if|if
condition|(
name|INKERNEL
argument_list|(
name|addr
argument_list|)
condition|)
block|{
comment|/* 		 * In system space get system pte.  If 		 * valid or reclaimable then physical address 		 * is combination of its page number and the page 		 * offset of the original address. 		 */
ifdef|#
directive|ifdef
name|NEWVM
comment|/* locate PTE page in segtab */
if|if
condition|(
name|is68040
condition|)
block|{
name|int
name|steaddr
decl_stmt|;
name|steaddr
operator|=
name|KVTOPH
argument_list|(
call|(
name|int
call|)
argument_list|(
name|sbr
operator|+
operator|(
name|addr
operator|>>
name|SG4_SHIFT1
operator|)
argument_list|)
argument_list|)
expr_stmt|;
name|lseek
argument_list|(
name|fcor
argument_list|,
operator|(
name|off_t
operator|)
name|steaddr
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|read
argument_list|(
name|fcor
argument_list|,
operator|&
name|pte
argument_list|,
sizeof|sizeof
name|pte
argument_list|)
expr_stmt|;
if|#
directive|if
literal|0
block|printf("va %X: ste1 %X@%X", 			       addr, *(int *)&pte, steaddr);
endif|#
directive|endif
if|if
condition|(
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
operator|==
name|SG_NV
condition|)
goto|goto
name|bad
goto|;
name|steaddr
operator|=
call|(
name|int
call|)
argument_list|(
operator|(
operator|(
name|int
operator|*
operator|)
operator|(
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
operator|&
name|SG4_ADDR1
operator|)
operator|)
operator|+
operator|(
operator|(
name|addr
operator|&
name|SG4_MASK2
operator|)
operator|>>
name|SG4_SHIFT2
operator|)
argument_list|)
expr_stmt|;
name|physrw
argument_list|(
name|fcor
argument_list|,
name|steaddr
argument_list|,
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|#
directive|if
literal|0
block|printf(" ste2 %X@%X", *(int *)&pte, steaddr);
endif|#
directive|endif
block|}
else|else
block|{
name|lseek
argument_list|(
name|fcor
argument_list|,
operator|(
name|off_t
operator|)
name|KVTOPH
argument_list|(
call|(
name|int
call|)
argument_list|(
name|sbr
operator|+
operator|(
name|addr
operator|>>
name|SG_ISHIFT
operator|)
argument_list|)
argument_list|)
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|read
argument_list|(
name|fcor
argument_list|,
operator|&
name|pte
argument_list|,
sizeof|sizeof
name|pte
argument_list|)
expr_stmt|;
if|#
directive|if
literal|0
block|printf("va %X: ste %X@%X", 			       addr, *(int *)&pte, sbr+(addr>>SG_ISHIFT));
endif|#
directive|endif
block|}
comment|/* see if STE is valid */
if|if
condition|(
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
operator|==
name|SG_NV
condition|)
block|{
name|bad
label|:
name|errflg
operator|=
literal|"address out of segment"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
comment|/* desired PTE is within that page */
name|v
operator|=
name|btop
argument_list|(
name|addr
operator|&
name|SG_PMASK
argument_list|)
expr_stmt|;
name|addr
operator|=
operator|(
name|pte
operator|.
name|pg_pfnum
operator|<<
name|PGSHIFT
operator|)
operator|+
operator|(
name|v
operator|*
sizeof|sizeof
name|pte
operator|)
expr_stmt|;
else|#
directive|else
name|v
operator|=
name|btop
argument_list|(
name|addr
operator|-
name|KERNOFF
argument_list|)
expr_stmt|;
name|addr
operator|=
call|(
name|long
call|)
argument_list|(
name|sbr
operator|+
name|v
argument_list|)
operator|+
name|lowram
expr_stmt|;
endif|#
directive|endif
block|}
elseif|else
if|if
condition|(
name|INUDOT
argument_list|(
name|addr
argument_list|)
condition|)
block|{
name|addr
operator|-=
name|kernudot
expr_stmt|;
name|addr
operator|+=
name|masterpcbb
expr_stmt|;
return|return
operator|(
name|vtophys
argument_list|(
name|addr
argument_list|)
operator|)
return|;
block|}
else|else
comment|/* user space */
block|{
ifdef|#
directive|ifdef
name|NEWVM
name|errflg
operator|=
literal|"cannot translate user addresses"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
else|#
directive|else
name|v
operator|=
name|btop
argument_list|(
name|addr
argument_list|)
expr_stmt|;
comment|/* 		 * Must be within bounds of p0 or p1 regions. 		 */
if|if
condition|(
name|v
operator|<
name|pcb
operator|.
name|pcb_p0lr
condition|)
name|addr
operator|=
name|pcb
operator|.
name|pcb_p0br
operator|+
name|v
expr_stmt|;
elseif|else
if|if
condition|(
name|v
operator|>=
name|pcb
operator|.
name|pcb_p1lr
condition|)
name|addr
operator|=
name|pcb
operator|.
name|pcb_p1br
operator|+
name|v
expr_stmt|;
else|else
block|{
name|errflg
operator|=
literal|"address out of segment"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
comment|/* 		 * For p0/p1 address, user-level page table should 		 * be in kernel vm.  Do second-level indirect by recursing. 		 */
if|if
condition|(
operator|!
name|INKERNEL
argument_list|(
name|addr
argument_list|)
condition|)
block|{
name|errflg
operator|=
literal|"bad p0br or p1br in pcb"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
name|addr
operator|=
name|vtophys
argument_list|(
name|addr
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Addr is now address of the pte of the page we 	 * are interested in; get the pte and paste up the 	 * physical address. 	 */
if|if
condition|(
name|physrw
argument_list|(
name|fcor
argument_list|,
name|addr
argument_list|,
operator|(
name|int
operator|*
operator|)
operator|&
name|pte
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
condition|)
block|{
name|errflg
operator|=
literal|"page table botch"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
if|if
condition|(
name|pte
operator|.
name|pg_v
operator|==
literal|0
operator|&&
ifdef|#
directive|ifdef
name|NEWVM
name|pte
operator|.
name|pg_pfnum
operator|==
literal|0
else|#
directive|else
operator|(
name|pte
operator|.
name|pg_fod
operator|||
name|pte
operator|.
name|pg_pfnum
operator|==
literal|0
operator|)
endif|#
directive|endif
condition|)
block|{
name|errflg
operator|=
literal|"page not valid/reclaimable"
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
if|#
directive|if
literal|0
block|printf(" -> pte %X@%X -> addr %X\n", 	       *(int *)&pte, addr, ptob(pte.pg_pfnum) + (oldaddr& PGOFSET));
endif|#
directive|endif
return|return
operator|(
name|ptob
argument_list|(
name|pte
operator|.
name|pg_pfnum
argument_list|)
operator|+
operator|(
name|oldaddr
operator|&
name|PGOFSET
operator|)
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|!
name|vax
operator|&&
operator|!
name|hp300
end_if

begin_expr_stmt
name|help
operator|!
endif|#
directive|endif
name|rwerr
argument_list|(
argument|space
argument_list|)
name|int
name|space
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
name|space
operator|&
name|DSP
condition|)
name|errflg
operator|=
literal|"data address not found"
expr_stmt|;
elseif|else
if|if
condition|(
name|space
operator|&
name|PSP
condition|)
name|errflg
operator|=
literal|"physical address not found"
expr_stmt|;
else|else
name|errflg
operator|=
literal|"text address not found"
expr_stmt|;
block|}
end_block

begin_macro
name|physrw
argument_list|(
argument|file
argument_list|,
argument|addr
argument_list|,
argument|aw
argument_list|,
argument|rd
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|addr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|aw
decl_stmt|,
name|rd
decl_stmt|;
end_decl_stmt

begin_block
block|{
ifdef|#
directive|ifdef
name|hp300
if|if
condition|(
name|kcore
operator|&&
operator|!
name|kmem
operator|&&
name|file
operator|==
name|fcor
condition|)
name|addr
operator|-=
name|lowram
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|longseek
argument_list|(
name|file
argument_list|,
name|addr
argument_list|)
operator|==
literal|0
operator|||
operator|(
name|rd
condition|?
name|read
argument_list|(
name|file
argument_list|,
name|aw
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
else|:
name|write
argument_list|(
name|file
argument_list|,
name|aw
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
operator|)
operator|<
literal|1
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_macro
name|chkmap
argument_list|(
argument|addr
argument_list|,
argument|space
argument_list|)
end_macro

begin_decl_stmt
name|REG
name|L_INT
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|REG
name|INT
name|space
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|REG
name|MAPPTR
name|amap
decl_stmt|;
name|amap
operator|=
name|mapptr
argument_list|(
name|space
argument_list|)
expr_stmt|;
name|IF
name|space
operator|&
name|STAR
name|ORF
operator|!
name|within
argument_list|(
argument|*addr
argument_list|,
argument|amap->b1
argument_list|,
argument|amap->e1
argument_list|)
name|THEN
name|IF
name|within
argument_list|(
argument|*addr
argument_list|,
argument|amap->b2
argument_list|,
argument|amap->e2
argument_list|)
name|THEN
operator|*
name|addr
operator|+=
operator|(
name|amap
operator|->
name|f2
operator|)
operator|-
operator|(
name|amap
operator|->
name|b2
operator|)
expr_stmt|;
name|ELSE
name|rwerr
parameter_list|(
name|space
parameter_list|)
function_decl|;
return|return
operator|(
literal|0
operator|)
return|;
name|FI
name|ELSE
operator|*
name|addr
operator|+=
operator|(
name|amap
operator|->
name|f1
operator|)
operator|-
operator|(
name|amap
operator|->
name|b1
operator|)
expr_stmt|;
name|FI
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_macro
name|within
argument_list|(
argument|addr
argument_list|,
argument|lbd
argument_list|,
argument|ubd
argument_list|)
end_macro

begin_decl_stmt
name|u_int
name|addr
decl_stmt|,
name|lbd
decl_stmt|,
name|ubd
decl_stmt|;
end_decl_stmt

begin_block
block|{
return|return
operator|(
name|addr
operator|>=
name|lbd
operator|&&
name|addr
operator|<
name|ubd
operator|)
return|;
block|}
end_block

begin_macro
name|longseek
argument_list|(
argument|f
argument_list|,
argument|a
argument_list|)
end_macro

begin_decl_stmt
name|off_t
name|a
decl_stmt|;
end_decl_stmt

begin_block
block|{
return|return
operator|(
name|lseek
argument_list|(
name|f
argument_list|,
name|a
argument_list|,
literal|0
argument_list|)
operator|!=
operator|-
literal|1
operator|)
return|;
block|}
end_block

end_unit

