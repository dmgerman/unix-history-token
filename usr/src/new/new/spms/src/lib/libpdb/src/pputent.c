begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $Header$ */
end_comment

begin_comment
comment|/*  * Author: Peter J. Nicklin  */
end_comment

begin_comment
comment|/*  * pputent() writes buffer pdbp->pbuf to a database. Returns integer NO if  * write error or buffer exceeded, otherwise YES.  */
end_comment

begin_include
include|#
directive|include
file|"stdio.h"
end_include

begin_include
include|#
directive|include
file|"path.h"
end_include

begin_include
include|#
directive|include
file|"pdb.h"
end_include

begin_include
include|#
directive|include
file|"yesno.h"
end_include

begin_expr_stmt
name|pputent
argument_list|(
name|pdbp
argument_list|)
specifier|register
name|PDB
operator|*
name|pdbp
expr_stmt|;
end_expr_stmt

begin_comment
comment|/* database stream */
end_comment

begin_block
block|{
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
comment|/* buffer pointer */
specifier|register
name|FILE
modifier|*
name|fp
decl_stmt|;
comment|/* file stream */
specifier|register
name|int
name|i
decl_stmt|;
comment|/* buffer counter */
name|char
modifier|*
name|sprintf
parameter_list|()
function_decl|;
comment|/* print output to string */
if|if
condition|(
operator|(
name|pdbp
operator|->
name|flag
operator|&
name|_PERR
operator|)
operator|!=
literal|0
condition|)
return|return
operator|(
name|NO
operator|)
return|;
if|if
condition|(
operator|(
name|pdbp
operator|->
name|flag
operator|&
name|_PACCESS
operator|)
operator|==
operator|(
name|_PREAD
operator||
name|_PWRITE
operator|)
condition|)
name|fp
operator|=
name|pdbp
operator|->
name|tfp
expr_stmt|;
elseif|else
if|if
condition|(
operator|(
name|pdbp
operator|->
name|flag
operator|&
operator|(
name|_PWRITE
operator||
name|_PAPPEND
operator|)
operator|)
operator|!=
literal|0
condition|)
name|fp
operator|=
name|pdbp
operator|->
name|fp
expr_stmt|;
else|else
block|{
name|sprintf
argument_list|(
name|pdbp
operator|->
name|perr
argument_list|,
literal|"%s read access only"
argument_list|,
name|pdbp
operator|->
name|path
argument_list|)
expr_stmt|;
name|pdbp
operator|->
name|flag
operator||=
name|_PERR
expr_stmt|;
return|return
operator|(
name|NO
operator|)
return|;
block|}
name|i
operator|=
literal|0
expr_stmt|;
name|bp
operator|=
name|pdbp
operator|->
name|pbuf
expr_stmt|;
name|pdbp
operator|->
name|flag
operator||=
name|_PUPDATE
expr_stmt|;
while|while
condition|(
name|i
operator|<
name|PBUFSIZE
operator|&&
operator|*
name|bp
operator|++
operator|!=
literal|'\0'
condition|)
name|putc
argument_list|(
name|pdbp
operator|->
name|pbuf
index|[
name|i
operator|++
index|]
argument_list|,
name|fp
argument_list|)
expr_stmt|;
if|if
condition|(
name|putc
argument_list|(
literal|'\n'
argument_list|,
name|fp
argument_list|)
operator|==
name|EOF
condition|)
block|{
name|sprintf
argument_list|(
name|pdbp
operator|->
name|perr
argument_list|,
literal|"%s write error"
argument_list|,
name|pdbp
operator|->
name|path
argument_list|)
expr_stmt|;
name|pdbp
operator|->
name|flag
operator||=
name|_PERR
expr_stmt|;
return|return
operator|(
name|NO
operator|)
return|;
block|}
if|if
condition|(
name|i
operator|>=
name|PBUFSIZE
condition|)
block|{
name|sprintf
argument_list|(
name|pdbp
operator|->
name|perr
argument_list|,
literal|"%s database buffer exceeded"
argument_list|,
name|pdbp
operator|->
name|path
argument_list|)
expr_stmt|;
name|pdbp
operator|->
name|flag
operator||=
name|_PERR
expr_stmt|;
return|return
operator|(
name|NO
operator|)
return|;
block|}
return|return
operator|(
name|YES
operator|)
return|;
block|}
end_block

end_unit

