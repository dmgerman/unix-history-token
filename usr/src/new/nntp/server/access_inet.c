begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|sccsid
init|=
literal|"@(#)access_inet.c	1.2	(Berkeley) 10/15/87"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<strings.h>
end_include

begin_include
include|#
directive|include
file|<netdb.h>
end_include

begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<arpa/inet.h>
end_include

begin_include
include|#
directive|include
file|"../common/conf.h"
end_include

begin_comment
comment|/*  * inet_netnames -- return the network, subnet, and host names of  * our peer process for the Internet domain.  *  *	Parameters:	"sock" is our socket, which we don't need.  *			"sin" is a pointer to the result of  *			a getpeername() call.  *			"net_name", "subnet_name", and "host_name"  *			are filled in by this routine with the  *			corresponding ASCII names of our peer.  *	Returns:	Nothing.  *	Side effects:	None.  */
end_comment

begin_macro
name|inet_netnames
argument_list|(
argument|sock
argument_list|,
argument|sin
argument_list|,
argument|net_name
argument_list|,
argument|subnet_name
argument_list|,
argument|host_name
argument_list|)
end_macro

begin_decl_stmt
name|int
name|sock
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr_in
modifier|*
name|sin
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|net_name
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|subnet_name
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|host_name
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|static
name|int
name|gotsubnetconf
decl_stmt|;
name|u_long
name|net_addr
decl_stmt|;
name|u_long
name|subnet_addr
decl_stmt|;
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|struct
name|netent
modifier|*
name|np
decl_stmt|;
ifdef|#
directive|ifdef
name|SUBNET
if|if
condition|(
operator|!
name|gotsubnetconf
condition|)
block|{
if|if
condition|(
name|getifconf
argument_list|()
operator|<
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|SYSLOG
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"host_access: getifconf: %m"
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return;
block|}
name|gotsubnetconf
operator|=
literal|1
expr_stmt|;
block|}
endif|#
directive|endif
name|net_addr
operator|=
name|inet_netof
argument_list|(
name|sin
operator|->
name|sin_addr
argument_list|)
expr_stmt|;
comment|/* net_addr in host order */
name|np
operator|=
name|getnetbyaddr
argument_list|(
name|net_addr
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
if|if
condition|(
name|np
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|net_name
argument_list|,
name|np
operator|->
name|n_name
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|net_name
argument_list|,
name|inet_ntoa
argument_list|(
operator|*
operator|(
expr|struct
name|in_addr
operator|*
operator|)
operator|&
name|net_addr
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SUBNET
name|subnet_addr
operator|=
name|inet_snetof
argument_list|(
name|sin
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|subnet_addr
operator|==
literal|0
condition|)
name|subnet_name
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
else|else
block|{
name|np
operator|=
name|getnetbyaddr
argument_list|(
name|subnet_addr
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
if|if
condition|(
name|np
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|subnet_name
argument_list|,
name|np
operator|->
name|n_name
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|subnet_name
argument_list|,
name|inet_ntoa
argument_list|(
operator|*
operator|(
expr|struct
name|in_addr
operator|*
operator|)
operator|&
name|subnet_addr
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
name|subnet_name
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
endif|#
directive|endif
endif|SUBNET
name|hp
operator|=
name|gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sin
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|,
sizeof|sizeof
argument_list|(
name|sin
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|host_name
argument_list|,
name|hp
operator|->
name|h_name
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|host_name
argument_list|,
name|inet_ntoa
argument_list|(
name|sin
operator|->
name|sin_addr
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

