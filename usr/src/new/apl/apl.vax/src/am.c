begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_decl_stmt
specifier|static
name|char
name|Sccsid
index|[]
init|=
literal|"am.c @(#)am.c	1.4	6/16/87 Berkeley "
decl_stmt|;
end_decl_stmt

begin_include
include|#
directive|include
file|"apl.h"
end_include

begin_include
include|#
directive|include
file|<sgtty.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|VLIMIT
end_ifdef

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* The following code is so system-dependent that it was moved to  * a separate file.  It conditions the terminal for APL mode if called  * with a non-zero argument, or returns it to its normal state if called  * with a zero argument.  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|mkcore
decl_stmt|;
end_decl_stmt

begin_ifndef
ifndef|#
directive|ifndef
name|PURDUE_EE
end_ifndef

begin_macro
name|aplmod
argument_list|(
argument|n
argument_list|)
end_macro

begin_block
block|{
specifier|static
name|struct
name|sgttyb
name|m
decl_stmt|;
if|if
condition|(
name|n
condition|)
block|{
ifdef|#
directive|ifdef
name|VLIMIT
if|if
condition|(
operator|!
name|mkcore
condition|)
block|{
name|struct
name|rlimit
name|rl
decl_stmt|;
name|getrlimit
argument_list|(
name|RLIMIT_CORE
argument_list|,
operator|&
name|rl
argument_list|)
expr_stmt|;
name|rl
operator|.
name|rlim_cur
operator|=
literal|0
expr_stmt|;
name|setrlimit
argument_list|(
name|RLIMIT_CORE
argument_list|,
operator|&
name|rl
argument_list|)
expr_stmt|;
comment|/* no core file! */
block|}
endif|#
directive|endif
name|gtty
argument_list|(
literal|0
argument_list|,
operator|&
name|m
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|m
operator|.
name|sg_erase
operator|==
literal|'\b'
operator|||
name|m
operator|.
name|sg_kill
operator|==
literal|'\b'
operator|)
operator|&&
operator|!
name|prwsflg
condition|)
name|printf
argument_list|(
literal|"[warning: erase char is ctl-h]\n"
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|vax
end_ifdef

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_expr_stmt
specifier|static
name|modesave
expr_stmt|;
end_expr_stmt

begin_expr_stmt
specifier|static
name|aplmode
operator|=
name|LAPL
expr_stmt|;
end_expr_stmt

begin_expr_stmt
specifier|static
name|discsave
expr_stmt|;
end_expr_stmt

begin_expr_stmt
specifier|static
name|olddisc
operator|=
name|OTTYDISC
expr_stmt|;
end_expr_stmt

begin_expr_stmt
specifier|static
name|turkey
operator|=
name|LTURKEY
expr_stmt|;
end_expr_stmt

begin_expr_stmt
specifier|static
name|ctlech
operator|=
name|LCTLECH
expr_stmt|;
end_expr_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_macro
name|aplmod
argument_list|(
argument|n
argument_list|)
end_macro

begin_block
block|{
specifier|static
name|struct
name|sgttyb
name|m
decl_stmt|;
if|if
condition|(
name|n
condition|)
block|{
name|gtty
argument_list|(
literal|0
argument_list|,
operator|&
name|m
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|m
operator|.
name|sg_erase
operator|==
literal|'\b'
operator|||
name|m
operator|.
name|sg_kill
operator|==
literal|'\b'
operator|)
operator|&&
operator|!
name|prwsflg
condition|)
name|printf
argument_list|(
literal|"[warning: erase char is ctl-h]\n"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|VLIMIT
if|if
condition|(
operator|!
name|mkcore
condition|)
block|{
name|struct
name|rlimit
name|rl
decl_stmt|;
name|getrlimit
argument_list|(
name|RLIMIT_CORE
argument_list|,
operator|&
name|rl
argument_list|)
expr_stmt|;
name|rl
operator|.
name|rlim_cur
operator|=
literal|0
expr_stmt|;
name|setrlimit
argument_list|(
name|RLIMIT_CORE
argument_list|,
operator|&
name|rl
argument_list|)
expr_stmt|;
comment|/* no core file! */
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|vax
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCLGET
argument_list|,
operator|&
name|modesave
argument_list|)
expr_stmt|;
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCLBIC
argument_list|,
operator|&
name|ctlech
argument_list|)
expr_stmt|;
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCLBIC
argument_list|,
operator|&
name|turkey
argument_list|)
expr_stmt|;
else|#
directive|else
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCNNOCTRL
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* Turn on apl mode if requested */
if|if
condition|(
name|apl_term
condition|)
block|{
ifndef|#
directive|ifndef
name|vax
name|m
operator|.
name|sg_flags
operator||=
name|APLMOD
expr_stmt|;
name|stty
argument_list|(
literal|0
argument_list|,
operator|&
name|m
argument_list|)
expr_stmt|;
else|#
directive|else
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCGETD
argument_list|,
operator|&
name|discsave
argument_list|)
expr_stmt|;
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCSETD
argument_list|,
operator|&
name|olddisc
argument_list|)
expr_stmt|;
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCLBIS
argument_list|,
operator|&
name|aplmode
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|printf
argument_list|(
literal|"set terminal to apl mode\n"
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
comment|/* Turn off apl mode */
ifndef|#
directive|ifndef
name|vax
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCSNOCTRL
argument_list|)
expr_stmt|;
name|gtty
argument_list|(
literal|0
argument_list|,
operator|&
name|m
argument_list|)
expr_stmt|;
name|m
operator|.
name|sg_flags
operator|&=
operator|~
name|APLMOD
expr_stmt|;
name|stty
argument_list|(
literal|0
argument_list|,
operator|&
name|m
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
name|apl_term
condition|)
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCSETD
argument_list|,
operator|&
name|discsave
argument_list|)
expr_stmt|;
name|ioctl
argument_list|(
literal|0
argument_list|,
name|TIOCLSET
argument_list|,
operator|&
name|modesave
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

