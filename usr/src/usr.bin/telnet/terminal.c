begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<arpa/telnet.h>
end_include

begin_include
include|#
directive|include
file|"externs.h"
end_include

begin_include
include|#
directive|include
file|"types.h"
end_include

begin_decl_stmt
name|char
name|ttyobuf
index|[
literal|2
operator|*
name|BUFSIZ
index|]
decl_stmt|,
modifier|*
name|tfrontp
decl_stmt|,
modifier|*
name|tbackp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|termEofChar
decl_stmt|,
name|termEraseChar
decl_stmt|,
name|termFlushChar
decl_stmt|,
name|termIntChar
decl_stmt|,
name|termKillChar
decl_stmt|,
name|termLiteralNextChar
decl_stmt|,
name|termQuitChar
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * initialize the terminal data structures.  */
end_comment

begin_macro
name|init_terminal
argument_list|()
end_macro

begin_block
block|{
name|tfrontp
operator|=
name|tbackp
operator|=
name|ttyobuf
expr_stmt|;
name|autoflush
operator|=
name|TerminalAutoFlush
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  *		Send as much data as possible to the terminal.  *  *		The return value indicates whether we did any  *	useful work.  */
end_comment

begin_function
name|int
name|ttyflush
parameter_list|(
name|drop
parameter_list|)
name|int
name|drop
decl_stmt|;
block|{
name|int
name|n
decl_stmt|;
if|if
condition|(
operator|(
name|n
operator|=
name|tfrontp
operator|-
name|tbackp
operator|)
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|drop
condition|)
block|{
name|TerminalFlushOutput
argument_list|()
expr_stmt|;
comment|/* we leave 'n' alone! */
block|}
else|else
block|{
name|n
operator|=
name|TerminalWrite
argument_list|(
name|tout
argument_list|,
name|tbackp
argument_list|,
name|n
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|n
operator|>=
literal|0
condition|)
block|{
name|tbackp
operator|+=
name|n
expr_stmt|;
if|if
condition|(
name|tbackp
operator|==
name|tfrontp
condition|)
block|{
name|tbackp
operator|=
name|tfrontp
operator|=
name|ttyobuf
expr_stmt|;
block|}
block|}
return|return
name|n
operator|>
literal|0
return|;
block|}
end_function

begin_escape
end_escape

begin_if
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
end_if

begin_comment
comment|/*  * Various signal handling routines.  */
end_comment

begin_function
name|void
name|deadpeer
parameter_list|()
block|{
name|setcommandmode
argument_list|()
expr_stmt|;
name|longjmp
argument_list|(
name|peerdied
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|intr
parameter_list|()
block|{
if|if
condition|(
name|localchars
condition|)
block|{
name|intp
argument_list|()
expr_stmt|;
return|return;
block|}
name|setcommandmode
argument_list|()
expr_stmt|;
name|longjmp
argument_list|(
name|toplevel
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|intr2
parameter_list|()
block|{
if|if
condition|(
name|localchars
condition|)
block|{
name|sendbrk
argument_list|()
expr_stmt|;
return|return;
block|}
block|}
end_function

begin_function
name|void
name|doescape
parameter_list|()
block|{
name|command
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(unix) */
end_comment

begin_escape
end_escape

begin_comment
comment|/*  * These routines decides on what the mode should be (based on the values  * of various global variables).  */
end_comment

begin_function
name|int
name|getconnmode
parameter_list|()
block|{
specifier|static
name|char
name|newmode
index|[
literal|16
index|]
init|=
block|{
literal|4
block|,
literal|5
block|,
literal|3
block|,
literal|3
block|,
literal|2
block|,
literal|2
block|,
literal|1
block|,
literal|1
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|}
decl_stmt|;
name|int
name|modeindex
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|dontlecho
operator|&&
operator|(
name|clocks
operator|.
name|echotoggle
operator|>
name|clocks
operator|.
name|modenegotiated
operator|)
condition|)
block|{
name|modeindex
operator|+=
literal|1
expr_stmt|;
block|}
if|if
condition|(
name|hisopts
index|[
name|TELOPT_ECHO
index|]
condition|)
block|{
name|modeindex
operator|+=
literal|2
expr_stmt|;
block|}
if|if
condition|(
name|hisopts
index|[
name|TELOPT_SGA
index|]
condition|)
block|{
name|modeindex
operator|+=
literal|4
expr_stmt|;
block|}
if|if
condition|(
name|In3270
condition|)
block|{
name|modeindex
operator|+=
literal|8
expr_stmt|;
block|}
return|return
name|newmode
index|[
name|modeindex
index|]
return|;
block|}
end_function

begin_function
name|void
name|setconnmode
parameter_list|()
block|{
name|TerminalNewMode
argument_list|(
name|tin
argument_list|,
name|tout
argument_list|,
name|getconnmode
argument_list|()
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|setcommandmode
parameter_list|()
block|{
name|TerminalNewMode
argument_list|(
name|tin
argument_list|,
name|tout
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

