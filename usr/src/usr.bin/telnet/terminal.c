begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<arpa/telnet.h>
end_include

begin_include
include|#
directive|include
file|"externs.h"
end_include

begin_include
include|#
directive|include
file|"types.h"
end_include

begin_decl_stmt
name|char
name|ttyobuf
index|[
literal|2
operator|*
name|BUFSIZ
index|]
decl_stmt|,
modifier|*
name|tfrontp
decl_stmt|,
modifier|*
name|tbackp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|termEofChar
decl_stmt|,
name|termEraseChar
decl_stmt|,
name|termFlushChar
decl_stmt|,
name|termIntChar
decl_stmt|,
name|termKillChar
decl_stmt|,
name|termLiteralNextChar
decl_stmt|,
name|termQuitChar
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * initialize the terminal data structures.  */
end_comment

begin_macro
name|init_terminal
argument_list|()
end_macro

begin_block
block|{
name|tfrontp
operator|=
name|tbackp
operator|=
name|ttyobuf
expr_stmt|;
name|autoflush
operator|=
name|TerminalAutoFlush
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  *		Send as much data as possible to the terminal.  *  *		The return value indicates whether we did any  *	useful work.  */
end_comment

begin_function
name|int
name|ttyflush
parameter_list|()
block|{
name|int
name|n
decl_stmt|;
if|if
condition|(
operator|(
name|n
operator|=
name|tfrontp
operator|-
name|tbackp
operator|)
operator|>
literal|0
condition|)
block|{
if|if
condition|(
operator|!
operator|(
name|SYNCHing
operator|||
name|flushout
operator|)
condition|)
block|{
name|n
operator|=
name|TerminalWrite
argument_list|(
name|tout
argument_list|,
name|tbackp
argument_list|,
name|n
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|TerminalFlushOutput
argument_list|()
expr_stmt|;
comment|/* we leave 'n' alone! */
block|}
block|}
if|if
condition|(
name|n
operator|>=
literal|0
condition|)
block|{
name|tbackp
operator|+=
name|n
expr_stmt|;
if|if
condition|(
name|tbackp
operator|==
name|tfrontp
condition|)
block|{
name|tbackp
operator|=
name|tfrontp
operator|=
name|ttyobuf
expr_stmt|;
block|}
block|}
return|return
name|n
operator|>
literal|0
return|;
block|}
end_function

begin_if
if|#
directive|if
name|defined
argument_list|(
name|TN3270
argument_list|)
end_if

begin_if
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
end_if

begin_function
specifier|static
name|void
name|inputAvailable
parameter_list|()
block|{
name|HaveInput
operator|=
literal|1
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(unix) */
end_comment

begin_function
name|void
name|outputPurge
parameter_list|()
block|{
name|int
name|tmp
init|=
name|flushout
decl_stmt|;
name|flushout
operator|=
literal|1
expr_stmt|;
name|ttyflush
argument_list|()
expr_stmt|;
name|flushout
operator|=
name|tmp
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(TN3270) */
end_comment

begin_escape
end_escape

begin_if
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
end_if

begin_comment
comment|/*  * Various signal handling routines.  */
end_comment

begin_function
name|void
name|deadpeer
parameter_list|()
block|{
name|setcommandmode
argument_list|()
expr_stmt|;
name|longjmp
argument_list|(
name|peerdied
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|intr
parameter_list|()
block|{
if|if
condition|(
name|localchars
condition|)
block|{
name|intp
argument_list|()
expr_stmt|;
return|return;
block|}
name|setcommandmode
argument_list|()
expr_stmt|;
name|longjmp
argument_list|(
name|toplevel
argument_list|,
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|intr2
parameter_list|()
block|{
if|if
condition|(
name|localchars
condition|)
block|{
name|sendbrk
argument_list|()
expr_stmt|;
return|return;
block|}
block|}
end_function

begin_function
name|void
name|doescape
parameter_list|()
block|{
name|command
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(unix) */
end_comment

begin_escape
end_escape

begin_comment
comment|/*  * These routines decides on what the mode should be (based on the values  * of various global variables).  */
end_comment

begin_function
name|int
name|getconnmode
parameter_list|()
block|{
specifier|static
name|char
name|newmode
index|[
literal|16
index|]
init|=
block|{
literal|4
block|,
literal|5
block|,
literal|3
block|,
literal|3
block|,
literal|2
block|,
literal|2
block|,
literal|1
block|,
literal|1
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|,
literal|6
block|}
decl_stmt|;
name|int
name|modeindex
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|dontlecho
operator|&&
operator|(
name|clocks
operator|.
name|echotoggle
operator|>
name|clocks
operator|.
name|modenegotiated
operator|)
condition|)
block|{
name|modeindex
operator|+=
literal|1
expr_stmt|;
block|}
if|if
condition|(
name|hisopts
index|[
name|TELOPT_ECHO
index|]
condition|)
block|{
name|modeindex
operator|+=
literal|2
expr_stmt|;
block|}
if|if
condition|(
name|hisopts
index|[
name|TELOPT_SGA
index|]
condition|)
block|{
name|modeindex
operator|+=
literal|4
expr_stmt|;
block|}
if|if
condition|(
name|In3270
condition|)
block|{
name|modeindex
operator|+=
literal|8
expr_stmt|;
block|}
return|return
name|newmode
index|[
name|modeindex
index|]
return|;
block|}
end_function

begin_function
name|void
name|setconnmode
parameter_list|()
block|{
name|TerminalNewMode
argument_list|(
name|tin
argument_list|,
name|tout
argument_list|,
name|getconnmode
argument_list|()
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|setcommandmode
parameter_list|()
block|{
name|TerminalNewMode
argument_list|(
name|tin
argument_list|,
name|tout
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_if
if|#
directive|if
name|defined
argument_list|(
name|TN3270
argument_list|)
end_if

begin_comment
comment|/*  * The following routines are places where the various tn3270  * routines make calls into telnet.c.  */
end_comment

begin_comment
comment|/* TtyChars() - returns the number of characters in the TTY buffer */
end_comment

begin_macro
name|TtyChars
argument_list|()
end_macro

begin_block
block|{
return|return
operator|(
name|tfrontp
operator|-
name|tbackp
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * DataToNetwork - queue up some data to go to network.  If "done" is set,  * then when last byte is queued, we add on an IAC EOR sequence (so,  * don't call us with "done" until you want that done...)  *  * We actually do send all the data to the network buffer, since our  * only client needs for us to do that.  */
end_comment

begin_function
name|int
name|DataToNetwork
parameter_list|(
name|buffer
parameter_list|,
name|count
parameter_list|,
name|done
parameter_list|)
specifier|register
name|char
modifier|*
name|buffer
decl_stmt|;
comment|/* where the data is */
specifier|register
name|int
name|count
decl_stmt|;
comment|/* how much to send */
name|int
name|done
decl_stmt|;
comment|/* is this the last of a logical block */
block|{
specifier|register
name|int
name|c
decl_stmt|;
name|int
name|origCount
decl_stmt|;
name|fd_set
name|o
decl_stmt|;
name|origCount
operator|=
name|count
expr_stmt|;
name|FD_ZERO
argument_list|(
operator|&
name|o
argument_list|)
expr_stmt|;
while|while
condition|(
name|count
condition|)
block|{
if|if
condition|(
operator|(
name|netobuf
operator|+
sizeof|sizeof
name|netobuf
operator|-
name|nfrontp
operator|)
operator|<
literal|6
condition|)
block|{
name|netflush
argument_list|()
expr_stmt|;
while|while
condition|(
operator|(
name|netobuf
operator|+
sizeof|sizeof
name|netobuf
operator|-
name|nfrontp
operator|)
operator|<
literal|6
condition|)
block|{
name|FD_SET
argument_list|(
name|net
argument_list|,
operator|&
name|o
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|select
argument_list|(
name|net
operator|+
literal|1
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|o
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
name|netflush
argument_list|()
expr_stmt|;
block|}
block|}
name|c
operator|=
operator|*
name|buffer
operator|++
expr_stmt|;
name|count
operator|--
expr_stmt|;
if|if
condition|(
name|c
operator|==
name|IAC
condition|)
block|{
operator|*
name|nfrontp
operator|++
operator|=
name|IAC
expr_stmt|;
operator|*
name|nfrontp
operator|++
operator|=
name|IAC
expr_stmt|;
block|}
else|else
block|{
operator|*
name|nfrontp
operator|++
operator|=
name|c
expr_stmt|;
block|}
block|}
if|if
condition|(
name|done
operator|&&
operator|!
name|count
condition|)
block|{
operator|*
name|nfrontp
operator|++
operator|=
name|IAC
expr_stmt|;
operator|*
name|nfrontp
operator|++
operator|=
name|EOR
expr_stmt|;
name|netflush
argument_list|()
expr_stmt|;
comment|/* try to move along as quickly as ... */
block|}
return|return
operator|(
name|origCount
operator|-
name|count
operator|)
return|;
block|}
end_function

begin_comment
comment|/* DataToTerminal - queue up some data to go to terminal. */
end_comment

begin_function
name|int
name|DataToTerminal
parameter_list|(
name|buffer
parameter_list|,
name|count
parameter_list|)
specifier|register
name|char
modifier|*
name|buffer
decl_stmt|;
comment|/* where the data is */
specifier|register
name|int
name|count
decl_stmt|;
comment|/* how much to send */
block|{
name|int
name|origCount
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|fd_set
name|o
decl_stmt|;
name|FD_ZERO
argument_list|(
operator|&
name|o
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(unix) */
name|origCount
operator|=
name|count
expr_stmt|;
while|while
condition|(
name|count
condition|)
block|{
if|if
condition|(
name|tfrontp
operator|>=
name|ttyobuf
operator|+
sizeof|sizeof
name|ttyobuf
condition|)
block|{
name|ttyflush
argument_list|()
expr_stmt|;
while|while
condition|(
name|tfrontp
operator|>=
name|ttyobuf
operator|+
sizeof|sizeof
name|ttyobuf
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|FD_SET
argument_list|(
name|tout
argument_list|,
operator|&
name|o
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|select
argument_list|(
name|tout
operator|+
literal|1
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|o
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(unix) */
name|ttyflush
argument_list|()
expr_stmt|;
block|}
block|}
operator|*
name|tfrontp
operator|++
operator|=
operator|*
name|buffer
operator|++
expr_stmt|;
name|count
operator|--
expr_stmt|;
block|}
return|return
operator|(
name|origCount
operator|-
name|count
operator|)
return|;
block|}
end_function

begin_comment
comment|/* EmptyTerminal - called to make sure that the terminal buffer is empty.  *			Note that we consider the buffer to run all the  *			way to the kernel (thus the select).  */
end_comment

begin_function
name|void
name|EmptyTerminal
parameter_list|()
block|{
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|fd_set
name|o
decl_stmt|;
name|FD_ZERO
argument_list|(
operator|&
name|o
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(unix) */
if|if
condition|(
name|tfrontp
operator|==
name|tbackp
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|FD_SET
argument_list|(
name|tout
argument_list|,
operator|&
name|o
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|select
argument_list|(
name|tout
operator|+
literal|1
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|o
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
comment|/* wait for TTLOWAT */
endif|#
directive|endif
comment|/* defined(unix) */
block|}
else|else
block|{
while|while
condition|(
name|tfrontp
operator|!=
name|tbackp
condition|)
block|{
name|ttyflush
argument_list|()
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|FD_SET
argument_list|(
name|tout
argument_list|,
operator|&
name|o
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|select
argument_list|(
name|tout
operator|+
literal|1
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|o
argument_list|,
operator|(
name|int
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
comment|/* wait for TTLOWAT */
endif|#
directive|endif
comment|/* defined(unix) */
block|}
block|}
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/*  * Push3270 - Try to send data along the 3270 output (to screen) direction.  */
end_comment

begin_function
specifier|static
name|int
name|Push3270
parameter_list|()
block|{
name|int
name|save
init|=
name|scc
decl_stmt|;
if|if
condition|(
name|scc
condition|)
block|{
if|if
condition|(
name|Ifrontp
operator|+
name|scc
operator|>
name|Ibuf
operator|+
sizeof|sizeof
name|Ibuf
condition|)
block|{
if|if
condition|(
name|Ibackp
operator|!=
name|Ibuf
condition|)
block|{
name|memcpy
argument_list|(
name|Ibuf
argument_list|,
name|Ibackp
argument_list|,
name|Ifrontp
operator|-
name|Ibackp
argument_list|)
expr_stmt|;
name|Ifrontp
operator|-=
operator|(
name|Ibackp
operator|-
name|Ibuf
operator|)
expr_stmt|;
name|Ibackp
operator|=
name|Ibuf
expr_stmt|;
block|}
block|}
if|if
condition|(
name|Ifrontp
operator|+
name|scc
operator|<
name|Ibuf
operator|+
sizeof|sizeof
name|Ibuf
condition|)
block|{
name|telrcv
argument_list|()
expr_stmt|;
block|}
block|}
return|return
name|save
operator|!=
name|scc
return|;
block|}
end_function

begin_comment
comment|/*  * Finish3270 - get the last dregs of 3270 data out to the terminal  *		before quitting.  */
end_comment

begin_function
specifier|static
name|void
name|Finish3270
parameter_list|()
block|{
while|while
condition|(
name|Push3270
argument_list|()
operator|||
operator|!
name|DoTerminalOutput
argument_list|()
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|unix
argument_list|)
name|HaveInput
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
comment|/* defined(unix) */
empty_stmt|;
block|}
block|}
end_function

begin_comment
comment|/* StringToTerminal - output a null terminated string to the terminal */
end_comment

begin_function
name|void
name|StringToTerminal
parameter_list|(
name|s
parameter_list|)
name|char
modifier|*
name|s
decl_stmt|;
block|{
name|int
name|count
decl_stmt|;
name|count
operator|=
name|strlen
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|count
condition|)
block|{
operator|(
name|void
operator|)
name|DataToTerminal
argument_list|(
name|s
argument_list|,
name|count
argument_list|)
expr_stmt|;
comment|/* we know it always goes... */
block|}
block|}
end_function

begin_if
if|#
directive|if
operator|(
operator|(
operator|!
name|defined
argument_list|(
name|NOT43
argument_list|)
operator|)
operator|||
name|defined
argument_list|(
name|PUTCHAR
argument_list|)
operator|)
end_if

begin_comment
comment|/* _putchar - output a single character to the terminal.  This name is so that  *	curses(3x) can call us to send out data.  */
end_comment

begin_function
name|void
name|_putchar
parameter_list|(
name|c
parameter_list|)
name|char
name|c
decl_stmt|;
block|{
if|if
condition|(
name|tfrontp
operator|>=
name|ttyobuf
operator|+
sizeof|sizeof
name|ttyobuf
condition|)
block|{
operator|(
name|void
operator|)
name|DataToTerminal
argument_list|(
operator|&
name|c
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|*
name|tfrontp
operator|++
operator|=
name|c
expr_stmt|;
comment|/* optimize if possible. */
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ((!defined(NOT43)) || defined(PUTCHAR)) */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(TN3270) */
end_comment

end_unit

