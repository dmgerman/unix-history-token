begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1990 The Regents of the University of California.  * All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * Cimarron D. Taylor of the University of California, Berkeley.  *  * %sccs.include.redist.c%  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)operator.c	5.1 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"find.h"
end_include

begin_comment
comment|/*  * find_yanknode --  *	destructively removes the top from the plan  */
end_comment

begin_function
name|PLAN
modifier|*
name|find_yanknode
parameter_list|(
name|planp
parameter_list|)
name|PLAN
modifier|*
modifier|*
name|planp
decl_stmt|;
comment|/* pointer to top of plan (modified) */
block|{
name|PLAN
modifier|*
name|node
decl_stmt|;
comment|/* top node removed from the plan */
if|if
condition|(
operator|(
name|node
operator|=
operator|(
operator|*
name|planp
operator|)
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
operator|(
operator|*
name|planp
operator|)
operator|=
operator|(
operator|*
name|planp
operator|)
operator|->
name|next
expr_stmt|;
name|node
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
return|return
operator|(
name|node
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * find_yankexpr --  *	Removes one expression from the plan.  This is used mainly by  *	find_squish_paren.  In comments below, an expression is either  *	a simple node or a T_EXPR node containing a list of simple nodes.  */
end_comment

begin_function
name|PLAN
modifier|*
name|find_yankexpr
parameter_list|(
name|planp
parameter_list|)
name|PLAN
modifier|*
modifier|*
name|planp
decl_stmt|;
comment|/* pointer to top of plan (modified) */
block|{
specifier|register
name|PLAN
modifier|*
name|next
decl_stmt|;
comment|/* temp node holding subexpression results */
name|PLAN
modifier|*
name|node
decl_stmt|;
comment|/* pointer to returned node or expression */
name|PLAN
modifier|*
name|tail
decl_stmt|;
comment|/* pointer to tail of subplan */
name|PLAN
modifier|*
name|subplan
decl_stmt|;
comment|/* pointer to head of ( ) expression */
name|int
name|f_expr
parameter_list|()
function_decl|;
comment|/* first pull the top node from the plan */
if|if
condition|(
operator|(
name|node
operator|=
name|find_yanknode
argument_list|(
name|planp
argument_list|)
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
comment|/* 	 * If the node is an '(' then we recursively slurp up expressions 	 * until we find its associated ')'.  If it's a closing paren we 	 * just return it and unwind our recursion; all other nodes are 	 * complete expressions, so just return them. 	 */
if|if
condition|(
name|node
operator|->
name|type
operator|==
name|T_OPENPAREN
condition|)
for|for
control|(
name|tail
operator|=
name|subplan
operator|=
name|NULL
init|;
condition|;
control|)
block|{
if|if
condition|(
operator|(
name|next
operator|=
name|find_yankexpr
argument_list|(
name|planp
argument_list|)
operator|)
operator|==
name|NULL
condition|)
name|bad_arg
argument_list|(
literal|"("
argument_list|,
literal|"missing closing ')'"
argument_list|)
expr_stmt|;
comment|/* 			 * If we find a closing ')' we store the collected 			 * subplan in our '(' node and convert the node to 			 * a T_EXPR.  The ')' we found is ignored.  Otherwise, 			 * we just continue to add whatever we get to our 			 * subplan. 			 */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_CLOSEPAREN
condition|)
block|{
if|if
condition|(
name|subplan
operator|==
name|NULL
condition|)
name|bad_arg
argument_list|(
literal|"()"
argument_list|,
literal|"empty inner expression"
argument_list|)
expr_stmt|;
name|node
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|subplan
expr_stmt|;
name|node
operator|->
name|type
operator|=
name|T_EXPR
expr_stmt|;
name|node
operator|->
name|eval
operator|=
name|f_expr
expr_stmt|;
break|break;
block|}
else|else
block|{
if|if
condition|(
name|subplan
operator|==
name|NULL
condition|)
name|tail
operator|=
name|subplan
operator|=
name|next
expr_stmt|;
else|else
block|{
name|tail
operator|->
name|next
operator|=
name|next
expr_stmt|;
name|tail
operator|=
name|next
expr_stmt|;
block|}
name|tail
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
block|}
block|}
return|return
operator|(
name|node
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * find_squish_paren --  *	replaces "parentheisized" plans in our search plan with "expr" nodes.  */
end_comment

begin_function
name|PLAN
modifier|*
name|find_squish_paren
parameter_list|(
name|plan
parameter_list|)
name|PLAN
modifier|*
name|plan
decl_stmt|;
comment|/* plan with ( ) nodes */
block|{
specifier|register
name|PLAN
modifier|*
name|expr
decl_stmt|;
comment|/* pointer to next expression */
specifier|register
name|PLAN
modifier|*
name|tail
decl_stmt|;
comment|/* pointer to tail of result plan */
name|PLAN
modifier|*
name|result
decl_stmt|;
comment|/* pointer to head of result plan */
name|result
operator|=
name|tail
operator|=
name|NULL
expr_stmt|;
comment|/* 	 * the basic idea is to have find_yankexpr do all our work and just 	 * collect it's results together. 	 */
while|while
condition|(
operator|(
name|expr
operator|=
name|find_yankexpr
argument_list|(
operator|&
name|plan
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
comment|/* 		 * if we find an unclaimed ')' it means there is a missing 		 * '(' someplace. 		 */
if|if
condition|(
name|expr
operator|->
name|type
operator|==
name|T_CLOSEPAREN
condition|)
name|bad_arg
argument_list|(
literal|")"
argument_list|,
literal|"no beginning '('"
argument_list|)
expr_stmt|;
comment|/* add the expression to our result plan */
if|if
condition|(
name|result
operator|==
name|NULL
condition|)
name|tail
operator|=
name|result
operator|=
name|expr
expr_stmt|;
else|else
block|{
name|tail
operator|->
name|next
operator|=
name|expr
expr_stmt|;
name|tail
operator|=
name|expr
expr_stmt|;
block|}
name|tail
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
block|}
return|return
operator|(
name|result
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * find_squish_not --  *	compresses "!" expressions in our search plan.  */
end_comment

begin_function
name|PLAN
modifier|*
name|find_squish_not
parameter_list|(
name|plan
parameter_list|)
name|PLAN
modifier|*
name|plan
decl_stmt|;
comment|/* plan to process */
block|{
specifier|register
name|PLAN
modifier|*
name|next
decl_stmt|;
comment|/* next node being processed */
specifier|register
name|PLAN
modifier|*
name|node
decl_stmt|;
comment|/* temporary node used in T_NOT processing */
specifier|register
name|PLAN
modifier|*
name|tail
decl_stmt|;
comment|/* pointer to tail of result plan */
name|PLAN
modifier|*
name|result
decl_stmt|;
comment|/* pointer to head of result plan */
name|tail
operator|=
name|result
operator|=
name|next
operator|=
name|NULL
expr_stmt|;
while|while
condition|(
operator|(
name|next
operator|=
name|find_yanknode
argument_list|(
operator|&
name|plan
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
comment|/* 		 * if we encounter a ( expression ) then look for nots in 		 * the expr subplan. 		 */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_EXPR
condition|)
name|next
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|find_squish_not
argument_list|(
name|next
operator|->
name|p_data
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* 		 * if we encounter a not, then snag the next node and place 		 * it in the not's subplan.  As an optimization we compress 		 * several not's to zero or one not. 		 */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_NOT
condition|)
block|{
name|int
name|notlevel
init|=
literal|1
decl_stmt|;
name|node
operator|=
name|find_yanknode
argument_list|(
operator|&
name|plan
argument_list|)
expr_stmt|;
while|while
condition|(
name|node
operator|->
name|type
operator|==
name|T_NOT
condition|)
block|{
operator|++
name|notlevel
expr_stmt|;
name|node
operator|=
name|find_yanknode
argument_list|(
operator|&
name|plan
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|node
operator|==
name|NULL
condition|)
name|bad_arg
argument_list|(
literal|"!"
argument_list|,
literal|"no following expression"
argument_list|)
expr_stmt|;
if|if
condition|(
name|node
operator|->
name|type
operator|==
name|T_OR
condition|)
name|bad_arg
argument_list|(
literal|"!"
argument_list|,
literal|"nothing between ! and -o"
argument_list|)
expr_stmt|;
if|if
condition|(
name|notlevel
operator|%
literal|2
operator|!=
literal|1
condition|)
name|next
operator|=
name|node
expr_stmt|;
else|else
name|next
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|node
expr_stmt|;
block|}
comment|/* add the node to our result plan */
if|if
condition|(
name|result
operator|==
name|NULL
condition|)
name|tail
operator|=
name|result
operator|=
name|next
expr_stmt|;
else|else
block|{
name|tail
operator|->
name|next
operator|=
name|next
expr_stmt|;
name|tail
operator|=
name|next
expr_stmt|;
block|}
name|tail
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
block|}
return|return
operator|(
name|result
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * find_squish_or --  *	compresses -o expressions in our search plan.  */
end_comment

begin_function
name|PLAN
modifier|*
name|find_squish_or
parameter_list|(
name|plan
parameter_list|)
name|PLAN
modifier|*
name|plan
decl_stmt|;
comment|/* plan with ors to be squished */
block|{
specifier|register
name|PLAN
modifier|*
name|next
decl_stmt|;
comment|/* next node being processed */
specifier|register
name|PLAN
modifier|*
name|tail
decl_stmt|;
comment|/* pointer to tail of result plan */
name|PLAN
modifier|*
name|result
decl_stmt|;
comment|/* pointer to head of result plan */
name|tail
operator|=
name|result
operator|=
name|next
operator|=
name|NULL
expr_stmt|;
while|while
condition|(
operator|(
name|next
operator|=
name|find_yanknode
argument_list|(
operator|&
name|plan
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
comment|/* 		 * if we encounter a ( expression ) then look for or's in 		 * the expr subplan. 		 */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_EXPR
condition|)
name|next
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|find_squish_or
argument_list|(
name|next
operator|->
name|p_data
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* if we encounter a not then look for not's in the subplan */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_NOT
condition|)
name|next
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|find_squish_or
argument_list|(
name|next
operator|->
name|p_data
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
comment|/* 		 * if we encounter an or, then place our collected plan in the 		 * or's first subplan and then recursively collect the 		 * remaining stuff into the second subplan and return the or. 		 */
if|if
condition|(
name|next
operator|->
name|type
operator|==
name|T_OR
condition|)
block|{
if|if
condition|(
name|result
operator|==
name|NULL
condition|)
name|bad_arg
argument_list|(
literal|"-o"
argument_list|,
literal|"no expression before -o"
argument_list|)
expr_stmt|;
name|next
operator|->
name|p_data
index|[
literal|0
index|]
operator|=
name|result
expr_stmt|;
name|next
operator|->
name|p_data
index|[
literal|1
index|]
operator|=
name|find_squish_or
argument_list|(
name|plan
argument_list|)
expr_stmt|;
if|if
condition|(
name|next
operator|->
name|p_data
index|[
literal|1
index|]
operator|==
name|NULL
condition|)
name|bad_arg
argument_list|(
literal|"-o"
argument_list|,
literal|"no expression after -o"
argument_list|)
expr_stmt|;
return|return
operator|(
name|next
operator|)
return|;
block|}
comment|/* add the node to our result plan */
if|if
condition|(
name|result
operator|==
name|NULL
condition|)
name|tail
operator|=
name|result
operator|=
name|next
expr_stmt|;
else|else
block|{
name|tail
operator|->
name|next
operator|=
name|next
expr_stmt|;
name|tail
operator|=
name|next
expr_stmt|;
block|}
name|tail
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
block|}
return|return
operator|(
name|result
operator|)
return|;
block|}
end_function

end_unit

