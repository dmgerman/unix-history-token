begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1980 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)fstat_.c	5.1	6/7/85  */
end_comment

begin_comment
comment|/*  * get file status  *  * calling sequence:  *	integer fstat, statb(12)  *	call fstat (name, statb)  * where:  *	'statb' will receive the stat structure for file 'name'.  */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|"../libI77/f_errno.h"
end_include

begin_include
include|#
directive|include
file|"../libI77/fiodefs.h"
end_include

begin_decl_stmt
specifier|extern
name|unit
name|units
index|[]
decl_stmt|;
end_decl_stmt

begin_function
name|long
name|fstat_
parameter_list|(
name|lunit
parameter_list|,
name|stbuf
parameter_list|)
name|long
modifier|*
name|lunit
decl_stmt|,
decl|*
name|stbuf
decl_stmt|;
end_function

begin_block
block|{
name|struct
name|stat
name|statb
decl_stmt|;
if|if
condition|(
operator|*
name|lunit
operator|<
literal|0
operator|||
operator|*
name|lunit
operator|>=
name|MXUNIT
condition|)
return|return
operator|(
call|(
name|long
call|)
argument_list|(
name|errno
operator|=
name|F_ERUNIT
argument_list|)
operator|)
return|;
if|if
condition|(
operator|!
name|units
index|[
operator|*
name|lunit
index|]
operator|.
name|ufd
condition|)
return|return
operator|(
call|(
name|long
call|)
argument_list|(
name|errno
operator|=
name|F_ERNOPEN
argument_list|)
operator|)
return|;
if|if
condition|(
name|fstat
argument_list|(
name|fileno
argument_list|(
name|units
index|[
operator|*
name|lunit
index|]
operator|.
name|ufd
argument_list|)
argument_list|,
operator|&
name|statb
argument_list|)
operator|==
literal|0
condition|)
block|{
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_dev
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_ino
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_mode
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_nlink
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_uid
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_gid
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_rdev
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_size
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_atime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_mtime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_ctime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_blksize
expr_stmt|;
return|return
operator|(
literal|0L
operator|)
return|;
block|}
return|return
operator|(
operator|(
name|long
operator|)
name|errno
operator|)
return|;
block|}
end_block

end_unit

