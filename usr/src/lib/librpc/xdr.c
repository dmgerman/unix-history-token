begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Sun RPC is a product of Sun Microsystems, Inc. and is provided for  * unrestricted use provided that this legend is included on all tape  * media and as a part of the software program in whole or part.  Users  * may copy or modify Sun RPC without charge, but are not authorized  * to license or distribute it to anyone else except as part of a product or  * program developed by the user.  *   * SUN RPC IS PROVIDED AS IS WITH NO WARRANTIES OF ANY KIND INCLUDING THE  * WARRANTIES OF DESIGN, MERCHANTIBILITY AND FITNESS FOR A PARTICULAR  * PURPOSE, OR ARISING FROM A COURSE OF DEALING, USAGE OR TRADE PRACTICE.  *   * Sun RPC is provided with no support and without any obligation on the  * part of Sun Microsystems, Inc. to assist in its use, correction,  * modification or enhancement.  *   * SUN MICROSYSTEMS, INC. SHALL HAVE NO LIABILITY WITH RESPECT TO THE  * INFRINGEMENT OF COPYRIGHTS, TRADE SECRETS OR ANY PATENTS BY SUN RPC  * OR ANY PART THEREOF.  *   * In no event will Sun Microsystems, Inc. be liable for any lost revenue  * or profits or other special, indirect and consequential damages, even if  * Sun has been advised of the possibility of such damages.  *   * Sun Microsystems, Inc.  * 2550 Garcia Avenue  * Mountain View, California  94043  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)xdr.c 1.3 85/02/26 Copyr 1984 Sun Micro"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * xdr.c, Generic XDR routines impelmentation.  *  * Copyright (C) 1984, Sun Microsystems, Inc.  *  * These are the "generic" xdr routines used to serialize and de-serialize  * most common data items.  See xdr.h for more info on the interface to  * xdr.  */
end_comment

begin_include
include|#
directive|include
file|"types.h"
end_include

begin_include
include|#
directive|include
file|"xdr.h"
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_function_decl
name|char
modifier|*
name|mem_alloc
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * constants specific to the xdr "protocol"  */
end_comment

begin_define
define|#
directive|define
name|XDR_FALSE
value|((long) 0)
end_define

begin_define
define|#
directive|define
name|XDR_TRUE
value|((long) 1)
end_define

begin_define
define|#
directive|define
name|LASTUNSIGNED
value|((u_int) 0-1)
end_define

begin_comment
comment|/*  * for unit alignment  */
end_comment

begin_decl_stmt
specifier|static
name|char
name|xdr_zero
index|[
name|BYTES_PER_XDR_UNIT
index|]
init|=
block|{
literal|0
block|,
literal|0
block|,
literal|0
block|,
literal|0
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * XDR nothing  */
end_comment

begin_function
name|bool_t
name|xdr_void
parameter_list|(
comment|/* xdrs, addr */
parameter_list|)
comment|/* XDR *xdrs; */
comment|/* caddr_t addr; */
block|{
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR integers  */
end_comment

begin_function
name|bool_t
name|xdr_int
parameter_list|(
name|xdrs
parameter_list|,
name|ip
parameter_list|)
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|int
modifier|*
name|ip
decl_stmt|;
block|{
if|if
condition|(
sizeof|sizeof
argument_list|(
name|int
argument_list|)
operator|==
sizeof|sizeof
argument_list|(
name|long
argument_list|)
condition|)
block|{
return|return
operator|(
name|xdr_long
argument_list|(
name|xdrs
argument_list|,
operator|(
name|long
operator|*
operator|)
name|ip
argument_list|)
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|xdr_short
argument_list|(
name|xdrs
argument_list|,
operator|(
name|short
operator|*
operator|)
name|ip
argument_list|)
operator|)
return|;
block|}
block|}
end_function

begin_comment
comment|/*  * XDR unsigned integers  */
end_comment

begin_function
name|bool_t
name|xdr_u_int
parameter_list|(
name|xdrs
parameter_list|,
name|up
parameter_list|)
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|u_int
modifier|*
name|up
decl_stmt|;
block|{
if|if
condition|(
sizeof|sizeof
argument_list|(
name|u_int
argument_list|)
operator|==
sizeof|sizeof
argument_list|(
name|u_long
argument_list|)
condition|)
block|{
return|return
operator|(
name|xdr_u_long
argument_list|(
name|xdrs
argument_list|,
operator|(
name|u_long
operator|*
operator|)
name|up
argument_list|)
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|xdr_short
argument_list|(
name|xdrs
argument_list|,
operator|(
name|short
operator|*
operator|)
name|up
argument_list|)
operator|)
return|;
block|}
block|}
end_function

begin_comment
comment|/*  * XDR long integers  * same as xdr_u_long - open coded to save a proc call!  */
end_comment

begin_function
name|bool_t
name|xdr_long
parameter_list|(
name|xdrs
parameter_list|,
name|lp
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|long
modifier|*
name|lp
decl_stmt|;
block|{
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_ENCODE
condition|)
return|return
operator|(
name|XDR_PUTLONG
argument_list|(
name|xdrs
argument_list|,
name|lp
argument_list|)
operator|)
return|;
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_DECODE
condition|)
return|return
operator|(
name|XDR_GETLONG
argument_list|(
name|xdrs
argument_list|,
name|lp
argument_list|)
operator|)
return|;
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_FREE
condition|)
return|return
operator|(
name|TRUE
operator|)
return|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR unsigned long integers  * same as xdr_long - open coded to save a proc call!  */
end_comment

begin_function
name|bool_t
name|xdr_u_long
parameter_list|(
name|xdrs
parameter_list|,
name|ulp
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|u_long
modifier|*
name|ulp
decl_stmt|;
block|{
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_DECODE
condition|)
return|return
operator|(
name|XDR_GETLONG
argument_list|(
name|xdrs
argument_list|,
operator|(
name|long
operator|*
operator|)
name|ulp
argument_list|)
operator|)
return|;
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_ENCODE
condition|)
return|return
operator|(
name|XDR_PUTLONG
argument_list|(
name|xdrs
argument_list|,
operator|(
name|long
operator|*
operator|)
name|ulp
argument_list|)
operator|)
return|;
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_FREE
condition|)
return|return
operator|(
name|TRUE
operator|)
return|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR short integers  */
end_comment

begin_function
name|bool_t
name|xdr_short
parameter_list|(
name|xdrs
parameter_list|,
name|sp
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|short
modifier|*
name|sp
decl_stmt|;
block|{
name|long
name|l
decl_stmt|;
switch|switch
condition|(
name|xdrs
operator|->
name|x_op
condition|)
block|{
case|case
name|XDR_ENCODE
case|:
name|l
operator|=
operator|(
name|long
operator|)
operator|*
name|sp
expr_stmt|;
return|return
operator|(
name|XDR_PUTLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|l
argument_list|)
operator|)
return|;
case|case
name|XDR_DECODE
case|:
if|if
condition|(
operator|!
name|XDR_GETLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|l
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
operator|*
name|sp
operator|=
operator|(
name|short
operator|)
name|l
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
case|case
name|XDR_FREE
case|:
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR unsigned short integers  */
end_comment

begin_function
name|bool_t
name|xdr_u_short
parameter_list|(
name|xdrs
parameter_list|,
name|usp
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|u_short
modifier|*
name|usp
decl_stmt|;
block|{
name|u_long
name|l
decl_stmt|;
switch|switch
condition|(
name|xdrs
operator|->
name|x_op
condition|)
block|{
case|case
name|XDR_ENCODE
case|:
name|l
operator|=
operator|(
name|u_long
operator|)
operator|*
name|usp
expr_stmt|;
return|return
operator|(
name|XDR_PUTLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|l
argument_list|)
operator|)
return|;
case|case
name|XDR_DECODE
case|:
if|if
condition|(
operator|!
name|XDR_GETLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|l
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
operator|*
name|usp
operator|=
operator|(
name|u_short
operator|)
name|l
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
case|case
name|XDR_FREE
case|:
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR booleans  */
end_comment

begin_function
name|bool_t
name|xdr_bool
parameter_list|(
name|xdrs
parameter_list|,
name|bp
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|bool_t
modifier|*
name|bp
decl_stmt|;
block|{
name|long
name|lb
decl_stmt|;
switch|switch
condition|(
name|xdrs
operator|->
name|x_op
condition|)
block|{
case|case
name|XDR_ENCODE
case|:
name|lb
operator|=
operator|*
name|bp
condition|?
name|XDR_TRUE
else|:
name|XDR_FALSE
expr_stmt|;
return|return
operator|(
name|XDR_PUTLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|lb
argument_list|)
operator|)
return|;
case|case
name|XDR_DECODE
case|:
if|if
condition|(
operator|!
name|XDR_GETLONG
argument_list|(
name|xdrs
argument_list|,
operator|&
name|lb
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
operator|*
name|bp
operator|=
operator|(
name|lb
operator|==
name|XDR_FALSE
operator|)
condition|?
name|FALSE
else|:
name|TRUE
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
case|case
name|XDR_FREE
case|:
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR enumerations  */
end_comment

begin_function
name|bool_t
name|xdr_enum
parameter_list|(
name|xdrs
parameter_list|,
name|ep
parameter_list|)
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|enum_t
modifier|*
name|ep
decl_stmt|;
block|{
comment|/* 	 * enums are treated as ints 	 */
if|if
condition|(
sizeof|sizeof
argument_list|(
name|enum_t
argument_list|)
operator|==
sizeof|sizeof
argument_list|(
name|long
argument_list|)
condition|)
block|{
return|return
operator|(
name|xdr_long
argument_list|(
name|xdrs
argument_list|,
operator|(
name|long
operator|*
operator|)
name|ep
argument_list|)
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|xdr_short
argument_list|(
name|xdrs
argument_list|,
operator|(
name|short
operator|*
operator|)
name|ep
argument_list|)
operator|)
return|;
block|}
block|}
end_function

begin_comment
comment|/*  * XDR opaque data  * Allows the specification of a fixed size sequence of opaque bytes.  * cp points to the opaque object and cnt gives the byte length.  */
end_comment

begin_function
name|bool_t
name|xdr_opaque
parameter_list|(
name|xdrs
parameter_list|,
name|cp
parameter_list|,
name|cnt
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|caddr_t
name|cp
decl_stmt|;
specifier|register
name|u_int
name|cnt
decl_stmt|;
block|{
specifier|register
name|u_int
name|rndup
decl_stmt|;
specifier|static
name|crud
index|[
name|BYTES_PER_XDR_UNIT
index|]
expr_stmt|;
comment|/* 	 * if no data we are done 	 */
if|if
condition|(
name|cnt
operator|==
literal|0
condition|)
return|return
operator|(
name|TRUE
operator|)
return|;
comment|/* 	 * round byte count to full xdr units 	 */
name|rndup
operator|=
name|cnt
operator|%
name|BYTES_PER_XDR_UNIT
expr_stmt|;
if|if
condition|(
name|rndup
operator|>
literal|0
condition|)
name|rndup
operator|=
name|BYTES_PER_XDR_UNIT
operator|-
name|rndup
expr_stmt|;
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_DECODE
condition|)
block|{
if|if
condition|(
operator|!
name|XDR_GETBYTES
argument_list|(
name|xdrs
argument_list|,
name|cp
argument_list|,
name|cnt
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
if|if
condition|(
name|rndup
operator|==
literal|0
condition|)
return|return
operator|(
name|TRUE
operator|)
return|;
return|return
operator|(
name|XDR_GETBYTES
argument_list|(
name|xdrs
argument_list|,
name|crud
argument_list|,
name|rndup
argument_list|)
operator|)
return|;
block|}
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_ENCODE
condition|)
block|{
if|if
condition|(
operator|!
name|XDR_PUTBYTES
argument_list|(
name|xdrs
argument_list|,
name|cp
argument_list|,
name|cnt
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
if|if
condition|(
name|rndup
operator|==
literal|0
condition|)
return|return
operator|(
name|TRUE
operator|)
return|;
return|return
operator|(
name|XDR_PUTBYTES
argument_list|(
name|xdrs
argument_list|,
name|xdr_zero
argument_list|,
name|rndup
argument_list|)
operator|)
return|;
block|}
if|if
condition|(
name|xdrs
operator|->
name|x_op
operator|==
name|XDR_FREE
condition|)
block|{
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR counted bytes  * *cpp is a pointer to the bytes, *sizep is the count.  * If *cpp is NULL maxsize bytes are allocated  */
end_comment

begin_function
name|bool_t
name|xdr_bytes
parameter_list|(
name|xdrs
parameter_list|,
name|cpp
parameter_list|,
name|sizep
parameter_list|,
name|maxsize
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|char
modifier|*
modifier|*
name|cpp
decl_stmt|;
specifier|register
name|u_int
modifier|*
name|sizep
decl_stmt|;
name|u_int
name|maxsize
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|sp
init|=
operator|*
name|cpp
decl_stmt|;
comment|/* sp is the actual string pointer */
specifier|register
name|u_int
name|nodesize
decl_stmt|;
comment|/* 	 * first deal with the length since xdr bytes are counted 	 */
if|if
condition|(
operator|!
name|xdr_u_int
argument_list|(
name|xdrs
argument_list|,
name|sizep
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
name|nodesize
operator|=
operator|*
name|sizep
expr_stmt|;
if|if
condition|(
operator|(
name|nodesize
operator|>
name|maxsize
operator|)
operator|&&
operator|(
name|xdrs
operator|->
name|x_op
operator|!=
name|XDR_FREE
operator|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
comment|/* 	 * now deal with the actual bytes 	 */
switch|switch
condition|(
name|xdrs
operator|->
name|x_op
condition|)
block|{
case|case
name|XDR_DECODE
case|:
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
block|{
operator|*
name|cpp
operator|=
name|sp
operator|=
name|mem_alloc
argument_list|(
name|nodesize
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"xdr_bytes: out of memory\n"
argument_list|)
expr_stmt|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
comment|/* fall into ... */
case|case
name|XDR_ENCODE
case|:
return|return
operator|(
name|xdr_opaque
argument_list|(
name|xdrs
argument_list|,
name|sp
argument_list|,
name|nodesize
argument_list|)
operator|)
return|;
case|case
name|XDR_FREE
case|:
if|if
condition|(
name|sp
operator|!=
name|NULL
condition|)
block|{
name|mem_free
argument_list|(
name|sp
argument_list|,
name|nodesize
argument_list|)
expr_stmt|;
operator|*
name|cpp
operator|=
name|NULL
expr_stmt|;
block|}
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * XDR a descriminated union  * Support routine for discriminated unions.  * You create an array of xdrdiscrim structures, terminated with  * an entry with a null procedure pointer.  The routine gets  * the discriminant value and then searches the array of xdrdiscrims  * looking for that value.  It calls the procedure given in the xdrdiscrim  * to handle the discriminant.  If there is no specific routine a default  * routine may be called.  * If there is no specific or default routine an error is returned.  */
end_comment

begin_function
name|bool_t
name|xdr_union
parameter_list|(
name|xdrs
parameter_list|,
name|dscmp
parameter_list|,
name|unp
parameter_list|,
name|choices
parameter_list|,
name|dfault
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|enum_t
modifier|*
name|dscmp
decl_stmt|;
comment|/* enum to decide which arm to work on */
name|caddr_t
name|unp
decl_stmt|;
comment|/* the union itself */
name|struct
name|xdr_discrim
modifier|*
name|choices
decl_stmt|;
comment|/* [value, xdr proc] for each arm */
name|xdrproc_t
name|dfault
decl_stmt|;
comment|/* default xdr routine */
block|{
specifier|register
name|enum_t
name|dscm
decl_stmt|;
comment|/* 	 * we deal with the discriminator;  it's an enum 	 */
if|if
condition|(
operator|!
name|xdr_enum
argument_list|(
name|xdrs
argument_list|,
name|dscmp
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
name|dscm
operator|=
operator|*
name|dscmp
expr_stmt|;
comment|/* 	 * search choices for a value that matches the discriminator. 	 * if we find one, execute the xdr routine for that value. 	 */
for|for
control|(
init|;
name|choices
operator|->
name|proc
operator|!=
name|NULL_xdrproc_t
condition|;
name|choices
operator|++
control|)
block|{
if|if
condition|(
name|choices
operator|->
name|value
operator|==
name|dscm
condition|)
return|return
operator|(
operator|(
operator|*
operator|(
name|choices
operator|->
name|proc
operator|)
operator|)
operator|(
name|xdrs
operator|,
name|unp
operator|,
name|LASTUNSIGNED
operator|)
operator|)
return|;
block|}
comment|/* 	 * no match - execute the default xdr routine if there is one 	 */
return|return
operator|(
operator|(
name|dfault
operator|==
name|NULL_xdrproc_t
operator|)
condition|?
name|FALSE
else|:
call|(
modifier|*
name|dfault
call|)
argument_list|(
name|xdrs
argument_list|,
name|unp
argument_list|,
name|LASTUNSIGNED
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Non-portable xdr primitives.  * Care should be taken when moving these routines to new architectures.  */
end_comment

begin_comment
comment|/*  * XDR null terminated ASCII strings  * xdr_string deals with "C strings" - arrays of bytes that are  * terminated by a NULL character.  The parameter cpp references a  * pointer to storage; If the pointer is null, then the necessary  * storage is allocated.  The last parameter is the max allowed length  * of the string as specified by a protocol.  */
end_comment

begin_function
name|bool_t
name|xdr_string
parameter_list|(
name|xdrs
parameter_list|,
name|cpp
parameter_list|,
name|maxsize
parameter_list|)
specifier|register
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|char
modifier|*
modifier|*
name|cpp
decl_stmt|;
name|u_int
name|maxsize
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|sp
init|=
operator|*
name|cpp
decl_stmt|;
comment|/* sp is the actual string pointer */
name|u_int
name|size
decl_stmt|;
name|u_int
name|nodesize
decl_stmt|;
comment|/* 	 * first deal with the length since xdr strings are counted-strings 	 */
if|if
condition|(
operator|(
name|xdrs
operator|->
name|x_op
operator|)
operator|!=
name|XDR_DECODE
condition|)
name|size
operator|=
name|strlen
argument_list|(
name|sp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|xdr_u_int
argument_list|(
name|xdrs
argument_list|,
operator|&
name|size
argument_list|)
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
if|if
condition|(
name|size
operator|>
name|maxsize
condition|)
block|{
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
name|nodesize
operator|=
name|size
operator|+
literal|1
expr_stmt|;
comment|/* 	 * now deal with the actual bytes 	 */
switch|switch
condition|(
name|xdrs
operator|->
name|x_op
condition|)
block|{
case|case
name|XDR_DECODE
case|:
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
operator|*
name|cpp
operator|=
name|sp
operator|=
name|mem_alloc
argument_list|(
name|nodesize
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"xdr_string: out of memory\n"
argument_list|)
expr_stmt|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
name|sp
index|[
name|size
index|]
operator|=
literal|0
expr_stmt|;
comment|/* fall into ... */
case|case
name|XDR_ENCODE
case|:
return|return
operator|(
name|xdr_opaque
argument_list|(
name|xdrs
argument_list|,
name|sp
argument_list|,
name|size
argument_list|)
operator|)
return|;
case|case
name|XDR_FREE
case|:
if|if
condition|(
name|sp
operator|!=
name|NULL
condition|)
block|{
name|mem_free
argument_list|(
name|sp
argument_list|,
name|nodesize
argument_list|)
expr_stmt|;
operator|*
name|cpp
operator|=
name|NULL
expr_stmt|;
block|}
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/*   * Wrapper for xdr_string that can be called directly from   * routines like clnt_call  */
end_comment

begin_function
name|bool_t
name|xdr_wrapstring
parameter_list|(
name|xdrs
parameter_list|,
name|cpp
parameter_list|)
name|XDR
modifier|*
name|xdrs
decl_stmt|;
name|char
modifier|*
modifier|*
name|cpp
decl_stmt|;
block|{
if|if
condition|(
name|xdr_string
argument_list|(
name|xdrs
argument_list|,
name|cpp
argument_list|,
name|BUFSIZ
argument_list|)
condition|)
block|{
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

end_unit

