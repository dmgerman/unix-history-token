begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)res_init.c	4.1 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<nameser.h>
end_include

begin_include
include|#
directive|include
file|<resolv.h>
end_include

begin_comment
comment|/*  * Resolver state default settings  */
end_comment

begin_decl_stmt
name|struct
name|state
name|_res
init|=
block|{
literal|90
block|,
literal|2
block|,
name|RES_RECURSE
operator||
name|RES_DEFNAMES
block|, }
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Read the configuration file for default settings.  * Return true if the name server address is initialized.  */
end_comment

begin_macro
name|res_init
argument_list|()
end_macro

begin_block
block|{
name|FILE
modifier|*
name|fp
decl_stmt|;
name|char
name|buf
index|[
name|BUFSIZ
index|]
decl_stmt|,
modifier|*
name|cp
decl_stmt|;
name|int
name|n
decl_stmt|;
specifier|extern
name|u_long
name|inet_addr
parameter_list|()
function_decl|;
specifier|extern
name|char
modifier|*
name|index
parameter_list|()
function_decl|;
name|_res
operator|.
name|options
operator||=
name|RES_INIT
expr_stmt|;
name|_res
operator|.
name|nsaddr
operator|.
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|_res
operator|.
name|nsaddr
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|INADDR_ANY
expr_stmt|;
name|_res
operator|.
name|nsaddr
operator|.
name|sin_port
operator|=
name|HTONS
argument_list|(
literal|53
argument_list|)
expr_stmt|;
comment|/* well known port number */
comment|/* first try reading the config file */
if|if
condition|(
operator|(
name|fp
operator|=
name|fopen
argument_list|(
name|CONFFILE
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|fgets
argument_list|(
name|_res
operator|.
name|defdname
argument_list|,
name|MAXDNAME
argument_list|,
name|fp
argument_list|)
operator|==
name|NULL
condition|)
name|_res
operator|.
name|defdname
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
elseif|else
if|if
condition|(
operator|(
name|cp
operator|=
name|index
argument_list|(
name|_res
operator|.
name|defdname
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|fgets
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|fp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
name|_res
operator|.
name|nsaddr
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|inet_addr
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
block|}
comment|/* next, try getting the address of this host */
comment|/* finally, try broadcast */
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

