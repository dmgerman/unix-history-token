begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	raw_usrreq.c	4.6	82/02/01	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/mtpr.h"
end_include

begin_include
include|#
directive|include
file|"../net/in.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/if.h"
end_include

begin_include
include|#
directive|include
file|"../net/raw_cb.h"
end_include

begin_include
include|#
directive|include
file|"/usr/include/errno.h"
end_include

begin_comment
comment|/*  * Initialize raw connection block q. */
end_comment

begin_macro
name|raw_init
argument_list|()
end_macro

begin_block
block|{
name|COUNT
argument_list|(
name|RAW_INIT
argument_list|)
expr_stmt|;
name|rawcb
operator|.
name|rcb_next
operator|=
name|rawcb
operator|.
name|rcb_prev
operator|=
operator|&
name|rawcb
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Raw protocol interface.  */
end_comment

begin_macro
name|raw_input
argument_list|(
argument|m0
argument_list|,
argument|pf
argument_list|,
argument|daf
argument_list|,
argument|saf
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockproto
name|pf
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr
name|daf
decl_stmt|,
name|saf
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|struct
name|raw_header
modifier|*
name|rh
decl_stmt|;
name|int
name|s
decl_stmt|;
comment|/* 	 * Rip off an mbuf for a generic header. 	 */
name|m
operator|=
name|m_get
argument_list|(
name|M_DONTWAIT
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
block|{
name|m_freem
argument_list|(
name|m0
argument_list|)
expr_stmt|;
return|return;
block|}
name|m
operator|->
name|m_next
operator|=
name|m0
expr_stmt|;
name|m
operator|->
name|m_off
operator|=
name|MMINOFF
expr_stmt|;
name|m
operator|->
name|m_len
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|raw_header
argument_list|)
expr_stmt|;
name|rh
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|raw_header
operator|*
argument_list|)
expr_stmt|;
name|rh
operator|->
name|raw_dst
operator|=
name|daf
expr_stmt|;
name|rh
operator|->
name|raw_src
operator|=
name|saf
expr_stmt|;
name|rh
operator|->
name|raw_protocol
operator|=
name|pf
expr_stmt|;
comment|/* 	 * Header now contains enough info to decide 	 * which socket to place packet in (if any). 	 * Queue it up for the raw protocol process 	 * running at software interrupt level. 	 */
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
name|IF_ENQUEUE
argument_list|(
operator|&
name|rawintrq
argument_list|,
name|m
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|setrawintr
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Raw protocol input routine.  Process packets entered  * into the queue at interrupt time.  Find the socket  * associated with the packet(s) and move them over.  If  * nothing exists for this packet, drop it.  */
end_comment

begin_macro
name|rawintr
argument_list|()
end_macro

begin_block
block|{
name|int
name|s
decl_stmt|;
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
specifier|register
name|struct
name|rawcb
modifier|*
name|rp
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
specifier|register
name|struct
name|protosw
modifier|*
name|pr
decl_stmt|;
specifier|register
name|struct
name|sockproto
modifier|*
name|sp
decl_stmt|;
specifier|register
name|struct
name|sockaddr
modifier|*
name|sa
decl_stmt|;
name|struct
name|raw_header
modifier|*
name|rawp
decl_stmt|;
name|struct
name|socket
modifier|*
name|last
decl_stmt|;
name|COUNT
argument_list|(
name|RAWINTR
argument_list|)
expr_stmt|;
name|next
label|:
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
name|IF_DEQUEUE
argument_list|(
operator|&
name|rawintrq
argument_list|,
name|m
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
return|return;
name|rawp
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|raw_header
operator|*
argument_list|)
expr_stmt|;
name|sp
operator|=
operator|&
name|rawp
operator|->
name|raw_protocol
expr_stmt|;
name|sa
operator|=
operator|&
name|rawp
operator|->
name|raw_dst
expr_stmt|;
comment|/* 	 * Find the appropriate socket(s) in which to place this 	 * packet.  This is done by matching the protocol and 	 * address information prepended by raw_input against 	 * the info stored in the control block structures. 	 */
name|last
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|rp
operator|=
name|rawcb
operator|.
name|rcb_next
init|;
name|rp
operator|!=
operator|&
name|rawcb
condition|;
name|rp
operator|=
name|rp
operator|->
name|rcb_next
control|)
block|{
name|so
operator|=
name|rp
operator|->
name|rcb_socket
expr_stmt|;
name|pr
operator|=
name|so
operator|->
name|so_proto
expr_stmt|;
if|if
condition|(
name|pr
operator|->
name|pr_family
operator|!=
name|sp
operator|->
name|sp_family
operator|||
name|pr
operator|->
name|pr_protocol
operator|!=
name|sp
operator|->
name|sp_protocol
condition|)
continue|continue;
if|if
condition|(
name|sa
operator|->
name|sa_family
operator|!=
name|so
operator|->
name|so_addr
operator|.
name|sa_family
condition|)
continue|continue;
comment|/* 		 * We assume the lower level routines have 		 * placed the address in a canonical format 		 * suitable for a structure comparison. Packets 		 * are duplicated for each receiving socket. 		 */
if|if
condition|(
operator|(
name|rp
operator|->
name|rcb_flags
operator|&
name|RAW_ADDR
operator|)
operator|&&
name|bcmp
argument_list|(
name|sa
operator|->
name|sa_data
argument_list|,
name|so
operator|->
name|so_addr
operator|.
name|sa_data
argument_list|,
literal|14
argument_list|)
operator|!=
literal|0
condition|)
continue|continue;
comment|/* 		 * To avoid extraneous packet copies, we keep 		 * track of the last socket the packet should be 		 * placed in, and make copies only after finding a 		 * socket which "collides". 		 */
if|if
condition|(
name|last
condition|)
block|{
name|struct
name|mbuf
modifier|*
name|n
decl_stmt|;
if|if
condition|(
name|n
operator|=
name|m_copy
argument_list|(
name|m
operator|->
name|m_next
argument_list|,
literal|0
argument_list|,
name|M_COPYALL
argument_list|)
condition|)
goto|goto
name|nospace
goto|;
if|if
condition|(
name|sbappendaddr
argument_list|(
operator|&
name|last
operator|->
name|so_rcv
argument_list|,
operator|&
name|rawp
operator|->
name|raw_src
argument_list|,
name|n
argument_list|)
operator|==
literal|0
condition|)
block|{
comment|/* 				 * Should drop notification of lost packet 				 * into this guy's queue, but... 				 */
name|m_freem
argument_list|(
name|n
argument_list|)
expr_stmt|;
goto|goto
name|nospace
goto|;
block|}
name|sorwakeup
argument_list|(
name|last
argument_list|)
expr_stmt|;
block|}
name|nospace
label|:
name|last
operator|=
name|so
expr_stmt|;
block|}
if|if
condition|(
name|last
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
if|if
condition|(
name|sbappendaddr
argument_list|(
operator|&
name|last
operator|->
name|so_rcv
argument_list|,
operator|&
name|rawp
operator|->
name|raw_src
argument_list|,
name|m
operator|->
name|m_next
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
operator|(
name|void
operator|)
name|m_free
argument_list|(
name|m
argument_list|)
expr_stmt|;
comment|/* generic header */
name|sorwakeup
argument_list|(
name|last
argument_list|)
expr_stmt|;
goto|goto
name|next
goto|;
name|drop
label|:
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
goto|goto
name|next
goto|;
block|}
end_block

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|raw_usrreq
argument_list|(
argument|so
argument_list|,
argument|req
argument_list|,
argument|m
argument_list|,
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|req
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|rawcb
modifier|*
name|rp
init|=
name|sotorawcb
argument_list|(
name|so
argument_list|)
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
name|COUNT
argument_list|(
name|RAW_USRREQ
argument_list|)
expr_stmt|;
if|if
condition|(
name|rp
operator|==
literal|0
operator|&&
name|req
operator|!=
name|PRU_ATTACH
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
switch|switch
condition|(
name|req
condition|)
block|{
comment|/* 	 * Allocate a raw control block and fill in the 	 * necessary info to allow packets to be routed to 	 * the appropriate raw interface routine. 	 */
case|case
name|PRU_ATTACH
case|:
if|if
condition|(
name|rp
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
empty_stmt|;
name|error
operator|=
name|raw_attach
argument_list|(
name|so
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Destroy state just before socket deallocation. 	 * Flush data or not depending on the options. 	 */
case|case
name|PRU_DETACH
case|:
if|if
condition|(
name|rp
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOTCONN
operator|)
return|;
name|raw_detach
argument_list|(
name|rp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * If a socket isn't bound to a single address, 	 * the raw input routine will hand it anything 	 * within that protocol family (assuming there's 	 * nothing else around it should go to).  	 */
case|case
name|PRU_CONNECT
case|:
if|if
condition|(
name|rp
operator|->
name|rcb_flags
operator|&
name|RAW_ADDR
condition|)
return|return
operator|(
name|EISCONN
operator|)
return|;
name|raw_connaddr
argument_list|(
name|rp
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
name|soisconnected
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
case|case
name|PRU_DISCONNECT
case|:
if|if
condition|(
operator|(
name|rp
operator|->
name|rcb_flags
operator|&
name|RAW_ADDR
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOTCONN
operator|)
return|;
name|raw_disconnect
argument_list|(
name|rp
argument_list|)
expr_stmt|;
name|soisdisconnected
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Mark the connection as being incapable of further input. 	 */
case|case
name|PRU_SHUTDOWN
case|:
name|socantsendmore
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Ship a packet out.  The appropriate raw output 	 * routine handles any massaging necessary. 	 */
case|case
name|PRU_SEND
case|:
if|if
condition|(
name|addr
condition|)
block|{
if|if
condition|(
name|rp
operator|->
name|rcb_flags
operator|&
name|RAW_ADDR
condition|)
return|return
operator|(
name|EISCONN
operator|)
return|;
name|raw_connaddr
argument_list|(
name|rp
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|(
name|rp
operator|->
name|rcb_flags
operator|&
name|RAW_ADDR
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOTCONN
operator|)
return|;
call|(
name|void
call|)
argument_list|(
operator|*
name|so
operator|->
name|so_proto
operator|->
name|pr_output
argument_list|)
argument_list|(
name|m
argument_list|,
name|so
argument_list|)
expr_stmt|;
if|if
condition|(
name|addr
condition|)
name|rp
operator|->
name|rcb_flags
operator|&=
operator|~
name|RAW_ADDR
expr_stmt|;
break|break;
case|case
name|PRU_ABORT
case|:
name|raw_disconnect
argument_list|(
name|rp
argument_list|)
expr_stmt|;
name|sofree
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|soisdisconnected
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Not supported. 	 */
case|case
name|PRU_ACCEPT
case|:
case|case
name|PRU_RCVD
case|:
case|case
name|PRU_CONTROL
case|:
case|case
name|PRU_SENSE
case|:
case|case
name|PRU_RCVOOB
case|:
case|case
name|PRU_SENDOOB
case|:
name|error
operator|=
name|EOPNOTSUPP
expr_stmt|;
break|break;
default|default:
name|panic
argument_list|(
literal|"raw_usrreq"
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

end_unit

