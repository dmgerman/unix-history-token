begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* tcp_usrreq.c 1.26 81/11/08 */
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/imp.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp.h"
end_include

begin_define
define|#
directive|define
name|TCPFSTAB
end_define

begin_ifdef
ifdef|#
directive|ifdef
name|TCPDEBUG
end_ifdef

begin_define
define|#
directive|define
name|TCPSTATES
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"../net/tcp_fsm.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_var.h"
end_include

begin_include
include|#
directive|include
file|"/usr/include/errno.h"
end_include

begin_function_decl
name|struct
name|tcb
modifier|*
name|tcp_attach
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * Tcp initialization  */
end_comment

begin_macro
name|tcp_init
argument_list|()
end_macro

begin_block
block|{
name|tcp_iss
operator|=
literal|1
expr_stmt|;
comment|/* wrong */
name|tcb
operator|.
name|tcb_next
operator|=
name|tcb
operator|.
name|tcb_prev
operator|=
operator|(
expr|struct
name|tcb
operator|*
operator|)
operator|&
name|tcb
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Tcp finite state machine entries for timer and user generated  * requests.  These routines raise the ipl to that of the network  * to prevent reentry.  In particluar, this requires that the software  * clock interrupt have lower priority than the network so that  * we can enter the network from timeout routines without improperly  * nesting the interrupt stack.  */
end_comment

begin_comment
comment|/*  * Tcp protocol timeout routine called every 500 ms.  * Updates the timers in all active tcb's and  * causes finite state machine actions if timers expire.  */
end_comment

begin_macro
name|tcp_slowtimo
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
specifier|register
name|short
modifier|*
name|tmp
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_TIMEO
argument_list|)
expr_stmt|;
comment|/* 	 * Search through tcb's and update active timers. 	 */
name|tp
operator|=
name|tcb
operator|.
name|tcb_next
expr_stmt|;
for|for
control|(
init|;
name|tp
operator|!=
operator|(
expr|struct
name|tcb
operator|*
operator|)
operator|&
name|tcb
condition|;
name|tp
operator|=
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_next
control|)
block|{
name|tmp
operator|=
operator|&
name|tp
operator|->
name|t_init
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|TNTIMERS
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
operator|*
name|tmp
operator|&&
operator|--
operator|*
name|tmp
operator|==
literal|0
condition|)
name|tcp_usrreq
argument_list|(
name|tp
operator|->
name|t_socket
argument_list|,
name|PRU_SLOWTIMO
argument_list|,
literal|0
argument_list|,
name|i
argument_list|)
expr_stmt|;
name|tmp
operator|++
expr_stmt|;
block|}
name|tp
operator|->
name|t_xmt
operator|++
expr_stmt|;
block|}
name|tcp_iss
operator|+=
name|ISSINCR
operator|/
literal|2
expr_stmt|;
comment|/* increment iss */
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Cancel all timers for tcp tp.  */
end_comment

begin_macro
name|tcp_tcancel
argument_list|(
argument|tp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|short
modifier|*
name|tmp
init|=
operator|&
name|tp
operator|->
name|t_init
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|TNTIMERS
condition|;
name|i
operator|++
control|)
operator|*
name|tmp
operator|++
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Process a TCP user request for tcp tb.  If this is a send request  * then m is the mbuf chain of send data.  If this is a timer expiration  * (called from the software clock routine), then timertype tells which timer.  */
end_comment

begin_macro
name|tcp_usrreq
argument_list|(
argument|so
argument_list|,
argument|req
argument_list|,
argument|m
argument_list|,
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|req
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
init|=
operator|(
expr|struct
name|tcb
operator|*
operator|)
name|so
operator|->
name|so_pcb
decl_stmt|;
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
specifier|register
name|int
name|nstate
decl_stmt|;
ifdef|#
directive|ifdef
name|TCPDEBUG
name|struct
name|tcp_debug
name|tdb
decl_stmt|;
endif|#
directive|endif
name|int
name|error
init|=
literal|0
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_USRREQ
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
condition|)
block|{
name|nstate
operator|=
name|tp
operator|->
name|t_state
expr_stmt|;
name|tp
operator|->
name|tc_flags
operator|&=
operator|~
name|TC_NET_KEEP
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|req
operator|!=
name|PRU_ATTACH
condition|)
return|return
operator|(
name|ENOTCONN
operator|)
return|;
ifdef|#
directive|ifdef
name|KPROF
name|acounts
index|[
name|nstate
index|]
index|[
name|req
index|]
operator|++
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|TCPDEBUG
if|if
condition|(
name|tp
operator|&&
operator|(
operator|(
name|tp
operator|->
name|t_socket
operator|->
name|so_options
operator|&
name|SO_DEBUG
operator|)
operator|||
name|tcpconsdebug
operator|)
condition|)
block|{
name|tdb_setup
argument_list|(
name|tp
argument_list|,
operator|(
expr|struct
name|th
operator|*
operator|)
literal|0
argument_list|,
name|req
argument_list|,
operator|&
name|tdb
argument_list|)
expr_stmt|;
name|tdb
operator|.
name|td_tim
operator|=
name|timertype
expr_stmt|;
block|}
else|else
name|tdb
operator|.
name|td_tod
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
switch|switch
condition|(
name|req
condition|)
block|{
comment|/* 	 * Attach a tcp control block to this socket. 	 * TCP is not multicast, so this is possible 	 * only if no connection currently exists. 	 */
case|case
name|PRU_ATTACH
case|:
if|if
condition|(
name|tp
condition|)
name|error
operator|=
name|EISCONN
expr_stmt|;
else|else
name|tp
operator|=
name|tcp_attach
argument_list|(
name|so
argument_list|,
operator|&
name|error
argument_list|)
expr_stmt|;
name|nstate
operator|=
name|CLOSED
expr_stmt|;
break|break;
comment|/* 	 * Detach the TCP from this socket.  This 	 * is possible only if a connection currently exists. 	 */
case|case
name|PRU_DETACH
case|:
name|so
operator|->
name|so_pcb
operator|=
literal|0
expr_stmt|;
break|break;
comment|/* 	 * Form connection: send a SYN. 	 */
case|case
name|PRU_CONNECT
case|:
if|if
condition|(
name|nstate
operator|!=
literal|0
operator|&&
name|nstate
operator|!=
name|CLOSED
condition|)
goto|goto
name|bad
goto|;
name|tcp_sndctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|nstate
operator|=
name|SYN_SENT
expr_stmt|;
break|break;
case|case
name|PRU_DISCONNECT
case|:
name|so
operator|->
name|so_pcb
operator|=
literal|0
expr_stmt|;
name|tcp_destroy
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Declare no further transmissions. 	 * Can be generated by a user ioctl (half-close), 	 * or when higher level close occurs, if a close hasn't happened 	 * already. 	 */
case|case
name|PRU_SHUTDOWN
case|:
switch|switch
condition|(
name|nstate
condition|)
block|{
comment|/* 		 * If we are aborting out of a listener or a active 		 * connection which has not yet completed we can just 		 * delete the tcb. 		 */
case|case
name|LISTEN
case|:
case|case
name|SYN_SENT
case|:
name|nstate
operator|=
name|CLOSED
expr_stmt|;
break|break;
comment|/* 		 * If we have gotten as far as receiving a syn from 		 * our foreign peer, we must be sure to send a FIN. 		 * If we have gotten a FIN from the foreign peer already 		 * (CLOSE_WAIT state), then all that remains is to wait 		 * for his ack of the FIN (LAST_ACK state).  If we have 		 * not gotten a FIN from the foreign peer then we need 		 * to either: 		 *	1. rcv ack of our FIN (to FIN_W2) and then 		 *	   send an ACK (to TIME_WAIT) and timeout at 2*MSL. 		 * or	2. receive hist FIN (to CLOSING), send an ACK 		 *	   (to TIME_WAIT), and then timeout. 		 * In any case this starts with a transition to FIN_W1 here. 		 */
case|case
name|SYN_RCVD
case|:
case|case
name|L_SYN_RCVD
case|:
case|case
name|ESTAB
case|:
case|case
name|CLOSE_WAIT
case|:
name|tp
operator|->
name|tc_flags
operator||=
name|TC_SND_FIN
expr_stmt|;
name|tcp_sndctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|tc_flags
operator||=
name|TC_USR_CLOSED
expr_stmt|;
name|nstate
operator|=
name|nstate
operator|!=
name|CLOSE_WAIT
condition|?
name|FIN_W1
else|:
name|LAST_ACK
expr_stmt|;
break|break;
comment|/* 		 * In these states the user has already closed; 		 * trying to close again is an error. 		 */
case|case
name|FIN_W1
case|:
case|case
name|FIN_W2
case|:
case|case
name|TIME_WAIT
case|:
case|case
name|CLOSING
case|:
case|case
name|LAST_ACK
case|:
case|case
name|RCV_WAIT
case|:
break|break;
default|default:
goto|goto
name|bad
goto|;
block|}
break|break;
comment|/* 	 * User notification of more window availability after 	 * reading out data.  This should not happen before a connection 	 * is established or after it is closed. 	 * If the foreign peer has closed and the local entity 	 * has not, inform him of the FIN (give end of file). 	 * If the local entity is in RCV_WAIT state (draining data 	 * out of the TCP buffers after foreign close) and there 	 * is no more data, institute a close. 	 */
case|case
name|PRU_RCVD
case|:
if|if
condition|(
name|nstate
operator|<
name|ESTAB
operator|||
name|nstate
operator|==
name|CLOSED
condition|)
goto|goto
name|bad
goto|;
name|tcp_sndwin
argument_list|(
name|tp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|tp
operator|->
name|tc_flags
operator|&
name|TC_FIN_RCVD
operator|)
operator|&&
operator|(
name|tp
operator|->
name|tc_flags
operator|&
name|TC_USR_CLOSED
operator|)
operator|==
literal|0
operator|&&
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
name|tcp_error
argument_list|(
name|tp
argument_list|,
name|ESHUTDOWN
argument_list|)
expr_stmt|;
if|if
condition|(
name|nstate
operator|==
name|RCV_WAIT
operator|&&
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
name|nstate
operator|=
name|CLOSED
expr_stmt|;
break|break;
comment|/* 	 * Send request on open connection. 	 * Should not happen if the connection is not yet established. 	 * Allowed only on ESTAB connection and after FIN from 	 * foreign peer. 	 */
case|case
name|PRU_SEND
case|:
switch|switch
condition|(
name|nstate
condition|)
block|{
case|case
name|ESTAB
case|:
case|case
name|CLOSE_WAIT
case|:
name|nstate
operator|=
name|tcp_usrsend
argument_list|(
name|tp
argument_list|,
name|m
argument_list|)
expr_stmt|;
break|break;
default|default:
if|if
condition|(
name|nstate
operator|<
name|ESTAB
condition|)
goto|goto
name|bad
goto|;
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
comment|/* tcp_user(tp, UCLSERR); */
break|break;
block|}
break|break;
comment|/* 	 * User abort of connection. 	 * If a SYN has been received, but we have not exchanged FINs 	 * then we need to send an RST.  In any case we then  	 * enter closed state. 	 */
case|case
name|PRU_ABORT
case|:
if|if
condition|(
name|nstate
operator|==
literal|0
operator|||
name|nstate
operator|==
name|CLOSED
condition|)
break|break;
switch|switch
condition|(
name|nstate
condition|)
block|{
case|case
literal|0
case|:
case|case
name|CLOSED
case|:
break|break;
case|case
name|SYN_RCVD
case|:
case|case
name|ESTAB
case|:
case|case
name|FIN_W1
case|:
case|case
name|FIN_W2
case|:
case|case
name|CLOSE_WAIT
case|:
name|tp
operator|->
name|tc_flags
operator||=
name|TC_SND_RST
expr_stmt|;
name|tcp_sndnull
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* fall into ... */
default|default:
name|nstate
operator|=
name|CLOSED
expr_stmt|;
block|}
break|break;
comment|/* 	 * Network down entry.  Discard the tcb and force 	 * the state to be closed, ungracefully. 	 */
case|case
name|PRU_CLEAR
case|:
if|if
condition|(
name|nstate
operator|==
literal|0
operator|||
name|nstate
operator|==
name|CLOSED
condition|)
break|break;
name|nstate
operator|=
name|CLOSED
expr_stmt|;
break|break;
comment|/* 	 * Ioctl on protocols. 	 */
case|case
name|PRU_CONTROL
case|:
break|break;
comment|/* 	 * TCP Timer processing. 	 * Timers should expire only on open connections 	 * not in LISTEN state. 	 */
case|case
name|PRU_SLOWTIMO
case|:
switch|switch
condition|(
name|nstate
condition|)
block|{
case|case
literal|0
case|:
case|case
name|CLOSED
case|:
case|case
name|LISTEN
case|:
goto|goto
name|bad
goto|;
default|default:
name|nstate
operator|=
name|tcp_timers
argument_list|(
name|tp
argument_list|,
operator|(
name|int
operator|)
name|addr
argument_list|)
expr_stmt|;
block|}
break|break;
default|default:
name|panic
argument_list|(
literal|"tcp_usrreq"
argument_list|)
expr_stmt|;
name|bad
label|:
name|printf
argument_list|(
literal|"tcp: bad state: tcb=%x state=%d input=%d\n"
argument_list|,
name|tp
argument_list|,
name|tp
operator|->
name|t_state
argument_list|,
name|req
argument_list|)
expr_stmt|;
name|nstate
operator|=
name|EFAILEC
expr_stmt|;
break|break;
block|}
ifdef|#
directive|ifdef
name|TCPDEBUG
if|if
condition|(
name|tdb
operator|.
name|td_tod
condition|)
name|tdb_stuff
argument_list|(
operator|&
name|tdb
argument_list|,
name|nstate
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* YECH */
switch|switch
condition|(
name|nstate
condition|)
block|{
case|case
name|CLOSED
case|:
case|case
name|SAME
case|:
break|break;
case|case
name|EFAILEC
case|:
if|if
condition|(
name|m
condition|)
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|m
argument_list|)
argument_list|)
expr_stmt|;
break|break;
default|default:
name|tp
operator|->
name|t_state
operator|=
name|nstate
expr_stmt|;
break|break;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|tcp_sense
argument_list|()
end_macro

begin_block
block|{  }
end_block

begin_comment
comment|/*  * Open routine, called to initialize newly created tcb fields.  */
end_comment

begin_function
name|struct
name|tcb
modifier|*
name|tcp_attach
parameter_list|(
name|so
parameter_list|)
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_ATTACH
argument_list|)
expr_stmt|;
comment|/* 	 * Link in tcb queue and make 	 * initialize empty reassembly queue. 	 */
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_next
operator|=
name|tcb
operator|.
name|tcb_next
expr_stmt|;
name|tcb
operator|.
name|tcb_next
operator|->
name|tcb_hd
operator|.
name|tcb_prev
operator|=
name|tp
expr_stmt|;
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_prev
operator|=
operator|(
expr|struct
name|tcb
operator|*
operator|)
operator|&
name|tcb
expr_stmt|;
name|tcb
operator|.
name|tcb_next
operator|=
name|tp
expr_stmt|;
name|tp
operator|->
name|tcb_hd
operator|.
name|seg_next
operator|=
name|tp
operator|->
name|tcb_hd
operator|.
name|seg_prev
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|tp
expr_stmt|;
comment|/* 	 * Initialize sequence numbers and 	 * round trip retransmit timer. 	 * (Other fields were init'd to zero when tcb allocated.) 	 */
name|tp
operator|->
name|t_xmtime
operator|=
name|T_REXMT
expr_stmt|;
name|tp
operator|->
name|snd_end
operator|=
name|tp
operator|->
name|seq_fin
operator|=
name|tp
operator|->
name|snd_nxt
operator|=
name|tp
operator|->
name|snd_hi
operator|=
name|tp
operator|->
name|snd_una
operator|=
name|tp
operator|->
name|iss
operator|=
name|tcp_iss
expr_stmt|;
name|tp
operator|->
name|snd_off
operator|=
name|tp
operator|->
name|iss
operator|+
literal|1
expr_stmt|;
name|tcp_iss
operator|+=
operator|(
name|ISSINCR
operator|>>
literal|1
operator|)
operator|+
literal|1
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Destroy a tcb.  */
end_comment

begin_expr_stmt
name|tcp_detach
argument_list|(
name|tp
argument_list|)
specifier|register
expr|struct
name|tcb
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|socket
modifier|*
name|so
init|=
name|tp
operator|->
name|t_socket
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|t
decl_stmt|;
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_DETACH
argument_list|)
expr_stmt|;
comment|/* 	 * Remove from tcb queue and cancel timers. 	 */
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_prev
operator|->
name|tcb_hd
operator|.
name|tcb_next
operator|=
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_next
expr_stmt|;
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_next
operator|->
name|tcb_hd
operator|.
name|tcb_prev
operator|=
name|tp
operator|->
name|tcb_hd
operator|.
name|tcb_prev
expr_stmt|;
name|tcp_tcancel
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* 	 * Discard all buffers. 	 */
for|for
control|(
name|t
operator|=
name|tp
operator|->
name|tcb_hd
operator|.
name|seg_next
init|;
name|t
operator|!=
operator|(
expr|struct
name|th
operator|*
operator|)
name|tp
condition|;
name|t
operator|=
name|t
operator|->
name|t_next
control|)
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|t
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
condition|)
block|{
name|m_freem
argument_list|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|=
literal|0
expr_stmt|;
block|}
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|=
literal|0
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mbcnt
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_snd
operator|.
name|sb_mb
condition|)
block|{
name|m_freem
argument_list|(
name|so
operator|->
name|so_snd
operator|.
name|sb_mb
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|=
literal|0
expr_stmt|;
block|}
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
operator|=
literal|0
expr_stmt|;
name|so
operator|->
name|so_snd
operator|.
name|sb_mbcnt
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|m
operator|=
name|tp
operator|->
name|seg_unack
init|;
name|m
condition|;
name|m
operator|=
name|m
operator|->
name|m_act
control|)
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
name|tp
operator|->
name|seg_unack
operator|=
literal|0
expr_stmt|;
comment|/* 	 * Free routing table entry. 	 */
if|if
condition|(
name|tp
operator|->
name|t_host
condition|)
block|{
name|h_free
argument_list|(
name|tp
operator|->
name|t_host
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_host
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 	 * Free tcp send template, the tcb itself, 	 * and the space we had reserved in the meory pool. 	 */
if|if
condition|(
name|tp
operator|->
name|t_template
condition|)
block|{
name|m_free
argument_list|(
name|dtom
argument_list|(
name|tp
operator|->
name|t_template
argument_list|)
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_template
operator|=
literal|0
expr_stmt|;
block|}
name|wmemfree
argument_list|(
operator|(
name|caddr_t
operator|)
name|tp
argument_list|,
literal|1024
argument_list|)
expr_stmt|;
name|m_release
argument_list|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_hiwat
operator|+
name|so
operator|->
name|so_snd
operator|.
name|sb_hiwat
operator|+
literal|2
operator|*
name|MSIZE
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Send data queue headed by m0 into the protocol.  */
end_comment

begin_expr_stmt
name|tcp_usrsend
argument_list|(
name|tp
argument_list|,
name|m0
argument_list|)
specifier|register
expr|struct
name|tcb
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m0
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|,
modifier|*
name|n
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
init|=
name|tp
operator|->
name|t_socket
decl_stmt|;
specifier|register
name|off
expr_stmt|;
name|seq_t
name|last
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_USRSEND
argument_list|)
expr_stmt|;
name|last
operator|=
name|tp
operator|->
name|snd_off
expr_stmt|;
for|for
control|(
name|m
operator|=
name|n
operator|=
name|m0
init|;
name|m
operator|!=
name|NULL
condition|;
name|m
operator|=
name|m
operator|->
name|m_next
control|)
block|{
name|so
operator|->
name|so_snd
operator|.
name|sb_mbcnt
operator|++
expr_stmt|;
if|if
condition|(
name|m
operator|->
name|m_off
operator|>
name|MMAXOFF
condition|)
name|so
operator|->
name|so_snd
operator|.
name|sb_mbcnt
operator|+=
name|NMBPG
expr_stmt|;
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|m
operator|=
name|so
operator|->
name|so_snd
operator|.
name|sb_mb
operator|)
operator|==
name|NULL
condition|)
name|so
operator|->
name|so_snd
operator|.
name|sb_mb
operator|=
name|n
expr_stmt|;
else|else
block|{
while|while
condition|(
name|m
operator|->
name|m_next
operator|!=
name|NULL
condition|)
block|{
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
block|}
if|if
condition|(
name|m
operator|->
name|m_off
operator|<=
name|MMAXOFF
condition|)
block|{
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
name|off
operator|=
name|m
operator|->
name|m_off
operator|+
name|m
operator|->
name|m_len
expr_stmt|;
while|while
condition|(
name|n
operator|&&
name|n
operator|->
name|m_off
operator|<=
name|MMAXOFF
operator|&&
operator|(
name|MMAXOFF
operator|-
name|off
operator|)
operator|>=
name|n
operator|->
name|m_len
condition|)
block|{
name|bcopy
argument_list|(
call|(
name|caddr_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|n
operator|+
name|n
operator|->
name|m_off
argument_list|)
argument_list|,
call|(
name|caddr_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|m
operator|+
name|off
argument_list|)
argument_list|,
name|n
operator|->
name|m_len
argument_list|)
expr_stmt|;
name|m
operator|->
name|m_len
operator|+=
name|n
operator|->
name|m_len
expr_stmt|;
name|off
operator|+=
name|n
operator|->
name|m_len
expr_stmt|;
name|so
operator|->
name|so_snd
operator|.
name|sb_mbcnt
operator|--
expr_stmt|;
name|n
operator|=
name|m_free
argument_list|(
name|n
argument_list|)
expr_stmt|;
block|}
block|}
name|m
operator|->
name|m_next
operator|=
name|n
expr_stmt|;
block|}
if|if
condition|(
name|tp
operator|->
name|t_options
operator|&
name|TO_EOL
condition|)
name|tp
operator|->
name|snd_end
operator|=
name|last
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_options
operator|&
name|TO_URG
condition|)
block|{
name|tp
operator|->
name|snd_urp
operator|=
name|last
operator|+
literal|1
expr_stmt|;
name|tp
operator|->
name|tc_flags
operator||=
name|TC_SND_URG
expr_stmt|;
block|}
name|tcp_send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * TCP timer went off processing.  */
end_comment

begin_expr_stmt
name|tcp_timers
argument_list|(
name|tp
argument_list|,
name|timertype
argument_list|)
specifier|register
expr|struct
name|tcb
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|timertype
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|TCP_TIMERS
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|timertype
condition|)
block|{
case|case
name|TINIT
case|:
comment|/* initialization timer */
if|if
condition|(
operator|(
name|tp
operator|->
name|tc_flags
operator|&
name|TC_SYN_ACKED
operator|)
operator|==
literal|0
condition|)
block|{
comment|/* 35 */
comment|/* XXX */
comment|/* tcp_close(tp, UINTIMO); */
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
case|case
name|TFINACK
case|:
comment|/* fin-ack timer */
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
case|case
name|TIME_WAIT
case|:
comment|/* 			 * We can be sure our ACK of foreign FIN was rcvd, 			 * and can close if no data left for user. 			 */
if|if
condition|(
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
block|{
comment|/* XXX */
comment|/* tcp_close(tp, UCLOSED); */
comment|/* 14 */
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
return|return
operator|(
name|RCV_WAIT
operator|)
return|;
comment|/* 17 */
case|case
name|CLOSING
case|:
name|tp
operator|->
name|tc_flags
operator||=
name|TC_WAITED_2_ML
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
default|default:
return|return
operator|(
name|SAME
operator|)
return|;
block|}
case|case
name|TREXMT
case|:
comment|/* retransmission timer */
if|if
condition|(
name|tp
operator|->
name|t_rexmt_val
operator|>
name|tp
operator|->
name|snd_una
condition|)
block|{
comment|/* 34 */
comment|/* 			 * Set so for a retransmission, increase rexmt time 			 * in case of multiple retransmissions. 			 */
name|tp
operator|->
name|snd_nxt
operator|=
name|tp
operator|->
name|snd_una
expr_stmt|;
name|tp
operator|->
name|tc_flags
operator||=
name|TC_REXMT
expr_stmt|;
name|tp
operator|->
name|t_xmtime
operator|=
name|tp
operator|->
name|t_xmtime
operator|<<
literal|1
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_xmtime
operator|>
name|T_REMAX
condition|)
name|tp
operator|->
name|t_xmtime
operator|=
name|T_REMAX
expr_stmt|;
name|tcp_send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
case|case
name|TREXMTTL
case|:
comment|/* retransmit too long */
if|if
condition|(
name|tp
operator|->
name|t_rtl_val
operator|>
name|tp
operator|->
name|snd_una
condition|)
comment|/* 36 */
comment|/* XXX */
comment|/* to_user(tp->t_socket, URXTIMO); */
empty_stmt|;
comment|/* 		 * If user has already closed, abort the connection. 		 */
if|if
condition|(
name|tp
operator|->
name|tc_flags
operator|&
name|TC_USR_CLOSED
condition|)
block|{
comment|/* XXX */
comment|/* tcp_close(tp, URXTIMO); */
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
case|case
name|TPERSIST
case|:
comment|/* persist timer */
comment|/* 		 * Force a byte send through closed window. 		 */
name|tp
operator|->
name|tc_flags
operator||=
name|TC_FORCE_ONE
expr_stmt|;
name|tcp_send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
name|panic
argument_list|(
literal|"tcp_timers"
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|tcp_error
argument_list|(
argument|so
argument_list|,
argument|errno
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|errno
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|TO_USER
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_error
operator|=
name|errno
expr_stmt|;
name|sowakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
block|}
end_block

begin_ifdef
ifdef|#
directive|ifdef
name|TCPDEBUG
end_ifdef

begin_comment
comment|/*  * TCP debugging utility subroutines.  * THE NAMES OF THE FIELDS USED BY THESE ROUTINES ARE STUPID.  */
end_comment

begin_macro
name|tdb_setup
argument_list|(
argument|tp
argument_list|,
argument|n
argument_list|,
argument|input
argument_list|,
argument|tdp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|input
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|tcp_debug
modifier|*
name|tdp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|TDB_SETUP
argument_list|)
expr_stmt|;
name|tdp
operator|->
name|td_tod
operator|=
name|time
expr_stmt|;
name|tdp
operator|->
name|td_tcb
operator|=
name|tp
expr_stmt|;
name|tdp
operator|->
name|td_old
operator|=
name|tp
operator|->
name|t_state
expr_stmt|;
name|tdp
operator|->
name|td_inp
operator|=
name|input
expr_stmt|;
name|tdp
operator|->
name|td_tim
operator|=
literal|0
expr_stmt|;
name|tdp
operator|->
name|td_new
operator|=
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|n
condition|)
block|{
name|tdp
operator|->
name|td_sno
operator|=
name|n
operator|->
name|t_seq
expr_stmt|;
name|tdp
operator|->
name|td_ano
operator|=
name|n
operator|->
name|t_ackno
expr_stmt|;
name|tdp
operator|->
name|td_wno
operator|=
name|n
operator|->
name|t_win
expr_stmt|;
name|tdp
operator|->
name|td_lno
operator|=
name|n
operator|->
name|t_len
expr_stmt|;
name|tdp
operator|->
name|td_flg
operator|=
name|n
operator|->
name|th_flags
expr_stmt|;
block|}
else|else
name|tdp
operator|->
name|td_sno
operator|=
name|tdp
operator|->
name|td_ano
operator|=
name|tdp
operator|->
name|td_wno
operator|=
name|tdp
operator|->
name|td_lno
operator|=
name|tdp
operator|->
name|td_flg
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_macro
name|tdb_stuff
argument_list|(
argument|tdp
argument_list|,
argument|nstate
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tcp_debug
modifier|*
name|tdp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|nstate
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|TDB_STUFF
argument_list|)
expr_stmt|;
name|tdp
operator|->
name|td_new
operator|=
name|nstate
expr_stmt|;
name|tcp_debug
index|[
name|tdbx
operator|++
operator|%
name|TDBSIZE
index|]
operator|=
operator|*
name|tdp
expr_stmt|;
if|if
condition|(
name|tcpconsdebug
operator|&
literal|2
condition|)
name|tcp_prt
argument_list|(
name|tdp
argument_list|)
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|tcp_prt
argument_list|(
name|tdp
argument_list|)
specifier|register
expr|struct
name|tcp_debug
operator|*
name|tdp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|TCP_PRT
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%x "
argument_list|,
operator|(
operator|(
name|int
operator|)
name|tdp
operator|->
name|td_tcb
operator|)
operator|&
literal|0xffffff
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_inp
operator|==
name|INSEND
condition|)
block|{
name|printf
argument_list|(
literal|"SEND #%x"
argument_list|,
name|tdp
operator|->
name|td_sno
argument_list|)
expr_stmt|;
name|tdp
operator|->
name|td_lno
operator|=
name|ntohs
argument_list|(
name|tdp
operator|->
name|td_lno
argument_list|)
expr_stmt|;
name|tdp
operator|->
name|td_wno
operator|=
name|ntohs
argument_list|(
name|tdp
operator|->
name|td_wno
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|tdp
operator|->
name|td_inp
operator|==
name|INRECV
condition|)
name|printf
argument_list|(
literal|"RCV #%x "
argument_list|,
name|tdp
operator|->
name|td_sno
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%s.%s"
argument_list|,
name|tcpstates
index|[
name|tdp
operator|->
name|td_old
index|]
argument_list|,
name|tcpinputs
index|[
name|tdp
operator|->
name|td_inp
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_inp
operator|==
name|ISTIMER
condition|)
name|printf
argument_list|(
literal|"(%s)"
argument_list|,
name|tcptimers
index|[
name|tdp
operator|->
name|td_tim
index|]
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|" -> %s"
argument_list|,
name|tcpstates
index|[
operator|(
name|tdp
operator|->
name|td_new
operator|>
literal|0
operator|)
condition|?
name|tdp
operator|->
name|td_new
else|:
name|tdp
operator|->
name|td_old
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_new
operator|==
operator|-
literal|1
condition|)
name|printf
argument_list|(
literal|" (FAILED)"
argument_list|)
expr_stmt|;
block|}
comment|/* GROSS... DEPENDS ON SIGN EXTENSION OF CHARACTERS */
if|if
condition|(
name|tdp
operator|->
name|td_lno
condition|)
name|printf
argument_list|(
literal|" len=%d"
argument_list|,
name|tdp
operator|->
name|td_lno
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_wno
condition|)
name|printf
argument_list|(
literal|" win=%d"
argument_list|,
name|tdp
operator|->
name|td_wno
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_FIN
condition|)
name|printf
argument_list|(
literal|" FIN"
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_SYN
condition|)
name|printf
argument_list|(
literal|" SYN"
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_RST
condition|)
name|printf
argument_list|(
literal|" RST"
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_EOL
condition|)
name|printf
argument_list|(
literal|" EOL"
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_ACK
condition|)
name|printf
argument_list|(
literal|" ACK %x"
argument_list|,
name|tdp
operator|->
name|td_ano
argument_list|)
expr_stmt|;
if|if
condition|(
name|tdp
operator|->
name|td_flg
operator|&
name|TH_URG
condition|)
name|printf
argument_list|(
literal|" URG"
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

