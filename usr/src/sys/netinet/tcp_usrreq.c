begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* tcp_usrreq.c 1.1 81/10/14 */
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/net.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/tcp.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/ip.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/imp.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/ucb.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/fsm.h"
end_include

begin_include
include|#
directive|include
file|"../bbnnet/tcp_pred.h"
end_include

begin_macro
name|lis_cls
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* passive open (1) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|LIS_CLS
argument_list|)
expr_stmt|;
name|t_open
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|,
name|PASSIVE
argument_list|)
expr_stmt|;
return|return
operator|(
name|LISTEN
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|sys_cls
argument_list|(
name|wp
argument_list|)
comment|/* active open (6) */
specifier|register
expr|struct
name|work
operator|*
name|wp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|SYS_CLS
argument_list|)
expr_stmt|;
name|t_open
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|,
name|ACTIVE
argument_list|)
expr_stmt|;
name|send_ctl
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|)
expr_stmt|;
comment|/* send SYN */
return|return
operator|(
name|SYN_SENT
operator|)
return|;
block|}
end_block

begin_macro
name|cls_opn
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* close request before receiving foreign SYN (10) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|CLS_OPN
argument_list|)
expr_stmt|;
name|t_close
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|,
name|UCLOSED
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
end_block

begin_macro
name|cl2_clw
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* close request after receiving foreign FIN (13) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|CL2_CLW
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|tp
operator|->
name|snd_fin
operator|=
name|TRUE
expr_stmt|;
comment|/* send our own FIN */
name|send_ctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|usr_closed
operator|=
name|TRUE
expr_stmt|;
return|return
operator|(
name|CLOSING2
operator|)
return|;
block|}
end_block

begin_macro
name|cls_rwt
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* rcv request after foreign close (20) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|CLS_RWT
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* present any remaining data */
if|if
condition|(
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
block|{
name|t_close
argument_list|(
name|tp
argument_list|,
name|UCLOSED
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
else|else
return|return
operator|(
name|RCV_WAIT
operator|)
return|;
block|}
end_block

begin_macro
name|fw1_syr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* close request on synced connection (24,25) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|FW1_SYR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|tp
operator|->
name|snd_fin
operator|=
name|TRUE
expr_stmt|;
comment|/* send FIN */
name|send_ctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|usr_closed
operator|=
name|TRUE
expr_stmt|;
return|return
operator|(
name|FIN_W1
operator|)
return|;
block|}
end_block

begin_macro
name|sss_syn
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seq on open connection (39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|SSS_SYN
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|wp
operator|->
name|w_dat
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|sss_snd
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* send request on open connection (40,41) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|,
modifier|*
name|n
decl_stmt|;
specifier|register
name|struct
name|ucb
modifier|*
name|up
decl_stmt|;
specifier|register
name|off
expr_stmt|;
name|sequence
name|last
decl_stmt|;
name|COUNT
argument_list|(
name|SSS_SND
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|up
operator|=
name|tp
operator|->
name|t_ucb
expr_stmt|;
name|last
operator|=
name|tp
operator|->
name|snd_off
expr_stmt|;
comment|/* count number of mbufs in send data */
for|for
control|(
name|m
operator|=
name|n
operator|=
operator|(
expr|struct
name|mbuf
operator|*
operator|)
name|wp
operator|->
name|w_dat
init|;
name|m
operator|!=
name|NULL
condition|;
name|m
operator|=
name|m
operator|->
name|m_next
control|)
block|{
name|up
operator|->
name|uc_ssize
operator|++
expr_stmt|;
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
block|}
comment|/* find end of send buffer and append data */
if|if
condition|(
operator|(
name|m
operator|=
name|up
operator|->
name|uc_sbuf
operator|)
operator|!=
name|NULL
condition|)
block|{
comment|/* something in send buffer */
while|while
condition|(
name|m
operator|->
name|m_next
operator|!=
name|NULL
condition|)
block|{
comment|/* find the end */
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
block|}
name|last
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
comment|/* if there's room in old buffer for new data, consolidate */
name|off
operator|=
name|m
operator|->
name|m_off
operator|+
name|m
operator|->
name|m_len
expr_stmt|;
while|while
condition|(
name|n
operator|!=
name|NULL
operator|&&
operator|(
name|MSIZE
operator|-
name|off
operator|)
operator|>=
name|n
operator|->
name|m_len
condition|)
block|{
name|bcopy
argument_list|(
call|(
name|caddr_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|n
operator|+
name|n
operator|->
name|m_off
argument_list|)
argument_list|,
call|(
name|caddr_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|m
operator|+
name|off
argument_list|)
argument_list|,
name|n
operator|->
name|m_len
argument_list|)
expr_stmt|;
name|m
operator|->
name|m_len
operator|+=
name|n
operator|->
name|m_len
expr_stmt|;
name|off
operator|+=
name|n
operator|->
name|m_len
expr_stmt|;
name|up
operator|->
name|uc_ssize
operator|--
expr_stmt|;
name|n
operator|=
name|m_free
argument_list|(
name|n
argument_list|)
expr_stmt|;
block|}
name|m
operator|->
name|m_next
operator|=
name|n
expr_stmt|;
block|}
else|else
comment|/* nothing in send buffer */
name|up
operator|->
name|uc_sbuf
operator|=
name|n
expr_stmt|;
if|if
condition|(
name|up
operator|->
name|uc_flags
operator|&
name|UEOL
condition|)
block|{
comment|/* set EOL */
name|tp
operator|->
name|snd_end
operator|=
name|last
expr_stmt|;
block|}
if|if
condition|(
name|up
operator|->
name|uc_flags
operator|&
name|UURG
condition|)
block|{
comment|/* urgent data */
name|tp
operator|->
name|snd_urp
operator|=
name|last
operator|+
literal|1
expr_stmt|;
name|tp
operator|->
name|snd_urg
operator|=
name|TRUE
expr_stmt|;
block|}
name|send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|sss_rcv
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* rcv request on open connection (42) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|SSS_RCV
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|send_ctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* send new window */
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|cls_nsy
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* abort request on unsynced connection (44) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|CLS_NSY
argument_list|)
expr_stmt|;
name|t_close
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|,
name|UABORT
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
end_block

begin_macro
name|cls_syn
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* abort request on synced connection (45) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
name|COUNT
argument_list|(
name|CLS_SYN
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|tp
operator|->
name|snd_rst
operator|=
name|TRUE
expr_stmt|;
comment|/* send reset */
name|send_null
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|t_close
argument_list|(
name|tp
argument_list|,
name|UABORT
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
end_block

begin_macro
name|cls_act
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* net closing open connection (47) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|CLS_ACT
argument_list|)
expr_stmt|;
name|t_close
argument_list|(
name|wp
operator|->
name|w_tcb
argument_list|,
name|UNETDWN
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
end_block

begin_macro
name|cls_err
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* invalid user request in closing states */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|CLS_ERR
argument_list|)
expr_stmt|;
name|to_user
argument_list|(
name|wp
operator|->
name|w_tcb
operator|->
name|t_ucb
argument_list|,
name|UCLSERR
argument_list|)
expr_stmt|;
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|lis_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg in LISTEN (3,4) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|LIS_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
if|if
condition|(
operator|!
name|syn_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
comment|/* must have good SYN */
return|return
operator|(
name|EFAILEC
operator|)
return|;
comment|/* fill in unspecified foreign host address.  get/create entry 	   in foreign host table.  if none available, ignore.  probably 	   should send reset here. */
if|if
condition|(
operator|(
name|tp
operator|->
name|t_ucb
operator|->
name|uc_host
operator|=
name|h_make
argument_list|(
operator|&
name|n
operator|->
name|t_s
argument_list|)
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|EFAILEC
operator|)
return|;
name|tp
operator|->
name|t_fport
operator|=
name|n
operator|->
name|t_src
expr_stmt|;
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|tp
operator|->
name|fin_rcvd
condition|)
block|{
comment|/* no FIN (4) */
comment|/* start init timer now that we have foreign host */
name|tp
operator|->
name|t_init
operator|=
name|T_INIT
operator|/
literal|2
expr_stmt|;
return|return
operator|(
name|L_SYN_RCVD
operator|)
return|;
block|}
else|else
block|{
comment|/* got a FIN, start timer (3) */
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
return|return
operator|(
name|CLOSE_WAIT
operator|)
return|;
block|}
block|}
end_block

begin_macro
name|sys_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming segment after SYN sent (8,9,11,32) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|SYS_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
if|if
condition|(
operator|!
name|syn_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
comment|/* must have good SYN */
return|return
operator|(
name|EFAILEC
operator|)
return|;
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
block|{
comment|/* got a FIN */
comment|/* if good ACK, present any data */
if|if
condition|(
name|n
operator|->
name|t_ack
condition|)
block|{
if|if
condition|(
name|n
operator|->
name|t_ackno
operator|>
name|tp
operator|->
name|iss
condition|)
comment|/* 32 */
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* 9 */
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
block|}
return|return
operator|(
name|CLOSE_WAIT
operator|)
return|;
block|}
elseif|else
comment|/* no FIN */
comment|/* if good ACK, open connection, otherwise wait for one */
if|if
condition|(
name|n
operator|->
name|t_ack
condition|)
block|{
comment|/* 11 */
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return
operator|(
name|ESTAB
operator|)
return|;
block|}
else|else
return|return
operator|(
name|SYN_RCVD
operator|)
return|;
comment|/* 8 */
block|}
end_block

begin_macro
name|cl1_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg after we closed (15,18,22,23,30,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|CL1_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
if|if
condition|(
name|ack_fin
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
comment|/* got ACK of our FIN */
if|if
condition|(
name|n
operator|->
name|t_fin
condition|)
block|{
comment|/* got for FIN (23) */
name|rcv_ctl
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
return|return
operator|(
name|TIME_WAIT
operator|)
return|;
block|}
else|else
block|{
comment|/* if wait done, see if any data left for user */
if|if
condition|(
name|tp
operator|->
name|waited_2_ml
condition|)
if|if
condition|(
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
block|{
comment|/* 15 */
name|t_close
argument_list|(
name|tp
argument_list|,
name|UCLOSED
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
else|else
return|return
operator|(
name|RCV_WAIT
operator|)
return|;
comment|/* 18 */
else|else
return|return
operator|(
name|TIME_WAIT
operator|)
return|;
comment|/* 22 */
block|}
elseif|else
comment|/* our FIN not ACKed yet */
if|if
condition|(
name|n
operator|->
name|t_fin
condition|)
block|{
comment|/* rcvd for FIN (30) */
name|rcv_ctl
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
block|}
else|else
block|{
comment|/* no FIN, just proc new data (39) */
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|cl2_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg after foreign close (16,19,31,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|CL2_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
if|if
condition|(
name|ack_fin
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
block|{
comment|/* this is ACK of our fin */
comment|/* if no data left for user, close; otherwise wait */
if|if
condition|(
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
block|{
comment|/* 16 */
name|t_close
argument_list|(
name|tp
argument_list|,
name|UCLOSED
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
else|else
comment|/* 19 */
return|return
operator|(
name|RCV_WAIT
operator|)
return|;
block|}
elseif|else
comment|/* no ACK of our FIN */
comment|/* duplicate FIN or data */
if|if
condition|(
name|n
operator|->
name|t_fin
condition|)
comment|/* 31 */
name|send_ctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* ACK duplicate FIN */
else|else
block|{
comment|/* 39 */
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|fw1_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg after user close (26,27,28,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|FW1_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
comment|/* process any incoming data, since we closed but they didn't */
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ack_fin
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
comment|/* our FIN got ACKed */
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
block|{
comment|/* got for FIN (28) */
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
return|return
operator|(
name|TIME_WAIT
operator|)
return|;
block|}
else|else
comment|/* no FIN, wait (27) */
return|return
operator|(
name|FIN_W2
operator|)
return|;
elseif|else
comment|/* no ACK of FIN */
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
block|{
comment|/* got for FIN (26) */
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
return|return
operator|(
name|CLOSING1
operator|)
return|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
comment|/* 39 */
block|}
end_block

begin_macro
name|syr_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg after SYN rcvd (5,33) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|SYR_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
if|if
condition|(
operator|!
name|n
operator|->
name|t_ack
operator|||
operator|(
name|n
operator|->
name|t_ack
operator|&&
name|n
operator|->
name|t_ackno
operator|<=
name|tp
operator|->
name|iss
operator|)
condition|)
comment|/* must have ACK of our SYN */
return|return
operator|(
name|EFAILEC
operator|)
return|;
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* if no FIN, open connection, otherwise wait for user close */
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
comment|/* 33 */
return|return
operator|(
name|CLOSE_WAIT
operator|)
return|;
else|else
comment|/* 5 */
return|return
operator|(
name|ESTAB
operator|)
return|;
block|}
end_block

begin_macro
name|est_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg on open connection (12,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|EST_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* if no FIN, remain open, otherwise wait for user close */
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
comment|/* 12 */
return|return
operator|(
name|CLOSE_WAIT
operator|)
return|;
else|else
comment|/* 39 */
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|fw2_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg while waiting for for FIN (12,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|FW2_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
comment|/* process data since we closed, but they may not have */
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
comment|/* if we get the FIN, start the finack timer, else keep waiting */
if|if
condition|(
name|tp
operator|->
name|fin_rcvd
condition|)
block|{
comment|/* got for FIN (29) */
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
return|return
operator|(
name|TIME_WAIT
operator|)
return|;
block|}
else|else
comment|/* 39 */
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|cwt_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg after exchange of FINs (30,31,39) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|CWT_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
comment|/* either duplicate FIN or data */
if|if
condition|(
name|n
operator|->
name|t_fin
condition|)
block|{
if|if
condition|(
name|n
operator|->
name|t_ack
operator|&&
name|n
operator|->
name|t_ackno
operator|<=
name|tp
operator|->
name|seq_fin
condition|)
block|{
comment|/* dup ACK (30) */
name|rcv_ctl
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
block|}
else|else
comment|/* 31 */
name|send_ctl
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* duplicate data (39) */
name|rcv_data
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|present_data
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|rwt_netr
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* incoming seg while waiting for user rcv (30,21) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
name|COUNT
argument_list|(
name|RWT_NETR
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|n
operator|=
operator|(
expr|struct
name|th
operator|*
operator|)
name|wp
operator|->
name|w_dat
expr_stmt|;
comment|/* handle duplicate ACK of our FIN */
if|if
condition|(
name|n
operator|->
name|t_fin
operator|&&
name|n
operator|->
name|t_ack
operator|&&
name|n
operator|->
name|t_ackno
operator|<=
name|tp
operator|->
name|seq_fin
condition|)
block|{
comment|/* 30 */
name|rcv_ctl
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_finack
operator|=
name|T_2ML
expr_stmt|;
name|tp
operator|->
name|waited_2_ml
operator|=
name|FALSE
expr_stmt|;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_macro
name|timers
argument_list|(
argument|wp
argument_list|)
end_macro

begin_comment
comment|/* timer processor (14,17,34,35,36,37,38) */
end_comment

begin_decl_stmt
name|struct
name|work
modifier|*
name|wp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|type
expr_stmt|;
name|COUNT
argument_list|(
name|TIMERS
argument_list|)
expr_stmt|;
name|tp
operator|=
name|wp
operator|->
name|w_tcb
expr_stmt|;
name|type
operator|=
name|wp
operator|->
name|w_stype
expr_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|TINIT
case|:
comment|/* initialization timer */
if|if
condition|(
operator|!
name|tp
operator|->
name|syn_acked
condition|)
block|{
comment|/* haven't got ACK of our SYN (35) */
name|t_close
argument_list|(
name|tp
argument_list|,
name|UINTIMO
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
break|break;
case|case
name|TFINACK
case|:
comment|/* fin-ack timer */
if|if
condition|(
name|tp
operator|->
name|t_state
operator|==
name|TIME_WAIT
condition|)
block|{
comment|/* can be sure our ACK of for FIN was rcvd, 			   can close if no data left for user */
if|if
condition|(
name|rcv_empty
argument_list|(
name|tp
argument_list|)
condition|)
block|{
comment|/* 14 */
name|t_close
argument_list|(
name|tp
argument_list|,
name|UCLOSED
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
else|else
comment|/* 17 */
return|return
operator|(
name|RCV_WAIT
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|tp
operator|->
name|t_state
operator|==
name|CLOSING1
condition|)
comment|/* 37 */
comment|/* safe to close */
name|tp
operator|->
name|waited_2_ml
operator|=
name|TRUE
expr_stmt|;
break|break;
case|case
name|TREXMT
case|:
comment|/* retransmission timer */
comment|/* set up for a retransmission, increase rexmt time 		   in case of multiple retransmissions. */
if|if
condition|(
name|tp
operator|->
name|t_rexmt_val
operator|>
name|tp
operator|->
name|snd_una
condition|)
block|{
comment|/* 34 */
name|tp
operator|->
name|snd_nxt
operator|=
name|tp
operator|->
name|snd_una
expr_stmt|;
name|tp
operator|->
name|rexmt
operator|=
name|TRUE
expr_stmt|;
name|tp
operator|->
name|t_xmtime
operator|=
name|tp
operator|->
name|t_xmtime
operator|<<
literal|1
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_xmtime
operator|>
name|T_REMAX
condition|)
name|tp
operator|->
name|t_xmtime
operator|=
name|T_REMAX
expr_stmt|;
name|send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
break|break;
case|case
name|TREXMTTL
case|:
comment|/* retransmit too long */
comment|/* tell user */
if|if
condition|(
name|tp
operator|->
name|t_rtl_val
operator|>
name|tp
operator|->
name|snd_una
condition|)
comment|/* 36 */
name|to_user
argument_list|(
name|tp
operator|->
name|t_ucb
argument_list|,
name|URXTIMO
argument_list|)
expr_stmt|;
comment|/* if user has already closed, abort the connection */
if|if
condition|(
name|tp
operator|->
name|usr_closed
condition|)
block|{
name|t_close
argument_list|(
name|tp
argument_list|,
name|URXTIMO
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
break|break;
case|case
name|TPERSIST
case|:
comment|/* persist timer */
comment|/* force a byte send through closed window */
name|tp
operator|->
name|force_one
operator|=
name|TRUE
expr_stmt|;
comment|/* 38 */
name|send
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
block|}
return|return
operator|(
name|SAME
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|netprepr
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
comment|/* network preproc (66,67,68,69,70,71,72,73,74,75,76) */
specifier|register
expr|struct
name|tcb
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|th
modifier|*
name|n
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|NETPREPR
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
case|case
name|LISTEN
case|:
if|if
condition|(
name|n
operator|->
name|t_ack
operator|||
operator|!
name|syn_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
name|send_rst
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
operator|!
name|n
operator|->
name|t_rst
condition|)
return|return
operator|(
literal|0
operator|)
return|;
break|break;
case|case
name|SYN_SENT
case|:
if|if
condition|(
operator|!
name|ack_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
operator|||
operator|!
name|syn_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
name|send_rst
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
comment|/* 71,72,75 */
elseif|else
if|if
condition|(
name|n
operator|->
name|t_rst
condition|)
block|{
name|t_close
argument_list|(
name|tp
argument_list|,
name|URESET
argument_list|)
expr_stmt|;
comment|/* 70 */
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
else|else
return|return
operator|(
literal|0
operator|)
return|;
break|break;
default|default:
if|if
condition|(
name|n
operator|->
name|t_rst
condition|)
block|{
comment|/* any resets? */
if|if
condition|(
name|n
operator|->
name|t_seq
operator|>=
name|tp
operator|->
name|rcv_nxt
condition|)
block|{
comment|/* good reset */
if|if
condition|(
name|tp
operator|->
name|t_state
operator|==
name|L_SYN_RCVD
condition|)
block|{
if|if
condition|(
name|ack_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
block|{
comment|/* 67 */
name|t_cancel
argument_list|(
name|tp
argument_list|,
name|TREXMT
argument_list|)
expr_stmt|;
name|t_cancel
argument_list|(
name|tp
argument_list|,
name|TREXMTTL
argument_list|)
expr_stmt|;
name|t_cancel
argument_list|(
name|tp
argument_list|,
name|TPERSIST
argument_list|)
expr_stmt|;
name|h_free
argument_list|(
name|tp
operator|->
name|t_ucb
operator|->
name|uc_host
argument_list|)
expr_stmt|;
return|return
operator|(
name|LISTEN
operator|)
return|;
block|}
block|}
else|else
block|{
comment|/* 66 */
name|t_close
argument_list|(
name|tp
argument_list|,
name|URESET
argument_list|)
expr_stmt|;
return|return
operator|(
name|CLOSED
operator|)
return|;
block|}
block|}
comment|/* else 69 */
break|break;
block|}
case|case
name|SYN_RCVD
case|:
if|if
condition|(
name|ack_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
condition|)
comment|/* acceptable ack */
if|if
condition|(
name|syn_ok
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
operator|&&
name|n
operator|->
name|t_seq
operator|!=
name|tp
operator|->
name|irs
condition|)
comment|/* bad syn (73,75,76) */
name|send_null
argument_list|(
name|tp
argument_list|)
expr_stmt|;
else|else
return|return
operator|(
literal|0
operator|)
return|;
comment|/* acceptable segment */
else|else
name|send_rst
argument_list|(
name|tp
argument_list|,
name|n
argument_list|)
expr_stmt|;
comment|/* bad ack (74) */
block|}
return|return
operator|(
operator|-
literal|1
operator|)
return|;
comment|/* tell caller to eat segment (unacceptable) */
block|}
end_block

end_unit

