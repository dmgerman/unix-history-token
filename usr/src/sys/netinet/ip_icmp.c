begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	ip_icmp.c	4.12	82/01/19	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/clock.h"
end_include

begin_include
include|#
directive|include
file|"../net/in.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip_icmp.h"
end_include

begin_comment
comment|/*  * ICMP routines: error generation, receive packet processing, and  * routines to turnaround packets back to the originator, and  * host table maintenance routines.  */
end_comment

begin_comment
comment|/*  * Generate an error packet of type error in response to bad packet ip.  */
end_comment

begin_macro
name|icmp_error
argument_list|(
argument|oip
argument_list|,
argument|type
argument_list|,
argument|code
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|ip
modifier|*
name|oip
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|type
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|unsigned
name|oiplen
init|=
name|oip
operator|->
name|ip_hl
operator|<<
literal|2
decl_stmt|;
name|struct
name|icmp
modifier|*
name|icp
init|=
operator|(
expr|struct
name|icmp
operator|*
operator|)
operator|(
operator|(
name|int
operator|)
name|oip
operator|+
name|oiplen
operator|)
decl_stmt|;
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|struct
name|ip
modifier|*
name|nip
decl_stmt|;
name|COUNT
argument_list|(
name|ICMP_ERROR
argument_list|)
expr_stmt|;
comment|/* 	 * Make sure that the old IP packet had 8 bytes of data to return; 	 * if not, don't bother.  Also don't EVER error if the old 	 * packet protocol was ICMP. 	 */
if|if
condition|(
name|oip
operator|->
name|ip_len
operator|-
name|oiplen
operator|<
literal|8
operator|||
name|oip
operator|->
name|ip_p
operator|==
name|IPPROTO_ICMP
condition|)
goto|goto
name|free
goto|;
comment|/* 	 * Get a new mbuf, and fill in a ICMP header at the bottom 	 * of the mbuf, followed by the old IP header and 8 bytes 	 * of its data. 	 */
name|m
operator|=
name|m_get
argument_list|(
name|M_DONTWAIT
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
goto|goto
name|free
goto|;
name|m
operator|->
name|m_off
operator|=
name|MMAXOFF
operator|-
operator|(
name|oiplen
operator|+
literal|8
operator|)
expr_stmt|;
name|icp
operator|->
name|icmp_type
operator|=
name|type
expr_stmt|;
if|if
condition|(
name|type
operator|==
name|ICMP_PARAMPROB
condition|)
block|{
name|icp
operator|->
name|icmp_code
operator|=
literal|0
expr_stmt|;
name|icp
operator|->
name|icmp_pptr
operator|=
name|code
expr_stmt|;
block|}
else|else
name|icp
operator|->
name|icmp_code
operator|=
name|code
expr_stmt|;
name|bcopy
argument_list|(
operator|(
name|caddr_t
operator|)
name|oip
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|icp
operator|->
name|icmp_ip
argument_list|,
name|oiplen
operator|+
literal|8
argument_list|)
expr_stmt|;
comment|/* 	 * Now prepend an IP header and reflect this packet back to 	 * the source. 	 */
name|m
operator|->
name|m_off
operator|-=
sizeof|sizeof
argument_list|(
expr|struct
name|ip
argument_list|)
expr_stmt|;
name|m
operator|->
name|m_len
operator|+=
sizeof|sizeof
argument_list|(
expr|struct
name|ip
argument_list|)
expr_stmt|;
name|nip
operator|=
operator|(
expr|struct
name|ip
operator|*
operator|)
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|ip
operator|*
argument_list|)
expr_stmt|;
operator|*
name|nip
operator|=
operator|*
name|oip
expr_stmt|;
name|icmp_reflect
argument_list|(
name|nip
argument_list|)
expr_stmt|;
return|return;
comment|/* 	 * Discard mbufs of original datagram 	 */
name|free
label|:
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|oip
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Process a received ICMP message.  */
end_comment

begin_macro
name|icmp_input
argument_list|(
argument|m
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|icmp
modifier|*
name|icp
decl_stmt|;
specifier|register
name|struct
name|ip
modifier|*
name|ip
init|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|ip
operator|*
argument_list|)
decl_stmt|;
name|int
name|hlen
init|=
name|ip
operator|->
name|ip_hl
operator|<<
literal|2
decl_stmt|;
name|int
name|icmplen
init|=
name|ip
operator|->
name|ip_len
operator|-
name|hlen
decl_stmt|;
name|int
name|i
decl_stmt|;
specifier|extern
name|u_char
name|ip_protox
index|[]
decl_stmt|;
name|COUNT
argument_list|(
name|ICMP_INPUT
argument_list|)
expr_stmt|;
comment|/* 	 * Locate icmp structure in mbuf, and check 	 * that not corrupted and of at least minimum length. 	 */
name|m
operator|->
name|m_len
operator|-=
name|hlen
expr_stmt|;
name|m
operator|->
name|m_off
operator|+=
name|hlen
expr_stmt|;
comment|/* need routine to make sure header is in this mbuf here */
name|icp
operator|=
operator|(
expr|struct
name|icmp
operator|*
operator|)
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|icmp
operator|*
argument_list|)
expr_stmt|;
name|i
operator|=
name|icp
operator|->
name|icmp_cksum
expr_stmt|;
name|icp
operator|->
name|icmp_cksum
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|i
operator|!=
name|in_cksum
argument_list|(
name|m
argument_list|,
name|icmplen
argument_list|)
operator|||
name|icmplen
operator|<
name|ICMP_MINLEN
condition|)
goto|goto
name|free
goto|;
comment|/* 	 * Message type specific processing. 	 */
switch|switch
condition|(
name|icp
operator|->
name|icmp_type
condition|)
block|{
case|case
name|ICMP_UNREACH
case|:
case|case
name|ICMP_TIMXCEED
case|:
case|case
name|ICMP_PARAMPROB
case|:
case|case
name|ICMP_SOURCEQUENCH
case|:
case|case
name|ICMP_REDIRECT
case|:
comment|/* 		 * Problem with previous datagram; advise 		 * higher level routines. 		 */
if|if
condition|(
name|icmplen
operator|<
name|ICMP_ADVLENMIN
operator|||
name|icmplen
operator|<
name|ICMP_ADVLEN
argument_list|(
name|icp
argument_list|)
condition|)
goto|goto
name|free
goto|;
operator|(
operator|*
name|protosw
index|[
name|ip_protox
index|[
name|ip
operator|->
name|ip_p
index|]
index|]
operator|.
name|pr_ctlinput
operator|)
operator|(
name|m
operator|)
expr_stmt|;
goto|goto
name|free
goto|;
case|case
name|ICMP_ECHO
case|:
name|icp
operator|->
name|icmp_type
operator|=
name|ICMP_ECHOREPLY
expr_stmt|;
goto|goto
name|reflect
goto|;
case|case
name|ICMP_TSTAMP
case|:
if|if
condition|(
name|icmplen
operator|<
name|ICMP_TSLEN
condition|)
goto|goto
name|free
goto|;
name|icp
operator|->
name|icmp_type
operator|=
name|ICMP_TSTAMPREPLY
expr_stmt|;
name|icp
operator|->
name|icmp_rtime
operator|=
name|iptime
argument_list|()
expr_stmt|;
name|icp
operator|->
name|icmp_ttime
operator|=
name|icp
operator|->
name|icmp_rtime
expr_stmt|;
comment|/* bogus, do later! */
goto|goto
name|reflect
goto|;
case|case
name|ICMP_IREQ
case|:
comment|/* fill in source address zero fields! */
goto|goto
name|reflect
goto|;
case|case
name|ICMP_ECHOREPLY
case|:
case|case
name|ICMP_TSTAMPREPLY
case|:
case|case
name|ICMP_IREQREPLY
case|:
if|if
condition|(
name|icmplen
operator|<
name|ICMP_ADVLENMIN
operator|||
name|icmplen
operator|<
name|ICMP_ADVLEN
argument_list|(
name|icp
argument_list|)
condition|)
goto|goto
name|free
goto|;
name|icmp_gotreply
argument_list|(
name|icp
argument_list|)
expr_stmt|;
goto|goto
name|free
goto|;
default|default:
goto|goto
name|free
goto|;
block|}
name|reflect
label|:
name|icmp_reflect
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|free
label|:
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Reflect the ip packet back to the source  */
end_comment

begin_macro
name|icmp_reflect
argument_list|(
argument|ip
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|ip
modifier|*
name|ip
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|in_addr
name|t
decl_stmt|;
name|COUNT
argument_list|(
name|ICMP_REFLECT
argument_list|)
expr_stmt|;
name|t
operator|=
name|ip
operator|->
name|ip_src
expr_stmt|;
name|ip
operator|->
name|ip_dst
operator|=
name|ip
operator|->
name|ip_src
expr_stmt|;
name|ip
operator|->
name|ip_src
operator|=
name|t
expr_stmt|;
comment|/* 	 * This is a little naive... do we have to munge the options 	 * to reverse source routing? 	 */
name|icmp_send
argument_list|(
name|ip
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Send an icmp packet back to the ip level, after  * supplying a checksum.  */
end_comment

begin_macro
name|icmp_send
argument_list|(
argument|ip
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|ip
modifier|*
name|ip
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|ICMP_SEND
argument_list|)
expr_stmt|;
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|icmp_ctlinput
argument_list|(
argument|m
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|ICMP_CTLINPUT
argument_list|)
expr_stmt|;
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Got a reply, e.g. to an echo message or a timestamp  * message; nothing is done with these yet.  */
end_comment

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|icmp_gotreply
argument_list|(
argument|icp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|icmp
modifier|*
name|icp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|ICMP_GOTREPLY
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|icmp_drain
argument_list|()
end_macro

begin_block
block|{
name|COUNT
argument_list|(
name|ICMP_DRAIN
argument_list|)
expr_stmt|;
block|}
end_block

begin_function
name|n_time
name|iptime
parameter_list|()
block|{
name|int
name|s
init|=
name|spl6
argument_list|()
decl_stmt|;
name|u_long
name|t
decl_stmt|;
name|COUNT
argument_list|(
name|IPTIME
argument_list|)
expr_stmt|;
name|t
operator|=
operator|(
name|time
operator|%
name|SECDAY
operator|)
operator|*
literal|1000
operator|+
name|lbolt
operator|*
name|hz
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|htonl
argument_list|(
name|t
argument_list|)
operator|)
return|;
block|}
end_function

end_unit

