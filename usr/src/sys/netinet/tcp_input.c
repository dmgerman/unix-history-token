begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	tcp_input.c	1.32	81/11/26	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../net/in.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_pcb.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/if.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip_var.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_fsm.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_seq.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_timer.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_var.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcpip.h"
end_include

begin_include
include|#
directive|include
file|"/usr/include/errno.h"
end_include

begin_decl_stmt
name|int
name|tcpcksum
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr_in
name|tcp_sockaddr
init|=
block|{
name|AF_INET
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * TCP input routine, follows pages 65-76 of the  * protocol specification dated September, 1981 very closely.  */
end_comment

begin_macro
name|tcp_input
argument_list|(
argument|m0
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m0
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcpiphdr
modifier|*
name|ti
decl_stmt|;
name|struct
name|inpcb
modifier|*
name|inp
decl_stmt|;
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|int
name|len
decl_stmt|,
name|tlen
decl_stmt|,
name|off
decl_stmt|;
specifier|register
name|struct
name|tcpcb
modifier|*
name|tp
decl_stmt|;
specifier|register
name|int
name|tiflags
decl_stmt|;
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
name|int
name|acceptable
decl_stmt|;
name|tcp_seq
name|todrop
decl_stmt|,
name|acked
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_INPUT
argument_list|)
expr_stmt|;
comment|/* 	 * Get ip and tcp header together in first mbuf. 	 */
name|m
operator|=
name|m0
expr_stmt|;
name|ti
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|tcpiphdr
operator|*
argument_list|)
expr_stmt|;
if|if
condition|(
name|ti
operator|->
name|ti_len
operator|>
sizeof|sizeof
argument_list|(
expr|struct
name|ip
argument_list|)
condition|)
name|ip_stripoptions
argument_list|(
operator|(
expr|struct
name|ip
operator|*
operator|)
name|ti
argument_list|,
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|->
name|m_len
operator|<
sizeof|sizeof
argument_list|(
expr|struct
name|tcpiphdr
argument_list|)
condition|)
block|{
if|if
condition|(
name|m_pullup
argument_list|(
name|m
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|tcpiphdr
argument_list|)
argument_list|)
operator|==
literal|0
condition|)
block|{
name|tcpstat
operator|.
name|tcps_hdrops
operator|++
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
name|ti
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|tcpiphdr
operator|*
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Checksum extended tcp header and data. 	 */
name|tlen
operator|=
operator|(
operator|(
expr|struct
name|ip
operator|*
operator|)
name|ti
operator|)
operator|->
name|ip_len
expr_stmt|;
name|len
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|ip
argument_list|)
operator|+
name|tlen
expr_stmt|;
if|if
condition|(
name|tcpcksum
condition|)
block|{
name|ti
operator|->
name|ti_next
operator|=
name|ti
operator|->
name|ti_prev
operator|=
literal|0
expr_stmt|;
name|ti
operator|->
name|ti_x1
operator|=
literal|0
expr_stmt|;
name|ti
operator|->
name|ti_len
operator|=
name|htons
argument_list|(
operator|(
name|u_short
operator|)
name|tlen
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|ti
operator|->
name|ti_sum
operator|=
name|in_cksum
argument_list|(
name|m
argument_list|,
name|len
argument_list|)
operator|)
operator|!=
literal|0xffff
condition|)
block|{
name|tcpstat
operator|.
name|tcps_badsum
operator|++
expr_stmt|;
name|printf
argument_list|(
literal|"tcp cksum %x\n"
argument_list|,
name|ti
operator|->
name|ti_sum
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
block|}
comment|/* 	 * Check that tcp offset makes sense, 	 * process tcp options and adjust length. 	 */
name|off
operator|=
name|ti
operator|->
name|ti_off
operator|<<
literal|2
expr_stmt|;
if|if
condition|(
name|off
operator|<
sizeof|sizeof
argument_list|(
expr|struct
name|tcphdr
argument_list|)
operator|||
name|off
operator|>
name|ti
operator|->
name|ti_len
condition|)
block|{
name|tcpstat
operator|.
name|tcps_badoff
operator|++
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
name|ti
operator|->
name|ti_len
operator|=
name|tlen
operator|-
name|off
expr_stmt|;
if|#
directive|if
literal|0
block|if (off> sizeof (struct tcphdr)>> 2) 		tcp_options(ti);
endif|#
directive|endif
name|tiflags
operator|=
name|ti
operator|->
name|ti_flags
expr_stmt|;
comment|/* 	 * Convert tcp protocol specific fields to host format. 	 */
name|ti
operator|->
name|ti_seq
operator|=
name|ntohl
argument_list|(
name|ti
operator|->
name|ti_seq
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_ack
operator|=
name|ntohl
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_win
operator|=
name|ntohs
argument_list|(
name|ti
operator|->
name|ti_win
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_urp
operator|=
name|ntohs
argument_list|(
name|ti
operator|->
name|ti_urp
argument_list|)
expr_stmt|;
comment|/* 	 * Locate pcb for segment. 	 */
name|inp
operator|=
name|in_pcblookup
argument_list|(
operator|&
name|tcb
argument_list|,
name|ti
operator|->
name|ti_src
argument_list|,
name|ti
operator|->
name|ti_sport
argument_list|,
name|ti
operator|->
name|ti_dst
argument_list|,
name|ti
operator|->
name|ti_dport
argument_list|)
expr_stmt|;
comment|/* 	 * If the state is CLOSED (i.e., TCB does not exist) then 	 * all data in the incoming segment is discarded.  (p. 65). 	 */
if|if
condition|(
name|inp
operator|==
literal|0
condition|)
goto|goto
name|dropwithreset
goto|;
name|tp
operator|=
name|intotcpcb
argument_list|(
name|inp
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
operator|==
literal|0
condition|)
goto|goto
name|dropwithreset
goto|;
comment|/* 	 * Calculate amount of space in receive window, 	 * and then do TCP input processing. 	 */
name|tp
operator|->
name|rcv_wnd
operator|=
name|sbspace
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
comment|/* 	 * If the state is LISTEN then ignore segment if it contains an RST. 	 * If the segment contains an ACK then it is bad and send a RST. 	 * If it does not contain a SYN then it is not interesting; drop it. 	 * Otherwise initialize tp->rcv_nxt, and tp->irs, select an initial 	 * tp->iss, and send a segment: 	 *<SEQ=ISS><ACK=RCV_NXT><CTL=SYN,ACK> 	 * Also initialize tp->snd_nxt to tp->iss+1 and tp->snd_una to tp->iss. 	 * Fill in remote peer address fields if not previously specified. 	 * Enter SYN_RECEIVED state, and process any other fields of this 	 * segment in this state.  (p. 65) 	 */
case|case
name|TCPS_LISTEN
case|:
if|if
condition|(
name|tiflags
operator|&
name|TH_RST
condition|)
goto|goto
name|drop
goto|;
if|if
condition|(
name|tiflags
operator|&
name|TH_ACK
condition|)
goto|goto
name|dropwithreset
goto|;
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_SYN
operator|)
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
name|tp
operator|->
name|iss
operator|=
name|tcp_iss
expr_stmt|;
name|tcp_iss
operator|+=
name|TCP_ISSINCR
operator|/
literal|2
expr_stmt|;
name|tp
operator|->
name|irs
operator|=
name|ti
operator|->
name|ti_seq
expr_stmt|;
name|tcp_sendseqinit
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tcp_rcvseqinit
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_state
operator|=
name|TCPS_SYN_RECEIVED
expr_stmt|;
if|if
condition|(
name|inp
operator|->
name|inp_faddr
operator|.
name|s_addr
operator|==
literal|0
condition|)
block|{
name|inp
operator|->
name|inp_faddr
operator|=
name|ti
operator|->
name|ti_src
expr_stmt|;
name|inp
operator|->
name|inp_fport
operator|=
name|ti
operator|->
name|ti_sport
expr_stmt|;
block|}
goto|goto
name|trimthenstep6
goto|;
comment|/* 	 * If the state is SYN_SENT: 	 *	if seg contains an ACK, but not for our SYN, drop the input. 	 *	if seg contains a RST, then drop the connection. 	 *	if seg does not contain SYN, then drop it. 	 * Otherwise this is an acceptable SYN segment 	 *	initialize tp->rcv_nxt and tp->irs 	 *	if seg contains ack then advance tp->snd_una 	 *	if SYN has been acked change to ESTABLISHED else SYN_RCVD state 	 *	arrange for segment to be acked (eventually) 	 *	continue processing rest of data/controls, beginning with URG 	 */
case|case
name|TCPS_SYN_SENT
case|:
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_ACK
operator|)
operator|&&
operator|(
name|SEQ_LEQ
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|,
name|tp
operator|->
name|iss
argument_list|)
operator|||
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|,
name|tp
operator|->
name|snd_nxt
argument_list|)
operator|)
condition|)
goto|goto
name|dropwithreset
goto|;
if|if
condition|(
name|tiflags
operator|&
name|TH_RST
condition|)
block|{
if|if
condition|(
name|tiflags
operator|&
name|TH_ACK
condition|)
name|tcp_drop
argument_list|(
name|tp
argument_list|,
name|ECONNRESET
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_SYN
operator|)
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
name|tp
operator|->
name|iss
operator|=
name|ti
operator|->
name|ti_ack
expr_stmt|;
name|tcp_sendseqinit
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|irs
operator|=
name|ti
operator|->
name|ti_seq
expr_stmt|;
name|tcp_rcvseqinit
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_flags
operator||=
name|TF_ACKNOW
expr_stmt|;
if|if
condition|(
name|SEQ_GT
argument_list|(
name|tp
operator|->
name|snd_una
argument_list|,
name|tp
operator|->
name|iss
argument_list|)
condition|)
name|tp
operator|->
name|t_state
operator|=
name|TCPS_ESTABLISHED
expr_stmt|;
else|else
name|tp
operator|->
name|t_state
operator|=
name|TCPS_SYN_RECEIVED
expr_stmt|;
goto|goto
name|trimthenstep6
goto|;
name|trimthenstep6
label|:
comment|/* 		 * If had syn, advance ti->ti_seq to correspond 		 * to first data byte. 		 */
if|if
condition|(
name|tiflags
operator|&
name|TH_SYN
condition|)
name|ti
operator|->
name|ti_seq
operator|++
expr_stmt|;
comment|/* 		 * If data, trim to stay within window, 		 * dropping FIN if necessary. 		 */
if|if
condition|(
name|ti
operator|->
name|ti_len
operator|>
name|tp
operator|->
name|rcv_wnd
condition|)
block|{
name|todrop
operator|=
name|ti
operator|->
name|ti_len
operator|-
name|tp
operator|->
name|rcv_wnd
expr_stmt|;
name|m_adj
argument_list|(
name|m
argument_list|,
operator|-
name|todrop
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_len
operator|=
name|tp
operator|->
name|rcv_wnd
expr_stmt|;
name|ti
operator|->
name|ti_flags
operator|&=
operator|~
name|TH_FIN
expr_stmt|;
block|}
goto|goto
name|step6
goto|;
block|}
comment|/* 	 * States other than LISTEN or SYN_SENT. 	 * First check that at least some bytes of segment are within  	 * receive window. 	 */
if|if
condition|(
name|tp
operator|->
name|rcv_wnd
operator|==
literal|0
condition|)
block|{
comment|/* 		 * If window is closed can only take segments at 		 * window edge, and have to drop data and EOL from 		 * incoming segments. 		 */
if|if
condition|(
name|tp
operator|->
name|rcv_nxt
operator|!=
name|ti
operator|->
name|ti_seq
condition|)
goto|goto
name|dropafterack
goto|;
if|if
condition|(
name|ti
operator|->
name|ti_len
operator|>
literal|0
condition|)
block|{
name|ti
operator|->
name|ti_len
operator|=
literal|0
expr_stmt|;
name|ti
operator|->
name|ti_flags
operator|&=
operator|~
operator|(
name|TH_PUSH
operator||
name|TH_FIN
operator|)
expr_stmt|;
block|}
block|}
else|else
block|{
comment|/* 		 * If segment begins before rcv_next, drop leading 		 * data (and SYN); if nothing left, just ack. 		 */
if|if
condition|(
name|SEQ_GT
argument_list|(
name|tp
operator|->
name|rcv_nxt
argument_list|,
name|ti
operator|->
name|ti_seq
argument_list|)
condition|)
block|{
name|todrop
operator|=
name|tp
operator|->
name|rcv_nxt
operator|-
name|ti
operator|->
name|ti_seq
expr_stmt|;
if|if
condition|(
name|tiflags
operator|&
name|TH_SYN
condition|)
block|{
name|ti
operator|->
name|ti_seq
operator|++
expr_stmt|;
if|if
condition|(
name|ti
operator|->
name|ti_urp
operator|>
literal|1
condition|)
name|ti
operator|->
name|ti_urp
operator|--
expr_stmt|;
else|else
name|tiflags
operator|&=
operator|~
name|TH_URG
expr_stmt|;
name|todrop
operator|--
expr_stmt|;
block|}
if|if
condition|(
name|todrop
operator|>
name|ti
operator|->
name|ti_len
condition|)
goto|goto
name|dropafterack
goto|;
name|m_adj
argument_list|(
name|m
argument_list|,
name|todrop
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_seq
operator|+=
name|todrop
expr_stmt|;
name|ti
operator|->
name|ti_len
operator|-=
name|todrop
expr_stmt|;
if|if
condition|(
name|ti
operator|->
name|ti_urp
operator|>
name|todrop
condition|)
name|ti
operator|->
name|ti_urp
operator|-=
name|todrop
expr_stmt|;
else|else
block|{
name|tiflags
operator|&=
operator|~
name|TH_URG
expr_stmt|;
comment|/* ti->ti_flags&= ~TH_URG; */
comment|/* ti->ti_urp = 0; */
block|}
comment|/* tiflags&= ~TH_SYN; */
comment|/* ti->ti_flags&= ~TH_SYN; */
block|}
comment|/* 		 * If segment ends after window, drop trailing data 		 * (and PUSH and FIN); if nothing left, just ACK. 		 */
if|if
condition|(
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_seq
operator|+
name|ti
operator|->
name|ti_len
argument_list|,
name|tp
operator|->
name|rcv_nxt
operator|+
name|tp
operator|->
name|rcv_wnd
argument_list|)
condition|)
block|{
name|todrop
operator|=
name|ti
operator|->
name|ti_seq
operator|+
name|ti
operator|->
name|ti_len
operator|-
operator|(
name|tp
operator|->
name|rcv_nxt
operator|+
name|tp
operator|->
name|rcv_wnd
operator|)
expr_stmt|;
if|if
condition|(
name|todrop
operator|>
name|ti
operator|->
name|ti_len
condition|)
goto|goto
name|dropafterack
goto|;
name|m_adj
argument_list|(
name|m
argument_list|,
operator|-
name|todrop
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_len
operator|-=
name|todrop
expr_stmt|;
name|ti
operator|->
name|ti_flags
operator|&=
operator|~
operator|(
name|TH_PUSH
operator||
name|TH_FIN
operator|)
expr_stmt|;
block|}
block|}
comment|/* 	 * If the RST bit is set examine the state: 	 *    SYN_RECEIVED STATE: 	 *	If passive open, return to LISTEN state. 	 *	If active open, inform user that connection was refused. 	 *    ESTABLISHED, FIN_WAIT_1, FIN_WAIT2, CLOSE_WAIT STATES: 	 *	Inform user that connection was reset, and close tcb. 	 *    CLOSING, LAST_ACK, TIME_WAIT STATES 	 *	Close the tcb. 	 */
if|if
condition|(
name|tiflags
operator|&
name|TH_RST
condition|)
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
case|case
name|TCPS_SYN_RECEIVED
case|:
if|if
condition|(
name|inp
operator|->
name|inp_socket
operator|->
name|so_options
operator|&
name|SO_ACCEPTCONN
condition|)
block|{
name|tp
operator|->
name|t_state
operator|=
name|TCPS_LISTEN
expr_stmt|;
name|inp
operator|->
name|inp_faddr
operator|.
name|s_addr
operator|=
literal|0
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
name|tcp_drop
argument_list|(
name|tp
argument_list|,
name|ECONNREFUSED
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
case|case
name|TCPS_ESTABLISHED
case|:
case|case
name|TCPS_FIN_WAIT_1
case|:
case|case
name|TCPS_FIN_WAIT_2
case|:
case|case
name|TCPS_CLOSE_WAIT
case|:
name|tcp_drop
argument_list|(
name|tp
argument_list|,
name|ECONNRESET
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
case|case
name|TCPS_CLOSING
case|:
case|case
name|TCPS_LAST_ACK
case|:
case|case
name|TCPS_TIME_WAIT
case|:
name|tcp_close
argument_list|(
name|tp
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
block|}
comment|/* 	 * If a SYN is in the window, then this is an 	 * error and we send an RST and drop the connection. 	 */
if|if
condition|(
name|tiflags
operator|&
name|TH_SYN
condition|)
block|{
name|tcp_drop
argument_list|(
name|tp
argument_list|,
name|ECONNABORTED
argument_list|)
expr_stmt|;
goto|goto
name|dropwithreset
goto|;
block|}
comment|/* 	 * If the ACK bit is off we drop the segment and return. 	 */
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_ACK
operator|)
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
comment|/* 	 * Ack processing. 	 */
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
comment|/* 	 * In SYN_RECEIVED state if the ack ACKs our SYN then enter 	 * ESTABLISHED state and continue processing, othewise 	 * send an RST. 	 */
case|case
name|TCPS_SYN_RECEIVED
case|:
if|if
condition|(
name|SEQ_GT
argument_list|(
name|tp
operator|->
name|snd_una
argument_list|,
name|ti
operator|->
name|ti_ack
argument_list|)
operator|||
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|,
name|tp
operator|->
name|snd_nxt
argument_list|)
condition|)
goto|goto
name|dropwithreset
goto|;
name|soisconnected
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_state
operator|=
name|TCPS_ESTABLISHED
expr_stmt|;
comment|/* fall into ... */
comment|/* 	 * In ESTABLISHED state: drop duplicate ACKs; ACK out of range 	 * ACKs.  If the ack is in the range 	 *	tp->snd_una< ti->ti_ack<= tp->snd_nxt 	 * then advance tp->snd_una to ti->ti_ack and drop 	 * data from the retransmission queue.  If this ACK reflects 	 * more up to date window information we update our window information. 	 */
case|case
name|TCPS_ESTABLISHED
case|:
case|case
name|TCPS_FIN_WAIT_1
case|:
case|case
name|TCPS_FIN_WAIT_2
case|:
case|case
name|TCPS_CLOSE_WAIT
case|:
case|case
name|TCPS_CLOSING
case|:
define|#
directive|define
name|ourfinisacked
value|(acked> 0)
if|if
condition|(
name|SEQ_LT
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|,
name|tp
operator|->
name|snd_una
argument_list|)
condition|)
break|break;
if|if
condition|(
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_ack
argument_list|,
name|tp
operator|->
name|snd_nxt
argument_list|)
condition|)
goto|goto
name|dropafterack
goto|;
name|acked
operator|=
name|ti
operator|->
name|ti_ack
operator|-
name|tp
operator|->
name|snd_una
expr_stmt|;
if|if
condition|(
name|acked
operator|>
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
condition|)
block|{
name|sbflush
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|)
expr_stmt|;
name|acked
operator|-=
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
expr_stmt|;
block|}
else|else
block|{
name|sbdrop
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
name|acked
argument_list|)
expr_stmt|;
name|acked
operator|=
literal|0
expr_stmt|;
block|}
comment|/* if acked our FIN is acked */
name|tp
operator|->
name|snd_una
operator|=
name|ti
operator|->
name|ti_ack
expr_stmt|;
comment|/* 		 * Update window information. 		 */
if|if
condition|(
name|SEQ_LT
argument_list|(
name|tp
operator|->
name|snd_wl1
argument_list|,
name|ti
operator|->
name|ti_seq
argument_list|)
operator|||
name|tp
operator|->
name|snd_wl1
operator|==
name|ti
operator|->
name|ti_seq
operator|&&
name|SEQ_LEQ
argument_list|(
name|tp
operator|->
name|snd_wl2
argument_list|,
name|ti
operator|->
name|ti_seq
argument_list|)
condition|)
block|{
name|tp
operator|->
name|snd_wnd
operator|=
name|ti
operator|->
name|ti_win
expr_stmt|;
name|tp
operator|->
name|snd_wl1
operator|=
name|ti
operator|->
name|ti_seq
expr_stmt|;
name|tp
operator|->
name|snd_wl2
operator|=
name|ti
operator|->
name|ti_ack
expr_stmt|;
block|}
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
comment|/* 		 * In FIN_WAIT_1 STATE in addition to the processing 		 * for the ESTABLISHED state if our FIN is now acknowledged 		 * then enter FIN_WAIT_2. 		 */
case|case
name|TCPS_FIN_WAIT_1
case|:
if|if
condition|(
name|ourfinisacked
condition|)
name|tp
operator|->
name|t_state
operator|=
name|TCPS_FIN_WAIT_2
expr_stmt|;
break|break;
comment|/* 		 * In CLOSING STATE in addition to the processing for 		 * the ESTABLISHED state if the ACK acknowledges our FIN 		 * then enter the TIME-WAIT state, otherwise ignore 		 * the segment. 		 */
case|case
name|TCPS_CLOSING
case|:
if|if
condition|(
name|ourfinisacked
condition|)
name|tp
operator|->
name|t_state
operator|=
name|TCPS_TIME_WAIT
expr_stmt|;
goto|goto
name|drop
goto|;
comment|/* 		 * The only thing that can arrive in  LAST_ACK state 		 * is an acknowledgment of our FIN.  If our FIN is now 		 * acknowledged, delete the TCB, enter the closed state 		 * and return. 		 */
case|case
name|TCPS_LAST_ACK
case|:
if|if
condition|(
name|ourfinisacked
condition|)
name|tcp_close
argument_list|(
name|tp
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
comment|/* 		 * In TIME_WAIT state the only thing that should arrive 		 * is a retransmission of the remote FIN.  Acknowledge 		 * it and restart the finack timer. 		 */
case|case
name|TCPS_TIME_WAIT
case|:
name|tp
operator|->
name|t_timer
index|[
name|TCPT_2MSL
index|]
operator|=
literal|2
operator|*
name|TCPSC_MSL
expr_stmt|;
goto|goto
name|dropafterack
goto|;
block|}
undef|#
directive|undef
name|ourfinisacked
block|}
name|step6
label|:
comment|/* 	 * If an URG bit is set in the segment and is greater than the 	 * current known urgent pointer, then signal the user that the 	 * remote side has urgent data.  This should not happen 	 * in CLOSE_WAIT, CLOSING, LAST-ACK or TIME_WAIT STATES since 	 * a FIN has been received from the remote side.  In these states 	 * we ignore the URG. 	 */
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_URG
operator|)
operator|==
literal|0
operator|&&
name|TCPS_HAVERCVDFIN
argument_list|(
name|tp
operator|->
name|t_state
argument_list|)
operator|==
literal|0
condition|)
if|if
condition|(
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_urp
argument_list|,
name|tp
operator|->
name|rcv_up
argument_list|)
condition|)
block|{
name|tp
operator|->
name|rcv_up
operator|=
name|ti
operator|->
name|ti_urp
expr_stmt|;
if|#
directive|if
literal|0
block|soisurgendata(so);
comment|/* XXX */
endif|#
directive|endif
block|}
comment|/* 	 * Process the segment text, merging it into the TCP sequencing queue, 	 * and arranging for acknowledgment of receipt if necessary. 	 * This process logically involves adjusting tp->rcv_wnd as data 	 * is presented to the user (this happens in tcp_usrreq.c, 	 * case PRU_RCVD).  If a FIN has already been received on this 	 * connection then we just ignore the text. 	 */
if|if
condition|(
name|ti
operator|->
name|ti_len
condition|)
block|{
if|if
condition|(
name|TCPS_HAVERCVDFIN
argument_list|(
name|tp
operator|->
name|t_state
argument_list|)
condition|)
goto|goto
name|drop
goto|;
name|off
operator|+=
sizeof|sizeof
argument_list|(
expr|struct
name|ip
argument_list|)
expr_stmt|;
comment|/* drop IP header */
name|m
operator|->
name|m_off
operator|+=
name|off
expr_stmt|;
name|m
operator|->
name|m_len
operator|-=
name|off
expr_stmt|;
name|tiflags
operator|=
name|tcp_reass
argument_list|(
name|tp
argument_list|,
name|ti
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_flags
operator||=
name|TF_ACKNOW
expr_stmt|;
comment|/* XXX TF_DELACK */
block|}
else|else
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
comment|/* 	 * If FIN is received then if we haven't received SYN and 	 * therefore can't validate drop the segment.  Otherwise ACK 	 * the FIN and let the user know that the connection is closing. 	 */
if|if
condition|(
operator|(
name|tiflags
operator|&
name|TH_FIN
operator|)
condition|)
block|{
if|if
condition|(
name|TCPS_HAVERCVDSYN
argument_list|(
name|tp
operator|->
name|t_state
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|drop
goto|;
name|socantrcvmore
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_flags
operator||=
name|TF_ACKNOW
expr_stmt|;
name|tp
operator|->
name|rcv_nxt
operator|++
expr_stmt|;
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
comment|/* 		 * In SYN_RECEIVED and ESTABLISHED STATES 		 * enter the CLOSE_WAIT state. 		 */
case|case
name|TCPS_SYN_RECEIVED
case|:
case|case
name|TCPS_ESTABLISHED
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_CLOSE_WAIT
expr_stmt|;
break|break;
comment|/* 		 * If still in FIN_WAIT_1 STATE FIN has not been acked so 		 * enter the CLOSING state. 		 */
case|case
name|TCPS_FIN_WAIT_1
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_CLOSING
expr_stmt|;
break|break;
comment|/* 		 * In FIN_WAIT_2 state enter the TIME_WAIT state, 		 * starting the time-wait timer, turning off the other  		 * standard timers. 		 */
case|case
name|TCPS_FIN_WAIT_2
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_TIME_WAIT
expr_stmt|;
empty_stmt|;
name|tcp_canceltimers
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_timer
index|[
name|TCPT_2MSL
index|]
operator|=
name|TCPSC_2MSL
expr_stmt|;
break|break;
comment|/* 		 * In TIME_WAIT state restart the 2 MSL time_wait timer. 		 */
case|case
name|TCPS_TIME_WAIT
case|:
name|tp
operator|->
name|t_timer
index|[
name|TCPT_2MSL
index|]
operator|=
name|TCPSC_2MSL
expr_stmt|;
break|break;
block|}
block|}
comment|/* 	 * Return any desired output. 	 */
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
return|return;
name|dropafterack
label|:
comment|/* 	 * Generate an ACK, then drop incoming segment. 	 * Make ACK reflect our state. 	 */
if|if
condition|(
name|tiflags
operator|&
name|TH_RST
condition|)
goto|goto
name|drop
goto|;
name|tcp_respond
argument_list|(
name|ti
argument_list|,
name|tp
operator|->
name|rcv_nxt
argument_list|,
name|tp
operator|->
name|snd_nxt
argument_list|,
name|TH_ACK
argument_list|)
expr_stmt|;
goto|goto
name|drop
goto|;
name|dropwithreset
label|:
comment|/* 	 * Generate a RST, then drop incoming segment. 	 * Make ACK acceptable to originator of segment. 	 */
if|if
condition|(
name|tiflags
operator|&
name|TH_RST
condition|)
goto|goto
name|drop
goto|;
if|if
condition|(
name|tiflags
operator|&
name|TH_ACK
condition|)
name|tcp_respond
argument_list|(
name|ti
argument_list|,
literal|0
argument_list|,
name|ti
operator|->
name|ti_ack
argument_list|,
name|TH_RST
argument_list|)
expr_stmt|;
else|else
block|{
if|if
condition|(
name|tiflags
operator|&
name|TH_SYN
condition|)
name|ti
operator|->
name|ti_len
operator|++
expr_stmt|;
name|tcp_respond
argument_list|(
name|ti
argument_list|,
name|ti
operator|->
name|ti_seq
operator|+
name|ti
operator|->
name|ti_len
argument_list|,
literal|0
argument_list|,
name|TH_RST
operator||
name|TH_ACK
argument_list|)
expr_stmt|;
block|}
goto|goto
name|drop
goto|;
name|drop
label|:
comment|/* 	 * Drop space held by incoming segment and return. 	 */
name|m_freem
argument_list|(
name|m
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Insert segment ti into reassembly queue of tcp with  * control block tp.  Return TH_FIN if reassembly now includes  * a segment with FIN.  */
end_comment

begin_expr_stmt
name|tcp_reass
argument_list|(
name|tp
argument_list|,
name|ti
argument_list|,
name|endp
argument_list|)
specifier|register
expr|struct
name|tcpcb
operator|*
name|tp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|tcpiphdr
modifier|*
name|ti
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|endp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcpiphdr
modifier|*
name|q
decl_stmt|;
name|struct
name|socket
modifier|*
name|so
init|=
name|tp
operator|->
name|t_inpcb
operator|->
name|inp_socket
decl_stmt|;
name|int
name|flags
init|=
literal|0
decl_stmt|;
comment|/* no FIN */
name|int
name|overage
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_REASS
argument_list|)
expr_stmt|;
comment|/* 	 * If no data in this segment may want 	 * to move data up to socket structure (if 	 * connection is now established). 	 */
if|if
condition|(
name|ti
operator|->
name|ti_len
operator|==
literal|0
condition|)
block|{
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|ti
argument_list|)
argument_list|)
expr_stmt|;
goto|goto
name|present
goto|;
block|}
comment|/* 	 * Find a segment which begins after this one does. 	 */
for|for
control|(
name|q
operator|=
name|tp
operator|->
name|seg_next
init|;
name|q
operator|!=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|tp
condition|;
name|q
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|q
operator|->
name|ti_next
control|)
if|if
condition|(
name|SEQ_GT
argument_list|(
name|q
operator|->
name|ti_seq
argument_list|,
name|ti
operator|->
name|ti_seq
argument_list|)
condition|)
break|break;
comment|/* 	 * If there is a preceding segment, it may provide some of 	 * our data already.  If so, drop the data from the incoming 	 * segment.  If it provides all of our data, drop us. 	 */
if|if
condition|(
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|q
operator|->
name|ti_prev
operator|!=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|tp
condition|)
block|{
specifier|register
name|int
name|i
decl_stmt|;
name|q
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
operator|(
name|q
operator|->
name|ti_prev
operator|)
expr_stmt|;
comment|/* conversion to int (in i) handles seq wraparound */
name|i
operator|=
name|q
operator|->
name|ti_seq
operator|+
name|q
operator|->
name|ti_len
operator|-
name|ti
operator|->
name|ti_seq
expr_stmt|;
if|if
condition|(
name|i
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|i
operator|>=
name|ti
operator|->
name|ti_len
condition|)
goto|goto
name|drop
goto|;
name|m_adj
argument_list|(
name|dtom
argument_list|(
name|tp
argument_list|)
argument_list|,
name|i
argument_list|)
expr_stmt|;
name|ti
operator|->
name|ti_len
operator|-=
name|i
expr_stmt|;
name|ti
operator|->
name|ti_seq
operator|+=
name|i
expr_stmt|;
block|}
name|q
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
operator|(
name|q
operator|->
name|ti_next
operator|)
expr_stmt|;
block|}
comment|/* 	 * While we overlap succeeding segments trim them or, 	 * if they are completely covered, dequeue them. 	 */
while|while
condition|(
name|q
operator|!=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|tp
operator|&&
name|SEQ_GT
argument_list|(
name|ti
operator|->
name|ti_seq
operator|+
name|ti
operator|->
name|ti_len
argument_list|,
name|q
operator|->
name|ti_seq
argument_list|)
condition|)
block|{
specifier|register
name|int
name|i
init|=
operator|(
name|ti
operator|->
name|ti_seq
operator|+
name|ti
operator|->
name|ti_len
operator|)
operator|-
name|q
operator|->
name|ti_seq
decl_stmt|;
if|if
condition|(
name|i
operator|<
name|q
operator|->
name|ti_len
condition|)
block|{
name|q
operator|->
name|ti_len
operator|-=
name|i
expr_stmt|;
name|m_adj
argument_list|(
name|dtom
argument_list|(
name|q
argument_list|)
argument_list|,
name|i
argument_list|)
expr_stmt|;
break|break;
block|}
name|q
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|q
operator|->
name|ti_next
expr_stmt|;
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|q
operator|->
name|ti_prev
argument_list|)
argument_list|)
expr_stmt|;
name|remque
argument_list|(
name|q
operator|->
name|ti_prev
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Stick new segment in its place. 	 */
name|insque
argument_list|(
name|ti
argument_list|,
name|q
operator|->
name|ti_prev
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_seqcnt
operator|+=
name|ti
operator|->
name|ti_len
expr_stmt|;
comment|/* 	 * Calculate available space and discard segments for 	 * which there is too much. 	 */
name|overage
operator|=
operator|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|+
name|tp
operator|->
name|t_seqcnt
operator|)
operator|-
name|so
operator|->
name|so_rcv
operator|.
name|sb_hiwat
expr_stmt|;
if|if
condition|(
name|overage
operator|>
literal|0
condition|)
block|{
name|q
operator|=
name|tp
operator|->
name|seg_prev
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
specifier|register
name|int
name|i
init|=
name|MIN
argument_list|(
name|q
operator|->
name|ti_len
argument_list|,
name|overage
argument_list|)
decl_stmt|;
name|overage
operator|-=
name|i
expr_stmt|;
name|tp
operator|->
name|t_seqcnt
operator|-=
name|i
expr_stmt|;
name|q
operator|->
name|ti_len
operator|-=
name|i
expr_stmt|;
name|m_adj
argument_list|(
name|dtom
argument_list|(
name|q
argument_list|)
argument_list|,
operator|-
name|i
argument_list|)
expr_stmt|;
if|if
condition|(
name|q
operator|->
name|ti_len
condition|)
break|break;
if|if
condition|(
name|q
operator|==
name|ti
condition|)
name|panic
argument_list|(
literal|"tcp_reass dropall"
argument_list|)
expr_stmt|;
name|q
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|q
operator|->
name|ti_prev
expr_stmt|;
name|remque
argument_list|(
name|q
operator|->
name|ti_next
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* 	 * Advance rcv_next through newly completed sequence space. 	 */
while|while
condition|(
name|ti
operator|->
name|ti_seq
operator|==
name|tp
operator|->
name|rcv_nxt
condition|)
block|{
name|tp
operator|->
name|rcv_nxt
operator|+=
name|ti
operator|->
name|ti_len
expr_stmt|;
name|flags
operator|=
name|ti
operator|->
name|ti_flags
operator|&
name|TH_FIN
expr_stmt|;
name|ti
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|ti
operator|->
name|ti_next
expr_stmt|;
if|if
condition|(
name|ti
operator|==
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|tp
condition|)
break|break;
block|}
name|present
label|:
comment|/* 	 * Present data to user. 	 */
if|if
condition|(
name|tp
operator|->
name|t_state
operator|<
name|TCPS_ESTABLISHED
condition|)
return|return
operator|(
name|flags
operator|)
return|;
name|ti
operator|=
name|tp
operator|->
name|seg_next
expr_stmt|;
while|while
condition|(
name|ti
operator|!=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|tp
operator|&&
name|ti
operator|->
name|ti_seq
operator|<
name|tp
operator|->
name|rcv_nxt
condition|)
block|{
name|remque
argument_list|(
name|ti
argument_list|)
expr_stmt|;
name|sbappend
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|,
name|dtom
argument_list|(
name|ti
argument_list|)
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_seqcnt
operator|-=
name|ti
operator|->
name|ti_len
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_seqcnt
operator|<
literal|0
condition|)
name|panic
argument_list|(
literal|"tcp_reass"
argument_list|)
expr_stmt|;
name|ti
operator|=
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
name|ti
operator|->
name|ti_next
expr_stmt|;
block|}
if|if
condition|(
name|so
operator|->
name|so_state
operator|&
name|SS_CANTRCVMORE
condition|)
name|sbflush
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|)
expr_stmt|;
else|else
name|sorwakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
return|return
operator|(
name|flags
operator|)
return|;
name|drop
label|:
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|ti
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|flags
operator|)
return|;
block|}
end_block

end_unit

