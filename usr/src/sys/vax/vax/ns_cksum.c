begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	ns_cksum.c	6.1	85/05/30	*/
end_comment

begin_include
include|#
directive|include
file|"types.h"
end_include

begin_include
include|#
directive|include
file|"mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../netns/ns.h"
end_include

begin_comment
comment|/*  * Checksum routine for Network Systems Protocol Packets (VAX Version).  *  * This routine is very heavily used in the network  * code and should be modified for each CPU to be as fast as possible.  */
end_comment

begin_function
name|u_short
name|ns_cksum
parameter_list|(
name|m
parameter_list|,
name|len
parameter_list|)
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
specifier|register
name|int
name|len
decl_stmt|;
block|{
specifier|register
name|u_short
modifier|*
name|w
decl_stmt|;
comment|/* on vax, known to be r9 */
specifier|register
name|int
name|sum
init|=
literal|0
decl_stmt|;
comment|/* on vax, known to be r8 */
specifier|register
name|int
name|low
init|=
literal|0
decl_stmt|;
comment|/* on vax, known to be r7 */
specifier|register
name|int
name|mlen
init|=
literal|0
decl_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
comment|/* 		 * Each trip around loop adds in 		 * word from one mbuf segment. 		 */
name|w
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
name|u_short
operator|*
argument_list|)
expr_stmt|;
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* 			 * There is a byte left from the last segment; 			 * add it into the checksum.  Don't have to worry 			 * about a carry-out here because although we may do 			 * the 16th and 17th additions, the contribution 			 * of this byte and the previous cannot cause 			 * more than 1 carry into the high order 16 bits. 			 */
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
operator|<<
literal|8
expr_stmt|;
name|sum
operator|+=
name|sum
expr_stmt|;
name|w
operator|=
operator|(
name|u_short
operator|*
operator|)
operator|(
operator|(
name|char
operator|*
operator|)
name|w
operator|+
literal|1
operator|)
expr_stmt|;
name|mlen
operator|=
name|m
operator|->
name|m_len
operator|-
literal|1
expr_stmt|;
name|len
operator|--
expr_stmt|;
block|}
else|else
name|mlen
operator|=
name|m
operator|->
name|m_len
expr_stmt|;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
if|if
condition|(
name|len
operator|<
name|mlen
condition|)
name|mlen
operator|=
name|len
expr_stmt|;
name|len
operator|-=
name|mlen
expr_stmt|;
comment|/* 		 * This loop is unrolled to make overhead from 		 * branches&c small. 		 * 		 * We can do a 16 bit ones complement sum 32 bits at a time 		 * by using regular arithmetic for 16 additions, then 		 * folding the carries in.  Each addition can generate 		 * no more than one carry. 		 * 		 */
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|32
operator|)
operator|>=
literal|0
condition|)
block|{
undef|#
directive|undef
name|ADD
define|#
directive|define
name|ADD
value|asm("movw (r9)+,r7")asm("addl2 r7,r8")asm("addl2 r8,r8")
define|#
directive|define
name|FOLD
value|{ asm("ashl $-16,r8,r0")asm(" addw2 r0,r8"); \ 		  asm("adwc $0,r8")asm(" movzwl r8,r8"); }
name|FOLD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|FOLD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
block|}
name|mlen
operator|+=
literal|32
expr_stmt|;
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|16
operator|)
operator|>=
literal|0
condition|)
block|{
name|FOLD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
block|}
name|mlen
operator|+=
literal|16
expr_stmt|;
comment|/* 		 * Now eliminate the possibility of carry-out's by 		 * folding back to a 16 bit number (adding high and 		 * low parts together.)  Then mop up trailing words 		 * and maybe an odd byte. There can be at most 7 		 * words added in this loop, or 14 carries. 		 */
name|FOLD
expr_stmt|;
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|2
operator|)
operator|>=
literal|0
condition|)
block|{
name|ADD
expr_stmt|;
block|}
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
expr_stmt|;
block|}
if|if
condition|(
name|len
operator|==
literal|0
condition|)
break|break;
comment|/* 		 * Locate the next block with some data. 		 * If there is a word split across a boundary we 		 * will wrap to the top with mlen == -1 and 		 * then add it in shifted appropriately. 		 */
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|m
operator|==
literal|0
condition|)
block|{
name|printf
argument_list|(
literal|"cksum: out of data\n"
argument_list|)
expr_stmt|;
goto|goto
name|done
goto|;
block|}
if|if
condition|(
name|m
operator|->
name|m_len
condition|)
break|break;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
block|}
block|}
name|done
label|:
comment|/* 	 * Add together high and low parts of sum 	 * and carry to get cksum. 	 * Have to be careful to not drop the last 	 * carry here. 	 */
name|FOLD
expr_stmt|;
if|if
condition|(
name|sum
operator|==
literal|0xffff
condition|)
name|sum
operator|=
literal|0
expr_stmt|;
return|return
operator|(
operator|(
name|u_short
operator|)
name|sum
operator|)
return|;
block|}
end_function

end_unit

