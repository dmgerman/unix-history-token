begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	in_cksum.c	6.1	83/07/29	*/
end_comment

begin_include
include|#
directive|include
file|"../h/types.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../netinet/in.h"
end_include

begin_include
include|#
directive|include
file|"../netinet/in_systm.h"
end_include

begin_if
if|#
directive|if
name|defined
argument_list|(
name|tahoe
argument_list|)
end_if

begin_comment
comment|/*  * Checksum routine for Internet Protocol family headers.  *  * This routine is very heavily used in the network  * code and should be modified for each CPU to be as fast as possible.  *   * This implementation is TAHOE version.  */
end_comment

begin_undef
undef|#
directive|undef
name|ADDCARRY
end_undef

begin_define
define|#
directive|define
name|ADDCARRY
parameter_list|(
name|sum
parameter_list|)
value|{				\ 			if (sum& 0xffff0000) {		\ 				sum&= 0xffff;		\ 				sum++;			\ 			}				\ 		}
end_define

begin_expr_stmt
name|in_cksum
argument_list|(
name|m
argument_list|,
name|len
argument_list|)
specifier|register
expr|struct
name|mbuf
operator|*
name|m
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|len
decl_stmt|;
end_decl_stmt

begin_block
block|{
union|union
name|word
block|{
name|char
name|c
index|[
literal|2
index|]
decl_stmt|;
name|u_short
name|s
decl_stmt|;
block|}
name|u
union|;
specifier|register
name|u_short
modifier|*
name|w
decl_stmt|;
specifier|register
name|int
name|sum
init|=
literal|0
decl_stmt|;
specifier|register
name|int
name|mlen
init|=
literal|0
decl_stmt|;
for|for
control|(
init|;
name|m
operator|&&
name|len
condition|;
name|m
operator|=
name|m
operator|->
name|m_next
control|)
block|{
if|if
condition|(
name|m
operator|->
name|m_len
operator|==
literal|0
condition|)
block|{
continue|continue;
block|}
name|w
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
name|u_short
operator|*
argument_list|)
expr_stmt|;
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* 			 * The first byte of this mbuf is the continuation 			 * of a word spanning between this mbuf and the 			 * last mbuf. 			 */
comment|/* u.c[0] is already saved when scanning previous  			 * mbuf. 			 */
name|u
operator|.
name|c
index|[
literal|1
index|]
operator|=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
expr_stmt|;
name|sum
operator|+=
name|u
operator|.
name|s
expr_stmt|;
name|ADDCARRY
argument_list|(
name|sum
argument_list|)
expr_stmt|;
name|w
operator|=
operator|(
name|u_short
operator|*
operator|)
operator|(
operator|(
name|char
operator|*
operator|)
name|w
operator|+
literal|1
operator|)
expr_stmt|;
name|mlen
operator|=
name|m
operator|->
name|m_len
operator|-
literal|1
expr_stmt|;
name|len
operator|--
expr_stmt|;
block|}
else|else
name|mlen
operator|=
name|m
operator|->
name|m_len
expr_stmt|;
if|if
condition|(
name|len
operator|<
name|mlen
condition|)
name|mlen
operator|=
name|len
expr_stmt|;
name|len
operator|-=
name|mlen
expr_stmt|;
comment|/* 		 * add by words. 		 */
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|2
operator|)
operator|>=
literal|0
condition|)
block|{
if|if
condition|(
operator|(
name|int
operator|)
name|w
operator|&
literal|0x1
condition|)
block|{
comment|/* word is not aligned */
name|u
operator|.
name|c
index|[
literal|0
index|]
operator|=
operator|*
operator|(
name|char
operator|*
operator|)
name|w
expr_stmt|;
name|u
operator|.
name|c
index|[
literal|1
index|]
operator|=
operator|*
operator|(
operator|(
name|char
operator|*
operator|)
name|w
operator|+
literal|1
operator|)
expr_stmt|;
name|sum
operator|+=
name|u
operator|.
name|s
expr_stmt|;
name|w
operator|++
expr_stmt|;
block|}
else|else
block|{
name|sum
operator|+=
operator|*
name|w
operator|++
expr_stmt|;
block|}
name|ADDCARRY
argument_list|(
name|sum
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* 			 * This mbuf has odd number of bytes.  			 * There could be a word split betwen 			 * this mbuf and the next mbuf. 			 * Save the last byte (to prepend to next mbuf). 			 */
name|u
operator|.
name|c
index|[
literal|0
index|]
operator|=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
expr_stmt|;
block|}
block|}
if|if
condition|(
name|len
condition|)
name|printf
argument_list|(
literal|"cksum: out of data\n"
argument_list|)
expr_stmt|;
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* The last mbuf has odd # of bytes. Follow the 		   standard (the odd byte is shifted left by 8 bits) */
name|u
operator|.
name|c
index|[
literal|1
index|]
operator|=
literal|0
expr_stmt|;
name|sum
operator|+=
name|u
operator|.
name|s
expr_stmt|;
name|ADDCARRY
argument_list|(
name|sum
argument_list|)
expr_stmt|;
block|}
name|done
label|:
return|return
operator|(
operator|~
name|sum
operator|&
literal|0xffff
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|vax
argument_list|)
end_if

begin_comment
comment|/*  * Checksum routine for Internet Protocol family headers (VAX Version).  *  * This routine is very heavily used in the network  * code and should be modified for each CPU to be as fast as possible.  */
end_comment

begin_expr_stmt
name|in_cksum
argument_list|(
name|m
argument_list|,
name|len
argument_list|)
specifier|register
expr|struct
name|mbuf
operator|*
name|m
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|len
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|u_short
modifier|*
name|w
decl_stmt|;
comment|/* on Tahoe, known to be r10 */
specifier|register
name|int
name|sum
init|=
literal|0
decl_stmt|;
comment|/* on Tahoe, known to be r9 */
specifier|register
name|int
name|mlen
init|=
literal|0
decl_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
comment|/* 		 * Each trip around loop adds in 		 * word from one mbuf segment. 		 */
name|w
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
name|u_short
operator|*
argument_list|)
expr_stmt|;
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* 			 * There is a byte left from the last segment; 			 * add it into the checksum.  Don't have to worry 			 * about a carry-out here because we make sure 			 * that high part of (32 bit) sum is small below. 			 */
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
operator|<<
literal|8
expr_stmt|;
name|w
operator|=
operator|(
name|u_short
operator|*
operator|)
operator|(
operator|(
name|char
operator|*
operator|)
name|w
operator|+
literal|1
operator|)
expr_stmt|;
name|mlen
operator|=
name|m
operator|->
name|m_len
operator|-
literal|1
expr_stmt|;
name|len
operator|--
expr_stmt|;
block|}
else|else
name|mlen
operator|=
name|m
operator|->
name|m_len
expr_stmt|;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
if|if
condition|(
name|len
operator|<
name|mlen
condition|)
name|mlen
operator|=
name|len
expr_stmt|;
name|len
operator|-=
name|mlen
expr_stmt|;
comment|/* 		 * Force to long boundary so we do longword aligned 		 * memory operations.   		 */
if|if
condition|(
operator|(
operator|(
name|int
operator|)
name|w
operator|&
literal|0x1
operator|)
operator|&&
name|mlen
operator|>=
literal|1
condition|)
block|{
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
operator|++
expr_stmt|;
name|mlen
operator|-=
literal|1
expr_stmt|;
block|}
if|if
condition|(
operator|(
operator|(
name|int
operator|)
name|w
operator|&
literal|0x2
operator|)
operator|&&
name|mlen
operator|>=
literal|2
condition|)
block|{
name|sum
operator|+=
operator|*
name|w
operator|++
expr_stmt|;
name|mlen
operator|-=
literal|2
expr_stmt|;
block|}
comment|/* 		 * Do as much of the checksum as possible 32 bits at at time. 		 * In fact, this loop is unrolled to make overhead from 		 * branches&c small. 		 * 		 * We can do a 16 bit ones complement sum 32 bits at a time 		 * because the 32 bit register is acting as two 16 bit 		 * registers for adding, with carries from the low added 		 * into the high (by normal carry-chaining) and carries 		 * from the high carried into the low on the next word 		 * by use of the adwc instruction.  This lets us run 		 * this loop at almost memory speed. 		 * 		 * Here there is the danger of high order carry out, and 		 * we carefully use adwc. 		 */
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|32
operator|)
operator|>=
literal|0
condition|)
block|{
undef|#
directive|undef
name|ADD
asm|asm("clrl r0");
comment|/* clears carry */
define|#
directive|define
name|ADD
value|asm("adwc (r10),r9;addl2 $4,r10; ");
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
asm|asm("adwc $0,r9");
block|}
name|mlen
operator|+=
literal|32
expr_stmt|;
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|8
operator|)
operator|>=
literal|0
condition|)
block|{
asm|asm("clrl r0");
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
asm|asm("adwc $0,r9");
block|}
name|mlen
operator|+=
literal|8
expr_stmt|;
comment|/* 		 * Now eliminate the possibility of carry-out's by 		 * folding back to a 16 bit number (adding high and 		 * low parts together.)  Then mop up trailing words 		 * and maybe an odd byte. 		 */
block|{
asm|asm("shal $-16,r9,r0; addw2 r0,r9");
asm|asm("adwc $0,r9; movzwl r9,r9");
block|}
while|while
condition|(
operator|(
name|mlen
operator|-=
literal|2
operator|)
operator|>=
literal|0
condition|)
block|{
asm|asm("movzwl (r10),r0;addl2 $4,r10;  addl2 r0,r9");
block|}
if|if
condition|(
name|mlen
operator|==
operator|-
literal|1
condition|)
block|{
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
expr_stmt|;
block|}
if|if
condition|(
name|len
operator|==
literal|0
condition|)
break|break;
comment|/* 		 * Locate the next block with some data. 		 * If there is a word split across a boundary we 		 * will wrap to the top with mlen == -1 and 		 * then add it in shifted appropriately. 		 */
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|m
operator|==
literal|0
condition|)
block|{
name|printf
argument_list|(
literal|"cksum: out of data\n"
argument_list|)
expr_stmt|;
goto|goto
name|done
goto|;
block|}
if|if
condition|(
name|m
operator|->
name|m_len
condition|)
break|break;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
block|}
block|}
name|done
label|:
comment|/* 	 * Add together high and low parts of sum 	 * and carry to get cksum. 	 * Have to be careful to not drop the last 	 * carry here. 	 */
block|{
asm|asm("shal $-16,r9,r0; addw2 r0,r9; adwc $0,r9");
asm|asm("mcoml r9,r9; movzwl r9,r9");
block|}
return|return
operator|(
name|sum
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

