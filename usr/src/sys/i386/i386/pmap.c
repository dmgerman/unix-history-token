begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*   * Copyright (c) 1987 Carnegie-Mellon University  * Copyright (c) 1991 Regents of the University of California.  * All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * The Mach Operating System project at Carnegie-Mellon University.  *  * The CMU software License Agreement specifies the terms and conditions  * for use and redistribution.  *  * Derived from hp300 version by Mike Hibler, this version by William  * Jolitz uses a recursive map [a pde points to the page directory] to  * map the page tables using the pagetables themselves. This is done to  * reduce the impact on kernel virtual memory for lots of sparse address  * space, and to reduce the cost of memory to each process.  *  *	Derived from: hp300/@(#)pmap.c	7.1 (Berkeley) 12/5/90  *	@(#)pmap.c	7.1	%G%  */
end_comment

begin_comment
comment|/*  *	Reno i386 version, from Mike Hibler's hp300 version.  */
end_comment

begin_comment
comment|/*  *	Manages physical address maps.  *  *	In addition to hardware address maps, this  *	module is called upon to provide software-use-only  *	maps which may or may not be stored in the same  *	form as hardware maps.  These pseudo-maps are  *	used to store intermediate results from copy  *	operations to and from address spaces.  *  *	Since the information managed by this module is  *	also stored by the logical address mapping module,  *	this module may throw away valid virtual-to-physical  *	mappings at almost any time.  However, invalidations  *	of virtual-to-physical mappings must be done as  *	requested.  *  *	In order to cope with hardware architectures which  *	make virtual-to-physical map invalidates expensive,  *	this module may delay invalidate or reduced protection  *	operations until such time as they are actually  *	necessary.  This module is given full information as  *	to which processors are currently using which maps,  *	and to when physical maps must be made correct.  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_param.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"lock.h"
end_include

begin_include
include|#
directive|include
file|"malloc.h"
end_include

begin_include
include|#
directive|include
file|"../vm/pmap.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_map.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_kern.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_prot.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_page.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_pageout.h"
end_include

begin_include
include|#
directive|include
file|"machine/isa.h"
end_include

begin_comment
comment|/*  * Allocate various and sundry SYSMAPs used in the days of old VM  * and not yet converted.  XXX.  */
end_comment

begin_define
define|#
directive|define
name|BSDVM_COMPAT
value|1
end_define

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_struct
struct|struct
block|{
name|int
name|collectscans
decl_stmt|;
name|int
name|collectpages
decl_stmt|;
name|int
name|kpttotal
decl_stmt|;
name|int
name|kptinuse
decl_stmt|;
name|int
name|kptmaxuse
decl_stmt|;
block|}
name|kpt_stats
struct|;
end_struct

begin_struct
struct|struct
block|{
name|int
name|kernel
decl_stmt|;
comment|/* entering kernel mapping */
name|int
name|user
decl_stmt|;
comment|/* entering user mapping */
name|int
name|ptpneeded
decl_stmt|;
comment|/* needed to allocate a PT page */
name|int
name|pwchange
decl_stmt|;
comment|/* no mapping change, just wiring or protection */
name|int
name|wchange
decl_stmt|;
comment|/* no mapping change, just wiring */
name|int
name|mchange
decl_stmt|;
comment|/* was mapped but mapping to different page */
name|int
name|managed
decl_stmt|;
comment|/* a managed page */
name|int
name|firstpv
decl_stmt|;
comment|/* first mapping for this PA */
name|int
name|secondpv
decl_stmt|;
comment|/* second mapping for this PA */
name|int
name|ci
decl_stmt|;
comment|/* cache inhibited */
name|int
name|unmanaged
decl_stmt|;
comment|/* not a managed page */
name|int
name|flushes
decl_stmt|;
comment|/* cache flushes */
block|}
name|enter_stats
struct|;
end_struct

begin_struct
struct|struct
block|{
name|int
name|calls
decl_stmt|;
name|int
name|removes
decl_stmt|;
name|int
name|pvfirst
decl_stmt|;
name|int
name|pvsearch
decl_stmt|;
name|int
name|ptinvalid
decl_stmt|;
name|int
name|uflushes
decl_stmt|;
name|int
name|sflushes
decl_stmt|;
block|}
name|remove_stats
struct|;
end_struct

begin_decl_stmt
name|int
name|debugmap
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|pmapdebug
init|=
literal|0
comment|/*0xffff*/
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|PDB_FOLLOW
value|0x0001
end_define

begin_define
define|#
directive|define
name|PDB_INIT
value|0x0002
end_define

begin_define
define|#
directive|define
name|PDB_ENTER
value|0x0004
end_define

begin_define
define|#
directive|define
name|PDB_REMOVE
value|0x0008
end_define

begin_define
define|#
directive|define
name|PDB_CREATE
value|0x0010
end_define

begin_define
define|#
directive|define
name|PDB_PTPAGE
value|0x0020
end_define

begin_define
define|#
directive|define
name|PDB_CACHE
value|0x0040
end_define

begin_define
define|#
directive|define
name|PDB_BITS
value|0x0080
end_define

begin_define
define|#
directive|define
name|PDB_COLLECT
value|0x0100
end_define

begin_define
define|#
directive|define
name|PDB_PROTECT
value|0x0200
end_define

begin_define
define|#
directive|define
name|PDB_PDRTAB
value|0x0400
end_define

begin_define
define|#
directive|define
name|PDB_PARANOIA
value|0x2000
end_define

begin_define
define|#
directive|define
name|PDB_WIRING
value|0x4000
end_define

begin_define
define|#
directive|define
name|PDB_PVDUMP
value|0x8000
end_define

begin_decl_stmt
name|int
name|pmapvacflush
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|PVF_ENTER
value|0x01
end_define

begin_define
define|#
directive|define
name|PVF_REMOVE
value|0x02
end_define

begin_define
define|#
directive|define
name|PVF_PROTECT
value|0x04
end_define

begin_define
define|#
directive|define
name|PVF_TOTAL
value|0x80
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Get PDEs and PTEs for user/kernel address space  */
end_comment

begin_define
define|#
directive|define
name|pmap_pde
parameter_list|(
name|m
parameter_list|,
name|v
parameter_list|)
value|(&((m)->pm_pdir[((vm_offset_t)(v)>> PD_SHIFT)&1023]))
end_define

begin_comment
comment|/*#define pmap_pte(m, v)	(&((m)->pm_ptab[(vm_offset_t)(v)>> PG_SHIFT]))*/
end_comment

begin_comment
comment|/*#define pmap_pte(m, v)	(vtopte(v))*/
end_comment

begin_define
define|#
directive|define
name|pmap_pte_pa
parameter_list|(
name|pte
parameter_list|)
value|(*(int *)(pte)& PG_FRAME)
end_define

begin_define
define|#
directive|define
name|pmap_pde_v
parameter_list|(
name|pte
parameter_list|)
value|((pte)->pd_v)
end_define

begin_define
define|#
directive|define
name|pmap_pte_w
parameter_list|(
name|pte
parameter_list|)
value|((pte)->pg_w)
end_define

begin_comment
comment|/* #define pmap_pte_ci(pte)	((pte)->pg_ci) */
end_comment

begin_define
define|#
directive|define
name|pmap_pte_m
parameter_list|(
name|pte
parameter_list|)
value|((pte)->pg_m)
end_define

begin_define
define|#
directive|define
name|pmap_pte_u
parameter_list|(
name|pte
parameter_list|)
value|((pte)->pg_u)
end_define

begin_define
define|#
directive|define
name|pmap_pte_v
parameter_list|(
name|pte
parameter_list|)
value|((pte)->pg_v)
end_define

begin_define
define|#
directive|define
name|pmap_pte_set_w
parameter_list|(
name|pte
parameter_list|,
name|v
parameter_list|)
value|((pte)->pg_w = (v))
end_define

begin_define
define|#
directive|define
name|pmap_pte_set_prot
parameter_list|(
name|pte
parameter_list|,
name|v
parameter_list|)
value|((pte)->pg_prot = (v))
end_define

begin_comment
comment|/*  * Given a map and a machine independent protection code,  * convert to a vax protection code.  */
end_comment

begin_define
define|#
directive|define
name|pte_prot
parameter_list|(
name|m
parameter_list|,
name|p
parameter_list|)
value|(protection_codes[p])
end_define

begin_decl_stmt
name|int
name|protection_codes
index|[
literal|8
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Kernel page table page management.  */
end_comment

begin_struct
struct|struct
name|kpt_page
block|{
name|struct
name|kpt_page
modifier|*
name|kpt_next
decl_stmt|;
comment|/* link on either used or free list */
name|vm_offset_t
name|kpt_va
decl_stmt|;
comment|/* always valid kernel VA */
name|vm_offset_t
name|kpt_pa
decl_stmt|;
comment|/* PA of this page (for speed) */
block|}
struct|;
end_struct

begin_decl_stmt
name|struct
name|kpt_page
modifier|*
name|kpt_free_list
decl_stmt|,
modifier|*
name|kpt_used_list
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|kpt_page
modifier|*
name|kpt_pages
decl_stmt|;
end_decl_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|junk
end_ifdef

begin_comment
comment|/*  * Kernel page directory and page table and page table map.  * The page table map gives us a level of indirection we need to dynamically  * expand the page table.  It is essentially a copy of the page directory table  * with PTEs instead of PDEs.  All are initialized in locore at boot time.  * Sysmap will initially contain VM_KERNEL_PT_PAGES pages of PTEs.  *  * PTDtabzero is an prototype page directory table which all processes  * share til they reference something.  */
end_comment

begin_decl_stmt
name|pd_entry_t
modifier|*
name|SysPDT
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|pt_entry_t
modifier|*
name|Sysmap
decl_stmt|,
modifier|*
name|Sysptmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|pd_entry_t
modifier|*
name|PDTtabzero
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
name|BSDVM_COMPAT
end_if

begin_decl_stmt
name|vm_size_t
name|Sysptsize
init|=
name|VM_KERNEL_PT_PAGES
operator|+
literal|4
operator|/
name|NPTEPG
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|vm_size_t
name|Sysptsize
init|=
name|VM_KERNEL_PT_PAGES
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|struct
name|pmap
name|kernel_pmap_store
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|pmap_t
name|kernel_pmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_map_t
name|pt_map
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_offset_t
name|avail_start
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* PA of first available physical page */
end_comment

begin_decl_stmt
name|vm_offset_t
name|avail_end
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* PA of last available physical page */
end_comment

begin_decl_stmt
name|vm_size_t
name|mem_size
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* memory size in bytes */
end_comment

begin_decl_stmt
name|vm_offset_t
name|virtual_avail
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* VA of first avail page (after kernel bss)*/
end_comment

begin_decl_stmt
name|vm_offset_t
name|virtual_end
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* VA of last avail page (end of kernel AS) */
end_comment

begin_decl_stmt
name|vm_offset_t
name|vm_first_phys
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* PA of first managed page */
end_comment

begin_decl_stmt
name|vm_offset_t
name|vm_last_phys
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* PA just past last managed page */
end_comment

begin_decl_stmt
name|int
name|i386pagesperpage
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* PAGE_SIZE / I386_PAGE_SIZE */
end_comment

begin_decl_stmt
name|boolean_t
name|pmap_initialized
init|=
name|FALSE
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Has pmap_init completed? */
end_comment

begin_decl_stmt
name|char
modifier|*
name|pmap_attributes
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* reference and modify bits */
end_comment

begin_function_decl
name|boolean_t
name|pmap_testbit
parameter_list|()
function_decl|;
end_function_decl

begin_decl_stmt
name|void
name|pmap_enter_ptpage
argument_list|()
decl_stmt|,
name|pmap_clear_modify
argument_list|()
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
name|BSDVM_COMPAT
end_if

begin_include
include|#
directive|include
file|"msgbuf.h"
end_include

begin_comment
comment|/*  * All those kernel PT submaps that BSD is so fond of  */
end_comment

begin_decl_stmt
name|struct
name|pte
modifier|*
name|Swtchmap
decl_stmt|,
modifier|*
name|CMAP1
decl_stmt|,
modifier|*
name|CMAP2
decl_stmt|,
modifier|*
name|mmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|Swtchbase
decl_stmt|,
name|CADDR1
decl_stmt|,
name|CADDR2
decl_stmt|,
name|vmmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|pte
modifier|*
name|msgbufmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|msgbuf
modifier|*
name|msgbufp
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  *	Bootstrap the system enough to run with virtual memory.  *	Map the kernel's code and data, and allocate the system page table.  *  *	On the I386 this is called after mapping has already been enabled  *	and just syncs the pmap module with what has already been done.  *	[We can't call it easily with mapping off since the kernel is not  *	mapped with PA == VA, hence we would have to relocate every address  *	from the linked base (virtual) address 0xFE000000 to the actual  *	(physical) address starting relative to 0]  */
end_comment

begin_function_decl
name|struct
name|pte
modifier|*
name|pmap_pte
parameter_list|()
function_decl|;
end_function_decl

begin_decl_stmt
specifier|extern
name|vm_offset_t
name|atdevbase
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|pmap_bootstrap
parameter_list|(
name|firstaddr
parameter_list|,
name|loadaddr
parameter_list|)
name|vm_offset_t
name|firstaddr
decl_stmt|;
name|vm_offset_t
name|loadaddr
decl_stmt|;
block|{
if|#
directive|if
name|BSDVM_COMPAT
name|vm_offset_t
name|va
decl_stmt|;
name|struct
name|pte
modifier|*
name|pte
decl_stmt|;
endif|#
directive|endif
specifier|extern
name|vm_offset_t
name|maxmem
decl_stmt|,
name|physmem
decl_stmt|;
specifier|extern
name|int
name|IdlePTD
decl_stmt|;
name|avail_start
operator|=
literal|0x100000
comment|/* firstaddr */
expr_stmt|;
comment|/* XXX */
name|avail_end
operator|=
name|maxmem
operator|<<
name|PG_SHIFT
expr_stmt|;
comment|/* XXX: allow for msgbuf */
name|avail_end
operator|-=
name|i386_round_page
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|msgbuf
argument_list|)
argument_list|)
expr_stmt|;
name|mem_size
operator|=
name|physmem
operator|<<
name|PG_SHIFT
expr_stmt|;
name|virtual_avail
operator|=
name|atdevbase
operator|+
literal|0x100000
operator|-
literal|0xa0000
operator|+
literal|10
operator|*
name|NBPG
expr_stmt|;
name|virtual_end
operator|=
name|VM_MAX_KERNEL_ADDRESS
expr_stmt|;
name|printf
argument_list|(
literal|"avail [%x %x] virtual [%x %x]\n"
argument_list|,
name|avail_start
argument_list|,
name|avail_end
argument_list|,
name|virtual_avail
argument_list|,
name|virtual_end
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"cr3 %x"
argument_list|,
name|rcr3
argument_list|()
argument_list|)
expr_stmt|;
name|i386pagesperpage
operator|=
name|PAGE_SIZE
operator|/
name|I386_PAGE_SIZE
expr_stmt|;
comment|/* 	 * Initialize protection array. 	 */
name|i386_protection_init
argument_list|()
expr_stmt|;
comment|/* 	 * The kernel's pmap is statically allocated so we don't 	 * have to use pmap_create, which is unlikely to work 	 * correctly at this part of the boot sequence. 	 */
name|kernel_pmap
operator|=
operator|&
name|kernel_pmap_store
expr_stmt|;
ifdef|#
directive|ifdef
name|notdef
comment|/* 	 * Create Kernel page directory table and page maps. 	 */
name|bzero
argument_list|(
name|firstaddr
argument_list|,
literal|4
operator|*
name|NBPG
argument_list|)
expr_stmt|;
name|kernel_pmap
operator|->
name|pm_pdir
operator|=
name|firstaddr
operator|+
name|VM_MIN_KERNEL_ADDRESS
expr_stmt|;
name|kernel_pmap
operator|->
name|pm_ptab
operator|=
name|firstaddr
operator|+
name|VM_MIN_KERNEL_ADDRESS
operator|+
name|NBPG
expr_stmt|;
name|firstaddr
operator|+=
name|NBPG
expr_stmt|;
for|for
control|(
name|x
operator|=
name|i386_btod
argument_list|(
name|VM_MIN_KERNEL_ADDRESS
argument_list|)
init|;
name|x
operator|<
name|i386_btod
argument_list|(
name|VM_MIN_KERNEL_ADDRESS
argument_list|)
operator|+
literal|3
condition|;
name|x
operator|++
control|)
block|{
name|struct
name|pde
modifier|*
name|pde
decl_stmt|;
name|pde
operator|=
name|kernel_pmap
operator|->
name|pm_pdir
operator|+
name|x
expr_stmt|;
operator|*
operator|(
name|int
operator|*
operator|)
name|pde
operator|=
name|firstaddr
operator|+
name|x
operator|*
name|NBPG
operator||
name|PG_V
operator||
name|PG_KW
expr_stmt|;
block|}
else|#
directive|else
name|kernel_pmap
operator|->
name|pm_pdir
operator|=
operator|(
name|pd_entry_t
operator|*
operator|)
operator|(
literal|0xfe000000
operator|+
name|IdlePTD
operator|)
expr_stmt|;
endif|#
directive|endif
name|simple_lock_init
argument_list|(
operator|&
name|kernel_pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
name|kernel_pmap
operator|->
name|pm_count
operator|=
literal|1
expr_stmt|;
if|#
directive|if
name|BSDVM_COMPAT
comment|/* 	 * Allocate all the submaps we need 	 */
define|#
directive|define
name|SYSMAP
parameter_list|(
name|c
parameter_list|,
name|p
parameter_list|,
name|v
parameter_list|,
name|n
parameter_list|)
define|\
value|v = (c)va; va += ((n)*I386_PAGE_SIZE); p = pte; pte += (n);
name|va
operator|=
name|virtual_avail
expr_stmt|;
name|pte
operator|=
name|pmap_pte
argument_list|(
name|kernel_pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"pte %x va %x\n"
argument_list|,
name|pte
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|SYSMAP
argument_list|(
argument|caddr_t
argument_list|,
argument|Swtchmap
argument_list|,
argument|Swtchbase
argument_list|,
literal|1
argument_list|)
name|SYSMAP
argument_list|(
argument|caddr_t
argument_list|,
argument|CMAP1
argument_list|,
argument|CADDR1
argument_list|,
literal|1
argument_list|)
name|SYSMAP
argument_list|(
argument|caddr_t
argument_list|,
argument|CMAP2
argument_list|,
argument|CADDR2
argument_list|,
literal|1
argument_list|)
name|SYSMAP
argument_list|(
argument|caddr_t
argument_list|,
argument|mmap
argument_list|,
argument|vmmap
argument_list|,
literal|1
argument_list|)
name|SYSMAP
argument_list|(
argument|struct msgbuf *
argument_list|,
argument|msgbufmap
argument_list|,
argument|msgbufp
argument_list|,
literal|1
argument_list|)
name|printf
argument_list|(
literal|"pte %x va %x\n"
argument_list|,
name|pte
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|virtual_avail
operator|=
name|va
expr_stmt|;
endif|#
directive|endif
operator|*
operator|(
name|int
operator|*
operator|)
name|PTD
operator|=
literal|0
expr_stmt|;
name|load_cr3
argument_list|(
name|rcr3
argument_list|()
argument_list|)
expr_stmt|;
return|return
operator|(
name|firstaddr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	Initialize the pmap module.  *	Called by vm_init, to initialize any structures that the pmap  *	system needs to map virtual memory.  */
end_comment

begin_function
name|void
name|pmap_init
parameter_list|(
name|phys_start
parameter_list|,
name|phys_end
parameter_list|)
name|vm_offset_t
name|phys_start
decl_stmt|,
name|phys_end
decl_stmt|;
block|{
name|vm_offset_t
name|addr
decl_stmt|,
name|addr2
decl_stmt|;
name|vm_size_t
name|npg
decl_stmt|,
name|s
decl_stmt|;
name|int
name|rv
decl_stmt|;
specifier|extern
name|int
name|KPTphys
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_init(%x, %x)\n"
argument_list|,
name|phys_start
argument_list|,
name|phys_end
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Now that kernel map has been allocated, we can mark as 	 * unavailable regions which we have mapped in locore. 	 */
name|printf
argument_list|(
literal|"atdevbase %x"
argument_list|,
name|atdevbase
argument_list|)
expr_stmt|;
name|addr
operator|=
name|atdevbase
expr_stmt|;
operator|(
name|void
operator|)
name|vm_map_find
argument_list|(
name|kernel_map
argument_list|,
name|VM_OBJECT_NULL
argument_list|,
operator|(
name|vm_offset_t
operator|)
literal|0
argument_list|,
operator|&
name|addr
argument_list|,
operator|(
literal|0x100000
operator|-
literal|0xa0000
operator|)
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"addr %x"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|notdef
if|if
condition|(
name|addr
operator|!=
name|ATDevmem
condition|)
goto|goto
name|bogons
goto|;
endif|#
directive|endif
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
literal|0xfe000000
operator|+
name|KPTphys
comment|/* *NBPG */
expr_stmt|;
name|vm_object_reference
argument_list|(
name|kernel_object
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|vm_map_find
argument_list|(
name|kernel_map
argument_list|,
name|kernel_object
argument_list|,
name|addr
argument_list|,
operator|&
name|addr
argument_list|,
literal|2
operator|*
name|NBPG
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
comment|/* 	 * If this fails it is probably because the static portion of 	 * the kernel page table isn't big enough and we overran the 	 * page table map.   Need to adjust pmap_size() in i386_init.c. 	 */
ifdef|#
directive|ifdef
name|notdef
if|if
condition|(
name|addr
operator|!=
operator|(
name|vm_offset_t
operator|)
name|Sysmap
condition|)
goto|goto
name|bogons
goto|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|not
comment|/* u. is actually in process address space */
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
operator|&
name|u
expr_stmt|;
name|vm_object_reference
argument_list|(
name|kernel_object
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|vm_map_find
argument_list|(
name|kernel_map
argument_list|,
name|kernel_object
argument_list|,
name|addr
argument_list|,
operator|&
name|addr
argument_list|,
name|i386_ptob
argument_list|(
name|UPAGES
argument_list|)
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|addr
operator|!=
operator|(
name|vm_offset_t
operator|)
operator|&
name|u
condition|)
name|bogons
label|:
name|panic
argument_list|(
literal|"pmap_init: bogons in the VM system!\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_INIT
condition|)
block|{
name|printf
argument_list|(
literal|"pmap_init: SysPDT %x, Sysmap %x, Sysptmap %x\n"
argument_list|,
name|SysPDT
argument_list|,
name|Sysmap
argument_list|,
name|Sysptmap
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"  pstart %x, pend %x, vstart %x, vend %x\n"
argument_list|,
name|avail_start
argument_list|,
name|avail_end
argument_list|,
name|virtual_avail
argument_list|,
name|virtual_end
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* 	 * Allocate memory for random pmap data structures.  Includes the 	 * pv_head_table and pmap_attributes. 	 */
name|npg
operator|=
name|atop
argument_list|(
name|phys_end
operator|-
name|phys_start
argument_list|)
expr_stmt|;
name|s
operator|=
call|(
name|vm_size_t
call|)
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|pv_entry
argument_list|)
operator|*
name|npg
operator|+
name|npg
argument_list|)
expr_stmt|;
name|s
operator|=
name|round_page
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
name|kmem_alloc
argument_list|(
name|kernel_map
argument_list|,
name|s
argument_list|)
expr_stmt|;
name|pv_table
operator|=
operator|(
name|pv_entry_t
operator|)
name|addr
expr_stmt|;
name|addr
operator|+=
sizeof|sizeof
argument_list|(
expr|struct
name|pv_entry
argument_list|)
operator|*
name|npg
expr_stmt|;
name|pmap_attributes
operator|=
operator|(
name|char
operator|*
operator|)
name|addr
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_INIT
condition|)
name|printf
argument_list|(
literal|"pmap_init: %x bytes (%x pgs): tbl %x attr %x\n"
argument_list|,
name|s
argument_list|,
name|npg
argument_list|,
name|pv_table
argument_list|,
name|pmap_attributes
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|notdef
comment|/* we don't know if we will use kpt pages at all yet */
comment|/* 	 * Allocate physical memory for kernel PT pages and their management. 	 * We need 1 PT page per possible task plus some slop. 	 */
name|npg
operator|=
name|min
argument_list|(
name|atop
argument_list|(
name|I386_MAX_KPTSIZE
argument_list|)
argument_list|,
name|nproc
operator|+
literal|16
argument_list|)
expr_stmt|;
name|s
operator|=
name|ptoa
argument_list|(
name|npg
argument_list|)
operator|+
name|round_page
argument_list|(
name|npg
operator|*
sizeof|sizeof
argument_list|(
expr|struct
name|kpt_page
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * Verify that space will be allocated in region for which 	 * we already have kernel PT pages. 	 */
name|addr
operator|=
literal|0
expr_stmt|;
name|rv
operator|=
name|vm_map_find
argument_list|(
name|kernel_map
argument_list|,
name|VM_OBJECT_NULL
argument_list|,
literal|0
argument_list|,
operator|&
name|addr
argument_list|,
name|s
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
operator|||
name|addr
operator|+
name|s
operator|>=
operator|(
name|vm_offset_t
operator|)
name|Sysmap
condition|)
name|panic
argument_list|(
literal|"pmap_init: kernel PT too small"
argument_list|)
expr_stmt|;
name|vm_map_remove
argument_list|(
name|kernel_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|s
argument_list|)
expr_stmt|;
comment|/* 	 * Now allocate the space and link the pages together to 	 * form the KPT free list. 	 */
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
name|kmem_alloc
argument_list|(
name|kernel_map
argument_list|,
name|s
argument_list|)
expr_stmt|;
name|s
operator|=
name|ptoa
argument_list|(
name|npg
argument_list|)
expr_stmt|;
name|addr2
operator|=
name|addr
operator|+
name|s
expr_stmt|;
name|kpt_pages
operator|=
operator|&
operator|(
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
name|addr2
operator|)
index|[
name|npg
index|]
expr_stmt|;
name|kpt_free_list
operator|=
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
literal|0
expr_stmt|;
do|do
block|{
name|addr2
operator|-=
name|I386_PAGE_SIZE
expr_stmt|;
operator|(
operator|--
name|kpt_pages
operator|)
operator|->
name|kpt_next
operator|=
name|kpt_free_list
expr_stmt|;
name|kpt_free_list
operator|=
name|kpt_pages
expr_stmt|;
name|kpt_pages
operator|->
name|kpt_va
operator|=
name|addr2
expr_stmt|;
name|kpt_pages
operator|->
name|kpt_pa
operator|=
name|pmap_extract
argument_list|(
name|kernel_pmap
argument_list|,
name|addr2
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|addr
operator|!=
name|addr2
condition|)
do|;
ifdef|#
directive|ifdef
name|DEBUG
name|kpt_stats
operator|.
name|kpttotal
operator|=
name|atop
argument_list|(
name|s
argument_list|)
expr_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_INIT
condition|)
name|printf
argument_list|(
literal|"pmap_init: KPT: %d pages from %x to %x\n"
argument_list|,
name|atop
argument_list|(
name|s
argument_list|)
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|s
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
ifdef|#
directive|ifdef
name|notdef
comment|/* we don't know yet if we want a upt map elsewhere */
comment|/* 	 * Slightly modified version of kmem_suballoc() to get page table 	 * map where we want it. 	 */
name|addr
operator|=
name|I386_PTBASE
expr_stmt|;
name|s
operator|=
name|min
argument_list|(
name|I386_PTMAXSIZE
argument_list|,
name|nproc
operator|*
name|I386_MAX_PTSIZE
argument_list|)
expr_stmt|;
name|addr2
operator|=
name|addr
operator|+
name|s
expr_stmt|;
name|rv
operator|=
name|vm_map_find
argument_list|(
name|kernel_map
argument_list|,
name|VM_OBJECT_NULL
argument_list|,
literal|0
argument_list|,
operator|&
name|addr
argument_list|,
name|s
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
name|panic
argument_list|(
literal|"pmap_init: cannot allocate space for PT map"
argument_list|)
expr_stmt|;
name|pmap_reference
argument_list|(
name|vm_map_pmap
argument_list|(
name|kernel_map
argument_list|)
argument_list|)
expr_stmt|;
name|pt_map
operator|=
name|vm_map_create
argument_list|(
name|vm_map_pmap
argument_list|(
name|kernel_map
argument_list|)
argument_list|,
name|addr
argument_list|,
name|addr2
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|pt_map
operator|==
name|VM_MAP_NULL
condition|)
name|panic
argument_list|(
literal|"pmap_init: cannot create pt_map"
argument_list|)
expr_stmt|;
name|rv
operator|=
name|vm_map_submap
argument_list|(
name|kernel_map
argument_list|,
name|addr
argument_list|,
name|addr2
argument_list|,
name|pt_map
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
name|panic
argument_list|(
literal|"pmap_init: cannot map range to pt_map"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_INIT
condition|)
name|printf
argument_list|(
literal|"pmap_init: pt_map [%x - %x)\n"
argument_list|,
name|addr
argument_list|,
name|addr2
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* 	 * Now it is safe to enable pv_table recording. 	 */
name|vm_first_phys
operator|=
name|phys_start
expr_stmt|;
name|vm_last_phys
operator|=
name|phys_end
expr_stmt|;
name|pmap_initialized
operator|=
name|TRUE
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Used to map a range of physical addresses into kernel  *	virtual address space.  *  *	For now, VM is already on, we only need to map the  *	specified memory.  */
end_comment

begin_function
name|vm_offset_t
name|pmap_map
parameter_list|(
name|virt
parameter_list|,
name|start
parameter_list|,
name|end
parameter_list|,
name|prot
parameter_list|)
name|vm_offset_t
name|virt
decl_stmt|;
name|vm_offset_t
name|start
decl_stmt|;
name|vm_offset_t
name|end
decl_stmt|;
name|int
name|prot
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_map(%x, %x, %x, %x)\n"
argument_list|,
name|virt
argument_list|,
name|start
argument_list|,
name|end
argument_list|,
name|prot
argument_list|)
expr_stmt|;
endif|#
directive|endif
while|while
condition|(
name|start
operator|<
name|end
condition|)
block|{
name|pmap_enter
argument_list|(
name|kernel_pmap
argument_list|,
name|virt
argument_list|,
name|start
argument_list|,
name|prot
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
name|virt
operator|+=
name|PAGE_SIZE
expr_stmt|;
name|start
operator|+=
name|PAGE_SIZE
expr_stmt|;
block|}
return|return
operator|(
name|virt
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	Create and return a physical map.  *  *	If the size specified for the map  *	is zero, the map is an actual physical  *	map, and may be referenced by the  *	hardware.  *  *	If the size specified is non-zero,  *	the map will be used in software only, and  *	is bounded by that size.  *  * [ just allocate a ptd and mark it uninitialize -- should we track  *   with a table which process has which ptd? -wfj]  */
end_comment

begin_function
name|pmap_t
name|pmap_create
parameter_list|(
name|size
parameter_list|)
name|vm_size_t
name|size
decl_stmt|;
block|{
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_CREATE
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_create(%x)\n"
argument_list|,
name|size
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Software use map does not need a pmap 	 */
if|if
condition|(
name|size
condition|)
return|return
operator|(
name|PMAP_NULL
operator|)
return|;
comment|/* XXX: is it ok to wait here? */
name|pmap
operator|=
operator|(
name|pmap_t
operator|)
name|malloc
argument_list|(
sizeof|sizeof
expr|*
name|pmap
argument_list|,
name|M_VMPMAP
argument_list|,
name|M_WAITOK
argument_list|)
expr_stmt|;
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
name|panic
argument_list|(
literal|"pmap_create: cannot allocate a pmap"
argument_list|)
expr_stmt|;
comment|/* 	 * No need to allocate page table space yet but we do need a 	 * valid page directory table. 	 */
ifdef|#
directive|ifdef
name|junk
name|pmap
operator|->
name|pm_ptab
operator|=
name|PT_ENTRY_NULL
expr_stmt|;
endif|#
directive|endif
name|pmap
operator|->
name|pm_pdir
operator|=
operator|(
name|pd_entry_t
operator|*
operator|)
name|kmem_alloc
argument_list|(
name|kernel_map
argument_list|,
name|NBPG
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"pdir %x pdt.. %x "
argument_list|,
name|pmap
operator|->
name|pm_pdir
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
operator|(
name|PTD
operator|+
name|KPTDI_FIRST
operator|)
argument_list|)
expr_stmt|;
name|bcopy
argument_list|(
name|PTD
operator|+
name|KPTDI_FIRST
argument_list|,
name|pmap
operator|->
name|pm_pdir
operator|+
name|KPTDI_FIRST
argument_list|,
operator|(
name|KPTDI_LAST
operator|-
name|KPTDI_FIRST
operator|+
literal|1
operator|)
operator|*
literal|4
argument_list|)
expr_stmt|;
operator|*
operator|(
name|int
operator|*
operator|)
operator|(
name|pmap
operator|->
name|pm_pdir
operator|+
name|PTDPTDI
operator|)
operator|=
operator|(
name|int
operator|)
name|pmap_extract
argument_list|(
name|kernel_pmap
argument_list|,
name|pmap
operator|->
name|pm_pdir
argument_list|)
operator||
name|PG_V
operator||
name|PG_URKW
expr_stmt|;
comment|/*pmap->pm_ptobj = (caddr_t) vm_object_allocate(1024);*/
name|pmap
operator|->
name|pm_dref
operator|=
literal|0
expr_stmt|;
name|pmap
operator|->
name|pm_count
operator|=
literal|1
expr_stmt|;
name|simple_lock_init
argument_list|(
operator|&
name|pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
name|pmap
operator|->
name|pm_stats
operator|.
name|resident_count
operator|=
literal|0
expr_stmt|;
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|=
literal|0
expr_stmt|;
name|pmap
operator|->
name|pm_ptpages
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|pmap
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	Retire the given physical map from service.  *	Should only be called if the map contains  *	no valid mappings.  */
end_comment

begin_function
name|void
name|pmap_destroy
parameter_list|(
name|pmap
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
block|{
name|int
name|count
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_destroy(%x)\n"
argument_list|,
name|pmap
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
return|return;
name|simple_lock
argument_list|(
operator|&
name|pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
name|count
operator|=
operator|--
name|pmap
operator|->
name|pm_count
expr_stmt|;
name|simple_unlock
argument_list|(
operator|&
name|pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
if|if
condition|(
name|count
condition|)
return|return;
ifdef|#
directive|ifdef
name|junk
if|if
condition|(
name|pmap
operator|->
name|pm_ptab
condition|)
name|kmem_free_wakeup
argument_list|(
name|pt_map
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|pmap
operator|->
name|pm_ptab
argument_list|,
name|I386_MAX_PTSIZE
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/*vm_object_deallocate((vm_object_t)pmap->pm_ptobj);*/
name|kmem_free
argument_list|(
name|kernel_map
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|pmap
operator|->
name|pm_pdir
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|free
argument_list|(
operator|(
name|caddr_t
operator|)
name|pmap
argument_list|,
name|M_VMPMAP
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Add a reference to the specified pmap.  */
end_comment

begin_function
name|void
name|pmap_reference
parameter_list|(
name|pmap
parameter_list|)
name|pmap_t
name|pmap
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_reference(%x)\n"
argument_list|,
name|pmap
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|!=
name|PMAP_NULL
condition|)
block|{
name|simple_lock
argument_list|(
operator|&
name|pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
name|pmap
operator|->
name|pm_count
operator|++
expr_stmt|;
name|simple_unlock
argument_list|(
operator|&
name|pmap
operator|->
name|pm_lock
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_define
define|#
directive|define
name|DCIS
parameter_list|()
end_define

begin_define
define|#
directive|define
name|DCIU
parameter_list|()
end_define

begin_define
define|#
directive|define
name|DCIA
parameter_list|()
end_define

begin_comment
comment|/*  *	Remove the given range of addresses from the specified map.  *  *	It is assumed that the start and end are properly  *	rounded to the page size.  */
end_comment

begin_function
name|void
name|pmap_remove
parameter_list|(
name|pmap
parameter_list|,
name|sva
parameter_list|,
name|eva
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|sva
decl_stmt|,
name|eva
decl_stmt|;
block|{
specifier|register
name|vm_offset_t
name|pa
decl_stmt|,
name|va
decl_stmt|;
specifier|register
name|pt_entry_t
modifier|*
name|pte
decl_stmt|;
specifier|register
name|pv_entry_t
name|pv
decl_stmt|,
name|npv
decl_stmt|;
specifier|register
name|int
name|ix
decl_stmt|;
name|pmap_t
name|ptpmap
decl_stmt|;
name|int
modifier|*
name|pde
decl_stmt|,
name|s
decl_stmt|,
name|bits
decl_stmt|;
name|boolean_t
name|firstpage
init|=
name|TRUE
decl_stmt|;
name|boolean_t
name|flushcache
init|=
name|FALSE
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|pt_entry_t
name|opte
decl_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_REMOVE
operator||
name|PDB_PROTECT
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_remove(%x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|sva
argument_list|,
name|eva
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
return|return;
ifdef|#
directive|ifdef
name|DEBUG
name|remove_stats
operator|.
name|calls
operator|++
expr_stmt|;
endif|#
directive|endif
for|for
control|(
name|va
operator|=
name|sva
init|;
name|va
operator|<
name|eva
condition|;
name|va
operator|+=
name|PAGE_SIZE
control|)
block|{
comment|/* 		 * Weed out invalid mappings. 		 * Note: we assume that the page directory table is always allocated. 		 */
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
name|va
operator|=
name|i386_round_pdr
argument_list|(
name|va
operator|+
name|PAGE_SIZE
argument_list|)
operator|-
name|PAGE_SIZE
expr_stmt|;
continue|continue;
block|}
name|pte
operator|=
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|pa
operator|=
name|pmap_pte_pa
argument_list|(
name|pte
argument_list|)
expr_stmt|;
if|if
condition|(
name|pa
operator|==
literal|0
condition|)
continue|continue;
ifdef|#
directive|ifdef
name|DEBUG
name|opte
operator|=
operator|*
name|pte
expr_stmt|;
name|remove_stats
operator|.
name|removes
operator|++
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * Update statistics 		 */
if|if
condition|(
name|pmap_pte_w
argument_list|(
name|pte
argument_list|)
condition|)
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|--
expr_stmt|;
name|pmap
operator|->
name|pm_stats
operator|.
name|resident_count
operator|--
expr_stmt|;
comment|/* 		 * Invalidate the PTEs. 		 * XXX: should cluster them up and invalidate as many 		 * as possible at once. 		 */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_REMOVE
condition|)
name|printf
argument_list|(
literal|"remove: invalidating %x ptes at %x\n"
argument_list|,
name|i386pagesperpage
argument_list|,
name|pte
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|bits
operator|=
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
name|bits
operator||=
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|&
operator|(
name|PG_U
operator||
name|PG_M
operator|)
expr_stmt|;
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|++
operator|=
literal|0
expr_stmt|;
comment|/*TBIS(va + ix * I386_PAGE_SIZE);*/
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
comment|/*if (pmap == u.u_procp->p_map->pmap) 			pmap_activate(pmap, (struct pcb *)u.u_procp->p_addr); */
name|load_cr3
argument_list|(
name|u
operator|.
name|u_pcb
operator|.
name|pcb_ptd
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|unneeded
comment|/* 		 * For user mappings decrement the wiring count on 		 * the PT page. 		 */
if|if
condition|(
name|pmap
operator|!=
name|kernel_pmap
condition|)
block|{
name|vm_page_t
name|mem
decl_stmt|;
name|mem
operator|=
name|vm_page_lookup
argument_list|(
name|pmap
operator|->
name|pm_ptobj
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|pdei
argument_list|(
name|va
argument_list|)
argument_list|)
expr_stmt|;
name|mem
operator|->
name|wire_count
operator|--
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* 		 * Remove from the PV table (raise IPL since we 		 * may be called at interrupt time). 		 */
if|if
condition|(
name|pa
operator|<
name|vm_first_phys
operator|||
name|pa
operator|>=
name|vm_last_phys
condition|)
continue|continue;
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
name|pde
operator|=
operator|(
name|int
operator|*
operator|)
literal|0
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
comment|/* 		 * If it is the first entry on the list, it is actually 		 * in the header and we must copy the following entry up 		 * to the header.  Otherwise we must search the list for 		 * the entry.  In either case we free the now unused entry. 		 */
if|if
condition|(
name|pmap
operator|==
name|pv
operator|->
name|pv_pmap
operator|&&
name|va
operator|==
name|pv
operator|->
name|pv_va
condition|)
block|{
name|pde
operator|=
operator|(
name|int
operator|*
operator|)
name|pv
operator|->
name|pv_ptpde
expr_stmt|;
name|ptpmap
operator|=
name|pv
operator|->
name|pv_ptpmap
expr_stmt|;
name|npv
operator|=
name|pv
operator|->
name|pv_next
expr_stmt|;
if|if
condition|(
name|npv
condition|)
block|{
operator|*
name|pv
operator|=
operator|*
name|npv
expr_stmt|;
name|free
argument_list|(
operator|(
name|caddr_t
operator|)
name|npv
argument_list|,
name|M_VMPVENT
argument_list|)
expr_stmt|;
block|}
else|else
name|pv
operator|->
name|pv_pmap
operator|=
name|PMAP_NULL
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|remove_stats
operator|.
name|pvfirst
operator|++
expr_stmt|;
endif|#
directive|endif
block|}
else|else
block|{
for|for
control|(
name|npv
operator|=
name|pv
operator|->
name|pv_next
init|;
name|npv
condition|;
name|npv
operator|=
name|npv
operator|->
name|pv_next
control|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|remove_stats
operator|.
name|pvsearch
operator|++
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|npv
operator|->
name|pv_pmap
operator|&&
name|va
operator|==
name|npv
operator|->
name|pv_va
condition|)
break|break;
name|pv
operator|=
name|npv
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|npv
operator|==
name|PV_ENTRY_NULL
condition|)
name|panic
argument_list|(
literal|"pmap_remove: PA not in pv_tab"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pde
operator|=
operator|(
name|int
operator|*
operator|)
name|npv
operator|->
name|pv_ptpde
expr_stmt|;
name|ptpmap
operator|=
name|npv
operator|->
name|pv_ptpmap
expr_stmt|;
name|pv
operator|->
name|pv_next
operator|=
name|npv
operator|->
name|pv_next
expr_stmt|;
name|free
argument_list|(
operator|(
name|caddr_t
operator|)
name|npv
argument_list|,
name|M_VMPVENT
argument_list|)
expr_stmt|;
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
block|}
comment|/* 		 * If only one mapping left we no longer need to cache inhibit 		 */
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|&&
name|pv
operator|->
name|pv_next
operator|==
name|PV_ENTRY_NULL
operator|&&
operator|(
name|pv
operator|->
name|pv_flags
operator|&
name|PV_CI
operator|)
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_CACHE
condition|)
name|pg
argument_list|(
literal|"remove: clearing CI for pa %x\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pv
operator|->
name|pv_flags
operator|&=
operator|~
name|PV_CI
expr_stmt|;
name|pmap_changebit
argument_list|(
name|pa
argument_list|,
name|PG_CI
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|(
name|pmapdebug
operator|&
operator|(
name|PDB_CACHE
operator||
name|PDB_PVDUMP
operator|)
operator|)
operator|==
operator|(
name|PDB_CACHE
operator||
name|PDB_PVDUMP
operator|)
condition|)
name|pmap_pvdump
argument_list|(
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 		 * If this was a PT page we must also remove the 		 * mapping from the associated page directory table. 		 */
if|if
condition|(
name|pde
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|remove_stats
operator|.
name|ptinvalid
operator|++
expr_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_REMOVE
operator||
name|PDB_PTPAGE
operator|)
condition|)
block|{
name|pg
argument_list|(
literal|"remove: pde was %x@%x pte was %x@%x\n"
argument_list|,
operator|*
name|pde
argument_list|,
name|pde
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|opte
argument_list|,
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
operator|*
name|pde
operator|=
literal|0
expr_stmt|;
comment|/*{ vm_page_t	mem; 					mem = vm_page_lookup(pmap->pm_ptobj, 				(vm_offset_t)pdei(va)); 					mem->wire_count = 0 					vm_page_free(mem);*/
comment|/* 			 * If it was a user PT page, we decrement the 			 * reference count on the page directory table as well, 			 * freeing it if it is now empty. 			 */
if|if
condition|(
name|ptpmap
operator|!=
name|kernel_pmap
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_REMOVE
operator||
name|PDB_PDRTAB
operator|)
condition|)
name|pg
argument_list|(
literal|"remove: pdir %x, refcnt %d\n"
argument_list|,
name|ptpmap
operator|->
name|pm_pdir
argument_list|,
name|ptpmap
operator|->
name|pm_dref
operator|-
literal|1
argument_list|)
expr_stmt|;
comment|/*if ((pmapdebug& PDB_PARANOIA)&& 				    ptpmap->pm_pdir != (struct pd_entry_t *)trunc_page(pde)) 					panic("remove: bogus pde");*/
endif|#
directive|endif
block|}
comment|/*if (ptpmap == kernel_pmap) 				TBIAS(); 			else 				TBIAU(); */
name|pv
operator|->
name|pv_flags
operator|&=
operator|~
name|PV_PTPAGE
expr_stmt|;
name|ptpmap
operator|->
name|pm_ptpages
operator|--
expr_stmt|;
block|}
comment|/* 		 * Update saved attributes for managed page 		 */
name|pmap_attributes
index|[
name|pa_index
argument_list|(
name|pa
argument_list|)
index|]
operator||=
name|bits
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapvacflush
operator|&
name|PVF_REMOVE
condition|)
block|{
if|if
condition|(
name|pmapvacflush
operator|&
name|PVF_TOTAL
condition|)
name|DCIA
argument_list|()
expr_stmt|;
elseif|else
if|if
condition|(
name|pmap
operator|==
name|kernel_pmap
condition|)
name|DCIS
argument_list|()
expr_stmt|;
else|else
name|DCIU
argument_list|()
expr_stmt|;
block|}
endif|#
directive|endif
comment|/*if (flushcache) { 		if (pmap == kernel_pmap) { 			DCIS(); #ifdef DEBUG 			remove_stats.sflushes++; #endif 		} else { 			DCIU(); #ifdef DEBUG 			remove_stats.uflushes++; #endif 		} 	}*/
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_remove_all  *	Function:  *		Removes this physical page from  *		all physical maps in which it resides.  *		Reflects back modify bits to the pager.  */
end_comment

begin_function
name|void
name|pmap_remove_all
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
name|int
name|s
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_REMOVE
operator||
name|PDB_PROTECT
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_remove_all(%x)\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Not one of ours 	 */
if|if
condition|(
name|pa
operator|<
name|vm_first_phys
operator|||
name|pa
operator|>=
name|vm_last_phys
condition|)
block|{
name|pg
argument_list|(
literal|"RET! "
argument_list|)
expr_stmt|;
return|return;
block|}
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
comment|/* 	 * Do it the easy way for now 	 */
while|while
condition|(
name|pv
operator|->
name|pv_pmap
operator|!=
name|PMAP_NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|)
argument_list|)
operator|||
name|pmap_pte_pa
argument_list|(
name|pmap_pte
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|)
argument_list|)
operator|!=
name|pa
condition|)
name|panic
argument_list|(
literal|"pmap_remove_all: bad mapping"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_remove
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
name|pv
operator|->
name|pv_va
operator|+
name|PAGE_SIZE
argument_list|)
expr_stmt|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_copy_on_write  *	Function:  *		Remove write privileges from all  *		physical maps for this physical page.  */
end_comment

begin_function
name|void
name|pmap_copy_on_write
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_PROTECT
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_copy_on_write(%x)\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_changebit
argument_list|(
name|pa
argument_list|,
name|PG_RO
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Set the physical protection on the  *	specified range of this map as requested.  */
end_comment

begin_function
name|void
name|pmap_protect
parameter_list|(
name|pmap
parameter_list|,
name|sva
parameter_list|,
name|eva
parameter_list|,
name|prot
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|sva
decl_stmt|,
name|eva
decl_stmt|;
name|vm_prot_t
name|prot
decl_stmt|;
block|{
specifier|register
name|pt_entry_t
modifier|*
name|pte
decl_stmt|;
specifier|register
name|vm_offset_t
name|va
decl_stmt|;
specifier|register
name|int
name|ix
decl_stmt|;
name|int
name|i386prot
decl_stmt|;
name|boolean_t
name|firstpage
init|=
name|TRUE
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_PROTECT
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_protect(%x, %x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|sva
argument_list|,
name|eva
argument_list|,
name|prot
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
return|return;
comment|/* 	if ((prot& VM_PROT_READ) == VM_PROT_NONE) { 		pmap_remove(pmap, sva, eva); 		return; 	} 	if (prot& VM_PROT_WRITE) 		return;*/
name|pte
operator|=
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|sva
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pte
condition|)
return|return;
for|for
control|(
name|va
operator|=
name|sva
init|;
name|va
operator|<
name|eva
condition|;
name|va
operator|+=
name|PAGE_SIZE
control|)
block|{
comment|/* 		 * Page table page is not allocated. 		 * Skip it, we don't want to force allocation 		 * of unnecessary PTE pages just to set the protection. 		 */
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
comment|/* XXX: avoid address wrap around */
if|if
condition|(
name|va
operator|>=
name|i386_trunc_pdr
argument_list|(
operator|(
name|vm_offset_t
operator|)
operator|-
literal|1
argument_list|)
condition|)
break|break;
name|va
operator|=
name|i386_round_pdr
argument_list|(
name|va
operator|+
name|PAGE_SIZE
argument_list|)
operator|-
name|PAGE_SIZE
expr_stmt|;
name|pte
operator|=
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|pte
operator|+=
name|i386pagesperpage
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
operator|!
name|pte
condition|)
return|return;
comment|/* 		 * Page not valid.  Again, skip it. 		 * Should we do this?  Or set protection anyway? 		 */
if|if
condition|(
operator|!
name|pmap_pte_v
argument_list|(
name|pte
argument_list|)
condition|)
block|{
name|pte
operator|+=
name|i386pagesperpage
expr_stmt|;
continue|continue;
block|}
name|ix
operator|=
literal|0
expr_stmt|;
name|i386prot
operator|=
name|pte_prot
argument_list|(
name|pmap
argument_list|,
name|prot
argument_list|)
expr_stmt|;
if|if
condition|(
name|va
operator|<
name|UPT_MAX_ADDRESS
condition|)
name|i386prot
operator||=
literal|2
comment|/*PG_u*/
expr_stmt|;
do|do
block|{
comment|/* clear VAC here if PG_RO? */
name|pmap_pte_set_prot
argument_list|(
name|pte
operator|++
argument_list|,
name|i386prot
argument_list|)
expr_stmt|;
comment|/*TBIS(va + ix * I386_PAGE_SIZE);*/
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
block|}
if|if
condition|(
name|pmap
operator|==
name|u
operator|.
name|u_procp
operator|->
name|p_map
operator|->
name|pmap
condition|)
name|pmap_activate
argument_list|(
name|pmap
argument_list|,
operator|(
expr|struct
name|pcb
operator|*
operator|)
name|u
operator|.
name|u_procp
operator|->
name|p_addr
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Insert the given physical page (p) at  *	the specified virtual address (v) in the  *	target physical map with the protection requested.  *  *	If specified, the page will be wired down, meaning  *	that the related pte can not be reclaimed.  *  *	NB:  This is the only routine which MAY NOT lazy-evaluate  *	or lose information.  That is, this routine must actually  *	insert this page into the given map NOW.  */
end_comment

begin_function
name|void
name|pmap_enter
parameter_list|(
name|pmap
parameter_list|,
name|va
parameter_list|,
name|pa
parameter_list|,
name|prot
parameter_list|,
name|wired
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|va
decl_stmt|;
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
name|vm_prot_t
name|prot
decl_stmt|;
name|boolean_t
name|wired
decl_stmt|;
block|{
specifier|register
name|pt_entry_t
modifier|*
name|pte
decl_stmt|;
specifier|register
name|int
name|npte
decl_stmt|,
name|ix
decl_stmt|;
name|vm_offset_t
name|opa
decl_stmt|;
name|boolean_t
name|cacheable
init|=
name|TRUE
decl_stmt|;
name|boolean_t
name|checkpv
init|=
name|TRUE
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_ENTER
operator|)
condition|)
name|printf
argument_list|(
literal|"pmap_enter(%x, %x, %x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|va
argument_list|,
name|pa
argument_list|,
name|prot
argument_list|,
name|wired
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
return|return;
if|if
condition|(
name|va
operator|>
name|VM_MAX_KERNEL_ADDRESS
condition|)
name|panic
argument_list|(
literal|"pmap_enter: toobig"
argument_list|)
expr_stmt|;
comment|/* also, should not muck with PTD va! */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmap
operator|==
name|kernel_pmap
condition|)
name|enter_stats
operator|.
name|kernel
operator|++
expr_stmt|;
else|else
name|enter_stats
operator|.
name|user
operator|++
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|junk
index|[
name|should
name|insure
name|we
name|have
literal|1
index|)
name|pagetable
expr|for
name|u
operator|.
expr|/
name|kstack
operator|&
name|page
expr|]
comment|/* 	 * For user mapping, allocate kernel VM resources if necessary. 	 */
if|if
condition|(
name|pmap
operator|->
name|pm_ptab
operator|==
name|PT_ENTRY_NULL
condition|)
name|pmap
operator|->
name|pm_ptab
operator|=
operator|(
name|pt_entry_t
operator|*
operator|)
name|kmem_alloc_wait
argument_list|(
name|pt_map
argument_list|,
name|I386_MAX_PTSIZE
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Page Directory table entry not valid, we need a new PT page 	 */
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
name|pg
argument_list|(
literal|"ptdi %x"
argument_list|,
name|pmap
operator|->
name|pm_pdir
index|[
name|PTDPTDI
index|]
argument_list|)
expr_stmt|;
name|pmap_enter_ptpage
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
block|}
name|pte
operator|=
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|opa
operator|=
name|pmap_pte_pa
argument_list|(
name|pte
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_ENTER
condition|)
name|printf
argument_list|(
literal|"enter: pte %x, *pte %x\n"
argument_list|,
name|pte
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Mapping has not changed, must be protection or wiring change. 	 */
if|if
condition|(
name|opa
operator|==
name|pa
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|pwchange
operator|++
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * Wiring change, just update stats. 		 * We don't worry about wiring PT pages as they remain 		 * resident as long as there are valid mappings in them. 		 * Hence, if a user page is wired, the PT page will be also. 		 */
if|if
condition|(
name|wired
operator|&&
operator|!
name|pmap_pte_w
argument_list|(
name|pte
argument_list|)
operator|||
operator|!
name|wired
operator|&&
name|pmap_pte_w
argument_list|(
name|pte
argument_list|)
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_ENTER
condition|)
name|pg
argument_list|(
literal|"enter: wiring change -> %x\n"
argument_list|,
name|wired
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|wired
condition|)
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|++
expr_stmt|;
else|else
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|--
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|wchange
operator|++
expr_stmt|;
endif|#
directive|endif
block|}
ifdef|#
directive|ifdef
name|junk
comment|/* 		 * Retain cache inhibition status 		 */
name|checkpv
operator|=
name|FALSE
expr_stmt|;
if|if
condition|(
name|pmap_pte_ci
argument_list|(
name|pte
argument_list|)
condition|)
name|cacheable
operator|=
name|FALSE
expr_stmt|;
endif|#
directive|endif
goto|goto
name|validate
goto|;
block|}
comment|/* 	 * Mapping has changed, invalidate old range and fall through to 	 * handle validating new mapping. 	 */
if|if
condition|(
name|opa
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_ENTER
condition|)
name|pg
argument_list|(
literal|"enter: removing old mapping %x pa %x\n"
argument_list|,
name|va
argument_list|,
name|opa
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_remove
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|,
name|va
operator|+
name|PAGE_SIZE
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|mchange
operator|++
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * If this is a new user mapping, increment the wiring count 	 * on this PT page.  PT pages are wired down as long as there 	 * is a valid mapping in the page. 	 */
if|if
condition|(
name|pmap
operator|!=
name|kernel_pmap
condition|)
block|{
name|vm_page_t
name|mem
decl_stmt|;
comment|/*	mem = vm_page_lookup(pmap->pm_ptobj, (vm_offset_t)pdei(va));*/
name|mem
operator|->
name|wire_count
operator|++
expr_stmt|;
block|}
comment|/* 	 * Enter on the PV list if part of our managed memory 	 * Note that we raise IPL while manipulating pv_table 	 * since pmap_enter can be called at interrupt time. 	 */
if|if
condition|(
name|pa
operator|>=
name|vm_first_phys
operator|&&
name|pa
operator|<
name|vm_last_phys
condition|)
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|,
name|npv
decl_stmt|;
name|int
name|s
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|managed
operator|++
expr_stmt|;
endif|#
directive|endif
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_ENTER
condition|)
name|printf
argument_list|(
literal|"enter: pv at %x: %x/%x/%x\n"
argument_list|,
name|pv
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_next
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * No entries yet, use header as the first entry 		 */
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|==
name|PMAP_NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|firstpv
operator|++
expr_stmt|;
endif|#
directive|endif
name|pv
operator|->
name|pv_va
operator|=
name|va
expr_stmt|;
name|pv
operator|->
name|pv_pmap
operator|=
name|pmap
expr_stmt|;
name|pv
operator|->
name|pv_next
operator|=
name|PV_ENTRY_NULL
expr_stmt|;
name|pv
operator|->
name|pv_ptpde
operator|=
name|PD_ENTRY_NULL
expr_stmt|;
name|pv
operator|->
name|pv_ptpmap
operator|=
name|PMAP_NULL
expr_stmt|;
name|pv
operator|->
name|pv_flags
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 		 * There is at least one other VA mapping this page. 		 * Place this entry after the header. 		 */
else|else
block|{
comment|/*printf("second time: ");*/
ifdef|#
directive|ifdef
name|DEBUG
for|for
control|(
name|npv
operator|=
name|pv
init|;
name|npv
condition|;
name|npv
operator|=
name|npv
operator|->
name|pv_next
control|)
if|if
condition|(
name|pmap
operator|==
name|npv
operator|->
name|pv_pmap
operator|&&
name|va
operator|==
name|npv
operator|->
name|pv_va
condition|)
name|panic
argument_list|(
literal|"pmap_enter: already in pv_tab"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|npv
operator|=
operator|(
name|pv_entry_t
operator|)
name|malloc
argument_list|(
sizeof|sizeof
expr|*
name|npv
argument_list|,
name|M_VMPVENT
argument_list|,
name|M_NOWAIT
argument_list|)
expr_stmt|;
name|npv
operator|->
name|pv_va
operator|=
name|va
expr_stmt|;
name|npv
operator|->
name|pv_pmap
operator|=
name|pmap
expr_stmt|;
name|npv
operator|->
name|pv_next
operator|=
name|pv
operator|->
name|pv_next
expr_stmt|;
name|npv
operator|->
name|pv_ptpde
operator|=
name|PD_ENTRY_NULL
expr_stmt|;
name|npv
operator|->
name|pv_ptpmap
operator|=
name|PMAP_NULL
expr_stmt|;
name|pv
operator|->
name|pv_next
operator|=
name|npv
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|!
name|npv
operator|->
name|pv_next
condition|)
name|enter_stats
operator|.
name|secondpv
operator|++
expr_stmt|;
endif|#
directive|endif
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* 	 * Assumption: if it is not part of our managed memory 	 * then it must be device memory which may be volitile. 	 */
if|if
condition|(
name|pmap_initialized
condition|)
block|{
name|checkpv
operator|=
name|cacheable
operator|=
name|FALSE
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|enter_stats
operator|.
name|unmanaged
operator|++
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Increment counters 	 */
name|pmap
operator|->
name|pm_stats
operator|.
name|resident_count
operator|++
expr_stmt|;
if|if
condition|(
name|wired
condition|)
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|++
expr_stmt|;
name|validate
label|:
comment|/* 	 * Now validate mapping with desired protection/wiring. 	 * Assume uniform modified and referenced status for all 	 * I386 pages in a MACH page. 	 */
name|npte
operator|=
operator|(
name|pa
operator|&
name|PG_FRAME
operator|)
operator||
name|pte_prot
argument_list|(
name|pmap
argument_list|,
name|prot
argument_list|)
operator||
name|PG_V
expr_stmt|;
name|npte
operator||=
operator|(
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|&
operator|(
name|PG_M
operator||
name|PG_U
operator|)
operator|)
expr_stmt|;
if|if
condition|(
name|wired
condition|)
name|npte
operator||=
name|PG_W
expr_stmt|;
if|if
condition|(
name|va
operator|<
name|UPT_MIN_ADDRESS
condition|)
name|npte
operator||=
name|PG_u
expr_stmt|;
elseif|else
if|if
condition|(
name|va
operator|<
name|UPT_MAX_ADDRESS
condition|)
name|npte
operator||=
name|PG_u
operator||
name|PG_RW
expr_stmt|;
comment|/*if (!checkpv&& !cacheable) 		npte |= PG_CI;*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_ENTER
condition|)
name|printf
argument_list|(
literal|"enter: new pte value %x\n"
argument_list|,
name|npte
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|++
operator|=
name|npte
expr_stmt|;
comment|/*TBIS(va);*/
name|npte
operator|+=
name|I386_PAGE_SIZE
expr_stmt|;
name|va
operator|+=
name|I386_PAGE_SIZE
expr_stmt|;
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
name|pte
operator|--
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUGx
elseif|else
if|if
condition|(
name|pmapvacflush
operator|&
name|PVF_ENTER
condition|)
block|{
if|if
condition|(
name|pmapvacflush
operator|&
name|PVF_TOTAL
condition|)
name|DCIA
argument_list|()
expr_stmt|;
elseif|else
if|if
condition|(
name|pmap
operator|==
name|kernel_pmap
condition|)
name|DCIS
argument_list|()
expr_stmt|;
else|else
name|DCIU
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|pmapdebug
operator|&
name|PDB_WIRING
operator|)
operator|&&
name|pmap
operator|!=
name|kernel_pmap
condition|)
block|{
name|va
operator|-=
name|PAGE_SIZE
expr_stmt|;
name|pmap_check_wiring
argument_list|(
literal|"enter"
argument_list|,
name|trunc_page
argument_list|(
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
block|}
comment|/* note: during vm_mem_init(), is called before u.u_procp et al is set up */
if|if
condition|(
name|pmap
operator|==
name|u
operator|.
name|u_procp
operator|->
name|p_map
operator|->
name|pmap
condition|)
name|pmap_activate
argument_list|(
name|pmap
argument_list|,
operator|(
expr|struct
name|pcb
operator|*
operator|)
name|u
operator|.
name|u_procp
operator|->
name|p_addr
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|load_cr3
argument_list|(
name|u
operator|.
name|u_pcb
operator|.
name|pcb_ptd
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_change_wiring  *	Function:	Change the wiring attribute for a map/virtual-address  *			pair.  *	In/out conditions:  *			The mapping must already exist in the pmap.  */
end_comment

begin_function
name|void
name|pmap_change_wiring
parameter_list|(
name|pmap
parameter_list|,
name|va
parameter_list|,
name|wired
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|va
decl_stmt|;
name|boolean_t
name|wired
decl_stmt|;
block|{
specifier|register
name|pt_entry_t
modifier|*
name|pte
decl_stmt|;
specifier|register
name|int
name|ix
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_change_wiring(%x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|va
argument_list|,
name|wired
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|==
name|PMAP_NULL
condition|)
return|return;
name|pte
operator|=
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
comment|/* 	 * Page table page is not allocated. 	 * Should this ever happen?  Ignore it for now, 	 * we don't want to force allocation of unnecessary PTE pages. 	 */
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_PARANOIA
condition|)
name|pg
argument_list|(
literal|"pmap_change_wiring: invalid PDE for %x\n"
argument_list|,
name|va
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Page not valid.  Should this ever happen? 	 * Just continue and change wiring anyway. 	 */
if|if
condition|(
operator|!
name|pmap_pte_v
argument_list|(
name|pte
argument_list|)
condition|)
block|{
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_PARANOIA
condition|)
name|pg
argument_list|(
literal|"pmap_change_wiring: invalid PTE for %x\n"
argument_list|,
name|va
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|wired
operator|&&
operator|!
name|pmap_pte_w
argument_list|(
name|pte
argument_list|)
operator|||
operator|!
name|wired
operator|&&
name|pmap_pte_w
argument_list|(
name|pte
argument_list|)
condition|)
block|{
if|if
condition|(
name|wired
condition|)
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|++
expr_stmt|;
else|else
name|pmap
operator|->
name|pm_stats
operator|.
name|wired_count
operator|--
expr_stmt|;
block|}
comment|/* 	 * Wiring is not a hardware characteristic so there is no need 	 * to invalidate TLB. 	 */
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
name|pmap_pte_set_w
argument_list|(
name|pte
operator|++
argument_list|,
name|wired
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_pte  *	Function:  *		Extract the page table entry associated  *		with the given map/virtual_address pair.  */
end_comment

begin_function
name|struct
name|pte
modifier|*
name|pmap_pte
parameter_list|(
name|pmap
parameter_list|,
name|va
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|va
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_pte(%x, %x) ->\n"
argument_list|,
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|&&
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
comment|/* are we current address space or kernel? */
if|if
condition|(
operator|*
operator|(
name|int
operator|*
operator|)
operator|(
name|pmap
operator|->
name|pm_pdir
operator|+
name|PTDPTDI
operator|)
operator|==
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|PTDpde
operator|||
name|pmap
operator|==
name|kernel_pmap
condition|)
return|return
operator|(
operator|(
expr|struct
name|pte
operator|*
operator|)
name|vtopte
argument_list|(
name|va
argument_list|)
operator|)
return|;
comment|/* otherwise, we are alternate address space */
else|else
block|{
if|if
condition|(
operator|*
operator|(
name|int
operator|*
operator|)
operator|(
name|pmap
operator|->
name|pm_pdir
operator|+
name|PTDPTDI
operator|)
operator|!=
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|APTDpde
condition|)
block|{
name|APTDpde
operator|=
name|pmap
operator|->
name|pm_pdir
index|[
name|PTDPTDI
index|]
expr_stmt|;
comment|/*pg("reload APT %x", *(int *)&APTDpde);*/
name|load_cr3
argument_list|(
name|u
operator|.
name|u_pcb
operator|.
name|pcb_ptd
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
operator|(
expr|struct
name|pte
operator|*
operator|)
name|avtopte
argument_list|(
name|va
argument_list|)
operator|)
return|;
block|}
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_extract  *	Function:  *		Extract the physical page address associated  *		with the given map/virtual_address pair.  */
end_comment

begin_function
name|vm_offset_t
name|pmap_extract
parameter_list|(
name|pmap
parameter_list|,
name|va
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|vm_offset_t
name|va
decl_stmt|;
block|{
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_extract(%x, %x) -> "
argument_list|,
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pa
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|pmap
operator|&&
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
block|{
ifdef|#
directive|ifdef
name|notyet
comment|/* are we current address space or kernel? */
if|if
condition|(
name|pmap
operator|->
name|pm_pdir
index|[
name|PTDPTDI
index|]
operator|==
name|PTDpde
operator|||
name|pmap
operator|==
name|kernel_pmap
condition|)
name|pa
operator|=
operator|*
operator|(
name|int
operator|*
operator|)
name|vtopte
argument_list|(
name|va
argument_list|)
expr_stmt|;
ifndef|#
directive|ifndef
name|notyet
else|else
block|{
comment|/* we are alternate address space */
if|if
condition|(
name|pmap
operator|->
name|pm_pdir
index|[
name|PTDPTDI
index|]
operator|!=
name|APTDpde
condition|)
block|{
name|APTDpde
operator|=
name|pmap
operator|->
name|pm_pdir
index|[
name|PTDPTDI
index|]
expr_stmt|;
comment|/*pg("reload APT %x", *(int *) APTDpde);*/
name|load_cr3
argument_list|(
name|u
operator|.
name|u_pcb
operator|.
name|pcb_ptd
argument_list|)
expr_stmt|;
block|}
name|pa
operator|=
operator|*
operator|(
name|int
operator|*
operator|)
name|avtopte
argument_list|(
name|va
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
else|else
block|{
comment|/* extract pte from physical page table */
operator|*
operator|(
name|int
operator|*
operator|)
name|CMAP1
operator|=
operator|(
name|int
operator|)
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|pg
argument_list|(
literal|"other space %x"
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
name|CMAP1
argument_list|)
expr_stmt|;
name|load_cr3
argument_list|(
name|u
operator|.
name|u_pcb
operator|.
name|pcb_ptd
argument_list|)
expr_stmt|;
name|pa
operator|=
operator|*
operator|(
name|int
operator|*
operator|)
name|CADDR1
index|[
name|i386_btop
argument_list|(
name|va
argument_list|)
operator|&
literal|1023
index|]
expr_stmt|;
block|}
endif|#
directive|endif
else|#
directive|else
name|pa
operator|=
operator|*
operator|(
name|int
operator|*
operator|)
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
if|if
condition|(
name|pa
condition|)
name|pa
operator|=
operator|(
name|pa
operator|&
name|PG_FRAME
operator|)
operator||
operator|(
name|va
operator|&
operator|~
name|PG_FRAME
operator|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"%x\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|pa
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	Copy the range specified by src_addr/len  *	from the source map to the range dst_addr/len  *	in the destination map.  *  *	This routine is only advisory and need not do anything.  */
end_comment

begin_function
name|void
name|pmap_copy
parameter_list|(
name|dst_pmap
parameter_list|,
name|src_pmap
parameter_list|,
name|dst_addr
parameter_list|,
name|len
parameter_list|,
name|src_addr
parameter_list|)
name|pmap_t
name|dst_pmap
decl_stmt|;
name|pmap_t
name|src_pmap
decl_stmt|;
name|vm_offset_t
name|dst_addr
decl_stmt|;
name|vm_size_t
name|len
decl_stmt|;
name|vm_offset_t
name|src_addr
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_copy(%x, %x, %x, %x, %x)\n"
argument_list|,
name|dst_pmap
argument_list|,
name|src_pmap
argument_list|,
name|dst_addr
argument_list|,
name|len
argument_list|,
name|src_addr
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_comment
comment|/*  *	Require that all active physical maps contain no  *	incorrect entries NOW.  [This update includes  *	forcing updates of any address map caching.]  *  *	Generally used to insure that a thread about  *	to run will see a semantically correct world.  */
end_comment

begin_function
name|void
name|pmap_update
parameter_list|()
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_update()\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/*TBIA(); */
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_collect  *	Function:  *		Garbage collects the physical map system for  *		pages which are no longer used.  *		Success need not be guaranteed -- that is, there  *		may well be pages which are not referenced, but  *		others may be collected.  *	Usage:  *		Called by the pageout daemon when pages are scarce.  */
end_comment

begin_function
name|void
name|pmap_collect
parameter_list|(
name|pmap
parameter_list|)
name|pmap_t
name|pmap
decl_stmt|;
block|{
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
specifier|register
name|int
modifier|*
name|pte
decl_stmt|;
name|vm_offset_t
name|kpa
decl_stmt|;
name|int
name|s
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|int
modifier|*
name|pde
decl_stmt|;
name|int
name|opmapdebug
decl_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pmap
operator|!=
name|kernel_pmap
condition|)
return|return;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_collect(%x)\n"
argument_list|,
name|pmap
argument_list|)
expr_stmt|;
name|kpt_stats
operator|.
name|collectscans
operator|++
expr_stmt|;
endif|#
directive|endif
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
for|for
control|(
name|pa
operator|=
name|vm_first_phys
init|;
name|pa
operator|<
name|vm_last_phys
condition|;
name|pa
operator|+=
name|PAGE_SIZE
control|)
block|{
specifier|register
name|struct
name|kpt_page
modifier|*
name|kpt
decl_stmt|,
modifier|*
modifier|*
name|pkpt
decl_stmt|;
comment|/* 		 * Locate physical pages which are being used as kernel 		 * page table pages. 		 */
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|!=
name|kernel_pmap
operator|||
operator|!
operator|(
name|pv
operator|->
name|pv_flags
operator|&
name|PV_PTPAGE
operator|)
condition|)
continue|continue;
do|do
block|{
if|if
condition|(
name|pv
operator|->
name|pv_ptpde
operator|&&
name|pv
operator|->
name|pv_ptpmap
operator|==
name|kernel_pmap
condition|)
break|break;
block|}
do|while
condition|(
name|pv
operator|=
name|pv
operator|->
name|pv_next
condition|)
do|;
if|if
condition|(
name|pv
operator|==
name|PV_ENTRY_NULL
condition|)
continue|continue;
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pv
operator|->
name|pv_va
operator|<
operator|(
name|vm_offset_t
operator|)
name|Sysmap
operator|||
name|pv
operator|->
name|pv_va
operator|>=
operator|(
name|vm_offset_t
operator|)
name|Sysmap
operator|+
name|I386_MAX_PTSIZE
condition|)
name|pg
argument_list|(
literal|"collect: kernel PT VA out of range\n"
argument_list|)
expr_stmt|;
else|else
goto|goto
name|ok
goto|;
name|pmap_pvdump
argument_list|(
name|pa
argument_list|)
expr_stmt|;
continue|continue;
name|ok
label|:
endif|#
directive|endif
name|pte
operator|=
operator|(
name|int
operator|*
operator|)
operator|(
name|pv
operator|->
name|pv_va
operator|+
name|I386_PAGE_SIZE
operator|)
expr_stmt|;
while|while
condition|(
operator|--
name|pte
operator|>=
operator|(
name|int
operator|*
operator|)
name|pv
operator|->
name|pv_va
operator|&&
operator|*
name|pte
operator|==
literal|0
condition|)
empty_stmt|;
if|if
condition|(
name|pte
operator|>=
operator|(
name|int
operator|*
operator|)
name|pv
operator|->
name|pv_va
condition|)
continue|continue;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_PTPAGE
operator||
name|PDB_COLLECT
operator|)
condition|)
block|{
name|pg
argument_list|(
literal|"collect: freeing KPT page at %x (pde %x@%x)\n"
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
name|pv
operator|->
name|pv_ptpde
argument_list|,
name|pv
operator|->
name|pv_ptpde
argument_list|)
expr_stmt|;
name|opmapdebug
operator|=
name|pmapdebug
expr_stmt|;
name|pmapdebug
operator||=
name|PDB_PTPAGE
expr_stmt|;
block|}
name|pde
operator|=
operator|(
name|int
operator|*
operator|)
name|pv
operator|->
name|pv_ptpde
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * If all entries were invalid we can remove the page. 		 * We call pmap_remove to take care of invalidating PD 		 * and Sysptmap entries. 		 */
name|kpa
operator|=
name|pmap_extract
argument_list|(
name|pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|)
expr_stmt|;
name|pmap_remove
argument_list|(
name|pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
name|pv
operator|->
name|pv_va
operator|+
name|I386_PAGE_SIZE
argument_list|)
expr_stmt|;
comment|/* 		 * Use the physical address to locate the original 		 * (kmem_alloc assigned) address for the page and put 		 * that page back on the free list. 		 */
for|for
control|(
name|pkpt
operator|=
operator|&
name|kpt_used_list
operator|,
name|kpt
operator|=
operator|*
name|pkpt
init|;
name|kpt
operator|!=
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
literal|0
condition|;
name|pkpt
operator|=
operator|&
name|kpt
operator|->
name|kpt_next
operator|,
name|kpt
operator|=
operator|*
name|pkpt
control|)
if|if
condition|(
name|kpt
operator|->
name|kpt_pa
operator|==
name|kpa
condition|)
break|break;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|kpt
operator|==
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
literal|0
condition|)
name|panic
argument_list|(
literal|"pmap_collect: lost a KPT page"
argument_list|)
expr_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_PTPAGE
operator||
name|PDB_COLLECT
operator|)
condition|)
name|pg
argument_list|(
literal|"collect: %x (%x) to free list\n"
argument_list|,
name|kpt
operator|->
name|kpt_va
argument_list|,
name|kpa
argument_list|)
expr_stmt|;
endif|#
directive|endif
operator|*
name|pkpt
operator|=
name|kpt
operator|->
name|kpt_next
expr_stmt|;
name|kpt
operator|->
name|kpt_next
operator|=
name|kpt_free_list
expr_stmt|;
name|kpt_free_list
operator|=
name|kpt
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUGx
name|kpt_stats
operator|.
name|kptinuse
operator|--
expr_stmt|;
name|kpt_stats
operator|.
name|collectpages
operator|++
expr_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_PTPAGE
operator||
name|PDB_COLLECT
operator|)
condition|)
name|pmapdebug
operator|=
name|opmapdebug
expr_stmt|;
if|if
condition|(
operator|*
name|pde
condition|)
name|pg
argument_list|(
literal|"collect: kernel PDE at %x still valid (%x)\n"
argument_list|,
name|pde
argument_list|,
operator|*
name|pde
argument_list|)
expr_stmt|;
name|pde
operator|=
operator|(
name|int
operator|*
operator|)
operator|&
name|Sysptmap
index|[
operator|(
name|pd_entry_t
operator|*
operator|)
name|pde
operator|-
name|pmap_pde
argument_list|(
name|kernel_pmap
argument_list|,
literal|0
argument_list|)
index|]
expr_stmt|;
if|if
condition|(
operator|*
name|pde
condition|)
name|pg
argument_list|(
literal|"collect: kernel PTmap at %x still valid (%x)\n"
argument_list|,
name|pde
argument_list|,
operator|*
name|pde
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|pmap_activate
parameter_list|(
name|pmap
parameter_list|,
name|pcbp
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
name|struct
name|pcb
modifier|*
name|pcbp
decl_stmt|;
block|{
name|int
name|x
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_PDRTAB
operator|)
condition|)
name|printf
argument_list|(
literal|"pmap_activate(%x, %x) "
argument_list|,
name|pmap
argument_list|,
name|pcbp
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|PMAP_ACTIVATE
argument_list|(
name|pmap
argument_list|,
name|pcbp
argument_list|)
expr_stmt|;
comment|/*printf("pde "); for(x=0x3f6; x< 0x3fA; x++) 	printf("%x ", pmap->pm_pdir[x]);*/
comment|/*pads(pmap); pg(" pcb_cr3 %x", pcbp->pcb_cr3);*/
block|}
end_function

begin_comment
comment|/*  *	Routine:	pmap_kernel  *	Function:  *		Returns the physical map handle for the kernel.  */
end_comment

begin_function
name|pmap_t
name|pmap_kernel
parameter_list|()
block|{
return|return
operator|(
name|kernel_pmap
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	pmap_zero_page zeros the specified (machine independent)  *	page by mapping the page into virtual memory and using  *	bzero to clear its contents, one machine dependent page  *	at a time.  */
end_comment

begin_expr_stmt
name|pmap_zero_page
argument_list|(
name|phys
argument_list|)
specifier|register
name|vm_offset_t
name|phys
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|ix
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_zero_page(%x)\n"
argument_list|,
name|phys
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|phys
operator|>>=
name|PG_SHIFT
expr_stmt|;
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
name|clearseg
argument_list|(
name|phys
operator|++
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
block|}
end_block

begin_comment
comment|/*  *	pmap_copy_page copies the specified (machine independent)  *	page by mapping the page into virtual memory and using  *	bcopy to copy the page, one machine dependent page at a  *	time.  */
end_comment

begin_expr_stmt
name|pmap_copy_page
argument_list|(
name|src
argument_list|,
name|dst
argument_list|)
specifier|register
name|vm_offset_t
name|src
operator|,
name|dst
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|ix
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_copy_page(%x, %x)\n"
argument_list|,
name|src
argument_list|,
name|dst
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|src
operator|>>=
name|PG_SHIFT
expr_stmt|;
name|dst
operator|>>=
name|PG_SHIFT
expr_stmt|;
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
name|physcopyseg
argument_list|(
name|src
operator|++
argument_list|,
name|dst
operator|++
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
block|}
end_block

begin_comment
comment|/*  *	Routine:	pmap_pageable  *	Function:  *		Make the specified pages (by pmap, offset)  *		pageable (or not) as requested.  *  *		A page which is not pageable may not take  *		a fault; therefore, its page table entry  *		must remain valid for the duration.  *  *		This routine is merely advisory; pmap_enter  *		will specify that these pages are to be wired  *		down (or not) as appropriate.  */
end_comment

begin_macro
name|pmap_pageable
argument_list|(
argument|pmap
argument_list|,
argument|sva
argument_list|,
argument|eva
argument_list|,
argument|pageable
argument_list|)
end_macro

begin_decl_stmt
name|pmap_t
name|pmap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_offset_t
name|sva
decl_stmt|,
name|eva
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|boolean_t
name|pageable
decl_stmt|;
end_decl_stmt

begin_block
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"pmap_pageable(%x, %x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|sva
argument_list|,
name|eva
argument_list|,
name|pageable
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * If we are making a PT page pageable then all valid 	 * mappings must be gone from that page.  Hence it should 	 * be all zeros and there is no need to clean it. 	 * Assumptions: 	 *	- we are called with only one page at a time 	 *	- PT pages have only one pv_table entry 	 */
if|if
condition|(
name|pmap
operator|==
name|kernel_pmap
operator|&&
name|pageable
operator|&&
name|sva
operator|+
name|PAGE_SIZE
operator|==
name|eva
condition|)
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_PTPAGE
operator|)
operator|)
operator|==
name|PDB_PTPAGE
condition|)
name|pg
argument_list|(
literal|"pmap_pageable(%x, %x, %x, %x)\n"
argument_list|,
name|pmap
argument_list|,
name|sva
argument_list|,
name|eva
argument_list|,
name|pageable
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|sva
argument_list|)
argument_list|)
condition|)
return|return;
name|pa
operator|=
name|pmap_pte_pa
argument_list|(
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|sva
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|pa
operator|<
name|vm_first_phys
operator|||
name|pa
operator|>=
name|vm_last_phys
condition|)
return|return;
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
if|if
condition|(
name|pv
operator|->
name|pv_ptpde
operator|==
name|PD_ENTRY_NULL
condition|)
return|return;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pv
operator|->
name|pv_va
operator|!=
name|sva
operator|||
name|pv
operator|->
name|pv_next
condition|)
block|{
name|pg
argument_list|(
literal|"pmap_pageable: bad PT page va %x next %x\n"
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
name|pv
operator|->
name|pv_next
argument_list|)
expr_stmt|;
return|return;
block|}
endif|#
directive|endif
comment|/* 		 * Mark it unmodified to avoid pageout 		 */
name|pmap_clear_modify
argument_list|(
name|pa
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_PTPAGE
condition|)
name|pg
argument_list|(
literal|"pmap_pageable: PT page %x(%x) unmodified\n"
argument_list|,
name|sva
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|sva
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_WIRING
condition|)
name|pmap_check_wiring
argument_list|(
literal|"pageable"
argument_list|,
name|sva
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
block|}
end_block

begin_comment
comment|/*  *	Clear the modify bits on the specified physical page.  */
end_comment

begin_function
name|void
name|pmap_clear_modify
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_clear_modify(%x)\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_changebit
argument_list|(
name|pa
argument_list|,
name|PG_M
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	pmap_clear_reference:  *  *	Clear the reference bit on the specified physical page.  */
end_comment

begin_function
name|void
name|pmap_clear_reference
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
name|pg
argument_list|(
literal|"pmap_clear_reference(%x)\n"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_changebit
argument_list|(
name|pa
argument_list|,
name|PG_U
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  *	pmap_is_referenced:  *  *	Return whether or not the specified physical page is referenced  *	by any physical maps.  */
end_comment

begin_function
name|boolean_t
name|pmap_is_referenced
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
block|{
name|boolean_t
name|rv
init|=
name|pmap_testbit
argument_list|(
name|pa
argument_list|,
name|PG_U
argument_list|)
decl_stmt|;
name|pg
argument_list|(
literal|"pmap_is_referenced(%x) -> %c\n"
argument_list|,
name|pa
argument_list|,
literal|"FT"
index|[
name|rv
index|]
argument_list|)
expr_stmt|;
return|return
operator|(
name|rv
operator|)
return|;
block|}
endif|#
directive|endif
return|return
operator|(
name|pmap_testbit
argument_list|(
name|pa
argument_list|,
name|PG_U
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *	pmap_is_modified:  *  *	Return whether or not the specified physical page is modified  *	by any physical maps.  */
end_comment

begin_function
name|boolean_t
name|pmap_is_modified
parameter_list|(
name|pa
parameter_list|)
name|vm_offset_t
name|pa
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_FOLLOW
condition|)
block|{
name|boolean_t
name|rv
init|=
name|pmap_testbit
argument_list|(
name|pa
argument_list|,
name|PG_M
argument_list|)
decl_stmt|;
name|pg
argument_list|(
literal|"pmap_is_modified(%x) -> %c\n"
argument_list|,
name|pa
argument_list|,
literal|"FT"
index|[
name|rv
index|]
argument_list|)
expr_stmt|;
return|return
operator|(
name|rv
operator|)
return|;
block|}
endif|#
directive|endif
return|return
operator|(
name|pmap_testbit
argument_list|(
name|pa
argument_list|,
name|PG_M
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|vm_offset_t
name|pmap_phys_address
parameter_list|(
name|ppn
parameter_list|)
name|int
name|ppn
decl_stmt|;
block|{
return|return
operator|(
name|i386_ptob
argument_list|(
name|ppn
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Miscellaneous support routines follow  */
end_comment

begin_comment
comment|/* static */
end_comment

begin_macro
name|i386_protection_init
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|int
modifier|*
name|kp
decl_stmt|,
name|prot
decl_stmt|;
name|kp
operator|=
name|protection_codes
expr_stmt|;
for|for
control|(
name|prot
operator|=
literal|0
init|;
name|prot
operator|<
literal|8
condition|;
name|prot
operator|++
control|)
block|{
switch|switch
condition|(
name|prot
condition|)
block|{
case|case
name|VM_PROT_NONE
operator||
name|VM_PROT_NONE
operator||
name|VM_PROT_NONE
case|:
operator|*
name|kp
operator|++
operator|=
literal|0
expr_stmt|;
break|break;
case|case
name|VM_PROT_READ
operator||
name|VM_PROT_NONE
operator||
name|VM_PROT_NONE
case|:
case|case
name|VM_PROT_READ
operator||
name|VM_PROT_NONE
operator||
name|VM_PROT_EXECUTE
case|:
case|case
name|VM_PROT_NONE
operator||
name|VM_PROT_NONE
operator||
name|VM_PROT_EXECUTE
case|:
operator|*
name|kp
operator|++
operator|=
name|PG_RO
expr_stmt|;
break|break;
case|case
name|VM_PROT_NONE
operator||
name|VM_PROT_WRITE
operator||
name|VM_PROT_NONE
case|:
case|case
name|VM_PROT_NONE
operator||
name|VM_PROT_WRITE
operator||
name|VM_PROT_EXECUTE
case|:
case|case
name|VM_PROT_READ
operator||
name|VM_PROT_WRITE
operator||
name|VM_PROT_NONE
case|:
case|case
name|VM_PROT_READ
operator||
name|VM_PROT_WRITE
operator||
name|VM_PROT_EXECUTE
case|:
operator|*
name|kp
operator|++
operator|=
name|PG_RW
expr_stmt|;
break|break;
block|}
block|}
block|}
end_block

begin_comment
comment|/* static */
end_comment

begin_function
name|boolean_t
name|pmap_testbit
parameter_list|(
name|pa
parameter_list|,
name|bit
parameter_list|)
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
name|int
name|bit
decl_stmt|;
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
specifier|register
name|int
modifier|*
name|pte
decl_stmt|,
name|ix
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|pa
operator|<
name|vm_first_phys
operator|||
name|pa
operator|>=
name|vm_last_phys
condition|)
return|return
operator|(
name|FALSE
operator|)
return|;
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
comment|/* 	 * Check saved info first 	 */
if|if
condition|(
name|pmap_attributes
index|[
name|pa_index
argument_list|(
name|pa
argument_list|)
index|]
operator|&
name|bit
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
comment|/* 	 * Not found, check current mappings returning 	 * immediately if found. 	 */
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|!=
name|PMAP_NULL
condition|)
block|{
for|for
control|(
init|;
name|pv
condition|;
name|pv
operator|=
name|pv
operator|->
name|pv_next
control|)
block|{
name|pte
operator|=
operator|(
name|int
operator|*
operator|)
name|pmap_pte
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|)
expr_stmt|;
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
if|if
condition|(
operator|*
name|pte
operator|++
operator|&
name|bit
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|TRUE
operator|)
return|;
block|}
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
block|}
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
end_function

begin_comment
comment|/* static */
end_comment

begin_expr_stmt
name|pmap_changebit
argument_list|(
name|pa
argument_list|,
name|bit
argument_list|,
name|setem
argument_list|)
specifier|register
name|vm_offset_t
name|pa
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|bit
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|boolean_t
name|setem
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
specifier|register
name|int
modifier|*
name|pte
decl_stmt|,
name|npte
decl_stmt|,
name|ix
decl_stmt|;
name|vm_offset_t
name|va
decl_stmt|;
name|int
name|s
decl_stmt|;
name|boolean_t
name|firstpage
init|=
name|TRUE
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_BITS
condition|)
name|printf
argument_list|(
literal|"pmap_changebit(%x, %x, %s)\n"
argument_list|,
name|pa
argument_list|,
name|bit
argument_list|,
name|setem
condition|?
literal|"set"
else|:
literal|"clear"
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pa
operator|<
name|vm_first_phys
operator|||
name|pa
operator|>=
name|vm_last_phys
condition|)
return|return;
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
comment|/* 	 * Clear saved attributes (modify, reference) 	 */
if|if
condition|(
operator|!
name|setem
condition|)
name|pmap_attributes
index|[
name|pa_index
argument_list|(
name|pa
argument_list|)
index|]
operator|&=
operator|~
name|bit
expr_stmt|;
comment|/* 	 * Loop over all current mappings setting/clearing as appropos 	 * If setting RO do we need to clear the VAC? 	 */
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|!=
name|PMAP_NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|int
name|toflush
init|=
literal|0
decl_stmt|;
endif|#
directive|endif
for|for
control|(
init|;
name|pv
condition|;
name|pv
operator|=
name|pv
operator|->
name|pv_next
control|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
name|toflush
operator||=
operator|(
name|pv
operator|->
name|pv_pmap
operator|==
name|kernel_pmap
operator|)
condition|?
literal|2
else|:
literal|1
expr_stmt|;
endif|#
directive|endif
name|va
operator|=
name|pv
operator|->
name|pv_va
expr_stmt|;
name|pte
operator|=
operator|(
name|int
operator|*
operator|)
name|pmap_pte
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|ix
operator|=
literal|0
expr_stmt|;
do|do
block|{
if|if
condition|(
name|setem
condition|)
name|npte
operator|=
operator|*
name|pte
operator||
name|bit
expr_stmt|;
else|else
name|npte
operator|=
operator|*
name|pte
operator|&
operator|~
name|bit
expr_stmt|;
if|if
condition|(
operator|*
name|pte
operator|!=
name|npte
condition|)
block|{
operator|*
name|pte
operator|=
name|npte
expr_stmt|;
comment|/*TBIS(va);*/
block|}
name|va
operator|+=
name|I386_PAGE_SIZE
expr_stmt|;
name|pte
operator|++
expr_stmt|;
block|}
do|while
condition|(
operator|++
name|ix
operator|!=
name|i386pagesperpage
condition|)
do|;
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|==
name|u
operator|.
name|u_procp
operator|->
name|p_map
operator|->
name|pmap
condition|)
name|pmap_activate
argument_list|(
name|pv
operator|->
name|pv_pmap
argument_list|,
operator|(
expr|struct
name|pcb
operator|*
operator|)
name|u
operator|.
name|u_procp
operator|->
name|p_addr
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|setem
operator|&&
name|bit
operator|==
name|PG_RO
operator|&&
operator|(
name|pmapvacflush
operator|&
name|PVF_PROTECT
operator|)
condition|)
block|{
if|if
condition|(
operator|(
name|pmapvacflush
operator|&
name|PVF_TOTAL
operator|)
operator|||
name|toflush
operator|==
literal|3
condition|)
name|DCIA
argument_list|()
expr_stmt|;
elseif|else
if|if
condition|(
name|toflush
operator|==
literal|2
condition|)
name|DCIS
argument_list|()
expr_stmt|;
else|else
name|DCIU
argument_list|()
expr_stmt|;
block|}
endif|#
directive|endif
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/* static */
end_comment

begin_function
name|void
name|pmap_enter_ptpage
parameter_list|(
name|pmap
parameter_list|,
name|va
parameter_list|)
specifier|register
name|pmap_t
name|pmap
decl_stmt|;
specifier|register
name|vm_offset_t
name|va
decl_stmt|;
block|{
specifier|register
name|vm_offset_t
name|ptpa
decl_stmt|;
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
name|pd_entry_t
modifier|*
name|pde
decl_stmt|;
name|int
name|s
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_FOLLOW
operator||
name|PDB_ENTER
operator||
name|PDB_PTPAGE
operator|)
condition|)
name|pg
argument_list|(
literal|"pmap_enter_ptpage: pmap %x, va %x\n"
argument_list|,
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|enter_stats
operator|.
name|ptpneeded
operator|++
expr_stmt|;
endif|#
directive|endif
name|pde
operator|=
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
expr_stmt|;
name|va
operator|=
name|trunc_page
argument_list|(
operator|(
name|vm_offset_t
operator|)
name|pmap_pte
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|)
argument_list|)
expr_stmt|;
comment|/* 	 * In the kernel we allocate a page from the kernel PT page 	 * free list and map it into the kernel page table map (via 	 * pmap_enter). 	 */
if|if
condition|(
name|pmap
operator|==
name|kernel_pmap
condition|)
block|{
name|panic
argument_list|(
literal|"pmap_enterptpage: kernel map"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|notquiteyet
specifier|register
name|struct
name|kpt_page
modifier|*
name|kpt
decl_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
if|if
condition|(
operator|(
name|kpt
operator|=
name|kpt_free_list
operator|)
operator|==
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
literal|0
condition|)
block|{
comment|/* 			 * No PT pages available. 			 * Try once to free up unused ones. 			 */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
name|PDB_COLLECT
condition|)
name|pg
argument_list|(
literal|"enter: no KPT pages, collecting...\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pmap_collect
argument_list|(
name|kernel_pmap
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|kpt
operator|=
name|kpt_free_list
operator|)
operator|==
operator|(
expr|struct
name|kpt_page
operator|*
operator|)
literal|0
condition|)
name|panic
argument_list|(
literal|"pmap_enter_ptpage: can't get KPT page"
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|++
name|kpt_stats
operator|.
name|kptinuse
operator|>
name|kpt_stats
operator|.
name|kptmaxuse
condition|)
name|kpt_stats
operator|.
name|kptmaxuse
operator|=
name|kpt_stats
operator|.
name|kptinuse
expr_stmt|;
endif|#
directive|endif
name|kpt_free_list
operator|=
name|kpt
operator|->
name|kpt_next
expr_stmt|;
name|kpt
operator|->
name|kpt_next
operator|=
name|kpt_used_list
expr_stmt|;
name|kpt_used_list
operator|=
name|kpt
expr_stmt|;
name|ptpa
operator|=
name|kpt
operator|->
name|kpt_pa
expr_stmt|;
name|bzero
argument_list|(
name|kpt
operator|->
name|kpt_va
argument_list|,
name|I386_PAGE_SIZE
argument_list|)
expr_stmt|;
name|pmap_enter
argument_list|(
name|pmap
argument_list|,
name|va
argument_list|,
name|ptpa
argument_list|,
name|VM_PROT_DEFAULT
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUGx
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_ENTER
operator||
name|PDB_PTPAGE
operator|)
condition|)
name|pg
argument_list|(
literal|"enter: add&Sysptmap[%d]: %x (KPT page %x)\n"
argument_list|,
name|pde
operator|-
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
literal|0
argument_list|)
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
operator|&
name|Sysptmap
index|[
name|pde
operator|-
name|pmap_pde
argument_list|(
name|pmap
argument_list|,
literal|0
argument_list|)
index|]
argument_list|,
name|kpt
operator|->
name|kpt_va
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * For user processes we just simulate a fault on that location 	 * letting the VM system allocate a zero-filled page. 	 */
else|else
block|{
name|vm_page_t
name|mem
decl_stmt|;
name|vm_offset_t
name|offset
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_ENTER
operator||
name|PDB_PTPAGE
operator|)
condition|)
name|pg
argument_list|(
literal|"enter: about to load UPT pg at %x\n"
argument_list|,
name|va
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|needtotranslate
name|offset
operator|=
name|pdei
argument_list|(
name|va
argument_list|)
expr_stmt|;
name|vm_object_lock
argument_list|(
operator|(
name|vm_object_t
operator|)
name|pmap
operator|->
name|pm_ptobj
argument_list|)
expr_stmt|;
while|while
condition|(
operator|(
name|mem
operator|=
name|vm_page_alloc
argument_list|(
operator|(
name|vm_object_t
operator|)
name|pmap
operator|->
name|pm_ptobj
argument_list|,
name|offset
argument_list|)
operator|)
operator|==
name|VM_PAGE_NULL
condition|)
block|{
name|vm_object_unlock
argument_list|(
operator|(
name|vm_object_t
operator|)
name|pmap
operator|->
name|pm_ptobj
argument_list|)
expr_stmt|;
name|VM_WAIT
expr_stmt|;
name|vm_object_lock
argument_list|(
operator|(
name|vm_object_t
operator|)
name|pmap
operator|->
name|pm_ptobj
argument_list|)
expr_stmt|;
block|}
name|vm_page_zero_fill
argument_list|(
name|mem
argument_list|)
expr_stmt|;
name|mem
operator|->
name|busy
operator|=
name|FALSE
expr_stmt|;
name|mem
operator|->
name|ptpage
operator|=
name|TRUE
expr_stmt|;
name|ptpa
operator|=
name|mem
operator|->
name|phys_addr
expr_stmt|;
name|vm_object_unlock
argument_list|(
operator|(
name|vm_object_t
operator|)
name|pmap
operator|->
name|pm_ptobj
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Locate the PV entry in the kernel for this PT page and 	 * record the PDE address.  This is so that we can invalidate 	 * the PDE when we remove the mapping for the page. 	 */
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|ptpa
argument_list|)
expr_stmt|;
name|s
operator|=
name|splimp
argument_list|()
expr_stmt|;
if|if
condition|(
name|pv
condition|)
block|{
name|pv
operator|->
name|pv_flags
operator||=
name|PV_PTPAGE
expr_stmt|;
do|do
block|{
if|if
condition|(
name|pv
operator|->
name|pv_pmap
operator|==
name|kernel_pmap
operator|&&
name|pv
operator|->
name|pv_va
operator|==
name|va
condition|)
break|break;
block|}
do|while
condition|(
name|pv
operator|=
name|pv
operator|->
name|pv_next
condition|)
do|;
block|}
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pv
operator|==
name|PV_ENTRY_NULL
condition|)
name|panic
argument_list|(
literal|"pmap_enter_ptpage: PT page not entered"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pv
operator|->
name|pv_ptpde
operator|=
name|pde
expr_stmt|;
name|pv
operator|->
name|pv_ptpmap
operator|=
name|pmap
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_ENTER
operator||
name|PDB_PTPAGE
operator|)
condition|)
name|pg
argument_list|(
literal|"enter: new PT page at PA %x, pde at %x\n"
argument_list|,
name|ptpa
argument_list|,
name|pde
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Map the new PT page into the page directory table. 	 *Also increment the reference count on the page directory table if this 	 * was a user page table page.  Note that we don't use vm_map_pageable 	 * to keep the count like we do for PT pages, this is mostly because 	 * it would be difficult to identify PD pages in pmap_pageable to 	 * release them.  We also avoid the overhead of vm_map_pageable. 	 */
operator|*
operator|(
name|int
operator|*
operator|)
name|pde
operator|=
operator|(
name|ptpa
operator|&
name|PG_FRAME
operator|)
operator||
name|PG_RW
operator||
name|PG_V
expr_stmt|;
if|if
condition|(
name|pmap
operator|!=
name|kernel_pmap
condition|)
block|{
name|pmap
operator|->
name|pm_dref
operator|++
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|pmapdebug
operator|&
operator|(
name|PDB_ENTER
operator||
name|PDB_PTPAGE
operator||
name|PDB_PDRTAB
operator|)
condition|)
name|pg
argument_list|(
literal|"enter: pdtab %x refcnt %d\n"
argument_list|,
name|pmap
operator|->
name|pm_pdir
argument_list|,
name|pmap
operator|->
name|pm_dref
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Flush stale TLB info. 	 */
comment|/*if (pmap == kernel_pmap) 		TBIAS(); 	else 		TBIAU(); */
if|if
condition|(
name|pmap
operator|==
name|u
operator|.
name|u_procp
operator|->
name|p_map
operator|->
name|pmap
condition|)
name|pmap_activate
argument_list|(
name|pmap
argument_list|,
operator|(
expr|struct
name|pcb
operator|*
operator|)
name|u
operator|.
name|u_procp
operator|->
name|p_addr
argument_list|)
expr_stmt|;
name|pmap
operator|->
name|pm_ptpages
operator|++
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_macro
name|pmap_pvdump
argument_list|(
argument|pa
argument_list|)
end_macro

begin_decl_stmt
name|vm_offset_t
name|pa
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|pv_entry_t
name|pv
decl_stmt|;
name|pg
argument_list|(
literal|"pa %x"
argument_list|,
name|pa
argument_list|)
expr_stmt|;
for|for
control|(
name|pv
operator|=
name|pa_to_pvh
argument_list|(
name|pa
argument_list|)
init|;
name|pv
condition|;
name|pv
operator|=
name|pv
operator|->
name|pv_next
control|)
name|pg
argument_list|(
literal|" -> pmap %x, va %x, ptpde %x, ptpmap %x, flags %x"
argument_list|,
name|pv
operator|->
name|pv_pmap
argument_list|,
name|pv
operator|->
name|pv_va
argument_list|,
name|pv
operator|->
name|pv_ptpde
argument_list|,
name|pv
operator|->
name|pv_ptpmap
argument_list|,
name|pv
operator|->
name|pv_flags
argument_list|)
expr_stmt|;
name|pg
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|pmap_check_wiring
argument_list|(
argument|str
argument_list|,
argument|va
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|str
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_offset_t
name|va
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vm_map_entry_t
name|entry
decl_stmt|;
specifier|register
name|int
name|count
decl_stmt|,
modifier|*
name|pte
decl_stmt|;
name|va
operator|=
name|trunc_page
argument_list|(
name|va
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pmap_pde_v
argument_list|(
name|pmap_pde
argument_list|(
name|kernel_pmap
argument_list|,
name|va
argument_list|)
argument_list|)
operator|||
operator|!
name|pmap_pte_v
argument_list|(
name|pmap_pte
argument_list|(
name|kernel_pmap
argument_list|,
name|va
argument_list|)
argument_list|)
condition|)
return|return;
if|if
condition|(
operator|!
name|vm_map_lookup_entry
argument_list|(
name|pt_map
argument_list|,
name|va
argument_list|,
operator|&
name|entry
argument_list|)
condition|)
block|{
name|pg
argument_list|(
literal|"wired_check: entry for %x not found\n"
argument_list|,
name|va
argument_list|)
expr_stmt|;
return|return;
block|}
name|count
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|pte
operator|=
operator|(
name|int
operator|*
operator|)
name|va
init|;
name|pte
operator|<
operator|(
name|int
operator|*
operator|)
operator|(
name|va
operator|+
name|PAGE_SIZE
operator|)
condition|;
name|pte
operator|++
control|)
if|if
condition|(
operator|*
name|pte
condition|)
name|count
operator|++
expr_stmt|;
if|if
condition|(
name|entry
operator|->
name|wired_count
operator|!=
name|count
condition|)
name|pg
argument_list|(
literal|"*%s*: %x: w%d/a%d\n"
argument_list|,
name|str
argument_list|,
name|va
argument_list|,
name|entry
operator|->
name|wired_count
argument_list|,
name|count
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/* print address space of pmap*/
end_comment

begin_macro
name|pads
argument_list|(
argument|pm
argument_list|)
end_macro

begin_decl_stmt
name|pmap_t
name|pm
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|unsigned
name|va
decl_stmt|,
name|i
decl_stmt|,
name|j
decl_stmt|;
name|struct
name|pte
modifier|*
name|ptep
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|1024
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|pm
operator|->
name|pm_pdir
index|[
name|i
index|]
operator|.
name|pd_v
condition|)
for|for
control|(
name|j
operator|=
literal|0
init|;
name|j
operator|<
literal|1024
condition|;
name|j
operator|++
control|)
block|{
name|va
operator|=
operator|(
name|i
operator|<<
literal|22
operator|)
operator|+
operator|(
name|j
operator|<<
literal|12
operator|)
expr_stmt|;
if|if
condition|(
name|pm
operator|==
name|kernel_pmap
operator|&&
name|va
operator|<
literal|0xfe000000
condition|)
continue|continue;
if|if
condition|(
name|pm
operator|!=
name|kernel_pmap
operator|&&
name|va
operator|>
literal|0xfdc00000
condition|)
continue|continue;
name|ptep
operator|=
name|pmap_pte
argument_list|(
name|pm
argument_list|,
name|va
argument_list|)
expr_stmt|;
if|if
condition|(
name|pmap_pte_v
argument_list|(
name|ptep
argument_list|)
condition|)
name|printf
argument_list|(
literal|"%x:%x "
argument_list|,
name|va
argument_list|,
operator|*
operator|(
name|int
operator|*
operator|)
name|ptep
argument_list|)
expr_stmt|;
block|}
empty_stmt|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

