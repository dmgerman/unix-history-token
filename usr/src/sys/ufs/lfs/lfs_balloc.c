begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1989, 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)lfs_balloc.c	7.38 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/resourcevar.h>
end_include

begin_include
include|#
directive|include
file|<sys/trace.h>
end_include

begin_include
include|#
directive|include
file|<miscfs/specfs/specdev.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs_extern.h>
end_include

begin_decl_stmt
name|int
name|lfs_getlbns
name|__P
argument_list|(
operator|(
expr|struct
name|vnode
operator|*
operator|,
name|daddr_t
operator|,
name|INDIR
operator|*
operator|,
name|int
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Bmap converts a the logical block number of a file to its physical block  * number on the disk. The conversion is done by using the logical block  * number to index into the array of block pointers described by the dinode.  */
end_comment

begin_function
name|int
name|lfs_bmap
parameter_list|(
name|ap
parameter_list|)
name|struct
name|vop_bmap_args
comment|/* { 		struct vnode *a_vp; 		daddr_t  a_bn; 		struct vnode **a_vpp; 		daddr_t *a_bnp; 	} */
modifier|*
name|ap
decl_stmt|;
block|{
comment|/* 	 * Check for underlying vnode requests and ensure that logical 	 * to physical mapping is requested. 	 */
if|if
condition|(
name|ap
operator|->
name|a_vpp
operator|!=
name|NULL
condition|)
operator|*
name|ap
operator|->
name|a_vpp
operator|=
name|VTOI
argument_list|(
name|ap
operator|->
name|a_vp
argument_list|)
operator|->
name|i_devvp
expr_stmt|;
if|if
condition|(
name|ap
operator|->
name|a_bnp
operator|==
name|NULL
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
name|lfs_bmaparray
argument_list|(
name|ap
operator|->
name|a_vp
argument_list|,
name|ap
operator|->
name|a_bn
argument_list|,
name|ap
operator|->
name|a_bnp
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * LFS has a different version of bmap from FFS because of a naming conflict.  * In FFS, meta blocks are given real disk addresses at allocation time, and  * are linked into the device vnode, using a logical block number which is  * the same as the physical block number.  This can't be done by LFS because  * blocks aren't given disk addresses until they're written, so there's no  * way to distinguish the meta-data blocks for one file from any other file.  * This means that meta-data blocks have to be on the vnode for the file so  * they can be found, and have to have "names" different from the standard  * data blocks.  To do this, we divide the name space into positive and  * negative block numbers, and give the meta-data blocks negative logical  * numbers.  Indirect blocks are addressed by the negative address of the  * first data block to which they point.  Double indirect blocks are addressed  * by one less than the address of the first indirect block to which they  * point.  Triple indirect blocks are addressed by one less than the address  * of the first double indirect block to which they point.  */
end_comment

begin_function
name|int
name|lfs_bmaparray
parameter_list|(
name|vp
parameter_list|,
name|bn
parameter_list|,
name|bnp
parameter_list|,
name|ap
parameter_list|,
name|nump
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
specifier|register
name|daddr_t
name|bn
decl_stmt|;
name|daddr_t
modifier|*
name|bnp
decl_stmt|;
name|INDIR
modifier|*
name|ap
decl_stmt|;
name|int
modifier|*
name|nump
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
name|struct
name|vnode
modifier|*
name|devvp
decl_stmt|;
name|INDIR
name|a
index|[
name|NIADDR
index|]
decl_stmt|,
modifier|*
name|xap
decl_stmt|;
name|daddr_t
modifier|*
name|bap
decl_stmt|,
name|daddr
decl_stmt|;
name|long
name|metalbn
decl_stmt|;
name|int
name|bb
decl_stmt|,
name|error
decl_stmt|,
name|num
decl_stmt|,
name|off
decl_stmt|;
name|struct
name|vop_strategy_args
name|vop_strategy_a
decl_stmt|;
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|ap
operator|!=
name|NULL
operator|&&
name|nump
operator|==
name|NULL
operator|||
name|ap
operator|==
name|NULL
operator|&&
name|nump
operator|!=
name|NULL
condition|)
name|panic
argument_list|(
literal|"lfs_bmaparray: invalid arguments"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|xap
operator|=
name|ap
operator|==
name|NULL
condition|?
name|a
else|:
name|ap
expr_stmt|;
if|if
condition|(
operator|!
name|nump
condition|)
name|nump
operator|=
operator|&
name|num
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|lfs_getlbns
argument_list|(
name|vp
argument_list|,
name|bn
argument_list|,
name|xap
argument_list|,
name|nump
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|num
operator|=
operator|*
name|nump
expr_stmt|;
if|if
condition|(
name|num
operator|==
literal|0
condition|)
block|{
operator|*
name|bnp
operator|=
name|ip
operator|->
name|i_db
index|[
name|bn
index|]
expr_stmt|;
if|if
condition|(
operator|*
name|bnp
operator|==
literal|0
condition|)
operator|*
name|bnp
operator|=
name|UNASSIGNED
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* Get disk address out of indirect block array */
name|daddr
operator|=
name|ip
operator|->
name|i_ib
index|[
name|xap
operator|->
name|in_off
index|]
expr_stmt|;
comment|/* Fetch through the indirect blocks. */
name|fs
operator|=
name|ip
operator|->
name|i_lfs
expr_stmt|;
name|devvp
operator|=
name|VFSTOUFS
argument_list|(
name|vp
operator|->
name|v_mount
argument_list|)
operator|->
name|um_devvp
expr_stmt|;
for|for
control|(
name|bp
operator|=
name|NULL
operator|,
operator|++
name|xap
init|;
operator|--
name|num
condition|;
operator|++
name|xap
control|)
block|{
comment|/* 		 * If we were called explicitly then we don't want to create 		 * indirect blocks.  Since BMAP calls pass NULL for the ap, 		 * we can use that to detect if we are called from BMAP or not. 		 */
if|if
condition|(
name|daddr
operator|==
literal|0
operator|&&
name|ap
operator|!=
name|NULL
condition|)
break|break;
comment|/* If looking for a meta-block, break out when we find it. */
name|metalbn
operator|=
name|xap
operator|->
name|in_lbn
expr_stmt|;
if|if
condition|(
name|metalbn
operator|==
name|bn
condition|)
break|break;
comment|/* 		 * Read in the appropriate indirect block.  LFS can't do a 		 * bread because bread knows that FFS will hand it the device 		 * vnode, not the file vnode, so the b_dev and b_blkno would 		 * be wrong. 		 * 		 * XXX 		 * This REALLY needs to be fixed, at the very least it needs 		 * to be rethought when the buffer cache goes away.  When it's 		 * fixed, change lfs_bmaparray and lfs_getlbns to take an ip, 		 * not a vp. 		 */
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bp
operator|=
name|getblk
argument_list|(
name|vp
argument_list|,
name|metalbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
condition|)
block|{
name|trace
argument_list|(
name|TR_BREADHIT
argument_list|,
name|pack
argument_list|(
name|vp
argument_list|,
name|size
argument_list|)
argument_list|,
name|metalbn
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|daddr
condition|)
block|{
comment|/* Need to create an indirect block */
name|trace
argument_list|(
name|TR_BREADMISS
argument_list|,
name|pack
argument_list|(
name|vp
argument_list|,
name|size
argument_list|)
argument_list|,
name|metalbn
argument_list|)
expr_stmt|;
name|bzero
argument_list|(
name|bp
operator|->
name|b_un
operator|.
name|b_addr
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|)
expr_stmt|;
operator|*
name|bnp
operator|=
name|UNASSIGNED
expr_stmt|;
name|bb
operator|=
name|fsbtodb
argument_list|(
name|fs
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* XXX Need to figure out how to get a cred */
if|if
condition|(
operator|!
name|ISSPACE_XXX
argument_list|(
name|fs
argument_list|,
name|bb
argument_list|)
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOSPC
operator|)
return|;
block|}
name|daddr
operator|=
name|bp
operator|->
name|b_un
operator|.
name|b_daddr
index|[
name|xap
operator|->
name|in_off
index|]
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|VOP_BWRITE
argument_list|(
name|bp
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|ip
operator|->
name|i_blocks
operator|+=
name|bb
expr_stmt|;
name|fs
operator|->
name|lfs_bfree
operator|-=
name|bb
expr_stmt|;
name|bp
operator|=
name|NULL
expr_stmt|;
continue|continue;
block|}
else|else
block|{
name|trace
argument_list|(
name|TR_BREADMISS
argument_list|,
name|pack
argument_list|(
name|vp
argument_list|,
name|size
argument_list|)
argument_list|,
name|metalbn
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_blkno
operator|=
name|daddr
expr_stmt|;
name|bp
operator|->
name|b_flags
operator||=
name|B_READ
expr_stmt|;
name|bp
operator|->
name|b_dev
operator|=
name|devvp
operator|->
name|v_rdev
expr_stmt|;
comment|/* 			 * Call a strategy VOP by hand. 			 */
name|vop_strategy_a
operator|.
name|a_desc
operator|=
name|VDESC
argument_list|(
name|vop_strategy
argument_list|)
expr_stmt|;
name|vop_strategy_a
operator|.
name|a_bp
operator|=
name|bp
expr_stmt|;
name|VOCALL
argument_list|(
name|devvp
operator|->
name|v_op
argument_list|,
name|VOFFSET
argument_list|(
name|vop_strategy
argument_list|)
argument_list|, \
operator|&
name|vop_strategy_a
argument_list|)
expr_stmt|;
name|curproc
operator|->
name|p_stats
operator|->
name|p_ru
operator|.
name|ru_inblock
operator|++
expr_stmt|;
comment|/* XXX */
if|if
condition|(
name|error
operator|=
name|biowait
argument_list|(
name|bp
argument_list|)
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
block|}
name|daddr
operator|=
name|bp
operator|->
name|b_un
operator|.
name|b_daddr
index|[
name|xap
operator|->
name|in_off
index|]
expr_stmt|;
block|}
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
operator|*
name|bnp
operator|=
name|daddr
operator|==
literal|0
condition|?
name|UNASSIGNED
else|:
name|daddr
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Create an array of logical block number/offset pairs which represent the  * path of indirect blocks required to access a data block.  The first "pair"  * contains the logical block number of the appropriate single, double or  * triple indirect block and the offset into the inode indirect block array.  * Note, the logical block number of the inode single/double/triple indirect  * block appears twice in the array, once with the offset into the i_ib and  * once with the offset into the page itself.  */
end_comment

begin_function
name|int
name|lfs_getlbns
parameter_list|(
name|vp
parameter_list|,
name|bn
parameter_list|,
name|ap
parameter_list|,
name|nump
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
specifier|register
name|daddr_t
name|bn
decl_stmt|;
name|INDIR
modifier|*
name|ap
decl_stmt|;
name|int
modifier|*
name|nump
decl_stmt|;
block|{
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
name|long
name|metalbn
decl_stmt|,
name|realbn
decl_stmt|;
name|int
name|j
decl_stmt|,
name|numlevels
decl_stmt|,
name|off
decl_stmt|,
name|sh
decl_stmt|;
if|if
condition|(
name|nump
condition|)
operator|*
name|nump
operator|=
literal|0
expr_stmt|;
name|numlevels
operator|=
literal|0
expr_stmt|;
name|realbn
operator|=
name|bn
expr_stmt|;
if|if
condition|(
operator|(
name|long
operator|)
name|bn
operator|<
literal|0
condition|)
name|bn
operator|=
operator|-
operator|(
name|long
operator|)
name|bn
expr_stmt|;
comment|/* The first NDADDR blocks are direct blocks. */
if|if
condition|(
name|bn
operator|<
name|NDADDR
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/*  	 * Determine the number of levels of indirection.  After this loop 	 * is done, sh indicates the number of data blocks possible at the 	 * given level of indirection, and NIADDR - j is the number of levels 	 * of indirection needed to locate the requested block. 	 */
name|bn
operator|-=
name|NDADDR
expr_stmt|;
name|fs
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
operator|->
name|i_lfs
expr_stmt|;
name|sh
operator|=
literal|1
expr_stmt|;
for|for
control|(
name|j
operator|=
name|NIADDR
init|;
name|j
operator|>
literal|0
condition|;
name|j
operator|--
control|)
block|{
name|sh
operator|*=
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
if|if
condition|(
name|bn
operator|<
name|sh
condition|)
break|break;
name|bn
operator|-=
name|sh
expr_stmt|;
block|}
if|if
condition|(
name|j
operator|==
literal|0
condition|)
return|return
operator|(
name|EFBIG
operator|)
return|;
comment|/* Calculate the address of the first meta-block. */
if|if
condition|(
name|realbn
operator|>=
literal|0
condition|)
name|metalbn
operator|=
operator|-
operator|(
name|realbn
operator|-
name|bn
operator|+
name|NIADDR
operator|-
name|j
operator|)
expr_stmt|;
else|else
name|metalbn
operator|=
operator|-
operator|(
operator|-
name|realbn
operator|-
name|bn
operator|+
name|NIADDR
operator|-
name|j
operator|)
expr_stmt|;
comment|/*  	 * At each iteration, off is the offset into the bap array which is 	 * an array of disk addresses at the current level of indirection. 	 * The logical block number and the offset in that block are stored 	 * into the argument array. 	 */
operator|++
name|numlevels
expr_stmt|;
name|ap
operator|->
name|in_lbn
operator|=
name|metalbn
expr_stmt|;
name|ap
operator|->
name|in_off
operator|=
name|off
operator|=
name|NIADDR
operator|-
name|j
expr_stmt|;
name|ap
operator|++
expr_stmt|;
for|for
control|(
init|;
name|j
operator|<=
name|NIADDR
condition|;
name|j
operator|++
control|)
block|{
comment|/* If searching for a meta-data block, quit when found. */
if|if
condition|(
name|metalbn
operator|==
name|realbn
condition|)
break|break;
name|sh
operator|/=
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
name|off
operator|=
operator|(
name|bn
operator|/
name|sh
operator|)
operator|%
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
operator|++
name|numlevels
expr_stmt|;
name|ap
operator|->
name|in_lbn
operator|=
name|metalbn
expr_stmt|;
name|ap
operator|->
name|in_off
operator|=
name|off
expr_stmt|;
operator|++
name|ap
expr_stmt|;
name|metalbn
operator|-=
operator|-
literal|1
operator|+
name|off
operator|*
name|sh
expr_stmt|;
block|}
if|if
condition|(
name|nump
condition|)
operator|*
name|nump
operator|=
name|numlevels
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
name|int
name|lfs_balloc
parameter_list|(
name|vp
parameter_list|,
name|iosize
parameter_list|,
name|lbn
parameter_list|,
name|bpp
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|u_long
name|iosize
decl_stmt|;
name|daddr_t
name|lbn
decl_stmt|;
name|struct
name|buf
modifier|*
modifier|*
name|bpp
decl_stmt|;
block|{
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
name|daddr_t
name|daddr
decl_stmt|;
name|int
name|bb
decl_stmt|,
name|error
decl_stmt|;
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
name|fs
operator|=
name|ip
operator|->
name|i_lfs
expr_stmt|;
comment|/*  	 * Three cases: it's a block beyond the end of file, it's a block in 	 * the file that may or may not have been assigned a disk address or 	 * we're writing an entire block.  Note, if the daddr is unassigned, 	 * the block might still have existed in the cache.  If it did, make 	 * sure we don't count it as a new block or zero out its contents. 	 * Note that we always call bmap, even if it's a new block beyond 	 * the end of file. The reason is so that we can allocate any new 	 * indirect blocks that are necessary. 	 */
operator|*
name|bpp
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|VOP_BMAP
argument_list|(
name|vp
argument_list|,
name|lbn
argument_list|,
name|NULL
argument_list|,
operator|&
name|daddr
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
if|if
condition|(
name|daddr
operator|==
name|UNASSIGNED
operator|||
name|iosize
operator|==
name|fs
operator|->
name|lfs_bsize
condition|)
block|{
operator|*
name|bpp
operator|=
name|bp
operator|=
name|getblk
argument_list|(
name|vp
argument_list|,
name|lbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|daddr
operator|==
name|UNASSIGNED
operator|&&
operator|!
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_CACHE
operator|)
condition|)
block|{
name|bb
operator|=
name|fsbtodb
argument_list|(
name|fs
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|ISSPACE_XXX
argument_list|(
name|fs
argument_list|,
name|bb
argument_list|)
condition|)
block|{
comment|/* Pretend we never allocated the buffer */
name|bp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
operator|*
name|bpp
operator|=
name|NULL
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOSPC
operator|)
return|;
block|}
name|ip
operator|->
name|i_blocks
operator|+=
name|bb
expr_stmt|;
name|fs
operator|->
name|lfs_bfree
operator|-=
name|bb
expr_stmt|;
if|if
condition|(
name|iosize
operator|!=
name|fs
operator|->
name|lfs_bsize
condition|)
name|clrbuf
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
return|return
operator|(
name|bread
argument_list|(
name|vp
argument_list|,
name|lbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|,
name|NOCRED
argument_list|,
name|bpp
argument_list|)
operator|)
return|;
block|}
end_function

end_unit

