begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1989, 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)lfs_balloc.c	7.42 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/resourcevar.h>
end_include

begin_include
include|#
directive|include
file|<sys/trace.h>
end_include

begin_include
include|#
directive|include
file|<miscfs/specfs/specdev.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs_extern.h>
end_include

begin_function
name|int
name|lfs_balloc
parameter_list|(
name|vp
parameter_list|,
name|iosize
parameter_list|,
name|lbn
parameter_list|,
name|bpp
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|u_long
name|iosize
decl_stmt|;
name|daddr_t
name|lbn
decl_stmt|;
name|struct
name|buf
modifier|*
modifier|*
name|bpp
decl_stmt|;
block|{
name|struct
name|buf
modifier|*
name|ibp
decl_stmt|,
modifier|*
name|bp
decl_stmt|;
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
name|struct
name|indir
name|indirs
index|[
name|NIADDR
operator|+
literal|2
index|]
decl_stmt|;
name|daddr_t
name|daddr
decl_stmt|;
name|int
name|bb
decl_stmt|,
name|error
decl_stmt|,
name|i
decl_stmt|,
name|num
decl_stmt|;
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
name|fs
operator|=
name|ip
operator|->
name|i_lfs
expr_stmt|;
comment|/*  	 * Three cases: it's a block beyond the end of file, it's a block in 	 * the file that may or may not have been assigned a disk address or 	 * we're writing an entire block.  Note, if the daddr is unassigned, 	 * the block might still have existed in the cache (if it was read 	 * or written earlier).  If it did, make sure we don't count it as a 	 * new block or zero out its contents.  If it did not, make sure 	 * we allocate any necessary indirect blocks. 	 */
operator|*
name|bpp
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|ufs_bmaparray
argument_list|(
name|vp
argument_list|,
name|lbn
argument_list|,
operator|&
name|daddr
argument_list|,
operator|&
name|indirs
index|[
literal|0
index|]
argument_list|,
operator|&
name|num
argument_list|,
name|NULL
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
operator|*
name|bpp
operator|=
name|bp
operator|=
name|getblk
argument_list|(
name|vp
argument_list|,
name|lbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|bb
operator|=
name|VFSTOUFS
argument_list|(
name|vp
operator|->
name|v_mount
argument_list|)
operator|->
name|um_seqinc
expr_stmt|;
if|if
condition|(
name|daddr
operator|==
name|UNASSIGNED
condition|)
comment|/* May need to allocate indirect blocks */
for|for
control|(
name|i
operator|=
literal|1
init|;
name|i
operator|<
name|num
condition|;
operator|++
name|i
control|)
if|if
condition|(
operator|!
name|indirs
index|[
name|i
index|]
operator|.
name|in_exists
condition|)
block|{
name|ibp
operator|=
name|getblk
argument_list|(
name|vp
argument_list|,
name|indirs
index|[
name|i
index|]
operator|.
name|in_lbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|ibp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
operator|)
condition|)
block|{
if|if
condition|(
operator|!
name|ISSPACE
argument_list|(
name|fs
argument_list|,
name|bb
argument_list|,
name|curproc
operator|->
name|p_ucred
argument_list|)
condition|)
block|{
name|ibp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
name|brelse
argument_list|(
name|ibp
argument_list|)
expr_stmt|;
name|error
operator|=
name|ENOSPC
expr_stmt|;
block|}
else|else
block|{
name|ip
operator|->
name|i_blocks
operator|+=
name|bb
expr_stmt|;
name|ip
operator|->
name|i_lfs
operator|->
name|lfs_bfree
operator|-=
name|bb
expr_stmt|;
name|clrbuf
argument_list|(
name|ibp
argument_list|)
expr_stmt|;
name|error
operator|=
name|VOP_BWRITE
argument_list|(
name|ibp
argument_list|)
expr_stmt|;
block|}
block|}
else|else
name|panic
argument_list|(
literal|"Indirect block should not exist"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|error
condition|)
block|{
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
comment|/* Now, we may need to allocate the data block */
if|if
condition|(
operator|!
operator|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_CACHE
operator||
name|B_DONE
operator||
name|B_DELWRI
operator|)
operator|)
condition|)
block|{
if|if
condition|(
name|daddr
operator|==
name|UNASSIGNED
condition|)
if|if
condition|(
operator|!
name|ISSPACE
argument_list|(
name|fs
argument_list|,
name|bb
argument_list|,
name|curproc
operator|->
name|p_ucred
argument_list|)
condition|)
block|{
name|bp
operator|->
name|b_flags
operator||=
name|B_INVAL
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOSPC
operator|)
return|;
block|}
else|else
block|{
name|ip
operator|->
name|i_blocks
operator|+=
name|bb
expr_stmt|;
name|ip
operator|->
name|i_lfs
operator|->
name|lfs_bfree
operator|-=
name|bb
expr_stmt|;
if|if
condition|(
name|iosize
operator|!=
name|fs
operator|->
name|lfs_bsize
condition|)
name|clrbuf
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|iosize
operator|==
name|fs
operator|->
name|lfs_bsize
condition|)
name|bp
operator|->
name|b_blkno
operator|=
name|daddr
expr_stmt|;
comment|/* Skip the I/O */
else|else
block|{
name|bp
operator|->
name|b_blkno
operator|=
name|daddr
expr_stmt|;
name|bp
operator|->
name|b_flags
operator||=
name|B_READ
expr_stmt|;
name|VOP_STRATEGY
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|biowait
argument_list|(
name|bp
argument_list|)
operator|)
return|;
block|}
block|}
return|return
operator|(
name|error
operator|)
return|;
block|}
end_function

end_unit

