begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1989, 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)lfs_balloc.c	7.22 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/resourcevar.h>
end_include

begin_include
include|#
directive|include
file|<sys/specdev.h>
end_include

begin_include
include|#
directive|include
file|<sys/trace.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs_extern.h>
end_include

begin_comment
comment|/*  * Bmap converts a the logical block number of a file to its physical block  * number on the disk. The conversion is done by using the logical block  * number to index into the array of block pointers described by the dinode.  *  * LFS has a different version of bmap from FFS because of a naming conflict.  * In FFS, meta blocks are given real disk addresses at allocation time, and  * are linked into the device vnode, using a logical block number which is  * the same as the physical block number.  This can't be done by LFS because  * blocks aren't given disk addresses until they're written, so there's no  * way to distinguish the meta-data blocks for one file from any other file.  * This means that meta-data blocks have to be on the vnode for the file so  * they can be found, and have to have "names" different from the standard  * data blocks.  To do this, we divide the name space into positive and  * negative block numbers, and give the meta-data blocks negative logical  * numbers.  Indirect blocks are addressed by the negative address of the  * first data block to which they point.  Double indirect blocks are addressed  * by one less than the address of the first indirect block to which they  * point.  Triple indirect blocks are addressed by one less than the address  * of the first double indirect block to which they point.  */
end_comment

begin_function
name|int
name|lfs_bmap
parameter_list|(
name|vp
parameter_list|,
name|bn
parameter_list|,
name|vpp
parameter_list|,
name|bnp
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
specifier|register
name|daddr_t
name|bn
decl_stmt|;
name|struct
name|vnode
modifier|*
modifier|*
name|vpp
decl_stmt|;
name|daddr_t
modifier|*
name|bnp
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
specifier|register
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
specifier|register
name|daddr_t
name|nb
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|struct
name|vnode
modifier|*
name|devvp
decl_stmt|;
name|daddr_t
modifier|*
name|bap
decl_stmt|,
name|daddr
decl_stmt|,
name|metalbn
decl_stmt|;
name|long
name|realbn
decl_stmt|;
name|int
name|error
decl_stmt|,
name|j
decl_stmt|,
name|off
decl_stmt|,
name|sh
decl_stmt|;
comment|/* 	 * Check for underlying vnode requests and ensure that logical 	 * to physical mapping is requested. 	 */
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
if|if
condition|(
name|vpp
operator|!=
name|NULL
condition|)
operator|*
name|vpp
operator|=
name|ip
operator|->
name|i_devvp
expr_stmt|;
if|if
condition|(
name|bnp
operator|==
name|NULL
condition|)
return|return
operator|(
literal|0
operator|)
return|;
ifdef|#
directive|ifdef
name|VERBOSE
name|printf
argument_list|(
literal|"lfs_bmap: block number %d, inode %d\n"
argument_list|,
name|bn
argument_list|,
name|ip
operator|->
name|i_number
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|realbn
operator|=
name|bn
expr_stmt|;
if|if
condition|(
operator|(
name|long
operator|)
name|bn
operator|<
literal|0
condition|)
name|bn
operator|=
operator|-
operator|(
name|long
operator|)
name|bn
expr_stmt|;
comment|/* The first NDADDR blocks are direct blocks. */
if|if
condition|(
name|bn
operator|<
name|NDADDR
condition|)
block|{
name|nb
operator|=
name|ip
operator|->
name|i_db
index|[
name|bn
index|]
expr_stmt|;
if|if
condition|(
name|nb
operator|==
literal|0
condition|)
block|{
operator|*
name|bnp
operator|=
name|UNASSIGNED
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
operator|*
name|bnp
operator|=
name|nb
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/*  	 * Determine the number of levels of indirection.  After this loop 	 * is done, sh indicates the number of data blocks possible at the 	 * given level of indirection, and NIADDR - j is the number of levels 	 * of indirection needed to locate the requested block. 	 */
name|bn
operator|-=
name|NDADDR
expr_stmt|;
name|fs
operator|=
name|ip
operator|->
name|i_lfs
expr_stmt|;
name|sh
operator|=
literal|1
expr_stmt|;
for|for
control|(
name|j
operator|=
name|NIADDR
init|;
name|j
operator|>
literal|0
condition|;
name|j
operator|--
control|)
block|{
name|sh
operator|*=
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
if|if
condition|(
name|bn
operator|<
name|sh
condition|)
break|break;
name|bn
operator|-=
name|sh
expr_stmt|;
block|}
if|if
condition|(
name|j
operator|==
literal|0
condition|)
return|return
operator|(
name|EFBIG
operator|)
return|;
comment|/* Calculate the address of the first meta-block. */
if|if
condition|(
name|realbn
operator|>=
literal|0
condition|)
name|metalbn
operator|=
operator|-
operator|(
name|realbn
operator|-
name|bn
operator|+
name|NIADDR
operator|-
name|j
operator|)
expr_stmt|;
else|else
name|metalbn
operator|=
operator|-
operator|(
operator|-
name|realbn
operator|-
name|bn
operator|+
name|NIADDR
operator|-
name|j
operator|)
expr_stmt|;
comment|/*  	 * Fetch through the indirect blocks.  At each iteration, off is the 	 * offset into the bap array which is an array of disk addresses at 	 * the current level of indirection. 	 */
name|bp
operator|=
name|NULL
expr_stmt|;
name|devvp
operator|=
name|VFSTOUFS
argument_list|(
name|vp
operator|->
name|v_mount
argument_list|)
operator|->
name|um_devvp
expr_stmt|;
for|for
control|(
name|off
operator|=
name|NIADDR
operator|-
name|j
operator|,
name|bap
operator|=
name|ip
operator|->
name|i_ib
init|;
name|j
operator|<=
name|NIADDR
condition|;
name|j
operator|++
control|)
block|{
comment|/* 		 * In LFS, it's possible to have a block appended to a file 		 * for which the meta-blocks have not yet been allocated. 		 * This is a win if the file never gets written or if the 		 * file's growing. 		 */
if|if
condition|(
operator|(
name|daddr
operator|=
name|bap
index|[
name|off
index|]
operator|)
operator|==
literal|0
condition|)
block|{
name|daddr
operator|=
name|UNASSIGNED
expr_stmt|;
break|break;
block|}
comment|/* If searching for a meta-data block, quit when found. */
if|if
condition|(
name|metalbn
operator|==
name|realbn
condition|)
break|break;
comment|/* 		 * Read in the appropriate indirect block.  LFS can't do a 		 * bread because bread knows that FFS will hand it the device 		 * vnode, not the file vnode, so the b_dev and b_blkno would 		 * be wrong. 		 * 		 * XXX 		 * This REALLY needs to be fixed, at the very least it needs 		 * to be rethought when the buffer cache goes away. 		 */
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bp
operator|=
name|getblk
argument_list|(
name|vp
argument_list|,
name|metalbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|b_flags
operator|&
operator|(
name|B_DONE
operator||
name|B_DELWRI
operator|)
condition|)
block|{
name|trace
argument_list|(
name|TR_BREADHIT
argument_list|,
name|pack
argument_list|(
name|vp
argument_list|,
name|size
argument_list|)
argument_list|,
name|metalbn
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|trace
argument_list|(
name|TR_BREADMISS
argument_list|,
name|pack
argument_list|(
name|vp
argument_list|,
name|size
argument_list|)
argument_list|,
name|metalbn
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_blkno
operator|=
name|daddr
expr_stmt|;
name|bp
operator|->
name|b_flags
operator||=
name|B_READ
expr_stmt|;
name|bp
operator|->
name|b_dev
operator|=
name|devvp
operator|->
name|v_rdev
expr_stmt|;
call|(
name|devvp
operator|->
name|v_op
operator|->
name|vop_strategy
call|)
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|curproc
operator|->
name|p_stats
operator|->
name|p_ru
operator|.
name|ru_inblock
operator|++
expr_stmt|;
comment|/* XXX */
if|if
condition|(
name|error
operator|=
name|biowait
argument_list|(
name|bp
argument_list|)
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
block|}
name|bap
operator|=
name|bp
operator|->
name|b_un
operator|.
name|b_daddr
expr_stmt|;
name|sh
operator|/=
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
name|off
operator|=
operator|(
name|bn
operator|/
name|sh
operator|)
operator|%
name|NINDIR
argument_list|(
name|fs
argument_list|)
expr_stmt|;
name|metalbn
operator|-=
operator|-
literal|1
operator|+
name|off
operator|*
name|sh
expr_stmt|;
block|}
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
operator|*
name|bnp
operator|=
name|daddr
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

