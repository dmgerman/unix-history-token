begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1986, 1989, 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)lfs_inode.c	7.51 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufs_extern.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs.h>
end_include

begin_include
include|#
directive|include
file|<ufs/lfs/lfs_extern.h>
end_include

begin_function
name|int
name|lfs_init
parameter_list|()
block|{
ifdef|#
directive|ifdef
name|VERBOSE
name|printf
argument_list|(
literal|"lfs_init\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
name|ufs_init
argument_list|()
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Look up an LFS dinode number to find its incore vnode.  If not already  * in core, read it in from the specified device.  Return the inode locked.  * Detection and handling of mount points must be done by the calling routine.  */
end_comment

begin_function
name|int
name|lfs_vget
parameter_list|(
name|mntp
parameter_list|,
name|ino
parameter_list|,
name|vpp
parameter_list|)
name|struct
name|mount
modifier|*
name|mntp
decl_stmt|;
name|ino_t
name|ino
decl_stmt|;
name|struct
name|vnode
modifier|*
modifier|*
name|vpp
decl_stmt|;
block|{
specifier|register
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|struct
name|ufsmount
modifier|*
name|ump
decl_stmt|;
name|dev_t
name|dev
decl_stmt|;
name|int
name|error
decl_stmt|;
ifdef|#
directive|ifdef
name|VERBOSE
name|printf
argument_list|(
literal|"lfs_vget\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|ump
operator|=
name|VFSTOUFS
argument_list|(
name|mntp
argument_list|)
expr_stmt|;
name|dev
operator|=
name|ump
operator|->
name|um_dev
expr_stmt|;
if|if
condition|(
operator|(
operator|*
name|vpp
operator|=
name|ufs_ihashget
argument_list|(
name|dev
argument_list|,
name|ino
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* Allocate new vnode/inode. */
if|if
condition|(
name|error
operator|=
name|lfs_vcreate
argument_list|(
name|mntp
argument_list|,
name|ino
argument_list|,
operator|&
name|vp
argument_list|)
condition|)
block|{
operator|*
name|vpp
operator|=
name|NULL
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
comment|/* 	 * Put it onto its hash chain and lock it so that other requests for 	 * this inode will block if they arrive while we are sleeping waiting 	 * for old data structures to be purged or for the contents of the 	 * disk portion of this inode to be read. 	 */
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
name|ufs_ihashins
argument_list|(
name|ip
argument_list|)
expr_stmt|;
comment|/* Read in the disk contents for the inode, copy into the inode. */
name|ip
operator|->
name|i_lfs
operator|=
name|fs
operator|=
name|ump
operator|->
name|um_lfs
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|bread
argument_list|(
name|ump
operator|->
name|um_devvp
argument_list|,
name|lfs_itod
argument_list|(
name|fs
argument_list|,
name|ino
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|fs
operator|->
name|lfs_bsize
argument_list|,
name|NOCRED
argument_list|,
operator|&
name|bp
argument_list|)
condition|)
block|{
comment|/* 		 * The inode does not contain anything useful, so it 		 * would be misleading to leave it on its hash chain. 		 * Iput() will return it to the free list. 		 */
name|remque
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_forw
operator|=
name|ip
expr_stmt|;
name|ip
operator|->
name|i_back
operator|=
name|ip
expr_stmt|;
comment|/* Unlock and discard unneeded inode. */
name|ufs_iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
operator|*
name|vpp
operator|=
name|NULL
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
name|ip
operator|->
name|i_din
operator|=
operator|*
name|lfs_ifind
argument_list|(
name|fs
argument_list|,
name|ino
argument_list|,
name|bp
operator|->
name|b_un
operator|.
name|b_dino
argument_list|)
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
comment|/* 	 * Initialize the vnode from the inode, check for aliases.  In all 	 * cases re-init ip, the underlying vnode/inode may have changed. 	 */
if|if
condition|(
name|error
operator|=
name|ufs_vinit
argument_list|(
name|mntp
argument_list|,
operator|&
name|lfs_specops
argument_list|,
name|LFS_FIFOOPS
argument_list|,
operator|&
name|vp
argument_list|)
condition|)
block|{
name|ufs_iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
operator|*
name|vpp
operator|=
name|NULL
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
comment|/* 	 * Finish inode initialization now that aliasing has been resolved. 	 */
name|ip
operator|->
name|i_devvp
operator|=
name|ump
operator|->
name|um_devvp
expr_stmt|;
name|VREF
argument_list|(
name|ip
operator|->
name|i_devvp
argument_list|)
expr_stmt|;
operator|*
name|vpp
operator|=
name|vp
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
name|int
name|lfs_update
parameter_list|(
name|vp
parameter_list|,
name|ta
parameter_list|,
name|tm
parameter_list|,
name|waitfor
parameter_list|)
specifier|register
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|struct
name|timeval
modifier|*
name|ta
decl_stmt|,
decl|*
name|tm
decl_stmt|;
end_function

begin_decl_stmt
name|int
name|waitfor
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
ifdef|#
directive|ifdef
name|VERBOSE
name|printf
argument_list|(
literal|"lfs_update\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|vp
operator|->
name|v_mount
operator|->
name|mnt_flag
operator|&
name|MNT_RDONLY
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|ip
operator|->
name|i_flag
operator|&
operator|(
name|IUPD
operator||
name|IACC
operator||
name|ICHG
operator||
name|IMOD
operator|)
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IACC
condition|)
name|ip
operator|->
name|i_atime
operator|=
name|ta
operator|->
name|tv_sec
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IUPD
condition|)
block|{
name|ip
operator|->
name|i_mtime
operator|=
name|tm
operator|->
name|tv_sec
expr_stmt|;
name|INCRQUAD
argument_list|(
operator|(
name|ip
operator|)
operator|->
name|i_modrev
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ICHG
condition|)
name|ip
operator|->
name|i_ctime
operator|=
name|time
operator|.
name|tv_sec
expr_stmt|;
name|ip
operator|->
name|i_flag
operator|&=
operator|~
operator|(
name|IUPD
operator||
name|IACC
operator||
name|ICHG
operator||
name|IMOD
operator|)
expr_stmt|;
comment|/* 	 * XXX 	 * I'm not real sure what to do here; once we have fsync and partial 	 * segments working in the LFS context, this must be fixed to be 	 * correct.  The contents of the inode have to be pushed back to 	 * stable storage. 	 */
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Truncate the inode ip to at most length size.  *  * NB: triple indirect blocks are untested.  */
end_comment

begin_comment
comment|/* ARGSUSED */
end_comment

begin_function
name|int
name|lfs_truncate
parameter_list|(
name|ovp
parameter_list|,
name|length
parameter_list|,
name|flags
parameter_list|)
name|struct
name|vnode
modifier|*
name|ovp
decl_stmt|;
name|u_long
name|length
decl_stmt|;
name|int
name|flags
decl_stmt|;
block|{
specifier|register
name|struct
name|lfs
modifier|*
name|fs
decl_stmt|;
specifier|register
name|struct
name|inode
modifier|*
name|oip
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|daddr_t
name|lbn
decl_stmt|;
name|int
name|error
decl_stmt|,
name|offset
decl_stmt|,
name|size
decl_stmt|;
ifdef|#
directive|ifdef
name|VERBOSE
name|printf
argument_list|(
literal|"lfs_truncate\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|vnode_pager_setsize
argument_list|(
name|ovp
argument_list|,
name|length
argument_list|)
expr_stmt|;
name|oip
operator|=
name|VTOI
argument_list|(
name|ovp
argument_list|)
expr_stmt|;
comment|/* If length is larger than the file, just update the times. */
if|if
condition|(
name|oip
operator|->
name|i_size
operator|<=
name|length
condition|)
block|{
name|oip
operator|->
name|i_flag
operator||=
name|ICHG
operator||
name|IUPD
expr_stmt|;
name|ITIMES
argument_list|(
name|oip
argument_list|,
operator|&
name|time
argument_list|,
operator|&
name|time
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* 	 * Update the size of the file. If the file is not being truncated to 	 * a block boundry, the contents of the partial block following the end 	 * of the file must be zero'ed in case it ever become accessable again 	 * because of subsequent file growth. 	 */
name|fs
operator|=
name|oip
operator|->
name|i_lfs
expr_stmt|;
name|offset
operator|=
name|blkoff
argument_list|(
name|fs
argument_list|,
name|length
argument_list|)
expr_stmt|;
if|if
condition|(
name|offset
operator|==
literal|0
condition|)
name|oip
operator|->
name|i_size
operator|=
name|length
expr_stmt|;
else|else
block|{
name|lbn
operator|=
name|lblkno
argument_list|(
name|fs
argument_list|,
name|length
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|QUOTA
if|if
condition|(
name|error
operator|=
name|getinoquota
argument_list|(
name|oip
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
endif|#
directive|endif
if|if
condition|(
name|error
operator|=
name|bread
argument_list|(
name|ovp
argument_list|,
name|lbn
argument_list|,
name|fs
operator|->
name|lfs_bsize
argument_list|,
name|NOCRED
argument_list|,
operator|&
name|bp
argument_list|)
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|oip
operator|->
name|i_size
operator|=
name|length
expr_stmt|;
name|size
operator|=
name|blksize
argument_list|(
name|fs
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|vnode_pager_uncache
argument_list|(
name|ovp
argument_list|)
expr_stmt|;
name|bzero
argument_list|(
name|bp
operator|->
name|b_un
operator|.
name|b_addr
operator|+
name|offset
argument_list|,
call|(
name|unsigned
call|)
argument_list|(
name|size
operator|-
name|offset
argument_list|)
argument_list|)
expr_stmt|;
name|allocbuf
argument_list|(
name|bp
argument_list|,
name|size
argument_list|)
expr_stmt|;
name|LFS_UBWRITE
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * XXX 	 * Bzero inode block pointers here, for consistency with ffs. 	 * Segment usage information has to be updated when the blocks 	 * are free. 	 * Block count in the inode has to be fixed when blocks are 	 * free. 	 */
operator|(
name|void
operator|)
name|vinvalbuf
argument_list|(
name|ovp
argument_list|,
name|length
operator|>
literal|0
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

