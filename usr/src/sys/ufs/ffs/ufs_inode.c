begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)ufs_inode.c	7.43 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufs_extern.h>
end_include

begin_decl_stmt
name|u_long
name|nextgennumber
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Next generation number to assign. */
end_comment

begin_decl_stmt
name|int
name|prtactive
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* 1 => print out reclaim of active vnodes */
end_comment

begin_function
name|int
name|ufs_init
parameter_list|()
block|{
specifier|static
name|int
name|first
init|=
literal|1
decl_stmt|;
if|if
condition|(
operator|!
name|first
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|first
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
operator|(
sizeof|sizeof
argument_list|(
expr|struct
name|inode
argument_list|)
operator|-
literal|1
operator|)
operator|&
sizeof|sizeof
argument_list|(
expr|struct
name|inode
argument_list|)
condition|)
name|printf
argument_list|(
literal|"ufs_init: bad size %d\n"
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|inode
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|ufs_ihashinit
argument_list|()
expr_stmt|;
name|dqinit
argument_list|()
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Unlock and decrement the reference count of an inode structure.  */
end_comment

begin_function
name|void
name|ufs_iput
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
if|if
condition|(
operator|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"iput"
argument_list|)
expr_stmt|;
name|IUNLOCK
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|vrele
argument_list|(
name|ITOV
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Reclaim an inode so that it can be used for other purposes.  */
end_comment

begin_function
name|int
name|ufs_reclaim
parameter_list|(
name|vp
parameter_list|)
specifier|register
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|int
name|i
decl_stmt|,
name|type
decl_stmt|;
if|if
condition|(
name|prtactive
operator|&&
name|vp
operator|->
name|v_usecount
operator|!=
literal|0
condition|)
name|vprint
argument_list|(
literal|"ufs_reclaim: pushing active"
argument_list|,
name|vp
argument_list|)
expr_stmt|;
comment|/* 	 * Remove the inode from its hash chain. 	 */
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
name|remque
argument_list|(
name|ip
argument_list|)
expr_stmt|;
comment|/* 	 * Purge old data structures associated with the inode. 	 */
name|cache_purge
argument_list|(
name|vp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_devvp
condition|)
block|{
name|vrele
argument_list|(
name|ip
operator|->
name|i_devvp
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_devvp
operator|=
literal|0
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|QUOTA
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|MAXQUOTAS
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
operator|!=
name|NODQUOT
condition|)
block|{
name|dqrele
argument_list|(
name|vp
argument_list|,
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
operator|=
name|NODQUOT
expr_stmt|;
block|}
block|}
endif|#
directive|endif
switch|switch
condition|(
name|vp
operator|->
name|v_mount
operator|->
name|mnt_stat
operator|.
name|f_type
condition|)
block|{
case|case
name|MOUNT_UFS
case|:
name|type
operator|=
name|M_FFSNODE
expr_stmt|;
break|break;
case|case
name|MOUNT_MFS
case|:
name|type
operator|=
name|M_MFSNODE
expr_stmt|;
break|break;
case|case
name|MOUNT_LFS
case|:
name|type
operator|=
name|M_LFSNODE
expr_stmt|;
break|break;
default|default:
name|panic
argument_list|(
literal|"ufs_reclaim: not ufs file"
argument_list|)
expr_stmt|;
block|}
name|FREE
argument_list|(
name|vp
operator|->
name|v_data
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|vp
operator|->
name|v_data
operator|=
name|NULL
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Lock an inode. If its already locked, set the WANT bit and sleep.  */
end_comment

begin_function
name|void
name|ufs_ilock
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
while|while
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
condition|)
block|{
name|ip
operator|->
name|i_flag
operator||=
name|IWANT
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_lockholder
operator|==
name|curproc
operator|->
name|p_pid
condition|)
name|panic
argument_list|(
literal|"locking against myself"
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_lockwaiter
operator|=
name|curproc
operator|->
name|p_pid
expr_stmt|;
operator|(
name|void
operator|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|ip
argument_list|,
name|PINOD
argument_list|)
expr_stmt|;
block|}
name|ip
operator|->
name|i_lockwaiter
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_lockholder
operator|=
name|curproc
operator|->
name|p_pid
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|ILOCKED
expr_stmt|;
name|curproc
operator|->
name|p_spare
index|[
literal|2
index|]
operator|++
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Unlock an inode.  If WANT bit is on, wakeup.  */
end_comment

begin_function
name|void
name|ufs_iunlock
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
if|if
condition|(
operator|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
operator|)
operator|==
literal|0
condition|)
name|vprint
argument_list|(
literal|"ufs_iunlock: unlocked inode"
argument_list|,
name|ITOV
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_lockholder
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_flag
operator|&=
operator|~
name|ILOCKED
expr_stmt|;
name|curproc
operator|->
name|p_spare
index|[
literal|2
index|]
operator|--
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IWANT
condition|)
block|{
name|ip
operator|->
name|i_flag
operator|&=
operator|~
name|IWANT
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|ip
argument_list|)
expr_stmt|;
block|}
block|}
end_function

end_unit

