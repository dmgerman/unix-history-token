begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986, 1989, 1991, 1993  *	The Regents of the University of California.  All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)ufs_ihash.c	8.5 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufs_extern.h>
end_include

begin_comment
comment|/*  * Structures associated with inode cacheing.  */
end_comment

begin_expr_stmt
name|LIST_HEAD
argument_list|(
name|ihashhead
argument_list|,
name|inode
argument_list|)
operator|*
name|ihashtbl
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|u_long
name|ihash
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* size of hash table - 1 */
end_comment

begin_define
define|#
directive|define
name|INOHASH
parameter_list|(
name|device
parameter_list|,
name|inum
parameter_list|)
value|(&ihashtbl[((device) + (inum))& ihash])
end_define

begin_comment
comment|/*  * Initialize inode hash table.  */
end_comment

begin_function
name|void
name|ufs_ihashinit
parameter_list|()
block|{
name|ihashtbl
operator|=
name|hashinit
argument_list|(
name|desiredvnodes
argument_list|,
name|M_UFSMNT
argument_list|,
operator|&
name|ihash
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Use the device/inum pair to find the incore inode, and return a pointer  * to it. If it is in core, return it, even if it is locked.  */
end_comment

begin_function
name|struct
name|vnode
modifier|*
name|ufs_ihashlookup
parameter_list|(
name|dev
parameter_list|,
name|inum
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|ino_t
name|inum
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
for|for
control|(
name|ip
operator|=
name|INOHASH
argument_list|(
name|dev
argument_list|,
name|inum
argument_list|)
operator|->
name|lh_first
init|;
name|ip
condition|;
name|ip
operator|=
name|ip
operator|->
name|i_hash
operator|.
name|le_next
control|)
block|{
if|if
condition|(
name|inum
operator|==
name|ip
operator|->
name|i_number
operator|&&
name|dev
operator|==
name|ip
operator|->
name|i_dev
condition|)
return|return
operator|(
name|ITOV
argument_list|(
name|ip
argument_list|)
operator|)
return|;
block|}
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Use the device/inum pair to find the incore inode, and return a pointer  * to it. If it is in core, but locked, wait for it.  */
end_comment

begin_function
name|struct
name|vnode
modifier|*
name|ufs_ihashget
parameter_list|(
name|dev
parameter_list|,
name|inum
parameter_list|)
name|dev_t
name|dev
decl_stmt|;
name|ino_t
name|inum
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|loop
label|:
for|for
control|(
name|ip
operator|=
name|INOHASH
argument_list|(
name|dev
argument_list|,
name|inum
argument_list|)
operator|->
name|lh_first
init|;
name|ip
condition|;
name|ip
operator|=
name|ip
operator|->
name|i_hash
operator|.
name|le_next
control|)
block|{
if|if
condition|(
name|inum
operator|==
name|ip
operator|->
name|i_number
operator|&&
name|dev
operator|==
name|ip
operator|->
name|i_dev
condition|)
block|{
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IN_LOCKED
condition|)
block|{
name|ip
operator|->
name|i_flag
operator||=
name|IN_WANTED
expr_stmt|;
name|sleep
argument_list|(
name|ip
argument_list|,
name|PINOD
argument_list|)
expr_stmt|;
goto|goto
name|loop
goto|;
block|}
name|vp
operator|=
name|ITOV
argument_list|(
name|ip
argument_list|)
expr_stmt|;
if|if
condition|(
name|vget
argument_list|(
name|vp
argument_list|,
literal|1
argument_list|)
condition|)
goto|goto
name|loop
goto|;
return|return
operator|(
name|vp
operator|)
return|;
block|}
block|}
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

begin_comment
comment|/* * Insert the inode into the hash table, and return it locked.  */
end_comment

begin_function
name|void
name|ufs_ihashins
parameter_list|(
name|ip
parameter_list|)
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
name|struct
name|ihashhead
modifier|*
name|ipp
decl_stmt|;
name|ipp
operator|=
name|INOHASH
argument_list|(
name|ip
operator|->
name|i_dev
argument_list|,
name|ip
operator|->
name|i_number
argument_list|)
expr_stmt|;
name|LIST_INSERT_HEAD
argument_list|(
name|ipp
argument_list|,
name|ip
argument_list|,
name|i_hash
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IN_LOCKED
condition|)
name|panic
argument_list|(
literal|"ufs_ihashins: already locked"
argument_list|)
expr_stmt|;
if|if
condition|(
name|curproc
condition|)
name|ip
operator|->
name|i_lockholder
operator|=
name|curproc
operator|->
name|p_pid
expr_stmt|;
else|else
name|ip
operator|->
name|i_lockholder
operator|=
operator|-
literal|1
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|IN_LOCKED
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Remove the inode from the hash table.  */
end_comment

begin_function
name|void
name|ufs_ihashrem
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|iq
decl_stmt|;
name|LIST_REMOVE
argument_list|(
name|ip
argument_list|,
name|i_hash
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
name|ip
operator|->
name|i_hash
operator|.
name|le_next
operator|=
name|NULL
expr_stmt|;
name|ip
operator|->
name|i_hash
operator|.
name|le_prev
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
block|}
end_function

end_unit

