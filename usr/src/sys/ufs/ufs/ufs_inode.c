begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1991 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)ufs_inode.c	7.41 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/quota.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/inode.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufsmount.h>
end_include

begin_include
include|#
directive|include
file|<ufs/ufs/ufs_extern.h>
end_include

begin_decl_stmt
name|u_long
name|nextgennumber
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Next generation number to assign. */
end_comment

begin_decl_stmt
name|int
name|prtactive
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* 1 => print out reclaim of active vnodes */
end_comment

begin_function
name|int
name|ufs_init
parameter_list|()
block|{
specifier|static
name|int
name|first
init|=
literal|1
decl_stmt|;
if|if
condition|(
operator|!
name|first
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|first
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|VN_MAXPRIVATE
operator|<
sizeof|sizeof
argument_list|(
expr|struct
name|inode
argument_list|)
condition|)
name|panic
argument_list|(
literal|"ufs_init: inode too small"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|ufs_ihashinit
argument_list|()
expr_stmt|;
name|dqinit
argument_list|()
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Unlock and decrement the reference count of an inode structure.  */
end_comment

begin_function
name|void
name|ufs_iput
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
if|if
condition|(
operator|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"iput"
argument_list|)
expr_stmt|;
name|IUNLOCK
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|vrele
argument_list|(
name|ITOV
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Last reference to an inode, write the inode out and if necessary,  * truncate and deallocate the file.  */
end_comment

begin_function
name|int
name|ufs_inactive
parameter_list|(
name|vp
parameter_list|,
name|p
parameter_list|)
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|ufsmount
modifier|*
name|ump
decl_stmt|;
name|int
name|mode
decl_stmt|,
name|error
decl_stmt|;
if|if
condition|(
name|prtactive
operator|&&
name|vp
operator|->
name|v_usecount
operator|!=
literal|0
condition|)
name|vprint
argument_list|(
literal|"ufs_inactive: pushing active"
argument_list|,
name|vp
argument_list|)
expr_stmt|;
comment|/* Get rid of inodes related to stale file handles. */
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_mode
operator|==
literal|0
condition|)
block|{
if|if
condition|(
operator|(
name|vp
operator|->
name|v_flag
operator|&
name|VXLOCK
operator|)
operator|==
literal|0
condition|)
name|vgone
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|error
operator|=
literal|0
expr_stmt|;
name|ump
operator|=
name|VFSTOUFS
argument_list|(
name|vp
operator|->
name|v_mount
argument_list|)
expr_stmt|;
name|ILOCK
argument_list|(
name|ip
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_nlink
operator|<=
literal|0
operator|&&
operator|(
name|vp
operator|->
name|v_mount
operator|->
name|mnt_flag
operator|&
name|MNT_RDONLY
operator|)
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|QUOTA
if|if
condition|(
operator|!
name|getinoquota
argument_list|(
name|ip
argument_list|)
condition|)
operator|(
name|void
operator|)
name|chkiq
argument_list|(
name|ip
argument_list|,
operator|-
literal|1
argument_list|,
name|NOCRED
argument_list|,
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|error
operator|=
call|(
name|ump
operator|->
name|um_itrunc
call|)
argument_list|(
name|ip
argument_list|,
operator|(
name|u_long
operator|)
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|mode
operator|=
name|ip
operator|->
name|i_mode
expr_stmt|;
name|ip
operator|->
name|i_mode
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_rdev
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|IUPD
operator||
name|ICHG
expr_stmt|;
call|(
name|ump
operator|->
name|um_ifree
call|)
argument_list|(
name|ip
argument_list|,
name|ip
operator|->
name|i_number
argument_list|,
name|mode
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
operator|(
name|IUPD
operator||
name|IACC
operator||
name|ICHG
operator||
name|IMOD
operator|)
condition|)
call|(
name|ump
operator|->
name|um_iupdat
call|)
argument_list|(
name|ip
argument_list|,
operator|&
name|time
argument_list|,
operator|&
name|time
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|IUNLOCK
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_flag
operator|=
literal|0
expr_stmt|;
comment|/* 	 * If we are done with the inode, reclaim it 	 * so that it can be reused immediately. 	 */
if|if
condition|(
name|vp
operator|->
name|v_usecount
operator|==
literal|0
operator|&&
name|ip
operator|->
name|i_mode
operator|==
literal|0
condition|)
name|vgone
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Reclaim an inode so that it can be used for other purposes.  */
end_comment

begin_function
name|int
name|ufs_reclaim
parameter_list|(
name|vp
parameter_list|)
specifier|register
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|int
name|i
decl_stmt|;
if|if
condition|(
name|prtactive
operator|&&
name|vp
operator|->
name|v_usecount
operator|!=
literal|0
condition|)
name|vprint
argument_list|(
literal|"ufs_reclaim: pushing active"
argument_list|,
name|vp
argument_list|)
expr_stmt|;
comment|/* 	 * Remove the inode from its hash chain. 	 */
name|ip
operator|=
name|VTOI
argument_list|(
name|vp
argument_list|)
expr_stmt|;
name|remque
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_forw
operator|=
name|ip
expr_stmt|;
name|ip
operator|->
name|i_back
operator|=
name|ip
expr_stmt|;
comment|/* 	 * Purge old data structures associated with the inode. 	 */
name|cache_purge
argument_list|(
name|vp
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_devvp
condition|)
block|{
name|vrele
argument_list|(
name|ip
operator|->
name|i_devvp
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_devvp
operator|=
literal|0
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|QUOTA
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|MAXQUOTAS
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
operator|!=
name|NODQUOT
condition|)
block|{
name|dqrele
argument_list|(
name|vp
argument_list|,
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_dquot
index|[
name|i
index|]
operator|=
name|NODQUOT
expr_stmt|;
block|}
block|}
endif|#
directive|endif
name|ip
operator|->
name|i_flag
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Lock an inode. If its already locked, set the WANT bit and sleep.  */
end_comment

begin_function
name|void
name|ufs_ilock
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
while|while
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
condition|)
block|{
name|ip
operator|->
name|i_flag
operator||=
name|IWANT
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_spare0
operator|==
name|curproc
operator|->
name|p_pid
condition|)
name|panic
argument_list|(
literal|"locking against myself"
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_spare1
operator|=
name|curproc
operator|->
name|p_pid
expr_stmt|;
operator|(
name|void
operator|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
name|ip
argument_list|,
name|PINOD
argument_list|)
expr_stmt|;
block|}
name|ip
operator|->
name|i_spare1
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_spare0
operator|=
name|curproc
operator|->
name|p_pid
expr_stmt|;
name|ip
operator|->
name|i_flag
operator||=
name|ILOCKED
expr_stmt|;
name|curproc
operator|->
name|p_spare
index|[
literal|2
index|]
operator|++
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Unlock an inode.  If WANT bit is on, wakeup.  */
end_comment

begin_function
name|void
name|ufs_iunlock
parameter_list|(
name|ip
parameter_list|)
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
block|{
if|if
condition|(
operator|(
name|ip
operator|->
name|i_flag
operator|&
name|ILOCKED
operator|)
operator|==
literal|0
condition|)
name|vprint
argument_list|(
literal|"ufs_iunlock: unlocked inode"
argument_list|,
name|ITOV
argument_list|(
name|ip
argument_list|)
argument_list|)
expr_stmt|;
name|ip
operator|->
name|i_spare0
operator|=
literal|0
expr_stmt|;
name|ip
operator|->
name|i_flag
operator|&=
operator|~
name|ILOCKED
expr_stmt|;
name|curproc
operator|->
name|p_spare
index|[
literal|2
index|]
operator|--
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|IWANT
condition|)
block|{
name|ip
operator|->
name|i_flag
operator|&=
operator|~
name|IWANT
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
name|ip
argument_list|)
expr_stmt|;
block|}
block|}
end_function

end_unit

