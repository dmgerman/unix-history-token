begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	uipc_socket.c	4.2	81/11/08	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/dir.h"
end_include

begin_include
include|#
directive|include
file|"../h/user.h"
end_include

begin_include
include|#
directive|include
file|"../h/proc.h"
end_include

begin_include
include|#
directive|include
file|"../h/file.h"
end_include

begin_include
include|#
directive|include
file|"../h/inode.h"
end_include

begin_include
include|#
directive|include
file|"../h/buf.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protocol.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/inaddr.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet_systm.h"
end_include

begin_decl_stmt
name|struct
name|socket
name|zerosocket
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|in_addr
name|zeroin_addr
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Socket system call interface.  Copy in arguments  * set up file descriptor and call internal socket  * creation routine.  */
end_comment

begin_macro
name|ssocket
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|type
decl_stmt|;
name|struct
name|in_addr
modifier|*
name|ain
decl_stmt|;
name|int
name|options
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|in_addr
name|in
decl_stmt|;
name|struct
name|socket
modifier|*
name|so0
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
if|if
condition|(
operator|(
name|fp
operator|=
name|falloc
argument_list|()
operator|)
operator|==
name|NULL
condition|)
return|return;
name|fp
operator|->
name|f_flag
operator|=
name|FSOCKET
operator||
name|FREAD
operator||
name|FWRITE
expr_stmt|;
if|if
condition|(
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|ain
argument_list|,
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|socket
argument_list|(
operator|&
name|so0
argument_list|,
name|uap
operator|->
name|type
argument_list|,
operator|&
name|in
argument_list|,
name|uap
operator|->
name|options
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
goto|goto
name|bad
goto|;
name|fp
operator|->
name|f_socket
operator|=
name|so
expr_stmt|;
return|return;
name|bad
label|:
name|u
operator|.
name|u_ofile
index|[
name|u
operator|.
name|u_r
operator|.
name|r_val1
index|]
operator|=
literal|0
expr_stmt|;
name|fp
operator|->
name|f_count
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Create a socket.  */
end_comment

begin_macro
name|socket
argument_list|(
argument|aso
argument_list|,
argument|type
argument_list|,
argument|iap
argument_list|,
argument|options
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
modifier|*
name|aso
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|type
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|struct
name|in_addr
modifier|*
name|iap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|options
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|protosw
modifier|*
name|prp
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
name|int
name|pf
decl_stmt|,
name|proto
decl_stmt|;
comment|/* 	 * Pin down protocol if possible. 	 * If no address specified, use a generic protocol. 	 */
if|if
condition|(
name|iap
operator|==
literal|0
condition|)
block|{
name|pf
operator|=
name|PF_GENERIC
expr_stmt|;
name|proto
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
name|pf
operator|=
name|iap
operator|->
name|ia_pf
expr_stmt|;
name|proto
operator|=
name|iap
operator|->
name|ia_proto
expr_stmt|;
block|}
if|if
condition|(
name|proto
condition|)
block|{
comment|/* 		 * A specific protocol was requested.  Look 		 * for the protocol.  If not found, then we 		 * don't support it. 		 */
name|prp
operator|=
name|pf_findproto
argument_list|(
name|pf
argument_list|,
name|proto
argument_list|)
expr_stmt|;
if|if
condition|(
name|prp
operator|==
literal|0
condition|)
return|return
operator|(
name|EPROTONOSUPPORT
operator|)
return|;
block|}
else|else
block|{
comment|/* 		 * No specific protocol was requested.  Look 		 * in the specified (or generic) protocol set 		 * for a protocol of this type. 		 */
name|prp
operator|=
name|pf_findtype
argument_list|(
name|pf
argument_list|,
name|type
argument_list|)
expr_stmt|;
if|if
condition|(
name|prp
operator|==
literal|0
condition|)
return|return
operator|(
name|pf
operator|==
name|PF_GENERIC
condition|?
name|ESOCKTNOSUPPORT
else|:
name|EPROTONOSUPPORT
operator|)
return|;
block|}
comment|/* 	 * Get a socket structure. 	 */
name|m
operator|=
name|m_get
argument_list|(
name|M_WAIT
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOBUFS
operator|)
return|;
name|m
operator|->
name|m_off
operator|=
name|MMINOFF
expr_stmt|;
name|so
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|socket
operator|*
argument_list|)
expr_stmt|;
operator|*
name|so
operator|=
name|zerosocket
expr_stmt|;
name|so
operator|->
name|so_options
operator|=
name|options
expr_stmt|;
comment|/* 	 * An early call to protocol initialization.  If protocol 	 * actually hasn't been decided on yet (till we know 	 * peer), then the generic protocol allocated so far can 	 * just make sure a reasonable amount of resources will 	 * be available to it (say by allocating liberally now 	 * and returning some of the resources later). 	 */
name|so
operator|->
name|so_proto
operator|=
name|prp
expr_stmt|;
call|(
modifier|*
name|prp
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_ATTACH
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_error
condition|)
block|{
name|m_free
argument_list|(
name|dtom
argument_list|(
name|so
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|u
operator|.
name|u_error
operator|)
return|;
block|}
operator|*
name|aso
operator|=
name|so
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_macro
name|spipe
argument_list|()
end_macro

begin_block
block|{  }
end_block

begin_expr_stmt
name|skclose
argument_list|(
name|so
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
name|so
operator|->
name|so_pcb
condition|)
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_DETACH
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Select a socket.  */
end_comment

begin_expr_stmt
name|soselect
argument_list|(
name|so
argument_list|,
name|flag
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|flag
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
operator|(
name|p
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|)
operator|&&
name|p
operator|->
name|p_wchan
operator|==
operator|(
name|caddr_t
operator|)
name|select
condition|)
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator||=
name|SB_COLL
expr_stmt|;
else|else
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Wakeup read sleep/select'ers.  */
end_comment

begin_macro
name|sowakeup
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|&&
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
condition|)
block|{
name|selwakeup
argument_list|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
argument_list|,
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&
name|SB_COLL
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|=
literal|0
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&=
operator|~
name|SB_COLL
expr_stmt|;
block|}
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&
name|SB_WAIT
condition|)
block|{
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&=
operator|~
name|SB_WAIT
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Connect socket to foreign peer; system call  * interface.  Copy in arguments and call internal routine.  */
end_comment

begin_macro
name|sconnect
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|struct
name|in_addr
modifier|*
name|a
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|in_addr
name|in
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|a
argument_list|,
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
name|so
operator|=
name|fp
operator|->
name|f_socket
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|connect
argument_list|(
name|so
argument_list|,
operator|&
name|in
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
name|s
operator|=
name|splnet
argument_list|()
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
comment|/* should use tsleep here */
if|if
condition|(
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_ISCONN
argument_list|,
literal|0
argument_list|,
operator|&
name|in
argument_list|)
operator|==
literal|0
condition|)
break|break;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|connect
argument_list|(
argument|so
argument_list|,
argument|iap
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|in_addr
modifier|*
name|iap
decl_stmt|;
end_decl_stmt

begin_block
block|{
return|return
operator|(
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_CONNECT
argument_list|,
literal|0
argument_list|,
name|iap
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Disconnect socket from foreign peer; system call  * interface.  Copy in arguments and call internal routine.  */
end_comment

begin_macro
name|sdisconnect
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|in_addr
modifier|*
name|addr
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|in_addr
name|in
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|addr
operator|&&
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|addr
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
name|disconnect
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|uap
operator|->
name|addr
condition|?
operator|&
name|in
else|:
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|disconnect
argument_list|(
argument|so
argument_list|,
argument|iap
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|in_addr
modifier|*
name|iap
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|u
operator|.
name|u_error
operator|=
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_DISCONNECT
argument_list|,
literal|0
argument_list|,
name|iap
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Send data on socket.  */
end_comment

begin_macro
name|ssend
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|in_addr
modifier|*
name|ain
decl_stmt|;
name|caddr_t
name|cbuf
decl_stmt|;
name|int
name|count
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|struct
name|in_addr
name|in
decl_stmt|;
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|count
operator|<
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_base
operator|=
name|uap
operator|->
name|cbuf
expr_stmt|;
name|u
operator|.
name|u_count
operator|=
name|uap
operator|->
name|count
expr_stmt|;
name|u
operator|.
name|u_segflg
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|useracc
argument_list|(
name|u
operator|.
name|u_base
argument_list|,
name|u
operator|.
name|u_count
argument_list|,
name|B_READ
argument_list|)
operator|==
literal|0
operator|||
name|uap
operator|->
name|ain
operator|&&
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|ain
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|send
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|uap
operator|->
name|ain
condition|?
operator|&
name|in
else|:
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|send
argument_list|(
name|so
argument_list|,
name|iap
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|in_addr
modifier|*
name|iap
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|,
modifier|*
modifier|*
name|mp
decl_stmt|;
name|struct
name|mbuf
modifier|*
name|top
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|so
operator|->
name|so_proto
operator|->
name|pr_flags
operator|&
name|PR_ATOMIC
condition|)
block|{
if|if
condition|(
name|u
operator|.
name|u_count
operator|>
name|so
operator|->
name|so_snd
operator|.
name|sb_hiwat
condition|)
block|{
name|error
operator|=
name|EMSGSIZE
expr_stmt|;
goto|goto
name|release
goto|;
block|}
block|}
name|again
label|:
while|while
condition|(
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator|&
name|SB_LOCK
condition|)
block|{
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator||=
name|SB_WANT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|so
operator|->
name|so_snd
operator|.
name|sb_hiwat
operator|-
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
operator|<
name|u
operator|.
name|u_count
condition|)
block|{
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator||=
name|SB_WAIT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
goto|goto
name|again
goto|;
block|}
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator||=
name|SB_LOCK
expr_stmt|;
while|while
condition|(
name|u
operator|.
name|u_count
operator|>
literal|0
condition|)
block|{
specifier|register
name|int
name|bufs
init|=
name|so
operator|->
name|so_snd
operator|.
name|sb_mbmax
operator|-
name|so
operator|->
name|so_snd
operator|.
name|sb_mbcnt
decl_stmt|;
while|while
condition|(
name|bufs
operator|==
literal|0
condition|)
block|{
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator||=
name|SB_WAIT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
name|mp
operator|=
operator|&
name|top
expr_stmt|;
name|top
operator|=
literal|0
expr_stmt|;
while|while
condition|(
operator|--
name|bufs
operator|>=
literal|0
operator|&&
name|u
operator|.
name|u_count
operator|>
literal|0
condition|)
block|{
specifier|register
name|int
name|len
decl_stmt|;
name|MGET
argument_list|(
name|m
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
name|NULL
condition|)
block|{
name|error
operator|=
name|ENOBUFS
expr_stmt|;
name|m_freem
argument_list|(
name|top
argument_list|)
expr_stmt|;
goto|goto
name|release
goto|;
block|}
if|if
condition|(
name|u
operator|.
name|u_count
operator|>=
name|PGSIZE
operator|&&
name|bufs
operator|>=
name|NMBPG
condition|)
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|p
decl_stmt|;
name|MPGET
argument_list|(
name|p
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
literal|0
condition|)
goto|goto
name|nopages
goto|;
name|m
operator|->
name|m_off
operator|=
operator|(
name|int
operator|)
name|p
operator|-
operator|(
name|int
operator|)
name|m
expr_stmt|;
name|len
operator|=
name|PGSIZE
expr_stmt|;
block|}
else|else
block|{
name|nopages
label|:
name|m
operator|->
name|m_off
operator|=
name|MMINOFF
expr_stmt|;
name|len
operator|=
name|MIN
argument_list|(
name|MLEN
argument_list|,
name|u
operator|.
name|u_count
argument_list|)
expr_stmt|;
block|}
name|iomove
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
argument_list|,
name|len
argument_list|,
name|B_WRITE
argument_list|)
expr_stmt|;
name|m
operator|->
name|m_len
operator|=
name|len
expr_stmt|;
operator|*
name|mp
operator|=
name|m
expr_stmt|;
name|mp
operator|=
operator|&
name|m
operator|->
name|m_next
expr_stmt|;
block|}
block|{
specifier|register
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
name|error
operator|=
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_SEND
argument_list|,
name|top
argument_list|,
name|iap
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|error
condition|)
break|break;
block|}
name|release
label|:
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator|&=
operator|~
name|SB_LOCK
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
operator|&
name|SB_WANT
condition|)
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_snd
operator|.
name|sb_flags
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Receive data on socket.  */
end_comment

begin_macro
name|sreceive
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|in_addr
modifier|*
name|ain
decl_stmt|;
name|caddr_t
name|cbuf
decl_stmt|;
name|int
name|count
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|struct
name|in_addr
modifier|*
name|in
decl_stmt|;
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|count
operator|<
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_base
operator|=
name|uap
operator|->
name|cbuf
expr_stmt|;
name|u
operator|.
name|u_count
operator|=
name|uap
operator|->
name|count
expr_stmt|;
name|u
operator|.
name|u_segflg
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|useracc
argument_list|(
name|u
operator|.
name|u_base
argument_list|,
name|u
operator|.
name|u_count
argument_list|,
name|B_WRITE
argument_list|)
operator|==
literal|0
operator|||
name|uap
operator|->
name|ain
operator|&&
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|ain
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|receive
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|uap
operator|->
name|ain
condition|?
operator|&
name|in
else|:
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|receive
argument_list|(
name|so
argument_list|,
name|iap
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|in_addr
modifier|*
name|iap
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|,
modifier|*
name|n
decl_stmt|;
specifier|register
name|int
name|eor
decl_stmt|,
name|len
decl_stmt|,
name|s
decl_stmt|;
name|again
label|:
while|while
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&
name|SB_LOCK
condition|)
block|{
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator||=
name|SB_WANT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|==
literal|0
condition|)
block|{
if|if
condition|(
call|(
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_ISDISCONN
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
operator|==
literal|0
condition|)
return|return;
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator||=
name|SB_WAIT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
goto|goto
name|again
goto|;
block|}
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator||=
name|SB_LOCK
expr_stmt|;
name|m
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"receive"
argument_list|)
expr_stmt|;
name|eor
operator|=
literal|0
expr_stmt|;
do|do
block|{
name|len
operator|=
name|MIN
argument_list|(
name|m
operator|->
name|m_len
argument_list|,
name|u
operator|.
name|u_count
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|==
name|m
operator|->
name|m_len
condition|)
block|{
name|eor
operator|=
operator|(
name|int
operator|)
name|m
operator|->
name|m_act
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|-=
name|len
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|<
literal|0
condition|)
name|panic
argument_list|(
literal|"receive 2"
argument_list|)
expr_stmt|;
block|}
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|iomove
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
argument_list|,
name|len
argument_list|,
name|B_READ
argument_list|)
expr_stmt|;
name|s
operator|=
name|splnet
argument_list|()
expr_stmt|;
if|if
condition|(
name|len
operator|==
name|m
operator|->
name|m_len
condition|)
block|{
name|MFREE
argument_list|(
name|m
argument_list|,
name|n
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|m
operator|->
name|m_off
operator|+=
name|len
expr_stmt|;
name|m
operator|->
name|m_len
operator|-=
name|len
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|-=
name|len
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|<
literal|0
condition|)
name|panic
argument_list|(
literal|"receive 3"
argument_list|)
expr_stmt|;
block|}
block|}
do|while
condition|(
operator|(
name|m
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|)
operator|&&
name|u
operator|.
name|u_count
operator|&&
operator|!
name|eor
condition|)
do|;
if|if
condition|(
operator|(
name|so
operator|->
name|so_proto
operator|->
name|pr_flags
operator|&
name|PR_ATOMIC
operator|)
operator|&&
name|eor
operator|==
literal|0
condition|)
do|do
block|{
name|m
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"receive 4"
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|-=
name|m
operator|->
name|m_len
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|<
literal|0
condition|)
name|panic
argument_list|(
literal|"receive 5"
argument_list|)
expr_stmt|;
name|eor
operator|=
operator|(
name|int
operator|)
name|m
operator|->
name|m_act
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|MFREE
argument_list|(
name|m
argument_list|,
name|n
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|eor
operator|==
literal|0
condition|)
do|;
if|if
condition|(
name|iap
condition|)
if|if
condition|(
operator|(
name|so
operator|->
name|so_proto
operator|->
name|pr_flags
operator|&
name|PR_ADDR
operator|)
condition|)
block|{
name|m
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"receive 6"
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_mb
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
operator|-=
name|m
operator|->
name|m_len
expr_stmt|;
name|len
operator|=
name|MIN
argument_list|(
name|m
operator|->
name|m_len
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|in_addr
argument_list|)
argument_list|)
expr_stmt|;
name|bcopy
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
argument_list|,
operator|(
name|caddr_t
operator|)
name|iap
argument_list|,
name|len
argument_list|)
expr_stmt|;
block|}
else|else
operator|*
name|iap
operator|=
name|zeroin_addr
expr_stmt|;
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_RCVD
argument_list|,
name|m
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

begin_expr_stmt
name|skioctl
argument_list|(
name|so
argument_list|,
name|cmd
argument_list|,
name|cmdp
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|cmd
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|caddr_t
name|cmdp
decl_stmt|;
end_decl_stmt

begin_block
block|{
switch|switch
condition|(
name|cmdp
condition|)
block|{  	}
switch|switch
condition|(
name|so
operator|->
name|so_type
condition|)
block|{
case|case
name|SOCK_STREAM
case|:
break|break;
case|case
name|SOCK_DGRAM
case|:
break|break;
case|case
name|SOCK_RDM
case|:
break|break;
case|case
name|SOCK_RAW
case|:
break|break;
block|}
block|}
end_block

begin_macro
name|sostat
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{  }
end_block

begin_comment
comment|/*  * Generic protocol handler.  */
end_comment

begin_macro
name|gen_usrreq
argument_list|()
end_macro

begin_block
block|{  }
end_block

end_unit

