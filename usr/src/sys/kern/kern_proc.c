begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1982, 1986, 1989 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  *	@(#)kern_proc.c	7.13 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"map.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"kernel.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"buf.h"
end_include

begin_include
include|#
directive|include
file|"seg.h"
end_include

begin_include
include|#
directive|include
file|"acct.h"
end_include

begin_include
include|#
directive|include
file|"wait.h"
end_include

begin_include
include|#
directive|include
file|"file.h"
end_include

begin_include
include|#
directive|include
file|"../ufs/quota.h"
end_include

begin_include
include|#
directive|include
file|"uio.h"
end_include

begin_include
include|#
directive|include
file|"malloc.h"
end_include

begin_include
include|#
directive|include
file|"mbuf.h"
end_include

begin_include
include|#
directive|include
file|"ioctl.h"
end_include

begin_include
include|#
directive|include
file|"tty.h"
end_include

begin_include
include|#
directive|include
file|"machine/reg.h"
end_include

begin_include
include|#
directive|include
file|"machine/psl.h"
end_include

begin_comment
comment|/*  * Is p an inferior of the current process?  */
end_comment

begin_expr_stmt
name|inferior
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
for|for
control|(
init|;
name|p
operator|!=
name|u
operator|.
name|u_procp
condition|;
name|p
operator|=
name|p
operator|->
name|p_pptr
control|)
if|if
condition|(
name|p
operator|->
name|p_ppid
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Locate a process by number  */
end_comment

begin_decl_stmt
name|struct
name|proc
modifier|*
name|pfind
argument_list|(
name|pid
argument_list|)
decl|register
name|pid
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
init|=
name|pidhash
index|[
name|PIDHASH
argument_list|(
name|pid
argument_list|)
index|]
decl_stmt|;
for|for
control|(
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|p_hash
control|)
if|if
condition|(
name|p
operator|->
name|p_pid
operator|==
name|pid
condition|)
return|return
operator|(
name|p
operator|)
return|;
return|return
operator|(
operator|(
expr|struct
name|proc
operator|*
operator|)
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Locate a process group by number  */
end_comment

begin_function
name|struct
name|pgrp
modifier|*
name|pgfind
parameter_list|(
name|pgid
parameter_list|)
specifier|register
name|pid_t
name|pgid
decl_stmt|;
block|{
specifier|register
name|struct
name|pgrp
modifier|*
name|pgrp
init|=
name|pgrphash
index|[
name|PIDHASH
argument_list|(
name|pgid
argument_list|)
index|]
decl_stmt|;
for|for
control|(
init|;
name|pgrp
condition|;
name|pgrp
operator|=
name|pgrp
operator|->
name|pg_hforw
control|)
if|if
condition|(
name|pgrp
operator|->
name|pg_id
operator|==
name|pgid
condition|)
return|return
operator|(
name|pgrp
operator|)
return|;
return|return
operator|(
operator|(
expr|struct
name|pgrp
operator|*
operator|)
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Move p to a new or existing process group (and session)  */
end_comment

begin_expr_stmt
name|pgmv
argument_list|(
name|p
argument_list|,
name|pgid
argument_list|,
name|mksess
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|pid_t
name|pgid
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pgrp
modifier|*
name|pgrp
init|=
name|pgfind
argument_list|(
name|pgid
argument_list|)
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
modifier|*
name|pp
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|cp
decl_stmt|;
name|int
name|n
decl_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|pgrp
operator|&&
name|mksess
condition|)
comment|/* firewalls */
name|panic
argument_list|(
literal|"pgmv: setsid into non-empty pgrp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|SESS_LEADER
argument_list|(
name|p
argument_list|)
condition|)
name|panic
argument_list|(
literal|"pgmv: session leader attempted setpgrp"
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|pgrp
operator|==
name|NULL
condition|)
block|{
comment|/* 		 * new process group 		 */
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|p
operator|->
name|p_pid
operator|!=
name|pgid
condition|)
name|panic
argument_list|(
literal|"pgmv: new pgrp and pid != pgid"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|MALLOC
argument_list|(
name|pgrp
argument_list|,
expr|struct
name|pgrp
operator|*
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|pgrp
argument_list|)
argument_list|,
name|M_PGRP
argument_list|,
name|M_WAITOK
argument_list|)
expr_stmt|;
if|if
condition|(
name|mksess
condition|)
block|{
specifier|register
name|struct
name|session
modifier|*
name|sess
decl_stmt|;
comment|/* 			 * new session 			 */
name|MALLOC
argument_list|(
name|sess
argument_list|,
expr|struct
name|session
operator|*
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|session
argument_list|)
argument_list|,
name|M_SESSION
argument_list|,
name|M_WAITOK
argument_list|)
expr_stmt|;
name|sess
operator|->
name|s_leader
operator|=
name|p
expr_stmt|;
name|sess
operator|->
name|s_count
operator|=
literal|1
expr_stmt|;
name|sess
operator|->
name|s_ttyvp
operator|=
name|NULL
expr_stmt|;
name|sess
operator|->
name|s_ttyp
operator|=
name|NULL
expr_stmt|;
name|p
operator|->
name|p_flag
operator|&=
operator|~
name|SCTTY
expr_stmt|;
name|pgrp
operator|->
name|pg_session
operator|=
name|sess
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|p
operator|!=
name|u
operator|.
name|u_procp
condition|)
name|panic
argument_list|(
literal|"pgmv: mksession and p != u.u_procp"
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
else|else
block|{
name|pgrp
operator|->
name|pg_session
operator|=
name|p
operator|->
name|p_session
expr_stmt|;
name|pgrp
operator|->
name|pg_session
operator|->
name|s_count
operator|++
expr_stmt|;
block|}
name|pgrp
operator|->
name|pg_id
operator|=
name|pgid
expr_stmt|;
name|pgrp
operator|->
name|pg_hforw
operator|=
name|pgrphash
index|[
name|n
operator|=
name|PIDHASH
argument_list|(
name|pgid
argument_list|)
index|]
expr_stmt|;
name|pgrphash
index|[
name|n
index|]
operator|=
name|pgrp
expr_stmt|;
name|pgrp
operator|->
name|pg_jobc
operator|=
literal|0
expr_stmt|;
name|pgrp
operator|->
name|pg_mem
operator|=
name|NULL
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|pgrp
operator|==
name|p
operator|->
name|p_pgrp
condition|)
return|return;
comment|/* 	 * Adjust eligibility of affected pgrps to participate in job control. 	 * Increment eligibility counts before decrementing, otherwise we 	 * could reach 0 spuriously during the first call. 	 */
name|fixjobc
argument_list|(
name|p
argument_list|,
name|pgrp
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|fixjobc
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_pgrp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * unlink p from old process group 	 */
for|for
control|(
name|pp
operator|=
operator|&
name|p
operator|->
name|p_pgrp
operator|->
name|pg_mem
init|;
operator|*
name|pp
condition|;
name|pp
operator|=
operator|&
operator|(
operator|*
name|pp
operator|)
operator|->
name|p_pgrpnxt
control|)
if|if
condition|(
operator|*
name|pp
operator|==
name|p
condition|)
block|{
operator|*
name|pp
operator|=
name|p
operator|->
name|p_pgrpnxt
expr_stmt|;
goto|goto
name|done
goto|;
block|}
name|panic
argument_list|(
literal|"pgmv: can't find p on old pgrp"
argument_list|)
expr_stmt|;
name|done
label|:
comment|/* 	 * delete old if empty 	 */
if|if
condition|(
name|p
operator|->
name|p_pgrp
operator|->
name|pg_mem
operator|==
literal|0
condition|)
name|pgdelete
argument_list|(
name|p
operator|->
name|p_pgrp
argument_list|)
expr_stmt|;
comment|/* 	 * link into new one 	 */
name|p
operator|->
name|p_pgrp
operator|=
name|pgrp
expr_stmt|;
name|p
operator|->
name|p_pgrpnxt
operator|=
name|pgrp
operator|->
name|pg_mem
expr_stmt|;
name|pgrp
operator|->
name|pg_mem
operator|=
name|p
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * remove process from process group  */
end_comment

begin_expr_stmt
name|pgrm
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
modifier|*
name|pp
init|=
operator|&
name|p
operator|->
name|p_pgrp
operator|->
name|pg_mem
decl_stmt|;
for|for
control|(
init|;
operator|*
name|pp
condition|;
name|pp
operator|=
operator|&
operator|(
operator|*
name|pp
operator|)
operator|->
name|p_pgrpnxt
control|)
if|if
condition|(
operator|*
name|pp
operator|==
name|p
condition|)
block|{
operator|*
name|pp
operator|=
name|p
operator|->
name|p_pgrpnxt
expr_stmt|;
goto|goto
name|done
goto|;
block|}
name|panic
argument_list|(
literal|"pgrm: can't find p in pgrp"
argument_list|)
expr_stmt|;
name|done
label|:
if|if
condition|(
operator|!
name|p
operator|->
name|p_pgrp
operator|->
name|pg_mem
condition|)
name|pgdelete
argument_list|(
name|p
operator|->
name|p_pgrp
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_pgrp
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * delete a process group  */
end_comment

begin_expr_stmt
name|pgdelete
argument_list|(
name|pgrp
argument_list|)
specifier|register
expr|struct
name|pgrp
operator|*
name|pgrp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|pgrp
modifier|*
modifier|*
name|pgp
init|=
operator|&
name|pgrphash
index|[
name|PIDHASH
argument_list|(
name|pgrp
operator|->
name|pg_id
argument_list|)
index|]
decl_stmt|;
if|if
condition|(
name|pgrp
operator|->
name|pg_session
operator|->
name|s_ttyp
operator|!=
name|NULL
operator|&&
name|pgrp
operator|->
name|pg_session
operator|->
name|s_ttyp
operator|->
name|t_pgrp
operator|==
name|pgrp
condition|)
name|pgrp
operator|->
name|pg_session
operator|->
name|s_ttyp
operator|->
name|t_pgrp
operator|=
name|NULL
expr_stmt|;
for|for
control|(
init|;
operator|*
name|pgp
condition|;
name|pgp
operator|=
operator|&
operator|(
operator|*
name|pgp
operator|)
operator|->
name|pg_hforw
control|)
if|if
condition|(
operator|*
name|pgp
operator|==
name|pgrp
condition|)
block|{
operator|*
name|pgp
operator|=
name|pgrp
operator|->
name|pg_hforw
expr_stmt|;
goto|goto
name|done
goto|;
block|}
name|panic
argument_list|(
literal|"pgdelete: can't find pgrp on hash chain"
argument_list|)
expr_stmt|;
name|done
label|:
if|if
condition|(
operator|--
name|pgrp
operator|->
name|pg_session
operator|->
name|s_count
operator|==
literal|0
condition|)
name|FREE
argument_list|(
name|pgrp
operator|->
name|pg_session
argument_list|,
name|M_SESSION
argument_list|)
expr_stmt|;
name|FREE
argument_list|(
name|pgrp
argument_list|,
name|M_PGRP
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Adjust pgrp jobc counters when specified process changes process group.  * We count the number of processes in each process group that "qualify"  * the group for terminal job control (those with a parent in a different  * process group of the same session).  If that count reaches zero, the  * process group becomes orphaned.  Check both the specified process'  * process group and that of its children.  * entering == 0 => p is leaving specified group.  * entering == 1 => p is entering specified group.  */
end_comment

begin_expr_stmt
name|fixjobc
argument_list|(
name|p
argument_list|,
name|pgrp
argument_list|,
name|entering
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|pgrp
modifier|*
name|pgrp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|entering
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pgrp
modifier|*
name|hispgrp
decl_stmt|;
specifier|register
name|struct
name|session
modifier|*
name|mysession
init|=
name|pgrp
operator|->
name|pg_session
decl_stmt|;
comment|/* 	 * Check p's parent to see whether p qualifies its own process 	 * group; if so, adjust count for p's process group. 	 */
if|if
condition|(
operator|(
name|hispgrp
operator|=
name|p
operator|->
name|p_pptr
operator|->
name|p_pgrp
operator|)
operator|!=
name|pgrp
operator|&&
name|hispgrp
operator|->
name|pg_session
operator|==
name|mysession
condition|)
if|if
condition|(
name|entering
condition|)
name|pgrp
operator|->
name|pg_jobc
operator|++
expr_stmt|;
elseif|else
if|if
condition|(
operator|--
name|pgrp
operator|->
name|pg_jobc
operator|==
literal|0
condition|)
name|orphanpg
argument_list|(
name|pgrp
argument_list|)
expr_stmt|;
comment|/* 	 * Check this process' children to see whether they qualify 	 * their process groups; if so, adjust counts for children's 	 * process groups. 	 */
for|for
control|(
name|p
operator|=
name|p
operator|->
name|p_cptr
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|p_osptr
control|)
if|if
condition|(
operator|(
name|hispgrp
operator|=
name|p
operator|->
name|p_pgrp
operator|)
operator|!=
name|pgrp
operator|&&
name|hispgrp
operator|->
name|pg_session
operator|==
name|mysession
operator|&&
name|p
operator|->
name|p_stat
operator|!=
name|SZOMB
condition|)
if|if
condition|(
name|entering
condition|)
name|hispgrp
operator|->
name|pg_jobc
operator|++
expr_stmt|;
elseif|else
if|if
condition|(
operator|--
name|hispgrp
operator|->
name|pg_jobc
operator|==
literal|0
condition|)
name|orphanpg
argument_list|(
name|hispgrp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*   * A process group has become orphaned;  * if there are any stopped processes in the group,  * hang-up all process in that group.  */
end_comment

begin_expr_stmt
specifier|static
name|orphanpg
argument_list|(
argument|pg
argument_list|)
expr|struct
name|pgrp
operator|*
name|pg
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
for|for
control|(
name|p
operator|=
name|pg
operator|->
name|pg_mem
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|p_pgrpnxt
control|)
block|{
if|if
condition|(
name|p
operator|->
name|p_stat
operator|==
name|SSTOP
condition|)
block|{
for|for
control|(
name|p
operator|=
name|pg
operator|->
name|pg_mem
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|p_pgrpnxt
control|)
block|{
name|psignal
argument_list|(
name|p
argument_list|,
name|SIGHUP
argument_list|)
expr_stmt|;
name|psignal
argument_list|(
name|p
argument_list|,
name|SIGCONT
argument_list|)
expr_stmt|;
block|}
return|return;
block|}
block|}
block|}
end_block

begin_comment
comment|/*  * init the process queues  */
end_comment

begin_macro
name|pqinit
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
comment|/* 	 * most procs are initially on freequeue 	 *	nb: we place them there in their "natural" order. 	 */
name|freeproc
operator|=
name|NULL
expr_stmt|;
for|for
control|(
name|p
operator|=
name|procNPROC
init|;
operator|--
name|p
operator|>
name|proc
condition|;
name|freeproc
operator|=
name|p
control|)
name|p
operator|->
name|p_nxt
operator|=
name|freeproc
expr_stmt|;
comment|/* 	 * but proc[0] is special ... 	 */
name|allproc
operator|=
name|p
expr_stmt|;
name|p
operator|->
name|p_nxt
operator|=
name|NULL
expr_stmt|;
name|p
operator|->
name|p_prev
operator|=
operator|&
name|allproc
expr_stmt|;
name|zombproc
operator|=
name|NULL
expr_stmt|;
block|}
end_block

begin_ifdef
ifdef|#
directive|ifdef
name|debug
end_ifdef

begin_comment
comment|/* DEBUG */
end_comment

begin_macro
name|pgrpdump
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|struct
name|pgrp
modifier|*
name|pgrp
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
specifier|register
name|i
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|PIDHSZ
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|pgrphash
index|[
name|i
index|]
condition|)
block|{
name|printf
argument_list|(
literal|"\tindx %d\n"
argument_list|,
name|i
argument_list|)
expr_stmt|;
for|for
control|(
name|pgrp
operator|=
name|pgrphash
index|[
name|i
index|]
init|;
name|pgrp
condition|;
name|pgrp
operator|=
name|pgrp
operator|->
name|pg_hforw
control|)
block|{
name|printf
argument_list|(
literal|"\tpgrp %x, pgid %d, sess %x, sesscnt %d, mem %x\n"
argument_list|,
name|pgrp
argument_list|,
name|pgrp
operator|->
name|pg_id
argument_list|,
name|pgrp
operator|->
name|pg_session
argument_list|,
name|pgrp
operator|->
name|pg_session
operator|->
name|s_count
argument_list|,
name|pgrp
operator|->
name|pg_mem
argument_list|)
expr_stmt|;
for|for
control|(
name|p
operator|=
name|pgrp
operator|->
name|pg_mem
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|p_pgrpnxt
control|)
block|{
name|printf
argument_list|(
literal|"\t\tpid %d addr %x pgrp %x\n"
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
name|p
argument_list|,
name|p
operator|->
name|p_pgrp
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* debug */
end_comment

end_unit

