begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	uipc_syscalls.c	4.1	81/11/10	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/dir.h"
end_include

begin_include
include|#
directive|include
file|"../h/user.h"
end_include

begin_include
include|#
directive|include
file|"../h/proc.h"
end_include

begin_include
include|#
directive|include
file|"../h/file.h"
end_include

begin_include
include|#
directive|include
file|"../h/inode.h"
end_include

begin_include
include|#
directive|include
file|"../h/buf.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protocol.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/inaddr.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet_systm.h"
end_include

begin_comment
comment|/*  * Socket system call interface.  *  * These routines interface the socket routines to UNIX,  * isolating the system interface from the socket-protocol interface.  *  * DO SPLICE STUFF  * DO PIPE STUFF  * DO PORTALS  * DO ASSOCIATIONS  * DO NEWFD STUFF  */
end_comment

begin_comment
comment|/*  * Socket system call interface.  Copy in arguments  * set up file descriptor and call internal socket  * creation routine.  */
end_comment

begin_macro
name|ssocket
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|type
decl_stmt|;
name|struct
name|in_addr
modifier|*
name|ain
decl_stmt|;
name|int
name|options
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|in_addr
name|in
decl_stmt|;
name|struct
name|socket
modifier|*
name|so0
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
if|if
condition|(
operator|(
name|fp
operator|=
name|falloc
argument_list|()
operator|)
operator|==
name|NULL
condition|)
return|return;
name|fp
operator|->
name|f_flag
operator|=
name|FSOCKET
operator||
name|FREAD
operator||
name|FWRITE
expr_stmt|;
if|if
condition|(
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|ain
argument_list|,
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|socket
argument_list|(
operator|&
name|so0
argument_list|,
name|uap
operator|->
name|type
argument_list|,
operator|&
name|in
argument_list|,
name|uap
operator|->
name|options
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
goto|goto
name|bad
goto|;
name|fp
operator|->
name|f_socket
operator|=
name|so
expr_stmt|;
return|return;
name|bad
label|:
name|u
operator|.
name|u_ofile
index|[
name|u
operator|.
name|u_r
operator|.
name|r_val1
index|]
operator|=
literal|0
expr_stmt|;
name|fp
operator|->
name|f_count
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Pipe system call interface.  */
end_comment

begin_macro
name|spipe
argument_list|()
end_macro

begin_block
block|{  }
end_block

begin_decl_stmt
specifier|static
name|struct
name|in_addr
name|portalproto
init|=
block|{
name|PF_PORTAL
block|,
comment|/* rest don't care */
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Portal system call interface.  *  * This call creates a portal.  * All the difficulty here is in dealing with errors.  * A long sequence of steps is necessary:  *	1. a socket must be allocated  *	2. the server name length must be determined  *	3. the protal must be entered into the file system  *	4. the portal type and server must be entered into the portals' file  *	5. a file descriptor referencing the socket+inode must be returned  * If any errors occur in this process we must back it all out.  */
end_comment

begin_macro
name|sportal
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|caddr_t
name|name
decl_stmt|;
name|int
name|mode
decl_stmt|;
name|caddr_t
name|server
decl_stmt|;
name|int
name|kind
decl_stmt|;
block|}
modifier|*
name|ap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|int
name|err
decl_stmt|,
name|len
decl_stmt|;
name|char
name|ch
decl_stmt|;
comment|/* 	 * Allocate the socket for the portal. 	 */
name|u
operator|.
name|u_error
operator|=
name|socket
argument_list|(
operator|&
name|so
argument_list|,
name|SOCK_STREAM
argument_list|,
operator|&
name|portalproto
argument_list|,
name|SO_NEWFDONCONN
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
comment|/* 	 * Check that server name fis in a file system buffer. 	 * This to simplify the creation of the portal service process. 	 */
if|if
condition|(
name|ap
operator|->
name|server
condition|)
block|{
name|u
operator|.
name|u_dirp
operator|=
name|ap
operator|->
name|server
expr_stmt|;
for|for
control|(
name|len
operator|=
literal|0
init|;
name|len
operator|<
name|BSIZE
operator|-
literal|2
condition|;
name|len
operator|++
control|)
block|{
specifier|register
name|c
operator|=
name|uchar
argument_list|()
expr_stmt|;
if|if
condition|(
name|c
operator|<
literal|0
condition|)
goto|goto
name|bad
goto|;
if|if
condition|(
name|c
operator|==
literal|0
condition|)
break|break;
block|}
if|if
condition|(
name|len
operator|==
name|BSIZE
operator|-
literal|2
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
goto|goto
name|bad
goto|;
block|}
block|}
comment|/* 	 * Make sure that nothing with the portal's name exists. 	 */
name|u
operator|.
name|u_dirp
operator|=
name|ap
operator|->
name|name
expr_stmt|;
name|ip
operator|=
name|namei
argument_list|(
name|uchar
argument_list|,
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|!=
name|NULL
condition|)
block|{
name|iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|EEXIST
expr_stmt|;
block|}
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
goto|goto
name|bad
goto|;
comment|/* 	 * Make a node in the file system for the portal. 	 */
name|ip
operator|=
name|maknode
argument_list|(
operator|(
name|ap
operator|->
name|mode
operator|&
literal|0x7777
operator|)
operator||
name|IFPORTAL
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
goto|goto
name|bad
goto|;
comment|/* 	 * Make the first character of the contents of the 	 * portal be the portal type and the rest of the portal be 	 * the pathname of the server (if one was given). 	 */
name|ch
operator|=
operator|(
name|char
operator|)
name|ap
operator|->
name|kind
expr_stmt|;
name|u
operator|.
name|u_base
operator|=
operator|(
name|caddr_t
operator|)
operator|&
name|ch
expr_stmt|;
name|u
operator|.
name|u_count
operator|=
literal|1
expr_stmt|;
name|u
operator|.
name|u_offset
operator|=
literal|0
expr_stmt|;
name|u
operator|.
name|u_segflg
operator|=
literal|1
expr_stmt|;
name|writei
argument_list|(
name|ip
argument_list|)
expr_stmt|;
if|if
condition|(
name|ap
operator|->
name|server
condition|)
block|{
name|u
operator|.
name|u_base
operator|=
name|ap
operator|->
name|server
expr_stmt|;
name|u
operator|.
name|u_count
operator|=
name|len
expr_stmt|;
name|u
operator|.
name|u_segflg
operator|=
literal|0
expr_stmt|;
name|writei
argument_list|(
name|ip
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
goto|goto
name|bad2
goto|;
comment|/* 	 * Allocate a file descriptor and make it reference both 	 * the inode representing the portal and the call director 	 * socket for the portal. 	 */
name|fp
operator|=
name|falloc
argument_list|()
expr_stmt|;
if|if
condition|(
name|fp
operator|==
name|NULL
condition|)
goto|goto
name|bad2
goto|;
name|fp
operator|->
name|f_flags
operator|=
name|FPORTAL
operator||
name|FSOCKET
expr_stmt|;
name|fp
operator|->
name|f_inode
operator|=
name|ip
expr_stmt|;
name|fp
operator|->
name|f_socket
operator|=
name|s
expr_stmt|;
comment|/* 	 * Make the in-core inode reference the socket. 	 */
name|ip
operator|->
name|i_socket
operator|=
name|s
expr_stmt|;
name|prele
argument_list|(
name|ip
argument_list|)
expr_stmt|;
return|return;
name|bad2
label|:
name|err
operator|=
name|u
operator|.
name|u_error
expr_stmt|;
name|iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_dirp
operator|=
name|ap
operator|->
name|name
expr_stmt|;
name|unlink
argument_list|()
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|err
expr_stmt|;
name|bad
label|:
name|sofree
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Close a socket on last file table reference removal.  * Initiate disconnect if connected.  * Free socket when disconnect complete.  */
end_comment

begin_expr_stmt
name|skclose
argument_list|(
name|so
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
comment|/* conservative */
if|if
condition|(
name|so
operator|->
name|so_pcb
operator|==
literal|0
condition|)
goto|goto
name|discard
goto|;
if|if
condition|(
name|so
operator|->
name|so_state
operator|&
name|SS_ISCONNECTED
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|disconnect
argument_list|(
name|so
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
operator|(
name|so
operator|->
name|so_state
operator|&
name|SS_ISDISCONNECTING
operator|)
operator|&&
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_NBIO
operator|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINPROGRESS
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return;
block|}
while|while
condition|(
name|so
operator|->
name|so_state
operator|&
name|SS_ISCONNECTED
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
name|u
operator|.
name|u_error
operator|=
call|(
modifier|*
name|so
operator|->
name|so_proto
operator|->
name|pr_usrreq
call|)
argument_list|(
name|so
argument_list|,
name|PRU_DETACH
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|discard
label|:
name|sofree
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Select a socket.  */
end_comment

begin_expr_stmt
name|soselect
argument_list|(
name|so
argument_list|,
name|flag
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|flag
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|soreadable
argument_list|(
name|so
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
operator|(
name|p
operator|=
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|)
operator|&&
name|p
operator|->
name|p_wchan
operator|==
operator|(
name|caddr_t
operator|)
name|select
condition|)
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator||=
name|SB_COLL
expr_stmt|;
else|else
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Wakeup read sleep/select'ers.  */
end_comment

begin_macro
name|sowakeup
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|&&
name|soreadable
argument_list|(
name|so
argument_list|)
condition|)
block|{
name|selwakeup
argument_list|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
argument_list|,
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&
name|SB_COLL
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_sel
operator|=
literal|0
expr_stmt|;
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&=
operator|~
name|SB_COLL
expr_stmt|;
block|}
if|if
condition|(
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&
name|SB_WAIT
condition|)
block|{
name|so
operator|->
name|so_rcv
operator|.
name|sb_flags
operator|&=
operator|~
name|SB_WAIT
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_rcv
operator|.
name|sb_cc
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Connect socket to foreign peer; system call  * interface.  Copy in arguments and call internal routine.  */
end_comment

begin_macro
name|sconnect
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|struct
name|in_addr
modifier|*
name|a
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|in_addr
name|in
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
specifier|register
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|a
argument_list|,
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
name|so
operator|=
name|fp
operator|->
name|f_socket
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|connect
argument_list|(
name|so
argument_list|,
operator|&
name|in
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
name|s
operator|=
name|splnet
argument_list|()
expr_stmt|;
if|if
condition|(
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_NBIO
operator|)
operator|&&
operator|(
name|so
operator|->
name|so_state
operator|&
name|SS_ISCONN
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINPROGRESS
expr_stmt|;
return|return;
block|}
while|while
condition|(
operator|(
name|so
operator|->
name|so_state
operator|&
operator|(
name|SS_ISCONN
operator||
name|SS_ISCONNING
operator|)
operator|)
operator|==
name|SS_ISCONNING
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|so
operator|->
name|so_error
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Disconnect socket from foreign peer; system call  * interface.  Copy in arguments and call internal routine.  */
end_comment

begin_macro
name|sdisconnect
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|in_addr
modifier|*
name|addr
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
name|in_addr
name|in
decl_stmt|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|int
name|s
decl_stmt|;
if|if
condition|(
name|uap
operator|->
name|addr
operator|&&
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|addr
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|disconnect
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|uap
operator|->
name|addr
condition|?
operator|&
name|in
else|:
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_error
condition|)
return|return;
name|s
operator|=
name|splnet
argument_list|()
expr_stmt|;
if|if
condition|(
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_NBIO
operator|)
operator|&&
operator|(
name|so
operator|->
name|so_state
operator|&
name|SS_ISCONNECTED
operator|)
condition|)
return|return
operator|(
name|EINPROGRESS
operator|)
return|;
while|while
condition|(
operator|(
name|so
operator|)
operator|->
name|so_state
operator|&
operator|(
name|SS_ISCONNECTED
operator||
name|SS_ISDISCONNECTING
operator|)
operator|==
name|SS_ISDISCONNECTING
condition|)
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|so
operator|->
name|so_error
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Send data on socket.  */
end_comment

begin_macro
name|ssend
argument_list|()
end_macro

begin_block
block|{
specifier|register
struct|struct
name|a
block|{
name|int
name|fdes
decl_stmt|;
name|in_addr
modifier|*
name|ain
decl_stmt|;
name|caddr_t
name|cbuf
decl_stmt|;
name|int
name|count
decl_stmt|;
block|}
modifier|*
name|uap
init|=
operator|(
expr|struct
name|a
operator|*
operator|)
name|u
operator|.
name|u_ap
struct|;
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|struct
name|in_addr
name|in
decl_stmt|;
name|fp
operator|=
name|getf
argument_list|(
name|uap
operator|->
name|fdes
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|==
literal|0
condition|)
return|return;
if|if
condition|(
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
operator|)
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ENOTSOCK
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|uap
operator|->
name|count
operator|<
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EINVAL
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_base
operator|=
name|uap
operator|->
name|cbuf
expr_stmt|;
name|u
operator|.
name|u_count
operator|=
name|uap
operator|->
name|count
expr_stmt|;
name|u
operator|.
name|u_segflg
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|useracc
argument_list|(
name|u
operator|.
name|u_base
argument_list|,
name|u
operator|.
name|u_count
argument_list|,
name|B_READ
argument_list|)
operator|==
literal|0
operator|||
name|uap
operator|->
name|ain
operator|&&
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|ain
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|in
argument_list|,
sizeof|sizeof
argument_list|(
name|in
argument_list|)
argument_list|)
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EFAULT
expr_stmt|;
return|return;
block|}
name|u
operator|.
name|u_error
operator|=
name|send
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|uap
operator|->
name|ain
condition|?
operator|&
name|in
else|:
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

