begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*   * Copyright (c) 1995  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * Avadis Tevanian, Jr., Michael Wayne Young, and the Mach Operating  * System project at Carnegie-Mellon University.  *  * %sccs.include.redist.c%  *  *	@(#)kern_lock.c	8.1 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_comment
comment|/*  * Locking primitives implementation.  * Locks provide shared/exclusive sychronization.  */
end_comment

begin_if
if|#
directive|if
name|NCPUS
operator|>
literal|1
end_if

begin_comment
comment|/*  * For multiprocessor system, try spin lock first.  *  * This should be inline expanded below, but we cannot have #if  * inside a multiline define.  */
end_comment

begin_decl_stmt
name|int
name|lock_wait_time
init|=
literal|100
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|PAUSE
parameter_list|(
name|lkp
parameter_list|,
name|wanted
parameter_list|)
define|\
value|if (lock_wait_time> 0) {				\ 			int i;						\ 									\ 			atomic_unlock(&lkp->lk_interlock);		\ 			for (i = lock_wait_time; i> 0; i--)		\ 				if (!(wanted))				\ 					break;				\ 			atomic_lock(&lkp->lk_interlock);		\ 		}							\ 		if (!(wanted))						\ 			break;
end_define

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* NCPUS == 1 */
end_comment

begin_comment
comment|/*  * It is an error to spin on a uniprocessor as nothing will ever cause  * the atomic lock to clear while we are executing.  */
end_comment

begin_define
define|#
directive|define
name|PAUSE
parameter_list|(
name|lkp
parameter_list|,
name|wanted
parameter_list|)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NCPUS == 1 */
end_comment

begin_comment
comment|/*  * Acquire a resource.  */
end_comment

begin_define
define|#
directive|define
name|ACQUIRE
parameter_list|(
name|lkp
parameter_list|,
name|error
parameter_list|,
name|extflags
parameter_list|,
name|wanted
parameter_list|)
define|\
value|PAUSE(lkp, wanted);						\ 	for (error = 0; wanted; ) {					\ 		(lkp)->lk_flags |= LK_WAITING;				\ 		atomic_unlock(&(lkp)->lk_interlock);			\ 		error = tsleep(lkp, (lkp)->lk_prio, (lkp)->lk_wmesg,	\ 		    (lkp)->lk_timo);					\ 		atomic_lock(&(lkp)->lk_interlock);			\ 		(lkp)->lk_flags |= LK_SLEPT;				\ 		if (error)						\ 			break;						\ 		if ((extflags)& LK_SLEEPFAIL) {			\ 			error = ENOLCK;					\ 			break;						\ 		}							\ 	}
end_define

begin_comment
comment|/*  * Initialize a lock; required before use.  */
end_comment

begin_function
name|void
name|lock_init
parameter_list|(
name|lkp
parameter_list|,
name|prio
parameter_list|,
name|wmesg
parameter_list|,
name|timo
parameter_list|,
name|flags
parameter_list|)
name|struct
name|lock
modifier|*
name|lkp
decl_stmt|;
name|int
name|prio
decl_stmt|;
name|char
modifier|*
name|wmesg
decl_stmt|;
name|int
name|timo
decl_stmt|;
name|int
name|flags
decl_stmt|;
block|{
name|bzero
argument_list|(
name|lkp
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|lock
argument_list|)
argument_list|)
expr_stmt|;
name|atomic_lock_init
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_flags
operator|=
name|flags
operator|&
name|LK_EXTFLG_MASK
expr_stmt|;
name|lkp
operator|->
name|lk_prio
operator|=
name|prio
expr_stmt|;
name|lkp
operator|->
name|lk_timo
operator|=
name|timo
expr_stmt|;
name|lkp
operator|->
name|lk_wmesg
operator|=
name|wmesg
expr_stmt|;
name|lkp
operator|->
name|lk_lockholder
operator|=
name|LK_NOPROC
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Set, change, or release a lock.  *  * Shared requests increment the shared count. Exclusive requests set the  * LK_WANT_EXCL flag (preventing further shared locks), and wait for already  * accepted shared locks and shared-to-exclusive upgrades to go away.  */
end_comment

begin_macro
name|lockmgr
argument_list|(
argument|lkp
argument_list|,
argument|p
argument_list|,
argument|flags
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|lock
modifier|*
name|lkp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|flags
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|pid_t
name|pid
decl_stmt|;
name|int
name|error
decl_stmt|,
name|extflags
decl_stmt|;
name|pid
operator|=
name|p
operator|->
name|p_pid
expr_stmt|;
name|extflags
operator|=
operator|(
name|flags
operator||
name|lkp
operator|->
name|lk_flags
operator|)
operator|&
name|LK_EXTFLG_MASK
expr_stmt|;
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_SLEPT
expr_stmt|;
switch|switch
condition|(
name|flags
operator|&
name|LK_TYPE_MASK
condition|)
block|{
case|case
name|LK_SHARED
case|:
name|atomic_lock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_lockholder
operator|!=
name|pid
condition|)
block|{
comment|/* 			 * If just polling, check to see if we will block. 			 */
if|if
condition|(
operator|(
name|extflags
operator|&
name|LK_NOWAIT
operator|)
operator|&&
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
operator|(
name|LK_HAVE_EXCL
operator||
name|LK_WANT_EXCL
operator||
name|LK_WANT_UPGRADE
operator|)
operator|)
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|EBUSY
operator|)
return|;
block|}
comment|/* 			 * Wait for exclusive locks and upgrades to clear. 			 */
name|ACQUIRE
argument_list|(
name|lkp
argument_list|,
name|error
argument_list|,
name|extflags
argument_list|,
name|lkp
operator|->
name|lk_flags
operator|&
operator|(
name|LK_HAVE_EXCL
operator||
name|LK_WANT_EXCL
operator||
name|LK_WANT_UPGRADE
operator|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
name|lkp
operator|->
name|lk_sharecount
operator|++
expr_stmt|;
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* 		 * We hold an exclusive lock, so downgrade it to shared. 		 * An alternative would be to fail with EDEADLK. 		 */
name|lkp
operator|->
name|lk_sharecount
operator|++
expr_stmt|;
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
comment|/* fall into downgrade */
case|case
name|LK_DOWNGRADE
case|:
name|atomic_lock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_lockholder
operator|!=
name|pid
operator|||
name|lkp
operator|->
name|lk_exclusivecount
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"lockmgr: not holding exclusive lock"
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_sharecount
operator|+=
name|lkp
operator|->
name|lk_exclusivecount
expr_stmt|;
name|lkp
operator|->
name|lk_exclusivecount
operator|=
literal|0
expr_stmt|;
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_HAVE_EXCL
expr_stmt|;
name|lkp
operator|->
name|lk_lockholder
operator|=
name|LK_NOPROC
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WAITING
condition|)
block|{
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_WAITING
expr_stmt|;
name|wakeup
argument_list|(
name|lkp
argument_list|)
expr_stmt|;
block|}
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
case|case
name|LK_UPGRADE
case|:
comment|/* 		 * Upgrade a shared lock to an exclusive one. If another 		 * shared lock has already requested an upgrade to an 		 * exclusive lock, our shared lock is released and an 		 * exclusive lock is requested (which will be granted 		 * after the upgrade). 		 */
name|atomic_lock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_lockholder
operator|==
name|pid
operator|||
name|lkp
operator|->
name|lk_sharecount
operator|<=
literal|0
condition|)
name|panic
argument_list|(
literal|"lockmgr: upgrade exclusive lock"
argument_list|)
expr_stmt|;
comment|/* 		 * If we are just polling, check to see if we will block. 		 */
if|if
condition|(
operator|(
name|extflags
operator|&
name|LK_NOWAIT
operator|)
operator|&&
operator|(
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WANT_UPGRADE
operator|)
operator|||
name|lkp
operator|->
name|lk_sharecount
operator|>
literal|1
operator|)
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|EBUSY
operator|)
return|;
block|}
name|lkp
operator|->
name|lk_sharecount
operator|--
expr_stmt|;
if|if
condition|(
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WANT_UPGRADE
operator|)
operator|==
literal|0
condition|)
block|{
comment|/* 			 * We are first shared lock to request an upgrade, so 			 * request upgrade and wait for the shared count to 			 * drop to zero, then take exclusive lock. 			 */
name|lkp
operator|->
name|lk_flags
operator||=
name|LK_WANT_UPGRADE
expr_stmt|;
name|ACQUIRE
argument_list|(
name|lkp
argument_list|,
name|error
argument_list|,
name|extflags
argument_list|,
name|lkp
operator|->
name|lk_sharecount
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_WANT_UPGRADE
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
name|lkp
operator|->
name|lk_flags
operator||=
name|LK_HAVE_EXCL
expr_stmt|;
name|lkp
operator|->
name|lk_lockholder
operator|=
name|pid
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_exclusivecount
operator|!=
literal|0
condition|)
name|panic
argument_list|(
literal|"lockmgr: non-zero exclusive count"
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_exclusivecount
operator|=
literal|1
expr_stmt|;
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* 		 * Someone else has requested upgrade. Release our shared 		 * lock, awaken upgrade requestor if we are the last shared 		 * lock, then request an exclusive lock. 		 */
if|if
condition|(
name|lkp
operator|->
name|lk_sharecount
operator|==
literal|0
operator|&&
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WAITING
operator|)
condition|)
block|{
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_WAITING
expr_stmt|;
name|wakeup
argument_list|(
name|lkp
argument_list|)
expr_stmt|;
block|}
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
comment|/* fall into exclusive request */
case|case
name|LK_EXCLUSIVE
case|:
name|atomic_lock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_lockholder
operator|==
name|pid
condition|)
block|{
comment|/* 			 *	Recursive lock. 			 */
if|if
condition|(
operator|(
name|extflags
operator|&
name|LK_CANRECURSE
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"lockmgr: locking against myself"
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_exclusivecount
operator|++
expr_stmt|;
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* 		 * If we are just polling, check to see if we will sleep. 		 */
if|if
condition|(
operator|(
name|extflags
operator|&
name|LK_NOWAIT
operator|)
operator|&&
operator|(
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
operator|(
name|LK_HAVE_EXCL
operator||
name|LK_WANT_EXCL
operator||
name|LK_WANT_UPGRADE
operator|)
operator|)
operator|||
name|lkp
operator|->
name|lk_sharecount
operator|!=
literal|0
operator|)
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|EBUSY
operator|)
return|;
block|}
comment|/* 		 * Try to acquire the want_exclusive flag. 		 */
name|ACQUIRE
argument_list|(
name|lkp
argument_list|,
name|error
argument_list|,
name|extflags
argument_list|,
name|lkp
operator|->
name|lk_flags
operator|&
operator|(
name|LK_HAVE_EXCL
operator||
name|LK_WANT_EXCL
operator|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
name|lkp
operator|->
name|lk_flags
operator||=
name|LK_WANT_EXCL
expr_stmt|;
comment|/* 		 * Wait for shared locks and upgrades to finish. 		 */
name|ACQUIRE
argument_list|(
name|lkp
argument_list|,
name|error
argument_list|,
name|extflags
argument_list|,
name|lkp
operator|->
name|lk_sharecount
operator|!=
literal|0
operator|||
operator|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WANT_UPGRADE
operator|)
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_WANT_EXCL
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
name|lkp
operator|->
name|lk_flags
operator||=
name|LK_HAVE_EXCL
expr_stmt|;
name|lkp
operator|->
name|lk_lockholder
operator|=
name|pid
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_exclusivecount
operator|!=
literal|0
condition|)
name|panic
argument_list|(
literal|"lockmgr: non-zero exclusive count"
argument_list|)
expr_stmt|;
name|lkp
operator|->
name|lk_exclusivecount
operator|=
literal|1
expr_stmt|;
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
case|case
name|LK_RELEASE
case|:
name|atomic_lock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_exclusivecount
operator|!=
literal|0
condition|)
block|{
name|lkp
operator|->
name|lk_exclusivecount
operator|--
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_exclusivecount
operator|==
literal|0
condition|)
block|{
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_HAVE_EXCL
expr_stmt|;
name|lkp
operator|->
name|lk_lockholder
operator|=
name|LK_NOPROC
expr_stmt|;
block|}
block|}
elseif|else
if|if
condition|(
name|lkp
operator|->
name|lk_sharecount
operator|!=
literal|0
condition|)
name|lkp
operator|->
name|lk_sharecount
operator|--
expr_stmt|;
if|if
condition|(
name|lkp
operator|->
name|lk_flags
operator|&
name|LK_WAITING
condition|)
block|{
name|lkp
operator|->
name|lk_flags
operator|&=
operator|~
name|LK_WAITING
expr_stmt|;
name|wakeup
argument_list|(
name|lkp
argument_list|)
expr_stmt|;
block|}
name|atomic_unlock
argument_list|(
operator|&
name|lkp
operator|->
name|lk_interlock
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
default|default:
name|panic
argument_list|(
literal|"lockmgr: unknown locktype request %d"
argument_list|,
name|flags
operator|&
name|LK_TYPE_MASK
argument_list|)
expr_stmt|;
block|}
block|}
end_block

end_unit

