begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	uipc_socket2.c	4.5	81/11/21	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/dir.h"
end_include

begin_include
include|#
directive|include
file|"../h/user.h"
end_include

begin_include
include|#
directive|include
file|"../h/proc.h"
end_include

begin_include
include|#
directive|include
file|"../h/file.h"
end_include

begin_include
include|#
directive|include
file|"../h/inode.h"
end_include

begin_include
include|#
directive|include
file|"../h/buf.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet.h"
end_include

begin_include
include|#
directive|include
file|"../net/inet_systm.h"
end_include

begin_comment
comment|/*  * Primitive routines for operating on sockets and socket buffers  */
end_comment

begin_comment
comment|/*  * Procedures to manipulate state flags of socket  * and do appropriate wakeups.  */
end_comment

begin_macro
name|soisconnecting
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator|&=
operator|~
operator|(
name|SS_ISCONNECTED
operator||
name|SS_ISDISCONNECTING
operator|)
expr_stmt|;
name|so
operator|->
name|so_state
operator||=
name|SS_ISCONNECTING
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|soisconnected
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator|&=
operator|~
operator|(
name|SS_ISCONNECTING
operator||
name|SS_ISDISCONNECTING
operator|)
expr_stmt|;
name|so
operator|->
name|so_state
operator||=
name|SS_ISCONNECTED
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|soisdisconnecting
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator|&=
operator|~
operator|(
name|SS_ISCONNECTED
operator||
name|SS_ISCONNECTING
operator|)
expr_stmt|;
name|so
operator|->
name|so_state
operator||=
operator|(
name|SS_ISDISCONNECTING
operator||
name|SS_CANTRCVMORE
operator||
name|SS_CANTSENDMORE
operator|)
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|soisdisconnected
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator|&=
operator|~
operator|(
name|SS_ISCONNECTING
operator||
name|SS_ISCONNECTED
operator||
name|SS_ISDISCONNECTING
operator|)
expr_stmt|;
name|so
operator|->
name|so_state
operator||=
operator|(
name|SS_CANTRCVMORE
operator||
name|SS_CANTSENDMORE
operator|)
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|so
operator|->
name|so_timeo
argument_list|)
expr_stmt|;
name|sowwakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|sorwakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|socantsendmore
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator||=
name|SS_CANTSENDMORE
expr_stmt|;
name|sowwakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|socantrcvmore
argument_list|(
argument|so
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|so
operator|->
name|so_state
operator||=
name|SS_CANTRCVMORE
expr_stmt|;
name|sorwakeup
argument_list|(
name|so
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Select a socket.  */
end_comment

begin_expr_stmt
name|soselect
argument_list|(
name|so
argument_list|,
name|flag
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|flag
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|flag
operator|&
name|FREAD
condition|)
block|{
if|if
condition|(
name|soreadable
argument_list|(
name|so
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|sbselqueue
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|flag
operator|&
name|FWRITE
condition|)
block|{
if|if
condition|(
name|sowriteable
argument_list|(
name|so
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|sbselqueue
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Queue a process for a select on a socket buffer.  */
end_comment

begin_macro
name|sbselqueue
argument_list|(
argument|sb
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
operator|(
name|p
operator|=
name|sb
operator|->
name|sb_sel
operator|)
operator|&&
name|p
operator|->
name|p_wchan
operator|==
operator|(
name|caddr_t
operator|)
operator|&
name|selwait
condition|)
name|sb
operator|->
name|sb_flags
operator||=
name|SB_COLL
expr_stmt|;
else|else
name|sb
operator|->
name|sb_sel
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Wait for data to arrive at/drain from a socket buffer.  */
end_comment

begin_macro
name|sbwait
argument_list|(
argument|sb
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|sb
operator|->
name|sb_flags
operator||=
name|SB_WAIT
expr_stmt|;
name|sleep
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|sb
operator|->
name|sb_cc
argument_list|,
name|PZERO
operator|+
literal|1
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Wakeup processes waiting on a socket buffer.  */
end_comment

begin_macro
name|sbwakeup
argument_list|(
argument|sb
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|sb
operator|->
name|sb_sel
condition|)
block|{
name|selwakeup
argument_list|(
name|sb
operator|->
name|sb_sel
argument_list|,
name|sb
operator|->
name|sb_flags
operator|&
name|SB_COLL
argument_list|)
expr_stmt|;
name|sb
operator|->
name|sb_sel
operator|=
literal|0
expr_stmt|;
name|sb
operator|->
name|sb_flags
operator|&=
operator|~
name|SB_COLL
expr_stmt|;
block|}
if|if
condition|(
name|sb
operator|->
name|sb_flags
operator|&
name|SB_WAIT
condition|)
block|{
name|sb
operator|->
name|sb_flags
operator|&=
operator|~
name|SB_WAIT
expr_stmt|;
name|wakeup
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|sb
operator|->
name|sb_cc
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Allot mbufs to a sockbuf.  */
end_comment

begin_macro
name|sbreserve
argument_list|(
argument|sb
argument_list|,
argument|cc
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|m_reserve
argument_list|(
name|cc
argument_list|)
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|sb
operator|->
name|sb_hiwat
operator|=
name|cc
expr_stmt|;
name|sb
operator|->
name|sb_mbmax
operator|=
operator|(
name|cc
operator|*
literal|2
operator|)
operator|/
name|MSIZE
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Free mbufs held by a socket, and reserved mbuf space.  */
end_comment

begin_macro
name|sbrelease
argument_list|(
argument|sb
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|sbflush
argument_list|(
name|sb
argument_list|)
expr_stmt|;
name|m_release
argument_list|(
name|sb
operator|->
name|sb_hiwat
argument_list|)
expr_stmt|;
name|sb
operator|->
name|sb_hiwat
operator|=
name|sb
operator|->
name|sb_mbmax
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Routines to add (at the end) and remove (from the beginning)  * data from a mbuf queue.  */
end_comment

begin_comment
comment|/*  * Append mbuf queue m to sockbuf sb.  */
end_comment

begin_expr_stmt
name|sbappend
argument_list|(
name|sb
argument_list|,
name|m
argument_list|)
specifier|register
expr|struct
name|mbuf
operator|*
name|m
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
modifier|*
name|np
decl_stmt|,
modifier|*
name|n
decl_stmt|;
name|np
operator|=
operator|&
name|sb
operator|->
name|sb_mb
expr_stmt|;
name|n
operator|=
literal|0
expr_stmt|;
while|while
condition|(
operator|*
name|np
condition|)
block|{
name|n
operator|=
operator|*
name|np
expr_stmt|;
name|np
operator|=
operator|&
name|n
operator|->
name|m_next
expr_stmt|;
block|}
while|while
condition|(
name|m
condition|)
block|{
if|if
condition|(
name|n
operator|&&
name|n
operator|->
name|m_off
operator|<=
name|MMAXOFF
operator|&&
name|m
operator|->
name|m_off
operator|<=
name|MMAXOFF
operator|&&
operator|(
name|int
operator|)
name|n
operator|->
name|m_act
operator|==
literal|0
operator|&&
operator|(
name|int
operator|)
name|m
operator|->
name|m_act
operator|==
literal|0
operator|&&
operator|(
name|n
operator|->
name|m_off
operator|+
name|n
operator|->
name|m_len
operator|+
name|m
operator|->
name|m_off
operator|)
operator|<=
name|MMAXOFF
condition|)
block|{
name|bcopy
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
argument_list|,
name|mtod
argument_list|(
name|n
argument_list|,
name|caddr_t
argument_list|)
argument_list|,
operator|(
name|unsigned
operator|)
name|m
operator|->
name|m_len
argument_list|)
expr_stmt|;
name|n
operator|->
name|m_len
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
name|sb
operator|->
name|sb_cc
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
name|m
operator|=
name|m_free
argument_list|(
name|m
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|sballoc
argument_list|(
name|sb
argument_list|,
name|m
argument_list|)
expr_stmt|;
operator|*
name|np
operator|=
name|m
expr_stmt|;
name|n
operator|=
name|m
expr_stmt|;
name|np
operator|=
operator|&
name|n
operator|->
name|m_next
expr_stmt|;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
block|}
block|}
end_block

begin_macro
name|sbappendaddr
argument_list|(
argument|sb
argument_list|,
argument|asa
argument_list|,
argument|m0
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr
modifier|*
name|asa
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m0
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|sockaddr
modifier|*
name|msa
decl_stmt|;
specifier|register
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
specifier|register
name|int
name|len
init|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr
argument_list|)
decl_stmt|;
for|for
control|(
name|m
operator|=
name|m0
init|;
name|m
condition|;
name|m
operator|=
name|m
operator|->
name|m_next
control|)
name|len
operator|+=
name|m
operator|->
name|m_len
expr_stmt|;
if|if
condition|(
name|len
operator|>
name|sbspace
argument_list|(
name|sb
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|m
operator|=
name|m_get
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|m
operator|->
name|m_off
operator|=
name|MMINOFF
expr_stmt|;
name|m
operator|->
name|m_len
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr
argument_list|)
expr_stmt|;
name|msa
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|sockaddr
operator|*
argument_list|)
expr_stmt|;
operator|*
name|msa
operator|=
operator|*
name|asa
expr_stmt|;
name|m
operator|->
name|m_act
operator|=
operator|(
expr|struct
name|mbuf
operator|*
operator|)
literal|1
expr_stmt|;
name|sbappend
argument_list|(
name|sb
argument_list|,
name|m
argument_list|)
expr_stmt|;
name|m0
operator|->
name|m_act
operator|=
operator|(
expr|struct
name|mbuf
operator|*
operator|)
literal|1
expr_stmt|;
name|sbappend
argument_list|(
name|sb
argument_list|,
name|m0
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Free all mbufs on a sockbuf mbuf chain.  * Check that resource allocations return to 0.  */
end_comment

begin_macro
name|sbflush
argument_list|(
argument|sb
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|sockbuf
modifier|*
name|sb
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|sb
operator|->
name|sb_flags
operator|&
name|SB_LOCK
condition|)
name|panic
argument_list|(
literal|"sbflush"
argument_list|)
expr_stmt|;
name|sbdrop
argument_list|(
name|sb
argument_list|,
name|sb
operator|->
name|sb_cc
argument_list|)
expr_stmt|;
if|if
condition|(
name|sb
operator|->
name|sb_cc
operator|||
name|sb
operator|->
name|sb_mbcnt
operator|||
name|sb
operator|->
name|sb_mb
condition|)
name|panic
argument_list|(
literal|"sbflush 2"
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Drop data from (the front of) a sockbuf chain.  */
end_comment

begin_expr_stmt
name|sbdrop
argument_list|(
name|sb
argument_list|,
name|len
argument_list|)
specifier|register
expr|struct
name|sockbuf
operator|*
name|sb
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|len
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|mbuf
modifier|*
name|m
init|=
name|sb
operator|->
name|sb_mb
decl_stmt|,
modifier|*
name|mn
decl_stmt|;
while|while
condition|(
name|len
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|m
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"sbdrop"
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|->
name|m_len
operator|<=
name|len
condition|)
block|{
name|len
operator|-=
name|m
operator|->
name|m_len
expr_stmt|;
name|sbfree
argument_list|(
name|sb
argument_list|,
name|m
argument_list|)
expr_stmt|;
name|MFREE
argument_list|(
name|m
argument_list|,
name|mn
argument_list|)
expr_stmt|;
name|m
operator|=
name|mn
expr_stmt|;
block|}
else|else
block|{
name|m
operator|->
name|m_len
operator|-=
name|len
expr_stmt|;
name|m
operator|->
name|m_off
operator|+=
name|len
expr_stmt|;
name|sb
operator|->
name|sb_cc
operator|-=
name|len
expr_stmt|;
break|break;
block|}
block|}
name|sb
operator|->
name|sb_mb
operator|=
name|m
expr_stmt|;
block|}
end_block

end_unit

