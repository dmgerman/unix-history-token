begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1988 University of Utah.  * Copyright (c) 1982, 1986, 1990, 1993  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * the Systems Programming Group of the University of Utah Computer  * Science Department.  *  * %sccs.include.redist.c%  *  * from: Utah $Hdr: mem.c 1.14 90/10/12$  *  *	@(#)mem.c	8.3 (Berkeley) %G%  */
end_comment

begin_comment
comment|/*  * Memory special file  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/conf.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<machine/cpu.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm_param.h>
end_include

begin_include
include|#
directive|include
file|<vm/lock.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm_prot.h>
end_include

begin_include
include|#
directive|include
file|<vm/pmap.h>
end_include

begin_decl_stmt
specifier|extern
name|u_int
name|lowram
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|zeropage
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|mmrw
argument_list|(
argument|dev
argument_list|,
argument|uio
argument_list|,
argument|flags
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|uio
modifier|*
name|uio
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|flags
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|o
decl_stmt|;
specifier|register
name|u_int
name|c
decl_stmt|,
name|v
decl_stmt|;
specifier|register
name|struct
name|iovec
modifier|*
name|iov
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
name|int
name|kernloc
decl_stmt|;
while|while
condition|(
name|uio
operator|->
name|uio_resid
operator|>
literal|0
operator|&&
name|error
operator|==
literal|0
condition|)
block|{
name|iov
operator|=
name|uio
operator|->
name|uio_iov
expr_stmt|;
if|if
condition|(
name|iov
operator|->
name|iov_len
operator|==
literal|0
condition|)
block|{
name|uio
operator|->
name|uio_iov
operator|++
expr_stmt|;
name|uio
operator|->
name|uio_iovcnt
operator|--
expr_stmt|;
if|if
condition|(
name|uio
operator|->
name|uio_iovcnt
operator|<
literal|0
condition|)
name|panic
argument_list|(
literal|"mmrw"
argument_list|)
expr_stmt|;
continue|continue;
block|}
switch|switch
condition|(
name|minor
argument_list|(
name|dev
argument_list|)
condition|)
block|{
comment|/* minor device 0 is physical memory */
case|case
literal|0
case|:
name|v
operator|=
name|uio
operator|->
name|uio_offset
expr_stmt|;
ifndef|#
directive|ifndef
name|DEBUG
comment|/* allow reads only in RAM (except for DEBUG) */
if|if
condition|(
name|v
operator|>=
literal|0xFFFFFFFC
operator|||
name|v
operator|<
name|lowram
condition|)
return|return
operator|(
name|EFAULT
operator|)
return|;
endif|#
directive|endif
name|pmap_enter
argument_list|(
name|kernel_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|vmmap
argument_list|,
name|trunc_page
argument_list|(
name|v
argument_list|)
argument_list|,
name|uio
operator|->
name|uio_rw
operator|==
name|UIO_READ
condition|?
name|VM_PROT_READ
else|:
name|VM_PROT_WRITE
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
name|o
operator|=
operator|(
name|int
operator|)
name|uio
operator|->
name|uio_offset
operator|&
name|PGOFSET
expr_stmt|;
name|c
operator|=
call|(
name|u_int
call|)
argument_list|(
name|NBPG
operator|-
operator|(
operator|(
name|int
operator|)
name|iov
operator|->
name|iov_base
operator|&
name|PGOFSET
operator|)
argument_list|)
expr_stmt|;
name|c
operator|=
name|min
argument_list|(
name|c
argument_list|,
call|(
name|u_int
call|)
argument_list|(
name|NBPG
operator|-
name|o
argument_list|)
argument_list|)
expr_stmt|;
name|c
operator|=
name|min
argument_list|(
name|c
argument_list|,
operator|(
name|u_int
operator|)
name|iov
operator|->
name|iov_len
argument_list|)
expr_stmt|;
name|error
operator|=
name|uiomove
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|vmmap
index|[
name|o
index|]
argument_list|,
operator|(
name|int
operator|)
name|c
argument_list|,
name|uio
argument_list|)
expr_stmt|;
name|pmap_remove
argument_list|(
name|kernel_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|vmmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
operator|&
name|vmmap
index|[
name|NBPG
index|]
argument_list|)
expr_stmt|;
continue|continue;
comment|/* minor device 1 is kernel memory */
case|case
literal|1
case|:
name|kernloc
operator|=
name|uio
operator|->
name|uio_offset
expr_stmt|;
name|c
operator|=
name|min
argument_list|(
name|iov
operator|->
name|iov_len
argument_list|,
name|MAXPHYS
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|kernacc
argument_list|(
operator|(
name|caddr_t
operator|)
name|kernloc
argument_list|,
name|c
argument_list|,
name|uio
operator|->
name|uio_rw
operator|==
name|UIO_READ
condition|?
name|B_READ
else|:
name|B_WRITE
argument_list|)
condition|)
return|return
operator|(
name|EFAULT
operator|)
return|;
name|error
operator|=
name|uiomove
argument_list|(
operator|(
name|caddr_t
operator|)
name|kernloc
argument_list|,
operator|(
name|int
operator|)
name|c
argument_list|,
name|uio
argument_list|)
expr_stmt|;
continue|continue;
comment|/* minor device 2 is EOF/RATHOLE */
case|case
literal|2
case|:
if|if
condition|(
name|uio
operator|->
name|uio_rw
operator|==
name|UIO_WRITE
condition|)
name|uio
operator|->
name|uio_resid
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
comment|/* minor device 12 (/dev/zero) is source of nulls on read, rathole on write */
case|case
literal|12
case|:
if|if
condition|(
name|uio
operator|->
name|uio_rw
operator|==
name|UIO_WRITE
condition|)
block|{
name|c
operator|=
name|iov
operator|->
name|iov_len
expr_stmt|;
break|break;
block|}
comment|/* 			 * On the first call, allocate and zero a page 			 * of memory for use with /dev/zero. 			 * 			 * XXX on the hp300 we already know where there 			 * is a global zeroed page, the null segment table. 			 */
if|if
condition|(
name|zeropage
operator|==
name|NULL
condition|)
block|{
if|#
directive|if
name|CLBYTES
operator|==
name|NBPG
specifier|extern
name|caddr_t
name|Segtabzero
decl_stmt|;
name|zeropage
operator|=
name|Segtabzero
expr_stmt|;
else|#
directive|else
name|zeropage
operator|=
operator|(
name|caddr_t
operator|)
name|malloc
argument_list|(
name|CLBYTES
argument_list|,
name|M_TEMP
argument_list|,
name|M_WAITOK
argument_list|)
expr_stmt|;
name|bzero
argument_list|(
name|zeropage
argument_list|,
name|CLBYTES
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
name|c
operator|=
name|min
argument_list|(
name|iov
operator|->
name|iov_len
argument_list|,
name|CLBYTES
argument_list|)
expr_stmt|;
name|error
operator|=
name|uiomove
argument_list|(
name|zeropage
argument_list|,
operator|(
name|int
operator|)
name|c
argument_list|,
name|uio
argument_list|)
expr_stmt|;
continue|continue;
default|default:
return|return
operator|(
name|ENXIO
operator|)
return|;
block|}
if|if
condition|(
name|error
condition|)
break|break;
name|iov
operator|->
name|iov_base
operator|+=
name|c
expr_stmt|;
name|iov
operator|->
name|iov_len
operator|-=
name|c
expr_stmt|;
name|uio
operator|->
name|uio_offset
operator|+=
name|c
expr_stmt|;
name|uio
operator|->
name|uio_resid
operator|-=
name|c
expr_stmt|;
block|}
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_macro
name|mmmap
argument_list|(
argument|dev
argument_list|,
argument|off
argument_list|,
argument|prot
argument_list|)
end_macro

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|off
decl_stmt|,
name|prot
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* 	 * /dev/mem is the only one that makes sense through this 	 * interface.  For /dev/kmem any physaddr we return here 	 * could be transient and hence incorrect or invalid at 	 * a later time.  /dev/null just doesn't make any sense 	 * and /dev/zero is a hack that is handled via the default 	 * pager in mmap(). 	 */
if|if
condition|(
name|minor
argument_list|(
name|dev
argument_list|)
operator|!=
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
comment|/* 	 * Allow access only in RAM. 	 * 	 * XXX could be extended to allow access to IO space but must 	 * be very careful. 	 */
if|if
condition|(
operator|(
name|unsigned
operator|)
name|off
operator|<
name|lowram
operator|||
operator|(
name|unsigned
operator|)
name|off
operator|>=
literal|0xFFFFFFFC
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
return|return
operator|(
name|hp300_btop
argument_list|(
name|off
argument_list|)
operator|)
return|;
block|}
end_block

end_unit

