begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1993  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1993 Jan-Simon Pendry  *  * This code is derived from software contributed to Berkeley by  * Jan-Simon Pendry.  *  * %sccs.include.redist.c%  *  *	@(#)procfs_machdep.c	8.3 (Berkeley) %G%  *  * From:  *	$Id: procfs_i386.c,v 3.2 1993/12/15 09:40:17 jsp Exp $  */
end_comment

begin_comment
comment|/*  * Functions to be implemented here are:  *  * procfs_read_regs(proc, regs)  *	Get the current user-visible register set from the process  *	and copy it into the regs structure (<machine/reg.h>).  *	The process is stopped at the time read_regs is called.  *  * procfs_write_regs(proc, regs)  *	Update the current register set from the passed in regs  *	structure.  Take care to avoid clobbering special CPU  *	registers or privileged bits in the PSL.  *	The process is stopped at the time write_regs is called.  *  * procfs_read_fpregs, procfs_write_fpregs  *	deal with the floating point register set, otherwise as above.  *  * procfs_sstep(proc)  *	Arrange for the process to trap after executing a single instruction.  *  * procfs_fix_sstep(proc)  *	Cleanup process state after executing a single-step instruction.  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/user.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<machine/psl.h>
end_include

begin_include
include|#
directive|include
file|<machine/reg.h>
end_include

begin_comment
comment|/*#include<machine/frame.h>*/
end_comment

begin_include
include|#
directive|include
file|<miscfs/procfs/procfs.h>
end_include

begin_function
name|int
name|procfs_read_regs
parameter_list|(
name|p
parameter_list|,
name|regs
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|struct
name|reg
modifier|*
name|regs
decl_stmt|;
block|{
name|struct
name|frame
modifier|*
name|f
decl_stmt|;
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|P_INMEM
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
name|EIO
operator|)
return|;
name|f
operator|=
operator|(
expr|struct
name|frame
operator|*
operator|)
name|p
operator|->
name|p_md
operator|.
name|md_regs
expr_stmt|;
name|bcopy
argument_list|(
operator|(
name|void
operator|*
operator|)
name|f
operator|->
name|f_regs
argument_list|,
operator|(
name|void
operator|*
operator|)
name|regs
operator|->
name|r_regs
argument_list|,
sizeof|sizeof
argument_list|(
name|regs
operator|->
name|r_regs
argument_list|)
argument_list|)
expr_stmt|;
name|regs
operator|->
name|r_pc
operator|=
name|f
operator|->
name|f_pc
expr_stmt|;
name|regs
operator|->
name|r_sr
operator|=
name|f
operator|->
name|f_sr
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Update the process's current register  * set.  Depending on the architecture this  * may have fix-up work to do, especially  * if the IAR or PCW are modified.  */
end_comment

begin_function
name|int
name|procfs_write_regs
parameter_list|(
name|p
parameter_list|,
name|regs
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|struct
name|reg
modifier|*
name|regs
decl_stmt|;
block|{
name|struct
name|frame
modifier|*
name|f
decl_stmt|;
if|if
condition|(
operator|(
name|p
operator|->
name|p_flag
operator|&
name|P_INMEM
operator|)
operator|==
literal|0
condition|)
return|return
operator|(
name|EIO
operator|)
return|;
name|f
operator|=
operator|(
expr|struct
name|frame
operator|*
operator|)
name|p
operator|->
name|p_md
operator|.
name|md_regs
expr_stmt|;
name|bcopy
argument_list|(
operator|(
name|void
operator|*
operator|)
name|regs
operator|->
name|r_regs
argument_list|,
operator|(
name|void
operator|*
operator|)
name|f
operator|->
name|f_regs
argument_list|,
sizeof|sizeof
argument_list|(
name|f
operator|->
name|f_regs
argument_list|)
argument_list|)
expr_stmt|;
name|f
operator|->
name|f_pc
operator|=
name|regs
operator|->
name|r_pc
expr_stmt|;
name|f
operator|->
name|f_sr
operator|=
name|regs
operator|->
name|r_sr
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
name|int
name|procfs_read_fpregs
parameter_list|(
name|p
parameter_list|,
name|fpregs
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|struct
name|fpreg
modifier|*
name|fpregs
decl_stmt|;
block|{
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_function

begin_function
name|int
name|procfs_write_fpregs
parameter_list|(
name|p
parameter_list|,
name|fpregs
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|struct
name|fpreg
modifier|*
name|fpregs
decl_stmt|;
block|{
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_function

begin_function
name|int
name|procfs_sstep
parameter_list|(
name|p
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
block|{
name|int
name|error
decl_stmt|;
name|struct
name|reg
name|r
decl_stmt|;
name|error
operator|=
name|procfs_read_regs
argument_list|(
name|p
argument_list|,
operator|&
name|r
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
literal|0
condition|)
block|{
name|r
operator|.
name|r_sr
operator||=
name|PSL_T
expr_stmt|;
name|error
operator|=
name|procfs_write_regs
argument_list|(
name|p
argument_list|,
operator|&
name|r
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|error
operator|)
return|;
block|}
end_function

begin_function
name|void
name|procfs_fix_sstep
parameter_list|(
name|p
parameter_list|)
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
block|{ }
end_function

end_unit

