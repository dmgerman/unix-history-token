begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1988 University of Utah.  * Copyright (c) 1982, 1986, 1990, 1993  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * the Systems Programming Group of the University of Utah Computer  * Science Department.  *  * %sccs.include.redist.c%  *  * from: Utah $Hdr: vm_machdep.c 1.21 91/04/06$  *  *	@(#)vm_machdep.c	8.5 (Berkeley) %G%  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/user.h>
end_include

begin_include
include|#
directive|include
file|<machine/cpu.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm_kern.h>
end_include

begin_include
include|#
directive|include
file|<hp300/hp300/pte.h>
end_include

begin_comment
comment|/*  * Finish a fork operation, with process p2 nearly set up.  * Copy and update the kernel stack and pcb, making the child  * ready to run, and marking it so that it can return differently  * than the parent.  Returns 1 in the child process, 0 in the parent.  * We currently double-map the user area so that the stack is at the same  * address in each process; in the future we will probably relocate  * the frame pointers on the stack after copying.  */
end_comment

begin_expr_stmt
name|cpu_fork
argument_list|(
name|p1
argument_list|,
name|p2
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p1
operator|,
operator|*
name|p2
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|user
modifier|*
name|up
init|=
name|p2
operator|->
name|p_addr
decl_stmt|;
name|int
name|offset
decl_stmt|;
specifier|extern
name|caddr_t
name|getsp
parameter_list|()
function_decl|;
specifier|extern
name|char
name|kstack
index|[]
decl_stmt|;
name|p2
operator|->
name|p_md
operator|.
name|md_regs
operator|=
name|p1
operator|->
name|p_md
operator|.
name|md_regs
expr_stmt|;
name|p2
operator|->
name|p_md
operator|.
name|md_flags
operator|=
operator|(
name|p1
operator|->
name|p_md
operator|.
name|md_flags
operator|&
operator|~
operator|(
name|MDP_AST
operator||
name|MDP_HPUXTRACE
operator|)
operator|)
expr_stmt|;
comment|/* 	 * Copy pcb and stack from proc p1 to p2.  	 * We do this as cheaply as possible, copying only the active 	 * part of the stack.  The stack and pcb need to agree; 	 * this is tricky, as the final pcb is constructed by savectx, 	 * but its frame isn't yet on the stack when the stack is copied. 	 * switch compensates for this when the child eventually runs. 	 * This should be done differently, with a single call 	 * that copies and updates the pcb+stack, 	 * replacing the bcopy and savectx. 	 */
name|p2
operator|->
name|p_addr
operator|->
name|u_pcb
operator|=
name|p1
operator|->
name|p_addr
operator|->
name|u_pcb
expr_stmt|;
name|offset
operator|=
name|getsp
argument_list|()
operator|-
name|kstack
expr_stmt|;
name|bcopy
argument_list|(
operator|(
name|caddr_t
operator|)
name|kstack
operator|+
name|offset
argument_list|,
operator|(
name|caddr_t
operator|)
name|p2
operator|->
name|p_addr
operator|+
name|offset
argument_list|,
operator|(
name|unsigned
operator|)
name|ctob
argument_list|(
name|UPAGES
argument_list|)
operator|-
name|offset
argument_list|)
expr_stmt|;
name|PMAP_ACTIVATE
argument_list|(
operator|&
name|p2
operator|->
name|p_vmspace
operator|->
name|vm_pmap
argument_list|,
operator|&
name|up
operator|->
name|u_pcb
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Arrange for a non-local goto when the new process 	 * is started, to resume here, returning nonzero from setjmp. 	 */
if|if
condition|(
name|savectx
argument_list|(
name|up
argument_list|,
literal|1
argument_list|)
condition|)
block|{
comment|/* 		 * Return 1 in child. 		 */
return|return
operator|(
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * cpu_exit is called as the last action during exit.  * We release the address space and machine-dependent resources,  * including the memory for the user structure and kernel stack.  * Once finished, we call switch_exit, which switches to a temporary  * pcb and stack and never returns.  We block memory allocation  * until switch_exit has made things safe again.  */
end_comment

begin_macro
name|cpu_exit
argument_list|(
argument|p
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vmspace_free
argument_list|(
name|p
operator|->
name|p_vmspace
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|splimp
argument_list|()
expr_stmt|;
name|kmem_free
argument_list|(
name|kernel_map
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|p
operator|->
name|p_addr
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|)
expr_stmt|;
name|switch_exit
argument_list|()
expr_stmt|;
comment|/* NOTREACHED */
block|}
end_block

begin_comment
comment|/*  * Dump the machine specific header information at the start of a core dump.  */
end_comment

begin_macro
name|cpu_coredump
argument_list|(
argument|p
argument_list|,
argument|vp
argument_list|,
argument|cred
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|ucred
modifier|*
name|cred
decl_stmt|;
end_decl_stmt

begin_block
block|{
ifdef|#
directive|ifdef
name|HPUXCOMPAT
comment|/* 	 * If we loaded from an HP-UX format binary file we dump enough 	 * of an HP-UX style user struct so that the HP-UX debuggers can 	 * grok it. 	 */
if|if
condition|(
name|p
operator|->
name|p_md
operator|.
name|md_flags
operator|&
name|MDP_HPUX
condition|)
return|return
operator|(
name|hpuxdumpu
argument_list|(
name|vp
argument_list|,
name|cred
argument_list|)
operator|)
return|;
endif|#
directive|endif
return|return
operator|(
name|vn_rdwr
argument_list|(
name|UIO_WRITE
argument_list|,
name|vp
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
operator|->
name|p_addr
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|,
operator|(
name|off_t
operator|)
literal|0
argument_list|,
name|UIO_SYSSPACE
argument_list|,
name|IO_NODELOCKED
operator||
name|IO_UNIT
argument_list|,
name|cred
argument_list|,
operator|(
name|int
operator|*
operator|)
name|NULL
argument_list|,
name|p
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Move pages from one kernel virtual address to another.  * Both addresses are assumed to reside in the Sysmap,  * and size must be a multiple of CLSIZE.  */
end_comment

begin_expr_stmt
name|pagemove
argument_list|(
name|from
argument_list|,
name|to
argument_list|,
name|size
argument_list|)
specifier|register
name|caddr_t
name|from
operator|,
name|to
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|size
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pte
modifier|*
name|fpte
decl_stmt|,
modifier|*
name|tpte
decl_stmt|;
if|if
condition|(
name|size
operator|%
name|CLBYTES
condition|)
name|panic
argument_list|(
literal|"pagemove"
argument_list|)
expr_stmt|;
name|fpte
operator|=
name|kvtopte
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|tpte
operator|=
name|kvtopte
argument_list|(
name|to
argument_list|)
expr_stmt|;
while|while
condition|(
name|size
operator|>
literal|0
condition|)
block|{
operator|*
name|tpte
operator|++
operator|=
operator|*
name|fpte
expr_stmt|;
operator|*
operator|(
name|int
operator|*
operator|)
name|fpte
operator|++
operator|=
name|PG_NV
expr_stmt|;
name|TBIS
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|TBIS
argument_list|(
name|to
argument_list|)
expr_stmt|;
name|from
operator|+=
name|NBPG
expr_stmt|;
name|to
operator|+=
name|NBPG
expr_stmt|;
name|size
operator|-=
name|NBPG
expr_stmt|;
block|}
name|DCIS
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Map `size' bytes of physical memory starting at `paddr' into  * kernel VA space at `vaddr'.  Read/write and cache-inhibit status  * are specified by `prot'.  */
end_comment

begin_macro
name|physaccess
argument_list|(
argument|vaddr
argument_list|,
argument|paddr
argument_list|,
argument|size
argument_list|,
argument|prot
argument_list|)
end_macro

begin_decl_stmt
name|caddr_t
name|vaddr
decl_stmt|,
name|paddr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|int
name|size
decl_stmt|,
name|prot
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pte
modifier|*
name|pte
decl_stmt|;
specifier|register
name|u_int
name|page
decl_stmt|;
name|pte
operator|=
name|kvtopte
argument_list|(
name|vaddr
argument_list|)
expr_stmt|;
name|page
operator|=
operator|(
name|u_int
operator|)
name|paddr
operator|&
name|PG_FRAME
expr_stmt|;
for|for
control|(
name|size
operator|=
name|btoc
argument_list|(
name|size
argument_list|)
init|;
name|size
condition|;
name|size
operator|--
control|)
block|{
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|++
operator|=
name|PG_V
operator||
name|prot
operator||
name|page
expr_stmt|;
name|page
operator|+=
name|NBPG
expr_stmt|;
block|}
name|TBIAS
argument_list|()
expr_stmt|;
block|}
end_block

begin_macro
name|physunaccess
argument_list|(
argument|vaddr
argument_list|,
argument|size
argument_list|)
end_macro

begin_decl_stmt
name|caddr_t
name|vaddr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|int
name|size
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pte
modifier|*
name|pte
decl_stmt|;
name|pte
operator|=
name|kvtopte
argument_list|(
name|vaddr
argument_list|)
expr_stmt|;
for|for
control|(
name|size
operator|=
name|btoc
argument_list|(
name|size
argument_list|)
init|;
name|size
condition|;
name|size
operator|--
control|)
operator|*
operator|(
name|int
operator|*
operator|)
name|pte
operator|++
operator|=
name|PG_NV
expr_stmt|;
name|TBIAS
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Set a red zone in the kernel stack after the u. area.  * We don't support a redzone right now.  It really isn't clear  * that it is a good idea since, if the kernel stack were to roll  * into a write protected page, the processor would lock up (since  * it cannot create an exception frame) and we would get no useful  * post-mortem info.  Currently, under the DEBUG option, we just  * check at every clock interrupt to see if the current k-stack has  * gone too far (i.e. into the "redzone" page) and if so, panic.  * Look at _lev6intr in locore.s for more details.  */
end_comment

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_macro
name|setredzone
argument_list|(
argument|pte
argument_list|,
argument|vaddr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|pte
modifier|*
name|pte
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|vaddr
decl_stmt|;
end_decl_stmt

begin_block
block|{ }
end_block

begin_comment
comment|/*  * Convert kernel VA to physical address  */
end_comment

begin_expr_stmt
name|kvtop
argument_list|(
name|addr
argument_list|)
specifier|register
name|caddr_t
name|addr
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|vm_offset_t
name|va
decl_stmt|;
name|va
operator|=
name|pmap_extract
argument_list|(
name|kernel_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|va
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"kvtop: zero page frame"
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|int
operator|)
name|va
operator|)
return|;
block|}
end_block

begin_decl_stmt
specifier|extern
name|vm_map_t
name|phys_map
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Map an IO request into kernel virtual address space.  *  * XXX we allocate KVA space by using kmem_alloc_wait which we know  * allocates space without backing physical memory.  This implementation  * is a total crock, the multiple mappings of these physical pages should  * be reflected in the higher-level VM structures to avoid problems.  */
end_comment

begin_expr_stmt
name|vmapbuf
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|npf
decl_stmt|;
specifier|register
name|caddr_t
name|addr
decl_stmt|;
specifier|register
name|long
name|flags
init|=
name|bp
operator|->
name|b_flags
decl_stmt|;
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|int
name|off
decl_stmt|;
name|vm_offset_t
name|kva
decl_stmt|;
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
if|if
condition|(
operator|(
name|flags
operator|&
name|B_PHYS
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vmapbuf"
argument_list|)
expr_stmt|;
name|addr
operator|=
name|bp
operator|->
name|b_saveaddr
operator|=
name|bp
operator|->
name|b_data
expr_stmt|;
name|off
operator|=
operator|(
name|int
operator|)
name|addr
operator|&
name|PGOFSET
expr_stmt|;
name|p
operator|=
name|bp
operator|->
name|b_proc
expr_stmt|;
name|npf
operator|=
name|btoc
argument_list|(
name|round_page
argument_list|(
name|bp
operator|->
name|b_bcount
operator|+
name|off
argument_list|)
argument_list|)
expr_stmt|;
name|kva
operator|=
name|kmem_alloc_wait
argument_list|(
name|phys_map
argument_list|,
name|ctob
argument_list|(
name|npf
argument_list|)
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_data
operator|=
call|(
name|caddr_t
call|)
argument_list|(
name|kva
operator|+
name|off
argument_list|)
expr_stmt|;
while|while
condition|(
name|npf
operator|--
condition|)
block|{
name|pa
operator|=
name|pmap_extract
argument_list|(
name|vm_map_pmap
argument_list|(
operator|&
name|p
operator|->
name|p_vmspace
operator|->
name|vm_map
argument_list|)
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|pa
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vmapbuf: null page frame"
argument_list|)
expr_stmt|;
name|pmap_enter
argument_list|(
name|vm_map_pmap
argument_list|(
name|phys_map
argument_list|)
argument_list|,
name|kva
argument_list|,
name|trunc_page
argument_list|(
name|pa
argument_list|)
argument_list|,
name|VM_PROT_READ
operator||
name|VM_PROT_WRITE
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
name|addr
operator|+=
name|PAGE_SIZE
expr_stmt|;
name|kva
operator|+=
name|PAGE_SIZE
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Free the io map PTEs associated with this IO operation.  */
end_comment

begin_expr_stmt
name|vunmapbuf
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|caddr_t
name|addr
decl_stmt|;
specifier|register
name|int
name|npf
decl_stmt|;
name|vm_offset_t
name|kva
decl_stmt|;
if|if
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_PHYS
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vunmapbuf"
argument_list|)
expr_stmt|;
name|addr
operator|=
name|bp
operator|->
name|b_data
expr_stmt|;
name|npf
operator|=
name|btoc
argument_list|(
name|round_page
argument_list|(
name|bp
operator|->
name|b_bcount
operator|+
operator|(
operator|(
name|int
operator|)
name|addr
operator|&
name|PGOFSET
operator|)
argument_list|)
argument_list|)
expr_stmt|;
name|kva
operator|=
call|(
name|vm_offset_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|addr
operator|&
operator|~
name|PGOFSET
argument_list|)
expr_stmt|;
name|kmem_free_wakeup
argument_list|(
name|phys_map
argument_list|,
name|kva
argument_list|,
name|ctob
argument_list|(
name|npf
argument_list|)
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_data
operator|=
name|bp
operator|->
name|b_saveaddr
expr_stmt|;
name|bp
operator|->
name|b_saveaddr
operator|=
name|NULL
expr_stmt|;
block|}
end_block

begin_ifdef
ifdef|#
directive|ifdef
name|MAPPEDCOPY
end_ifdef

begin_decl_stmt
name|u_int
name|mappedcopysize
init|=
literal|4096
decl_stmt|;
end_decl_stmt

begin_expr_stmt
name|mappedcopyin
argument_list|(
name|fromp
argument_list|,
name|top
argument_list|,
name|count
argument_list|)
specifier|register
name|char
operator|*
name|fromp
operator|,
operator|*
name|top
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|count
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|vm_offset_t
name|kva
decl_stmt|,
name|upa
decl_stmt|;
specifier|register
name|int
name|off
decl_stmt|,
name|len
decl_stmt|;
name|int
name|alignable
decl_stmt|;
name|pmap_t
name|upmap
decl_stmt|;
specifier|extern
name|caddr_t
name|CADDR1
decl_stmt|;
name|kva
operator|=
operator|(
name|vm_offset_t
operator|)
name|CADDR1
expr_stmt|;
name|off
operator|=
operator|(
name|vm_offset_t
operator|)
name|fromp
operator|&
name|PAGE_MASK
expr_stmt|;
name|alignable
operator|=
operator|(
name|off
operator|==
operator|(
operator|(
name|vm_offset_t
operator|)
name|top
operator|&
name|PAGE_MASK
operator|)
operator|)
expr_stmt|;
name|upmap
operator|=
name|vm_map_pmap
argument_list|(
operator|&
name|curproc
operator|->
name|p_vmspace
operator|->
name|vm_map
argument_list|)
expr_stmt|;
while|while
condition|(
name|count
operator|>
literal|0
condition|)
block|{
comment|/* 		 * First access of a page, use fubyte to make sure 		 * page is faulted in and read access allowed. 		 */
if|if
condition|(
name|fubyte
argument_list|(
name|fromp
argument_list|)
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
name|EFAULT
operator|)
return|;
comment|/* 		 * Map in the page and bcopy data in from it 		 */
name|upa
operator|=
name|pmap_extract
argument_list|(
name|upmap
argument_list|,
name|trunc_page
argument_list|(
name|fromp
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|upa
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"mappedcopyin"
argument_list|)
expr_stmt|;
name|len
operator|=
name|min
argument_list|(
name|count
argument_list|,
name|PAGE_SIZE
operator|-
name|off
argument_list|)
expr_stmt|;
name|pmap_enter
argument_list|(
name|kernel_pmap
argument_list|,
name|kva
argument_list|,
name|upa
argument_list|,
name|VM_PROT_READ
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|==
name|PAGE_SIZE
operator|&&
name|alignable
operator|&&
name|off
operator|==
literal|0
condition|)
name|copypage
argument_list|(
name|kva
argument_list|,
name|top
argument_list|)
expr_stmt|;
else|else
name|bcopy
argument_list|(
call|(
name|caddr_t
call|)
argument_list|(
name|kva
operator|+
name|off
argument_list|)
argument_list|,
name|top
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|fromp
operator|+=
name|len
expr_stmt|;
name|top
operator|+=
name|len
expr_stmt|;
name|count
operator|-=
name|len
expr_stmt|;
name|off
operator|=
literal|0
expr_stmt|;
block|}
name|pmap_remove
argument_list|(
name|kernel_pmap
argument_list|,
name|kva
argument_list|,
name|kva
operator|+
name|PAGE_SIZE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|mappedcopyout
argument_list|(
name|fromp
argument_list|,
name|top
argument_list|,
name|count
argument_list|)
specifier|register
name|char
operator|*
name|fromp
operator|,
operator|*
name|top
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|count
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|vm_offset_t
name|kva
decl_stmt|,
name|upa
decl_stmt|;
specifier|register
name|int
name|off
decl_stmt|,
name|len
decl_stmt|;
name|int
name|alignable
decl_stmt|;
name|pmap_t
name|upmap
decl_stmt|;
specifier|extern
name|caddr_t
name|CADDR2
decl_stmt|;
name|kva
operator|=
operator|(
name|vm_offset_t
operator|)
name|CADDR2
expr_stmt|;
name|off
operator|=
operator|(
name|vm_offset_t
operator|)
name|top
operator|&
name|PAGE_MASK
expr_stmt|;
name|alignable
operator|=
operator|(
name|off
operator|==
operator|(
operator|(
name|vm_offset_t
operator|)
name|fromp
operator|&
name|PAGE_MASK
operator|)
operator|)
expr_stmt|;
name|upmap
operator|=
name|vm_map_pmap
argument_list|(
operator|&
name|curproc
operator|->
name|p_vmspace
operator|->
name|vm_map
argument_list|)
expr_stmt|;
while|while
condition|(
name|count
operator|>
literal|0
condition|)
block|{
comment|/* 		 * First access of a page, use subyte to make sure 		 * page is faulted in and write access allowed. 		 */
if|if
condition|(
name|subyte
argument_list|(
name|top
argument_list|,
operator|*
name|fromp
argument_list|)
operator|==
operator|-
literal|1
condition|)
return|return
operator|(
name|EFAULT
operator|)
return|;
comment|/* 		 * Map in the page and bcopy data out to it 		 */
name|upa
operator|=
name|pmap_extract
argument_list|(
name|upmap
argument_list|,
name|trunc_page
argument_list|(
name|top
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|upa
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"mappedcopyout"
argument_list|)
expr_stmt|;
name|len
operator|=
name|min
argument_list|(
name|count
argument_list|,
name|PAGE_SIZE
operator|-
name|off
argument_list|)
expr_stmt|;
name|pmap_enter
argument_list|(
name|kernel_pmap
argument_list|,
name|kva
argument_list|,
name|upa
argument_list|,
name|VM_PROT_READ
operator||
name|VM_PROT_WRITE
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|==
name|PAGE_SIZE
operator|&&
name|alignable
operator|&&
name|off
operator|==
literal|0
condition|)
name|copypage
argument_list|(
name|fromp
argument_list|,
name|kva
argument_list|)
expr_stmt|;
else|else
name|bcopy
argument_list|(
name|fromp
argument_list|,
call|(
name|caddr_t
call|)
argument_list|(
name|kva
operator|+
name|off
argument_list|)
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|fromp
operator|+=
name|len
expr_stmt|;
name|top
operator|+=
name|len
expr_stmt|;
name|count
operator|-=
name|len
expr_stmt|;
name|off
operator|=
literal|0
expr_stmt|;
block|}
name|pmap_remove
argument_list|(
name|kernel_pmap
argument_list|,
name|kva
argument_list|,
name|kva
operator|+
name|PAGE_SIZE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

end_unit

