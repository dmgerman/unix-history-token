begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1988, 1990 Regents of the University of California.  * All rights reserved.  *  * %sccs.include.redist.c%  *  * from: Utah $Hdr: in_cksum.c 1.6 89/08/24$  *  *	@(#)in_cksum.c	7.1 (Berkeley) %G%  */
end_comment

begin_comment
comment|/*  * in_cksum - checksum routine for the Internet Protocol family.  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../netinet/in.h"
end_include

begin_include
include|#
directive|include
file|"../netinet/in_systm.h"
end_include

begin_function_decl
specifier|extern
name|int
name|oc_cksum
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * Checksum routine for the Internet Protocol family.  *  * This isn't as bad as it looks.  For ip headers the "while" isn't  * executed and we just drop through to the return statement at the  * end.  For the usual tcp or udp packet (a single header mbuf  * chained onto a cluster of data, we make exactly one trip through  * the while (for the header mbuf) and never do the hairy code  * inside the "if".  If fact, if m_copydata& sb_compact are doing  * their job, we should never do the hairy code inside the "if".  */
end_comment

begin_expr_stmt
name|in_cksum
argument_list|(
name|m
argument_list|,
name|len
argument_list|)
specifier|register
expr|struct
name|mbuf
operator|*
name|m
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|int
name|len
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|sum
init|=
literal|0
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
while|while
condition|(
name|len
operator|>
name|m
operator|->
name|m_len
condition|)
block|{
name|sum
operator|=
name|oc_cksum
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|u_char
operator|*
argument_list|)
argument_list|,
name|i
operator|=
name|m
operator|->
name|m_len
argument_list|,
name|sum
argument_list|)
expr_stmt|;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|len
operator|-=
name|i
expr_stmt|;
if|if
condition|(
name|i
operator|&
literal|1
condition|)
block|{
comment|/* 			 * ouch - we ended on an odd byte with more 			 * to do.  This xfer is obviously not interested 			 * in performance so finish things slowly. 			 */
specifier|register
name|u_char
modifier|*
name|cp
decl_stmt|;
while|while
condition|(
name|len
operator|>
name|m
operator|->
name|m_len
condition|)
block|{
name|cp
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
name|u_char
operator|*
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|&
literal|1
condition|)
block|{
name|i
operator|=
name|m
operator|->
name|m_len
operator|-
literal|1
expr_stmt|;
operator|--
name|len
expr_stmt|;
name|sum
operator|+=
operator|*
name|cp
operator|++
expr_stmt|;
block|}
else|else
name|i
operator|=
name|m
operator|->
name|m_len
expr_stmt|;
name|sum
operator|=
name|oc_cksum
argument_list|(
name|cp
argument_list|,
name|i
argument_list|,
name|sum
argument_list|)
expr_stmt|;
name|m
operator|=
name|m
operator|->
name|m_next
expr_stmt|;
name|len
operator|-=
name|i
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|&
literal|1
condition|)
block|{
name|cp
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
name|u_char
operator|*
argument_list|)
expr_stmt|;
name|sum
operator|+=
operator|*
name|cp
operator|++
expr_stmt|;
return|return
operator|(
literal|0xffff
operator|&
operator|~
name|oc_cksum
argument_list|(
name|cp
argument_list|,
name|len
operator|-
literal|1
argument_list|,
name|sum
argument_list|)
operator|)
return|;
block|}
block|}
block|}
return|return
operator|(
literal|0xffff
operator|&
operator|~
name|oc_cksum
argument_list|(
name|mtod
argument_list|(
name|m
argument_list|,
name|u_char
operator|*
argument_list|)
argument_list|,
name|len
argument_list|,
name|sum
argument_list|)
operator|)
return|;
block|}
end_block

end_unit

