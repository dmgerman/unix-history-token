begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|sccsid
index|[]
init|=
literal|"@(#)inet.c	4.1 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Temporarily, copy these routines from the kernel,  * as we need to know about subnets.  */
end_comment

begin_include
include|#
directive|include
file|"defs.h"
end_include

begin_comment
comment|/*  * Formulate an Internet address from network + host.  Used in  * building addresses stored in the ifnet structure.  */
end_comment

begin_function
name|struct
name|in_addr
name|if_makeaddr
parameter_list|(
name|net
parameter_list|,
name|host
parameter_list|)
name|int
name|net
decl_stmt|,
name|host
decl_stmt|;
block|{
name|u_long
name|addr
decl_stmt|;
if|if
condition|(
name|net
operator|<
name|IN_CLASSA_MAX
condition|)
name|addr
operator|=
operator|(
name|net
operator|<<
name|IN_CLASSA_NSHIFT
operator|)
operator||
name|host
expr_stmt|;
elseif|else
if|if
condition|(
name|net
operator|<
name|IN_CLASSB_MAX
condition|)
name|addr
operator|=
operator|(
name|net
operator|<<
name|IN_CLASSB_NSHIFT
operator|)
operator||
name|host
expr_stmt|;
else|else
name|addr
operator|=
operator|(
name|net
operator|<<
name|IN_CLASSC_NSHIFT
operator|)
operator||
name|host
expr_stmt|;
name|addr
operator|=
name|htonl
argument_list|(
name|addr
argument_list|)
expr_stmt|;
return|return
operator|(
operator|*
operator|(
expr|struct
name|in_addr
operator|*
operator|)
operator|&
name|addr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Return the network number from an internet address.  */
end_comment

begin_macro
name|inet_netof
argument_list|(
argument|in
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|in_addr
name|in
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|u_long
name|i
init|=
name|ntohl
argument_list|(
name|in
operator|.
name|s_addr
argument_list|)
decl_stmt|;
specifier|register
name|net
expr_stmt|;
if|if
condition|(
name|IN_CLASSA
argument_list|(
name|i
argument_list|)
condition|)
block|{
name|net
operator|=
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSA_NET
operator|)
operator|>>
name|IN_CLASSA_NSHIFT
expr_stmt|;
if|if
condition|(
name|IN_SUBNETA
argument_list|(
name|i
argument_list|)
operator|&&
name|inet_localnet
argument_list|(
name|net
argument_list|)
condition|)
return|return
operator|(
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSA_SUBNET
operator|)
operator|>>
name|IN_CLASSA_SUBNSHIFT
operator|)
return|;
else|else
return|return
operator|(
name|net
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|IN_CLASSB
argument_list|(
name|i
argument_list|)
condition|)
block|{
name|net
operator|=
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSB_NET
operator|)
operator|>>
name|IN_CLASSB_NSHIFT
expr_stmt|;
if|if
condition|(
name|IN_SUBNETB
argument_list|(
name|i
argument_list|)
operator|&&
name|inet_localnet
argument_list|(
name|net
argument_list|)
condition|)
return|return
operator|(
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSB_SUBNET
operator|)
operator|>>
name|IN_CLASSB_SUBNSHIFT
operator|)
return|;
else|else
return|return
operator|(
name|net
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSC_NET
operator|)
operator|>>
name|IN_CLASSC_NSHIFT
operator|)
return|;
block|}
block|}
end_block

begin_comment
comment|/*  * Return the host portion of an internet address.  */
end_comment

begin_macro
name|inet_lnaof
argument_list|(
argument|in
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|in_addr
name|in
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|u_long
name|i
init|=
name|ntohl
argument_list|(
name|in
operator|.
name|s_addr
argument_list|)
decl_stmt|;
if|if
condition|(
name|IN_CLASSA
argument_list|(
name|i
argument_list|)
condition|)
block|{
if|if
condition|(
name|IN_SUBNETA
argument_list|(
name|i
argument_list|)
operator|&&
name|inet_localnet
argument_list|(
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSA_NET
operator|)
operator|>>
name|IN_CLASSA_NSHIFT
argument_list|)
condition|)
return|return
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSA_SUBHOST
operator|)
return|;
else|else
return|return
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSA_HOST
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|IN_CLASSB
argument_list|(
name|i
argument_list|)
condition|)
block|{
if|if
condition|(
name|IN_SUBNETB
argument_list|(
name|i
argument_list|)
operator|&&
name|inet_localnet
argument_list|(
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSB_NET
operator|)
operator|>>
name|IN_CLASSB_NSHIFT
argument_list|)
condition|)
return|return
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSB_SUBHOST
operator|)
return|;
else|else
return|return
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSB_HOST
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
operator|(
name|i
operator|)
operator|&
name|IN_CLASSC_HOST
operator|)
return|;
block|}
block|}
end_block

begin_comment
comment|/*  * Return true if the network is a ``local'' net  * (one for which we can interpret the host part).  */
end_comment

begin_expr_stmt
name|inet_localnet
argument_list|(
name|net
argument_list|)
specifier|register
name|int
name|net
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|interface
modifier|*
name|ifp
decl_stmt|;
specifier|extern
name|struct
name|interface
modifier|*
name|ifnet
decl_stmt|;
for|for
control|(
name|ifp
operator|=
name|ifnet
init|;
name|ifp
condition|;
name|ifp
operator|=
name|ifp
operator|->
name|int_next
control|)
block|{
if|if
condition|(
name|ifp
operator|->
name|int_flags
operator|&
name|IFF_LOCAL
condition|)
block|{
if|if
condition|(
name|ifp
operator|->
name|int_net
operator|==
name|net
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
operator|(
name|ifp
operator|->
name|int_net
operator|>>
name|SUBNETSHIFT
operator|)
operator|==
name|net
condition|)
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

