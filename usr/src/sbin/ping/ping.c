begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  *			P I N G . C  *  * Using the InterNet Control Message Protocol (ICMP) "ECHO" facility,  * measure round-trip-delays and packet loss across network paths.  *  * Author -  *	Mike Muuss  *	U. S. Army Ballistic Research Laboratory  *	December, 1983  *  * Target System -  *	4.2 BSD with MIT and BRL fixes to /sys/netinet/ip_icmp.c et.al.  *  * Status -  *	Public Domain.  Distribution Unlimited.  *  * Bugs -  *	Divide by zero if no packets return.  *	More statistics could always be gathered.  *	This program has to run SUID to ROOT to access the ICMP socket.  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in_systm.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<netinet/ip.h>
end_include

begin_include
include|#
directive|include
file|<netinet/ip_icmp.h>
end_include

begin_include
include|#
directive|include
file|<netdb.h>
end_include

begin_decl_stmt
name|char
name|ttyobuf
index|[
literal|4096
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|u_char
name|packet
index|[
literal|1024
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|options
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|int
name|errno
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|s
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Socket file descriptor */
end_comment

begin_decl_stmt
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Pointer to host info */
end_comment

begin_decl_stmt
name|struct
name|timezone
name|tz
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* leftover */
end_comment

begin_decl_stmt
name|struct
name|sockaddr
name|whereto
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Who to ping */
end_comment

begin_decl_stmt
name|int
name|datalen
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* How much data */
end_comment

begin_decl_stmt
name|char
name|usage
index|[]
init|=
literal|"Usage:  ping [-d] host [data size]\n"
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|hostname
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|hnamebuf
index|[
literal|64
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|ntransmitted
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* sequence # for outbound packets = #sent */
end_comment

begin_decl_stmt
name|int
name|ident
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|nreceived
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* # of packets we got back */
end_comment

begin_decl_stmt
name|int
name|tmin
init|=
literal|999999999
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|tmax
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|tsum
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* sum of all times, for doing average */
end_comment

begin_function_decl
name|int
name|finish
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * 			M A I N  */
end_comment

begin_function
name|main
parameter_list|(
name|argc
parameter_list|,
name|argv
parameter_list|)
name|char
modifier|*
name|argv
index|[]
decl_stmt|;
block|{
name|struct
name|sockaddr_in
name|from
decl_stmt|;
name|char
modifier|*
modifier|*
name|av
init|=
name|argv
decl_stmt|;
name|struct
name|sockaddr_in
modifier|*
name|to
init|=
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
operator|&
name|whereto
decl_stmt|;
if|if
condition|(
name|argc
operator|>
literal|0
operator|&&
operator|!
name|strcmp
argument_list|(
name|argv
index|[
literal|0
index|]
argument_list|,
literal|"-d"
argument_list|)
condition|)
block|{
name|options
operator||=
name|SO_DEBUG
expr_stmt|;
name|argc
operator|--
operator|,
name|av
operator|++
expr_stmt|;
block|}
if|if
condition|(
name|argc
operator|<
literal|2
operator|||
name|argc
operator|>
literal|3
condition|)
block|{
name|printf
argument_list|(
name|usage
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|bzero
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|whereto
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr
argument_list|)
argument_list|)
expr_stmt|;
name|hp
operator|=
name|gethostbyname
argument_list|(
name|av
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
condition|)
block|{
name|to
operator|->
name|sin_family
operator|=
name|hp
operator|->
name|h_addrtype
expr_stmt|;
name|bcopy
argument_list|(
name|hp
operator|->
name|h_addr
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|to
operator|->
name|sin_addr
argument_list|,
name|hp
operator|->
name|h_length
argument_list|)
expr_stmt|;
name|hostname
operator|=
name|hp
operator|->
name|h_name
expr_stmt|;
block|}
else|else
block|{
name|to
operator|->
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|to
operator|->
name|sin_addr
operator|.
name|s_addr
operator|=
name|inet_addr
argument_list|(
name|av
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|to
operator|->
name|sin_addr
operator|.
name|s_addr
operator|==
operator|-
literal|1
condition|)
block|{
name|printf
argument_list|(
literal|"%s: unknown host %s\n"
argument_list|,
name|argv
index|[
literal|0
index|]
argument_list|,
name|av
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
return|return;
block|}
name|strcpy
argument_list|(
name|hnamebuf
argument_list|,
name|argv
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|hostname
operator|=
name|hnamebuf
expr_stmt|;
block|}
if|if
condition|(
name|argc
operator|==
literal|3
condition|)
name|datalen
operator|=
name|atoi
argument_list|(
name|argv
index|[
literal|2
index|]
argument_list|)
expr_stmt|;
else|else
name|datalen
operator|=
literal|64
operator|-
literal|8
expr_stmt|;
name|ident
operator|=
name|getpid
argument_list|()
operator|&
literal|0xFFFF
expr_stmt|;
while|while
condition|(
operator|(
name|s
operator|=
name|socket
argument_list|(
name|AF_INET
argument_list|,
name|SOCK_RAW
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
operator|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"ping: socket"
argument_list|)
expr_stmt|;
name|sleep
argument_list|(
literal|5
argument_list|)
expr_stmt|;
block|}
name|printf
argument_list|(
literal|"PING %s: %d data bytes\n"
argument_list|,
name|hostname
argument_list|,
name|datalen
argument_list|)
expr_stmt|;
name|setbuffer
argument_list|(
name|stdout
argument_list|,
name|ttyobuf
argument_list|,
sizeof|sizeof
argument_list|(
name|ttyobuf
argument_list|)
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGINT
argument_list|,
name|finish
argument_list|)
expr_stmt|;
name|catcher
argument_list|()
expr_stmt|;
comment|/* start things going */
for|for
control|(
init|;
condition|;
control|)
block|{
name|int
name|len
init|=
sizeof|sizeof
argument_list|(
name|packet
argument_list|)
decl_stmt|;
name|int
name|fromlen
init|=
sizeof|sizeof
argument_list|(
name|from
argument_list|)
decl_stmt|;
name|int
name|cc
decl_stmt|;
comment|/* cc = recvfrom(s, buf, len, flags, from, fromlen) */
if|if
condition|(
operator|(
name|cc
operator|=
name|recvfrom
argument_list|(
name|s
argument_list|,
name|packet
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
operator|&
name|from
argument_list|,
name|fromlen
argument_list|)
operator|)
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EINTR
condition|)
continue|continue;
name|perror
argument_list|(
literal|"ping: recvfrom"
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|pr_pack
argument_list|(
name|packet
argument_list|,
name|cc
argument_list|,
operator|&
name|from
argument_list|)
expr_stmt|;
block|}
comment|/*NOTREACHED*/
block|}
end_function

begin_comment
comment|/*  * 			C A T C H E R  *   * This routine causes another PING to be transmitted, and then  * schedules another SIGALRM for 1 second from now.  *   * Bug -  * 	Our sense of time will slowly skew (ie, packets will not be launched  * 	exactly at 1-second intervals).  This does not affect the quality  *	of the delay and loss statistics.  */
end_comment

begin_macro
name|catcher
argument_list|()
end_macro

begin_block
block|{
name|signal
argument_list|(
name|SIGALRM
argument_list|,
name|catcher
argument_list|)
expr_stmt|;
name|pinger
argument_list|()
expr_stmt|;
name|alarm
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * 			P I N G E R  *   * Compose and transmit an ICMP ECHO REQUEST packet.  The IP packet  * will be added on by the kernel.  The ID field is our UNIX process ID,  * and the sequence number is an ascending integer.  The first 8 bytes  * of the data portion are used to hold a UNIX "timeval" struct in VAX  * byte-order, to compute the round-trip time.  */
end_comment

begin_macro
name|pinger
argument_list|()
end_macro

begin_block
block|{
specifier|static
name|u_char
name|outpack
index|[
literal|1024
index|]
decl_stmt|;
specifier|register
name|struct
name|icmp
modifier|*
name|icp
init|=
operator|(
expr|struct
name|icmp
operator|*
operator|)
name|outpack
decl_stmt|;
name|int
name|i
decl_stmt|,
name|cc
decl_stmt|;
specifier|register
name|struct
name|timeval
modifier|*
name|tp
init|=
operator|(
expr|struct
name|timeval
operator|*
operator|)
operator|&
name|outpack
index|[
literal|8
index|]
decl_stmt|;
specifier|register
name|u_char
modifier|*
name|datap
init|=
operator|&
name|outpack
index|[
literal|8
operator|+
sizeof|sizeof
argument_list|(
expr|struct
name|timeval
argument_list|)
index|]
decl_stmt|;
name|icp
operator|->
name|icmp_type
operator|=
name|ICMP_ECHO
expr_stmt|;
name|icp
operator|->
name|icmp_code
operator|=
literal|0
expr_stmt|;
name|icp
operator|->
name|icmp_cksum
operator|=
literal|0
expr_stmt|;
name|icp
operator|->
name|icmp_seq
operator|=
name|ntransmitted
operator|++
expr_stmt|;
name|icp
operator|->
name|icmp_id
operator|=
name|ident
expr_stmt|;
comment|/* ID */
name|cc
operator|=
name|datalen
operator|+
literal|8
expr_stmt|;
comment|/* skips ICMP portion */
name|gettimeofday
argument_list|(
name|tp
argument_list|,
operator|&
name|tz
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|8
init|;
name|i
operator|<
name|datalen
condition|;
name|i
operator|++
control|)
comment|/* skip 8 for time */
operator|*
name|datap
operator|++
operator|=
name|i
expr_stmt|;
comment|/* Compute ICMP checksum here */
name|icp
operator|->
name|icmp_cksum
operator|=
name|in_cksum
argument_list|(
name|icp
argument_list|,
name|cc
argument_list|)
expr_stmt|;
comment|/* cc = sendto(s, msg, len, flags, to, tolen) */
name|i
operator|=
name|sendto
argument_list|(
name|s
argument_list|,
name|outpack
argument_list|,
name|cc
argument_list|,
literal|0
argument_list|,
operator|&
name|whereto
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|<
literal|0
operator|||
name|i
operator|!=
name|cc
condition|)
block|{
if|if
condition|(
name|i
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"sendto"
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"ping: wrote %s %d chars, ret=%d\n"
argument_list|,
name|hostname
argument_list|,
name|cc
argument_list|,
name|i
argument_list|)
expr_stmt|;
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * 			P R _ T Y P E  *  * Convert an ICMP "type" field to a printable string.  */
end_comment

begin_function
name|char
modifier|*
name|pr_type
parameter_list|(
name|t
parameter_list|)
specifier|register
name|int
name|t
decl_stmt|;
block|{
specifier|static
name|char
modifier|*
name|ttab
index|[]
init|=
block|{
literal|"Echo Reply"
block|,
literal|"ICMP 1"
block|,
literal|"ICMP 2"
block|,
literal|"Dest Unreachable"
block|,
literal|"Source Quence"
block|,
literal|"Redirect"
block|,
literal|"ICMP 6"
block|,
literal|"ICMP 7"
block|,
literal|"Echo"
block|,
literal|"ICMP 9"
block|,
literal|"ICMP 10"
block|,
literal|"Time Exceeded"
block|,
literal|"Parameter Problem"
block|,
literal|"Timestamp"
block|,
literal|"Timestamp Reply"
block|,
literal|"Info Request"
block|,
literal|"Info Reply"
block|}
decl_stmt|;
if|if
condition|(
name|t
operator|<
literal|0
operator|||
name|t
operator|>
literal|16
condition|)
return|return
operator|(
literal|"OUT-OF-RANGE"
operator|)
return|;
return|return
operator|(
name|ttab
index|[
name|t
index|]
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *			P R _ P A C K  *  * Print out the packet, if it came from us.  This logic is necessary  * because ALL readers of the ICMP socket get a copy of ALL ICMP packets  * which arrive ('tis only fair).  This permits multiple copies of this  * program to be run without having intermingled output (or statistics!).  */
end_comment

begin_expr_stmt
name|pr_pack
argument_list|(
name|icp
argument_list|,
name|cc
argument_list|,
name|from
argument_list|)
specifier|register
expr|struct
name|icmp
operator|*
name|icp
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|cc
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr_in
modifier|*
name|from
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|long
modifier|*
name|lp
init|=
operator|(
name|long
operator|*
operator|)
name|packet
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
name|struct
name|timeval
name|tv
decl_stmt|;
name|struct
name|timeval
modifier|*
name|tp
init|=
operator|(
expr|struct
name|timeval
operator|*
operator|)
operator|&
name|packet
index|[
literal|8
index|]
decl_stmt|;
name|int
name|triptime
decl_stmt|;
name|from
operator|->
name|sin_addr
operator|.
name|s_addr
operator|=
name|ntohl
argument_list|(
name|from
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
expr_stmt|;
name|gettimeofday
argument_list|(
operator|&
name|tv
argument_list|,
operator|&
name|tz
argument_list|)
expr_stmt|;
if|if
condition|(
name|icp
operator|->
name|icmp_type
operator|!=
name|ICMP_ECHOREPLY
condition|)
block|{
name|printf
argument_list|(
literal|"%d bytes from x%x: "
argument_list|,
name|cc
argument_list|,
name|from
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"icmp_type=%d (%s)\n"
argument_list|,
name|icp
operator|->
name|icmp_type
argument_list|,
name|pr_type
argument_list|(
name|icp
operator|->
name|icmp_type
argument_list|)
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
literal|12
condition|;
name|i
operator|++
control|)
name|printf
argument_list|(
literal|"x%2.2x: x%8.8x\n"
argument_list|,
name|i
operator|*
sizeof|sizeof
argument_list|(
name|long
argument_list|)
argument_list|,
operator|*
name|lp
operator|++
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"icmp_code=%d\n"
argument_list|,
name|icp
operator|->
name|icmp_code
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|icp
operator|->
name|icmp_id
operator|!=
name|ident
condition|)
return|return;
comment|/* 'Twas not our ECHO */
name|printf
argument_list|(
literal|"%d bytes from x%x: "
argument_list|,
name|cc
argument_list|,
name|from
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"icmp_seq=%d. "
argument_list|,
name|icp
operator|->
name|icmp_seq
argument_list|)
expr_stmt|;
name|tvsub
argument_list|(
operator|&
name|tv
argument_list|,
name|tp
argument_list|)
expr_stmt|;
name|triptime
operator|=
name|tv
operator|.
name|tv_sec
operator|*
literal|1000
operator|+
operator|(
name|tv
operator|.
name|tv_usec
operator|/
literal|1000
operator|)
expr_stmt|;
name|printf
argument_list|(
literal|"time=%d. ms\n"
argument_list|,
name|triptime
argument_list|)
expr_stmt|;
name|nreceived
operator|++
expr_stmt|;
name|tsum
operator|+=
name|triptime
expr_stmt|;
if|if
condition|(
name|triptime
operator|<
name|tmin
condition|)
name|tmin
operator|=
name|triptime
expr_stmt|;
if|if
condition|(
name|triptime
operator|>
name|tmax
condition|)
name|tmax
operator|=
name|triptime
expr_stmt|;
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  *			I N _ C K S U M  *  * Checksum routine for Internet Protocol family headers (VAX Version).  *  * Shamelessly pilfered from /sys/vax/in_cksum.c, with all the MBUF stuff  * ripped out.  */
end_comment

begin_macro
name|in_cksum
argument_list|(
argument|addr
argument_list|,
argument|len
argument_list|)
end_macro

begin_decl_stmt
name|u_short
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|len
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|nleft
init|=
name|len
decl_stmt|;
comment|/* on vax, (user mode), r11 */
specifier|register
name|int
name|xxx
decl_stmt|;
comment|/* on vax, (user mode), r10 */
specifier|register
name|u_short
modifier|*
name|w
init|=
name|addr
decl_stmt|;
comment|/* on vax, known to be r9 */
specifier|register
name|int
name|sum
init|=
literal|0
decl_stmt|;
comment|/* on vax, known to be r8 */
comment|/* 	 * Force to long boundary so we do longword aligned 	 * memory operations.  It is too hard to do byte 	 * adjustment, do only word adjustment. 	 */
if|if
condition|(
operator|(
operator|(
name|int
operator|)
name|w
operator|&
literal|0x2
operator|)
operator|&&
name|nleft
operator|>=
literal|2
condition|)
block|{
name|sum
operator|+=
operator|*
name|w
operator|++
expr_stmt|;
name|nleft
operator|-=
literal|2
expr_stmt|;
block|}
comment|/* 	 * Do as much of the checksum as possible 32 bits at at time. 	 * In fact, this loop is unrolled to make overhead from 	 * branches&c small. 	 * 	 * We can do a 16 bit ones complement sum 32 bits at a time 	 * because the 32 bit register is acting as two 16 bit 	 * registers for adding, with carries from the low added 	 * into the high (by normal carry-chaining) and carries 	 * from the high carried into the low on the next word 	 * by use of the adwc instruction.  This lets us run 	 * this loop at almost memory speed. 	 * 	 * Here there is the danger of high order carry out, and 	 * we carefully use adwc. 	 */
while|while
condition|(
operator|(
name|nleft
operator|-=
literal|32
operator|)
operator|>=
literal|0
condition|)
block|{
undef|#
directive|undef
name|ADD
asm|asm("clrl r0");
comment|/* clears carry */
define|#
directive|define
name|ADD
value|asm("adwc (r9)+,r8;");
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
asm|asm("adwc $0,r8");
block|}
name|nleft
operator|+=
literal|32
expr_stmt|;
while|while
condition|(
operator|(
name|nleft
operator|-=
literal|8
operator|)
operator|>=
literal|0
condition|)
block|{
asm|asm("clrl r0");
name|ADD
expr_stmt|;
name|ADD
expr_stmt|;
asm|asm("adwc $0,r8");
block|}
name|nleft
operator|+=
literal|8
expr_stmt|;
comment|/* 	 * Now eliminate the possibility of carry-out's by 	 * folding back to a 16 bit number (adding high and 	 * low parts together.)  Then mop up trailing words 	 * and maybe an odd byte. 	 */
block|{
asm|asm("ashl $-16,r8,r0; addw2 r0,r8");
asm|asm("adwc $0,r8; movzwl r8,r8");
block|}
while|while
condition|(
operator|(
name|nleft
operator|-=
literal|2
operator|)
operator|>=
literal|0
condition|)
block|{
asm|asm("movzwl (r9)+,r0; addl2 r0,r8");
block|}
if|if
condition|(
name|nleft
operator|==
operator|-
literal|1
condition|)
block|{
name|sum
operator|+=
operator|*
operator|(
name|u_char
operator|*
operator|)
name|w
expr_stmt|;
block|}
comment|/* 	 * Add together high and low parts of sum 	 * and carry to get cksum. 	 * Have to be careful to not drop the last 	 * carry here. 	 */
block|{
asm|asm("ashl $-16,r8,r0; addw2 r0,r8; adwc $0,r8");
asm|asm("mcoml r8,r8; movzwl r8,r8");
block|}
return|return
operator|(
name|sum
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * 			T V S U B  *   * Subtract 2 timeval structs:  out = out - in.  *   * Out is assumed to be>= in.  */
end_comment

begin_expr_stmt
name|tvsub
argument_list|(
name|out
argument_list|,
name|in
argument_list|)
specifier|register
expr|struct
name|timeval
operator|*
name|out
operator|,
operator|*
name|in
expr_stmt|;
end_expr_stmt

begin_block
block|{
if|if
condition|(
operator|(
name|out
operator|->
name|tv_usec
operator|-=
name|in
operator|->
name|tv_usec
operator|)
operator|<
literal|0
condition|)
block|{
name|out
operator|->
name|tv_sec
operator|--
expr_stmt|;
name|out
operator|->
name|tv_usec
operator|+=
literal|1000000
expr_stmt|;
block|}
name|out
operator|->
name|tv_sec
operator|-=
name|in
operator|->
name|tv_sec
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  *			F I N I S H  *  * Print out statistics, and give up.  * Heavily buffered STDIO is used here, so that all the statistics  * will be written with 1 sys-write call.  This is nice when more  * than one copy of the program is running on a terminal;  it prevents  * the statistics output from becomming intermingled.  */
end_comment

begin_macro
name|finish
argument_list|()
end_macro

begin_block
block|{
name|ntransmitted
operator|--
expr_stmt|;
comment|/* we will never hear the last one */
name|printf
argument_list|(
literal|"\n----%s PING Statistics----\n"
argument_list|,
name|hostname
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%d packets transmitted, "
argument_list|,
name|ntransmitted
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%d packets received, "
argument_list|,
name|nreceived
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%d%% packet loss\n"
argument_list|,
call|(
name|int
call|)
argument_list|(
operator|(
operator|(
name|ntransmitted
operator|-
name|nreceived
operator|)
operator|*
literal|100
operator|)
operator|/
name|ntransmitted
argument_list|)
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"round-trip (ms)  min/avg/max = %d/%d/%d\n"
argument_list|,
name|tmin
argument_list|,
name|tsum
operator|/
name|nreceived
argument_list|,
name|tmax
argument_list|)
expr_stmt|;
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

