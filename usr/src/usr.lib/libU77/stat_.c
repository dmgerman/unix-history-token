begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1980 Regents of the University of California.  * All rights reserved.  The Berkeley software License Agreement  * specifies the terms and conditions for redistribution.  *  *	@(#)stat_.c	5.1	6/7/85  */
end_comment

begin_comment
comment|/*  * get file status  *  * calling sequence:  *	integer stat, statb(12)  *	call stat (name, statb)  * where:  *	'statb' will receive the stat structure for file 'name'.  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|MAXPATHLEN
end_ifndef

begin_define
define|#
directive|define
name|MAXPATHLEN
value|128
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|"../libI77/f_errno.h"
end_include

begin_function
name|long
name|stat_
parameter_list|(
name|name
parameter_list|,
name|stbuf
parameter_list|,
name|namlen
parameter_list|)
name|char
modifier|*
name|name
decl_stmt|;
name|long
modifier|*
name|stbuf
decl_stmt|,
name|namlen
decl_stmt|;
block|{
name|char
name|buf
index|[
name|MAXPATHLEN
index|]
decl_stmt|;
name|struct
name|stat
name|statb
decl_stmt|;
if|if
condition|(
name|namlen
operator|>=
sizeof|sizeof
name|buf
condition|)
return|return
operator|(
call|(
name|long
call|)
argument_list|(
name|errno
operator|=
name|F_ERARG
argument_list|)
operator|)
return|;
name|g_char
argument_list|(
name|name
argument_list|,
name|namlen
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|buf
argument_list|,
operator|&
name|statb
argument_list|)
operator|==
literal|0
condition|)
block|{
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_dev
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_ino
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_mode
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_nlink
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_uid
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_gid
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_rdev
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_size
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_atime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_mtime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_ctime
expr_stmt|;
operator|*
name|stbuf
operator|++
operator|=
name|statb
operator|.
name|st_blksize
expr_stmt|;
return|return
operator|(
literal|0L
operator|)
return|;
block|}
return|return
operator|(
operator|(
name|long
operator|)
name|errno
operator|)
return|;
block|}
end_function

end_unit

