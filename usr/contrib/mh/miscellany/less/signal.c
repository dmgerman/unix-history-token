begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Routines dealing with signals.  *  * A signal usually merely causes a bit to be set in the "signals" word.  * At some convenient time, the mainline code checks to see if any  * signals need processing by calling psignal().  * An exception is made if we are reading from the keyboard when the  * signal is received.  Some operating systems will simply call the  * signal handler and NOT return from the read (with EINTR).  * To handle this case, we service the interrupt directly from  * the handler if we are reading from the keyboard.  */
end_comment

begin_include
include|#
directive|include
file|"less.h"
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_include
include|#
directive|include
file|<setjmp.h>
end_include

begin_comment
comment|/*  * The type of signal handler functions.  * Usually int, although it should be void.  */
end_comment

begin_typedef
typedef|typedef
name|int
name|HANDLER
typedef|;
end_typedef

begin_comment
comment|/*  * "sigs" contains bits indicating signals which need to be processed.  */
end_comment

begin_decl_stmt
name|public
name|int
name|sigs
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|S_INTERRUPT
value|01
end_define

begin_ifdef
ifdef|#
directive|ifdef
name|SIGTSTP
end_ifdef

begin_define
define|#
directive|define
name|S_STOP
value|02
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|NRTC
end_ifdef

begin_define
define|#
directive|define
name|S_QUIT
value|04
end_define

begin_endif
endif|#
directive|endif
endif|NRTC
end_endif

begin_decl_stmt
specifier|extern
name|int
name|reading
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|char
modifier|*
name|first_cmd
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|jmp_buf
name|main_loop
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Interrupt signal handler.  */
end_comment

begin_function
specifier|static
name|HANDLER
name|interrupt
parameter_list|()
block|{
name|SIGNAL
argument_list|(
name|SIGINT
argument_list|,
name|interrupt
argument_list|)
expr_stmt|;
name|sigs
operator||=
name|S_INTERRUPT
expr_stmt|;
if|if
condition|(
name|reading
condition|)
name|psignals
argument_list|()
expr_stmt|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|NRTC
end_ifdef

begin_comment
comment|/*  * Interrupt signal handler.  */
end_comment

begin_function
specifier|static
name|HANDLER
name|quitnow
parameter_list|()
block|{
name|SIGNAL
argument_list|(
name|SIGQUIT
argument_list|,
name|quitnow
argument_list|)
expr_stmt|;
name|sigs
operator||=
name|S_QUIT
expr_stmt|;
if|if
condition|(
name|reading
condition|)
name|psignals
argument_list|()
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
endif|NRTC
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|SIGTSTP
end_ifdef

begin_comment
comment|/*  * "Stop" (^Z) signal handler.  */
end_comment

begin_function
specifier|static
name|HANDLER
name|stop
parameter_list|()
block|{
name|SIGNAL
argument_list|(
name|SIGTSTP
argument_list|,
name|stop
argument_list|)
expr_stmt|;
name|sigs
operator||=
name|S_STOP
expr_stmt|;
if|if
condition|(
name|reading
condition|)
name|psignals
argument_list|()
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Set up the signal handlers.  */
end_comment

begin_function
name|public
name|void
name|init_signals
parameter_list|()
block|{
operator|(
name|void
operator|)
name|SIGNAL
argument_list|(
name|SIGINT
argument_list|,
name|interrupt
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|NRTC
operator|(
name|void
operator|)
name|SIGNAL
argument_list|(
name|SIGQUIT
argument_list|,
name|quitnow
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|NRTC
ifdef|#
directive|ifdef
name|SIGTSTP
operator|(
name|void
operator|)
name|SIGNAL
argument_list|(
name|SIGTSTP
argument_list|,
name|stop
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_comment
comment|/*  * Process any signals we have recieved.  * A received signal cause a bit to be set in "sigs".  */
end_comment

begin_function
name|public
name|void
name|psignals
parameter_list|()
block|{
specifier|register
name|int
name|tsignals
decl_stmt|;
name|tsignals
operator|=
name|sigs
expr_stmt|;
name|sigs
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|tsignals
operator|==
literal|0
condition|)
return|return;
name|dropout
argument_list|()
expr_stmt|;
comment|/* Discard any buffered output */
ifdef|#
directive|ifdef
name|SIGTSTP
if|if
condition|(
name|tsignals
operator|&
name|S_STOP
condition|)
block|{
comment|/* 		 * Clean up the terminal. 		 */
ifdef|#
directive|ifdef
name|SIGTTOU
name|SIGNAL
argument_list|(
name|SIGTTOU
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|lower_left
argument_list|()
expr_stmt|;
name|clear_eol
argument_list|()
expr_stmt|;
name|flush
argument_list|()
expr_stmt|;
name|raw_mode
argument_list|(
literal|0
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SIGTTOU
name|SIGNAL
argument_list|(
name|SIGTTOU
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|SIGNAL
argument_list|(
name|SIGTSTP
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
if|#
directive|if
name|SIGSETMASK
comment|/* 		 * This system will not allow us to send a  		 * stop signal (SIGTSTP) to ourself 		 * while we are in the signal handler, like maybe now. 		 * (This can be the case if we are reading; see comment above.) 		 * So we ask the silly system for permission to do so. 		 */
name|sigsetmask
argument_list|(
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|kill
argument_list|(
name|getpid
argument_list|()
argument_list|,
name|SIGTSTP
argument_list|)
expr_stmt|;
comment|/* 		 * ... Bye bye. ... 		 * Hopefully we'll be back later and resume here... 		 * Reset the terminal and arrange to repaint the 		 * screen when we get back to the main command loop. 		 */
name|SIGNAL
argument_list|(
name|SIGTSTP
argument_list|,
name|stop
argument_list|)
expr_stmt|;
name|raw_mode
argument_list|(
literal|1
argument_list|)
expr_stmt|;
name|first_cmd
operator|=
literal|"r"
expr_stmt|;
name|longjmp
argument_list|(
name|main_loop
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|NRTC
if|if
condition|(
name|tsignals
operator|&
name|S_QUIT
condition|)
name|quit
argument_list|()
expr_stmt|;
endif|#
directive|endif
endif|NRTC
if|if
condition|(
name|tsignals
operator|&
name|S_INTERRUPT
condition|)
block|{
name|bell
argument_list|()
expr_stmt|;
comment|/* 		 * {{ You may wish to replace the bell() with  		 *    error("Interrupt"); }} 		 */
block|}
name|longjmp
argument_list|(
name|main_loop
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Pass the specified command to a shell to be executed.  * Like plain "system()", but handles resetting terminal modes, etc.  */
end_comment

begin_function
name|public
name|void
name|lsystem
parameter_list|(
name|cmd
parameter_list|)
name|char
modifier|*
name|cmd
decl_stmt|;
block|{
name|int
name|inp
decl_stmt|;
comment|/* 	 * Print the command which is to be executed. 	 */
name|lower_left
argument_list|()
expr_stmt|;
name|clear_eol
argument_list|()
expr_stmt|;
name|puts
argument_list|(
literal|"!"
argument_list|)
expr_stmt|;
name|puts
argument_list|(
name|cmd
argument_list|)
expr_stmt|;
name|puts
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
comment|/* 	 * De-initialize the terminal and take out of raw mode. 	 */
name|deinit
argument_list|()
expr_stmt|;
name|flush
argument_list|()
expr_stmt|;
name|raw_mode
argument_list|(
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Restore signals to their defaults. 	 */
name|SIGNAL
argument_list|(
name|SIGINT
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SIGTSTP
name|SIGNAL
argument_list|(
name|SIGTSTP
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Pass the command to the system to be executed. 	 */
name|inp
operator|=
name|dup
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|close
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|system
argument_list|(
name|cmd
argument_list|)
expr_stmt|;
name|close
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|dup
argument_list|(
name|inp
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|inp
argument_list|)
expr_stmt|;
comment|/* 	 * Reset signals, raw mode, etc. 	 */
name|init_signals
argument_list|()
expr_stmt|;
name|raw_mode
argument_list|(
literal|1
argument_list|)
expr_stmt|;
name|init
argument_list|()
expr_stmt|;
block|}
end_function

end_unit

