begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_comment
comment|/*  * pull(x) - pull an element from end of list x.  */
end_comment

begin_macro
name|Xpull
argument_list|(
argument|nargs
argument_list|,
argument|arg1
argument_list|,
argument|arg0
argument_list|)
end_macro

begin_decl_stmt
name|int
name|nargs
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|descrip
name|arg1
decl_stmt|,
name|arg0
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|struct
name|b_list
modifier|*
name|hp
decl_stmt|;
specifier|register
name|struct
name|b_lelem
modifier|*
name|bp
decl_stmt|;
specifier|extern
name|struct
name|b_lelem
modifier|*
name|alclstb
parameter_list|()
function_decl|;
comment|/*     * x must be a list.     */
name|DeRef
argument_list|(
argument|arg1
argument_list|)
if|if
condition|(
name|QUAL
argument_list|(
name|arg1
argument_list|)
operator|||
name|TYPE
argument_list|(
name|arg1
argument_list|)
operator|!=
name|T_LIST
condition|)
name|runerr
argument_list|(
literal|108
argument_list|,
operator|&
name|arg1
argument_list|)
expr_stmt|;
comment|/*     * Point at list header block and fail if the list is empty.     */
name|hp
operator|=
operator|(
expr|struct
name|b_list
operator|*
operator|)
name|BLKLOC
argument_list|(
name|arg1
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|->
name|cursize
operator|<=
literal|0
condition|)
name|fail
argument_list|()
expr_stmt|;
comment|/*     * Point bp at the last list element block.  If the last block has no     *  elements in use, point bp at the previous list element block.     */
name|bp
operator|=
operator|(
expr|struct
name|b_lelem
operator|*
operator|)
name|BLKLOC
argument_list|(
name|hp
operator|->
name|listtail
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|nused
operator|<=
literal|0
condition|)
block|{
name|bp
operator|=
operator|(
expr|struct
name|b_lelem
operator|*
operator|)
name|BLKLOC
argument_list|(
name|bp
operator|->
name|listprev
argument_list|)
expr_stmt|;
name|BLKLOC
argument_list|(
name|hp
operator|->
name|listtail
argument_list|)
operator|=
operator|(
expr|union
name|block
operator|*
operator|)
name|bp
expr_stmt|;
name|bp
operator|->
name|listnext
operator|=
name|nulldesc
expr_stmt|;
block|}
comment|/*     * Set i to position of last element and assign the element to     *  arg0 for return.  Decrement the usage count for the block     *  and the size of the list.     */
name|i
operator|=
name|bp
operator|->
name|first
operator|+
name|bp
operator|->
name|nused
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|i
operator|>=
name|bp
operator|->
name|nelem
condition|)
name|i
operator|-=
name|bp
operator|->
name|nelem
expr_stmt|;
name|arg0
operator|=
name|bp
operator|->
name|lslots
index|[
name|i
index|]
expr_stmt|;
name|bp
operator|->
name|nused
operator|--
expr_stmt|;
name|hp
operator|->
name|cursize
operator|--
expr_stmt|;
block|}
end_block

begin_macro
name|Procblock
argument_list|(
argument|pull
argument_list|,
literal|1
argument_list|)
end_macro

end_unit

