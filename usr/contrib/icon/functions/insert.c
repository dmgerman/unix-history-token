begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|SETS
end_ifdef

begin_comment
comment|/*  * insert(S,x) - insert element x into set S if not already there  *  (always succeeds and returns S).  */
end_comment

begin_macro
name|Xinsert
argument_list|(
argument|nargs
argument_list|,
argument|arg2
argument_list|,
argument|arg1
argument_list|,
argument|arg0
argument_list|)
end_macro

begin_decl_stmt
name|int
name|nargs
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|descrip
name|arg0
decl_stmt|,
name|arg1
decl_stmt|,
name|arg2
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|descrip
modifier|*
name|pd
decl_stmt|;
specifier|register
name|int
name|hn
decl_stmt|;
name|int
name|res
decl_stmt|;
specifier|extern
name|struct
name|b_selem
modifier|*
name|alcselem
parameter_list|()
function_decl|;
specifier|extern
name|struct
name|descrip
modifier|*
name|memb
parameter_list|()
function_decl|;
name|DeRef
argument_list|(
argument|arg1
argument_list|)
name|DeRef
argument_list|(
argument|arg2
argument_list|)
name|arg0
operator|=
name|arg1
expr_stmt|;
if|if
condition|(
name|QUAL
argument_list|(
name|arg1
argument_list|)
operator|||
name|TYPE
argument_list|(
name|arg1
argument_list|)
operator|!=
name|T_SET
condition|)
name|runerr
argument_list|(
literal|119
argument_list|,
operator|&
name|arg1
argument_list|)
expr_stmt|;
comment|/*       * We may need at most one new element.       */
name|hneed
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|b_selem
argument_list|)
argument_list|)
expr_stmt|;
name|hn
operator|=
name|hash
argument_list|(
operator|&
name|arg2
argument_list|)
expr_stmt|;
comment|/*     * If arg2 is a member of set arg1 then res will have the     *  value 1 and pd will have a pointer to the descriptor     *  that points to that member.     *  If arg2 is not a member of the set then res will have     *  the value 0 and pd will point to the descriptor     *  which should point to the member - thus we know where     *  to link in the new element without having to do any     *  repetitive looking.     */
name|pd
operator|=
name|memb
argument_list|(
name|BLKLOC
argument_list|(
name|arg1
argument_list|)
argument_list|,
operator|&
name|arg2
argument_list|,
name|hn
argument_list|,
operator|&
name|res
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
literal|0
condition|)
comment|/*       * The element is not in the set - insert it.       */
name|addmem
argument_list|(
name|BLKLOC
argument_list|(
name|arg1
argument_list|)
argument_list|,
name|alcselem
argument_list|(
operator|&
name|arg2
argument_list|,
name|hn
argument_list|)
argument_list|,
name|pd
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|Procblock
argument_list|(
argument|insert
argument_list|,
literal|2
argument_list|)
end_macro

begin_else
else|#
directive|else
else|SETS
end_else

begin_decl_stmt
name|char
name|junk
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* prevent null object file  */
end_comment

begin_endif
endif|#
directive|endif
endif|SETS
end_endif

end_unit

