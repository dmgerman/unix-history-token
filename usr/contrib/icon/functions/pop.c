begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_comment
comment|/*  * pop(x) - pop an element from beginning of list x.  */
end_comment

begin_macro
name|Xpop
argument_list|(
argument|nargs
argument_list|,
argument|arg1
argument_list|,
argument|arg0
argument_list|)
end_macro

begin_decl_stmt
name|int
name|nargs
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|descrip
name|arg1
decl_stmt|,
name|arg0
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|struct
name|b_list
modifier|*
name|hp
decl_stmt|;
specifier|register
name|struct
name|b_lelem
modifier|*
name|bp
decl_stmt|;
specifier|extern
name|struct
name|b_lelem
modifier|*
name|alclstb
parameter_list|()
function_decl|;
comment|/*     * x must be a list.     */
name|DeRef
argument_list|(
argument|arg1
argument_list|)
if|if
condition|(
name|QUAL
argument_list|(
name|arg1
argument_list|)
operator|||
name|TYPE
argument_list|(
name|arg1
argument_list|)
operator|!=
name|T_LIST
condition|)
name|runerr
argument_list|(
literal|108
argument_list|,
operator|&
name|arg1
argument_list|)
expr_stmt|;
comment|/*     * Fail if the list is empty.     */
name|hp
operator|=
operator|(
expr|struct
name|b_list
operator|*
operator|)
name|BLKLOC
argument_list|(
name|arg1
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|->
name|cursize
operator|<=
literal|0
condition|)
name|fail
argument_list|()
expr_stmt|;
comment|/*     * Point bp at the first list block.  If the first block has no     *  elements in use, point bp at the next list block.     */
name|bp
operator|=
operator|(
expr|struct
name|b_lelem
operator|*
operator|)
name|BLKLOC
argument_list|(
name|hp
operator|->
name|listhead
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|->
name|nused
operator|<=
literal|0
condition|)
block|{
name|bp
operator|=
operator|(
expr|struct
name|b_lelem
operator|*
operator|)
name|BLKLOC
argument_list|(
name|bp
operator|->
name|listnext
argument_list|)
expr_stmt|;
name|BLKLOC
argument_list|(
name|hp
operator|->
name|listhead
argument_list|)
operator|=
operator|(
expr|union
name|block
operator|*
operator|)
name|bp
expr_stmt|;
name|bp
operator|->
name|listprev
operator|=
name|nulldesc
expr_stmt|;
block|}
comment|/*     * Locate first element and assign it to arg0 for return.     */
name|i
operator|=
name|bp
operator|->
name|first
expr_stmt|;
name|arg0
operator|=
name|bp
operator|->
name|lslots
index|[
name|i
index|]
expr_stmt|;
comment|/*     * Set bp->first to new first element, or 0 if the block is now     *  empty.  Decrement the usage count for the block and the size     *  of the list.     */
if|if
condition|(
operator|++
name|i
operator|>=
name|bp
operator|->
name|nelem
condition|)
name|i
operator|=
literal|0
expr_stmt|;
name|bp
operator|->
name|first
operator|=
name|i
expr_stmt|;
name|bp
operator|->
name|nused
operator|--
expr_stmt|;
name|hp
operator|->
name|cursize
operator|--
expr_stmt|;
block|}
end_block

begin_macro
name|Procblock
argument_list|(
argument|pop
argument_list|,
literal|1
argument_list|)
end_macro

end_unit

