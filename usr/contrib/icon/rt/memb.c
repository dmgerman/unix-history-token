begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|SETS
end_ifdef

begin_comment
comment|/*  * memb - sets res flag to 1 if x is a member of set S,  *  sets res flag to 0 if not.  *  Returns pointer to the descriptor which points to  *  the element (or which would point to it if it were  *  there).  */
end_comment

begin_function
name|struct
name|descrip
modifier|*
name|memb
parameter_list|(
name|ps
parameter_list|,
name|x
parameter_list|,
name|hn
parameter_list|,
name|res
parameter_list|)
name|int
modifier|*
name|res
decl_stmt|;
comment|/* pointer to integer result flag */
name|struct
name|b_set
modifier|*
name|ps
decl_stmt|;
name|struct
name|descrip
modifier|*
name|x
decl_stmt|;
name|int
name|hn
decl_stmt|;
block|{
specifier|register
name|struct
name|descrip
modifier|*
name|lp
decl_stmt|;
specifier|register
name|struct
name|b_selem
modifier|*
name|pe
decl_stmt|;
specifier|extern
name|int
name|equiv
parameter_list|()
function_decl|;
name|lp
operator|=
operator|&
operator|(
name|ps
operator|->
name|sbucks
index|[
name|hn
operator|%
name|NBUCKETS
index|]
operator|)
expr_stmt|;
comment|/*     * Look for x in the hash chain.     */
operator|*
name|res
operator|=
literal|0
expr_stmt|;
while|while
condition|(
name|BLKLOC
argument_list|(
operator|*
name|lp
argument_list|)
operator|!=
name|NULL
condition|)
block|{
name|pe
operator|=
operator|(
expr|struct
name|b_selem
operator|*
operator|)
name|BLKLOC
argument_list|(
operator|*
name|lp
argument_list|)
expr_stmt|;
if|if
condition|(
name|pe
operator|->
name|hnum
operator|>
name|hn
condition|)
comment|/* too far - it isn't there */
return|return
operator|(
name|lp
operator|)
return|;
elseif|else
if|if
condition|(
operator|(
name|pe
operator|->
name|hnum
operator|==
name|hn
operator|)
operator|&&
operator|(
name|equiv
argument_list|(
operator|&
name|pe
operator|->
name|setmem
argument_list|,
name|x
argument_list|)
operator|)
condition|)
block|{
operator|*
name|res
operator|=
literal|1
expr_stmt|;
return|return
operator|(
name|lp
operator|)
return|;
block|}
comment|/*     * We haven't reached the right hashnumber yet or     *  the element isn't the right one so keep looking.     */
name|lp
operator|=
operator|&
operator|(
name|pe
operator|->
name|sblink
operator|)
expr_stmt|;
block|}
comment|/*     *  At end of chain - not there.     */
return|return
operator|(
name|lp
operator|)
return|;
block|}
end_function

begin_else
else|#
directive|else
else|SETS
end_else

begin_decl_stmt
name|char
name|junk
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* prevent null object file */
end_comment

begin_endif
endif|#
directive|endif
endif|SETS
end_endif

end_unit

