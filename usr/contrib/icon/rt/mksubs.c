begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"../h/rt.h"
end_include

begin_comment
comment|/*  * mksubs - form a substring.  var is a descriptor for the string from  *  which the substring is to be formed.  var may be a variable.  val  *  is a dereferenced version of val.  The descriptor for the resulting  *  substring is placed in *result.  The substring starts at position  *  i and extends for j characters.  */
end_comment

begin_function
name|struct
name|descrip
name|mksubs
parameter_list|(
name|var
parameter_list|,
name|val
parameter_list|,
name|i
parameter_list|,
name|j
parameter_list|,
name|result
parameter_list|)
specifier|register
name|struct
name|descrip
modifier|*
name|var
decl_stmt|,
decl|*
name|val
decl_stmt|,
modifier|*
name|result
decl_stmt|;
end_function

begin_decl_stmt
name|int
name|i
decl_stmt|,
name|j
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|extern
name|struct
name|b_tvsubs
modifier|*
name|alcsubs
parameter_list|()
function_decl|;
if|if
condition|(
name|QUAL
argument_list|(
operator|*
name|var
argument_list|)
operator|||
operator|!
name|VAR
argument_list|(
operator|*
name|var
argument_list|)
condition|)
block|{
comment|/*        * var isn't a variable, just form a descriptor that points into        *  the string named by val.        */
name|STRLEN
argument_list|(
operator|*
name|result
argument_list|)
operator|=
name|j
expr_stmt|;
name|STRLOC
argument_list|(
operator|*
name|result
argument_list|)
operator|=
name|STRLOC
argument_list|(
operator|*
name|val
argument_list|)
operator|+
name|i
operator|-
literal|1
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|TVAR
argument_list|(
operator|*
name|var
argument_list|)
condition|)
block|{
comment|/*        * var is a trapped variable.  If it is a substring trapped variable,        *  adjust the position and make var the substrung string.  For other        *  trapped variables, don't do anything.        */
switch|switch
condition|(
name|TYPE
argument_list|(
operator|*
name|var
argument_list|)
condition|)
block|{
case|case
name|T_TVSUBS
case|:
name|i
operator|+=
name|BLKLOC
argument_list|(
operator|*
name|var
argument_list|)
operator|->
name|tvsubs
operator|.
name|sspos
operator|-
literal|1
expr_stmt|;
name|var
operator|=
operator|&
name|BLKLOC
argument_list|(
operator|*
name|var
argument_list|)
operator|->
name|tvsubs
operator|.
name|ssvar
expr_stmt|;
break|break;
default|default:
break|break;
block|}
block|}
comment|/*     * Make a substring trapped variable by passing the buck to alcsubs.     */
name|result
operator|->
name|type
operator|=
name|D_TVSUBS
expr_stmt|;
name|BLKLOC
argument_list|(
operator|*
name|result
argument_list|)
operator|=
operator|(
expr|union
name|block
operator|*
operator|)
name|alcsubs
argument_list|(
name|j
argument_list|,
name|i
argument_list|,
name|var
argument_list|)
expr_stmt|;
return|return;
block|}
end_block

end_unit

