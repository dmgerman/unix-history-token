begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* The thing that makes children, remembers them, and contains wait loops. */
end_comment

begin_comment
comment|/* This file works under BSD, System V, minix, and Posix systems. */
end_comment

begin_comment
comment|/* Copyright (C) 1987,1989 Free Software Foundation, Inc.  This file is part of GNU Bash, the Bourne Again SHell.  Bash is free software; you can redistribute it and/or modify it under the terms of the GNU General Public License as published by the Free Software Foundation; either version 1, or (at your option) any later version.  Bash is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License for more details.  You should have received a copy of the GNU General Public License along with Bash; see the file COPYING.  If not, write to the Free Software Foundation, 675 Mass Ave, Cambridge, MA 02139, USA. */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_include
include|#
directive|include
file|<setjmp.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_include
include|#
directive|include
file|"general.h"
end_include

begin_include
include|#
directive|include
file|"filecntl.h"
end_include

begin_include
include|#
directive|include
file|"jobs.h"
end_include

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|USG
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sgtty.h>
end_include

begin_else
else|#
directive|else
end_else

begin_if
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<termio.h>
end_include

begin_include
include|#
directive|include
file|<sys/ttold.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !POSIX_VERSION */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* USG&& _POSIX_VERSION */
end_comment

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|SIGABRT
argument_list|)
end_if

begin_define
define|#
directive|define
name|SIGABRT
value|SIGIOT
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !SIGABRT */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|USG
argument_list|)
operator|||
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_define
define|#
directive|define
name|killpg
parameter_list|(
name|pg
parameter_list|,
name|sig
parameter_list|)
value|kill(-(pg),(sig))
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* USG || _POSIX_VERSION */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|USG
argument_list|)
end_if

begin_define
define|#
directive|define
name|siginterrupt
parameter_list|(
name|sig
parameter_list|,
name|code
parameter_list|)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* USG */
end_comment

begin_if
if|#
directive|if
name|defned
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_define
define|#
directive|define
name|WAITPID
parameter_list|(
name|pid
parameter_list|,
name|statusp
parameter_list|,
name|options
parameter_list|)
value|waitpid (pid, statusp, options)
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|WAITPID
parameter_list|(
name|pid
parameter_list|,
name|statusp
parameter_list|,
name|options
parameter_list|)
value|wait (statusp)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* !_POSIX_VERSION */
end_comment

begin_decl_stmt
name|pid_t
name|last_made_pid
init|=
operator|(
name|pid_t
operator|)
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|pid_t
name|last_asynchronous_pid
init|=
operator|(
name|pid_t
operator|)
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|int
name|errno
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Initialize the job control mechanism, and set up the tty stuff. */
end_comment

begin_macro
name|initialize_jobs
argument_list|()
end_macro

begin_block
block|{
name|get_tty_state
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/* Setup this shell to handle C-C, etc. */
end_comment

begin_macro
name|initialize_job_signals
argument_list|()
end_macro

begin_block
block|{
specifier|extern
name|int
name|login_shell
decl_stmt|;
specifier|extern
name|sighandler
name|sigint_sighandler
parameter_list|()
function_decl|;
name|signal
argument_list|(
name|SIGINT
argument_list|,
name|sigint_sighandler
argument_list|)
expr_stmt|;
comment|/* If this is a login shell we don't wish to be disturbed by      stop signals. */
if|if
condition|(
name|login_shell
condition|)
block|{
ifdef|#
directive|ifdef
name|SIGTSTP
name|signal
argument_list|(
name|SIGTSTP
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTOU
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTIN
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
block|}
end_block

begin_if
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_comment
comment|/* Collect the status of all zombie children so that their system    resources can be deallocated. */
end_comment

begin_function
specifier|static
name|void
name|reap_zombie_children
parameter_list|()
block|{
if|#
directive|if
name|defined
argument_list|(
name|WNOHANG
argument_list|)
while|while
condition|(
name|waitpid
argument_list|(
operator|-
literal|1
argument_list|,
operator|(
name|int
operator|*
operator|)
name|NULL
argument_list|,
name|WNOHANG
argument_list|)
operator|!=
operator|-
literal|1
condition|)
empty_stmt|;
endif|#
directive|endif
comment|/* WNOHANG */
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _POSIX_VERSION */
end_comment

begin_comment
comment|/* Fork, handling errors.  Returns the pid of the newly made child, or 0.    COMMAND is just for remembering the name of the command; we don't do    anything else with it.  ASYNC_P says what to do with the tty.  If    non-zero, then don't give it away. */
end_comment

begin_function
name|pid_t
name|make_child
parameter_list|(
name|command
parameter_list|,
name|async_p
parameter_list|)
name|char
modifier|*
name|command
decl_stmt|;
name|int
name|async_p
decl_stmt|;
block|{
name|pid_t
name|pid
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
name|int
name|retry
init|=
literal|1
decl_stmt|;
endif|#
directive|endif
comment|/* _POSIX_VERSION */
comment|/* Discard saved memory. */
if|if
condition|(
name|command
condition|)
name|free
argument_list|(
name|command
argument_list|)
expr_stmt|;
comment|/* Make new environment array if neccessary. */
name|maybe_make_export_env
argument_list|()
expr_stmt|;
comment|/* Create the child, handle severe errors. */
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
name|retry_fork
label|:
endif|#
directive|endif
comment|/* _POSIX_VERSION */
if|if
condition|(
operator|(
name|pid
operator|=
name|fork
argument_list|()
operator|)
operator|<
literal|0
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
comment|/* Posix systems with a non-blocking waitpid () system call available 	 get another chance after zombies are reaped. */
if|if
condition|(
name|errno
operator|==
name|EAGAIN
operator|&&
name|retry
condition|)
block|{
name|reap_zombie_chilren
argument_list|()
expr_stmt|;
name|retry
operator|=
literal|0
expr_stmt|;
goto|goto
name|retry_fork
goto|;
block|}
endif|#
directive|endif
comment|/* _POSIX_VERSION */
name|report_error
argument_list|(
literal|"fork: %s"
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|throw_to_top_level
argument_list|()
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|pid
condition|)
block|{
comment|/* Cancel shell traps. */
name|restore_original_signals
argument_list|()
expr_stmt|;
comment|/* Ignore INT and QUIT in asynchronous children. */
if|if
condition|(
name|async_p
condition|)
block|{
name|signal
argument_list|(
name|SIGINT
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGQUIT
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
name|last_asynchronous_pid
operator|=
name|getpid
argument_list|()
expr_stmt|;
block|}
else|else
block|{
if|#
directive|if
name|defined
argument_list|(
name|SIGTSTP
argument_list|)
name|signal
argument_list|(
name|SIGTSTP
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTIN
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
name|signal
argument_list|(
name|SIGTTOU
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
block|}
else|else
block|{
comment|/* In the parent. */
name|last_made_pid
operator|=
name|pid
expr_stmt|;
if|if
condition|(
name|async_p
condition|)
name|last_asynchronous_pid
operator|=
name|pid
expr_stmt|;
block|}
return|return
operator|(
name|pid
operator|)
return|;
block|}
end_function

begin_comment
comment|/* Wait for a single pid (PID) and return its exit status. */
end_comment

begin_macro
name|wait_for_single_pid
argument_list|(
argument|pid
argument_list|)
end_macro

begin_decl_stmt
name|pid_t
name|pid
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|pid_t
name|got_pid
decl_stmt|;
name|WAIT
name|status
decl_stmt|;
comment|/* Make sure that the process we are waiting for is valid. This      check is not necessary on Posix systems. */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
if|if
condition|(
operator|(
name|kill
argument_list|(
name|pid
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
operator|)
operator|&&
name|errno
operator|==
name|ESRCH
condition|)
return|return
operator|(
literal|127
operator|)
return|;
endif|#
directive|endif
comment|/* !_POSIX_VERSION */
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|1
argument_list|)
expr_stmt|;
while|while
condition|(
operator|(
name|got_pid
operator|=
name|WAITPID
argument_list|(
name|pid
argument_list|,
operator|&
name|status
argument_list|,
literal|0
argument_list|)
operator|)
operator|!=
name|pid
condition|)
block|{
if|if
condition|(
name|got_pid
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|errno
operator|!=
name|EINTR
operator|&&
name|errno
operator|!=
name|ECHILD
condition|)
block|{
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|file_error
argument_list|(
literal|"wait"
argument_list|)
expr_stmt|;
block|}
break|break;
block|}
block|}
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|QUIT
expr_stmt|;
block|}
end_block

begin_comment
comment|/* Wait for all of the shell's children to exit. */
end_comment

begin_macro
name|wait_for_background_pids
argument_list|()
end_macro

begin_block
block|{
comment|/* If we aren't using job control, we let the kernel take care of the      bookkeeping for us.  wait () will return -1 and set errno to ECHILD       when there are no more unwaited-for child processes on both      4.2 BSD-based and System V-based systems. */
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|1
argument_list|)
expr_stmt|;
while|while
condition|(
literal|1
condition|)
block|{
name|pid_t
name|got_pid
decl_stmt|;
name|WAIT
name|status
decl_stmt|;
comment|/* Wait for ECHILD */
while|while
condition|(
operator|(
name|got_pid
operator|=
name|WAITPID
argument_list|(
operator|-
literal|1
argument_list|,
operator|&
name|status
argument_list|,
literal|0
argument_list|)
operator|)
operator|!=
operator|-
literal|1
condition|)
empty_stmt|;
if|if
condition|(
name|errno
operator|!=
name|EINTR
operator|&&
name|errno
operator|!=
name|ECHILD
condition|)
block|{
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|file_error
argument_list|(
literal|"wait"
argument_list|)
expr_stmt|;
block|}
break|break;
block|}
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|QUIT
expr_stmt|;
block|}
end_block

begin_comment
comment|/* Wait for pid (one of our children) to terminate. */
end_comment

begin_function
name|int
name|wait_for
parameter_list|(
name|pid
parameter_list|)
name|pid_t
name|pid
decl_stmt|;
block|{
specifier|extern
name|int
name|interactive
decl_stmt|;
name|int
name|return_val
decl_stmt|;
name|pid_t
name|got_pid
decl_stmt|;
name|WAIT
name|status
decl_stmt|;
comment|/* Make sure that the process we are waiting for is valid.  This check      is not necessary on Posix systems. */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
if|if
condition|(
operator|(
name|kill
argument_list|(
name|pid
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
operator|)
operator|&&
operator|(
name|errno
operator|==
name|ESRCH
operator|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
endif|#
directive|endif
comment|/* !_POSIX_VERSION */
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|1
argument_list|)
expr_stmt|;
while|while
condition|(
operator|(
name|got_pid
operator|=
name|WAITPID
argument_list|(
name|pid
argument_list|,
operator|&
name|status
argument_list|,
literal|0
argument_list|)
operator|)
operator|!=
name|pid
condition|)
block|{
if|if
condition|(
name|got_pid
operator|<
literal|0
operator|&&
name|errno
operator|==
name|ECHILD
condition|)
block|{
if|#
directive|if
operator|!
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
name|status
operator|.
name|w_termsig
operator|=
name|status
operator|.
name|w_retcode
operator|=
literal|0
expr_stmt|;
else|#
directive|else
name|status
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
comment|/* _POSIX_VERSION */
break|break;
block|}
elseif|else
if|if
condition|(
name|got_pid
operator|<
literal|0
operator|&&
name|errno
operator|!=
name|EINTR
condition|)
name|programming_error
argument_list|(
literal|"got errno %d while waiting for %d"
argument_list|,
name|errno
argument_list|,
name|pid
argument_list|)
expr_stmt|;
block|}
name|siginterrupt
argument_list|(
name|SIGINT
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* Default return value. */
comment|/* ``a full 8 bits of status is returned'' */
if|if
condition|(
name|WIFSIGNALED
argument_list|(
name|status
argument_list|)
condition|)
name|return_val
operator|=
literal|128
operator|+
name|WTERMSIG
argument_list|(
name|status
argument_list|)
expr_stmt|;
else|else
name|return_val
operator|=
name|WEXITSTATUS
argument_list|(
name|status
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|WIFSTOPPED
argument_list|(
name|status
argument_list|)
operator|&&
name|WIFSIGNALED
argument_list|(
name|status
argument_list|)
operator|&&
operator|(
name|WTERMSIG
argument_list|(
name|status
argument_list|)
operator|!=
name|SIGINT
operator|)
condition|)
block|{
specifier|extern
name|char
modifier|*
name|sys_siglist
index|[]
decl_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s"
argument_list|,
name|sys_siglist
index|[
name|WTERMSIG
argument_list|(
name|status
argument_list|)
index|]
argument_list|)
expr_stmt|;
if|if
condition|(
name|WIFCORED
argument_list|(
name|status
argument_list|)
condition|)
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|" (core dumped)"
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"\n"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|WIFSIGNALED
argument_list|(
name|status
argument_list|)
operator|||
name|WIFSTOPPED
argument_list|(
name|status
argument_list|)
condition|)
name|set_tty_state
argument_list|()
expr_stmt|;
else|else
name|get_tty_state
argument_list|()
expr_stmt|;
return|return
operator|(
name|return_val
operator|)
return|;
block|}
end_function

begin_comment
comment|/* Give PID SIGNAL.  This determines what job the pid belongs to (if any).    If PID does belong to a job, and the job is stopped, then CONTinue the    job after giving it SIGNAL.  Returns -1 on failure.  If GROUP is non-null,    then kill the process group associated with PID. */
end_comment

begin_function
name|int
name|kill_pid
parameter_list|(
name|pid
parameter_list|,
name|signal
parameter_list|,
name|group
parameter_list|)
name|pid_t
name|pid
decl_stmt|;
name|int
name|signal
decl_stmt|,
name|group
decl_stmt|;
block|{
name|int
name|result
decl_stmt|;
if|if
condition|(
name|group
condition|)
name|result
operator|=
name|killpg
argument_list|(
name|pid
argument_list|,
name|signal
argument_list|)
expr_stmt|;
else|else
name|result
operator|=
name|kill
argument_list|(
name|pid
argument_list|,
name|signal
argument_list|)
expr_stmt|;
return|return
operator|(
name|result
operator|)
return|;
block|}
end_function

begin_if
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|struct
name|termios
name|shell_tty_info
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_if
if|#
directive|if
name|defined
argument_list|(
name|USG
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|struct
name|termio
name|shell_tty_info
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|struct
name|sgttyb
name|shell_tty_info
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* USG */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _POSIX_VERSION */
end_comment

begin_decl_stmt
specifier|static
name|int
name|got_tty_state
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Fill the contents of shell_tty_info with the current tty info. */
end_comment

begin_macro
name|get_tty_state
argument_list|()
end_macro

begin_block
block|{
name|int
name|tty
init|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
name|O_RDONLY
argument_list|)
decl_stmt|;
if|if
condition|(
name|tty
operator|!=
operator|-
literal|1
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
name|tcgetattr
argument_list|(
name|tty
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
else|#
directive|else
if|#
directive|if
name|defined
argument_list|(
name|USG
argument_list|)
name|ioctl
argument_list|(
name|tty
argument_list|,
name|TCGETA
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
else|#
directive|else
name|ioctl
argument_list|(
name|tty
argument_list|,
name|TIOCGETP
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
name|close
argument_list|(
name|tty
argument_list|)
expr_stmt|;
name|got_tty_state
operator|=
literal|1
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/* Make the current tty use the state in shell_tty_info. */
end_comment

begin_macro
name|set_tty_state
argument_list|()
end_macro

begin_block
block|{
name|int
name|tty
init|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
name|O_RDONLY
argument_list|)
decl_stmt|;
if|if
condition|(
name|tty
operator|!=
operator|-
literal|1
condition|)
block|{
if|if
condition|(
operator|!
name|got_tty_state
condition|)
block|{
name|close
argument_list|(
name|tty
argument_list|)
expr_stmt|;
return|return;
block|}
if|#
directive|if
name|defined
argument_list|(
name|_POSIX_VERSION
argument_list|)
name|tcsetattr
argument_list|(
name|tty
argument_list|,
name|TCSADRAIN
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
else|#
directive|else
if|#
directive|if
name|defined
argument_list|(
name|USG
argument_list|)
name|ioctl
argument_list|(
name|tty
argument_list|,
name|TCSETAW
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
comment|/* Wait for output, no flush */
else|#
directive|else
name|ioctl
argument_list|(
name|tty
argument_list|,
name|TIOCSETN
argument_list|,
operator|&
name|shell_tty_info
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
name|close
argument_list|(
name|tty
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/* Give the terminal to PGRP.  */
end_comment

begin_macro
name|give_terminal_to
argument_list|(
argument|pgrp
argument_list|)
end_macro

begin_decl_stmt
name|pid_t
name|pgrp
decl_stmt|;
end_decl_stmt

begin_block
block|{ }
end_block

begin_comment
comment|/* Stop a pipeline. */
end_comment

begin_macro
name|stop_pipeline
argument_list|(
argument|async
argument_list|,
argument|ignore
argument_list|)
end_macro

begin_decl_stmt
name|int
name|async
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|ignore
decl_stmt|;
end_decl_stmt

begin_block
block|{ }
end_block

begin_comment
comment|/* Print descriptive information about the job with leader pid PID. */
end_comment

begin_macro
name|describe_pid
argument_list|(
argument|pid
argument_list|)
end_macro

begin_decl_stmt
name|pid_t
name|pid
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"<%d>\n"
argument_list|,
operator|(
name|int
operator|)
name|pid
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

