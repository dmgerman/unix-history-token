begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1988 University of Utah.  * Copyright (c) 1991 The Regents of the University of California.  * All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * the Systems Programming Group of the University of Utah Computer  * Science Department.  *  * %sccs.include.redist.c%  *  * from: Utah $Hdr: vm_mmap.c 1.3 90/01/21$  *  *	@(#)vm_mmap.c	7.1 (Berkeley) %G%  */
end_comment

begin_comment
comment|/*  * Mapped file (mmap) interface to VM  */
end_comment

begin_include
include|#
directive|include
file|"param.h"
end_include

begin_include
include|#
directive|include
file|"systm.h"
end_include

begin_include
include|#
directive|include
file|"user.h"
end_include

begin_include
include|#
directive|include
file|"proc.h"
end_include

begin_include
include|#
directive|include
file|"vnode.h"
end_include

begin_include
include|#
directive|include
file|"specdev.h"
end_include

begin_include
include|#
directive|include
file|"file.h"
end_include

begin_include
include|#
directive|include
file|"mman.h"
end_include

begin_include
include|#
directive|include
file|"conf.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_param.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_map.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_pager.h"
end_include

begin_include
include|#
directive|include
file|"../vm/vm_prot.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_decl_stmt
name|int
name|mmapdebug
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_define
define|#
directive|define
name|MDB_FOLLOW
value|0x01
end_define

begin_define
define|#
directive|define
name|MDB_SYNC
value|0x02
end_define

begin_define
define|#
directive|define
name|MDB_MAPIT
value|0x04
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|getpagesize
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|args
modifier|*
name|uap
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
operator|*
name|retval
operator|=
name|NBPG
operator|*
name|CLSIZE
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|sbrk
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|int
name|incr
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* Not yet implemented */
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|sstk
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|int
name|incr
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* Not yet implemented */
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|smmap
argument_list|(
name|p
argument_list|,
name|uap
argument_list|,
name|retval
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_struct
specifier|register
struct|struct
name|args
block|{
name|caddr_t
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
name|int
name|prot
decl_stmt|;
name|int
name|flags
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|off_t
name|pos
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|vm_offset_t
name|addr
decl_stmt|;
name|vm_size_t
name|size
decl_stmt|;
name|vm_prot_t
name|prot
decl_stmt|;
name|caddr_t
name|handle
decl_stmt|;
name|int
name|mtype
decl_stmt|,
name|error
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"mmap(%d): addr %x len %x pro %x flg %x fd %d pos %x\n"
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
name|uap
operator|->
name|addr
argument_list|,
name|uap
operator|->
name|len
argument_list|,
name|uap
operator|->
name|prot
argument_list|,
name|uap
operator|->
name|flags
argument_list|,
name|uap
operator|->
name|fd
argument_list|,
name|uap
operator|->
name|pos
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Make sure one of the sharing types is specified 	 */
name|mtype
operator|=
name|uap
operator|->
name|flags
operator|&
name|MAP_TYPE
expr_stmt|;
switch|switch
condition|(
name|mtype
condition|)
block|{
case|case
name|MAP_FILE
case|:
case|case
name|MAP_ANON
case|:
break|break;
default|default:
return|return
operator|(
name|EINVAL
operator|)
return|;
block|}
comment|/* 	 * Address (if FIXED) and size must be page aligned 	 */
name|size
operator|=
operator|(
name|vm_size_t
operator|)
name|uap
operator|->
name|len
expr_stmt|;
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
name|uap
operator|->
name|addr
expr_stmt|;
if|if
condition|(
operator|(
name|size
operator|&
name|page_mask
operator|)
operator|||
operator|(
name|uap
operator|->
name|flags
operator|&
name|MAP_FIXED
operator|)
operator|&&
operator|(
name|addr
operator|&
name|page_mask
operator|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* 	 * Mapping file or named anonymous, get fp for validation 	 */
if|if
condition|(
name|mtype
operator|==
name|MAP_FILE
operator|||
name|uap
operator|->
name|fd
operator|!=
operator|-
literal|1
condition|)
block|{
if|if
condition|(
operator|(
name|unsigned
operator|)
name|uap
operator|->
name|fd
operator|>=
name|NOFILE
operator|||
operator|(
name|fp
operator|=
name|u
operator|.
name|u_ofile
index|[
name|uap
operator|->
name|fd
index|]
operator|)
operator|==
name|NULL
condition|)
return|return
operator|(
name|EBADF
operator|)
return|;
block|}
comment|/* 	 * If we are mapping a file we need to check various 	 * file/vnode related things. 	 */
if|if
condition|(
name|mtype
operator|==
name|MAP_FILE
condition|)
block|{
comment|/* 		 * Obtain vnode and make sure it is of appropriate type 		 */
if|if
condition|(
name|fp
operator|->
name|f_type
operator|!=
name|DTYPE_VNODE
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
name|vp
operator|=
operator|(
expr|struct
name|vnode
operator|*
operator|)
name|fp
operator|->
name|f_data
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|v_type
operator|!=
name|VREG
operator|&&
name|vp
operator|->
name|v_type
operator|!=
name|VCHR
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* 		 * Ensure that file protection and desired protection 		 * are compatible.  Note that we only worry about writability 		 * if mapping is shared. 		 */
if|if
condition|(
operator|(
name|uap
operator|->
name|prot
operator|&
name|PROT_READ
operator|)
operator|&&
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FREAD
operator|)
operator|==
literal|0
operator|||
operator|(
operator|(
name|uap
operator|->
name|flags
operator|&
name|MAP_SHARED
operator|)
operator|&&
operator|(
name|uap
operator|->
name|prot
operator|&
name|PROT_WRITE
operator|)
operator|&&
operator|(
name|fp
operator|->
name|f_flag
operator|&
name|FWRITE
operator|)
operator|==
literal|0
operator|)
condition|)
return|return
operator|(
name|EACCES
operator|)
return|;
name|handle
operator|=
operator|(
name|caddr_t
operator|)
name|vp
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|uap
operator|->
name|fd
operator|!=
operator|-
literal|1
condition|)
name|handle
operator|=
operator|(
name|caddr_t
operator|)
name|fp
expr_stmt|;
else|else
name|handle
operator|=
name|NULL
expr_stmt|;
comment|/* 	 * Map protections to MACH style 	 */
name|prot
operator|=
name|VM_PROT_NONE
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_READ
condition|)
name|prot
operator||=
name|VM_PROT_READ
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_WRITE
condition|)
name|prot
operator||=
name|VM_PROT_WRITE
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_EXEC
condition|)
name|prot
operator||=
name|VM_PROT_EXECUTE
expr_stmt|;
name|error
operator|=
name|vm_mmap
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
operator|&
name|addr
argument_list|,
name|size
argument_list|,
name|prot
argument_list|,
name|uap
operator|->
name|flags
argument_list|,
name|handle
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|uap
operator|->
name|pos
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
literal|0
condition|)
operator|*
name|retval
operator|=
operator|(
name|int
operator|)
name|addr
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_macro
name|msync
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|char
modifier|*
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vm_offset_t
name|addr
decl_stmt|,
name|objoff
decl_stmt|,
name|oaddr
decl_stmt|;
name|vm_size_t
name|size
decl_stmt|,
name|osize
decl_stmt|;
name|vm_prot_t
name|prot
decl_stmt|,
name|mprot
decl_stmt|;
name|vm_inherit_t
name|inherit
decl_stmt|;
name|vm_object_t
name|object
decl_stmt|;
name|boolean_t
name|shared
decl_stmt|;
name|int
name|rv
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
operator|(
name|MDB_FOLLOW
operator||
name|MDB_SYNC
operator|)
condition|)
name|printf
argument_list|(
literal|"msync(%d): addr %x len %x\n"
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
name|uap
operator|->
name|addr
argument_list|,
name|uap
operator|->
name|len
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|(
operator|(
name|int
operator|)
name|uap
operator|->
name|addr
operator|&
name|page_mask
operator|)
operator|||
operator|(
name|uap
operator|->
name|len
operator|&
name|page_mask
operator|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
name|addr
operator|=
name|oaddr
operator|=
operator|(
name|vm_offset_t
operator|)
name|uap
operator|->
name|addr
expr_stmt|;
name|osize
operator|=
operator|(
name|vm_size_t
operator|)
name|uap
operator|->
name|len
expr_stmt|;
comment|/* 	 * Region must be entirely contained in a single entry 	 */
if|if
condition|(
operator|!
name|vm_map_is_allocated
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|osize
argument_list|,
name|TRUE
argument_list|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* 	 * Determine the object associated with that entry 	 * (object is returned locked on KERN_SUCCESS) 	 */
name|rv
operator|=
name|vm_region
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
operator|&
name|addr
argument_list|,
operator|&
name|size
argument_list|,
operator|&
name|prot
argument_list|,
operator|&
name|mprot
argument_list|,
operator|&
name|inherit
argument_list|,
operator|&
name|shared
argument_list|,
operator|&
name|object
argument_list|,
operator|&
name|objoff
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_SYNC
condition|)
name|printf
argument_list|(
literal|"msync: region: object %x addr %x size %d objoff %d\n"
argument_list|,
name|object
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|objoff
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Do not msync non-vnoded backed objects. 	 */
if|if
condition|(
name|object
operator|->
name|internal
operator|||
name|object
operator|->
name|pager
operator|==
name|vm_pager_null
operator|||
name|object
operator|->
name|pager
operator|->
name|pg_type
operator|!=
name|PG_VNODE
condition|)
block|{
name|vm_object_unlock
argument_list|(
name|object
argument_list|)
expr_stmt|;
return|return
operator|(
name|EINVAL
operator|)
return|;
block|}
name|objoff
operator|+=
name|oaddr
operator|-
name|addr
expr_stmt|;
if|if
condition|(
name|osize
operator|==
literal|0
condition|)
name|osize
operator|=
name|size
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_SYNC
condition|)
name|printf
argument_list|(
literal|"msync: cleaning/flushing object range [%x-%x)\n"
argument_list|,
name|objoff
argument_list|,
name|objoff
operator|+
name|osize
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|prot
operator|&
name|VM_PROT_WRITE
condition|)
name|vm_object_page_clean
argument_list|(
name|object
argument_list|,
name|objoff
argument_list|,
name|objoff
operator|+
name|osize
argument_list|)
expr_stmt|;
comment|/* 	 * (XXX) 	 * Bummer, gotta flush all cached pages to ensure 	 * consistency with the file system cache. 	 */
name|vm_object_page_remove
argument_list|(
name|object
argument_list|,
name|objoff
argument_list|,
name|objoff
operator|+
name|osize
argument_list|)
expr_stmt|;
name|vm_object_unlock
argument_list|(
name|object
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|munmap
argument_list|(
name|p
argument_list|,
name|uap
argument_list|,
name|retval
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_struct
specifier|register
struct|struct
name|args
block|{
name|caddr_t
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vm_offset_t
name|addr
decl_stmt|;
name|vm_size_t
name|size
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"munmap(%d): addr %x len %x\n"
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
name|uap
operator|->
name|addr
argument_list|,
name|uap
operator|->
name|len
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
name|uap
operator|->
name|addr
expr_stmt|;
name|size
operator|=
operator|(
name|vm_size_t
operator|)
name|uap
operator|->
name|len
expr_stmt|;
if|if
condition|(
operator|(
name|addr
operator|&
name|page_mask
operator|)
operator|||
operator|(
name|size
operator|&
name|page_mask
operator|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
if|if
condition|(
name|size
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
operator|!
name|vm_map_is_allocated
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|size
argument_list|,
name|FALSE
argument_list|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* returns nothing but KERN_SUCCESS anyway */
operator|(
name|void
operator|)
name|vm_map_remove
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_macro
name|munmapfd
argument_list|(
argument|fd
argument_list|)
end_macro

begin_block
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"munmapfd(%d): fd %d\n"
argument_list|,
name|u
operator|.
name|u_procp
operator|->
name|p_pid
argument_list|,
name|fd
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * XXX -- should vm_deallocate any regions mapped to this file 	 */
name|u
operator|.
name|u_pofile
index|[
name|fd
index|]
operator|&=
operator|~
name|UF_MAPPED
expr_stmt|;
block|}
end_block

begin_macro
name|mprotect
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|char
modifier|*
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
name|int
name|prot
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|vm_offset_t
name|addr
decl_stmt|;
name|vm_size_t
name|size
decl_stmt|;
specifier|register
name|vm_prot_t
name|prot
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_FOLLOW
condition|)
name|printf
argument_list|(
literal|"mprotect(%d): addr %x len %x prot %d\n"
argument_list|,
name|p
operator|->
name|p_pid
argument_list|,
name|uap
operator|->
name|addr
argument_list|,
name|uap
operator|->
name|len
argument_list|,
name|uap
operator|->
name|prot
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|addr
operator|=
operator|(
name|vm_offset_t
operator|)
name|uap
operator|->
name|addr
expr_stmt|;
name|size
operator|=
operator|(
name|vm_size_t
operator|)
name|uap
operator|->
name|len
expr_stmt|;
if|if
condition|(
operator|(
name|addr
operator|&
name|page_mask
operator|)
operator|||
operator|(
name|size
operator|&
name|page_mask
operator|)
condition|)
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* 	 * Map protections 	 */
name|prot
operator|=
name|VM_PROT_NONE
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_READ
condition|)
name|prot
operator||=
name|VM_PROT_READ
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_WRITE
condition|)
name|prot
operator||=
name|VM_PROT_WRITE
expr_stmt|;
if|if
condition|(
name|uap
operator|->
name|prot
operator|&
name|PROT_EXEC
condition|)
name|prot
operator||=
name|VM_PROT_EXECUTE
expr_stmt|;
switch|switch
condition|(
name|vm_map_protect
argument_list|(
name|p
operator|->
name|p_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|size
argument_list|,
name|prot
argument_list|,
name|FALSE
argument_list|)
condition|)
block|{
case|case
name|KERN_SUCCESS
case|:
return|return
operator|(
literal|0
operator|)
return|;
case|case
name|KERN_PROTECTION_FAILURE
case|:
return|return
operator|(
name|EACCES
operator|)
return|;
block|}
return|return
operator|(
name|EINVAL
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|madvise
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|char
modifier|*
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
name|int
name|behav
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* Not yet implemented */
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_block

begin_comment
comment|/* ARGSUSED */
end_comment

begin_macro
name|mincore
argument_list|(
argument|p
argument_list|,
argument|uap
argument_list|,
argument|retval
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|args
block|{
name|char
modifier|*
name|addr
decl_stmt|;
name|int
name|len
decl_stmt|;
name|char
modifier|*
name|vec
decl_stmt|;
block|}
modifier|*
name|uap
struct|;
end_struct

begin_decl_stmt
name|int
modifier|*
name|retval
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* Not yet implemented */
return|return
operator|(
name|EOPNOTSUPP
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Internal version of mmap.  * Currently used by mmap, exec, and sys5 shared memory.  * Handle is:  *	MAP_FILE: a vnode pointer  *	MAP_ANON: NULL or a file pointer  */
end_comment

begin_expr_stmt
name|vm_mmap
argument_list|(
name|map
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|prot
argument_list|,
name|flags
argument_list|,
name|handle
argument_list|,
name|foff
argument_list|)
specifier|register
name|vm_map_t
name|map
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|vm_offset_t
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|vm_size_t
name|size
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_prot_t
name|prot
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|int
name|flags
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|handle
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* XXX should be vp */
end_comment

begin_decl_stmt
name|vm_offset_t
name|foff
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|vm_pager_t
name|pager
decl_stmt|;
name|boolean_t
name|fitit
decl_stmt|;
name|vm_object_t
name|object
decl_stmt|;
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
name|int
name|type
decl_stmt|;
name|int
name|rv
init|=
name|KERN_SUCCESS
decl_stmt|;
if|if
condition|(
name|size
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
operator|(
name|flags
operator|&
name|MAP_FIXED
operator|)
operator|==
literal|0
condition|)
block|{
name|fitit
operator|=
name|TRUE
expr_stmt|;
operator|*
name|addr
operator|=
name|round_page
argument_list|(
operator|*
name|addr
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|fitit
operator|=
name|FALSE
expr_stmt|;
operator|(
name|void
operator|)
name|vm_deallocate
argument_list|(
name|map
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Lookup/allocate pager.  All except an unnamed anonymous lookup 	 * gain a reference to ensure continued existance of the object. 	 * (XXX the exception is to appease the pageout daemon) 	 */
if|if
condition|(
operator|(
name|flags
operator|&
name|MAP_TYPE
operator|)
operator|==
name|MAP_ANON
condition|)
name|type
operator|=
name|PG_DFLT
expr_stmt|;
else|else
block|{
name|vp
operator|=
operator|(
expr|struct
name|vnode
operator|*
operator|)
name|handle
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|v_type
operator|==
name|VCHR
condition|)
block|{
name|type
operator|=
name|PG_DEVICE
expr_stmt|;
name|handle
operator|=
operator|(
name|caddr_t
operator|)
name|vp
operator|->
name|v_rdev
expr_stmt|;
block|}
else|else
name|type
operator|=
name|PG_VNODE
expr_stmt|;
block|}
name|pager
operator|=
name|vm_pager_allocate
argument_list|(
name|type
argument_list|,
name|handle
argument_list|,
name|size
argument_list|,
name|prot
argument_list|)
expr_stmt|;
if|if
condition|(
name|pager
operator|==
name|VM_PAGER_NULL
condition|)
return|return
operator|(
name|type
operator|==
name|PG_DEVICE
condition|?
name|EINVAL
else|:
name|ENOMEM
operator|)
return|;
comment|/* 	 * Find object and release extra reference gained by lookup 	 */
name|object
operator|=
name|vm_object_lookup
argument_list|(
name|pager
argument_list|)
expr_stmt|;
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
comment|/* 	 * Anonymous memory. 	 */
if|if
condition|(
operator|(
name|flags
operator|&
name|MAP_TYPE
operator|)
operator|==
name|MAP_ANON
condition|)
block|{
name|rv
operator|=
name|vm_allocate_with_pager
argument_list|(
name|map
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|,
name|pager
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
if|if
condition|(
name|handle
operator|==
name|NULL
condition|)
name|vm_pager_deallocate
argument_list|(
name|pager
argument_list|)
expr_stmt|;
else|else
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/* 		 * Don't cache anonymous objects. 		 * Loses the reference gained by vm_pager_allocate. 		 */
operator|(
name|void
operator|)
name|pager_cache
argument_list|(
name|object
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_MAPIT
condition|)
name|printf
argument_list|(
literal|"vm_mmap(%d): ANON *addr %x size %x pager %x\n"
argument_list|,
name|u
operator|.
name|u_procp
operator|->
name|p_pid
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|,
name|pager
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Must be type MAP_FILE. 	 * Distinguish between character special and regular files. 	 */
elseif|else
if|if
condition|(
name|vp
operator|->
name|v_type
operator|==
name|VCHR
condition|)
block|{
name|rv
operator|=
name|vm_allocate_with_pager
argument_list|(
name|map
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|,
name|pager
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
comment|/* 		 * Uncache the object and lose the reference gained 		 * by vm_pager_allocate().  If the call to 		 * vm_allocate_with_pager() was sucessful, then we 		 * gained an additional reference ensuring the object 		 * will continue to exist.  If the call failed then 		 * the deallocate call below will terminate the 		 * object which is fine. 		 */
operator|(
name|void
operator|)
name|pager_cache
argument_list|(
name|object
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
goto|goto
name|out
goto|;
block|}
comment|/* 	 * A regular file 	 */
else|else
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|object
operator|==
name|VM_OBJECT_NULL
condition|)
name|printf
argument_list|(
literal|"vm_mmap: no object: vp %x, pager %x\n"
argument_list|,
name|vp
argument_list|,
name|pager
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * Map it directly. 		 * Allows modifications to go out to the vnode. 		 */
if|if
condition|(
name|flags
operator|&
name|MAP_SHARED
condition|)
block|{
name|rv
operator|=
name|vm_allocate_with_pager
argument_list|(
name|map
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|,
name|pager
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/* 			 * Don't cache the object.  This is the easiest way 			 * of ensuring that data gets back to the filesystem 			 * because vnode_pager_deallocate() will fsync the 			 * vnode.  pager_cache() will lose the extra ref. 			 */
if|if
condition|(
name|prot
operator|&
name|VM_PROT_WRITE
condition|)
name|pager_cache
argument_list|(
name|object
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
else|else
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
block|}
comment|/* 		 * Copy-on-write of file.  Two flavors. 		 * MAP_COPY is true COW, you essentially get a snapshot of 		 * the region at the time of mapping.  MAP_PRIVATE means only 		 * that your changes are not reflected back to the object. 		 * Changes made by others will be seen. 		 */
else|else
block|{
name|vm_map_t
name|tmap
decl_stmt|;
name|vm_offset_t
name|off
decl_stmt|;
comment|/* locate and allocate the target address space */
name|rv
operator|=
name|vm_map_find
argument_list|(
name|map
argument_list|,
name|VM_OBJECT_NULL
argument_list|,
operator|(
name|vm_offset_t
operator|)
literal|0
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|tmap
operator|=
name|vm_map_create
argument_list|(
name|pmap_create
argument_list|(
name|size
argument_list|)
argument_list|,
name|VM_MIN_ADDRESS
argument_list|,
name|VM_MIN_ADDRESS
operator|+
name|size
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
name|off
operator|=
name|VM_MIN_ADDRESS
expr_stmt|;
name|rv
operator|=
name|vm_allocate_with_pager
argument_list|(
name|tmap
argument_list|,
operator|&
name|off
argument_list|,
name|size
argument_list|,
name|TRUE
argument_list|,
name|pager
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
name|vm_map_deallocate
argument_list|(
name|tmap
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
comment|/* 			 * (XXX) 			 * MAP_PRIVATE implies that we see changes made by 			 * others.  To ensure that we need to guarentee that 			 * no copy object is created (otherwise original 			 * pages would be pushed to the copy object and we 			 * would never see changes made by others).  We 			 * totally sleeze it right now by marking the object 			 * internal temporarily. 			 */
if|if
condition|(
operator|(
name|flags
operator|&
name|MAP_COPY
operator|)
operator|==
literal|0
condition|)
name|object
operator|->
name|internal
operator|=
name|TRUE
expr_stmt|;
name|rv
operator|=
name|vm_map_copy
argument_list|(
name|map
argument_list|,
name|tmap
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|,
name|off
argument_list|,
name|FALSE
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
name|object
operator|->
name|internal
operator|=
name|FALSE
expr_stmt|;
comment|/* 			 * (XXX) 			 * My oh my, this only gets worse... 			 * Force creation of a shadow object so that 			 * vm_map_fork will do the right thing. 			 */
if|if
condition|(
operator|(
name|flags
operator|&
name|MAP_COPY
operator|)
operator|==
literal|0
condition|)
block|{
name|vm_map_t
name|tmap
decl_stmt|;
name|vm_map_entry_t
name|tentry
decl_stmt|;
name|vm_object_t
name|tobject
decl_stmt|;
name|vm_offset_t
name|toffset
decl_stmt|;
name|vm_prot_t
name|tprot
decl_stmt|;
name|boolean_t
name|twired
decl_stmt|,
name|tsu
decl_stmt|;
name|tmap
operator|=
name|map
expr_stmt|;
name|vm_map_lookup
argument_list|(
operator|&
name|tmap
argument_list|,
operator|*
name|addr
argument_list|,
name|VM_PROT_WRITE
argument_list|,
operator|&
name|tentry
argument_list|,
operator|&
name|tobject
argument_list|,
operator|&
name|toffset
argument_list|,
operator|&
name|tprot
argument_list|,
operator|&
name|twired
argument_list|,
operator|&
name|tsu
argument_list|)
expr_stmt|;
name|vm_map_lookup_done
argument_list|(
name|tmap
argument_list|,
name|tentry
argument_list|)
expr_stmt|;
block|}
comment|/* 			 * (XXX) 			 * Map copy code cannot detect sharing unless a 			 * sharing map is involved.  So we cheat and write 			 * protect everything ourselves.  Note we cannot 			 * use vm_object_pmap_copy() because that relies 			 * on the page copy_on_write bit which isn't 			 * always accurate with shared objects. 			 */
name|vm_object_pmap_force_copy
argument_list|(
name|object
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|foff
operator|+
name|size
argument_list|)
expr_stmt|;
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
name|vm_map_deallocate
argument_list|(
name|tmap
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
goto|goto
name|out
goto|;
block|}
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_MAPIT
condition|)
name|printf
argument_list|(
literal|"vm_mmap(%d): FILE *addr %x size %x pager %x\n"
argument_list|,
name|u
operator|.
name|u_procp
operator|->
name|p_pid
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|,
name|pager
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
comment|/* 	 * Correct protection (default is VM_PROT_ALL). 	 * Note that we set the maximum protection.  This may not be 	 * entirely correct.  Maybe the maximum protection should be based 	 * on the object permissions where it makes sense (e.g. a vnode). 	 * 	 * Changed my mind: leave max prot at VM_PROT_ALL. 	 */
if|if
condition|(
name|prot
operator|!=
name|VM_PROT_ALL
condition|)
block|{
name|rv
operator|=
name|vm_map_protect
argument_list|(
name|map
argument_list|,
operator|*
name|addr
argument_list|,
operator|*
name|addr
operator|+
name|size
argument_list|,
name|prot
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
operator|(
name|void
operator|)
name|vm_deallocate
argument_list|(
name|map
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
block|}
comment|/* 	 * Shared memory is also shared with children. 	 */
if|if
condition|(
name|flags
operator|&
name|MAP_SHARED
condition|)
block|{
name|rv
operator|=
name|vm_inherit
argument_list|(
name|map
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|,
name|VM_INHERIT_SHARE
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|KERN_SUCCESS
condition|)
block|{
operator|(
name|void
operator|)
name|vm_deallocate
argument_list|(
name|map
argument_list|,
operator|*
name|addr
argument_list|,
name|size
argument_list|)
expr_stmt|;
goto|goto
name|out
goto|;
block|}
block|}
name|out
label|:
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|mmapdebug
operator|&
name|MDB_MAPIT
condition|)
name|printf
argument_list|(
literal|"vm_mmap: rv %d\n"
argument_list|,
name|rv
argument_list|)
expr_stmt|;
endif|#
directive|endif
switch|switch
condition|(
name|rv
condition|)
block|{
case|case
name|KERN_SUCCESS
case|:
return|return
operator|(
literal|0
operator|)
return|;
case|case
name|KERN_INVALID_ADDRESS
case|:
case|case
name|KERN_NO_SPACE
case|:
return|return
operator|(
name|ENOMEM
operator|)
return|;
case|case
name|KERN_PROTECTION_FAILURE
case|:
return|return
operator|(
name|EACCES
operator|)
return|;
default|default:
return|return
operator|(
name|EINVAL
operator|)
return|;
block|}
block|}
end_block

begin_comment
comment|/*  * Internal bastardized version of MACHs vm_region system call.  * Given address and size it returns map attributes as well  * as the (locked) object mapped at that location.   */
end_comment

begin_macro
name|vm_region
argument_list|(
argument|map
argument_list|,
argument|addr
argument_list|,
argument|size
argument_list|,
argument|prot
argument_list|,
argument|max_prot
argument_list|,
argument|inheritance
argument_list|,
argument|shared
argument_list|,
argument|object
argument_list|,
argument|objoff
argument_list|)
end_macro

begin_decl_stmt
name|vm_map_t
name|map
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_offset_t
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* IN/OUT */
end_comment

begin_decl_stmt
name|vm_size_t
modifier|*
name|size
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|vm_prot_t
modifier|*
name|prot
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|vm_prot_t
modifier|*
name|max_prot
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|vm_inherit_t
modifier|*
name|inheritance
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|boolean_t
modifier|*
name|shared
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|vm_object_t
modifier|*
name|object
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_decl_stmt
name|vm_offset_t
modifier|*
name|objoff
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* OUT */
end_comment

begin_block
block|{
name|vm_map_entry_t
name|tmp_entry
decl_stmt|;
specifier|register
name|vm_map_entry_t
name|entry
decl_stmt|;
specifier|register
name|vm_offset_t
name|tmp_offset
decl_stmt|;
name|vm_offset_t
name|start
decl_stmt|;
if|if
condition|(
name|map
operator|==
name|VM_MAP_NULL
condition|)
return|return
operator|(
name|KERN_INVALID_ARGUMENT
operator|)
return|;
name|start
operator|=
operator|*
name|addr
expr_stmt|;
name|vm_map_lock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|vm_map_lookup_entry
argument_list|(
name|map
argument_list|,
name|start
argument_list|,
operator|&
name|tmp_entry
argument_list|)
condition|)
block|{
if|if
condition|(
operator|(
name|entry
operator|=
name|tmp_entry
operator|->
name|next
operator|)
operator|==
operator|&
name|map
operator|->
name|header
condition|)
block|{
name|vm_map_unlock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
return|return
operator|(
name|KERN_NO_SPACE
operator|)
return|;
block|}
name|start
operator|=
name|entry
operator|->
name|start
expr_stmt|;
operator|*
name|addr
operator|=
name|start
expr_stmt|;
block|}
else|else
name|entry
operator|=
name|tmp_entry
expr_stmt|;
operator|*
name|prot
operator|=
name|entry
operator|->
name|protection
expr_stmt|;
operator|*
name|max_prot
operator|=
name|entry
operator|->
name|max_protection
expr_stmt|;
operator|*
name|inheritance
operator|=
name|entry
operator|->
name|inheritance
expr_stmt|;
name|tmp_offset
operator|=
name|entry
operator|->
name|offset
operator|+
operator|(
name|start
operator|-
name|entry
operator|->
name|start
operator|)
expr_stmt|;
operator|*
name|size
operator|=
operator|(
name|entry
operator|->
name|end
operator|-
name|start
operator|)
expr_stmt|;
if|if
condition|(
name|entry
operator|->
name|is_a_map
condition|)
block|{
specifier|register
name|vm_map_t
name|share_map
decl_stmt|;
name|vm_size_t
name|share_size
decl_stmt|;
name|share_map
operator|=
name|entry
operator|->
name|object
operator|.
name|share_map
expr_stmt|;
name|vm_map_lock_read
argument_list|(
name|share_map
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|vm_map_lookup_entry
argument_list|(
name|share_map
argument_list|,
name|tmp_offset
argument_list|,
operator|&
name|tmp_entry
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|share_size
operator|=
operator|(
name|tmp_entry
operator|->
name|end
operator|-
name|tmp_offset
operator|)
operator|)
operator|<
operator|*
name|size
condition|)
operator|*
name|size
operator|=
name|share_size
expr_stmt|;
name|vm_object_lock
argument_list|(
name|tmp_entry
operator|->
name|object
argument_list|)
expr_stmt|;
operator|*
name|object
operator|=
name|tmp_entry
operator|->
name|object
operator|.
name|vm_object
expr_stmt|;
operator|*
name|objoff
operator|=
name|tmp_entry
operator|->
name|offset
operator|+
operator|(
name|tmp_offset
operator|-
name|tmp_entry
operator|->
name|start
operator|)
expr_stmt|;
operator|*
name|shared
operator|=
operator|(
name|share_map
operator|->
name|ref_count
operator|!=
literal|1
operator|)
expr_stmt|;
name|vm_map_unlock_read
argument_list|(
name|share_map
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|vm_object_lock
argument_list|(
name|entry
operator|->
name|object
argument_list|)
expr_stmt|;
operator|*
name|object
operator|=
name|entry
operator|->
name|object
operator|.
name|vm_object
expr_stmt|;
operator|*
name|objoff
operator|=
name|tmp_offset
expr_stmt|;
operator|*
name|shared
operator|=
name|FALSE
expr_stmt|;
block|}
name|vm_map_unlock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
return|return
operator|(
name|KERN_SUCCESS
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Yet another bastard routine.  */
end_comment

begin_expr_stmt
name|vm_allocate_with_pager
argument_list|(
name|map
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|,
name|pager
argument_list|,
name|poffset
argument_list|,
name|internal
argument_list|)
specifier|register
name|vm_map_t
name|map
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|vm_offset_t
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|register
name|vm_size_t
name|size
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|boolean_t
name|fitit
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_pager_t
name|pager
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|vm_offset_t
name|poffset
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|boolean_t
name|internal
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|vm_object_t
name|object
decl_stmt|;
specifier|register
name|int
name|result
decl_stmt|;
if|if
condition|(
name|map
operator|==
name|VM_MAP_NULL
condition|)
return|return
operator|(
name|KERN_INVALID_ARGUMENT
operator|)
return|;
operator|*
name|addr
operator|=
name|trunc_page
argument_list|(
operator|*
name|addr
argument_list|)
expr_stmt|;
name|size
operator|=
name|round_page
argument_list|(
name|size
argument_list|)
expr_stmt|;
comment|/* 	 *	Lookup the pager/paging-space in the object cache. 	 *	If it's not there, then create a new object and cache 	 *	it. 	 */
name|object
operator|=
name|vm_object_lookup
argument_list|(
name|pager
argument_list|)
expr_stmt|;
name|vm_stat
operator|.
name|lookups
operator|++
expr_stmt|;
if|if
condition|(
name|object
operator|==
name|VM_OBJECT_NULL
condition|)
block|{
name|object
operator|=
name|vm_object_allocate
argument_list|(
name|size
argument_list|)
expr_stmt|;
name|vm_object_enter
argument_list|(
name|object
argument_list|,
name|pager
argument_list|)
expr_stmt|;
block|}
else|else
name|vm_stat
operator|.
name|hits
operator|++
expr_stmt|;
name|object
operator|->
name|internal
operator|=
name|internal
expr_stmt|;
name|result
operator|=
name|vm_map_find
argument_list|(
name|map
argument_list|,
name|object
argument_list|,
name|poffset
argument_list|,
name|addr
argument_list|,
name|size
argument_list|,
name|fitit
argument_list|)
expr_stmt|;
if|if
condition|(
name|result
operator|!=
name|KERN_SUCCESS
condition|)
name|vm_object_deallocate
argument_list|(
name|object
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|pager
operator|!=
name|vm_pager_null
condition|)
name|vm_object_setpager
argument_list|(
name|object
argument_list|,
name|pager
argument_list|,
operator|(
name|vm_offset_t
operator|)
literal|0
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
return|return
operator|(
name|result
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * XXX: this routine belongs in vm_map.c.  *  * Returns TRUE if the range [start - end) is allocated in either  * a single entry (single_entry == TRUE) or multiple contiguous  * entries (single_entry == FALSE).  *  * start and end should be page aligned.  */
end_comment

begin_function
name|boolean_t
name|vm_map_is_allocated
parameter_list|(
name|map
parameter_list|,
name|start
parameter_list|,
name|end
parameter_list|,
name|single_entry
parameter_list|)
name|vm_map_t
name|map
decl_stmt|;
name|vm_offset_t
name|start
decl_stmt|,
name|end
decl_stmt|;
name|boolean_t
name|single_entry
decl_stmt|;
block|{
name|vm_map_entry_t
name|mapent
decl_stmt|;
specifier|register
name|vm_offset_t
name|nend
decl_stmt|;
name|vm_map_lock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
comment|/* 	 * Start address not in any entry 	 */
if|if
condition|(
operator|!
name|vm_map_lookup_entry
argument_list|(
name|map
argument_list|,
name|start
argument_list|,
operator|&
name|mapent
argument_list|)
condition|)
block|{
name|vm_map_unlock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
return|return
operator|(
name|FALSE
operator|)
return|;
block|}
comment|/* 	 * Find the maximum stretch of contiguously allocated space 	 */
name|nend
operator|=
name|mapent
operator|->
name|end
expr_stmt|;
if|if
condition|(
operator|!
name|single_entry
condition|)
block|{
name|mapent
operator|=
name|mapent
operator|->
name|next
expr_stmt|;
while|while
condition|(
name|mapent
operator|!=
operator|&
name|map
operator|->
name|header
operator|&&
name|mapent
operator|->
name|start
operator|==
name|nend
condition|)
block|{
name|nend
operator|=
name|mapent
operator|->
name|end
expr_stmt|;
name|mapent
operator|=
name|mapent
operator|->
name|next
expr_stmt|;
block|}
block|}
name|vm_map_unlock_read
argument_list|(
name|map
argument_list|)
expr_stmt|;
return|return
operator|(
name|end
operator|<=
name|nend
operator|)
return|;
block|}
end_function

begin_include
include|#
directive|include
file|"../vm/vm_page.h"
end_include

begin_comment
comment|/*  * Doesn't trust the COW bit in the page structure.  * vm_fault can improperly set it.  */
end_comment

begin_function
name|void
name|vm_object_pmap_force_copy
parameter_list|(
name|object
parameter_list|,
name|start
parameter_list|,
name|end
parameter_list|)
specifier|register
name|vm_object_t
name|object
decl_stmt|;
specifier|register
name|vm_offset_t
name|start
decl_stmt|;
specifier|register
name|vm_offset_t
name|end
decl_stmt|;
block|{
specifier|register
name|vm_page_t
name|p
decl_stmt|;
if|if
condition|(
name|object
operator|==
name|VM_OBJECT_NULL
condition|)
return|return;
name|vm_object_lock
argument_list|(
name|object
argument_list|)
expr_stmt|;
name|p
operator|=
operator|(
name|vm_page_t
operator|)
name|queue_first
argument_list|(
operator|&
name|object
operator|->
name|memq
argument_list|)
expr_stmt|;
while|while
condition|(
operator|!
name|queue_end
argument_list|(
operator|&
name|object
operator|->
name|memq
argument_list|,
operator|(
name|queue_entry_t
operator|)
name|p
argument_list|)
condition|)
block|{
if|if
condition|(
name|start
operator|<=
name|p
operator|->
name|offset
operator|&&
name|p
operator|->
name|offset
operator|<
name|end
condition|)
block|{
name|pmap_copy_on_write
argument_list|(
name|VM_PAGE_TO_PHYS
argument_list|(
name|p
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|->
name|copy_on_write
operator|=
name|TRUE
expr_stmt|;
block|}
name|p
operator|=
operator|(
name|vm_page_t
operator|)
name|queue_next
argument_list|(
operator|&
name|p
operator|->
name|listq
argument_list|)
expr_stmt|;
block|}
name|vm_object_unlock
argument_list|(
name|object
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

