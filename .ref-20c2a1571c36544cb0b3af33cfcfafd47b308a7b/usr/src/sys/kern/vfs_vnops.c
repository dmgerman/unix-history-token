begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	vfs_vnops.c	4.20	82/01/25	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/dir.h"
end_include

begin_include
include|#
directive|include
file|"../h/user.h"
end_include

begin_include
include|#
directive|include
file|"../h/filsys.h"
end_include

begin_include
include|#
directive|include
file|"../h/file.h"
end_include

begin_include
include|#
directive|include
file|"../h/conf.h"
end_include

begin_include
include|#
directive|include
file|"../h/inode.h"
end_include

begin_include
include|#
directive|include
file|"../h/reg.h"
end_include

begin_include
include|#
directive|include
file|"../h/acct.h"
end_include

begin_include
include|#
directive|include
file|"../h/mount.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/proc.h"
end_include

begin_comment
comment|/*  * Convert a user supplied file descriptor into a pointer  * to a file structure.  Only task is to check range of the descriptor.  * Critical paths should use the GETF macro, defined in inline.h.  */
end_comment

begin_function
name|struct
name|file
modifier|*
name|getf
parameter_list|(
name|f
parameter_list|)
specifier|register
name|int
name|f
decl_stmt|;
block|{
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
if|if
condition|(
operator|(
name|unsigned
operator|)
name|f
operator|>=
name|NOFILE
operator|||
operator|(
name|fp
operator|=
name|u
operator|.
name|u_ofile
index|[
name|f
index|]
operator|)
operator|==
name|NULL
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EBADF
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
return|return
operator|(
name|fp
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Internal form of close.  * Decrement reference count on  * file structure.  * Also make sure the pipe protocol  * does not constipate.  *  * Decrement reference count on the inode following  * removal to the referencing file structure.  * Call device handler on last close.  * Nouser indicates that the user isn't available to present  * errors to.  */
end_comment

begin_expr_stmt
name|closef
argument_list|(
name|fp
argument_list|,
name|nouser
argument_list|)
specifier|register
expr|struct
name|file
operator|*
name|fp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
specifier|register
name|struct
name|mount
modifier|*
name|mp
decl_stmt|;
name|int
name|flag
decl_stmt|,
name|mode
decl_stmt|;
name|dev_t
name|dev
decl_stmt|;
specifier|register
name|int
function_decl|(
modifier|*
name|cfunc
function_decl|)
parameter_list|()
function_decl|;
if|if
condition|(
name|fp
operator|==
name|NULL
condition|)
return|return;
if|if
condition|(
name|fp
operator|->
name|f_count
operator|>
literal|1
condition|)
block|{
name|fp
operator|->
name|f_count
operator|--
expr_stmt|;
return|return;
block|}
name|flag
operator|=
name|fp
operator|->
name|f_flag
expr_stmt|;
if|if
condition|(
name|flag
operator|&
name|FSOCKET
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
literal|0
expr_stmt|;
comment|/* XXX */
name|soclose
argument_list|(
name|fp
operator|->
name|f_socket
argument_list|,
name|nouser
argument_list|)
expr_stmt|;
if|if
condition|(
name|nouser
operator|==
literal|0
operator|&&
name|u
operator|.
name|u_error
condition|)
return|return;
name|fp
operator|->
name|f_socket
operator|=
literal|0
expr_stmt|;
name|fp
operator|->
name|f_count
operator|=
literal|0
expr_stmt|;
return|return;
block|}
name|ip
operator|=
name|fp
operator|->
name|f_inode
expr_stmt|;
name|dev
operator|=
operator|(
name|dev_t
operator|)
name|ip
operator|->
name|i_un
operator|.
name|i_rdev
expr_stmt|;
name|mode
operator|=
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
expr_stmt|;
name|ilock
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
name|fp
operator|->
name|f_count
operator|=
literal|0
expr_stmt|;
switch|switch
condition|(
name|mode
condition|)
block|{
case|case
name|IFCHR
case|:
name|cfunc
operator|=
name|cdevsw
index|[
name|major
argument_list|(
name|dev
argument_list|)
index|]
operator|.
name|d_close
expr_stmt|;
break|break;
case|case
name|IFBLK
case|:
comment|/* 		 * We don't want to really close the device if it is mounted 		 */
for|for
control|(
name|mp
operator|=
name|mount
init|;
name|mp
operator|<
operator|&
name|mount
index|[
name|NMOUNT
index|]
condition|;
name|mp
operator|++
control|)
if|if
condition|(
name|mp
operator|->
name|m_bufp
operator|!=
name|NULL
operator|&&
name|mp
operator|->
name|m_dev
operator|==
name|dev
condition|)
return|return;
name|cfunc
operator|=
name|bdevsw
index|[
name|major
argument_list|(
name|dev
argument_list|)
index|]
operator|.
name|d_close
expr_stmt|;
break|break;
default|default:
return|return;
block|}
for|for
control|(
name|fp
operator|=
name|file
init|;
name|fp
operator|<
name|fileNFILE
condition|;
name|fp
operator|++
control|)
block|{
if|if
condition|(
name|fp
operator|->
name|f_flag
operator|&
name|FSOCKET
condition|)
continue|continue;
if|if
condition|(
name|fp
operator|->
name|f_count
operator|&&
operator|(
name|ip
operator|=
name|fp
operator|->
name|f_inode
operator|)
operator|&&
name|ip
operator|->
name|i_un
operator|.
name|i_rdev
operator|==
name|dev
operator|&&
operator|(
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
operator|)
operator|==
name|mode
condition|)
return|return;
block|}
if|if
condition|(
name|mode
operator|==
name|IFBLK
condition|)
block|{
comment|/* 		 * On last close of a block device (that isn't mounted) 		 * we must invalidate any in core blocks 		 */
name|bflush
argument_list|(
name|dev
argument_list|)
expr_stmt|;
name|binval
argument_list|(
name|dev
argument_list|)
expr_stmt|;
block|}
call|(
modifier|*
name|cfunc
call|)
argument_list|(
name|dev
argument_list|,
name|flag
argument_list|,
name|fp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Openi called to allow handler  * of special files to initialize and  * validate before actual IO.  */
end_comment

begin_expr_stmt
name|openi
argument_list|(
name|ip
argument_list|,
name|rw
argument_list|)
specifier|register
expr|struct
name|inode
operator|*
name|ip
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|dev_t
name|dev
decl_stmt|;
specifier|register
name|unsigned
name|int
name|maj
decl_stmt|;
name|dev
operator|=
operator|(
name|dev_t
operator|)
name|ip
operator|->
name|i_un
operator|.
name|i_rdev
expr_stmt|;
name|maj
operator|=
name|major
argument_list|(
name|dev
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|ip
operator|->
name|i_mode
operator|&
name|IFMT
condition|)
block|{
case|case
name|IFCHR
case|:
if|if
condition|(
name|maj
operator|>=
name|nchrdev
condition|)
goto|goto
name|bad
goto|;
operator|(
operator|*
name|cdevsw
index|[
name|maj
index|]
operator|.
name|d_open
operator|)
operator|(
name|dev
operator|,
name|rw
operator|)
expr_stmt|;
break|break;
case|case
name|IFBLK
case|:
if|if
condition|(
name|maj
operator|>=
name|nblkdev
condition|)
goto|goto
name|bad
goto|;
operator|(
operator|*
name|bdevsw
index|[
name|maj
index|]
operator|.
name|d_open
operator|)
operator|(
name|dev
operator|,
name|rw
operator|)
expr_stmt|;
block|}
return|return;
name|bad
label|:
name|u
operator|.
name|u_error
operator|=
name|ENXIO
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Check mode permission on inode pointer.  * Mode is READ, WRITE or EXEC.  * In the case of WRITE, the  * read-only status of the file  * system is checked.  * Also in WRITE, prototype text  * segments cannot be written.  * The mode is shifted to select  * the owner/group/other fields.  * The super user is granted all  * permissions.  */
end_comment

begin_expr_stmt
name|access
argument_list|(
name|ip
argument_list|,
name|mode
argument_list|)
specifier|register
expr|struct
name|inode
operator|*
name|ip
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|mode
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|m
expr_stmt|;
name|m
operator|=
name|mode
expr_stmt|;
if|if
condition|(
name|m
operator|==
name|IWRITE
condition|)
block|{
if|if
condition|(
name|getfs
argument_list|(
name|ip
operator|->
name|i_dev
argument_list|)
operator|->
name|s_ronly
operator|!=
literal|0
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|EROFS
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ITEXT
condition|)
comment|/* try to free text */
name|xrele
argument_list|(
name|ip
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|->
name|i_flag
operator|&
name|ITEXT
condition|)
block|{
name|u
operator|.
name|u_error
operator|=
name|ETXTBSY
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
if|if
condition|(
name|u
operator|.
name|u_uid
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|u
operator|.
name|u_uid
operator|!=
name|ip
operator|->
name|i_uid
condition|)
block|{
name|m
operator|>>=
literal|3
expr_stmt|;
if|if
condition|(
name|u
operator|.
name|u_gid
operator|!=
name|ip
operator|->
name|i_gid
condition|)
name|m
operator|>>=
literal|3
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|ip
operator|->
name|i_mode
operator|&
name|m
operator|)
operator|!=
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|u
operator|.
name|u_error
operator|=
name|EACCES
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Look up a pathname and test if  * the resultant inode is owned by the  * current user.  * If not, try for super-user.  * If permission is granted,  * return inode pointer.  */
end_comment

begin_function
name|struct
name|inode
modifier|*
name|owner
parameter_list|()
block|{
specifier|register
name|struct
name|inode
modifier|*
name|ip
decl_stmt|;
name|ip
operator|=
name|namei
argument_list|(
name|uchar
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
if|if
condition|(
name|u
operator|.
name|u_uid
operator|==
name|ip
operator|->
name|i_uid
condition|)
return|return
operator|(
name|ip
operator|)
return|;
if|if
condition|(
name|suser
argument_list|()
condition|)
return|return
operator|(
name|ip
operator|)
return|;
name|iput
argument_list|(
name|ip
argument_list|)
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Test if the current user is the  * super user.  */
end_comment

begin_macro
name|suser
argument_list|()
end_macro

begin_block
block|{
if|if
condition|(
name|u
operator|.
name|u_uid
operator|==
literal|0
condition|)
block|{
name|u
operator|.
name|u_acflag
operator||=
name|ASU
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|u
operator|.
name|u_error
operator|=
name|EPERM
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Allocate a user file descriptor.  */
end_comment

begin_macro
name|ufalloc
argument_list|()
end_macro

begin_block
block|{
specifier|register
name|i
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NOFILE
condition|;
name|i
operator|++
control|)
if|if
condition|(
name|u
operator|.
name|u_ofile
index|[
name|i
index|]
operator|==
name|NULL
condition|)
block|{
name|u
operator|.
name|u_r
operator|.
name|r_val1
operator|=
name|i
expr_stmt|;
name|u
operator|.
name|u_pofile
index|[
name|i
index|]
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|i
operator|)
return|;
block|}
name|u
operator|.
name|u_error
operator|=
name|EMFILE
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
end_block

begin_decl_stmt
name|struct
name|file
modifier|*
name|lastf
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Allocate a user file descriptor  * and a file structure.  * Initialize the descriptor  * to point at the file structure.  */
end_comment

begin_function
name|struct
name|file
modifier|*
name|falloc
parameter_list|()
block|{
specifier|register
name|struct
name|file
modifier|*
name|fp
decl_stmt|;
specifier|register
name|i
expr_stmt|;
name|i
operator|=
name|ufalloc
argument_list|()
expr_stmt|;
if|if
condition|(
name|i
operator|<
literal|0
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
if|if
condition|(
name|lastf
operator|==
literal|0
condition|)
name|lastf
operator|=
name|file
expr_stmt|;
for|for
control|(
name|fp
operator|=
name|lastf
init|;
name|fp
operator|<
name|fileNFILE
condition|;
name|fp
operator|++
control|)
if|if
condition|(
name|fp
operator|->
name|f_count
operator|==
literal|0
condition|)
goto|goto
name|slot
goto|;
for|for
control|(
name|fp
operator|=
name|file
init|;
name|fp
operator|<
name|lastf
condition|;
name|fp
operator|++
control|)
if|if
condition|(
name|fp
operator|->
name|f_count
operator|==
literal|0
condition|)
goto|goto
name|slot
goto|;
name|tablefull
argument_list|(
literal|"file"
argument_list|)
expr_stmt|;
name|u
operator|.
name|u_error
operator|=
name|ENFILE
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
name|slot
label|:
name|u
operator|.
name|u_ofile
index|[
name|i
index|]
operator|=
name|fp
expr_stmt|;
name|fp
operator|->
name|f_count
operator|++
expr_stmt|;
name|fp
operator|->
name|f_offset
operator|=
literal|0
expr_stmt|;
name|fp
operator|->
name|f_inode
operator|=
literal|0
expr_stmt|;
name|lastf
operator|=
name|fp
operator|+
literal|1
expr_stmt|;
return|return
operator|(
name|fp
operator|)
return|;
block|}
end_function

end_unit

