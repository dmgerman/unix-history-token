begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	raw_cb.c	4.2	82/02/01	*/
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/mtpr.h"
end_include

begin_include
include|#
directive|include
file|"../net/in.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/if.h"
end_include

begin_include
include|#
directive|include
file|"../net/raw_cb.h"
end_include

begin_include
include|#
directive|include
file|"/usr/include/errno.h"
end_include

begin_comment
comment|/*  * Routines to manage the raw protocol control blocks.   *  * TODO:  *	hash lookups by protocol family/protocol + address family  *	take care of unique address problems per AF  */
end_comment

begin_comment
comment|/*  * Allocate a control block and a nominal amount  * of buffer space for the socket.  */
end_comment

begin_expr_stmt
name|raw_attach
argument_list|(
name|so
argument_list|,
name|addr
argument_list|)
specifier|register
expr|struct
name|socket
operator|*
name|so
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|struct
name|sockaddr
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
specifier|register
name|struct
name|rawcb
modifier|*
name|rp
decl_stmt|;
name|struct
name|ifnet
modifier|*
name|ifp
init|=
name|ifnet
decl_stmt|;
name|COUNT
argument_list|(
name|RAW_ATTACH
argument_list|)
expr_stmt|;
comment|/* 	 * Should we verify address not already in use? 	 * Some say yes, others no. 	 */
if|if
condition|(
name|addr
condition|)
switch|switch
condition|(
name|addr
operator|->
name|sa_family
condition|)
block|{
case|case
name|AF_INET
case|:
block|{
specifier|register
name|struct
name|sockaddr_in
modifier|*
name|sin
init|=
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|addr
decl_stmt|;
if|if
condition|(
name|ifnet
operator|&&
name|sin
operator|->
name|sin_addr
operator|.
name|s_addr
operator|==
literal|0
condition|)
name|sin
operator|->
name|sin_addr
operator|=
name|ifnet
operator|->
name|if_addr
expr_stmt|;
name|ifp
operator|=
name|if_ifwithaddr
argument_list|(
name|sin
operator|->
name|sin_addr
argument_list|)
expr_stmt|;
break|break;
block|}
default|default:
return|return
operator|(
name|EAFNOSUPPORT
operator|)
return|;
block|}
if|if
condition|(
name|ifp
operator|==
literal|0
condition|)
return|return
operator|(
name|EADDRNOTAVAIL
operator|)
return|;
name|m
operator|=
name|m_getclr
argument_list|(
name|M_DONTWAIT
argument_list|)
expr_stmt|;
if|if
condition|(
name|m
operator|==
literal|0
condition|)
return|return
operator|(
name|ENOBUFS
operator|)
return|;
if|if
condition|(
name|sbreserve
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
name|RAWSNDQ
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|bad
goto|;
if|if
condition|(
name|sbreserve
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|,
name|RAWRCVQ
argument_list|)
operator|==
literal|0
condition|)
goto|goto
name|bad2
goto|;
name|rp
operator|=
name|mtod
argument_list|(
name|m
argument_list|,
expr|struct
name|rawcb
operator|*
argument_list|)
expr_stmt|;
name|rp
operator|->
name|rcb_socket
operator|=
name|so
expr_stmt|;
name|insque
argument_list|(
name|rp
argument_list|,
operator|&
name|rawcb
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_pcb
operator|=
operator|(
name|caddr_t
operator|)
name|rp
expr_stmt|;
name|rp
operator|->
name|rcb_pcb
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|addr
condition|)
name|bcopy
argument_list|(
name|addr
argument_list|,
operator|&
name|so
operator|->
name|so_addr
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|addr
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
name|bad2
label|:
name|sbrelease
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|)
expr_stmt|;
name|bad
label|:
operator|(
name|void
operator|)
name|m_free
argument_list|(
name|m
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOBUFS
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Detach the raw connection block and discard  * socket resources.  */
end_comment

begin_expr_stmt
name|raw_detach
argument_list|(
name|rp
argument_list|)
specifier|register
expr|struct
name|rawcb
operator|*
name|rp
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|struct
name|socket
modifier|*
name|so
init|=
name|rp
operator|->
name|rcb_socket
decl_stmt|;
name|COUNT
argument_list|(
name|RAW_DETACH
argument_list|)
expr_stmt|;
name|so
operator|->
name|so_pcb
operator|=
literal|0
expr_stmt|;
name|sofree
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|remque
argument_list|(
name|rp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|m_freem
argument_list|(
name|dtom
argument_list|(
name|rp
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Disconnect and possibly release resources.  */
end_comment

begin_macro
name|raw_disconnect
argument_list|(
argument|rp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|rawcb
modifier|*
name|rp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|RAW_DISCONNECT
argument_list|)
expr_stmt|;
name|rp
operator|->
name|rcb_flags
operator|&=
operator|~
name|RAW_ADDR
expr_stmt|;
if|if
condition|(
name|rp
operator|->
name|rcb_socket
operator|->
name|so_state
operator|&
name|SS_USERGONE
condition|)
name|raw_detach
argument_list|(
name|rp
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Associate a peer's address with a  * raw connection block.  */
end_comment

begin_macro
name|raw_connaddr
argument_list|(
argument|rp
argument_list|,
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|rawcb
modifier|*
name|rp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr
modifier|*
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|COUNT
argument_list|(
name|RAW_CONNADDR
argument_list|)
expr_stmt|;
name|bcopy
argument_list|(
name|addr
argument_list|,
operator|&
name|rp
operator|->
name|rcb_addr
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr
argument_list|)
argument_list|)
expr_stmt|;
name|rp
operator|->
name|rcb_flags
operator||=
name|RAW_ADDR
expr_stmt|;
block|}
end_block

end_unit

