begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* tcp_usrreq.c 1.49 82/01/18 */
end_comment

begin_include
include|#
directive|include
file|"../h/param.h"
end_include

begin_include
include|#
directive|include
file|"../h/systm.h"
end_include

begin_include
include|#
directive|include
file|"../h/mbuf.h"
end_include

begin_include
include|#
directive|include
file|"../h/socket.h"
end_include

begin_include
include|#
directive|include
file|"../h/socketvar.h"
end_include

begin_include
include|#
directive|include
file|"../h/protosw.h"
end_include

begin_include
include|#
directive|include
file|"../net/in.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_pcb.h"
end_include

begin_include
include|#
directive|include
file|"../net/in_systm.h"
end_include

begin_include
include|#
directive|include
file|"../net/if.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip.h"
end_include

begin_include
include|#
directive|include
file|"../net/ip_var.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_fsm.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_seq.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_timer.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_var.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcpip.h"
end_include

begin_include
include|#
directive|include
file|"../net/tcp_debug.h"
end_include

begin_include
include|#
directive|include
file|"../errno.h"
end_include

begin_comment
comment|/*  * TCP protocol interface to socket abstraction.  */
end_comment

begin_decl_stmt
specifier|extern
name|char
modifier|*
name|tcpstates
index|[]
decl_stmt|;
end_decl_stmt

begin_function_decl
name|struct
name|tcpcb
modifier|*
name|tcp_newtcpcb
parameter_list|()
function_decl|;
end_function_decl

begin_comment
comment|/*  * Process a TCP user request for TCP tb.  If this is a send request  * then m is the mbuf chain of send data.  If this is a timer expiration  * (called from the software clock routine), then timertype tells which timer.  */
end_comment

begin_macro
name|tcp_usrreq
argument_list|(
argument|so
argument_list|,
argument|req
argument_list|,
argument|m
argument_list|,
argument|addr
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|req
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|mbuf
modifier|*
name|m
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|addr
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|inpcb
modifier|*
name|inp
init|=
name|sotoinpcb
argument_list|(
name|so
argument_list|)
decl_stmt|;
specifier|register
name|struct
name|tcpcb
modifier|*
name|tp
decl_stmt|;
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
name|int
name|ostate
decl_stmt|;
name|COUNT
argument_list|(
name|TCP_USRREQ
argument_list|)
expr_stmt|;
comment|/* 	 * When a TCP is attached to a socket, then there will be 	 * a (struct inpcb) pointed at by the socket, and this 	 * structure will point at a subsidary (struct tcpcb). 	 * The normal sequence of events is: 	 *	PRU_ATTACH		creating these structures 	 *	PRU_CONNECT		connecting to a remote peer 	 *	(PRU_SEND|PRU_RCVD)*	exchanging data 	 *	PRU_DISCONNECT		disconnecting from remote peer 	 *	PRU_DETACH		deleting the structures 	 * With the operations from PRU_CONNECT through PRU_DISCONNECT 	 * possible repeated several times. 	 * 	 * MULTIPLE CONNECTS ARE NOT YET IMPLEMENTED. 	 */
if|if
condition|(
name|inp
operator|==
literal|0
operator|&&
name|req
operator|!=
name|PRU_ATTACH
condition|)
block|{
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|EINVAL
operator|)
return|;
comment|/* XXX */
block|}
if|if
condition|(
name|inp
condition|)
block|{
name|tp
operator|=
name|intotcpcb
argument_list|(
name|inp
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|KPROF
name|tcp_acounts
index|[
name|tp
operator|->
name|t_state
index|]
index|[
name|req
index|]
operator|++
expr_stmt|;
endif|#
directive|endif
name|ostate
operator|=
name|tp
operator|->
name|t_state
expr_stmt|;
block|}
switch|switch
condition|(
name|req
condition|)
block|{
comment|/* 	 * TCP attaches to socket via PRU_ATTACH, reserving space, 	 * and internet and TCP control blocks. 	 * If the socket is to receive connections, 	 * then the LISTEN state is entered. 	 */
case|case
name|PRU_ATTACH
case|:
if|if
condition|(
name|inp
condition|)
block|{
name|error
operator|=
name|EISCONN
expr_stmt|;
break|break;
block|}
name|error
operator|=
name|tcp_attach
argument_list|(
name|so
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
break|break;
if|if
condition|(
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_DONTLINGER
operator|)
operator|==
literal|0
condition|)
name|so
operator|->
name|so_linger
operator|=
name|TCP_LINGERTIME
expr_stmt|;
name|tp
operator|=
name|sototcpcb
argument_list|(
name|so
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * PRU_DETACH detaches the TCP protocol from the socket. 	 * If the protocol state is non-embryonic, then can't 	 * do this directly: have to initiate a PRU_DISCONNECT, 	 * which may finish later; embryonic TCB's can just 	 * be discarded here. 	 */
case|case
name|PRU_DETACH
case|:
if|if
condition|(
name|tp
operator|->
name|t_state
operator|>
name|TCPS_LISTEN
condition|)
name|tcp_disconnect
argument_list|(
name|tp
argument_list|)
expr_stmt|;
else|else
block|{
name|tcp_close
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|=
literal|0
expr_stmt|;
block|}
break|break;
comment|/* 	 * Initiate connection to peer. 	 * Create a template for use in transmissions on this connection. 	 * Enter SYN_SENT state, and mark socket as connecting. 	 * Start keep-alive timer, and seed output sequence space. 	 * Send initial segment on connection. 	 */
case|case
name|PRU_CONNECT
case|:
name|error
operator|=
name|in_pcbconnect
argument_list|(
name|inp
argument_list|,
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
break|break;
name|tp
operator|->
name|t_template
operator|=
name|tcp_template
argument_list|(
name|tp
argument_list|)
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_template
operator|==
literal|0
condition|)
block|{
name|in_pcbdisconnect
argument_list|(
name|inp
argument_list|)
expr_stmt|;
name|error
operator|=
name|ENOBUFS
expr_stmt|;
break|break;
block|}
name|soisconnecting
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_state
operator|=
name|TCPS_SYN_SENT
expr_stmt|;
name|tp
operator|->
name|t_timer
index|[
name|TCPT_KEEP
index|]
operator|=
name|TCPTV_KEEP
expr_stmt|;
name|tp
operator|->
name|iss
operator|=
name|tcp_iss
expr_stmt|;
name|tcp_iss
operator|+=
name|TCP_ISSINCR
operator|/
literal|2
expr_stmt|;
name|tcp_sendseqinit
argument_list|(
name|tp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Initiate disconnect from peer. 	 * If connection never passed embryonic stage, just drop; 	 * else if don't need to let data drain, then can just drop anyways, 	 * else have to begin TCP shutdown process: mark socket disconnecting, 	 * drain unread data, state switch to reflect user close, and 	 * send segment (e.g. FIN) to peer.  Socket will be really disconnected 	 * when peer sends FIN and acks ours. 	 * 	 * SHOULD IMPLEMENT LATER PRU_CONNECT VIA REALLOC TCPCB. 	 */
case|case
name|PRU_DISCONNECT
case|:
name|tcp_disconnect
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Accept a connection.  Essentially all the work is 	 * done at higher levels; just return the address 	 * of the peer, storing through addr. 	 */
case|case
name|PRU_ACCEPT
case|:
name|in_pcbconnaddr
argument_list|(
name|inp
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Mark the connection as being incapable of further output. 	 */
case|case
name|PRU_SHUTDOWN
case|:
name|socantsendmore
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|tcp_usrclosed
argument_list|(
name|tp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * After a receive, possibly send window update to peer. 	 */
case|case
name|PRU_RCVD
case|:
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Do a send by putting data in output queue and updating urgent 	 * marker if URG set.  Possibly send more data. 	 */
case|case
name|PRU_SEND
case|:
name|sbappend
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
name|m
argument_list|)
expr_stmt|;
comment|/* 		if (tp->t_flags& TF_PUSH) 			tp->snd_end = tp->snd_una + so->so_snd.sb_cc;  */
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
comment|/* 	 * Abort the TCP. 	 */
case|case
name|PRU_ABORT
case|:
name|tcp_drop
argument_list|(
name|tp
argument_list|,
name|ECONNABORTED
argument_list|)
expr_stmt|;
break|break;
comment|/* SOME AS YET UNIMPLEMENTED HOOKS */
case|case
name|PRU_CONTROL
case|:
name|error
operator|=
name|EOPNOTSUPP
expr_stmt|;
break|break;
case|case
name|PRU_SENSE
case|:
name|error
operator|=
name|EOPNOTSUPP
expr_stmt|;
break|break;
comment|/* END UNIMPLEMENTED HOOKS */
case|case
name|PRU_RCVOOB
case|:
if|if
condition|(
name|so
operator|->
name|so_oobmark
operator|==
literal|0
operator|&&
operator|(
name|so
operator|->
name|so_state
operator|&
name|SS_RCVATMARK
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|EINVAL
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|(
name|tp
operator|->
name|t_oobflags
operator|&
name|TCPOOB_HAVEDATA
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|EWOULDBLOCK
expr_stmt|;
break|break;
block|}
operator|*
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
operator|=
name|tp
operator|->
name|t_iobc
expr_stmt|;
break|break;
case|case
name|PRU_SENDOOB
case|:
ifdef|#
directive|ifdef
name|TCPTRUEOOB
if|if
condition|(
name|tp
operator|->
name|t_flags
operator|&
name|TF_DOOOB
condition|)
block|{
name|tp
operator|->
name|t_oobseq
operator|++
expr_stmt|;
name|tp
operator|->
name|t_oobc
operator|=
operator|*
name|mtod
argument_list|(
name|m
argument_list|,
name|caddr_t
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_oobmark
operator|=
name|tp
operator|->
name|snd_una
operator|+
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
expr_stmt|;
name|printf
argument_list|(
literal|"sendoob seq now %x oobc %x\n"
argument_list|,
name|tp
operator|->
name|t_oobseq
argument_list|,
name|tp
operator|->
name|t_oobc
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_oobflags
operator||=
name|TCPOOB_NEEDACK
expr_stmt|;
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|if
condition|(
name|sbspace
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|)
operator|<
operator|-
literal|512
condition|)
block|{
name|error
operator|=
name|ENOBUFS
expr_stmt|;
break|break;
block|}
name|tp
operator|->
name|snd_up
operator|=
name|tp
operator|->
name|snd_una
operator|+
name|so
operator|->
name|so_snd
operator|.
name|sb_cc
operator|+
literal|1
expr_stmt|;
name|sbappend
argument_list|(
operator|&
name|so
operator|->
name|so_snd
argument_list|,
name|m
argument_list|)
expr_stmt|;
comment|/* 		if (tp->t_flags& TF_PUSH) 			tp->snd_end = tp->snd_una + so->so_snd.sb_cc;  */
name|tp
operator|->
name|t_force
operator|=
literal|1
expr_stmt|;
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
name|tp
operator|->
name|t_force
operator|=
literal|0
expr_stmt|;
break|break;
comment|/* 	 * TCP slow timer went off; going through this 	 * routine for tracing's sake. 	 */
case|case
name|PRU_SLOWTIMO
case|:
name|tcp_timers
argument_list|(
name|tp
argument_list|,
operator|(
name|int
operator|)
name|addr
argument_list|)
expr_stmt|;
name|req
operator||=
operator|(
name|int
operator|)
name|addr
operator|<<
literal|8
expr_stmt|;
comment|/* for debug's sake */
break|break;
default|default:
name|panic
argument_list|(
literal|"tcp_usrreq"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tp
operator|&&
operator|(
name|so
operator|->
name|so_options
operator|&
name|SO_DEBUG
operator|)
condition|)
name|tcp_trace
argument_list|(
name|TA_USER
argument_list|,
name|ostate
argument_list|,
name|tp
argument_list|,
operator|(
expr|struct
name|tcpiphdr
operator|*
operator|)
literal|0
argument_list|,
name|req
argument_list|)
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
return|return
operator|(
name|error
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Attach TCP protocol to socket, allocating  * internet protocol control block, tcp control block,  * bufer space, and entering LISTEN state if to accept connections.  */
end_comment

begin_macro
name|tcp_attach
argument_list|(
argument|so
argument_list|,
argument|sa
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|socket
modifier|*
name|so
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockaddr
modifier|*
name|sa
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|tcpcb
modifier|*
name|tp
decl_stmt|;
name|struct
name|inpcb
modifier|*
name|inp
decl_stmt|;
name|int
name|error
decl_stmt|;
name|error
operator|=
name|in_pcbattach
argument_list|(
name|so
argument_list|,
operator|&
name|tcb
argument_list|,
literal|2048
argument_list|,
literal|2048
argument_list|,
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|sa
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
operator|(
name|error
operator|)
return|;
name|inp
operator|=
operator|(
expr|struct
name|inpcb
operator|*
operator|)
name|so
operator|->
name|so_pcb
expr_stmt|;
name|tp
operator|=
name|tcp_newtcpcb
argument_list|(
name|inp
argument_list|)
expr_stmt|;
if|if
condition|(
name|so
operator|->
name|so_options
operator|&
name|SO_ACCEPTCONN
condition|)
block|{
if|if
condition|(
name|tp
operator|==
literal|0
condition|)
block|{
name|in_pcbdetach
argument_list|(
name|inp
argument_list|)
expr_stmt|;
return|return
operator|(
name|ENOBUFS
operator|)
return|;
block|}
name|tp
operator|->
name|t_state
operator|=
name|TCPS_LISTEN
expr_stmt|;
block|}
else|else
name|tp
operator|->
name|t_state
operator|=
name|TCPS_CLOSED
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Initiate (or continue) disconnect.  * If embryonic state, just send reset (once).  * If not in ``let data drain'' option, just drop.  * Otherwise (hard), mark socket disconnecting and drop  * current input data; switch states based on user close, and  * send segment to peer (with FIN).  */
end_comment

begin_macro
name|tcp_disconnect
argument_list|(
argument|tp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tcpcb
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|struct
name|socket
modifier|*
name|so
init|=
name|tp
operator|->
name|t_inpcb
operator|->
name|inp_socket
decl_stmt|;
if|if
condition|(
name|tp
operator|->
name|t_state
operator|<
name|TCPS_ESTABLISHED
condition|)
name|tcp_close
argument_list|(
name|tp
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|so
operator|->
name|so_linger
operator|==
literal|0
condition|)
name|tcp_drop
argument_list|(
name|tp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|else
block|{
name|soisdisconnecting
argument_list|(
name|so
argument_list|)
expr_stmt|;
name|sbflush
argument_list|(
operator|&
name|so
operator|->
name|so_rcv
argument_list|)
expr_stmt|;
name|tcp_usrclosed
argument_list|(
name|tp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|tcp_output
argument_list|(
name|tp
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * User issued close, and wish to trail through shutdown states:  * if never received SYN, just forget it.  If got a SYN from peer,  * but haven't sent FIN, then go to FIN_WAIT_1 state to send peer a FIN.  * If already got a FIN from peer, then almost done; go to LAST_ACK  * state.  In all other cases, have already sent FIN to peer (e.g.  * after PRU_SHUTDOWN), and just have to play tedious game waiting  * for peer to send FIN or not respond to keep-alives, etc.  */
end_comment

begin_macro
name|tcp_usrclosed
argument_list|(
argument|tp
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|tcpcb
modifier|*
name|tp
decl_stmt|;
end_decl_stmt

begin_block
block|{
switch|switch
condition|(
name|tp
operator|->
name|t_state
condition|)
block|{
case|case
name|TCPS_LISTEN
case|:
case|case
name|TCPS_SYN_SENT
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_CLOSED
expr_stmt|;
name|tcp_close
argument_list|(
name|tp
argument_list|)
expr_stmt|;
break|break;
case|case
name|TCPS_SYN_RECEIVED
case|:
case|case
name|TCPS_ESTABLISHED
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_FIN_WAIT_1
expr_stmt|;
break|break;
case|case
name|TCPS_CLOSE_WAIT
case|:
name|tp
operator|->
name|t_state
operator|=
name|TCPS_LAST_ACK
expr_stmt|;
break|break;
block|}
block|}
end_block

end_unit

