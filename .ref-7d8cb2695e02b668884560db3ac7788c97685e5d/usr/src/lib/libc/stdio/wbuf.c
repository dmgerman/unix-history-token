begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1990, 1993  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * Chris Torek.  *  * %sccs.include.redist.c%  */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|LIBC_SCCS
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|lint
argument_list|)
end_if

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)wbuf.c	8.1 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* LIBC_SCCS and not lint */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"local.h"
end_include

begin_comment
comment|/*  * Write the given character into the (probably full) buffer for  * the given file.  Flush the buffer out if it is or becomes full,  * or if c=='\n' and the file is line buffered.  */
end_comment

begin_expr_stmt
name|__swbuf
argument_list|(
name|c
argument_list|,
name|fp
argument_list|)
specifier|register
name|int
name|c
expr_stmt|;
end_expr_stmt

begin_decl_stmt
specifier|register
name|FILE
modifier|*
name|fp
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|n
decl_stmt|;
comment|/* 	 * In case we cannot write, or longjmp takes us out early, 	 * make sure _w is 0 (if fully- or un-buffered) or -_bf._size 	 * (if line buffered) so that we will get called again. 	 * If we did not do this, a sufficient number of putc() 	 * calls might wrap _w from negative to positive. 	 */
name|fp
operator|->
name|_w
operator|=
name|fp
operator|->
name|_lbfsize
expr_stmt|;
if|if
condition|(
name|cantwrite
argument_list|(
name|fp
argument_list|)
condition|)
return|return
operator|(
name|EOF
operator|)
return|;
name|c
operator|=
operator|(
name|unsigned
name|char
operator|)
name|c
expr_stmt|;
comment|/* 	 * If it is completely full, flush it out.  Then, in any case, 	 * stuff c into the buffer.  If this causes the buffer to fill 	 * completely, or if c is '\n' and the file is line buffered, 	 * flush it (perhaps a second time).  The second flush will always 	 * happen on unbuffered streams, where _bf._size==1; fflush() 	 * guarantees that putc() will always call wbuf() by setting _w 	 * to 0, so we need not do anything else. 	 */
name|n
operator|=
name|fp
operator|->
name|_p
operator|-
name|fp
operator|->
name|_bf
operator|.
name|_base
expr_stmt|;
if|if
condition|(
name|n
operator|>=
name|fp
operator|->
name|_bf
operator|.
name|_size
condition|)
block|{
if|if
condition|(
name|fflush
argument_list|(
name|fp
argument_list|)
condition|)
return|return
operator|(
name|EOF
operator|)
return|;
name|n
operator|=
literal|0
expr_stmt|;
block|}
name|fp
operator|->
name|_w
operator|--
expr_stmt|;
operator|*
name|fp
operator|->
name|_p
operator|++
operator|=
name|c
expr_stmt|;
if|if
condition|(
operator|++
name|n
operator|==
name|fp
operator|->
name|_bf
operator|.
name|_size
operator|||
operator|(
name|fp
operator|->
name|_flags
operator|&
name|__SLBF
operator|&&
name|c
operator|==
literal|'\n'
operator|)
condition|)
if|if
condition|(
name|fflush
argument_list|(
name|fp
argument_list|)
condition|)
return|return
operator|(
name|EOF
operator|)
return|;
return|return
operator|(
name|c
operator|)
return|;
block|}
end_block

end_unit

