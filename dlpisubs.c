begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * This code is derived from code formerly in pcap-dlpi.c, originally  * contributed by Atanu Ghosh (atanu@cs.ucl.ac.uk), University College  * London, and subsequently modified by Guy Harris (guy@alum.mit.edu),  * Mark Pizzolato<List-tcpdump-workers@subscriptions.pizzolato.net>,  * Mark C. Brown (mbrown@hp.com), and Sagun Shakya<Sagun.Shakya@Sun.COM>.  */
end_comment

begin_comment
comment|/*  * This file contains dlpi/libdlpi related common functions used  * by pcap-[dlpi,libdlpi].c.  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|DL_IPATM
end_ifndef

begin_define
define|#
directive|define
name|DL_IPATM
value|0x12
end_define

begin_comment
comment|/* ATM Classical IP interface */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
end_ifdef

begin_comment
comment|/* 	 * Size of a bufmod chunk to pass upstream; that appears to be the 	 * biggest value to which you can set it, and setting it to that value 	 * (which is bigger than what appears to be the Solaris default of 8192) 	 * reduces the number of packet drops. 	 */
end_comment

begin_define
define|#
directive|define
name|CHUNKSIZE
value|65536
end_define

begin_comment
comment|/* 	 * Size of the buffer to allocate for packet data we read; it must be 	 * large enough to hold a chunk. 	 */
end_comment

begin_define
define|#
directive|define
name|PKTBUFSIZE
value|CHUNKSIZE
end_define

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* HAVE_SYS_BUFMOD_H */
end_comment

begin_comment
comment|/* 	 * Size of the buffer to allocate for packet data we read; this is 	 * what the value used to be - there's no particular reason why it 	 * should be tied to MAXDLBUF, but we'll leave it as this for now. 	 */
end_comment

begin_define
define|#
directive|define
name|MAXDLBUF
value|8192
end_define

begin_define
define|#
directive|define
name|PKTBUFSIZE
value|(MAXDLBUF * sizeof(bpf_u_int32))
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/bufmod.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/dlpi.h>
end_include

begin_include
include|#
directive|include
file|<sys/stream.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<memory.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<stropts.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_LIBDLPI
end_ifdef

begin_include
include|#
directive|include
file|<libdlpi.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"pcap-int.h"
end_include

begin_include
include|#
directive|include
file|"dlpisubs.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
end_ifdef

begin_function_decl
specifier|static
name|void
name|pcap_stream_err
parameter_list|(
specifier|const
name|char
modifier|*
parameter_list|,
name|int
parameter_list|,
name|char
modifier|*
parameter_list|)
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Get the packet statistics.  */
end_comment

begin_function
name|int
name|pcap_stats_dlpi
parameter_list|(
name|pcap_t
modifier|*
name|p
parameter_list|,
name|struct
name|pcap_stat
modifier|*
name|ps
parameter_list|)
block|{
name|struct
name|pcap_dlpi
modifier|*
name|pd
init|=
name|p
operator|->
name|priv
decl_stmt|;
comment|/* 	 * "ps_recv" counts packets handed to the filter, not packets 	 * that passed the filter.  As filtering is done in userland, 	 * this would not include packets dropped because we ran out 	 * of buffer space; in order to make this more like other 	 * platforms (Linux 2.4 and later, BSDs with BPF), where the 	 * "packets received" count includes packets received but dropped 	 * due to running out of buffer space, and to keep from confusing 	 * applications that, for example, compute packet drop percentages, 	 * we also make it count packets dropped by "bufmod" (otherwise we 	 * might run the risk of the packet drop count being bigger than 	 * the received-packet count). 	 * 	 * "ps_drop" counts packets dropped by "bufmod" because of 	 * flow control requirements or resource exhaustion; it doesn't 	 * count packets dropped by the interface driver, or packets 	 * dropped upstream.  As filtering is done in userland, it counts 	 * packets regardless of whether they would've passed the filter. 	 * 	 * These statistics don't include packets not yet read from 	 * the kernel by libpcap, but they may include packets not 	 * yet read from libpcap by the application. 	 */
operator|*
name|ps
operator|=
name|pd
operator|->
name|stat
expr_stmt|;
comment|/* 	 * Add in the drop count, as per the above comment. 	 */
name|ps
operator|->
name|ps_recv
operator|+=
name|ps
operator|->
name|ps_drop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Loop through the packets and call the callback for each packet.  * Return the number of packets read.  */
end_comment

begin_function
name|int
name|pcap_process_pkts
parameter_list|(
name|pcap_t
modifier|*
name|p
parameter_list|,
name|pcap_handler
name|callback
parameter_list|,
name|u_char
modifier|*
name|user
parameter_list|,
name|int
name|count
parameter_list|,
name|u_char
modifier|*
name|bufp
parameter_list|,
name|int
name|len
parameter_list|)
block|{
name|struct
name|pcap_dlpi
modifier|*
name|pd
init|=
name|p
operator|->
name|priv
decl_stmt|;
name|int
name|n
decl_stmt|,
name|caplen
decl_stmt|,
name|origlen
decl_stmt|;
name|u_char
modifier|*
name|ep
decl_stmt|,
modifier|*
name|pk
decl_stmt|;
name|struct
name|pcap_pkthdr
name|pkthdr
decl_stmt|;
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
name|struct
name|sb_hdr
modifier|*
name|sbp
decl_stmt|;
ifdef|#
directive|ifdef
name|LBL_ALIGN
name|struct
name|sb_hdr
name|sbhdr
decl_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* Loop through packets */
name|ep
operator|=
name|bufp
operator|+
name|len
expr_stmt|;
name|n
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
while|while
condition|(
name|bufp
operator|<
name|ep
condition|)
block|{
comment|/* 		 * Has "pcap_breakloop()" been called? 		 * If so, return immediately - if we haven't read any 		 * packets, clear the flag and return -2 to indicate 		 * that we were told to break out of the loop, otherwise 		 * leave the flag set, so that the *next* call will break 		 * out of the loop without having read any packets, and 		 * return the number of packets we've processed so far. 		 */
if|if
condition|(
name|p
operator|->
name|break_loop
condition|)
block|{
if|if
condition|(
name|n
operator|==
literal|0
condition|)
block|{
name|p
operator|->
name|break_loop
operator|=
literal|0
expr_stmt|;
return|return
operator|(
operator|-
literal|2
operator|)
return|;
block|}
else|else
block|{
name|p
operator|->
name|bp
operator|=
name|bufp
expr_stmt|;
name|p
operator|->
name|cc
operator|=
name|ep
operator|-
name|bufp
expr_stmt|;
return|return
operator|(
name|n
operator|)
return|;
block|}
block|}
ifdef|#
directive|ifdef
name|LBL_ALIGN
if|if
condition|(
operator|(
name|long
operator|)
name|bufp
operator|&
literal|3
condition|)
block|{
name|sbp
operator|=
operator|&
name|sbhdr
expr_stmt|;
name|memcpy
argument_list|(
name|sbp
argument_list|,
name|bufp
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|sbp
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
endif|#
directive|endif
name|sbp
operator|=
operator|(
expr|struct
name|sb_hdr
operator|*
operator|)
name|bufp
expr_stmt|;
name|pd
operator|->
name|stat
operator|.
name|ps_drop
operator|=
name|sbp
operator|->
name|sbh_drops
expr_stmt|;
name|pk
operator|=
name|bufp
operator|+
sizeof|sizeof
argument_list|(
operator|*
name|sbp
argument_list|)
expr_stmt|;
name|bufp
operator|+=
name|sbp
operator|->
name|sbh_totlen
expr_stmt|;
name|origlen
operator|=
name|sbp
operator|->
name|sbh_origlen
expr_stmt|;
name|caplen
operator|=
name|sbp
operator|->
name|sbh_msglen
expr_stmt|;
else|#
directive|else
name|origlen
operator|=
name|len
expr_stmt|;
name|caplen
operator|=
name|min
argument_list|(
name|p
operator|->
name|snapshot
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|pk
operator|=
name|bufp
expr_stmt|;
name|bufp
operator|+=
name|caplen
expr_stmt|;
endif|#
directive|endif
operator|++
name|pd
operator|->
name|stat
operator|.
name|ps_recv
expr_stmt|;
if|if
condition|(
name|bpf_filter
argument_list|(
name|p
operator|->
name|fcode
operator|.
name|bf_insns
argument_list|,
name|pk
argument_list|,
name|origlen
argument_list|,
name|caplen
argument_list|)
condition|)
block|{
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
name|pkthdr
operator|.
name|ts
operator|.
name|tv_sec
operator|=
name|sbp
operator|->
name|sbh_timestamp
operator|.
name|tv_sec
expr_stmt|;
name|pkthdr
operator|.
name|ts
operator|.
name|tv_usec
operator|=
name|sbp
operator|->
name|sbh_timestamp
operator|.
name|tv_usec
expr_stmt|;
else|#
directive|else
operator|(
name|void
operator|)
name|gettimeofday
argument_list|(
operator|&
name|pkthdr
operator|.
name|ts
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|pkthdr
operator|.
name|len
operator|=
name|origlen
expr_stmt|;
name|pkthdr
operator|.
name|caplen
operator|=
name|caplen
expr_stmt|;
comment|/* Insure caplen does not exceed snapshot */
if|if
condition|(
name|pkthdr
operator|.
name|caplen
operator|>
operator|(
name|bpf_u_int32
operator|)
name|p
operator|->
name|snapshot
condition|)
name|pkthdr
operator|.
name|caplen
operator|=
operator|(
name|bpf_u_int32
operator|)
name|p
operator|->
name|snapshot
expr_stmt|;
call|(
modifier|*
name|callback
call|)
argument_list|(
name|user
argument_list|,
operator|&
name|pkthdr
argument_list|,
name|pk
argument_list|)
expr_stmt|;
if|if
condition|(
operator|++
name|n
operator|>=
name|count
operator|&&
operator|!
name|PACKET_COUNT_IS_UNLIMITED
argument_list|(
name|count
argument_list|)
condition|)
block|{
name|p
operator|->
name|cc
operator|=
name|ep
operator|-
name|bufp
expr_stmt|;
name|p
operator|->
name|bp
operator|=
name|bufp
expr_stmt|;
return|return
operator|(
name|n
operator|)
return|;
block|}
block|}
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
block|}
endif|#
directive|endif
name|p
operator|->
name|cc
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|n
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Process the mac type. Returns -1 if no matching mac type found, otherwise 0.  */
end_comment

begin_function
name|int
name|pcap_process_mactype
parameter_list|(
name|pcap_t
modifier|*
name|p
parameter_list|,
name|u_int
name|mactype
parameter_list|)
block|{
name|int
name|retv
init|=
literal|0
decl_stmt|;
switch|switch
condition|(
name|mactype
condition|)
block|{
case|case
name|DL_CSMACD
case|:
case|case
name|DL_ETHER
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_EN10MB
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|2
expr_stmt|;
comment|/* 		 * This is (presumably) a real Ethernet capture; give it a 		 * link-layer-type list with DLT_EN10MB and DLT_DOCSIS, so 		 * that an application can let you choose it, in case you're 		 * capturing DOCSIS traffic that a Cisco Cable Modem 		 * Termination System is putting out onto an Ethernet (it 		 * doesn't put an Ethernet header onto the wire, it puts raw 		 * DOCSIS frames out on the wire inside the low-level 		 * Ethernet framing). 		 */
name|p
operator|->
name|dlt_list
operator|=
operator|(
name|u_int
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
name|u_int
argument_list|)
operator|*
literal|2
argument_list|)
expr_stmt|;
comment|/* 		 * If that fails, just leave the list empty. 		 */
if|if
condition|(
name|p
operator|->
name|dlt_list
operator|!=
name|NULL
condition|)
block|{
name|p
operator|->
name|dlt_list
index|[
literal|0
index|]
operator|=
name|DLT_EN10MB
expr_stmt|;
name|p
operator|->
name|dlt_list
index|[
literal|1
index|]
operator|=
name|DLT_DOCSIS
expr_stmt|;
name|p
operator|->
name|dlt_count
operator|=
literal|2
expr_stmt|;
block|}
break|break;
case|case
name|DL_FDDI
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_FDDI
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|3
expr_stmt|;
break|break;
case|case
name|DL_TPR
case|:
comment|/* XXX - what about DL_TPB?  Is that Token Bus?  */
name|p
operator|->
name|linktype
operator|=
name|DLT_IEEE802
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|2
expr_stmt|;
break|break;
ifdef|#
directive|ifdef
name|HAVE_SOLARIS
case|case
name|DL_IPATM
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_SUNATM
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|0
expr_stmt|;
comment|/* works for LANE and LLC encapsulation */
break|break;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DL_IPV4
case|case
name|DL_IPV4
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_IPV4
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|0
expr_stmt|;
break|break;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DL_IPV6
case|case
name|DL_IPV6
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_IPV6
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|0
expr_stmt|;
break|break;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|DL_IPNET
case|case
name|DL_IPNET
case|:
name|p
operator|->
name|linktype
operator|=
name|DLT_IPNET
expr_stmt|;
name|p
operator|->
name|offset
operator|=
literal|0
expr_stmt|;
break|break;
endif|#
directive|endif
default|default:
name|pcap_snprintf
argument_list|(
name|p
operator|->
name|errbuf
argument_list|,
name|PCAP_ERRBUF_SIZE
argument_list|,
literal|"unknown mactype 0x%x"
argument_list|,
name|mactype
argument_list|)
expr_stmt|;
name|retv
operator|=
operator|-
literal|1
expr_stmt|;
block|}
return|return
operator|(
name|retv
operator|)
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
end_ifdef

begin_comment
comment|/*  * Push and configure the buffer module. Returns -1 for error, otherwise 0.  */
end_comment

begin_function
name|int
name|pcap_conf_bufmod
parameter_list|(
name|pcap_t
modifier|*
name|p
parameter_list|,
name|int
name|snaplen
parameter_list|)
block|{
name|struct
name|timeval
name|to
decl_stmt|;
name|bpf_u_int32
name|ss
decl_stmt|,
name|chunksize
decl_stmt|;
comment|/* Non-standard call to get the data nicely buffered. */
if|if
condition|(
name|ioctl
argument_list|(
name|p
operator|->
name|fd
argument_list|,
name|I_PUSH
argument_list|,
literal|"bufmod"
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|pcap_stream_err
argument_list|(
literal|"I_PUSH bufmod"
argument_list|,
name|errno
argument_list|,
name|p
operator|->
name|errbuf
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
name|ss
operator|=
name|snaplen
expr_stmt|;
if|if
condition|(
name|ss
operator|>
literal|0
operator|&&
name|strioctl
argument_list|(
name|p
operator|->
name|fd
argument_list|,
name|SBIOCSSNAP
argument_list|,
sizeof|sizeof
argument_list|(
name|ss
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|ss
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|pcap_stream_err
argument_list|(
literal|"SBIOCSSNAP"
argument_list|,
name|errno
argument_list|,
name|p
operator|->
name|errbuf
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
if|if
condition|(
name|p
operator|->
name|opt
operator|.
name|immediate
condition|)
block|{
comment|/* Set the timeout to zero, for immediate delivery. */
name|to
operator|.
name|tv_sec
operator|=
literal|0
expr_stmt|;
name|to
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|strioctl
argument_list|(
name|p
operator|->
name|fd
argument_list|,
name|SBIOCSTIME
argument_list|,
sizeof|sizeof
argument_list|(
name|to
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|to
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|pcap_stream_err
argument_list|(
literal|"SBIOCSTIME"
argument_list|,
name|errno
argument_list|,
name|p
operator|->
name|errbuf
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
block|}
else|else
block|{
comment|/* Set up the bufmod timeout. */
if|if
condition|(
name|p
operator|->
name|opt
operator|.
name|timeout
operator|!=
literal|0
condition|)
block|{
name|to
operator|.
name|tv_sec
operator|=
name|p
operator|->
name|opt
operator|.
name|timeout
operator|/
literal|1000
expr_stmt|;
name|to
operator|.
name|tv_usec
operator|=
operator|(
name|p
operator|->
name|opt
operator|.
name|timeout
operator|*
literal|1000
operator|)
operator|%
literal|1000000
expr_stmt|;
if|if
condition|(
name|strioctl
argument_list|(
name|p
operator|->
name|fd
argument_list|,
name|SBIOCSTIME
argument_list|,
sizeof|sizeof
argument_list|(
name|to
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|to
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|pcap_stream_err
argument_list|(
literal|"SBIOCSTIME"
argument_list|,
name|errno
argument_list|,
name|p
operator|->
name|errbuf
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
block|}
comment|/* Set the chunk length. */
name|chunksize
operator|=
name|CHUNKSIZE
expr_stmt|;
if|if
condition|(
name|strioctl
argument_list|(
name|p
operator|->
name|fd
argument_list|,
name|SBIOCSCHUNK
argument_list|,
sizeof|sizeof
argument_list|(
name|chunksize
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|chunksize
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|pcap_stream_err
argument_list|(
literal|"SBIOCSCHUNKP"
argument_list|,
name|errno
argument_list|,
name|p
operator|->
name|errbuf
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_SYS_BUFMOD_H */
end_comment

begin_comment
comment|/*  * Allocate data buffer. Returns -1 if memory allocation fails, else 0.  */
end_comment

begin_function
name|int
name|pcap_alloc_databuf
parameter_list|(
name|pcap_t
modifier|*
name|p
parameter_list|)
block|{
name|p
operator|->
name|bufsize
operator|=
name|PKTBUFSIZE
expr_stmt|;
name|p
operator|->
name|buffer
operator|=
name|malloc
argument_list|(
name|p
operator|->
name|bufsize
operator|+
name|p
operator|->
name|offset
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|->
name|buffer
operator|==
name|NULL
condition|)
block|{
name|strlcpy
argument_list|(
name|p
operator|->
name|errbuf
argument_list|,
name|pcap_strerror
argument_list|(
name|errno
argument_list|)
argument_list|,
name|PCAP_ERRBUF_SIZE
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Issue a STREAMS I_STR ioctl. Returns -1 on error, otherwise  * length of returned data on success.  */
end_comment

begin_function
name|int
name|strioctl
parameter_list|(
name|int
name|fd
parameter_list|,
name|int
name|cmd
parameter_list|,
name|int
name|len
parameter_list|,
name|char
modifier|*
name|dp
parameter_list|)
block|{
name|struct
name|strioctl
name|str
decl_stmt|;
name|int
name|retv
decl_stmt|;
name|str
operator|.
name|ic_cmd
operator|=
name|cmd
expr_stmt|;
name|str
operator|.
name|ic_timout
operator|=
operator|-
literal|1
expr_stmt|;
name|str
operator|.
name|ic_len
operator|=
name|len
expr_stmt|;
name|str
operator|.
name|ic_dp
operator|=
name|dp
expr_stmt|;
if|if
condition|(
operator|(
name|retv
operator|=
name|ioctl
argument_list|(
name|fd
argument_list|,
name|I_STR
argument_list|,
operator|&
name|str
argument_list|)
operator|)
operator|<
literal|0
condition|)
return|return
operator|(
name|retv
operator|)
return|;
return|return
operator|(
name|str
operator|.
name|ic_len
operator|)
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_BUFMOD_H
end_ifdef

begin_comment
comment|/*  * Write stream error message to errbuf.  */
end_comment

begin_function
specifier|static
name|void
name|pcap_stream_err
parameter_list|(
specifier|const
name|char
modifier|*
name|func
parameter_list|,
name|int
name|err
parameter_list|,
name|char
modifier|*
name|errbuf
parameter_list|)
block|{
name|pcap_snprintf
argument_list|(
name|errbuf
argument_list|,
name|PCAP_ERRBUF_SIZE
argument_list|,
literal|"%s: %s"
argument_list|,
name|func
argument_list|,
name|pcap_strerror
argument_list|(
name|err
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

