begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * cmdline.c:  command-line processing  *  * ====================================================================  *    Licensed to the Apache Software Foundation (ASF) under one  *    or more contributor license agreements.  See the NOTICE file  *    distributed with this work for additional information  *    regarding copyright ownership.  The ASF licenses this file  *    to you under the Apache License, Version 2.0 (the  *    "License"); you may not use this file except in compliance  *    with the License.  You may obtain a copy of the License at  *  *      http://www.apache.org/licenses/LICENSE-2.0  *  *    Unless required by applicable law or agreed to in writing,  *    software distributed under the License is distributed on an  *    "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY  *    KIND, either express or implied.  See the License for the  *    specific language governing permissions and limitations  *    under the License.  * ====================================================================  */
end_comment

begin_comment
comment|/* ==================================================================== */
end_comment

begin_escape
end_escape

begin_comment
comment|/*** Includes. ***/
end_comment

begin_include
include|#
directive|include
file|"svn_client.h"
end_include

begin_include
include|#
directive|include
file|"svn_error.h"
end_include

begin_include
include|#
directive|include
file|"svn_dirent_uri.h"
end_include

begin_include
include|#
directive|include
file|"svn_path.h"
end_include

begin_include
include|#
directive|include
file|"svn_opt.h"
end_include

begin_include
include|#
directive|include
file|"svn_utf.h"
end_include

begin_include
include|#
directive|include
file|"client.h"
end_include

begin_include
include|#
directive|include
file|"private/svn_opt_private.h"
end_include

begin_include
include|#
directive|include
file|"svn_private_config.h"
end_include

begin_escape
end_escape

begin_comment
comment|/*** Code. ***/
end_comment

begin_define
define|#
directive|define
name|DEFAULT_ARRAY_SIZE
value|5
end_define

begin_comment
comment|/* Attempt to find the repository root url for TARGET, possibly using CTX for  * authentication.  If one is found and *ROOT_URL is not NULL, then just check  * that the root url for TARGET matches the value given in *ROOT_URL and  * return an error if it does not.  If one is found and *ROOT_URL is NULL then  * set *ROOT_URL to the root url for TARGET, allocated from POOL.  * If a root url is not found for TARGET because it does not exist in the  * repository, then return with no error.  *  * TARGET is a UTF-8 encoded string that is fully canonicalized and escaped.  */
end_comment

begin_function
specifier|static
name|svn_error_t
modifier|*
name|check_root_url_of_target
parameter_list|(
specifier|const
name|char
modifier|*
modifier|*
name|root_url
parameter_list|,
specifier|const
name|char
modifier|*
name|target
parameter_list|,
name|svn_client_ctx_t
modifier|*
name|ctx
parameter_list|,
name|apr_pool_t
modifier|*
name|pool
parameter_list|)
block|{
name|svn_error_t
modifier|*
name|err
decl_stmt|;
specifier|const
name|char
modifier|*
name|tmp_root_url
decl_stmt|;
specifier|const
name|char
modifier|*
name|truepath
decl_stmt|;
name|svn_opt_revision_t
name|opt_rev
decl_stmt|;
name|SVN_ERR
argument_list|(
name|svn_opt_parse_path
argument_list|(
operator|&
name|opt_rev
argument_list|,
operator|&
name|truepath
argument_list|,
name|target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|svn_path_is_url
argument_list|(
name|truepath
argument_list|)
condition|)
name|SVN_ERR
argument_list|(
name|svn_dirent_get_absolute
argument_list|(
operator|&
name|truepath
argument_list|,
name|truepath
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|err
operator|=
name|svn_client_get_repos_root
argument_list|(
operator|&
name|tmp_root_url
argument_list|,
name|NULL
argument_list|,
name|truepath
argument_list|,
name|ctx
argument_list|,
name|pool
argument_list|,
name|pool
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
condition|)
block|{
comment|/* It is OK if the given target does not exist, it just means        * we will not be able to determine the root url from this particular        * argument.        *        * If the target itself is a URL to a repository that does not exist,        * that's fine, too. The callers will deal with this argument in an        * appropriate manner if it does not make any sense.        *        * Also tolerate locally added targets ("bad revision" error).        */
if|if
condition|(
operator|(
name|err
operator|->
name|apr_err
operator|==
name|SVN_ERR_ENTRY_NOT_FOUND
operator|)
operator|||
operator|(
name|err
operator|->
name|apr_err
operator|==
name|SVN_ERR_WC_PATH_NOT_FOUND
operator|)
operator|||
operator|(
name|err
operator|->
name|apr_err
operator|==
name|SVN_ERR_WC_NOT_WORKING_COPY
operator|)
operator|||
operator|(
name|err
operator|->
name|apr_err
operator|==
name|SVN_ERR_RA_LOCAL_REPOS_OPEN_FAILED
operator|)
operator|||
operator|(
name|err
operator|->
name|apr_err
operator|==
name|SVN_ERR_CLIENT_BAD_REVISION
operator|)
condition|)
block|{
name|svn_error_clear
argument_list|(
name|err
argument_list|)
expr_stmt|;
return|return
name|SVN_NO_ERROR
return|;
block|}
else|else
return|return
name|svn_error_trace
argument_list|(
name|err
argument_list|)
return|;
block|}
if|if
condition|(
operator|*
name|root_url
operator|&&
name|tmp_root_url
condition|)
block|{
if|if
condition|(
name|strcmp
argument_list|(
operator|*
name|root_url
argument_list|,
name|tmp_root_url
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|svn_error_createf
argument_list|(
name|SVN_ERR_ILLEGAL_TARGET
argument_list|,
name|NULL
argument_list|,
name|_
argument_list|(
literal|"All non-relative targets must have "
literal|"the same root URL"
argument_list|)
argument_list|)
return|;
block|}
else|else
operator|*
name|root_url
operator|=
name|tmp_root_url
expr_stmt|;
return|return
name|SVN_NO_ERROR
return|;
block|}
end_function

begin_comment
comment|/* Note: This is substantially copied from svn_opt__args_to_target_array() in  * order to move to libsvn_client while maintaining backward compatibility. */
end_comment

begin_function
name|svn_error_t
modifier|*
name|svn_client_args_to_target_array2
parameter_list|(
name|apr_array_header_t
modifier|*
modifier|*
name|targets_p
parameter_list|,
name|apr_getopt_t
modifier|*
name|os
parameter_list|,
specifier|const
name|apr_array_header_t
modifier|*
name|known_targets
parameter_list|,
name|svn_client_ctx_t
modifier|*
name|ctx
parameter_list|,
name|svn_boolean_t
name|keep_last_origpath_on_truepath_collision
parameter_list|,
name|apr_pool_t
modifier|*
name|pool
parameter_list|)
block|{
name|int
name|i
decl_stmt|;
name|svn_boolean_t
name|rel_url_found
init|=
name|FALSE
decl_stmt|;
specifier|const
name|char
modifier|*
name|root_url
init|=
name|NULL
decl_stmt|;
name|apr_array_header_t
modifier|*
name|input_targets
init|=
name|apr_array_make
argument_list|(
name|pool
argument_list|,
name|DEFAULT_ARRAY_SIZE
argument_list|,
sizeof|sizeof
argument_list|(
specifier|const
name|char
operator|*
argument_list|)
argument_list|)
decl_stmt|;
name|apr_array_header_t
modifier|*
name|output_targets
init|=
name|apr_array_make
argument_list|(
name|pool
argument_list|,
name|DEFAULT_ARRAY_SIZE
argument_list|,
sizeof|sizeof
argument_list|(
specifier|const
name|char
operator|*
argument_list|)
argument_list|)
decl_stmt|;
name|apr_array_header_t
modifier|*
name|reserved_names
init|=
name|NULL
decl_stmt|;
comment|/* Step 1:  create a master array of targets that are in UTF-8      encoding, and come from concatenating the targets left by apr_getopt,      plus any extra targets (e.g., from the --targets switch.)      If any of the targets are relative urls, then set the rel_url_found      flag.*/
for|for
control|(
init|;
name|os
operator|->
name|ind
operator|<
name|os
operator|->
name|argc
condition|;
name|os
operator|->
name|ind
operator|++
control|)
block|{
comment|/* The apr_getopt targets are still in native encoding. */
specifier|const
name|char
modifier|*
name|raw_target
init|=
name|os
operator|->
name|argv
index|[
name|os
operator|->
name|ind
index|]
decl_stmt|;
specifier|const
name|char
modifier|*
name|utf8_target
decl_stmt|;
name|SVN_ERR
argument_list|(
name|svn_utf_cstring_to_utf8
argument_list|(
operator|&
name|utf8_target
argument_list|,
name|raw_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|svn_path_is_repos_relative_url
argument_list|(
name|utf8_target
argument_list|)
condition|)
name|rel_url_found
operator|=
name|TRUE
expr_stmt|;
name|APR_ARRAY_PUSH
argument_list|(
name|input_targets
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|utf8_target
expr_stmt|;
block|}
if|if
condition|(
name|known_targets
condition|)
block|{
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|known_targets
operator|->
name|nelts
condition|;
name|i
operator|++
control|)
block|{
comment|/* The --targets array have already been converted to UTF-8,              because we needed to split up the list with svn_cstring_split. */
specifier|const
name|char
modifier|*
name|utf8_target
init|=
name|APR_ARRAY_IDX
argument_list|(
name|known_targets
argument_list|,
name|i
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
decl_stmt|;
if|if
condition|(
name|svn_path_is_repos_relative_url
argument_list|(
name|utf8_target
argument_list|)
condition|)
name|rel_url_found
operator|=
name|TRUE
expr_stmt|;
name|APR_ARRAY_PUSH
argument_list|(
name|input_targets
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|utf8_target
expr_stmt|;
block|}
block|}
comment|/* Step 2:  process each target.  */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|input_targets
operator|->
name|nelts
condition|;
name|i
operator|++
control|)
block|{
specifier|const
name|char
modifier|*
name|utf8_target
init|=
name|APR_ARRAY_IDX
argument_list|(
name|input_targets
argument_list|,
name|i
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
decl_stmt|;
comment|/* Relative urls will be canonicalized when they are resolved later in        * the function        */
if|if
condition|(
name|svn_path_is_repos_relative_url
argument_list|(
name|utf8_target
argument_list|)
condition|)
block|{
name|APR_ARRAY_PUSH
argument_list|(
name|output_targets
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|utf8_target
expr_stmt|;
block|}
else|else
block|{
specifier|const
name|char
modifier|*
name|true_target
decl_stmt|;
specifier|const
name|char
modifier|*
name|peg_rev
decl_stmt|;
specifier|const
name|char
modifier|*
name|target
decl_stmt|;
comment|/*            * This is needed so that the target can be properly canonicalized,            * otherwise the canonicalization does not treat a ".@BASE" as a "."            * with a BASE peg revision, and it is not canonicalized to "@BASE".            * If any peg revision exists, it is appended to the final            * canonicalized path or URL.  Do not use svn_opt_parse_path()            * because the resulting peg revision is a structure that would have            * to be converted back into a string.  Converting from a string date            * to the apr_time_t field in the svn_opt_revision_value_t and back to            * a string would not necessarily preserve the exact bytes of the            * input date, so its easier just to keep it in string form.            */
name|SVN_ERR
argument_list|(
name|svn_opt__split_arg_at_peg_revision
argument_list|(
operator|&
name|true_target
argument_list|,
operator|&
name|peg_rev
argument_list|,
name|utf8_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
comment|/* URLs and wc-paths get treated differently. */
if|if
condition|(
name|svn_path_is_url
argument_list|(
name|true_target
argument_list|)
condition|)
block|{
name|SVN_ERR
argument_list|(
name|svn_opt__arg_canonicalize_url
argument_list|(
operator|&
name|true_target
argument_list|,
name|true_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
comment|/* not a url, so treat as a path */
block|{
specifier|const
name|char
modifier|*
name|base_name
decl_stmt|;
specifier|const
name|char
modifier|*
name|original_target
decl_stmt|;
name|original_target
operator|=
name|svn_dirent_internal_style
argument_list|(
name|true_target
argument_list|,
name|pool
argument_list|)
expr_stmt|;
name|SVN_ERR
argument_list|(
name|svn_opt__arg_canonicalize_path
argument_list|(
operator|&
name|true_target
argument_list|,
name|true_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
comment|/* There are two situations in which a 'truepath-conversion'                  (case-canonicalization to on-disk path on case-insensitive                  filesystem) needs to be undone:                   1. If KEEP_LAST_ORIGPATH_ON_TRUEPATH_COLLISION is TRUE, and                     this is the last target of a 2-element target list, and                     both targets have the same truepath. */
if|if
condition|(
name|keep_last_origpath_on_truepath_collision
operator|&&
name|input_targets
operator|->
name|nelts
operator|==
literal|2
operator|&&
name|i
operator|==
literal|1
operator|&&
name|strcmp
argument_list|(
name|original_target
argument_list|,
name|true_target
argument_list|)
operator|!=
literal|0
condition|)
block|{
specifier|const
name|char
modifier|*
name|src_truepath
init|=
name|APR_ARRAY_IDX
argument_list|(
name|output_targets
argument_list|,
literal|0
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
decl_stmt|;
if|if
condition|(
name|strcmp
argument_list|(
name|src_truepath
argument_list|,
name|true_target
argument_list|)
operator|==
literal|0
condition|)
name|true_target
operator|=
name|original_target
expr_stmt|;
block|}
comment|/* 2. If there is an exact match in the wc-db without a                     corresponding on-disk path (e.g. a scheduled-for-delete                     file only differing in case from an on-disk file). */
if|if
condition|(
name|strcmp
argument_list|(
name|original_target
argument_list|,
name|true_target
argument_list|)
operator|!=
literal|0
condition|)
block|{
specifier|const
name|char
modifier|*
name|target_abspath
decl_stmt|;
name|svn_node_kind_t
name|kind
decl_stmt|;
name|svn_error_t
modifier|*
name|err2
decl_stmt|;
name|SVN_ERR
argument_list|(
name|svn_dirent_get_absolute
argument_list|(
operator|&
name|target_abspath
argument_list|,
name|original_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|err2
operator|=
name|svn_wc_read_kind2
argument_list|(
operator|&
name|kind
argument_list|,
name|ctx
operator|->
name|wc_ctx
argument_list|,
name|target_abspath
argument_list|,
name|TRUE
argument_list|,
name|FALSE
argument_list|,
name|pool
argument_list|)
expr_stmt|;
if|if
condition|(
name|err2
operator|&&
operator|(
name|err2
operator|->
name|apr_err
operator|==
name|SVN_ERR_WC_NOT_WORKING_COPY
operator|||
name|err2
operator|->
name|apr_err
operator|==
name|SVN_ERR_WC_UPGRADE_REQUIRED
operator|)
condition|)
block|{
name|svn_error_clear
argument_list|(
name|err2
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|SVN_ERR
argument_list|(
name|err2
argument_list|)
expr_stmt|;
comment|/* We successfully did a lookup in the wc-db. Now see                          if it's something interesting. */
if|if
condition|(
name|kind
operator|==
name|svn_node_file
operator|||
name|kind
operator|==
name|svn_node_dir
condition|)
name|true_target
operator|=
name|original_target
expr_stmt|;
block|}
block|}
comment|/* If the target has the same name as a Subversion                  working copy administrative dir, skip it. */
name|base_name
operator|=
name|svn_dirent_basename
argument_list|(
name|true_target
argument_list|,
name|pool
argument_list|)
expr_stmt|;
if|if
condition|(
name|svn_wc_is_adm_dir
argument_list|(
name|base_name
argument_list|,
name|pool
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|reserved_names
condition|)
name|reserved_names
operator|=
name|apr_array_make
argument_list|(
name|pool
argument_list|,
name|DEFAULT_ARRAY_SIZE
argument_list|,
sizeof|sizeof
argument_list|(
specifier|const
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
name|APR_ARRAY_PUSH
argument_list|(
name|reserved_names
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|utf8_target
expr_stmt|;
continue|continue;
block|}
block|}
name|target
operator|=
name|apr_pstrcat
argument_list|(
name|pool
argument_list|,
name|true_target
argument_list|,
name|peg_rev
argument_list|,
operator|(
name|char
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|rel_url_found
condition|)
block|{
comment|/* Later targets have priority over earlier target, I                  don't know why, see basic_relative_url_multi_repo. */
name|SVN_ERR
argument_list|(
name|check_root_url_of_target
argument_list|(
operator|&
name|root_url
argument_list|,
name|target
argument_list|,
name|ctx
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
block|}
name|APR_ARRAY_PUSH
argument_list|(
name|output_targets
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|target
expr_stmt|;
block|}
block|}
comment|/* Only resolve relative urls if there were some actually found earlier. */
if|if
condition|(
name|rel_url_found
condition|)
block|{
comment|/*        * Use the current directory's root url if one wasn't found using the        * arguments.        */
if|if
condition|(
name|root_url
operator|==
name|NULL
condition|)
block|{
specifier|const
name|char
modifier|*
name|current_abspath
decl_stmt|;
name|svn_error_t
modifier|*
name|err
decl_stmt|;
name|SVN_ERR
argument_list|(
name|svn_dirent_get_absolute
argument_list|(
operator|&
name|current_abspath
argument_list|,
literal|""
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|err
operator|=
name|svn_client_get_repos_root
argument_list|(
operator|&
name|root_url
argument_list|,
name|NULL
comment|/* uuid */
argument_list|,
name|current_abspath
argument_list|,
name|ctx
argument_list|,
name|pool
argument_list|,
name|pool
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|||
name|root_url
operator|==
name|NULL
condition|)
return|return
name|svn_error_create
argument_list|(
name|SVN_ERR_WC_NOT_WORKING_COPY
argument_list|,
name|err
argument_list|,
name|_
argument_list|(
literal|"Resolving '^/': no repository root "
literal|"found in the target arguments or "
literal|"in the current directory"
argument_list|)
argument_list|)
return|;
block|}
operator|*
name|targets_p
operator|=
name|apr_array_make
argument_list|(
name|pool
argument_list|,
name|output_targets
operator|->
name|nelts
argument_list|,
sizeof|sizeof
argument_list|(
specifier|const
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|output_targets
operator|->
name|nelts
condition|;
name|i
operator|++
control|)
block|{
specifier|const
name|char
modifier|*
name|target
init|=
name|APR_ARRAY_IDX
argument_list|(
name|output_targets
argument_list|,
name|i
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
decl_stmt|;
if|if
condition|(
name|svn_path_is_repos_relative_url
argument_list|(
name|target
argument_list|)
condition|)
block|{
specifier|const
name|char
modifier|*
name|abs_target
decl_stmt|;
specifier|const
name|char
modifier|*
name|true_target
decl_stmt|;
specifier|const
name|char
modifier|*
name|peg_rev
decl_stmt|;
name|SVN_ERR
argument_list|(
name|svn_opt__split_arg_at_peg_revision
argument_list|(
operator|&
name|true_target
argument_list|,
operator|&
name|peg_rev
argument_list|,
name|target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|SVN_ERR
argument_list|(
name|svn_path_resolve_repos_relative_url
argument_list|(
operator|&
name|abs_target
argument_list|,
name|true_target
argument_list|,
name|root_url
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|SVN_ERR
argument_list|(
name|svn_opt__arg_canonicalize_url
argument_list|(
operator|&
name|true_target
argument_list|,
name|abs_target
argument_list|,
name|pool
argument_list|)
argument_list|)
expr_stmt|;
name|target
operator|=
name|apr_pstrcat
argument_list|(
name|pool
argument_list|,
name|true_target
argument_list|,
name|peg_rev
argument_list|,
operator|(
name|char
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
block|}
name|APR_ARRAY_PUSH
argument_list|(
operator|*
name|targets_p
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
operator|=
name|target
expr_stmt|;
block|}
block|}
else|else
operator|*
name|targets_p
operator|=
name|output_targets
expr_stmt|;
if|if
condition|(
name|reserved_names
condition|)
block|{
name|svn_error_t
modifier|*
name|err
init|=
name|SVN_NO_ERROR
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|reserved_names
operator|->
name|nelts
condition|;
operator|++
name|i
control|)
name|err
operator|=
name|svn_error_createf
argument_list|(
name|SVN_ERR_RESERVED_FILENAME_SPECIFIED
argument_list|,
name|err
argument_list|,
name|_
argument_list|(
literal|"'%s' ends in a reserved name"
argument_list|)
argument_list|,
name|APR_ARRAY_IDX
argument_list|(
name|reserved_names
argument_list|,
name|i
argument_list|,
specifier|const
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|svn_error_trace
argument_list|(
name|err
argument_list|)
return|;
block|}
return|return
name|SVN_NO_ERROR
return|;
block|}
end_function

end_unit

