begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"monitor.h"
end_include

begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<aux.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_expr_stmt
name|SCCSID
argument_list|(
argument|@
operator|(
operator|#
operator|)
name|include
operator|.
name|c
literal|7.1
literal|2
operator|/
literal|5
operator|/
literal|81
argument_list|)
comment|/* **  INCLUDE FILE ** **	A file name, which must follow the \i, is read and inserted **	into the text stream at this location.  It may include all of **	the standard control functions.  Includes may be nested. ** **	If the parameter is 0, the file name is taken from the input; **	otherwise it is taken directly from the parameter.  In this **	mode, errors are not printed. ** **	Prompts are turned off during the include. */
name|include
argument_list|(
argument|filename
argument_list|)
name|char
operator|*
name|filename
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|int
name|savendf
decl_stmt|;
name|FILE
modifier|*
name|saveinp
decl_stmt|;
specifier|register
name|char
modifier|*
name|f
decl_stmt|;
specifier|register
name|FILE
modifier|*
name|b
decl_stmt|;
specifier|extern
name|char
modifier|*
name|getfilenm
parameter_list|()
function_decl|;
name|f
operator|=
name|filename
expr_stmt|;
if|if
condition|(
name|f
operator|==
literal|0
condition|)
name|f
operator|=
name|getfilenm
argument_list|()
expr_stmt|;
if|if
condition|(
name|sequal
argument_list|(
name|f
argument_list|,
literal|"-"
argument_list|)
condition|)
block|{
comment|/* read keyboard */
name|b
operator|=
name|stdin
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|*
name|f
operator|==
literal|0
condition|)
block|{
comment|/* back up one level (EOF on next read) */
name|GiveEof
operator|=
name|TRUE
expr_stmt|;
return|return;
block|}
else|else
block|{
comment|/* read file */
if|if
condition|(
operator|(
name|b
operator|=
name|fopen
argument_list|(
name|f
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|filename
operator|==
literal|0
condition|)
name|printf
argument_list|(
literal|"Cannot open \"%s\"\n"
argument_list|,
name|f
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
comment|/* check for too deep */
if|if
condition|(
name|Idepth
operator|>=
literal|5
condition|)
block|{
name|printf
argument_list|(
literal|"Include nested too deep\n"
argument_list|)
expr_stmt|;
if|if
condition|(
name|b
condition|)
name|fclose
argument_list|(
name|b
argument_list|)
expr_stmt|;
return|return;
block|}
name|Idepth
operator|++
expr_stmt|;
comment|/* get input from alternate file */
name|savendf
operator|=
name|Nodayfile
expr_stmt|;
if|if
condition|(
name|b
operator|==
name|stdin
condition|)
block|{
name|Nodayfile
operator|=
name|Userdflag
expr_stmt|;
name|prompt
argument_list|(
literal|"<<input>>"
argument_list|)
expr_stmt|;
block|}
else|else
name|Nodayfile
operator|=
operator|-
literal|1
expr_stmt|;
name|saveinp
operator|=
name|Input
expr_stmt|;
name|Input
operator|=
name|b
expr_stmt|;
name|monitor
argument_list|()
expr_stmt|;
comment|/* done -- restore old file */
name|Input
operator|=
name|saveinp
expr_stmt|;
name|Nodayfile
operator|=
name|savendf
expr_stmt|;
name|Idepth
operator|--
expr_stmt|;
block|}
end_block

end_unit

