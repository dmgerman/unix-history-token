begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ingres.h>
end_include

begin_include
include|#
directive|include
file|<access.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)closer.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **	closer - close a relation ** **	CLOSER is used to close a relation which was opened by OPENR. **	CLOSER should always be called once for each OPENR. ** **	function values: ** **<0  fatal error **		 0  success **		 1  relation was not open */
end_comment

begin_macro
name|closer
argument_list|(
argument|d
argument_list|)
end_macro

begin_decl_stmt
name|DESC
modifier|*
name|d
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|DESC
modifier|*
name|dx
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|;
name|dx
operator|=
name|d
expr_stmt|;
ifdef|#
directive|ifdef
name|xATR1
if|if
condition|(
name|tTf
argument_list|(
literal|21
argument_list|,
literal|8
argument_list|)
condition|)
name|printf
argument_list|(
literal|"closer: %.14s,%ld\n"
argument_list|,
name|dx
operator|->
name|reldum
operator|.
name|relid
argument_list|,
name|dx
operator|->
name|reladds
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|i
operator|=
name|noclose
argument_list|(
name|dx
argument_list|)
condition|)
return|return
operator|(
name|i
operator|)
return|;
name|flush_rel
argument_list|(
name|dx
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
comment|/* No error is possible since noclose() 				** has already flushed any pages 				*/
if|if
condition|(
name|close
argument_list|(
name|dx
operator|->
name|relfp
argument_list|)
condition|)
comment|/*close the relation*/
name|i
operator|=
name|acc_err
argument_list|(
name|AMCLOSE_ERR
argument_list|)
expr_stmt|;
name|dx
operator|->
name|relopn
operator|=
literal|0
expr_stmt|;
return|return
operator|(
name|i
operator|)
return|;
block|}
end_block

end_unit

