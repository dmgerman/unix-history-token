begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|"ctlmod.h"
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_include
include|#
directive|include
file|<sccs.h>
end_include

begin_macro
name|SCCSID
argument_list|(
argument|@(#)cm_cleanup.c
literal|7.1
literal|2
argument|/
literal|5
argument|/
literal|81
argument_list|)
end_macro

begin_comment
comment|/* **  CM_CLEANUP -- cleanup after interrupt or error. ** **	This routine does things like call the interrupt cleanup **	function, reset the input, etc. ** **	Parameters: **		typ -- the type of cleanup: **			1 -- fatal error (from error [error.c]). **			2 -- keyboard interrupt. ** **	Returns: **		never (uses non-local jump to ctlmod/main.c). ** **	Side Effects: **		Proc_name& Cm.cm_input are reset. ** **	Trace Flags: **		0 */
end_comment

begin_macro
name|cm_cleanup
argument_list|(
argument|typ
argument_list|)
end_macro

begin_decl_stmt
name|int
name|typ
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|struct
name|fn_def
modifier|*
name|f
decl_stmt|;
specifier|extern
name|char
modifier|*
name|Proc_name
decl_stmt|;
specifier|extern
name|jmp_buf
name|CmReset
decl_stmt|;
extern|extern			rubcatch(
block|)
end_block

begin_empty_stmt
empty_stmt|;
end_empty_stmt

begin_decl_stmt
specifier|register
name|ctx_t
modifier|*
name|ctx
decl_stmt|;
end_decl_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|xCTR2
end_ifdef

begin_if
if|if
condition|(
name|tTf
argument_list|(
literal|0
argument_list|,
literal|13
argument_list|)
condition|)
name|printf
argument_list|(
literal|"cm_cleanup: %d\n"
argument_list|,
name|typ
argument_list|)
expr_stmt|;
end_if

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* 	**  Call all interrupt cleanup functions for active 	**	modules. 	*/
end_comment

begin_for
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NumFunc
condition|;
name|i
operator|++
control|)
block|{
name|f
operator|=
name|FuncVect
index|[
name|i
index|]
expr_stmt|;
if|if
condition|(
name|f
operator|->
name|fn_active
operator|>
literal|0
condition|)
block|{
name|Ctx
operator|.
name|ctx_name
operator|=
name|Proc_name
operator|=
name|f
operator|->
name|fn_name
expr_stmt|;
call|(
modifier|*
name|f
operator|->
name|fn_cleanup
call|)
argument_list|(
name|typ
argument_list|)
expr_stmt|;
block|}
block|}
end_for

begin_comment
comment|/* clean up memory */
end_comment

begin_for
for|for
control|(
name|ctx
operator|=
operator|&
name|Ctx
init|;
name|ctx
operator|!=
name|NULL
condition|;
name|ctx
operator|=
name|ctx
operator|->
name|ctx_link
control|)
block|{
if|if
condition|(
name|ctx
operator|->
name|ctx_qt
operator|!=
name|NULL
condition|)
name|free
argument_list|(
name|ctx
operator|->
name|ctx_qt
argument_list|)
expr_stmt|;
if|if
condition|(
name|ctx
operator|->
name|ctx_glob
operator|!=
name|NULL
condition|)
block|{
name|bmove
argument_list|(
name|ctx
operator|->
name|ctx_glob
argument_list|,
name|ctx
operator|->
name|ctx_fn
operator|->
name|fn_gptr
argument_list|,
name|ctx
operator|->
name|ctx_fn
operator|->
name|fn_gsize
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|ctx
operator|->
name|ctx_glob
argument_list|)
expr_stmt|;
block|}
block|}
end_for

begin_comment
comment|/* return to top of loop */
end_comment

begin_expr_stmt
name|longjmp
argument_list|(
name|CmReset
argument_list|,
name|typ
argument_list|)
expr_stmt|;
end_expr_stmt

unit|}
end_unit

