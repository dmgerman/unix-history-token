begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<sgtty.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|LOG_IPC
end_ifdef

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|LOG_OLDIPC
end_ifdef

begin_include
include|#
directive|include
file|<net/in.h>
end_include

begin_else
else|#
directive|else
else|LOG_OLDIPC
end_else

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<netdb.h>
end_include

begin_endif
endif|#
directive|endif
endif|LOG_OLDIPC
end_endif

begin_endif
endif|#
directive|endif
endif|LOG_IPC
end_endif

begin_decl_stmt
specifier|static
name|char
name|SccsId
index|[]
init|=
literal|"@(#)syslog.c	2.15		12/16/82"
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  SYSLOG -- print message on log file ** **	This routine looks a lot like printf, except that it **	outputs to the log file instead of the standard output. **	Also, it prints the module name in front of lines, **	and has some other formatting types (or will sometime). **	Also, it adds a newline on the end of messages. ** **	The output of this routine is intended to be read by **	/etc/syslog, which will add timestamps. ** **	Parameters: **		pri -- the message priority. **		fmt -- the format string. **		p0 -- the first of many parameters. ** **	Returns: **		none ** **	Side Effects: **		output to log. */
end_comment

begin_define
define|#
directive|define
name|MAXLINE
value|1000
end_define

begin_comment
comment|/* maximum line length */
end_comment

begin_define
define|#
directive|define
name|BUFSLOP
value|20
end_define

begin_comment
comment|/* space to allow for "extra stuff" */
end_comment

begin_define
define|#
directive|define
name|NULL
value|0
end_define

begin_comment
comment|/* manifest */
end_comment

begin_decl_stmt
name|int
name|LogFile
init|=
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* fd for log */
end_comment

begin_decl_stmt
name|int
name|LogStat
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* status bits, set by initlog */
end_comment

begin_decl_stmt
name|char
modifier|*
name|LogTag
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* string to tag the entry with */
end_comment

begin_decl_stmt
name|int
name|LogMask
init|=
name|LOG_DEBUG
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* lowest priority to be logged */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|LOG_IPC
end_ifdef

begin_ifndef
ifndef|#
directive|ifndef
name|LOG_OLDIPC
end_ifndef

begin_decl_stmt
name|struct
name|sockaddr_in
name|SyslogAddr
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
else|LOG_OLDIPC
end_else

begin_decl_stmt
name|struct
name|sockaddr_in
name|SyslogAddr
init|=
block|{
name|AF_INET
block|,
name|LOG_PORT
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|sockproto
name|SyslogProto
init|=
block|{
name|PF_INET
block|,
name|IPPROTO_UDP
block|}
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|LOG_OLDIPC
end_endif

begin_decl_stmt
specifier|static
name|char
modifier|*
name|SyslogHost
init|=
name|LOG_HOST
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|LOG_IPC
end_endif

begin_macro
name|syslog
argument_list|(
argument|pri
argument_list|,
argument|fmt
argument_list|,
argument|p0
argument_list|,
argument|p1
argument_list|,
argument|p2
argument_list|,
argument|p3
argument_list|,
argument|p4
argument_list|)
end_macro

begin_decl_stmt
name|int
name|pri
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|fmt
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|buf
index|[
name|MAXLINE
operator|+
name|BUFSLOP
index|]
decl_stmt|;
specifier|register
name|char
modifier|*
name|b
decl_stmt|;
name|char
modifier|*
name|f
decl_stmt|;
name|int
name|prec
decl_stmt|;
name|int
name|len
decl_stmt|;
specifier|register
name|char
name|c
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
name|int
name|i
decl_stmt|;
specifier|extern
name|int
name|errno
decl_stmt|;
specifier|extern
name|int
name|sys_nerr
decl_stmt|;
specifier|extern
name|char
modifier|*
name|sys_errlist
index|[]
decl_stmt|;
specifier|extern
name|char
modifier|*
name|logcvt
parameter_list|()
function_decl|;
name|char
name|outline
index|[
name|MAXLINE
operator|+
literal|1
index|]
decl_stmt|;
comment|/* if we have no log, open it */
if|if
condition|(
name|LogFile
operator|<
literal|0
condition|)
name|openlog
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* see if we should just throw out this message */
if|if
condition|(
name|pri
operator|>
name|LogMask
condition|)
return|return;
name|f
operator|=
name|fmt
expr_stmt|;
while|while
condition|(
operator|*
name|f
operator|!=
literal|'\0'
condition|)
block|{
comment|/* beginning of line */
name|b
operator|=
name|buf
expr_stmt|;
comment|/* insert priority code */
if|if
condition|(
name|pri
operator|>
literal|0
operator|&&
operator|(
name|LogStat
operator|&
name|LOG_COOLIT
operator|)
operator|==
literal|0
condition|)
block|{
operator|*
name|b
operator|++
operator|=
literal|'<'
expr_stmt|;
operator|*
name|b
operator|++
operator|=
name|pri
operator|+
literal|'0'
expr_stmt|;
operator|*
name|b
operator|++
operator|=
literal|'>'
expr_stmt|;
block|}
comment|/* output current process ID */
if|if
condition|(
operator|(
name|LogStat
operator|&
name|LOG_PID
operator|)
operator|!=
literal|0
condition|)
block|{
name|sprintf
argument_list|(
name|b
argument_list|,
literal|"%d "
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
name|b
operator|+=
name|strlen
argument_list|(
name|b
argument_list|)
expr_stmt|;
block|}
comment|/* and module name */
if|if
condition|(
name|LogTag
operator|!=
literal|0
condition|)
block|{
for|for
control|(
name|p
operator|=
name|LogTag
init|;
operator|*
name|p
operator|!=
literal|'\0'
condition|;
control|)
operator|*
name|b
operator|++
operator|=
operator|*
name|p
operator|++
expr_stmt|;
operator|*
name|b
operator|++
operator|=
literal|':'
expr_stmt|;
operator|*
name|b
operator|++
operator|=
literal|' '
expr_stmt|;
block|}
while|while
condition|(
operator|(
name|c
operator|=
operator|*
name|f
operator|++
operator|)
operator|!=
literal|'\0'
operator|&&
name|c
operator|!=
literal|'\n'
condition|)
block|{
comment|/* output character directly if not interpolated */
if|if
condition|(
name|c
operator|!=
literal|'%'
condition|)
block|{
operator|*
name|b
operator|++
operator|=
name|c
expr_stmt|;
continue|continue;
block|}
name|c
operator|=
operator|*
name|f
operator|++
expr_stmt|;
switch|switch
condition|(
name|c
condition|)
block|{
case|case
literal|'m'
case|:
comment|/* output error code */
if|if
condition|(
name|errno
operator|<
literal|0
operator|||
name|errno
operator|>
name|sys_nerr
condition|)
name|sprintf
argument_list|(
name|b
argument_list|,
literal|"error %d"
argument_list|,
name|errno
argument_list|)
expr_stmt|;
else|else
name|sprintf
argument_list|(
name|b
argument_list|,
literal|"%s"
argument_list|,
name|sys_errlist
index|[
name|errno
index|]
argument_list|)
expr_stmt|;
break|break;
default|default:
operator|*
name|b
operator|++
operator|=
literal|'%'
expr_stmt|;
operator|*
name|b
operator|++
operator|=
name|c
expr_stmt|;
operator|*
name|b
operator|=
literal|'\0'
expr_stmt|;
break|break;
block|}
name|b
operator|+=
name|strlen
argument_list|(
name|b
argument_list|)
expr_stmt|;
if|if
condition|(
name|b
operator|>=
operator|&
name|buf
index|[
name|MAXLINE
index|]
condition|)
break|break;
block|}
if|if
condition|(
name|c
operator|==
literal|'\0'
condition|)
name|f
operator|--
expr_stmt|;
comment|/* add trailing newline */
operator|*
name|b
operator|++
operator|=
literal|'\n'
expr_stmt|;
operator|*
name|b
operator|=
literal|'\0'
expr_stmt|;
comment|/* output string */
name|sprintf
argument_list|(
name|outline
argument_list|,
name|buf
argument_list|,
name|p0
argument_list|,
name|p1
argument_list|,
name|p2
argument_list|,
name|p3
argument_list|,
name|p4
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|LOG_IPC
if|if
condition|(
name|LogStat
operator|&
name|LOG_DGRAM
condition|)
block|{
specifier|register
name|int
name|r
decl_stmt|;
ifndef|#
directive|ifndef
name|LOG_OLDIPC
name|r
operator|=
name|sendto
argument_list|(
name|LogFile
argument_list|,
name|outline
argument_list|,
name|strlen
argument_list|(
name|outline
argument_list|)
argument_list|,
literal|0
argument_list|,
operator|&
name|SyslogAddr
argument_list|,
sizeof|sizeof
name|SyslogAddr
argument_list|)
expr_stmt|;
else|#
directive|else
else|LOG_OLDIPC
name|r
operator|=
name|send
argument_list|(
name|LogFile
argument_list|,
operator|&
name|SyslogAddr
argument_list|,
name|outline
argument_list|,
name|strlen
argument_list|(
name|outline
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|LOG_OLDIPC
ifdef|#
directive|ifdef
name|EBUG
if|if
condition|(
name|r
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"syslog: send"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|EBUG
block|}
else|else
endif|#
directive|endif
endif|LOG_IPC
name|write
argument_list|(
name|LogFile
argument_list|,
name|outline
argument_list|,
name|strlen
argument_list|(
name|outline
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/* **  OPENLOG -- open system log ** **	This happens automatically with reasonable defaults if you **	do nothing. ** **	Parameters: **		ident -- the name to be printed as a header for **			all messages. **		logstat -- a status word, interpreted as follows: **			LOG_PID -- log the pid with each message. ** **	Returns: **		0 -- success. **		-1 -- failure; logging on /dev/console instead. ** **	Side Effects: **		Several global variables get set. */
end_comment

begin_macro
name|openlog
argument_list|(
argument|ident
argument_list|,
argument|logstat
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|ident
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|logstat
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|int
name|fd
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
ifndef|#
directive|ifndef
name|LOG_OLDIPC
ifdef|#
directive|ifdef
name|LOG_IPC
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
endif|#
directive|endif
endif|LOG_IPC
endif|#
directive|endif
endif|LOG_OLDIPC
name|LogTag
operator|=
name|ident
expr_stmt|;
name|LogStat
operator|=
name|logstat
expr_stmt|;
if|if
condition|(
name|LogFile
operator|>=
literal|0
condition|)
return|return;
ifdef|#
directive|ifdef
name|LOG_IPC
ifndef|#
directive|ifndef
name|LOG_OLDIPC
name|sp
operator|=
name|getservbyname
argument_list|(
literal|"syslog"
argument_list|,
literal|"udp"
argument_list|)
expr_stmt|;
name|hp
operator|=
name|gethostbyname
argument_list|(
name|SyslogHost
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|!=
name|NULL
operator|&&
name|hp
operator|!=
name|NULL
condition|)
block|{
name|bzero
argument_list|(
operator|&
name|SyslogAddr
argument_list|,
sizeof|sizeof
name|SyslogAddr
argument_list|)
expr_stmt|;
name|SyslogAddr
operator|.
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|LogFile
operator|=
name|socket
argument_list|(
name|AF_INET
argument_list|,
name|SOCK_DGRAM
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|LogFile
operator|>=
literal|0
operator|&&
name|bind
argument_list|(
name|LogFile
argument_list|,
operator|&
name|SyslogAddr
argument_list|,
sizeof|sizeof
name|SyslogAddr
argument_list|,
literal|0
argument_list|)
operator|<
literal|0
condition|)
block|{
name|close
argument_list|(
name|LogFile
argument_list|)
expr_stmt|;
name|LogFile
operator|=
operator|-
literal|1
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|EBUG
if|if
condition|(
name|LogFile
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"syslog: socket"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|EBUG
name|SyslogAddr
operator|.
name|sin_port
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
name|bcopy
argument_list|(
name|hp
operator|->
name|h_addr
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|SyslogAddr
operator|.
name|sin_addr
argument_list|,
name|hp
operator|->
name|h_length
argument_list|)
expr_stmt|;
name|LogStat
operator||=
name|LOG_DGRAM
expr_stmt|;
block|}
else|#
directive|else
else|LOG_OLDIPC
name|SyslogAddr
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|rhost
argument_list|(
operator|&
name|SyslogHost
argument_list|)
expr_stmt|;
name|LogFile
operator|=
name|socket
argument_list|(
name|SOCK_DGRAM
argument_list|,
operator|&
name|SyslogProto
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|EBUG
if|if
condition|(
name|LogFile
operator|<
literal|0
condition|)
name|perror
argument_list|(
literal|"syslog: socket"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|EBUG
name|LogStat
operator||=
name|LOG_DGRAM
expr_stmt|;
endif|#
directive|endif
endif|LOG_OLDIPC
else|#
directive|else
else|LOG_IPC
name|LogFile
operator|=
name|open
argument_list|(
literal|"/dev/log"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|LOG_IPC
if|if
condition|(
name|LogFile
operator|<
literal|0
condition|)
block|{
name|nolog
label|:
name|LogStat
operator||=
name|LOG_COOLIT
expr_stmt|;
name|LogStat
operator|&=
operator|~
name|LOG_DGRAM
expr_stmt|;
name|LogMask
operator|=
name|LOG_CRIT
expr_stmt|;
ifdef|#
directive|ifdef
name|EBUG
name|LogFile
operator|=
operator|-
literal|1
expr_stmt|;
else|#
directive|else
else|EBUG
name|LogFile
operator|=
name|open
argument_list|(
literal|"/dev/console"
argument_list|,
literal|1
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|EBUG
if|if
condition|(
name|LogFile
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"Cannot open /dev/console"
argument_list|)
expr_stmt|;
name|LogFile
operator|=
literal|2
expr_stmt|;
block|}
block|}
ifndef|#
directive|ifndef
name|LOG_IPC
if|if
condition|(
name|fstat
argument_list|(
name|LogFile
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|nolog
goto|;
switch|switch
condition|(
name|st
operator|.
name|st_mode
operator|&
name|S_IFMT
condition|)
block|{
case|case
name|S_IFREG
case|:
case|case
name|S_IFDIR
case|:
operator|(
name|void
operator|)
name|close
argument_list|(
name|LogFile
argument_list|)
expr_stmt|;
goto|goto
name|nolog
goto|;
block|}
ifdef|#
directive|ifdef
name|FIOCLEX
comment|/* have it close automatically on exec */
name|ioctl
argument_list|(
name|LogFile
argument_list|,
name|FIOCLEX
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|FIOCLEX
endif|#
directive|endif
endif|LOG_IPC
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/* **  CLOSELOG -- close the system log ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		The system log is closed. */
end_comment

begin_macro
name|closelog
argument_list|()
end_macro

begin_block
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|LogFile
argument_list|)
expr_stmt|;
name|LogFile
operator|=
operator|-
literal|1
expr_stmt|;
block|}
end_block

end_unit

