begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/******************************************************************************  *                                                                            *  * Copyright (c) David L. Mills 1993, 1994                                    *  *                                                                            *  * Permission to use, copy, modify, and distribute this software and its      *  * documentation for any purpose and without fee is hereby granted, provided  *  * that the above copyright notice appears in all copies and that both the    *  * copyright notice and this permission notice appear in supporting           *  * documentation, and that the name University of Delaware not be used in     *  * advertising or publicity pertaining to distribution of the software        *  * without specific, written prior permission.  The University of Delaware    *  * makes no representations about the suitability this software for any       *  * purpose.  It is provided "as is" without express or implied warranty.      *  *                                                                            *  ******************************************************************************/
end_comment

begin_comment
comment|/*  * Modification history kern_ntptime.c  *  * 24 Sep 94	David L. Mills  *	Tightened code at exits.  *  * 24 Mar 94	David L. Mills  *	Revised syscall interface to include new variables for PPS  *	time discipline.  *  * 14 Feb 94	David L. Mills  *	Added code for external clock  *  * 28 Nov 93	David L. Mills  *	Revised frequency scaling to conform with adjusted parameters  *  * 17 Sep 93	David L. Mills  *	Created file  */
end_comment

begin_comment
comment|/*  * ntp_gettime(), ntp_adjtime() - precision time interface for SunOS  * V4.1.1 and V4.1.3  *  * These routines consitute the Network Time Protocol (NTP) interfaces  * for user and daemon application programs. The ntp_gettime() routine  * provides the time, maximum error (synch distance) and estimated error  * (dispersion) to client user application programs. The ntp_adjtime()  * routine is used by the NTP daemon to adjust the system clock to an  * externally derived time. The time offset and related variables set by  * this routine are used by hardclock() to adjust the phase and  * frequency of the phase-lock loop which controls the system clock.  */
end_comment

begin_include
include|#
directive|include
file|"opt_ntp.h"
end_include

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/sysproto.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/timex.h>
end_include

begin_include
include|#
directive|include
file|<sys/sysctl.h>
end_include

begin_comment
comment|/*  * Phase/frequency-lock loop (PLL/FLL) definitions  *  * The following variables are read and set by the ntp_adjtime() system  * call.  *  * time_state shows the state of the system clock, with values defined  * in the timex.h header file.  *  * time_status shows the status of the system clock, with bits defined  * in the timex.h header file.  *  * time_offset is used by the PLL/FLL to adjust the system time in small  * increments.  *  * time_constant determines the bandwidth or "stiffness" of the PLL.  *  * time_tolerance determines maximum frequency error or tolerance of the  * CPU clock oscillator and is a property of the architecture; however,  * in principle it could change as result of the presence of external  * discipline signals, for instance.  *  * time_precision is usually equal to the kernel tick variable; however,  * in cases where a precision clock counter or external clock is  * available, the resolution can be much less than this and depend on  * whether the external clock is working or not.  *  * time_maxerror is initialized by a ntp_adjtime() call and increased by  * the kernel once each second to reflect the maximum error  * bound growth.  *  * time_esterror is set and read by the ntp_adjtime() call, but  * otherwise not used by the kernel.  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|time_status
init|=
name|STA_UNSYNC
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* clock status bits */
end_comment

begin_decl_stmt
specifier|static
name|int
name|time_state
init|=
name|TIME_OK
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* clock state */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_offset
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* time offset (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_constant
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pll time constant */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_tolerance
init|=
name|MAXFREQ
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* frequency tolerance (scaled ppm) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_precision
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* clock precision (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_maxerror
init|=
name|MAXPHASE
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* maximum error (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_esterror
init|=
name|MAXPHASE
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* estimated error (us) */
end_comment

begin_comment
comment|/*  * The following variables establish the state of the PLL/FLL and the  * residual time and frequency offset of the local clock. The scale  * factors are defined in the timex.h header file.  *  * time_phase and time_freq are the phase increment and the frequency  * increment, respectively, of the kernel time variable at each tick of  * the clock.  *  * time_freq is set via ntp_adjtime() from a value stored in a file when  * the synchronization daemon is first started. Its value is retrieved  * via ntp_adjtime() and written to the file about once per hour by the  * daemon.  *  * time_adj is the adjustment added to the value of tick at each timer  * interrupt and is recomputed from time_phase and time_freq at each  * seconds rollover.  *  * time_reftime is the second's portion of the system time on the last  * call to ntp_adjtime(). It is used to adjust the time_freq variable  * and to increase the time_maxerror as the time since last update  * increases.  */
end_comment

begin_decl_stmt
name|long
name|time_phase
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* phase offset (scaled us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_freq
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* frequency offset (scaled ppm) */
end_comment

begin_decl_stmt
name|long
name|time_adj
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* tick adjust (scaled 1 / hz) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|time_reftime
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* time at last adjustment (s) */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|PPS_SYNC
end_ifdef

begin_comment
comment|/*  * The following variables are used only if the kernel PPS discipline  * code is configured (PPS_SYNC). The scale factors are defined in the  * timex.h header file.  *  * pps_time contains the time at each calibration interval, as read by  * microtime(). pps_count counts the seconds of the calibration  * interval, the duration of which is nominally pps_shift in powers of  * two.  *  * pps_offset is the time offset produced by the time median filter  * pps_tf[], while pps_jitter is the dispersion (jitter) measured by  * this filter.  *  * pps_freq is the frequency offset produced by the frequency median  * filter pps_ff[], while pps_stabil is the dispersion (wander) measured  * by this filter.  *  * pps_usec is latched from a high resolution counter or external clock  * at pps_time. Here we want the hardware counter contents only, not the  * contents plus the time_tv.usec as usual.  *  * pps_valid counts the number of seconds since the last PPS update. It  * is used as a watchdog timer to disable the PPS discipline should the  * PPS signal be lost.  *  * pps_glitch counts the number of seconds since the beginning of an  * offset burst more than tick/2 from current nominal offset. It is used  * mainly to suppress error bursts due to priority conflicts between the  * PPS interrupt and timer interrupt.  *  * pps_intcnt counts the calibration intervals for use in the interval-  * adaptation algorithm. It's just too complicated for words.  */
end_comment

begin_decl_stmt
specifier|static
name|struct
name|timeval
name|pps_time
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* kernel time at last interval */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_offset
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pps time offset (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_jitter
init|=
name|MAXTIME
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pps time dispersion (jitter) (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_tf
index|[]
init|=
block|{
literal|0
block|,
literal|0
block|,
literal|0
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pps time offset median filter (us) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_freq
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* frequency offset (scaled ppm) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_stabil
init|=
name|MAXFREQ
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* frequency dispersion (scaled ppm) */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_ff
index|[]
init|=
block|{
literal|0
block|,
literal|0
block|,
literal|0
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* frequency offset median filter */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_usec
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* microsec counter at last interval */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_valid
init|=
name|PPS_VALID
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pps signal watchdog counter */
end_comment

begin_decl_stmt
specifier|static
name|int
name|pps_glitch
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* pps signal glitch counter */
end_comment

begin_decl_stmt
specifier|static
name|int
name|pps_count
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* calibration interval counter (s) */
end_comment

begin_decl_stmt
specifier|static
name|int
name|pps_shift
init|=
name|PPS_SHIFT
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* interval duration (s) (shift) */
end_comment

begin_decl_stmt
specifier|static
name|int
name|pps_intcnt
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* intervals at current duration */
end_comment

begin_comment
comment|/*  * PPS signal quality monitors  *  * pps_jitcnt counts the seconds that have been discarded because the  * jitter measured by the time median filter exceeds the limit MAXTIME  * (100 us).  *  * pps_calcnt counts the frequency calibration intervals, which are  * variable from 4 s to 256 s.  *  * pps_errcnt counts the calibration intervals which have been discarded  * because the wander exceeds the limit MAXFREQ (100 ppm) or where the  * calibration interval jitter exceeds two ticks.  *  * pps_stbcnt counts the calibration intervals that have been discarded  * because the frequency wander exceeds the limit MAXFREQ / 4 (25 us).  */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_jitcnt
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* jitter limit exceeded */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_calcnt
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* calibration intervals */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_errcnt
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* calibration errors */
end_comment

begin_decl_stmt
specifier|static
name|long
name|pps_stbcnt
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* stability limit exceeded */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* PPS_SYNC */
end_comment

begin_decl_stmt
specifier|static
name|void
name|hardupdate
name|__P
argument_list|(
operator|(
name|long
name|offset
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * hardupdate() - local clock update  *  * This routine is called by ntp_adjtime() to update the local clock  * phase and frequency. The implementation is of an adaptive-parameter,  * hybrid phase/frequency-lock loop (PLL/FLL). The routine computes new  * time and frequency offset estimates for each call. If the kernel PPS  * discipline code is configured (PPS_SYNC), the PPS signal itself  * determines the new time offset, instead of the calling argument.  * Presumably, calls to ntp_adjtime() occur only when the caller  * believes the local clock is valid within some bound (+-128 ms with  * NTP). If the caller's time is far different than the PPS time, an  * argument will ensue, and it's not clear who will lose.  *  * For uncompensated quartz crystal oscillatores and nominal update  * intervals less than 1024 s, operation should be in phase-lock mode  * (STA_FLL = 0), where the loop is disciplined to phase. For update  * intervals greater than thiss, operation should be in frequency-lock  * mode (STA_FLL = 1), where the loop is disciplined to frequency.  *  * Note: splclock() is in effect.  */
end_comment

begin_function
specifier|static
name|void
name|hardupdate
parameter_list|(
name|offset
parameter_list|)
name|long
name|offset
decl_stmt|;
block|{
name|long
name|ltemp
decl_stmt|,
name|mtemp
decl_stmt|;
if|if
condition|(
operator|!
operator|(
name|time_status
operator|&
name|STA_PLL
operator|)
operator|&&
operator|!
operator|(
name|time_status
operator|&
name|STA_PPSTIME
operator|)
condition|)
return|return;
name|ltemp
operator|=
name|offset
expr_stmt|;
ifdef|#
directive|ifdef
name|PPS_SYNC
if|if
condition|(
name|time_status
operator|&
name|STA_PPSTIME
operator|&&
name|time_status
operator|&
name|STA_PPSSIGNAL
condition|)
name|ltemp
operator|=
name|pps_offset
expr_stmt|;
endif|#
directive|endif
comment|/* PPS_SYNC */
comment|/* 	 * Scale the phase adjustment and clamp to the operating range. 	 */
if|if
condition|(
name|ltemp
operator|>
name|MAXPHASE
condition|)
name|time_offset
operator|=
name|MAXPHASE
operator|<<
name|SHIFT_UPDATE
expr_stmt|;
elseif|else
if|if
condition|(
name|ltemp
operator|<
operator|-
name|MAXPHASE
condition|)
name|time_offset
operator|=
operator|-
operator|(
name|MAXPHASE
operator|<<
name|SHIFT_UPDATE
operator|)
expr_stmt|;
else|else
name|time_offset
operator|=
name|ltemp
operator|<<
name|SHIFT_UPDATE
expr_stmt|;
comment|/* 	 * Select whether the frequency is to be controlled and in which 	 * mode (PLL or FLL). Clamp to the operating range. Ugly 	 * multiply/divide should be replaced someday. 	 */
if|if
condition|(
name|time_status
operator|&
name|STA_FREQHOLD
operator|||
name|time_reftime
operator|==
literal|0
condition|)
name|time_reftime
operator|=
name|time
operator|.
name|tv_sec
expr_stmt|;
name|mtemp
operator|=
name|time
operator|.
name|tv_sec
operator|-
name|time_reftime
expr_stmt|;
name|time_reftime
operator|=
name|time
operator|.
name|tv_sec
expr_stmt|;
if|if
condition|(
name|time_status
operator|&
name|STA_FLL
condition|)
block|{
if|if
condition|(
name|mtemp
operator|>=
name|MINSEC
condition|)
block|{
name|ltemp
operator|=
operator|(
operator|(
name|time_offset
operator|/
name|mtemp
operator|)
operator|<<
operator|(
name|SHIFT_USEC
operator|-
name|SHIFT_UPDATE
operator|)
operator|)
expr_stmt|;
if|if
condition|(
name|ltemp
operator|<
literal|0
condition|)
name|time_freq
operator|-=
operator|-
name|ltemp
operator|>>
name|SHIFT_KH
expr_stmt|;
else|else
name|time_freq
operator|+=
name|ltemp
operator|>>
name|SHIFT_KH
expr_stmt|;
block|}
block|}
else|else
block|{
if|if
condition|(
name|mtemp
operator|<
name|MAXSEC
condition|)
block|{
name|ltemp
operator|*=
name|mtemp
expr_stmt|;
if|if
condition|(
name|ltemp
operator|<
literal|0
condition|)
name|time_freq
operator|-=
operator|-
name|ltemp
operator|>>
operator|(
name|time_constant
operator|+
name|time_constant
operator|+
name|SHIFT_KF
operator|-
name|SHIFT_USEC
operator|)
expr_stmt|;
else|else
name|time_freq
operator|+=
name|ltemp
operator|>>
operator|(
name|time_constant
operator|+
name|time_constant
operator|+
name|SHIFT_KF
operator|-
name|SHIFT_USEC
operator|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|time_freq
operator|>
name|time_tolerance
condition|)
name|time_freq
operator|=
name|time_tolerance
expr_stmt|;
elseif|else
if|if
condition|(
name|time_freq
operator|<
operator|-
name|time_tolerance
condition|)
name|time_freq
operator|=
operator|-
name|time_tolerance
expr_stmt|;
block|}
end_function

begin_function
name|void
name|ntp_update_second
parameter_list|(
name|long
modifier|*
name|newsec
parameter_list|)
block|{
name|long
name|ltemp
decl_stmt|;
name|time_maxerror
operator|+=
name|time_tolerance
operator|>>
name|SHIFT_USEC
expr_stmt|;
comment|/* 	* Compute the phase adjustment for the next second. In 	* PLL mode, the offset is reduced by a fixed factor 	* times the time constant. In FLL mode the offset is 	* used directly. In either mode, the maximum phase 	* adjustment for each second is clamped so as to spread 	* the adjustment over not more than the number of 	* seconds between updates. 	*/
if|if
condition|(
name|time_offset
operator|<
literal|0
condition|)
block|{
name|ltemp
operator|=
operator|-
name|time_offset
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|time_status
operator|&
name|STA_FLL
operator|)
condition|)
name|ltemp
operator|>>=
name|SHIFT_KG
operator|+
name|time_constant
expr_stmt|;
if|if
condition|(
name|ltemp
operator|>
operator|(
name|MAXPHASE
operator|/
name|MINSEC
operator|)
operator|<<
name|SHIFT_UPDATE
condition|)
name|ltemp
operator|=
operator|(
name|MAXPHASE
operator|/
name|MINSEC
operator|)
operator|<<
name|SHIFT_UPDATE
expr_stmt|;
name|time_offset
operator|+=
name|ltemp
expr_stmt|;
name|time_adj
operator|=
operator|-
name|ltemp
operator|<<
operator|(
name|SHIFT_SCALE
operator|-
name|SHIFT_HZ
operator|-
name|SHIFT_UPDATE
operator|)
expr_stmt|;
block|}
else|else
block|{
name|ltemp
operator|=
name|time_offset
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|time_status
operator|&
name|STA_FLL
operator|)
condition|)
name|ltemp
operator|>>=
name|SHIFT_KG
operator|+
name|time_constant
expr_stmt|;
if|if
condition|(
name|ltemp
operator|>
operator|(
name|MAXPHASE
operator|/
name|MINSEC
operator|)
operator|<<
name|SHIFT_UPDATE
condition|)
name|ltemp
operator|=
operator|(
name|MAXPHASE
operator|/
name|MINSEC
operator|)
operator|<<
name|SHIFT_UPDATE
expr_stmt|;
name|time_offset
operator|-=
name|ltemp
expr_stmt|;
name|time_adj
operator|=
name|ltemp
operator|<<
operator|(
name|SHIFT_SCALE
operator|-
name|SHIFT_HZ
operator|-
name|SHIFT_UPDATE
operator|)
expr_stmt|;
block|}
comment|/* 	* Compute the frequency estimate and additional phase 	* adjustment due to frequency error for the next 	* second. When the PPS signal is engaged, gnaw on the 	* watchdog counter and update the frequency computed by 	* the pll and the PPS signal. 	*/
ifdef|#
directive|ifdef
name|PPS_SYNC
name|pps_valid
operator|++
expr_stmt|;
if|if
condition|(
name|pps_valid
operator|==
name|PPS_VALID
condition|)
block|{
name|pps_jitter
operator|=
name|MAXTIME
expr_stmt|;
name|pps_stabil
operator|=
name|MAXFREQ
expr_stmt|;
name|time_status
operator|&=
operator|~
operator|(
name|STA_PPSSIGNAL
operator||
name|STA_PPSJITTER
operator||
name|STA_PPSWANDER
operator||
name|STA_PPSERROR
operator|)
expr_stmt|;
block|}
name|ltemp
operator|=
name|time_freq
operator|+
name|pps_freq
expr_stmt|;
else|#
directive|else
name|ltemp
operator|=
name|time_freq
expr_stmt|;
endif|#
directive|endif
comment|/* PPS_SYNC */
if|if
condition|(
name|ltemp
operator|<
literal|0
condition|)
name|time_adj
operator|-=
operator|-
name|ltemp
operator|>>
operator|(
name|SHIFT_USEC
operator|+
name|SHIFT_HZ
operator|-
name|SHIFT_SCALE
operator|)
expr_stmt|;
else|else
name|time_adj
operator|+=
name|ltemp
operator|>>
operator|(
name|SHIFT_USEC
operator|+
name|SHIFT_HZ
operator|-
name|SHIFT_SCALE
operator|)
expr_stmt|;
if|#
directive|if
name|SHIFT_HZ
operator|==
literal|7
comment|/* 	* When the CPU clock oscillator frequency is not a 	* power of two in Hz, the SHIFT_HZ is only an 	* approximate scale factor. In the SunOS kernel, this 	* results in a PLL gain factor of 1/1.28 = 0.78 what it 	* should be. In the following code the overall gain is 	* increased by a factor of 1.25, which results in a 	* residual error less than 3 percent. 	*/
comment|/* Same thing applies for FreeBSD --GAW */
if|if
condition|(
name|hz
operator|==
literal|100
condition|)
block|{
if|if
condition|(
name|time_adj
operator|<
literal|0
condition|)
name|time_adj
operator|-=
operator|-
name|time_adj
operator|>>
literal|2
expr_stmt|;
else|else
name|time_adj
operator|+=
name|time_adj
operator|>>
literal|2
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SHIFT_HZ */
comment|/* XXX - this is really bogus, but can't be fixed until 	xntpd's idea of the system clock is fixed to know how 	the user wants leap seconds handled; in the mean time, 	we assume that users of NTP are running without proper 	leap second support (this is now the default anyway) */
comment|/* 	* Leap second processing. If in leap-insert state at 	* the end of the day, the system clock is set back one 	* second; if in leap-delete state, the system clock is 	* set ahead one second. The microtime() routine or 	* external clock driver will insure that reported time 	* is always monotonic. The ugly divides should be 	* replaced. 	*/
switch|switch
condition|(
name|time_state
condition|)
block|{
case|case
name|TIME_OK
case|:
if|if
condition|(
name|time_status
operator|&
name|STA_INS
condition|)
name|time_state
operator|=
name|TIME_INS
expr_stmt|;
elseif|else
if|if
condition|(
name|time_status
operator|&
name|STA_DEL
condition|)
name|time_state
operator|=
name|TIME_DEL
expr_stmt|;
break|break;
case|case
name|TIME_INS
case|:
if|if
condition|(
operator|(
operator|*
name|newsec
operator|)
operator|%
literal|86400
operator|==
literal|0
condition|)
block|{
operator|(
operator|*
name|newsec
operator|)
operator|--
expr_stmt|;
name|time_state
operator|=
name|TIME_OOP
expr_stmt|;
block|}
break|break;
case|case
name|TIME_DEL
case|:
if|if
condition|(
operator|(
operator|(
operator|*
name|newsec
operator|)
operator|+
literal|1
operator|)
operator|%
literal|86400
operator|==
literal|0
condition|)
block|{
operator|(
operator|*
name|newsec
operator|)
operator|++
expr_stmt|;
name|time_state
operator|=
name|TIME_WAIT
expr_stmt|;
block|}
break|break;
case|case
name|TIME_OOP
case|:
name|time_state
operator|=
name|TIME_WAIT
expr_stmt|;
break|break;
case|case
name|TIME_WAIT
case|:
if|if
condition|(
operator|!
operator|(
name|time_status
operator|&
operator|(
name|STA_INS
operator||
name|STA_DEL
operator|)
operator|)
condition|)
name|time_state
operator|=
name|TIME_OK
expr_stmt|;
break|break;
block|}
block|}
end_function

begin_decl_stmt
specifier|static
name|int
name|ntp_sysctl
name|SYSCTL_HANDLER_ARGS
block|{
name|struct
name|timeval
name|atv
decl_stmt|;
name|struct
name|ntptimeval
name|ntv
decl_stmt|;
name|int
name|s
decl_stmt|;
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
name|microtime
argument_list|(
operator|&
name|atv
argument_list|)
expr_stmt|;
name|ntv
operator|.
name|time
operator|=
name|atv
expr_stmt|;
name|ntv
operator|.
name|maxerror
operator|=
name|time_maxerror
expr_stmt|;
name|ntv
operator|.
name|esterror
operator|=
name|time_esterror
expr_stmt|;
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|ntv
operator|.
name|time_state
operator|=
name|time_state
expr_stmt|;
comment|/* 	 * Status word error decode. If any of these conditions 	 * occur, an error is returned, instead of the status 	 * word. Most applications will care only about the fact 	 * the system clock may not be trusted, not about the 	 * details. 	 * 	 * Hardware or software error 	 */
if|if
condition|(
name|time_status
operator|&
operator|(
name|STA_UNSYNC
operator||
name|STA_CLOCKERR
operator|)
condition|)
block|{
name|ntv
operator|.
name|time_state
operator|=
name|TIME_ERROR
expr_stmt|;
block|}
comment|/* 	 * PPS signal lost when either time or frequency 	 * synchronization requested 	 */
if|if
condition|(
name|time_status
operator|&
operator|(
name|STA_PPSFREQ
operator||
name|STA_PPSTIME
operator|)
operator|&&
operator|!
operator|(
name|time_status
operator|&
name|STA_PPSSIGNAL
operator|)
condition|)
block|{
name|ntv
operator|.
name|time_state
operator|=
name|TIME_ERROR
expr_stmt|;
block|}
comment|/* 	 * PPS jitter exceeded when time synchronization 	 * requested 	 */
if|if
condition|(
name|time_status
operator|&
name|STA_PPSTIME
operator|&&
name|time_status
operator|&
name|STA_PPSJITTER
condition|)
block|{
name|ntv
operator|.
name|time_state
operator|=
name|TIME_ERROR
expr_stmt|;
block|}
comment|/* 	 * PPS wander exceeded or calibration error when 	 * frequency synchronization requested 	 */
if|if
condition|(
name|time_status
operator|&
name|STA_PPSFREQ
operator|&&
name|time_status
operator|&
operator|(
name|STA_PPSWANDER
operator||
name|STA_PPSERROR
operator|)
condition|)
block|{
name|ntv
operator|.
name|time_state
operator|=
name|TIME_ERROR
expr_stmt|;
block|}
return|return
operator|(
name|sysctl_handle_opaque
argument_list|(
name|oidp
argument_list|,
operator|&
name|ntv
argument_list|,
sizeof|sizeof
name|ntv
argument_list|,
name|req
argument_list|)
operator|)
return|;
block|}
end_decl_stmt

begin_expr_stmt
name|SYSCTL_NODE
argument_list|(
name|_kern
argument_list|,
name|KERN_NTP_PLL
argument_list|,
name|ntp_pll
argument_list|,
name|CTLFLAG_RW
argument_list|,
literal|0
argument_list|,
literal|"NTP kernel PLL related stuff"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_expr_stmt
name|SYSCTL_PROC
argument_list|(
name|_kern_ntp_pll
argument_list|,
name|NTP_PLL_GETTIME
argument_list|,
name|gettime
argument_list|,
name|CTLTYPE_OPAQUE
operator||
name|CTLFLAG_RD
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|ntptimeval
argument_list|)
argument_list|,
name|ntp_sysctl
argument_list|,
literal|"S,ntptimeval"
argument_list|,
literal|""
argument_list|)
expr_stmt|;
end_expr_stmt

begin_comment
comment|/*  * ntp_adjtime() - NTP daemon application interface  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|_SYS_SYSPROTO_H_
end_ifndef

begin_struct
struct|struct
name|ntp_adjtime_args
block|{
name|struct
name|timex
modifier|*
name|tp
decl_stmt|;
block|}
struct|;
end_struct

begin_endif
endif|#
directive|endif
end_endif

begin_function
name|int
name|ntp_adjtime
parameter_list|(
name|struct
name|proc
modifier|*
name|p
parameter_list|,
name|struct
name|ntp_adjtime_args
modifier|*
name|uap
parameter_list|)
block|{
name|struct
name|timex
name|ntv
decl_stmt|;
name|int
name|modes
decl_stmt|;
name|int
name|s
decl_stmt|;
name|int
name|error
decl_stmt|;
name|error
operator|=
name|copyin
argument_list|(
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tp
argument_list|,
operator|(
name|caddr_t
operator|)
operator|&
name|ntv
argument_list|,
sizeof|sizeof
argument_list|(
name|ntv
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
comment|/* 	 * Update selected clock variables - only the superuser can 	 * change anything. Note that there is no error checking here on 	 * the assumption the superuser should know what it is doing. 	 */
name|modes
operator|=
name|ntv
operator|.
name|modes
expr_stmt|;
if|if
condition|(
operator|(
name|modes
operator|!=
literal|0
operator|)
operator|&&
operator|(
name|error
operator|=
name|suser
argument_list|(
name|p
operator|->
name|p_cred
operator|->
name|pc_ucred
argument_list|,
operator|&
name|p
operator|->
name|p_acflag
argument_list|)
operator|)
condition|)
return|return
name|error
return|;
name|s
operator|=
name|splclock
argument_list|()
expr_stmt|;
if|if
condition|(
name|modes
operator|&
name|MOD_FREQUENCY
condition|)
ifdef|#
directive|ifdef
name|PPS_SYNC
name|time_freq
operator|=
name|ntv
operator|.
name|freq
operator|-
name|pps_freq
expr_stmt|;
else|#
directive|else
comment|/* PPS_SYNC */
name|time_freq
operator|=
name|ntv
operator|.
name|freq
expr_stmt|;
endif|#
directive|endif
comment|/* PPS_SYNC */
if|if
condition|(
name|modes
operator|&
name|MOD_MAXERROR
condition|)
name|time_maxerror
operator|=
name|ntv
operator|.
name|maxerror
expr_stmt|;
if|if
condition|(
name|modes
operator|&
name|MOD_ESTERROR
condition|)
name|time_esterror
operator|=
name|ntv
operator|.
name|esterror
expr_stmt|;
if|if
condition|(
name|modes
operator|&
name|MOD_STATUS
condition|)
block|{
name|time_status
operator|&=
name|STA_RONLY
expr_stmt|;
name|time_status
operator||=
name|ntv
operator|.
name|status
operator|&
operator|~
name|STA_RONLY
expr_stmt|;
block|}
if|if
condition|(
name|modes
operator|&
name|MOD_TIMECONST
condition|)
name|time_constant
operator|=
name|ntv
operator|.
name|constant
expr_stmt|;
if|if
condition|(
name|modes
operator|&
name|MOD_OFFSET
condition|)
name|hardupdate
argument_list|(
name|ntv
operator|.
name|offset
argument_list|)
expr_stmt|;
comment|/* 	 * Retrieve all clock variables 	 */
if|if
condition|(
name|time_offset
operator|<
literal|0
condition|)
name|ntv
operator|.
name|offset
operator|=
operator|-
operator|(
operator|-
name|time_offset
operator|>>
name|SHIFT_UPDATE
operator|)
expr_stmt|;
else|else
name|ntv
operator|.
name|offset
operator|=
name|time_offset
operator|>>
name|SHIFT_UPDATE
expr_stmt|;
ifdef|#
directive|ifdef
name|PPS_SYNC
name|ntv
operator|.
name|freq
operator|=
name|time_freq
operator|+
name|pps_freq
expr_stmt|;
else|#
directive|else
comment|/* PPS_SYNC */
name|ntv
operator|.
name|freq
operator|=
name|time_freq
expr_stmt|;
endif|#
directive|endif
comment|/* PPS_SYNC */
name|ntv
operator|.
name|maxerror
operator|=
name|time_maxerror
expr_stmt|;
name|ntv
operator|.
name|esterror
operator|=
name|time_esterror
expr_stmt|;
name|ntv
operator|.
name|status
operator|=
name|time_status
expr_stmt|;
name|ntv
operator|.
name|constant
operator|=
name|time_constant
expr_stmt|;
name|ntv
operator|.
name|precision
operator|=
name|time_precision
expr_stmt|;
name|ntv
operator|.
name|tolerance
operator|=
name|time_tolerance
expr_stmt|;
ifdef|#
directive|ifdef
name|PPS_SYNC
name|ntv
operator|.
name|shift
operator|=
name|pps_shift
expr_stmt|;
name|ntv
operator|.
name|ppsfreq
operator|=
name|pps_freq
expr_stmt|;
name|ntv
operator|.
name|jitter
operator|=
name|pps_jitter
operator|>>
name|PPS_AVG
expr_stmt|;
name|ntv
operator|.
name|stabil
operator|=
name|pps_stabil
expr_stmt|;
name|ntv
operator|.
name|calcnt
operator|=
name|pps_calcnt
expr_stmt|;
name|ntv
operator|.
name|errcnt
operator|=
name|pps_errcnt
expr_stmt|;
name|ntv
operator|.
name|jitcnt
operator|=
name|pps_jitcnt
expr_stmt|;
name|ntv
operator|.
name|stbcnt
operator|=
name|pps_stbcnt
expr_stmt|;
endif|#
directive|endif
comment|/* PPS_SYNC */
operator|(
name|void
operator|)
name|splx
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|error
operator|=
name|copyout
argument_list|(
operator|(
name|caddr_t
operator|)
operator|&
name|ntv
argument_list|,
operator|(
name|caddr_t
operator|)
name|uap
operator|->
name|tp
argument_list|,
sizeof|sizeof
argument_list|(
name|ntv
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|error
condition|)
block|{
comment|/* 		 * Status word error decode. See comments in 		 * ntp_gettime() routine. 		 */
name|p
operator|->
name|p_retval
index|[
literal|0
index|]
operator|=
name|time_state
expr_stmt|;
if|if
condition|(
name|time_status
operator|&
operator|(
name|STA_UNSYNC
operator||
name|STA_CLOCKERR
operator|)
condition|)
name|p
operator|->
name|p_retval
index|[
literal|0
index|]
operator|=
name|TIME_ERROR
expr_stmt|;
if|if
condition|(
name|time_status
operator|&
operator|(
name|STA_PPSFREQ
operator||
name|STA_PPSTIME
operator|)
operator|&&
operator|!
operator|(
name|time_status
operator|&
name|STA_PPSSIGNAL
operator|)
condition|)
name|p
operator|->
name|p_retval
index|[
literal|0
index|]
operator|=
name|TIME_ERROR
expr_stmt|;
if|if
condition|(
name|time_status
operator|&
name|STA_PPSTIME
operator|&&
name|time_status
operator|&
name|STA_PPSJITTER
condition|)
name|p
operator|->
name|p_retval
index|[
literal|0
index|]
operator|=
name|TIME_ERROR
expr_stmt|;
if|if
condition|(
name|time_status
operator|&
name|STA_PPSFREQ
operator|&&
name|time_status
operator|&
operator|(
name|STA_PPSWANDER
operator||
name|STA_PPSERROR
operator|)
condition|)
name|p
operator|->
name|p_retval
index|[
literal|0
index|]
operator|=
name|TIME_ERROR
expr_stmt|;
block|}
return|return
name|error
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|PPS_SYNC
end_ifdef

begin_comment
comment|/* We need this ugly monster twice, so let's macroize it. */
end_comment

begin_define
define|#
directive|define
name|MEDIAN3X
parameter_list|(
name|a
parameter_list|,
name|m
parameter_list|,
name|s
parameter_list|,
name|i1
parameter_list|,
name|i2
parameter_list|,
name|i3
parameter_list|)
define|\
value|do {							\ 	m = a[i2];						\ 	s = a[i1] - a[i3];					\ 	} while (0)
end_define

begin_define
define|#
directive|define
name|MEDIAN3
parameter_list|(
name|a
parameter_list|,
name|m
parameter_list|,
name|s
parameter_list|)
define|\
value|do {							\ 		if (a[0]> a[1]) {				\ 			if (a[1]> a[2])			\ 				MEDIAN3X(a, m, s, 0, 1, 2);	\ 			else if (a[2]> a[0])			\ 				MEDIAN3X(a, m, s, 2, 0, 1);	\ 			else					\ 				MEDIAN3X(a, m, s, 0, 2, 1);	\ 		} else {					\ 			if (a[2]> a[1])			\ 				MEDIAN3X(a, m, s, 2, 1, 0);	\ 			else  if (a[0]> a[2])			\ 				MEDIAN3X(a, m, s, 1, 0, 2);	\ 			else					\ 				MEDIAN3X(a, m, s, 1, 2, 0);	\ 		}						\ 	} while (0)
end_define

begin_comment
comment|/*  * hardpps() - discipline CPU clock oscillator to external PPS signal  *  * This routine is called at each PPS interrupt in order to discipline  * the CPU clock oscillator to the PPS signal. It measures the PPS phase  * and leaves it in a handy spot for the hardclock() routine. It  * integrates successive PPS phase differences and calculates the  * frequency offset. This is used in hardclock() to discipline the CPU  * clock oscillator so that intrinsic frequency error is cancelled out.  * The code requires the caller to capture the time and hardware counter  * value at the on-time PPS signal transition.  *  * Note that, on some Unix systems, this routine runs at an interrupt  * priority level higher than the timer interrupt routine hardclock().  * Therefore, the variables used are distinct from the hardclock()  * variables, except for certain exceptions: The PPS frequency pps_freq  * and phase pps_offset variables are determined by this routine and  * updated atomically. The time_tolerance variable can be considered a  * constant, since it is infrequently changed, and then only when the  * PPS signal is disabled. The watchdog counter pps_valid is updated  * once per second by hardclock() and is atomically cleared in this  * routine.  */
end_comment

begin_function
name|void
name|hardpps
parameter_list|(
name|tvp
parameter_list|,
name|p_usec
parameter_list|)
name|struct
name|timeval
modifier|*
name|tvp
decl_stmt|;
comment|/* time at PPS */
name|long
name|p_usec
decl_stmt|;
comment|/* hardware counter at PPS */
block|{
name|long
name|u_usec
decl_stmt|,
name|v_usec
decl_stmt|,
name|bigtick
decl_stmt|;
name|long
name|cal_sec
decl_stmt|,
name|cal_usec
decl_stmt|;
comment|/* 	 * An occasional glitch can be produced when the PPS interrupt 	 * occurs in the hardclock() routine before the time variable is 	 * updated. Here the offset is discarded when the difference 	 * between it and the last one is greater than tick/2, but not 	 * if the interval since the first discard exceeds 30 s. 	 */
name|time_status
operator||=
name|STA_PPSSIGNAL
expr_stmt|;
name|time_status
operator|&=
operator|~
operator|(
name|STA_PPSJITTER
operator||
name|STA_PPSWANDER
operator||
name|STA_PPSERROR
operator|)
expr_stmt|;
name|pps_valid
operator|=
literal|0
expr_stmt|;
name|u_usec
operator|=
operator|-
name|tvp
operator|->
name|tv_usec
expr_stmt|;
if|if
condition|(
name|u_usec
operator|<
operator|-
literal|500000
condition|)
name|u_usec
operator|+=
literal|1000000
expr_stmt|;
name|v_usec
operator|=
name|pps_offset
operator|-
name|u_usec
expr_stmt|;
if|if
condition|(
name|v_usec
operator|<
literal|0
condition|)
name|v_usec
operator|=
operator|-
name|v_usec
expr_stmt|;
if|if
condition|(
name|v_usec
operator|>
operator|(
name|tick
operator|>>
literal|1
operator|)
condition|)
block|{
if|if
condition|(
name|pps_glitch
operator|>
name|MAXGLITCH
condition|)
block|{
name|pps_glitch
operator|=
literal|0
expr_stmt|;
name|pps_tf
index|[
literal|2
index|]
operator|=
name|u_usec
expr_stmt|;
name|pps_tf
index|[
literal|1
index|]
operator|=
name|u_usec
expr_stmt|;
block|}
else|else
block|{
name|pps_glitch
operator|++
expr_stmt|;
name|u_usec
operator|=
name|pps_offset
expr_stmt|;
block|}
block|}
else|else
name|pps_glitch
operator|=
literal|0
expr_stmt|;
comment|/* 	 * A three-stage median filter is used to help deglitch the pps 	 * time. The median sample becomes the time offset estimate; the 	 * difference between the other two samples becomes the time 	 * dispersion (jitter) estimate. 	 */
name|pps_tf
index|[
literal|2
index|]
operator|=
name|pps_tf
index|[
literal|1
index|]
expr_stmt|;
name|pps_tf
index|[
literal|1
index|]
operator|=
name|pps_tf
index|[
literal|0
index|]
expr_stmt|;
name|pps_tf
index|[
literal|0
index|]
operator|=
name|u_usec
expr_stmt|;
name|MEDIAN3
argument_list|(
name|pps_tf
argument_list|,
name|pps_offset
argument_list|,
name|v_usec
argument_list|)
expr_stmt|;
if|if
condition|(
name|v_usec
operator|>
name|MAXTIME
condition|)
name|pps_jitcnt
operator|++
expr_stmt|;
name|v_usec
operator|=
operator|(
name|v_usec
operator|<<
name|PPS_AVG
operator|)
operator|-
name|pps_jitter
expr_stmt|;
if|if
condition|(
name|v_usec
operator|<
literal|0
condition|)
name|pps_jitter
operator|-=
operator|-
name|v_usec
operator|>>
name|PPS_AVG
expr_stmt|;
else|else
name|pps_jitter
operator|+=
name|v_usec
operator|>>
name|PPS_AVG
expr_stmt|;
if|if
condition|(
name|pps_jitter
operator|>
operator|(
name|MAXTIME
operator|>>
literal|1
operator|)
condition|)
name|time_status
operator||=
name|STA_PPSJITTER
expr_stmt|;
comment|/* 	 * During the calibration interval adjust the starting time when 	 * the tick overflows. At the end of the interval compute the 	 * duration of the interval and the difference of the hardware 	 * counters at the beginning and end of the interval. This code 	 * is deliciously complicated by the fact valid differences may 	 * exceed the value of tick when using long calibration 	 * intervals and small ticks. Note that the counter can be 	 * greater than tick if caught at just the wrong instant, but 	 * the values returned and used here are correct. 	 */
name|bigtick
operator|=
operator|(
name|long
operator|)
name|tick
operator|<<
name|SHIFT_USEC
expr_stmt|;
name|pps_usec
operator|-=
name|pps_freq
expr_stmt|;
if|if
condition|(
name|pps_usec
operator|>=
name|bigtick
condition|)
name|pps_usec
operator|-=
name|bigtick
expr_stmt|;
if|if
condition|(
name|pps_usec
operator|<
literal|0
condition|)
name|pps_usec
operator|+=
name|bigtick
expr_stmt|;
name|pps_time
operator|.
name|tv_sec
operator|++
expr_stmt|;
name|pps_count
operator|++
expr_stmt|;
if|if
condition|(
name|pps_count
operator|<
operator|(
literal|1
operator|<<
name|pps_shift
operator|)
condition|)
return|return;
name|pps_count
operator|=
literal|0
expr_stmt|;
name|pps_calcnt
operator|++
expr_stmt|;
name|u_usec
operator|=
name|p_usec
operator|<<
name|SHIFT_USEC
expr_stmt|;
name|v_usec
operator|=
name|pps_usec
operator|-
name|u_usec
expr_stmt|;
if|if
condition|(
name|v_usec
operator|>=
name|bigtick
operator|>>
literal|1
condition|)
name|v_usec
operator|-=
name|bigtick
expr_stmt|;
if|if
condition|(
name|v_usec
operator|<
operator|-
operator|(
name|bigtick
operator|>>
literal|1
operator|)
condition|)
name|v_usec
operator|+=
name|bigtick
expr_stmt|;
if|if
condition|(
name|v_usec
operator|<
literal|0
condition|)
name|v_usec
operator|=
operator|-
operator|(
operator|-
name|v_usec
operator|>>
name|pps_shift
operator|)
expr_stmt|;
else|else
name|v_usec
operator|=
name|v_usec
operator|>>
name|pps_shift
expr_stmt|;
name|pps_usec
operator|=
name|u_usec
expr_stmt|;
name|cal_sec
operator|=
name|tvp
operator|->
name|tv_sec
expr_stmt|;
name|cal_usec
operator|=
name|tvp
operator|->
name|tv_usec
expr_stmt|;
name|cal_sec
operator|-=
name|pps_time
operator|.
name|tv_sec
expr_stmt|;
name|cal_usec
operator|-=
name|pps_time
operator|.
name|tv_usec
expr_stmt|;
if|if
condition|(
name|cal_usec
operator|<
literal|0
condition|)
block|{
name|cal_usec
operator|+=
literal|1000000
expr_stmt|;
name|cal_sec
operator|--
expr_stmt|;
block|}
name|pps_time
operator|=
operator|*
name|tvp
expr_stmt|;
comment|/* 	 * Check for lost interrupts, noise, excessive jitter and 	 * excessive frequency error. The number of timer ticks during 	 * the interval may vary +-1 tick. Add to this a margin of one 	 * tick for the PPS signal jitter and maximum frequency 	 * deviation. If the limits are exceeded, the calibration 	 * interval is reset to the minimum and we start over. 	 */
name|u_usec
operator|=
operator|(
name|long
operator|)
name|tick
operator|<<
literal|1
expr_stmt|;
if|if
condition|(
operator|!
operator|(
operator|(
name|cal_sec
operator|==
operator|-
literal|1
operator|&&
name|cal_usec
operator|>
operator|(
literal|1000000
operator|-
name|u_usec
operator|)
operator|)
operator|||
operator|(
name|cal_sec
operator|==
literal|0
operator|&&
name|cal_usec
operator|<
name|u_usec
operator|)
operator|)
operator|||
name|v_usec
operator|>
name|time_tolerance
operator|||
name|v_usec
operator|<
operator|-
name|time_tolerance
condition|)
block|{
name|pps_errcnt
operator|++
expr_stmt|;
name|pps_shift
operator|=
name|PPS_SHIFT
expr_stmt|;
name|pps_intcnt
operator|=
literal|0
expr_stmt|;
name|time_status
operator||=
name|STA_PPSERROR
expr_stmt|;
return|return;
block|}
comment|/* 	 * A three-stage median filter is used to help deglitch the pps 	 * frequency. The median sample becomes the frequency offset 	 * estimate; the difference between the other two samples 	 * becomes the frequency dispersion (stability) estimate. 	 */
name|pps_ff
index|[
literal|2
index|]
operator|=
name|pps_ff
index|[
literal|1
index|]
expr_stmt|;
name|pps_ff
index|[
literal|1
index|]
operator|=
name|pps_ff
index|[
literal|0
index|]
expr_stmt|;
name|pps_ff
index|[
literal|0
index|]
operator|=
name|v_usec
expr_stmt|;
name|MEDIAN3
argument_list|(
name|pps_ff
argument_list|,
name|u_usec
argument_list|,
name|v_usec
argument_list|)
expr_stmt|;
comment|/* 	 * Here the frequency dispersion (stability) is updated. If it 	 * is less than one-fourth the maximum (MAXFREQ), the frequency 	 * offset is updated as well, but clamped to the tolerance. It 	 * will be processed later by the hardclock() routine. 	 */
name|v_usec
operator|=
operator|(
name|v_usec
operator|>>
literal|1
operator|)
operator|-
name|pps_stabil
expr_stmt|;
if|if
condition|(
name|v_usec
operator|<
literal|0
condition|)
name|pps_stabil
operator|-=
operator|-
name|v_usec
operator|>>
name|PPS_AVG
expr_stmt|;
else|else
name|pps_stabil
operator|+=
name|v_usec
operator|>>
name|PPS_AVG
expr_stmt|;
if|if
condition|(
name|pps_stabil
operator|>
name|MAXFREQ
operator|>>
literal|2
condition|)
block|{
name|pps_stbcnt
operator|++
expr_stmt|;
name|time_status
operator||=
name|STA_PPSWANDER
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|time_status
operator|&
name|STA_PPSFREQ
condition|)
block|{
if|if
condition|(
name|u_usec
operator|<
literal|0
condition|)
block|{
name|pps_freq
operator|-=
operator|-
name|u_usec
operator|>>
name|PPS_AVG
expr_stmt|;
if|if
condition|(
name|pps_freq
operator|<
operator|-
name|time_tolerance
condition|)
name|pps_freq
operator|=
operator|-
name|time_tolerance
expr_stmt|;
name|u_usec
operator|=
operator|-
name|u_usec
expr_stmt|;
block|}
else|else
block|{
name|pps_freq
operator|+=
name|u_usec
operator|>>
name|PPS_AVG
expr_stmt|;
if|if
condition|(
name|pps_freq
operator|>
name|time_tolerance
condition|)
name|pps_freq
operator|=
name|time_tolerance
expr_stmt|;
block|}
block|}
comment|/* 	 * Here the calibration interval is adjusted. If the maximum 	 * time difference is greater than tick / 4, reduce the interval 	 * by half. If this is not the case for four consecutive 	 * intervals, double the interval. 	 */
if|if
condition|(
name|u_usec
operator|<<
name|pps_shift
operator|>
name|bigtick
operator|>>
literal|2
condition|)
block|{
name|pps_intcnt
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|pps_shift
operator|>
name|PPS_SHIFT
condition|)
name|pps_shift
operator|--
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|pps_intcnt
operator|>=
literal|4
condition|)
block|{
name|pps_intcnt
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|pps_shift
operator|<
name|PPS_SHIFTMAX
condition|)
name|pps_shift
operator|++
expr_stmt|;
block|}
else|else
name|pps_intcnt
operator|++
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* PPS_SYNC */
end_comment

end_unit

