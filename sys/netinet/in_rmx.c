begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright 1994, Massachusetts Institute of Technology.  All Rights Reserved.  *  * You may copy this file verbatim until I find the official   * Institute boilerplate.  *  * $Id$  */
end_comment

begin_comment
comment|/*  * This code does two things necessary for the enhanced TCP metrics to  * function in a useful manner:  *  1) It marks all non-host routes as `cloning', thus ensuring that  *     every actual reference to such a route actually gets turned  *     into a reference to a host route to the specific destination  *     requested.  *  2) When such routes lose all their references, it arranges for them  *     to be deleted in some random collection of circumstances, so that  *     a large quantity of stale routing data is not kept in kernel memory  *     indefinitely.  See in_rtqtimo() below for the exact mechanism.  *  * At least initially, we think that this should have lower overhead than  * using the existing `expire' mechanism and walking the radix tree  * periodically, deleting things as we go.  That method would be relatively  * easy to implement within the framework used here, and in the future  * we made code both ways, so that folks with large routing tables can use  * the external queue, and the majority with small routing tables can do  * the tree-walk.  */
end_comment

begin_comment
comment|/*  * XXX - look for races  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<sys/socketvar.h>
end_include

begin_include
include|#
directive|include
file|<sys/mbuf.h>
end_include

begin_include
include|#
directive|include
file|<net/if.h>
end_include

begin_include
include|#
directive|include
file|<net/route.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in_systm.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in_var.h>
end_include

begin_comment
comment|/*  * The list of unreferenced IP routes...  */
end_comment

begin_struct
struct|struct
name|in_rtq
block|{
name|TAILQ_ENTRY
argument_list|(
argument|in_rtq
argument_list|)
name|inr_entry
expr_stmt|;
name|struct
name|rtentry
modifier|*
name|inr_rt
decl_stmt|;
name|time_t
name|inr_whenadded
decl_stmt|;
block|}
struct|;
end_struct

begin_macro
name|TAILQ_HEAD
argument_list|(
argument_list|,
argument|in_rtq
argument_list|)
end_macro

begin_expr_stmt
name|inr_head
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|inr_nelem
init|=
operator|-
literal|1
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Do what we need to do when inserting a route.  * Note that we don't automatically add the route to the queue, because  * our caller, rtrequest(), will immediately bump the refcount and give  * a the route to someone.  */
end_comment

begin_function
specifier|static
name|struct
name|radix_node
modifier|*
name|in_addroute
parameter_list|(
name|void
modifier|*
name|v_arg
parameter_list|,
name|void
modifier|*
name|n_arg
parameter_list|,
name|struct
name|radix_node_head
modifier|*
name|head
parameter_list|,
name|struct
name|radix_node
modifier|*
name|treenodes
parameter_list|)
block|{
name|struct
name|rtentry
modifier|*
name|rt
init|=
operator|(
expr|struct
name|rtentry
operator|*
operator|)
name|treenodes
decl_stmt|;
name|struct
name|radix_node
modifier|*
name|rn
decl_stmt|;
name|struct
name|in_rtq
modifier|*
name|inr
decl_stmt|;
comment|/* 	 * For IP, all non-host routes are automatically cloning. 	 */
if|if
condition|(
operator|!
operator|(
name|rt
operator|->
name|rt_flags
operator|&
name|RTF_HOST
operator|)
condition|)
name|rt
operator|->
name|rt_flags
operator||=
name|RTF_CLONING
expr_stmt|;
name|rn
operator|=
name|rn_addroute
argument_list|(
name|v_arg
argument_list|,
name|n_arg
argument_list|,
name|head
argument_list|,
name|treenodes
argument_list|)
expr_stmt|;
name|rt
operator|=
operator|(
expr|struct
name|rtentry
operator|*
operator|)
name|rn
expr_stmt|;
if|if
condition|(
operator|!
name|rt
operator|||
operator|!
operator|(
name|rt
operator|->
name|rt_flags
operator|&
name|RTF_HOST
operator|)
operator|||
operator|(
name|rt
operator|->
name|rt_flags
operator|&
operator|(
name|RTF_LLINFO
operator||
name|RTF_STATIC
operator||
name|RTF_DYNAMIC
operator|)
operator|)
condition|)
return|return
name|rn
return|;
return|return
name|rn
return|;
block|}
end_function

begin_function
specifier|static
name|struct
name|radix_node
modifier|*
name|in_delroute
parameter_list|(
name|void
modifier|*
name|v_arg
parameter_list|,
name|void
modifier|*
name|netmask_arg
parameter_list|,
name|struct
name|radix_node_head
modifier|*
name|head
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|DIAGNOSTIC
name|int
name|nentries
init|=
literal|0
decl_stmt|;
endif|#
directive|endif
name|struct
name|rtentry
modifier|*
name|rt
init|=
operator|(
expr|struct
name|rtentry
operator|*
operator|)
name|rn_search
argument_list|(
name|v_arg
argument_list|,
name|head
operator|->
name|rnh_treetop
argument_list|)
decl_stmt|;
if|if
condition|(
name|rt
operator|&&
name|rt
operator|->
name|rt_refcnt
operator|<=
literal|0
operator|&&
operator|(
operator|(
name|rt
operator|->
name|rt_flags
operator|&
operator|(
name|RTF_HOST
operator||
name|RTF_LLINFO
operator||
name|RTF_STATIC
operator|)
operator|)
operator|==
name|RTF_HOST
operator|)
condition|)
block|{
name|struct
name|in_rtq
modifier|*
name|inr
init|=
name|inr_head
operator|.
name|tqh_first
decl_stmt|;
while|while
condition|(
name|inr
condition|)
block|{
if|if
condition|(
name|inr
operator|->
name|inr_rt
operator|==
name|rt
condition|)
block|{
name|TAILQ_REMOVE
argument_list|(
operator|&
name|inr_head
argument_list|,
name|inr
argument_list|,
name|inr_entry
argument_list|)
expr_stmt|;
name|inr_nelem
operator|--
expr_stmt|;
name|FREE
argument_list|(
name|inr
argument_list|,
name|M_RTABLE
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DIAGNOSTIC
name|nentries
operator|++
expr_stmt|;
else|#
directive|else
break|break;
endif|#
directive|endif
block|}
name|inr
operator|=
name|inr
operator|->
name|inr_entry
operator|.
name|tqe_next
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DIAGNOSTIC
if|if
condition|(
name|nentries
operator|!=
literal|1
condition|)
block|{
name|log
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"route %p had %d queue entries!\n"
argument_list|,
operator|(
name|void
operator|*
operator|)
name|rt
argument_list|,
name|nentries
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
return|return
name|rn_delete
argument_list|(
name|v_arg
argument_list|,
name|netmask_arg
argument_list|,
name|head
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * Find something in the queue.  */
end_comment

begin_function
specifier|static
specifier|inline
name|struct
name|in_rtq
modifier|*
name|inr_findit
parameter_list|(
name|struct
name|rtentry
modifier|*
name|rt
parameter_list|)
block|{
name|struct
name|in_rtq
modifier|*
name|inr
decl_stmt|;
ifndef|#
directive|ifndef
name|DIAGNOSTIC
if|if
condition|(
operator|(
name|rt
operator|->
name|rt_flags
operator|&
operator|(
name|RTF_HOST
operator||
name|RTF_LLINFO
operator||
name|RTF_STATIC
operator|)
operator|)
operator|!=
name|RTF_HOST
condition|)
return|return
literal|0
return|;
endif|#
directive|endif
name|inr
operator|=
name|inr_head
operator|.
name|tqh_first
expr_stmt|;
while|while
condition|(
name|inr
condition|)
block|{
if|if
condition|(
name|inr
operator|->
name|inr_rt
operator|==
name|rt
condition|)
return|return
name|inr
return|;
name|inr
operator|=
name|inr
operator|->
name|inr_entry
operator|.
name|tqe_next
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * This code is the inverse of in_clsroute: on first reference, we remove the  * route from our queue (if it's in there)  */
end_comment

begin_function
specifier|static
name|struct
name|radix_node
modifier|*
name|in_matroute
parameter_list|(
name|void
modifier|*
name|v_arg
parameter_list|,
name|struct
name|radix_node_head
modifier|*
name|head
parameter_list|)
block|{
name|struct
name|radix_node
modifier|*
name|rn
init|=
name|rn_match
argument_list|(
name|v_arg
argument_list|,
name|head
argument_list|)
decl_stmt|;
name|struct
name|rtentry
modifier|*
name|rt
init|=
operator|(
expr|struct
name|rtentry
operator|*
operator|)
name|rn
decl_stmt|;
if|if
condition|(
name|rt
operator|&&
name|rt
operator|->
name|rt_refcnt
operator|==
literal|0
condition|)
block|{
comment|/* this is first reference */
name|struct
name|in_rtq
modifier|*
name|inr
init|=
name|inr_findit
argument_list|(
name|rt
argument_list|)
decl_stmt|;
if|if
condition|(
name|inr
condition|)
block|{
name|TAILQ_REMOVE
argument_list|(
operator|&
name|inr_head
argument_list|,
name|inr
argument_list|,
name|inr_entry
argument_list|)
expr_stmt|;
name|inr_nelem
operator|--
expr_stmt|;
name|FREE
argument_list|(
name|inr
argument_list|,
name|M_RTABLE
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|rn
return|;
block|}
end_function

begin_comment
comment|/*  * On last reference drop, add the route to the queue so that it can be  * timed out.  */
end_comment

begin_function
specifier|static
name|void
name|in_clsroute
parameter_list|(
name|struct
name|radix_node
modifier|*
name|rn
parameter_list|,
name|struct
name|radix_node_head
modifier|*
name|head
parameter_list|)
block|{
name|struct
name|rtentry
modifier|*
name|rt
init|=
operator|(
expr|struct
name|rtentry
operator|*
operator|)
name|rn
decl_stmt|;
name|struct
name|in_rtq
modifier|*
name|inr
decl_stmt|;
name|inr
operator|=
name|inr_findit
argument_list|(
name|rt
argument_list|)
expr_stmt|;
if|if
condition|(
name|inr
condition|)
block|{
comment|/* 		 * In this case, we are probably in the process of deleting 		 * this route; the code path in route.c results in rnh_close() 		 * being called when during deletion of unreferenced routes. 		 * In any case, don't allocate a new queue entry for this 		 * route because there already is one. 		 */
return|return;
block|}
name|MALLOC
argument_list|(
name|inr
argument_list|,
expr|struct
name|in_rtq
operator|*
argument_list|,
sizeof|sizeof
expr|*
name|inr
argument_list|,
name|M_RTABLE
argument_list|,
name|M_NOWAIT
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|inr
condition|)
return|return;
comment|/* oops... no matter */
name|inr
operator|->
name|inr_rt
operator|=
name|rt
expr_stmt|;
name|inr
operator|->
name|inr_whenadded
operator|=
name|time
operator|.
name|tv_sec
expr_stmt|;
name|TAILQ_INSERT_TAIL
argument_list|(
operator|&
name|inr_head
argument_list|,
name|inr
argument_list|,
name|inr_entry
argument_list|)
expr_stmt|;
name|inr_nelem
operator|++
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Get rid of everything in the queue, we are short on memory.  * Although this looks like an infinite loop, it really isn't;  * rtrequest() eventually calls in_delroute() which ends up deleting  * the node at the head (or so we hope).  This should be called from  * ip_drain().  */
end_comment

begin_function
name|void
name|in_rtqdrain
parameter_list|(
name|void
parameter_list|)
block|{
name|struct
name|in_rtq
modifier|*
name|inr
decl_stmt|;
while|while
condition|(
name|inr
operator|=
name|inr_head
operator|.
name|tqh_first
condition|)
block|{
name|rtrequest
argument_list|(
name|RTM_DELETE
argument_list|,
name|rt_key
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|inr
operator|->
name|inr_rt
operator|->
name|rt_gateway
argument_list|,
name|rt_mask
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|RTF_HOST
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* KILL! KILL! KILL! */
block|}
block|}
end_function

begin_define
define|#
directive|define
name|RTQ_TIMEOUT
value|(60*hz)
end_define

begin_comment
comment|/* run once a minute */
end_comment

begin_define
define|#
directive|define
name|RTQ_REALLYOLD
value|4*60*60
end_define

begin_comment
comment|/* four hours is ``really old'' */
end_comment

begin_define
define|#
directive|define
name|RTQ_TOOMANY
value|128
end_define

begin_comment
comment|/*> 128 routes is ``too many'' */
end_comment

begin_comment
comment|/*  * Get rid of old routes.  We have two strategies here:  * 1) If there are more than RTQ_TOOMANY routes, delete half of them.  * 2) Delete all routes older than RTQ_REALLYOLD (the LRU nature of the  *    queue ensures that these are all at the front).  */
end_comment

begin_function
specifier|static
name|void
name|in_rtqtimo
parameter_list|(
name|void
modifier|*
name|rock
parameter_list|)
block|{
name|int
name|s
init|=
name|splnet
argument_list|()
decl_stmt|;
name|struct
name|in_rtq
modifier|*
name|inr
init|=
name|inr_head
operator|.
name|tqh_first
decl_stmt|;
if|if
condition|(
name|inr_nelem
operator|>
name|RTQ_TOOMANY
condition|)
block|{
name|int
name|ntodel
init|=
name|inr_nelem
operator|/
literal|2
decl_stmt|;
for|for
control|(
init|;
name|inr
operator|&&
name|ntodel
condition|;
name|inr
operator|=
name|inr_head
operator|.
name|tqh_first
operator|,
name|ntodel
operator|--
control|)
block|{
name|rtrequest
argument_list|(
name|RTM_DELETE
argument_list|,
name|rt_key
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|inr
operator|->
name|inr_rt
operator|->
name|rt_gateway
argument_list|,
name|rt_mask
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|RTF_HOST
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
block|}
while|while
condition|(
name|inr
operator|&&
operator|(
name|time
operator|.
name|tv_sec
operator|-
name|inr
operator|->
name|inr_whenadded
operator|)
operator|>
name|RTQ_REALLYOLD
condition|)
block|{
name|rtrequest
argument_list|(
name|RTM_DELETE
argument_list|,
name|rt_key
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|inr
operator|->
name|inr_rt
operator|->
name|rt_gateway
argument_list|,
name|rt_mask
argument_list|(
name|inr
operator|->
name|inr_rt
argument_list|)
argument_list|,
name|RTF_HOST
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|inr
operator|=
name|inr_head
operator|.
name|tqh_first
expr_stmt|;
block|}
name|timeout
argument_list|(
name|in_rtqtimo
argument_list|,
operator|(
name|void
operator|*
operator|)
literal|0
argument_list|,
name|RTQ_TIMEOUT
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Initialize our routing tree.  */
end_comment

begin_function
name|int
name|in_inithead
parameter_list|(
name|void
modifier|*
modifier|*
name|head
parameter_list|,
name|int
name|off
parameter_list|)
block|{
name|struct
name|radix_node_head
modifier|*
name|rnh
decl_stmt|;
if|if
condition|(
operator|!
name|rn_inithead
argument_list|(
name|head
argument_list|,
name|off
argument_list|)
condition|)
return|return
literal|0
return|;
name|rnh
operator|=
operator|*
name|head
expr_stmt|;
name|rnh
operator|->
name|rnh_addaddr
operator|=
name|in_addroute
expr_stmt|;
name|rnh
operator|->
name|rnh_deladdr
operator|=
name|in_delroute
expr_stmt|;
name|rnh
operator|->
name|rnh_matchaddr
operator|=
name|in_matroute
expr_stmt|;
name|rnh
operator|->
name|rnh_close
operator|=
name|in_clsroute
expr_stmt|;
name|TAILQ_INIT
argument_list|(
operator|&
name|inr_head
argument_list|)
expr_stmt|;
name|inr_nelem
operator|=
literal|0
expr_stmt|;
name|in_rtqtimo
argument_list|(
literal|0
argument_list|)
expr_stmt|;
comment|/* kick off timeout first time */
return|return
literal|1
return|;
block|}
end_function

end_unit

