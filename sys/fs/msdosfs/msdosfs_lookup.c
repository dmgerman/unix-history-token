begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Written by Paul Popelka (paulp@uts.amdahl.com)  *   * You can do anything you want with this software, just don't say you wrote  * it, and don't remove this notice.  *   * This software is provided "as is".  *   * The author supplies this software to be publicly redistributed on the  * understanding that the author is not responsible for the correct  * functioning of this software in any circumstances and is not liable for  * any damages caused by this software.  *   * October 1992  *   *	from NetBSD:	msdosfs_lookup.c,v 1.5 1993/12/18 00:51:09 mycroft Exp  *	$Id: msdosfs_lookup.c,v 1.1 1994/01/24 06:04:55 rgrimes Exp $  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/namei.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/mount.h>
end_include

begin_include
include|#
directive|include
file|<fs/msdosfs/bpb.h>
end_include

begin_include
include|#
directive|include
file|<fs/msdosfs/direntry.h>
end_include

begin_include
include|#
directive|include
file|<fs/msdosfs/denode.h>
end_include

begin_include
include|#
directive|include
file|<fs/msdosfs/msdosfsmount.h>
end_include

begin_include
include|#
directive|include
file|<fs/msdosfs/fat.h>
end_include

begin_comment
comment|/*  * When we search a directory the blocks containing directory entries are  * read and examined.  The directory entries contain information that would  * normally be in the inode of a unix filesystem.  This means that some of  * a directory's contents may also be in memory resident denodes (sort of  * an inode).  This can cause problems if we are searching while some other  * process is modifying a directory.  To prevent one process from accessing  * incompletely modified directory information we depend upon being the  * soul owner of a directory block.  bread/brelse provide this service.  * This being the case, when a process modifies a directory it must first  * acquire the disk block that contains the directory entry to be modified.  * Then update the disk block and the denode, and then write the disk block  * out to disk.  This way disk blocks containing directory entries and in  * memory denode's will be in synch.  */
end_comment

begin_function
name|int
name|msdosfs_lookup
parameter_list|(
name|vdp
parameter_list|,
name|ndp
parameter_list|,
name|p
parameter_list|)
name|struct
name|vnode
modifier|*
name|vdp
decl_stmt|;
comment|/* vnode of directory to search		 */
name|struct
name|nameidata
modifier|*
name|ndp
decl_stmt|;
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
block|{
name|daddr_t
name|bn
decl_stmt|;
name|int
name|flag
decl_stmt|;
name|int
name|error
decl_stmt|;
name|int
name|lockparent
decl_stmt|;
name|int
name|wantparent
decl_stmt|;
name|int
name|slotstatus
decl_stmt|;
define|#
directive|define
name|NONE
value|0
define|#
directive|define
name|FOUND
value|1
name|int
name|slotoffset
decl_stmt|;
name|int
name|slotcluster
decl_stmt|;
name|int
name|frcn
decl_stmt|;
name|u_long
name|cluster
decl_stmt|;
name|int
name|rootreloff
decl_stmt|;
name|int
name|diroff
decl_stmt|;
name|int
name|isadir
decl_stmt|;
comment|/* ~0 if found direntry is a directory	 */
name|u_long
name|scn
decl_stmt|;
comment|/* starting cluster number		 */
name|struct
name|denode
modifier|*
name|dp
decl_stmt|;
name|struct
name|denode
modifier|*
name|pdp
decl_stmt|;
name|struct
name|denode
modifier|*
name|tdp
decl_stmt|;
name|struct
name|msdosfsmount
modifier|*
name|pmp
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
init|=
literal|0
decl_stmt|;
name|struct
name|direntry
modifier|*
name|dep
decl_stmt|;
name|u_char
name|dosfilename
index|[
literal|12
index|]
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): looking for %s\n"
argument_list|,
name|ndp
operator|->
name|ni_ptr
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
name|ndp
operator|->
name|ni_dvp
operator|=
name|vdp
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|NULL
expr_stmt|;
name|dp
operator|=
name|VTODE
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
name|pmp
operator|=
name|dp
operator|->
name|de_pmp
expr_stmt|;
name|lockparent
operator|=
name|ndp
operator|->
name|ni_nameiop
operator|&
name|LOCKPARENT
expr_stmt|;
name|flag
operator|=
name|ndp
operator|->
name|ni_nameiop
operator|&
name|OPMASK
expr_stmt|;
name|wantparent
operator|=
name|ndp
operator|->
name|ni_nameiop
operator|&
operator|(
name|LOCKPARENT
operator||
name|WANTPARENT
operator|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): vdp %08x, dp %08x, Attr %02x\n"
argument_list|,
name|vdp
argument_list|,
name|dp
argument_list|,
name|dp
operator|->
name|de_Attributes
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
comment|/* 	 * Be sure vdp is a directory.  Since dos filesystems don't have 	 * the concept of execute permission anybody can search a 	 * directory. 	 */
if|if
condition|(
operator|(
name|dp
operator|->
name|de_Attributes
operator|&
name|ATTR_DIRECTORY
operator|)
operator|==
literal|0
condition|)
return|return
name|ENOTDIR
return|;
comment|/* 	 * See if the component of the pathname we are looking for is in 	 * the directory cache.  If so then do a few things and return. 	 */
if|if
condition|(
name|error
operator|=
name|cache_lookup
argument_list|(
name|ndp
argument_list|)
condition|)
block|{
name|int
name|vpid
decl_stmt|;
if|if
condition|(
name|error
operator|==
name|ENOENT
condition|)
return|return
name|error
return|;
ifdef|#
directive|ifdef
name|PARANOID
if|if
condition|(
name|vdp
operator|==
name|ndp
operator|->
name|ni_rootdir
operator|&&
name|ndp
operator|->
name|ni_isdotdot
condition|)
name|panic
argument_list|(
literal|"msdosfs_lookup: .. thru root"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* PARANOID */
name|pdp
operator|=
name|dp
expr_stmt|;
name|vdp
operator|=
name|ndp
operator|->
name|ni_vp
expr_stmt|;
name|dp
operator|=
name|VTODE
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
name|vpid
operator|=
name|vdp
operator|->
name|v_id
expr_stmt|;
if|if
condition|(
name|pdp
operator|==
name|dp
condition|)
block|{
name|VREF
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
name|error
operator|=
literal|0
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|ndp
operator|->
name|ni_isdotdot
condition|)
block|{
name|DEUNLOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
name|error
operator|=
name|vget
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|error
operator|&&
name|lockparent
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
name|DELOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|error
operator|=
name|vget
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|lockparent
operator|||
name|error
operator|||
operator|*
name|ndp
operator|->
name|ni_next
operator|!=
literal|'\0'
condition|)
name|DEUNLOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|error
condition|)
block|{
if|if
condition|(
name|vpid
operator|==
name|vdp
operator|->
name|v_id
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): cache hit, vnode %08x, file %s\n"
argument_list|,
name|vdp
argument_list|,
name|dp
operator|->
name|de_Name
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
return|return
literal|0
return|;
block|}
name|deput
argument_list|(
name|dp
argument_list|)
expr_stmt|;
if|if
condition|(
name|lockparent
operator|&&
name|pdp
operator|!=
name|dp
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
name|DEUNLOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
block|}
name|DELOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
name|dp
operator|=
name|pdp
expr_stmt|;
name|vdp
operator|=
name|DETOV
argument_list|(
name|dp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|NULL
expr_stmt|;
block|}
comment|/* 	 * If they are going after the . or .. entry in the root directory, 	 * they won't find it.  DOS filesystems don't have them in the root 	 * directory.  So, we fake it. deget() is in on this scam too. 	 */
if|if
condition|(
operator|(
name|vdp
operator|->
name|v_flag
operator|&
name|VROOT
operator|)
operator|&&
name|ndp
operator|->
name|ni_ptr
index|[
literal|0
index|]
operator|==
literal|'.'
operator|&&
operator|(
name|ndp
operator|->
name|ni_namelen
operator|==
literal|1
operator|||
operator|(
name|ndp
operator|->
name|ni_namelen
operator|==
literal|2
operator|&&
name|ndp
operator|->
name|ni_ptr
index|[
literal|1
index|]
operator|==
literal|'.'
operator|)
operator|)
condition|)
block|{
name|isadir
operator|=
name|ATTR_DIRECTORY
expr_stmt|;
name|scn
operator|=
name|MSDOSFSROOT
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): looking for . or .. in root directory\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
name|cluster
operator|==
name|MSDOSFSROOT
expr_stmt|;
name|diroff
operator|=
name|MSDOSFSROOT_OFS
expr_stmt|;
goto|goto
name|foundroot
goto|;
block|}
comment|/* 	 * Don't search for free slots unless we are creating a filename 	 * and we are at the end of the pathname. 	 */
name|slotstatus
operator|=
name|FOUND
expr_stmt|;
if|if
condition|(
operator|(
name|flag
operator|==
name|CREATE
operator|||
name|flag
operator|==
name|RENAME
operator|)
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
block|{
name|slotstatus
operator|=
name|NONE
expr_stmt|;
name|slotoffset
operator|=
operator|-
literal|1
expr_stmt|;
block|}
name|unix2dosfn
argument_list|(
operator|(
name|u_char
operator|*
operator|)
name|ndp
operator|->
name|ni_ptr
argument_list|,
name|dosfilename
argument_list|,
name|ndp
operator|->
name|ni_namelen
argument_list|)
expr_stmt|;
name|dosfilename
index|[
literal|11
index|]
operator|=
literal|0
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): dos version of filename %s, length %d\n"
argument_list|,
name|dosfilename
argument_list|,
name|ndp
operator|->
name|ni_namelen
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
comment|/* 	 * Search the directory pointed at by vdp for the name pointed at 	 * by ndp->ni_ptr. 	 */
name|tdp
operator|=
name|NULL
expr_stmt|;
comment|/* 	 * The outer loop ranges over the clusters that make up the 	 * directory.  Note that the root directory is different from all 	 * other directories.  It has a fixed number of blocks that are not 	 * part of the pool of allocatable clusters.  So, we treat it a 	 * little differently. The root directory starts at "cluster" 0. 	 */
name|rootreloff
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|frcn
operator|=
literal|0
init|;
condition|;
name|frcn
operator|++
control|)
block|{
if|if
condition|(
name|error
operator|=
name|pcbmap
argument_list|(
name|dp
argument_list|,
name|frcn
argument_list|,
operator|&
name|bn
argument_list|,
operator|&
name|cluster
argument_list|)
condition|)
block|{
if|if
condition|(
name|error
operator|==
name|E2BIG
condition|)
break|break;
return|return
name|error
return|;
block|}
if|if
condition|(
name|error
operator|=
name|bread
argument_list|(
name|pmp
operator|->
name|pm_devvp
argument_list|,
name|bn
argument_list|,
name|pmp
operator|->
name|pm_bpcluster
argument_list|,
name|NOCRED
argument_list|,
operator|&
name|bp
argument_list|)
condition|)
return|return
name|error
return|;
for|for
control|(
name|diroff
operator|=
literal|0
init|;
name|diroff
operator|<
name|pmp
operator|->
name|pm_depclust
condition|;
name|diroff
operator|++
control|)
block|{
name|dep
operator|=
operator|(
expr|struct
name|direntry
operator|*
operator|)
name|bp
operator|->
name|b_un
operator|.
name|b_addr
operator|+
name|diroff
expr_stmt|;
comment|/* 			 * If the slot is empty and we are still looking 			 * for an empty then remember this one.  If the 			 * slot is not empty then check to see if it 			 * matches what we are looking for.  If the slot 			 * has never been filled with anything, then the 			 * remainder of the directory has never been used, 			 * so there is no point in searching it. 			 */
if|if
condition|(
name|dep
operator|->
name|deName
index|[
literal|0
index|]
operator|==
name|SLOT_EMPTY
operator|||
name|dep
operator|->
name|deName
index|[
literal|0
index|]
operator|==
name|SLOT_DELETED
condition|)
block|{
if|if
condition|(
name|slotstatus
operator|!=
name|FOUND
condition|)
block|{
name|slotstatus
operator|=
name|FOUND
expr_stmt|;
if|if
condition|(
name|cluster
operator|==
name|MSDOSFSROOT
condition|)
name|slotoffset
operator|=
name|rootreloff
expr_stmt|;
else|else
name|slotoffset
operator|=
name|diroff
expr_stmt|;
name|slotcluster
operator|=
name|cluster
expr_stmt|;
block|}
if|if
condition|(
name|dep
operator|->
name|deName
index|[
literal|0
index|]
operator|==
name|SLOT_EMPTY
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
goto|goto
name|notfound
goto|;
block|}
block|}
else|else
block|{
comment|/* 				 * Ignore volume labels (anywhere, not just 				 * the root directory). 				 */
if|if
condition|(
operator|(
name|dep
operator|->
name|deAttributes
operator|&
name|ATTR_VOLUME
operator|)
operator|==
literal|0
operator|&&
name|bcmp
argument_list|(
name|dosfilename
argument_list|,
name|dep
operator|->
name|deName
argument_list|,
literal|11
argument_list|)
operator|==
literal|0
condition|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): match diroff %d, rootreloff %d\n"
argument_list|,
name|diroff
argument_list|,
name|rootreloff
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
comment|/* 					 * Remember where this directory 					 * entry came from for whoever did 					 * this lookup. If this is the root 					 * directory we are interested in 					 * the offset relative to the 					 * beginning of the directory (not 					 * the beginning of the cluster). 					 */
if|if
condition|(
name|cluster
operator|==
name|MSDOSFSROOT
condition|)
name|diroff
operator|=
name|rootreloff
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
operator|=
name|diroff
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
operator|=
name|cluster
expr_stmt|;
goto|goto
name|found
goto|;
block|}
block|}
name|rootreloff
operator|++
expr_stmt|;
block|}
comment|/* for (diroff = 0; .... */
comment|/* 		 * Release the buffer holding the directory cluster just 		 * searched. 		 */
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
comment|/* for (frcn = 0; ; frcn++) */
name|notfound
label|:
empty_stmt|;
comment|/* 	 * We hold no disk buffers at this point. 	 */
comment|/* 	 * If we get here we didn't find the entry we were looking for. But 	 * that's ok if we are creating or renaming and are at the end of 	 * the pathname and the directory hasn't been removed. 	 */
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): flag %d, refcnt %d, slotstatus %d\n"
argument_list|,
name|flag
argument_list|,
name|dp
operator|->
name|de_refcnt
argument_list|,
name|slotstatus
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"               slotoffset %d, slotcluster %d\n"
argument_list|,
name|slotoffset
argument_list|,
name|slotcluster
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
if|if
condition|(
operator|(
name|flag
operator|==
name|CREATE
operator|||
name|flag
operator|==
name|RENAME
operator|)
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
operator|&&
name|dp
operator|->
name|de_refcnt
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|slotstatus
operator|==
name|NONE
condition|)
block|{
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
operator|=
literal|0
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
operator|=
literal|0
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_count
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"msdosfs_lookup(): saving empty slot location\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
operator|=
name|slotoffset
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
operator|=
name|slotcluster
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_count
operator|=
literal|1
expr_stmt|;
block|}
comment|/* dp->de_flag |= DEUPD; /* never update dos directories */
name|ndp
operator|->
name|ni_nameiop
operator||=
name|SAVENAME
expr_stmt|;
if|if
condition|(
operator|!
name|lockparent
condition|)
comment|/* leave searched dir locked?	 */
name|DEUNLOCK
argument_list|(
name|dp
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Insert name in cache as non-existant if not trying to create it. 	 */
if|if
condition|(
name|ndp
operator|->
name|ni_makeentry
operator|&&
name|flag
operator|!=
name|CREATE
condition|)
name|cache_enter
argument_list|(
name|ndp
argument_list|)
expr_stmt|;
if|if
condition|(
name|flag
operator|==
name|CREATE
operator|||
name|flag
operator|==
name|RENAME
condition|)
return|return
name|EJUSTRETURN
return|;
return|return
name|ENOENT
return|;
name|found
label|:
empty_stmt|;
comment|/* 	 * NOTE:  We still have the buffer with matched directory entry at 	 * this point. 	 */
name|isadir
operator|=
name|dep
operator|->
name|deAttributes
operator|&
name|ATTR_DIRECTORY
expr_stmt|;
name|scn
operator|=
name|dep
operator|->
name|deStartCluster
expr_stmt|;
name|foundroot
label|:
empty_stmt|;
comment|/* 	 * If we entered at foundroot, then we are looking for the . or .. 	 * entry of the filesystems root directory.  isadir and scn were 	 * setup before jumping here.  And, bp is null.  There is no buf 	 * header. 	 */
comment|/* 	 * If deleting and at the end of the path, then if we matched on 	 * "." then don't deget() we would probably panic().  Otherwise 	 * deget() the directory entry. 	 */
if|if
condition|(
name|flag
operator|==
name|DELETE
operator|&&
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
block|{
if|if
condition|(
name|dp
operator|->
name|de_StartCluster
operator|==
name|scn
operator|&&
name|isadir
condition|)
block|{
comment|/* "." */
name|VREF
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|vdp
expr_stmt|;
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|cluster
argument_list|,
name|diroff
argument_list|,
name|dep
argument_list|,
operator|&
name|tdp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
name|ndp
operator|->
name|ni_vp
operator|=
name|DETOV
argument_list|(
name|tdp
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|lockparent
condition|)
name|DEUNLOCK
argument_list|(
name|dp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* 	 * If renaming. 	 */
if|if
condition|(
name|flag
operator|==
name|RENAME
operator|&&
name|wantparent
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
block|{
if|if
condition|(
name|dp
operator|->
name|de_StartCluster
operator|==
name|scn
operator|&&
name|isadir
condition|)
block|{
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
name|EISDIR
return|;
block|}
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|cluster
argument_list|,
name|diroff
argument_list|,
name|dep
argument_list|,
operator|&
name|tdp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
name|ndp
operator|->
name|ni_vp
operator|=
name|DETOV
argument_list|(
name|tdp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_nameiop
operator||=
name|SAVENAME
expr_stmt|;
if|if
condition|(
operator|!
name|lockparent
condition|)
name|DEUNLOCK
argument_list|(
name|dp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* 	 * ? 	 */
name|pdp
operator|=
name|dp
expr_stmt|;
if|if
condition|(
name|ndp
operator|->
name|ni_isdotdot
condition|)
block|{
name|DEUNLOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|cluster
argument_list|,
name|diroff
argument_list|,
name|dep
argument_list|,
operator|&
name|tdp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
name|DELOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
if|if
condition|(
name|lockparent
operator|&&
operator|*
name|ndp
operator|->
name|ni_next
operator|==
literal|'\0'
condition|)
name|DELOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|DETOV
argument_list|(
name|tdp
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|dp
operator|->
name|de_StartCluster
operator|==
name|scn
operator|&&
name|isadir
condition|)
block|{
comment|/* "." */
name|VREF
argument_list|(
name|vdp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|vdp
expr_stmt|;
block|}
else|else
block|{
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|cluster
argument_list|,
name|diroff
argument_list|,
name|dep
argument_list|,
operator|&
name|tdp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
if|if
condition|(
operator|!
name|lockparent
operator|||
operator|*
name|ndp
operator|->
name|ni_next
operator|!=
literal|'\0'
condition|)
name|DEUNLOCK
argument_list|(
name|pdp
argument_list|)
expr_stmt|;
name|ndp
operator|->
name|ni_vp
operator|=
name|DETOV
argument_list|(
name|tdp
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
comment|/* 	 * Insert name in cache if wanted. 	 */
if|if
condition|(
name|ndp
operator|->
name|ni_makeentry
condition|)
name|cache_enter
argument_list|(
name|ndp
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * dep - directory to copy into the directory ndp - nameidata structure  * containing info on where to put the directory entry in the directory.  * depp - return the address of the denode for the created directory entry  * if depp != 0  */
end_comment

begin_function
name|int
name|createde
parameter_list|(
name|dep
parameter_list|,
name|ndp
parameter_list|,
name|depp
parameter_list|)
name|struct
name|denode
modifier|*
name|dep
decl_stmt|;
name|struct
name|nameidata
modifier|*
name|ndp
decl_stmt|;
name|struct
name|denode
modifier|*
modifier|*
name|depp
decl_stmt|;
block|{
name|int
name|bn
decl_stmt|;
name|int
name|error
decl_stmt|;
name|u_long
name|dirclust
decl_stmt|,
name|diroffset
decl_stmt|;
name|struct
name|direntry
modifier|*
name|ndep
decl_stmt|;
name|struct
name|denode
modifier|*
name|ddep
init|=
name|VTODE
argument_list|(
name|ndp
operator|->
name|ni_dvp
argument_list|)
decl_stmt|;
comment|/* directory to add to */
name|struct
name|msdosfsmount
modifier|*
name|pmp
init|=
name|dep
operator|->
name|de_pmp
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"createde(dep %08x, ndp %08x, depp %08x)\n"
argument_list|,
name|dep
argument_list|,
name|ndp
argument_list|,
name|depp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
comment|/* 	 * If no space left in the directory then allocate another cluster 	 * and chain it onto the end of the file.  There is one exception 	 * to this.  That is, if the root directory has no more space it 	 * can NOT be expanded.  extendfile() checks for and fails attempts 	 * to extend the root directory.  We just return an error in that 	 * case. 	 */
if|if
condition|(
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_count
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|error
operator|=
name|extendfile
argument_list|(
name|ddep
argument_list|,
operator|&
name|bp
argument_list|,
operator|&
name|dirclust
argument_list|)
condition|)
return|return
name|error
return|;
name|ndep
operator|=
operator|(
expr|struct
name|direntry
operator|*
operator|)
name|bp
operator|->
name|b_un
operator|.
name|b_addr
expr_stmt|;
comment|/* 		 * Let caller know where we put the directory entry. 		 */
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
operator|=
name|dirclust
expr_stmt|;
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
operator|=
name|diroffset
operator|=
literal|0
expr_stmt|;
comment|/* 		 * Update the size of the directory 		 */
name|ddep
operator|->
name|de_FileSize
operator|+=
name|pmp
operator|->
name|pm_bpcluster
expr_stmt|;
block|}
else|else
block|{
comment|/* 		 * There is space in the existing directory.  So, we just 		 * read in the cluster with space.  Copy the new directory 		 * entry in.  Then write it to disk. NOTE:  DOS directories 		 * do not get smaller as clusters are emptied. 		 */
name|dirclust
operator|=
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
expr_stmt|;
name|diroffset
operator|=
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
expr_stmt|;
name|error
operator|=
name|readep
argument_list|(
name|pmp
argument_list|,
name|dirclust
argument_list|,
name|diroffset
argument_list|,
operator|&
name|bp
argument_list|,
operator|&
name|ndep
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
block|}
operator|*
name|ndep
operator|=
name|dep
operator|->
name|de_de
expr_stmt|;
comment|/* 	 * If they want us to return with the denode gotten. 	 */
if|if
condition|(
name|depp
condition|)
block|{
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|dirclust
argument_list|,
name|diroffset
argument_list|,
name|ndep
argument_list|,
name|depp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
block|}
if|if
condition|(
name|error
operator|=
name|bwrite
argument_list|(
name|bp
argument_list|)
condition|)
block|{
name|deput
argument_list|(
operator|*
name|depp
argument_list|)
expr_stmt|;
comment|/* free the vnode we got on error */
return|return
name|error
return|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Read in a directory entry and mark it as being deleted.  */
end_comment

begin_function
name|int
name|markdeleted
parameter_list|(
name|pmp
parameter_list|,
name|dirclust
parameter_list|,
name|diroffset
parameter_list|)
name|struct
name|msdosfsmount
modifier|*
name|pmp
decl_stmt|;
name|u_long
name|dirclust
decl_stmt|;
name|u_long
name|diroffset
decl_stmt|;
block|{
name|int
name|error
decl_stmt|;
name|struct
name|direntry
modifier|*
name|ep
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|error
operator|=
name|readep
argument_list|(
name|pmp
argument_list|,
name|dirclust
argument_list|,
name|diroffset
argument_list|,
operator|&
name|bp
argument_list|,
operator|&
name|ep
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
name|ep
operator|->
name|deName
index|[
literal|0
index|]
operator|=
name|SLOT_DELETED
expr_stmt|;
return|return
name|bwrite
argument_list|(
name|bp
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * Remove a directory entry. At this point the file represented by the  * directory entry to be removed is still full length until no one has it  * open.  When the file no longer being used msdosfs_inactive() is called  * and will truncate the file to 0 length.  When the vnode containing the  * denode is needed for some other purpose by VFS it will call  * msdosfs_reclaim() which will remove the denode from the denode cache.  */
end_comment

begin_function
name|int
name|removede
parameter_list|(
name|ndp
parameter_list|)
name|struct
name|nameidata
modifier|*
name|ndp
decl_stmt|;
block|{
name|struct
name|denode
modifier|*
name|dep
init|=
name|VTODE
argument_list|(
name|ndp
operator|->
name|ni_vp
argument_list|)
decl_stmt|;
comment|/* the file being removed */
name|struct
name|msdosfsmount
modifier|*
name|pmp
init|=
name|dep
operator|->
name|de_pmp
decl_stmt|;
name|int
name|error
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
comment|/* 	 * printf("removede(): filename %s\n", dep->de_Name); 	 * printf("rmde(): dep %08x, ndpcluster %d, ndpoffset %d\n", dep, 	 * 		ndp->ni_msdosfs.msdosfs_cluster, 	 * 		ndp->ni_msdosfs.msdosfs_offset); 	 */
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
comment|/* 	 * Read the directory block containing the directory entry we are 	 * to make free.  The nameidata structure holds the cluster number 	 * and directory entry index number of the entry to free. 	 */
name|error
operator|=
name|markdeleted
argument_list|(
name|pmp
argument_list|,
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_cluster
argument_list|,
name|ndp
operator|->
name|ni_msdosfs
operator|.
name|msdosfs_offset
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
literal|0
condition|)
name|dep
operator|->
name|de_refcnt
operator|--
expr_stmt|;
return|return
name|error
return|;
block|}
end_function

begin_comment
comment|/*  * Be sure a directory is empty except for "." and "..". Return 1 if empty,  * return 0 if not empty or error.  */
end_comment

begin_function
name|int
name|dosdirempty
parameter_list|(
name|dep
parameter_list|)
name|struct
name|denode
modifier|*
name|dep
decl_stmt|;
block|{
name|int
name|dei
decl_stmt|;
name|int
name|error
decl_stmt|;
name|u_long
name|cn
decl_stmt|;
name|daddr_t
name|bn
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
decl_stmt|;
name|struct
name|msdosfsmount
modifier|*
name|pmp
init|=
name|dep
operator|->
name|de_pmp
decl_stmt|;
name|struct
name|direntry
modifier|*
name|dentp
decl_stmt|;
comment|/* 	 * Since the filesize field in directory entries for a directory is 	 * zero, we just have to feel our way through the directory until 	 * we hit end of file. 	 */
for|for
control|(
name|cn
operator|=
literal|0
init|;
condition|;
name|cn
operator|++
control|)
block|{
name|error
operator|=
name|pcbmap
argument_list|(
name|dep
argument_list|,
name|cn
argument_list|,
operator|&
name|bn
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
name|E2BIG
condition|)
return|return
literal|1
return|;
comment|/* it's empty */
name|error
operator|=
name|bread
argument_list|(
name|pmp
operator|->
name|pm_devvp
argument_list|,
name|bn
argument_list|,
name|pmp
operator|->
name|pm_bpcluster
argument_list|,
name|NOCRED
argument_list|,
operator|&
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
name|dentp
operator|=
operator|(
expr|struct
name|direntry
operator|*
operator|)
name|bp
operator|->
name|b_un
operator|.
name|b_addr
expr_stmt|;
for|for
control|(
name|dei
operator|=
literal|0
init|;
name|dei
operator|<
name|pmp
operator|->
name|pm_depclust
condition|;
name|dei
operator|++
control|)
block|{
if|if
condition|(
name|dentp
operator|->
name|deName
index|[
literal|0
index|]
operator|!=
name|SLOT_DELETED
condition|)
block|{
comment|/* 				 * In dos directories an entry whose name 				 * starts with SLOT_EMPTY (0) starts the 				 * beginning of the unused part of the 				 * directory, so we can just return that it 				 * is empty. 				 */
if|if
condition|(
name|dentp
operator|->
name|deName
index|[
literal|0
index|]
operator|==
name|SLOT_EMPTY
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
comment|/* 				 * Any names other than "." and ".." in a 				 * directory mean it is not empty. 				 */
if|if
condition|(
name|bcmp
argument_list|(
name|dentp
operator|->
name|deName
argument_list|,
literal|".          "
argument_list|,
literal|11
argument_list|)
operator|&&
name|bcmp
argument_list|(
name|dentp
operator|->
name|deName
argument_list|,
literal|"..         "
argument_list|,
literal|11
argument_list|)
condition|)
block|{
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|MSDOSFSDEBUG
argument_list|)
name|printf
argument_list|(
literal|"dosdirempty(): entry %d found %02x, %02x\n"
argument_list|,
name|dei
argument_list|,
name|dentp
operator|->
name|deName
index|[
literal|0
index|]
argument_list|,
name|dentp
operator|->
name|deName
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* defined(MSDOSFSDEBUG) */
return|return
literal|0
return|;
comment|/* not empty */
block|}
block|}
name|dentp
operator|++
expr_stmt|;
block|}
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
block|}
comment|/* NOTREACHED */
block|}
end_function

begin_comment
comment|/*  * Check to see if the directory described by target is in some  * subdirectory of source.  This prevents something like the following from  * succeeding and leaving a bunch or files and directories orphaned. mv  * /a/b/c /a/b/c/d/e/f Where c and f are directories. source - the inode  * for /a/b/c target - the inode for /a/b/c/d/e/f Returns 0 if target is  * NOT a subdirectory of source. Otherwise returns a non-zero error number.  * The target inode is always unlocked on return.  */
end_comment

begin_function
name|int
name|doscheckpath
parameter_list|(
name|source
parameter_list|,
name|target
parameter_list|)
name|struct
name|denode
modifier|*
name|source
decl_stmt|;
name|struct
name|denode
modifier|*
name|target
decl_stmt|;
block|{
name|daddr_t
name|scn
decl_stmt|;
name|struct
name|denode
name|dummy
decl_stmt|;
name|struct
name|msdosfsmount
modifier|*
name|pmp
decl_stmt|;
name|struct
name|direntry
modifier|*
name|ep
decl_stmt|;
name|struct
name|denode
modifier|*
name|dep
decl_stmt|;
name|struct
name|buf
modifier|*
name|bp
init|=
name|NULL
decl_stmt|;
name|int
name|error
init|=
literal|0
decl_stmt|;
name|dep
operator|=
name|target
expr_stmt|;
if|if
condition|(
operator|(
name|target
operator|->
name|de_Attributes
operator|&
name|ATTR_DIRECTORY
operator|)
operator|==
literal|0
operator|||
operator|(
name|source
operator|->
name|de_Attributes
operator|&
name|ATTR_DIRECTORY
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|ENOTDIR
expr_stmt|;
goto|goto
name|out
goto|;
block|}
if|if
condition|(
name|dep
operator|->
name|de_StartCluster
operator|==
name|source
operator|->
name|de_StartCluster
condition|)
block|{
name|error
operator|=
name|EEXIST
expr_stmt|;
goto|goto
name|out
goto|;
block|}
if|if
condition|(
name|dep
operator|->
name|de_StartCluster
operator|==
name|MSDOSFSROOT
condition|)
goto|goto
name|out
goto|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
operator|(
name|dep
operator|->
name|de_Attributes
operator|&
name|ATTR_DIRECTORY
operator|)
operator|==
literal|0
condition|)
block|{
name|error
operator|=
name|ENOTDIR
expr_stmt|;
goto|goto
name|out
goto|;
block|}
name|pmp
operator|=
name|dep
operator|->
name|de_pmp
expr_stmt|;
name|scn
operator|=
name|dep
operator|->
name|de_StartCluster
expr_stmt|;
name|error
operator|=
name|bread
argument_list|(
name|pmp
operator|->
name|pm_devvp
argument_list|,
name|cntobn
argument_list|(
name|pmp
argument_list|,
name|scn
argument_list|)
argument_list|,
name|pmp
operator|->
name|pm_bpcluster
argument_list|,
name|NOCRED
argument_list|,
operator|&
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
block|{
break|break;
block|}
name|ep
operator|=
operator|(
expr|struct
name|direntry
operator|*
operator|)
name|bp
operator|->
name|b_un
operator|.
name|b_addr
operator|+
literal|1
expr_stmt|;
if|if
condition|(
operator|(
name|ep
operator|->
name|deAttributes
operator|&
name|ATTR_DIRECTORY
operator|)
operator|==
literal|0
operator|||
name|bcmp
argument_list|(
name|ep
operator|->
name|deName
argument_list|,
literal|"..         "
argument_list|,
literal|11
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|error
operator|=
name|ENOTDIR
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|ep
operator|->
name|deStartCluster
operator|==
name|source
operator|->
name|de_StartCluster
condition|)
block|{
name|error
operator|=
name|EINVAL
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|ep
operator|->
name|deStartCluster
operator|==
name|MSDOSFSROOT
condition|)
break|break;
name|deput
argument_list|(
name|dep
argument_list|)
expr_stmt|;
comment|/* NOTE: deget() clears dep on error */
name|error
operator|=
name|deget
argument_list|(
name|pmp
argument_list|,
name|ep
operator|->
name|deStartCluster
argument_list|,
literal|0
argument_list|,
name|ep
argument_list|,
operator|&
name|dep
argument_list|)
expr_stmt|;
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
name|bp
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|error
condition|)
break|break;
block|}
name|out
label|:
empty_stmt|;
if|if
condition|(
name|bp
condition|)
name|brelse
argument_list|(
name|bp
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|==
name|ENOTDIR
condition|)
name|printf
argument_list|(
literal|"doscheckpath(): .. not a directory?\n"
argument_list|)
expr_stmt|;
if|if
condition|(
name|dep
operator|!=
name|NULL
condition|)
name|deput
argument_list|(
name|dep
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
end_function

begin_comment
comment|/*  * Read in the disk block containing the directory entry (dirclu, dirofs)  * and return the address of the buf header, and the address of the  * directory entry within the block.  */
end_comment

begin_function
name|int
name|readep
parameter_list|(
name|pmp
parameter_list|,
name|dirclu
parameter_list|,
name|dirofs
parameter_list|,
name|bpp
parameter_list|,
name|epp
parameter_list|)
name|struct
name|msdosfsmount
modifier|*
name|pmp
decl_stmt|;
name|u_long
name|dirclu
decl_stmt|,
name|dirofs
decl_stmt|;
name|struct
name|buf
modifier|*
modifier|*
name|bpp
decl_stmt|;
name|struct
name|direntry
modifier|*
modifier|*
name|epp
decl_stmt|;
block|{
name|int
name|error
decl_stmt|;
name|daddr_t
name|bn
decl_stmt|;
name|bn
operator|=
name|detobn
argument_list|(
name|pmp
argument_list|,
name|dirclu
argument_list|,
name|dirofs
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|=
name|bread
argument_list|(
name|pmp
operator|->
name|pm_devvp
argument_list|,
name|bn
argument_list|,
name|pmp
operator|->
name|pm_bpcluster
argument_list|,
name|NOCRED
argument_list|,
name|bpp
argument_list|)
condition|)
block|{
operator|*
name|bpp
operator|=
name|NULL
expr_stmt|;
return|return
name|error
return|;
block|}
if|if
condition|(
name|epp
condition|)
operator|*
name|epp
operator|=
name|bptoep
argument_list|(
name|pmp
argument_list|,
operator|*
name|bpp
argument_list|,
name|dirofs
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*  * Read in the disk block containing the directory entry dep came from and  * return the address of the buf header, and the address of the directory  * entry within the block.  */
end_comment

begin_function
name|int
name|readde
parameter_list|(
name|dep
parameter_list|,
name|bpp
parameter_list|,
name|epp
parameter_list|)
name|struct
name|denode
modifier|*
name|dep
decl_stmt|;
name|struct
name|buf
modifier|*
modifier|*
name|bpp
decl_stmt|;
name|struct
name|direntry
modifier|*
modifier|*
name|epp
decl_stmt|;
block|{
return|return
name|readep
argument_list|(
name|dep
operator|->
name|de_pmp
argument_list|,
name|dep
operator|->
name|de_dirclust
argument_list|,
name|dep
operator|->
name|de_diroffset
argument_list|,
name|bpp
argument_list|,
name|epp
argument_list|)
return|;
block|}
end_function

end_unit

