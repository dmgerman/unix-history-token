begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* adler32.c -- compute the Adler-32 checksum of a data stream  * Copyright (C) 1995-2004 Mark Adler  * For conditions of distribution and use, see copyright notice in zlib.h  */
end_comment

begin_pragma
pragma|#
directive|pragma
name|ident
literal|"%Z%%M%	%I%	%E% SMI"
end_pragma

begin_define
define|#
directive|define
name|ZLIB_INTERNAL
end_define

begin_include
include|#
directive|include
file|"zlib.h"
end_include

begin_define
define|#
directive|define
name|BASE
value|65521UL
end_define

begin_comment
comment|/* largest prime smaller than 65536 */
end_comment

begin_define
define|#
directive|define
name|NMAX
value|5552
end_define

begin_comment
comment|/* NMAX is the largest n such that 255n(n+1)/2 + (n+1)(BASE-1)<= 2^32-1 */
end_comment

begin_define
define|#
directive|define
name|DO1
parameter_list|(
name|buf
parameter_list|,
name|i
parameter_list|)
value|{adler += (buf)[i]; sum2 += adler;}
end_define

begin_define
define|#
directive|define
name|DO2
parameter_list|(
name|buf
parameter_list|,
name|i
parameter_list|)
value|DO1(buf,i); DO1(buf,i+1);
end_define

begin_define
define|#
directive|define
name|DO4
parameter_list|(
name|buf
parameter_list|,
name|i
parameter_list|)
value|DO2(buf,i); DO2(buf,i+2);
end_define

begin_define
define|#
directive|define
name|DO8
parameter_list|(
name|buf
parameter_list|,
name|i
parameter_list|)
value|DO4(buf,i); DO4(buf,i+4);
end_define

begin_define
define|#
directive|define
name|DO16
parameter_list|(
name|buf
parameter_list|)
value|DO8(buf,0); DO8(buf,8);
end_define

begin_comment
comment|/* use NO_DIVIDE if your processor does not do division in hardware */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NO_DIVIDE
end_ifdef

begin_define
define|#
directive|define
name|MOD
parameter_list|(
name|a
parameter_list|)
define|\
value|do { \         if (a>= (BASE<< 16)) a -= (BASE<< 16); \         if (a>= (BASE<< 15)) a -= (BASE<< 15); \         if (a>= (BASE<< 14)) a -= (BASE<< 14); \         if (a>= (BASE<< 13)) a -= (BASE<< 13); \         if (a>= (BASE<< 12)) a -= (BASE<< 12); \         if (a>= (BASE<< 11)) a -= (BASE<< 11); \         if (a>= (BASE<< 10)) a -= (BASE<< 10); \         if (a>= (BASE<< 9)) a -= (BASE<< 9); \         if (a>= (BASE<< 8)) a -= (BASE<< 8); \         if (a>= (BASE<< 7)) a -= (BASE<< 7); \         if (a>= (BASE<< 6)) a -= (BASE<< 6); \         if (a>= (BASE<< 5)) a -= (BASE<< 5); \         if (a>= (BASE<< 4)) a -= (BASE<< 4); \         if (a>= (BASE<< 3)) a -= (BASE<< 3); \         if (a>= (BASE<< 2)) a -= (BASE<< 2); \         if (a>= (BASE<< 1)) a -= (BASE<< 1); \         if (a>= BASE) a -= BASE; \     } while (0)
end_define

begin_define
define|#
directive|define
name|MOD4
parameter_list|(
name|a
parameter_list|)
define|\
value|do { \         if (a>= (BASE<< 4)) a -= (BASE<< 4); \         if (a>= (BASE<< 3)) a -= (BASE<< 3); \         if (a>= (BASE<< 2)) a -= (BASE<< 2); \         if (a>= (BASE<< 1)) a -= (BASE<< 1); \         if (a>= BASE) a -= BASE; \     } while (0)
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|MOD
parameter_list|(
name|a
parameter_list|)
value|a %= BASE
end_define

begin_define
define|#
directive|define
name|MOD4
parameter_list|(
name|a
parameter_list|)
value|a %= BASE
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ========================================================================= */
end_comment

begin_function
name|uLong
name|ZEXPORT
name|adler32
parameter_list|(
name|adler
parameter_list|,
name|buf
parameter_list|,
name|len
parameter_list|)
name|uLong
name|adler
decl_stmt|;
specifier|const
name|Bytef
modifier|*
name|buf
decl_stmt|;
name|uInt
name|len
decl_stmt|;
block|{
name|unsigned
name|long
name|sum2
decl_stmt|;
name|unsigned
name|n
decl_stmt|;
comment|/* split Adler-32 into component sums */
name|sum2
operator|=
operator|(
name|adler
operator|>>
literal|16
operator|)
operator|&
literal|0xffff
expr_stmt|;
name|adler
operator|&=
literal|0xffff
expr_stmt|;
comment|/* in case user likes doing a byte at a time, keep it fast */
if|if
condition|(
name|len
operator|==
literal|1
condition|)
block|{
name|adler
operator|+=
name|buf
index|[
literal|0
index|]
expr_stmt|;
if|if
condition|(
name|adler
operator|>=
name|BASE
condition|)
name|adler
operator|-=
name|BASE
expr_stmt|;
name|sum2
operator|+=
name|adler
expr_stmt|;
if|if
condition|(
name|sum2
operator|>=
name|BASE
condition|)
name|sum2
operator|-=
name|BASE
expr_stmt|;
return|return
name|adler
operator||
operator|(
name|sum2
operator|<<
literal|16
operator|)
return|;
block|}
comment|/* initial Adler-32 value (deferred check for len == 1 speed) */
if|if
condition|(
name|buf
operator|==
name|Z_NULL
condition|)
return|return
literal|1L
return|;
comment|/* in case short lengths are provided, keep it somewhat fast */
if|if
condition|(
name|len
operator|<
literal|16
condition|)
block|{
while|while
condition|(
name|len
operator|--
condition|)
block|{
name|adler
operator|+=
operator|*
name|buf
operator|++
expr_stmt|;
name|sum2
operator|+=
name|adler
expr_stmt|;
block|}
if|if
condition|(
name|adler
operator|>=
name|BASE
condition|)
name|adler
operator|-=
name|BASE
expr_stmt|;
name|MOD4
argument_list|(
name|sum2
argument_list|)
expr_stmt|;
comment|/* only added so many BASE's */
return|return
name|adler
operator||
operator|(
name|sum2
operator|<<
literal|16
operator|)
return|;
block|}
comment|/* do length NMAX blocks -- requires just one modulo operation */
while|while
condition|(
name|len
operator|>=
name|NMAX
condition|)
block|{
name|len
operator|-=
name|NMAX
expr_stmt|;
name|n
operator|=
name|NMAX
operator|/
literal|16
expr_stmt|;
comment|/* NMAX is divisible by 16 */
do|do
block|{
name|DO16
argument_list|(
name|buf
argument_list|)
expr_stmt|;
comment|/* 16 sums unrolled */
name|buf
operator|+=
literal|16
expr_stmt|;
block|}
do|while
condition|(
operator|--
name|n
condition|)
do|;
name|MOD
argument_list|(
name|adler
argument_list|)
expr_stmt|;
name|MOD
argument_list|(
name|sum2
argument_list|)
expr_stmt|;
block|}
comment|/* do remaining bytes (less than NMAX, still just one modulo) */
if|if
condition|(
name|len
condition|)
block|{
comment|/* avoid modulos if none remaining */
while|while
condition|(
name|len
operator|>=
literal|16
condition|)
block|{
name|len
operator|-=
literal|16
expr_stmt|;
name|DO16
argument_list|(
name|buf
argument_list|)
expr_stmt|;
name|buf
operator|+=
literal|16
expr_stmt|;
block|}
while|while
condition|(
name|len
operator|--
condition|)
block|{
name|adler
operator|+=
operator|*
name|buf
operator|++
expr_stmt|;
name|sum2
operator|+=
name|adler
expr_stmt|;
block|}
name|MOD
argument_list|(
name|adler
argument_list|)
expr_stmt|;
name|MOD
argument_list|(
name|sum2
argument_list|)
expr_stmt|;
block|}
comment|/* return recombined sums */
return|return
name|adler
operator||
operator|(
name|sum2
operator|<<
literal|16
operator|)
return|;
block|}
end_function

begin_comment
comment|/* ========================================================================= */
end_comment

begin_function
name|uLong
name|ZEXPORT
name|adler32_combine
parameter_list|(
name|adler1
parameter_list|,
name|adler2
parameter_list|,
name|len2
parameter_list|)
name|uLong
name|adler1
decl_stmt|;
name|uLong
name|adler2
decl_stmt|;
name|z_off_t
name|len2
decl_stmt|;
block|{
name|unsigned
name|long
name|sum1
decl_stmt|;
name|unsigned
name|long
name|sum2
decl_stmt|;
name|unsigned
name|rem
decl_stmt|;
comment|/* the derivation of this formula is left as an exercise for the reader */
name|rem
operator|=
call|(
name|unsigned
call|)
argument_list|(
name|len2
operator|%
name|BASE
argument_list|)
expr_stmt|;
name|sum1
operator|=
name|adler1
operator|&
literal|0xffff
expr_stmt|;
name|sum2
operator|=
name|rem
operator|*
name|sum1
expr_stmt|;
name|MOD
argument_list|(
name|sum2
argument_list|)
expr_stmt|;
name|sum1
operator|+=
operator|(
name|adler2
operator|&
literal|0xffff
operator|)
operator|+
name|BASE
operator|-
literal|1
expr_stmt|;
name|sum2
operator|+=
operator|(
operator|(
name|adler1
operator|>>
literal|16
operator|)
operator|&
literal|0xffff
operator|)
operator|+
operator|(
operator|(
name|adler2
operator|>>
literal|16
operator|)
operator|&
literal|0xffff
operator|)
operator|+
name|BASE
operator|-
name|rem
expr_stmt|;
if|if
condition|(
name|sum1
operator|>
name|BASE
condition|)
name|sum1
operator|-=
name|BASE
expr_stmt|;
if|if
condition|(
name|sum1
operator|>
name|BASE
condition|)
name|sum1
operator|-=
name|BASE
expr_stmt|;
if|if
condition|(
name|sum2
operator|>
operator|(
name|BASE
operator|<<
literal|1
operator|)
condition|)
name|sum2
operator|-=
operator|(
name|BASE
operator|<<
literal|1
operator|)
expr_stmt|;
if|if
condition|(
name|sum2
operator|>
name|BASE
condition|)
name|sum2
operator|-=
name|BASE
expr_stmt|;
return|return
name|sum1
operator||
operator|(
name|sum2
operator|<<
literal|16
operator|)
return|;
block|}
end_function

end_unit

