begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * CDDL HEADER START  *  * This file and its contents are supplied under the terms of the  * Common Development and Distribution License ("CDDL"), version 1.0.  * You may only use this file in accordance with the terms of version  * 1.0 of the CDDL.  *  * A full copy of the text of the CDDL should have accompanied this  * source.  A copy of the CDDL is also available via the Internet at  * http://www.illumos.org/license/CDDL.  *  * CDDL HEADER END  */
end_comment

begin_comment
comment|/*  * Copyright (c) 2013 by Delphix. All rights reserved.  */
end_comment

begin_comment
comment|/*  * Embedded-data Block Pointers  *  * Normally, block pointers point (via their DVAs) to a block which holds data.  * If the data that we need to store is very small, this is an inefficient  * use of space, because a block must be at minimum 1 sector (typically 512  * bytes or 4KB).  Additionally, reading these small blocks tends to generate  * more random reads.  *  * Embedded-data Block Pointers allow small pieces of data (the "payload",  * up to 112 bytes) to be stored in the block pointer itself, instead of  * being pointed to.  The "Pointer" part of this name is a bit of a  * misnomer, as nothing is pointed to.  *  * BP_EMBEDDED_TYPE_DATA block pointers allow highly-compressible data to  * be embedded in the block pointer.  The logic for this is handled in  * the SPA, by the zio pipeline.  Therefore most code outside the zio  * pipeline doesn't need special-cases to handle these block pointers.  *  * See spa.h for details on the exact layout of embedded block pointers.  */
end_comment

begin_comment
comment|/*  * buf must be at least BPE_GET_PSIZE(bp) bytes long (which will never be  * more than BPE_PAYLOAD_SIZE bytes).  */
end_comment

begin_function
name|void
name|decode_embedded_bp_compressed
parameter_list|(
specifier|const
name|blkptr_t
modifier|*
name|bp
parameter_list|,
name|void
modifier|*
name|buf
parameter_list|)
block|{
name|int
name|psize
decl_stmt|;
name|uint8_t
modifier|*
name|buf8
init|=
name|buf
decl_stmt|;
name|uint64_t
name|w
init|=
literal|0
decl_stmt|;
specifier|const
name|uint64_t
modifier|*
name|bp64
init|=
operator|(
specifier|const
name|uint64_t
operator|*
operator|)
name|bp
decl_stmt|;
name|ASSERT
argument_list|(
name|BP_IS_EMBEDDED
argument_list|(
name|bp
argument_list|)
argument_list|)
expr_stmt|;
name|psize
operator|=
name|BPE_GET_PSIZE
argument_list|(
name|bp
argument_list|)
expr_stmt|;
comment|/* 	 * Decode the words of the block pointer into the byte array. 	 * Low bits of first word are the first byte (little endian). 	 */
for|for
control|(
name|int
name|i
init|=
literal|0
init|;
name|i
operator|<
name|psize
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|i
operator|%
sizeof|sizeof
argument_list|(
name|w
argument_list|)
operator|==
literal|0
condition|)
block|{
comment|/* beginning of a word */
name|ASSERT3P
argument_list|(
name|bp64
argument_list|,
operator|<
argument_list|,
name|bp
operator|+
literal|1
argument_list|)
expr_stmt|;
name|w
operator|=
operator|*
name|bp64
expr_stmt|;
name|bp64
operator|++
expr_stmt|;
if|if
condition|(
operator|!
name|BPE_IS_PAYLOADWORD
argument_list|(
name|bp
argument_list|,
name|bp64
argument_list|)
condition|)
name|bp64
operator|++
expr_stmt|;
block|}
name|buf8
index|[
name|i
index|]
operator|=
name|BF64_GET
argument_list|(
name|w
argument_list|,
operator|(
name|i
operator|%
sizeof|sizeof
argument_list|(
name|w
argument_list|)
operator|)
operator|*
name|NBBY
argument_list|,
name|NBBY
argument_list|)
expr_stmt|;
block|}
block|}
end_function

end_unit

