begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* FUNCTION<<memset>>---set an area of memory  INDEX 	memset  ANSI_SYNOPSIS 	#include<string.h> 	void *memset(void *<[dst]>, int<[c]>, size_t<[length]>);  TRAD_SYNOPSIS 	#include<string.h> 	void *memset(<[dst]>,<[c]>,<[length]>) 	void *<[dst]>; 	int<[c]>; 	size_t<[length]>;  DESCRIPTION 	This function converts the argument<[c]> into an unsigned 	char and fills the first<[length]> characters of the array 	pointed to by<[dst]> to the value.  RETURNS<<memset>> returns the value of<[dst]>.  PORTABILITY<<memset>> is ANSI C.<<memset>> requires no supporting OS subroutines.  QUICKREF 	memset ansi pure */
end_comment

begin_include
include|#
directive|include
file|"shim.h"
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_define
define|#
directive|define
name|LBLOCKSIZE
value|(sizeof(long))
end_define

begin_define
define|#
directive|define
name|UNALIGNED
parameter_list|(
name|X
parameter_list|)
value|((long)X& (LBLOCKSIZE - 1))
end_define

begin_define
define|#
directive|define
name|TOO_SMALL
parameter_list|(
name|LEN
parameter_list|)
value|((LEN)< LBLOCKSIZE)
end_define

begin_decl_stmt
name|_PTR
name|_DEFUN
argument_list|(
name|memset
argument_list|,
operator|(
name|m
operator|,
name|c
operator|,
name|n
operator|)
argument_list|,
name|_PTR
name|m
name|_AND
name|int
name|c
name|_AND
name|size_t
name|n
argument_list|)
block|{
name|char
modifier|*
name|s
init|=
operator|(
name|char
operator|*
operator|)
name|m
decl_stmt|;
if|#
directive|if
operator|!
name|defined
argument_list|(
name|PREFER_SIZE_OVER_SPEED
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|__OPTIMIZE_SIZE__
argument_list|)
name|int
name|i
decl_stmt|;
name|unsigned
name|long
name|buffer
decl_stmt|;
name|unsigned
name|long
modifier|*
name|aligned_addr
decl_stmt|;
name|unsigned
name|int
name|d
init|=
name|c
operator|&
literal|0xff
decl_stmt|;
comment|/* To avoid sign extension, copy C to an 				   unsigned variable.  */
while|while
condition|(
name|UNALIGNED
argument_list|(
name|s
argument_list|)
condition|)
block|{
if|if
condition|(
name|n
operator|--
condition|)
operator|*
name|s
operator|++
operator|=
operator|(
name|char
operator|)
name|c
expr_stmt|;
else|else
return|return
name|m
return|;
block|}
if|if
condition|(
operator|!
name|TOO_SMALL
argument_list|(
name|n
argument_list|)
condition|)
block|{
comment|/* If we get this far, we know that n is large and s is word-aligned. */
name|aligned_addr
operator|=
operator|(
name|unsigned
name|long
operator|*
operator|)
name|s
expr_stmt|;
comment|/* Store D into each char sized location in BUFFER so that          we can set large blocks quickly.  */
name|buffer
operator|=
operator|(
name|d
operator|<<
literal|8
operator|)
operator||
name|d
expr_stmt|;
name|buffer
operator||=
operator|(
name|buffer
operator|<<
literal|16
operator|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|32
init|;
name|i
operator|<
name|LBLOCKSIZE
operator|*
literal|8
condition|;
name|i
operator|<<=
literal|1
control|)
name|buffer
operator|=
operator|(
name|buffer
operator|<<
name|i
operator|)
operator||
name|buffer
expr_stmt|;
comment|/* Unroll the loop.  */
while|while
condition|(
name|n
operator|>=
name|LBLOCKSIZE
operator|*
literal|4
condition|)
block|{
operator|*
name|aligned_addr
operator|++
operator|=
name|buffer
expr_stmt|;
operator|*
name|aligned_addr
operator|++
operator|=
name|buffer
expr_stmt|;
operator|*
name|aligned_addr
operator|++
operator|=
name|buffer
expr_stmt|;
operator|*
name|aligned_addr
operator|++
operator|=
name|buffer
expr_stmt|;
name|n
operator|-=
literal|4
operator|*
name|LBLOCKSIZE
expr_stmt|;
block|}
while|while
condition|(
name|n
operator|>=
name|LBLOCKSIZE
condition|)
block|{
operator|*
name|aligned_addr
operator|++
operator|=
name|buffer
expr_stmt|;
name|n
operator|-=
name|LBLOCKSIZE
expr_stmt|;
block|}
comment|/* Pick up the remainder with a bytewise loop.  */
name|s
operator|=
operator|(
name|char
operator|*
operator|)
name|aligned_addr
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* not PREFER_SIZE_OVER_SPEED */
while|while
condition|(
name|n
operator|--
condition|)
operator|*
name|s
operator|++
operator|=
operator|(
name|char
operator|)
name|c
expr_stmt|;
return|return
name|m
return|;
block|}
end_decl_stmt

end_unit

