begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* FUNCTION<<strchr>>---search for character in string  INDEX 	strchr  ANSI_SYNOPSIS 	#include<string.h> 	char * strchr(const char *<[string]>, int<[c]>);  TRAD_SYNOPSIS 	#include<string.h> 	char * strchr(<[string]>,<[c]>); 	const char *<[string]>; 	int<[c]>;  DESCRIPTION 	This function finds the first occurence of<[c]> (converted to 	a char) in the string pointed to by<[string]> (including the 	terminating null character).  RETURNS 	Returns a pointer to the located character, or a null pointer 	if<[c]> does not occur in<[string]>.  PORTABILITY<<strchr>> is ANSI C.<<strchr>> requires no supporting OS subroutines.  QUICKREF 	strchr ansi pure */
end_comment

begin_include
include|#
directive|include
file|"shim.h"
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_undef
undef|#
directive|undef
name|strchr
end_undef

begin_comment
comment|/* Nonzero if X is not aligned on a "long" boundary.  */
end_comment

begin_define
define|#
directive|define
name|UNALIGNED
parameter_list|(
name|X
parameter_list|)
value|((long)X& (sizeof (long) - 1))
end_define

begin_comment
comment|/* How many bytes are loaded each iteration of the word copy loop.  */
end_comment

begin_define
define|#
directive|define
name|LBLOCKSIZE
value|(sizeof (long))
end_define

begin_if
if|#
directive|if
name|LONG_MAX
operator|==
literal|2147483647L
end_if

begin_define
define|#
directive|define
name|DETECTNULL
parameter_list|(
name|X
parameter_list|)
value|(((X) - 0x01010101)& ~(X)& 0x80808080)
end_define

begin_else
else|#
directive|else
end_else

begin_if
if|#
directive|if
name|LONG_MAX
operator|==
literal|9223372036854775807L
end_if

begin_comment
comment|/* Nonzero if X (a long int) contains a NULL byte. */
end_comment

begin_define
define|#
directive|define
name|DETECTNULL
parameter_list|(
name|X
parameter_list|)
value|(((X) - 0x0101010101010101)& ~(X)& 0x8080808080808080)
end_define

begin_else
else|#
directive|else
end_else

begin_error
error|#
directive|error
error|long int is not a 32bit or 64bit type.
end_error

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DETECTCHAR returns nonzero if (long)X contains the byte used    to fill (long)MASK. */
end_comment

begin_define
define|#
directive|define
name|DETECTCHAR
parameter_list|(
name|X
parameter_list|,
name|MASK
parameter_list|)
value|(DETECTNULL(X ^ MASK))
end_define

begin_decl_stmt
name|char
modifier|*
name|_DEFUN
argument_list|(
name|strchr
argument_list|,
operator|(
name|s1
operator|,
name|i
operator|)
argument_list|,
name|_CONST
name|char
operator|*
name|s1
name|_AND
name|int
name|i
argument_list|)
block|{
name|_CONST
name|unsigned
name|char
modifier|*
name|s
init|=
operator|(
name|_CONST
name|unsigned
name|char
operator|*
operator|)
name|s1
decl_stmt|;
name|unsigned
name|char
name|c
init|=
name|i
decl_stmt|;
if|#
directive|if
operator|!
name|defined
argument_list|(
name|PREFER_SIZE_OVER_SPEED
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|__OPTIMIZE_SIZE__
argument_list|)
name|unsigned
name|long
name|mask
decl_stmt|,
name|j
decl_stmt|;
name|unsigned
name|long
modifier|*
name|aligned_addr
decl_stmt|;
comment|/* Special case for finding 0.  */
if|if
condition|(
operator|!
name|c
condition|)
block|{
while|while
condition|(
name|UNALIGNED
argument_list|(
name|s
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
operator|*
name|s
condition|)
return|return
operator|(
name|char
operator|*
operator|)
name|s
return|;
name|s
operator|++
expr_stmt|;
block|}
comment|/* Operate a word at a time.  */
name|aligned_addr
operator|=
operator|(
name|unsigned
name|long
operator|*
operator|)
name|s
expr_stmt|;
while|while
condition|(
operator|!
name|DETECTNULL
argument_list|(
operator|*
name|aligned_addr
argument_list|)
condition|)
name|aligned_addr
operator|++
expr_stmt|;
comment|/* Found the end of string.  */
name|s
operator|=
operator|(
specifier|const
name|unsigned
name|char
operator|*
operator|)
name|aligned_addr
expr_stmt|;
while|while
condition|(
operator|*
name|s
condition|)
name|s
operator|++
expr_stmt|;
return|return
operator|(
name|char
operator|*
operator|)
name|s
return|;
block|}
comment|/* All other bytes.  Align the pointer, then search a long at a time.  */
while|while
condition|(
name|UNALIGNED
argument_list|(
name|s
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
operator|*
name|s
condition|)
return|return
name|NULL
return|;
if|if
condition|(
operator|*
name|s
operator|==
name|c
condition|)
return|return
operator|(
name|char
operator|*
operator|)
name|s
return|;
name|s
operator|++
expr_stmt|;
block|}
name|mask
operator|=
name|c
expr_stmt|;
for|for
control|(
name|j
operator|=
literal|8
init|;
name|j
operator|<
name|LBLOCKSIZE
operator|*
literal|8
condition|;
name|j
operator|<<=
literal|1
control|)
name|mask
operator|=
operator|(
name|mask
operator|<<
name|j
operator|)
operator||
name|mask
expr_stmt|;
name|aligned_addr
operator|=
operator|(
name|unsigned
name|long
operator|*
operator|)
name|s
expr_stmt|;
while|while
condition|(
operator|!
name|DETECTNULL
argument_list|(
operator|*
name|aligned_addr
argument_list|)
operator|&&
operator|!
name|DETECTCHAR
argument_list|(
operator|*
name|aligned_addr
argument_list|,
name|mask
argument_list|)
condition|)
name|aligned_addr
operator|++
expr_stmt|;
comment|/* The block of bytes currently pointed to by aligned_addr      contains either a null or the target char, or both.  We      catch it using the bytewise search.  */
name|s
operator|=
operator|(
name|unsigned
name|char
operator|*
operator|)
name|aligned_addr
expr_stmt|;
endif|#
directive|endif
comment|/* not PREFER_SIZE_OVER_SPEED */
while|while
condition|(
operator|*
name|s
operator|&&
operator|*
name|s
operator|!=
name|c
condition|)
name|s
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|s
operator|==
name|c
condition|)
return|return
operator|(
name|char
operator|*
operator|)
name|s
return|;
return|return
name|NULL
return|;
block|}
end_decl_stmt

end_unit

