begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	$OpenBSD: getoldopt.c,v 1.4 2000/01/22 20:24:51 deraadt Exp $	*/
end_comment

begin_comment
comment|/*	$NetBSD: getoldopt.c,v 1.3 1995/03/21 09:07:28 cgd Exp $	*/
end_comment

begin_comment
comment|/*  * Plug-compatible replacement for getopt() for parsing tar-like  * arguments.  If the first argument begins with "-", it uses getopt;  * otherwise, it uses the old rules used by tar, dump, and ps.  *  * Written 25 August 1985 by John Gilmore (ihnp4!hoptoad!gnu) and placed  * in the Pubic Domain for your edification and enjoyment.  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|rcsid
index|[]
init|=
literal|"$FreeBSD$"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_function
name|int
name|getoldopt
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
modifier|*
name|argv
parameter_list|,
name|char
modifier|*
name|optstring
parameter_list|)
block|{
specifier|static
name|char
modifier|*
name|key
decl_stmt|;
comment|/* Points to next keyletter */
specifier|static
name|char
name|use_getopt
decl_stmt|;
comment|/* !=0 if argv[1][0] was '-' */
name|char
name|c
decl_stmt|;
name|char
modifier|*
name|place
decl_stmt|;
name|optarg
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|key
operator|==
name|NULL
condition|)
block|{
comment|/* First time */
if|if
condition|(
name|argc
operator|<
literal|2
condition|)
return|return
name|EOF
return|;
name|key
operator|=
name|argv
index|[
literal|1
index|]
expr_stmt|;
if|if
condition|(
operator|*
name|key
operator|==
literal|'-'
condition|)
name|use_getopt
operator|++
expr_stmt|;
else|else
name|optind
operator|=
literal|2
expr_stmt|;
block|}
if|if
condition|(
name|use_getopt
condition|)
return|return
name|getopt
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|,
name|optstring
argument_list|)
return|;
name|c
operator|=
operator|*
name|key
operator|++
expr_stmt|;
if|if
condition|(
name|c
operator|==
literal|'\0'
condition|)
block|{
name|key
operator|--
expr_stmt|;
return|return
name|EOF
return|;
block|}
name|place
operator|=
name|strchr
argument_list|(
name|optstring
argument_list|,
name|c
argument_list|)
expr_stmt|;
if|if
condition|(
name|place
operator|==
name|NULL
operator|||
name|c
operator|==
literal|':'
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s: unknown option %c\n"
argument_list|,
name|argv
index|[
literal|0
index|]
argument_list|,
name|c
argument_list|)
expr_stmt|;
return|return
operator|(
literal|'?'
operator|)
return|;
block|}
name|place
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|place
operator|==
literal|':'
condition|)
block|{
if|if
condition|(
name|optind
operator|<
name|argc
condition|)
block|{
name|optarg
operator|=
name|argv
index|[
name|optind
index|]
expr_stmt|;
name|optind
operator|++
expr_stmt|;
block|}
else|else
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s: %c argument missing\n"
argument_list|,
name|argv
index|[
literal|0
index|]
argument_list|,
name|c
argument_list|)
expr_stmt|;
return|return
operator|(
literal|'?'
operator|)
return|;
block|}
block|}
return|return
operator|(
name|c
operator|)
return|;
block|}
end_function

end_unit

