begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* ntp_unixclock.c,v 3.1 1993/07/06 01:11:30 jbj Exp  * ntp_unixclock.c - routines for reading and adjusting a 4BSD-style  *		     system clock  */
end_comment

begin_include
include|#
directive|include
file|<nlist.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_HPUX
argument_list|)
operator|||
name|defined
argument_list|(
name|sgi
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_BSDI
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<utmp.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_unixtime.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_LIBKVM
argument_list|)
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_BSDI
end_ifdef

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_BSDI */
end_comment

begin_include
include|#
directive|include
file|<kvm.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|_POSIX2_LINE_MAX
end_ifndef

begin_define
define|#
directive|define
name|_POSIX2_LINE_MAX
value|2048
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_LIBKVM */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|RS6000
end_ifdef

begin_undef
undef|#
directive|undef
name|hz
end_undef

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* RS6000 */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|debug
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * These routines (init_systime, get_systime, step_systime, adj_systime)  * implement an interface between the (more or less) system independent  * bits of NTP and the peculiarities of dealing with the Unix system  * clock.  These routines will run with good precision fairly independently  * of your kernel's value of tickadj.  I couldn't tell the difference  * between tickadj==40 and tickadj==5 on a microvax, though I prefer  * to set tickadj == 500/hz when in doubt.  At your option you  * may compile this so that your system's clock is always slewed to the  * correct time even for large corrections.  Of course, all of this takes  * a lot of code which wouldn't be needed with a reasonable tickadj and  * a willingness to let the clock be stepped occasionally.  Oh well.  */
end_comment

begin_comment
comment|/*  * Clock variables.  We round calls to adjtime() to adj_precision  * microseconds, and limit the adjustment to tvu_maxslew microseconds  * (tsf_maxslew fractional sec) in one adjustment interval.  As we are  * thus limited in the speed and precision with which we can adjust the  * clock, we compensate by keeping the known "error" in the system time  * in sys_clock_offset.  This is added to timestamps returned by get_systime().  * We also remember the clock precision we computed from the kernel in  * case someone asks us.  */
end_comment

begin_decl_stmt
specifier|extern
name|LONG
name|adj_precision
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* adj precision in usec (tickadj) */
end_comment

begin_decl_stmt
specifier|extern
name|LONG
name|tvu_maxslew
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* maximum adjust doable in 1<<CLOCK_ADJ sec (usec) */
end_comment

begin_decl_stmt
specifier|extern
name|U_LONG
name|tsf_maxslew
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* same as above, as LONG format */
end_comment

begin_decl_stmt
specifier|extern
name|l_fp
name|sys_clock_offset
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* correction for current system time */
end_comment

begin_comment
comment|/*  * Import sys_clock (it is updated in get_systime)  */
end_comment

begin_decl_stmt
specifier|extern
name|LONG
name|sys_clock
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|clock_parms
name|P
argument_list|(
operator|(
name|U_LONG
operator|*
operator|,
name|U_LONG
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * init_systime - initialize the system clock support code, return  *		  clock precision.  *  * Note that this code obtains to kernel variables related to the local  * clock, tickadj and tick.  The code knows how the Berkeley adjtime  * call works, and assumes these two variables are obtainable and are  * used in the same manner.  Tick is supposed to be the number of  * microseconds which are added to the system clock at clock interrupt  * time when the time isn't being slewed.  Tickadj is supposed to be  * the number of microseconds which are added or subtracted from tick when  * the time is being slewed.  *  * If either of these two variables is missing, or is there but is used  * for a purpose different than that described, you are SOL and may have  * to do some custom kludging.  *  * This really shouldn't be in here.  */
end_comment

begin_function
name|void
name|init_systime
parameter_list|()
block|{
name|U_LONG
name|tickadj
decl_stmt|;
name|U_LONG
name|tick
decl_stmt|;
name|U_LONG
name|hz
decl_stmt|;
comment|/* 	 * Obtain the values 	 */
name|clock_parms
argument_list|(
operator|&
name|tickadj
argument_list|,
operator|&
name|tick
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"kernel vars: tickadj = %d, tick = %d\n"
argument_list|,
name|tickadj
argument_list|,
name|tick
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * If tickadj or hz wasn't found, we're doomed.  If hz is 	 * unreasonably small, forget it. 	 */
if|if
condition|(
name|tickadj
operator|==
literal|0
operator|||
name|tick
operator|==
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"tickadj or tick unknown, exiting"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tick
operator|>
literal|65535
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"tick value of %lu is unreasonably large"
argument_list|,
name|tick
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Estimate hz from tick 	 */
name|hz
operator|=
literal|1000000L
operator|/
name|tick
expr_stmt|;
comment|/* 	 * Set adj_precision and the maximum slew based on this.  Note 	 * that maxslew is set slightly shorter than it needs to be as 	 * insurance that all slews requested will complete in 1<<CLOCK_ADJ 	 * seconds. 	 */
ifdef|#
directive|ifdef
name|ADJTIME_IS_ACCURATE
name|adj_precision
operator|=
literal|1
expr_stmt|;
else|#
directive|else
name|adj_precision
operator|=
name|tickadj
expr_stmt|;
endif|#
directive|endif
comment|/* ADJTIME_IS_ACCURATE */
if|#
directive|if
name|defined
argument_list|(
name|SLEWALWAYS
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|ADJTIME_IS_ACCURATE
argument_list|)
comment|/* 	 * give us more time if we are always slewing... just in case 	 */
name|tvu_maxslew
operator|=
name|tickadj
operator|*
operator|(
name|hz
operator|-
literal|3
operator|)
operator|*
operator|(
literal|1
operator|<<
name|CLOCK_ADJ
operator|)
expr_stmt|;
else|#
directive|else
name|tvu_maxslew
operator|=
name|tickadj
operator|*
operator|(
name|hz
operator|-
literal|1
operator|)
operator|*
operator|(
literal|1
operator|<<
name|CLOCK_ADJ
operator|)
expr_stmt|;
endif|#
directive|endif
comment|/* SLEWALWAYS */
if|if
condition|(
name|tvu_maxslew
operator|>
literal|999990
condition|)
block|{
comment|/* 		 * Don't let the maximum slew exceed 1 second in 4.  This 		 * simplifies calculations a lot since we can then deal 		 * with less-than-one-second fractions. 		 */
name|tvu_maxslew
operator|=
operator|(
literal|999990
operator|/
name|adj_precision
operator|)
operator|*
name|adj_precision
expr_stmt|;
block|}
name|TVUTOTSF
argument_list|(
name|tvu_maxslew
argument_list|,
name|tsf_maxslew
argument_list|)
expr_stmt|;
name|syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"tickadj = %d, tick = %d, tvu_maxslew = %d"
argument_list|,
name|tickadj
argument_list|,
name|tick
argument_list|,
name|tvu_maxslew
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"adj_precision = %d, tvu_maxslew = %d, tsf_maxslew = 0.%08x\n"
argument_list|,
name|adj_precision
argument_list|,
name|tvu_maxslew
argument_list|,
name|tsf_maxslew
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * Set the current offset to 0 	 */
name|sys_clock_offset
operator|.
name|l_ui
operator|=
name|sys_clock_offset
operator|.
name|l_uf
operator|=
literal|0
expr_stmt|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_LIBKVM
end_ifdef

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * This version uses the SunOS libkvm (or the bsd compatability version).  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
specifier|static
name|struct
name|nlist
name|nl
index|[]
init|=
block|{
define|#
directive|define
name|N_TICKADJ
value|0
block|{
literal|"_tickadj"
block|}
block|,
define|#
directive|define
name|N_TICK
value|1
block|{
literal|"_tick"
block|}
block|,
block|{
literal|""
block|}
block|, 	}
decl_stmt|;
if|#
directive|if
name|__convex__
comment|/* { */
if|if
condition|(
name|K_open
argument_list|(
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|,
name|O_RDONLY
argument_list|,
literal|"/vmunix"
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"K_open failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
name|kusenlist
argument_list|(
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|knlist
argument_list|(
name|nl
argument_list|)
operator|!=
literal|0
operator|||
name|nl
index|[
name|N_TICKADJ
index|]
operator|.
name|n_value
operator|==
literal|0
operator|||
name|nl
index|[
name|N_TICK
index|]
operator|.
name|n_value
operator|==
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"knlist failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|K_read
argument_list|(
name|tickadj
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|tickadj
argument_list|)
argument_list|,
name|nl
index|[
name|N_TICKADJ
index|]
operator|.
name|n_value
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
operator|*
name|tickadj
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"K_read tickadj failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|K_read
argument_list|(
name|tick
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|tick
argument_list|)
argument_list|,
name|nl
index|[
name|N_TICK
index|]
operator|.
name|n_value
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
operator|*
name|tick
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"K_read tick failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|K_close
argument_list|()
expr_stmt|;
else|#
directive|else
comment|/* }__convex__{ */
specifier|register
name|kvm_t
modifier|*
name|kd
decl_stmt|;
if|if
condition|(
operator|(
name|kd
operator|=
name|kvm_open
argument_list|(
name|NULL
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|O_RDONLY
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"kvm_open failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|kvm_nlist
argument_list|(
name|kd
argument_list|,
name|nl
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"kvm_nlist failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|kvm_read
argument_list|(
name|kd
argument_list|,
name|nl
index|[
name|N_TICKADJ
index|]
operator|.
name|n_value
argument_list|,
operator|(
name|char
operator|*
operator|)
name|tickadj
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|tickadj
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
operator|*
name|tickadj
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"kvm_read tickadj failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|kvm_read
argument_list|(
name|kd
argument_list|,
name|nl
index|[
name|N_TICK
index|]
operator|.
name|n_value
argument_list|,
operator|(
name|char
operator|*
operator|)
name|tick
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|tick
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
operator|*
name|tick
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"kvm_read tick failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|kvm_close
argument_list|(
name|kd
argument_list|)
operator|<
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"kvm_close failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/*}convex*/
undef|#
directive|undef
name|N_TICKADJ
undef|#
directive|undef
name|N_TICK
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_LIBKVM */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_READKMEM
end_ifdef

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * Note that this version grovels about in /dev/kmem to determine  * these values.  This probably should be elsewhere.  */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_UNIXWARE1
argument_list|)
end_if

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * The values set here were determined experimentally on a 486 system  * I'm not confident in them. - RAS  *  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
operator|*
name|tick
operator|=
literal|10000
expr_stmt|;
comment|/* microseconds */
operator|*
name|tickadj
operator|=
literal|80
expr_stmt|;
comment|/* microseconds */
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* SYS_UNIXWARE1 */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_AUX3
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_AUX2
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_SVR4
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_PTX
argument_list|)
end_if

begin_define
define|#
directive|define
name|K_TICKADJ_NAME
value|"tickadj"
end_define

begin_define
define|#
directive|define
name|K_TICK_NAME
value|"tick"
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_HPUX
end_ifdef

begin_define
define|#
directive|define
name|K_TICKADJ_NAME
value|"_tickadj"
end_define

begin_define
define|#
directive|define
name|K_TICK_NAME
value|"_old_tick"
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* The defaults if not defined previously */
end_comment

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|K_TICKADJ_NAME
argument_list|)
end_if

begin_define
define|#
directive|define
name|K_TICKADJ_NAME
value|"_tickadj"
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|K_TICK_NAME
argument_list|)
end_if

begin_define
define|#
directive|define
name|K_TICK_NAME
value|"_tick"
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
specifier|register
name|int
name|i
decl_stmt|;
name|int
name|kmem
decl_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|HAVE_N_UN
argument_list|)
define|#
directive|define
name|N_NAME
value|n_un.n_name
specifier|static
name|struct
name|nlist
name|nl
index|[]
init|=
block|{
block|{
block|{
name|K_TICKADJ_NAME
block|}
block|}
block|,
block|{
block|{
name|K_TICK_NAME
block|}
block|}
block|,
block|{
block|{
literal|""
block|}
block|}
block|, 	}
decl_stmt|;
else|#
directive|else
define|#
directive|define
name|N_NAME
value|n_name
specifier|static
name|struct
name|nlist
name|nl
index|[]
init|=
block|{
block|{
name|K_TICKADJ_NAME
block|}
block|,
block|{
name|K_TICK_NAME
block|}
block|,
block|{
literal|""
block|}
block|, 	}
decl_stmt|;
endif|#
directive|endif
specifier|static
name|char
modifier|*
name|kernelnames
index|[]
init|=
block|{
literal|"/vmunix"
block|,
literal|"/unix"
block|,
literal|"/mach"
block|,
literal|"/hp-ux"
block|,
literal|"/386bsd"
block|,
literal|"/netbsd"
block|,
ifdef|#
directive|ifdef
name|KERNELFILE
name|KERNELFILE
block|,
endif|#
directive|endif
name|NULL
block|}
decl_stmt|;
name|struct
name|stat
name|stbuf
decl_stmt|;
name|int
name|vars
index|[
literal|2
index|]
decl_stmt|;
define|#
directive|define
name|K_TICKADJ
value|0
define|#
directive|define
name|K_TICK
value|1
comment|/*  	 * Check to see what to use for the object file for names and get 	 * the locations of the necessary kernel variables. 	 */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|kernelnames
index|[
name|i
index|]
operator|!=
name|NULL
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|stat
argument_list|(
name|kernelnames
index|[
name|i
index|]
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|==
operator|-
literal|1
condition|)
continue|continue;
if|if
condition|(
name|nlist
argument_list|(
name|kernelnames
index|[
name|i
index|]
argument_list|,
name|nl
argument_list|)
operator|>=
literal|0
condition|)
break|break;
block|}
if|if
condition|(
name|kernelnames
index|[
name|i
index|]
operator|==
name|NULL
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Clock init couldn't find kernel object file"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Read clock parameters from kernel 	 */
name|kmem
operator|=
name|open
argument_list|(
literal|"/dev/kmem"
argument_list|,
name|O_RDONLY
argument_list|)
expr_stmt|;
if|if
condition|(
name|kmem
operator|<
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Can't open /dev/kmem for reading: %m"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|perror
argument_list|(
literal|"/dev/kmem"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
operator|(
sizeof|sizeof
argument_list|(
name|vars
argument_list|)
operator|/
sizeof|sizeof
argument_list|(
name|vars
index|[
literal|0
index|]
argument_list|)
operator|)
condition|;
name|i
operator|++
control|)
block|{
name|off_t
name|where
decl_stmt|;
name|vars
index|[
name|i
index|]
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|(
name|where
operator|=
name|nl
index|[
name|i
index|]
operator|.
name|n_value
operator|)
operator|==
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Unknown kernal var %s"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|lseek
argument_list|(
name|kmem
argument_list|,
name|where
argument_list|,
name|SEEK_SET
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"lseek for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|read
argument_list|(
name|kmem
argument_list|,
operator|&
name|vars
index|[
name|i
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
name|int
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"read for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
block|}
block|}
name|close
argument_list|(
name|kmem
argument_list|)
expr_stmt|;
operator|*
name|tickadj
operator|=
operator|(
name|U_LONG
operator|)
name|vars
index|[
name|K_TICKADJ
index|]
expr_stmt|;
operator|*
name|tick
operator|=
operator|(
name|U_LONG
operator|)
name|vars
index|[
name|K_TICK
index|]
expr_stmt|;
undef|#
directive|undef
name|K_TICKADJ
undef|#
directive|undef
name|K_TICK
undef|#
directive|undef
name|K_TICKADJ_NAME
undef|#
directive|undef
name|K_TICK_NAME
undef|#
directive|undef
name|N_NAME
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_UNIXWARE1 */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_READKMEM */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SOLARIS
argument_list|)
operator|&&
name|defined
argument_list|(
name|ADJTIME_IS_ACCURATE
argument_list|)
end_if

begin_comment
comment|/*  * clock_parms for Solaris 2.2 and later, with high-res timer kernel code.  * The clock code changed in Solaris 2.2, and tickadj went away.  * The good news is that ADJTIME_IS_ACCURATE and tick is available through   * sysconf().  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
name|int
name|hz
decl_stmt|;
name|hz
operator|=
operator|(
name|int
operator|)
name|sysconf
argument_list|(
name|_SC_CLK_TCK
argument_list|)
expr_stmt|;
operator|*
name|tick
operator|=
literal|1000000L
operator|/
name|hz
expr_stmt|;
operator|*
name|tickadj
operator|=
operator|(
operator|*
name|tick
operator|/
literal|16
operator|)
expr_stmt|;
comment|/* There is no tickadj, and it is only set here  				for tvu_maxslew calculation above. Really, 				clock_parms should return adj_precision 				and tvu_maxslew, instead of the very  				BSD-centric tickadj */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"Solaris tick = %d\n"
argument_list|,
operator|*
name|tick
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SOLARIS_HRTIME */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|sgi
argument_list|)
end_if

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * The values set here were determined experimentally on a 4D/220 and  * an R4000-50 server under IRIX 4.0.5.  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
operator|*
name|tick
operator|=
literal|10000
expr_stmt|;
operator|*
name|tickadj
operator|=
literal|150
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* sgi */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NOKMEM
end_ifdef

begin_ifndef
ifndef|#
directive|ifndef
name|HZ
end_ifndef

begin_define
define|#
directive|define
name|HZ
value|60
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * Note that this version uses static values!  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|RS6000
operator|*
name|tickadj
operator|=
literal|1000
expr_stmt|;
else|#
directive|else
comment|/*RS6000*/
if|#
directive|if
name|SYS_DOMAINOS
operator|*
name|tickadj
operator|=
literal|668
expr_stmt|;
else|#
directive|else
comment|/*SYS_DOMAINOS*/
operator|*
name|tickadj
operator|=
literal|500
operator|/
name|HZ
expr_stmt|;
endif|#
directive|endif
comment|/*SYS_DOMAINOS*/
endif|#
directive|endif
comment|/*RS6000*/
operator|*
name|tick
operator|=
literal|1000000L
operator|/
name|HZ
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"NOTE: Using preset values for tick and tickadj !!\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*NOKMEM*/
end_comment

begin_if
if|#
directive|if
operator|(
operator|(
name|defined
argument_list|(
name|SOLARIS
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|ADJTIME_IS_ACCURATE
argument_list|)
operator|)
operator|||
operator|(
name|defined
argument_list|(
name|RS6000
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|NOKMEM
argument_list|)
operator|)
operator|||
name|defined
argument_list|(
name|SYS_SINIXM
argument_list|)
operator|)
end_if

begin_ifndef
ifndef|#
directive|ifndef
name|_SC_CLK_TCK
end_ifndef

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * clock_parms - return the local clock tickadj and tick parameters  *  * Note that this version grovels about in /dev/kmem to determine  * these values.  This probably should be elsewhere.  */
end_comment

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
specifier|register
name|int
name|i
decl_stmt|;
name|int
name|kmem
decl_stmt|;
define|#
directive|define
name|N_NAME
value|n_name
specifier|static
name|struct
name|nlist
name|nl
index|[]
init|=
block|{
block|{
literal|"tickadj"
block|}
block|,
block|{
literal|""
block|}
block|, 	}
decl_stmt|;
specifier|static
name|char
modifier|*
name|kernelnames
index|[]
init|=
block|{
literal|"/kernel/unix"
block|,
literal|"/unix"
block|,
name|NULL
block|}
decl_stmt|;
name|struct
name|stat
name|stbuf
decl_stmt|;
name|int
name|vars
index|[
literal|1
index|]
decl_stmt|;
define|#
directive|define
name|K_TICKADJ
value|0
comment|/* 	 * Read clock parameters from kernel 	 */
name|kmem
operator|=
name|open
argument_list|(
literal|"/dev/kmem"
argument_list|,
name|O_RDONLY
argument_list|)
expr_stmt|;
if|if
condition|(
name|kmem
operator|<
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Can't open /dev/kmem for reading: %m"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|perror
argument_list|(
literal|"/dev/kmem"
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|kernelnames
index|[
name|i
index|]
operator|!=
name|NULL
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|stat
argument_list|(
name|kernelnames
index|[
name|i
index|]
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|==
operator|-
literal|1
condition|)
continue|continue;
if|if
condition|(
name|nlist
argument_list|(
name|kernelnames
index|[
name|i
index|]
argument_list|,
name|nl
argument_list|)
operator|>=
literal|0
condition|)
break|break;
block|}
if|if
condition|(
name|kernelnames
index|[
name|i
index|]
operator|==
name|NULL
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Clock init couldn't find kernel as either /vmunix or /unix"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
operator|(
sizeof|sizeof
argument_list|(
name|vars
argument_list|)
operator|/
sizeof|sizeof
argument_list|(
name|vars
index|[
literal|0
index|]
argument_list|)
operator|)
condition|;
name|i
operator|++
control|)
block|{
name|off_t
name|where
decl_stmt|;
name|vars
index|[
name|i
index|]
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|(
name|where
operator|=
name|nl
index|[
name|i
index|]
operator|.
name|n_value
operator|)
operator|==
literal|0
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Unknown kernal var %s"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|lseek
argument_list|(
name|kmem
argument_list|,
name|where
argument_list|,
name|SEEK_SET
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"lseek for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|read
argument_list|(
name|kmem
argument_list|,
operator|&
name|vars
index|[
name|i
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
name|int
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"read for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
name|defined
argument_list|(
name|RS6000
argument_list|)
comment|/*                  * Aix requires one more round of indirection.                  */
if|if
condition|(
name|lseek
argument_list|(
name|kmem
argument_list|,
name|vars
index|[
name|i
index|]
argument_list|,
name|SEEK_SET
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"lseek for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|read
argument_list|(
name|kmem
argument_list|,
operator|&
name|vars
index|[
name|i
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
operator|!=
sizeof|sizeof
argument_list|(
name|int
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"read for %s fails: %m"
argument_list|,
name|nl
index|[
name|i
index|]
operator|.
name|N_NAME
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
name|close
argument_list|(
name|kmem
argument_list|)
expr_stmt|;
operator|*
name|tickadj
operator|=
operator|(
name|U_LONG
operator|)
name|vars
index|[
name|K_TICKADJ
index|]
expr_stmt|;
operator|*
name|tick
operator|=
call|(
name|U_LONG
call|)
argument_list|(
literal|1000000
operator|/
name|sysconf
argument_list|(
name|_SC_CLK_TCK
argument_list|)
argument_list|)
expr_stmt|;
undef|#
directive|undef
name|K_TICKADJ
undef|#
directive|undef
name|N_NAME
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SOLARIS */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_LINUX
end_ifdef

begin_include
include|#
directive|include
file|<sys/timex.h>
end_include

begin_function
specifier|static
name|void
name|clock_parms
parameter_list|(
name|tickadj
parameter_list|,
name|tick
parameter_list|)
name|U_LONG
modifier|*
name|tickadj
decl_stmt|;
name|U_LONG
modifier|*
name|tick
decl_stmt|;
block|{
name|struct
name|timex
name|txc
decl_stmt|;
name|txc
operator|.
name|mode
operator|=
literal|0
expr_stmt|;
name|__adjtimex
argument_list|(
operator|&
name|txc
argument_list|)
expr_stmt|;
operator|*
name|tickadj
operator|=
operator|(
name|U_LONG
operator|)
literal|1
expr_stmt|;
comment|/* our adjtime is accurate */
operator|*
name|tick
operator|=
operator|(
name|U_LONG
operator|)
name|txc
operator|.
name|tick
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_LINUX */
end_comment

end_unit

