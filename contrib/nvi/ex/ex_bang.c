begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)ex_bang.c	10.33 (Berkeley) 9/23/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"../vi/vi.h"
end_include

begin_comment
comment|/*  * ex_bang -- :[line [,line]] ! command  *  * Pass the rest of the line after the ! character to the program named by  * the O_SHELL option.  *  * Historical vi did NOT do shell expansion on the arguments before passing  * them, only file name expansion.  This means that the O_SHELL program got  * "$t" as an argument if that is what the user entered.  Also, there's a  * special expansion done for the bang command.  Any exclamation points in  * the user's argument are replaced by the last, expanded ! command.  *  * There's some fairly amazing slop in this routine to make the different  * ways of getting here display the right things.  It took a long time to  * get it right (wrong?), so be careful.  *  * PUBLIC: int ex_bang __P((SCR *, EXCMD *));  */
end_comment

begin_function
name|int
name|ex_bang
parameter_list|(
name|sp
parameter_list|,
name|cmdp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|EXCMD
modifier|*
name|cmdp
decl_stmt|;
block|{
name|enum
name|filtertype
name|ftype
decl_stmt|;
name|ARGS
modifier|*
name|ap
decl_stmt|;
name|EX_PRIVATE
modifier|*
name|exp
decl_stmt|;
name|MARK
name|rm
decl_stmt|;
name|recno_t
name|lno
decl_stmt|;
name|int
name|rval
decl_stmt|;
specifier|const
name|char
modifier|*
name|msg
decl_stmt|;
name|ap
operator|=
name|cmdp
operator|->
name|argv
index|[
literal|0
index|]
expr_stmt|;
if|if
condition|(
name|ap
operator|->
name|len
operator|==
literal|0
condition|)
block|{
name|ex_emsg
argument_list|(
name|sp
argument_list|,
name|cmdp
operator|->
name|cmd
operator|->
name|usage
argument_list|,
name|EXM_USAGE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* Set the "last bang command" remembered value. */
name|exp
operator|=
name|EXP
argument_list|(
name|sp
argument_list|)
expr_stmt|;
if|if
condition|(
name|exp
operator|->
name|lastbcomm
operator|!=
name|NULL
condition|)
name|free
argument_list|(
name|exp
operator|->
name|lastbcomm
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|exp
operator|->
name|lastbcomm
operator|=
name|strdup
argument_list|(
name|ap
operator|->
name|bp
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_SYSERR
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * If the command was modified by the expansion, it was historically 	 * redisplayed. 	 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|cmdp
argument_list|,
name|E_MODIFY
argument_list|)
operator|&&
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_EX_SILENT
argument_list|)
condition|)
block|{
comment|/* 		 * Display the command if modified.  Historic ex/vi displayed 		 * the command if it was modified due to file name and/or bang 		 * expansion.  If piping lines in vi, it would be immediately 		 * overwritten by any error or line change reporting. 		 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_VI
argument_list|)
condition|)
name|vs_update
argument_list|(
name|sp
argument_list|,
literal|"!"
argument_list|,
name|ap
operator|->
name|bp
argument_list|)
expr_stmt|;
else|else
block|{
operator|(
name|void
operator|)
name|ex_printf
argument_list|(
name|sp
argument_list|,
literal|"!%s\n"
argument_list|,
name|ap
operator|->
name|bp
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|ex_fflush
argument_list|(
name|sp
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* 	 * If no addresses were specified, run the command.  If there's an 	 * underlying file, it's been modified and autowrite is set, write 	 * the file back.  If the file has been modified, autowrite is not 	 * set and the warn option is set, tell the user about the file. 	 */
if|if
condition|(
name|cmdp
operator|->
name|addrcnt
operator|==
literal|0
condition|)
block|{
name|msg
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|sp
operator|->
name|ep
operator|!=
name|NULL
operator|&&
name|F_ISSET
argument_list|(
name|sp
operator|->
name|ep
argument_list|,
name|F_MODIFIED
argument_list|)
condition|)
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_AUTOWRITE
argument_list|)
condition|)
block|{
if|if
condition|(
name|file_aw
argument_list|(
name|sp
argument_list|,
name|FS_ALL
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_WARN
argument_list|)
operator|&&
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_EX_SILENT
argument_list|)
condition|)
name|msg
operator|=
name|msg_cat
argument_list|(
name|sp
argument_list|,
literal|"303|File modified since last write."
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* If we're still in a vi screen, move out explicitly. */
operator|(
name|void
operator|)
name|ex_exec_proc
argument_list|(
name|sp
argument_list|,
name|cmdp
argument_list|,
name|ap
operator|->
name|bp
argument_list|,
name|msg
argument_list|,
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_EX
operator||
name|SC_SCR_EXWROTE
argument_list|)
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * If addresses were specified, pipe lines from the file through the 	 * command. 	 * 	 * Historically, vi lines were replaced by both the stdout and stderr 	 * lines of the command, but ex lines by only the stdout lines.  This 	 * makes no sense to me, so nvi makes it consistent for both, and 	 * matches vi's historic behavior. 	 */
else|else
block|{
name|NEEDFILE
argument_list|(
name|sp
argument_list|,
name|cmdp
argument_list|)
expr_stmt|;
comment|/* Autoprint is set historically, even if the command fails. */
name|F_SET
argument_list|(
name|cmdp
argument_list|,
name|E_AUTOPRINT
argument_list|)
expr_stmt|;
comment|/* 		 * !!! 		 * Historical vi permitted "!!" in an empty file.  When this 		 * happens, we arrive here with two addresses of 1,1 and a 		 * bad attitude.  The simple solution is to turn it into a 		 * FILTER_READ operation, with the exception that stdin isn't 		 * opened for the utility, and the cursor position isn't the 		 * same.  The only historic glitch (I think) is that we don't 		 * put an empty line into the default cut buffer, as historic 		 * vi did.  Imagine, if you can, my disappointment. 		 */
name|ftype
operator|=
name|FILTER_BANG
expr_stmt|;
if|if
condition|(
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|==
literal|1
operator|&&
name|cmdp
operator|->
name|addr2
operator|.
name|lno
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|lno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|lno
operator|==
literal|0
condition|)
block|{
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|=
name|cmdp
operator|->
name|addr2
operator|.
name|lno
operator|=
literal|0
expr_stmt|;
name|ftype
operator|=
name|FILTER_RBANG
expr_stmt|;
block|}
block|}
name|rval
operator|=
name|ex_filter
argument_list|(
name|sp
argument_list|,
name|cmdp
argument_list|,
operator|&
name|cmdp
operator|->
name|addr1
argument_list|,
operator|&
name|cmdp
operator|->
name|addr2
argument_list|,
operator|&
name|rm
argument_list|,
name|ap
operator|->
name|bp
argument_list|,
name|ftype
argument_list|)
expr_stmt|;
comment|/* 		 * If in vi mode, move to the first nonblank. 		 * 		 * !!! 		 * Historic vi wasn't consistent in this area -- if you used 		 * a forward motion it moved to the first nonblank, but if you 		 * did a backward motion it didn't.  And, if you followed a 		 * backward motion with a forward motion, it wouldn't move to 		 * the nonblank for either.  Going to the nonblank generally 		 * seems more useful and consistent, so we do it. 		 */
name|sp
operator|->
name|lno
operator|=
name|rm
operator|.
name|lno
expr_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_VI
argument_list|)
condition|)
block|{
name|sp
operator|->
name|cno
operator|=
literal|0
expr_stmt|;
operator|(
name|void
operator|)
name|nonblank
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
operator|&
name|sp
operator|->
name|cno
argument_list|)
expr_stmt|;
block|}
else|else
name|sp
operator|->
name|cno
operator|=
name|rm
operator|.
name|cno
expr_stmt|;
block|}
comment|/* Ex terminates with a bang, even if the command fails. */
if|if
condition|(
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_VI
argument_list|)
operator|&&
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_EX_SILENT
argument_list|)
condition|)
operator|(
name|void
operator|)
name|ex_puts
argument_list|(
name|sp
argument_list|,
literal|"!\n"
argument_list|)
expr_stmt|;
comment|/* 	 * XXX 	 * The ! commands never return an error, so that autoprint always 	 * happens in the ex parser. 	 */
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

