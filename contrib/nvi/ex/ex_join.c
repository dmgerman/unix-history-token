begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)ex_join.c	10.10 (Berkeley) 9/15/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_comment
comment|/*  * ex_join -- :[line [,line]] j[oin][!] [count] [flags]  *	Join lines.  *  * PUBLIC: int ex_join __P((SCR *, EXCMD *));  */
end_comment

begin_function
name|int
name|ex_join
parameter_list|(
name|sp
parameter_list|,
name|cmdp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|EXCMD
modifier|*
name|cmdp
decl_stmt|;
block|{
name|recno_t
name|from
decl_stmt|,
name|to
decl_stmt|;
name|size_t
name|blen
decl_stmt|,
name|clen
decl_stmt|,
name|len
decl_stmt|,
name|tlen
decl_stmt|;
name|int
name|echar
decl_stmt|,
name|extra
decl_stmt|,
name|first
decl_stmt|;
name|char
modifier|*
name|bp
decl_stmt|,
modifier|*
name|p
decl_stmt|,
modifier|*
name|tbp
decl_stmt|;
name|NEEDFILE
argument_list|(
name|sp
argument_list|,
name|cmdp
argument_list|)
expr_stmt|;
name|from
operator|=
name|cmdp
operator|->
name|addr1
operator|.
name|lno
expr_stmt|;
name|to
operator|=
name|cmdp
operator|->
name|addr2
operator|.
name|lno
expr_stmt|;
comment|/* Check for no lines to join. */
if|if
condition|(
operator|!
name|db_exist
argument_list|(
name|sp
argument_list|,
name|from
operator|+
literal|1
argument_list|)
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"131|No following lines to join"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|GET_SPACE_RET
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|,
literal|256
argument_list|)
expr_stmt|;
comment|/* 	 * The count for the join command was off-by-one, 	 * historically, to other counts for other commands. 	 */
if|if
condition|(
name|FL_ISSET
argument_list|(
name|cmdp
operator|->
name|iflags
argument_list|,
name|E_C_COUNT
argument_list|)
condition|)
operator|++
name|cmdp
operator|->
name|addr2
operator|.
name|lno
expr_stmt|;
comment|/* 	 * If only a single address specified, or, the same address 	 * specified twice, the from/two addresses will be the same. 	 */
if|if
condition|(
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|==
name|cmdp
operator|->
name|addr2
operator|.
name|lno
condition|)
operator|++
name|cmdp
operator|->
name|addr2
operator|.
name|lno
expr_stmt|;
name|clen
operator|=
name|tlen
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|first
operator|=
literal|1
operator|,
name|from
operator|=
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|,
name|to
operator|=
name|cmdp
operator|->
name|addr2
operator|.
name|lno
init|;
name|from
operator|<=
name|to
condition|;
operator|++
name|from
control|)
block|{
comment|/* 		 * Get next line.  Historic versions of vi allowed "10J" while 		 * less than 10 lines from the end-of-file, so we do too. 		 */
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|from
argument_list|,
literal|0
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
block|{
name|cmdp
operator|->
name|addr2
operator|.
name|lno
operator|=
name|from
operator|-
literal|1
expr_stmt|;
break|break;
block|}
comment|/* Empty lines just go away. */
if|if
condition|(
name|len
operator|==
literal|0
condition|)
continue|continue;
comment|/* 		 * Get more space if necessary.  Note, tlen isn't the length 		 * of the new line, it's roughly the amount of space needed. 		 * tbp - bp is the length of the new line. 		 */
name|tlen
operator|+=
name|len
operator|+
literal|2
expr_stmt|;
name|ADD_SPACE_RET
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|,
name|tlen
argument_list|)
expr_stmt|;
name|tbp
operator|=
name|bp
operator|+
name|clen
expr_stmt|;
comment|/* 		 * Historic practice: 		 * 		 * If force specified, join without modification. 		 * If the current line ends with whitespace, strip leading 		 *    whitespace from the joined line. 		 * If the next line starts with a ), do nothing. 		 * If the current line ends with ., insert two spaces. 		 * Else, insert one space. 		 * 		 * One change -- add ? and ! to the list of characters for 		 * which we insert two spaces.  I expect that POSIX 1003.2 		 * will require this as well. 		 * 		 * Echar is the last character in the last line joined. 		 */
name|extra
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|!
name|first
operator|&&
operator|!
name|FL_ISSET
argument_list|(
name|cmdp
operator|->
name|iflags
argument_list|,
name|E_C_FORCE
argument_list|)
condition|)
block|{
if|if
condition|(
name|isblank
argument_list|(
name|echar
argument_list|)
condition|)
for|for
control|(
init|;
name|len
operator|&&
name|isblank
argument_list|(
operator|*
name|p
argument_list|)
condition|;
operator|--
name|len
operator|,
operator|++
name|p
control|)
empty_stmt|;
elseif|else
if|if
condition|(
name|p
index|[
literal|0
index|]
operator|!=
literal|')'
condition|)
block|{
if|if
condition|(
name|strchr
argument_list|(
literal|".?!"
argument_list|,
name|echar
argument_list|)
condition|)
block|{
operator|*
name|tbp
operator|++
operator|=
literal|' '
expr_stmt|;
operator|++
name|clen
expr_stmt|;
name|extra
operator|=
literal|1
expr_stmt|;
block|}
operator|*
name|tbp
operator|++
operator|=
literal|' '
expr_stmt|;
operator|++
name|clen
expr_stmt|;
for|for
control|(
init|;
name|len
operator|&&
name|isblank
argument_list|(
operator|*
name|p
argument_list|)
condition|;
operator|--
name|len
operator|,
operator|++
name|p
control|)
empty_stmt|;
block|}
block|}
if|if
condition|(
name|len
operator|!=
literal|0
condition|)
block|{
name|memcpy
argument_list|(
name|tbp
argument_list|,
name|p
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|tbp
operator|+=
name|len
expr_stmt|;
name|clen
operator|+=
name|len
expr_stmt|;
name|echar
operator|=
name|p
index|[
name|len
operator|-
literal|1
index|]
expr_stmt|;
block|}
else|else
name|echar
operator|=
literal|' '
expr_stmt|;
comment|/* 		 * Historic practice for vi was to put the cursor at the first 		 * inserted whitespace character, if there was one, or the 		 * first character of the joined line, if there wasn't, or the 		 * last character of the line if joined to an empty line.  If 		 * a count was specified, the cursor was moved as described 		 * for the first line joined, ignoring subsequent lines.  If 		 * the join was a ':' command, the cursor was placed at the 		 * first non-blank character of the line unless the cursor was 		 * "attracted" to the end of line when the command was executed 		 * in which case it moved to the new end of line.  There are 		 * probably several more special cases, but frankly, my dear, 		 * I don't give a damn.  This implementation puts the cursor 		 * on the first inserted whitespace character, the first 		 * character of the joined line, or the last character of the 		 * line regardless.  Note, if the cursor isn't on the joined 		 * line (possible with : commands), it is reset to the starting 		 * line. 		 */
if|if
condition|(
name|first
condition|)
block|{
name|sp
operator|->
name|cno
operator|=
operator|(
name|tbp
operator|-
name|bp
operator|)
operator|-
operator|(
literal|1
operator|+
name|extra
operator|)
expr_stmt|;
name|first
operator|=
literal|0
expr_stmt|;
block|}
else|else
name|sp
operator|->
name|cno
operator|=
operator|(
name|tbp
operator|-
name|bp
operator|)
operator|-
name|len
operator|-
operator|(
literal|1
operator|+
name|extra
operator|)
expr_stmt|;
block|}
name|sp
operator|->
name|lno
operator|=
name|cmdp
operator|->
name|addr1
operator|.
name|lno
expr_stmt|;
comment|/* Delete the joined lines. */
for|for
control|(
name|from
operator|=
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|,
name|to
operator|=
name|cmdp
operator|->
name|addr2
operator|.
name|lno
init|;
name|to
operator|>
name|from
condition|;
operator|--
name|to
control|)
if|if
condition|(
name|db_delete
argument_list|(
name|sp
argument_list|,
name|to
argument_list|)
condition|)
goto|goto
name|err
goto|;
comment|/* If the original line changed, reset it. */
if|if
condition|(
operator|!
name|first
operator|&&
name|db_set
argument_list|(
name|sp
argument_list|,
name|from
argument_list|,
name|bp
argument_list|,
name|tbp
operator|-
name|bp
argument_list|)
condition|)
block|{
name|err
label|:
name|FREE_SPACE
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|FREE_SPACE
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|)
expr_stmt|;
name|sp
operator|->
name|rptlines
index|[
name|L_JOINED
index|]
operator|+=
operator|(
name|cmdp
operator|->
name|addr2
operator|.
name|lno
operator|-
name|cmdp
operator|->
name|addr1
operator|.
name|lno
operator|)
operator|+
literal|1
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

