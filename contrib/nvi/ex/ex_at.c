begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)ex_at.c	10.12 (Berkeley) 9/15/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_comment
comment|/*  * ex_at -- :@[@ | buffer]  *	    :*[* | buffer]  *  *	Execute the contents of the buffer.  *  * PUBLIC: int ex_at __P((SCR *, EXCMD *));  */
end_comment

begin_function
name|int
name|ex_at
parameter_list|(
name|sp
parameter_list|,
name|cmdp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|EXCMD
modifier|*
name|cmdp
decl_stmt|;
block|{
name|CB
modifier|*
name|cbp
decl_stmt|;
name|CHAR_T
name|name
decl_stmt|;
name|EXCMD
modifier|*
name|ecp
decl_stmt|;
name|RANGE
modifier|*
name|rp
decl_stmt|;
name|TEXT
modifier|*
name|tp
decl_stmt|;
name|size_t
name|len
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
comment|/* 	 * !!! 	 * Historically, [@*]<carriage-return> and [@*][@*] executed the most 	 * recently executed buffer in ex mode. 	 */
name|name
operator|=
name|FL_ISSET
argument_list|(
name|cmdp
operator|->
name|iflags
argument_list|,
name|E_C_BUFFER
argument_list|)
condition|?
name|cmdp
operator|->
name|buffer
else|:
literal|'@'
expr_stmt|;
if|if
condition|(
name|name
operator|==
literal|'@'
operator|||
name|name
operator|==
literal|'*'
condition|)
block|{
if|if
condition|(
operator|!
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_AT_SET
argument_list|)
condition|)
block|{
name|ex_emsg
argument_list|(
name|sp
argument_list|,
name|NULL
argument_list|,
name|EXM_NOPREVBUF
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|name
operator|=
name|sp
operator|->
name|at_lbuf
expr_stmt|;
block|}
name|sp
operator|->
name|at_lbuf
operator|=
name|name
expr_stmt|;
name|F_SET
argument_list|(
name|sp
argument_list|,
name|SC_AT_SET
argument_list|)
expr_stmt|;
name|CBNAME
argument_list|(
name|sp
argument_list|,
name|cbp
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|cbp
operator|==
name|NULL
condition|)
block|{
name|ex_emsg
argument_list|(
name|sp
argument_list|,
name|KEY_NAME
argument_list|(
name|sp
argument_list|,
name|name
argument_list|)
argument_list|,
name|EXM_EMPTYBUF
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * !!! 	 * Historically the @ command took a range of lines, and the @ buffer 	 * was executed once per line.  The historic vi could be trashed by 	 * this because it didn't notice if the underlying file changed, or, 	 * for that matter, if there were no more lines on which to operate. 	 * For example, take a 10 line file, load "%delete" into a buffer, 	 * and enter :8,10@<buffer>. 	 * 	 * The solution is a bit tricky.  If the user specifies a range, take 	 * the same approach as for global commands, and discard the command 	 * if exit or switch to a new file/screen.  If the user doesn't specify 	 * the  range, continue to execute after a file/screen switch, which 	 * means @ buffers are still useful in a multi-screen environment. 	 */
name|CALLOC_RET
argument_list|(
name|sp
argument_list|,
name|ecp
argument_list|,
name|EXCMD
operator|*
argument_list|,
literal|1
argument_list|,
sizeof|sizeof
argument_list|(
name|EXCMD
argument_list|)
argument_list|)
expr_stmt|;
name|CIRCLEQ_INIT
argument_list|(
operator|&
name|ecp
operator|->
name|rq
argument_list|)
expr_stmt|;
name|CALLOC_RET
argument_list|(
name|sp
argument_list|,
name|rp
argument_list|,
name|RANGE
operator|*
argument_list|,
literal|1
argument_list|,
sizeof|sizeof
argument_list|(
name|RANGE
argument_list|)
argument_list|)
expr_stmt|;
name|rp
operator|->
name|start
operator|=
name|cmdp
operator|->
name|addr1
operator|.
name|lno
expr_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|cmdp
argument_list|,
name|E_ADDR_DEF
argument_list|)
condition|)
block|{
name|rp
operator|->
name|stop
operator|=
name|rp
operator|->
name|start
expr_stmt|;
name|FL_SET
argument_list|(
name|ecp
operator|->
name|agv_flags
argument_list|,
name|AGV_AT_NORANGE
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|rp
operator|->
name|stop
operator|=
name|cmdp
operator|->
name|addr2
operator|.
name|lno
expr_stmt|;
name|FL_SET
argument_list|(
name|ecp
operator|->
name|agv_flags
argument_list|,
name|AGV_AT
argument_list|)
expr_stmt|;
block|}
name|CIRCLEQ_INSERT_HEAD
argument_list|(
operator|&
name|ecp
operator|->
name|rq
argument_list|,
name|rp
argument_list|,
name|q
argument_list|)
expr_stmt|;
comment|/* 	 * Buffers executed in ex mode or from the colon command line in vi 	 * were ex commands.  We can't push it on the terminal queue, since 	 * it has to be executed immediately, and we may be in the middle of 	 * an ex command already.  Push the command on the ex command stack. 	 * Build two copies of the command.  We need two copies because the 	 * ex parser may step on the command string when it's parsing it. 	 */
for|for
control|(
name|len
operator|=
literal|0
operator|,
name|tp
operator|=
name|cbp
operator|->
name|textq
operator|.
name|cqh_last
init|;
name|tp
operator|!=
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|;
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_prev
control|)
name|len
operator|+=
name|tp
operator|->
name|len
operator|+
literal|1
expr_stmt|;
name|MALLOC_RET
argument_list|(
name|sp
argument_list|,
name|ecp
operator|->
name|cp
argument_list|,
name|char
operator|*
argument_list|,
name|len
operator|*
literal|2
argument_list|)
expr_stmt|;
name|ecp
operator|->
name|o_cp
operator|=
name|ecp
operator|->
name|cp
expr_stmt|;
name|ecp
operator|->
name|o_clen
operator|=
name|len
expr_stmt|;
name|ecp
operator|->
name|cp
index|[
name|len
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/* Copy the buffer into the command space. */
for|for
control|(
name|p
operator|=
name|ecp
operator|->
name|cp
operator|+
name|len
operator|,
name|tp
operator|=
name|cbp
operator|->
name|textq
operator|.
name|cqh_last
init|;
name|tp
operator|!=
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|;
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_prev
control|)
block|{
name|memcpy
argument_list|(
name|p
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|len
argument_list|)
expr_stmt|;
name|p
operator|+=
name|tp
operator|->
name|len
expr_stmt|;
operator|*
name|p
operator|++
operator|=
literal|'\n'
expr_stmt|;
block|}
name|LIST_INSERT_HEAD
argument_list|(
operator|&
name|sp
operator|->
name|gp
operator|->
name|ecq
argument_list|,
name|ecp
argument_list|,
name|q
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

