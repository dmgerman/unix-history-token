begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)put.c	10.11 (Berkeley) 9/23/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_comment
comment|/*  * put --  *	Put text buffer contents into the file.  *  * PUBLIC: int put __P((SCR *, CB *, CHAR_T *, MARK *, MARK *, int));  */
end_comment

begin_function
name|int
name|put
parameter_list|(
name|sp
parameter_list|,
name|cbp
parameter_list|,
name|namep
parameter_list|,
name|cp
parameter_list|,
name|rp
parameter_list|,
name|append
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|CB
modifier|*
name|cbp
decl_stmt|;
name|CHAR_T
modifier|*
name|namep
decl_stmt|;
name|MARK
modifier|*
name|cp
decl_stmt|,
decl|*
name|rp
decl_stmt|;
end_function

begin_decl_stmt
name|int
name|append
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|CHAR_T
name|name
decl_stmt|;
name|TEXT
modifier|*
name|ltp
decl_stmt|,
modifier|*
name|tp
decl_stmt|;
name|recno_t
name|lno
decl_stmt|;
name|size_t
name|blen
decl_stmt|,
name|clen
decl_stmt|,
name|len
decl_stmt|;
name|int
name|rval
decl_stmt|;
name|char
modifier|*
name|bp
decl_stmt|,
modifier|*
name|p
decl_stmt|,
modifier|*
name|t
decl_stmt|;
if|if
condition|(
name|cbp
operator|==
name|NULL
condition|)
if|if
condition|(
name|namep
operator|==
name|NULL
condition|)
block|{
name|cbp
operator|=
name|sp
operator|->
name|gp
operator|->
name|dcbp
expr_stmt|;
if|if
condition|(
name|cbp
operator|==
name|NULL
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"053|The default buffer is empty"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
else|else
block|{
name|name
operator|=
operator|*
name|namep
expr_stmt|;
name|CBNAME
argument_list|(
name|sp
argument_list|,
name|cbp
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|cbp
operator|==
name|NULL
condition|)
block|{
name|msgq
argument_list|(
name|sp
argument_list|,
name|M_ERR
argument_list|,
literal|"054|Buffer %s is empty"
argument_list|,
name|KEY_NAME
argument_list|(
name|sp
argument_list|,
name|name
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
name|tp
operator|=
name|cbp
operator|->
name|textq
operator|.
name|cqh_first
expr_stmt|;
comment|/* 	 * It's possible to do a put into an empty file, meaning that the cut 	 * buffer simply becomes the file.  It's a special case so that we can 	 * ignore it in general. 	 * 	 * !!! 	 * Historically, pasting into a file with no lines in vi would preserve 	 * the single blank line.  This is surely a result of the fact that the 	 * historic vi couldn't deal with a file that had no lines in it.  This 	 * implementation treats that as a bug, and does not retain the blank 	 * line. 	 * 	 * Historical practice is that the cursor ends at the first character 	 * in the file. 	 */
if|if
condition|(
name|cp
operator|->
name|lno
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|lno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|lno
operator|==
literal|0
condition|)
block|{
for|for
control|(
init|;
name|tp
operator|!=
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|;
operator|++
name|lno
operator|,
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_ADDED
index|]
operator|,
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_next
control|)
if|if
condition|(
name|db_append
argument_list|(
name|sp
argument_list|,
literal|1
argument_list|,
name|lno
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|rp
operator|->
name|lno
operator|=
literal|1
expr_stmt|;
name|rp
operator|->
name|cno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
block|}
comment|/* If a line mode buffer, append each new line into the file. */
if|if
condition|(
name|F_ISSET
argument_list|(
name|cbp
argument_list|,
name|CB_LMODE
argument_list|)
condition|)
block|{
name|lno
operator|=
name|append
condition|?
name|cp
operator|->
name|lno
else|:
name|cp
operator|->
name|lno
operator|-
literal|1
expr_stmt|;
name|rp
operator|->
name|lno
operator|=
name|lno
operator|+
literal|1
expr_stmt|;
for|for
control|(
init|;
name|tp
operator|!=
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|;
operator|++
name|lno
operator|,
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_ADDED
index|]
operator|,
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_next
control|)
if|if
condition|(
name|db_append
argument_list|(
name|sp
argument_list|,
literal|1
argument_list|,
name|lno
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|rp
operator|->
name|cno
operator|=
literal|0
expr_stmt|;
operator|(
name|void
operator|)
name|nonblank
argument_list|(
name|sp
argument_list|,
name|rp
operator|->
name|lno
argument_list|,
operator|&
name|rp
operator|->
name|cno
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
comment|/* 	 * If buffer was cut in character mode, replace the current line with 	 * one built from the portion of the first line to the left of the 	 * split plus the first line in the CB.  Append each intermediate line 	 * in the CB.  Append a line built from the portion of the first line 	 * to the right of the split plus the last line in the CB. 	 * 	 * Get the first line. 	 */
name|lno
operator|=
name|cp
operator|->
name|lno
expr_stmt|;
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|DBG_FATAL
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|GET_SPACE_RET
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|,
name|tp
operator|->
name|len
operator|+
name|len
operator|+
literal|1
argument_list|)
expr_stmt|;
name|t
operator|=
name|bp
expr_stmt|;
comment|/* Original line, left of the split. */
if|if
condition|(
name|len
operator|>
literal|0
operator|&&
operator|(
name|clen
operator|=
name|cp
operator|->
name|cno
operator|+
operator|(
name|append
condition|?
literal|1
else|:
literal|0
operator|)
operator|)
operator|>
literal|0
condition|)
block|{
name|memcpy
argument_list|(
name|bp
argument_list|,
name|p
argument_list|,
name|clen
argument_list|)
expr_stmt|;
name|p
operator|+=
name|clen
expr_stmt|;
name|t
operator|+=
name|clen
expr_stmt|;
block|}
comment|/* First line from the CB. */
if|if
condition|(
name|tp
operator|->
name|len
operator|!=
literal|0
condition|)
block|{
name|memcpy
argument_list|(
name|t
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|len
argument_list|)
expr_stmt|;
name|t
operator|+=
name|tp
operator|->
name|len
expr_stmt|;
block|}
comment|/* Calculate length left in the original line. */
name|clen
operator|=
name|len
operator|==
literal|0
condition|?
literal|0
else|:
name|len
operator|-
operator|(
name|cp
operator|->
name|cno
operator|+
operator|(
name|append
condition|?
literal|1
else|:
literal|0
operator|)
operator|)
expr_stmt|;
comment|/* 	 * !!! 	 * In the historical 4BSD version of vi, character mode puts within 	 * a single line have two cursor behaviors: if the put is from the 	 * unnamed buffer, the cursor moves to the character inserted which 	 * appears last in the file.  If the put is from a named buffer, 	 * the cursor moves to the character inserted which appears first 	 * in the file.  In System III/V, it was changed at some point and 	 * the cursor always moves to the first character.  In both versions 	 * of vi, character mode puts that cross line boundaries leave the 	 * cursor on the first character.  Nvi implements the System III/V 	 * behavior, and expect POSIX.2 to do so as well. 	 */
name|rp
operator|->
name|lno
operator|=
name|lno
expr_stmt|;
name|rp
operator|->
name|cno
operator|=
name|len
operator|==
literal|0
condition|?
literal|0
else|:
name|sp
operator|->
name|cno
operator|+
operator|(
name|append
operator|&&
name|tp
operator|->
name|len
condition|?
literal|1
else|:
literal|0
operator|)
expr_stmt|;
comment|/* 	 * If no more lines in the CB, append the rest of the original 	 * line and quit.  Otherwise, build the last line before doing 	 * the intermediate lines, because the line changes will lose 	 * the cached line. 	 */
if|if
condition|(
name|tp
operator|->
name|q
operator|.
name|cqe_next
operator|==
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|)
block|{
if|if
condition|(
name|clen
operator|>
literal|0
condition|)
block|{
name|memcpy
argument_list|(
name|t
argument_list|,
name|p
argument_list|,
name|clen
argument_list|)
expr_stmt|;
name|t
operator|+=
name|clen
expr_stmt|;
block|}
if|if
condition|(
name|db_set
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|bp
argument_list|,
name|t
operator|-
name|bp
argument_list|)
condition|)
goto|goto
name|err
goto|;
if|if
condition|(
name|sp
operator|->
name|rptlchange
operator|!=
name|lno
condition|)
block|{
name|sp
operator|->
name|rptlchange
operator|=
name|lno
expr_stmt|;
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_CHANGED
index|]
expr_stmt|;
block|}
block|}
else|else
block|{
comment|/* 		 * Have to build both the first and last lines of the 		 * put before doing any sets or we'll lose the cached 		 * line.  Build both the first and last lines in the 		 * same buffer, so we don't have to have another buffer 		 * floating around. 		 * 		 * Last part of original line; check for space, reset 		 * the pointer into the buffer. 		 */
name|ltp
operator|=
name|cbp
operator|->
name|textq
operator|.
name|cqh_last
expr_stmt|;
name|len
operator|=
name|t
operator|-
name|bp
expr_stmt|;
name|ADD_SPACE_RET
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|,
name|ltp
operator|->
name|len
operator|+
name|clen
argument_list|)
expr_stmt|;
name|t
operator|=
name|bp
operator|+
name|len
expr_stmt|;
comment|/* Add in last part of the CB. */
name|memcpy
argument_list|(
name|t
argument_list|,
name|ltp
operator|->
name|lb
argument_list|,
name|ltp
operator|->
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|clen
condition|)
name|memcpy
argument_list|(
name|t
operator|+
name|ltp
operator|->
name|len
argument_list|,
name|p
argument_list|,
name|clen
argument_list|)
expr_stmt|;
name|clen
operator|+=
name|ltp
operator|->
name|len
expr_stmt|;
comment|/* 		 * Now: bp points to the first character of the first 		 * line, t points to the last character of the last 		 * line, t - bp is the length of the first line, and 		 * clen is the length of the last.  Just figured you'd 		 * want to know. 		 * 		 * Output the line replacing the original line. 		 */
if|if
condition|(
name|db_set
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|bp
argument_list|,
name|t
operator|-
name|bp
argument_list|)
condition|)
goto|goto
name|err
goto|;
if|if
condition|(
name|sp
operator|->
name|rptlchange
operator|!=
name|lno
condition|)
block|{
name|sp
operator|->
name|rptlchange
operator|=
name|lno
expr_stmt|;
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_CHANGED
index|]
expr_stmt|;
block|}
comment|/* Output any intermediate lines in the CB. */
for|for
control|(
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_next
init|;
name|tp
operator|->
name|q
operator|.
name|cqe_next
operator|!=
operator|(
name|void
operator|*
operator|)
operator|&
name|cbp
operator|->
name|textq
condition|;
operator|++
name|lno
operator|,
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_ADDED
index|]
operator|,
name|tp
operator|=
name|tp
operator|->
name|q
operator|.
name|cqe_next
control|)
if|if
condition|(
name|db_append
argument_list|(
name|sp
argument_list|,
literal|1
argument_list|,
name|lno
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|len
argument_list|)
condition|)
goto|goto
name|err
goto|;
if|if
condition|(
name|db_append
argument_list|(
name|sp
argument_list|,
literal|1
argument_list|,
name|lno
argument_list|,
name|t
argument_list|,
name|clen
argument_list|)
condition|)
goto|goto
name|err
goto|;
operator|++
name|sp
operator|->
name|rptlines
index|[
name|L_ADDED
index|]
expr_stmt|;
block|}
name|rval
operator|=
literal|0
expr_stmt|;
if|if
condition|(
literal|0
condition|)
name|err
label|:
name|rval
operator|=
literal|1
expr_stmt|;
name|FREE_SPACE
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|)
expr_stmt|;
return|return
operator|(
name|rval
operator|)
return|;
block|}
end_block

end_unit

