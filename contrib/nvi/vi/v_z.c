begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_z.c	10.10 (Berkeley) 5/16/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_comment
comment|/*  * v_z -- [count]z[count][-.+^<CR>]  *	Move the screen.  *  * PUBLIC: int v_z __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_z
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|lno
decl_stmt|;
name|u_int
name|value
decl_stmt|;
comment|/* 	 * The first count is the line to use.  If the value doesn't 	 * exist, use the last line. 	 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
block|{
name|lno
operator|=
name|vp
operator|->
name|count
expr_stmt|;
if|if
condition|(
operator|!
name|db_exist
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|)
operator|&&
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|lno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
block|}
else|else
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
comment|/* Set default return cursor line. */
name|vp
operator|->
name|m_final
operator|.
name|lno
operator|=
name|lno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|.
name|cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
comment|/* 	 * The second count is the displayed window size, i.e. the 'z' command 	 * is another way to get artificially small windows.  Note, you can't 	 * grow beyond the size of the window. 	 * 	 * !!! 	 * A window size of 0 was historically allowed, and simply ignored. 	 * This could be much more simply done by modifying the value of the 	 * O_WINDOW option, but that's not how it worked historically. 	 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C2SET
argument_list|)
operator|&&
name|vp
operator|->
name|count2
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|vp
operator|->
name|count2
operator|>
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
condition|)
name|vp
operator|->
name|count2
operator|=
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
expr_stmt|;
if|if
condition|(
name|vs_crel
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|count2
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
block|}
switch|switch
condition|(
name|vp
operator|->
name|character
condition|)
block|{
case|case
literal|'-'
case|:
comment|/* Put the line at the bottom. */
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|P_BOTTOM
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
case|case
literal|'.'
case|:
comment|/* Put the line in the middle. */
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|P_MIDDLE
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
case|case
literal|'+'
case|:
comment|/* 		 * If the user specified a line number, put that line at the 		 * top and move the cursor to it.  Otherwise, scroll forward 		 * a screen from the current screen. 		 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
block|{
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|P_TOP
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|vs_sm_position
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_final
argument_list|,
literal|0
argument_list|,
name|P_TOP
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_final
argument_list|,
name|sp
operator|->
name|t_rows
argument_list|,
name|Z_PLUS
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
case|case
literal|'^'
case|:
comment|/* 		 * If the user specified a line number, put that line at the 		 * bottom, move the cursor to it, and then display the screen 		 * before that one.  Otherwise, scroll backward a screen from 		 * the current screen. 		 * 		 * !!! 		 * Note, we match the off-by-one characteristics of historic 		 * vi, here. 		 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
block|{
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|P_BOTTOM
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|vs_sm_position
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_final
argument_list|,
literal|0
argument_list|,
name|P_TOP
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_final
operator|.
name|lno
argument_list|,
name|P_BOTTOM
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_final
argument_list|,
name|sp
operator|->
name|t_rows
argument_list|,
name|Z_CARAT
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
default|default:
comment|/* Put the line at the top for<cr>. */
name|value
operator|=
name|KEY_VAL
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|character
argument_list|)
expr_stmt|;
if|if
condition|(
name|value
operator|!=
name|K_CR
operator|&&
name|value
operator|!=
name|K_NL
condition|)
block|{
name|v_emsg
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|kp
operator|->
name|usage
argument_list|,
name|VIM_USAGE
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
if|if
condition|(
name|vs_sm_fill
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
name|P_TOP
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * vs_crel --  *	Change the relative size of the current screen.  *  * PUBLIC: int vs_crel __P((SCR *, long));  */
end_comment

begin_function
name|int
name|vs_crel
parameter_list|(
name|sp
parameter_list|,
name|count
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|long
name|count
decl_stmt|;
block|{
name|sp
operator|->
name|t_minrows
operator|=
name|sp
operator|->
name|t_rows
operator|=
name|count
expr_stmt|;
if|if
condition|(
name|sp
operator|->
name|t_rows
operator|>
name|sp
operator|->
name|rows
operator|-
literal|1
condition|)
name|sp
operator|->
name|t_minrows
operator|=
name|sp
operator|->
name|t_rows
operator|=
name|sp
operator|->
name|rows
operator|-
literal|1
expr_stmt|;
name|TMAP
operator|=
name|HMAP
operator|+
operator|(
name|sp
operator|->
name|t_rows
operator|-
literal|1
operator|)
expr_stmt|;
name|F_SET
argument_list|(
name|sp
argument_list|,
name|SC_SCR_REDRAW
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

