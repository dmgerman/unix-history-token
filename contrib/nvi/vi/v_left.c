begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_left.c	10.7 (Berkeley) 3/6/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_comment
comment|/*  * v_left -- [count]^H, [count]h  *	Move left by columns.  *  * PUBLIC: int v_left __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_left
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|cnt
decl_stmt|;
comment|/* 	 * !!! 	 * The ^H and h commands always failed in the first column. 	 */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
literal|0
condition|)
block|{
name|v_sol
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* Find the end of the range. */
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|>
name|cnt
condition|)
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|-
name|cnt
expr_stmt|;
else|else
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
comment|/* 	 * All commands move to the end of the range.  Motion commands 	 * adjust the starting point to the character before the current 	 * one. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_cfirst -- [count]_  *	Move to the first non-blank character in a line.  *  * PUBLIC: int v_cfirst __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_cfirst
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|cnt
decl_stmt|,
name|lno
decl_stmt|;
comment|/* 	 * !!! 	 * If the _ is a motion component, it makes the command a line motion 	 * e.g. "d_" deletes the line.  It also means that the cursor doesn't 	 * move. 	 * 	 * The _ command never failed in the first column. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_LMODE
argument_list|)
expr_stmt|;
comment|/* 	 * !!! 	 * Historically a specified count makes _ move down count - 1 	 * rows, so, "3_" is the same as "2j_". 	 */
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
if|if
condition|(
name|cnt
operator|!=
literal|1
condition|)
block|{
operator|--
name|vp
operator|->
name|count
expr_stmt|;
return|return
operator|(
name|v_down
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|)
operator|)
return|;
block|}
comment|/* 	 * Move to the first non-blank. 	 * 	 * Can't just use RCM_SET_FNB, in case _ is used as the motion 	 * component of another command. 	 */
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|nonblank
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_stop
operator|.
name|lno
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
operator|.
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * !!! 	 * The _ command has to fail if the file is empty and we're doing 	 * a delete.  If deleting line 1, and 0 is the first nonblank, 	 * make the check. 	 */
if|if
condition|(
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|==
literal|1
operator|&&
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|==
literal|0
operator|&&
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'d'
argument_list|)
condition|)
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|lno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|lno
operator|==
literal|0
condition|)
block|{
name|v_sol
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
comment|/* 	 * Delete and non-motion commands move to the end of the range, 	 * yank stays at the start.  Ignore others. 	 */
name|vp
operator|->
name|m_final
operator|=
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'y'
argument_list|)
condition|?
name|vp
operator|->
name|m_start
else|:
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_first -- ^  *	Move to the first non-blank character in this line.  *  * PUBLIC: int v_first __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_first
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * !!! 	 * Yielding to none in our quest for compatibility with every 	 * historical blemish of vi, no matter how strange it might be, 	 * we permit the user to enter a count and then ignore it. 	 */
comment|/* 	 * Move to the first non-blank. 	 * 	 * Can't just use RCM_SET_FNB, in case ^ is used as the motion 	 * component of another command. 	 */
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|nonblank
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_stop
operator|.
name|lno
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
operator|.
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * !!! 	 * The ^ command succeeded if used as a command when the cursor was 	 * on the first non-blank in the line, but failed if used as a motion 	 * component in the same situation. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
name|vp
operator|->
name|m_stop
operator|.
name|cno
condition|)
block|{
name|v_sol
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * If moving right, non-motion commands move to the end of the range. 	 * Delete and yank stay at the start.  Motion commands adjust the 	 * ending point to the character before the current ending charcter. 	 * 	 * If moving left, all commands move to the end of the range.  Motion 	 * commands adjust the starting point to the character before the 	 * current starting character. 	 */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|<
name|vp
operator|->
name|m_stop
operator|.
name|cno
condition|)
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
operator|--
name|vp
operator|->
name|m_stop
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
block|}
else|else
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
else|else
block|{
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_ncol -- [count]|  *	Move to column count or the first column on this line.  If the  *	requested column is past EOL, move to EOL.  The nasty part is  *	that we have to know character column widths to make this work.  *  * PUBLIC: int v_ncol __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_ncol
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
operator|&&
name|vp
operator|->
name|count
operator|>
literal|1
condition|)
block|{
operator|--
name|vp
operator|->
name|count
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|vs_colpos
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|(
name|size_t
operator|)
name|vp
operator|->
name|count
argument_list|)
expr_stmt|;
comment|/* 		 * !!! 		 * The | command succeeded if used as a command and the cursor 		 * didn't move, but failed if used as a motion component in the 		 * same situation. 		 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
name|v_nomove
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
else|else
block|{
comment|/* 		 * !!! 		 * The | command succeeded if used as a command in column 0 		 * without a count, but failed if used as a motion component 		 * in the same situation. 		 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
literal|0
condition|)
block|{
name|v_sol
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 	 * If moving right, non-motion commands move to the end of the range. 	 * Delete and yank stay at the start.  Motion commands adjust the 	 * ending point to the character before the current ending charcter. 	 * 	 * If moving left, all commands move to the end of the range.  Motion 	 * commands adjust the starting point to the character before the 	 * current starting character. 	 */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|<
name|vp
operator|->
name|m_stop
operator|.
name|cno
condition|)
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
operator|--
name|vp
operator|->
name|m_stop
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
block|}
else|else
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
else|else
block|{
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_zero -- 0  *	Move to the first column on this line.  *  * PUBLIC: int v_zero __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_zero
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * !!! 	 * The 0 command succeeded if used as a command in the first column 	 * but failed if used as a motion component in the same situation. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
literal|0
condition|)
block|{
name|v_sol
argument_list|(
name|sp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* 	 * All commands move to the end of the range.  Motion commands 	 * adjust the starting point to the character before the current 	 * one. 	 */
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

