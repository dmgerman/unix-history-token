begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_scroll.c	10.9 (Berkeley) 4/27/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_decl_stmt
specifier|static
name|void
name|goto_adjust
name|__P
argument_list|(
operator|(
name|VICMD
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * The historic vi had a problem in that all movements were by physical  * lines, not by logical, or screen lines.  Arguments can be made that this  * is the right thing to do.  For example, single line movements, such as  * 'j' or 'k', should probably work on physical lines.  Commands like "dj",  * or "j.", where '.' is a change command, make more sense for physical lines  * than they do for logical lines.  *  * These arguments, however, don't apply to scrolling commands like ^D and  * ^F -- if the window is fairly small, using physical lines can result in  * a half-page scroll repainting the entire screen, which is not what the  * user wanted.  Second, if the line is larger than the screen, using physical  * lines can make it impossible to display parts of the line -- there aren't  * any commands that don't display the beginning of the line in historic vi,  * and if both the beginning and end of the line can't be on the screen at  * the same time, you lose.  This is even worse in the case of the H, L, and  * M commands -- for large lines, they may all refer to the same line and  * will result in no movement at all.  *  * Another issue is that page and half-page scrolling commands historically  * moved to the first non-blank character in the new line.  If the line is  * approximately the same size as the screen, this loses because the cursor  * before and after a ^D, may refer to the same location on the screen.  In  * this implementation, scrolling commands set the cursor to the first non-  * blank character if the line changes because of the scroll.  Otherwise,  * the cursor is left alone.  *  * This implementation does the scrolling (^B, ^D, ^F, ^U, ^Y, ^E), and the  * cursor positioning commands (H, L, M) commands using logical lines, not  * physical.  */
end_comment

begin_comment
comment|/*  * v_lgoto -- [count]G  *	Go to first non-blank character of the line count, the last line  *	of the file by default.  *  * PUBLIC: int v_lgoto __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_lgoto
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|nlines
decl_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|db_exist
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|count
argument_list|)
condition|)
block|{
comment|/* 			 * !!! 			 * Historically, 1G was legal in an empty file. 			 */
if|if
condition|(
name|vp
operator|->
name|count
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|nlines
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|nlines
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|v_eof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|vp
operator|->
name|count
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|nlines
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|nlines
condition|?
name|nlines
else|:
literal|1
expr_stmt|;
block|}
name|goto_adjust
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_home -- [count]H  *	Move to the first non-blank character of the logical line  *	count - 1 from the top of the screen, 0 by default.  *  * PUBLIC: int v_home __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_home
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
if|if
condition|(
name|vs_sm_position
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
operator|-
literal|1
else|:
literal|0
argument_list|,
name|P_TOP
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|goto_adjust
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_middle -- M  *	Move to the first non-blank character of the logical line  *	in the middle of the screen.  *  * PUBLIC: int v_middle __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_middle
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * Yielding to none in our quest for compatibility with every 	 * historical blemish of vi, no matter how strange it might be, 	 * we permit the user to enter a count and then ignore it. 	 */
if|if
condition|(
name|vs_sm_position
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
literal|0
argument_list|,
name|P_MIDDLE
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|goto_adjust
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_bottom -- [count]L  *	Move to the first non-blank character of the logical line  *	count - 1 from the bottom of the screen, 0 by default.  *  * PUBLIC: int v_bottom __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_bottom
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
if|if
condition|(
name|vs_sm_position
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
operator|-
literal|1
else|:
literal|0
argument_list|,
name|P_BOTTOM
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|goto_adjust
argument_list|(
name|vp
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|goto_adjust
parameter_list|(
name|vp
parameter_list|)
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* Guess that it's the end of the range. */
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
comment|/* 	 * Non-motion commands move the cursor to the end of the range, and 	 * then to the NEXT nonblank of the line.  Historic vi always moved 	 * to the first nonblank in the line; since the H, M, and L commands 	 * are logical motions in this implementation, we do the next nonblank 	 * so that it looks approximately the same to the user.  To make this 	 * happen, the VM_RCM_SETNNB flag is set in the vcmd.c command table. 	 * 	 * If it's a motion, it's more complicated.  The best possible solution 	 * is probably to display the first nonblank of the line the cursor 	 * will eventually rest on.  This is tricky, particularly given that if 	 * the associated command is a delete, we don't yet know what line that 	 * will be.  So, we clear the VM_RCM_SETNNB flag, and set the first 	 * nonblank flag (VM_RCM_SETFNB).  Note, if the lines are sufficiently 	 * long, this can cause the cursor to warp out of the screen.  It's too 	 * hard to fix. 	 * 	 * XXX 	 * The G command is always first nonblank, so it's okay to reset it. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
name|F_CLR
argument_list|(
name|vp
argument_list|,
name|VM_RCM_MASK
argument_list|)
expr_stmt|;
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_RCM_SETFNB
argument_list|)
expr_stmt|;
block|}
else|else
return|return;
comment|/* 	 * If moving backward in the file, delete and yank move to the end 	 * of the range, unless the line didn't change, in which case yank 	 * doesn't move.  If moving forward in the file, delete and yank 	 * stay at the start of the range.  Ignore others. 	 */
if|if
condition|(
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|<
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|||
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|&&
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|<
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
if|if
condition|(
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'y'
argument_list|)
operator|&&
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|lno
condition|)
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
block|}
else|else
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * v_up -- [count]^P, [count]k, [count]-  *	Move up by lines.  *  * PUBLIC: int v_up __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_up
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|lno
decl_stmt|;
name|lno
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|<=
name|lno
condition|)
block|{
name|v_sof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|-
name|lno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_cr -- [count]^M  *	In a script window, send the line to the shell.  *	In a regular window, move down by lines.  *  * PUBLIC: int v_cr __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_cr
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* If it's a colon command-line edit window, it's an ex command. */
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_COMEDIT
argument_list|)
condition|)
return|return
operator|(
name|v_ecl_exec
argument_list|(
name|sp
argument_list|)
operator|)
return|;
comment|/* If it's a script window, exec the line. */
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_SCRIPT
argument_list|)
condition|)
return|return
operator|(
name|sscr_exec
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|)
operator|)
return|;
comment|/* Otherwise, it's the same as v_down(). */
return|return
operator|(
name|v_down
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_down -- [count]^J, [count]^N, [count]j, [count]^M, [count]+  *	Move down by lines.  *  * PUBLIC: int v_down __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_down
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|lno
decl_stmt|;
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|+
operator|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
operator|)
expr_stmt|;
if|if
condition|(
operator|!
name|db_exist
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|)
condition|)
block|{
name|v_eof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|lno
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|?
name|vp
operator|->
name|m_start
else|:
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_hpageup -- [count]^U  *	Page up half screens.  *  * PUBLIC: int v_hpageup __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_hpageup
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * Half screens always succeed unless already at SOF. 	 * 	 * !!! 	 * Half screens set the scroll value, even if the command 	 * ultimately failed, in historic vi.  Probably a don't care. 	 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
name|sp
operator|->
name|defscroll
operator|=
name|vp
operator|->
name|count
expr_stmt|;
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|sp
operator|->
name|defscroll
argument_list|,
name|CNTRL_U
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_hpagedown -- [count]^D  *	Page down half screens.  *  * PUBLIC: int v_hpagedown __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_hpagedown
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * Half screens always succeed unless already at EOF. 	 * 	 * !!! 	 * Half screens set the scroll value, even if the command 	 * ultimately failed, in historic vi.  Probably a don't care. 	 */
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|)
name|sp
operator|->
name|defscroll
operator|=
name|vp
operator|->
name|count
expr_stmt|;
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|sp
operator|->
name|defscroll
argument_list|,
name|CNTRL_D
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_pagedown -- [count]^F  *	Page down full screens.  * !!!  * Historic vi did not move to the EOF if the screen couldn't move, i.e.  * if EOF was already displayed on the screen.  This implementation does  * move to EOF in that case, making ^F more like the the historic ^D.  *  * PUBLIC: int v_pagedown __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_pagedown
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|offset
decl_stmt|;
comment|/* 	 * !!! 	 * The calculation in IEEE Std 1003.2-1992 (POSIX) is: 	 * 	 *	top_line = top_line + count * (window - 2); 	 * 	 * which was historically wrong.  The correct one is: 	 * 	 *	top_line = top_line + count * window - 2; 	 * 	 * i.e. the two line "overlap" was only subtracted once.  Which 	 * makes no sense, but then again, an overlap makes no sense for 	 * any screen but the "next" one anyway.  We do it the historical 	 * way as there's no good reason to change it. 	 * 	 * If the screen has been split, use the smaller of the current 	 * window size and the window option value. 	 * 	 * It possible for this calculation to be less than 1; move at 	 * least one line. 	 */
name|offset
operator|=
operator|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
operator|)
operator|*
operator|(
name|IS_SPLIT
argument_list|(
name|sp
argument_list|)
condition|?
name|MIN
argument_list|(
name|sp
operator|->
name|t_maxrows
argument_list|,
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
argument_list|)
else|:
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|)
expr_stmt|;
name|offset
operator|=
name|offset
operator|<=
literal|2
condition|?
literal|1
else|:
name|offset
operator|-
literal|2
expr_stmt|;
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|offset
argument_list|,
name|CNTRL_F
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_pageup -- [count]^B  *	Page up full screens.  *  * !!!  * Historic vi did not move to the SOF if the screen couldn't move, i.e.  * if SOF was already displayed on the screen.  This implementation does  * move to SOF in that case, making ^B more like the the historic ^U.  *  * PUBLIC: int v_pageup __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_pageup
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|recno_t
name|offset
decl_stmt|;
comment|/* 	 * !!! 	 * The calculation in IEEE Std 1003.2-1992 (POSIX) is: 	 * 	 *	top_line = top_line - count * (window - 2); 	 * 	 * which was historically wrong.  The correct one is: 	 * 	 *	top_line = (top_line - count * window) + 2; 	 * 	 * A simpler expression is that, as with ^F, we scroll exactly: 	 * 	 *	count * window - 2 	 * 	 * lines. 	 * 	 * Bizarre.  As with ^F, an overlap makes no sense for anything 	 * but the first screen.  We do it the historical way as there's 	 * no good reason to change it. 	 * 	 * If the screen has been split, use the smaller of the current 	 * window size and the window option value. 	 * 	 * It possible for this calculation to be less than 1; move at 	 * least one line. 	 */
name|offset
operator|=
operator|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
operator|)
operator|*
operator|(
name|IS_SPLIT
argument_list|(
name|sp
argument_list|)
condition|?
name|MIN
argument_list|(
name|sp
operator|->
name|t_maxrows
argument_list|,
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
argument_list|)
else|:
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_WINDOW
argument_list|)
operator|)
expr_stmt|;
name|offset
operator|=
name|offset
operator|<=
literal|2
condition|?
literal|1
else|:
name|offset
operator|-
literal|2
expr_stmt|;
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|offset
argument_list|,
name|CNTRL_B
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_lineup -- [count]^Y  *	Page up by lines.  *  * PUBLIC: int v_lineup __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_lineup
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * The cursor moves down, staying with its original line, unless it 	 * reaches the bottom of the screen. 	 */
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|CNTRL_Y
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_linedown -- [count]^E  *	Page down by lines.  *  * PUBLIC: int v_linedown __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_linedown
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
comment|/* 	 * The cursor moves up, staying with its original line, unless it 	 * reaches the top of the screen. 	 */
if|if
condition|(
name|vs_sm_scroll
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|CNTRL_E
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

