begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_itxt.c	10.16 (Berkeley) 10/23/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_comment
comment|/*  * !!!  * Repeated input in the historic vi is mostly wrong and this isn't very  * backward compatible.  For example, if the user entered "3Aab\ncd" in  * the historic vi, the "ab" was repeated 3 times, and the "\ncd" was then  * appended to the result.  There was also a hack which I don't remember  * right now, where "3o" would open 3 lines and then let the user fill them  * in, to make screen movements on 300 baud modems more tolerable.  I don't  * think it's going to be missed.  *  * !!!  * There's a problem with the way that we do logging for change commands with  * implied motions (e.g. A, I, O, cc, etc.).  Since the main vi loop logs the  * starting cursor position before the change command "moves" the cursor, the  * cursor position to which we return on undo will be where the user entered  * the change command, not the start of the change.  Several of the following  * routines re-log the cursor to make this work correctly.  Historic vi tried  * to do the same thing, and mostly got it right.  (The only spectacular way  * it fails is if the user entered 'o' from anywhere but the last character of  * the line, the undo returned the cursor to the start of the line.  If the  * user was on the last character of the line, the cursor returned to that  * position.)  We also check for mapped keys waiting, i.e. if we're in the  * middle of a map, don't bother logging the cursor.  */
end_comment

begin_define
define|#
directive|define
name|LOG_CORRECT
value|{							\ 	if (!MAPPED_KEYS_WAITING(sp))					\ 		(void)log_cursor(sp);					\ }
end_define

begin_decl_stmt
specifier|static
name|u_int32_t
name|set_txt_std
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|VICMD
operator|*
operator|,
name|u_int32_t
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * v_iA -- [count]A  *	Append text to the end of the line.  *  * PUBLIC: int v_iA __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_iA
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|len
decl_stmt|;
if|if
condition|(
operator|!
name|db_get
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
operator|&
name|len
argument_list|)
condition|)
name|sp
operator|->
name|cno
operator|=
name|len
operator|==
literal|0
condition|?
literal|0
else|:
name|len
operator|-
literal|1
expr_stmt|;
name|LOG_CORRECT
expr_stmt|;
return|return
operator|(
name|v_ia
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_ia -- [count]a  *	   [count]A  *	Append text to the cursor position.  *  * PUBLIC: int v_ia __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_ia
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|int
name|isempty
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_APPEND
expr_stmt|;
name|sp
operator|->
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
comment|/* Move the cursor one column to the right and repaint the screen. */
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|len
operator|=
literal|0
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|len
condition|)
block|{
if|if
condition|(
name|len
operator|==
name|sp
operator|->
name|cno
operator|+
literal|1
condition|)
block|{
name|sp
operator|->
name|cno
operator|=
name|len
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
block|}
else|else
operator|++
name|sp
operator|->
name|cno
expr_stmt|;
block|}
else|else
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|NULL
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_iI -- [count]I  *	Insert text at the first nonblank.  *  * PUBLIC: int v_iI __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_iI
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|sp
operator|->
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|nonblank
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|sp
operator|->
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|LOG_CORRECT
expr_stmt|;
return|return
operator|(
name|v_ii
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_ii -- [count]i  *	   [count]I  *	Insert text at the cursor position.  *  * PUBLIC: int v_ii __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_ii
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|int
name|isempty
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_INSERT
expr_stmt|;
name|sp
operator|->
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|len
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
name|len
operator|==
literal|0
condition|)
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|NULL
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
end_function

begin_enum
enum|enum
name|which
block|{
name|o_cmd
block|,
name|O_cmd
block|}
enum|;
end_enum

begin_decl_stmt
specifier|static
name|int
name|io
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|VICMD
operator|*
operator|,
expr|enum
name|which
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * v_iO -- [count]O  *	Insert text above this line.  *  * PUBLIC: int v_iO __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_iO
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|io
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|O_cmd
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_io -- [count]o  *	Insert text after this line.  *  * PUBLIC: int v_io __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_io
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|io
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|o_cmd
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|io
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|,
name|cmd
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
name|enum
name|which
name|cmd
decl_stmt|;
block|{
name|recno_t
name|ai_line
decl_stmt|,
name|lno
decl_stmt|;
name|size_t
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|TXT_ADDNEWLINE
operator||
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_INSERT
expr_stmt|;
if|if
condition|(
name|sp
operator|->
name|lno
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|db_last
argument_list|(
name|sp
argument_list|,
operator|&
name|lno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|lno
operator|!=
literal|0
condition|)
goto|goto
name|insert
goto|;
name|p
operator|=
name|NULL
expr_stmt|;
name|len
operator|=
literal|0
expr_stmt|;
name|ai_line
operator|=
name|OOBLNO
expr_stmt|;
block|}
else|else
block|{
name|insert
label|:
name|p
operator|=
literal|""
expr_stmt|;
name|sp
operator|->
name|cno
operator|=
literal|0
expr_stmt|;
name|LOG_CORRECT
expr_stmt|;
if|if
condition|(
name|cmd
operator|==
name|O_cmd
condition|)
block|{
if|if
condition|(
name|db_insert
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
name|p
argument_list|,
literal|0
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
name|DBG_FATAL
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|ai_line
operator|=
name|sp
operator|->
name|lno
operator|+
literal|1
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|db_append
argument_list|(
name|sp
argument_list|,
literal|1
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
name|p
argument_list|,
literal|0
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
operator|++
name|sp
operator|->
name|lno
argument_list|,
name|DBG_FATAL
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|ai_line
operator|=
name|sp
operator|->
name|lno
operator|-
literal|1
expr_stmt|;
block|}
block|}
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|NULL
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|ai_line
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_change -- [buffer][count]c[count]motion  *	       [buffer][count]C  *	       [buffer][count]S  *	Change command.  *  * PUBLIC: int v_change __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_change
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|blen
decl_stmt|,
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|int
name|isempty
decl_stmt|,
name|lmode
decl_stmt|,
name|rval
decl_stmt|;
name|char
modifier|*
name|bp
decl_stmt|,
modifier|*
name|p
decl_stmt|;
comment|/* 	 * 'c' can be combined with motion commands that set the resulting 	 * cursor position, i.e. "cG".  Clear the VM_RCM flags and make the 	 * resulting cursor position stick, inserting text has its own rules 	 * for cursor positioning. 	 */
name|F_CLR
argument_list|(
name|vp
argument_list|,
name|VM_RCM_MASK
argument_list|)
expr_stmt|;
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_RCM_SET
argument_list|)
expr_stmt|;
comment|/* 	 * Find out if the file is empty, it's easier to handle it as a 	 * special case. 	 */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|==
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|&&
name|db_eget
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
name|v_ia
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|)
operator|)
return|;
block|}
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_CHANGE
expr_stmt|;
comment|/* 	 * Move the cursor to the start of the change.  Note, if autoindent 	 * is turned on, the cc command in line mode changes from the first 	 * *non-blank* character of the line, not the first character.  And, 	 * to make it just a bit more exciting, the initial space is handled 	 * as auto-indent characters. 	 */
name|lmode
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VM_LMODE
argument_list|)
condition|?
name|CUT_LINEMODE
else|:
literal|0
expr_stmt|;
if|if
condition|(
name|lmode
condition|)
block|{
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_AUTOINDENT
argument_list|)
condition|)
block|{
if|if
condition|(
name|nonblank
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|vp
operator|->
name|m_start
operator|.
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|LF_SET
argument_list|(
name|TXT_AICHARS
argument_list|)
expr_stmt|;
block|}
block|}
name|sp
operator|->
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|sp
operator|->
name|cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
name|LOG_CORRECT
expr_stmt|;
comment|/* 	 * If not in line mode and changing within a single line, copy the 	 * text and overwrite it. 	 */
if|if
condition|(
operator|!
name|lmode
operator|&&
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|==
name|vp
operator|->
name|m_stop
operator|.
name|lno
condition|)
block|{
comment|/* 		 * !!! 		 * Historic practice, c did not cut into the numeric buffers, 		 * only the unnamed one. 		 */
if|if
condition|(
name|cut
argument_list|(
name|sp
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_BUFFER
argument_list|)
condition|?
operator|&
name|vp
operator|->
name|buffer
else|:
name|NULL
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|lmode
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|len
operator|==
literal|0
condition|)
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_EMARK
operator||
name|TXT_OVERWRITE
argument_list|)
expr_stmt|;
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
comment|/* 	 * It's trickier if in line mode or changing over multiple lines.  If 	 * we're in line mode delete all of the lines and insert a replacement 	 * line which the user edits.  If there was leading whitespace in the 	 * first line being changed, we copy it and use it as the replacement. 	 * If we're not in line mode, we delete the text and start inserting. 	 * 	 * !!! 	 * Copy the text.  Historic practice, c did not cut into the numeric 	 * buffers, only the unnamed one. 	 */
if|if
condition|(
name|cut
argument_list|(
name|sp
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_BUFFER
argument_list|)
condition|?
operator|&
name|vp
operator|->
name|buffer
else|:
name|NULL
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|lmode
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* If replacing entire lines and there's leading text. */
if|if
condition|(
name|lmode
operator|&&
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
comment|/* 		 * Get a copy of the first line changed, and copy out the 		 * leading text. 		 */
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
name|DBG_FATAL
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|GET_SPACE_RET
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|cno
argument_list|)
expr_stmt|;
name|memmove
argument_list|(
name|bp
argument_list|,
name|p
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|cno
argument_list|)
expr_stmt|;
block|}
else|else
name|bp
operator|=
name|NULL
expr_stmt|;
comment|/* Delete the text. */
if|if
condition|(
name|del
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|lmode
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* If replacing entire lines, insert a replacement line. */
if|if
condition|(
name|lmode
condition|)
block|{
if|if
condition|(
name|db_insert
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
name|bp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|sp
operator|->
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|len
operator|=
name|sp
operator|->
name|cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
block|}
comment|/* Get the line we're editing. */
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|len
operator|=
literal|0
expr_stmt|;
block|}
comment|/* Check to see if we're appending to the line. */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|>=
name|len
condition|)
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
name|rval
operator|=
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|NULL
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
expr_stmt|;
if|if
condition|(
name|bp
operator|!=
name|NULL
condition|)
name|FREE_SPACE
argument_list|(
name|sp
argument_list|,
name|bp
argument_list|,
name|blen
argument_list|)
expr_stmt|;
return|return
operator|(
name|rval
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_Replace -- [count]R  *	Overwrite multiple characters.  *  * PUBLIC: int v_Replace __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_Replace
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|int
name|isempty
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_REPLACE
expr_stmt|;
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|len
operator|=
literal|0
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|len
operator|==
literal|0
condition|)
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_OVERWRITE
operator||
name|TXT_REPLACE
argument_list|)
expr_stmt|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|len
condition|?
name|len
operator|-
literal|1
else|:
literal|0
expr_stmt|;
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_subst -- [buffer][count]s  *	Substitute characters.  *  * PUBLIC: int v_subst __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_subst
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
name|size_t
name|len
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
name|int
name|isempty
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
name|flags
operator|=
name|set_txt_std
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|sp
operator|->
name|showmode
operator|=
name|SM_CHANGE
expr_stmt|;
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|len
operator|=
literal|0
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|len
operator|==
literal|0
condition|)
name|LF_SET
argument_list|(
name|TXT_APPENDEOL
argument_list|)
expr_stmt|;
name|LF_SET
argument_list|(
name|TXT_EMARK
operator||
name|TXT_OVERWRITE
argument_list|)
expr_stmt|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|+
operator|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
operator|-
literal|1
else|:
literal|0
operator|)
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|>
name|len
operator|-
literal|1
condition|)
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|len
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
operator|&&
name|cut
argument_list|(
name|sp
argument_list|,
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_BUFFER
argument_list|)
condition|?
operator|&
name|vp
operator|->
name|buffer
else|:
name|NULL
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
literal|0
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
name|v_txt
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
argument_list|,
name|p
argument_list|,
name|len
argument_list|,
literal|0
argument_list|,
name|OOBLNO
argument_list|,
literal|1
argument_list|,
name|flags
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * set_txt_std --  *	Initialize text processing flags.  */
end_comment

begin_function
specifier|static
name|u_int32_t
name|set_txt_std
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|,
name|flags
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
block|{
name|LF_SET
argument_list|(
name|TXT_CNTRLT
operator||
name|TXT_ESCAPE
operator||
name|TXT_MAPINPUT
operator||
name|TXT_RECORD
operator||
name|TXT_RESOLVE
argument_list|)
expr_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_ISDOT
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_REPLAY
argument_list|)
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_ALTWERASE
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_ALTWERASE
argument_list|)
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_AUTOINDENT
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_BEAUTIFY
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_BEAUTIFY
argument_list|)
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_SHOWMATCH
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_SHOWMATCH
argument_list|)
expr_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|sp
argument_list|,
name|SC_SCRIPT
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_CR
argument_list|)
expr_stmt|;
if|if
condition|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_TTYWERASE
argument_list|)
condition|)
name|LF_SET
argument_list|(
name|TXT_TTYWERASE
argument_list|)
expr_stmt|;
comment|/* 	 * !!! 	 * Mapped keys were sometimes unaffected by the wrapmargin option 	 * in the historic 4BSD vi.  Consider the following commands, where 	 * each is executed on an empty line, in an 80 column screen, with 	 * the wrapmargin value set to 60. 	 * 	 *	aABC DEF<ESC>.... 	 *	:map K aABC DEF ^V<ESC><CR>KKKKK 	 *	:map K 5aABC DEF ^V<ESC><CR>K 	 * 	 * The first and second commands are affected by wrapmargin.  The 	 * third is not.  (If the inserted text is itself longer than the 	 * wrapmargin value, i.e. if the "ABC DEF " string is replaced by 	 * something that's longer than 60 columns from the beginning of 	 * the line, the first two commands behave as before, but the third 	 * command gets fairly strange.)  The problem is that people wrote 	 * macros that depended on the third command NOT being affected by 	 * wrapmargin, as in this gem which centers lines: 	 * 	 *	map #c $mq81a ^V^[81^V^V|D`qld0:s/  / /g^V^M$p 	 * 	 * For compatibility reasons, we try and make it all work here.  I 	 * offer no hope that this is right, but it's probably pretty close. 	 * 	 * XXX 	 * Once I work my courage up, this is all gonna go away.  It's too 	 * evil to survive. 	 */
if|if
condition|(
operator|(
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_WRAPLEN
argument_list|)
operator|||
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_WRAPMARGIN
argument_list|)
operator|)
operator|&&
operator|(
operator|!
name|MAPPED_KEYS_WAITING
argument_list|(
name|sp
argument_list|)
operator|||
operator|!
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
operator|)
condition|)
name|LF_SET
argument_list|(
name|TXT_WRAPMARGIN
argument_list|)
expr_stmt|;
return|return
operator|(
name|flags
operator|)
return|;
block|}
end_function

end_unit

