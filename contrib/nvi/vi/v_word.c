begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_word.c	10.5 (Berkeley) 3/6/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_comment
comment|/*  * There are two types of "words".  Bigwords are easy -- groups of anything  * delimited by whitespace.  Normal words are trickier.  They are either a  * group of characters, numbers and underscores, or a group of anything but,  * delimited by whitespace.  When for a word, if you're in whitespace, it's  * easy, just remove the whitespace and go to the beginning or end of the  * word.  Otherwise, figure out if the next character is in a different group.  * If it is, go to the beginning or end of that group, otherwise, go to the  * beginning or end of the current group.  The historic version of vi didn't  * get this right, so, for example, there were cases where "4e" was not the  * same as "eeee" -- in particular, single character words, and commands that  * began in whitespace were almost always handled incorrectly.  To get it right  * you have to resolve the cursor after each search so that the look-ahead to  * figure out what type of "word" the cursor is in will be correct.  *  * Empty lines, and lines that consist of only white-space characters count  * as a single word, and the beginning and end of the file counts as an  * infinite number of words.  *  * Movements associated with commands are different than movement commands.  * For example, in "abc  def", with the cursor on the 'a', "cw" is from  * 'a' to 'c', while "w" is from 'a' to 'd'.  In general, trailing white  * space is discarded from the change movement.  Another example is that,  * in the same string, a "cw" on any white space character replaces that  * single character, and nothing else.  Ain't nothin' in here that's easy.  *  * One historic note -- in the original vi, the 'w', 'W' and 'B' commands  * would treat groups of empty lines as individual words, i.e. the command  * would move the cursor to each new empty line.  The 'e' and 'E' commands  * would treat groups of empty lines as a single word, i.e. the first use  * would move past the group of lines.  The 'b' command would just beep at  * you, or, if you did it from the start of the line as part of a motion  * command, go absolutely nuts.  If the lines contained only white-space  * characters, the 'w' and 'W' commands would just beep at you, and the 'B',  * 'b', 'E' and 'e' commands would treat the group as a single word, and  * the 'B' and 'b' commands will treat the lines as individual words.  This  * implementation treats all of these cases as a single white-space word.  */
end_comment

begin_enum
enum|enum
name|which
block|{
name|BIGWORD
block|,
name|LITTLEWORD
block|}
enum|;
end_enum

begin_decl_stmt
specifier|static
name|int
name|bword
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|VICMD
operator|*
operator|,
expr|enum
name|which
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|eword
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|VICMD
operator|*
operator|,
expr|enum
name|which
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|fword
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|VICMD
operator|*
operator|,
expr|enum
name|which
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * v_wordW -- [count]W  *	Move forward a bigword at a time.  *  * PUBLIC: int v_wordW __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_wordW
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|fword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|BIGWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_wordw -- [count]w  *	Move forward a word at a time.  *  * PUBLIC: int v_wordw __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_wordw
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|fword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|LITTLEWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * fword --  *	Move forward by words.  */
end_comment

begin_function
specifier|static
name|int
name|fword
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|,
name|type
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
name|enum
name|which
name|type
decl_stmt|;
block|{
enum|enum
block|{
name|INWORD
block|,
name|NOTWORD
block|}
name|state
enum|;
name|VCS
name|cs
decl_stmt|;
name|u_long
name|cnt
decl_stmt|;
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
name|cs
operator|.
name|cs_lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|cs
operator|.
name|cs_cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
if|if
condition|(
name|cs_init
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * If in white-space: 	 *	If the count is 1, and it's a change command, we're done. 	 *	Else, move to the first non-white-space character, which 	 *	counts as a single word move.  If it's a motion command, 	 *	don't move off the end of the line. 	 */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EMP
operator|||
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
block|{
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|cs
operator|.
name|cs_flags
operator|!=
name|CS_EMP
operator|&&
name|cnt
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'c'
argument_list|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
if|if
condition|(
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'d'
argument_list|)
operator|||
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'y'
argument_list|)
condition|)
block|{
if|if
condition|(
name|cs_fspace
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
goto|goto
name|ret
goto|;
block|}
block|}
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
operator|--
name|cnt
expr_stmt|;
block|}
comment|/* 	 * Cyclically move to the next word -- this involves skipping 	 * over word characters and then any trailing non-word characters. 	 * Note, for the 'w' command, the definition of a word keeps 	 * switching. 	 */
if|if
condition|(
name|type
operator|==
name|BIGWORD
condition|)
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* 			 * If a motion command and we're at the end of the 			 * last word, we're done.  Delete and yank eat any 			 * trailing blanks, but we don't move off the end 			 * of the line regardless. 			 */
if|if
condition|(
name|cnt
operator|==
literal|0
operator|&&
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
if|if
condition|(
operator|(
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'d'
argument_list|)
operator|||
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'y'
argument_list|)
operator|)
operator|&&
name|cs_fspace
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
block|}
else|else
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
name|state
operator|=
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|?
name|INWORD
else|:
name|NOTWORD
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
if|if
condition|(
name|state
operator|==
name|INWORD
condition|)
block|{
if|if
condition|(
operator|!
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
elseif|else
if|if
condition|(
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* See comment above. */
if|if
condition|(
name|cnt
operator|==
literal|0
operator|&&
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
if|if
condition|(
operator|(
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'d'
argument_list|)
operator|||
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'y'
argument_list|)
operator|)
operator|&&
name|cs_fspace
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
block|}
comment|/* 	 * If we didn't move, we must be at EOF. 	 * 	 * !!! 	 * That's okay for motion commands, however. 	 */
name|ret
label|:
if|if
condition|(
operator|!
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|cs
operator|.
name|cs_lno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|&&
name|cs
operator|.
name|cs_cno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
name|v_eof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* Adjust the end of the range for motion commands. */
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|cs
operator|.
name|cs_lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|cs
operator|.
name|cs_cno
expr_stmt|;
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|cs
operator|.
name|cs_flags
operator|==
literal|0
condition|)
operator|--
name|vp
operator|->
name|m_stop
operator|.
name|cno
expr_stmt|;
comment|/* 	 * Non-motion commands move to the end of the range.  Delete 	 * and yank stay at the start, ignore others. 	 */
name|vp
operator|->
name|m_final
operator|=
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|?
name|vp
operator|->
name|m_start
else|:
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_wordE -- [count]E  *	Move forward to the end of the bigword.  *  * PUBLIC: int v_wordE __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_wordE
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|eword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|BIGWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_worde -- [count]e  *	Move forward to the end of the word.  *  * PUBLIC: int v_worde __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_worde
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|eword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|LITTLEWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * eword --  *	Move forward to the end of the word.  */
end_comment

begin_function
specifier|static
name|int
name|eword
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|,
name|type
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
name|enum
name|which
name|type
decl_stmt|;
block|{
enum|enum
block|{
name|INWORD
block|,
name|NOTWORD
block|}
name|state
enum|;
name|VCS
name|cs
decl_stmt|;
name|u_long
name|cnt
decl_stmt|;
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
name|cs
operator|.
name|cs_lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|cs
operator|.
name|cs_cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
if|if
condition|(
name|cs_init
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * !!! 	 * If in whitespace, or the next character is whitespace, move past 	 * it.  (This doesn't count as a word move.)  Stay at the character 	 * past the current one, it sets word "state" for the 'e' command. 	 */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
operator|!
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
block|{
if|if
condition|(
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
operator|!
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
goto|goto
name|start
goto|;
block|}
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * Cyclically move to the next word -- this involves skipping 	 * over word characters and then any trailing non-word characters. 	 * Note, for the 'e' command, the definition of a word keeps 	 * switching. 	 */
name|start
label|:
if|if
condition|(
name|type
operator|==
name|BIGWORD
condition|)
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* 			 * When we reach the start of the word after the last 			 * word, we're done.  If we changed state, back up one 			 * to the end of the previous word. 			 */
if|if
condition|(
name|cnt
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|cs_prev
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
block|}
else|else
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
name|state
operator|=
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|?
name|INWORD
else|:
name|NOTWORD
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
if|if
condition|(
name|state
operator|==
name|INWORD
condition|)
block|{
if|if
condition|(
operator|!
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
elseif|else
if|if
condition|(
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* See comment above. */
if|if
condition|(
name|cnt
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|cs_prev
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
if|if
condition|(
name|cs_fblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_EOF
condition|)
goto|goto
name|ret
goto|;
block|}
comment|/* 	 * If we didn't move, we must be at EOF. 	 * 	 * !!! 	 * That's okay for motion commands, however. 	 */
name|ret
label|:
if|if
condition|(
operator|!
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|cs
operator|.
name|cs_lno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|&&
name|cs
operator|.
name|cs_cno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
name|v_eof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* Set the end of the range for motion commands. */
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|cs
operator|.
name|cs_lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|cs
operator|.
name|cs_cno
expr_stmt|;
comment|/* 	 * Non-motion commands move to the end of the range. 	 * Delete and yank stay at the start, ignore others. 	 */
name|vp
operator|->
name|m_final
operator|=
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|?
name|vp
operator|->
name|m_start
else|:
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_WordB -- [count]B  *	Move backward a bigword at a time.  *  * PUBLIC: int v_wordB __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_wordB
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|bword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|BIGWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_wordb -- [count]b  *	Move backward a word at a time.  *  * PUBLIC: int v_wordb __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_wordb
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
return|return
operator|(
name|bword
argument_list|(
name|sp
argument_list|,
name|vp
argument_list|,
name|LITTLEWORD
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * bword --  *	Move backward by words.  */
end_comment

begin_function
specifier|static
name|int
name|bword
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|,
name|type
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
name|enum
name|which
name|type
decl_stmt|;
block|{
enum|enum
block|{
name|INWORD
block|,
name|NOTWORD
block|}
name|state
enum|;
name|VCS
name|cs
decl_stmt|;
name|u_long
name|cnt
decl_stmt|;
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
name|cs
operator|.
name|cs_lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|cs
operator|.
name|cs_cno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
if|if
condition|(
name|cs_init
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * !!! 	 * If in whitespace, or the previous character is whitespace, move 	 * past it.  (This doesn't count as a word move.)  Stay at the 	 * character before the current one, it sets word "state" for the 	 * 'b' command. 	 */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
operator|!
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
block|{
if|if
condition|(
name|cs_prev
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
operator|!
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
goto|goto
name|start
goto|;
block|}
if|if
condition|(
name|cs_bblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
comment|/* 	 * Cyclically move to the beginning of the previous word -- this 	 * involves skipping over word characters and then any trailing 	 * non-word characters.  Note, for the 'b' command, the definition 	 * of a word keeps switching. 	 */
name|start
label|:
if|if
condition|(
name|type
operator|==
name|BIGWORD
condition|)
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_prev
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_SOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* 			 * When we reach the end of the word before the last 			 * word, we're done.  If we changed state, move forward 			 * one to the end of the next word. 			 */
if|if
condition|(
name|cnt
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs_bblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_SOF
condition|)
goto|goto
name|ret
goto|;
block|}
else|else
while|while
condition|(
name|cnt
operator|--
condition|)
block|{
name|state
operator|=
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|?
name|INWORD
else|:
name|NOTWORD
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|cs_prev
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_SOF
condition|)
goto|goto
name|ret
goto|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
if|if
condition|(
name|state
operator|==
name|INWORD
condition|)
block|{
if|if
condition|(
operator|!
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
elseif|else
if|if
condition|(
name|inword
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
break|break;
block|}
comment|/* See comment above. */
if|if
condition|(
name|cnt
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
literal|0
operator|&&
name|cs_next
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
break|break;
block|}
comment|/* Eat whitespace characters. */
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|!=
literal|0
operator|||
name|isblank
argument_list|(
name|cs
operator|.
name|cs_ch
argument_list|)
condition|)
if|if
condition|(
name|cs_bblank
argument_list|(
name|sp
argument_list|,
operator|&
name|cs
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|cs
operator|.
name|cs_flags
operator|==
name|CS_SOF
condition|)
goto|goto
name|ret
goto|;
block|}
comment|/* If we didn't move, we must be at SOF. */
name|ret
label|:
if|if
condition|(
name|cs
operator|.
name|cs_lno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|&&
name|cs
operator|.
name|cs_cno
operator|==
name|vp
operator|->
name|m_start
operator|.
name|cno
condition|)
block|{
name|v_sof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
comment|/* Set the end of the range for motion commands. */
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|cs
operator|.
name|cs_lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|cs
operator|.
name|cs_cno
expr_stmt|;
comment|/* 	 * All commands move to the end of the range.  Motion commands 	 * adjust the starting point to the character before the current 	 * one. 	 * 	 * !!! 	 * The historic vi didn't get this right -- the `yb' command yanked 	 * the right stuff and even updated the cursor value, but the cursor 	 * was not actually updated on the screen. 	 */
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

