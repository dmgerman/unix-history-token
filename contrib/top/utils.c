begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  *  Top users/processes display for Unix  *  Version 3  *  *  This program may be freely redistributed,  *  but this entire comment MUST remain intact.  *  *  Copyright (c) 1984, 1989, William LeFebvre, Rice University  *  Copyright (c) 1989, 1990, 1992, William LeFebvre, Northwestern University  */
end_comment

begin_comment
comment|/*  *  This file contains various handy utilities used by top.  */
end_comment

begin_include
include|#
directive|include
file|"top.h"
end_include

begin_include
include|#
directive|include
file|"os.h"
end_include

begin_function
name|int
name|atoiwi
parameter_list|(
name|str
parameter_list|)
name|char
modifier|*
name|str
decl_stmt|;
block|{
specifier|register
name|int
name|len
decl_stmt|;
name|len
operator|=
name|strlen
argument_list|(
name|str
argument_list|)
expr_stmt|;
if|if
condition|(
name|len
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|strncmp
argument_list|(
name|str
argument_list|,
literal|"infinity"
argument_list|,
name|len
argument_list|)
operator|==
literal|0
operator|||
name|strncmp
argument_list|(
name|str
argument_list|,
literal|"all"
argument_list|,
name|len
argument_list|)
operator|==
literal|0
operator|||
name|strncmp
argument_list|(
name|str
argument_list|,
literal|"maximum"
argument_list|,
name|len
argument_list|)
operator|==
literal|0
condition|)
block|{
return|return
operator|(
name|Infinity
operator|)
return|;
block|}
elseif|else
if|if
condition|(
name|str
index|[
literal|0
index|]
operator|==
literal|'-'
condition|)
block|{
return|return
operator|(
name|Invalid
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|atoi
argument_list|(
name|str
argument_list|)
operator|)
return|;
block|}
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *  itoa - convert integer (decimal) to ascii string for positive numbers  *  	   only (we don't bother with negative numbers since we know we  *	   don't use them).  */
end_comment

begin_comment
comment|/* 				 * How do we know that 16 will suffice? 				 * Because the biggest number that we will 				 * ever convert will be 2^32-1, which is 10 				 * digits. 				 */
end_comment

begin_function
name|char
modifier|*
name|itoa
parameter_list|(
name|val
parameter_list|)
specifier|register
name|int
name|val
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|ptr
decl_stmt|;
specifier|static
name|char
name|buffer
index|[
literal|16
index|]
decl_stmt|;
comment|/* result is built here */
comment|/* 16 is sufficient since the largest number 				   we will ever convert will be 2^32-1, 				   which is 10 digits. */
name|ptr
operator|=
name|buffer
operator|+
sizeof|sizeof
argument_list|(
name|buffer
argument_list|)
expr_stmt|;
operator|*
operator|--
name|ptr
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|val
operator|==
literal|0
condition|)
block|{
operator|*
operator|--
name|ptr
operator|=
literal|'0'
expr_stmt|;
block|}
else|else
while|while
condition|(
name|val
operator|!=
literal|0
condition|)
block|{
operator|*
operator|--
name|ptr
operator|=
operator|(
name|val
operator|%
literal|10
operator|)
operator|+
literal|'0'
expr_stmt|;
name|val
operator|/=
literal|10
expr_stmt|;
block|}
return|return
operator|(
name|ptr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *  itoa7(val) - like itoa, except the number is right justified in a 7  *	character field.  This code is a duplication of itoa instead of  *	a front end to a more general routine for efficiency.  */
end_comment

begin_function
name|char
modifier|*
name|itoa7
parameter_list|(
name|val
parameter_list|)
specifier|register
name|int
name|val
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|ptr
decl_stmt|;
specifier|static
name|char
name|buffer
index|[
literal|16
index|]
decl_stmt|;
comment|/* result is built here */
comment|/* 16 is sufficient since the largest number 				   we will ever convert will be 2^32-1, 				   which is 10 digits. */
name|ptr
operator|=
name|buffer
operator|+
sizeof|sizeof
argument_list|(
name|buffer
argument_list|)
expr_stmt|;
operator|*
operator|--
name|ptr
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|val
operator|==
literal|0
condition|)
block|{
operator|*
operator|--
name|ptr
operator|=
literal|'0'
expr_stmt|;
block|}
else|else
while|while
condition|(
name|val
operator|!=
literal|0
condition|)
block|{
operator|*
operator|--
name|ptr
operator|=
operator|(
name|val
operator|%
literal|10
operator|)
operator|+
literal|'0'
expr_stmt|;
name|val
operator|/=
literal|10
expr_stmt|;
block|}
while|while
condition|(
name|ptr
operator|>
name|buffer
operator|+
sizeof|sizeof
argument_list|(
name|buffer
argument_list|)
operator|-
literal|7
condition|)
block|{
operator|*
operator|--
name|ptr
operator|=
literal|' '
expr_stmt|;
block|}
return|return
operator|(
name|ptr
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *  digits(val) - return number of decimal digits in val.  Only works for  *	positive numbers.  If val<= 0 then digits(val) == 0.  */
end_comment

begin_function
name|int
name|digits
parameter_list|(
name|val
parameter_list|)
name|int
name|val
decl_stmt|;
block|{
specifier|register
name|int
name|cnt
init|=
literal|0
decl_stmt|;
while|while
condition|(
name|val
operator|>
literal|0
condition|)
block|{
name|cnt
operator|++
expr_stmt|;
name|val
operator|/=
literal|10
expr_stmt|;
block|}
return|return
operator|(
name|cnt
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *  strecpy(to, from) - copy string "from" into "to" and return a pointer  *	to the END of the string "to".  */
end_comment

begin_function
name|char
modifier|*
name|strecpy
parameter_list|(
name|to
parameter_list|,
name|from
parameter_list|)
specifier|register
name|char
modifier|*
name|to
decl_stmt|;
specifier|register
name|char
modifier|*
name|from
decl_stmt|;
block|{
while|while
condition|(
operator|(
operator|*
name|to
operator|++
operator|=
operator|*
name|from
operator|++
operator|)
operator|!=
literal|'\0'
condition|)
empty_stmt|;
return|return
operator|(
operator|--
name|to
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * string_index(string, array) - find string in array and return index  */
end_comment

begin_function
name|int
name|string_index
parameter_list|(
name|string
parameter_list|,
name|array
parameter_list|)
name|char
modifier|*
name|string
decl_stmt|;
name|char
modifier|*
modifier|*
name|array
decl_stmt|;
block|{
specifier|register
name|int
name|i
init|=
literal|0
decl_stmt|;
while|while
condition|(
operator|*
name|array
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|strcmp
argument_list|(
name|string
argument_list|,
operator|*
name|array
argument_list|)
operator|==
literal|0
condition|)
block|{
return|return
operator|(
name|i
operator|)
return|;
block|}
name|array
operator|++
expr_stmt|;
name|i
operator|++
expr_stmt|;
block|}
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * argparse(line, cntp) - parse arguments in string "line", separating them  *	out into an argv-like array, and setting *cntp to the number of  *	arguments encountered.  This is a simple parser that doesn't understand  *	squat about quotes.  */
end_comment

begin_function
name|char
modifier|*
modifier|*
name|argparse
parameter_list|(
name|line
parameter_list|,
name|cntp
parameter_list|)
name|char
modifier|*
name|line
decl_stmt|;
name|int
modifier|*
name|cntp
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|from
decl_stmt|;
specifier|register
name|char
modifier|*
name|to
decl_stmt|;
specifier|register
name|int
name|cnt
decl_stmt|;
specifier|register
name|int
name|ch
decl_stmt|;
name|int
name|length
decl_stmt|;
name|int
name|lastch
decl_stmt|;
specifier|register
name|char
modifier|*
modifier|*
name|argv
decl_stmt|;
name|char
modifier|*
modifier|*
name|argarray
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
comment|/* unfortunately, the only real way to do this is to go thru the        input string twice. */
comment|/* step thru the string counting the white space sections */
name|from
operator|=
name|line
expr_stmt|;
name|lastch
operator|=
name|cnt
operator|=
name|length
operator|=
literal|0
expr_stmt|;
while|while
condition|(
operator|(
name|ch
operator|=
operator|*
name|from
operator|++
operator|)
operator|!=
literal|'\0'
condition|)
block|{
name|length
operator|++
expr_stmt|;
if|if
condition|(
name|ch
operator|==
literal|' '
operator|&&
name|lastch
operator|!=
literal|' '
condition|)
block|{
name|cnt
operator|++
expr_stmt|;
block|}
name|lastch
operator|=
name|ch
expr_stmt|;
block|}
comment|/* add three to the count:  one for the initial "dummy" argument,        one for the last argument and one for NULL */
name|cnt
operator|+=
literal|3
expr_stmt|;
comment|/* allocate a char * array to hold the pointers */
name|argarray
operator|=
operator|(
name|char
operator|*
operator|*
operator|)
name|malloc
argument_list|(
name|cnt
operator|*
sizeof|sizeof
argument_list|(
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
comment|/* allocate another array to hold the strings themselves */
name|args
operator|=
operator|(
name|char
operator|*
operator|)
name|malloc
argument_list|(
name|length
operator|+
literal|2
argument_list|)
expr_stmt|;
comment|/* initialization for main loop */
name|from
operator|=
name|line
expr_stmt|;
name|to
operator|=
name|args
expr_stmt|;
name|argv
operator|=
name|argarray
expr_stmt|;
name|lastch
operator|=
literal|'\0'
expr_stmt|;
comment|/* create a dummy argument to keep getopt happy */
operator|*
name|argv
operator|++
operator|=
name|to
expr_stmt|;
operator|*
name|to
operator|++
operator|=
literal|'\0'
expr_stmt|;
name|cnt
operator|=
literal|2
expr_stmt|;
comment|/* now build argv while copying characters */
operator|*
name|argv
operator|++
operator|=
name|to
expr_stmt|;
while|while
condition|(
operator|(
name|ch
operator|=
operator|*
name|from
operator|++
operator|)
operator|!=
literal|'\0'
condition|)
block|{
if|if
condition|(
name|ch
operator|!=
literal|' '
condition|)
block|{
if|if
condition|(
name|lastch
operator|==
literal|' '
condition|)
block|{
operator|*
name|to
operator|++
operator|=
literal|'\0'
expr_stmt|;
operator|*
name|argv
operator|++
operator|=
name|to
expr_stmt|;
name|cnt
operator|++
expr_stmt|;
block|}
operator|*
name|to
operator|++
operator|=
name|ch
expr_stmt|;
block|}
name|lastch
operator|=
name|ch
expr_stmt|;
block|}
operator|*
name|to
operator|++
operator|=
literal|'\0'
expr_stmt|;
comment|/* set cntp and return the allocated array */
operator|*
name|cntp
operator|=
name|cnt
expr_stmt|;
return|return
operator|(
name|argarray
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  *  percentages(cnt, out, new, old, diffs) - calculate percentage change  *	between array "old" and "new", putting the percentages i "out".  *	"cnt" is size of each array and "diffs" is used for scratch space.  *	The array "old" is updated on each call.  *	The routine assumes modulo arithmetic.  This function is especially  *	useful on BSD mchines for calculating cpu state percentages.  */
end_comment

begin_function
name|long
name|percentages
parameter_list|(
name|cnt
parameter_list|,
name|out
parameter_list|,
name|new
parameter_list|,
name|old
parameter_list|,
name|diffs
parameter_list|)
name|int
name|cnt
decl_stmt|;
name|int
modifier|*
name|out
decl_stmt|;
specifier|register
name|long
modifier|*
name|new
decl_stmt|;
specifier|register
name|long
modifier|*
name|old
decl_stmt|;
name|long
modifier|*
name|diffs
decl_stmt|;
block|{
specifier|register
name|int
name|i
decl_stmt|;
specifier|register
name|long
name|change
decl_stmt|;
specifier|register
name|long
name|total_change
decl_stmt|;
specifier|register
name|long
modifier|*
name|dp
decl_stmt|;
name|long
name|half_total
decl_stmt|;
comment|/* initialization */
name|total_change
operator|=
literal|0
expr_stmt|;
name|dp
operator|=
name|diffs
expr_stmt|;
comment|/* calculate changes for each state and the overall change */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|cnt
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
operator|(
name|change
operator|=
operator|*
name|new
operator|-
operator|*
name|old
operator|)
operator|<
literal|0
condition|)
block|{
comment|/* this only happens when the counter wraps */
name|change
operator|=
call|(
name|int
call|)
argument_list|(
operator|(
name|unsigned
name|long
operator|)
operator|*
name|new
operator|-
operator|(
name|unsigned
name|long
operator|)
operator|*
name|old
argument_list|)
expr_stmt|;
block|}
name|total_change
operator|+=
operator|(
operator|*
name|dp
operator|++
operator|=
name|change
operator|)
expr_stmt|;
operator|*
name|old
operator|++
operator|=
operator|*
name|new
operator|++
expr_stmt|;
block|}
comment|/* avoid divide by zero potential */
if|if
condition|(
name|total_change
operator|==
literal|0
condition|)
block|{
name|total_change
operator|=
literal|1
expr_stmt|;
block|}
comment|/* calculate percentages based on overall change, rounding up */
name|half_total
operator|=
name|total_change
operator|/
literal|2l
expr_stmt|;
comment|/* Do not divide by 0. Causes Floating point exception */
if|if
condition|(
name|total_change
condition|)
block|{
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|cnt
condition|;
name|i
operator|++
control|)
block|{
operator|*
name|out
operator|++
operator|=
call|(
name|int
call|)
argument_list|(
operator|(
operator|*
name|diffs
operator|++
operator|*
literal|1000
operator|+
name|half_total
operator|)
operator|/
name|total_change
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* return the total in case the caller wants to use it */
return|return
operator|(
name|total_change
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * errmsg(errnum) - return an error message string appropriate to the  *           error number "errnum".  This is a substitute for the System V  *           function "strerror" with one important difference:  the string  *           returned by this function does NOT end in a newline!  *           N.B.:  there appears to be no reliable way to determine if  *           "strerror" exists at compile time, so I make do by providing  *           something of similar functionality.  */
end_comment

begin_comment
comment|/* externs referenced by errmsg */
end_comment

begin_function
name|char
modifier|*
name|errmsg
parameter_list|(
name|errnum
parameter_list|)
name|int
name|errnum
decl_stmt|;
block|{
if|if
condition|(
name|errnum
operator|>
literal|0
operator|&&
name|errnum
operator|<
name|sys_nerr
condition|)
block|{
return|return
operator|(
operator|(
name|char
operator|*
operator|)
name|sys_errlist
index|[
name|errnum
index|]
operator|)
return|;
block|}
return|return
operator|(
literal|"No error"
operator|)
return|;
block|}
end_function

begin_comment
comment|/* format_time(seconds) - format number of seconds into a suitable  *		display that will fit within 6 characters.  Note that this  *		routine builds its string in a static area.  If it needs  *		to be called more than once without overwriting previous data,  *		then we will need to adopt a technique similar to the  *		one used for format_k.  */
end_comment

begin_comment
comment|/* Explanation:    We want to keep the output within 6 characters.  For low values we use    the format mm:ss.  For values that exceed 999:59, we switch to a format    that displays hours and fractions:  hhh.tH.  For values that exceed    999.9, we use hhhh.t and drop the "H" designator.  For values that    exceed 9999.9, we use "???".  */
end_comment

begin_function
name|char
modifier|*
name|format_time
parameter_list|(
name|seconds
parameter_list|)
name|long
name|seconds
decl_stmt|;
block|{
specifier|register
name|int
name|value
decl_stmt|;
specifier|register
name|int
name|digit
decl_stmt|;
specifier|register
name|char
modifier|*
name|ptr
decl_stmt|;
specifier|static
name|char
name|result
index|[
literal|10
index|]
decl_stmt|;
comment|/* sanity protection */
if|if
condition|(
name|seconds
operator|<
literal|0
operator|||
name|seconds
operator|>
operator|(
literal|99999l
operator|*
literal|360l
operator|)
condition|)
block|{
name|strcpy
argument_list|(
name|result
argument_list|,
literal|"   ???"
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|seconds
operator|>=
operator|(
literal|1000l
operator|*
literal|60l
operator|)
condition|)
block|{
comment|/* alternate (slow) method displaying hours and tenths */
name|sprintf
argument_list|(
name|result
argument_list|,
literal|"%5.1fH"
argument_list|,
operator|(
name|double
operator|)
name|seconds
operator|/
call|(
name|double
call|)
argument_list|(
literal|60l
operator|*
literal|60l
argument_list|)
argument_list|)
expr_stmt|;
comment|/* It is possible that the sprintf took more than 6 characters. 	   If so, then the "H" appears as result[6].  If not, then there 	   is a \0 in result[6].  Either way, it is safe to step on. 	 */
name|result
index|[
literal|6
index|]
operator|=
literal|'\0'
expr_stmt|;
block|}
else|else
block|{
comment|/* standard method produces MMM:SS */
comment|/* we avoid printf as must as possible to make this quick */
name|sprintf
argument_list|(
name|result
argument_list|,
literal|"%3ld:%02ld"
argument_list|,
call|(
name|long
call|)
argument_list|(
name|seconds
operator|/
literal|60
argument_list|)
argument_list|,
call|(
name|long
call|)
argument_list|(
name|seconds
operator|%
literal|60
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|result
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * format_k(amt) - format a kilobyte memory value, returning a string  *		suitable for display.  Returns a pointer to a static  *		area that changes each call.  "amt" is converted to a  *		string with a trailing "K".  If "amt" is 10000 or greater,  *		then it is formatted as megabytes (rounded) with a  *		trailing "M".  */
end_comment

begin_comment
comment|/*  * Compromise time.  We need to return a string, but we don't want the  * caller to have to worry about freeing a dynamically allocated string.  * Unfortunately, we can't just return a pointer to a static area as one  * of the common uses of this function is in a large call to sprintf where  * it might get invoked several times.  Our compromise is to maintain an  * array of strings and cycle thru them with each invocation.  We make the  * array large enough to handle the above mentioned case.  The constant  * NUM_STRINGS defines the number of strings in this array:  we can tolerate  * up to NUM_STRINGS calls before we start overwriting old information.  * Keeping NUM_STRINGS a power of two will allow an intelligent optimizer  * to convert the modulo operation into something quicker.  What a hack!  */
end_comment

begin_define
define|#
directive|define
name|NUM_STRINGS
value|8
end_define

begin_function
name|char
modifier|*
name|format_k
parameter_list|(
name|amt
parameter_list|)
name|int
name|amt
decl_stmt|;
block|{
specifier|static
name|char
name|retarray
index|[
name|NUM_STRINGS
index|]
index|[
literal|16
index|]
decl_stmt|;
specifier|static
name|int
name|index
init|=
literal|0
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|char
modifier|*
name|ret
decl_stmt|;
specifier|register
name|char
name|tag
init|=
literal|'K'
decl_stmt|;
name|p
operator|=
name|ret
operator|=
name|retarray
index|[
name|index
index|]
expr_stmt|;
name|index
operator|=
operator|(
name|index
operator|+
literal|1
operator|)
operator|%
name|NUM_STRINGS
expr_stmt|;
if|if
condition|(
name|amt
operator|>=
literal|10000
condition|)
block|{
name|amt
operator|=
operator|(
name|amt
operator|+
literal|512
operator|)
operator|/
literal|1024
expr_stmt|;
name|tag
operator|=
literal|'M'
expr_stmt|;
if|if
condition|(
name|amt
operator|>=
literal|10000
condition|)
block|{
name|amt
operator|=
operator|(
name|amt
operator|+
literal|512
operator|)
operator|/
literal|1024
expr_stmt|;
name|tag
operator|=
literal|'G'
expr_stmt|;
block|}
block|}
name|p
operator|=
name|strecpy
argument_list|(
name|p
argument_list|,
name|itoa
argument_list|(
name|amt
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|p
operator|++
operator|=
name|tag
expr_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|char
modifier|*
name|format_k2
parameter_list|(
name|amt
parameter_list|)
name|int
name|amt
decl_stmt|;
block|{
specifier|static
name|char
name|retarray
index|[
name|NUM_STRINGS
index|]
index|[
literal|16
index|]
decl_stmt|;
specifier|static
name|int
name|index
init|=
literal|0
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|char
modifier|*
name|ret
decl_stmt|;
specifier|register
name|char
name|tag
init|=
literal|'K'
decl_stmt|;
name|p
operator|=
name|ret
operator|=
name|retarray
index|[
name|index
index|]
expr_stmt|;
name|index
operator|=
operator|(
name|index
operator|+
literal|1
operator|)
operator|%
name|NUM_STRINGS
expr_stmt|;
if|if
condition|(
name|amt
operator|>=
literal|100000
condition|)
block|{
name|amt
operator|=
operator|(
name|amt
operator|+
literal|512
operator|)
operator|/
literal|1024
expr_stmt|;
name|tag
operator|=
literal|'M'
expr_stmt|;
if|if
condition|(
name|amt
operator|>=
literal|100000
condition|)
block|{
name|amt
operator|=
operator|(
name|amt
operator|+
literal|512
operator|)
operator|/
literal|1024
expr_stmt|;
name|tag
operator|=
literal|'G'
expr_stmt|;
block|}
block|}
name|p
operator|=
name|strecpy
argument_list|(
name|p
argument_list|,
name|itoa
argument_list|(
name|amt
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|p
operator|++
operator|=
name|tag
expr_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

end_unit

