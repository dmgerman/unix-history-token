begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_define
define|#
directive|define
name|JEMALLOC_CHUNK_MMAP_C_
end_define

begin_include
include|#
directive|include
file|"jemalloc/internal/jemalloc_internal.h"
end_include

begin_comment
comment|/******************************************************************************/
end_comment

begin_function
specifier|static
name|void
modifier|*
name|chunk_alloc_mmap_slow
parameter_list|(
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
modifier|*
name|zero
parameter_list|,
name|bool
modifier|*
name|commit
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
name|size_t
name|alloc_size
decl_stmt|;
name|alloc_size
operator|=
name|size
operator|+
name|alignment
operator|-
name|PAGE
expr_stmt|;
comment|/* Beware size_t wrap-around. */
if|if
condition|(
name|alloc_size
operator|<
name|size
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
do|do
block|{
name|void
modifier|*
name|pages
decl_stmt|;
name|size_t
name|leadsize
decl_stmt|;
name|pages
operator|=
name|pages_map
argument_list|(
name|NULL
argument_list|,
name|alloc_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|pages
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|leadsize
operator|=
name|ALIGNMENT_CEILING
argument_list|(
operator|(
name|uintptr_t
operator|)
name|pages
argument_list|,
name|alignment
argument_list|)
operator|-
operator|(
name|uintptr_t
operator|)
name|pages
expr_stmt|;
name|ret
operator|=
name|pages_trim
argument_list|(
name|pages
argument_list|,
name|alloc_size
argument_list|,
name|leadsize
argument_list|,
name|size
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|ret
operator|==
name|NULL
condition|)
do|;
name|assert
argument_list|(
name|ret
operator|!=
name|NULL
argument_list|)
expr_stmt|;
operator|*
name|zero
operator|=
name|true
expr_stmt|;
if|if
condition|(
operator|!
operator|*
name|commit
condition|)
operator|*
name|commit
operator|=
name|pages_decommit
argument_list|(
name|ret
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|void
modifier|*
name|chunk_alloc_mmap
parameter_list|(
name|void
modifier|*
name|new_addr
parameter_list|,
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
modifier|*
name|zero
parameter_list|,
name|bool
modifier|*
name|commit
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
name|size_t
name|offset
decl_stmt|;
comment|/* 	 * Ideally, there would be a way to specify alignment to mmap() (like 	 * NetBSD has), but in the absence of such a feature, we have to work 	 * hard to efficiently create aligned mappings.  The reliable, but 	 * slow method is to create a mapping that is over-sized, then trim the 	 * excess.  However, that always results in one or two calls to 	 * pages_unmap(). 	 * 	 * Optimistically try mapping precisely the right amount before falling 	 * back to the slow method, with the expectation that the optimistic 	 * approach works most of the time. 	 */
name|assert
argument_list|(
name|alignment
operator|!=
literal|0
argument_list|)
expr_stmt|;
name|assert
argument_list|(
operator|(
name|alignment
operator|&
name|chunksize_mask
operator|)
operator|==
literal|0
argument_list|)
expr_stmt|;
name|ret
operator|=
name|pages_map
argument_list|(
name|new_addr
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|NULL
operator|||
name|ret
operator|==
name|new_addr
condition|)
return|return
operator|(
name|ret
operator|)
return|;
name|assert
argument_list|(
name|new_addr
operator|==
name|NULL
argument_list|)
expr_stmt|;
name|offset
operator|=
name|ALIGNMENT_ADDR2OFFSET
argument_list|(
name|ret
argument_list|,
name|alignment
argument_list|)
expr_stmt|;
if|if
condition|(
name|offset
operator|!=
literal|0
condition|)
block|{
name|pages_unmap
argument_list|(
name|ret
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
name|chunk_alloc_mmap_slow
argument_list|(
name|size
argument_list|,
name|alignment
argument_list|,
name|zero
argument_list|,
name|commit
argument_list|)
operator|)
return|;
block|}
name|assert
argument_list|(
name|ret
operator|!=
name|NULL
argument_list|)
expr_stmt|;
operator|*
name|zero
operator|=
name|true
expr_stmt|;
if|if
condition|(
operator|!
operator|*
name|commit
condition|)
operator|*
name|commit
operator|=
name|pages_decommit
argument_list|(
name|ret
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|bool
name|chunk_dalloc_mmap
parameter_list|(
name|void
modifier|*
name|chunk
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
if|if
condition|(
name|config_munmap
condition|)
name|pages_unmap
argument_list|(
name|chunk
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
operator|!
name|config_munmap
operator|)
return|;
block|}
end_function

end_unit

