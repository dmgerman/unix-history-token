begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_define
define|#
directive|define
name|JEMALLOC_CHUNK_MMAP_C_
end_define

begin_include
include|#
directive|include
file|"jemalloc/internal/jemalloc_internal.h"
end_include

begin_comment
comment|/******************************************************************************/
end_comment

begin_comment
comment|/* Function prototypes for non-inline static functions. */
end_comment

begin_function_decl
specifier|static
name|void
modifier|*
name|pages_map
parameter_list|(
name|void
modifier|*
name|addr
parameter_list|,
name|size_t
name|size
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|static
name|void
name|pages_unmap
parameter_list|(
name|void
modifier|*
name|addr
parameter_list|,
name|size_t
name|size
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|static
name|void
modifier|*
name|chunk_alloc_mmap_slow
parameter_list|(
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
name|unaligned
parameter_list|,
name|bool
modifier|*
name|zero
parameter_list|)
function_decl|;
end_function_decl

begin_comment
comment|/******************************************************************************/
end_comment

begin_function
specifier|static
name|void
modifier|*
name|pages_map
parameter_list|(
name|void
modifier|*
name|addr
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
comment|/* 	 * We don't use MAP_FIXED here, because it can cause the *replacement* 	 * of existing mappings, and we only want to create new mappings. 	 */
name|ret
operator|=
name|mmap
argument_list|(
name|addr
argument_list|,
name|size
argument_list|,
name|PROT_READ
operator||
name|PROT_WRITE
argument_list|,
name|MAP_PRIVATE
operator||
name|MAP_ANON
argument_list|,
operator|-
literal|1
argument_list|,
literal|0
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|ret
operator|!=
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|MAP_FAILED
condition|)
name|ret
operator|=
name|NULL
expr_stmt|;
elseif|else
if|if
condition|(
name|addr
operator|!=
name|NULL
operator|&&
name|ret
operator|!=
name|addr
condition|)
block|{
comment|/* 		 * We succeeded in mapping memory, but not in the right place. 		 */
if|if
condition|(
name|munmap
argument_list|(
name|ret
argument_list|,
name|size
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|char
name|buf
index|[
name|BUFERROR_BUF
index|]
decl_stmt|;
name|buferror
argument_list|(
name|errno
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|malloc_printf
argument_list|(
literal|"<jemalloc: Error in munmap(): %s\n"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|opt_abort
condition|)
name|abort
argument_list|()
expr_stmt|;
block|}
name|ret
operator|=
name|NULL
expr_stmt|;
block|}
name|assert
argument_list|(
name|ret
operator|==
name|NULL
operator|||
operator|(
name|addr
operator|==
name|NULL
operator|&&
name|ret
operator|!=
name|addr
operator|)
operator|||
operator|(
name|addr
operator|!=
name|NULL
operator|&&
name|ret
operator|==
name|addr
operator|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|pages_unmap
parameter_list|(
name|void
modifier|*
name|addr
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
if|if
condition|(
name|munmap
argument_list|(
name|addr
argument_list|,
name|size
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|char
name|buf
index|[
name|BUFERROR_BUF
index|]
decl_stmt|;
name|buferror
argument_list|(
name|errno
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|malloc_printf
argument_list|(
literal|"<jemalloc>: Error in munmap(): %s\n"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|opt_abort
condition|)
name|abort
argument_list|()
expr_stmt|;
block|}
block|}
end_function

begin_function
name|void
name|pages_purge
parameter_list|(
name|void
modifier|*
name|addr
parameter_list|,
name|size_t
name|length
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|JEMALLOC_PURGE_MADVISE_DONTNEED
define|#
directive|define
name|JEMALLOC_MADV_PURGE
value|MADV_DONTNEED
elif|#
directive|elif
name|defined
argument_list|(
name|JEMALLOC_PURGE_MADVISE_FREE
argument_list|)
define|#
directive|define
name|JEMALLOC_MADV_PURGE
value|MADV_FREE
else|#
directive|else
error|#
directive|error
literal|"No method defined for purging unused dirty pages."
endif|#
directive|endif
name|madvise
argument_list|(
name|addr
argument_list|,
name|length
argument_list|,
name|JEMALLOC_MADV_PURGE
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|void
modifier|*
name|chunk_alloc_mmap_slow
parameter_list|(
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
name|unaligned
parameter_list|,
name|bool
modifier|*
name|zero
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|,
modifier|*
name|pages
decl_stmt|;
name|size_t
name|alloc_size
decl_stmt|,
name|leadsize
decl_stmt|,
name|trailsize
decl_stmt|;
name|alloc_size
operator|=
name|size
operator|+
name|alignment
operator|-
name|PAGE
expr_stmt|;
comment|/* Beware size_t wrap-around. */
if|if
condition|(
name|alloc_size
operator|<
name|size
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|pages
operator|=
name|pages_map
argument_list|(
name|NULL
argument_list|,
name|alloc_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|pages
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|leadsize
operator|=
name|ALIGNMENT_CEILING
argument_list|(
operator|(
name|uintptr_t
operator|)
name|pages
argument_list|,
name|alignment
argument_list|)
operator|-
operator|(
name|uintptr_t
operator|)
name|pages
expr_stmt|;
name|assert
argument_list|(
name|alloc_size
operator|>=
name|leadsize
operator|+
name|size
argument_list|)
expr_stmt|;
name|trailsize
operator|=
name|alloc_size
operator|-
name|leadsize
operator|-
name|size
expr_stmt|;
name|ret
operator|=
operator|(
name|void
operator|*
operator|)
operator|(
operator|(
name|uintptr_t
operator|)
name|pages
operator|+
name|leadsize
operator|)
expr_stmt|;
if|if
condition|(
name|leadsize
operator|!=
literal|0
condition|)
block|{
comment|/* Note that mmap() returned an unaligned mapping. */
name|unaligned
operator|=
name|true
expr_stmt|;
name|pages_unmap
argument_list|(
name|pages
argument_list|,
name|leadsize
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|trailsize
operator|!=
literal|0
condition|)
name|pages_unmap
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|(
operator|(
name|uintptr_t
operator|)
name|ret
operator|+
name|size
operator|)
argument_list|,
name|trailsize
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|ret
operator|!=
name|NULL
argument_list|)
expr_stmt|;
operator|*
name|zero
operator|=
name|true
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|void
modifier|*
name|chunk_alloc_mmap
parameter_list|(
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
modifier|*
name|zero
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
name|size_t
name|offset
decl_stmt|;
comment|/* 	 * Ideally, there would be a way to specify alignment to mmap() (like 	 * NetBSD has), but in the absence of such a feature, we have to work 	 * hard to efficiently create aligned mappings.  The reliable, but 	 * slow method is to create a mapping that is over-sized, then trim the 	 * excess.  However, that always results in at least one call to 	 * pages_unmap(). 	 * 	 * A more optimistic approach is to try mapping precisely the right 	 * amount, then try to append another mapping if alignment is off.  In 	 * practice, this works out well as long as the application is not 	 * interleaving mappings via direct mmap() calls.  If we do run into a 	 * situation where there is an interleaved mapping and we are unable to 	 * extend an unaligned mapping, our best option is to switch to the 	 * slow method until mmap() returns another aligned mapping.  This will 	 * tend to leave a gap in the memory map that is too small to cause 	 * later problems for the optimistic method. 	 * 	 * Another possible confounding factor is address space layout 	 * randomization (ASLR), which causes mmap(2) to disregard the 	 * requested address.  As such, repeatedly trying to extend unaligned 	 * mappings could result in an infinite loop, so if extension fails, 	 * immediately fall back to the reliable method of over-allocation 	 * followed by trimming. 	 */
name|ret
operator|=
name|pages_map
argument_list|(
name|NULL
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
name|offset
operator|=
name|ALIGNMENT_ADDR2OFFSET
argument_list|(
name|ret
argument_list|,
name|alignment
argument_list|)
expr_stmt|;
if|if
condition|(
name|offset
operator|!=
literal|0
condition|)
block|{
comment|/* Try to extend chunk boundary. */
if|if
condition|(
name|pages_map
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|(
operator|(
name|uintptr_t
operator|)
name|ret
operator|+
name|size
operator|)
argument_list|,
name|chunksize
operator|-
name|offset
argument_list|)
operator|==
name|NULL
condition|)
block|{
comment|/* 			 * Extension failed.  Clean up, then fall back to the 			 * reliable-but-expensive method. 			 */
name|pages_unmap
argument_list|(
name|ret
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
name|chunk_alloc_mmap_slow
argument_list|(
name|size
argument_list|,
name|alignment
argument_list|,
name|true
argument_list|,
name|zero
argument_list|)
operator|)
return|;
block|}
else|else
block|{
comment|/* Clean up unneeded leading space. */
name|pages_unmap
argument_list|(
name|ret
argument_list|,
name|chunksize
operator|-
name|offset
argument_list|)
expr_stmt|;
name|ret
operator|=
operator|(
name|void
operator|*
operator|)
operator|(
operator|(
name|uintptr_t
operator|)
name|ret
operator|+
operator|(
name|chunksize
operator|-
name|offset
operator|)
operator|)
expr_stmt|;
block|}
block|}
name|assert
argument_list|(
name|ret
operator|!=
name|NULL
argument_list|)
expr_stmt|;
operator|*
name|zero
operator|=
name|true
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|bool
name|chunk_dealloc_mmap
parameter_list|(
name|void
modifier|*
name|chunk
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
if|if
condition|(
name|config_munmap
condition|)
name|pages_unmap
argument_list|(
name|chunk
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
operator|(
name|config_munmap
operator|==
name|false
operator|)
return|;
block|}
end_function

end_unit

