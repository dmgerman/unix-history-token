begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_define
define|#
directive|define
name|JEMALLOC_HUGE_C_
end_define

begin_include
include|#
directive|include
file|"jemalloc/internal/jemalloc_internal.h"
end_include

begin_comment
comment|/******************************************************************************/
end_comment

begin_comment
comment|/* Data. */
end_comment

begin_decl_stmt
name|uint64_t
name|huge_nmalloc
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|uint64_t
name|huge_ndalloc
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|size_t
name|huge_allocated
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|malloc_mutex_t
name|huge_mtx
decl_stmt|;
end_decl_stmt

begin_comment
comment|/******************************************************************************/
end_comment

begin_comment
comment|/* Tree of chunks that are stand-alone huge allocations. */
end_comment

begin_decl_stmt
specifier|static
name|extent_tree_t
name|huge
decl_stmt|;
end_decl_stmt

begin_function
name|void
modifier|*
name|huge_malloc
parameter_list|(
name|size_t
name|size
parameter_list|,
name|bool
name|zero
parameter_list|,
name|dss_prec_t
name|dss_prec
parameter_list|)
block|{
return|return
operator|(
name|huge_palloc
argument_list|(
name|size
argument_list|,
name|chunksize
argument_list|,
name|zero
argument_list|,
name|dss_prec
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|void
modifier|*
name|huge_palloc
parameter_list|(
name|size_t
name|size
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
name|zero
parameter_list|,
name|dss_prec_t
name|dss_prec
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
name|size_t
name|csize
decl_stmt|;
name|extent_node_t
modifier|*
name|node
decl_stmt|;
name|bool
name|is_zeroed
decl_stmt|;
comment|/* Allocate one or more contiguous chunks for this request. */
name|csize
operator|=
name|CHUNK_CEILING
argument_list|(
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|csize
operator|==
literal|0
condition|)
block|{
comment|/* size is large enough to cause size_t wrap-around. */
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* Allocate an extent node with which to track the chunk. */
name|node
operator|=
name|base_node_alloc
argument_list|()
expr_stmt|;
if|if
condition|(
name|node
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
comment|/* 	 * Copy zero into is_zeroed and pass the copy to chunk_alloc(), so that 	 * it is possible to make correct junk/zero fill decisions below. 	 */
name|is_zeroed
operator|=
name|zero
expr_stmt|;
name|ret
operator|=
name|chunk_alloc
argument_list|(
name|csize
argument_list|,
name|alignment
argument_list|,
name|false
argument_list|,
operator|&
name|is_zeroed
argument_list|,
name|dss_prec
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|NULL
condition|)
block|{
name|base_node_dealloc
argument_list|(
name|node
argument_list|)
expr_stmt|;
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* Insert node into huge. */
name|node
operator|->
name|addr
operator|=
name|ret
expr_stmt|;
name|node
operator|->
name|size
operator|=
name|csize
expr_stmt|;
name|malloc_mutex_lock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
name|extent_tree_ad_insert
argument_list|(
operator|&
name|huge
argument_list|,
name|node
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_stats
condition|)
block|{
name|stats_cactive_add
argument_list|(
name|csize
argument_list|)
expr_stmt|;
name|huge_nmalloc
operator|++
expr_stmt|;
name|huge_allocated
operator|+=
name|csize
expr_stmt|;
block|}
name|malloc_mutex_unlock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_fill
operator|&&
name|zero
operator|==
name|false
condition|)
block|{
if|if
condition|(
name|opt_junk
condition|)
name|memset
argument_list|(
name|ret
argument_list|,
literal|0xa5
argument_list|,
name|csize
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|opt_zero
operator|&&
name|is_zeroed
operator|==
name|false
condition|)
name|memset
argument_list|(
name|ret
argument_list|,
literal|0
argument_list|,
name|csize
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|bool
name|huge_ralloc_no_move
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|,
name|size_t
name|oldsize
parameter_list|,
name|size_t
name|size
parameter_list|,
name|size_t
name|extra
parameter_list|)
block|{
comment|/* 	 * Avoid moving the allocation if the size class can be left the same. 	 */
if|if
condition|(
name|oldsize
operator|>
name|arena_maxclass
operator|&&
name|CHUNK_CEILING
argument_list|(
name|oldsize
argument_list|)
operator|>=
name|CHUNK_CEILING
argument_list|(
name|size
argument_list|)
operator|&&
name|CHUNK_CEILING
argument_list|(
name|oldsize
argument_list|)
operator|<=
name|CHUNK_CEILING
argument_list|(
name|size
operator|+
name|extra
argument_list|)
condition|)
block|{
name|assert
argument_list|(
name|CHUNK_CEILING
argument_list|(
name|oldsize
argument_list|)
operator|==
name|oldsize
argument_list|)
expr_stmt|;
return|return
operator|(
name|false
operator|)
return|;
block|}
comment|/* Reallocation would require a move. */
return|return
operator|(
name|true
operator|)
return|;
block|}
end_function

begin_function
name|void
modifier|*
name|huge_ralloc
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|,
name|size_t
name|oldsize
parameter_list|,
name|size_t
name|size
parameter_list|,
name|size_t
name|extra
parameter_list|,
name|size_t
name|alignment
parameter_list|,
name|bool
name|zero
parameter_list|,
name|bool
name|try_tcache_dalloc
parameter_list|,
name|dss_prec_t
name|dss_prec
parameter_list|)
block|{
name|void
modifier|*
name|ret
decl_stmt|;
name|size_t
name|copysize
decl_stmt|;
comment|/* Try to avoid moving the allocation. */
if|if
condition|(
name|huge_ralloc_no_move
argument_list|(
name|ptr
argument_list|,
name|oldsize
argument_list|,
name|size
argument_list|,
name|extra
argument_list|)
operator|==
name|false
condition|)
return|return
operator|(
name|ptr
operator|)
return|;
comment|/* 	 * size and oldsize are different enough that we need to use a 	 * different size class.  In that case, fall back to allocating new 	 * space and copying. 	 */
if|if
condition|(
name|alignment
operator|>
name|chunksize
condition|)
name|ret
operator|=
name|huge_palloc
argument_list|(
name|size
operator|+
name|extra
argument_list|,
name|alignment
argument_list|,
name|zero
argument_list|,
name|dss_prec
argument_list|)
expr_stmt|;
else|else
name|ret
operator|=
name|huge_malloc
argument_list|(
name|size
operator|+
name|extra
argument_list|,
name|zero
argument_list|,
name|dss_prec
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|extra
operator|==
literal|0
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
comment|/* Try again, this time without extra. */
if|if
condition|(
name|alignment
operator|>
name|chunksize
condition|)
name|ret
operator|=
name|huge_palloc
argument_list|(
name|size
argument_list|,
name|alignment
argument_list|,
name|zero
argument_list|,
name|dss_prec
argument_list|)
expr_stmt|;
else|else
name|ret
operator|=
name|huge_malloc
argument_list|(
name|size
argument_list|,
name|zero
argument_list|,
name|dss_prec
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|==
name|NULL
condition|)
return|return
operator|(
name|NULL
operator|)
return|;
block|}
comment|/* 	 * Copy at most size bytes (not size+extra), since the caller has no 	 * expectation that the extra bytes will be reliably preserved. 	 */
name|copysize
operator|=
operator|(
name|size
operator|<
name|oldsize
operator|)
condition|?
name|size
else|:
name|oldsize
expr_stmt|;
ifdef|#
directive|ifdef
name|JEMALLOC_MREMAP
comment|/* 	 * Use mremap(2) if this is a huge-->huge reallocation, and neither the 	 * source nor the destination are in dss. 	 */
if|if
condition|(
name|oldsize
operator|>=
name|chunksize
operator|&&
operator|(
name|config_dss
operator|==
name|false
operator|||
operator|(
name|chunk_in_dss
argument_list|(
name|ptr
argument_list|)
operator|==
name|false
operator|&&
name|chunk_in_dss
argument_list|(
name|ret
argument_list|)
operator|==
name|false
operator|)
operator|)
condition|)
block|{
name|size_t
name|newsize
init|=
name|huge_salloc
argument_list|(
name|ret
argument_list|)
decl_stmt|;
comment|/* 		 * Remove ptr from the tree of huge allocations before 		 * performing the remap operation, in order to avoid the 		 * possibility of another thread acquiring that mapping before 		 * this one removes it from the tree. 		 */
name|huge_dalloc
argument_list|(
name|ptr
argument_list|,
name|false
argument_list|)
expr_stmt|;
if|if
condition|(
name|mremap
argument_list|(
name|ptr
argument_list|,
name|oldsize
argument_list|,
name|newsize
argument_list|,
name|MREMAP_MAYMOVE
operator||
name|MREMAP_FIXED
argument_list|,
name|ret
argument_list|)
operator|==
name|MAP_FAILED
condition|)
block|{
comment|/* 			 * Assuming no chunk management bugs in the allocator, 			 * the only documented way an error can occur here is 			 * if the application changed the map type for a 			 * portion of the old allocation.  This is firmly in 			 * undefined behavior territory, so write a diagnostic 			 * message, and optionally abort. 			 */
name|char
name|buf
index|[
name|BUFERROR_BUF
index|]
decl_stmt|;
name|buferror
argument_list|(
name|get_errno
argument_list|()
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|malloc_printf
argument_list|(
literal|"<jemalloc>: Error in mremap(): %s\n"
argument_list|,
name|buf
argument_list|)
expr_stmt|;
if|if
condition|(
name|opt_abort
condition|)
name|abort
argument_list|()
expr_stmt|;
name|memcpy
argument_list|(
name|ret
argument_list|,
name|ptr
argument_list|,
name|copysize
argument_list|)
expr_stmt|;
name|chunk_dealloc_mmap
argument_list|(
name|ptr
argument_list|,
name|oldsize
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|config_fill
operator|&&
name|zero
operator|==
name|false
operator|&&
name|opt_junk
operator|&&
name|oldsize
operator|<
name|newsize
condition|)
block|{
comment|/* 			 * mremap(2) clobbers the original mapping, so 			 * junk/zero filling is not preserved.  There is no 			 * need to zero fill here, since any trailing 			 * uninititialized memory is demand-zeroed by the 			 * kernel, but junk filling must be redone. 			 */
name|memset
argument_list|(
name|ret
operator|+
name|oldsize
argument_list|,
literal|0xa5
argument_list|,
name|newsize
operator|-
name|oldsize
argument_list|)
expr_stmt|;
block|}
block|}
else|else
endif|#
directive|endif
block|{
name|memcpy
argument_list|(
name|ret
argument_list|,
name|ptr
argument_list|,
name|copysize
argument_list|)
expr_stmt|;
name|iqalloct
argument_list|(
name|ptr
argument_list|,
name|try_tcache_dalloc
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|JEMALLOC_JET
end_ifdef

begin_undef
undef|#
directive|undef
name|huge_dalloc_junk
end_undef

begin_define
define|#
directive|define
name|huge_dalloc_junk
value|JEMALLOC_N(huge_dalloc_junk_impl)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|void
name|huge_dalloc_junk
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|,
name|size_t
name|usize
parameter_list|)
block|{
if|if
condition|(
name|config_fill
operator|&&
name|config_dss
operator|&&
name|opt_junk
condition|)
block|{
comment|/* 		 * Only bother junk filling if the chunk isn't about to be 		 * unmapped. 		 */
if|if
condition|(
name|config_munmap
operator|==
name|false
operator|||
operator|(
name|config_dss
operator|&&
name|chunk_in_dss
argument_list|(
name|ptr
argument_list|)
operator|)
condition|)
name|memset
argument_list|(
name|ptr
argument_list|,
literal|0x5a
argument_list|,
name|usize
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|JEMALLOC_JET
end_ifdef

begin_undef
undef|#
directive|undef
name|huge_dalloc_junk
end_undef

begin_define
define|#
directive|define
name|huge_dalloc_junk
value|JEMALLOC_N(huge_dalloc_junk)
end_define

begin_decl_stmt
name|huge_dalloc_junk_t
modifier|*
name|huge_dalloc_junk
init|=
name|JEMALLOC_N
argument_list|(
name|huge_dalloc_junk_impl
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_function
name|void
name|huge_dalloc
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|,
name|bool
name|unmap
parameter_list|)
block|{
name|extent_node_t
modifier|*
name|node
decl_stmt|,
name|key
decl_stmt|;
name|malloc_mutex_lock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
comment|/* Extract from tree of huge allocations. */
name|key
operator|.
name|addr
operator|=
name|ptr
expr_stmt|;
name|node
operator|=
name|extent_tree_ad_search
argument_list|(
operator|&
name|huge
argument_list|,
operator|&
name|key
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|node
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|node
operator|->
name|addr
operator|==
name|ptr
argument_list|)
expr_stmt|;
name|extent_tree_ad_remove
argument_list|(
operator|&
name|huge
argument_list|,
name|node
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_stats
condition|)
block|{
name|stats_cactive_sub
argument_list|(
name|node
operator|->
name|size
argument_list|)
expr_stmt|;
name|huge_ndalloc
operator|++
expr_stmt|;
name|huge_allocated
operator|-=
name|node
operator|->
name|size
expr_stmt|;
block|}
name|malloc_mutex_unlock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
if|if
condition|(
name|unmap
condition|)
name|huge_dalloc_junk
argument_list|(
name|node
operator|->
name|addr
argument_list|,
name|node
operator|->
name|size
argument_list|)
expr_stmt|;
name|chunk_dealloc
argument_list|(
name|node
operator|->
name|addr
argument_list|,
name|node
operator|->
name|size
argument_list|,
name|unmap
argument_list|)
expr_stmt|;
name|base_node_dealloc
argument_list|(
name|node
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|size_t
name|huge_salloc
parameter_list|(
specifier|const
name|void
modifier|*
name|ptr
parameter_list|)
block|{
name|size_t
name|ret
decl_stmt|;
name|extent_node_t
modifier|*
name|node
decl_stmt|,
name|key
decl_stmt|;
name|malloc_mutex_lock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
comment|/* Extract from tree of huge allocations. */
name|key
operator|.
name|addr
operator|=
name|__DECONST
argument_list|(
name|void
operator|*
argument_list|,
name|ptr
argument_list|)
expr_stmt|;
name|node
operator|=
name|extent_tree_ad_search
argument_list|(
operator|&
name|huge
argument_list|,
operator|&
name|key
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|node
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|ret
operator|=
name|node
operator|->
name|size
expr_stmt|;
name|malloc_mutex_unlock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|dss_prec_t
name|huge_dss_prec_get
parameter_list|(
name|arena_t
modifier|*
name|arena
parameter_list|)
block|{
return|return
operator|(
name|arena_dss_prec_get
argument_list|(
name|choose_arena
argument_list|(
name|arena
argument_list|)
argument_list|)
operator|)
return|;
block|}
end_function

begin_function
name|prof_ctx_t
modifier|*
name|huge_prof_ctx_get
parameter_list|(
specifier|const
name|void
modifier|*
name|ptr
parameter_list|)
block|{
name|prof_ctx_t
modifier|*
name|ret
decl_stmt|;
name|extent_node_t
modifier|*
name|node
decl_stmt|,
name|key
decl_stmt|;
name|malloc_mutex_lock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
comment|/* Extract from tree of huge allocations. */
name|key
operator|.
name|addr
operator|=
name|__DECONST
argument_list|(
name|void
operator|*
argument_list|,
name|ptr
argument_list|)
expr_stmt|;
name|node
operator|=
name|extent_tree_ad_search
argument_list|(
operator|&
name|huge
argument_list|,
operator|&
name|key
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|node
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|ret
operator|=
name|node
operator|->
name|prof_ctx
expr_stmt|;
name|malloc_mutex_unlock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
return|return
operator|(
name|ret
operator|)
return|;
block|}
end_function

begin_function
name|void
name|huge_prof_ctx_set
parameter_list|(
specifier|const
name|void
modifier|*
name|ptr
parameter_list|,
name|prof_ctx_t
modifier|*
name|ctx
parameter_list|)
block|{
name|extent_node_t
modifier|*
name|node
decl_stmt|,
name|key
decl_stmt|;
name|malloc_mutex_lock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
comment|/* Extract from tree of huge allocations. */
name|key
operator|.
name|addr
operator|=
name|__DECONST
argument_list|(
name|void
operator|*
argument_list|,
name|ptr
argument_list|)
expr_stmt|;
name|node
operator|=
name|extent_tree_ad_search
argument_list|(
operator|&
name|huge
argument_list|,
operator|&
name|key
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|node
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|node
operator|->
name|prof_ctx
operator|=
name|ctx
expr_stmt|;
name|malloc_mutex_unlock
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|bool
name|huge_boot
parameter_list|(
name|void
parameter_list|)
block|{
comment|/* Initialize chunks data. */
if|if
condition|(
name|malloc_mutex_init
argument_list|(
operator|&
name|huge_mtx
argument_list|)
condition|)
return|return
operator|(
name|true
operator|)
return|;
name|extent_tree_ad_new
argument_list|(
operator|&
name|huge
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_stats
condition|)
block|{
name|huge_nmalloc
operator|=
literal|0
expr_stmt|;
name|huge_ndalloc
operator|=
literal|0
expr_stmt|;
name|huge_allocated
operator|=
literal|0
expr_stmt|;
block|}
return|return
operator|(
name|false
operator|)
return|;
block|}
end_function

begin_function
name|void
name|huge_prefork
parameter_list|(
name|void
parameter_list|)
block|{
name|malloc_mutex_prefork
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|huge_postfork_parent
parameter_list|(
name|void
parameter_list|)
block|{
name|malloc_mutex_postfork_parent
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
name|huge_postfork_child
parameter_list|(
name|void
parameter_list|)
block|{
name|malloc_mutex_postfork_child
argument_list|(
operator|&
name|huge_mtx
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

