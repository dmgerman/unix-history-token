begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/**  * Copyright (c) 2016-present, Yann Collet, Facebook, Inc.  * All rights reserved.  *  * This source code is licensed under the BSD-style license found in the  * LICENSE file in the root directory of this source tree. An additional grant  * of patent rights can be found in the PATENTS file in the same directory.  */
end_comment

begin_comment
comment|/**  * This fuzz target attempts to decompress the fuzzed data with the simple  * decompression function to ensure the decompressor never crashes.  */
end_comment

begin_define
define|#
directive|define
name|ZSTD_STATIC_LINKING_ONLY
end_define

begin_include
include|#
directive|include
file|<stddef.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"fuzz_helpers.h"
end_include

begin_include
include|#
directive|include
file|"zstd.h"
end_include

begin_decl_stmt
specifier|static
name|size_t
specifier|const
name|kBufSize
init|=
name|ZSTD_BLOCKSIZE_ABSOLUTEMAX
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|ZSTD_DStream
modifier|*
name|dstream
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
modifier|*
name|buf
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|uint32_t
name|seed
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|ZSTD_outBuffer
name|makeOutBuffer
parameter_list|(
name|void
parameter_list|)
block|{
name|ZSTD_outBuffer
name|buffer
init|=
block|{
name|buf
block|,
literal|0
block|,
literal|0
block|}
decl_stmt|;
name|buffer
operator|.
name|size
operator|=
operator|(
name|FUZZ_rand
argument_list|(
operator|&
name|seed
argument_list|)
operator|%
name|kBufSize
operator|)
operator|+
literal|1
expr_stmt|;
name|FUZZ_ASSERT
argument_list|(
name|buffer
operator|.
name|size
operator|<=
name|kBufSize
argument_list|)
expr_stmt|;
return|return
name|buffer
return|;
block|}
end_function

begin_function
specifier|static
name|ZSTD_inBuffer
name|makeInBuffer
parameter_list|(
specifier|const
name|uint8_t
modifier|*
modifier|*
name|src
parameter_list|,
name|size_t
modifier|*
name|size
parameter_list|)
block|{
name|ZSTD_inBuffer
name|buffer
init|=
block|{
operator|*
name|src
block|,
literal|0
block|,
literal|0
block|}
decl_stmt|;
name|FUZZ_ASSERT
argument_list|(
operator|*
name|size
operator|>
literal|0
argument_list|)
expr_stmt|;
name|buffer
operator|.
name|size
operator|=
operator|(
name|FUZZ_rand
argument_list|(
operator|&
name|seed
argument_list|)
operator|%
operator|*
name|size
operator|)
operator|+
literal|1
expr_stmt|;
name|FUZZ_ASSERT
argument_list|(
name|buffer
operator|.
name|size
operator|<=
operator|*
name|size
argument_list|)
expr_stmt|;
operator|*
name|src
operator|+=
name|buffer
operator|.
name|size
expr_stmt|;
operator|*
name|size
operator|-=
name|buffer
operator|.
name|size
expr_stmt|;
return|return
name|buffer
return|;
block|}
end_function

begin_function
name|int
name|LLVMFuzzerTestOneInput
parameter_list|(
specifier|const
name|uint8_t
modifier|*
name|src
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
name|seed
operator|=
name|FUZZ_seed
argument_list|(
name|src
argument_list|,
name|size
argument_list|)
expr_stmt|;
comment|/* Allocate all buffers and contexts if not already allocated */
if|if
condition|(
operator|!
name|buf
condition|)
block|{
name|buf
operator|=
name|malloc
argument_list|(
name|kBufSize
argument_list|)
expr_stmt|;
name|FUZZ_ASSERT
argument_list|(
name|buf
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|dstream
condition|)
block|{
name|dstream
operator|=
name|ZSTD_createDStream
argument_list|()
expr_stmt|;
name|FUZZ_ASSERT
argument_list|(
name|dstream
argument_list|)
expr_stmt|;
name|FUZZ_ASSERT
argument_list|(
operator|!
name|ZSTD_isError
argument_list|(
name|ZSTD_initDStream
argument_list|(
name|dstream
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|FUZZ_ASSERT
argument_list|(
operator|!
name|ZSTD_isError
argument_list|(
name|ZSTD_resetDStream
argument_list|(
name|dstream
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
block|}
while|while
condition|(
name|size
operator|>
literal|0
condition|)
block|{
name|ZSTD_inBuffer
name|in
init|=
name|makeInBuffer
argument_list|(
operator|&
name|src
argument_list|,
operator|&
name|size
argument_list|)
decl_stmt|;
while|while
condition|(
name|in
operator|.
name|pos
operator|!=
name|in
operator|.
name|size
condition|)
block|{
name|ZSTD_outBuffer
name|out
init|=
name|makeOutBuffer
argument_list|()
decl_stmt|;
name|size_t
specifier|const
name|rc
init|=
name|ZSTD_decompressStream
argument_list|(
name|dstream
argument_list|,
operator|&
name|out
argument_list|,
operator|&
name|in
argument_list|)
decl_stmt|;
if|if
condition|(
name|ZSTD_isError
argument_list|(
name|rc
argument_list|)
condition|)
goto|goto
name|error
goto|;
if|if
condition|(
name|rc
operator|==
literal|0
condition|)
name|FUZZ_ASSERT
argument_list|(
operator|!
name|ZSTD_isError
argument_list|(
name|ZSTD_resetDStream
argument_list|(
name|dstream
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
name|error
label|:
ifndef|#
directive|ifndef
name|STATEFULL_FUZZING
name|ZSTD_freeDStream
argument_list|(
name|dstream
argument_list|)
expr_stmt|;
name|dstream
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
return|return
literal|0
return|;
block|}
end_function

end_unit

