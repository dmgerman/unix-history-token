begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*   * Routines for controlled evaluation of host names, user names, and so on.   * They are, in fact, wrappers around the functions that are specific for   * the sockets or TLI programming interfaces. The request_info and host_info   * structures are used for result cacheing.   *    * These routines allows us to postpone expensive operations until their   * results are really needed. Examples are hostname lookups and double   * checks, or username lookups. Information that cannot be retrieved is   * given the value "unknown" ("paranoid" in case of hostname problems).   *    * When ALWAYS_HOSTNAME is off, hostname lookup is done only when required by   * tcpd paranoid mode, by access control patterns, or by %letter expansions.   *    * When ALWAYS_RFC931 mode is off, user lookup is done only when required by   * access control patterns or %letter expansions.   *    * Author: Wietse Venema, Eindhoven University of Technology, The Netherlands.   */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#) eval.c 1.3 95/01/30 19:51:45"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* System libraries. */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_comment
comment|/* Local stuff. */
end_comment

begin_include
include|#
directive|include
file|"tcpd.h"
end_include

begin_comment
comment|/*   * When a string has the value STRING_UNKNOWN, it means: don't bother, I   * tried to look up the data but it was unavailable for some reason. When a   * host name has the value STRING_PARANOID it means there was a name/address   * conflict.   */
end_comment

begin_decl_stmt
name|char
name|unknown
index|[]
init|=
name|STRING_UNKNOWN
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|paranoid
index|[]
init|=
name|STRING_PARANOID
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* eval_user - look up user name */
end_comment

begin_function
name|char
modifier|*
name|eval_user
parameter_list|(
name|request
parameter_list|)
name|struct
name|request_info
modifier|*
name|request
decl_stmt|;
block|{
if|if
condition|(
name|request
operator|->
name|user
index|[
literal|0
index|]
operator|==
literal|0
condition|)
block|{
name|strcpy
argument_list|(
name|request
operator|->
name|user
argument_list|,
name|unknown
argument_list|)
expr_stmt|;
if|if
condition|(
name|request
operator|->
name|sink
operator|==
literal|0
operator|&&
name|request
operator|->
name|client
operator|->
name|sin
operator|&&
name|request
operator|->
name|server
operator|->
name|sin
condition|)
name|rfc931
argument_list|(
name|request
operator|->
name|client
operator|->
name|sin
argument_list|,
name|request
operator|->
name|server
operator|->
name|sin
argument_list|,
name|request
operator|->
name|user
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|request
operator|->
name|user
operator|)
return|;
block|}
end_function

begin_comment
comment|/* eval_hostaddr - look up printable address */
end_comment

begin_function
name|char
modifier|*
name|eval_hostaddr
parameter_list|(
name|host
parameter_list|)
name|struct
name|host_info
modifier|*
name|host
decl_stmt|;
block|{
if|if
condition|(
name|host
operator|->
name|addr
index|[
literal|0
index|]
operator|==
literal|0
condition|)
block|{
name|strcpy
argument_list|(
name|host
operator|->
name|addr
argument_list|,
name|unknown
argument_list|)
expr_stmt|;
if|if
condition|(
name|host
operator|->
name|request
operator|->
name|hostaddr
operator|!=
literal|0
condition|)
name|host
operator|->
name|request
operator|->
name|hostaddr
argument_list|(
name|host
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|host
operator|->
name|addr
operator|)
return|;
block|}
end_function

begin_comment
comment|/* eval_hostname - look up host name */
end_comment

begin_function
name|char
modifier|*
name|eval_hostname
parameter_list|(
name|host
parameter_list|)
name|struct
name|host_info
modifier|*
name|host
decl_stmt|;
block|{
if|if
condition|(
name|host
operator|->
name|name
index|[
literal|0
index|]
operator|==
literal|0
condition|)
block|{
name|strcpy
argument_list|(
name|host
operator|->
name|name
argument_list|,
name|unknown
argument_list|)
expr_stmt|;
if|if
condition|(
name|host
operator|->
name|request
operator|->
name|hostname
operator|!=
literal|0
condition|)
name|host
operator|->
name|request
operator|->
name|hostname
argument_list|(
name|host
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|host
operator|->
name|name
operator|)
return|;
block|}
end_function

begin_comment
comment|/* eval_hostinfo - return string with host name (preferred) or address */
end_comment

begin_function
name|char
modifier|*
name|eval_hostinfo
parameter_list|(
name|host
parameter_list|)
name|struct
name|host_info
modifier|*
name|host
decl_stmt|;
block|{
name|char
modifier|*
name|hostname
decl_stmt|;
ifndef|#
directive|ifndef
name|ALWAYS_HOSTNAME
comment|/* no implicit host lookups */
if|if
condition|(
name|host
operator|->
name|name
index|[
literal|0
index|]
operator|==
literal|0
condition|)
return|return
operator|(
name|eval_hostaddr
argument_list|(
name|host
argument_list|)
operator|)
return|;
endif|#
directive|endif
name|hostname
operator|=
name|eval_hostname
argument_list|(
name|host
argument_list|)
expr_stmt|;
if|if
condition|(
name|HOSTNAME_KNOWN
argument_list|(
name|hostname
argument_list|)
condition|)
block|{
return|return
operator|(
name|host
operator|->
name|name
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|eval_hostaddr
argument_list|(
name|host
argument_list|)
operator|)
return|;
block|}
block|}
end_function

begin_comment
comment|/* eval_client - return string with as much about the client as we know */
end_comment

begin_function
name|char
modifier|*
name|eval_client
parameter_list|(
name|request
parameter_list|)
name|struct
name|request_info
modifier|*
name|request
decl_stmt|;
block|{
specifier|static
name|char
name|both
index|[
literal|2
operator|*
name|STRING_LENGTH
index|]
decl_stmt|;
name|char
modifier|*
name|hostinfo
init|=
name|eval_hostinfo
argument_list|(
name|request
operator|->
name|client
argument_list|)
decl_stmt|;
ifndef|#
directive|ifndef
name|ALWAYS_RFC931
comment|/* no implicit user lookups */
if|if
condition|(
name|request
operator|->
name|user
index|[
literal|0
index|]
operator|==
literal|0
condition|)
return|return
operator|(
name|hostinfo
operator|)
return|;
endif|#
directive|endif
if|if
condition|(
name|STR_NE
argument_list|(
name|eval_user
argument_list|(
name|request
argument_list|)
argument_list|,
name|unknown
argument_list|)
condition|)
block|{
name|sprintf
argument_list|(
name|both
argument_list|,
literal|"%s@%s"
argument_list|,
name|request
operator|->
name|user
argument_list|,
name|hostinfo
argument_list|)
expr_stmt|;
return|return
operator|(
name|both
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|hostinfo
operator|)
return|;
block|}
block|}
end_function

begin_comment
comment|/* eval_server - return string with as much about the server as we know */
end_comment

begin_function
name|char
modifier|*
name|eval_server
parameter_list|(
name|request
parameter_list|)
name|struct
name|request_info
modifier|*
name|request
decl_stmt|;
block|{
specifier|static
name|char
name|both
index|[
literal|2
operator|*
name|STRING_LENGTH
index|]
decl_stmt|;
name|char
modifier|*
name|host
init|=
name|eval_hostinfo
argument_list|(
name|request
operator|->
name|server
argument_list|)
decl_stmt|;
name|char
modifier|*
name|daemon
init|=
name|eval_daemon
argument_list|(
name|request
argument_list|)
decl_stmt|;
if|if
condition|(
name|STR_NE
argument_list|(
name|host
argument_list|,
name|unknown
argument_list|)
condition|)
block|{
name|sprintf
argument_list|(
name|both
argument_list|,
literal|"%s@%s"
argument_list|,
name|daemon
argument_list|,
name|host
argument_list|)
expr_stmt|;
return|return
operator|(
name|both
operator|)
return|;
block|}
else|else
block|{
return|return
operator|(
name|daemon
operator|)
return|;
block|}
block|}
end_function

end_unit

