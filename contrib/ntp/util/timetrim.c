begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifdef
ifdef|#
directive|ifdef
name|sgi
end_ifdef

begin_comment
comment|/*  * timetrim.c  *   * "timetrim" allows setting and adjustment of the system clock frequency  * trim parameter on Silicon Graphics machines.  The trim value native  * units are nanoseconds per second (10**-9), so a trim value of 1 makes  * the system clock step ahead 1 nanosecond more per second than a value  * of zero.  Xntpd currently uses units of 2**-20 secs for its frequency  * offset (drift) values; to convert to a timetrim value, multiply by  * 1E9 / 2**20 (about 954).  *   * "timetrim" with no arguments just prints out the current kernel value.  * With a numeric argument, the kernel value is set to the supplied value.  * The "-i" flag causes the supplied value to be added to the kernel value.  * The "-n" option causes all input and output to be in xntpd units rather  * than timetrim native units.  *  * Note that there is a limit of +-3000000 (0.3%) on the timetrim value  * which is (silently?) enforced by the kernel.  *   */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_SYSSGI_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/syssgi.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_define
define|#
directive|define
name|abs
parameter_list|(
name|X
parameter_list|)
value|(((X)< 0) ? -(X) : (X))
end_define

begin_define
define|#
directive|define
name|USAGE
value|"usage: timetrim [-n] [[-i] value]\n"
end_define

begin_define
define|#
directive|define
name|SGITONTP
parameter_list|(
name|X
parameter_list|)
value|((double)(X) * 1048576.0/1.0e9)
end_define

begin_define
define|#
directive|define
name|NTPTOSGI
parameter_list|(
name|X
parameter_list|)
value|((long)((X) * 1.0e9/1048576.0))
end_define

begin_function
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
name|char
modifier|*
name|rem
decl_stmt|;
name|int
name|c
decl_stmt|,
name|incremental
init|=
literal|0
decl_stmt|,
name|ntpunits
init|=
literal|0
decl_stmt|;
name|long
name|timetrim
decl_stmt|;
name|double
name|value
decl_stmt|,
name|strtod
argument_list|()
decl_stmt|;
while|while
condition|(
operator|--
name|argc
operator|&&
operator|*
operator|*
operator|++
name|argv
operator|==
literal|'-'
operator|&&
name|isalpha
argument_list|(
name|argv
index|[
literal|0
index|]
index|[
literal|1
index|]
argument_list|)
condition|)
block|{
switch|switch
condition|(
name|argv
index|[
literal|0
index|]
index|[
literal|1
index|]
condition|)
block|{
case|case
literal|'i'
case|:
name|incremental
operator|++
expr_stmt|;
break|break;
case|case
literal|'n'
case|:
name|ntpunits
operator|++
expr_stmt|;
break|break;
default|default:
name|fprintf
argument_list|(
name|stderr
argument_list|,
name|USAGE
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|syssgi
argument_list|(
name|SGI_GETTIMETRIM
argument_list|,
operator|&
name|timetrim
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"syssgi"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|2
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|argc
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|ntpunits
condition|)
name|fprintf
argument_list|(
name|stdout
argument_list|,
literal|"%0.5lf\n"
argument_list|,
name|SGITONTP
argument_list|(
name|timetrim
argument_list|)
argument_list|)
expr_stmt|;
else|else
name|fprintf
argument_list|(
name|stdout
argument_list|,
literal|"%ld\n"
argument_list|,
name|timetrim
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|argc
operator|!=
literal|1
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
name|USAGE
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|value
operator|=
name|strtod
argument_list|(
name|argv
index|[
literal|0
index|]
argument_list|,
operator|&
name|rem
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|rem
operator|!=
literal|'\0'
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
name|USAGE
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|ntpunits
condition|)
name|value
operator|=
name|NTPTOSGI
argument_list|(
name|value
argument_list|)
expr_stmt|;
if|if
condition|(
name|incremental
condition|)
name|timetrim
operator|+=
name|value
expr_stmt|;
else|else
name|timetrim
operator|=
name|value
expr_stmt|;
if|if
condition|(
name|syssgi
argument_list|(
name|SGI_SETTIMETRIM
argument_list|,
name|timetrim
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"syssgi"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|2
argument_list|)
expr_stmt|;
block|}
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

