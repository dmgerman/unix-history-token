begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* ntp_prio_q.c  *  * This file contains the priority queue implementation used by the  * discrete event simulator.  *  * Written By:	Sachin Kamboj  *		University of Delaware  *		Newark, DE 19711  * Copyright (c) 2006  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<ntp_stdlib.h>
end_include

begin_include
include|#
directive|include
file|<ntp_prio_q.h>
end_include

begin_comment
comment|/* Priority Queue  * --------------  * Define a priority queue in which the relative priority of the elements  * is determined by a function 'get_order' which is supplied to the  * priority_queue  */
end_comment

begin_function
name|queue
modifier|*
name|debug_create_priority_queue
parameter_list|(
name|q_order_func
name|get_order
ifdef|#
directive|ifdef
name|_CRTDBG_MAP_ALLOC
parameter_list|,
specifier|const
name|char
modifier|*
name|sourcefile
parameter_list|,
name|int
name|line_num
endif|#
directive|endif
parameter_list|)
block|{
name|queue
modifier|*
name|my_queue
decl_stmt|;
ifndef|#
directive|ifndef
name|_CRTDBG_MAP_ALLOC
name|my_queue
operator|=
name|emalloc
argument_list|(
sizeof|sizeof
argument_list|(
name|queue
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* preserve original callsite __FILE__ and __LINE__ for leak report */
name|my_queue
operator|=
name|debug_erealloc
argument_list|(
name|NULL
argument_list|,
sizeof|sizeof
argument_list|(
name|queue
argument_list|)
argument_list|,
name|sourcefile
argument_list|,
name|line_num
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|my_queue
operator|->
name|get_order
operator|=
name|get_order
expr_stmt|;
name|my_queue
operator|->
name|front
operator|=
name|NULL
expr_stmt|;
name|my_queue
operator|->
name|no_of_elements
operator|=
literal|0
expr_stmt|;
return|return
name|my_queue
return|;
block|}
end_function

begin_comment
comment|/* Define a function to "destroy" a priority queue, freeing-up  * all the allocated resources in the process   */
end_comment

begin_function
name|void
name|destroy_queue
parameter_list|(
name|queue
modifier|*
name|my_queue
parameter_list|)
block|{
name|node
modifier|*
name|temp
init|=
name|NULL
decl_stmt|;
comment|/* Empty out the queue elements if they are not already empty */
while|while
condition|(
name|my_queue
operator|->
name|front
operator|!=
name|NULL
condition|)
block|{
name|temp
operator|=
name|my_queue
operator|->
name|front
expr_stmt|;
name|my_queue
operator|->
name|front
operator|=
name|my_queue
operator|->
name|front
operator|->
name|node_next
expr_stmt|;
name|free
argument_list|(
name|temp
argument_list|)
expr_stmt|;
block|}
comment|/* Now free the queue */
name|free
argument_list|(
name|my_queue
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* Define a function to allocate memory for one element   * of the queue. The allocated memory consists of size  * bytes plus the number of bytes needed for bookkeeping  */
end_comment

begin_function
name|void
modifier|*
name|debug_get_node
parameter_list|(
name|size_t
name|size
ifdef|#
directive|ifdef
name|_CRTDBG_MAP_ALLOC
parameter_list|,
specifier|const
name|char
modifier|*
name|sourcefile
parameter_list|,
name|int
name|line_num
endif|#
directive|endif
parameter_list|)
block|{
name|node
modifier|*
name|new_node
decl_stmt|;
ifndef|#
directive|ifndef
name|_CRTDBG_MAP_ALLOC
name|new_node
operator|=
name|emalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|new_node
argument_list|)
operator|+
name|size
argument_list|)
expr_stmt|;
else|#
directive|else
name|new_node
operator|=
name|debug_erealloc
argument_list|(
name|NULL
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|new_node
argument_list|)
operator|+
name|size
argument_list|,
name|sourcefile
argument_list|,
name|line_num
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|new_node
operator|->
name|node_next
operator|=
name|NULL
expr_stmt|;
return|return
name|new_node
operator|+
literal|1
return|;
block|}
end_function

begin_comment
comment|/* Define a function to free the allocated memory for a queue node */
end_comment

begin_function
name|void
name|free_node
parameter_list|(
name|void
modifier|*
name|my_node
parameter_list|)
block|{
name|node
modifier|*
name|old_node
init|=
name|my_node
decl_stmt|;
name|free
argument_list|(
name|old_node
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|void
modifier|*
name|next_node
parameter_list|(
name|void
modifier|*
name|pv
parameter_list|)
block|{
name|node
modifier|*
name|pn
decl_stmt|;
name|pn
operator|=
name|pv
expr_stmt|;
name|pn
operator|--
expr_stmt|;
if|if
condition|(
name|pn
operator|->
name|node_next
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
return|return
name|pn
operator|->
name|node_next
operator|+
literal|1
return|;
block|}
end_function

begin_comment
comment|/* Define a function to check if the queue is empty. */
end_comment

begin_function
name|int
name|empty
parameter_list|(
name|queue
modifier|*
name|my_queue
parameter_list|)
block|{
return|return
operator|(
operator|!
name|my_queue
operator|||
operator|!
name|my_queue
operator|->
name|front
operator|)
return|;
block|}
end_function

begin_function
name|void
modifier|*
name|queue_head
parameter_list|(
name|queue
modifier|*
name|q
parameter_list|)
block|{
if|if
condition|(
name|NULL
operator|==
name|q
operator|||
name|NULL
operator|==
name|q
operator|->
name|front
condition|)
return|return
name|NULL
return|;
return|return
name|q
operator|->
name|front
operator|+
literal|1
return|;
block|}
end_function

begin_comment
comment|/* Define a function to add an element to the priority queue.  * The element is added according to its priority -   * relative priority is given by the get_order function  */
end_comment

begin_function
name|queue
modifier|*
name|enqueue
parameter_list|(
name|queue
modifier|*
name|my_queue
parameter_list|,
name|void
modifier|*
name|my_node
parameter_list|)
block|{
name|node
modifier|*
name|new_node
init|=
operator|(
name|node
operator|*
operator|)
name|my_node
operator|-
literal|1
decl_stmt|;
name|node
modifier|*
name|i
init|=
name|NULL
decl_stmt|;
name|node
modifier|*
name|j
init|=
name|my_queue
operator|->
name|front
decl_stmt|;
while|while
condition|(
name|j
operator|!=
name|NULL
operator|&&
call|(
modifier|*
name|my_queue
operator|->
name|get_order
call|)
argument_list|(
name|new_node
operator|+
literal|1
argument_list|,
name|j
operator|+
literal|1
argument_list|)
operator|>
literal|0
condition|)
block|{
name|i
operator|=
name|j
expr_stmt|;
name|j
operator|=
name|j
operator|->
name|node_next
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|==
name|NULL
condition|)
block|{
comment|/* Insert at beginning of the queue */
name|new_node
operator|->
name|node_next
operator|=
name|my_queue
operator|->
name|front
expr_stmt|;
name|my_queue
operator|->
name|front
operator|=
name|new_node
expr_stmt|;
block|}
else|else
block|{
comment|/* Insert Elsewhere, including the end */
name|new_node
operator|->
name|node_next
operator|=
name|i
operator|->
name|node_next
expr_stmt|;
name|i
operator|->
name|node_next
operator|=
name|new_node
expr_stmt|;
block|}
operator|++
name|my_queue
operator|->
name|no_of_elements
expr_stmt|;
return|return
name|my_queue
return|;
block|}
end_function

begin_comment
comment|/* Define a function to dequeue the first element from the priority  * queue and return it  */
end_comment

begin_function
name|void
modifier|*
name|dequeue
parameter_list|(
name|queue
modifier|*
name|my_queue
parameter_list|)
block|{
name|node
modifier|*
name|my_node
init|=
name|my_queue
operator|->
name|front
decl_stmt|;
if|if
condition|(
name|my_node
operator|!=
name|NULL
condition|)
block|{
name|my_queue
operator|->
name|front
operator|=
name|my_node
operator|->
name|node_next
expr_stmt|;
operator|--
name|my_queue
operator|->
name|no_of_elements
expr_stmt|;
return|return
name|my_node
operator|+
literal|1
return|;
block|}
else|else
return|return
name|NULL
return|;
block|}
end_function

begin_comment
comment|/* Define a function that returns the number of elements in the   * priority queue  */
end_comment

begin_function
name|int
name|get_no_of_elements
parameter_list|(
name|queue
modifier|*
name|my_queue
parameter_list|)
block|{
return|return
name|my_queue
operator|->
name|no_of_elements
return|;
block|}
end_function

begin_comment
comment|/* Define a function to append a queue onto another.  * Note: there is a faster way (O(1) as opposed to O(n))  * to do this for simple (FIFO) queues, but we can't rely on  * that for priority queues. (Given the current representation)  *   * I don't anticipate this to be a problem. If it does turn  * out to be a bottleneck, I will consider replacing the   * current implementation with a binomial or fibonacci heap.  */
end_comment

begin_function
name|void
name|append_queue
parameter_list|(
name|queue
modifier|*
name|q1
parameter_list|,
name|queue
modifier|*
name|q2
parameter_list|)
block|{
while|while
condition|(
operator|!
name|empty
argument_list|(
name|q2
argument_list|)
condition|)
name|enqueue
argument_list|(
name|q1
argument_list|,
name|dequeue
argument_list|(
name|q2
argument_list|)
argument_list|)
expr_stmt|;
name|destroy_queue
argument_list|(
name|q2
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* FIFO Queue  * ----------  * Use the priority queue to create a traditional FIFO queue.  * The only extra function needed is the create_queue   */
end_comment

begin_comment
comment|/* C is not Lisp and does not allow anonymous lambda functions :-(.   * So define a get_fifo_order function here  */
end_comment

begin_function
name|int
name|get_fifo_order
parameter_list|(
specifier|const
name|void
modifier|*
name|el1
parameter_list|,
specifier|const
name|void
modifier|*
name|el2
parameter_list|)
block|{
return|return
literal|1
return|;
block|}
end_function

end_unit

