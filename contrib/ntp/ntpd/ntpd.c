begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * ntpd.c - main program for the fixed point NTP daemon  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_UNISTD_H
end_ifdef

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_STAT_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|SYS_WINNT
end_ifndef

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
end_if

begin_comment
comment|/*wjm*/
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* VMS */
end_comment

begin_include
include|#
directive|include
file|<sys/signal.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_IOCTL_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_SYS_IOCTL_H */
end_comment

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_RESOURCE_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_SYS_RESOURCE_H */
end_comment

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_include
include|#
directive|include
file|<process.h>
end_include

begin_include
include|#
directive|include
file|<io.h>
end_include

begin_include
include|#
directive|include
file|"../libntp/log.h"
end_include

begin_include
include|#
directive|include
file|<crtdbg.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_RTPRIO
argument_list|)
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_RESOURCE_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_LOCK_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/rtprio.h>
end_include

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_PLOCK
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_LOCK_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SCHED_SETSCHEDULER
argument_list|)
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SCHED_H
end_ifdef

begin_include
include|#
directive|include
file|<sched.h>
end_include

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_SCHED_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/sched.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SYS_MMAN_H
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sys/mman.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_TERMIOS_H
end_ifdef

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_DOMAINOS
end_ifdef

begin_include
include|#
directive|include
file|<apollo/base.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_DOMAINOS */
end_comment

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|"recvbuff.h"
end_include

begin_if
if|#
directive|if
literal|0
end_if

begin_comment
comment|/* HMS: I don't think we need this. 961223 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|LOCK_PROCESS
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_SOLARIS
end_ifdef

begin_include
include|#
directive|include
file|<sys/mman.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|_AIX
end_ifdef

begin_include
include|#
directive|include
file|<ulimit.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _AIX */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SCO5_CLOCK
end_ifdef

begin_include
include|#
directive|include
file|<sys/ci/ciioctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Signals we catch for debugging.	If not debugging we ignore them.  */
end_comment

begin_define
define|#
directive|define
name|MOREDEBUGSIG
value|SIGUSR1
end_define

begin_define
define|#
directive|define
name|LESSDEBUGSIG
value|SIGUSR2
end_define

begin_comment
comment|/*  * Signals which terminate us gracefully.  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|SYS_WINNT
end_ifndef

begin_define
define|#
directive|define
name|SIGDIE1
value|SIGHUP
end_define

begin_define
define|#
directive|define
name|SIGDIE3
value|SIGQUIT
end_define

begin_define
define|#
directive|define
name|SIGDIE2
value|SIGINT
end_define

begin_define
define|#
directive|define
name|SIGDIE4
value|SIGTERM
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_if
if|#
directive|if
name|defined
name|SYS_WINNT
end_if

begin_comment
comment|/* handles for various threads, process, and objects */
end_comment

begin_decl_stmt
name|HANDLE
name|ResolverThreadHandle
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* variables used to inform the Service Control Manager of our current state */
end_comment

begin_decl_stmt
name|SERVICE_STATUS
name|ssStatus
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|SERVICE_STATUS_HANDLE
name|sshStatusHandle
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|HANDLE
name|WaitHandles
index|[
literal|2
index|]
init|=
block|{
name|NULL
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|szMsgPath
index|[
literal|255
index|]
decl_stmt|;
end_decl_stmt

begin_function_decl
specifier|static
name|BOOL
name|WINAPI
name|OnConsoleEvent
parameter_list|(
name|DWORD
name|dwCtrlType
parameter_list|)
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_comment
comment|/*  * Scheduling priority we run at  */
end_comment

begin_define
define|#
directive|define
name|NTPD_PRIO
value|(-12)
end_define

begin_comment
comment|/*  * Debugging flag  */
end_comment

begin_decl_stmt
specifier|volatile
name|int
name|debug
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * No-fork flag.  If set, we do not become a background daemon.  */
end_comment

begin_decl_stmt
name|int
name|nofork
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Initializing flag.  All async routines watch this and only do their  * thing when it is clear.  */
end_comment

begin_decl_stmt
name|int
name|initializing
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Version declaration  */
end_comment

begin_decl_stmt
specifier|extern
specifier|const
name|char
modifier|*
name|Version
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|was_alarmed
decl_stmt|;
end_decl_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|DECL_SYSCALL
end_ifdef

begin_comment
comment|/*  * We put this here, since the argument profile is syscall-specific  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|syscall
name|P
argument_list|(
operator|(
name|int
operator|,
operator|...
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DECL_SYSCALL */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SIGDIE2
end_ifdef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|finish
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SIGDIE2 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|moredebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|lessdebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* not DEBUG */
end_comment

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|no_debug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not DEBUG */
end_comment

begin_decl_stmt
name|int
name|ntpdmain
name|P
argument_list|(
operator|(
name|int
operator|,
name|char
operator|*
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|set_process_priority
name|P
argument_list|(
operator|(
name|void
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|NO_MAIN_ALLOWED
end_ifdef

begin_expr_stmt
name|CALL
argument_list|(
name|ntpd
argument_list|,
literal|"ntpd"
argument_list|,
name|ntpdmain
argument_list|)
expr_stmt|;
end_expr_stmt

begin_else
else|#
directive|else
end_else

begin_function
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
return|return
name|ntpdmain
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|_AIX
end_ifdef

begin_comment
comment|/*  * OK. AIX is different than solaris in how it implements plock().  * If you do NOT adjust the stack limit, you will get the MAXIMUM  * stack size allocated and PINNED with you program. To check the   * value, use ulimit -a.   *  * To fix this, we create an automatic variable and set our stack limit   * to that PLUS 32KB of extra space (we need some headroom).  *  * This subroutine gets the stack address.  *  * Grover Davidson and Matt Ladendorf  *  */
end_comment

begin_function
specifier|static
name|char
modifier|*
name|get_aix_stack
parameter_list|(
name|void
parameter_list|)
block|{
name|char
name|ch
decl_stmt|;
return|return
operator|(
operator|&
name|ch
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Signal handler for SIGDANGER.  */
end_comment

begin_function
specifier|static
name|void
name|catch_danger
parameter_list|(
name|int
name|signo
parameter_list|)
block|{
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"ntpd: setpgid(): %m"
argument_list|)
expr_stmt|;
comment|/* Make the system believe we'll free something, but don't do it! */
return|return;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _AIX */
end_comment

begin_comment
comment|/*  * Set the process priority  */
end_comment

begin_function
specifier|static
name|void
name|set_process_priority
parameter_list|(
name|void
parameter_list|)
block|{
name|int
name|done
init|=
literal|0
decl_stmt|;
ifdef|#
directive|ifdef
name|SYS_WINNT
name|DWORD
name|SingleCPUMask
init|=
literal|0
decl_stmt|;
name|DWORD
name|ProcessAffinityMask
decl_stmt|,
name|SystemAffinityMask
decl_stmt|;
if|if
condition|(
operator|!
name|GetProcessAffinityMask
argument_list|(
name|GetCurrentProcess
argument_list|()
argument_list|,
operator|&
name|ProcessAffinityMask
argument_list|,
operator|&
name|SystemAffinityMask
argument_list|)
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"GetProcessAffinityMask: %m"
argument_list|)
expr_stmt|;
else|else
block|{
name|SingleCPUMask
operator|=
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"System AffinityMask = %x"
argument_list|,
name|SystemAffinityMask
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
while|while
condition|(
name|SingleCPUMask
operator|&&
operator|!
operator|(
name|SingleCPUMask
operator|&
name|SystemAffinityMask
operator|)
condition|)
block|{
name|SingleCPUMask
operator|=
name|SingleCPUMask
operator|<<
literal|1
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|SingleCPUMask
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Can't set Processor Affinity Mask"
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
operator|!
name|SetProcessAffinityMask
argument_list|(
name|GetCurrentProcess
argument_list|()
argument_list|,
name|SingleCPUMask
argument_list|)
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetProcessAffinityMask: %m"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
else|else
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"ProcessorAffinity Mask: %x"
argument_list|,
name|SingleCPUMask
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|!
name|SetPriorityClass
argument_list|(
name|GetCurrentProcess
argument_list|()
argument_list|,
operator|(
name|DWORD
operator|)
name|REALTIME_PRIORITY_CLASS
argument_list|)
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetPriorityClass: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SCHED_SETSCHEDULER
argument_list|)
if|if
condition|(
operator|!
name|done
condition|)
block|{
specifier|extern
name|int
name|config_priority_override
decl_stmt|,
name|config_priority
decl_stmt|;
name|int
name|pmax
decl_stmt|,
name|pmin
decl_stmt|;
name|struct
name|sched_param
name|sched
decl_stmt|;
name|pmax
operator|=
name|sched_get_priority_max
argument_list|(
name|SCHED_FIFO
argument_list|)
expr_stmt|;
name|sched
operator|.
name|sched_priority
operator|=
name|pmax
expr_stmt|;
if|if
condition|(
name|config_priority_override
condition|)
block|{
name|pmin
operator|=
name|sched_get_priority_min
argument_list|(
name|SCHED_FIFO
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_priority
operator|>
name|pmax
condition|)
name|sched
operator|.
name|sched_priority
operator|=
name|pmax
expr_stmt|;
elseif|else
if|if
condition|(
name|config_priority
operator|<
name|pmin
condition|)
name|sched
operator|.
name|sched_priority
operator|=
name|pmin
expr_stmt|;
else|else
name|sched
operator|.
name|sched_priority
operator|=
name|config_priority
expr_stmt|;
block|}
if|if
condition|(
name|sched_setscheduler
argument_list|(
literal|0
argument_list|,
name|SCHED_FIFO
argument_list|,
operator|&
name|sched
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"sched_setscheduler(): %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_SCHED_SETSCHEDULER */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_RTPRIO
argument_list|)
ifdef|#
directive|ifdef
name|RTP_SET
if|if
condition|(
operator|!
name|done
condition|)
block|{
name|struct
name|rtprio
name|srtp
decl_stmt|;
name|srtp
operator|.
name|type
operator|=
name|RTP_PRIO_REALTIME
expr_stmt|;
comment|/* was: RTP_PRIO_NORMAL */
name|srtp
operator|.
name|prio
operator|=
literal|0
expr_stmt|;
comment|/* 0 (hi) -> RTP_PRIO_MAX (31,lo) */
if|if
condition|(
name|rtprio
argument_list|(
name|RTP_SET
argument_list|,
name|getpid
argument_list|()
argument_list|,
operator|&
name|srtp
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"rtprio() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
block|}
else|#
directive|else
comment|/* not RTP_SET */
if|if
condition|(
operator|!
name|done
condition|)
block|{
if|if
condition|(
name|rtprio
argument_list|(
literal|0
argument_list|,
literal|120
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"rtprio() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* not RTP_SET */
endif|#
directive|endif
comment|/* HAVE_RTPRIO */
if|#
directive|if
name|defined
argument_list|(
name|NTPD_PRIO
argument_list|)
operator|&&
name|NTPD_PRIO
operator|!=
literal|0
ifdef|#
directive|ifdef
name|HAVE_ATT_NICE
if|if
condition|(
operator|!
name|done
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|-
literal|1
operator|==
name|nice
argument_list|(
name|NTPD_PRIO
argument_list|)
operator|&&
name|errno
operator|!=
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"nice() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_ATT_NICE */
ifdef|#
directive|ifdef
name|HAVE_BSD_NICE
if|if
condition|(
operator|!
name|done
condition|)
block|{
if|if
condition|(
operator|-
literal|1
operator|==
name|setpriority
argument_list|(
name|PRIO_PROCESS
argument_list|,
literal|0
argument_list|,
name|NTPD_PRIO
argument_list|)
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"setpriority() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_BSD_NICE */
endif|#
directive|endif
comment|/* NTPD_PRIO&& NTPD_PRIO != 0 */
if|if
condition|(
operator|!
name|done
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"set_process_priority: No way found to improve our priority"
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Main program.  Initialize us, disconnect us from the tty if necessary,  * and loop waiting for I/O and/or timer expiries.  */
end_comment

begin_function
name|int
name|ntpdmain
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
name|l_fp
name|now
decl_stmt|;
name|char
modifier|*
name|cp
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuflist
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuf
decl_stmt|;
ifdef|#
directive|ifdef
name|_AIX
comment|/* HMS: ifdef SIGDANGER? */
name|struct
name|sigaction
name|sa
decl_stmt|;
endif|#
directive|endif
name|initializing
operator|=
literal|1
expr_stmt|;
comment|/* mark that we are initializing */
name|debug
operator|=
literal|0
expr_stmt|;
comment|/* no debugging by default */
name|nofork
operator|=
literal|0
expr_stmt|;
comment|/* will fork by default */
ifdef|#
directive|ifdef
name|HAVE_UMASK
block|{
name|unsigned
name|int
name|uv
decl_stmt|;
name|uv
operator|=
name|umask
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|uv
condition|)
operator|(
name|void
operator|)
name|umask
argument_list|(
name|uv
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|umask
argument_list|(
literal|022
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|HAVE_GETUID
block|{
name|uid_t
name|uid
decl_stmt|;
name|uid
operator|=
name|getuid
argument_list|()
expr_stmt|;
if|if
condition|(
name|uid
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: must be run as root, not uid %ld"
argument_list|,
operator|(
name|long
operator|)
name|uid
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|SYS_WINNT
comment|/* Set the Event-ID message-file name. */
if|if
condition|(
operator|!
name|GetModuleFileName
argument_list|(
name|NULL
argument_list|,
name|szMsgPath
argument_list|,
sizeof|sizeof
argument_list|(
name|szMsgPath
argument_list|)
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"GetModuleFileName(PGM_EXE_FILE) failed: %m\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|addSourceToRegistry
argument_list|(
literal|"NTP"
argument_list|,
name|szMsgPath
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|get_systime
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
name|SRANDOM
argument_list|(
call|(
name|int
call|)
argument_list|(
name|now
operator|.
name|l_i
operator|*
name|now
operator|.
name|l_uf
argument_list|)
argument_list|)
expr_stmt|;
name|getstartup
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
comment|/* startup configuration, may set debug */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifndef|#
directive|ifndef
name|NODETACH
comment|/* 	 * Detach us from the terminal.  May need an #ifndef GIZMO. 	 */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
operator|!
name|debug
operator|&&
operator|!
name|nofork
condition|)
else|#
directive|else
comment|/* DEBUG */
if|if
condition|(
operator|!
name|nofork
condition|)
endif|#
directive|endif
comment|/* DEBUG */
block|{
ifndef|#
directive|ifndef
name|SYS_WINNT
ifdef|#
directive|ifdef
name|HAVE_DAEMON
name|daemon
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not HAVE_DAEMON */
if|if
condition|(
name|fork
argument_list|()
condition|)
comment|/* HMS: What about a -1? */
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|{
if|#
directive|if
operator|!
name|defined
argument_list|(
name|F_CLOSEM
argument_list|)
name|u_long
name|s
decl_stmt|;
name|int
name|max_fd
decl_stmt|;
endif|#
directive|endif
comment|/* not F_CLOSEM */
comment|/* 			 * From 'Writing Reliable AIX Daemons,' SG24-4946-00, 			 * by Eric Agar (saves us from doing 32767 system 			 * calls) 			 */
if|#
directive|if
name|defined
argument_list|(
name|F_CLOSEM
argument_list|)
if|if
condition|(
name|fcntl
argument_list|(
literal|0
argument_list|,
name|F_CLOSEM
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: failed to close open files(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not F_CLOSEM */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SYSCONF
argument_list|)
operator|&&
name|defined
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
name|max_fd
operator|=
name|sysconf
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* HAVE_SYSCONF&& _SC_OPEN_MAX */
name|max_fd
operator|=
name|getdtablesize
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SYSCONF&& _SC_OPEN_MAX */
for|for
control|(
name|s
operator|=
literal|0
init|;
name|s
operator|<
name|max_fd
condition|;
name|s
operator|++
control|)
operator|(
name|void
operator|)
name|close
argument_list|(
operator|(
name|int
operator|)
name|s
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* not F_CLOSEM */
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|2
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SYS_DOMAINOS
block|{
name|uid_$t
name|puid
decl_stmt|;
name|status_$t
name|st
decl_stmt|;
name|proc2_$who_am_i
argument_list|(
operator|&
name|puid
argument_list|)
expr_stmt|;
name|proc2_$make_server
argument_list|(
operator|&
name|puid
argument_list|,
operator|&
name|st
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SYS_DOMAINOS */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SETPGID
argument_list|)
operator|||
name|defined
argument_list|(
name|HAVE_SETSID
argument_list|)
ifdef|#
directive|ifdef
name|HAVE_SETSID
if|if
condition|(
name|setsid
argument_list|()
operator|==
operator|(
name|pid_t
operator|)
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: setsid(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
name|setpgid
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: setpgid(): %m"
argument_list|)
expr_stmt|;
endif|#
directive|endif
else|#
directive|else
comment|/* HAVE_SETPGID || HAVE_SETSID */
block|{
if|#
directive|if
name|defined
argument_list|(
name|TIOCNOTTY
argument_list|)
name|int
name|fid
decl_stmt|;
name|fid
operator|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
if|if
condition|(
name|fid
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|ioctl
argument_list|(
name|fid
argument_list|,
operator|(
name|u_long
operator|)
name|TIOCNOTTY
argument_list|,
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|fid
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* defined(TIOCNOTTY) */
ifdef|#
directive|ifdef
name|HAVE_SETPGRP_0
operator|(
name|void
operator|)
name|setpgrp
argument_list|()
expr_stmt|;
else|#
directive|else
comment|/* HAVE_SETPGRP_0 */
operator|(
name|void
operator|)
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SETPGRP_0 */
block|}
endif|#
directive|endif
comment|/* HAVE_SETPGID || HAVE_SETSID */
ifdef|#
directive|ifdef
name|_AIX
comment|/* Don't get killed by low-on-memory signal. */
name|sa
operator|.
name|sa_handler
operator|=
name|catch_danger
expr_stmt|;
name|sigemptyset
argument_list|(
operator|&
name|sa
operator|.
name|sa_mask
argument_list|)
expr_stmt|;
name|sa
operator|.
name|sa_flags
operator|=
name|SA_RESTART
expr_stmt|;
operator|(
name|void
operator|)
name|sigaction
argument_list|(
name|SIGDANGER
argument_list|,
operator|&
name|sa
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* _AIX */
block|}
endif|#
directive|endif
comment|/* not HAVE_DAEMON */
else|#
directive|else
comment|/* SYS_WINNT */
block|{
name|SERVICE_TABLE_ENTRY
name|dispatchTable
index|[]
init|=
block|{
block|{
name|TEXT
argument_list|(
literal|"NetworkTimeProtocol"
argument_list|)
block|,
operator|(
name|LPSERVICE_MAIN_FUNCTION
operator|)
name|service_main
block|}
block|,
block|{
name|NULL
block|,
name|NULL
block|}
block|}
decl_stmt|;
comment|/* daemonize */
if|if
condition|(
operator|!
name|StartServiceCtrlDispatcher
argument_list|(
name|dispatchTable
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"StartServiceCtrlDispatcher: %m"
argument_list|)
expr_stmt|;
name|ExitProcess
argument_list|(
literal|2
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* SYS_WINNT */
block|}
endif|#
directive|endif
comment|/* NODETACH */
if|#
directive|if
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|NODETACH
argument_list|)
else|else
name|service_main
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
comment|/* must return a value */
block|}
end_function

begin_comment
comment|/* end main */
end_comment

begin_comment
comment|/*  * If this runs as a service under NT, the main thread will block at  * StartServiceCtrlDispatcher() and another thread will be started by the  * Service Control Dispatcher which will begin execution at the routine  * specified in that call (viz. service_main)  */
end_comment

begin_function
name|void
name|service_main
parameter_list|(
name|DWORD
name|argc
parameter_list|,
name|LPTSTR
modifier|*
name|argv
parameter_list|)
block|{
name|char
modifier|*
name|cp
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuflist
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuf
decl_stmt|;
if|if
condition|(
operator|!
name|debug
condition|)
block|{
comment|/* register our service control handler */
if|if
condition|(
operator|!
operator|(
name|sshStatusHandle
operator|=
name|RegisterServiceCtrlHandler
argument_list|(
name|TEXT
argument_list|(
literal|"NetworkTimeProtocol"
argument_list|)
argument_list|,
operator|(
name|LPHANDLER_FUNCTION
operator|)
name|service_ctrl
argument_list|)
operator|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"RegisterServiceCtrlHandler failed: %m"
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* report pending status to Service Control Manager */
name|ssStatus
operator|.
name|dwServiceType
operator|=
name|SERVICE_WIN32_OWN_PROCESS
expr_stmt|;
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|SERVICE_START_PENDING
expr_stmt|;
name|ssStatus
operator|.
name|dwControlsAccepted
operator|=
name|SERVICE_ACCEPT_STOP
expr_stmt|;
name|ssStatus
operator|.
name|dwWin32ExitCode
operator|=
name|NO_ERROR
expr_stmt|;
name|ssStatus
operator|.
name|dwServiceSpecificExitCode
operator|=
literal|0
expr_stmt|;
name|ssStatus
operator|.
name|dwCheckPoint
operator|=
literal|1
expr_stmt|;
name|ssStatus
operator|.
name|dwWaitHint
operator|=
literal|5000
expr_stmt|;
if|if
condition|(
operator|!
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetServiceStatus: %m"
argument_list|)
expr_stmt|;
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|SERVICE_STOPPED
expr_stmt|;
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
comment|/* debug */
endif|#
directive|endif
comment|/* defined(SYS_WINNT)&& !defined(NODETACH) */
endif|#
directive|endif
comment|/* VMS */
comment|/* 	 * Logging.  This may actually work on the gizmo board.  Find a name 	 * to log with by using the basename of argv[0] 	 */
name|cp
operator|=
name|strrchr
argument_list|(
name|argv
index|[
literal|0
index|]
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
literal|0
condition|)
name|cp
operator|=
name|argv
index|[
literal|0
index|]
expr_stmt|;
else|else
name|cp
operator|++
expr_stmt|;
name|debug
operator|=
literal|0
expr_stmt|;
comment|/* will be immediately re-initialized 8-( */
name|getstartup
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
comment|/* startup configuration, catch logfile this time */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifndef|#
directive|ifndef
name|LOG_DAEMON
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* LOG_DAEMON */
ifndef|#
directive|ifndef
name|LOG_NTP
define|#
directive|define
name|LOG_NTP
value|LOG_DAEMON
endif|#
directive|endif
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
operator||
name|LOG_NDELAY
argument_list|,
name|LOG_NTP
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
else|else
endif|#
directive|endif
comment|/* DEBUG */
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
comment|/* @@@ was INFO */
endif|#
directive|endif
comment|/* LOG_DAEMON */
endif|#
directive|endif
comment|/* !SYS_WINNT&& !VMS */
name|NLOG
argument_list|(
argument|NLOG_SYSINFO
argument_list|)
comment|/* conditional if clause for conditional syslog */
name|msyslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"%s"
argument_list|,
name|Version
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SYS_WINNT
comment|/* GMS 1/18/1997 	 * TODO: lock the process in memory using SetProcessWorkingSetSize() and VirtualLock() functions 	 * 	 process_handle = GetCurrentProcess(); 	 if (SetProcessWorkingSetSize(process_handle, 2097152 , 4194304 ) == TRUE) { 	 if (VirtualLock(0 , 4194304) == FALSE) 	 msyslog(LOG_ERR, "VirtualLock() failed: %m"); 	 } else { 	 msyslog(LOG_ERR, "SetProcessWorkingSetSize() failed: %m"); 	 } 	*/
endif|#
directive|endif
comment|/* SYS_WINNT */
ifdef|#
directive|ifdef
name|SCO5_CLOCK
comment|/* 	 * SCO OpenServer's system clock offers much more precise timekeeping 	 * on the base CPU than the other CPUs (for multiprocessor systems), 	 * so we must lock to the base CPU. 	 */
block|{
name|int
name|fd
init|=
name|open
argument_list|(
literal|"/dev/at1"
argument_list|,
name|O_RDONLY
argument_list|)
decl_stmt|;
if|if
condition|(
name|fd
operator|>=
literal|0
condition|)
block|{
name|int
name|zero
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|ioctl
argument_list|(
name|fd
argument_list|,
name|ACPU_LOCK
argument_list|,
operator|&
name|zero
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"cannot lock to base CPU: %m\n"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
comment|/* else ... 	       *   If we can't open the device, this probably just isn't 	       *   a multiprocessor system, so we're A-OK. 	       */
block|}
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|HAVE_MLOCKALL
argument_list|)
operator|&&
name|defined
argument_list|(
name|MCL_CURRENT
argument_list|)
operator|&&
name|defined
argument_list|(
name|MCL_FUTURE
argument_list|)
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|mlockall
argument_list|(
name|MCL_CURRENT
operator||
name|MCL_FUTURE
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"mlockall(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not (HAVE_MLOCKALL&& MCL_CURRENT&& MCL_FUTURE) */
ifdef|#
directive|ifdef
name|HAVE_PLOCK
ifdef|#
directive|ifdef
name|PROCLOCK
ifdef|#
directive|ifdef
name|_AIX
comment|/*  	 * set the stack limit for AIX for plock(). 	 * see get_aix_stack for more info. 	 */
if|if
condition|(
name|ulimit
argument_list|(
name|SET_STACKLIM
argument_list|,
operator|(
name|get_aix_stack
argument_list|()
operator|-
literal|8
operator|*
literal|4096
operator|)
argument_list|)
operator|<
literal|0
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot adjust stack limit for plock on AIX: %m"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* _AIX */
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|plock
argument_list|(
name|PROCLOCK
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock(PROCLOCK): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not PROCLOCK */
ifdef|#
directive|ifdef
name|TXTLOCK
comment|/* 	 * Lock text into ram 	 */
if|if
condition|(
name|plock
argument_list|(
name|TXTLOCK
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock(TXTLOCK) error: %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not TXTLOCK */
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock() - don't know what to lock!"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* not TXTLOCK */
endif|#
directive|endif
comment|/* not PROCLOCK */
endif|#
directive|endif
comment|/* HAVE_PLOCK */
endif|#
directive|endif
comment|/* not (HAVE_MLOCKALL&& MCL_CURRENT&& MCL_FUTURE) */
comment|/* 	 * Set up signals we pay attention to locally. 	 */
ifdef|#
directive|ifdef
name|SIGDIE1
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE1
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE1 */
ifdef|#
directive|ifdef
name|SIGDIE2
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE2
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE2 */
ifdef|#
directive|ifdef
name|SIGDIE3
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE3
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE3 */
ifdef|#
directive|ifdef
name|SIGDIE4
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE4
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE4 */
ifdef|#
directive|ifdef
name|SIGBUS
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGBUS
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGBUS */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifdef|#
directive|ifdef
name|DEBUG
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|moredebug
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|lessdebug
argument_list|)
expr_stmt|;
else|#
directive|else
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|no_debug
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|no_debug
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* DEBUG */
endif|#
directive|endif
comment|/* !SYS_WINNT&& !VMS */
comment|/* 	 * Set up signals we should never pay attention to. 	 */
if|#
directive|if
name|defined
name|SIGPIPE
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGPIPE
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGPIPE */
if|#
directive|if
name|defined
name|SYS_WINNT
if|if
condition|(
operator|!
name|SetConsoleCtrlHandler
argument_list|(
name|OnConsoleEvent
argument_list|,
name|TRUE
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Can't set console control handler: %m"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* 	 * Call the init_ routines to initialize the data structures. 	 */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_IO_COMPLETION_PORT
argument_list|)
name|init_io_completion_port
argument_list|()
expr_stmt|;
name|init_winnt_time
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|init_auth
argument_list|()
expr_stmt|;
name|init_util
argument_list|()
expr_stmt|;
name|init_restrict
argument_list|()
expr_stmt|;
name|init_mon
argument_list|()
expr_stmt|;
name|init_timer
argument_list|()
expr_stmt|;
name|init_lib
argument_list|()
expr_stmt|;
name|init_random
argument_list|()
expr_stmt|;
name|init_request
argument_list|()
expr_stmt|;
name|init_control
argument_list|()
expr_stmt|;
name|init_peer
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|REFCLOCK
name|init_refclock
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|set_process_priority
argument_list|()
expr_stmt|;
name|init_proto
argument_list|()
expr_stmt|;
name|init_io
argument_list|()
expr_stmt|;
name|init_loopfilter
argument_list|()
expr_stmt|;
name|mon_start
argument_list|(
name|MON_ON
argument_list|)
expr_stmt|;
comment|/* monitor on by default now	  */
comment|/* turn off in config if unwanted */
comment|/* 	 * Get configuration.  This (including argument list parsing) is 	 * done in a separate module since this will definitely be different 	 * for the gizmo board. 	 */
name|getconfig
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|initializing
operator|=
literal|0
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|NODETACH
argument_list|)
if|#
directive|if
name|defined
argument_list|(
name|DEBUG
argument_list|)
if|if
condition|(
operator|!
name|debug
condition|)
block|{
endif|#
directive|endif
comment|/* report to the service control manager that the service is running */
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|SERVICE_RUNNING
expr_stmt|;
name|ssStatus
operator|.
name|dwWin32ExitCode
operator|=
name|NO_ERROR
expr_stmt|;
if|if
condition|(
operator|!
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetServiceStatus: %m"
argument_list|)
expr_stmt|;
if|if
condition|(
name|ResolverThreadHandle
operator|!=
name|NULL
condition|)
name|CloseHandle
argument_list|(
name|ResolverThreadHandle
argument_list|)
expr_stmt|;
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|SERVICE_STOPPED
expr_stmt|;
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
expr_stmt|;
return|return;
block|}
if|#
directive|if
name|defined
argument_list|(
name|DEBUG
argument_list|)
block|}
endif|#
directive|endif
endif|#
directive|endif
comment|/* 	 * Report that we're up to any trappers 	 */
name|report_event
argument_list|(
name|EVNT_SYSRESTART
argument_list|,
operator|(
expr|struct
name|peer
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Use select() on all on all input fd's for unlimited 	 * time.  select() will terminate on SIGALARM or on the 	 * reception of input.	Using select() means we can't do 	 * robust signal handling and we get a potential race 	 * between checking for alarms and doing the select(). 	 * Mostly harmless, I think. 	 */
comment|/* On VMS, I suspect that select() can't be interrupted 	 * by a "signal" either, so I take the easy way out and 	 * have select() time out after one second. 	 * System clock updates really aren't time-critical, 	 * and - lacking a hardware reference clock - I have 	 * yet to learn about anything else that is. 	 */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_IO_COMPLETION_PORT
argument_list|)
name|WaitHandles
index|[
literal|0
index|]
operator|=
name|CreateEvent
argument_list|(
name|NULL
argument_list|,
name|FALSE
argument_list|,
name|FALSE
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* exit reques */
name|WaitHandles
index|[
literal|1
index|]
operator|=
name|get_timer_handle
argument_list|()
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|DWORD
name|Index
init|=
name|WaitForMultipleObjectsEx
argument_list|(
sizeof|sizeof
argument_list|(
name|WaitHandles
argument_list|)
operator|/
sizeof|sizeof
argument_list|(
name|WaitHandles
index|[
literal|0
index|]
argument_list|)
argument_list|,
name|WaitHandles
argument_list|,
name|FALSE
argument_list|,
literal|1000
argument_list|,
name|MWMO_ALERTABLE
argument_list|)
decl_stmt|;
switch|switch
condition|(
name|Index
condition|)
block|{
case|case
name|WAIT_OBJECT_0
operator|+
literal|0
case|:
comment|/* exit request */
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
break|break;
case|case
name|WAIT_OBJECT_0
operator|+
literal|1
case|:
comment|/* timer */
name|timer
argument_list|()
expr_stmt|;
break|break;
case|case
name|WAIT_OBJECT_0
operator|+
literal|2
case|:
block|{
comment|/* Windows message */
name|MSG
name|msg
decl_stmt|;
while|while
condition|(
name|PeekMessage
argument_list|(
operator|&
name|msg
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
name|PM_REMOVE
argument_list|)
condition|)
block|{
if|if
condition|(
name|msg
operator|.
name|message
operator|==
name|WM_QUIT
condition|)
block|{
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
name|DispatchMessage
argument_list|(
operator|&
name|msg
argument_list|)
expr_stmt|;
block|}
block|}
break|break;
case|case
name|WAIT_IO_COMPLETION
case|:
comment|/* loop */
case|case
name|WAIT_TIMEOUT
case|:
break|break;
block|}
comment|/* switch */
name|rbuflist
operator|=
name|getrecvbufs
argument_list|()
expr_stmt|;
comment|/* get received buffers */
else|#
directive|else
comment|/* normal I/O */
name|was_alarmed
operator|=
literal|0
expr_stmt|;
name|rbuflist
operator|=
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|#
directive|if
operator|!
name|defined
argument_list|(
name|HAVE_SIGNALED_IO
argument_list|)
specifier|extern
name|fd_set
name|activefds
decl_stmt|;
specifier|extern
name|int
name|maxactivefd
decl_stmt|;
name|fd_set
name|rdfdes
decl_stmt|;
name|int
name|nfound
decl_stmt|;
elif|#
directive|elif
name|defined
argument_list|(
name|HAVE_SIGNALED_IO
argument_list|)
name|block_io_and_alarm
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|rbuflist
operator|=
name|getrecvbufs
argument_list|()
expr_stmt|;
comment|/* get received buffers */
if|if
condition|(
name|alarm_flag
condition|)
comment|/* alarmed? */
block|{
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|was_alarmed
operator|&&
name|rbuflist
operator|==
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
condition|)
block|{
comment|/* 			 * Nothing to do.  Wait for something. 			 */
ifndef|#
directive|ifndef
name|HAVE_SIGNALED_IO
name|rdfdes
operator|=
name|activefds
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|VMS
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_VXWORKS
argument_list|)
comment|/* make select() wake up after one second */
block|{
name|struct
name|timeval
name|t1
decl_stmt|;
name|t1
operator|.
name|tv_sec
operator|=
literal|1
expr_stmt|;
name|t1
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
name|nfound
operator|=
name|select
argument_list|(
name|maxactivefd
operator|+
literal|1
argument_list|,
operator|&
name|rdfdes
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|t1
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
name|nfound
operator|=
name|select
argument_list|(
name|maxactivefd
operator|+
literal|1
argument_list|,
operator|&
name|rdfdes
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* VMS */
if|if
condition|(
name|nfound
operator|>
literal|0
condition|)
block|{
name|l_fp
name|ts
decl_stmt|;
name|get_systime
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|input_handler
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|nfound
operator|==
operator|-
literal|1
operator|&&
name|errno
operator|!=
name|EINTR
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"select() error: %m"
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|debug
condition|)
block|{
if|#
directive|if
operator|!
name|defined
name|SYS_VXWORKS
operator|&&
operator|!
name|defined
name|SCO5_CLOCK
comment|/* to unclutter log */
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"select(): nfound=%d, error: %m"
argument_list|,
name|nfound
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
else|#
directive|else
comment|/* HAVE_SIGNALED_IO */
name|wait_for_signal
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SIGNALED_IO */
if|if
condition|(
name|alarm_flag
condition|)
comment|/* alarmed? */
block|{
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
name|rbuflist
operator|=
name|getrecvbufs
argument_list|()
expr_stmt|;
comment|/* get received buffers */
block|}
ifdef|#
directive|ifdef
name|HAVE_SIGNALED_IO
name|unblock_io_and_alarm
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SIGNALED_IO */
comment|/* 		 * Out here, signals are unblocked.  Call timer routine 		 * to process expiry. 		 */
if|if
condition|(
name|was_alarmed
condition|)
block|{
name|timer
argument_list|()
expr_stmt|;
name|was_alarmed
operator|=
literal|0
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_IO_COMPLETION_PORT */
comment|/* 		 * Call the data procedure to handle each received 		 * packet. 		 */
while|while
condition|(
name|rbuflist
operator|!=
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
literal|0
condition|)
block|{
name|rbuf
operator|=
name|rbuflist
expr_stmt|;
name|rbuflist
operator|=
name|rbuf
operator|->
name|next
expr_stmt|;
call|(
name|rbuf
operator|->
name|receiver
call|)
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
name|freerecvbuf
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
name|defined
name|DEBUG
operator|&&
name|defined
name|SYS_WINNT
if|if
condition|(
name|debug
operator|>
literal|4
condition|)
name|printf
argument_list|(
literal|"getrecvbufs: %ld handler interrupts, %ld frames\n"
argument_list|,
name|handler_calls
argument_list|,
name|handler_pkts
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 		 * Go around again 		 */
block|}
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
comment|/* unreachable */
return|return
literal|1
return|;
comment|/* DEC OSF cc braindamage */
block|}
ifdef|#
directive|ifdef
name|SIGDIE2
comment|/*  * finish - exit gracefully  */
specifier|static
name|RETSIGTYPE
name|finish
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|msyslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"ntpd exiting on signal %d"
argument_list|,
name|sig
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|sig
condition|)
block|{
ifdef|#
directive|ifdef
name|SIGBUS
case|case
name|SIGBUS
case|:
name|printf
argument_list|(
literal|"\nfinish(SIGBUS)\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
case|case
literal|0
case|:
comment|/* Should never happen... */
return|return;
default|default:
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* SIGDIE2 */
ifdef|#
directive|ifdef
name|DEBUG
comment|/*  * moredebug - increase debugging verbosity  */
specifier|static
name|RETSIGTYPE
name|moredebug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
if|if
condition|(
name|debug
operator|<
literal|255
condition|)
block|{
name|debug
operator|++
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug raised to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
comment|/*  * lessdebug - decrease debugging verbosity  */
specifier|static
name|RETSIGTYPE
name|lessdebug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
if|if
condition|(
name|debug
operator|>
literal|0
condition|)
block|{
name|debug
operator|--
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug lowered to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
else|#
directive|else
comment|/* not DEBUG */
comment|/*  * no_debug - We don't do the debug here.  */
specifier|static
name|RETSIGTYPE
name|no_debug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"ntpd not compiled for debugging (signal %d)"
argument_list|,
name|sig
argument_list|)
expr_stmt|;
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* not DEBUG */
ifdef|#
directive|ifdef
name|SYS_WINNT
comment|/* service_ctrl - control handler for NTP service  * signals the service_main routine of start/stop requests  * from the control panel or other applications making  * win32API calls  */
name|void
name|service_ctrl
parameter_list|(
name|DWORD
name|dwCtrlCode
parameter_list|)
block|{
name|DWORD
name|dwState
init|=
name|SERVICE_RUNNING
decl_stmt|;
comment|/* Handle the requested control code */
switch|switch
condition|(
name|dwCtrlCode
condition|)
block|{
case|case
name|SERVICE_CONTROL_PAUSE
case|:
comment|/* see no reason to support this */
break|break;
case|case
name|SERVICE_CONTROL_CONTINUE
case|:
comment|/* see no reason to support this */
break|break;
case|case
name|SERVICE_CONTROL_STOP
case|:
name|dwState
operator|=
name|SERVICE_STOP_PENDING
expr_stmt|;
comment|/* 			 * Report the status, specifying the checkpoint and waithint, 			 *	before setting the termination event. 			 */
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|dwState
expr_stmt|;
name|ssStatus
operator|.
name|dwWin32ExitCode
operator|=
name|NO_ERROR
expr_stmt|;
name|ssStatus
operator|.
name|dwWaitHint
operator|=
literal|3000
expr_stmt|;
if|if
condition|(
operator|!
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetServiceStatus: %m"
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|WaitHandles
index|[
literal|0
index|]
operator|!=
name|NULL
condition|)
block|{
name|SetEvent
argument_list|(
name|WaitHandles
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
block|}
return|return;
case|case
name|SERVICE_CONTROL_INTERROGATE
case|:
comment|/* Update the service status */
break|break;
default|default:
comment|/* invalid control code */
break|break;
block|}
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|dwState
expr_stmt|;
name|ssStatus
operator|.
name|dwWin32ExitCode
operator|=
name|NO_ERROR
expr_stmt|;
if|if
condition|(
operator|!
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"SetServiceStatus: %m"
argument_list|)
expr_stmt|;
block|}
block|}
specifier|static
name|BOOL
name|WINAPI
name|OnConsoleEvent
parameter_list|(
name|DWORD
name|dwCtrlType
parameter_list|)
block|{
switch|switch
condition|(
name|dwCtrlType
condition|)
block|{
case|case
name|CTRL_BREAK_EVENT
case|:
if|if
condition|(
name|debug
operator|>
literal|0
condition|)
block|{
name|debug
operator|<<=
literal|1
expr_stmt|;
block|}
else|else
block|{
name|debug
operator|=
literal|1
expr_stmt|;
block|}
if|if
condition|(
name|debug
operator|>
literal|8
condition|)
block|{
name|debug
operator|=
literal|0
expr_stmt|;
block|}
name|printf
argument_list|(
literal|"debug level %d\n"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
break|break ;
case|case
name|CTRL_C_EVENT
case|:
case|case
name|CTRL_CLOSE_EVENT
case|:
case|case
name|CTRL_SHUTDOWN_EVENT
case|:
if|if
condition|(
name|WaitHandles
index|[
literal|0
index|]
operator|!=
name|NULL
condition|)
block|{
name|SetEvent
argument_list|(
name|WaitHandles
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
block|}
break|break;
default|default :
return|return
name|FALSE
return|;
block|}
return|return
name|TRUE
return|;
empty_stmt|;
block|}
comment|/*  *  NT version of exit() - all calls to exit() should be routed to  *  this function.  */
name|void
name|service_exit
parameter_list|(
name|int
name|status
parameter_list|)
block|{
if|if
condition|(
operator|!
name|debug
condition|)
block|{
comment|/* did not become a service, simply exit */
comment|/* service mode, need to have the service_main routine 		 * register with the service control manager that the  		 * service has stopped running, before exiting 		 */
name|ssStatus
operator|.
name|dwCurrentState
operator|=
name|SERVICE_STOPPED
expr_stmt|;
name|SetServiceStatus
argument_list|(
name|sshStatusHandle
argument_list|,
operator|&
name|ssStatus
argument_list|)
expr_stmt|;
block|}
name|uninit_io_completion_port
argument_list|()
expr_stmt|;
name|reset_winnt_time
argument_list|()
expr_stmt|;
if|#
directive|if
name|defined
name|_MSC_VER
name|_CrtDumpMemoryLeaks
argument_list|()
expr_stmt|;
endif|#
directive|endif
undef|#
directive|undef
name|exit
name|exit
argument_list|(
name|status
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

end_unit

