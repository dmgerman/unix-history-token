begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * ntpd.c - main program for the fixed point NTP daemon  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntp_machine.h"
end_include

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|<ntp_random.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|SIM
end_ifdef

begin_include
include|#
directive|include
file|"ntpsim.h"
end_include

begin_include
include|#
directive|include
file|"ntpdsim-opts.h"
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|"ntpd-opts.h"
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_UNISTD_H
end_ifdef

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_STAT_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
end_if

begin_comment
comment|/*wjm*/
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_PARAM_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* VMS */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_SIGNAL_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/signal.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_IOCTL_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/ioctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_SYS_IOCTL_H */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_RESOURCE_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_SYS_RESOURCE_H */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_RTPRIO
argument_list|)
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_RESOURCE_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/resource.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_LOCK_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/rtprio.h>
end_include

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_PLOCK
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_LOCK_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SCHED_SETSCHEDULER
argument_list|)
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SCHED_H
end_ifdef

begin_include
include|#
directive|include
file|<sched.h>
end_include

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_SYS_SCHED_H
end_ifdef

begin_include
include|#
directive|include
file|<sys/sched.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SYS_MMAN_H
argument_list|)
end_if

begin_include
include|#
directive|include
file|<sys/mman.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_TERMIOS_H
end_ifdef

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_DOMAINOS
end_ifdef

begin_include
include|#
directive|include
file|<apollo/base.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_DOMAINOS */
end_comment

begin_include
include|#
directive|include
file|"recvbuff.h"
end_include

begin_include
include|#
directive|include
file|"ntp_cmdargs.h"
end_include

begin_if
if|#
directive|if
literal|0
end_if

begin_comment
comment|/* HMS: I don't think we need this. 961223 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|LOCK_PROCESS
end_ifdef

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_SOLARIS
end_ifdef

begin_include
include|#
directive|include
file|<sys/mman.h>
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<sys/lock.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|_AIX
end_ifdef

begin_include
include|#
directive|include
file|<ulimit.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _AIX */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SCO5_CLOCK
end_ifdef

begin_include
include|#
directive|include
file|<sys/ci/ciioctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_DROPROOT
end_ifdef

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<grp.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_LINUX_CAPABILITIES
end_ifdef

begin_include
include|#
directive|include
file|<sys/capability.h>
end_include

begin_include
include|#
directive|include
file|<sys/prctl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Signals we catch for debugging.	If not debugging we ignore them.  */
end_comment

begin_define
define|#
directive|define
name|MOREDEBUGSIG
value|SIGUSR1
end_define

begin_define
define|#
directive|define
name|LESSDEBUGSIG
value|SIGUSR2
end_define

begin_comment
comment|/*  * Signals which terminate us gracefully.  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|SYS_WINNT
end_ifndef

begin_define
define|#
directive|define
name|SIGDIE1
value|SIGHUP
end_define

begin_define
define|#
directive|define
name|SIGDIE3
value|SIGQUIT
end_define

begin_define
define|#
directive|define
name|SIGDIE2
value|SIGINT
end_define

begin_define
define|#
directive|define
name|SIGDIE4
value|SIGTERM
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_DNSREGISTRATION
end_ifdef

begin_include
include|#
directive|include
file|<dns_sd.h>
end_include

begin_decl_stmt
name|DNSServiceRef
name|mdns
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Scheduling priority we run at  */
end_comment

begin_define
define|#
directive|define
name|NTPD_PRIO
value|(-12)
end_define

begin_decl_stmt
name|int
name|priority_done
init|=
literal|2
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* 0 - Set priority */
end_comment

begin_comment
comment|/* 1 - priority is OK where it is */
end_comment

begin_comment
comment|/* 2 - Don't set priority */
end_comment

begin_comment
comment|/* 1 and 2 are pretty much the same */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_comment
comment|/*  * Debugging flag  */
end_comment

begin_decl_stmt
specifier|volatile
name|int
name|debug
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* No debugging by default */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|int
name|listen_to_virtual_ips
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|const
name|char
modifier|*
name|specific_interface
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* interface name or IP address to bind to */
end_comment

begin_comment
comment|/*  * No-fork flag.  If set, we do not become a background daemon.  */
end_comment

begin_decl_stmt
name|int
name|nofork
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Fork by default */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_DROPROOT
end_ifdef

begin_decl_stmt
name|int
name|droproot
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|user
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* User to switch to */
end_comment

begin_decl_stmt
name|char
modifier|*
name|group
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* group to switch to */
end_comment

begin_decl_stmt
name|char
modifier|*
name|chrootdir
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* directory to chroot to */
end_comment

begin_decl_stmt
name|int
name|sw_uid
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|sw_gid
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|endp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|group
modifier|*
name|gr
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|passwd
modifier|*
name|pw
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAVE_DROPROOT */
end_comment

begin_comment
comment|/*  * Initializing flag.  All async routines watch this and only do their  * thing when it is clear.  */
end_comment

begin_decl_stmt
name|int
name|initializing
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Version declaration  */
end_comment

begin_decl_stmt
specifier|extern
specifier|const
name|char
modifier|*
name|Version
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
specifier|const
modifier|*
name|progname
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|int
name|was_alarmed
decl_stmt|;
end_decl_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|DECL_SYSCALL
end_ifdef

begin_comment
comment|/*  * We put this here, since the argument profile is syscall-specific  */
end_comment

begin_decl_stmt
specifier|extern
name|int
name|syscall
name|P
argument_list|(
operator|(
name|int
operator|,
operator|...
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* DECL_SYSCALL */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SIGDIE2
end_ifdef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|finish
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SIGDIE2 */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_ifndef
ifndef|#
directive|ifndef
name|SYS_WINNT
end_ifndef

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|moredebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|lessdebug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* not DEBUG */
end_comment

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|no_debug
name|P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not DEBUG */
end_comment

begin_decl_stmt
name|int
name|ntpdmain
name|P
argument_list|(
operator|(
name|int
operator|,
name|char
operator|*
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|set_process_priority
name|P
argument_list|(
operator|(
name|void
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|init_logging
name|P
argument_list|(
operator|(
name|char
specifier|const
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|setup_logfile
name|P
argument_list|(
operator|(
name|void
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Initialize the logging  */
end_comment

begin_function
name|void
name|init_logging
parameter_list|(
name|char
specifier|const
modifier|*
name|name
parameter_list|)
block|{
specifier|const
name|char
modifier|*
name|cp
decl_stmt|;
comment|/* 	 * Logging.  This may actually work on the gizmo board.  Find a name 	 * to log with by using the basename 	 */
name|cp
operator|=
name|strrchr
argument_list|(
name|name
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cp
operator|==
literal|0
condition|)
name|cp
operator|=
name|name
expr_stmt|;
else|else
name|cp
operator|++
expr_stmt|;
if|#
directive|if
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifndef|#
directive|ifndef
name|LOG_DAEMON
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* LOG_DAEMON */
ifndef|#
directive|ifndef
name|LOG_NTP
define|#
directive|define
name|LOG_NTP
value|LOG_DAEMON
endif|#
directive|endif
name|openlog
argument_list|(
name|cp
argument_list|,
name|LOG_PID
operator||
name|LOG_NDELAY
argument_list|,
name|LOG_NTP
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
else|else
endif|#
directive|endif
comment|/* DEBUG */
name|setlogmask
argument_list|(
name|LOG_UPTO
argument_list|(
name|LOG_DEBUG
argument_list|)
argument_list|)
expr_stmt|;
comment|/* @@@ was INFO */
endif|#
directive|endif
comment|/* LOG_DAEMON */
endif|#
directive|endif
comment|/* !SYS_WINNT&& !VMS */
name|NLOG
argument_list|(
argument|NLOG_SYSINFO
argument_list|)
comment|/* conditional if clause for conditional syslog */
name|msyslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"%s"
argument_list|,
name|Version
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * See if we should redirect the logfile  */
end_comment

begin_function
name|void
name|setup_logfile
parameter_list|(
name|void
parameter_list|)
block|{
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|LOGFILE
argument_list|)
condition|)
block|{
specifier|const
name|char
modifier|*
name|my_optarg
init|=
name|OPT_ARG
argument_list|(
name|LOGFILE
argument_list|)
decl_stmt|;
name|FILE
modifier|*
name|new_file
decl_stmt|;
if|if
condition|(
name|strcmp
argument_list|(
name|my_optarg
argument_list|,
literal|"stderr"
argument_list|)
operator|==
literal|0
condition|)
name|new_file
operator|=
name|stderr
expr_stmt|;
elseif|else
if|if
condition|(
name|strcmp
argument_list|(
name|my_optarg
argument_list|,
literal|"stdout"
argument_list|)
operator|==
literal|0
condition|)
name|new_file
operator|=
name|stdout
expr_stmt|;
else|else
name|new_file
operator|=
name|fopen
argument_list|(
name|my_optarg
argument_list|,
literal|"a"
argument_list|)
expr_stmt|;
if|if
condition|(
name|new_file
operator|!=
name|NULL
condition|)
block|{
name|NLOG
argument_list|(
argument|NLOG_SYSINFO
argument_list|)
name|msyslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"logging to file %s"
argument_list|,
name|my_optarg
argument_list|)
expr_stmt|;
if|if
condition|(
name|syslog_file
operator|!=
name|NULL
operator|&&
name|fileno
argument_list|(
name|syslog_file
argument_list|)
operator|!=
name|fileno
argument_list|(
name|new_file
argument_list|)
condition|)
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|syslog_file
argument_list|)
expr_stmt|;
name|syslog_file
operator|=
name|new_file
expr_stmt|;
name|syslogit
operator|=
literal|0
expr_stmt|;
block|}
else|else
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot open log file %s"
argument_list|,
name|my_optarg
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|SIM
end_ifdef

begin_function
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
return|return
name|ntpsim
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
return|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* SIM */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|NO_MAIN_ALLOWED
end_ifdef

begin_expr_stmt
name|CALL
argument_list|(
name|ntpd
argument_list|,
literal|"ntpd"
argument_list|,
name|ntpdmain
argument_list|)
expr_stmt|;
end_expr_stmt

begin_else
else|#
directive|else
end_else

begin_ifndef
ifndef|#
directive|ifndef
name|SYS_WINNT
end_ifndef

begin_function
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
return|return
name|ntpdmain
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NO_MAIN_ALLOWED */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SIM */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|_AIX
end_ifdef

begin_comment
comment|/*  * OK. AIX is different than solaris in how it implements plock().  * If you do NOT adjust the stack limit, you will get the MAXIMUM  * stack size allocated and PINNED with you program. To check the   * value, use ulimit -a.   *  * To fix this, we create an automatic variable and set our stack limit   * to that PLUS 32KB of extra space (we need some headroom).  *  * This subroutine gets the stack address.  *  * Grover Davidson and Matt Ladendorf  *  */
end_comment

begin_function
specifier|static
name|char
modifier|*
name|get_aix_stack
parameter_list|(
name|void
parameter_list|)
block|{
name|char
name|ch
decl_stmt|;
return|return
operator|(
operator|&
name|ch
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * Signal handler for SIGDANGER.  */
end_comment

begin_function
specifier|static
name|void
name|catch_danger
parameter_list|(
name|int
name|signo
parameter_list|)
block|{
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"ntpd: setpgid(): %m"
argument_list|)
expr_stmt|;
comment|/* Make the system believe we'll free something, but don't do it! */
return|return;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* _AIX */
end_comment

begin_comment
comment|/*  * Set the process priority  */
end_comment

begin_function
specifier|static
name|void
name|set_process_priority
parameter_list|(
name|void
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
operator|>
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"set_process_priority: %s: priority_done is<%d>"
argument_list|,
operator|(
operator|(
name|priority_done
operator|)
condition|?
literal|"Leave priority alone"
else|:
literal|"Attempt to set priority"
operator|)
argument_list|,
name|priority_done
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* DEBUG */
ifdef|#
directive|ifdef
name|SYS_WINNT
name|priority_done
operator|+=
name|NT_set_process_priority
argument_list|()
expr_stmt|;
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SCHED_SETSCHEDULER
argument_list|)
if|if
condition|(
operator|!
name|priority_done
condition|)
block|{
specifier|extern
name|int
name|config_priority_override
decl_stmt|,
name|config_priority
decl_stmt|;
name|int
name|pmax
decl_stmt|,
name|pmin
decl_stmt|;
name|struct
name|sched_param
name|sched
decl_stmt|;
name|pmax
operator|=
name|sched_get_priority_max
argument_list|(
name|SCHED_FIFO
argument_list|)
expr_stmt|;
name|sched
operator|.
name|sched_priority
operator|=
name|pmax
expr_stmt|;
if|if
condition|(
name|config_priority_override
condition|)
block|{
name|pmin
operator|=
name|sched_get_priority_min
argument_list|(
name|SCHED_FIFO
argument_list|)
expr_stmt|;
if|if
condition|(
name|config_priority
operator|>
name|pmax
condition|)
name|sched
operator|.
name|sched_priority
operator|=
name|pmax
expr_stmt|;
elseif|else
if|if
condition|(
name|config_priority
operator|<
name|pmin
condition|)
name|sched
operator|.
name|sched_priority
operator|=
name|pmin
expr_stmt|;
else|else
name|sched
operator|.
name|sched_priority
operator|=
name|config_priority
expr_stmt|;
block|}
if|if
condition|(
name|sched_setscheduler
argument_list|(
literal|0
argument_list|,
name|SCHED_FIFO
argument_list|,
operator|&
name|sched
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"sched_setscheduler(): %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|priority_done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_SCHED_SETSCHEDULER */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_RTPRIO
argument_list|)
ifdef|#
directive|ifdef
name|RTP_SET
if|if
condition|(
operator|!
name|priority_done
condition|)
block|{
name|struct
name|rtprio
name|srtp
decl_stmt|;
name|srtp
operator|.
name|type
operator|=
name|RTP_PRIO_REALTIME
expr_stmt|;
comment|/* was: RTP_PRIO_NORMAL */
name|srtp
operator|.
name|prio
operator|=
literal|0
expr_stmt|;
comment|/* 0 (hi) -> RTP_PRIO_MAX (31,lo) */
if|if
condition|(
name|rtprio
argument_list|(
name|RTP_SET
argument_list|,
name|getpid
argument_list|()
argument_list|,
operator|&
name|srtp
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"rtprio() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|priority_done
expr_stmt|;
block|}
else|#
directive|else
comment|/* not RTP_SET */
if|if
condition|(
operator|!
name|priority_done
condition|)
block|{
if|if
condition|(
name|rtprio
argument_list|(
literal|0
argument_list|,
literal|120
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"rtprio() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|priority_done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* not RTP_SET */
endif|#
directive|endif
comment|/* HAVE_RTPRIO */
if|#
directive|if
name|defined
argument_list|(
name|NTPD_PRIO
argument_list|)
operator|&&
name|NTPD_PRIO
operator|!=
literal|0
ifdef|#
directive|ifdef
name|HAVE_ATT_NICE
if|if
condition|(
operator|!
name|priority_done
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|-
literal|1
operator|==
name|nice
argument_list|(
name|NTPD_PRIO
argument_list|)
operator|&&
name|errno
operator|!=
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"nice() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|priority_done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_ATT_NICE */
ifdef|#
directive|ifdef
name|HAVE_BSD_NICE
if|if
condition|(
operator|!
name|priority_done
condition|)
block|{
if|if
condition|(
operator|-
literal|1
operator|==
name|setpriority
argument_list|(
name|PRIO_PROCESS
argument_list|,
literal|0
argument_list|,
name|NTPD_PRIO
argument_list|)
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"setpriority() error: %m"
argument_list|)
expr_stmt|;
else|else
operator|++
name|priority_done
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_BSD_NICE */
endif|#
directive|endif
comment|/* NTPD_PRIO&& NTPD_PRIO != 0 */
if|if
condition|(
operator|!
name|priority_done
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"set_process_priority: No way found to improve our priority"
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Main program.  Initialize us, disconnect us from the tty if necessary,  * and loop waiting for I/O and/or timer expiries.  */
end_comment

begin_function
name|int
name|ntpdmain
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
name|argv
index|[]
parameter_list|)
block|{
name|l_fp
name|now
decl_stmt|;
name|struct
name|recvbuf
modifier|*
name|rbuf
decl_stmt|;
ifdef|#
directive|ifdef
name|_AIX
comment|/* HMS: ifdef SIGDANGER? */
name|struct
name|sigaction
name|sa
decl_stmt|;
endif|#
directive|endif
name|progname
operator|=
name|argv
index|[
literal|0
index|]
expr_stmt|;
name|initializing
operator|=
literal|1
expr_stmt|;
comment|/* mark that we are initializing */
block|{
name|int
name|optct
init|=
name|optionProcess
argument_list|(
ifdef|#
directive|ifdef
name|SIM
operator|&
name|ntpdsimOptions
else|#
directive|else
operator|&
name|ntpdOptions
endif|#
directive|endif
argument_list|,
name|argc
argument_list|,
name|argv
argument_list|)
decl_stmt|;
name|argc
operator|-=
name|optct
expr_stmt|;
name|argv
operator|+=
name|optct
expr_stmt|;
block|}
comment|/* HMS: is this lame? Should we process -l first? */
name|init_logging
argument_list|(
name|progname
argument_list|)
expr_stmt|;
comment|/* Open the log file */
ifdef|#
directive|ifdef
name|HAVE_UMASK
block|{
name|mode_t
name|uv
decl_stmt|;
name|uv
operator|=
name|umask
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|uv
condition|)
operator|(
name|void
operator|)
name|umask
argument_list|(
name|uv
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|umask
argument_list|(
literal|022
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|HAVE_GETUID
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|MPE
argument_list|)
comment|/* MPE lacks the concept of root */
block|{
name|uid_t
name|uid
decl_stmt|;
name|uid
operator|=
name|getuid
argument_list|()
expr_stmt|;
if|if
condition|(
name|uid
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: must be run as root, not uid %ld"
argument_list|,
operator|(
name|long
operator|)
name|uid
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"must be run as root, not uid %ld"
argument_list|,
operator|(
name|long
operator|)
name|uid
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|OPENSSL
if|if
condition|(
operator|(
name|SSLeay
argument_list|()
operator|^
name|OPENSSL_VERSION_NUMBER
operator|)
operator|&
operator|~
literal|0xff0L
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: OpenSSL version mismatch. Built against %lx, you have %lx\n"
argument_list|,
name|OPENSSL_VERSION_NUMBER
argument_list|,
name|SSLeay
argument_list|()
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* getstartup(argc, argv); / * startup configuration, may set debug */
ifdef|#
directive|ifdef
name|DEBUG
name|debug
operator|=
name|DESC
argument_list|(
name|DEBUG_LEVEL
argument_list|)
operator|.
name|optOccCt
expr_stmt|;
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"%s\n"
argument_list|,
name|Version
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/*  * Enable the Multi-Media Timer for Windows?  */
ifdef|#
directive|ifdef
name|SYS_WINNT
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|MODIFYMMTIMER
argument_list|)
condition|)
name|set_mm_timer
argument_list|(
name|MM_TIMER_HIRES
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|NOFORK
argument_list|)
operator|||
name|HAVE_OPT
argument_list|(
name|QUIT
argument_list|)
condition|)
name|nofork
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|NOVIRTUALIPS
argument_list|)
condition|)
name|listen_to_virtual_ips
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|INTERFACE
argument_list|)
condition|)
block|{
if|#
directive|if
literal|0
block|int	ifacect = STACKCT_OPT( INTERFACE ); 		char**	ifaces  = STACKLST_OPT( INTERFACE );
comment|/* malloc space for the array of names */
block|while (ifacect--> 0) { 			next_iface = *ifaces++; 		}
else|#
directive|else
name|specific_interface
operator|=
name|OPT_ARG
argument_list|(
name|INTERFACE
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|NICE
argument_list|)
condition|)
name|priority_done
operator|=
literal|0
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SCHED_SETSCHEDULER
argument_list|)
if|if
condition|(
name|HAVE_OPT
argument_list|(
name|PRIORITY
argument_list|)
condition|)
block|{
name|config_priority
operator|=
name|OPT_VALUE_PRIORITY
expr_stmt|;
name|config_priority_override
operator|=
literal|1
expr_stmt|;
name|priority_done
operator|=
literal|0
expr_stmt|;
block|}
endif|#
directive|endif
ifdef|#
directive|ifdef
name|SYS_WINNT
comment|/* 	 * Initialize the time structures and variables 	 */
name|init_winnt_time
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|setup_logfile
argument_list|()
expr_stmt|;
comment|/* 	 * Initialize random generator and public key pair 	 */
name|get_systime
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
name|ntp_srandom
argument_list|(
call|(
name|int
call|)
argument_list|(
name|now
operator|.
name|l_i
operator|*
name|now
operator|.
name|l_uf
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|HAVE_DNSREGISTRATION
comment|/* HMS: does this have to happen this early? */
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"Attemping to register mDNS"
argument_list|)
expr_stmt|;
if|if
condition|(
name|DNSServiceRegister
argument_list|(
operator|&
name|mdns
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
literal|"_ntp._udp"
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|htons
argument_list|(
name|NTP_PORT
argument_list|)
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
operator|!=
name|kDNSServiceErr_NoError
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Unable to register mDNS"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
if|#
directive|if
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifndef|#
directive|ifndef
name|NODETACH
comment|/* 	 * Detach us from the terminal.  May need an #ifndef GIZMO. 	 */
if|if
condition|(
ifdef|#
directive|ifdef
name|DEBUG
operator|!
name|debug
operator|&&
endif|#
directive|endif
comment|/* DEBUG */
operator|!
name|nofork
condition|)
block|{
ifndef|#
directive|ifndef
name|SYS_WINNT
ifdef|#
directive|ifdef
name|HAVE_DAEMON
name|daemon
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not HAVE_DAEMON */
if|if
condition|(
name|fork
argument_list|()
condition|)
comment|/* HMS: What about a -1? */
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|{
if|#
directive|if
operator|!
name|defined
argument_list|(
name|F_CLOSEM
argument_list|)
name|u_long
name|s
decl_stmt|;
name|int
name|max_fd
decl_stmt|;
endif|#
directive|endif
comment|/* not F_CLOSEM */
if|#
directive|if
name|defined
argument_list|(
name|F_CLOSEM
argument_list|)
comment|/* 			 * From 'Writing Reliable AIX Daemons,' SG24-4946-00, 			 * by Eric Agar (saves us from doing 32767 system 			 * calls) 			 */
if|if
condition|(
name|fcntl
argument_list|(
literal|0
argument_list|,
name|F_CLOSEM
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: failed to close open files(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not F_CLOSEM */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SYSCONF
argument_list|)
operator|&&
name|defined
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
name|max_fd
operator|=
name|sysconf
argument_list|(
name|_SC_OPEN_MAX
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* HAVE_SYSCONF&& _SC_OPEN_MAX */
name|max_fd
operator|=
name|getdtablesize
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SYSCONF&& _SC_OPEN_MAX */
for|for
control|(
name|s
operator|=
literal|0
init|;
name|s
operator|<
name|max_fd
condition|;
name|s
operator|++
control|)
operator|(
name|void
operator|)
name|close
argument_list|(
operator|(
name|int
operator|)
name|s
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* not F_CLOSEM */
operator|(
name|void
operator|)
name|open
argument_list|(
literal|"/"
argument_list|,
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|dup2
argument_list|(
literal|0
argument_list|,
literal|2
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SYS_DOMAINOS
block|{
name|uid_$t
name|puid
decl_stmt|;
name|status_$t
name|st
decl_stmt|;
name|proc2_$who_am_i
argument_list|(
operator|&
name|puid
argument_list|)
expr_stmt|;
name|proc2_$make_server
argument_list|(
operator|&
name|puid
argument_list|,
operator|&
name|st
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SYS_DOMAINOS */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_SETPGID
argument_list|)
operator|||
name|defined
argument_list|(
name|HAVE_SETSID
argument_list|)
ifdef|#
directive|ifdef
name|HAVE_SETSID
if|if
condition|(
name|setsid
argument_list|()
operator|==
operator|(
name|pid_t
operator|)
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: setsid(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
name|setpgid
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"ntpd: setpgid(): %m"
argument_list|)
expr_stmt|;
endif|#
directive|endif
else|#
directive|else
comment|/* HAVE_SETPGID || HAVE_SETSID */
block|{
if|#
directive|if
name|defined
argument_list|(
name|TIOCNOTTY
argument_list|)
name|int
name|fid
decl_stmt|;
name|fid
operator|=
name|open
argument_list|(
literal|"/dev/tty"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
if|if
condition|(
name|fid
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|ioctl
argument_list|(
name|fid
argument_list|,
operator|(
name|u_long
operator|)
name|TIOCNOTTY
argument_list|,
operator|(
name|char
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|fid
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* defined(TIOCNOTTY) */
ifdef|#
directive|ifdef
name|HAVE_SETPGRP_0
operator|(
name|void
operator|)
name|setpgrp
argument_list|()
expr_stmt|;
else|#
directive|else
comment|/* HAVE_SETPGRP_0 */
operator|(
name|void
operator|)
name|setpgrp
argument_list|(
literal|0
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SETPGRP_0 */
block|}
endif|#
directive|endif
comment|/* HAVE_SETPGID || HAVE_SETSID */
ifdef|#
directive|ifdef
name|_AIX
comment|/* Don't get killed by low-on-memory signal. */
name|sa
operator|.
name|sa_handler
operator|=
name|catch_danger
expr_stmt|;
name|sigemptyset
argument_list|(
operator|&
name|sa
operator|.
name|sa_mask
argument_list|)
expr_stmt|;
name|sa
operator|.
name|sa_flags
operator|=
name|SA_RESTART
expr_stmt|;
operator|(
name|void
operator|)
name|sigaction
argument_list|(
name|SIGDANGER
argument_list|,
operator|&
name|sa
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* _AIX */
block|}
endif|#
directive|endif
comment|/* not HAVE_DAEMON */
endif|#
directive|endif
comment|/* SYS_WINNT */
block|}
endif|#
directive|endif
comment|/* NODETACH */
endif|#
directive|endif
comment|/* VMS */
name|setup_logfile
argument_list|()
expr_stmt|;
comment|/* We lost any redirect when we daemonized */
ifdef|#
directive|ifdef
name|SCO5_CLOCK
comment|/* 	 * SCO OpenServer's system clock offers much more precise timekeeping 	 * on the base CPU than the other CPUs (for multiprocessor systems), 	 * so we must lock to the base CPU. 	 */
block|{
name|int
name|fd
init|=
name|open
argument_list|(
literal|"/dev/at1"
argument_list|,
name|O_RDONLY
argument_list|)
decl_stmt|;
if|if
condition|(
name|fd
operator|>=
literal|0
condition|)
block|{
name|int
name|zero
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|ioctl
argument_list|(
name|fd
argument_list|,
name|ACPU_LOCK
argument_list|,
operator|&
name|zero
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"cannot lock to base CPU: %m"
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
block|}
comment|/* else ... 	       *   If we can't open the device, this probably just isn't 	       *   a multiprocessor system, so we're A-OK. 	       */
block|}
endif|#
directive|endif
if|#
directive|if
name|defined
argument_list|(
name|HAVE_MLOCKALL
argument_list|)
operator|&&
name|defined
argument_list|(
name|MCL_CURRENT
argument_list|)
operator|&&
name|defined
argument_list|(
name|MCL_FUTURE
argument_list|)
ifdef|#
directive|ifdef
name|HAVE_SETRLIMIT
comment|/* 	 * Set the stack limit to something smaller, so that we don't lock a lot 	 * of unused stack memory. 	 */
block|{
name|struct
name|rlimit
name|rl
decl_stmt|;
comment|/* HMS: must make the rlim_cur amount configurable */
if|if
condition|(
name|getrlimit
argument_list|(
name|RLIMIT_STACK
argument_list|,
operator|&
name|rl
argument_list|)
operator|!=
operator|-
literal|1
operator|&&
operator|(
name|rl
operator|.
name|rlim_cur
operator|=
literal|50
operator|*
literal|4096
operator|)
operator|<
name|rl
operator|.
name|rlim_max
condition|)
block|{
if|if
condition|(
name|setrlimit
argument_list|(
name|RLIMIT_STACK
argument_list|,
operator|&
name|rl
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot adjust stack limit for mlockall: %m"
argument_list|)
expr_stmt|;
block|}
block|}
ifdef|#
directive|ifdef
name|RLIMIT_MEMLOCK
comment|/* 	     * The default RLIMIT_MEMLOCK is very low on Linux systems. 	     * Unless we increase this limit malloc calls are likely to 	     * fail if we drop root privlege.  To be useful the value 	     * has to be larger than the largest ntpd resident set size. 	     */
name|rl
operator|.
name|rlim_cur
operator|=
name|rl
operator|.
name|rlim_max
operator|=
literal|32
operator|*
literal|1024
operator|*
literal|1024
expr_stmt|;
if|if
condition|(
name|setrlimit
argument_list|(
name|RLIMIT_MEMLOCK
argument_list|,
operator|&
name|rl
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot set RLIMIT_MEMLOCK: %m"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* RLIMIT_MEMLOCK */
block|}
endif|#
directive|endif
comment|/* HAVE_SETRLIMIT */
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|mlockall
argument_list|(
name|MCL_CURRENT
operator||
name|MCL_FUTURE
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"mlockall(): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not (HAVE_MLOCKALL&& MCL_CURRENT&& MCL_FUTURE) */
ifdef|#
directive|ifdef
name|HAVE_PLOCK
ifdef|#
directive|ifdef
name|PROCLOCK
ifdef|#
directive|ifdef
name|_AIX
comment|/*  	 * set the stack limit for AIX for plock(). 	 * see get_aix_stack() for more info. 	 */
if|if
condition|(
name|ulimit
argument_list|(
name|SET_STACKLIM
argument_list|,
operator|(
name|get_aix_stack
argument_list|()
operator|-
literal|8
operator|*
literal|4096
operator|)
argument_list|)
operator|<
literal|0
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot adjust stack limit for plock on AIX: %m"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* _AIX */
comment|/* 	 * lock the process into memory 	 */
if|if
condition|(
name|plock
argument_list|(
name|PROCLOCK
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock(PROCLOCK): %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not PROCLOCK */
ifdef|#
directive|ifdef
name|TXTLOCK
comment|/* 	 * Lock text into ram 	 */
if|if
condition|(
name|plock
argument_list|(
name|TXTLOCK
argument_list|)
operator|<
literal|0
condition|)
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock(TXTLOCK) error: %m"
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* not TXTLOCK */
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"plock() - don't know what to lock!"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* not TXTLOCK */
endif|#
directive|endif
comment|/* not PROCLOCK */
endif|#
directive|endif
comment|/* HAVE_PLOCK */
endif|#
directive|endif
comment|/* not (HAVE_MLOCKALL&& MCL_CURRENT&& MCL_FUTURE) */
comment|/* 	 * Set up signals we pay attention to locally. 	 */
ifdef|#
directive|ifdef
name|SIGDIE1
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE1
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE1 */
ifdef|#
directive|ifdef
name|SIGDIE2
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE2
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE2 */
ifdef|#
directive|ifdef
name|SIGDIE3
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE3
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE3 */
ifdef|#
directive|ifdef
name|SIGDIE4
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGDIE4
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGDIE4 */
ifdef|#
directive|ifdef
name|SIGBUS
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGBUS
argument_list|,
name|finish
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGBUS */
if|#
directive|if
operator|!
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|&&
operator|!
name|defined
argument_list|(
name|VMS
argument_list|)
ifdef|#
directive|ifdef
name|DEBUG
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|moredebug
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|lessdebug
argument_list|)
expr_stmt|;
else|#
directive|else
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|MOREDEBUGSIG
argument_list|,
name|no_debug
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|LESSDEBUGSIG
argument_list|,
name|no_debug
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* DEBUG */
endif|#
directive|endif
comment|/* !SYS_WINNT&& !VMS */
comment|/* 	 * Set up signals we should never pay attention to. 	 */
if|#
directive|if
name|defined
name|SIGPIPE
operator|(
name|void
operator|)
name|signal_no_reset
argument_list|(
name|SIGPIPE
argument_list|,
name|SIG_IGN
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGPIPE */
comment|/* 	 * Call the init_ routines to initialize the data structures. 	 * 	 * Exactly what command-line options are we expecting here? 	 */
name|init_auth
argument_list|()
expr_stmt|;
name|init_util
argument_list|()
expr_stmt|;
name|init_restrict
argument_list|()
expr_stmt|;
name|init_mon
argument_list|()
expr_stmt|;
name|init_timer
argument_list|()
expr_stmt|;
name|init_lib
argument_list|()
expr_stmt|;
name|init_request
argument_list|()
expr_stmt|;
name|init_control
argument_list|()
expr_stmt|;
name|init_peer
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|REFCLOCK
name|init_refclock
argument_list|()
expr_stmt|;
endif|#
directive|endif
name|set_process_priority
argument_list|()
expr_stmt|;
name|init_proto
argument_list|()
expr_stmt|;
comment|/* Call at high priority */
name|init_io
argument_list|()
expr_stmt|;
name|init_loopfilter
argument_list|()
expr_stmt|;
name|mon_start
argument_list|(
name|MON_ON
argument_list|)
expr_stmt|;
comment|/* monitor on by default now	  */
comment|/* turn off in config if unwanted */
comment|/* 	 * Get the configuration.  This is done in a separate module 	 * since this will definitely be different for the gizmo board. 	 */
name|getconfig
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|loop_config
argument_list|(
name|LOOP_DRIFTCOMP
argument_list|,
name|old_drift
operator|/
literal|1e6
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|OPENSSL
name|crypto_setup
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* OPENSSL */
name|initializing
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|HAVE_DROPROOT
if|if
condition|(
name|droproot
condition|)
block|{
comment|/* Drop super-user privileges and chroot now if the OS supports this */
ifdef|#
directive|ifdef
name|HAVE_LINUX_CAPABILITIES
comment|/* set flag: keep privileges accross setuid() call (we only really need cap_sys_time): */
if|if
condition|(
name|prctl
argument_list|(
name|PR_SET_KEEPCAPS
argument_list|,
literal|1L
argument_list|,
literal|0L
argument_list|,
literal|0L
argument_list|,
literal|0L
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"prctl( PR_SET_KEEPCAPS, 1L ) failed: %m"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
comment|/* we need a user to switch to */
if|if
condition|(
name|user
operator|==
name|NULL
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Need user name to drop root privileges (see -u flag!)"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_LINUX_CAPABILITIES */
if|if
condition|(
name|user
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|isdigit
argument_list|(
operator|(
name|unsigned
name|char
operator|)
operator|*
name|user
argument_list|)
condition|)
block|{
name|sw_uid
operator|=
operator|(
name|uid_t
operator|)
name|strtoul
argument_list|(
name|user
argument_list|,
operator|&
name|endp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|endp
operator|!=
literal|'\0'
condition|)
goto|goto
name|getuser
goto|;
block|}
else|else
block|{
name|getuser
label|:
if|if
condition|(
operator|(
name|pw
operator|=
name|getpwnam
argument_list|(
name|user
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
name|sw_uid
operator|=
name|pw
operator|->
name|pw_uid
expr_stmt|;
block|}
else|else
block|{
name|errno
operator|=
literal|0
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot find user `%s'"
argument_list|,
name|user
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
block|}
if|if
condition|(
name|group
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|isdigit
argument_list|(
operator|(
name|unsigned
name|char
operator|)
operator|*
name|group
argument_list|)
condition|)
block|{
name|sw_gid
operator|=
operator|(
name|gid_t
operator|)
name|strtoul
argument_list|(
name|group
argument_list|,
operator|&
name|endp
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|endp
operator|!=
literal|'\0'
condition|)
goto|goto
name|getgroup
goto|;
block|}
else|else
block|{
name|getgroup
label|:
if|if
condition|(
operator|(
name|gr
operator|=
name|getgrnam
argument_list|(
name|group
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
name|sw_gid
operator|=
name|gr
operator|->
name|gr_gid
expr_stmt|;
block|}
else|else
block|{
name|errno
operator|=
literal|0
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot find group `%s'"
argument_list|,
name|group
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
block|}
if|if
condition|(
name|chrootdir
condition|)
block|{
comment|/* make sure cwd is inside the jail: */
if|if
condition|(
name|chdir
argument_list|(
name|chrootdir
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot chdir() to `%s': %m"
argument_list|,
name|chrootdir
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|chroot
argument_list|(
name|chrootdir
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot chroot() to `%s': %m"
argument_list|,
name|chrootdir
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
block|}
if|if
condition|(
name|group
operator|&&
name|setgid
argument_list|(
name|sw_gid
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot setgid() to group `%s': %m"
argument_list|,
name|group
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|group
operator|&&
name|setegid
argument_list|(
name|sw_gid
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot setegid() to group `%s': %m"
argument_list|,
name|group
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|user
operator|&&
name|setuid
argument_list|(
name|sw_uid
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot setuid() to user `%s': %m"
argument_list|,
name|user
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|user
operator|&&
name|seteuid
argument_list|(
name|sw_uid
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Cannot seteuid() to user `%s': %m"
argument_list|,
name|user
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
ifndef|#
directive|ifndef
name|HAVE_LINUX_CAPABILITIES
comment|/* 		 * for now assume that the privilege to bind to privileged ports 		 * is associated with running with uid 0 - should be refined on 		 * ports that allow binding to NTP_PORT with uid != 0 		 */
name|disable_dynamic_updates
operator||=
operator|(
name|sw_uid
operator|!=
literal|0
operator|)
expr_stmt|;
comment|/* also notifies routing message listener */
endif|#
directive|endif
if|if
condition|(
name|disable_dynamic_updates
operator|&&
name|interface_interval
condition|)
block|{
name|interface_interval
operator|=
literal|0
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_INFO
argument_list|,
literal|"running in unprivileged mode disables dynamic interface tracking"
argument_list|)
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|HAVE_LINUX_CAPABILITIES
do|do
block|{
comment|/* 			 *  We may be running under non-root uid now, but we still hold full root privileges! 			 *  We drop all of them, except for the crucial one or two: cap_sys_time and 			 *  cap_net_bind_service if doing dynamic interface tracking. 			 */
name|cap_t
name|caps
decl_stmt|;
name|char
modifier|*
name|captext
init|=
name|interface_interval
condition|?
literal|"cap_sys_time,cap_net_bind_service=ipe"
else|:
literal|"cap_sys_time=ipe"
decl_stmt|;
if|if
condition|(
operator|!
operator|(
name|caps
operator|=
name|cap_from_text
argument_list|(
name|captext
argument_list|)
operator|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"cap_from_text() failed: %m"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|cap_set_proc
argument_list|(
name|caps
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"cap_set_proc() failed to drop root privileges: %m"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
block|}
name|cap_free
argument_list|(
name|caps
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
literal|0
condition|)
do|;
endif|#
directive|endif
comment|/* HAVE_LINUX_CAPABILITIES */
block|}
comment|/* if( droproot ) */
endif|#
directive|endif
comment|/* HAVE_DROPROOT */
comment|/* 	 * Report that we're up to any trappers 	 */
name|report_event
argument_list|(
name|EVNT_SYSRESTART
argument_list|,
operator|(
expr|struct
name|peer
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
comment|/* 	 * Use select() on all on all input fd's for unlimited 	 * time.  select() will terminate on SIGALARM or on the 	 * reception of input.	Using select() means we can't do 	 * robust signal handling and we get a potential race 	 * between checking for alarms and doing the select(). 	 * Mostly harmless, I think. 	 */
comment|/* On VMS, I suspect that select() can't be interrupted 	 * by a "signal" either, so I take the easy way out and 	 * have select() time out after one second. 	 * System clock updates really aren't time-critical, 	 * and - lacking a hardware reference clock - I have 	 * yet to learn about anything else that is. 	 */
if|#
directive|if
name|defined
argument_list|(
name|HAVE_IO_COMPLETION_PORT
argument_list|)
for|for
control|(
init|;
condition|;
control|)
block|{
name|GetReceivedBuffers
argument_list|()
expr_stmt|;
else|#
directive|else
comment|/* normal I/O */
name|BLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
name|was_alarmed
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|#
directive|if
operator|!
name|defined
argument_list|(
name|HAVE_SIGNALED_IO
argument_list|)
specifier|extern
name|fd_set
name|activefds
decl_stmt|;
specifier|extern
name|int
name|maxactivefd
decl_stmt|;
name|fd_set
name|rdfdes
decl_stmt|;
name|int
name|nfound
decl_stmt|;
endif|#
directive|endif
if|if
condition|(
name|alarm_flag
condition|)
comment|/* alarmed? */
block|{
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|was_alarmed
operator|&&
name|has_full_recv_buffer
argument_list|()
operator|==
name|ISC_FALSE
condition|)
block|{
comment|/* 			 * Nothing to do.  Wait for something. 			 */
ifndef|#
directive|ifndef
name|HAVE_SIGNALED_IO
name|rdfdes
operator|=
name|activefds
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|VMS
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_VXWORKS
argument_list|)
comment|/* make select() wake up after one second */
block|{
name|struct
name|timeval
name|t1
decl_stmt|;
name|t1
operator|.
name|tv_sec
operator|=
literal|1
expr_stmt|;
name|t1
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
name|nfound
operator|=
name|select
argument_list|(
name|maxactivefd
operator|+
literal|1
argument_list|,
operator|&
name|rdfdes
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|&
name|t1
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
name|nfound
operator|=
name|select
argument_list|(
name|maxactivefd
operator|+
literal|1
argument_list|,
operator|&
name|rdfdes
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
name|fd_set
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|timeval
operator|*
operator|)
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* VMS */
if|if
condition|(
name|nfound
operator|>
literal|0
condition|)
block|{
name|l_fp
name|ts
decl_stmt|;
name|get_systime
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|input_handler
argument_list|(
operator|&
name|ts
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|nfound
operator|==
operator|-
literal|1
operator|&&
name|errno
operator|!=
name|EINTR
condition|)
name|netsyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"select() error: %m"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
elseif|else
if|if
condition|(
name|debug
operator|>
literal|5
condition|)
name|netsyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"select(): nfound=%d, error: %m"
argument_list|,
name|nfound
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* DEBUG */
else|#
directive|else
comment|/* HAVE_SIGNALED_IO */
name|wait_for_signal
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* HAVE_SIGNALED_IO */
if|if
condition|(
name|alarm_flag
condition|)
comment|/* alarmed? */
block|{
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
block|}
if|if
condition|(
name|was_alarmed
condition|)
block|{
name|UNBLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
comment|/* 			 * Out here, signals are unblocked.  Call timer routine 			 * to process expiry. 			 */
name|timer
argument_list|()
expr_stmt|;
name|was_alarmed
operator|=
literal|0
expr_stmt|;
name|BLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* HAVE_IO_COMPLETION_PORT */
ifdef|#
directive|ifdef
name|DEBUG_TIMING
block|{
name|l_fp
name|pts
decl_stmt|;
name|l_fp
name|tsa
decl_stmt|,
name|tsb
decl_stmt|;
name|int
name|bufcount
init|=
literal|0
decl_stmt|;
name|get_systime
argument_list|(
operator|&
name|pts
argument_list|)
expr_stmt|;
name|tsa
operator|=
name|pts
expr_stmt|;
endif|#
directive|endif
name|rbuf
operator|=
name|get_full_recv_buffer
argument_list|()
expr_stmt|;
while|while
condition|(
name|rbuf
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|alarm_flag
condition|)
block|{
name|was_alarmed
operator|=
literal|1
expr_stmt|;
name|alarm_flag
operator|=
literal|0
expr_stmt|;
block|}
name|UNBLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
if|if
condition|(
name|was_alarmed
condition|)
block|{
comment|/* avoid timer starvation during lengthy I/O handling */
name|timer
argument_list|()
expr_stmt|;
name|was_alarmed
operator|=
literal|0
expr_stmt|;
block|}
comment|/* 				 * Call the data procedure to handle each received 				 * packet. 				 */
if|if
condition|(
name|rbuf
operator|->
name|receiver
operator|!=
name|NULL
condition|)
comment|/* This should always be true */
block|{
ifdef|#
directive|ifdef
name|DEBUG_TIMING
name|l_fp
name|dts
init|=
name|pts
decl_stmt|;
name|L_SUB
argument_list|(
operator|&
name|dts
argument_list|,
operator|&
name|rbuf
operator|->
name|recv_time
argument_list|)
expr_stmt|;
name|DPRINTF
argument_list|(
literal|2
argument_list|,
operator|(
literal|"processing timestamp delta %s (with prec. fuzz)\n"
operator|,
name|lfptoa
argument_list|(
operator|&
name|dts
argument_list|,
literal|9
argument_list|)
operator|)
argument_list|)
expr_stmt|;
name|collect_timing
argument_list|(
name|rbuf
argument_list|,
literal|"buffer processing delay"
argument_list|,
literal|1
argument_list|,
operator|&
name|dts
argument_list|)
expr_stmt|;
name|bufcount
operator|++
expr_stmt|;
endif|#
directive|endif
call|(
name|rbuf
operator|->
name|receiver
call|)
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"receive buffer corruption - receiver found to be NULL - ABORTING"
argument_list|)
expr_stmt|;
name|abort
argument_list|()
expr_stmt|;
block|}
name|BLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
name|freerecvbuf
argument_list|(
name|rbuf
argument_list|)
expr_stmt|;
name|rbuf
operator|=
name|get_full_recv_buffer
argument_list|()
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|DEBUG_TIMING
name|get_systime
argument_list|(
operator|&
name|tsb
argument_list|)
expr_stmt|;
name|L_SUB
argument_list|(
operator|&
name|tsb
argument_list|,
operator|&
name|tsa
argument_list|)
expr_stmt|;
if|if
condition|(
name|bufcount
condition|)
block|{
name|collect_timing
argument_list|(
name|NULL
argument_list|,
literal|"processing"
argument_list|,
name|bufcount
argument_list|,
operator|&
name|tsb
argument_list|)
expr_stmt|;
name|DPRINTF
argument_list|(
literal|2
argument_list|,
operator|(
literal|"processing time for %d buffers %s\n"
operator|,
name|bufcount
operator|,
name|lfptoa
argument_list|(
operator|&
name|tsb
argument_list|,
literal|9
argument_list|)
operator|)
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* 		 * Go around again 		 */
block|}
name|UNBLOCK_IO_AND_ALARM
argument_list|()
expr_stmt|;
return|return
literal|1
return|;
block|}
ifdef|#
directive|ifdef
name|SIGDIE2
comment|/*  * finish - exit gracefully  */
specifier|static
name|RETSIGTYPE
name|finish
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|msyslog
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"ntpd exiting on signal %d"
argument_list|,
name|sig
argument_list|)
expr_stmt|;
name|write_stats
argument_list|()
expr_stmt|;
ifdef|#
directive|ifdef
name|HAVE_DNSREGISTRATION
if|if
condition|(
name|mdns
operator|!=
name|NULL
condition|)
name|DNSServiceRefDeallocate
argument_list|(
name|mdns
argument_list|)
expr_stmt|;
endif|#
directive|endif
switch|switch
condition|(
name|sig
condition|)
block|{
ifdef|#
directive|ifdef
name|SIGBUS
case|case
name|SIGBUS
case|:
name|printf
argument_list|(
literal|"\nfinish(SIGBUS)\n"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
endif|#
directive|endif
case|case
literal|0
case|:
comment|/* Should never happen... */
return|return;
default|default:
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* SIGDIE2 */
ifdef|#
directive|ifdef
name|DEBUG
ifndef|#
directive|ifndef
name|SYS_WINNT
comment|/*  * moredebug - increase debugging verbosity  */
specifier|static
name|RETSIGTYPE
name|moredebug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
if|if
condition|(
name|debug
operator|<
literal|255
condition|)
block|{
name|debug
operator|++
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug raised to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
comment|/*  * lessdebug - decrease debugging verbosity  */
specifier|static
name|RETSIGTYPE
name|lessdebug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
if|if
condition|(
name|debug
operator|>
literal|0
condition|)
block|{
name|debug
operator|--
expr_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"debug lowered to %d"
argument_list|,
name|debug
argument_list|)
expr_stmt|;
block|}
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
endif|#
directive|endif
else|#
directive|else
comment|/* not DEBUG */
ifndef|#
directive|ifndef
name|SYS_WINNT
comment|/*  * no_debug - We don't do the debug here.  */
specifier|static
name|RETSIGTYPE
name|no_debug
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|saved_errno
init|=
name|errno
decl_stmt|;
name|msyslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"ntpd not compiled for debugging (signal %d)"
argument_list|,
name|sig
argument_list|)
expr_stmt|;
name|errno
operator|=
name|saved_errno
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not SYS_WINNT */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not DEBUG */
end_comment

end_unit

