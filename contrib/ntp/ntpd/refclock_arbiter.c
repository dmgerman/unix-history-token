begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * refclock_arbiter - clock driver for Arbiter 1088A/B Satellite  *	Controlled Clock  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|REFCLOCK
argument_list|)
operator|&&
name|defined
argument_list|(
name|CLOCK_ARBITER
argument_list|)
end_if

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_refclock.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_comment
comment|/*  * This driver supports the Arbiter 1088A/B Satellite Controlled Clock.  * The claimed accuracy of this clock is 100 ns relative to the PPS  * output when receiving four or more satellites.  *  * The receiver should be configured before starting the NTP daemon, in  * order to establish reliable position and operating conditions. It  * does not initiate surveying or hold mode. For use with NTP, the  * daylight savings time feature should be disables (D0 command) and the  * broadcast mode set to operate in UTC (BU command).  *  * The timecode format supported by this driver is selected by the poll  * sequence "B5", which initiates a line in the following format to be  * repeated once per second until turned off by the "B0" poll sequence.  *  * Format B5 (24 ASCII printing characters):  *  *<cr><lf>i yy ddd hh:mm:ss.000bbb    *  *	on-time =<cr>  *	i = synchronization flag (' ' = locked, '?' = unlocked)  *	yy = year of century  *	ddd = day of year  *	hh:mm:ss = hours, minutes, seconds  *	.000 = fraction of second (not used)  *	bbb = tailing spaces for fill  *  * The alarm condition is indicated by a '?' at i, which indicates the  * receiver is not synchronized. In normal operation, a line consisting  * of the timecode followed by the time quality character (TQ) followed  * by the receiver status string (SR) is written to the clockstats file.  * The time quality character is encoded in IEEE P1344 standard:  *  * Format TQ (IEEE P1344 estimated worst-case time quality)  *  *	0	clock locked, maximum accuracy  *	F	clock failure, time not reliable  *	4	clock unlocked, accuracy< 1 us  *	5	clock unlocked, accuracy< 10 us  *	6	clock unlocked, accuracy< 100 us  *	7	clock unlocked, accuracy< 1 ms  *	8	clock unlocked, accuracy< 10 ms  *	9	clock unlocked, accuracy< 100 ms  *	A	clock unlocked, accuracy< 1 s  *	B	clock unlocked, accuracy< 10 s  *  * The status string is encoded as follows:  *  * Format SR (25 ASCII printing characters)  *  *	V=vv S=ss T=t P=pdop E=ee  *  *	vv = satellites visible  *	ss = relative signal strength  *	t = satellites tracked  *	pdop = position dilution of precision (meters)  *	ee = hardware errors  *  * If flag4 is set, an additional line consisting of the receiver  * latitude (LA), longitude (LO) and elevation (LH) (meters) is written  * to this file. If channel B is enabled for deviation mode and connected  * to a 1-PPS signal, the last two numbers on the line are the deviation  * and standard deviation averaged over the last 15 seconds.  */
end_comment

begin_comment
comment|/*  * Interface definitions  */
end_comment

begin_define
define|#
directive|define
name|DEVICE
value|"/dev/gps%d"
end_define

begin_comment
comment|/* device name and unit */
end_comment

begin_define
define|#
directive|define
name|SPEED232
value|B9600
end_define

begin_comment
comment|/* uart speed (9600 baud) */
end_comment

begin_define
define|#
directive|define
name|PRECISION
value|(-20)
end_define

begin_comment
comment|/* precision assumed (about 1 us) */
end_comment

begin_define
define|#
directive|define
name|REFID
value|"GPS "
end_define

begin_comment
comment|/* reference ID */
end_comment

begin_define
define|#
directive|define
name|DESCRIPTION
value|"Arbiter 1088A/B GPS Receiver"
end_define

begin_comment
comment|/* WRU */
end_comment

begin_define
define|#
directive|define
name|LENARB
value|24
end_define

begin_comment
comment|/* format B5 timecode length */
end_comment

begin_define
define|#
directive|define
name|MAXSTA
value|30
end_define

begin_comment
comment|/* max length of status string */
end_comment

begin_define
define|#
directive|define
name|MAXPOS
value|70
end_define

begin_comment
comment|/* max length of position string */
end_comment

begin_comment
comment|/*  * ARB unit control structure  */
end_comment

begin_struct
struct|struct
name|arbunit
block|{
name|l_fp
name|laststamp
decl_stmt|;
comment|/* last receive timestamp */
name|int
name|tcswitch
decl_stmt|;
comment|/* timecode switch/counter */
name|char
name|qualchar
decl_stmt|;
comment|/* IEEE P1344 quality (TQ command) */
name|char
name|status
index|[
name|MAXSTA
index|]
decl_stmt|;
comment|/* receiver status (SR command) */
name|char
name|latlon
index|[
name|MAXPOS
index|]
decl_stmt|;
comment|/* receiver position (lat/lon/alt) */
block|}
struct|;
end_struct

begin_comment
comment|/*  * Function prototypes  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|arb_start
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|arb_shutdown
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|arb_receive
name|P
argument_list|(
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|arb_poll
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Transfer vector  */
end_comment

begin_decl_stmt
name|struct
name|refclock
name|refclock_arbiter
init|=
block|{
name|arb_start
block|,
comment|/* start up driver */
name|arb_shutdown
block|,
comment|/* shut down driver */
name|arb_poll
block|,
comment|/* transmit poll message */
name|noentry
block|,
comment|/* not used (old arb_control) */
name|noentry
block|,
comment|/* initialize driver (not used) */
name|noentry
block|,
comment|/* not used (old arb_buginfo) */
name|NOFLAGS
comment|/* not used */
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * arb_start - open the devices and initialize data for processing  */
end_comment

begin_function
specifier|static
name|int
name|arb_start
parameter_list|(
name|int
name|unit
parameter_list|,
name|struct
name|peer
modifier|*
name|peer
parameter_list|)
block|{
specifier|register
name|struct
name|arbunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|char
name|device
index|[
literal|20
index|]
decl_stmt|;
comment|/* 	 * Open serial port. Use CLK line discipline, if available. 	 */
operator|(
name|void
operator|)
name|sprintf
argument_list|(
name|device
argument_list|,
name|DEVICE
argument_list|,
name|unit
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|fd
operator|=
name|refclock_open
argument_list|(
name|device
argument_list|,
name|SPEED232
argument_list|,
name|LDISC_CLK
argument_list|)
operator|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* 	 * Allocate and initialize unit structure 	 */
if|if
condition|(
operator|!
operator|(
name|up
operator|=
operator|(
expr|struct
name|arbunit
operator|*
operator|)
name|emalloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|arbunit
argument_list|)
argument_list|)
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|memset
argument_list|(
operator|(
name|char
operator|*
operator|)
name|up
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|arbunit
argument_list|)
argument_list|)
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|clock_recv
operator|=
name|arb_receive
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|srcclock
operator|=
operator|(
name|caddr_t
operator|)
name|peer
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|datalen
operator|=
literal|0
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|fd
operator|=
name|fd
expr_stmt|;
if|if
condition|(
operator|!
name|io_addclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|pp
operator|->
name|unitptr
operator|=
operator|(
name|caddr_t
operator|)
name|up
expr_stmt|;
comment|/* 	 * Initialize miscellaneous variables 	 */
name|peer
operator|->
name|precision
operator|=
name|PRECISION
expr_stmt|;
name|pp
operator|->
name|clockdesc
operator|=
name|DESCRIPTION
expr_stmt|;
name|memcpy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|pp
operator|->
name|refid
argument_list|,
name|REFID
argument_list|,
literal|4
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * arb_shutdown - shut down the clock  */
end_comment

begin_function
specifier|static
name|void
name|arb_shutdown
parameter_list|(
name|int
name|unit
parameter_list|,
name|struct
name|peer
modifier|*
name|peer
parameter_list|)
block|{
specifier|register
name|struct
name|arbunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|arbunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|io_closeclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * arb_receive - receive data from the serial interface  */
end_comment

begin_function
specifier|static
name|void
name|arb_receive
parameter_list|(
name|struct
name|recvbuf
modifier|*
name|rbufp
parameter_list|)
block|{
specifier|register
name|struct
name|arbunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|l_fp
name|trtmp
decl_stmt|;
name|int
name|temp
decl_stmt|;
name|u_char
name|syncchar
decl_stmt|;
comment|/* synchronization indicator */
comment|/* 	 * Initialize pointers and read the timecode and timestamp 	 */
name|peer
operator|=
operator|(
expr|struct
name|peer
operator|*
operator|)
name|rbufp
operator|->
name|recv_srcclock
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|arbunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|temp
operator|=
name|refclock_gtlin
argument_list|(
name|rbufp
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|,
name|BMAX
argument_list|,
operator|&
name|trtmp
argument_list|)
expr_stmt|;
comment|/* 	 * Note we get a buffer and timestamp for both a<cr> and<lf>, 	 * but only the<cr> timestamp is retained. The program first 	 * sends a TQ and expects the echo followed by the time quality 	 * character. It then sends a B5 starting the timecode broadcast 	 * and expects the echo followed some time later by the on-time 	 * character<cr> and then the<lf> beginning the timecode 	 * itself. Finally, at the<cr> beginning the next timecode at 	 * the next second, the program sends a B0 shutting down the 	 * timecode broadcast. 	 * 	 * If flag4 is set, the program snatches the latitude, longitude 	 * and elevation and writes it to the clockstats file. 	 */
if|if
condition|(
name|temp
operator|==
literal|0
condition|)
return|return;
name|pp
operator|->
name|lastrec
operator|=
name|up
operator|->
name|laststamp
expr_stmt|;
name|up
operator|->
name|laststamp
operator|=
name|trtmp
expr_stmt|;
if|if
condition|(
name|temp
operator|<
literal|3
condition|)
return|return;
if|if
condition|(
name|up
operator|->
name|tcswitch
operator|==
literal|0
condition|)
block|{
comment|/* 		 * Collect statistics. If nothing is recogized, just 		 * ignore; sometimes the clock doesn't stop spewing 		 * timecodes for awhile after the B0 commant. 		 */
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"TQ"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|up
operator|->
name|qualchar
operator|=
name|pp
operator|->
name|a_lastcode
index|[
literal|2
index|]
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"SR"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"SR"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|strcpy
argument_list|(
name|up
operator|->
name|status
argument_list|,
name|pp
operator|->
name|a_lastcode
operator|+
literal|2
argument_list|)
expr_stmt|;
if|if
condition|(
name|pp
operator|->
name|sloppyclockflag
operator|&
name|CLK_FLAG4
condition|)
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"LA"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
else|else
block|{
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B5"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|up
operator|->
name|tcswitch
operator|++
expr_stmt|;
block|}
block|}
elseif|else
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"LA"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|strcpy
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
name|pp
operator|->
name|a_lastcode
operator|+
literal|2
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"LO"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"LO"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
literal|" "
argument_list|)
expr_stmt|;
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
name|pp
operator|->
name|a_lastcode
operator|+
literal|2
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"LH"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"LH"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
literal|" "
argument_list|)
expr_stmt|;
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
name|pp
operator|->
name|a_lastcode
operator|+
literal|2
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"DB"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|strncmp
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"DB"
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
literal|" "
argument_list|)
expr_stmt|;
name|strcat
argument_list|(
name|up
operator|->
name|latlon
argument_list|,
name|pp
operator|->
name|a_lastcode
operator|+
literal|2
argument_list|)
expr_stmt|;
name|record_clock_stats
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|,
name|up
operator|->
name|latlon
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B5"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|up
operator|->
name|tcswitch
operator|++
expr_stmt|;
block|}
return|return;
block|}
name|pp
operator|->
name|lencode
operator|=
name|temp
expr_stmt|;
comment|/* 	 * We get down to business, check the timecode format and decode 	 * its contents. If the timecode has valid length, but not in 	 * proper format, we declare bad format and exit. If the 	 * timecode has invalid length, which sometimes occurs when the 	 * B0 amputates the broadcast, we just quietly steal away. Note 	 * that the time quality character and receiver status string is 	 * tacked on the end for clockstats display.  	 */
if|if
condition|(
name|pp
operator|->
name|lencode
operator|==
name|LENARB
condition|)
block|{
comment|/* 		 * Timecode format B5: "i yy ddd hh:mm:ss.000   " 		 */
name|pp
operator|->
name|a_lastcode
index|[
name|LENARB
operator|-
literal|2
index|]
operator|=
name|up
operator|->
name|qualchar
expr_stmt|;
name|strcat
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
name|up
operator|->
name|status
argument_list|)
expr_stmt|;
name|syncchar
operator|=
literal|' '
expr_stmt|;
if|if
condition|(
name|sscanf
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"%c%2d %3d %2d:%2d:%2d"
argument_list|,
operator|&
name|syncchar
argument_list|,
operator|&
name|pp
operator|->
name|year
argument_list|,
operator|&
name|pp
operator|->
name|day
argument_list|,
operator|&
name|pp
operator|->
name|hour
argument_list|,
operator|&
name|pp
operator|->
name|minute
argument_list|,
operator|&
name|pp
operator|->
name|second
argument_list|)
operator|!=
literal|6
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADREPLY
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
return|return;
block|}
block|}
else|else
block|{
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
return|return;
block|}
name|up
operator|->
name|tcswitch
operator|++
expr_stmt|;
comment|/* 	 * We decode the clock dispersion from the time quality 	 * character. 	 */
switch|switch
condition|(
name|up
operator|->
name|qualchar
condition|)
block|{
case|case
literal|'0'
case|:
comment|/* locked, max accuracy */
name|pp
operator|->
name|disp
operator|=
literal|1e-7
expr_stmt|;
break|break;
case|case
literal|'4'
case|:
comment|/* unlock accuracy< 1 us */
name|pp
operator|->
name|disp
operator|=
literal|1e-6
expr_stmt|;
break|break;
case|case
literal|'5'
case|:
comment|/* unlock accuracy< 10 us */
name|pp
operator|->
name|disp
operator|=
literal|1e-5
expr_stmt|;
break|break;
case|case
literal|'6'
case|:
comment|/* unlock accuracy< 100 us */
name|pp
operator|->
name|disp
operator|=
literal|1e-4
expr_stmt|;
break|break;
case|case
literal|'7'
case|:
comment|/* unlock accuracy< 1 ms */
name|pp
operator|->
name|disp
operator|=
literal|.001
expr_stmt|;
break|break;
case|case
literal|'8'
case|:
comment|/* unlock accuracy< 10 ms */
name|pp
operator|->
name|disp
operator|=
literal|.01
expr_stmt|;
break|break;
case|case
literal|'9'
case|:
comment|/* unlock accuracy< 100 ms */
name|pp
operator|->
name|disp
operator|=
literal|.1
expr_stmt|;
break|break;
case|case
literal|'A'
case|:
comment|/* unlock accuracy< 1 s */
name|pp
operator|->
name|disp
operator|=
literal|1
expr_stmt|;
break|break;
case|case
literal|'B'
case|:
comment|/* unlock accuracy< 10 s */
name|pp
operator|->
name|disp
operator|=
literal|10
expr_stmt|;
break|break;
case|case
literal|'F'
case|:
comment|/* clock failure */
name|pp
operator|->
name|disp
operator|=
name|MAXDISPERSE
expr_stmt|;
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_FAULT
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
return|return;
default|default:
name|pp
operator|->
name|disp
operator|=
name|MAXDISPERSE
expr_stmt|;
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADREPLY
argument_list|)
expr_stmt|;
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
name|syncchar
operator|!=
literal|' '
condition|)
name|pp
operator|->
name|leap
operator|=
name|LEAP_NOTINSYNC
expr_stmt|;
else|else
name|pp
operator|->
name|leap
operator|=
name|LEAP_NOWARNING
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"arbiter: timecode %d %s\n"
argument_list|,
name|pp
operator|->
name|lencode
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|up
operator|->
name|tcswitch
operator|>=
name|NSTAGE
condition|)
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"B0"
argument_list|,
literal|2
argument_list|)
expr_stmt|;
comment|/* 	 * Process the new sample in the median filter and determine the 	 * timecode timestamp. 	 */
if|if
condition|(
operator|!
name|refclock_process
argument_list|(
name|pp
argument_list|)
condition|)
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADTIME
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * arb_poll - called by the transmit procedure  */
end_comment

begin_function
specifier|static
name|void
name|arb_poll
parameter_list|(
name|int
name|unit
parameter_list|,
name|struct
name|peer
modifier|*
name|peer
parameter_list|)
block|{
specifier|register
name|struct
name|arbunit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
comment|/* 	 * Time to poll the clock. The Arbiter clock responds to a "B5" 	 * by returning a timecode in the format specified above. 	 * Transmission occurs once per second, unless turned off by a 	 * "B0". Note there is no checking on state, since this may not 	 * be the only customer reading the clock. Only one customer 	 * need poll the clock; all others just listen in. If nothing is 	 * heard from the clock for two polls, declare a timeout and 	 * keep going. 	 */
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|arbunit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|up
operator|->
name|tcswitch
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|write
argument_list|(
name|pp
operator|->
name|io
operator|.
name|fd
argument_list|,
literal|"TQ"
argument_list|,
literal|2
argument_list|)
operator|!=
literal|2
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_FAULT
argument_list|)
expr_stmt|;
block|}
else|else
name|pp
operator|->
name|polls
operator|++
expr_stmt|;
if|if
condition|(
name|pp
operator|->
name|coderecv
operator|==
name|pp
operator|->
name|codeproc
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_TIMEOUT
argument_list|)
expr_stmt|;
return|return;
block|}
name|record_clock_stats
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|)
expr_stmt|;
name|refclock_receive
argument_list|(
name|peer
argument_list|)
expr_stmt|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|int
name|refclock_arbiter_bs
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* REFCLOCK */
end_comment

end_unit

