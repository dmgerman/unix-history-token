begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * refclock_dumbclock - clock driver for a unknown time distribution system  * that only provides hh:mm:ss (in local time, yet!).  */
end_comment

begin_comment
comment|/*  * Must interpolate back to local time.  Very annoying.  */
end_comment

begin_define
define|#
directive|define
name|GET_LOCALTIME
end_define

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|defined
argument_list|(
name|REFCLOCK
argument_list|)
operator|&&
name|defined
argument_list|(
name|CLOCK_DUMBCLOCK
argument_list|)
end_if

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<time.h>
end_include

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_io.h"
end_include

begin_include
include|#
directive|include
file|"ntp_refclock.h"
end_include

begin_include
include|#
directive|include
file|"ntp_calendar.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_comment
comment|/*  * This driver supports a generic dumb clock that only outputs hh:mm:ss,  * in local time, no less.  *  * Input format:  *  *      hh:mm:ss<cr>  *  * hh:mm:ss -- what you'd expect, with a 24 hour clock.  (Heck, that's the only  * way it could get stupider.)  We take time on the<cr>.  *  * The original source of this module was the WWVB module.  */
end_comment

begin_comment
comment|/*  * Interface definitions  */
end_comment

begin_define
define|#
directive|define
name|DEVICE
value|"/dev/dumbclock%d"
end_define

begin_comment
comment|/* device name and unit */
end_comment

begin_define
define|#
directive|define
name|SPEED232
value|B9600
end_define

begin_comment
comment|/* uart speed (9600 baud) */
end_comment

begin_define
define|#
directive|define
name|PRECISION
value|(-13)
end_define

begin_comment
comment|/* precision assumed (about 100 us) */
end_comment

begin_define
define|#
directive|define
name|REFID
value|"dumbclock"
end_define

begin_comment
comment|/* reference ID */
end_comment

begin_define
define|#
directive|define
name|DESCRIPTION
value|"Dumb clock"
end_define

begin_comment
comment|/* WRU */
end_comment

begin_comment
comment|/*  * Insanity check.  Since the time is local, we need to make sure that during midnight  * transitions, we can convert back to Unix time.  If the conversion results in some number  * worse than this number of seconds away, assume the next day and retry.  */
end_comment

begin_define
define|#
directive|define
name|INSANE_SECONDS
value|3600
end_define

begin_comment
comment|/*  * Dumb clock control structure  */
end_comment

begin_struct
struct|struct
name|dumbclock_unit
block|{
name|u_char
name|tcswitch
decl_stmt|;
comment|/* timecode switch */
name|l_fp
name|laststamp
decl_stmt|;
comment|/* last receive timestamp */
name|u_char
name|lasthour
decl_stmt|;
comment|/* last hour (for monitor) */
name|u_char
name|linect
decl_stmt|;
comment|/* count ignored lines (for monitor */
name|struct
name|tm
name|ymd
decl_stmt|;
comment|/* struct tm for y/m/d only */
block|}
struct|;
end_struct

begin_comment
comment|/*  * Function prototypes  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|dumbclock_start
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|dumbclock_shutdown
name|P
argument_list|(
operator|(
name|int
operator|,
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|dumbclock_receive
name|P
argument_list|(
operator|(
expr|struct
name|recvbuf
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|static	void	dumbclock_poll		P((int, struct peer *));
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Transfer vector  */
end_comment

begin_decl_stmt
name|struct
name|refclock
name|refclock_dumbclock
init|=
block|{
name|dumbclock_start
block|,
comment|/* start up driver */
name|dumbclock_shutdown
block|,
comment|/* shut down driver */
name|noentry
block|,
comment|/* poll the driver -- a nice fabrication */
name|noentry
block|,
comment|/* not used */
name|noentry
block|,
comment|/* not used */
name|noentry
block|,
comment|/* not used */
name|NOFLAGS
comment|/* not used */
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * dumbclock_start - open the devices and initialize data for processing  */
end_comment

begin_function
specifier|static
name|int
name|dumbclock_start
parameter_list|(
name|int
name|unit
parameter_list|,
name|struct
name|peer
modifier|*
name|peer
parameter_list|)
block|{
specifier|register
name|struct
name|dumbclock_unit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|char
name|device
index|[
literal|20
index|]
decl_stmt|;
name|struct
name|tm
modifier|*
name|tm_time_p
decl_stmt|;
name|time_t
name|now
decl_stmt|;
comment|/* 	 * Open serial port. Don't bother with CLK line discipline, since 	 * it's not available. 	 */
operator|(
name|void
operator|)
name|sprintf
argument_list|(
name|device
argument_list|,
name|DEVICE
argument_list|,
name|unit
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"starting Dumbclock with device %s\n"
argument_list|,
name|device
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|!
operator|(
name|fd
operator|=
name|refclock_open
argument_list|(
name|device
argument_list|,
name|SPEED232
argument_list|,
literal|0
argument_list|)
operator|)
condition|)
return|return
operator|(
literal|0
operator|)
return|;
comment|/* 	 * Allocate and initialize unit structure 	 */
if|if
condition|(
operator|!
operator|(
name|up
operator|=
operator|(
expr|struct
name|dumbclock_unit
operator|*
operator|)
name|emalloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|dumbclock_unit
argument_list|)
argument_list|)
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|memset
argument_list|(
operator|(
name|char
operator|*
operator|)
name|up
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|dumbclock_unit
argument_list|)
argument_list|)
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|pp
operator|->
name|unitptr
operator|=
operator|(
name|caddr_t
operator|)
name|up
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|clock_recv
operator|=
name|dumbclock_receive
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|srcclock
operator|=
operator|(
name|caddr_t
operator|)
name|peer
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|datalen
operator|=
literal|0
expr_stmt|;
name|pp
operator|->
name|io
operator|.
name|fd
operator|=
name|fd
expr_stmt|;
if|if
condition|(
operator|!
name|io_addclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
name|time
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|GET_LOCALTIME
name|tm_time_p
operator|=
name|localtime
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
else|#
directive|else
name|tm_time_p
operator|=
name|gmtime
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|tm_time_p
condition|)
block|{
name|up
operator|->
name|ymd
operator|=
operator|*
name|tm_time_p
expr_stmt|;
block|}
else|else
block|{
return|return
literal|0
return|;
block|}
comment|/* 	 * Initialize miscellaneous variables 	 */
name|peer
operator|->
name|precision
operator|=
name|PRECISION
expr_stmt|;
name|pp
operator|->
name|clockdesc
operator|=
name|DESCRIPTION
expr_stmt|;
name|memcpy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|pp
operator|->
name|refid
argument_list|,
name|REFID
argument_list|,
literal|4
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * dumbclock_shutdown - shut down the clock  */
end_comment

begin_function
specifier|static
name|void
name|dumbclock_shutdown
parameter_list|(
name|int
name|unit
parameter_list|,
name|struct
name|peer
modifier|*
name|peer
parameter_list|)
block|{
specifier|register
name|struct
name|dumbclock_unit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|dumbclock_unit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|io_closeclock
argument_list|(
operator|&
name|pp
operator|->
name|io
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|up
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * dumbclock_receive - receive data from the serial interface  */
end_comment

begin_function
specifier|static
name|void
name|dumbclock_receive
parameter_list|(
name|struct
name|recvbuf
modifier|*
name|rbufp
parameter_list|)
block|{
name|struct
name|dumbclock_unit
modifier|*
name|up
decl_stmt|;
name|struct
name|refclockproc
modifier|*
name|pp
decl_stmt|;
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|l_fp
name|trtmp
decl_stmt|;
comment|/* arrival timestamp */
name|int
name|hours
decl_stmt|;
comment|/* hour-of-day */
name|int
name|minutes
decl_stmt|;
comment|/* minutes-past-the-hour */
name|int
name|seconds
decl_stmt|;
comment|/* seconds */
name|int
name|temp
decl_stmt|;
comment|/* int temp */
name|int
name|got_good
decl_stmt|;
comment|/* got a good time flag */
comment|/* 	 * Initialize pointers and read the timecode and timestamp 	 */
name|peer
operator|=
operator|(
expr|struct
name|peer
operator|*
operator|)
name|rbufp
operator|->
name|recv_srcclock
expr_stmt|;
name|pp
operator|=
name|peer
operator|->
name|procptr
expr_stmt|;
name|up
operator|=
operator|(
expr|struct
name|dumbclock_unit
operator|*
operator|)
name|pp
operator|->
name|unitptr
expr_stmt|;
name|temp
operator|=
name|refclock_gtlin
argument_list|(
name|rbufp
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|,
name|BMAX
argument_list|,
operator|&
name|trtmp
argument_list|)
expr_stmt|;
if|if
condition|(
name|temp
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|up
operator|->
name|tcswitch
operator|==
literal|0
condition|)
block|{
name|up
operator|->
name|tcswitch
operator|=
literal|1
expr_stmt|;
name|up
operator|->
name|laststamp
operator|=
name|trtmp
expr_stmt|;
block|}
else|else
name|up
operator|->
name|tcswitch
operator|=
literal|0
expr_stmt|;
return|return;
block|}
name|pp
operator|->
name|lencode
operator|=
name|temp
expr_stmt|;
name|pp
operator|->
name|lastrec
operator|=
name|up
operator|->
name|laststamp
expr_stmt|;
name|up
operator|->
name|laststamp
operator|=
name|trtmp
expr_stmt|;
name|up
operator|->
name|tcswitch
operator|=
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"dumbclock: timecode %d %s\n"
argument_list|,
name|pp
operator|->
name|lencode
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * We get down to business. Check the timecode format... 	 */
name|pp
operator|->
name|msec
operator|=
literal|0
expr_stmt|;
name|got_good
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|sscanf
argument_list|(
name|pp
operator|->
name|a_lastcode
argument_list|,
literal|"%02d:%02d:%02d"
argument_list|,
operator|&
name|hours
argument_list|,
operator|&
name|minutes
argument_list|,
operator|&
name|seconds
argument_list|)
operator|==
literal|3
condition|)
block|{
name|struct
name|tm
modifier|*
name|gmtp
decl_stmt|;
name|struct
name|tm
modifier|*
name|lt_p
decl_stmt|;
name|time_t
name|asserted_time
decl_stmt|;
comment|/* the SPM time based on the composite time+date */
name|struct
name|tm
name|asserted_tm
decl_stmt|;
comment|/* the struct tm of the same */
name|int
name|adjyear
decl_stmt|;
name|int
name|adjmon
decl_stmt|;
name|int
name|reality_delta
decl_stmt|;
name|time_t
name|now
decl_stmt|;
comment|/* 	     * Convert to GMT for sites that distribute localtime.  This              * means we have to figure out what day it is.  Easier said 	     * than done... 	     */
name|asserted_tm
operator|.
name|tm_year
operator|=
name|up
operator|->
name|ymd
operator|.
name|tm_year
expr_stmt|;
name|asserted_tm
operator|.
name|tm_mon
operator|=
name|up
operator|->
name|ymd
operator|.
name|tm_mon
expr_stmt|;
name|asserted_tm
operator|.
name|tm_mday
operator|=
name|up
operator|->
name|ymd
operator|.
name|tm_mday
expr_stmt|;
name|asserted_tm
operator|.
name|tm_hour
operator|=
name|hours
expr_stmt|;
name|asserted_tm
operator|.
name|tm_min
operator|=
name|minutes
expr_stmt|;
name|asserted_tm
operator|.
name|tm_sec
operator|=
name|seconds
expr_stmt|;
name|asserted_tm
operator|.
name|tm_isdst
operator|=
operator|-
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|GET_LOCALTIME
name|asserted_time
operator|=
name|mktime
argument_list|(
operator|&
name|asserted_tm
argument_list|)
expr_stmt|;
name|time
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
else|#
directive|else
include|#
directive|include
file|"GMT unsupported for dumbclock!"
endif|#
directive|endif
name|reality_delta
operator|=
name|asserted_time
operator|-
name|now
expr_stmt|;
comment|/* 	     * We assume that if the time is grossly wrong, it's because we got the 	     * year/month/day wrong. 	     */
if|if
condition|(
name|reality_delta
operator|>
name|INSANE_SECONDS
condition|)
block|{
name|asserted_time
operator|-=
name|SECSPERDAY
expr_stmt|;
comment|/* local clock behind real time */
block|}
elseif|else
if|if
condition|(
operator|-
name|reality_delta
operator|>
name|INSANE_SECONDS
condition|)
block|{
name|asserted_time
operator|+=
name|SECSPERDAY
expr_stmt|;
comment|/* local clock ahead of real time */
block|}
name|lt_p
operator|=
name|localtime
argument_list|(
operator|&
name|asserted_time
argument_list|)
expr_stmt|;
if|if
condition|(
name|lt_p
condition|)
block|{
name|up
operator|->
name|ymd
operator|=
operator|*
name|lt_p
expr_stmt|;
block|}
else|else
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_FAULT
argument_list|)
expr_stmt|;
return|return;
block|}
if|if
condition|(
operator|(
name|gmtp
operator|=
name|gmtime
argument_list|(
operator|&
name|asserted_time
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_FAULT
argument_list|)
expr_stmt|;
return|return;
block|}
name|adjyear
operator|=
name|gmtp
operator|->
name|tm_year
operator|+
literal|1900
expr_stmt|;
name|adjmon
operator|=
name|gmtp
operator|->
name|tm_mon
operator|+
literal|1
expr_stmt|;
name|pp
operator|->
name|day
operator|=
name|ymd2yd
argument_list|(
name|adjyear
argument_list|,
name|adjmon
argument_list|,
name|gmtp
operator|->
name|tm_mday
argument_list|)
expr_stmt|;
name|pp
operator|->
name|hour
operator|=
name|gmtp
operator|->
name|tm_hour
expr_stmt|;
name|pp
operator|->
name|minute
operator|=
name|gmtp
operator|->
name|tm_min
expr_stmt|;
name|pp
operator|->
name|second
operator|=
name|gmtp
operator|->
name|tm_sec
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"time is %04d/%02d/%02d %02d:%02d:%02d UTC\n"
argument_list|,
name|adjyear
argument_list|,
name|adjmon
argument_list|,
name|gmtp
operator|->
name|tm_mday
argument_list|,
name|pp
operator|->
name|hour
argument_list|,
name|pp
operator|->
name|minute
argument_list|,
name|pp
operator|->
name|second
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|got_good
operator|=
literal|1
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|got_good
condition|)
block|{
if|if
condition|(
name|up
operator|->
name|linect
operator|>
literal|0
condition|)
name|up
operator|->
name|linect
operator|--
expr_stmt|;
else|else
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADREPLY
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Process the new sample in the median filter and determine the 	 * timecode timestamp. 	 */
if|if
condition|(
operator|!
name|refclock_process
argument_list|(
name|pp
argument_list|)
condition|)
block|{
name|refclock_report
argument_list|(
name|peer
argument_list|,
name|CEVNT_BADTIME
argument_list|)
expr_stmt|;
return|return;
block|}
name|record_clock_stats
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|,
name|pp
operator|->
name|a_lastcode
argument_list|)
expr_stmt|;
name|refclock_receive
argument_list|(
name|peer
argument_list|)
expr_stmt|;
name|up
operator|->
name|lasthour
operator|=
name|pp
operator|->
name|hour
expr_stmt|;
block|}
end_function

begin_if
if|#
directive|if
literal|0
end_if

begin_comment
comment|/*  * dumbclock_poll - called by the transmit procedure  */
end_comment

begin_comment
unit|static void dumbclock_poll( 	int unit, 	struct peer *peer 	) { 	register struct dumbclock_unit *up; 	struct refclockproc *pp; 	char pollchar;
comment|/* 	 * Time to poll the clock. The Chrono-log clock is supposed to 	 * respond to a 'T' by returning a timecode in the format(s) 	 * specified above.  Ours does (can?) not, but this seems to be 	 * an installation-specific problem.  This code is dyked out, 	 * but may be re-enabled if anyone ever finds a Chrono-log that 	 * actually listens to this command. 	 */
end_comment

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|pp = peer->procptr; 	up = (struct dumbclock_unit *)pp->unitptr; 	if (peer->reach == 0) 		refclock_report(peer, CEVNT_TIMEOUT); 	if (up->linect> 0) 		pollchar = 'R'; 	else 		pollchar = 'T'; 	if (write(pp->io.fd,&pollchar, 1) != 1) 		refclock_report(peer, CEVNT_FAULT); 	else 		pp->polls++;
endif|#
directive|endif
end_endif

begin_endif
unit|}
endif|#
directive|endif
end_endif

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|int
name|refclock_dumbclock_bs
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* REFCLOCK */
end_comment

end_unit

