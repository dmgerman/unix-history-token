begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* machines.c - provide special support for peculiar architectures  *  * Real bummers unite !  *  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntp_machine.h"
end_include

begin_include
include|#
directive|include
file|"ntp_syslog.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|"ntp_unixtime.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_UNISTD_H
end_ifdef

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_WINNT
end_ifdef

begin_function_decl
name|int
name|_getch
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_comment
comment|/* Declare the one function rather than include conio.h */
end_comment

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_VXWORKS
end_ifdef

begin_include
include|#
directive|include
file|"taskLib.h"
end_include

begin_include
include|#
directive|include
file|"sysLib.h"
end_include

begin_include
include|#
directive|include
file|"time.h"
end_include

begin_include
include|#
directive|include
file|"ntp_syslog.h"
end_include

begin_comment
comment|/*	some translations to the world of vxWorkings -casey */
end_comment

begin_comment
comment|/* first some netdb type things */
end_comment

begin_include
include|#
directive|include
file|"ioLib.h"
end_include

begin_include
include|#
directive|include
file|<socket.h>
end_include

begin_decl_stmt
name|int
name|h_errno
decl_stmt|;
end_decl_stmt

begin_function
name|struct
name|hostent
modifier|*
name|gethostbyname
parameter_list|(
name|char
modifier|*
name|name
parameter_list|)
block|{
name|struct
name|hostent
modifier|*
name|host1
decl_stmt|;
name|h_errno
operator|=
literal|0
expr_stmt|;
comment|/* we are always successful!!! */
name|host1
operator|=
operator|(
expr|struct
name|hostent
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|hostent
argument_list|)
argument_list|)
expr_stmt|;
name|host1
operator|->
name|h_name
operator|=
name|name
expr_stmt|;
name|host1
operator|->
name|h_addrtype
operator|=
name|AF_INET
expr_stmt|;
name|host1
operator|->
name|h_aliases
operator|=
name|name
expr_stmt|;
name|host1
operator|->
name|h_length
operator|=
literal|4
expr_stmt|;
name|host1
operator|->
name|h_addr_list
index|[
literal|0
index|]
operator|=
operator|(
name|char
operator|*
operator|)
name|hostGetByName
argument_list|(
name|name
argument_list|)
expr_stmt|;
name|host1
operator|->
name|h_addr_list
index|[
literal|1
index|]
operator|=
name|NULL
expr_stmt|;
return|return
name|host1
return|;
block|}
end_function

begin_function
name|struct
name|hostent
modifier|*
name|gethostbyaddr
parameter_list|(
name|char
modifier|*
name|name
parameter_list|,
name|int
name|size
parameter_list|,
name|int
name|addr_type
parameter_list|)
block|{
name|struct
name|hostent
modifier|*
name|host1
decl_stmt|;
name|h_errno
operator|=
literal|0
expr_stmt|;
comment|/* we are always successful!!! */
name|host1
operator|=
operator|(
expr|struct
name|hostent
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|hostent
argument_list|)
argument_list|)
expr_stmt|;
name|host1
operator|->
name|h_name
operator|=
name|name
expr_stmt|;
name|host1
operator|->
name|h_addrtype
operator|=
name|AF_INET
expr_stmt|;
name|host1
operator|->
name|h_aliases
operator|=
name|name
expr_stmt|;
name|host1
operator|->
name|h_length
operator|=
literal|4
expr_stmt|;
name|host1
operator|->
name|h_addr_list
operator|=
name|NULL
expr_stmt|;
return|return
name|host1
return|;
block|}
end_function

begin_function
name|struct
name|servent
modifier|*
name|getservbyname
parameter_list|(
name|char
modifier|*
name|name
parameter_list|,
name|char
modifier|*
name|type
parameter_list|)
block|{
name|struct
name|servent
modifier|*
name|serv1
decl_stmt|;
name|serv1
operator|=
operator|(
expr|struct
name|servent
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|servent
argument_list|)
argument_list|)
expr_stmt|;
name|serv1
operator|->
name|s_name
operator|=
literal|"ntp"
expr_stmt|;
comment|/* official service name */
name|serv1
operator|->
name|s_aliases
operator|=
name|NULL
expr_stmt|;
comment|/* alias list */
name|serv1
operator|->
name|s_port
operator|=
literal|123
expr_stmt|;
comment|/* port # */
name|serv1
operator|->
name|s_proto
operator|=
literal|"udp"
expr_stmt|;
comment|/* protocol to use */
return|return
name|serv1
return|;
block|}
end_function

begin_comment
comment|/* second  * vxworks thinks it has insomnia  * we have to sleep for number of seconds  */
end_comment

begin_define
define|#
directive|define
name|CLKRATE
value|sysClkRateGet()
end_define

begin_comment
comment|/* I am not sure how valid the granularity is - it is from G. Eger's port */
end_comment

begin_define
define|#
directive|define
name|CLK_GRANULARITY
value|1
end_define

begin_comment
comment|/* Granularity of system clock in usec	*/
end_comment

begin_comment
comment|/* Used to round down # usecs/tick		*/
end_comment

begin_comment
comment|/* On a VCOM-100, PIT gets 8 MHz clk,	*/
end_comment

begin_comment
comment|/*& it prescales by 32, thus 4 usec	*/
end_comment

begin_comment
comment|/* on mv167, granularity is 1usec anyway*/
end_comment

begin_comment
comment|/* To defeat rounding, set to 1 		*/
end_comment

begin_define
define|#
directive|define
name|USECS_PER_SEC
value|MILLION
end_define

begin_comment
comment|/* Microseconds per second	*/
end_comment

begin_define
define|#
directive|define
name|TICK
value|(((USECS_PER_SEC / CLKRATE) / CLK_GRANULARITY) * CLK_GRANULARITY)
end_define

begin_comment
comment|/* emulate unix sleep  * casey  */
end_comment

begin_function
name|void
name|sleep
parameter_list|(
name|int
name|seconds
parameter_list|)
block|{
name|taskDelay
argument_list|(
name|seconds
operator|*
name|TICK
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* emulate unix alarm  * that pauses and calls SIGALRM after the seconds are up...  * so ... taskDelay() fudged for seconds should amount to the same thing.  * casey  */
end_comment

begin_function
name|void
name|alarm
parameter_list|(
name|int
name|seconds
parameter_list|)
block|{
name|sleep
argument_list|(
name|seconds
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_VXWORKS */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|SYS_PTX
end_ifdef

begin_comment
comment|/* Does PTX still need this? */
end_comment

begin_comment
comment|/*#include<sys/types.h>	*/
end_comment

begin_include
include|#
directive|include
file|<sys/procstats.h>
end_include

begin_function
name|int
name|gettimeofday
parameter_list|(
name|struct
name|timeval
modifier|*
name|tvp
parameter_list|)
block|{
comment|/* 	 * hi, this is Sequents sneak path to get to a clock 	 * this is also the most logical syscall for such a function 	 */
return|return
operator|(
name|get_process_stats
argument_list|(
name|tvp
argument_list|,
name|PS_SELF
argument_list|,
operator|(
expr|struct
name|procstats
operator|*
operator|)
literal|0
argument_list|,
operator|(
expr|struct
name|procstats
operator|*
operator|)
literal|0
argument_list|)
operator|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_PTX */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|MPE
end_ifdef

begin_comment
comment|/* This is a substitute for bind() that if called for an AF_INET socket port less than 1024, GETPRIVMODE() and GETUSERMODE() calls will be done. */
end_comment

begin_undef
undef|#
directive|undef
name|bind
end_undef

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<sys/un.h>
end_include

begin_function_decl
specifier|extern
name|void
name|GETPRIVMODE
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|extern
name|void
name|GETUSERMODE
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|int
name|__ntp_mpe_bind
parameter_list|(
name|int
name|s
parameter_list|,
name|void
modifier|*
name|addr
parameter_list|,
name|int
name|addrlen
parameter_list|)
function_decl|;
end_function_decl

begin_function
name|int
name|__ntp_mpe_bind
parameter_list|(
name|int
name|s
parameter_list|,
name|void
modifier|*
name|addr
parameter_list|,
name|int
name|addrlen
parameter_list|)
block|{
name|int
name|priv
init|=
literal|0
decl_stmt|;
name|int
name|result
decl_stmt|;
if|if
condition|(
name|addrlen
operator|==
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
condition|)
block|{
comment|/* AF_INET */
if|if
condition|(
operator|(
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|addr
operator|)
operator|->
name|sin_port
operator|>
literal|0
operator|&&
operator|(
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|addr
operator|)
operator|->
name|sin_port
operator|<
literal|1024
condition|)
block|{
name|priv
operator|=
literal|1
expr_stmt|;
name|GETPRIVMODE
argument_list|()
expr_stmt|;
block|}
comment|/*	((struct sockaddr_in *)addr)->sin_addr.s_addr = 0; */
name|result
operator|=
name|bind
argument_list|(
name|s
argument_list|,
name|addr
argument_list|,
name|addrlen
argument_list|)
expr_stmt|;
if|if
condition|(
name|priv
operator|==
literal|1
condition|)
name|GETUSERMODE
argument_list|()
expr_stmt|;
block|}
else|else
comment|/* AF_UNIX */
name|result
operator|=
name|bind
argument_list|(
name|s
argument_list|,
name|addr
argument_list|,
name|addrlen
argument_list|)
expr_stmt|;
return|return
name|result
return|;
block|}
end_function

begin_comment
comment|/*  * MPE stupidly requires sfcntl() to be used on sockets instead of fcntl(),  * so we define a wrapper to analyze the file descriptor and call the correct  * function.  */
end_comment

begin_undef
undef|#
directive|undef
name|fcntl
end_undef

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<fcntl.h>
end_include

begin_function_decl
name|int
name|__ntp_mpe_fcntl
parameter_list|(
name|int
name|fd
parameter_list|,
name|int
name|cmd
parameter_list|,
name|int
name|arg
parameter_list|)
function_decl|;
end_function_decl

begin_function
name|int
name|__ntp_mpe_fcntl
parameter_list|(
name|int
name|fd
parameter_list|,
name|int
name|cmd
parameter_list|,
name|int
name|arg
parameter_list|)
block|{
name|int
name|len
decl_stmt|;
name|struct
name|sockaddr
name|sa
decl_stmt|;
specifier|extern
name|int
name|sfcntl
argument_list|(
name|int
argument_list|,
name|int
argument_list|,
name|int
argument_list|)
decl_stmt|;
name|len
operator|=
sizeof|sizeof
name|sa
expr_stmt|;
if|if
condition|(
name|getsockname
argument_list|(
name|fd
argument_list|,
operator|&
name|sa
argument_list|,
operator|&
name|len
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
if|if
condition|(
name|errno
operator|==
name|EAFNOSUPPORT
condition|)
comment|/* AF_UNIX socket */
return|return
name|sfcntl
argument_list|(
name|fd
argument_list|,
name|cmd
argument_list|,
name|arg
argument_list|)
return|;
if|if
condition|(
name|errno
operator|==
name|ENOTSOCK
condition|)
comment|/* file or pipe */
return|return
name|fcntl
argument_list|(
name|fd
argument_list|,
name|cmd
argument_list|,
name|arg
argument_list|)
return|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
comment|/* unknown getsockname() failure */
block|}
else|else
comment|/* AF_INET socket */
return|return
name|sfcntl
argument_list|(
name|fd
argument_list|,
name|cmd
argument_list|,
name|arg
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * Setitimer emulation support.  Note that we implement this using alarm(),  * and since alarm() only delivers one signal, we must re-enable the alarm  * by enabling our own SIGALRM setitimer_mpe_handler routine to be called  * before the real handler routine and re-enable the alarm at that time.  *  * Note that this solution assumes that sigaction(SIGALRM) is called before  * calling setitimer().  If it should ever to become necessary to support  * sigaction(SIGALRM) after calling setitimer(), it will be necessary to trap  * those sigaction() calls.  */
end_comment

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_comment
comment|/*  * Some global data that needs to be shared between setitimer() and  * setitimer_mpe_handler().  */
end_comment

begin_struct
struct|struct
block|{
name|unsigned
name|long
name|current_msec
decl_stmt|;
comment|/* current alarm() value in effect */
name|unsigned
name|long
name|interval_msec
decl_stmt|;
comment|/* next alarm() value from setitimer */
name|unsigned
name|long
name|value_msec
decl_stmt|;
comment|/* first alarm() value from setitimer */
name|struct
name|itimerval
name|current_itimerval
decl_stmt|;
comment|/* current itimerval in effect */
name|struct
name|sigaction
name|oldact
decl_stmt|;
comment|/* SIGALRM state saved by setitimer */
block|}
name|setitimer_mpe_ctx
init|=
block|{
literal|0
block|,
literal|0
block|,
literal|0
block|}
struct|;
end_struct

begin_comment
comment|/*  * Undocumented, unsupported function to do alarm() in milliseconds.  */
end_comment

begin_function_decl
specifier|extern
name|unsigned
name|int
name|px_alarm
parameter_list|(
name|unsigned
name|long
parameter_list|,
name|int
modifier|*
parameter_list|)
function_decl|;
end_function_decl

begin_comment
comment|/*  * The SIGALRM handler routine enabled by setitimer().  Re-enable the alarm or  * restore the original SIGALRM setting if no more alarms are needed.  Then  * call the original SIGALRM handler (if any).  */
end_comment

begin_function
specifier|static
name|RETSIGTYPE
name|setitimer_mpe_handler
parameter_list|(
name|int
name|sig
parameter_list|)
block|{
name|int
name|alarm_hpe_status
decl_stmt|;
comment|/* Update the new current alarm value */
name|setitimer_mpe_ctx
operator|.
name|current_msec
operator|=
name|setitimer_mpe_ctx
operator|.
name|interval_msec
expr_stmt|;
if|if
condition|(
name|setitimer_mpe_ctx
operator|.
name|interval_msec
operator|>
literal|0
condition|)
block|{
comment|/* Additional intervals needed; re-arm the alarm timer */
name|px_alarm
argument_list|(
name|setitimer_mpe_ctx
operator|.
name|interval_msec
argument_list|,
operator|&
name|alarm_hpe_status
argument_list|)
expr_stmt|;
block|}
else|else
block|{
comment|/* No more intervals, so restore previous original SIGALRM handler */
name|sigaction
argument_list|(
name|SIGALRM
argument_list|,
operator|&
name|setitimer_mpe_ctx
operator|.
name|oldact
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
comment|/* Call the original SIGALRM handler if it is a function and not just a flag */
if|if
condition|(
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_handler
operator|!=
name|SIG_DFL
operator|&&
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_handler
operator|!=
name|SIG_ERR
operator|&&
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_handler
operator|!=
name|SIG_IGN
condition|)
call|(
modifier|*
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_handler
call|)
argument_list|(
name|SIGALRM
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * Our implementation of setitimer().  */
end_comment

begin_function
name|int
name|setitimer
parameter_list|(
name|int
name|which
parameter_list|,
name|struct
name|itimerval
modifier|*
name|value
parameter_list|,
name|struct
name|itimerval
modifier|*
name|ovalue
parameter_list|)
block|{
name|int
name|alarm_hpe_status
decl_stmt|;
name|unsigned
name|long
name|remaining_msec
decl_stmt|,
name|value_msec
decl_stmt|,
name|interval_msec
decl_stmt|;
name|struct
name|sigaction
name|newact
decl_stmt|;
comment|/*   * Convert the initial interval to milliseconds  */
if|if
condition|(
name|value
operator|->
name|it_value
operator|.
name|tv_sec
operator|>
operator|(
name|UINT_MAX
operator|/
literal|1000
operator|)
condition|)
name|value_msec
operator|=
name|UINT_MAX
expr_stmt|;
else|else
name|value_msec
operator|=
name|value
operator|->
name|it_value
operator|.
name|tv_sec
operator|*
literal|1000
expr_stmt|;
name|value_msec
operator|+=
name|value
operator|->
name|it_value
operator|.
name|tv_usec
operator|/
literal|1000
expr_stmt|;
comment|/*  * Convert the reset interval to milliseconds  */
if|if
condition|(
name|value
operator|->
name|it_interval
operator|.
name|tv_sec
operator|>
operator|(
name|UINT_MAX
operator|/
literal|1000
operator|)
condition|)
name|interval_msec
operator|=
name|UINT_MAX
expr_stmt|;
else|else
name|interval_msec
operator|=
name|value
operator|->
name|it_interval
operator|.
name|tv_sec
operator|*
literal|1000
expr_stmt|;
name|interval_msec
operator|+=
name|value
operator|->
name|it_interval
operator|.
name|tv_usec
operator|/
literal|1000
expr_stmt|;
if|if
condition|(
name|value_msec
operator|>
literal|0
operator|&&
name|interval_msec
operator|>
literal|0
condition|)
block|{
comment|/*    * We'll be starting an interval timer that will be repeating, so we need to    * insert our own SIGALRM signal handler to schedule the repeats.    */
comment|/* Read the current SIGALRM action */
if|if
condition|(
name|sigaction
argument_list|(
name|SIGALRM
argument_list|,
name|NULL
argument_list|,
operator|&
name|setitimer_mpe_ctx
operator|.
name|oldact
argument_list|)
operator|<
literal|0
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"MPE setitimer old handler failed, errno=%d\n"
argument_list|,
name|errno
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
comment|/* Initialize the new action to call our SIGALRM handler instead */
name|newact
operator|.
name|sa_handler
operator|=
operator|&
name|setitimer_mpe_handler
expr_stmt|;
name|newact
operator|.
name|sa_mask
operator|=
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_mask
expr_stmt|;
name|newact
operator|.
name|sa_flags
operator|=
name|setitimer_mpe_ctx
operator|.
name|oldact
operator|.
name|sa_flags
expr_stmt|;
if|if
condition|(
name|sigaction
argument_list|(
name|SIGALRM
argument_list|,
operator|&
name|newact
argument_list|,
name|NULL
argument_list|)
operator|<
literal|0
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"MPE setitimer new handler failed, errno=%d\n"
argument_list|,
name|errno
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
comment|/*  * Return previous itimerval if desired  */
if|if
condition|(
name|ovalue
operator|!=
name|NULL
condition|)
operator|*
name|ovalue
operator|=
name|setitimer_mpe_ctx
operator|.
name|current_itimerval
expr_stmt|;
comment|/*  * Save current parameters for later usage  */
name|setitimer_mpe_ctx
operator|.
name|current_itimerval
operator|=
operator|*
name|value
expr_stmt|;
name|setitimer_mpe_ctx
operator|.
name|current_msec
operator|=
name|value_msec
expr_stmt|;
name|setitimer_mpe_ctx
operator|.
name|value_msec
operator|=
name|value_msec
expr_stmt|;
name|setitimer_mpe_ctx
operator|.
name|interval_msec
operator|=
name|interval_msec
expr_stmt|;
comment|/*  * Schedule the first alarm  */
name|remaining_msec
operator|=
name|px_alarm
argument_list|(
name|value_msec
argument_list|,
operator|&
name|alarm_hpe_status
argument_list|)
expr_stmt|;
if|if
condition|(
name|alarm_hpe_status
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
else|else
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*   * MPE lacks gettimeofday(), so we define our own.  */
end_comment

begin_function
name|int
name|gettimeofday
parameter_list|(
name|struct
name|timeval
modifier|*
name|tvp
parameter_list|)
block|{
comment|/* Documented, supported MPE functions. */
specifier|extern
name|void
name|GETPRIVMODE
argument_list|(
name|void
argument_list|)
decl_stmt|;
specifier|extern
name|void
name|GETUSERMODE
argument_list|(
name|void
argument_list|)
decl_stmt|;
comment|/* Undocumented, unsupported MPE functions. */
specifier|extern
name|long
name|long
name|get_time
argument_list|(
name|void
argument_list|)
decl_stmt|;
specifier|extern
name|void
name|get_time_change_info
argument_list|(
name|long
name|long
operator|*
argument_list|,
name|char
operator|*
argument_list|,
name|char
operator|*
argument_list|)
decl_stmt|;
specifier|extern
name|long
name|long
name|ticks_to_micro
argument_list|(
name|long
name|long
argument_list|)
decl_stmt|;
name|char
name|pwf_since_boot
decl_stmt|,
name|recover_pwf_time
decl_stmt|;
name|long
name|long
name|mpetime
decl_stmt|,
name|offset_ticks
decl_stmt|,
name|offset_usec
decl_stmt|;
name|GETPRIVMODE
argument_list|()
expr_stmt|;
name|mpetime
operator|=
name|get_time
argument_list|()
expr_stmt|;
comment|/* MPE local time usecs since Jan 1 1970 */
name|get_time_change_info
argument_list|(
operator|&
name|offset_ticks
argument_list|,
operator|&
name|pwf_since_boot
argument_list|,
operator|&
name|recover_pwf_time
argument_list|)
expr_stmt|;
name|offset_usec
operator|=
name|ticks_to_micro
argument_list|(
name|offset_ticks
argument_list|)
expr_stmt|;
comment|/* UTC offset usecs */
name|GETUSERMODE
argument_list|()
expr_stmt|;
name|mpetime
operator|=
name|mpetime
operator|-
name|offset_usec
expr_stmt|;
comment|/* Convert from local time to UTC */
name|tvp
operator|->
name|tv_sec
operator|=
name|mpetime
operator|/
literal|1000000LL
expr_stmt|;
name|tvp
operator|->
name|tv_usec
operator|=
name|mpetime
operator|%
literal|1000000LL
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/*   * MPE lacks settimeofday(), so we define our own.  */
end_comment

begin_define
define|#
directive|define
name|HAVE_SETTIMEOFDAY
end_define

begin_function
name|int
name|settimeofday
parameter_list|(
name|struct
name|timeval
modifier|*
name|tvp
parameter_list|)
block|{
comment|/* Documented, supported MPE functions. */
specifier|extern
name|void
name|GETPRIVMODE
argument_list|(
name|void
argument_list|)
decl_stmt|;
specifier|extern
name|void
name|GETUSERMODE
argument_list|(
name|void
argument_list|)
decl_stmt|;
comment|/* Undocumented, unsupported MPE functions. */
specifier|extern
name|void
name|get_time_change_info
argument_list|(
name|long
name|long
operator|*
argument_list|,
name|char
operator|*
argument_list|,
name|char
operator|*
argument_list|)
decl_stmt|;
specifier|extern
name|void
name|initialize_system_time
argument_list|(
name|long
name|long
argument_list|,
name|int
argument_list|)
decl_stmt|;
specifier|extern
name|void
name|set_time_correction
argument_list|(
name|long
name|long
argument_list|,
name|int
argument_list|,
name|int
argument_list|)
decl_stmt|;
specifier|extern
name|long
name|long
name|ticks_to_micro
argument_list|(
name|long
name|long
argument_list|)
decl_stmt|;
name|char
name|pwf_since_boot
decl_stmt|,
name|recover_pwf_time
decl_stmt|;
name|long
name|long
name|big_sec
decl_stmt|,
name|big_usec
decl_stmt|,
name|mpetime
decl_stmt|,
name|offset_ticks
decl_stmt|,
name|offset_usec
decl_stmt|;
name|big_sec
operator|=
name|tvp
operator|->
name|tv_sec
expr_stmt|;
name|big_usec
operator|=
name|tvp
operator|->
name|tv_usec
expr_stmt|;
name|mpetime
operator|=
operator|(
name|big_sec
operator|*
literal|1000000LL
operator|)
operator|+
name|big_usec
expr_stmt|;
comment|/* Desired UTC microseconds */
name|GETPRIVMODE
argument_list|()
expr_stmt|;
name|set_time_correction
argument_list|(
literal|0LL
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* Cancel previous time correction, if any */
name|get_time_change_info
argument_list|(
operator|&
name|offset_ticks
argument_list|,
operator|&
name|pwf_since_boot
argument_list|,
operator|&
name|recover_pwf_time
argument_list|)
expr_stmt|;
name|offset_usec
operator|=
name|ticks_to_micro
argument_list|(
name|offset_ticks
argument_list|)
expr_stmt|;
comment|/* UTC offset microseconds */
name|mpetime
operator|=
name|mpetime
operator|+
name|offset_usec
expr_stmt|;
comment|/* Convert from UTC to local time */
name|initialize_system_time
argument_list|(
name|mpetime
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|GETUSERMODE
argument_list|()
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* MPE */
end_comment

begin_decl_stmt
specifier|const
name|char
modifier|*
name|set_tod_using
init|=
literal|"UNKNOWN"
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|ntp_set_tod
parameter_list|(
name|struct
name|timeval
modifier|*
name|tvp
parameter_list|,
name|void
modifier|*
name|tzp
parameter_list|)
block|{
name|int
name|rc
init|=
operator|-
literal|1
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"In ntp_set_tod\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|HAVE_CLOCK_SETTIME
if|if
condition|(
name|rc
condition|)
block|{
name|struct
name|timespec
name|ts
decl_stmt|;
name|set_tod_using
operator|=
literal|"clock_settime"
expr_stmt|;
comment|/* Convert timeval to timespec */
name|ts
operator|.
name|tv_sec
operator|=
name|tvp
operator|->
name|tv_sec
expr_stmt|;
name|ts
operator|.
name|tv_nsec
operator|=
literal|1000
operator|*
name|tvp
operator|->
name|tv_usec
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|rc
operator|=
name|clock_settime
argument_list|(
name|CLOCK_REALTIME
argument_list|,
operator|&
name|ts
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
name|printf
argument_list|(
literal|"ntp_set_tod: %s: %d: %s\n"
argument_list|,
name|set_tod_using
argument_list|,
name|rc
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
endif|#
directive|endif
comment|/* HAVE_CLOCK_SETTIME */
ifdef|#
directive|ifdef
name|HAVE_SETTIMEOFDAY
if|if
condition|(
name|rc
condition|)
block|{
name|struct
name|timeval
name|adjtv
decl_stmt|;
name|set_tod_using
operator|=
literal|"settimeofday"
expr_stmt|;
comment|/* 		 * Some broken systems don't reset adjtime() when the 		 * clock is stepped. 		 */
name|adjtv
operator|.
name|tv_sec
operator|=
name|adjtv
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
name|adjtime
argument_list|(
operator|&
name|adjtv
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|rc
operator|=
name|SETTIMEOFDAY
argument_list|(
name|tvp
argument_list|,
name|tzp
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
name|printf
argument_list|(
literal|"ntp_set_tod: %s: %d: %s\n"
argument_list|,
name|set_tod_using
argument_list|,
name|rc
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
endif|#
directive|endif
comment|/* HAVE_SETTIMEOFDAY */
ifdef|#
directive|ifdef
name|HAVE_STIME
if|if
condition|(
name|rc
condition|)
block|{
name|long
name|tp
init|=
name|tvp
operator|->
name|tv_sec
decl_stmt|;
name|set_tod_using
operator|=
literal|"stime"
expr_stmt|;
name|rc
operator|=
name|stime
argument_list|(
operator|&
name|tp
argument_list|)
expr_stmt|;
comment|/* lie as bad as SysVR4 */
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
name|printf
argument_list|(
literal|"ntp_set_tod: %s: %d: %s\n"
argument_list|,
name|set_tod_using
argument_list|,
name|rc
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
block|}
endif|#
directive|endif
comment|/* HAVE_STIME */
if|if
condition|(
name|rc
condition|)
name|set_tod_using
operator|=
literal|"Failed!"
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
block|{
name|printf
argument_list|(
literal|"ntp_set_tod: Final result: %s: %d: %s\n"
argument_list|,
name|set_tod_using
argument_list|,
name|rc
argument_list|,
name|strerror
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
return|return
name|rc
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not SYS_WINNT */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SYS_WINNT
argument_list|)
operator|||
name|defined
argument_list|(
name|SYS_VXWORKS
argument_list|)
operator|||
name|defined
argument_list|(
name|MPE
argument_list|)
end_if

begin_comment
comment|/* getpass is used in ntpq.c and ntpdc.c */
end_comment

begin_function
name|char
modifier|*
name|getpass
parameter_list|(
specifier|const
name|char
modifier|*
name|prompt
parameter_list|)
block|{
name|int
name|c
decl_stmt|,
name|i
decl_stmt|;
specifier|static
name|char
name|password
index|[
literal|32
index|]
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%s"
argument_list|,
name|prompt
argument_list|)
expr_stmt|;
name|fflush
argument_list|(
name|stderr
argument_list|)
expr_stmt|;
endif|#
directive|endif
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
sizeof|sizeof
argument_list|(
name|password
argument_list|)
operator|-
literal|1
operator|&&
operator|(
operator|(
name|c
operator|=
name|_getch
argument_list|()
operator|)
operator|!=
literal|'\n'
operator|&&
name|c
operator|!=
literal|'\r'
operator|)
condition|;
name|i
operator|++
control|)
block|{
name|password
index|[
name|i
index|]
operator|=
operator|(
name|char
operator|)
name|c
expr_stmt|;
block|}
name|password
index|[
name|i
index|]
operator|=
literal|'\0'
expr_stmt|;
return|return
name|password
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SYS_WINNT */
end_comment

begin_if
if|#
directive|if
operator|!
name|defined
argument_list|(
name|HAVE_MEMSET
argument_list|)
end_if

begin_function
name|void
name|ntp_memset
parameter_list|(
name|char
modifier|*
name|a
parameter_list|,
name|int
name|x
parameter_list|,
name|int
name|c
parameter_list|)
block|{
while|while
condition|(
name|c
operator|--
operator|>
literal|0
condition|)
operator|*
name|a
operator|++
operator|=
operator|(
name|char
operator|)
name|x
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*POSIX*/
end_comment

end_unit

