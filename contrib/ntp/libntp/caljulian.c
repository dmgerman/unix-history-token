begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * caljulian - determine the Julian date from an NTP time.  *  * (Note: since we use the GREGORIAN calendar, this should be renamed to  * 'calgregorian' eventually...)  */
end_comment

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|"ntp_types.h"
end_include

begin_include
include|#
directive|include
file|"ntp_calendar.h"
end_include

begin_if
if|#
directive|if
operator|!
operator|(
name|defined
argument_list|(
name|ISC_CHECK_ALL
argument_list|)
operator|||
name|defined
argument_list|(
name|ISC_CHECK_NONE
argument_list|)
operator|||
expr|\
name|defined
argument_list|(
name|ISC_CHECK_ENSURE
argument_list|)
operator|||
name|defined
argument_list|(
name|ISC_CHECK_INSIST
argument_list|)
operator|||
expr|\
name|defined
argument_list|(
name|ISC_CHECK_INVARIANT
argument_list|)
operator|)
end_if

begin_define
define|#
directive|define
name|ISC_CHECK_ALL
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntp_assert.h"
end_include

begin_function
name|void
name|caljulian
parameter_list|(
name|uint32_t
name|ntp
parameter_list|,
name|struct
name|calendar
modifier|*
name|jt
parameter_list|)
block|{
name|vint64
name|vlong
decl_stmt|;
name|ntpcal_split
name|split
decl_stmt|;
name|NTP_INSIST
argument_list|(
name|NULL
operator|!=
name|jt
argument_list|)
expr_stmt|;
comment|/* 	 * Unfold ntp time around current time into NTP domain. Split 	 * into days and seconds, shift days into CE domain and 	 * process the parts. 	 */
name|vlong
operator|=
name|ntpcal_ntp_to_ntp
argument_list|(
name|ntp
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|split
operator|=
name|ntpcal_daysplit
argument_list|(
operator|&
name|vlong
argument_list|)
expr_stmt|;
name|ntpcal_daysplit_to_date
argument_list|(
name|jt
argument_list|,
operator|&
name|split
argument_list|,
name|DAY_NTP_STARTS
argument_list|)
expr_stmt|;
block|}
end_function

end_unit

