begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*   * netof - return the net address part of an ip address in a sockaddr_storage structure  *         (zero out host part)  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"ntp_fp.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|"ntp.h"
end_include

begin_define
define|#
directive|define
name|NUM_NETOF_BUFS
value|10
end_define

begin_decl_stmt
specifier|static
name|struct
name|sockaddr_storage
name|ssbuf
index|[
name|NUM_NETOF_BUFS
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|next_ssbuf
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_function
name|struct
name|sockaddr_storage
modifier|*
name|netof
parameter_list|(
name|struct
name|sockaddr_storage
modifier|*
name|hostaddr
parameter_list|)
block|{
specifier|register
name|u_int32
name|netnum
decl_stmt|;
name|struct
name|sockaddr_storage
modifier|*
name|netaddr
decl_stmt|;
name|netaddr
operator|=
operator|&
name|ssbuf
index|[
name|next_ssbuf
operator|++
index|]
expr_stmt|;
if|if
condition|(
name|next_ssbuf
operator|==
name|NUM_NETOF_BUFS
condition|)
name|next_ssbuf
operator|=
literal|0
expr_stmt|;
name|memcpy
argument_list|(
name|netaddr
argument_list|,
name|hostaddr
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_storage
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|netaddr
operator|->
name|ss_family
operator|==
name|AF_INET
condition|)
block|{
name|netnum
operator|=
operator|(
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|netaddr
operator|)
operator|->
name|sin_addr
operator|.
name|s_addr
expr_stmt|;
comment|/* 		 * We live in a modern CIDR world where the basement nets, which 		 * used to be class A, are now probably associated with each 		 * host address. So, for class-A nets, all bits are significant. 		 */
if|if
condition|(
name|IN_CLASSC
argument_list|(
name|netnum
argument_list|)
condition|)
name|netnum
operator|&=
name|IN_CLASSC_NET
expr_stmt|;
elseif|else
if|if
condition|(
name|IN_CLASSB
argument_list|(
name|netnum
argument_list|)
condition|)
name|netnum
operator|&=
name|IN_CLASSB_NET
expr_stmt|;
operator|(
operator|(
expr|struct
name|sockaddr_in
operator|*
operator|)
name|netaddr
operator|)
operator|->
name|sin_addr
operator|.
name|s_addr
operator|=
name|netnum
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|netaddr
operator|->
name|ss_family
operator|==
name|AF_INET6
condition|)
block|{
comment|/* Here we put 0 at the local link address so we get net address */
name|memset
argument_list|(
operator|&
operator|(
operator|(
expr|struct
name|sockaddr_in6
operator|*
operator|)
name|netaddr
operator|)
operator|->
name|sin6_addr
operator|.
name|s6_addr
index|[
literal|8
index|]
argument_list|,
literal|0
argument_list|,
literal|8
operator|*
sizeof|sizeof
argument_list|(
name|u_char
argument_list|)
argument_list|)
expr_stmt|;
block|}
return|return
name|netaddr
return|;
block|}
end_function

end_unit

