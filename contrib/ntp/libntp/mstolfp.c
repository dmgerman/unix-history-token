begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * mstolfp - convert an ascii string in milliseconds to an l_fp number  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"ntp_fp.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_function
name|int
name|mstolfp
parameter_list|(
specifier|const
name|char
modifier|*
name|str
parameter_list|,
name|l_fp
modifier|*
name|lfp
parameter_list|)
block|{
specifier|register
specifier|const
name|char
modifier|*
name|cp
decl_stmt|;
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
specifier|register
specifier|const
name|char
modifier|*
name|cpdec
decl_stmt|;
name|char
name|buf
index|[
literal|100
index|]
decl_stmt|;
comment|/* 	 * We understand numbers of the form: 	 * 	 * [spaces][-][digits][.][digits][spaces|\n|\0] 	 * 	 * This is one enormous hack.  Since I didn't feel like 	 * rewriting the decoding routine for milliseconds, what 	 * is essentially done here is to make a copy of the string 	 * with the decimal moved over three places so the seconds 	 * decoding routine can be used. 	 */
name|bp
operator|=
name|buf
expr_stmt|;
name|cp
operator|=
name|str
expr_stmt|;
while|while
condition|(
name|isspace
argument_list|(
operator|(
name|int
operator|)
operator|*
name|cp
argument_list|)
condition|)
name|cp
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|cp
operator|==
literal|'-'
condition|)
block|{
operator|*
name|bp
operator|++
operator|=
literal|'-'
expr_stmt|;
name|cp
operator|++
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|cp
operator|!=
literal|'.'
operator|&&
operator|!
name|isdigit
argument_list|(
operator|(
name|int
operator|)
operator|*
name|cp
argument_list|)
condition|)
return|return
literal|0
return|;
comment|/* 	 * Search forward for the decimal point or the end of the string. 	 */
name|cpdec
operator|=
name|cp
expr_stmt|;
while|while
condition|(
name|isdigit
argument_list|(
operator|(
name|int
operator|)
operator|*
name|cpdec
argument_list|)
condition|)
name|cpdec
operator|++
expr_stmt|;
comment|/* 	 * Found something.  If we have more than three digits copy the 	 * excess over, else insert a leading 0. 	 */
if|if
condition|(
operator|(
name|cpdec
operator|-
name|cp
operator|)
operator|>
literal|3
condition|)
block|{
do|do
block|{
operator|*
name|bp
operator|++
operator|=
operator|(
name|char
operator|)
operator|*
name|cp
operator|++
expr_stmt|;
block|}
do|while
condition|(
operator|(
name|cpdec
operator|-
name|cp
operator|)
operator|>
literal|3
condition|)
do|;
block|}
else|else
block|{
operator|*
name|bp
operator|++
operator|=
literal|'0'
expr_stmt|;
block|}
comment|/* 	 * Stick the decimal in.  If we've got less than three digits in 	 * front of the millisecond decimal we insert the appropriate number 	 * of zeros. 	 */
operator|*
name|bp
operator|++
operator|=
literal|'.'
expr_stmt|;
if|if
condition|(
operator|(
name|cpdec
operator|-
name|cp
operator|)
operator|<
literal|3
condition|)
block|{
specifier|register
name|int
name|i
init|=
literal|3
operator|-
operator|(
name|cpdec
operator|-
name|cp
operator|)
decl_stmt|;
do|do
block|{
operator|*
name|bp
operator|++
operator|=
literal|'0'
expr_stmt|;
block|}
do|while
condition|(
operator|--
name|i
operator|>
literal|0
condition|)
do|;
block|}
comment|/* 	 * Copy the remainder up to the millisecond decimal.  If cpdec 	 * is pointing at a decimal point, copy in the trailing number too. 	 */
while|while
condition|(
name|cp
operator|<
name|cpdec
condition|)
operator|*
name|bp
operator|++
operator|=
operator|(
name|char
operator|)
operator|*
name|cp
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|cp
operator|==
literal|'.'
condition|)
block|{
name|cp
operator|++
expr_stmt|;
while|while
condition|(
name|isdigit
argument_list|(
operator|(
name|int
operator|)
operator|*
name|cp
argument_list|)
condition|)
operator|*
name|bp
operator|++
operator|=
operator|(
name|char
operator|)
operator|*
name|cp
operator|++
expr_stmt|;
block|}
operator|*
name|bp
operator|=
literal|'\0'
expr_stmt|;
comment|/* 	 * Check to make sure the string is properly terminated.  If 	 * so, give the buffer to the decoding routine. 	 */
if|if
condition|(
operator|*
name|cp
operator|!=
literal|'\0'
operator|&&
operator|!
name|isspace
argument_list|(
operator|(
name|int
operator|)
operator|*
name|cp
argument_list|)
condition|)
return|return
literal|0
return|;
return|return
name|atolfp
argument_list|(
name|buf
argument_list|,
name|lfp
argument_list|)
return|;
block|}
end_function

end_unit

