begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* From: Jeff Solomon<jsolomon@stanford.edu> Date: Fri,  9 Apr 1999 10:13:27 -0700 (PDT) To: chet@po.cwru.edu Subject: new readline example Message-ID:<14094.12094.527305.199695@mrclean.Stanford.EDU>  Chet,  I've been using readline 4.0. Specifically, I've been using the perl version Term::ReadLine::Gnu. It works great.  Anyway, I've been playing around the alternate interface and I wanted to contribute a little C program, callback.c, to you that you could use as an example of the alternate interface in the /examples directory of the readline distribution.  My example shows how, using the alternate interface, you can interactively change the prompt (which is very nice imo). Also, I point out that you must roll your own terminal setting when using the alternate interface because readline depreps (using your parlance) the terminal while in the user callback. I try to demostrate what I mean with an example. I've included the program below.  To compile, I just put the program in the examples directory and made the appropriate changes to the EXECUTABLES and OBJECTS line and added an additional target 'callback'.  I compiled on my Sun Solaris2.6 box using Sun's cc.  Let me know what you think.  Jeff */
end_comment

begin_comment
comment|/* Copyright (C) 1999 Jeff Solomon */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|HAVE_CONFIG_H
argument_list|)
end_if

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_UNISTD_H
end_ifdef

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<termios.h>
end_include

begin_comment
comment|/* xxx - should make this more general */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|READLINE_LIBRARY
end_ifdef

begin_include
include|#
directive|include
file|"readline.h"
end_include

begin_else
else|#
directive|else
end_else

begin_include
include|#
directive|include
file|<readline/readline.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* This little examples demonstrates the alternate interface to using readline.  * In the alternate interface, the user maintains control over program flow and  * only calls readline when STDIN is readable. Using the alternate interface,  * you can do anything else while still using readline (like talking to a  * network or another program) without blocking.  *  * Specifically, this program highlights two importants features of the  * alternate interface. The first is the ability to interactively change the  * prompt, which can't be done using the regular interface since rl_prompt is  * read-only.  *   * The second feature really highlights a subtle point when using the alternate  * interface. That is, readline will not alter the terminal when inside your  * callback handler. So let's so, your callback executes a user command that  * takes a non-trivial amount of time to complete (seconds). While your  * executing the command, the user continues to type keystrokes and expects them  * to be re-echoed on the new prompt when it returns. Unfortunately, the default  * terminal configuration doesn't do this. After the prompt returns, the user  * must hit one additional keystroke and then will see all of his previous  * keystrokes. To illustrate this, compile and run this program. Type "sleep" at  * the prompt and then type "bar" before the prompt returns (you have 3  * seconds). Notice how "bar" is re-echoed on the prompt after the prompt  * returns? This is what you expect to happen. Now comment out the 4 lines below  * the line that says COMMENT LINE BELOW. Recompile and rerun the program and do  * the same thing. When the prompt returns, you should not see "bar". Now type  * "f", see how "barf" magically appears? This behavior is un-expected and not  * desired.  */
end_comment

begin_function_decl
name|void
name|process_line
parameter_list|(
name|char
modifier|*
name|line
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|int
name|change_prompt
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|char
modifier|*
name|get_prompt
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_decl_stmt
name|int
name|prompt
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|prompt_buf
index|[
literal|40
index|]
decl_stmt|,
name|line_buf
index|[
literal|256
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|tcflag_t
name|old_lflag
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|cc_t
name|old_vtime
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|termios
name|term
decl_stmt|;
end_decl_stmt

begin_function
name|int
name|main
parameter_list|()
block|{
name|fd_set
name|fds
decl_stmt|;
comment|/* Adjust the terminal slightly before the handler is installed. Disable      * canonical mode processing and set the input character time flag to be      * non-blocking.      */
if|if
condition|(
name|tcgetattr
argument_list|(
name|STDIN_FILENO
argument_list|,
operator|&
name|term
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"tcgetattr"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|old_lflag
operator|=
name|term
operator|.
name|c_lflag
expr_stmt|;
name|old_vtime
operator|=
name|term
operator|.
name|c_cc
index|[
name|VTIME
index|]
expr_stmt|;
name|term
operator|.
name|c_lflag
operator|&=
operator|~
name|ICANON
expr_stmt|;
name|term
operator|.
name|c_cc
index|[
name|VTIME
index|]
operator|=
literal|1
expr_stmt|;
comment|/* COMMENT LINE BELOW - see above */
if|if
condition|(
name|tcsetattr
argument_list|(
name|STDIN_FILENO
argument_list|,
name|TCSANOW
argument_list|,
operator|&
name|term
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"tcsetattr"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|rl_add_defun
argument_list|(
literal|"change-prompt"
argument_list|,
name|change_prompt
argument_list|,
name|CTRL
argument_list|(
literal|'t'
argument_list|)
argument_list|)
expr_stmt|;
name|rl_callback_handler_install
argument_list|(
name|get_prompt
argument_list|()
argument_list|,
name|process_line
argument_list|)
expr_stmt|;
while|while
condition|(
literal|1
condition|)
block|{
name|FD_ZERO
argument_list|(
operator|&
name|fds
argument_list|)
expr_stmt|;
name|FD_SET
argument_list|(
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|,
operator|&
name|fds
argument_list|)
expr_stmt|;
if|if
condition|(
name|select
argument_list|(
name|FD_SETSIZE
argument_list|,
operator|&
name|fds
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"select"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|FD_ISSET
argument_list|(
name|fileno
argument_list|(
name|stdin
argument_list|)
argument_list|,
operator|&
name|fds
argument_list|)
condition|)
block|{
name|rl_callback_read_char
argument_list|()
expr_stmt|;
block|}
block|}
block|}
end_function

begin_function
name|void
name|process_line
parameter_list|(
name|char
modifier|*
name|line
parameter_list|)
block|{
if|if
condition|(
name|line
operator|==
name|NULL
condition|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"\n"
argument_list|,
name|line
argument_list|)
expr_stmt|;
comment|/* reset the old terminal setting before exiting */
name|term
operator|.
name|c_lflag
operator|=
name|old_lflag
expr_stmt|;
name|term
operator|.
name|c_cc
index|[
name|VTIME
index|]
operator|=
name|old_vtime
expr_stmt|;
if|if
condition|(
name|tcsetattr
argument_list|(
name|STDIN_FILENO
argument_list|,
name|TCSANOW
argument_list|,
operator|&
name|term
argument_list|)
operator|<
literal|0
condition|)
block|{
name|perror
argument_list|(
literal|"tcsetattr"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
name|exit
argument_list|(
literal|0
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|strcmp
argument_list|(
name|line
argument_list|,
literal|"sleep"
argument_list|)
operator|==
literal|0
condition|)
block|{
name|sleep
argument_list|(
literal|3
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"|%s|\n"
argument_list|,
name|line
argument_list|)
expr_stmt|;
block|}
name|free
argument_list|(
name|line
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|int
name|change_prompt
parameter_list|(
name|void
parameter_list|)
block|{
comment|/* toggle the prompt variable */
name|prompt
operator|=
operator|!
name|prompt
expr_stmt|;
comment|/* save away the current contents of the line */
name|strcpy
argument_list|(
name|line_buf
argument_list|,
name|rl_line_buffer
argument_list|)
expr_stmt|;
comment|/* install a new handler which will change the prompt and erase the current line */
name|rl_callback_handler_install
argument_list|(
name|get_prompt
argument_list|()
argument_list|,
name|process_line
argument_list|)
expr_stmt|;
comment|/* insert the old text on the new line */
name|rl_insert_text
argument_list|(
name|line_buf
argument_list|)
expr_stmt|;
comment|/* redraw the current line - this is an undocumented function. It invokes the    * redraw-current-line command.    */
name|rl_refresh_line
argument_list|(
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|char
modifier|*
name|get_prompt
parameter_list|(
name|void
parameter_list|)
block|{
comment|/* The prompts can even be different lengths! */
name|sprintf
argument_list|(
name|prompt_buf
argument_list|,
literal|"%s"
argument_list|,
name|prompt
condition|?
literal|"Hit ctrl-t to toggle prompt> "
else|:
literal|"Pretty cool huh?> "
argument_list|)
expr_stmt|;
return|return
name|prompt_buf
return|;
block|}
end_function

end_unit

