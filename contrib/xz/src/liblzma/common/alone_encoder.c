begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|///////////////////////////////////////////////////////////////////////////////
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|/// \file       alone_decoder.c
end_comment

begin_comment
comment|/// \brief      Decoder for LZMA_Alone files
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|//  Author:     Lasse Collin
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|//  This file has been put into the public domain.
end_comment

begin_comment
comment|//  You can do whatever you want with this file.
end_comment

begin_comment
comment|//
end_comment

begin_comment
comment|///////////////////////////////////////////////////////////////////////////////
end_comment

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_include
include|#
directive|include
file|"lzma_encoder.h"
end_include

begin_define
define|#
directive|define
name|ALONE_HEADER_SIZE
value|(1 + 4 + 8)
end_define

begin_struct
struct|struct
name|lzma_coder_s
block|{
name|lzma_next_coder
name|next
decl_stmt|;
enum|enum
block|{
name|SEQ_HEADER
block|,
name|SEQ_CODE
block|, 	}
name|sequence
enum|;
name|size_t
name|header_pos
decl_stmt|;
name|uint8_t
name|header
index|[
name|ALONE_HEADER_SIZE
index|]
decl_stmt|;
block|}
struct|;
end_struct

begin_decl_stmt
specifier|static
name|lzma_ret
name|alone_encode
argument_list|(
name|lzma_coder
operator|*
name|coder
argument_list|,
specifier|const
name|lzma_allocator
operator|*
name|allocator
name|lzma_attribute
argument_list|(
operator|(
name|__unused__
operator|)
argument_list|)
argument_list|,
specifier|const
name|uint8_t
operator|*
specifier|restrict
name|in
argument_list|,
name|size_t
operator|*
specifier|restrict
name|in_pos
argument_list|,
name|size_t
name|in_size
argument_list|,
name|uint8_t
operator|*
specifier|restrict
name|out
argument_list|,
name|size_t
operator|*
specifier|restrict
name|out_pos
argument_list|,
name|size_t
name|out_size
argument_list|,
name|lzma_action
name|action
argument_list|)
block|{
while|while
condition|(
operator|*
name|out_pos
operator|<
name|out_size
condition|)
switch|switch
condition|(
name|coder
operator|->
name|sequence
condition|)
block|{
case|case
name|SEQ_HEADER
case|:
name|lzma_bufcpy
argument_list|(
name|coder
operator|->
name|header
argument_list|,
operator|&
name|coder
operator|->
name|header_pos
argument_list|,
name|ALONE_HEADER_SIZE
argument_list|,
name|out
argument_list|,
name|out_pos
argument_list|,
name|out_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|coder
operator|->
name|header_pos
operator|<
name|ALONE_HEADER_SIZE
condition|)
return|return
name|LZMA_OK
return|;
name|coder
operator|->
name|sequence
operator|=
name|SEQ_CODE
expr_stmt|;
break|break;
case|case
name|SEQ_CODE
case|:
return|return
name|coder
operator|->
name|next
operator|.
name|code
argument_list|(
name|coder
operator|->
name|next
operator|.
name|coder
argument_list|,
name|allocator
argument_list|,
name|in
argument_list|,
name|in_pos
argument_list|,
name|in_size
argument_list|,
name|out
argument_list|,
name|out_pos
argument_list|,
name|out_size
argument_list|,
name|action
argument_list|)
return|;
default|default:
name|assert
argument_list|(
literal|0
argument_list|)
expr_stmt|;
return|return
name|LZMA_PROG_ERROR
return|;
block|}
return|return
name|LZMA_OK
return|;
block|}
end_decl_stmt

begin_function
specifier|static
name|void
name|alone_encoder_end
parameter_list|(
name|lzma_coder
modifier|*
name|coder
parameter_list|,
specifier|const
name|lzma_allocator
modifier|*
name|allocator
parameter_list|)
block|{
name|lzma_next_end
argument_list|(
operator|&
name|coder
operator|->
name|next
argument_list|,
name|allocator
argument_list|)
expr_stmt|;
name|lzma_free
argument_list|(
name|coder
argument_list|,
name|allocator
argument_list|)
expr_stmt|;
return|return;
block|}
end_function

begin_comment
comment|// At least for now, this is not used by any internal function.
end_comment

begin_function
specifier|static
name|lzma_ret
name|alone_encoder_init
parameter_list|(
name|lzma_next_coder
modifier|*
name|next
parameter_list|,
specifier|const
name|lzma_allocator
modifier|*
name|allocator
parameter_list|,
specifier|const
name|lzma_options_lzma
modifier|*
name|options
parameter_list|)
block|{
name|lzma_next_coder_init
argument_list|(
operator|&
name|alone_encoder_init
argument_list|,
name|next
argument_list|,
name|allocator
argument_list|)
expr_stmt|;
if|if
condition|(
name|next
operator|->
name|coder
operator|==
name|NULL
condition|)
block|{
name|next
operator|->
name|coder
operator|=
name|lzma_alloc
argument_list|(
sizeof|sizeof
argument_list|(
name|lzma_coder
argument_list|)
argument_list|,
name|allocator
argument_list|)
expr_stmt|;
if|if
condition|(
name|next
operator|->
name|coder
operator|==
name|NULL
condition|)
return|return
name|LZMA_MEM_ERROR
return|;
name|next
operator|->
name|code
operator|=
operator|&
name|alone_encode
expr_stmt|;
name|next
operator|->
name|end
operator|=
operator|&
name|alone_encoder_end
expr_stmt|;
name|next
operator|->
name|coder
operator|->
name|next
operator|=
name|LZMA_NEXT_CODER_INIT
expr_stmt|;
block|}
comment|// Basic initializations
name|next
operator|->
name|coder
operator|->
name|sequence
operator|=
name|SEQ_HEADER
expr_stmt|;
name|next
operator|->
name|coder
operator|->
name|header_pos
operator|=
literal|0
expr_stmt|;
comment|// Encode the header:
comment|// - Properties (1 byte)
if|if
condition|(
name|lzma_lzma_lclppb_encode
argument_list|(
name|options
argument_list|,
name|next
operator|->
name|coder
operator|->
name|header
argument_list|)
condition|)
return|return
name|LZMA_OPTIONS_ERROR
return|;
comment|// - Dictionary size (4 bytes)
if|if
condition|(
name|options
operator|->
name|dict_size
operator|<
name|LZMA_DICT_SIZE_MIN
condition|)
return|return
name|LZMA_OPTIONS_ERROR
return|;
comment|// Round up to the next 2^n or 2^n + 2^(n - 1) depending on which
comment|// one is the next unless it is UINT32_MAX. While the header would
comment|// allow any 32-bit integer, we do this to keep the decoder of liblzma
comment|// accepting the resulting files.
name|uint32_t
name|d
init|=
name|options
operator|->
name|dict_size
operator|-
literal|1
decl_stmt|;
name|d
operator||=
name|d
operator|>>
literal|2
expr_stmt|;
name|d
operator||=
name|d
operator|>>
literal|3
expr_stmt|;
name|d
operator||=
name|d
operator|>>
literal|4
expr_stmt|;
name|d
operator||=
name|d
operator|>>
literal|8
expr_stmt|;
name|d
operator||=
name|d
operator|>>
literal|16
expr_stmt|;
if|if
condition|(
name|d
operator|!=
name|UINT32_MAX
condition|)
operator|++
name|d
expr_stmt|;
name|unaligned_write32le
argument_list|(
name|next
operator|->
name|coder
operator|->
name|header
operator|+
literal|1
argument_list|,
name|d
argument_list|)
expr_stmt|;
comment|// - Uncompressed size (always unknown and using EOPM)
name|memset
argument_list|(
name|next
operator|->
name|coder
operator|->
name|header
operator|+
literal|1
operator|+
literal|4
argument_list|,
literal|0xFF
argument_list|,
literal|8
argument_list|)
expr_stmt|;
comment|// Initialize the LZMA encoder.
specifier|const
name|lzma_filter_info
name|filters
index|[
literal|2
index|]
init|=
block|{
block|{
operator|.
name|init
operator|=
operator|&
name|lzma_lzma_encoder_init
block|,
operator|.
name|options
operator|=
operator|(
name|void
operator|*
operator|)
operator|(
name|options
operator|)
block|, 		}
block|,
block|{
operator|.
name|init
operator|=
name|NULL
block|, 		}
block|}
decl_stmt|;
return|return
name|lzma_next_filter_init
argument_list|(
operator|&
name|next
operator|->
name|coder
operator|->
name|next
argument_list|,
name|allocator
argument_list|,
name|filters
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* extern lzma_ret lzma_alone_encoder_init(lzma_next_coder *next, const lzma_allocator *allocator, 		const lzma_options_alone *options) { 	lzma_next_coder_init(&alone_encoder_init, next, allocator, options); } */
end_comment

begin_extern
extern|extern LZMA_API(lzma_ret
end_extern

begin_macro
unit|)
name|lzma_alone_encoder
argument_list|(
argument|lzma_stream *strm
argument_list|,
argument|const lzma_options_lzma *options
argument_list|)
end_macro

begin_block
block|{
name|lzma_next_strm_init
argument_list|(
name|alone_encoder_init
argument_list|,
name|strm
argument_list|,
name|options
argument_list|)
expr_stmt|;
name|strm
operator|->
name|internal
operator|->
name|supported_actions
index|[
name|LZMA_RUN
index|]
operator|=
name|true
expr_stmt|;
name|strm
operator|->
name|internal
operator|->
name|supported_actions
index|[
name|LZMA_FINISH
index|]
operator|=
name|true
expr_stmt|;
return|return
name|LZMA_OK
return|;
block|}
end_block

end_unit

