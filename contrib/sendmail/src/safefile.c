begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1998 Sendmail, Inc.  All rights reserved.  * Copyright (c) 1983, 1995-1997 Eric P. Allman.  All rights reserved.  * Copyright (c) 1988, 1993  *	The Regents of the University of California.  All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)safefile.c	8.40 (Berkeley) 6/5/98"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|"sendmail.h"
end_include

begin_escape
end_escape

begin_comment
comment|/* **  SAFEFILE -- return true if a file exists and is safe for a user. ** **	Parameters: **		fn -- filename to check. **		uid -- user id to compare against. **		gid -- group id to compare against. **		uname -- user name to compare against (used for group **			sets). **		flags -- modifiers: **			SFF_MUSTOWN -- "uid" must own this file. **			SFF_NOSLINK -- file cannot be a symbolic link. **		mode -- mode bits that must match. **		st -- if set, points to a stat structure that will **			get the stat info for the file. ** **	Returns: **		0 if fn exists, is owned by uid, and matches mode. **		An errno otherwise.  The actual errno is cleared. ** **	Side Effects: **		none. */
end_comment

begin_include
include|#
directive|include
file|<grp.h>
end_include

begin_function
name|int
name|safefile
parameter_list|(
name|fn
parameter_list|,
name|uid
parameter_list|,
name|gid
parameter_list|,
name|uname
parameter_list|,
name|flags
parameter_list|,
name|mode
parameter_list|,
name|st
parameter_list|)
name|char
modifier|*
name|fn
decl_stmt|;
name|UID_T
name|uid
decl_stmt|;
name|GID_T
name|gid
decl_stmt|;
name|char
modifier|*
name|uname
decl_stmt|;
name|int
name|flags
decl_stmt|;
name|int
name|mode
decl_stmt|;
name|struct
name|stat
modifier|*
name|st
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|struct
name|group
modifier|*
name|gr
init|=
name|NULL
decl_stmt|;
name|int
name|file_errno
init|=
literal|0
decl_stmt|;
name|bool
name|checkpath
decl_stmt|;
name|struct
name|stat
name|stbuf
decl_stmt|;
name|struct
name|stat
name|fstbuf
decl_stmt|;
name|char
name|fbuf
index|[
name|MAXPATHLEN
operator|+
literal|1
index|]
decl_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"safefile(%s, uid=%d, gid=%d, flags=%x, mode=%o):\n"
argument_list|,
name|fn
argument_list|,
operator|(
name|int
operator|)
name|uid
argument_list|,
operator|(
name|int
operator|)
name|gid
argument_list|,
name|flags
argument_list|,
name|mode
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|st
operator|==
name|NULL
condition|)
name|st
operator|=
operator|&
name|fstbuf
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|fn
argument_list|)
operator|>
sizeof|sizeof
name|fbuf
operator|-
literal|1
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\tpathname too long\n"
argument_list|)
expr_stmt|;
return|return
name|ENAMETOOLONG
return|;
block|}
name|strcpy
argument_list|(
name|fbuf
argument_list|,
name|fn
argument_list|)
expr_stmt|;
name|fn
operator|=
name|fbuf
expr_stmt|;
comment|/* ignore SFF_SAFEDIRPATH if we are debugging */
if|if
condition|(
name|RealUid
operator|!=
literal|0
operator|&&
name|RunAsUid
operator|==
name|RealUid
condition|)
name|flags
operator|&=
operator|~
name|SFF_SAFEDIRPATH
expr_stmt|;
comment|/* first check to see if the file exists at all */
ifdef|#
directive|ifdef
name|HASLSTAT
if|if
condition|(
operator|(
name|bitset
argument_list|(
name|SFF_NOSLINK
argument_list|,
name|flags
argument_list|)
condition|?
name|lstat
argument_list|(
name|fn
argument_list|,
name|st
argument_list|)
else|:
name|stat
argument_list|(
name|fn
argument_list|,
name|st
argument_list|)
operator|)
operator|<
literal|0
condition|)
else|#
directive|else
if|if
condition|(
name|stat
argument_list|(
name|fn
argument_list|,
name|st
argument_list|)
operator|<
literal|0
condition|)
endif|#
directive|endif
block|{
name|file_errno
operator|=
name|errno
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|bitset
argument_list|(
name|SFF_SETUIDOK
argument_list|,
name|flags
argument_list|)
operator|&&
operator|!
name|bitset
argument_list|(
name|S_IXUSR
operator||
name|S_IXGRP
operator||
name|S_IXOTH
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
operator|&&
name|S_ISREG
argument_list|(
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
comment|/* 		**  If final file is setuid, run as the owner of that 		**  file.  Gotta be careful not to reveal anything too 		**  soon here! 		*/
ifdef|#
directive|ifdef
name|SUID_ROOT_FILES_OK
if|if
condition|(
name|bitset
argument_list|(
name|S_ISUID
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
condition|)
else|#
directive|else
if|if
condition|(
name|bitset
argument_list|(
name|S_ISUID
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
operator|&&
name|st
operator|->
name|st_uid
operator|!=
literal|0
operator|&&
name|st
operator|->
name|st_uid
operator|!=
name|TrustedFileUid
condition|)
endif|#
directive|endif
block|{
name|uid
operator|=
name|st
operator|->
name|st_uid
expr_stmt|;
name|uname
operator|=
name|NULL
expr_stmt|;
block|}
ifdef|#
directive|ifdef
name|SUID_ROOT_FILES_OK
if|if
condition|(
name|bitset
argument_list|(
name|S_ISGID
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
condition|)
else|#
directive|else
if|if
condition|(
name|bitset
argument_list|(
name|S_ISGID
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
operator|&&
name|st
operator|->
name|st_gid
operator|!=
literal|0
condition|)
endif|#
directive|endif
name|gid
operator|=
name|st
operator|->
name|st_gid
expr_stmt|;
block|}
name|checkpath
operator|=
operator|!
name|bitset
argument_list|(
name|SFF_NOPATHCHECK
argument_list|,
name|flags
argument_list|)
operator|||
operator|(
name|uid
operator|==
literal|0
operator|&&
operator|!
name|bitset
argument_list|(
name|SFF_ROOTOK
operator||
name|SFF_OPENASROOT
argument_list|,
name|flags
argument_list|)
operator|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|SFF_NOWLINK
argument_list|,
name|flags
argument_list|)
operator|&&
operator|!
name|bitset
argument_list|(
name|SFF_SAFEDIRPATH
argument_list|,
name|flags
argument_list|)
condition|)
block|{
name|int
name|ret
decl_stmt|;
comment|/* check the directory */
name|p
operator|=
name|strrchr
argument_list|(
name|fn
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
block|{
name|ret
operator|=
name|safedirpath
argument_list|(
literal|"."
argument_list|,
name|uid
argument_list|,
name|gid
argument_list|,
name|uname
argument_list|,
name|flags
operator||
name|SFF_SAFEDIRPATH
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
name|ret
operator|=
name|safedirpath
argument_list|(
name|fn
argument_list|,
name|uid
argument_list|,
name|gid
argument_list|,
name|uname
argument_list|,
name|flags
operator||
name|SFF_SAFEDIRPATH
argument_list|)
expr_stmt|;
operator|*
name|p
operator|=
literal|'/'
expr_stmt|;
block|}
if|if
condition|(
name|ret
operator|==
literal|0
condition|)
block|{
comment|/* directory is safe */
name|checkpath
operator|=
name|FALSE
expr_stmt|;
block|}
else|else
block|{
ifdef|#
directive|ifdef
name|HASLSTAT
comment|/* Need lstat() information if called stat() before */
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|SFF_NOSLINK
argument_list|,
name|flags
argument_list|)
operator|&&
name|lstat
argument_list|(
name|fn
argument_list|,
name|st
argument_list|)
operator|<
literal|0
condition|)
block|{
name|ret
operator|=
name|errno
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t%s\n"
argument_list|,
name|errstring
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
endif|#
directive|endif
comment|/* directory is writable: disallow links */
name|flags
operator||=
name|SFF_NOLINK
expr_stmt|;
block|}
block|}
if|if
condition|(
name|checkpath
condition|)
block|{
name|int
name|ret
decl_stmt|;
name|p
operator|=
name|strrchr
argument_list|(
name|fn
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
block|{
name|ret
operator|=
name|safedirpath
argument_list|(
literal|"."
argument_list|,
name|uid
argument_list|,
name|gid
argument_list|,
name|uname
argument_list|,
name|flags
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
name|ret
operator|=
name|safedirpath
argument_list|(
name|fn
argument_list|,
name|uid
argument_list|,
name|gid
argument_list|,
name|uname
argument_list|,
name|flags
argument_list|)
expr_stmt|;
operator|*
name|p
operator|=
literal|'/'
expr_stmt|;
block|}
if|if
condition|(
name|ret
operator|!=
literal|0
condition|)
return|return
name|ret
return|;
block|}
comment|/* 	**  If the target file doesn't exist, check the directory to 	**  ensure that it is writable by this user. 	*/
if|if
condition|(
name|file_errno
operator|!=
literal|0
condition|)
block|{
name|int
name|ret
init|=
name|file_errno
decl_stmt|;
name|char
modifier|*
name|dir
init|=
name|fn
decl_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t%s\n"
argument_list|,
name|errstring
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|SFF_CREAT
argument_list|,
name|flags
argument_list|)
operator|||
name|file_errno
operator|!=
name|ENOENT
condition|)
return|return
name|ret
return|;
comment|/* check to see if legal to create the file */
name|p
operator|=
name|strrchr
argument_list|(
name|dir
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
name|dir
operator|=
literal|"."
expr_stmt|;
elseif|else
if|if
condition|(
name|p
operator|==
name|dir
condition|)
name|dir
operator|=
literal|"/"
expr_stmt|;
else|else
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|dir
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|>=
literal|0
condition|)
block|{
name|int
name|md
init|=
name|S_IWRITE
operator||
name|S_IEXEC
decl_stmt|;
if|if
condition|(
name|stbuf
operator|.
name|st_uid
operator|==
name|uid
condition|)
empty_stmt|;
elseif|else
if|if
condition|(
name|uid
operator|==
literal|0
operator|&&
name|TrustedFileUid
operator|!=
literal|0
operator|&&
name|stbuf
operator|.
name|st_uid
operator|==
name|TrustedFileUid
condition|)
empty_stmt|;
else|else
block|{
name|md
operator|>>=
literal|3
expr_stmt|;
if|if
condition|(
name|stbuf
operator|.
name|st_gid
operator|==
name|gid
condition|)
empty_stmt|;
ifndef|#
directive|ifndef
name|NO_GROUP_SET
elseif|else
if|if
condition|(
name|uname
operator|!=
name|NULL
operator|&&
operator|!
name|DontInitGroups
operator|&&
operator|(
operator|(
name|gr
operator|!=
name|NULL
operator|&&
name|gr
operator|->
name|gr_gid
operator|==
name|stbuf
operator|.
name|st_gid
operator|)
operator|||
operator|(
name|gr
operator|=
name|getgrgid
argument_list|(
name|stbuf
operator|.
name|st_gid
argument_list|)
operator|)
operator|!=
name|NULL
operator|)
condition|)
block|{
specifier|register
name|char
modifier|*
modifier|*
name|gp
decl_stmt|;
for|for
control|(
name|gp
operator|=
name|gr
operator|->
name|gr_mem
init|;
operator|*
name|gp
operator|!=
name|NULL
condition|;
name|gp
operator|++
control|)
if|if
condition|(
name|strcmp
argument_list|(
operator|*
name|gp
argument_list|,
name|uname
argument_list|)
operator|==
literal|0
condition|)
break|break;
if|if
condition|(
operator|*
name|gp
operator|==
name|NULL
condition|)
name|md
operator|>>=
literal|3
expr_stmt|;
block|}
endif|#
directive|endif
else|else
name|md
operator|>>=
literal|3
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|stbuf
operator|.
name|st_mode
operator|&
name|md
operator|)
operator|!=
name|md
condition|)
name|errno
operator|=
name|EACCES
expr_stmt|;
block|}
name|ret
operator|=
name|errno
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[final dir %s uid %d mode %lo] %s\n"
argument_list|,
name|dir
argument_list|,
operator|(
name|int
operator|)
name|stbuf
operator|.
name|st_uid
argument_list|,
operator|(
name|u_long
operator|)
name|stbuf
operator|.
name|st_mode
argument_list|,
name|errstring
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|=
literal|'/'
expr_stmt|;
name|st
operator|->
name|st_mode
operator|=
name|ST_MODE_NOFILE
expr_stmt|;
return|return
name|ret
return|;
block|}
ifdef|#
directive|ifdef
name|S_ISLNK
if|if
condition|(
name|bitset
argument_list|(
name|SFF_NOSLINK
argument_list|,
name|flags
argument_list|)
operator|&&
name|S_ISLNK
argument_list|(
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[slink mode %lo]\tE_SM_NOSLINK\n"
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|)
expr_stmt|;
return|return
name|E_SM_NOSLINK
return|;
block|}
endif|#
directive|endif
if|if
condition|(
name|bitset
argument_list|(
name|SFF_REGONLY
argument_list|,
name|flags
argument_list|)
operator|&&
operator|!
name|S_ISREG
argument_list|(
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[non-reg mode %lo]\tE_SM_REGONLY\n"
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|)
expr_stmt|;
return|return
name|E_SM_REGONLY
return|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|SFF_NOGWFILES
argument_list|,
name|flags
argument_list|)
operator|&&
name|bitset
argument_list|(
name|S_IWGRP
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[write bits %lo]\tE_SM_GWFILE\n"
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|)
expr_stmt|;
return|return
name|E_SM_GWFILE
return|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|SFF_NOWWFILES
argument_list|,
name|flags
argument_list|)
operator|&&
name|bitset
argument_list|(
name|S_IWOTH
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[write bits %lo]\tE_SM_WWFILE\n"
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|)
expr_stmt|;
return|return
name|E_SM_WWFILE
return|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|S_IWUSR
operator||
name|S_IWGRP
operator||
name|S_IWOTH
argument_list|,
name|mode
argument_list|)
operator|&&
name|bitset
argument_list|(
name|S_IXUSR
operator||
name|S_IXGRP
operator||
name|S_IXOTH
argument_list|,
name|st
operator|->
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[exec bits %lo]\tE_SM_ISEXEC]\n"
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|)
expr_stmt|;
return|return
name|E_SM_ISEXEC
return|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|SFF_NOHLINK
argument_list|,
name|flags
argument_list|)
operator|&&
name|st
operator|->
name|st_nlink
operator|!=
literal|1
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[link count %d]\tE_SM_NOHLINK\n"
argument_list|,
operator|(
name|int
operator|)
name|st
operator|->
name|st_nlink
argument_list|)
expr_stmt|;
return|return
name|E_SM_NOHLINK
return|;
block|}
if|if
condition|(
name|uid
operator|==
literal|0
operator|&&
name|bitset
argument_list|(
name|SFF_OPENASROOT
argument_list|,
name|flags
argument_list|)
condition|)
empty_stmt|;
elseif|else
if|if
condition|(
name|uid
operator|==
literal|0
operator|&&
operator|!
name|bitset
argument_list|(
name|SFF_ROOTOK
argument_list|,
name|flags
argument_list|)
condition|)
name|mode
operator|>>=
literal|6
expr_stmt|;
elseif|else
if|if
condition|(
name|st
operator|->
name|st_uid
operator|==
name|uid
condition|)
empty_stmt|;
elseif|else
if|if
condition|(
name|uid
operator|==
literal|0
operator|&&
name|TrustedFileUid
operator|!=
literal|0
operator|&&
name|st
operator|->
name|st_uid
operator|==
name|TrustedFileUid
condition|)
empty_stmt|;
else|else
block|{
name|mode
operator|>>=
literal|3
expr_stmt|;
if|if
condition|(
name|st
operator|->
name|st_gid
operator|==
name|gid
condition|)
empty_stmt|;
ifndef|#
directive|ifndef
name|NO_GROUP_SET
elseif|else
if|if
condition|(
name|uname
operator|!=
name|NULL
operator|&&
operator|!
name|DontInitGroups
operator|&&
operator|(
operator|(
name|gr
operator|!=
name|NULL
operator|&&
name|gr
operator|->
name|gr_gid
operator|==
name|st
operator|->
name|st_gid
operator|)
operator|||
operator|(
name|gr
operator|=
name|getgrgid
argument_list|(
name|st
operator|->
name|st_gid
argument_list|)
operator|)
operator|!=
name|NULL
operator|)
condition|)
block|{
specifier|register
name|char
modifier|*
modifier|*
name|gp
decl_stmt|;
for|for
control|(
name|gp
operator|=
name|gr
operator|->
name|gr_mem
init|;
operator|*
name|gp
operator|!=
name|NULL
condition|;
name|gp
operator|++
control|)
if|if
condition|(
name|strcmp
argument_list|(
operator|*
name|gp
argument_list|,
name|uname
argument_list|)
operator|==
literal|0
condition|)
break|break;
if|if
condition|(
operator|*
name|gp
operator|==
name|NULL
condition|)
name|mode
operator|>>=
literal|3
expr_stmt|;
block|}
endif|#
directive|endif
else|else
name|mode
operator|>>=
literal|3
expr_stmt|;
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[uid %d, nlink %d, stat %lo, mode %lo] "
argument_list|,
operator|(
name|int
operator|)
name|st
operator|->
name|st_uid
argument_list|,
operator|(
name|int
operator|)
name|st
operator|->
name|st_nlink
argument_list|,
operator|(
name|u_long
operator|)
name|st
operator|->
name|st_mode
argument_list|,
operator|(
name|u_long
operator|)
name|mode
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|st
operator|->
name|st_uid
operator|==
name|uid
operator|||
name|st
operator|->
name|st_uid
operator|==
literal|0
operator|||
name|st
operator|->
name|st_uid
operator|==
name|TrustedFileUid
operator|||
operator|!
name|bitset
argument_list|(
name|SFF_MUSTOWN
argument_list|,
name|flags
argument_list|)
operator|)
operator|&&
operator|(
name|st
operator|->
name|st_mode
operator|&
name|mode
operator|)
operator|==
name|mode
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\tOK\n"
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\tEACCES\n"
argument_list|)
expr_stmt|;
return|return
name|EACCES
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  SAFEDIRPATH -- check to make sure a path to a directory is safe ** **	Safe means not writable and owned by the right folks. ** **	Parameters: **		fn -- filename to check. **		uid -- user id to compare against. **		gid -- group id to compare against. **		uname -- user name to compare against (used for group **			sets). **		flags -- modifiers: **			SFF_ROOTOK -- ok to use root permissions to open. **			SFF_SAFEDIRPATH -- writable directories are considered **				to be fatal errors. ** **	Returns: **		0 -- if the directory path is "safe". **		else -- an error number associated with the path. */
end_comment

begin_function
name|int
name|safedirpath
parameter_list|(
name|fn
parameter_list|,
name|uid
parameter_list|,
name|gid
parameter_list|,
name|uname
parameter_list|,
name|flags
parameter_list|)
name|char
modifier|*
name|fn
decl_stmt|;
name|UID_T
name|uid
decl_stmt|;
name|GID_T
name|gid
decl_stmt|;
name|char
modifier|*
name|uname
decl_stmt|;
name|int
name|flags
decl_stmt|;
block|{
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|struct
name|group
modifier|*
name|gr
init|=
name|NULL
decl_stmt|;
name|int
name|ret
init|=
literal|0
decl_stmt|;
name|int
name|mode
init|=
name|S_IWOTH
decl_stmt|;
name|struct
name|stat
name|stbuf
decl_stmt|;
comment|/* special case root directory */
if|if
condition|(
operator|*
name|fn
operator|==
literal|'\0'
condition|)
name|fn
operator|=
literal|"/"
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"safedirpath(%s, uid=%ld, gid=%ld, flags=%x):\n"
argument_list|,
name|fn
argument_list|,
operator|(
name|long
operator|)
name|uid
argument_list|,
operator|(
name|long
operator|)
name|gid
argument_list|,
name|flags
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|DBS_GROUPWRITABLEDIRPATHSAFE
argument_list|,
name|DontBlameSendmail
argument_list|)
condition|)
name|mode
operator||=
name|S_IWGRP
expr_stmt|;
name|p
operator|=
name|fn
expr_stmt|;
do|do
block|{
if|if
condition|(
operator|*
name|p
operator|==
literal|'\0'
condition|)
operator|*
name|p
operator|=
literal|'/'
expr_stmt|;
name|p
operator|=
name|strchr
argument_list|(
operator|++
name|p
argument_list|,
literal|'/'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|fn
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|<
literal|0
condition|)
block|{
name|ret
operator|=
name|errno
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|(
name|uid
operator|==
literal|0
operator|||
name|bitset
argument_list|(
name|SFF_SAFEDIRPATH
argument_list|,
name|flags
argument_list|)
operator|)
operator|&&
name|bitset
argument_list|(
name|mode
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[dir %s] mode %lo\n"
argument_list|,
name|fn
argument_list|,
operator|(
name|u_long
operator|)
name|stbuf
operator|.
name|st_mode
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|SFF_SAFEDIRPATH
argument_list|,
name|flags
argument_list|)
condition|)
block|{
if|if
condition|(
name|bitset
argument_list|(
name|S_IWOTH
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
name|ret
operator|=
name|E_SM_WWDIR
expr_stmt|;
else|else
name|ret
operator|=
name|E_SM_GWDIR
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|Verbose
operator|>
literal|1
condition|)
name|message
argument_list|(
literal|"051 WARNING: %s writable directory %s"
argument_list|,
name|bitset
argument_list|(
name|S_IWOTH
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|?
literal|"World"
else|:
literal|"Group"
argument_list|,
name|fn
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|uid
operator|==
literal|0
operator|&&
operator|!
name|bitset
argument_list|(
name|SFF_ROOTOK
operator||
name|SFF_OPENASROOT
argument_list|,
name|flags
argument_list|)
condition|)
block|{
if|if
condition|(
name|bitset
argument_list|(
name|S_IXOTH
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
continue|continue;
name|ret
operator|=
name|EACCES
expr_stmt|;
break|break;
block|}
comment|/* 		** Let OS determine access to file if we are not 		** running as a privileged user.  This allows ACLs 		** to work. 		*/
if|if
condition|(
name|geteuid
argument_list|()
operator|!=
literal|0
condition|)
continue|continue;
if|if
condition|(
name|stbuf
operator|.
name|st_uid
operator|==
name|uid
operator|&&
name|bitset
argument_list|(
name|S_IXUSR
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
continue|continue;
if|if
condition|(
name|stbuf
operator|.
name|st_gid
operator|==
name|gid
operator|&&
name|bitset
argument_list|(
name|S_IXGRP
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
continue|continue;
ifndef|#
directive|ifndef
name|NO_GROUP_SET
if|if
condition|(
name|uname
operator|!=
name|NULL
operator|&&
operator|!
name|DontInitGroups
operator|&&
operator|(
operator|(
name|gr
operator|!=
name|NULL
operator|&&
name|gr
operator|->
name|gr_gid
operator|==
name|stbuf
operator|.
name|st_gid
operator|)
operator|||
operator|(
name|gr
operator|=
name|getgrgid
argument_list|(
name|stbuf
operator|.
name|st_gid
argument_list|)
operator|)
operator|!=
name|NULL
operator|)
condition|)
block|{
specifier|register
name|char
modifier|*
modifier|*
name|gp
decl_stmt|;
for|for
control|(
name|gp
operator|=
name|gr
operator|->
name|gr_mem
init|;
name|gp
operator|!=
name|NULL
operator|&&
operator|*
name|gp
operator|!=
name|NULL
condition|;
name|gp
operator|++
control|)
if|if
condition|(
name|strcmp
argument_list|(
operator|*
name|gp
argument_list|,
name|uname
argument_list|)
operator|==
literal|0
condition|)
break|break;
if|if
condition|(
name|gp
operator|!=
name|NULL
operator|&&
operator|*
name|gp
operator|!=
name|NULL
operator|&&
name|bitset
argument_list|(
name|S_IXGRP
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
continue|continue;
block|}
endif|#
directive|endif
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|S_IXOTH
argument_list|,
name|stbuf
operator|.
name|st_mode
argument_list|)
condition|)
block|{
name|ret
operator|=
name|EACCES
expr_stmt|;
break|break;
block|}
block|}
do|while
condition|(
name|p
operator|!=
name|NULL
condition|)
do|;
if|if
condition|(
name|ret
operator|!=
literal|0
operator|&&
name|tTd
argument_list|(
literal|44
argument_list|,
literal|4
argument_list|)
condition|)
name|printf
argument_list|(
literal|"\t[dir %s] %s\n"
argument_list|,
name|fn
argument_list|,
name|errstring
argument_list|(
name|ret
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|=
literal|'/'
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  SAFEOPEN -- do a file open with extra checking ** **	Parameters: **		fn -- the file name to open. **		omode -- the open-style mode flags. **		cmode -- the create-style mode flags. **		sff -- safefile flags. ** **	Returns: **		Same as open. */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|O_ACCMODE
end_ifndef

begin_define
define|#
directive|define
name|O_ACCMODE
value|(O_RDONLY|O_WRONLY|O_RDWR)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_function
name|int
name|safeopen
parameter_list|(
name|fn
parameter_list|,
name|omode
parameter_list|,
name|cmode
parameter_list|,
name|sff
parameter_list|)
name|char
modifier|*
name|fn
decl_stmt|;
name|int
name|omode
decl_stmt|;
name|int
name|cmode
decl_stmt|;
name|int
name|sff
decl_stmt|;
block|{
name|int
name|rval
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|int
name|smode
decl_stmt|;
name|struct
name|stat
name|stb
decl_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|O_CREAT
argument_list|,
name|omode
argument_list|)
condition|)
name|sff
operator||=
name|SFF_CREAT
expr_stmt|;
name|omode
operator|&=
operator|~
name|O_CREAT
expr_stmt|;
name|smode
operator|=
literal|0
expr_stmt|;
switch|switch
condition|(
name|omode
operator|&
name|O_ACCMODE
condition|)
block|{
case|case
name|O_RDONLY
case|:
name|smode
operator|=
name|S_IREAD
expr_stmt|;
break|break;
case|case
name|O_WRONLY
case|:
name|smode
operator|=
name|S_IWRITE
expr_stmt|;
break|break;
case|case
name|O_RDWR
case|:
name|smode
operator|=
name|S_IREAD
operator||
name|S_IWRITE
expr_stmt|;
break|break;
default|default:
name|smode
operator|=
literal|0
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|SFF_OPENASROOT
argument_list|,
name|sff
argument_list|)
condition|)
name|rval
operator|=
name|safefile
argument_list|(
name|fn
argument_list|,
name|RunAsUid
argument_list|,
name|RunAsGid
argument_list|,
name|RunAsUserName
argument_list|,
name|sff
argument_list|,
name|smode
argument_list|,
operator|&
name|stb
argument_list|)
expr_stmt|;
else|else
name|rval
operator|=
name|safefile
argument_list|(
name|fn
argument_list|,
name|RealUid
argument_list|,
name|RealGid
argument_list|,
name|RealUserName
argument_list|,
name|sff
argument_list|,
name|smode
argument_list|,
operator|&
name|stb
argument_list|)
expr_stmt|;
if|if
condition|(
name|rval
operator|!=
literal|0
condition|)
block|{
name|errno
operator|=
name|rval
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|stb
operator|.
name|st_mode
operator|==
name|ST_MODE_NOFILE
operator|&&
name|bitset
argument_list|(
name|SFF_CREAT
argument_list|,
name|sff
argument_list|)
condition|)
name|omode
operator||=
name|O_EXCL
operator||
name|O_CREAT
expr_stmt|;
name|fd
operator|=
name|dfopen
argument_list|(
name|fn
argument_list|,
name|omode
argument_list|,
name|cmode
argument_list|,
name|sff
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|<
literal|0
condition|)
return|return
name|fd
return|;
if|if
condition|(
name|filechanged
argument_list|(
name|fn
argument_list|,
name|fd
argument_list|,
operator|&
name|stb
argument_list|)
condition|)
block|{
name|syserr
argument_list|(
literal|"554 cannot open: file %s changed after open"
argument_list|,
name|fn
argument_list|)
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
name|errno
operator|=
name|E_SM_FILECHANGE
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
name|fd
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  SAFEFOPEN -- do a file open with extra checking ** **	Parameters: **		fn -- the file name to open. **		omode -- the open-style mode flags. **		cmode -- the create-style mode flags. **		sff -- safefile flags. ** **	Returns: **		Same as fopen. */
end_comment

begin_function
name|FILE
modifier|*
name|safefopen
parameter_list|(
name|fn
parameter_list|,
name|omode
parameter_list|,
name|cmode
parameter_list|,
name|sff
parameter_list|)
name|char
modifier|*
name|fn
decl_stmt|;
name|int
name|omode
decl_stmt|;
name|int
name|cmode
decl_stmt|;
name|int
name|sff
decl_stmt|;
block|{
name|int
name|fd
decl_stmt|;
name|FILE
modifier|*
name|fp
decl_stmt|;
name|char
modifier|*
name|fmode
decl_stmt|;
switch|switch
condition|(
name|omode
operator|&
name|O_ACCMODE
condition|)
block|{
case|case
name|O_RDONLY
case|:
name|fmode
operator|=
literal|"r"
expr_stmt|;
break|break;
case|case
name|O_WRONLY
case|:
if|if
condition|(
name|bitset
argument_list|(
name|O_APPEND
argument_list|,
name|omode
argument_list|)
condition|)
name|fmode
operator|=
literal|"a"
expr_stmt|;
else|else
name|fmode
operator|=
literal|"w"
expr_stmt|;
break|break;
case|case
name|O_RDWR
case|:
if|if
condition|(
name|bitset
argument_list|(
name|O_TRUNC
argument_list|,
name|omode
argument_list|)
condition|)
name|fmode
operator|=
literal|"w+"
expr_stmt|;
elseif|else
if|if
condition|(
name|bitset
argument_list|(
name|O_APPEND
argument_list|,
name|omode
argument_list|)
condition|)
name|fmode
operator|=
literal|"a+"
expr_stmt|;
else|else
name|fmode
operator|=
literal|"r+"
expr_stmt|;
break|break;
default|default:
name|syserr
argument_list|(
literal|"safefopen: unknown omode %o"
argument_list|,
name|omode
argument_list|)
expr_stmt|;
name|fmode
operator|=
literal|"x"
expr_stmt|;
block|}
name|fd
operator|=
name|safeopen
argument_list|(
name|fn
argument_list|,
name|omode
argument_list|,
name|cmode
argument_list|,
name|sff
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|<
literal|0
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|10
argument_list|)
condition|)
name|printf
argument_list|(
literal|"safefopen: safeopen failed: %s\n"
argument_list|,
name|errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|fp
operator|=
name|fdopen
argument_list|(
name|fd
argument_list|,
name|fmode
argument_list|)
expr_stmt|;
if|if
condition|(
name|fp
operator|!=
name|NULL
condition|)
return|return
name|fp
return|;
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|10
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"safefopen: fdopen(%s, %s) failed: omode=%x, sff=%x, err=%s\n"
argument_list|,
name|fn
argument_list|,
name|fmode
argument_list|,
name|omode
argument_list|,
name|sff
argument_list|,
name|errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
ifndef|#
directive|ifndef
name|NOT_SENDMAIL
name|dumpfd
argument_list|(
name|fd
argument_list|,
name|TRUE
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
operator|(
name|void
operator|)
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  FILECHANGED -- check to see if file changed after being opened ** **	Parameters: **		fn -- pathname of file to check. **		fd -- file descriptor to check. **		stb -- stat structure from before open. ** **	Returns: **		TRUE -- if a problem was detected. **		FALSE -- if this file is still the same. */
end_comment

begin_function
name|bool
name|filechanged
parameter_list|(
name|fn
parameter_list|,
name|fd
parameter_list|,
name|stb
parameter_list|)
name|char
modifier|*
name|fn
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|struct
name|stat
modifier|*
name|stb
decl_stmt|;
block|{
name|struct
name|stat
name|sta
decl_stmt|;
if|if
condition|(
name|stb
operator|->
name|st_mode
operator|==
name|ST_MODE_NOFILE
condition|)
block|{
if|#
directive|if
name|HASLSTAT
operator|&&
name|BOGUS_O_EXCL
comment|/* only necessary if exclusive open follows symbolic links */
if|if
condition|(
name|lstat
argument_list|(
name|fn
argument_list|,
name|stb
argument_list|)
operator|<
literal|0
operator|||
name|stb
operator|->
name|st_nlink
operator|!=
literal|1
condition|)
return|return
name|TRUE
return|;
else|#
directive|else
return|return
name|FALSE
return|;
endif|#
directive|endif
block|}
if|if
condition|(
name|fstat
argument_list|(
name|fd
argument_list|,
operator|&
name|sta
argument_list|)
operator|<
literal|0
condition|)
return|return
name|TRUE
return|;
if|if
condition|(
name|sta
operator|.
name|st_nlink
operator|!=
name|stb
operator|->
name|st_nlink
operator|||
name|sta
operator|.
name|st_dev
operator|!=
name|stb
operator|->
name|st_dev
operator|||
name|sta
operator|.
name|st_ino
operator|!=
name|stb
operator|->
name|st_ino
operator|||
if|#
directive|if
name|HAS_ST_GEN
operator|&&
literal|0
comment|/* AFS returns garbage in st_gen */
name|sta
operator|.
name|st_gen
operator|!=
name|stb
operator|->
name|st_gen
operator|||
endif|#
directive|endif
name|sta
operator|.
name|st_uid
operator|!=
name|stb
operator|->
name|st_uid
operator|||
name|sta
operator|.
name|st_gid
operator|!=
name|stb
operator|->
name|st_gid
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|44
argument_list|,
literal|8
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"File changed after opening:\n"
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|" nlink	= %ld/%ld\n"
argument_list|,
operator|(
name|long
operator|)
name|stb
operator|->
name|st_nlink
argument_list|,
operator|(
name|long
operator|)
name|sta
operator|.
name|st_nlink
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|" dev	= %ld/%ld\n"
argument_list|,
operator|(
name|long
operator|)
name|stb
operator|->
name|st_dev
argument_list|,
operator|(
name|long
operator|)
name|sta
operator|.
name|st_dev
argument_list|)
expr_stmt|;
if|if
condition|(
sizeof|sizeof
name|sta
operator|.
name|st_ino
operator|>
sizeof|sizeof
argument_list|(
name|long
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|" ino	= %s/"
argument_list|,
name|quad_to_string
argument_list|(
name|stb
operator|->
name|st_ino
argument_list|)
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"%s\n"
argument_list|,
name|quad_to_string
argument_list|(
name|sta
operator|.
name|st_ino
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
name|printf
argument_list|(
literal|" ino	= %lu/%lu\n"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|stb
operator|->
name|st_ino
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|sta
operator|.
name|st_ino
argument_list|)
expr_stmt|;
if|#
directive|if
name|HAS_ST_GEN
name|printf
argument_list|(
literal|" gen	= %ld/%ld\n"
argument_list|,
operator|(
name|long
operator|)
name|stb
operator|->
name|st_gen
argument_list|,
operator|(
name|long
operator|)
name|sta
operator|.
name|st_gen
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|printf
argument_list|(
literal|" uid	= %ld/%ld\n"
argument_list|,
operator|(
name|long
operator|)
name|stb
operator|->
name|st_uid
argument_list|,
operator|(
name|long
operator|)
name|sta
operator|.
name|st_uid
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|" gid	= %ld/%ld\n"
argument_list|,
operator|(
name|long
operator|)
name|stb
operator|->
name|st_gid
argument_list|,
operator|(
name|long
operator|)
name|sta
operator|.
name|st_gid
argument_list|)
expr_stmt|;
block|}
return|return
name|TRUE
return|;
block|}
return|return
name|FALSE
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/* **  DFOPEN -- determined file open ** **	This routine has the semantics of open, except that it will **	keep trying a few times to make this happen.  The idea is that **	on very loaded systems, we may run out of resources (inodes, **	whatever), so this tries to get around it. */
end_comment

begin_function
name|int
name|dfopen
parameter_list|(
name|filename
parameter_list|,
name|omode
parameter_list|,
name|cmode
parameter_list|,
name|sff
parameter_list|)
name|char
modifier|*
name|filename
decl_stmt|;
name|int
name|omode
decl_stmt|;
name|int
name|cmode
decl_stmt|;
name|int
name|sff
decl_stmt|;
block|{
specifier|register
name|int
name|tries
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
for|for
control|(
name|tries
operator|=
literal|0
init|;
name|tries
operator|<
literal|10
condition|;
name|tries
operator|++
control|)
block|{
name|sleep
argument_list|(
call|(
name|unsigned
call|)
argument_list|(
literal|10
operator|*
name|tries
argument_list|)
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|fd
operator|=
name|open
argument_list|(
name|filename
argument_list|,
name|omode
argument_list|,
name|cmode
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|>=
literal|0
condition|)
break|break;
switch|switch
condition|(
name|errno
condition|)
block|{
case|case
name|ENFILE
case|:
comment|/* system file table full */
case|case
name|EINTR
case|:
comment|/* interrupted syscall */
ifdef|#
directive|ifdef
name|ETXTBSY
case|case
name|ETXTBSY
case|:
comment|/* Apollo: net file locked */
endif|#
directive|endif
continue|continue;
block|}
break|break;
block|}
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|SFF_NOLOCK
argument_list|,
name|sff
argument_list|)
operator|&&
name|fd
operator|>=
literal|0
operator|&&
name|fstat
argument_list|(
name|fd
argument_list|,
operator|&
name|st
argument_list|)
operator|>=
literal|0
operator|&&
name|S_ISREG
argument_list|(
name|st
operator|.
name|st_mode
argument_list|)
condition|)
block|{
name|int
name|locktype
decl_stmt|;
comment|/* lock the file to avoid accidental conflicts */
if|if
condition|(
operator|(
name|omode
operator|&
name|O_ACCMODE
operator|)
operator|!=
name|O_RDONLY
condition|)
name|locktype
operator|=
name|LOCK_EX
expr_stmt|;
else|else
name|locktype
operator|=
name|LOCK_SH
expr_stmt|;
operator|(
name|void
operator|)
name|lockfile
argument_list|(
name|fd
argument_list|,
name|filename
argument_list|,
name|NULL
argument_list|,
name|locktype
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
block|}
return|return
name|fd
return|;
block|}
end_function

end_unit

