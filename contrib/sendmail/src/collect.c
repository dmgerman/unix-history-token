begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1998-2006 Sendmail, Inc. and its suppliers.  *	All rights reserved.  * Copyright (c) 1983, 1995-1997 Eric P. Allman.  All rights reserved.  * Copyright (c) 1988, 1993  *	The Regents of the University of California.  All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_include
include|#
directive|include
file|<sendmail.h>
end_include

begin_macro
name|SM_RCSID
argument_list|(
literal|"@(#)$Id: collect.c,v 8.280 2006/11/29 00:20:40 ca Exp $"
argument_list|)
end_macro

begin_decl_stmt
specifier|static
name|void
name|eatfrom
name|__P
argument_list|(
operator|(
name|char
operator|*
specifier|volatile
operator|,
name|ENVELOPE
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|collect_doheader
name|__P
argument_list|(
operator|(
name|ENVELOPE
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|SM_FILE_T
modifier|*
name|collect_dfopen
name|__P
argument_list|(
operator|(
name|ENVELOPE
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|SM_FILE_T
modifier|*
name|collect_eoh
name|__P
argument_list|(
operator|(
name|ENVELOPE
operator|*
operator|,
name|int
operator|,
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  COLLECT_EOH -- end-of-header processing in collect() ** **	Called by collect() when it encounters the blank line **	separating the header from the message body, or when it **	encounters EOF in a message that contains only a header. ** **	Parameters: **		e -- envelope **		numhdrs -- number of headers **		hdrslen -- length of headers ** **	Results: **		NULL, or handle to open data file ** **	Side Effects: **		end-of-header check ruleset is invoked. **		envelope state is updated. **		headers may be added and deleted. **		selects the queue. **		opens the data file. */
end_comment

begin_function
specifier|static
name|SM_FILE_T
modifier|*
name|collect_eoh
parameter_list|(
name|e
parameter_list|,
name|numhdrs
parameter_list|,
name|hdrslen
parameter_list|)
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
name|int
name|numhdrs
decl_stmt|;
name|int
name|hdrslen
decl_stmt|;
block|{
name|char
name|hnum
index|[
literal|16
index|]
decl_stmt|;
name|char
name|hsize
index|[
literal|16
index|]
decl_stmt|;
comment|/* call the end-of-header check ruleset */
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|hnum
argument_list|,
sizeof|sizeof
argument_list|(
name|hnum
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|numhdrs
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|hsize
argument_list|,
sizeof|sizeof
argument_list|(
name|hsize
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|hdrslen
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|10
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"collect: rscheck(\"check_eoh\", \"%s $| %s\")\n"
argument_list|,
name|hnum
argument_list|,
name|hsize
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|rscheck
argument_list|(
literal|"check_eoh"
argument_list|,
name|hnum
argument_list|,
name|hsize
argument_list|,
name|e
argument_list|,
name|RSF_UNSTRUCTURED
operator||
name|RSF_COUNT
argument_list|,
literal|3
argument_list|,
name|NULL
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* 	**  Process the header, 	**  select the queue, open the data file. 	*/
name|collect_doheader
argument_list|(
name|e
argument_list|)
expr_stmt|;
return|return
name|collect_dfopen
argument_list|(
name|e
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* **  COLLECT_DOHEADER -- process header in collect() ** **	Called by collect() after it has finished parsing the header, **	but before it selects the queue and creates the data file. **	The results of processing the header will affect queue selection. ** **	Parameters: **		e -- envelope ** **	Results: **		none. ** **	Side Effects: **		envelope state is updated. **		headers may be added and deleted. */
end_comment

begin_function
specifier|static
name|void
name|collect_doheader
parameter_list|(
name|e
parameter_list|)
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
comment|/* 	**  Find out some information from the headers. 	**	Examples are who is the from person& the date. 	*/
name|eatheader
argument_list|(
name|e
argument_list|,
name|true
argument_list|,
name|false
argument_list|)
expr_stmt|;
if|if
condition|(
name|GrabTo
operator|&&
name|e
operator|->
name|e_sendqueue
operator|==
name|NULL
condition|)
name|usrerr
argument_list|(
literal|"No recipient addresses found in header"
argument_list|)
expr_stmt|;
comment|/* 	**  If we have a Return-Receipt-To:, turn it into a DSN. 	*/
if|if
condition|(
name|RrtImpliesDsn
operator|&&
name|hvalue
argument_list|(
literal|"return-receipt-to"
argument_list|,
name|e
operator|->
name|e_header
argument_list|)
operator|!=
name|NULL
condition|)
block|{
name|ADDRESS
modifier|*
name|q
decl_stmt|;
for|for
control|(
name|q
operator|=
name|e
operator|->
name|e_sendqueue
init|;
name|q
operator|!=
name|NULL
condition|;
name|q
operator|=
name|q
operator|->
name|q_next
control|)
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|QHASNOTIFY
argument_list|,
name|q
operator|->
name|q_flags
argument_list|)
condition|)
name|q
operator|->
name|q_flags
operator||=
name|QHASNOTIFY
operator||
name|QPINGONSUCCESS
expr_stmt|;
block|}
comment|/* 	**  Add an appropriate recipient line if we have none. 	*/
if|if
condition|(
name|hvalue
argument_list|(
literal|"to"
argument_list|,
name|e
operator|->
name|e_header
argument_list|)
operator|!=
name|NULL
operator|||
name|hvalue
argument_list|(
literal|"cc"
argument_list|,
name|e
operator|->
name|e_header
argument_list|)
operator|!=
name|NULL
operator|||
name|hvalue
argument_list|(
literal|"apparently-to"
argument_list|,
name|e
operator|->
name|e_header
argument_list|)
operator|!=
name|NULL
condition|)
block|{
comment|/* have a valid recipient header -- delete Bcc: headers */
name|e
operator|->
name|e_flags
operator||=
name|EF_DELETE_BCC
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|hvalue
argument_list|(
literal|"bcc"
argument_list|,
name|e
operator|->
name|e_header
argument_list|)
operator|==
name|NULL
condition|)
block|{
comment|/* no valid recipient headers */
specifier|register
name|ADDRESS
modifier|*
name|q
decl_stmt|;
name|char
modifier|*
name|hdr
init|=
name|NULL
decl_stmt|;
comment|/* create a recipient field */
switch|switch
condition|(
name|NoRecipientAction
condition|)
block|{
case|case
name|NRA_ADD_APPARENTLY_TO
case|:
name|hdr
operator|=
literal|"Apparently-To"
expr_stmt|;
break|break;
case|case
name|NRA_ADD_TO
case|:
name|hdr
operator|=
literal|"To"
expr_stmt|;
break|break;
case|case
name|NRA_ADD_BCC
case|:
name|addheader
argument_list|(
literal|"Bcc"
argument_list|,
literal|" "
argument_list|,
literal|0
argument_list|,
name|e
argument_list|,
name|true
argument_list|)
expr_stmt|;
break|break;
case|case
name|NRA_ADD_TO_UNDISCLOSED
case|:
name|addheader
argument_list|(
literal|"To"
argument_list|,
literal|"undisclosed-recipients:;"
argument_list|,
literal|0
argument_list|,
name|e
argument_list|,
name|true
argument_list|)
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|hdr
operator|!=
name|NULL
condition|)
block|{
for|for
control|(
name|q
operator|=
name|e
operator|->
name|e_sendqueue
init|;
name|q
operator|!=
name|NULL
condition|;
name|q
operator|=
name|q
operator|->
name|q_next
control|)
block|{
if|if
condition|(
name|q
operator|->
name|q_alias
operator|!=
name|NULL
condition|)
continue|continue;
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|3
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Adding %s: %s\n"
argument_list|,
name|hdr
argument_list|,
name|q
operator|->
name|q_paddr
argument_list|)
expr_stmt|;
name|addheader
argument_list|(
name|hdr
argument_list|,
name|q
operator|->
name|q_paddr
argument_list|,
literal|0
argument_list|,
name|e
argument_list|,
name|true
argument_list|)
expr_stmt|;
block|}
block|}
block|}
block|}
end_function

begin_comment
comment|/* **  COLLECT_DFOPEN -- open the message data file ** **	Called by collect() after it has finished processing the header. **	Queue selection occurs at this point, possibly based on the **	envelope's recipient list and on header information. ** **	Parameters: **		e -- envelope ** **	Results: **		NULL, or a pointer to an open data file, **		into which the message body will be written by collect(). ** **	Side Effects: **		Calls syserr, sets EF_FATALERRS and returns NULL **		if there is insufficient disk space. **		Aborts process if data file could not be opened. **		Otherwise, the queue is selected, **		e->e_{dfino,dfdev,msgsize,flags} are updated, **		and a pointer to an open data file is returned. */
end_comment

begin_function
specifier|static
name|SM_FILE_T
modifier|*
name|collect_dfopen
parameter_list|(
name|e
parameter_list|)
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
name|MODE_T
name|oldumask
init|=
literal|0
decl_stmt|;
name|int
name|dfd
decl_stmt|;
name|struct
name|stat
name|stbuf
decl_stmt|;
name|SM_FILE_T
modifier|*
name|df
decl_stmt|;
name|char
modifier|*
name|dfname
decl_stmt|;
if|if
condition|(
operator|!
name|setnewqueue
argument_list|(
name|e
argument_list|)
condition|)
return|return
name|NULL
return|;
name|dfname
operator|=
name|queuename
argument_list|(
name|e
argument_list|,
name|DATAFL_LETTER
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|S_IWGRP
argument_list|,
name|QueueFileMode
argument_list|)
condition|)
name|oldumask
operator|=
name|umask
argument_list|(
literal|002
argument_list|)
expr_stmt|;
name|df
operator|=
name|bfopen
argument_list|(
name|dfname
argument_list|,
name|QueueFileMode
argument_list|,
name|DataFileBufferSize
argument_list|,
name|SFF_OPENASROOT
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|S_IWGRP
argument_list|,
name|QueueFileMode
argument_list|)
condition|)
operator|(
name|void
operator|)
name|umask
argument_list|(
name|oldumask
argument_list|)
expr_stmt|;
if|if
condition|(
name|df
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"@Cannot create %s"
argument_list|,
name|dfname
argument_list|)
expr_stmt|;
name|e
operator|->
name|e_flags
operator||=
name|EF_NO_BODY_RETN
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
name|dfd
operator|=
name|sm_io_getinfo
argument_list|(
name|df
argument_list|,
name|SM_IO_WHAT_FD
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|dfd
operator|<
literal|0
operator|||
name|fstat
argument_list|(
name|dfd
argument_list|,
operator|&
name|stbuf
argument_list|)
operator|<
literal|0
condition|)
name|e
operator|->
name|e_dfino
operator|=
operator|-
literal|1
expr_stmt|;
else|else
block|{
name|e
operator|->
name|e_dfdev
operator|=
name|stbuf
operator|.
name|st_dev
expr_stmt|;
name|e
operator|->
name|e_dfino
operator|=
name|stbuf
operator|.
name|st_ino
expr_stmt|;
block|}
name|e
operator|->
name|e_flags
operator||=
name|EF_HAS_DF
expr_stmt|;
return|return
name|df
return|;
block|}
end_function

begin_comment
comment|/* **  COLLECT -- read& parse message header& make temp file. ** **	Creates a temporary file name and copies the standard **	input to that file.  Leading UNIX-style "From" lines are **	stripped off (after important information is extracted). ** **	Parameters: **		fp -- file to read. **		smtpmode -- if set, we are running SMTP: give an RFC821 **			style message to say we are ready to collect **			input, and never ignore a single dot to mean **			end of message. **		hdrp -- the location to stash the header. **		e -- the current envelope. **		rsetsize -- reset e_msgsize? ** **	Returns: **		none. ** **	Side Effects: **		If successful, **		- Data file is created and filled, and e->e_dfp is set. **		- The from person may be set. **		If the "enough disk space" check fails, **		- syserr is called. **		- e->e_dfp is NULL. **		- e->e_flags& EF_FATALERRS is set. **		- collect() returns. **		If data file cannot be created, the process is terminated. */
end_comment

begin_comment
comment|/* values for input state machine */
end_comment

begin_define
define|#
directive|define
name|IS_NORM
value|0
end_define

begin_comment
comment|/* middle of line */
end_comment

begin_define
define|#
directive|define
name|IS_BOL
value|1
end_define

begin_comment
comment|/* beginning of line */
end_comment

begin_define
define|#
directive|define
name|IS_DOT
value|2
end_define

begin_comment
comment|/* read a dot at beginning of line */
end_comment

begin_define
define|#
directive|define
name|IS_DOTCR
value|3
end_define

begin_comment
comment|/* read ".\r" at beginning of line */
end_comment

begin_define
define|#
directive|define
name|IS_CR
value|4
end_define

begin_comment
comment|/* read a carriage return */
end_comment

begin_comment
comment|/* values for message state machine */
end_comment

begin_define
define|#
directive|define
name|MS_UFROM
value|0
end_define

begin_comment
comment|/* reading Unix from line */
end_comment

begin_define
define|#
directive|define
name|MS_HEADER
value|1
end_define

begin_comment
comment|/* reading message header */
end_comment

begin_define
define|#
directive|define
name|MS_BODY
value|2
end_define

begin_comment
comment|/* reading message body */
end_comment

begin_define
define|#
directive|define
name|MS_DISCARD
value|3
end_define

begin_comment
comment|/* discarding rest of message */
end_comment

begin_function
name|void
name|collect
parameter_list|(
name|fp
parameter_list|,
name|smtpmode
parameter_list|,
name|hdrp
parameter_list|,
name|e
parameter_list|,
name|rsetsize
parameter_list|)
name|SM_FILE_T
modifier|*
name|fp
decl_stmt|;
name|bool
name|smtpmode
decl_stmt|;
name|HDR
modifier|*
modifier|*
name|hdrp
decl_stmt|;
specifier|register
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
name|bool
name|rsetsize
decl_stmt|;
block|{
specifier|register
name|SM_FILE_T
modifier|*
name|df
decl_stmt|;
name|bool
name|ignrdot
decl_stmt|;
name|int
name|dbto
decl_stmt|;
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
name|int
name|c
decl_stmt|;
name|bool
name|inputerr
decl_stmt|;
name|bool
name|headeronly
decl_stmt|;
name|char
modifier|*
name|buf
decl_stmt|;
name|int
name|buflen
decl_stmt|;
name|int
name|istate
decl_stmt|;
name|int
name|mstate
decl_stmt|;
name|int
name|hdrslen
decl_stmt|;
name|int
name|numhdrs
decl_stmt|;
name|int
name|afd
decl_stmt|;
name|unsigned
name|char
modifier|*
name|pbp
decl_stmt|;
name|unsigned
name|char
name|peekbuf
index|[
literal|8
index|]
decl_stmt|;
name|char
name|bufbuf
index|[
name|MAXLINE
index|]
decl_stmt|;
name|df
operator|=
name|NULL
expr_stmt|;
name|ignrdot
operator|=
name|smtpmode
condition|?
name|false
else|:
name|IgnrDot
expr_stmt|;
comment|/* timeout for I/O functions is in milliseconds */
name|dbto
operator|=
name|smtpmode
condition|?
operator|(
operator|(
name|int
operator|)
name|TimeOuts
operator|.
name|to_datablock
operator|*
literal|1000
operator|)
else|:
name|SM_TIME_FOREVER
expr_stmt|;
name|sm_io_setinfo
argument_list|(
name|fp
argument_list|,
name|SM_IO_WHAT_TIMEOUT
argument_list|,
operator|&
name|dbto
argument_list|)
expr_stmt|;
name|c
operator|=
name|SM_IO_EOF
expr_stmt|;
name|inputerr
operator|=
name|false
expr_stmt|;
name|headeronly
operator|=
name|hdrp
operator|!=
name|NULL
expr_stmt|;
name|hdrslen
operator|=
literal|0
expr_stmt|;
name|numhdrs
operator|=
literal|0
expr_stmt|;
name|HasEightBits
operator|=
name|false
expr_stmt|;
name|buf
operator|=
name|bp
operator|=
name|bufbuf
expr_stmt|;
name|buflen
operator|=
sizeof|sizeof
argument_list|(
name|bufbuf
argument_list|)
expr_stmt|;
name|pbp
operator|=
name|peekbuf
expr_stmt|;
name|istate
operator|=
name|IS_BOL
expr_stmt|;
name|mstate
operator|=
name|SaveFrom
condition|?
name|MS_HEADER
else|:
name|MS_UFROM
expr_stmt|;
comment|/* 	**  Tell ARPANET to go ahead. 	*/
if|if
condition|(
name|smtpmode
condition|)
name|message
argument_list|(
literal|"354 Enter mail, end with \".\" on a line by itself"
argument_list|)
expr_stmt|;
comment|/* simulate an I/O timeout when used as sink */
if|if
condition|(
name|tTd
argument_list|(
literal|83
argument_list|,
literal|101
argument_list|)
condition|)
name|sleep
argument_list|(
literal|319
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|2
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"collect\n"
argument_list|)
expr_stmt|;
comment|/* 	**  Read the message. 	** 	**	This is done using two interleaved state machines. 	**	The input state machine is looking for things like 	**	hidden dots; the message state machine is handling 	**	the larger picture (e.g., header versus body). 	*/
if|if
condition|(
name|rsetsize
condition|)
name|e
operator|->
name|e_msgsize
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|35
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"top, istate=%d, mstate=%d\n"
argument_list|,
name|istate
argument_list|,
name|mstate
argument_list|)
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|pbp
operator|>
name|peekbuf
condition|)
name|c
operator|=
operator|*
operator|--
name|pbp
expr_stmt|;
else|else
block|{
while|while
condition|(
operator|!
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
operator|&&
operator|!
name|sm_io_error
argument_list|(
name|fp
argument_list|)
condition|)
block|{
name|errno
operator|=
literal|0
expr_stmt|;
name|c
operator|=
name|sm_io_getc
argument_list|(
name|fp
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
if|if
condition|(
name|c
operator|==
name|SM_IO_EOF
operator|&&
name|errno
operator|==
name|EINTR
condition|)
block|{
comment|/* Interrupted, retry */
name|sm_io_clearerr
argument_list|(
name|fp
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* timeout? */
if|if
condition|(
name|c
operator|==
name|SM_IO_EOF
operator|&&
name|errno
operator|==
name|EAGAIN
operator|&&
name|smtpmode
condition|)
block|{
comment|/* 						**  Override e_message in 						**  usrerr() as this is the 						**  reason for failure that 						**  should be logged for 						**  undelivered recipients. 						*/
name|e
operator|->
name|e_message
operator|=
name|NULL
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|inputerr
operator|=
name|true
expr_stmt|;
goto|goto
name|readabort
goto|;
block|}
break|break;
block|}
if|if
condition|(
name|TrafficLogFile
operator|!=
name|NULL
operator|&&
operator|!
name|headeronly
condition|)
block|{
if|if
condition|(
name|istate
operator|==
name|IS_BOL
condition|)
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|TrafficLogFile
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"%05d<<< "
argument_list|,
operator|(
name|int
operator|)
name|CurrentPid
argument_list|)
expr_stmt|;
if|if
condition|(
name|c
operator|==
name|SM_IO_EOF
condition|)
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|TrafficLogFile
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"[EOF]\n"
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|sm_io_putc
argument_list|(
name|TrafficLogFile
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|c
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|c
operator|==
name|SM_IO_EOF
condition|)
goto|goto
name|readerr
goto|;
if|if
condition|(
name|SevenBitInput
condition|)
name|c
operator|&=
literal|0x7f
expr_stmt|;
else|else
name|HasEightBits
operator||=
name|bitset
argument_list|(
literal|0x80
argument_list|,
name|c
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|94
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"istate=%d, c=%c (0x%x)\n"
argument_list|,
name|istate
argument_list|,
operator|(
name|char
operator|)
name|c
argument_list|,
name|c
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|istate
condition|)
block|{
case|case
name|IS_BOL
case|:
if|if
condition|(
name|c
operator|==
literal|'.'
condition|)
block|{
name|istate
operator|=
name|IS_DOT
expr_stmt|;
continue|continue;
block|}
break|break;
case|case
name|IS_DOT
case|:
if|if
condition|(
name|c
operator|==
literal|'\n'
operator|&&
operator|!
name|ignrdot
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_NL_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
goto|goto
name|readerr
goto|;
elseif|else
if|if
condition|(
name|c
operator|==
literal|'\r'
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_CRLF_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|istate
operator|=
name|IS_DOTCR
expr_stmt|;
continue|continue;
block|}
elseif|else
if|if
condition|(
name|ignrdot
operator|||
operator|(
name|c
operator|!=
literal|'.'
operator|&&
name|OpMode
operator|!=
name|MD_SMTP
operator|&&
name|OpMode
operator|!=
name|MD_DAEMON
operator|&&
name|OpMode
operator|!=
name|MD_ARPAFTP
operator|)
condition|)
block|{
name|SM_ASSERT
argument_list|(
name|pbp
operator|<
name|peekbuf
operator|+
sizeof|sizeof
argument_list|(
name|peekbuf
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|pbp
operator|++
operator|=
name|c
expr_stmt|;
name|c
operator|=
literal|'.'
expr_stmt|;
block|}
break|break;
case|case
name|IS_DOTCR
case|:
if|if
condition|(
name|c
operator|==
literal|'\n'
operator|&&
operator|!
name|ignrdot
condition|)
goto|goto
name|readerr
goto|;
else|else
block|{
comment|/* push back the ".\rx" */
name|SM_ASSERT
argument_list|(
name|pbp
operator|<
name|peekbuf
operator|+
sizeof|sizeof
argument_list|(
name|peekbuf
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|pbp
operator|++
operator|=
name|c
expr_stmt|;
if|if
condition|(
name|OpMode
operator|!=
name|MD_SMTP
operator|&&
name|OpMode
operator|!=
name|MD_DAEMON
operator|&&
name|OpMode
operator|!=
name|MD_ARPAFTP
condition|)
block|{
name|SM_ASSERT
argument_list|(
name|pbp
operator|<
name|peekbuf
operator|+
sizeof|sizeof
argument_list|(
name|peekbuf
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|pbp
operator|++
operator|=
literal|'\r'
expr_stmt|;
name|c
operator|=
literal|'.'
expr_stmt|;
block|}
else|else
name|c
operator|=
literal|'\r'
expr_stmt|;
block|}
break|break;
case|case
name|IS_CR
case|:
if|if
condition|(
name|c
operator|==
literal|'\n'
condition|)
name|istate
operator|=
name|IS_BOL
expr_stmt|;
else|else
block|{
operator|(
name|void
operator|)
name|sm_io_ungetc
argument_list|(
name|fp
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|c
argument_list|)
expr_stmt|;
name|c
operator|=
literal|'\r'
expr_stmt|;
name|istate
operator|=
name|IS_NORM
expr_stmt|;
block|}
goto|goto
name|bufferchar
goto|;
block|}
if|if
condition|(
name|c
operator|==
literal|'\r'
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_CRLF_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|istate
operator|=
name|IS_CR
expr_stmt|;
continue|continue;
block|}
elseif|else
if|if
condition|(
name|c
operator|==
literal|'\n'
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_NL_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
name|istate
operator|=
name|IS_BOL
expr_stmt|;
else|else
name|istate
operator|=
name|IS_NORM
expr_stmt|;
name|bufferchar
label|:
if|if
condition|(
operator|!
name|headeronly
condition|)
block|{
comment|/* no overflow? */
if|if
condition|(
name|e
operator|->
name|e_msgsize
operator|>=
literal|0
condition|)
block|{
name|e
operator|->
name|e_msgsize
operator|++
expr_stmt|;
if|if
condition|(
name|MaxMessageSize
operator|>
literal|0
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_TOOBIG
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
operator|&&
name|e
operator|->
name|e_msgsize
operator|>
name|MaxMessageSize
condition|)
name|e
operator|->
name|e_flags
operator||=
name|EF_TOOBIG
expr_stmt|;
block|}
block|}
switch|switch
condition|(
name|mstate
condition|)
block|{
case|case
name|MS_BODY
case|:
comment|/* just put the character out */
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|EF_TOOBIG
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
operator|(
name|void
operator|)
name|sm_io_putc
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|c
argument_list|)
expr_stmt|;
comment|/* FALLTHROUGH */
case|case
name|MS_DISCARD
case|:
continue|continue;
block|}
name|SM_ASSERT
argument_list|(
name|mstate
operator|==
name|MS_UFROM
operator|||
name|mstate
operator|==
name|MS_HEADER
argument_list|)
expr_stmt|;
comment|/* header -- buffer up */
if|if
condition|(
name|bp
operator|>=
operator|&
name|buf
index|[
name|buflen
operator|-
literal|2
index|]
condition|)
block|{
name|char
modifier|*
name|obuf
decl_stmt|;
comment|/* out of space for header */
name|obuf
operator|=
name|buf
expr_stmt|;
if|if
condition|(
name|buflen
operator|<
name|MEMCHUNKSIZE
condition|)
name|buflen
operator|*=
literal|2
expr_stmt|;
else|else
name|buflen
operator|+=
name|MEMCHUNKSIZE
expr_stmt|;
if|if
condition|(
name|buflen
operator|<=
literal|0
condition|)
block|{
name|sm_syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"header overflow from %s during message collect"
argument_list|,
name|CURHOSTNAME
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|e
operator|->
name|e_flags
operator||=
name|EF_CLRQUEUE
expr_stmt|;
name|e
operator|->
name|e_status
operator|=
literal|"5.6.0"
expr_stmt|;
name|usrerrenh
argument_list|(
name|e
operator|->
name|e_status
argument_list|,
literal|"552 Headers too large"
argument_list|)
expr_stmt|;
goto|goto
name|discard
goto|;
block|}
name|buf
operator|=
name|xalloc
argument_list|(
name|buflen
argument_list|)
expr_stmt|;
name|memmove
argument_list|(
name|buf
argument_list|,
name|obuf
argument_list|,
name|bp
operator|-
name|obuf
argument_list|)
expr_stmt|;
name|bp
operator|=
operator|&
name|buf
index|[
name|bp
operator|-
name|obuf
index|]
expr_stmt|;
if|if
condition|(
name|obuf
operator|!=
name|bufbuf
condition|)
name|sm_free
argument_list|(
name|obuf
argument_list|)
expr_stmt|;
comment|/* XXX */
block|}
if|if
condition|(
name|c
operator|!=
literal|'\0'
condition|)
block|{
operator|*
name|bp
operator|++
operator|=
name|c
expr_stmt|;
operator|++
name|hdrslen
expr_stmt|;
if|if
condition|(
operator|!
name|headeronly
operator|&&
name|MaxHeadersLength
operator|>
literal|0
operator|&&
name|hdrslen
operator|>
name|MaxHeadersLength
condition|)
block|{
name|sm_syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"headers too large (%d max) from %s during message collect"
argument_list|,
name|MaxHeadersLength
argument_list|,
name|CURHOSTNAME
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|e
operator|->
name|e_flags
operator||=
name|EF_CLRQUEUE
expr_stmt|;
name|e
operator|->
name|e_status
operator|=
literal|"5.6.0"
expr_stmt|;
name|usrerrenh
argument_list|(
name|e
operator|->
name|e_status
argument_list|,
literal|"552 Headers too large (%d max)"
argument_list|,
name|MaxHeadersLength
argument_list|)
expr_stmt|;
name|discard
label|:
name|mstate
operator|=
name|MS_DISCARD
expr_stmt|;
block|}
block|}
if|if
condition|(
name|istate
operator|==
name|IS_BOL
condition|)
break|break;
block|}
operator|*
name|bp
operator|=
literal|'\0'
expr_stmt|;
name|nextstate
label|:
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|35
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"nextstate, istate=%d, mstate=%d, line=\"%s\"\n"
argument_list|,
name|istate
argument_list|,
name|mstate
argument_list|,
name|buf
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|mstate
condition|)
block|{
case|case
name|MS_UFROM
case|:
name|mstate
operator|=
name|MS_HEADER
expr_stmt|;
ifndef|#
directive|ifndef
name|NOTUNIX
if|if
condition|(
name|strncmp
argument_list|(
name|buf
argument_list|,
literal|"From "
argument_list|,
literal|5
argument_list|)
operator|==
literal|0
condition|)
block|{
name|bp
operator|=
name|buf
expr_stmt|;
name|eatfrom
argument_list|(
name|buf
argument_list|,
name|e
argument_list|)
expr_stmt|;
continue|continue;
block|}
endif|#
directive|endif
comment|/* ! NOTUNIX */
comment|/* FALLTHROUGH */
case|case
name|MS_HEADER
case|:
if|if
condition|(
operator|!
name|isheader
argument_list|(
name|buf
argument_list|)
condition|)
block|{
name|mstate
operator|=
name|MS_BODY
expr_stmt|;
goto|goto
name|nextstate
goto|;
block|}
comment|/* check for possible continuation line */
do|do
block|{
name|sm_io_clearerr
argument_list|(
name|fp
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|c
operator|=
name|sm_io_getc
argument_list|(
name|fp
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
comment|/* timeout? */
if|if
condition|(
name|c
operator|==
name|SM_IO_EOF
operator|&&
name|errno
operator|==
name|EAGAIN
operator|&&
name|smtpmode
condition|)
block|{
comment|/* 					**  Override e_message in 					**  usrerr() as this is the 					**  reason for failure that 					**  should be logged for 					**  undelivered recipients. 					*/
name|e
operator|->
name|e_message
operator|=
name|NULL
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|inputerr
operator|=
name|true
expr_stmt|;
goto|goto
name|readabort
goto|;
block|}
block|}
do|while
condition|(
name|c
operator|==
name|SM_IO_EOF
operator|&&
name|errno
operator|==
name|EINTR
condition|)
do|;
if|if
condition|(
name|c
operator|!=
name|SM_IO_EOF
condition|)
operator|(
name|void
operator|)
name|sm_io_ungetc
argument_list|(
name|fp
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|c
argument_list|)
expr_stmt|;
if|if
condition|(
name|c
operator|==
literal|' '
operator|||
name|c
operator|==
literal|'\t'
condition|)
block|{
comment|/* yep -- defer this */
continue|continue;
block|}
name|SM_ASSERT
argument_list|(
name|bp
operator|>
name|buf
argument_list|)
expr_stmt|;
comment|/* guaranteed by isheader(buf) */
name|SM_ASSERT
argument_list|(
operator|*
operator|(
name|bp
operator|-
literal|1
operator|)
operator|!=
literal|'\n'
operator|||
name|bp
operator|>
name|buf
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* trim off trailing CRLF or NL */
if|if
condition|(
operator|*
operator|--
name|bp
operator|!=
literal|'\n'
operator|||
operator|*
operator|--
name|bp
operator|!=
literal|'\r'
condition|)
name|bp
operator|++
expr_stmt|;
operator|*
name|bp
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|H_EOH
argument_list|,
name|chompheader
argument_list|(
name|buf
argument_list|,
name|CHHDR_CHECK
operator||
name|CHHDR_USER
argument_list|,
name|hdrp
argument_list|,
name|e
argument_list|)
argument_list|)
condition|)
block|{
name|mstate
operator|=
name|MS_BODY
expr_stmt|;
goto|goto
name|nextstate
goto|;
block|}
name|numhdrs
operator|++
expr_stmt|;
break|break;
case|case
name|MS_BODY
case|:
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"EOH\n"
argument_list|)
expr_stmt|;
if|if
condition|(
name|headeronly
condition|)
goto|goto
name|readerr
goto|;
name|df
operator|=
name|collect_eoh
argument_list|(
name|e
argument_list|,
name|numhdrs
argument_list|,
name|hdrslen
argument_list|)
expr_stmt|;
if|if
condition|(
name|df
operator|==
name|NULL
condition|)
name|e
operator|->
name|e_flags
operator||=
name|EF_TOOBIG
expr_stmt|;
name|bp
operator|=
name|buf
expr_stmt|;
comment|/* toss blank line */
if|if
condition|(
operator|(
operator|!
name|bitset
argument_list|(
name|EF_CRLF_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
operator|&&
name|bp
index|[
literal|0
index|]
operator|==
literal|'\r'
operator|&&
name|bp
index|[
literal|1
index|]
operator|==
literal|'\n'
operator|)
operator|||
operator|(
operator|!
name|bitset
argument_list|(
name|EF_NL_NOT_EOL
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
operator|&&
name|bp
index|[
literal|0
index|]
operator|==
literal|'\n'
operator|)
condition|)
block|{
break|break;
block|}
comment|/* if not a blank separator, write it out */
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|EF_TOOBIG
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
while|while
condition|(
operator|*
name|bp
operator|!=
literal|'\0'
condition|)
operator|(
name|void
operator|)
name|sm_io_putc
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|*
name|bp
operator|++
argument_list|)
expr_stmt|;
block|}
break|break;
block|}
name|bp
operator|=
name|buf
expr_stmt|;
block|}
name|readerr
label|:
if|if
condition|(
operator|(
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
operator|&&
name|smtpmode
operator|)
operator|||
name|sm_io_error
argument_list|(
name|fp
argument_list|)
condition|)
block|{
specifier|const
name|char
modifier|*
name|errmsg
decl_stmt|;
if|if
condition|(
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
condition|)
name|errmsg
operator|=
literal|"unexpected close"
expr_stmt|;
else|else
name|errmsg
operator|=
name|sm_errstring
argument_list|(
name|errno
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"collect: premature EOM: %s\n"
argument_list|,
name|errmsg
argument_list|)
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|1
condition|)
name|sm_syslog
argument_list|(
name|LOG_WARNING
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"collect: premature EOM: %s"
argument_list|,
name|errmsg
argument_list|)
expr_stmt|;
name|inputerr
operator|=
name|true
expr_stmt|;
block|}
if|if
condition|(
name|headeronly
condition|)
return|return;
if|if
condition|(
name|mstate
operator|!=
name|MS_BODY
condition|)
block|{
comment|/* no body or discard, so we never opened the data file */
name|SM_ASSERT
argument_list|(
name|df
operator|==
name|NULL
argument_list|)
expr_stmt|;
name|df
operator|=
name|collect_eoh
argument_list|(
name|e
argument_list|,
name|numhdrs
argument_list|,
name|hdrslen
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|df
operator|==
name|NULL
condition|)
block|{
comment|/* skip next few clauses */
comment|/* EMPTY */
block|}
elseif|else
if|if
condition|(
name|sm_io_flush
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
operator|!=
literal|0
operator|||
name|sm_io_error
argument_list|(
name|df
argument_list|)
condition|)
block|{
name|dferror
argument_list|(
name|df
argument_list|,
literal|"sm_io_flush||sm_io_error"
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
elseif|else
if|if
condition|(
name|SuperSafe
operator|==
name|SAFE_NO
operator|||
name|SuperSafe
operator|==
name|SAFE_INTERACTIVE
operator|||
operator|(
name|SuperSafe
operator|==
name|SAFE_REALLY_POSTMILTER
operator|&&
name|smtpmode
operator|)
condition|)
block|{
comment|/* skip next few clauses */
comment|/* EMPTY */
comment|/* Note: updfs() is not called in this case! */
block|}
elseif|else
if|if
condition|(
name|sm_io_setinfo
argument_list|(
name|df
argument_list|,
name|SM_BF_COMMIT
argument_list|,
name|NULL
argument_list|)
operator|<
literal|0
operator|&&
name|errno
operator|!=
name|EINVAL
condition|)
block|{
name|int
name|save_errno
init|=
name|errno
decl_stmt|;
if|if
condition|(
name|save_errno
operator|==
name|EEXIST
condition|)
block|{
name|char
modifier|*
name|dfile
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
name|int
name|dfd
decl_stmt|;
name|dfile
operator|=
name|queuename
argument_list|(
name|e
argument_list|,
name|DATAFL_LETTER
argument_list|)
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|dfile
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
condition|)
name|st
operator|.
name|st_size
operator|=
operator|-
literal|1
expr_stmt|;
name|errno
operator|=
name|EEXIST
expr_stmt|;
name|syserr
argument_list|(
literal|"@collect: bfcommit(%s): already on disk, size=%ld"
argument_list|,
name|dfile
argument_list|,
operator|(
name|long
operator|)
name|st
operator|.
name|st_size
argument_list|)
expr_stmt|;
name|dfd
operator|=
name|sm_io_getinfo
argument_list|(
name|df
argument_list|,
name|SM_IO_WHAT_FD
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|dfd
operator|>=
literal|0
condition|)
name|dumpfd
argument_list|(
name|dfd
argument_list|,
name|true
argument_list|,
name|true
argument_list|)
expr_stmt|;
block|}
name|errno
operator|=
name|save_errno
expr_stmt|;
name|dferror
argument_list|(
name|df
argument_list|,
literal|"bfcommit"
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|save_errno
operator|!=
name|EEXIST
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|(
name|afd
operator|=
name|sm_io_getinfo
argument_list|(
name|df
argument_list|,
name|SM_IO_WHAT_FD
argument_list|,
name|NULL
argument_list|)
operator|)
operator|<
literal|0
condition|)
block|{
name|dferror
argument_list|(
name|df
argument_list|,
literal|"sm_io_getinfo"
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
elseif|else
if|if
condition|(
name|fsync
argument_list|(
name|afd
argument_list|)
operator|<
literal|0
condition|)
block|{
name|dferror
argument_list|(
name|df
argument_list|,
literal|"fsync"
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
elseif|else
if|if
condition|(
name|sm_io_close
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
operator|<
literal|0
condition|)
block|{
name|dferror
argument_list|(
name|df
argument_list|,
literal|"sm_io_close"
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
else|else
block|{
comment|/* everything is happily flushed to disk */
name|df
operator|=
name|NULL
expr_stmt|;
comment|/* remove from available space in filesystem */
name|updfs
argument_list|(
name|e
argument_list|,
literal|0
argument_list|,
literal|1
argument_list|,
literal|"collect"
argument_list|)
expr_stmt|;
block|}
comment|/* An EOF when running SMTP is an error */
name|readabort
label|:
if|if
condition|(
name|inputerr
operator|&&
operator|(
name|OpMode
operator|==
name|MD_SMTP
operator|||
name|OpMode
operator|==
name|MD_DAEMON
operator|)
condition|)
block|{
name|char
modifier|*
name|host
decl_stmt|;
name|char
modifier|*
name|problem
decl_stmt|;
name|ADDRESS
modifier|*
name|q
decl_stmt|;
name|host
operator|=
name|RealHostName
expr_stmt|;
if|if
condition|(
name|host
operator|==
name|NULL
condition|)
name|host
operator|=
literal|"localhost"
expr_stmt|;
if|if
condition|(
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
condition|)
name|problem
operator|=
literal|"unexpected close"
expr_stmt|;
elseif|else
if|if
condition|(
name|sm_io_error
argument_list|(
name|fp
argument_list|)
condition|)
name|problem
operator|=
literal|"I/O error"
expr_stmt|;
else|else
name|problem
operator|=
literal|"read timeout"
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|0
operator|&&
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
condition|)
name|sm_syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"collect: %s on connection from %.100s, sender=%s"
argument_list|,
name|problem
argument_list|,
name|host
argument_list|,
name|shortenstring
argument_list|(
name|e
operator|->
name|e_from
operator|.
name|q_paddr
argument_list|,
name|MAXSHORTSTR
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|sm_io_eof
argument_list|(
name|fp
argument_list|)
condition|)
name|usrerr
argument_list|(
literal|"421 4.4.1 collect: %s on connection from %s, from=%s"
argument_list|,
name|problem
argument_list|,
name|host
argument_list|,
name|shortenstring
argument_list|(
name|e
operator|->
name|e_from
operator|.
name|q_paddr
argument_list|,
name|MAXSHORTSTR
argument_list|)
argument_list|)
expr_stmt|;
else|else
name|syserr
argument_list|(
literal|"421 4.4.1 collect: %s on connection from %s, from=%s"
argument_list|,
name|problem
argument_list|,
name|host
argument_list|,
name|shortenstring
argument_list|(
name|e
operator|->
name|e_from
operator|.
name|q_paddr
argument_list|,
name|MAXSHORTSTR
argument_list|)
argument_list|)
expr_stmt|;
name|flush_errors
argument_list|(
name|true
argument_list|)
expr_stmt|;
comment|/* don't return an error indication */
name|e
operator|->
name|e_to
operator|=
name|NULL
expr_stmt|;
name|e
operator|->
name|e_flags
operator|&=
operator|~
name|EF_FATALERRS
expr_stmt|;
name|e
operator|->
name|e_flags
operator||=
name|EF_CLRQUEUE
expr_stmt|;
comment|/* Don't send any message notification to sender */
for|for
control|(
name|q
operator|=
name|e
operator|->
name|e_sendqueue
init|;
name|q
operator|!=
name|NULL
condition|;
name|q
operator|=
name|q
operator|->
name|q_next
control|)
block|{
if|if
condition|(
name|QS_IS_DEAD
argument_list|(
name|q
operator|->
name|q_state
argument_list|)
condition|)
continue|continue;
name|q
operator|->
name|q_state
operator|=
name|QS_FATALERR
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|sm_io_close
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
name|df
operator|=
name|NULL
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
comment|/* Log collection information. */
if|if
condition|(
name|bitset
argument_list|(
name|EF_LOGSENDER
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
operator|&&
name|LogLevel
operator|>
literal|4
condition|)
block|{
name|logsender
argument_list|(
name|e
argument_list|,
name|e
operator|->
name|e_msgid
argument_list|)
expr_stmt|;
name|e
operator|->
name|e_flags
operator|&=
operator|~
name|EF_LOGSENDER
expr_stmt|;
block|}
comment|/* check for message too large */
if|if
condition|(
name|bitset
argument_list|(
name|EF_TOOBIG
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|e
operator|->
name|e_flags
operator||=
name|EF_NO_BODY_RETN
operator||
name|EF_CLRQUEUE
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|EF_FATALERRS
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|e
operator|->
name|e_status
operator|=
literal|"5.2.3"
expr_stmt|;
name|usrerrenh
argument_list|(
name|e
operator|->
name|e_status
argument_list|,
literal|"552 Message exceeds maximum fixed size (%ld)"
argument_list|,
name|MaxMessageSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|6
condition|)
name|sm_syslog
argument_list|(
name|LOG_NOTICE
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"message size (%ld) exceeds maximum (%ld)"
argument_list|,
name|e
operator|->
name|e_msgsize
argument_list|,
name|MaxMessageSize
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* check for illegal 8-bit data */
if|if
condition|(
name|HasEightBits
condition|)
block|{
name|e
operator|->
name|e_flags
operator||=
name|EF_HAS8BIT
expr_stmt|;
if|if
condition|(
operator|!
name|bitset
argument_list|(
name|MM_PASS8BIT
operator||
name|MM_MIME8BIT
argument_list|,
name|MimeMode
argument_list|)
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_IS_MIME
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|e
operator|->
name|e_status
operator|=
literal|"5.6.1"
expr_stmt|;
name|usrerrenh
argument_list|(
name|e
operator|->
name|e_status
argument_list|,
literal|"554 Eight bit data not allowed"
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
comment|/* if it claimed to be 8 bits, well, it lied.... */
if|if
condition|(
name|e
operator|->
name|e_bodytype
operator|!=
name|NULL
operator|&&
name|sm_strcasecmp
argument_list|(
name|e
operator|->
name|e_bodytype
argument_list|,
literal|"8BITMIME"
argument_list|)
operator|==
literal|0
condition|)
name|e
operator|->
name|e_bodytype
operator|=
literal|"7BIT"
expr_stmt|;
block|}
if|if
condition|(
name|SuperSafe
operator|==
name|SAFE_REALLY
operator|&&
operator|!
name|bitset
argument_list|(
name|EF_FATALERRS
argument_list|,
name|e
operator|->
name|e_flags
argument_list|)
condition|)
block|{
name|char
modifier|*
name|dfname
init|=
name|queuename
argument_list|(
name|e
argument_list|,
name|DATAFL_LETTER
argument_list|)
decl_stmt|;
if|if
condition|(
operator|(
name|e
operator|->
name|e_dfp
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdio
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|dfname
argument_list|,
name|SM_IO_RDONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
comment|/* we haven't acked receipt yet, so just chuck this */
name|syserr
argument_list|(
literal|"@Cannot reopen %s"
argument_list|,
name|dfname
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|true
argument_list|,
name|true
argument_list|,
name|ExitStat
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
block|}
else|else
name|e
operator|->
name|e_dfp
operator|=
name|df
expr_stmt|;
comment|/* collect statistics */
if|if
condition|(
name|OpMode
operator|!=
name|MD_VERIFY
condition|)
block|{
comment|/* 		**  Recalculate e_msgpriority, it is done at in eatheader() 		**  which is called (in 8.12) after the header is collected, 		**  hence e_msgsize is (most likely) incorrect. 		*/
name|e
operator|->
name|e_msgpriority
operator|=
name|e
operator|->
name|e_msgsize
operator|-
name|e
operator|->
name|e_class
operator|*
name|WkClassFact
operator|+
name|e
operator|->
name|e_nrcpts
operator|*
name|WkRecipFact
expr_stmt|;
name|markstats
argument_list|(
name|e
argument_list|,
operator|(
name|ADDRESS
operator|*
operator|)
name|NULL
argument_list|,
name|STATS_NORMAL
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/* **  DFERROR -- signal error on writing the data file. ** **	Called by collect().  Collect() always terminates the process **	immediately after calling dferror(), which means that the SMTP **	session will be terminated, which means that any error message **	issued by dferror must be a 421 error, as per RFC 821. ** **	Parameters: **		df -- the file pointer for the data file. **		msg -- detailed message. **		e -- the current envelope. ** **	Returns: **		none. ** **	Side Effects: **		Gives an error message. **		Arranges for following output to go elsewhere. */
end_comment

begin_function
name|void
name|dferror
parameter_list|(
name|df
parameter_list|,
name|msg
parameter_list|,
name|e
parameter_list|)
name|SM_FILE_T
modifier|*
specifier|volatile
name|df
decl_stmt|;
name|char
modifier|*
name|msg
decl_stmt|;
specifier|register
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
name|char
modifier|*
name|dfname
decl_stmt|;
name|dfname
operator|=
name|queuename
argument_list|(
name|e
argument_list|,
name|DATAFL_LETTER
argument_list|)
expr_stmt|;
name|setstat
argument_list|(
name|EX_IOERR
argument_list|)
expr_stmt|;
if|if
condition|(
name|errno
operator|==
name|ENOSPC
condition|)
block|{
if|#
directive|if
name|STAT64
operator|>
literal|0
name|struct
name|stat64
name|st
decl_stmt|;
else|#
directive|else
comment|/* STAT64> 0 */
name|struct
name|stat
name|st
decl_stmt|;
endif|#
directive|endif
comment|/* STAT64> 0 */
name|long
name|avail
decl_stmt|;
name|long
name|bsize
decl_stmt|;
name|e
operator|->
name|e_flags
operator||=
name|EF_NO_BODY_RETN
expr_stmt|;
if|if
condition|(
if|#
directive|if
name|STAT64
operator|>
literal|0
name|fstat64
argument_list|(
argument|sm_io_getinfo(df, SM_IO_WHAT_FD, NULL)
argument_list|,
argument|&st
argument_list|)
else|#
directive|else
comment|/* STAT64> 0 */
name|fstat
argument_list|(
name|sm_io_getinfo
argument_list|(
name|df
argument_list|,
name|SM_IO_WHAT_FD
argument_list|,
name|NULL
argument_list|)
argument_list|,
operator|&
name|st
argument_list|)
endif|#
directive|endif
comment|/* STAT64> 0 */
operator|<
literal|0
condition|)
name|st
operator|.
name|st_size
operator|=
literal|0
expr_stmt|;
operator|(
name|void
operator|)
name|sm_io_reopen
argument_list|(
name|SmFtStdio
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|dfname
argument_list|,
name|SM_IO_WRONLY_B
argument_list|,
name|NULL
argument_list|,
name|df
argument_list|)
expr_stmt|;
if|if
condition|(
name|st
operator|.
name|st_size
operator|<=
literal|0
condition|)
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"\n*** Mail could not be accepted"
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"\n*** Mail of at least %llu bytes could not be accepted\n"
argument_list|,
operator|(
name|ULONGLONG_T
operator|)
name|st
operator|.
name|st_size
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"*** at %s due to lack of disk space for temp file.\n"
argument_list|,
name|MyHostName
argument_list|)
expr_stmt|;
name|avail
operator|=
name|freediskspace
argument_list|(
name|qid_printqueue
argument_list|(
name|e
operator|->
name|e_qgrp
argument_list|,
name|e
operator|->
name|e_qdir
argument_list|)
argument_list|,
operator|&
name|bsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|avail
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|bsize
operator|>
literal|1024
condition|)
name|avail
operator|*=
name|bsize
operator|/
literal|1024
expr_stmt|;
elseif|else
if|if
condition|(
name|bsize
operator|<
literal|1024
condition|)
name|avail
operator|/=
literal|1024
operator|/
name|bsize
expr_stmt|;
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|df
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"*** Currently, %ld kilobytes are available for mail temp files.\n"
argument_list|,
name|avail
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
literal|0
comment|/* Wrong response code; should be 421. */
block|e->e_status = "4.3.1"; 		usrerrenh(e->e_status, "452 Out of disk space for temp file");
else|#
directive|else
comment|/* 0 */
name|syserr
argument_list|(
literal|"421 4.3.1 Out of disk space for temp file"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 0 */
block|}
else|else
name|syserr
argument_list|(
literal|"421 4.3.0 collect: Cannot write %s (%s, uid=%d, gid=%d)"
argument_list|,
name|dfname
argument_list|,
name|msg
argument_list|,
operator|(
name|int
operator|)
name|geteuid
argument_list|()
argument_list|,
operator|(
name|int
operator|)
name|getegid
argument_list|()
argument_list|)
expr_stmt|;
if|if
condition|(
name|sm_io_reopen
argument_list|(
name|SmFtStdio
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
name|SM_PATH_DEVNULL
argument_list|,
name|SM_IO_WRONLY
argument_list|,
name|NULL
argument_list|,
name|df
argument_list|)
operator|==
name|NULL
condition|)
name|sm_syslog
argument_list|(
name|LOG_ERR
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"dferror: sm_io_reopen(\"/dev/null\") failed: %s"
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  EATFROM -- chew up a UNIX style from line and process ** **	This does indeed make some assumptions about the format **	of UNIX messages. ** **	Parameters: **		fm -- the from line. **		e -- envelope ** **	Returns: **		none. ** **	Side Effects: **		extracts what information it can from the header, **		such as the date. */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|NOTUNIX
end_ifndef

begin_decl_stmt
specifier|static
name|char
modifier|*
name|DowList
index|[]
init|=
block|{
literal|"Sun"
block|,
literal|"Mon"
block|,
literal|"Tue"
block|,
literal|"Wed"
block|,
literal|"Thu"
block|,
literal|"Fri"
block|,
literal|"Sat"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|char
modifier|*
name|MonthList
index|[]
init|=
block|{
literal|"Jan"
block|,
literal|"Feb"
block|,
literal|"Mar"
block|,
literal|"Apr"
block|,
literal|"May"
block|,
literal|"Jun"
block|,
literal|"Jul"
block|,
literal|"Aug"
block|,
literal|"Sep"
block|,
literal|"Oct"
block|,
literal|"Nov"
block|,
literal|"Dec"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|void
name|eatfrom
parameter_list|(
name|fm
parameter_list|,
name|e
parameter_list|)
name|char
modifier|*
specifier|volatile
name|fm
decl_stmt|;
specifier|register
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|char
modifier|*
modifier|*
name|dt
decl_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|30
argument_list|,
literal|2
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"eatfrom(%s)\n"
argument_list|,
name|fm
argument_list|)
expr_stmt|;
comment|/* find the date part */
name|p
operator|=
name|fm
expr_stmt|;
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
block|{
comment|/* skip a word */
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
operator|&&
operator|*
name|p
operator|!=
literal|' '
condition|)
name|p
operator|++
expr_stmt|;
while|while
condition|(
operator|*
name|p
operator|==
literal|' '
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|p
argument_list|)
operator|<
literal|17
condition|)
block|{
comment|/* no room for the date */
return|return;
block|}
if|if
condition|(
operator|!
operator|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isupper
argument_list|(
operator|*
name|p
argument_list|)
operator|)
operator|||
name|p
index|[
literal|3
index|]
operator|!=
literal|' '
operator|||
name|p
index|[
literal|13
index|]
operator|!=
literal|':'
operator|||
name|p
index|[
literal|16
index|]
operator|!=
literal|':'
condition|)
continue|continue;
comment|/* we have a possible date */
for|for
control|(
name|dt
operator|=
name|DowList
init|;
operator|*
name|dt
operator|!=
name|NULL
condition|;
name|dt
operator|++
control|)
if|if
condition|(
name|strncmp
argument_list|(
operator|*
name|dt
argument_list|,
name|p
argument_list|,
literal|3
argument_list|)
operator|==
literal|0
condition|)
break|break;
if|if
condition|(
operator|*
name|dt
operator|==
name|NULL
condition|)
continue|continue;
for|for
control|(
name|dt
operator|=
name|MonthList
init|;
operator|*
name|dt
operator|!=
name|NULL
condition|;
name|dt
operator|++
control|)
block|{
if|if
condition|(
name|strncmp
argument_list|(
operator|*
name|dt
argument_list|,
operator|&
name|p
index|[
literal|4
index|]
argument_list|,
literal|3
argument_list|)
operator|==
literal|0
condition|)
break|break;
block|}
if|if
condition|(
operator|*
name|dt
operator|!=
name|NULL
condition|)
break|break;
block|}
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
block|{
name|char
modifier|*
name|q
decl_stmt|,
name|buf
index|[
literal|25
index|]
decl_stmt|;
comment|/* we have found a date */
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|buf
argument_list|,
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
name|q
operator|=
name|arpadate
argument_list|(
name|buf
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|e
operator|->
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
literal|'a'
argument_list|,
name|q
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ! NOTUNIX */
end_comment

end_unit

