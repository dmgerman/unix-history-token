begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 2001-2002 Sendmail, Inc. and its suppliers.  *	All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_include
include|#
directive|include
file|<sm/gen.h>
end_include

begin_macro
name|SM_RCSID
argument_list|(
literal|"@(#)$Id: sasl.c,v 8.22 2006/08/15 23:24:57 ca Exp $"
argument_list|)
end_macro

begin_if
if|#
directive|if
name|SASL
end_if

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<sendmail.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_comment
comment|/* **  In order to ensure that storage leaks are tracked, and to prevent **  conflicts between the sm_heap package and sasl, we tell sasl to **  use the following heap allocation functions.  Unfortunately, **  the sasl package incorrectly specifies the size of a block **  using unsigned long: for portability, it should be size_t. */
end_comment

begin_decl_stmt
name|void
modifier|*
name|sm_sasl_malloc
name|__P
argument_list|(
operator|(
name|unsigned
name|long
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
modifier|*
name|sm_sasl_calloc
name|__P
argument_list|(
operator|(
name|unsigned
name|long
operator|,
name|unsigned
name|long
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
modifier|*
name|sm_sasl_realloc
name|__P
argument_list|(
operator|(
name|void
operator|*
operator|,
name|unsigned
name|long
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|void
name|sm_sasl_free
name|__P
argument_list|(
operator|(
name|void
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  SASLv1: **  We can't use an rpool for Cyrus-SASL memory management routines, **	since the encryption/decryption routines in Cyrus-SASL **	allocate/deallocate a buffer each time. Since rpool **	don't release memory until the very end, memory consumption is **	proportional to the size of an e-mail, which is unacceptable. */
end_comment

begin_comment
comment|/* **  SM_SASL_MALLOC -- malloc() for SASL ** **	Parameters: **		size -- size of requested memory. ** **	Returns: **		pointer to memory. */
end_comment

begin_function
name|void
modifier|*
name|sm_sasl_malloc
parameter_list|(
name|size
parameter_list|)
name|unsigned
name|long
name|size
decl_stmt|;
block|{
return|return
name|sm_malloc
argument_list|(
operator|(
name|size_t
operator|)
name|size
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* **  SM_SASL_CALLOC -- calloc() for SASL ** **	Parameters: **		nelem -- number of elements. **		elemsize -- size of each element. ** **	Returns: **		pointer to memory. ** **	Notice: **		this isn't currently used by SASL. */
end_comment

begin_function
specifier|static
name|void
modifier|*
name|sm_sasl_calloc
parameter_list|(
name|nelem
parameter_list|,
name|elemsize
parameter_list|)
name|unsigned
name|long
name|nelem
decl_stmt|;
name|unsigned
name|long
name|elemsize
decl_stmt|;
block|{
name|size_t
name|size
decl_stmt|;
name|void
modifier|*
name|p
decl_stmt|;
name|size
operator|=
operator|(
name|size_t
operator|)
name|nelem
operator|*
operator|(
name|size_t
operator|)
name|elemsize
expr_stmt|;
name|p
operator|=
name|sm_malloc
argument_list|(
name|size
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|memset
argument_list|(
name|p
argument_list|,
literal|'\0'
argument_list|,
name|size
argument_list|)
expr_stmt|;
return|return
name|p
return|;
block|}
end_function

begin_comment
comment|/* **  SM_SASL_REALLOC -- realloc() for SASL ** **	Parameters: **		p -- pointer to old memory. **		size -- size of requested memory. ** **	Returns: **		pointer to new memory. */
end_comment

begin_function
specifier|static
name|void
modifier|*
name|sm_sasl_realloc
parameter_list|(
name|o
parameter_list|,
name|size
parameter_list|)
name|void
modifier|*
name|o
decl_stmt|;
name|unsigned
name|long
name|size
decl_stmt|;
block|{
return|return
name|sm_realloc
argument_list|(
name|o
argument_list|,
operator|(
name|size_t
operator|)
name|size
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* **  SM_SASL_FREE -- free() for SASL ** **	Parameters: **		p -- pointer to free. ** **	Returns: **		none */
end_comment

begin_function
name|void
name|sm_sasl_free
parameter_list|(
name|p
parameter_list|)
name|void
modifier|*
name|p
decl_stmt|;
block|{
name|sm_free
argument_list|(
name|p
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  SM_SASL_INIT -- sendmail specific SASL initialization ** **	Parameters: **		none. ** **	Returns: **		none ** **	Side Effects: **		installs memory management routines for SASL. */
end_comment

begin_function
name|void
name|sm_sasl_init
parameter_list|()
block|{
name|sasl_set_alloc
argument_list|(
name|sm_sasl_malloc
argument_list|,
name|sm_sasl_calloc
argument_list|,
name|sm_sasl_realloc
argument_list|,
name|sm_sasl_free
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  INTERSECT -- create the intersection between two lists ** **	Parameters: **		s1, s2 -- lists of items (separated by single blanks). **		rpool -- resource pool from which result is allocated. ** **	Returns: **		the intersection of both lists. */
end_comment

begin_function
name|char
modifier|*
name|intersect
parameter_list|(
name|s1
parameter_list|,
name|s2
parameter_list|,
name|rpool
parameter_list|)
name|char
modifier|*
name|s1
decl_stmt|,
decl|*
name|s2
decl_stmt|;
end_function

begin_decl_stmt
name|SM_RPOOL_T
modifier|*
name|rpool
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|hr
decl_stmt|,
modifier|*
name|h1
decl_stmt|,
modifier|*
name|h
decl_stmt|,
modifier|*
name|res
decl_stmt|;
name|int
name|l1
decl_stmt|,
name|l2
decl_stmt|,
name|rl
decl_stmt|;
if|if
condition|(
name|s1
operator|==
name|NULL
operator|||
name|s2
operator|==
name|NULL
condition|)
comment|/* NULL string(s) -> NULL result */
return|return
name|NULL
return|;
name|l1
operator|=
name|strlen
argument_list|(
name|s1
argument_list|)
expr_stmt|;
name|l2
operator|=
name|strlen
argument_list|(
name|s2
argument_list|)
expr_stmt|;
name|rl
operator|=
name|SM_MIN
argument_list|(
name|l1
argument_list|,
name|l2
argument_list|)
expr_stmt|;
name|res
operator|=
operator|(
name|char
operator|*
operator|)
name|sm_rpool_malloc
argument_list|(
name|rpool
argument_list|,
name|rl
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
operator|*
name|res
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|rl
operator|==
literal|0
condition|)
comment|/* at least one string empty? */
return|return
name|res
return|;
name|hr
operator|=
name|res
expr_stmt|;
name|h1
operator|=
name|s1
expr_stmt|;
name|h
operator|=
name|s1
expr_stmt|;
comment|/* walk through s1 */
while|while
condition|(
name|h
operator|!=
name|NULL
operator|&&
operator|*
name|h1
operator|!=
literal|'\0'
condition|)
block|{
comment|/* is there something after the current word? */
if|if
condition|(
operator|(
name|h
operator|=
name|strchr
argument_list|(
name|h1
argument_list|,
literal|' '
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|h
operator|=
literal|'\0'
expr_stmt|;
name|l1
operator|=
name|strlen
argument_list|(
name|h1
argument_list|)
expr_stmt|;
comment|/* does the current word appear in s2 ? */
if|if
condition|(
name|iteminlist
argument_list|(
name|h1
argument_list|,
name|s2
argument_list|,
literal|" "
argument_list|)
operator|!=
name|NULL
condition|)
block|{
comment|/* add a blank if not first item */
if|if
condition|(
name|hr
operator|!=
name|res
condition|)
operator|*
name|hr
operator|++
operator|=
literal|' '
expr_stmt|;
comment|/* copy the item */
name|memcpy
argument_list|(
name|hr
argument_list|,
name|h1
argument_list|,
name|l1
argument_list|)
expr_stmt|;
comment|/* advance pointer in result list */
name|hr
operator|+=
name|l1
expr_stmt|;
operator|*
name|hr
operator|=
literal|'\0'
expr_stmt|;
block|}
if|if
condition|(
name|h
operator|!=
name|NULL
condition|)
block|{
comment|/* there are more items */
operator|*
name|h
operator|=
literal|' '
expr_stmt|;
name|h1
operator|=
name|h
operator|+
literal|1
expr_stmt|;
block|}
block|}
return|return
name|res
return|;
block|}
end_block

begin_if
if|#
directive|if
name|SASL
operator|>=
literal|20000
end_if

begin_comment
comment|/* **  IPTOSTRING -- create string for SASL_IP*PORT property **		(borrowed from lib/iptostring.c in Cyrus-IMAP) ** **	Parameters: **		addr -- (pointer to) socket address **		addrlen -- length of socket address **		out -- output string (result) **		outlen -- maximum length of output string ** **	Returns: **		true iff successful. ** **	Side Effects: **		creates output string if successful. **		sets errno if unsuccessful. */
end_comment

begin_include
include|#
directive|include
file|<arpa/inet.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|NI_MAXHOST
end_ifndef

begin_define
define|#
directive|define
name|NI_MAXHOST
value|1025
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_ifndef
ifndef|#
directive|ifndef
name|NI_MAXSERV
end_ifndef

begin_define
define|#
directive|define
name|NI_MAXSERV
value|32
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_function
name|bool
name|iptostring
parameter_list|(
name|addr
parameter_list|,
name|addrlen
parameter_list|,
name|out
parameter_list|,
name|outlen
parameter_list|)
name|SOCKADDR
modifier|*
name|addr
decl_stmt|;
name|SOCKADDR_LEN_T
name|addrlen
decl_stmt|;
name|char
modifier|*
name|out
decl_stmt|;
name|unsigned
name|outlen
decl_stmt|;
block|{
name|char
name|hbuf
index|[
name|NI_MAXHOST
index|]
decl_stmt|,
name|pbuf
index|[
name|NI_MAXSERV
index|]
decl_stmt|;
if|#
directive|if
name|NETINET6
name|int
name|niflags
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|if
condition|(
name|addr
operator|==
name|NULL
operator|||
name|out
operator|==
name|NULL
condition|)
block|{
name|errno
operator|=
name|EINVAL
expr_stmt|;
return|return
name|false
return|;
block|}
if|#
directive|if
name|NETINET6
name|niflags
operator|=
operator|(
name|NI_NUMERICHOST
operator||
name|NI_NUMERICSERV
operator|)
expr_stmt|;
ifdef|#
directive|ifdef
name|NI_WITHSCOPEID
if|if
condition|(
name|addr
operator|->
name|sa
operator|.
name|sa_family
operator|==
name|AF_INET6
condition|)
name|niflags
operator||=
name|NI_WITHSCOPEID
expr_stmt|;
endif|#
directive|endif
comment|/* NI_WITHSCOPEID */
if|if
condition|(
name|getnameinfo
argument_list|(
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
name|addr
argument_list|,
name|addrlen
argument_list|,
name|hbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|,
name|pbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|pbuf
argument_list|)
argument_list|,
name|niflags
argument_list|)
operator|!=
literal|0
condition|)
return|return
name|false
return|;
else|#
directive|else
comment|/* NETINET6 */
if|if
condition|(
name|addr
operator|->
name|sa
operator|.
name|sa_family
operator|!=
name|AF_INET
condition|)
block|{
name|errno
operator|=
name|EINVAL
expr_stmt|;
return|return
name|false
return|;
block|}
if|if
condition|(
name|sm_strlcpy
argument_list|(
name|hbuf
argument_list|,
name|inet_ntoa
argument_list|(
name|addr
operator|->
name|sin
operator|.
name|sin_addr
argument_list|)
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|)
operator|>=
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
name|false
return|;
block|}
name|sm_snprintf
argument_list|(
name|pbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|pbuf
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|ntohs
argument_list|(
name|addr
operator|->
name|sin
operator|.
name|sin_port
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|if
condition|(
name|outlen
operator|<
name|strlen
argument_list|(
name|hbuf
argument_list|)
operator|+
name|strlen
argument_list|(
name|pbuf
argument_list|)
operator|+
literal|2
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
name|false
return|;
block|}
name|sm_snprintf
argument_list|(
name|out
argument_list|,
name|outlen
argument_list|,
literal|"%s;%s"
argument_list|,
name|hbuf
argument_list|,
name|pbuf
argument_list|)
expr_stmt|;
return|return
name|true
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SASL>= 20000 */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* SASL */
end_comment

end_unit

