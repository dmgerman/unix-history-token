begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1998-2007 Sendmail, Inc. and its suppliers.  *	All rights reserved.  * Copyright (c) 1983, 1995-1997 Eric P. Allman.  All rights reserved.  * Copyright (c) 1988, 1993  *	The Regents of the University of California.  All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_include
include|#
directive|include
file|<sendmail.h>
end_include

begin_include
include|#
directive|include
file|"map.h"
end_include

begin_macro
name|SM_RCSID
argument_list|(
literal|"@(#)$Id: daemon.c,v 8.678 2007/03/08 00:33:40 ca Exp $"
argument_list|)
end_macro

begin_if
if|#
directive|if
name|defined
argument_list|(
name|SOCK_STREAM
argument_list|)
operator|||
name|defined
argument_list|(
name|__GNU_LIBRARY__
argument_list|)
end_if

begin_define
define|#
directive|define
name|USE_SOCK_STREAM
value|1
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(SOCK_STREAM) || defined(__GNU_LIBRARY__) */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|USE_SOCK_STREAM
argument_list|)
end_if

begin_if
if|#
directive|if
name|NETINET
operator|||
name|NETINET6
end_if

begin_include
include|#
directive|include
file|<arpa/inet.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NETINET || NETINET6 */
end_comment

begin_if
if|#
directive|if
name|NAMED_BIND
end_if

begin_ifndef
ifndef|#
directive|ifndef
name|NO_DATA
end_ifndef

begin_define
define|#
directive|define
name|NO_DATA
value|NO_ADDRESS
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ! NO_DATA */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NAMED_BIND */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* defined(USE_SOCK_STREAM) */
end_comment

begin_if
if|#
directive|if
name|STARTTLS
end_if

begin_include
include|#
directive|include
file|<openssl/rand.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* STARTTLS */
end_comment

begin_include
include|#
directive|include
file|<sm/time.h>
end_include

begin_if
if|#
directive|if
name|IP_SRCROUTE
operator|&&
name|NETINET
end_if

begin_include
include|#
directive|include
file|<netinet/in_systm.h>
end_include

begin_include
include|#
directive|include
file|<netinet/ip.h>
end_include

begin_if
if|#
directive|if
name|HAS_IN_H
end_if

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|IPOPTION
end_ifndef

begin_define
define|#
directive|define
name|IPOPTION
value|ip_opts
end_define

begin_define
define|#
directive|define
name|IP_LIST
value|ip_opts
end_define

begin_define
define|#
directive|define
name|IP_DST
value|ip_dst
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ! IPOPTION */
end_comment

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* HAS_IN_H */
end_comment

begin_include
include|#
directive|include
file|<netinet/ip_var.h>
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|IPOPTION
end_ifndef

begin_define
define|#
directive|define
name|IPOPTION
value|ipoption
end_define

begin_define
define|#
directive|define
name|IP_LIST
value|ipopt_list
end_define

begin_define
define|#
directive|define
name|IP_DST
value|ipopt_dst
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* ! IPOPTION */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* HAS_IN_H */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* IP_SRCROUTE&& NETINET */
end_comment

begin_include
include|#
directive|include
file|<sm/fdset.h>
end_include

begin_define
define|#
directive|define
name|DAEMON_C
value|1
end_define

begin_include
include|#
directive|include
file|<daemon.h>
end_include

begin_decl_stmt
specifier|static
name|void
name|connecttimeout
name|__P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|opendaemonsocket
name|__P
argument_list|(
operator|(
name|DAEMON_T
operator|*
operator|,
name|bool
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|unsigned
name|short
name|setupdaemon
name|__P
argument_list|(
operator|(
name|SOCKADDR
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|getrequests_checkdiskspace
name|__P
argument_list|(
operator|(
name|ENVELOPE
operator|*
name|e
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|setsockaddroptions
name|__P
argument_list|(
operator|(
name|char
operator|*
operator|,
name|DAEMON_T
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|printdaemonflags
name|__P
argument_list|(
operator|(
name|DAEMON_T
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|addr_family
name|__P
argument_list|(
operator|(
name|char
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|addrcmp
name|__P
argument_list|(
operator|(
expr|struct
name|hostent
operator|*
operator|,
name|char
operator|*
operator|,
name|SOCKADDR
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|authtimeout
name|__P
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  DAEMON.C -- routines to use when running as a daemon. ** **	This entire file is highly dependent on the 4.2 BSD **	interprocess communication primitives.  No attempt has **	been made to make this file portable to Version 7, **	Version 6, MPX files, etc.  If you should try such a **	thing yourself, I recommend chucking the entire file **	and starting from scratch.  Basic semantics are: ** **	getrequests(e) **		Opens a port and initiates a connection. **		Returns in a child.  Must set InChannel and **		OutChannel appropriately. **	clrdaemon() **		Close any open files associated with getting **		the connection; this is used when running the queue, **		etc., to avoid having extra file descriptors during **		the queue run and to avoid confusing the network **		code (if it cares). **	makeconnection(host, port, mci, e, enough) **		Make a connection to the named host on the given **		port. Returns zero on success, else an exit status **		describing the error. **	host_map_lookup(map, hbuf, avp, pstat) **		Convert the entry in hbuf into a canonical form. */
end_comment

begin_decl_stmt
specifier|static
name|int
name|NDaemons
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* actual number of daemons */
end_comment

begin_decl_stmt
specifier|static
name|time_t
name|NextDiskSpaceCheck
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* **  GETREQUESTS -- open mail IPC port and get requests. ** **	Parameters: **		e -- the current envelope. ** **	Returns: **		pointer to flags. ** **	Side Effects: **		Waits until some interesting activity occurs.  When **		it does, a child is created to process it, and the **		parent waits for completion.  Return from this **		routine is always in the child.  The file pointers **		"InChannel" and "OutChannel" should be set to point **		to the communication channel. **		May restart persistent queue runners if they have ended **		for some reason. */
end_comment

begin_function
name|BITMAP256
modifier|*
name|getrequests
parameter_list|(
name|e
parameter_list|)
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
name|int
name|t
decl_stmt|;
name|int
name|idx
decl_stmt|,
name|curdaemon
init|=
operator|-
literal|1
decl_stmt|;
name|int
name|i
decl_stmt|,
name|olddaemon
init|=
literal|0
decl_stmt|;
if|#
directive|if
name|XDEBUG
name|bool
name|j_has_dot
decl_stmt|;
endif|#
directive|endif
comment|/* XDEBUG */
name|char
name|status
index|[
name|MAXLINE
index|]
decl_stmt|;
name|SOCKADDR
name|sa
decl_stmt|;
name|SOCKADDR_LEN_T
name|len
init|=
sizeof|sizeof
argument_list|(
name|sa
argument_list|)
decl_stmt|;
if|#
directive|if
name|_FFR_QUEUE_RUN_PARANOIA
name|time_t
name|lastrun
decl_stmt|;
endif|#
directive|endif
comment|/* _FFR_QUEUE_RUN_PARANOIA */
if|#
directive|if
name|NETUNIX
specifier|extern
name|int
name|ControlSocket
decl_stmt|;
endif|#
directive|endif
comment|/* NETUNIX */
specifier|extern
name|ENVELOPE
name|BlankEnvelope
decl_stmt|;
comment|/* initialize data for function that generates queue ids */
name|init_qid_alg
argument_list|()
expr_stmt|;
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_port
operator|=
name|setupdaemon
argument_list|(
operator|&
operator|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_addr
operator|)
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_firsttime
operator|=
name|true
expr_stmt|;
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_refuse_connections_until
operator|=
operator|(
name|time_t
operator|)
literal|0
expr_stmt|;
block|}
comment|/* 	**  Try to actually open the connection. 	*/
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|1
argument_list|)
condition|)
block|{
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
name|sm_dprintf
argument_list|(
literal|"getrequests: daemon %s: port %d\n"
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_name
argument_list|,
name|ntohs
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_port
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* get a socket for the SMTP connection */
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socksize
operator|=
name|opendaemonsocket
argument_list|(
operator|&
name|Daemons
index|[
name|idx
index|]
argument_list|,
name|true
argument_list|)
expr_stmt|;
if|if
condition|(
name|opencontrolsocket
argument_list|()
operator|<
literal|0
condition|)
name|sm_syslog
argument_list|(
name|LOG_WARNING
argument_list|,
name|NOQID
argument_list|,
literal|"daemon could not open control socket %s: %s"
argument_list|,
name|ControlSocketName
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
comment|/* If there are any queue runners released reapchild() co-ord's */
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGCHLD
argument_list|,
name|reapchild
argument_list|)
expr_stmt|;
comment|/* write the pid to file, command line args to syslog */
name|log_sendmail_pid
argument_list|(
name|e
argument_list|)
expr_stmt|;
if|#
directive|if
name|XDEBUG
block|{
name|char
name|jbuf
index|[
name|MAXHOSTNAMELEN
index|]
decl_stmt|;
name|expand
argument_list|(
literal|"\201j"
argument_list|,
name|jbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|jbuf
argument_list|)
argument_list|,
name|e
argument_list|)
expr_stmt|;
name|j_has_dot
operator|=
name|strchr
argument_list|(
name|jbuf
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* XDEBUG */
comment|/* Add parent process as first item */
name|proc_list_add
argument_list|(
name|CurrentPid
argument_list|,
literal|"Sendmail daemon"
argument_list|,
name|PROC_DAEMON
argument_list|,
literal|0
argument_list|,
operator|-
literal|1
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|1
argument_list|)
condition|)
block|{
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
name|sm_dprintf
argument_list|(
literal|"getrequests: daemon %s: %d\n"
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_name
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
block|}
for|for
control|(
init|;
condition|;
control|)
block|{
specifier|register
name|pid_t
name|pid
decl_stmt|;
specifier|auto
name|SOCKADDR_LEN_T
name|lotherend
decl_stmt|;
name|bool
name|timedout
init|=
name|false
decl_stmt|;
name|bool
name|control
init|=
name|false
decl_stmt|;
name|int
name|save_errno
decl_stmt|;
name|int
name|pipefd
index|[
literal|2
index|]
decl_stmt|;
name|time_t
name|now
decl_stmt|;
if|#
directive|if
name|STARTTLS
name|long
name|seed
decl_stmt|;
endif|#
directive|endif
comment|/* STARTTLS */
comment|/* see if we are rejecting connections */
operator|(
name|void
operator|)
name|sm_blocksignal
argument_list|(
name|SIGALRM
argument_list|)
expr_stmt|;
name|CHECK_RESTART
expr_stmt|;
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
comment|/* 			**  XXX do this call outside the loop? 			**	no: refuse_connections may sleep(). 			*/
name|now
operator|=
name|curtime
argument_list|()
expr_stmt|;
if|if
condition|(
name|now
operator|<
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_refuse_connections_until
condition|)
continue|continue;
if|if
condition|(
name|bitnset
argument_list|(
name|D_DISABLE
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
condition|)
continue|continue;
if|if
condition|(
name|refuseconnections
argument_list|(
name|e
argument_list|,
name|idx
argument_list|,
name|curdaemon
operator|==
name|idx
argument_list|)
condition|)
block|{
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|>=
literal|0
condition|)
block|{
comment|/* close socket so peer fails quickly */
operator|(
name|void
operator|)
name|close
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
block|}
comment|/* refuse connections for next 15 seconds */
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_refuse_connections_until
operator|=
name|now
operator|+
literal|15
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|<
literal|0
operator|||
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_firsttime
condition|)
block|{
if|if
condition|(
operator|!
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_firsttime
operator|&&
name|LogLevel
operator|>
literal|8
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|NOQID
argument_list|,
literal|"accepting connections again for daemon %s"
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_name
argument_list|)
expr_stmt|;
comment|/* arrange to (re)open the socket if needed */
operator|(
name|void
operator|)
name|opendaemonsocket
argument_list|(
operator|&
name|Daemons
index|[
name|idx
index|]
argument_list|,
name|false
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_firsttime
operator|=
name|false
expr_stmt|;
block|}
block|}
comment|/* May have been sleeping above, check again */
name|CHECK_RESTART
expr_stmt|;
name|getrequests_checkdiskspace
argument_list|(
name|e
argument_list|)
expr_stmt|;
if|#
directive|if
name|XDEBUG
comment|/* check for disaster */
block|{
name|char
name|jbuf
index|[
name|MAXHOSTNAMELEN
index|]
decl_stmt|;
name|expand
argument_list|(
literal|"\201j"
argument_list|,
name|jbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|jbuf
argument_list|)
argument_list|,
name|e
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|wordinclass
argument_list|(
name|jbuf
argument_list|,
literal|'w'
argument_list|)
condition|)
block|{
name|dumpstate
argument_list|(
literal|"daemon lost $j"
argument_list|)
expr_stmt|;
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"daemon process doesn't have $j in $=w; see syslog"
argument_list|)
expr_stmt|;
name|abort
argument_list|()
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|j_has_dot
operator|&&
name|strchr
argument_list|(
name|jbuf
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
condition|)
block|{
name|dumpstate
argument_list|(
literal|"daemon $j lost dot"
argument_list|)
expr_stmt|;
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"daemon process $j lost dot; see syslog"
argument_list|)
expr_stmt|;
name|abort
argument_list|()
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* XDEBUG */
if|#
directive|if
literal|0
comment|/* 		**  Andrew Sun<asun@ieps-sun.ml.com> claims that this will 		**  fix the SVr4 problem.  But it seems to have gone away, 		**  so is it worth doing this? 		*/
block|if (DaemonSocket>= 0&& 		    SetNonBlocking(DaemonSocket, false)< 0) 			log an error here;
endif|#
directive|endif
comment|/* 0 */
operator|(
name|void
operator|)
name|sm_releasesignal
argument_list|(
name|SIGALRM
argument_list|)
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|bool
name|setproc
init|=
name|false
decl_stmt|;
name|int
name|highest
init|=
operator|-
literal|1
decl_stmt|;
name|fd_set
name|readfds
decl_stmt|;
name|struct
name|timeval
name|timeout
decl_stmt|;
name|CHECK_RESTART
expr_stmt|;
name|FD_ZERO
argument_list|(
operator|&
name|readfds
argument_list|)
expr_stmt|;
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
comment|/* wait for a connection */
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|>=
literal|0
condition|)
block|{
if|if
condition|(
operator|!
name|setproc
operator|&&
operator|!
name|bitnset
argument_list|(
name|D_ETRNONLY
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
condition|)
block|{
name|sm_setproctitle
argument_list|(
name|true
argument_list|,
name|e
argument_list|,
literal|"accepting connections"
argument_list|)
expr_stmt|;
name|setproc
operator|=
name|true
expr_stmt|;
block|}
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|>
name|highest
condition|)
name|highest
operator|=
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
expr_stmt|;
name|SM_FD_SET
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|,
operator|&
name|readfds
argument_list|)
expr_stmt|;
block|}
block|}
if|#
directive|if
name|NETUNIX
if|if
condition|(
name|ControlSocket
operator|>=
literal|0
condition|)
block|{
if|if
condition|(
name|ControlSocket
operator|>
name|highest
condition|)
name|highest
operator|=
name|ControlSocket
expr_stmt|;
name|SM_FD_SET
argument_list|(
name|ControlSocket
argument_list|,
operator|&
name|readfds
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETUNIX */
name|timeout
operator|.
name|tv_sec
operator|=
literal|5
expr_stmt|;
name|timeout
operator|.
name|tv_usec
operator|=
literal|0
expr_stmt|;
name|t
operator|=
name|select
argument_list|(
name|highest
operator|+
literal|1
argument_list|,
name|FDSET_CAST
operator|&
name|readfds
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|,
operator|&
name|timeout
argument_list|)
expr_stmt|;
comment|/* Did someone signal while waiting? */
name|CHECK_RESTART
expr_stmt|;
name|curdaemon
operator|=
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|doqueuerun
argument_list|()
condition|)
block|{
operator|(
name|void
operator|)
name|runqueue
argument_list|(
name|true
argument_list|,
name|false
argument_list|,
name|false
argument_list|,
name|false
argument_list|)
expr_stmt|;
if|#
directive|if
name|_FFR_QUEUE_RUN_PARANOIA
name|lastrun
operator|=
name|now
expr_stmt|;
endif|#
directive|endif
comment|/* _FFR_QUEUE_RUN_PARANOIA */
block|}
if|#
directive|if
name|_FFR_QUEUE_RUN_PARANOIA
elseif|else
if|if
condition|(
name|CheckQueueRunners
operator|>
literal|0
operator|&&
name|QueueIntvl
operator|>
literal|0
operator|&&
name|lastrun
operator|+
name|QueueIntvl
operator|+
name|CheckQueueRunners
operator|<
name|now
condition|)
block|{
comment|/* 				**  set lastrun unconditionally to avoid 				**  calling checkqueuerunner() all the time. 				**  That's also why we currently ignore the 				**  result of the function call. 				*/
operator|(
name|void
operator|)
name|checkqueuerunner
argument_list|()
expr_stmt|;
name|lastrun
operator|=
name|now
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* _FFR_QUEUE_RUN_PARANOIA */
if|if
condition|(
name|t
operator|<=
literal|0
condition|)
block|{
name|timedout
operator|=
name|true
expr_stmt|;
break|break;
block|}
name|control
operator|=
name|false
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
comment|/* look "round-robin" for an active socket */
if|if
condition|(
operator|(
name|idx
operator|=
name|olddaemon
operator|+
literal|1
operator|)
operator|>=
name|NDaemons
condition|)
name|idx
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NDaemons
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|>=
literal|0
operator|&&
name|SM_FD_ISSET
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|,
operator|&
name|readfds
argument_list|)
condition|)
block|{
name|lotherend
operator|=
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socksize
expr_stmt|;
name|memset
argument_list|(
operator|&
name|RealHostAddr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|RealHostAddr
argument_list|)
argument_list|)
expr_stmt|;
name|t
operator|=
name|accept
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|RealHostAddr
argument_list|,
operator|&
name|lotherend
argument_list|)
expr_stmt|;
comment|/* 					**  If remote side closes before 					**  accept() finishes, sockaddr 					**  might not be fully filled in. 					*/
if|if
condition|(
name|t
operator|>=
literal|0
operator|&&
operator|(
name|lotherend
operator|==
literal|0
operator|||
ifdef|#
directive|ifdef
name|BSD4_4_SOCKADDR
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_len
operator|==
literal|0
operator|||
endif|#
directive|endif
comment|/* BSD4_4_SOCKADDR */
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
operator|!=
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|t
argument_list|)
expr_stmt|;
name|t
operator|=
operator|-
literal|1
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
block|}
name|olddaemon
operator|=
name|curdaemon
operator|=
name|idx
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|++
name|idx
operator|>=
name|NDaemons
condition|)
name|idx
operator|=
literal|0
expr_stmt|;
block|}
if|#
directive|if
name|NETUNIX
if|if
condition|(
name|curdaemon
operator|==
operator|-
literal|1
operator|&&
name|ControlSocket
operator|>=
literal|0
operator|&&
name|SM_FD_ISSET
argument_list|(
name|ControlSocket
argument_list|,
operator|&
name|readfds
argument_list|)
condition|)
block|{
name|struct
name|sockaddr_un
name|sa_un
decl_stmt|;
name|lotherend
operator|=
sizeof|sizeof
argument_list|(
name|sa_un
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|sa_un
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|sa_un
argument_list|)
argument_list|)
expr_stmt|;
name|t
operator|=
name|accept
argument_list|(
name|ControlSocket
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|sa_un
argument_list|,
operator|&
name|lotherend
argument_list|)
expr_stmt|;
comment|/* 				**  If remote side closes before 				**  accept() finishes, sockaddr 				**  might not be fully filled in. 				*/
if|if
condition|(
name|t
operator|>=
literal|0
operator|&&
operator|(
name|lotherend
operator|==
literal|0
operator|||
ifdef|#
directive|ifdef
name|BSD4_4_SOCKADDR
name|sa_un
operator|.
name|sun_len
operator|==
literal|0
operator|||
endif|#
directive|endif
comment|/* BSD4_4_SOCKADDR */
name|sa_un
operator|.
name|sun_family
operator|!=
name|AF_UNIX
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|t
argument_list|)
expr_stmt|;
name|t
operator|=
operator|-
literal|1
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
block|}
if|if
condition|(
name|t
operator|>=
literal|0
condition|)
name|control
operator|=
name|true
expr_stmt|;
block|}
else|#
directive|else
comment|/* NETUNIX */
if|if
condition|(
name|curdaemon
operator|==
operator|-
literal|1
condition|)
block|{
comment|/* No daemon to service */
continue|continue;
block|}
endif|#
directive|endif
comment|/* NETUNIX */
if|if
condition|(
name|t
operator|>=
literal|0
operator|||
name|errno
operator|!=
name|EINTR
condition|)
break|break;
block|}
if|if
condition|(
name|timedout
condition|)
block|{
name|timedout
operator|=
name|false
expr_stmt|;
continue|continue;
block|}
name|save_errno
operator|=
name|errno
expr_stmt|;
operator|(
name|void
operator|)
name|sm_blocksignal
argument_list|(
name|SIGALRM
argument_list|)
expr_stmt|;
if|if
condition|(
name|t
operator|<
literal|0
condition|)
block|{
name|errno
operator|=
name|save_errno
expr_stmt|;
comment|/* let's ignore these temporary errors */
if|if
condition|(
name|save_errno
operator|==
name|EINTR
ifdef|#
directive|ifdef
name|EAGAIN
operator|||
name|save_errno
operator|==
name|EAGAIN
endif|#
directive|endif
comment|/* EAGAIN */
ifdef|#
directive|ifdef
name|ECONNABORTED
operator|||
name|save_errno
operator|==
name|ECONNABORTED
endif|#
directive|endif
comment|/* ECONNABORTED */
ifdef|#
directive|ifdef
name|EWOULDBLOCK
operator|||
name|save_errno
operator|==
name|EWOULDBLOCK
endif|#
directive|endif
comment|/* EWOULDBLOCK */
condition|)
continue|continue;
name|syserr
argument_list|(
literal|"getrequests: accept"
argument_list|)
expr_stmt|;
if|if
condition|(
name|curdaemon
operator|>=
literal|0
condition|)
block|{
comment|/* arrange to re-open socket next time around */
operator|(
name|void
operator|)
name|close
argument_list|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
if|#
directive|if
name|SO_REUSEADDR_IS_BROKEN
comment|/* 				**  Give time for bound socket to be released. 				**  This creates a denial-of-service if you can 				**  force accept() to fail on affected systems. 				*/
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_refuse_connections_until
operator|=
name|curtime
argument_list|()
operator|+
literal|15
expr_stmt|;
endif|#
directive|endif
comment|/* SO_REUSEADDR_IS_BROKEN */
block|}
continue|continue;
block|}
if|if
condition|(
operator|!
name|control
condition|)
block|{
comment|/* set some daemon related macros */
switch|switch
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
case|case
name|AF_UNSPEC
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"unspec"
argument_list|)
expr_stmt|;
break|break;
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
if|#
directive|if
name|NETUNIX
case|case
name|AF_UNIX
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"local"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"inet"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"inet6"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"iso"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
if|#
directive|if
name|NETNS
case|case
name|AF_NS
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"ns"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETNS */
if|#
directive|if
name|NETX25
case|case
name|AF_CCITT
case|:
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_family}"
argument_list|)
argument_list|,
literal|"x.25"
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETX25 */
block|}
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_name}"
argument_list|)
argument_list|,
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_name
argument_list|)
expr_stmt|;
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_mflags
operator|!=
name|NULL
condition|)
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_flags}"
argument_list|)
argument_list|,
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_mflags
argument_list|)
expr_stmt|;
else|else
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{daemon_flags}"
argument_list|)
argument_list|,
literal|""
argument_list|)
expr_stmt|;
block|}
comment|/* 		**  If connection rate is exceeded here, connection shall be 		**  refused later by a new call after fork() by the 		**  validate_connection() function. Closing the connection 		**  at this point violates RFC 2821. 		**  Do NOT remove this call, its side effects are needed. 		*/
name|connection_rate_check
argument_list|(
operator|&
name|RealHostAddr
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* 		**  Create a subprocess to process the mail. 		*/
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getrequests: forking (fd = %d)\n"
argument_list|,
name|t
argument_list|)
expr_stmt|;
comment|/* 		**  Advance state of PRNG. 		**  This is necessary because otherwise all child processes 		**  will produce the same PRN sequence and hence the selection 		**  of a queue directory (and other things, e.g., MX selection) 		**  are not "really" random. 		*/
if|#
directive|if
name|STARTTLS
comment|/* XXX get some better "random" data? */
name|seed
operator|=
name|get_random
argument_list|()
expr_stmt|;
name|RAND_seed
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|&
name|NextDiskSpaceCheck
argument_list|,
sizeof|sizeof
argument_list|(
name|NextDiskSpaceCheck
argument_list|)
argument_list|)
expr_stmt|;
name|RAND_seed
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|&
name|now
argument_list|,
sizeof|sizeof
argument_list|(
name|now
argument_list|)
argument_list|)
expr_stmt|;
name|RAND_seed
argument_list|(
operator|(
name|void
operator|*
operator|)
operator|&
name|seed
argument_list|,
sizeof|sizeof
argument_list|(
name|seed
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* STARTTLS */
operator|(
name|void
operator|)
name|get_random
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* STARTTLS */
if|#
directive|if
name|NAMED_BIND
comment|/* 		**  Update MX records for FallbackMX. 		**  Let's hope this is fast otherwise we screw up the 		**  response time. 		*/
if|if
condition|(
name|FallbackMX
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|getfallbackmxrr
argument_list|(
name|FallbackMX
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
if|if
condition|(
name|tTd
argument_list|(
literal|93
argument_list|,
literal|100
argument_list|)
condition|)
block|{
comment|/* don't fork, handle connection in this process */
name|pid
operator|=
literal|0
expr_stmt|;
name|pipefd
index|[
literal|0
index|]
operator|=
name|pipefd
index|[
literal|1
index|]
operator|=
operator|-
literal|1
expr_stmt|;
block|}
else|else
block|{
comment|/* 			**  Create a pipe to keep the child from writing to 			**  the socket until after the parent has closed 			**  it.  Otherwise the parent may hang if the child 			**  has closed it first. 			*/
if|if
condition|(
name|pipe
argument_list|(
name|pipefd
argument_list|)
operator|<
literal|0
condition|)
name|pipefd
index|[
literal|0
index|]
operator|=
name|pipefd
index|[
literal|1
index|]
operator|=
operator|-
literal|1
expr_stmt|;
operator|(
name|void
operator|)
name|sm_blocksignal
argument_list|(
name|SIGCHLD
argument_list|)
expr_stmt|;
name|pid
operator|=
name|fork
argument_list|()
expr_stmt|;
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
block|{
name|syserr
argument_list|(
literal|"daemon: cannot fork"
argument_list|)
expr_stmt|;
if|if
condition|(
name|pipefd
index|[
literal|0
index|]
operator|!=
operator|-
literal|1
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|sm_releasesignal
argument_list|(
name|SIGCHLD
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sleep
argument_list|(
literal|10
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|t
argument_list|)
expr_stmt|;
continue|continue;
block|}
block|}
if|if
condition|(
name|pid
operator|==
literal|0
condition|)
block|{
name|char
modifier|*
name|p
decl_stmt|;
name|SM_FILE_T
modifier|*
name|inchannel
decl_stmt|,
modifier|*
name|outchannel
init|=
name|NULL
decl_stmt|;
comment|/* 			**  CHILD -- return to caller. 			**	Collect verified idea of sending host. 			**	Verify calling user id if possible here. 			*/
comment|/* Reset global flags */
name|RestartRequest
operator|=
name|NULL
expr_stmt|;
name|RestartWorkGroup
operator|=
name|false
expr_stmt|;
name|ShutdownRequest
operator|=
name|NULL
expr_stmt|;
name|PendingSignal
operator|=
literal|0
expr_stmt|;
name|CurrentPid
operator|=
name|getpid
argument_list|()
expr_stmt|;
name|close_sendmail_pid
argument_list|()
expr_stmt|;
operator|(
name|void
operator|)
name|sm_releasesignal
argument_list|(
name|SIGALRM
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_releasesignal
argument_list|(
name|SIGCHLD
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGCHLD
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGHUP
argument_list|,
name|SIG_DFL
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGTERM
argument_list|,
name|intsig
argument_list|)
expr_stmt|;
comment|/* turn on profiling */
comment|/* SM_PROF(0); */
comment|/* 			**  Initialize exception stack and default exception 			**  handler for child process. 			*/
name|sm_exc_newthread
argument_list|(
name|fatal_error
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|control
condition|)
block|{
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{daemon_addr}"
argument_list|)
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_addr
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|status
argument_list|,
sizeof|sizeof
argument_list|(
name|status
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|ntohs
argument_list|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_port
argument_list|)
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{daemon_port}"
argument_list|)
argument_list|,
name|status
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|>=
literal|0
condition|)
operator|(
name|void
operator|)
name|close
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
block|}
name|clrcontrol
argument_list|()
expr_stmt|;
comment|/* Avoid SMTP daemon actions if control command */
if|if
condition|(
name|control
condition|)
block|{
comment|/* Add control socket process */
name|proc_list_add
argument_list|(
name|CurrentPid
argument_list|,
literal|"console socket child"
argument_list|,
name|PROC_CONTROL_CHILD
argument_list|,
literal|0
argument_list|,
operator|-
literal|1
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|proc_list_clear
argument_list|()
expr_stmt|;
comment|/* clean up background delivery children */
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGCHLD
argument_list|,
name|reapchild
argument_list|)
expr_stmt|;
comment|/* Add parent process as first child item */
name|proc_list_add
argument_list|(
name|CurrentPid
argument_list|,
literal|"daemon child"
argument_list|,
name|PROC_DAEMON_CHILD
argument_list|,
literal|0
argument_list|,
operator|-
literal|1
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
comment|/* don't schedule queue runs if ETRN */
name|QueueIntvl
operator|=
literal|0
expr_stmt|;
comment|/* 				**  Hack: override global variables if 				**	the corresponding DaemonPortOption 				**	is set. 				*/
if|#
directive|if
name|_FFR_SS_PER_DAEMON
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_supersafe
operator|!=
name|DPO_NOTSET
condition|)
name|SuperSafe
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_supersafe
expr_stmt|;
endif|#
directive|endif
comment|/* _FFR_SS_PER_DAEMON */
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_dm
operator|!=
name|DM_NOTSET
condition|)
name|set_delivery_mode
argument_list|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_dm
argument_list|,
name|e
argument_list|)
expr_stmt|;
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_refuseLA
operator|!=
name|DPO_NOTSET
condition|)
name|RefuseLA
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_refuseLA
expr_stmt|;
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_queueLA
operator|!=
name|DPO_NOTSET
condition|)
name|QueueLA
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_queueLA
expr_stmt|;
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_delayLA
operator|!=
name|DPO_NOTSET
condition|)
name|DelayLA
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_delayLA
expr_stmt|;
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_maxchildren
operator|!=
name|DPO_NOTSET
condition|)
name|MaxChildren
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_maxchildren
expr_stmt|;
name|sm_setproctitle
argument_list|(
name|true
argument_list|,
name|e
argument_list|,
literal|"startup with %s"
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|pipefd
index|[
literal|0
index|]
operator|!=
operator|-
literal|1
condition|)
block|{
specifier|auto
name|char
name|c
decl_stmt|;
comment|/* 				**  Wait for the parent to close the write end 				**  of the pipe, which we will see as an EOF. 				**  This guarantees that we won't write to the 				**  socket until after the parent has closed 				**  the pipe. 				*/
comment|/* close the write end of the pipe */
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
comment|/* we shouldn't be interrupted, but ... */
while|while
condition|(
name|read
argument_list|(
name|pipefd
index|[
literal|0
index|]
argument_list|,
operator|&
name|c
argument_list|,
literal|1
argument_list|)
operator|<
literal|0
operator|&&
name|errno
operator|==
name|EINTR
condition|)
continue|continue;
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
block|}
comment|/* control socket processing */
if|if
condition|(
name|control
condition|)
block|{
name|control_command
argument_list|(
name|t
argument_list|,
name|e
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
name|exit
argument_list|(
name|EX_SOFTWARE
argument_list|)
expr_stmt|;
block|}
comment|/* determine host name */
name|p
operator|=
name|hostnamebyanyaddr
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|p
argument_list|)
operator|>
name|MAXNAME
condition|)
comment|/* XXX  - 1 ? */
name|p
index|[
name|MAXNAME
index|]
operator|=
literal|'\0'
expr_stmt|;
name|RealHostName
operator|=
name|newstr
argument_list|(
name|p
argument_list|)
expr_stmt|;
if|if
condition|(
name|RealHostName
index|[
literal|0
index|]
operator|==
literal|'['
condition|)
block|{
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{client_resolve}"
argument_list|)
argument_list|,
name|h_errno
operator|==
name|TRY_AGAIN
condition|?
literal|"TEMP"
else|:
literal|"FAIL"
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{client_resolve}"
argument_list|)
argument_list|,
literal|"OK"
argument_list|)
expr_stmt|;
block|}
name|sm_setproctitle
argument_list|(
name|true
argument_list|,
name|e
argument_list|,
literal|"startup with %s"
argument_list|,
name|p
argument_list|)
expr_stmt|;
name|markstats
argument_list|(
name|e
argument_list|,
name|NULL
argument_list|,
name|STATS_CONNECT
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|inchannel
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|t
argument_list|,
name|SM_IO_RDONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|(
name|t
operator|=
name|dup
argument_list|(
name|t
argument_list|)
operator|)
operator|<
literal|0
operator|||
operator|(
name|outchannel
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|t
argument_list|,
name|SM_IO_WRONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"cannot open SMTP server channel, fd=%d"
argument_list|,
name|t
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OK
argument_list|)
expr_stmt|;
block|}
name|sm_io_automode
argument_list|(
name|inchannel
argument_list|,
name|outchannel
argument_list|)
expr_stmt|;
name|InChannel
operator|=
name|inchannel
expr_stmt|;
name|OutChannel
operator|=
name|outchannel
expr_stmt|;
name|DisConnected
operator|=
name|false
expr_stmt|;
if|#
directive|if
name|XLA
if|if
condition|(
operator|!
name|xla_host_ok
argument_list|(
name|RealHostName
argument_list|)
condition|)
block|{
name|message
argument_list|(
literal|"421 4.4.5 Too many SMTP sessions for this host"
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OK
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* XLA */
comment|/* find out name for interface of connection */
if|if
condition|(
name|getsockname
argument_list|(
name|sm_io_getinfo
argument_list|(
name|InChannel
argument_list|,
name|SM_IO_WHAT_FD
argument_list|,
name|NULL
argument_list|)
argument_list|,
operator|&
name|sa
operator|.
name|sa
argument_list|,
operator|&
name|len
argument_list|)
operator|==
literal|0
condition|)
block|{
name|p
operator|=
name|hostnamebyanyaddr
argument_list|(
operator|&
name|sa
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|9
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getreq: got name %s\n"
argument_list|,
name|p
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_name}"
argument_list|)
argument_list|,
name|p
argument_list|)
expr_stmt|;
comment|/* 				**  Do this only if it is not the loopback 				**  interface. 				*/
if|if
condition|(
operator|!
name|isloopback
argument_list|(
name|sa
argument_list|)
condition|)
block|{
name|char
modifier|*
name|addr
decl_stmt|;
name|char
name|family
index|[
literal|5
index|]
decl_stmt|;
name|addr
operator|=
name|anynet_ntoa
argument_list|(
operator|&
name|sa
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|family
argument_list|,
sizeof|sizeof
argument_list|(
name|family
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|sa
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_addr}"
argument_list|)
argument_list|,
name|addr
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_family}"
argument_list|)
argument_list|,
name|family
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|7
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getreq: got addr %s and family %s\n"
argument_list|,
name|addr
argument_list|,
name|family
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_addr}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_family}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|7
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getreq: getsockname failed\n"
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_name}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_addr}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_family}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
break|break;
block|}
comment|/* parent -- keep track of children */
if|if
condition|(
name|control
condition|)
block|{
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|status
argument_list|,
sizeof|sizeof
argument_list|(
name|status
argument_list|)
argument_list|,
literal|"control socket server child"
argument_list|)
expr_stmt|;
name|proc_list_add
argument_list|(
name|pid
argument_list|,
name|status
argument_list|,
name|PROC_CONTROL
argument_list|,
literal|0
argument_list|,
operator|-
literal|1
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
else|else
block|{
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|status
argument_list|,
sizeof|sizeof
argument_list|(
name|status
argument_list|)
argument_list|,
literal|"SMTP server child for %s"
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
argument_list|)
expr_stmt|;
name|proc_list_add
argument_list|(
name|pid
argument_list|,
name|status
argument_list|,
name|PROC_DAEMON
argument_list|,
literal|0
argument_list|,
operator|-
literal|1
argument_list|,
operator|&
name|RealHostAddr
argument_list|)
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|sm_releasesignal
argument_list|(
name|SIGCHLD
argument_list|)
expr_stmt|;
comment|/* close the read end of the synchronization pipe */
if|if
condition|(
name|pipefd
index|[
literal|0
index|]
operator|!=
operator|-
literal|1
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
name|pipefd
index|[
literal|0
index|]
operator|=
operator|-
literal|1
expr_stmt|;
block|}
comment|/* close the port so that others will hang (for a while) */
operator|(
name|void
operator|)
name|close
argument_list|(
name|t
argument_list|)
expr_stmt|;
comment|/* release the child by closing the read end of the sync pipe */
if|if
condition|(
name|pipefd
index|[
literal|1
index|]
operator|!=
operator|-
literal|1
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|pipefd
index|[
literal|1
index|]
argument_list|)
expr_stmt|;
name|pipefd
index|[
literal|1
index|]
operator|=
operator|-
literal|1
expr_stmt|;
block|}
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getreq: returning\n"
argument_list|)
expr_stmt|;
if|#
directive|if
name|MILTER
comment|/* set the filters for this daemon */
if|if
condition|(
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_inputfilterlist
operator|!=
name|NULL
condition|)
block|{
for|for
control|(
name|i
operator|=
literal|0
init|;
operator|(
name|i
operator|<
name|MAXFILTERS
operator|&&
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_inputfilters
index|[
name|i
index|]
operator|!=
name|NULL
operator|)
condition|;
name|i
operator|++
control|)
block|{
name|InputFilters
index|[
name|i
index|]
operator|=
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_inputfilters
index|[
name|i
index|]
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|<
name|MAXFILTERS
condition|)
name|InputFilters
index|[
name|i
index|]
operator|=
name|NULL
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* MILTER */
return|return
operator|&
name|Daemons
index|[
name|curdaemon
index|]
operator|.
name|d_flags
return|;
block|}
end_function

begin_comment
comment|/* **  GETREQUESTS_CHECKDISKSPACE -- check available diskspace. ** **	Parameters: **		e -- envelope. ** **	Returns: **		none. ** **	Side Effects: **		Modifies Daemon flags (D_ETRNONLY) if not enough disk space. */
end_comment

begin_function
specifier|static
name|void
name|getrequests_checkdiskspace
parameter_list|(
name|e
parameter_list|)
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
block|{
name|bool
name|logged
init|=
name|false
decl_stmt|;
name|int
name|idx
decl_stmt|;
name|time_t
name|now
decl_stmt|;
name|now
operator|=
name|curtime
argument_list|()
expr_stmt|;
if|if
condition|(
name|now
operator|<
name|NextDiskSpaceCheck
condition|)
return|return;
comment|/* Check if there is available disk space in all queue groups. */
if|if
condition|(
operator|!
name|enoughdiskspace
argument_list|(
literal|0
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
operator|++
name|idx
control|)
block|{
if|if
condition|(
name|bitnset
argument_list|(
name|D_ETRNONLY
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
condition|)
continue|continue;
comment|/* log only if not logged before */
if|if
condition|(
operator|!
name|logged
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|8
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|NOQID
argument_list|,
literal|"rejecting new messages: min free: %ld"
argument_list|,
name|MinBlocksFree
argument_list|)
expr_stmt|;
name|sm_setproctitle
argument_list|(
name|true
argument_list|,
name|e
argument_list|,
literal|"rejecting new messages: min free: %ld"
argument_list|,
name|MinBlocksFree
argument_list|)
expr_stmt|;
name|logged
operator|=
name|true
expr_stmt|;
block|}
name|setbitn
argument_list|(
name|D_ETRNONLY
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
operator|++
name|idx
control|)
block|{
if|if
condition|(
operator|!
name|bitnset
argument_list|(
name|D_ETRNONLY
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
condition|)
continue|continue;
comment|/* log only if not logged before */
if|if
condition|(
operator|!
name|logged
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|8
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|NOQID
argument_list|,
literal|"accepting new messages (again)"
argument_list|)
expr_stmt|;
name|logged
operator|=
name|true
expr_stmt|;
block|}
comment|/* title will be set later */
name|clrbitn
argument_list|(
name|D_ETRNONLY
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_flags
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* only check disk space once a minute */
name|NextDiskSpaceCheck
operator|=
name|now
operator|+
literal|60
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  OPENDAEMONSOCKET -- open SMTP socket ** **	Deals with setting all appropriate options. ** **	Parameters: **		d -- the structure for the daemon to open. **		firsttime -- set if this is the initial open. ** **	Returns: **		Size in bytes of the daemon socket addr. ** **	Side Effects: **		Leaves DaemonSocket set to the open socket. **		Exits if the socket cannot be created. */
end_comment

begin_define
define|#
directive|define
name|MAXOPENTRIES
value|10
end_define

begin_comment
comment|/* maximum number of tries to open connection */
end_comment

begin_function
specifier|static
name|int
name|opendaemonsocket
parameter_list|(
name|d
parameter_list|,
name|firsttime
parameter_list|)
name|DAEMON_T
modifier|*
name|d
decl_stmt|;
name|bool
name|firsttime
decl_stmt|;
block|{
name|int
name|on
init|=
literal|1
decl_stmt|;
name|int
name|fdflags
decl_stmt|;
name|SOCKADDR_LEN_T
name|socksize
init|=
literal|0
decl_stmt|;
name|int
name|ntries
init|=
literal|0
decl_stmt|;
name|int
name|save_errno
decl_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"opendaemonsocket(%s)\n"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
do|do
block|{
if|if
condition|(
name|ntries
operator|>
literal|0
condition|)
operator|(
name|void
operator|)
name|sleep
argument_list|(
literal|5
argument_list|)
expr_stmt|;
if|if
condition|(
name|firsttime
operator|||
name|d
operator|->
name|d_socket
operator|<
literal|0
condition|)
block|{
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
if|#
directive|if
name|NETUNIX
if|if
condition|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|==
name|AF_UNIX
condition|)
block|{
name|int
name|rval
decl_stmt|;
name|long
name|sff
init|=
name|SFF_SAFEDIRPATH
operator||
name|SFF_OPENASROOT
operator||
name|SFF_NOLINK
operator||
name|SFF_ROOTOK
operator||
name|SFF_EXECOK
operator||
name|SFF_CREAT
decl_stmt|;
comment|/* if not safe, don't use it */
name|rval
operator|=
name|safefile
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|,
name|RunAsUid
argument_list|,
name|RunAsGid
argument_list|,
name|RunAsUserName
argument_list|,
name|sff
argument_list|,
name|S_IRUSR
operator||
name|S_IWUSR
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|rval
operator|!=
literal|0
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: unsafe domain socket %s"
argument_list|,
name|d
operator|->
name|d_name
argument_list|,
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
comment|/* Don't try to overtake an existing socket */
operator|(
name|void
operator|)
name|unlink
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DOMAIN_NETUNIX */
name|d
operator|->
name|d_socket
operator|=
name|socket
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|d
operator|->
name|d_socket
operator|<
literal|0
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: can't create server SMTP socket"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
name|fail
label|:
if|if
condition|(
name|bitnset
argument_list|(
name|D_OPTIONAL
argument_list|,
name|d
operator|->
name|d_flags
argument_list|)
operator|&&
operator|(
operator|!
name|transienterror
argument_list|(
name|save_errno
argument_list|)
operator|||
name|ntries
operator|>=
name|MAXOPENTRIES
operator|-
literal|1
operator|)
condition|)
block|{
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: optional socket disabled"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
name|setbitn
argument_list|(
name|D_DISABLE
argument_list|,
name|d
operator|->
name|d_flags
argument_list|)
expr_stmt|;
name|d
operator|->
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|severe
label|:
if|if
condition|(
name|LogLevel
operator|>
literal|0
condition|)
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"daemon %s: problem creating SMTP socket"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
name|d
operator|->
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|SM_FD_SETSIZE
operator|>
literal|0
operator|&&
name|d
operator|->
name|d_socket
operator|>=
name|SM_FD_SETSIZE
condition|)
block|{
name|save_errno
operator|=
name|EINVAL
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: server SMTP socket (%d) too large"
argument_list|,
name|d
operator|->
name|d_name
argument_list|,
name|d
operator|->
name|d_socket
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
comment|/* turn on network debugging? */
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|101
argument_list|)
condition|)
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_DEBUG
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_KEEPALIVE
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SO_RCVBUF
if|if
condition|(
name|d
operator|->
name|d_tcprcvbufsize
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|setsockopt
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_RCVBUF
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|d
operator|->
name|d_tcprcvbufsize
argument_list|,
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_tcprcvbufsize
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: setsockopt(SO_RCVBUF)"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SO_RCVBUF */
ifdef|#
directive|ifdef
name|SO_SNDBUF
if|if
condition|(
name|d
operator|->
name|d_tcpsndbufsize
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|setsockopt
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_SNDBUF
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|d
operator|->
name|d_tcpsndbufsize
argument_list|,
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_tcpsndbufsize
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: setsockopt(SO_SNDBUF)"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SO_SNDBUF */
if|if
condition|(
operator|(
name|fdflags
operator|=
name|fcntl
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|F_GETFD
argument_list|,
literal|0
argument_list|)
operator|)
operator|==
operator|-
literal|1
operator|||
name|fcntl
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|F_SETFD
argument_list|,
name|fdflags
operator||
name|FD_CLOEXEC
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: failed to %s close-on-exec flag: %s"
argument_list|,
name|d
operator|->
name|d_name
argument_list|,
name|fdflags
operator|==
operator|-
literal|1
condition|?
literal|"get"
else|:
literal|"set"
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|d
operator|->
name|d_socket
argument_list|)
expr_stmt|;
goto|goto
name|severe
goto|;
block|}
switch|switch
condition|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
ifdef|#
directive|ifdef
name|NETUNIX
case|case
name|AF_UNIX
case|:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sin
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sin6
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|siso
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
default|default:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
argument_list|)
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|bind
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sa
argument_list|,
name|socksize
argument_list|)
operator|<
literal|0
condition|)
block|{
comment|/* probably another daemon already */
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: cannot bind"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|d
operator|->
name|d_socket
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
block|}
if|if
condition|(
operator|!
name|firsttime
operator|&&
name|listen
argument_list|(
name|d
operator|->
name|d_socket
argument_list|,
name|d
operator|->
name|d_listenqueue
argument_list|)
operator|<
literal|0
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"opendaemonsocket: daemon %s: cannot listen"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|d
operator|->
name|d_socket
argument_list|)
expr_stmt|;
goto|goto
name|severe
goto|;
block|}
return|return
name|socksize
return|;
block|}
do|while
condition|(
name|ntries
operator|++
operator|<
name|MAXOPENTRIES
operator|&&
name|transienterror
argument_list|(
name|save_errno
argument_list|)
condition|)
do|;
name|syserr
argument_list|(
literal|"!opendaemonsocket: daemon %s: server SMTP socket wedged: exiting"
argument_list|,
name|d
operator|->
name|d_name
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
return|return
operator|-
literal|1
return|;
comment|/* avoid compiler warning on IRIX */
block|}
end_function

begin_comment
comment|/* **  SETUPDAEMON -- setup socket for daemon ** **	Parameters: **		daemonaddr -- socket for daemon ** **	Returns: **		port number on which daemon should run ** */
end_comment

begin_function
specifier|static
name|unsigned
name|short
name|setupdaemon
parameter_list|(
name|daemonaddr
parameter_list|)
name|SOCKADDR
modifier|*
name|daemonaddr
decl_stmt|;
block|{
name|unsigned
name|short
name|port
decl_stmt|;
comment|/* 	**  Set up the address for the mailer. 	*/
if|if
condition|(
name|daemonaddr
operator|->
name|sa
operator|.
name|sa_family
operator|==
name|AF_UNSPEC
condition|)
block|{
name|memset
argument_list|(
name|daemonaddr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|daemonaddr
argument_list|)
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET
name|daemonaddr
operator|->
name|sa
operator|.
name|sa_family
operator|=
name|AF_INET
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET */
block|}
switch|switch
condition|(
name|daemonaddr
operator|->
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|daemonaddr
operator|->
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|==
literal|0
condition|)
name|daemonaddr
operator|->
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|INADDR_ANY
expr_stmt|;
name|port
operator|=
name|daemonaddr
operator|->
name|sin
operator|.
name|sin_port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|IN6_IS_ADDR_UNSPECIFIED
argument_list|(
operator|&
name|daemonaddr
operator|->
name|sin6
operator|.
name|sin6_addr
argument_list|)
condition|)
name|daemonaddr
operator|->
name|sin6
operator|.
name|sin6_addr
operator|=
name|in6addr_any
expr_stmt|;
name|port
operator|=
name|daemonaddr
operator|->
name|sin6
operator|.
name|sin6_port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
comment|/* unknown protocol */
name|port
operator|=
literal|0
expr_stmt|;
break|break;
block|}
if|if
condition|(
name|port
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|port
operator|=
name|htons
argument_list|(
literal|25
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
block|{
specifier|register
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|sp
operator|=
name|getservbyname
argument_list|(
literal|"smtp"
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"554 5.3.5 service \"smtp\" unknown"
argument_list|)
expr_stmt|;
name|port
operator|=
name|htons
argument_list|(
literal|25
argument_list|)
expr_stmt|;
block|}
else|else
name|port
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
block|}
switch|switch
condition|(
name|daemonaddr
operator|->
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|daemonaddr
operator|->
name|sin
operator|.
name|sin_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|daemonaddr
operator|->
name|sin6
operator|.
name|sin6_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
comment|/* unknown protocol */
break|break;
block|}
return|return
name|port
return|;
block|}
end_function

begin_comment
comment|/* **  CLRDAEMON -- reset the daemon connection ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		releases any resources used by the passive daemon. */
end_comment

begin_function
name|void
name|clrdaemon
parameter_list|()
block|{
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NDaemons
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
operator|>=
literal|0
condition|)
operator|(
name|void
operator|)
name|close
argument_list|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/* **  GETMODIFIERS -- get modifier flags ** **	Parameters: **		v -- the modifiers (input text line). **		modifiers -- pointer to flag field to represent modifiers. ** **	Returns: **		(xallocat()ed) string representation of modifiers. ** **	Side Effects: **		fills in modifiers. */
end_comment

begin_function
name|char
modifier|*
name|getmodifiers
parameter_list|(
name|v
parameter_list|,
name|modifiers
parameter_list|)
name|char
modifier|*
name|v
decl_stmt|;
name|BITMAP256
name|modifiers
decl_stmt|;
block|{
name|int
name|l
decl_stmt|;
name|char
modifier|*
name|h
decl_stmt|,
modifier|*
name|f
decl_stmt|,
modifier|*
name|flags
decl_stmt|;
comment|/* maximum length of flags: upper case Option -> "OO " */
name|l
operator|=
literal|3
operator|*
name|strlen
argument_list|(
name|v
argument_list|)
operator|+
literal|3
expr_stmt|;
comment|/* is someone joking? */
if|if
condition|(
name|l
operator|<
literal|0
operator|||
name|l
operator|>
literal|256
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|2
condition|)
name|sm_syslog
argument_list|(
name|LOG_ERR
argument_list|,
name|NOQID
argument_list|,
literal|"getmodifiers too long, ignored"
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|flags
operator|=
name|xalloc
argument_list|(
name|l
argument_list|)
expr_stmt|;
name|f
operator|=
name|flags
expr_stmt|;
name|clrbitmap
argument_list|(
name|modifiers
argument_list|)
expr_stmt|;
for|for
control|(
name|h
operator|=
name|v
init|;
operator|*
name|h
operator|!=
literal|'\0'
condition|;
name|h
operator|++
control|)
block|{
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|h
argument_list|)
operator|&&
operator|!
name|isspace
argument_list|(
operator|*
name|h
argument_list|)
operator|&&
name|isprint
argument_list|(
operator|*
name|h
argument_list|)
condition|)
block|{
name|setbitn
argument_list|(
operator|*
name|h
argument_list|,
name|modifiers
argument_list|)
expr_stmt|;
if|if
condition|(
name|flags
operator|!=
name|f
condition|)
operator|*
name|flags
operator|++
operator|=
literal|' '
expr_stmt|;
operator|*
name|flags
operator|++
operator|=
operator|*
name|h
expr_stmt|;
if|if
condition|(
name|isupper
argument_list|(
operator|*
name|h
argument_list|)
condition|)
operator|*
name|flags
operator|++
operator|=
operator|*
name|h
expr_stmt|;
block|}
block|}
operator|*
name|flags
operator|++
operator|=
literal|'\0'
expr_stmt|;
return|return
name|f
return|;
block|}
end_function

begin_comment
comment|/* **  CHKDAEMONMODIFIERS -- check whether all daemons have set a flag. ** **	Parameters: **		flag -- the flag to test. ** **	Returns: **		true iff all daemons have set flag. */
end_comment

begin_function
name|bool
name|chkdaemonmodifiers
parameter_list|(
name|flag
parameter_list|)
name|int
name|flag
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NDaemons
condition|;
name|i
operator|++
control|)
if|if
condition|(
operator|!
name|bitnset
argument_list|(
operator|(
name|char
operator|)
name|flag
argument_list|,
name|Daemons
index|[
name|i
index|]
operator|.
name|d_flags
argument_list|)
condition|)
return|return
name|false
return|;
return|return
name|true
return|;
block|}
end_function

begin_comment
comment|/* **  SETSOCKADDROPTIONS -- set options for SOCKADDR (daemon or client) ** **	Parameters: **		p -- the options line. **		d -- the daemon structure to fill in. ** **	Returns: **		none. */
end_comment

begin_function
specifier|static
name|void
name|setsockaddroptions
parameter_list|(
name|p
parameter_list|,
name|d
parameter_list|)
name|char
modifier|*
name|p
decl_stmt|;
name|DAEMON_T
modifier|*
name|d
decl_stmt|;
block|{
if|#
directive|if
name|NETISO
name|short
name|portno
decl_stmt|;
endif|#
directive|endif
comment|/* NETISO */
name|char
modifier|*
name|port
init|=
name|NULL
decl_stmt|;
name|char
modifier|*
name|addr
init|=
name|NULL
decl_stmt|;
if|#
directive|if
name|NETINET
if|if
condition|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|==
name|AF_UNSPEC
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_INET
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|_FFR_SS_PER_DAEMON
name|d
operator|->
name|d_supersafe
operator|=
name|DPO_NOTSET
expr_stmt|;
endif|#
directive|endif
comment|/* _FFR_SS_PER_DAEMON */
name|d
operator|->
name|d_dm
operator|=
name|DM_NOTSET
expr_stmt|;
name|d
operator|->
name|d_refuseLA
operator|=
name|DPO_NOTSET
expr_stmt|;
name|d
operator|->
name|d_queueLA
operator|=
name|DPO_NOTSET
expr_stmt|;
name|d
operator|->
name|d_delayLA
operator|=
name|DPO_NOTSET
expr_stmt|;
name|d
operator|->
name|d_maxchildren
operator|=
name|DPO_NOTSET
expr_stmt|;
while|while
condition|(
name|p
operator|!=
name|NULL
condition|)
block|{
specifier|register
name|char
modifier|*
name|f
decl_stmt|;
specifier|register
name|char
modifier|*
name|v
decl_stmt|;
while|while
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|==
literal|'\0'
condition|)
break|break;
name|f
operator|=
name|p
expr_stmt|;
name|p
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|','
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|++
operator|=
literal|'\0'
expr_stmt|;
name|v
operator|=
name|strchr
argument_list|(
name|f
argument_list|,
literal|'='
argument_list|)
expr_stmt|;
if|if
condition|(
name|v
operator|==
name|NULL
condition|)
continue|continue;
while|while
condition|(
name|isascii
argument_list|(
operator|*
operator|++
name|v
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|v
argument_list|)
condition|)
continue|continue;
switch|switch
condition|(
operator|*
name|f
condition|)
block|{
case|case
literal|'A'
case|:
comment|/* address */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'a'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|addr
operator|=
name|v
expr_stmt|;
break|break;
case|case
literal|'c'
case|:
name|d
operator|->
name|d_maxchildren
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'D'
case|:
comment|/* DeliveryMode */
switch|switch
condition|(
operator|*
name|v
condition|)
block|{
case|case
name|SM_QUEUE
case|:
case|case
name|SM_DEFER
case|:
case|case
name|SM_DELIVER
case|:
case|case
name|SM_FORK
case|:
name|d
operator|->
name|d_dm
operator|=
operator|*
name|v
expr_stmt|;
break|break;
default|default:
name|syserr
argument_list|(
literal|"554 5.3.5 Unknown delivery mode %c"
argument_list|,
operator|*
name|v
argument_list|)
expr_stmt|;
break|break;
block|}
break|break;
case|case
literal|'d'
case|:
comment|/* delayLA */
name|d
operator|->
name|d_delayLA
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'F'
case|:
comment|/* address family */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'f'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|v
argument_list|)
operator|&&
name|isdigit
argument_list|(
operator|*
name|v
argument_list|)
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
ifdef|#
directive|ifdef
name|NETUNIX
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"unix"
argument_list|)
operator|==
literal|0
operator|||
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"local"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_UNIX
expr_stmt|;
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
if|#
directive|if
name|NETINET
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"inet"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_INET
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"inet6"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_INET6
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"iso"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_ISO
expr_stmt|;
endif|#
directive|endif
comment|/* NETISO */
if|#
directive|if
name|NETNS
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"ns"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_NS
expr_stmt|;
endif|#
directive|endif
comment|/* NETNS */
if|#
directive|if
name|NETX25
elseif|else
if|if
condition|(
name|sm_strcasecmp
argument_list|(
name|v
argument_list|,
literal|"x.25"
argument_list|)
operator|==
literal|0
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|AF_CCITT
expr_stmt|;
endif|#
directive|endif
comment|/* NETX25 */
else|else
name|syserr
argument_list|(
literal|"554 5.3.5 Unknown address family %s in Family=option"
argument_list|,
name|v
argument_list|)
expr_stmt|;
break|break;
if|#
directive|if
name|MILTER
case|case
literal|'I'
case|:
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'i'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|d
operator|->
name|d_inputfilterlist
operator|=
name|v
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* MILTER */
case|case
literal|'L'
case|:
comment|/* listen queue size */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'l'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|d
operator|->
name|d_listenqueue
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'M'
case|:
comment|/* modifiers (flags) */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'m'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|d
operator|->
name|d_mflags
operator|=
name|getmodifiers
argument_list|(
name|v
argument_list|,
name|d
operator|->
name|d_flags
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'N'
case|:
comment|/* name */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'n'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|d
operator|->
name|d_name
operator|=
name|v
expr_stmt|;
break|break;
case|case
literal|'P'
case|:
comment|/* port */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'p'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|port
operator|=
name|v
expr_stmt|;
break|break;
case|case
literal|'q'
case|:
name|d
operator|->
name|d_queueLA
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'R'
case|:
comment|/* receive buffer size */
name|d
operator|->
name|d_tcprcvbufsize
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'r'
case|:
name|d
operator|->
name|d_refuseLA
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
case|case
literal|'S'
case|:
comment|/* send buffer size */
if|#
directive|if
operator|!
name|_FFR_DPO_CS
case|case
literal|'s'
case|:
endif|#
directive|endif
comment|/* !_FFR_DPO_CS */
name|d
operator|->
name|d_tcpsndbufsize
operator|=
name|atoi
argument_list|(
name|v
argument_list|)
expr_stmt|;
break|break;
if|#
directive|if
name|_FFR_SS_PER_DAEMON
case|case
literal|'T'
case|:
comment|/* SuperSafe */
if|if
condition|(
name|tolower
argument_list|(
operator|*
name|v
argument_list|)
operator|==
literal|'i'
condition|)
name|d
operator|->
name|d_supersafe
operator|=
name|SAFE_INTERACTIVE
expr_stmt|;
elseif|else
if|if
condition|(
name|tolower
argument_list|(
operator|*
name|v
argument_list|)
operator|==
literal|'p'
condition|)
if|#
directive|if
name|MILTER
name|d
operator|->
name|d_supersafe
operator|=
name|SAFE_REALLY_POSTMILTER
expr_stmt|;
else|#
directive|else
comment|/* MILTER */
operator|(
name|void
operator|)
name|sm_io_fprintf
argument_list|(
name|smioout
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
literal|"Warning: SuperSafe=PostMilter requires Milter support (-DMILTER)\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* MILTER */
else|else
name|d
operator|->
name|d_supersafe
operator|=
name|atobool
argument_list|(
name|v
argument_list|)
condition|?
name|SAFE_REALLY
else|:
name|SAFE_NO
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* _FFR_SS_PER_DAEMON */
default|default:
name|syserr
argument_list|(
literal|"554 5.3.5 PortOptions parameter \"%s\" unknown"
argument_list|,
name|f
argument_list|)
expr_stmt|;
block|}
block|}
comment|/* Check addr and port after finding family */
if|if
condition|(
name|addr
operator|!=
name|NULL
condition|)
block|{
switch|switch
condition|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
if|#
directive|if
name|NETUNIX
case|case
name|AF_UNIX
case|:
if|if
condition|(
name|strlen
argument_list|(
name|addr
argument_list|)
operator|>=
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
condition|)
block|{
name|errno
operator|=
name|ENAMETOOLONG
expr_stmt|;
name|syserr
argument_list|(
literal|"setsockaddroptions: domain socket name too long: %s> %d"
argument_list|,
name|addr
argument_list|,
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
argument_list|)
expr_stmt|;
break|break;
block|}
comment|/* file safety check done in opendaemonsocket() */
operator|(
name|void
operator|)
name|memset
argument_list|(
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|,
name|addr
argument_list|,
sizeof|sizeof
argument_list|(
name|d
operator|->
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
operator|!
name|isascii
argument_list|(
operator|*
name|addr
argument_list|)
operator|||
operator|!
name|isdigit
argument_list|(
operator|*
name|addr
argument_list|)
operator|||
operator|(
operator|(
name|d
operator|->
name|d_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|inet_addr
argument_list|(
name|addr
argument_list|)
operator|)
operator|==
name|INADDR_NONE
operator|)
condition|)
block|{
specifier|register
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|addr
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.0 host \"%s\" unknown"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
else|else
block|{
while|while
condition|(
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
operator|!=
name|NULL
operator|&&
name|hp
operator|->
name|h_addrtype
operator|!=
name|AF_INET
condition|)
name|hp
operator|->
name|h_addr_list
operator|++
expr_stmt|;
if|if
condition|(
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.0 host \"%s\" unknown"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
else|else
name|memmove
argument_list|(
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sin
operator|.
name|sin_addr
argument_list|,
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
argument_list|,
name|INADDRSZ
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|anynet_pton
argument_list|(
name|AF_INET6
argument_list|,
name|addr
argument_list|,
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
operator|!=
literal|1
condition|)
block|{
specifier|register
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|addr
argument_list|,
name|AF_INET6
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.0 host \"%s\" unknown"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
else|else
block|{
while|while
condition|(
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
operator|!=
name|NULL
operator|&&
name|hp
operator|->
name|h_addrtype
operator|!=
name|AF_INET6
condition|)
name|hp
operator|->
name|h_addr_list
operator|++
expr_stmt|;
if|if
condition|(
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.0 host \"%s\" unknown"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
else|else
name|memmove
argument_list|(
operator|&
name|d
operator|->
name|d_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|,
operator|*
operator|(
name|hp
operator|->
name|h_addr_list
operator|)
argument_list|,
name|IN6ADDRSZ
argument_list|)
expr_stmt|;
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
block|}
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
name|syserr
argument_list|(
literal|"554 5.3.5 address= option unsupported for family %d"
argument_list|,
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
break|break;
block|}
block|}
if|if
condition|(
name|port
operator|!=
name|NULL
condition|)
block|{
switch|switch
condition|(
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|port
argument_list|)
operator|&&
name|isdigit
argument_list|(
operator|*
name|port
argument_list|)
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|htons
argument_list|(
operator|(
name|unsigned
name|short
operator|)
name|atoi
argument_list|(
operator|(
specifier|const
name|char
operator|*
operator|)
name|port
argument_list|)
argument_list|)
expr_stmt|;
else|else
block|{
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|syserr
argument_list|(
literal|"554 5.3.5 invalid port number: %s"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
specifier|register
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|sp
operator|=
name|getservbyname
argument_list|(
name|port
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.5 service \"%s\" unknown"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|else
name|d
operator|->
name|d_addr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|port
argument_list|)
operator|&&
name|isdigit
argument_list|(
operator|*
name|port
argument_list|)
condition|)
name|d
operator|->
name|d_addr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|htons
argument_list|(
operator|(
name|unsigned
name|short
operator|)
name|atoi
argument_list|(
name|port
argument_list|)
argument_list|)
expr_stmt|;
else|else
block|{
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|syserr
argument_list|(
literal|"554 5.3.5 invalid port number: %s"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
specifier|register
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|sp
operator|=
name|getservbyname
argument_list|(
name|port
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.5 service \"%s\" unknown"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|else
name|d
operator|->
name|d_addr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
comment|/* assume two byte transport selector */
if|if
condition|(
name|isascii
argument_list|(
operator|*
name|port
argument_list|)
operator|&&
name|isdigit
argument_list|(
operator|*
name|port
argument_list|)
condition|)
name|portno
operator|=
name|htons
argument_list|(
operator|(
name|unsigned
name|short
operator|)
name|atoi
argument_list|(
name|port
argument_list|)
argument_list|)
expr_stmt|;
else|else
block|{
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|syserr
argument_list|(
literal|"554 5.3.5 invalid port number: %s"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
specifier|register
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
name|sp
operator|=
name|getservbyname
argument_list|(
name|port
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
name|syserr
argument_list|(
literal|"554 5.3.5 service \"%s\" unknown"
argument_list|,
name|port
argument_list|)
expr_stmt|;
else|else
name|portno
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
block|}
name|memmove
argument_list|(
name|TSEL
argument_list|(
operator|&
name|d
operator|->
name|d_addr
operator|.
name|siso
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|portno
argument_list|,
literal|2
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
default|default:
name|syserr
argument_list|(
literal|"554 5.3.5 Port= option unsupported for family %d"
argument_list|,
name|d
operator|->
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
break|break;
block|}
block|}
block|}
end_function

begin_comment
comment|/* **  SETDAEMONOPTIONS -- set options for running the MTA daemon ** **	Parameters: **		p -- the options line. ** **	Returns: **		true if successful, false otherwise. ** **	Side Effects: **		increments number of daemons. */
end_comment

begin_define
define|#
directive|define
name|DEF_LISTENQUEUE
value|10
end_define

begin_struct
struct|struct
name|dflags
block|{
name|char
modifier|*
name|d_name
decl_stmt|;
name|int
name|d_flag
decl_stmt|;
block|}
struct|;
end_struct

begin_decl_stmt
specifier|static
name|struct
name|dflags
name|DaemonFlags
index|[]
init|=
block|{
block|{
literal|"AUTHREQ"
block|,
name|D_AUTHREQ
block|}
block|,
block|{
literal|"BINDIF"
block|,
name|D_BINDIF
block|}
block|,
block|{
literal|"CANONREQ"
block|,
name|D_CANONREQ
block|}
block|,
block|{
literal|"IFNHELO"
block|,
name|D_IFNHELO
block|}
block|,
block|{
literal|"FQMAIL"
block|,
name|D_FQMAIL
block|}
block|,
block|{
literal|"FQRCPT"
block|,
name|D_FQRCPT
block|}
block|,
block|{
literal|"SMTPS"
block|,
name|D_SMTPS
block|}
block|,
block|{
literal|"UNQUALOK"
block|,
name|D_UNQUALOK
block|}
block|,
block|{
literal|"NOAUTH"
block|,
name|D_NOAUTH
block|}
block|,
block|{
literal|"NOCANON"
block|,
name|D_NOCANON
block|}
block|,
block|{
literal|"NOETRN"
block|,
name|D_NOETRN
block|}
block|,
block|{
literal|"NOTLS"
block|,
name|D_NOTLS
block|}
block|,
block|{
literal|"ETRNONLY"
block|,
name|D_ETRNONLY
block|}
block|,
block|{
literal|"OPTIONAL"
block|,
name|D_OPTIONAL
block|}
block|,
block|{
literal|"DISABLE"
block|,
name|D_DISABLE
block|}
block|,
block|{
literal|"ISSET"
block|,
name|D_ISSET
block|}
block|,
block|{
name|NULL
block|,
literal|0
block|}
block|}
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|void
name|printdaemonflags
parameter_list|(
name|d
parameter_list|)
name|DAEMON_T
modifier|*
name|d
decl_stmt|;
block|{
specifier|register
name|struct
name|dflags
modifier|*
name|df
decl_stmt|;
name|bool
name|first
init|=
name|true
decl_stmt|;
for|for
control|(
name|df
operator|=
name|DaemonFlags
init|;
name|df
operator|->
name|d_name
operator|!=
name|NULL
condition|;
name|df
operator|++
control|)
block|{
if|if
condition|(
operator|!
name|bitnset
argument_list|(
name|df
operator|->
name|d_flag
argument_list|,
name|d
operator|->
name|d_flags
argument_list|)
condition|)
continue|continue;
if|if
condition|(
name|first
condition|)
name|sm_dprintf
argument_list|(
literal|"<%s"
argument_list|,
name|df
operator|->
name|d_name
argument_list|)
expr_stmt|;
else|else
name|sm_dprintf
argument_list|(
literal|",%s"
argument_list|,
name|df
operator|->
name|d_name
argument_list|)
expr_stmt|;
name|first
operator|=
name|false
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|first
condition|)
name|sm_dprintf
argument_list|(
literal|">"
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|bool
name|setdaemonoptions
parameter_list|(
name|p
parameter_list|)
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
block|{
if|if
condition|(
name|NDaemons
operator|>=
name|MAXDAEMONS
condition|)
return|return
name|false
return|;
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_listenqueue
operator|=
name|DEF_LISTENQUEUE
expr_stmt|;
name|clrbitmap
argument_list|(
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_flags
argument_list|)
expr_stmt|;
name|setsockaddroptions
argument_list|(
name|p
argument_list|,
operator|&
name|Daemons
index|[
name|NDaemons
index|]
argument_list|)
expr_stmt|;
if|#
directive|if
name|MILTER
if|if
condition|(
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_inputfilterlist
operator|!=
name|NULL
condition|)
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_inputfilterlist
operator|=
name|newstr
argument_list|(
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_inputfilterlist
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* MILTER */
if|if
condition|(
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
operator|!=
name|NULL
condition|)
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
operator|=
name|newstr
argument_list|(
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
argument_list|)
expr_stmt|;
else|else
block|{
name|char
name|num
index|[
literal|30
index|]
decl_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|num
argument_list|,
sizeof|sizeof
argument_list|(
name|num
argument_list|)
argument_list|,
literal|"Daemon%d"
argument_list|,
name|NDaemons
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
operator|=
name|newstr
argument_list|(
name|num
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|37
argument_list|,
literal|1
argument_list|)
condition|)
block|{
name|sm_dprintf
argument_list|(
literal|"Daemon %s flags: "
argument_list|,
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
argument_list|)
expr_stmt|;
name|printdaemonflags
argument_list|(
operator|&
name|Daemons
index|[
name|NDaemons
index|]
argument_list|)
expr_stmt|;
name|sm_dprintf
argument_list|(
literal|"\n"
argument_list|)
expr_stmt|;
block|}
operator|++
name|NDaemons
expr_stmt|;
return|return
name|true
return|;
block|}
end_function

begin_comment
comment|/* **  INITDAEMON -- initialize daemon if not yet done. ** **	Parameters: **		none ** **	Returns: **		none ** **	Side Effects: **		initializes structure for one daemon. */
end_comment

begin_function
name|void
name|initdaemon
parameter_list|()
block|{
if|if
condition|(
name|NDaemons
operator|==
literal|0
condition|)
block|{
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_listenqueue
operator|=
name|DEF_LISTENQUEUE
expr_stmt|;
name|Daemons
index|[
name|NDaemons
index|]
operator|.
name|d_name
operator|=
literal|"Daemon0"
expr_stmt|;
name|NDaemons
operator|=
literal|1
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/* **  SETCLIENTOPTIONS -- set options for running the client ** **	Parameters: **		p -- the options line. ** **	Returns: **		none. */
end_comment

begin_decl_stmt
specifier|static
name|DAEMON_T
name|ClientSettings
index|[
name|AF_MAX
operator|+
literal|1
index|]
decl_stmt|;
end_decl_stmt

begin_function
name|void
name|setclientoptions
parameter_list|(
name|p
parameter_list|)
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
block|{
name|int
name|family
decl_stmt|;
name|DAEMON_T
name|d
decl_stmt|;
name|memset
argument_list|(
operator|&
name|d
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|d
argument_list|)
argument_list|)
expr_stmt|;
name|setsockaddroptions
argument_list|(
name|p
argument_list|,
operator|&
name|d
argument_list|)
expr_stmt|;
comment|/* grab what we need */
name|family
operator|=
name|d
operator|.
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
expr_stmt|;
name|STRUCTCOPY
argument_list|(
name|d
argument_list|,
name|ClientSettings
index|[
name|family
index|]
argument_list|)
expr_stmt|;
name|setbitn
argument_list|(
name|D_ISSET
argument_list|,
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_flags
argument_list|)
expr_stmt|;
comment|/* mark as set */
if|if
condition|(
name|d
operator|.
name|d_name
operator|!=
name|NULL
condition|)
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_name
operator|=
name|newstr
argument_list|(
name|d
operator|.
name|d_name
argument_list|)
expr_stmt|;
else|else
block|{
name|char
name|num
index|[
literal|30
index|]
decl_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|num
argument_list|,
sizeof|sizeof
argument_list|(
name|num
argument_list|)
argument_list|,
literal|"Client%d"
argument_list|,
name|family
argument_list|)
expr_stmt|;
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_name
operator|=
name|newstr
argument_list|(
name|num
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/* **  ADDR_FAMILY -- determine address family from address ** **	Parameters: **		addr -- the string representation of the address ** **	Returns: **		AF_INET, AF_INET6 or AF_UNSPEC ** **	Side Effects: **		none. */
end_comment

begin_function
specifier|static
name|int
name|addr_family
parameter_list|(
name|addr
parameter_list|)
name|char
modifier|*
name|addr
decl_stmt|;
block|{
if|#
directive|if
name|NETINET6
name|SOCKADDR
name|clt_addr
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETINET
if|if
condition|(
name|inet_addr
argument_list|(
name|addr
argument_list|)
operator|!=
name|INADDR_NONE
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|9
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"addr_family(%s): INET\n"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
return|return
name|AF_INET
return|;
block|}
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
if|if
condition|(
name|anynet_pton
argument_list|(
name|AF_INET6
argument_list|,
name|addr
argument_list|,
operator|&
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
operator|==
literal|1
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|9
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"addr_family(%s): INET6\n"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
return|return
name|AF_INET6
return|;
block|}
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
if|#
directive|if
name|NETUNIX
if|if
condition|(
operator|*
name|addr
operator|==
literal|'/'
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|9
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"addr_family(%s): LOCAL\n"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
return|return
name|AF_UNIX
return|;
block|}
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|9
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"addr_family(%s): UNSPEC\n"
argument_list|,
name|addr
argument_list|)
expr_stmt|;
return|return
name|AF_UNSPEC
return|;
block|}
end_function

begin_comment
comment|/* **  CHKCLIENTMODIFIERS -- check whether all clients have set a flag. ** **	Parameters: **		flag -- the flag to test. ** **	Returns: **		true iff all configured clients have set the flag. */
end_comment

begin_function
name|bool
name|chkclientmodifiers
parameter_list|(
name|flag
parameter_list|)
name|int
name|flag
decl_stmt|;
block|{
name|int
name|i
decl_stmt|;
name|bool
name|flagisset
decl_stmt|;
name|flagisset
operator|=
name|false
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|AF_MAX
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|bitnset
argument_list|(
name|D_ISSET
argument_list|,
name|ClientSettings
index|[
name|i
index|]
operator|.
name|d_flags
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|bitnset
argument_list|(
operator|(
name|char
operator|)
name|flag
argument_list|,
name|ClientSettings
index|[
name|i
index|]
operator|.
name|d_flags
argument_list|)
condition|)
return|return
name|false
return|;
name|flagisset
operator|=
name|true
expr_stmt|;
block|}
block|}
return|return
name|flagisset
return|;
block|}
end_function

begin_if
if|#
directive|if
name|MILTER
end_if

begin_comment
comment|/* **  SETUP_DAEMON_FILTERS -- Parse per-socket filters ** **	Parameters: **		none ** **	Returns: **		none */
end_comment

begin_function
name|void
name|setup_daemon_milters
parameter_list|()
block|{
name|int
name|idx
decl_stmt|;
if|if
condition|(
name|OpMode
operator|==
name|MD_SMTP
condition|)
block|{
comment|/* no need to configure the daemons */
return|return;
block|}
for|for
control|(
name|idx
operator|=
literal|0
init|;
name|idx
operator|<
name|NDaemons
condition|;
name|idx
operator|++
control|)
block|{
if|if
condition|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_inputfilterlist
operator|!=
name|NULL
condition|)
block|{
name|milter_config
argument_list|(
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_inputfilterlist
argument_list|,
name|Daemons
index|[
name|idx
index|]
operator|.
name|d_inputfilters
argument_list|,
name|MAXFILTERS
argument_list|)
expr_stmt|;
block|}
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* MILTER */
end_comment

begin_comment
comment|/* **  MAKECONNECTION -- make a connection to an SMTP socket on a machine. ** **	Parameters: **		host -- the name of the host. **		port -- the port number to connect to. **		mci -- a pointer to the mail connection information **			structure to be filled in. **		e -- the current envelope. **		enough -- time at which to stop further connection attempts. **			(0 means no limit) ** **	Returns: **		An exit code telling whether the connection could be **			made and if not why not. ** **	Side Effects: **		none. */
end_comment

begin_decl_stmt
specifier|static
name|jmp_buf
name|CtxConnectTimeout
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|SOCKADDR
name|CurHostAddr
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* address of current host */
end_comment

begin_function
name|int
name|makeconnection
parameter_list|(
name|host
parameter_list|,
name|port
parameter_list|,
name|mci
parameter_list|,
name|e
parameter_list|,
name|enough
parameter_list|)
name|char
modifier|*
name|host
decl_stmt|;
specifier|volatile
name|unsigned
name|int
name|port
decl_stmt|;
specifier|register
name|MCI
modifier|*
name|mci
decl_stmt|;
name|ENVELOPE
modifier|*
name|e
decl_stmt|;
name|time_t
name|enough
decl_stmt|;
block|{
specifier|register
specifier|volatile
name|int
name|addrno
init|=
literal|0
decl_stmt|;
specifier|volatile
name|int
name|s
decl_stmt|;
specifier|register
name|struct
name|hostent
modifier|*
specifier|volatile
name|hp
init|=
operator|(
expr|struct
name|hostent
operator|*
operator|)
name|NULL
decl_stmt|;
name|SOCKADDR
name|addr
decl_stmt|;
name|SOCKADDR
name|clt_addr
decl_stmt|;
name|int
name|save_errno
init|=
literal|0
decl_stmt|;
specifier|volatile
name|SOCKADDR_LEN_T
name|addrlen
decl_stmt|;
specifier|volatile
name|bool
name|firstconnect
init|=
name|true
decl_stmt|;
name|SM_EVENT
modifier|*
specifier|volatile
name|ev
init|=
name|NULL
decl_stmt|;
if|#
directive|if
name|NETINET6
specifier|volatile
name|bool
name|v6found
init|=
name|false
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
specifier|volatile
name|int
name|family
init|=
name|InetMode
decl_stmt|;
name|SOCKADDR_LEN_T
name|len
decl_stmt|;
specifier|volatile
name|SOCKADDR_LEN_T
name|socksize
init|=
literal|0
decl_stmt|;
specifier|volatile
name|bool
name|clt_bind
decl_stmt|;
name|BITMAP256
name|d_flags
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
specifier|extern
name|ENVELOPE
name|BlankEnvelope
decl_stmt|;
comment|/* retranslate {daemon_flags} into bitmap */
name|clrbitmap
argument_list|(
name|d_flags
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|p
operator|=
name|macvalue
argument_list|(
name|macid
argument_list|(
literal|"{daemon_flags}"
argument_list|)
argument_list|,
name|e
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
for|for
control|(
init|;
operator|*
name|p
operator|!=
literal|'\0'
condition|;
name|p
operator|++
control|)
block|{
if|if
condition|(
operator|!
operator|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
operator|)
condition|)
name|setbitn
argument_list|(
name|bitidx
argument_list|(
operator|*
name|p
argument_list|)
argument_list|,
name|d_flags
argument_list|)
expr_stmt|;
block|}
block|}
if|#
directive|if
name|NETINET6
name|v4retry
label|:
endif|#
directive|endif
comment|/* NETINET6 */
name|clt_bind
operator|=
name|false
expr_stmt|;
comment|/* Set up the address for outgoing connection. */
if|if
condition|(
name|bitnset
argument_list|(
name|D_BINDIF
argument_list|,
name|d_flags
argument_list|)
operator|&&
operator|(
name|p
operator|=
name|macvalue
argument_list|(
name|macid
argument_list|(
literal|"{if_addr}"
argument_list|)
argument_list|,
name|e
argument_list|)
operator|)
operator|!=
name|NULL
operator|&&
operator|*
name|p
operator|!=
literal|'\0'
condition|)
block|{
if|#
directive|if
name|NETINET6
name|char
name|p6
index|[
name|INET6_ADDRSTRLEN
index|]
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
name|memset
argument_list|(
operator|&
name|clt_addr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|clt_addr
argument_list|)
argument_list|)
expr_stmt|;
comment|/* infer the address family from the address itself */
name|clt_addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|addr_family
argument_list|(
name|p
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|clt_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|clt_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|inet_addr
argument_list|(
name|p
argument_list|)
expr_stmt|;
if|if
condition|(
name|clt_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|!=
name|INADDR_NONE
operator|&&
name|clt_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|!=
name|INADDR_LOOPBACK
condition|)
block|{
name|clt_bind
operator|=
name|true
expr_stmt|;
name|socksize
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
expr_stmt|;
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|inet_addr
argument_list|(
name|p
argument_list|)
operator|!=
name|INADDR_NONE
condition|)
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|p6
argument_list|,
sizeof|sizeof
argument_list|(
name|p6
argument_list|)
argument_list|,
literal|"IPv6:::ffff:%s"
argument_list|,
name|p
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|p6
argument_list|,
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|p6
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|anynet_pton
argument_list|(
name|AF_INET6
argument_list|,
name|p6
argument_list|,
operator|&
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
operator|==
literal|1
operator|&&
operator|!
name|IN6_IS_ADDR_LOOPBACK
argument_list|(
operator|&
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
condition|)
block|{
name|clt_bind
operator|=
name|true
expr_stmt|;
name|socksize
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in6
argument_list|)
expr_stmt|;
block|}
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
literal|0
block|default: 			syserr("554 5.3.5 Address= option unsupported for family %d", 			       clt_addr.sa.sa_family); 			break;
endif|#
directive|endif
comment|/* 0 */
block|}
if|if
condition|(
name|clt_bind
condition|)
name|family
operator|=
name|clt_addr
operator|.
name|sa
operator|.
name|sa_family
expr_stmt|;
block|}
comment|/* D_BINDIF not set or not available, fallback to ClientPortOptions */
if|if
condition|(
operator|!
name|clt_bind
condition|)
block|{
name|STRUCTCOPY
argument_list|(
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_addr
argument_list|,
name|clt_addr
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|clt_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|clt_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|==
literal|0
condition|)
name|clt_addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|INADDR_ANY
expr_stmt|;
else|else
name|clt_bind
operator|=
name|true
expr_stmt|;
if|if
condition|(
name|clt_addr
operator|.
name|sin
operator|.
name|sin_port
operator|!=
literal|0
condition|)
name|clt_bind
operator|=
name|true
expr_stmt|;
name|socksize
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|IN6_IS_ADDR_UNSPECIFIED
argument_list|(
operator|&
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
condition|)
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_addr
operator|=
name|in6addr_any
expr_stmt|;
else|else
name|clt_bind
operator|=
name|true
expr_stmt|;
name|socksize
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in6
argument_list|)
expr_stmt|;
if|if
condition|(
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_port
operator|!=
literal|0
condition|)
name|clt_bind
operator|=
name|true
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
name|socksize
operator|=
sizeof|sizeof
argument_list|(
name|clt_addr
operator|.
name|siso
argument_list|)
expr_stmt|;
name|clt_bind
operator|=
name|true
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
default|default:
break|break;
block|}
block|}
comment|/* 	**  Set up the address for the mailer. 	**	Accept "[a.b.c.d]" syntax for host name. 	*/
name|SM_SET_H_ERRNO
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
name|memset
argument_list|(
operator|&
name|CurHostAddr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|CurHostAddr
argument_list|)
argument_list|)
expr_stmt|;
name|memset
argument_list|(
operator|&
name|addr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
name|SmtpPhase
operator|=
name|mci
operator|->
name|mci_phase
operator|=
literal|"initial connection"
expr_stmt|;
name|CurHostName
operator|=
name|host
expr_stmt|;
if|if
condition|(
name|host
index|[
literal|0
index|]
operator|==
literal|'['
condition|)
block|{
name|p
operator|=
name|strchr
argument_list|(
name|host
argument_list|,
literal|']'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|!=
name|NULL
condition|)
block|{
if|#
directive|if
name|NETINET
name|unsigned
name|long
name|hid
init|=
name|INADDR_NONE
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
name|struct
name|sockaddr_in6
name|hid6
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
if|#
directive|if
name|NETINET6
name|memset
argument_list|(
operator|&
name|hid6
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|hid6
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETINET
if|if
condition|(
name|family
operator|==
name|AF_INET
operator|&&
operator|(
name|hid
operator|=
name|inet_addr
argument_list|(
operator|&
name|host
index|[
literal|1
index|]
argument_list|)
operator|)
operator|!=
name|INADDR_NONE
condition|)
block|{
name|addr
operator|.
name|sin
operator|.
name|sin_family
operator|=
name|AF_INET
expr_stmt|;
name|addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|hid
expr_stmt|;
block|}
elseif|else
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
if|if
condition|(
name|family
operator|==
name|AF_INET6
operator|&&
name|anynet_pton
argument_list|(
name|AF_INET6
argument_list|,
operator|&
name|host
index|[
literal|1
index|]
argument_list|,
operator|&
name|hid6
operator|.
name|sin6_addr
argument_list|)
operator|==
literal|1
condition|)
block|{
name|addr
operator|.
name|sin6
operator|.
name|sin6_family
operator|=
name|AF_INET6
expr_stmt|;
name|addr
operator|.
name|sin6
operator|.
name|sin6_addr
operator|=
name|hid6
operator|.
name|sin6_addr
expr_stmt|;
block|}
else|else
endif|#
directive|endif
comment|/* NETINET6 */
block|{
comment|/* try it as a host name (avoid MX lookup) */
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
operator|&
name|host
index|[
literal|1
index|]
argument_list|,
name|family
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
operator|&&
name|p
index|[
operator|-
literal|1
index|]
operator|==
literal|'.'
condition|)
block|{
if|#
directive|if
name|NAMED_BIND
name|int
name|oldopts
init|=
name|_res
operator|.
name|options
decl_stmt|;
name|_res
operator|.
name|options
operator|&=
operator|~
operator|(
name|RES_DEFNAMES
operator||
name|RES_DNSRCH
operator|)
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
name|p
index|[
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
operator|&
name|host
index|[
literal|1
index|]
argument_list|,
name|family
argument_list|)
expr_stmt|;
name|p
index|[
operator|-
literal|1
index|]
operator|=
literal|'.'
expr_stmt|;
if|#
directive|if
name|NAMED_BIND
name|_res
operator|.
name|options
operator|=
name|oldopts
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
block|}
operator|*
name|p
operator|=
literal|']'
expr_stmt|;
goto|goto
name|gothostent
goto|;
block|}
operator|*
name|p
operator|=
literal|']'
expr_stmt|;
block|}
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
block|{
specifier|extern
name|char
name|MsgBuf
index|[]
decl_stmt|;
name|usrerrenh
argument_list|(
literal|"5.1.2"
argument_list|,
literal|"553 Invalid numeric domain spec \"%s\""
argument_list|,
name|host
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_NOHOST
argument_list|,
literal|"5.1.2"
argument_list|,
name|MsgBuf
argument_list|)
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
return|return
name|EX_NOHOST
return|;
block|}
block|}
else|else
block|{
comment|/* contortion to get around SGI cc complaints */
block|{
name|p
operator|=
operator|&
name|host
index|[
name|strlen
argument_list|(
name|host
argument_list|)
operator|-
literal|1
index|]
expr_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|host
argument_list|,
name|family
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
operator|&&
operator|*
name|p
operator|==
literal|'.'
condition|)
block|{
if|#
directive|if
name|NAMED_BIND
name|int
name|oldopts
init|=
name|_res
operator|.
name|options
decl_stmt|;
name|_res
operator|.
name|options
operator|&=
operator|~
operator|(
name|RES_DEFNAMES
operator||
name|RES_DNSRCH
operator|)
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|host
argument_list|,
name|family
argument_list|)
expr_stmt|;
operator|*
name|p
operator|=
literal|'.'
expr_stmt|;
if|#
directive|if
name|NAMED_BIND
name|_res
operator|.
name|options
operator|=
name|oldopts
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
block|}
block|}
name|gothostent
label|:
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
block|{
if|#
directive|if
name|NAMED_BIND
comment|/* check for name server timeouts */
if|#
directive|if
name|NETINET6
if|if
condition|(
name|WorkAroundBrokenAAAA
operator|&&
name|family
operator|==
name|AF_INET6
operator|&&
name|errno
operator|==
name|ETIMEDOUT
condition|)
block|{
comment|/* 				**  An attempt with family AF_INET may 				**  succeed By skipping the next section 				**  of code, we will try AF_INET before 				**  failing. 				*/
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|10
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"makeconnection: WorkAroundBrokenAAAA: Trying AF_INET lookup (AF_INET6 failed)\n"
argument_list|)
expr_stmt|;
block|}
else|else
endif|#
directive|endif
comment|/* NETINET6 */
block|{
if|if
condition|(
name|errno
operator|==
name|ETIMEDOUT
operator|||
if|#
directive|if
name|_FFR_GETHBN_ExFILE
ifdef|#
directive|ifdef
name|EMFILE
name|errno
operator|==
name|EMFILE
operator|||
endif|#
directive|endif
comment|/* EMFILE */
ifdef|#
directive|ifdef
name|ENFILE
name|errno
operator|==
name|ENFILE
operator|||
endif|#
directive|endif
comment|/* ENFILE */
endif|#
directive|endif
comment|/* _FFR_GETHBN_ExFILE */
name|h_errno
operator|==
name|TRY_AGAIN
operator|||
operator|(
name|errno
operator|==
name|ECONNREFUSED
operator|&&
name|UseNameServer
operator|)
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.3"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
block|}
endif|#
directive|endif
comment|/* NAMED_BIND */
if|#
directive|if
name|NETINET6
comment|/* 			**  Try v6 first, then fall back to v4. 			**  If we found a v6 address, but no v4 			**  addresses, then TEMPFAIL. 			*/
if|if
condition|(
name|family
operator|==
name|AF_INET6
condition|)
block|{
name|family
operator|=
name|AF_INET
expr_stmt|;
goto|goto
name|v4retry
goto|;
block|}
if|if
condition|(
name|v6found
condition|)
goto|goto
name|v6tempfail
goto|;
endif|#
directive|endif
comment|/* NETINET6 */
name|save_errno
operator|=
name|errno
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_NOHOST
argument_list|,
literal|"5.1.2"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_NOHOST
return|;
block|}
name|addr
operator|.
name|sa
operator|.
name|sa_family
operator|=
name|hp
operator|->
name|h_addrtype
expr_stmt|;
switch|switch
condition|(
name|hp
operator|->
name|h_addrtype
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|memmove
argument_list|(
operator|&
name|addr
operator|.
name|sin
operator|.
name|sin_addr
argument_list|,
name|hp
operator|->
name|h_addr
argument_list|,
name|INADDRSZ
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|memmove
argument_list|(
operator|&
name|addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|,
name|hp
operator|->
name|h_addr
argument_list|,
name|IN6ADDRSZ
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
if|if
condition|(
name|hp
operator|->
name|h_length
operator|>
sizeof|sizeof
argument_list|(
name|addr
operator|.
name|sa
operator|.
name|sa_data
argument_list|)
condition|)
block|{
name|syserr
argument_list|(
literal|"makeconnection: long sa_data: family %d len %d"
argument_list|,
name|hp
operator|->
name|h_addrtype
argument_list|,
name|hp
operator|->
name|h_length
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_NOHOST
argument_list|,
literal|"5.1.2"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
return|return
name|EX_NOHOST
return|;
block|}
name|memmove
argument_list|(
name|addr
operator|.
name|sa
operator|.
name|sa_data
argument_list|,
name|hp
operator|->
name|h_addr
argument_list|,
name|hp
operator|->
name|h_length
argument_list|)
expr_stmt|;
break|break;
block|}
name|addrno
operator|=
literal|1
expr_stmt|;
block|}
comment|/* 	**  Determine the port number. 	*/
if|if
condition|(
name|port
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|port
operator|=
name|htons
argument_list|(
literal|25
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
specifier|register
name|struct
name|servent
modifier|*
name|sp
init|=
name|getservbyname
argument_list|(
literal|"smtp"
argument_list|,
literal|"tcp"
argument_list|)
decl_stmt|;
if|if
condition|(
name|sp
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|2
condition|)
name|sm_syslog
argument_list|(
name|LOG_ERR
argument_list|,
name|NOQID
argument_list|,
literal|"makeconnection: service \"smtp\" unknown"
argument_list|)
expr_stmt|;
name|port
operator|=
name|htons
argument_list|(
literal|25
argument_list|)
expr_stmt|;
block|}
else|else
name|port
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
block|}
if|#
directive|if
name|NETINET6
if|if
condition|(
name|addr
operator|.
name|sa
operator|.
name|sa_family
operator|==
name|AF_INET6
operator|&&
name|IN6_IS_ADDR_V4MAPPED
argument_list|(
operator|&
name|addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
operator|&&
name|ClientSettings
index|[
name|AF_INET
index|]
operator|.
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|!=
literal|0
condition|)
block|{
comment|/* 		**  Ignore mapped IPv4 address since 		**  there is a ClientPortOptions setting 		**  for IPv4. 		*/
goto|goto
name|nextaddr
goto|;
block|}
endif|#
directive|endif
comment|/* NETINET6 */
switch|switch
condition|(
name|addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|addr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|port
expr_stmt|;
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|addr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|port
expr_stmt|;
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_in6
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
comment|/* assume two byte transport selector */
name|memmove
argument_list|(
name|TSEL
argument_list|(
operator|(
expr|struct
name|sockaddr_iso
operator|*
operator|)
operator|&
name|addr
argument_list|)
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|port
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|addrlen
operator|=
sizeof|sizeof
argument_list|(
expr|struct
name|sockaddr_iso
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
default|default:
name|syserr
argument_list|(
literal|"Can't connect to address family %d"
argument_list|,
name|addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_NOHOST
argument_list|,
literal|"5.1.2"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|EINVAL
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
return|return
name|EX_NOHOST
return|;
block|}
comment|/* 	**  Try to actually open the connection. 	*/
if|#
directive|if
name|XLA
comment|/* if too many connections, don't bother trying */
if|if
condition|(
operator|!
name|xla_noqueue_ok
argument_list|(
name|host
argument_list|)
condition|)
block|{
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
return|return
name|EX_TEMPFAIL
return|;
block|}
endif|#
directive|endif
comment|/* XLA */
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"makeconnection (%s [%s].%d (%d))\n"
argument_list|,
name|host
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|addr
argument_list|)
argument_list|,
name|ntohs
argument_list|(
name|port
argument_list|)
argument_list|,
operator|(
name|int
operator|)
name|addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
comment|/* save for logging */
name|CurHostAddr
operator|=
name|addr
expr_stmt|;
if|#
directive|if
name|HASRRESVPORT
if|if
condition|(
name|bitnset
argument_list|(
name|M_SECURE_PORT
argument_list|,
name|mci
operator|->
name|mci_mailer
operator|->
name|m_flags
argument_list|)
condition|)
block|{
name|int
name|rport
init|=
name|IPPORT_RESERVED
operator|-
literal|1
decl_stmt|;
name|s
operator|=
name|rresvport
argument_list|(
operator|&
name|rport
argument_list|)
expr_stmt|;
block|}
else|else
endif|#
directive|endif
comment|/* HASRRESVPORT */
block|{
name|s
operator|=
name|socket
argument_list|(
name|addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|s
operator|<
literal|0
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"makeconnection: cannot create socket"
argument_list|)
expr_stmt|;
if|#
directive|if
name|XLA
name|xla_host_end
argument_list|(
name|host
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* XLA */
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
ifdef|#
directive|ifdef
name|SO_SNDBUF
if|if
condition|(
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcpsndbufsize
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_SNDBUF
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcpsndbufsize
argument_list|,
sizeof|sizeof
argument_list|(
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcpsndbufsize
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|syserr
argument_list|(
literal|"makeconnection: setsockopt(SO_SNDBUF)"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SO_SNDBUF */
ifdef|#
directive|ifdef
name|SO_RCVBUF
if|if
condition|(
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcprcvbufsize
operator|>
literal|0
condition|)
block|{
if|if
condition|(
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_RCVBUF
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcprcvbufsize
argument_list|,
sizeof|sizeof
argument_list|(
name|ClientSettings
index|[
name|family
index|]
operator|.
name|d_tcprcvbufsize
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
name|syserr
argument_list|(
literal|"makeconnection: setsockopt(SO_RCVBUF)"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* SO_RCVBUF */
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"makeconnection: fd=%d\n"
argument_list|,
name|s
argument_list|)
expr_stmt|;
comment|/* turn on network debugging? */
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|101
argument_list|)
condition|)
block|{
name|int
name|on
init|=
literal|1
decl_stmt|;
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_DEBUG
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|e
operator|->
name|e_xfp
operator|!=
name|NULL
condition|)
comment|/* for debugging */
operator|(
name|void
operator|)
name|sm_io_flush
argument_list|(
name|e
operator|->
name|e_xfp
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
comment|/* for debugging */
if|if
condition|(
name|clt_bind
condition|)
block|{
name|int
name|on
init|=
literal|1
decl_stmt|;
switch|switch
condition|(
name|clt_addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|clt_addr
operator|.
name|sin
operator|.
name|sin_port
operator|!=
literal|0
condition|)
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|clt_addr
operator|.
name|sin6
operator|.
name|sin6_port
operator|!=
literal|0
condition|)
operator|(
name|void
operator|)
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|on
argument_list|,
sizeof|sizeof
argument_list|(
name|on
argument_list|)
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
if|if
condition|(
name|bind
argument_list|(
name|s
argument_list|,
operator|&
name|clt_addr
operator|.
name|sa
argument_list|,
name|socksize
argument_list|)
operator|<
literal|0
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
name|syserr
argument_list|(
literal|"makeconnection: cannot bind socket [%s]"
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|clt_addr
argument_list|)
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
block|}
comment|/* 		**  Linux seems to hang in connect for 90 minutes (!!!). 		**  Time out the connect to avoid this problem. 		*/
if|if
condition|(
name|setjmp
argument_list|(
name|CtxConnectTimeout
argument_list|)
operator|==
literal|0
condition|)
block|{
name|int
name|i
decl_stmt|;
if|if
condition|(
name|e
operator|->
name|e_ntries
operator|<=
literal|0
operator|&&
name|TimeOuts
operator|.
name|to_iconnect
operator|!=
literal|0
condition|)
name|ev
operator|=
name|sm_setevent
argument_list|(
name|TimeOuts
operator|.
name|to_iconnect
argument_list|,
name|connecttimeout
argument_list|,
literal|0
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
name|TimeOuts
operator|.
name|to_connect
operator|!=
literal|0
condition|)
name|ev
operator|=
name|sm_setevent
argument_list|(
name|TimeOuts
operator|.
name|to_connect
argument_list|,
name|connecttimeout
argument_list|,
literal|0
argument_list|)
expr_stmt|;
else|else
name|ev
operator|=
name|NULL
expr_stmt|;
switch|switch
condition|(
name|ConnectOnlyTo
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|addr
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
operator|=
name|ConnectOnlyTo
operator|.
name|sin
operator|.
name|sin_addr
operator|.
name|s_addr
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|memmove
argument_list|(
operator|&
name|addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|,
operator|&
name|ConnectOnlyTo
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|,
name|IN6ADDRSZ
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Connecting to [%s]...\n"
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|addr
argument_list|)
argument_list|)
expr_stmt|;
name|i
operator|=
name|connect
argument_list|(
name|s
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|addr
argument_list|,
name|addrlen
argument_list|)
expr_stmt|;
name|save_errno
operator|=
name|errno
expr_stmt|;
if|if
condition|(
name|ev
operator|!=
name|NULL
condition|)
name|sm_clrevent
argument_list|(
name|ev
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|>=
literal|0
condition|)
break|break;
block|}
else|else
name|save_errno
operator|=
name|errno
expr_stmt|;
comment|/* couldn't connect.... figure out why */
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
comment|/* if running demand-dialed connection, try again */
if|if
condition|(
name|DialDelay
operator|>
literal|0
operator|&&
name|firstconnect
operator|&&
name|bitnset
argument_list|(
name|M_DIALDELAY
argument_list|,
name|mci
operator|->
name|mci_mailer
operator|->
name|m_flags
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Connect failed (%s); trying again...\n"
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
name|firstconnect
operator|=
name|false
expr_stmt|;
operator|(
name|void
operator|)
name|sleep
argument_list|(
name|DialDelay
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|LogLevel
operator|>
literal|13
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"makeconnection (%s [%s]) failed: %s"
argument_list|,
name|host
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|addr
argument_list|)
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
name|nextaddr
label|:
endif|#
directive|endif
comment|/* NETINET6 */
if|if
condition|(
name|hp
operator|!=
name|NULL
operator|&&
name|hp
operator|->
name|h_addr_list
index|[
name|addrno
index|]
operator|!=
name|NULL
operator|&&
operator|(
name|enough
operator|==
literal|0
operator|||
name|curtime
argument_list|()
operator|<
name|enough
operator|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Connect failed (%s); trying new address....\n"
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|addr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|memmove
argument_list|(
operator|&
name|addr
operator|.
name|sin
operator|.
name|sin_addr
argument_list|,
name|hp
operator|->
name|h_addr_list
index|[
name|addrno
operator|++
index|]
argument_list|,
name|INADDRSZ
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|memmove
argument_list|(
operator|&
name|addr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|,
name|hp
operator|->
name|h_addr_list
index|[
name|addrno
operator|++
index|]
argument_list|,
name|IN6ADDRSZ
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
name|memmove
argument_list|(
name|addr
operator|.
name|sa
operator|.
name|sa_data
argument_list|,
name|hp
operator|->
name|h_addr_list
index|[
name|addrno
operator|++
index|]
argument_list|,
name|hp
operator|->
name|h_length
argument_list|)
expr_stmt|;
break|break;
block|}
continue|continue;
block|}
name|errno
operator|=
name|save_errno
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|family
operator|==
name|AF_INET6
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Connect failed (%s); retrying with AF_INET....\n"
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
name|v6found
operator|=
name|true
expr_stmt|;
name|family
operator|=
name|AF_INET
expr_stmt|;
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
block|{
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
block|}
goto|goto
name|v4retry
goto|;
block|}
name|v6tempfail
label|:
endif|#
directive|endif
comment|/* NETINET6 */
comment|/* couldn't open connection */
if|#
directive|if
name|NETINET6
comment|/* Don't clobber an already saved errno from v4retry */
if|if
condition|(
name|errno
operator|>
literal|0
condition|)
endif|#
directive|endif
comment|/* NETINET6 */
name|save_errno
operator|=
name|errno
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"Connect failed (%s)\n"
argument_list|,
name|sm_errstring
argument_list|(
name|save_errno
argument_list|)
argument_list|)
expr_stmt|;
if|#
directive|if
name|XLA
name|xla_host_end
argument_list|(
name|host
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* XLA */
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.1"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
block|{
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETINET6 */
comment|/* connection ok, put it into canonical form */
name|mci
operator|->
name|mci_out
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|(
name|mci
operator|->
name|mci_out
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|s
argument_list|,
name|SM_IO_WRONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|(
name|s
operator|=
name|dup
argument_list|(
name|s
argument_list|)
operator|)
operator|<
literal|0
operator|||
operator|(
name|mci
operator|->
name|mci_in
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|s
argument_list|,
name|SM_IO_RDONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"cannot open SMTP client channel, fd=%d"
argument_list|,
name|s
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|mci
operator|->
name|mci_out
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|sm_io_close
argument_list|(
name|mci
operator|->
name|mci_out
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
name|sm_io_automode
argument_list|(
name|mci
operator|->
name|mci_out
argument_list|,
name|mci
operator|->
name|mci_in
argument_list|)
expr_stmt|;
comment|/* set {client_flags} */
if|if
condition|(
name|ClientSettings
index|[
name|addr
operator|.
name|sa
operator|.
name|sa_family
index|]
operator|.
name|d_mflags
operator|!=
name|NULL
condition|)
block|{
name|macdefine
argument_list|(
operator|&
name|mci
operator|->
name|mci_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{client_flags}"
argument_list|)
argument_list|,
name|ClientSettings
index|[
name|addr
operator|.
name|sa
operator|.
name|sa_family
index|]
operator|.
name|d_mflags
argument_list|)
expr_stmt|;
block|}
else|else
name|macdefine
argument_list|(
operator|&
name|mci
operator|->
name|mci_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{client_flags}"
argument_list|)
argument_list|,
literal|""
argument_list|)
expr_stmt|;
comment|/* "add" {client_flags} to bitmap */
if|if
condition|(
name|bitnset
argument_list|(
name|D_IFNHELO
argument_list|,
name|ClientSettings
index|[
name|addr
operator|.
name|sa
operator|.
name|sa_family
index|]
operator|.
name|d_flags
argument_list|)
condition|)
block|{
comment|/* look for just this one flag */
name|setbitn
argument_list|(
name|D_IFNHELO
argument_list|,
name|d_flags
argument_list|)
expr_stmt|;
block|}
comment|/* find out name for Interface through which we connect */
name|len
operator|=
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|getsockname
argument_list|(
name|s
argument_list|,
operator|&
name|addr
operator|.
name|sa
argument_list|,
operator|&
name|len
argument_list|)
operator|==
literal|0
condition|)
block|{
name|char
modifier|*
name|name
decl_stmt|;
name|char
name|family
index|[
literal|5
index|]
decl_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_addr_out}"
argument_list|)
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|addr
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|family
argument_list|,
sizeof|sizeof
argument_list|(
name|family
argument_list|)
argument_list|,
literal|"%d"
argument_list|,
name|addr
operator|.
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_family_out}"
argument_list|)
argument_list|,
name|family
argument_list|)
expr_stmt|;
name|name
operator|=
name|hostnamebyanyaddr
argument_list|(
operator|&
name|addr
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_TEMP
argument_list|,
name|macid
argument_list|(
literal|"{if_name_out}"
argument_list|)
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|11
condition|)
block|{
comment|/* log connection information */
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|e
operator|->
name|e_id
argument_list|,
literal|"SMTP outgoing connect on %.40s"
argument_list|,
name|name
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|bitnset
argument_list|(
name|D_IFNHELO
argument_list|,
name|d_flags
argument_list|)
condition|)
block|{
if|if
condition|(
name|name
index|[
literal|0
index|]
operator|!=
literal|'['
operator|&&
name|strchr
argument_list|(
name|name
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
condition|)
name|mci
operator|->
name|mci_heloname
operator|=
name|newstr
argument_list|(
name|name
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_name_out}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_addr_out}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{if_family_out}"
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
comment|/* Use the configured HeloName as appropriate */
if|if
condition|(
name|HeloName
operator|!=
name|NULL
operator|&&
name|HeloName
index|[
literal|0
index|]
operator|!=
literal|'\0'
condition|)
name|mci
operator|->
name|mci_heloname
operator|=
name|newstr
argument_list|(
name|HeloName
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_OK
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
name|EX_OK
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|connecttimeout
parameter_list|(
name|ignore
parameter_list|)
name|int
name|ignore
decl_stmt|;
block|{
comment|/* 	**  NOTE: THIS CAN BE CALLED FROM A SIGNAL HANDLER.  DO NOT ADD 	**	ANYTHING TO THIS ROUTINE UNLESS YOU KNOW WHAT YOU ARE 	**	DOING. 	*/
name|errno
operator|=
name|ETIMEDOUT
expr_stmt|;
name|longjmp
argument_list|(
name|CtxConnectTimeout
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  MAKECONNECTION_DS -- make a connection to a domain socket. ** **	Parameters: **		mux_path -- the path of the socket to connect to. **		mci -- a pointer to the mail connection information **			structure to be filled in. ** **	Returns: **		An exit code telling whether the connection could be **			made and if not why not. ** **	Side Effects: **		none. */
end_comment

begin_if
if|#
directive|if
name|NETUNIX
end_if

begin_function
name|int
name|makeconnection_ds
parameter_list|(
name|mux_path
parameter_list|,
name|mci
parameter_list|)
name|char
modifier|*
name|mux_path
decl_stmt|;
specifier|register
name|MCI
modifier|*
name|mci
decl_stmt|;
block|{
name|int
name|sock
decl_stmt|;
name|int
name|rval
decl_stmt|,
name|save_errno
decl_stmt|;
name|long
name|sff
init|=
name|SFF_SAFEDIRPATH
operator||
name|SFF_OPENASROOT
operator||
name|SFF_NOLINK
operator||
name|SFF_ROOTOK
operator||
name|SFF_EXECOK
decl_stmt|;
name|struct
name|sockaddr_un
name|unix_addr
decl_stmt|;
comment|/* if not safe, don't connect */
name|rval
operator|=
name|safefile
argument_list|(
name|mux_path
argument_list|,
name|RunAsUid
argument_list|,
name|RunAsGid
argument_list|,
name|RunAsUserName
argument_list|,
name|sff
argument_list|,
name|S_IRUSR
operator||
name|S_IWUSR
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|rval
operator|!=
literal|0
condition|)
block|{
name|syserr
argument_list|(
literal|"makeconnection_ds: unsafe domain socket %s"
argument_list|,
name|mux_path
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.3.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|rval
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
comment|/* prepare address structure */
name|memset
argument_list|(
operator|&
name|unix_addr
argument_list|,
literal|'\0'
argument_list|,
sizeof|sizeof
argument_list|(
name|unix_addr
argument_list|)
argument_list|)
expr_stmt|;
name|unix_addr
operator|.
name|sun_family
operator|=
name|AF_UNIX
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|mux_path
argument_list|)
operator|>=
sizeof|sizeof
argument_list|(
name|unix_addr
operator|.
name|sun_path
argument_list|)
condition|)
block|{
name|syserr
argument_list|(
literal|"makeconnection_ds: domain socket name %s too long"
argument_list|,
name|mux_path
argument_list|)
expr_stmt|;
comment|/* XXX why TEMPFAIL but 5.x.y ? */
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"5.3.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|ENAMETOOLONG
expr_stmt|;
return|return
name|EX_UNAVAILABLE
return|;
block|}
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|unix_addr
operator|.
name|sun_path
argument_list|,
name|mux_path
argument_list|,
sizeof|sizeof
argument_list|(
name|unix_addr
operator|.
name|sun_path
argument_list|)
argument_list|)
expr_stmt|;
comment|/* initialize domain socket */
name|sock
operator|=
name|socket
argument_list|(
name|AF_UNIX
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|sock
operator|==
operator|-
literal|1
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"makeconnection_ds: could not create domain socket %s"
argument_list|,
name|mux_path
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
comment|/* connect to server */
if|if
condition|(
name|connect
argument_list|(
name|sock
argument_list|,
operator|(
expr|struct
name|sockaddr
operator|*
operator|)
operator|&
name|unix_addr
argument_list|,
sizeof|sizeof
argument_list|(
name|unix_addr
argument_list|)
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"Could not connect to socket %s"
argument_list|,
name|mux_path
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.1"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|sock
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
comment|/* connection ok, put it into canonical form */
name|mci
operator|->
name|mci_out
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|(
name|mci
operator|->
name|mci_out
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|sock
argument_list|,
name|SM_IO_WRONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
operator|||
operator|(
name|sock
operator|=
name|dup
argument_list|(
name|sock
argument_list|)
operator|)
operator|<
literal|0
operator|||
operator|(
name|mci
operator|->
name|mci_in
operator|=
name|sm_io_open
argument_list|(
name|SmFtStdiofd
argument_list|,
name|SM_TIME_DEFAULT
argument_list|,
operator|(
name|void
operator|*
operator|)
operator|&
name|sock
argument_list|,
name|SM_IO_RDONLY_B
argument_list|,
name|NULL
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
name|save_errno
operator|=
name|errno
expr_stmt|;
name|syserr
argument_list|(
literal|"cannot open SMTP client channel, fd=%d"
argument_list|,
name|sock
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_TEMPFAIL
argument_list|,
literal|"4.4.5"
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|mci
operator|->
name|mci_out
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|sm_io_close
argument_list|(
name|mci
operator|->
name|mci_out
argument_list|,
name|SM_TIME_DEFAULT
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|sock
argument_list|)
expr_stmt|;
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
name|EX_TEMPFAIL
return|;
block|}
name|sm_io_automode
argument_list|(
name|mci
operator|->
name|mci_out
argument_list|,
name|mci
operator|->
name|mci_in
argument_list|)
expr_stmt|;
name|mci_setstat
argument_list|(
name|mci
argument_list|,
name|EX_OK
argument_list|,
name|NULL
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
name|errno
operator|=
literal|0
expr_stmt|;
return|return
name|EX_OK
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NETUNIX */
end_comment

begin_comment
comment|/* **  SHUTDOWN_DAEMON -- Performs a clean shutdown of the daemon ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		closes control socket, exits. */
end_comment

begin_function
name|void
name|shutdown_daemon
parameter_list|()
block|{
name|int
name|i
decl_stmt|;
name|char
modifier|*
name|reason
decl_stmt|;
name|sm_allsignals
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|reason
operator|=
name|ShutdownRequest
expr_stmt|;
name|ShutdownRequest
operator|=
name|NULL
expr_stmt|;
name|PendingSignal
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|9
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|CurEnv
operator|->
name|e_id
argument_list|,
literal|"stopping daemon, reason=%s"
argument_list|,
name|reason
operator|==
name|NULL
condition|?
literal|"implicit call"
else|:
name|reason
argument_list|)
expr_stmt|;
name|FileName
operator|=
name|NULL
expr_stmt|;
name|closecontrolsocket
argument_list|(
name|true
argument_list|)
expr_stmt|;
if|#
directive|if
name|XLA
name|xla_all_end
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* XLA */
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NDaemons
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
operator|>=
literal|0
condition|)
block|{
operator|(
name|void
operator|)
name|close
argument_list|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
argument_list|)
expr_stmt|;
name|Daemons
index|[
name|i
index|]
operator|.
name|d_socket
operator|=
operator|-
literal|1
expr_stmt|;
if|#
directive|if
name|_FFR_DAEMON_NETUNIX
if|#
directive|if
name|NETUNIX
comment|/* Remove named sockets */
if|if
condition|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_addr
operator|.
name|sa
operator|.
name|sa_family
operator|==
name|AF_UNIX
condition|)
block|{
name|int
name|rval
decl_stmt|;
name|long
name|sff
init|=
name|SFF_SAFEDIRPATH
operator||
name|SFF_OPENASROOT
operator||
name|SFF_NOLINK
operator||
name|SFF_MUSTOWN
operator||
name|SFF_EXECOK
operator||
name|SFF_CREAT
decl_stmt|;
comment|/* if not safe, don't use it */
name|rval
operator|=
name|safefile
argument_list|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|,
name|RunAsUid
argument_list|,
name|RunAsGid
argument_list|,
name|RunAsUserName
argument_list|,
name|sff
argument_list|,
name|S_IRUSR
operator||
name|S_IWUSR
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|rval
operator|==
literal|0
operator|&&
name|unlink
argument_list|(
name|Daemons
index|[
name|i
index|]
operator|.
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|)
operator|<
literal|0
condition|)
block|{
name|sm_syslog
argument_list|(
name|LOG_WARNING
argument_list|,
name|NOQID
argument_list|,
literal|"Could not remove daemon %s socket: %s: %s"
argument_list|,
name|Daemons
index|[
name|i
index|]
operator|.
name|d_name
argument_list|,
name|Daemons
index|[
name|i
index|]
operator|.
name|d_addr
operator|.
name|sunix
operator|.
name|sun_path
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
block|}
block|}
endif|#
directive|endif
comment|/* NETUNIX */
endif|#
directive|endif
comment|/* _FFR_DAEMON_NETUNIX */
block|}
block|}
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OK
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* **  RESTART_DAEMON -- Performs a clean restart of the daemon ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		restarts the daemon or exits if restart fails. */
end_comment

begin_comment
comment|/* Make a non-DFL/IGN signal a noop */
end_comment

begin_define
define|#
directive|define
name|SM_NOOP_SIGNAL
parameter_list|(
name|sig
parameter_list|,
name|old
parameter_list|)
define|\
value|do								\ {								\ 	(old) = sm_signal((sig), sm_signal_noop);		\ 	if ((old) == SIG_IGN || (old) == SIG_DFL)		\ 		(void) sm_signal((sig), (old));			\ } while (0)
end_define

begin_function
name|void
name|restart_daemon
parameter_list|()
block|{
name|bool
name|drop
decl_stmt|;
name|int
name|save_errno
decl_stmt|;
name|char
modifier|*
name|reason
decl_stmt|;
name|sigfunc_t
name|ignore
decl_stmt|,
name|oalrm
decl_stmt|,
name|ousr1
decl_stmt|;
specifier|extern
name|int
name|DtableSize
decl_stmt|;
comment|/* clear the events to turn off SIGALRMs */
name|sm_clear_events
argument_list|()
expr_stmt|;
name|sm_allsignals
argument_list|(
name|true
argument_list|)
expr_stmt|;
name|reason
operator|=
name|RestartRequest
expr_stmt|;
name|RestartRequest
operator|=
name|NULL
expr_stmt|;
name|PendingSignal
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|SaveArgv
index|[
literal|0
index|]
index|[
literal|0
index|]
operator|!=
literal|'/'
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|3
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|NOQID
argument_list|,
literal|"could not restart: need full path"
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OSFILE
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
if|if
condition|(
name|LogLevel
operator|>
literal|3
condition|)
name|sm_syslog
argument_list|(
name|LOG_INFO
argument_list|,
name|NOQID
argument_list|,
literal|"restarting %s due to %s"
argument_list|,
name|SaveArgv
index|[
literal|0
index|]
argument_list|,
name|reason
operator|==
name|NULL
condition|?
literal|"implicit call"
else|:
name|reason
argument_list|)
expr_stmt|;
name|closecontrolsocket
argument_list|(
name|true
argument_list|)
expr_stmt|;
if|#
directive|if
name|SM_CONF_SHM
name|cleanup_shm
argument_list|(
name|DaemonPid
operator|==
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SM_CONF_SHM */
comment|/* close locked pid file */
name|close_sendmail_pid
argument_list|()
expr_stmt|;
comment|/* 	**  Want to drop to the user who started the process in all cases 	**  *but* when running as "smmsp" for the clientmqueue queue run 	**  daemon.  In that case, UseMSP will be true, RunAsUid should not 	**  be root, and RealUid should be either 0 or RunAsUid. 	*/
name|drop
operator|=
operator|!
operator|(
name|UseMSP
operator|&&
name|RunAsUid
operator|!=
literal|0
operator|&&
operator|(
name|RealUid
operator|==
literal|0
operator|||
name|RealUid
operator|==
name|RunAsUid
operator|)
operator|)
expr_stmt|;
if|if
condition|(
name|drop_privileges
argument_list|(
name|drop
argument_list|)
operator|!=
name|EX_OK
condition|)
block|{
if|if
condition|(
name|LogLevel
operator|>
literal|0
condition|)
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"could not drop privileges: %s"
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OSERR
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
name|sm_close_on_exec
argument_list|(
name|STDERR_FILENO
operator|+
literal|1
argument_list|,
name|DtableSize
argument_list|)
expr_stmt|;
comment|/* 	**  Need to allow signals before execve() to make them "harmless". 	**  However, the default action can be "terminate", so it isn't 	**  really harmless.  Setting signals to IGN will cause them to be 	**  ignored in the new process to, so that isn't a good alternative. 	*/
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGALRM
argument_list|,
name|oalrm
argument_list|)
expr_stmt|;
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGCHLD
argument_list|,
name|ignore
argument_list|)
expr_stmt|;
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGHUP
argument_list|,
name|ignore
argument_list|)
expr_stmt|;
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGINT
argument_list|,
name|ignore
argument_list|)
expr_stmt|;
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGPIPE
argument_list|,
name|ignore
argument_list|)
expr_stmt|;
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGTERM
argument_list|,
name|ignore
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SIGUSR1
name|SM_NOOP_SIGNAL
argument_list|(
name|SIGUSR1
argument_list|,
name|ousr1
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGUSR1 */
comment|/* Turn back on signals */
name|sm_allsignals
argument_list|(
name|false
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|execve
argument_list|(
name|SaveArgv
index|[
literal|0
index|]
argument_list|,
operator|(
name|ARGV_T
operator|)
name|SaveArgv
argument_list|,
operator|(
name|ARGV_T
operator|)
name|ExternalEnviron
argument_list|)
expr_stmt|;
name|save_errno
operator|=
name|errno
expr_stmt|;
comment|/* block signals again and restore needed signals */
name|sm_allsignals
argument_list|(
name|true
argument_list|)
expr_stmt|;
comment|/* For finis() events */
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGALRM
argument_list|,
name|oalrm
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|SIGUSR1
comment|/* For debugging finis() */
operator|(
name|void
operator|)
name|sm_signal
argument_list|(
name|SIGUSR1
argument_list|,
name|ousr1
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* SIGUSR1 */
name|errno
operator|=
name|save_errno
expr_stmt|;
if|if
condition|(
name|LogLevel
operator|>
literal|0
condition|)
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"could not exec %s: %s"
argument_list|,
name|SaveArgv
index|[
literal|0
index|]
argument_list|,
name|sm_errstring
argument_list|(
name|errno
argument_list|)
argument_list|)
expr_stmt|;
name|finis
argument_list|(
name|false
argument_list|,
name|true
argument_list|,
name|EX_OSFILE
argument_list|)
expr_stmt|;
comment|/* NOTREACHED */
block|}
end_function

begin_comment
comment|/* **  MYHOSTNAME -- return the name of this host. ** **	Parameters: **		hostbuf -- a place to return the name of this host. **		size -- the size of hostbuf. ** **	Returns: **		A list of aliases for this host. ** **	Side Effects: **		Adds numeric codes to $=w. */
end_comment

begin_function
name|struct
name|hostent
modifier|*
name|myhostname
parameter_list|(
name|hostbuf
parameter_list|,
name|size
parameter_list|)
name|char
name|hostbuf
index|[]
decl_stmt|;
name|int
name|size
decl_stmt|;
block|{
specifier|register
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
if|if
condition|(
name|gethostname
argument_list|(
name|hostbuf
argument_list|,
name|size
argument_list|)
operator|<
literal|0
operator|||
name|hostbuf
index|[
literal|0
index|]
operator|==
literal|'\0'
condition|)
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|hostbuf
argument_list|,
literal|"localhost"
argument_list|,
name|size
argument_list|)
expr_stmt|;
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|hostbuf
argument_list|,
name|InetMode
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET
operator|&&
name|NETINET6
if|if
condition|(
name|hp
operator|==
name|NULL
operator|&&
name|InetMode
operator|==
name|AF_INET6
condition|)
block|{
comment|/* 		**  It's possible that this IPv6 enabled machine doesn't 		**  actually have any IPv6 interfaces and, therefore, no 		**  IPv6 addresses.  Fall back to AF_INET. 		*/
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|hostbuf
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETINET&& NETINET6 */
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|strchr
argument_list|(
name|hp
operator|->
name|h_name
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
operator|||
name|strchr
argument_list|(
name|hostbuf
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
condition|)
operator|(
name|void
operator|)
name|cleanstrcpy
argument_list|(
name|hostbuf
argument_list|,
name|hp
operator|->
name|h_name
argument_list|,
name|size
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINFO
if|if
condition|(
name|strchr
argument_list|(
name|hostbuf
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
condition|)
block|{
name|char
modifier|*
name|domainname
decl_stmt|;
name|domainname
operator|=
name|ni_propval
argument_list|(
literal|"/locations"
argument_list|,
name|NULL
argument_list|,
literal|"resolver"
argument_list|,
literal|"domain"
argument_list|,
literal|'\0'
argument_list|)
expr_stmt|;
if|if
condition|(
name|domainname
operator|!=
name|NULL
operator|&&
name|strlen
argument_list|(
name|domainname
argument_list|)
operator|+
name|strlen
argument_list|(
name|hostbuf
argument_list|)
operator|+
literal|1
operator|<
name|size
condition|)
operator|(
name|void
operator|)
name|sm_strlcat2
argument_list|(
name|hostbuf
argument_list|,
literal|"."
argument_list|,
name|domainname
argument_list|,
name|size
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETINFO */
comment|/* 	**  If there is still no dot in the name, try looking for a 	**  dotted alias. 	*/
if|if
condition|(
name|strchr
argument_list|(
name|hostbuf
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
condition|)
block|{
name|char
modifier|*
modifier|*
name|ha
decl_stmt|;
for|for
control|(
name|ha
operator|=
name|hp
operator|->
name|h_aliases
init|;
name|ha
operator|!=
name|NULL
operator|&&
operator|*
name|ha
operator|!=
name|NULL
condition|;
name|ha
operator|++
control|)
block|{
if|if
condition|(
name|strchr
argument_list|(
operator|*
name|ha
argument_list|,
literal|'.'
argument_list|)
operator|!=
name|NULL
condition|)
block|{
operator|(
name|void
operator|)
name|cleanstrcpy
argument_list|(
name|hostbuf
argument_list|,
operator|*
name|ha
argument_list|,
name|size
operator|-
literal|1
argument_list|)
expr_stmt|;
name|hostbuf
index|[
name|size
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
break|break;
block|}
block|}
block|}
comment|/* 	**  If _still_ no dot, wait for a while and try again -- it is 	**  possible that some service is starting up.  This can result 	**  in excessive delays if the system is badly configured, but 	**  there really isn't a way around that, particularly given that 	**  the config file hasn't been read at this point. 	**  All in all, a bit of a mess. 	*/
if|if
condition|(
name|strchr
argument_list|(
name|hostbuf
argument_list|,
literal|'.'
argument_list|)
operator|==
name|NULL
operator|&&
operator|!
name|getcanonname
argument_list|(
name|hostbuf
argument_list|,
name|size
argument_list|,
name|true
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
name|sm_syslog
argument_list|(
name|LOG_CRIT
argument_list|,
name|NOQID
argument_list|,
literal|"My unqualified host name (%s) unknown; sleeping for retry"
argument_list|,
name|hostbuf
argument_list|)
expr_stmt|;
name|message
argument_list|(
literal|"My unqualified host name (%s) unknown; sleeping for retry"
argument_list|,
name|hostbuf
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sleep
argument_list|(
literal|60
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|getcanonname
argument_list|(
name|hostbuf
argument_list|,
name|size
argument_list|,
name|true
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
name|sm_syslog
argument_list|(
name|LOG_ALERT
argument_list|,
name|NOQID
argument_list|,
literal|"unable to qualify my own domain name (%s) -- using short name"
argument_list|,
name|hostbuf
argument_list|)
expr_stmt|;
name|message
argument_list|(
literal|"WARNING: unable to qualify my own domain name (%s) -- using short name"
argument_list|,
name|hostbuf
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|hp
return|;
block|}
end_function

begin_comment
comment|/* **  ADDRCMP -- compare two host addresses ** **	Parameters: **		hp -- hostent structure for the first address **		ha -- actual first address **		sa -- second address ** **	Returns: **		0 -- if ha and sa match **		else -- they don't match */
end_comment

begin_function
specifier|static
name|int
name|addrcmp
parameter_list|(
name|hp
parameter_list|,
name|ha
parameter_list|,
name|sa
parameter_list|)
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|char
modifier|*
name|ha
decl_stmt|;
name|SOCKADDR
modifier|*
name|sa
decl_stmt|;
block|{
if|#
directive|if
name|NETINET6
name|unsigned
name|char
modifier|*
name|a
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
switch|switch
condition|(
name|sa
operator|->
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|hp
operator|->
name|h_addrtype
operator|==
name|AF_INET
condition|)
return|return
name|memcmp
argument_list|(
name|ha
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|sa
operator|->
name|sin
operator|.
name|sin_addr
argument_list|,
name|INADDRSZ
argument_list|)
return|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|a
operator|=
operator|(
name|unsigned
name|char
operator|*
operator|)
operator|&
name|sa
operator|->
name|sin6
operator|.
name|sin6_addr
expr_stmt|;
comment|/* Straight binary comparison */
if|if
condition|(
name|hp
operator|->
name|h_addrtype
operator|==
name|AF_INET6
condition|)
return|return
name|memcmp
argument_list|(
name|ha
argument_list|,
name|a
argument_list|,
name|IN6ADDRSZ
argument_list|)
return|;
comment|/* If IPv4-mapped IPv6 address, compare the IPv4 section */
if|if
condition|(
name|hp
operator|->
name|h_addrtype
operator|==
name|AF_INET
operator|&&
name|IN6_IS_ADDR_V4MAPPED
argument_list|(
operator|&
name|sa
operator|->
name|sin6
operator|.
name|sin6_addr
argument_list|)
condition|)
return|return
name|memcmp
argument_list|(
name|a
operator|+
name|IN6ADDRSZ
operator|-
name|INADDRSZ
argument_list|,
name|ha
argument_list|,
name|INADDRSZ
argument_list|)
return|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_comment
comment|/* **  GETAUTHINFO -- get the real host name associated with a file descriptor ** **	Uses RFC1413 protocol to try to get info from the other end. ** **	Parameters: **		fd -- the descriptor **		may_be_forged -- an outage that is set to true if the **			forward lookup of RealHostName does not match **			RealHostAddr; set to false if they do match. ** **	Returns: **		The user@host information associated with this descriptor. */
end_comment

begin_decl_stmt
specifier|static
name|jmp_buf
name|CtxAuthTimeout
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|void
name|authtimeout
parameter_list|(
name|ignore
parameter_list|)
name|int
name|ignore
decl_stmt|;
block|{
comment|/* 	**  NOTE: THIS CAN BE CALLED FROM A SIGNAL HANDLER.  DO NOT ADD 	**	ANYTHING TO THIS ROUTINE UNLESS YOU KNOW WHAT YOU ARE 	**	DOING. 	*/
name|errno
operator|=
name|ETIMEDOUT
expr_stmt|;
name|longjmp
argument_list|(
name|CtxAuthTimeout
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|char
modifier|*
name|getauthinfo
parameter_list|(
name|fd
parameter_list|,
name|may_be_forged
parameter_list|)
name|int
name|fd
decl_stmt|;
name|bool
modifier|*
name|may_be_forged
decl_stmt|;
block|{
name|unsigned
name|short
name|SM_NONVOLATILE
name|port
init|=
literal|0
decl_stmt|;
name|SOCKADDR_LEN_T
name|falen
decl_stmt|;
specifier|register
name|char
modifier|*
specifier|volatile
name|p
init|=
name|NULL
decl_stmt|;
name|SOCKADDR
name|la
decl_stmt|;
name|SOCKADDR_LEN_T
name|lalen
decl_stmt|;
ifndef|#
directive|ifndef
name|NO_GETSERVBYNAME
specifier|register
name|struct
name|servent
modifier|*
name|sp
decl_stmt|;
if|#
directive|if
name|NETINET
specifier|static
name|unsigned
name|short
name|port4
init|=
literal|0
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
specifier|static
name|unsigned
name|short
name|port6
init|=
literal|0
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
endif|#
directive|endif
comment|/* ! NO_GETSERVBYNAME */
specifier|volatile
name|int
name|s
decl_stmt|;
name|int
name|i
init|=
literal|0
decl_stmt|;
name|size_t
name|len
decl_stmt|;
name|SM_EVENT
modifier|*
name|ev
decl_stmt|;
name|int
name|nleft
decl_stmt|;
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
name|char
modifier|*
name|ostype
init|=
name|NULL
decl_stmt|;
name|char
modifier|*
modifier|*
name|ha
decl_stmt|;
name|char
name|ibuf
index|[
name|MAXNAME
operator|+
literal|1
index|]
decl_stmt|;
specifier|static
name|char
name|hbuf
index|[
name|MAXNAME
operator|+
name|MAXAUTHINFO
operator|+
literal|11
index|]
decl_stmt|;
operator|*
name|may_be_forged
operator|=
name|false
expr_stmt|;
name|falen
operator|=
sizeof|sizeof
argument_list|(
name|RealHostAddr
argument_list|)
expr_stmt|;
if|if
condition|(
name|isatty
argument_list|(
name|fd
argument_list|)
operator|||
operator|(
name|i
operator|=
name|getpeername
argument_list|(
name|fd
argument_list|,
operator|&
name|RealHostAddr
operator|.
name|sa
argument_list|,
operator|&
name|falen
argument_list|)
operator|)
operator|<
literal|0
operator|||
name|falen
operator|<=
literal|0
operator|||
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|i
operator|<
literal|0
condition|)
block|{
comment|/* 			**  ENOTSOCK is OK: bail on anything else, but reset 			**  errno in this case, so a mis-report doesn't 			**  happen later. 			*/
if|if
condition|(
name|errno
operator|!=
name|ENOTSOCK
condition|)
return|return
name|NULL
return|;
name|errno
operator|=
literal|0
expr_stmt|;
block|}
operator|(
name|void
operator|)
name|sm_strlcpyn
argument_list|(
name|hbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|,
literal|2
argument_list|,
name|RealUserName
argument_list|,
literal|"@localhost"
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getauthinfo: %s\n"
argument_list|,
name|hbuf
argument_list|)
expr_stmt|;
return|return
name|hbuf
return|;
block|}
if|if
condition|(
name|RealHostName
operator|==
name|NULL
condition|)
block|{
comment|/* translate that to a host name */
name|RealHostName
operator|=
name|newstr
argument_list|(
name|hostnamebyanyaddr
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|strlen
argument_list|(
name|RealHostName
argument_list|)
operator|>
name|MAXNAME
condition|)
name|RealHostName
index|[
name|MAXNAME
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/* XXX - 1 ? */
block|}
comment|/* cross check RealHostName with forward DNS lookup */
if|if
condition|(
name|anynet_ntoa
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
index|[
literal|0
index|]
operator|!=
literal|'['
operator|&&
name|RealHostName
index|[
literal|0
index|]
operator|!=
literal|'['
condition|)
block|{
name|int
name|family
decl_stmt|;
name|family
operator|=
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
expr_stmt|;
if|#
directive|if
name|NETINET6
operator|&&
name|NEEDSGETIPNODE
comment|/* 		**  If RealHostAddr is an IPv6 connection with an 		**  IPv4-mapped address, we need RealHostName's IPv4 		**  address(es) for addrcmp() to compare against 		**  RealHostAddr. 		** 		**  Actually, we only need to do this for systems 		**  which NEEDSGETIPNODE since the real getipnodebyname() 		**  already does V4MAPPED address via the AI_V4MAPPEDCFG 		**  flag.  A better fix to this problem is to add this 		**  functionality to our stub getipnodebyname(). 		*/
if|if
condition|(
name|family
operator|==
name|AF_INET6
operator|&&
name|IN6_IS_ADDR_V4MAPPED
argument_list|(
operator|&
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_addr
argument_list|)
condition|)
name|family
operator|=
name|AF_INET
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6&& NEEDSGETIPNODE */
comment|/* try to match the reverse against the forward lookup */
name|hp
operator|=
name|sm_gethostbyname
argument_list|(
name|RealHostName
argument_list|,
name|family
argument_list|)
expr_stmt|;
if|if
condition|(
name|hp
operator|==
name|NULL
condition|)
block|{
comment|/* XXX: Could be a temporary error on forward lookup */
operator|*
name|may_be_forged
operator|=
name|true
expr_stmt|;
block|}
else|else
block|{
for|for
control|(
name|ha
operator|=
name|hp
operator|->
name|h_addr_list
init|;
operator|*
name|ha
operator|!=
name|NULL
condition|;
name|ha
operator|++
control|)
block|{
if|if
condition|(
name|addrcmp
argument_list|(
name|hp
argument_list|,
operator|*
name|ha
argument_list|,
operator|&
name|RealHostAddr
argument_list|)
operator|==
literal|0
condition|)
break|break;
block|}
operator|*
name|may_be_forged
operator|=
operator|*
name|ha
operator|==
name|NULL
expr_stmt|;
if|#
directive|if
name|NETINET6
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
block|}
if|if
condition|(
name|TimeOuts
operator|.
name|to_ident
operator|==
literal|0
condition|)
goto|goto
name|noident
goto|;
name|lalen
operator|=
sizeof|sizeof
argument_list|(
name|la
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|getsockname
argument_list|(
name|fd
argument_list|,
operator|&
name|la
operator|.
name|sa
argument_list|,
operator|&
name|lalen
argument_list|)
operator|<
literal|0
operator|||
name|lalen
operator|<=
literal|0
operator|||
name|la
operator|.
name|sa
operator|.
name|sa_family
operator|!=
name|AF_INET
condition|)
block|{
comment|/* no ident info */
goto|goto
name|noident
goto|;
block|}
name|port
operator|=
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
expr_stmt|;
comment|/* create ident query */
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|ibuf
argument_list|,
sizeof|sizeof
argument_list|(
name|ibuf
argument_list|)
argument_list|,
literal|"%d,%d\r\n"
argument_list|,
name|ntohs
argument_list|(
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
argument_list|)
argument_list|,
name|ntohs
argument_list|(
name|la
operator|.
name|sin
operator|.
name|sin_port
argument_list|)
argument_list|)
expr_stmt|;
comment|/* create local address */
name|la
operator|.
name|sin
operator|.
name|sin_port
operator|=
literal|0
expr_stmt|;
comment|/* create foreign address */
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|htons
argument_list|(
literal|113
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
comment|/* 		**  getservbyname() consumes about 5% of the time 		**  when receiving a small message (almost all of the time 		**  spent in this routine). 		**  Hence we store the port in a static variable 		**  to save this time. 		**  The portnumber shouldn't change very often... 		**  This code makes the assumption that the port number 		**  is not 0. 		*/
if|if
condition|(
name|port4
operator|==
literal|0
condition|)
block|{
name|sp
operator|=
name|getservbyname
argument_list|(
literal|"auth"
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|!=
name|NULL
condition|)
name|port4
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
else|else
name|port4
operator|=
name|htons
argument_list|(
literal|113
argument_list|)
expr_stmt|;
block|}
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|port4
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|getsockname
argument_list|(
name|fd
argument_list|,
operator|&
name|la
operator|.
name|sa
argument_list|,
operator|&
name|lalen
argument_list|)
operator|<
literal|0
operator|||
name|lalen
operator|<=
literal|0
operator|||
name|la
operator|.
name|sa
operator|.
name|sa_family
operator|!=
name|AF_INET6
condition|)
block|{
comment|/* no ident info */
goto|goto
name|noident
goto|;
block|}
name|port
operator|=
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
expr_stmt|;
comment|/* create ident query */
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|ibuf
argument_list|,
sizeof|sizeof
argument_list|(
name|ibuf
argument_list|)
argument_list|,
literal|"%d,%d\r\n"
argument_list|,
name|ntohs
argument_list|(
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
argument_list|)
argument_list|,
name|ntohs
argument_list|(
name|la
operator|.
name|sin6
operator|.
name|sin6_port
argument_list|)
argument_list|)
expr_stmt|;
comment|/* create local address */
name|la
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
literal|0
expr_stmt|;
comment|/* create foreign address */
ifdef|#
directive|ifdef
name|NO_GETSERVBYNAME
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|htons
argument_list|(
literal|113
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* NO_GETSERVBYNAME */
if|if
condition|(
name|port6
operator|==
literal|0
condition|)
block|{
name|sp
operator|=
name|getservbyname
argument_list|(
literal|"auth"
argument_list|,
literal|"tcp"
argument_list|)
expr_stmt|;
if|if
condition|(
name|sp
operator|!=
name|NULL
condition|)
name|port6
operator|=
name|sp
operator|->
name|s_port
expr_stmt|;
else|else
name|port6
operator|=
name|htons
argument_list|(
literal|113
argument_list|)
expr_stmt|;
block|}
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|port6
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NO_GETSERVBYNAME */
endif|#
directive|endif
comment|/* NETINET6 */
default|default:
comment|/* no ident info */
goto|goto
name|noident
goto|;
block|}
name|s
operator|=
operator|-
literal|1
expr_stmt|;
if|if
condition|(
name|setjmp
argument_list|(
name|CtxAuthTimeout
argument_list|)
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|s
operator|>=
literal|0
condition|)
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
goto|goto
name|noident
goto|;
block|}
comment|/* put a timeout around the whole thing */
name|ev
operator|=
name|sm_setevent
argument_list|(
name|TimeOuts
operator|.
name|to_ident
argument_list|,
name|authtimeout
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* connect to foreign IDENT server using same address as SMTP socket */
name|s
operator|=
name|socket
argument_list|(
name|la
operator|.
name|sa
operator|.
name|sa_family
argument_list|,
name|SOCK_STREAM
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|s
operator|<
literal|0
condition|)
block|{
name|sm_clrevent
argument_list|(
name|ev
argument_list|)
expr_stmt|;
goto|goto
name|noident
goto|;
block|}
if|if
condition|(
name|bind
argument_list|(
name|s
argument_list|,
operator|&
name|la
operator|.
name|sa
argument_list|,
name|lalen
argument_list|)
operator|<
literal|0
operator|||
name|connect
argument_list|(
name|s
argument_list|,
operator|&
name|RealHostAddr
operator|.
name|sa
argument_list|,
name|lalen
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|closeident
goto|;
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|10
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getauthinfo: sent %s"
argument_list|,
name|ibuf
argument_list|)
expr_stmt|;
comment|/* send query */
if|if
condition|(
name|write
argument_list|(
name|s
argument_list|,
name|ibuf
argument_list|,
name|strlen
argument_list|(
name|ibuf
argument_list|)
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|closeident
goto|;
comment|/* get result */
name|p
operator|=
operator|&
name|ibuf
index|[
literal|0
index|]
expr_stmt|;
name|nleft
operator|=
sizeof|sizeof
argument_list|(
name|ibuf
argument_list|)
operator|-
literal|1
expr_stmt|;
while|while
condition|(
operator|(
name|i
operator|=
name|read
argument_list|(
name|s
argument_list|,
name|p
argument_list|,
name|nleft
argument_list|)
operator|)
operator|>
literal|0
condition|)
block|{
name|char
modifier|*
name|s
decl_stmt|;
name|p
operator|+=
name|i
expr_stmt|;
name|nleft
operator|-=
name|i
expr_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
operator|(
name|s
operator|=
name|strchr
argument_list|(
name|ibuf
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|p
operator|>
name|s
operator|+
literal|1
condition|)
block|{
name|p
operator|=
name|s
operator|+
literal|1
expr_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
block|}
break|break;
block|}
if|if
condition|(
name|nleft
operator|<=
literal|0
condition|)
break|break;
block|}
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|sm_clrevent
argument_list|(
name|ev
argument_list|)
expr_stmt|;
if|if
condition|(
name|i
operator|<
literal|0
operator|||
name|p
operator|==
operator|&
name|ibuf
index|[
literal|0
index|]
condition|)
goto|goto
name|noident
goto|;
if|if
condition|(
name|p
operator|>=
operator|&
name|ibuf
index|[
literal|2
index|]
operator|&&
operator|*
operator|--
name|p
operator|==
literal|'\n'
operator|&&
operator|*
operator|--
name|p
operator|==
literal|'\r'
condition|)
name|p
operator|--
expr_stmt|;
operator|*
operator|++
name|p
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|3
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getauthinfo:  got %s\n"
argument_list|,
name|ibuf
argument_list|)
expr_stmt|;
comment|/* parse result */
name|p
operator|=
name|strchr
argument_list|(
name|ibuf
argument_list|,
literal|':'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
block|{
comment|/* malformed response */
goto|goto
name|noident
goto|;
block|}
while|while
condition|(
name|isascii
argument_list|(
operator|*
operator|++
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
continue|continue;
if|if
condition|(
name|sm_strncasecmp
argument_list|(
name|p
argument_list|,
literal|"userid"
argument_list|,
literal|6
argument_list|)
operator|!=
literal|0
condition|)
block|{
comment|/* presumably an error string */
goto|goto
name|noident
goto|;
block|}
name|p
operator|+=
literal|6
expr_stmt|;
while|while
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|++
operator|!=
literal|':'
condition|)
block|{
comment|/* either useridxx or malformed response */
goto|goto
name|noident
goto|;
block|}
comment|/* p now points to the OSTYPE field */
while|while
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
name|ostype
operator|=
name|p
expr_stmt|;
name|p
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|':'
argument_list|)
expr_stmt|;
if|if
condition|(
name|p
operator|==
name|NULL
condition|)
block|{
comment|/* malformed response */
goto|goto
name|noident
goto|;
block|}
else|else
block|{
name|char
modifier|*
name|charset
decl_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
name|charset
operator|=
name|strchr
argument_list|(
name|ostype
argument_list|,
literal|','
argument_list|)
expr_stmt|;
if|if
condition|(
name|charset
operator|!=
name|NULL
condition|)
operator|*
name|charset
operator|=
literal|'\0'
expr_stmt|;
block|}
comment|/* 1413 says don't do this -- but it's broken otherwise */
while|while
condition|(
name|isascii
argument_list|(
operator|*
operator|++
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
continue|continue;
comment|/* p now points to the authenticated name -- copy carefully */
if|if
condition|(
name|sm_strncasecmp
argument_list|(
name|ostype
argument_list|,
literal|"other"
argument_list|,
literal|5
argument_list|)
operator|==
literal|0
operator|&&
operator|(
name|ostype
index|[
literal|5
index|]
operator|==
literal|' '
operator|||
name|ostype
index|[
literal|5
index|]
operator|==
literal|'\0'
operator|)
condition|)
block|{
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|hbuf
argument_list|,
literal|"IDENT:"
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|)
expr_stmt|;
name|cleanstrcpy
argument_list|(
operator|&
name|hbuf
index|[
literal|6
index|]
argument_list|,
name|p
argument_list|,
name|MAXAUTHINFO
argument_list|)
expr_stmt|;
block|}
else|else
name|cleanstrcpy
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|,
name|MAXAUTHINFO
argument_list|)
expr_stmt|;
name|len
operator|=
name|strlen
argument_list|(
name|hbuf
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_strlcpyn
argument_list|(
operator|&
name|hbuf
index|[
name|len
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
operator|-
name|len
argument_list|,
literal|2
argument_list|,
literal|"@"
argument_list|,
name|RealHostName
operator|==
name|NULL
condition|?
literal|"localhost"
else|:
name|RealHostName
argument_list|)
expr_stmt|;
goto|goto
name|postident
goto|;
name|closeident
label|:
operator|(
name|void
operator|)
name|close
argument_list|(
name|s
argument_list|)
expr_stmt|;
name|sm_clrevent
argument_list|(
name|ev
argument_list|)
expr_stmt|;
name|noident
label|:
comment|/* put back the original incoming port */
switch|switch
condition|(
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|port
operator|>
literal|0
condition|)
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|port
operator|>
literal|0
condition|)
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
if|if
condition|(
name|RealHostName
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getauthinfo: NULL\n"
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|hbuf
argument_list|,
name|RealHostName
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|)
expr_stmt|;
name|postident
label|:
if|#
directive|if
name|IP_SRCROUTE
ifndef|#
directive|ifndef
name|GET_IPOPT_DST
define|#
directive|define
name|GET_IPOPT_DST
parameter_list|(
name|dst
parameter_list|)
value|(dst)
endif|#
directive|endif
comment|/* ! GET_IPOPT_DST */
comment|/* 	**  Extract IP source routing information. 	** 	**	Format of output for a connection from site a through b 	**	through c to d: 	**		loose:      @site-c@site-b:site-a 	**		strict:	   !@site-c@site-b:site-a 	** 	**	o - pointer within ipopt_list structure. 	**	q - pointer within ls/ss rr route data 	**	p - pointer to hbuf 	*/
if|if
condition|(
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
operator|==
name|AF_INET
condition|)
block|{
name|SOCKOPT_LEN_T
name|ipoptlen
decl_stmt|;
name|int
name|j
decl_stmt|;
name|unsigned
name|char
modifier|*
name|q
decl_stmt|;
name|unsigned
name|char
modifier|*
name|o
decl_stmt|;
name|int
name|l
decl_stmt|;
name|struct
name|IPOPTION
name|ipopt
decl_stmt|;
name|ipoptlen
operator|=
sizeof|sizeof
argument_list|(
name|ipopt
argument_list|)
expr_stmt|;
if|if
condition|(
name|getsockopt
argument_list|(
name|fd
argument_list|,
name|IPPROTO_IP
argument_list|,
name|IP_OPTIONS
argument_list|,
operator|(
name|char
operator|*
operator|)
operator|&
name|ipopt
argument_list|,
operator|&
name|ipoptlen
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|noipsr
goto|;
if|if
condition|(
name|ipoptlen
operator|==
literal|0
condition|)
goto|goto
name|noipsr
goto|;
name|o
operator|=
operator|(
name|unsigned
name|char
operator|*
operator|)
name|ipopt
operator|.
name|IP_LIST
expr_stmt|;
while|while
condition|(
name|o
operator|!=
name|NULL
operator|&&
name|o
operator|<
operator|(
name|unsigned
name|char
operator|*
operator|)
operator|&
name|ipopt
operator|+
name|ipoptlen
condition|)
block|{
switch|switch
condition|(
operator|*
name|o
condition|)
block|{
case|case
name|IPOPT_EOL
case|:
name|o
operator|=
name|NULL
expr_stmt|;
break|break;
case|case
name|IPOPT_NOP
case|:
name|o
operator|++
expr_stmt|;
break|break;
case|case
name|IPOPT_SSRR
case|:
case|case
name|IPOPT_LSRR
case|:
comment|/* 				**  Source routing. 				**	o[0] is the option type (loose/strict). 				**	o[1] is the length of this option, 				**		including option type and 				**		length. 				**	o[2] is the pointer into the route 				**		data. 				**	o[3] begins the route data. 				*/
name|p
operator|=
operator|&
name|hbuf
index|[
name|strlen
argument_list|(
name|hbuf
argument_list|)
index|]
expr_stmt|;
name|l
operator|=
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
operator|-
operator|(
name|hbuf
operator|-
name|p
operator|)
operator|-
literal|6
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|p
argument_list|,
name|SPACELEFT
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|)
argument_list|,
literal|" [%s@%.*s"
argument_list|,
operator|*
name|o
operator|==
name|IPOPT_SSRR
condition|?
literal|"!"
else|:
literal|""
argument_list|,
name|l
operator|>
literal|240
condition|?
literal|120
else|:
name|l
operator|/
literal|2
argument_list|,
name|inet_ntoa
argument_list|(
name|GET_IPOPT_DST
argument_list|(
name|ipopt
operator|.
name|IP_DST
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
name|i
operator|=
name|strlen
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|p
operator|+=
name|i
expr_stmt|;
name|l
operator|-=
name|strlen
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|j
operator|=
name|o
index|[
literal|1
index|]
operator|/
sizeof|sizeof
argument_list|(
expr|struct
name|in_addr
argument_list|)
operator|-
literal|1
expr_stmt|;
comment|/* q skips length and router pointer to data */
name|q
operator|=
operator|&
name|o
index|[
literal|3
index|]
expr_stmt|;
for|for
control|(
init|;
name|j
operator|>=
literal|0
condition|;
name|j
operator|--
control|)
block|{
name|struct
name|in_addr
name|addr
decl_stmt|;
name|memcpy
argument_list|(
operator|&
name|addr
argument_list|,
name|q
argument_list|,
sizeof|sizeof
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|p
argument_list|,
name|SPACELEFT
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|)
argument_list|,
literal|"%c%.*s"
argument_list|,
name|j
operator|!=
literal|0
condition|?
literal|'@'
else|:
literal|':'
argument_list|,
name|l
operator|>
literal|240
condition|?
literal|120
else|:
name|j
operator|==
literal|0
condition|?
name|l
else|:
name|l
operator|/
literal|2
argument_list|,
name|inet_ntoa
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
name|i
operator|=
name|strlen
argument_list|(
name|p
argument_list|)
expr_stmt|;
name|p
operator|+=
name|i
expr_stmt|;
name|l
operator|-=
name|i
operator|+
literal|1
expr_stmt|;
name|q
operator|+=
sizeof|sizeof
argument_list|(
expr|struct
name|in_addr
argument_list|)
expr_stmt|;
block|}
name|o
operator|+=
name|o
index|[
literal|1
index|]
expr_stmt|;
break|break;
default|default:
comment|/* Skip over option */
name|o
operator|+=
name|o
index|[
literal|1
index|]
expr_stmt|;
break|break;
block|}
block|}
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|p
argument_list|,
name|SPACELEFT
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|)
argument_list|,
literal|"]"
argument_list|)
expr_stmt|;
goto|goto
name|postipsr
goto|;
block|}
name|noipsr
label|:
endif|#
directive|endif
comment|/* IP_SRCROUTE */
if|if
condition|(
name|RealHostName
operator|!=
name|NULL
operator|&&
name|RealHostName
index|[
literal|0
index|]
operator|!=
literal|'['
condition|)
block|{
name|p
operator|=
operator|&
name|hbuf
index|[
name|strlen
argument_list|(
name|hbuf
argument_list|)
index|]
expr_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|p
argument_list|,
name|SPACELEFT
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|)
argument_list|,
literal|" [%.100s]"
argument_list|,
name|anynet_ntoa
argument_list|(
operator|&
name|RealHostAddr
argument_list|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|may_be_forged
condition|)
block|{
name|p
operator|=
operator|&
name|hbuf
index|[
name|strlen
argument_list|(
name|hbuf
argument_list|)
index|]
expr_stmt|;
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|p
argument_list|,
literal|" (may be forged)"
argument_list|,
name|SPACELEFT
argument_list|(
name|hbuf
argument_list|,
name|p
argument_list|)
argument_list|)
expr_stmt|;
name|macdefine
argument_list|(
operator|&
name|BlankEnvelope
operator|.
name|e_macro
argument_list|,
name|A_PERM
argument_list|,
name|macid
argument_list|(
literal|"{client_resolve}"
argument_list|)
argument_list|,
literal|"FORGED"
argument_list|)
expr_stmt|;
block|}
if|#
directive|if
name|IP_SRCROUTE
name|postipsr
label|:
endif|#
directive|endif
comment|/* IP_SRCROUTE */
comment|/* put back the original incoming port */
switch|switch
condition|(
name|RealHostAddr
operator|.
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
if|if
condition|(
name|port
operator|>
literal|0
condition|)
name|RealHostAddr
operator|.
name|sin
operator|.
name|sin_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
if|if
condition|(
name|port
operator|>
literal|0
condition|)
name|RealHostAddr
operator|.
name|sin6
operator|.
name|sin6_port
operator|=
name|port
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"getauthinfo: %s\n"
argument_list|,
name|hbuf
argument_list|)
expr_stmt|;
return|return
name|hbuf
return|;
block|}
end_function

begin_comment
comment|/* **  HOST_MAP_LOOKUP -- turn a hostname into canonical form ** **	Parameters: **		map -- a pointer to this map. **		name -- the (presumably unqualified) hostname. **		av -- unused -- for compatibility with other mapping **			functions. **		statp -- an exit status (out parameter) -- set to **			EX_TEMPFAIL if the name server is unavailable. ** **	Returns: **		The mapping, if found. **		NULL if no mapping found. ** **	Side Effects: **		Looks up the host specified in hbuf.  If it is not **		the canonical name for that host, return the canonical **		name (unless MF_MATCHONLY is set, which will cause the **		status only to be returned). */
end_comment

begin_function
name|char
modifier|*
name|host_map_lookup
parameter_list|(
name|map
parameter_list|,
name|name
parameter_list|,
name|av
parameter_list|,
name|statp
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|name
decl_stmt|;
name|char
modifier|*
modifier|*
name|av
decl_stmt|;
name|int
modifier|*
name|statp
decl_stmt|;
block|{
specifier|register
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
if|#
directive|if
name|NETINET
name|struct
name|in_addr
name|in_addr
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
name|struct
name|in6_addr
name|in6_addr
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
name|char
modifier|*
name|cp
decl_stmt|,
modifier|*
name|ans
init|=
name|NULL
decl_stmt|;
specifier|register
name|STAB
modifier|*
name|s
decl_stmt|;
name|time_t
name|now
decl_stmt|;
if|#
directive|if
name|NAMED_BIND
name|time_t
name|SM_NONVOLATILE
name|retrans
init|=
literal|0
decl_stmt|;
name|int
name|SM_NONVOLATILE
name|retry
init|=
literal|0
decl_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
name|char
name|hbuf
index|[
name|MAXNAME
operator|+
literal|1
index|]
decl_stmt|;
comment|/* 	**  See if we have already looked up this name.  If so, just 	**  return it (unless expired). 	*/
name|now
operator|=
name|curtime
argument_list|()
expr_stmt|;
name|s
operator|=
name|stab
argument_list|(
name|name
argument_list|,
name|ST_NAMECANON
argument_list|,
name|ST_ENTER
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|NCF_VALID
argument_list|,
name|s
operator|->
name|s_namecanon
operator|.
name|nc_flags
argument_list|)
operator|&&
name|s
operator|->
name|s_namecanon
operator|.
name|nc_exp
operator|>=
name|now
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"host_map_lookup(%s) => CACHE %s\n"
argument_list|,
name|name
argument_list|,
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
operator|==
name|NULL
condition|?
literal|"NULL"
else|:
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
argument_list|)
expr_stmt|;
name|errno
operator|=
name|s
operator|->
name|s_namecanon
operator|.
name|nc_errno
expr_stmt|;
name|SM_SET_H_ERRNO
argument_list|(
name|s
operator|->
name|s_namecanon
operator|.
name|nc_herrno
argument_list|)
expr_stmt|;
operator|*
name|statp
operator|=
name|s
operator|->
name|s_namecanon
operator|.
name|nc_stat
expr_stmt|;
if|if
condition|(
operator|*
name|statp
operator|==
name|EX_TEMPFAIL
condition|)
block|{
name|CurEnv
operator|->
name|e_status
operator|=
literal|"4.4.3"
expr_stmt|;
name|message
argument_list|(
literal|"851 %s: Name server timeout"
argument_list|,
name|shortenstring
argument_list|(
name|name
argument_list|,
literal|33
argument_list|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
operator|*
name|statp
operator|!=
name|EX_OK
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
operator|==
name|NULL
condition|)
block|{
name|syserr
argument_list|(
literal|"host_map_lookup(%s): bogus NULL cache entry, errno=%d, h_errno=%d"
argument_list|,
name|name
argument_list|,
name|s
operator|->
name|s_namecanon
operator|.
name|nc_errno
argument_list|,
name|s
operator|->
name|s_namecanon
operator|.
name|nc_herrno
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|bitset
argument_list|(
name|MF_MATCHONLY
argument_list|,
name|map
operator|->
name|map_mflags
argument_list|)
condition|)
name|cp
operator|=
name|map_rewrite
argument_list|(
name|map
argument_list|,
name|name
argument_list|,
name|strlen
argument_list|(
name|name
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|else
name|cp
operator|=
name|map_rewrite
argument_list|(
name|map
argument_list|,
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
argument_list|,
name|strlen
argument_list|(
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
argument_list|)
argument_list|,
name|av
argument_list|)
expr_stmt|;
return|return
name|cp
return|;
block|}
comment|/* 	**  If we are running without a regular network connection (usually 	**  dial-on-demand) and we are just queueing, we want to avoid DNS 	**  lookups because those could try to connect to a server. 	*/
if|if
condition|(
name|CurEnv
operator|->
name|e_sendmode
operator|==
name|SM_DEFER
operator|&&
name|bitset
argument_list|(
name|MF_DEFER
argument_list|,
name|map
operator|->
name|map_mflags
argument_list|)
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"host_map_lookup(%s) => DEFERRED\n"
argument_list|,
name|name
argument_list|)
expr_stmt|;
operator|*
name|statp
operator|=
name|EX_TEMPFAIL
expr_stmt|;
return|return
name|NULL
return|;
block|}
comment|/* 	**  If first character is a bracket, then it is an address 	**  lookup.  Address is copied into a temporary buffer to 	**  strip the brackets and to preserve name if address is 	**  unknown. 	*/
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"host_map_lookup(%s) => "
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|#
directive|if
name|NAMED_BIND
if|if
condition|(
name|map
operator|->
name|map_timeout
operator|>
literal|0
condition|)
block|{
name|retrans
operator|=
name|_res
operator|.
name|retrans
expr_stmt|;
name|_res
operator|.
name|retrans
operator|=
name|map
operator|->
name|map_timeout
expr_stmt|;
block|}
if|if
condition|(
name|map
operator|->
name|map_retry
operator|>
literal|0
condition|)
block|{
name|retry
operator|=
name|_res
operator|.
name|retry
expr_stmt|;
name|_res
operator|.
name|retry
operator|=
name|map
operator|->
name|map_retry
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NAMED_BIND */
comment|/* set default TTL */
name|s
operator|->
name|s_namecanon
operator|.
name|nc_exp
operator|=
name|now
operator|+
name|SM_DEFAULT_TTL
expr_stmt|;
if|if
condition|(
operator|*
name|name
operator|!=
literal|'['
condition|)
block|{
name|int
name|ttl
decl_stmt|;
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|hbuf
argument_list|,
name|name
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|getcanonname
argument_list|(
name|hbuf
argument_list|,
sizeof|sizeof
argument_list|(
name|hbuf
argument_list|)
operator|-
literal|1
argument_list|,
operator|!
name|HasWildcardMX
argument_list|,
operator|&
name|ttl
argument_list|)
condition|)
block|{
name|ans
operator|=
name|hbuf
expr_stmt|;
if|if
condition|(
name|ttl
operator|>
literal|0
condition|)
name|s
operator|->
name|s_namecanon
operator|.
name|nc_exp
operator|=
name|now
operator|+
name|SM_MIN
argument_list|(
name|ttl
argument_list|,
name|SM_DEFAULT_TTL
argument_list|)
expr_stmt|;
block|}
block|}
else|else
block|{
if|if
condition|(
operator|(
name|cp
operator|=
name|strchr
argument_list|(
name|name
argument_list|,
literal|']'
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"FAILED\n"
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
operator|*
name|cp
operator|=
literal|'\0'
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
if|#
directive|if
name|NETINET
if|if
condition|(
operator|(
name|in_addr
operator|.
name|s_addr
operator|=
name|inet_addr
argument_list|(
operator|&
name|name
index|[
literal|1
index|]
argument_list|)
operator|)
operator|!=
name|INADDR_NONE
condition|)
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|in_addr
argument_list|,
name|INADDRSZ
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|==
name|NULL
operator|&&
name|anynet_pton
argument_list|(
name|AF_INET6
argument_list|,
operator|&
name|name
index|[
literal|1
index|]
argument_list|,
operator|&
name|in6_addr
argument_list|)
operator|==
literal|1
condition|)
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|in6_addr
argument_list|,
name|IN6ADDRSZ
argument_list|,
name|AF_INET6
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
operator|*
name|cp
operator|=
literal|']'
expr_stmt|;
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
block|{
comment|/* found a match -- copy out */
name|ans
operator|=
name|denlstring
argument_list|(
operator|(
name|char
operator|*
operator|)
name|hp
operator|->
name|h_name
argument_list|,
name|true
argument_list|,
name|true
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|ans
operator|==
name|hp
operator|->
name|h_name
condition|)
block|{
specifier|static
name|char
name|n
index|[
name|MAXNAME
operator|+
literal|1
index|]
decl_stmt|;
comment|/* hp->h_name is about to disappear */
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|n
argument_list|,
name|ans
argument_list|,
sizeof|sizeof
argument_list|(
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|ans
operator|=
name|n
expr_stmt|;
block|}
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
block|}
block|}
if|#
directive|if
name|NAMED_BIND
if|if
condition|(
name|map
operator|->
name|map_timeout
operator|>
literal|0
condition|)
name|_res
operator|.
name|retrans
operator|=
name|retrans
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_retry
operator|>
literal|0
condition|)
name|_res
operator|.
name|retry
operator|=
name|retry
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
name|s
operator|->
name|s_namecanon
operator|.
name|nc_flags
operator||=
name|NCF_VALID
expr_stmt|;
comment|/* will be soon */
comment|/* Found an answer */
if|if
condition|(
name|ans
operator|!=
name|NULL
condition|)
block|{
name|s
operator|->
name|s_namecanon
operator|.
name|nc_stat
operator|=
operator|*
name|statp
operator|=
name|EX_OK
expr_stmt|;
if|if
condition|(
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
operator|!=
name|NULL
condition|)
name|sm_free
argument_list|(
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
argument_list|)
expr_stmt|;
name|s
operator|->
name|s_namecanon
operator|.
name|nc_cname
operator|=
name|sm_strdup_x
argument_list|(
name|ans
argument_list|)
expr_stmt|;
if|if
condition|(
name|bitset
argument_list|(
name|MF_MATCHONLY
argument_list|,
name|map
operator|->
name|map_mflags
argument_list|)
condition|)
name|cp
operator|=
name|map_rewrite
argument_list|(
name|map
argument_list|,
name|name
argument_list|,
name|strlen
argument_list|(
name|name
argument_list|)
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|else
name|cp
operator|=
name|map_rewrite
argument_list|(
name|map
argument_list|,
name|ans
argument_list|,
name|strlen
argument_list|(
name|ans
argument_list|)
argument_list|,
name|av
argument_list|)
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"FOUND %s\n"
argument_list|,
name|ans
argument_list|)
expr_stmt|;
return|return
name|cp
return|;
block|}
comment|/* No match found */
name|s
operator|->
name|s_namecanon
operator|.
name|nc_errno
operator|=
name|errno
expr_stmt|;
if|#
directive|if
name|NAMED_BIND
name|s
operator|->
name|s_namecanon
operator|.
name|nc_herrno
operator|=
name|h_errno
expr_stmt|;
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"FAIL (%d)\n"
argument_list|,
name|h_errno
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|h_errno
condition|)
block|{
case|case
name|TRY_AGAIN
case|:
if|if
condition|(
name|UseNameServer
condition|)
block|{
name|CurEnv
operator|->
name|e_status
operator|=
literal|"4.4.3"
expr_stmt|;
name|message
argument_list|(
literal|"851 %s: Name server timeout"
argument_list|,
name|shortenstring
argument_list|(
name|name
argument_list|,
literal|33
argument_list|)
argument_list|)
expr_stmt|;
block|}
operator|*
name|statp
operator|=
name|EX_TEMPFAIL
expr_stmt|;
break|break;
case|case
name|HOST_NOT_FOUND
case|:
case|case
name|NO_DATA
case|:
operator|*
name|statp
operator|=
name|EX_NOHOST
expr_stmt|;
break|break;
case|case
name|NO_RECOVERY
case|:
operator|*
name|statp
operator|=
name|EX_SOFTWARE
expr_stmt|;
break|break;
default|default:
operator|*
name|statp
operator|=
name|EX_UNAVAILABLE
expr_stmt|;
break|break;
block|}
else|#
directive|else
comment|/* NAMED_BIND */
if|if
condition|(
name|tTd
argument_list|(
literal|9
argument_list|,
literal|1
argument_list|)
condition|)
name|sm_dprintf
argument_list|(
literal|"FAIL\n"
argument_list|)
expr_stmt|;
operator|*
name|statp
operator|=
name|EX_NOHOST
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
name|s
operator|->
name|s_namecanon
operator|.
name|nc_stat
operator|=
operator|*
name|statp
expr_stmt|;
return|return
name|NULL
return|;
block|}
end_function

begin_comment
comment|/* **  HOST_MAP_INIT -- initialize host class structures ** **	Parameters: **		map -- a pointer to this map. **		args -- argument string. ** **	Returns: **		true. */
end_comment

begin_function
name|bool
name|host_map_init
parameter_list|(
name|map
parameter_list|,
name|args
parameter_list|)
name|MAP
modifier|*
name|map
decl_stmt|;
name|char
modifier|*
name|args
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|p
init|=
name|args
decl_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
while|while
condition|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'-'
condition|)
break|break;
switch|switch
condition|(
operator|*
operator|++
name|p
condition|)
block|{
case|case
literal|'a'
case|:
name|map
operator|->
name|map_app
operator|=
operator|++
name|p
expr_stmt|;
break|break;
case|case
literal|'T'
case|:
name|map
operator|->
name|map_tapp
operator|=
operator|++
name|p
expr_stmt|;
break|break;
case|case
literal|'m'
case|:
name|map
operator|->
name|map_mflags
operator||=
name|MF_MATCHONLY
expr_stmt|;
break|break;
case|case
literal|'t'
case|:
name|map
operator|->
name|map_mflags
operator||=
name|MF_NODEFER
expr_stmt|;
break|break;
case|case
literal|'S'
case|:
comment|/* only for consistency */
name|map
operator|->
name|map_spacesub
operator|=
operator|*
operator|++
name|p
expr_stmt|;
break|break;
case|case
literal|'D'
case|:
name|map
operator|->
name|map_mflags
operator||=
name|MF_DEFER
expr_stmt|;
break|break;
case|case
literal|'d'
case|:
block|{
name|char
modifier|*
name|h
decl_stmt|;
while|while
condition|(
name|isascii
argument_list|(
operator|*
operator|++
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
continue|continue;
name|h
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|' '
argument_list|)
expr_stmt|;
if|if
condition|(
name|h
operator|!=
name|NULL
condition|)
operator|*
name|h
operator|=
literal|'\0'
expr_stmt|;
name|map
operator|->
name|map_timeout
operator|=
name|convtime
argument_list|(
name|p
argument_list|,
literal|'s'
argument_list|)
expr_stmt|;
if|if
condition|(
name|h
operator|!=
name|NULL
condition|)
operator|*
name|h
operator|=
literal|' '
expr_stmt|;
block|}
break|break;
case|case
literal|'r'
case|:
while|while
condition|(
name|isascii
argument_list|(
operator|*
operator|++
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
condition|)
continue|continue;
name|map
operator|->
name|map_retry
operator|=
name|atoi
argument_list|(
name|p
argument_list|)
expr_stmt|;
break|break;
block|}
while|while
condition|(
operator|*
name|p
operator|!=
literal|'\0'
operator|&&
operator|!
operator|(
name|isascii
argument_list|(
operator|*
name|p
argument_list|)
operator|&&
name|isspace
argument_list|(
operator|*
name|p
argument_list|)
operator|)
condition|)
name|p
operator|++
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
literal|'\0'
condition|)
operator|*
name|p
operator|++
operator|=
literal|'\0'
expr_stmt|;
block|}
if|if
condition|(
name|map
operator|->
name|map_app
operator|!=
name|NULL
condition|)
name|map
operator|->
name|map_app
operator|=
name|newstr
argument_list|(
name|map
operator|->
name|map_app
argument_list|)
expr_stmt|;
if|if
condition|(
name|map
operator|->
name|map_tapp
operator|!=
name|NULL
condition|)
name|map
operator|->
name|map_tapp
operator|=
name|newstr
argument_list|(
name|map
operator|->
name|map_tapp
argument_list|)
expr_stmt|;
return|return
name|true
return|;
block|}
end_function

begin_if
if|#
directive|if
name|NETINET6
end_if

begin_comment
comment|/* **  ANYNET_NTOP -- convert an IPv6 network address to printable form. ** **	Parameters: **		s6a -- a pointer to an in6_addr structure. **		dst -- buffer to store result in **		dst_len -- size of dst buffer ** **	Returns: **		A printable version of that structure. */
end_comment

begin_function
name|char
modifier|*
name|anynet_ntop
parameter_list|(
name|s6a
parameter_list|,
name|dst
parameter_list|,
name|dst_len
parameter_list|)
name|struct
name|in6_addr
modifier|*
name|s6a
decl_stmt|;
name|char
modifier|*
name|dst
decl_stmt|;
name|size_t
name|dst_len
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|ap
decl_stmt|;
if|if
condition|(
name|IN6_IS_ADDR_V4MAPPED
argument_list|(
name|s6a
argument_list|)
condition|)
name|ap
operator|=
operator|(
name|char
operator|*
operator|)
name|inet_ntop
argument_list|(
name|AF_INET
argument_list|,
operator|&
name|s6a
operator|->
name|s6_addr
index|[
name|IN6ADDRSZ
operator|-
name|INADDRSZ
index|]
argument_list|,
name|dst
argument_list|,
name|dst_len
argument_list|)
expr_stmt|;
else|else
block|{
name|char
modifier|*
name|d
decl_stmt|;
name|size_t
name|sz
decl_stmt|;
comment|/* Save pointer to beginning of string */
name|d
operator|=
name|dst
expr_stmt|;
comment|/* Add IPv6: protocol tag */
name|sz
operator|=
name|sm_strlcpy
argument_list|(
name|dst
argument_list|,
literal|"IPv6:"
argument_list|,
name|dst_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|sz
operator|>=
name|dst_len
condition|)
return|return
name|NULL
return|;
name|dst
operator|+=
name|sz
expr_stmt|;
name|dst_len
operator|-=
name|sz
expr_stmt|;
name|ap
operator|=
operator|(
name|char
operator|*
operator|)
name|inet_ntop
argument_list|(
name|AF_INET6
argument_list|,
name|s6a
argument_list|,
name|dst
argument_list|,
name|dst_len
argument_list|)
expr_stmt|;
comment|/* Restore pointer to beginning of string */
if|if
condition|(
name|ap
operator|!=
name|NULL
condition|)
name|ap
operator|=
name|d
expr_stmt|;
block|}
return|return
name|ap
return|;
block|}
end_function

begin_comment
comment|/* **  ANYNET_PTON -- convert printed form to network address. ** **	Wrapper for inet_pton() which handles IPv6: labels. ** **	Parameters: **		family -- address family **		src -- string **		dst -- destination address structure ** **	Returns: **		1 if the address was valid **		0 if the address wasn't parseable **		-1 if error */
end_comment

begin_function
name|int
name|anynet_pton
parameter_list|(
name|family
parameter_list|,
name|src
parameter_list|,
name|dst
parameter_list|)
name|int
name|family
decl_stmt|;
specifier|const
name|char
modifier|*
name|src
decl_stmt|;
name|void
modifier|*
name|dst
decl_stmt|;
block|{
if|if
condition|(
name|family
operator|==
name|AF_INET6
operator|&&
name|sm_strncasecmp
argument_list|(
name|src
argument_list|,
literal|"IPv6:"
argument_list|,
literal|5
argument_list|)
operator|==
literal|0
condition|)
name|src
operator|+=
literal|5
expr_stmt|;
return|return
name|inet_pton
argument_list|(
name|family
argument_list|,
name|src
argument_list|,
name|dst
argument_list|)
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NETINET6 */
end_comment

begin_comment
comment|/* **  ANYNET_NTOA -- convert a network address to printable form. ** **	Parameters: **		sap -- a pointer to a sockaddr structure. ** **	Returns: **		A printable version of that sockaddr. */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|USE_SOCK_STREAM
end_ifdef

begin_if
if|#
directive|if
name|NETLINK
end_if

begin_include
include|#
directive|include
file|<net/if_dl.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* NETLINK */
end_comment

begin_function
name|char
modifier|*
name|anynet_ntoa
parameter_list|(
name|sap
parameter_list|)
specifier|register
name|SOCKADDR
modifier|*
name|sap
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|bp
decl_stmt|;
specifier|register
name|char
modifier|*
name|ap
decl_stmt|;
name|int
name|l
decl_stmt|;
specifier|static
name|char
name|buf
index|[
literal|100
index|]
decl_stmt|;
comment|/* check for null/zero family */
if|if
condition|(
name|sap
operator|==
name|NULL
condition|)
return|return
literal|"NULLADDR"
return|;
if|if
condition|(
name|sap
operator|->
name|sa
operator|.
name|sa_family
operator|==
literal|0
condition|)
return|return
literal|"0"
return|;
switch|switch
condition|(
name|sap
operator|->
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETUNIX
case|case
name|AF_UNIX
case|:
if|if
condition|(
name|sap
operator|->
name|sunix
operator|.
name|sun_path
index|[
literal|0
index|]
operator|!=
literal|'\0'
condition|)
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
literal|"[UNIX: %.64s]"
argument_list|,
name|sap
operator|->
name|sunix
operator|.
name|sun_path
argument_list|)
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|buf
argument_list|,
literal|"[UNIX: localhost]"
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
endif|#
directive|endif
comment|/* NETUNIX */
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
return|return
operator|(
name|char
operator|*
operator|)
name|inet_ntoa
argument_list|(
name|sap
operator|->
name|sin
operator|.
name|sin_addr
argument_list|)
return|;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|ap
operator|=
name|anynet_ntop
argument_list|(
operator|&
name|sap
operator|->
name|sin6
operator|.
name|sin6_addr
argument_list|,
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|ap
operator|!=
name|NULL
condition|)
return|return
name|ap
return|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETLINK
case|case
name|AF_LINK
case|:
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
literal|"[LINK: %s]"
argument_list|,
name|link_ntoa
argument_list|(
operator|(
expr|struct
name|sockaddr_dl
operator|*
operator|)
operator|&
name|sap
operator|->
name|sa
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
endif|#
directive|endif
comment|/* NETLINK */
default|default:
comment|/* this case is needed when nothing is #defined */
comment|/* in order to keep the switch syntactically correct */
break|break;
block|}
comment|/* unknown family -- just dump bytes */
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
literal|"Family %d: "
argument_list|,
name|sap
operator|->
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
name|bp
operator|=
operator|&
name|buf
index|[
name|strlen
argument_list|(
name|buf
argument_list|)
index|]
expr_stmt|;
name|ap
operator|=
name|sap
operator|->
name|sa
operator|.
name|sa_data
expr_stmt|;
for|for
control|(
name|l
operator|=
sizeof|sizeof
argument_list|(
name|sap
operator|->
name|sa
operator|.
name|sa_data
argument_list|)
init|;
operator|--
name|l
operator|>=
literal|0
condition|;
control|)
block|{
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|bp
argument_list|,
name|SPACELEFT
argument_list|(
name|buf
argument_list|,
name|bp
argument_list|)
argument_list|,
literal|"%02x:"
argument_list|,
operator|*
name|ap
operator|++
operator|&
literal|0377
argument_list|)
expr_stmt|;
name|bp
operator|+=
literal|3
expr_stmt|;
block|}
operator|*
operator|--
name|bp
operator|=
literal|'\0'
expr_stmt|;
return|return
name|buf
return|;
block|}
end_function

begin_comment
comment|/* **  HOSTNAMEBYANYADDR -- return name of host based on address ** **	Parameters: **		sap -- SOCKADDR pointer ** **	Returns: **		text representation of host name. ** **	Side Effects: **		none. */
end_comment

begin_function
name|char
modifier|*
name|hostnamebyanyaddr
parameter_list|(
name|sap
parameter_list|)
specifier|register
name|SOCKADDR
modifier|*
name|sap
decl_stmt|;
block|{
specifier|register
name|struct
name|hostent
modifier|*
name|hp
decl_stmt|;
if|#
directive|if
name|NAMED_BIND
name|int
name|saveretry
decl_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
if|#
directive|if
name|NETINET6
name|struct
name|in6_addr
name|in6_addr
decl_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NAMED_BIND
comment|/* shorten name server timeout to avoid higher level timeouts */
name|saveretry
operator|=
name|_res
operator|.
name|retry
expr_stmt|;
if|if
condition|(
name|_res
operator|.
name|retry
operator|*
name|_res
operator|.
name|retrans
operator|>
literal|20
condition|)
name|_res
operator|.
name|retry
operator|=
literal|20
operator|/
name|_res
operator|.
name|retrans
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
switch|switch
condition|(
name|sap
operator|->
name|sa
operator|.
name|sa_family
condition|)
block|{
if|#
directive|if
name|NETINET
case|case
name|AF_INET
case|:
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sap
operator|->
name|sin
operator|.
name|sin_addr
argument_list|,
name|INADDRSZ
argument_list|,
name|AF_INET
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET */
if|#
directive|if
name|NETINET6
case|case
name|AF_INET6
case|:
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sap
operator|->
name|sin6
operator|.
name|sin6_addr
argument_list|,
name|IN6ADDRSZ
argument_list|,
name|AF_INET6
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETISO
case|case
name|AF_ISO
case|:
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sap
operator|->
name|siso
operator|.
name|siso_addr
argument_list|,
sizeof|sizeof
argument_list|(
name|sap
operator|->
name|siso
operator|.
name|siso_addr
argument_list|)
argument_list|,
name|AF_ISO
argument_list|)
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETISO */
if|#
directive|if
name|NETUNIX
case|case
name|AF_UNIX
case|:
name|hp
operator|=
name|NULL
expr_stmt|;
break|break;
endif|#
directive|endif
comment|/* NETUNIX */
default|default:
name|hp
operator|=
name|sm_gethostbyaddr
argument_list|(
name|sap
operator|->
name|sa
operator|.
name|sa_data
argument_list|,
sizeof|sizeof
argument_list|(
name|sap
operator|->
name|sa
operator|.
name|sa_data
argument_list|)
argument_list|,
name|sap
operator|->
name|sa
operator|.
name|sa_family
argument_list|)
expr_stmt|;
break|break;
block|}
if|#
directive|if
name|NAMED_BIND
name|_res
operator|.
name|retry
operator|=
name|saveretry
expr_stmt|;
endif|#
directive|endif
comment|/* NAMED_BIND */
if|#
directive|if
name|NETINET
operator|||
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
operator|&&
name|hp
operator|->
name|h_name
index|[
literal|0
index|]
operator|!=
literal|'['
if|#
directive|if
name|NETINET6
operator|&&
name|inet_pton
argument_list|(
name|AF_INET6
argument_list|,
name|hp
operator|->
name|h_name
argument_list|,
operator|&
name|in6_addr
argument_list|)
operator|!=
literal|1
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETINET
operator|&&
name|inet_addr
argument_list|(
name|hp
operator|->
name|h_name
argument_list|)
operator|==
name|INADDR_NONE
endif|#
directive|endif
comment|/* NETINET */
condition|)
block|{
name|char
modifier|*
name|name
decl_stmt|;
name|name
operator|=
name|denlstring
argument_list|(
operator|(
name|char
operator|*
operator|)
name|hp
operator|->
name|h_name
argument_list|,
name|true
argument_list|,
name|true
argument_list|)
expr_stmt|;
if|#
directive|if
name|NETINET6
if|if
condition|(
name|name
operator|==
name|hp
operator|->
name|h_name
condition|)
block|{
specifier|static
name|char
name|n
index|[
name|MAXNAME
operator|+
literal|1
index|]
decl_stmt|;
comment|/* Copy the string, hp->h_name is about to disappear */
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|n
argument_list|,
name|name
argument_list|,
sizeof|sizeof
argument_list|(
name|n
argument_list|)
argument_list|)
expr_stmt|;
name|name
operator|=
name|n
expr_stmt|;
block|}
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* NETINET6 */
return|return
name|name
return|;
block|}
endif|#
directive|endif
comment|/* NETINET || NETINET6 */
if|#
directive|if
name|NETINET6
if|if
condition|(
name|hp
operator|!=
name|NULL
condition|)
block|{
name|freehostent
argument_list|(
name|hp
argument_list|)
expr_stmt|;
name|hp
operator|=
name|NULL
expr_stmt|;
block|}
endif|#
directive|endif
comment|/* NETINET6 */
if|#
directive|if
name|NETUNIX
if|if
condition|(
name|sap
operator|->
name|sa
operator|.
name|sa_family
operator|==
name|AF_UNIX
operator|&&
name|sap
operator|->
name|sunix
operator|.
name|sun_path
index|[
literal|0
index|]
operator|==
literal|'\0'
condition|)
return|return
literal|"localhost"
return|;
endif|#
directive|endif
comment|/* NETUNIX */
block|{
specifier|static
name|char
name|buf
index|[
literal|203
index|]
decl_stmt|;
operator|(
name|void
operator|)
name|sm_snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
literal|"[%.200s]"
argument_list|,
name|anynet_ntoa
argument_list|(
name|sap
argument_list|)
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* USE_SOCK_STREAM */
end_comment

end_unit

