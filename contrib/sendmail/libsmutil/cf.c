begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 2000-2002 Proofpoint, Inc. and its suppliers.  *	All rights reserved.  *  * By using this file, you agree to the terms and conditions set  * forth in the LICENSE file which can be found at the top level of  * the sendmail distribution.  *  */
end_comment

begin_include
include|#
directive|include
file|<sendmail.h>
end_include

begin_macro
name|SM_RCSID
argument_list|(
literal|"@(#)$Id: cf.c,v 8.20 2013-11-22 20:51:50 ca Exp $"
argument_list|)
end_macro

begin_include
include|#
directive|include
file|<sendmail/pathnames.h>
end_include

begin_comment
comment|/* **  GETCFNAME -- return the name of the .cf file to use. ** **	Some systems (e.g., NeXT) determine this dynamically. ** **	For others: returns submit.cf or sendmail.cf depending **		on the modes. ** **	Parameters: **		opmode -- operation mode. **		submitmode -- submit mode. **		cftype -- may request a certain cf file. **		conffile -- if set, return it. ** **	Returns: **		name of .cf file. */
end_comment

begin_function
name|char
modifier|*
name|getcfname
parameter_list|(
name|opmode
parameter_list|,
name|submitmode
parameter_list|,
name|cftype
parameter_list|,
name|conffile
parameter_list|)
name|int
name|opmode
decl_stmt|;
name|int
name|submitmode
decl_stmt|;
name|int
name|cftype
decl_stmt|;
name|char
modifier|*
name|conffile
decl_stmt|;
block|{
if|#
directive|if
name|NETINFO
name|char
modifier|*
name|cflocation
decl_stmt|;
endif|#
directive|endif
comment|/* NETINFO */
if|if
condition|(
name|conffile
operator|!=
name|NULL
condition|)
return|return
name|conffile
return|;
if|if
condition|(
name|cftype
operator|==
name|SM_GET_SUBMIT_CF
operator|||
operator|(
operator|(
name|submitmode
operator|!=
name|SUBMIT_UNKNOWN
operator|||
name|opmode
operator|==
name|MD_DELIVER
operator|||
name|opmode
operator|==
name|MD_ARPAFTP
operator|||
name|opmode
operator|==
name|MD_SMTP
operator|)
operator|&&
name|cftype
operator|!=
name|SM_GET_SENDMAIL_CF
operator|)
condition|)
block|{
name|struct
name|stat
name|sbuf
decl_stmt|;
specifier|static
name|char
name|cf
index|[
name|MAXPATHLEN
index|]
decl_stmt|;
if|#
directive|if
name|NETINFO
name|cflocation
operator|=
name|ni_propval
argument_list|(
literal|"/locations"
argument_list|,
name|NULL
argument_list|,
literal|"sendmail"
argument_list|,
literal|"submit.cf"
argument_list|,
literal|'\0'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cflocation
operator|!=
name|NULL
condition|)
operator|(
name|void
operator|)
name|sm_strlcpy
argument_list|(
name|cf
argument_list|,
name|cflocation
argument_list|,
sizeof|sizeof
name|cf
argument_list|)
expr_stmt|;
else|else
endif|#
directive|endif
comment|/* NETINFO */
operator|(
name|void
operator|)
name|sm_strlcpyn
argument_list|(
name|cf
argument_list|,
sizeof|sizeof
name|cf
argument_list|,
literal|2
argument_list|,
name|_DIR_SENDMAILCF
argument_list|,
literal|"submit.cf"
argument_list|)
expr_stmt|;
if|if
condition|(
name|cftype
operator|==
name|SM_GET_SUBMIT_CF
operator|||
name|stat
argument_list|(
name|cf
argument_list|,
operator|&
name|sbuf
argument_list|)
operator|==
literal|0
condition|)
return|return
name|cf
return|;
block|}
if|#
directive|if
name|NETINFO
name|cflocation
operator|=
name|ni_propval
argument_list|(
literal|"/locations"
argument_list|,
name|NULL
argument_list|,
literal|"sendmail"
argument_list|,
literal|"sendmail.cf"
argument_list|,
literal|'\0'
argument_list|)
expr_stmt|;
if|if
condition|(
name|cflocation
operator|!=
name|NULL
condition|)
return|return
name|cflocation
return|;
endif|#
directive|endif
comment|/* NETINFO */
return|return
name|_PATH_SENDMAILCF
return|;
block|}
end_function

end_unit

