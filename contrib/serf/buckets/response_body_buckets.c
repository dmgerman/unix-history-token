begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Copyright 2002-2004 Justin Erenkrantz and Greg Stein  *  * Licensed under the Apache License, Version 2.0 (the "License");  * you may not use this file except in compliance with the License.  * You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|<apr_pools.h>
end_include

begin_include
include|#
directive|include
file|"serf.h"
end_include

begin_include
include|#
directive|include
file|"serf_bucket_util.h"
end_include

begin_comment
comment|/* Older versions of APR do not have this macro.  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|APR_SIZE_MAX
end_ifdef

begin_define
define|#
directive|define
name|REQUESTED_MAX
value|APR_SIZE_MAX
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|REQUESTED_MAX
value|(~((apr_size_t)0))
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_typedef
typedef|typedef
struct|struct
block|{
name|serf_bucket_t
modifier|*
name|stream
decl_stmt|;
name|apr_uint64_t
name|remaining
decl_stmt|;
block|}
name|body_context_t
typedef|;
end_typedef

begin_function
name|serf_bucket_t
modifier|*
name|serf_bucket_response_body_create
parameter_list|(
name|serf_bucket_t
modifier|*
name|stream
parameter_list|,
name|apr_uint64_t
name|len
parameter_list|,
name|serf_bucket_alloc_t
modifier|*
name|allocator
parameter_list|)
block|{
name|body_context_t
modifier|*
name|ctx
decl_stmt|;
name|ctx
operator|=
name|serf_bucket_mem_alloc
argument_list|(
name|allocator
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|ctx
argument_list|)
argument_list|)
expr_stmt|;
name|ctx
operator|->
name|stream
operator|=
name|stream
expr_stmt|;
name|ctx
operator|->
name|remaining
operator|=
name|len
expr_stmt|;
return|return
name|serf_bucket_create
argument_list|(
operator|&
name|serf_bucket_type_response_body
argument_list|,
name|allocator
argument_list|,
name|ctx
argument_list|)
return|;
block|}
end_function

begin_function
specifier|static
name|apr_status_t
name|serf_response_body_read
parameter_list|(
name|serf_bucket_t
modifier|*
name|bucket
parameter_list|,
name|apr_size_t
name|requested
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|data
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|)
block|{
name|body_context_t
modifier|*
name|ctx
init|=
name|bucket
operator|->
name|data
decl_stmt|;
name|apr_status_t
name|status
decl_stmt|;
if|if
condition|(
operator|!
name|ctx
operator|->
name|remaining
condition|)
block|{
operator|*
name|len
operator|=
literal|0
expr_stmt|;
return|return
name|APR_EOF
return|;
block|}
if|if
condition|(
name|requested
operator|==
name|SERF_READ_ALL_AVAIL
operator|||
name|requested
operator|>
name|ctx
operator|->
name|remaining
condition|)
block|{
if|if
condition|(
name|ctx
operator|->
name|remaining
operator|<=
name|REQUESTED_MAX
condition|)
block|{
name|requested
operator|=
operator|(
name|apr_size_t
operator|)
name|ctx
operator|->
name|remaining
expr_stmt|;
block|}
else|else
block|{
name|requested
operator|=
name|REQUESTED_MAX
expr_stmt|;
block|}
block|}
name|status
operator|=
name|serf_bucket_read
argument_list|(
name|ctx
operator|->
name|stream
argument_list|,
name|requested
argument_list|,
name|data
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|SERF_BUCKET_READ_ERROR
argument_list|(
name|status
argument_list|)
condition|)
block|{
name|ctx
operator|->
name|remaining
operator|-=
operator|*
name|len
expr_stmt|;
block|}
if|if
condition|(
name|APR_STATUS_IS_EOF
argument_list|(
name|status
argument_list|)
operator|&&
name|ctx
operator|->
name|remaining
operator|>
literal|0
condition|)
block|{
comment|/* The server sent less data than expected. */
name|status
operator|=
name|SERF_ERROR_TRUNCATED_HTTP_RESPONSE
expr_stmt|;
block|}
return|return
name|status
return|;
block|}
end_function

begin_function
specifier|static
name|apr_status_t
name|serf_response_body_readline
parameter_list|(
name|serf_bucket_t
modifier|*
name|bucket
parameter_list|,
name|int
name|acceptable
parameter_list|,
name|int
modifier|*
name|found
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|data
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|)
block|{
name|body_context_t
modifier|*
name|ctx
init|=
name|bucket
operator|->
name|data
decl_stmt|;
name|apr_status_t
name|status
decl_stmt|;
if|if
condition|(
operator|!
name|ctx
operator|->
name|remaining
condition|)
block|{
operator|*
name|len
operator|=
literal|0
expr_stmt|;
return|return
name|APR_EOF
return|;
block|}
name|status
operator|=
name|serf_bucket_readline
argument_list|(
name|ctx
operator|->
name|stream
argument_list|,
name|acceptable
argument_list|,
name|found
argument_list|,
name|data
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|SERF_BUCKET_READ_ERROR
argument_list|(
name|status
argument_list|)
condition|)
block|{
name|ctx
operator|->
name|remaining
operator|-=
operator|*
name|len
expr_stmt|;
block|}
if|if
condition|(
name|APR_STATUS_IS_EOF
argument_list|(
name|status
argument_list|)
operator|&&
name|ctx
operator|->
name|remaining
operator|>
literal|0
condition|)
block|{
comment|/* The server sent less data than expected. */
name|status
operator|=
name|SERF_ERROR_TRUNCATED_HTTP_RESPONSE
expr_stmt|;
block|}
return|return
name|status
return|;
block|}
end_function

begin_function
specifier|static
name|apr_status_t
name|serf_response_body_peek
parameter_list|(
name|serf_bucket_t
modifier|*
name|bucket
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|data
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|)
block|{
name|body_context_t
modifier|*
name|ctx
init|=
name|bucket
operator|->
name|data
decl_stmt|;
return|return
name|serf_bucket_peek
argument_list|(
name|ctx
operator|->
name|stream
argument_list|,
name|data
argument_list|,
name|len
argument_list|)
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|serf_response_body_destroy
parameter_list|(
name|serf_bucket_t
modifier|*
name|bucket
parameter_list|)
block|{
name|body_context_t
modifier|*
name|ctx
init|=
name|bucket
operator|->
name|data
decl_stmt|;
name|serf_bucket_destroy
argument_list|(
name|ctx
operator|->
name|stream
argument_list|)
expr_stmt|;
name|serf_default_destroy_and_data
argument_list|(
name|bucket
argument_list|)
expr_stmt|;
block|}
end_function

begin_decl_stmt
specifier|const
name|serf_bucket_type_t
name|serf_bucket_type_response_body
init|=
block|{
literal|"RESPONSE_BODY"
block|,
name|serf_response_body_read
block|,
name|serf_response_body_readline
block|,
name|serf_default_read_iovec
block|,
name|serf_default_read_for_sendfile
block|,
name|serf_default_read_bucket
block|,
name|serf_response_body_peek
block|,
name|serf_response_body_destroy
block|, }
decl_stmt|;
end_decl_stmt

end_unit

