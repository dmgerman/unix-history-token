begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* pam_motd module */
end_comment

begin_comment
comment|/*  * Modified for pam_motd by Ben Collins<bcollins@debian.org>  *  * Based off of:  * $Id: pam_motd.c,v 1.1.1.1 2000/06/20 22:11:46 agmorgan Exp $  * $FreeBSD$  *   * Written by Michael K. Johnson<johnsonm@redhat.com> 1996/10/24  *  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|<fcntl.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<security/_pam_macros.h>
end_include

begin_comment
comment|/*  * here, we make a definition for the externally accessible function  * in this file (this definition is required for static a module  * but strongly encouraged generally) it is used to instruct the  * modules include file to define the function prototypes.  */
end_comment

begin_define
define|#
directive|define
name|PAM_SM_SESSION
end_define

begin_define
define|#
directive|define
name|DEFAULT_MOTD
value|"/etc/motd"
end_define

begin_include
include|#
directive|include
file|<security/pam_modules.h>
end_include

begin_comment
comment|/* --- session management functions (only) --- */
end_comment

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_close_session
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
return|return
name|PAM_IGNORE
return|;
block|}
end_function

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_open_session
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
name|int
name|retval
init|=
name|PAM_IGNORE
decl_stmt|;
name|int
name|fd
decl_stmt|;
name|char
modifier|*
name|mtmp
init|=
name|NULL
decl_stmt|,
modifier|*
name|motd_path
init|=
name|NULL
decl_stmt|;
name|struct
name|pam_conv
modifier|*
name|conversation
decl_stmt|;
name|struct
name|pam_message
name|message
decl_stmt|;
name|struct
name|pam_message
modifier|*
name|pmessage
init|=
operator|&
name|message
decl_stmt|;
name|struct
name|pam_response
modifier|*
name|resp
init|=
name|NULL
decl_stmt|;
name|struct
name|stat
name|st
decl_stmt|;
if|if
condition|(
name|flags
operator|&
name|PAM_SILENT
condition|)
block|{
return|return
name|retval
return|;
block|}
for|for
control|(
init|;
name|argc
operator|--
operator|>
literal|0
condition|;
operator|++
name|argv
control|)
block|{
if|if
condition|(
operator|!
name|strncmp
argument_list|(
operator|*
name|argv
argument_list|,
literal|"motd="
argument_list|,
literal|5
argument_list|)
condition|)
block|{
name|motd_path
operator|=
operator|(
name|char
operator|*
operator|)
name|strdup
argument_list|(
literal|5
operator|+
operator|*
name|argv
argument_list|)
expr_stmt|;
if|if
condition|(
name|motd_path
operator|!=
name|NULL
condition|)
block|{
name|D
argument_list|(
operator|(
literal|"set motd path: %s"
operator|,
name|motd_path
operator|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|D
argument_list|(
operator|(
literal|"failed to duplicate motd path - ignored"
operator|)
argument_list|)
expr_stmt|;
block|}
block|}
block|}
if|if
condition|(
name|motd_path
operator|==
name|NULL
condition|)
name|motd_path
operator|=
name|DEFAULT_MOTD
expr_stmt|;
name|message
operator|.
name|msg_style
operator|=
name|PAM_TEXT_INFO
expr_stmt|;
if|if
condition|(
operator|(
name|fd
operator|=
name|open
argument_list|(
name|motd_path
argument_list|,
name|O_RDONLY
argument_list|,
literal|0
argument_list|)
operator|)
operator|>=
literal|0
condition|)
block|{
comment|/* fill in message buffer with contents of motd */
if|if
condition|(
operator|(
name|fstat
argument_list|(
name|fd
argument_list|,
operator|&
name|st
argument_list|)
operator|<
literal|0
operator|)
operator|||
operator|!
name|st
operator|.
name|st_size
condition|)
return|return
name|retval
return|;
name|message
operator|.
name|msg
operator|=
name|mtmp
operator|=
name|malloc
argument_list|(
name|st
operator|.
name|st_size
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* if malloc failed... */
if|if
condition|(
operator|!
name|message
operator|.
name|msg
condition|)
return|return
name|retval
return|;
name|read
argument_list|(
name|fd
argument_list|,
name|mtmp
argument_list|,
name|st
operator|.
name|st_size
argument_list|)
expr_stmt|;
if|if
condition|(
name|mtmp
index|[
name|st
operator|.
name|st_size
operator|-
literal|1
index|]
operator|==
literal|'\n'
condition|)
name|mtmp
index|[
name|st
operator|.
name|st_size
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
else|else
name|mtmp
index|[
name|st
operator|.
name|st_size
index|]
operator|=
literal|'\0'
expr_stmt|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
comment|/* Use conversation function to give user contents of motd */
name|pam_get_item
argument_list|(
name|pamh
argument_list|,
name|PAM_CONV
argument_list|,
operator|(
specifier|const
name|void
operator|*
operator|*
operator|)
operator|&
name|conversation
argument_list|)
expr_stmt|;
name|conversation
operator|->
name|conv
argument_list|(
literal|1
argument_list|,
operator|(
specifier|const
expr|struct
name|pam_message
operator|*
operator|*
operator|)
operator|&
name|pmessage
argument_list|,
operator|&
name|resp
argument_list|,
name|conversation
operator|->
name|appdata_ptr
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|mtmp
argument_list|)
expr_stmt|;
if|if
condition|(
name|resp
condition|)
name|_pam_drop_reply
argument_list|(
name|resp
argument_list|,
literal|1
argument_list|)
expr_stmt|;
block|}
return|return
name|retval
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|PAM_STATIC
end_ifdef

begin_comment
comment|/* static module data */
end_comment

begin_decl_stmt
name|struct
name|pam_module
name|_pam_motd_modstruct
init|=
block|{
literal|"pam_motd"
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|pam_sm_open_session
block|,
name|pam_sm_close_session
block|,
name|NULL
block|, }
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* end of module definition */
end_comment

end_unit

