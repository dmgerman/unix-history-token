begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* pam_securetty module */
end_comment

begin_define
define|#
directive|define
name|SECURETTY_FILE
value|"/etc/securetty"
end_define

begin_define
define|#
directive|define
name|TTY_PREFIX
value|"/dev/"
end_define

begin_comment
comment|/*  * by Elliot Lee<sopwith@redhat.com>, Red Hat Software.  * July 25, 1996.  * This code shamelessly ripped from the pam_rootok module.  * Slight modifications AGM. 1996/12/3  * $FreeBSD$  */
end_comment

begin_define
define|#
directive|define
name|_GNU_SOURCE
end_define

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_include
include|#
directive|include
file|<stdarg.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_define
define|#
directive|define
name|PAM_SM_AUTH
end_define

begin_comment
comment|/*  * here, we make a definition for the externally accessible function  * in this file (this definition is required for static a module  * but strongly encouraged generally) it is used to instruct the  * modules include file to define the function prototypes.  */
end_comment

begin_define
define|#
directive|define
name|PAM_SM_AUTH
end_define

begin_include
include|#
directive|include
file|<security/pam_modules.h>
end_include

begin_comment
comment|/* some syslogging */
end_comment

begin_function
specifier|static
name|void
name|_pam_log
parameter_list|(
name|int
name|err
parameter_list|,
specifier|const
name|char
modifier|*
name|format
parameter_list|,
modifier|...
parameter_list|)
block|{
name|va_list
name|args
decl_stmt|;
name|va_start
argument_list|(
name|args
argument_list|,
name|format
argument_list|)
expr_stmt|;
name|openlog
argument_list|(
literal|"PAM-securetty"
argument_list|,
name|LOG_CONS
operator||
name|LOG_PID
argument_list|,
name|LOG_AUTH
argument_list|)
expr_stmt|;
name|vsyslog
argument_list|(
name|err
argument_list|,
name|format
argument_list|,
name|args
argument_list|)
expr_stmt|;
name|va_end
argument_list|(
name|args
argument_list|)
expr_stmt|;
name|closelog
argument_list|()
expr_stmt|;
block|}
end_function

begin_comment
comment|/* argument parsing */
end_comment

begin_define
define|#
directive|define
name|PAM_DEBUG_ARG
value|0x0001
end_define

begin_function
specifier|static
name|int
name|_pam_parse
parameter_list|(
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
name|int
name|ctrl
init|=
literal|0
decl_stmt|;
comment|/* step through arguments */
for|for
control|(
name|ctrl
operator|=
literal|0
init|;
name|argc
operator|--
operator|>
literal|0
condition|;
operator|++
name|argv
control|)
block|{
comment|/* generic options */
if|if
condition|(
operator|!
name|strcmp
argument_list|(
operator|*
name|argv
argument_list|,
literal|"debug"
argument_list|)
condition|)
name|ctrl
operator||=
name|PAM_DEBUG_ARG
expr_stmt|;
else|else
block|{
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
literal|"pam_parse: unknown option; %s"
argument_list|,
operator|*
name|argv
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|ctrl
return|;
block|}
end_function

begin_comment
comment|/* --- authentication management functions (only) --- */
end_comment

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_authenticate
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
name|int
name|retval
init|=
name|PAM_AUTH_ERR
decl_stmt|;
specifier|const
name|char
modifier|*
name|username
decl_stmt|;
name|char
modifier|*
name|uttyname
decl_stmt|;
name|char
name|ttyfileline
index|[
literal|256
index|]
decl_stmt|;
name|struct
name|stat
name|ttyfileinfo
decl_stmt|;
name|struct
name|passwd
modifier|*
name|user_pwd
decl_stmt|;
name|FILE
modifier|*
name|ttyfile
decl_stmt|;
name|int
name|ctrl
decl_stmt|;
comment|/* parse the arguments */
name|ctrl
operator|=
name|_pam_parse
argument_list|(
name|argc
argument_list|,
name|argv
argument_list|)
expr_stmt|;
name|retval
operator|=
name|pam_get_user
argument_list|(
name|pamh
argument_list|,
operator|&
name|username
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
operator|!=
name|PAM_SUCCESS
operator|||
name|username
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|ctrl
operator|&
name|PAM_DEBUG_ARG
condition|)
block|{
name|_pam_log
argument_list|(
name|LOG_WARNING
argument_list|,
literal|"cannot determine username"
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|retval
operator|==
name|PAM_CONV_AGAIN
condition|?
name|PAM_INCOMPLETE
else|:
name|PAM_SERVICE_ERR
operator|)
return|;
block|}
name|retval
operator|=
name|pam_get_item
argument_list|(
name|pamh
argument_list|,
name|PAM_TTY
argument_list|,
operator|(
specifier|const
name|void
operator|*
operator|*
operator|)
operator|&
name|uttyname
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
operator|!=
name|PAM_SUCCESS
operator|||
name|uttyname
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|ctrl
operator|&
name|PAM_DEBUG_ARG
condition|)
block|{
name|_pam_log
argument_list|(
name|LOG_WARNING
argument_list|,
literal|"cannot determine user's tty"
argument_list|)
expr_stmt|;
block|}
return|return
name|PAM_SERVICE_ERR
return|;
block|}
comment|/* The PAM_TTY item may be prefixed with "/dev/" - skip that */
if|if
condition|(
name|strncmp
argument_list|(
name|TTY_PREFIX
argument_list|,
name|uttyname
argument_list|,
sizeof|sizeof
argument_list|(
name|TTY_PREFIX
argument_list|)
operator|-
literal|1
argument_list|)
operator|==
literal|0
condition|)
name|uttyname
operator|+=
sizeof|sizeof
argument_list|(
name|TTY_PREFIX
argument_list|)
operator|-
literal|1
expr_stmt|;
name|user_pwd
operator|=
name|getpwnam
argument_list|(
name|username
argument_list|)
expr_stmt|;
if|if
condition|(
name|user_pwd
operator|==
name|NULL
condition|)
block|{
return|return
name|PAM_IGNORE
return|;
block|}
elseif|else
if|if
condition|(
name|user_pwd
operator|->
name|pw_uid
operator|!=
literal|0
condition|)
block|{
comment|/* If the user is not root, 					   securetty's does not apply 					   to them */
return|return
name|PAM_SUCCESS
return|;
block|}
if|if
condition|(
name|stat
argument_list|(
name|SECURETTY_FILE
argument_list|,
operator|&
name|ttyfileinfo
argument_list|)
condition|)
block|{
name|_pam_log
argument_list|(
name|LOG_NOTICE
argument_list|,
literal|"Couldn't open "
name|SECURETTY_FILE
argument_list|)
expr_stmt|;
return|return
name|PAM_SUCCESS
return|;
comment|/* for compatibility with old securetty handling, 			       this needs to succeed.  But we still log the 			       error. */
block|}
if|if
condition|(
operator|(
name|ttyfileinfo
operator|.
name|st_mode
operator|&
name|S_IWOTH
operator|)
operator|||
operator|!
name|S_ISREG
argument_list|(
name|ttyfileinfo
operator|.
name|st_mode
argument_list|)
condition|)
block|{
comment|/* If the file is world writable or is not a 	   normal file, return error */
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
name|SECURETTY_FILE
literal|" is either world writable or not a normal file"
argument_list|)
expr_stmt|;
return|return
name|PAM_AUTH_ERR
return|;
block|}
name|ttyfile
operator|=
name|fopen
argument_list|(
name|SECURETTY_FILE
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|ttyfile
operator|==
name|NULL
condition|)
block|{
comment|/* Check that we opened it successfully */
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Error opening "
name|SECURETTY_FILE
argument_list|)
expr_stmt|;
return|return
name|PAM_SERVICE_ERR
return|;
block|}
comment|/* There should be no more errors from here on */
name|retval
operator|=
name|PAM_AUTH_ERR
expr_stmt|;
comment|/* This loop assumes that PAM_SUCCESS == 0        and PAM_AUTH_ERR != 0 */
while|while
condition|(
operator|(
name|fgets
argument_list|(
name|ttyfileline
argument_list|,
sizeof|sizeof
argument_list|(
name|ttyfileline
argument_list|)
operator|-
literal|1
argument_list|,
name|ttyfile
argument_list|)
operator|!=
name|NULL
operator|)
operator|&&
name|retval
condition|)
block|{
if|if
condition|(
name|ttyfileline
index|[
name|strlen
argument_list|(
name|ttyfileline
argument_list|)
operator|-
literal|1
index|]
operator|==
literal|'\n'
condition|)
name|ttyfileline
index|[
name|strlen
argument_list|(
name|ttyfileline
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|retval
operator|=
name|strcmp
argument_list|(
name|ttyfileline
argument_list|,
name|uttyname
argument_list|)
expr_stmt|;
block|}
name|fclose
argument_list|(
name|ttyfile
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
condition|)
block|{
if|if
condition|(
name|ctrl
operator|&
name|PAM_DEBUG_ARG
condition|)
name|_pam_log
argument_list|(
name|LOG_WARNING
argument_list|,
literal|"access denied: tty '%s' is not secure !"
argument_list|,
name|uttyname
argument_list|)
expr_stmt|;
name|retval
operator|=
name|PAM_AUTH_ERR
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|retval
operator|==
name|PAM_SUCCESS
operator|)
operator|&&
operator|(
name|ctrl
operator|&
name|PAM_DEBUG_ARG
operator|)
condition|)
name|_pam_log
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"access allowed for '%s' on '%s'"
argument_list|,
name|username
argument_list|,
name|uttyname
argument_list|)
expr_stmt|;
return|return
name|retval
return|;
block|}
end_function

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_setcred
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
return|return
name|PAM_SUCCESS
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|PAM_STATIC
end_ifdef

begin_comment
comment|/* static module data */
end_comment

begin_decl_stmt
name|struct
name|pam_module
name|_pam_securetty_modstruct
init|=
block|{
literal|"pam_securetty"
block|,
name|pam_sm_authenticate
block|,
name|pam_sm_setcred
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|, }
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* end of module definition */
end_comment

end_unit

