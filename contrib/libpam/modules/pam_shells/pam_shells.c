begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* pam_shells module */
end_comment

begin_define
define|#
directive|define
name|SHELL_FILE
value|"/etc/shells"
end_define

begin_comment
comment|/*  * by Erik Troan<ewt@redhat.com>, Red Hat Software.  * August 5, 1996.  * This code shamelessly ripped from the pam_securetty module.  * $FreeBSD$  */
end_comment

begin_define
define|#
directive|define
name|_BSD_SOURCE
end_define

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<stdarg.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<syslog.h>
end_include

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_comment
comment|/*  * here, we make a definition for the externally accessible function  * in this file (this definition is required for static a module  * but strongly encouraged generally) it is used to instruct the  * modules include file to define the function prototypes.  */
end_comment

begin_define
define|#
directive|define
name|PAM_SM_AUTH
end_define

begin_include
include|#
directive|include
file|<security/pam_modules.h>
end_include

begin_comment
comment|/* some syslogging */
end_comment

begin_function
specifier|static
name|void
name|_pam_log
parameter_list|(
name|int
name|err
parameter_list|,
specifier|const
name|char
modifier|*
name|format
parameter_list|,
modifier|...
parameter_list|)
block|{
name|va_list
name|args
decl_stmt|;
name|va_start
argument_list|(
name|args
argument_list|,
name|format
argument_list|)
expr_stmt|;
name|openlog
argument_list|(
literal|"PAM-shells"
argument_list|,
name|LOG_CONS
operator||
name|LOG_PID
argument_list|,
name|LOG_AUTH
argument_list|)
expr_stmt|;
name|vsyslog
argument_list|(
name|err
argument_list|,
name|format
argument_list|,
name|args
argument_list|)
expr_stmt|;
name|va_end
argument_list|(
name|args
argument_list|)
expr_stmt|;
name|closelog
argument_list|()
expr_stmt|;
block|}
end_function

begin_comment
comment|/* --- authentication management functions (only) --- */
end_comment

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_authenticate
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
name|int
name|retval
init|=
name|PAM_AUTH_ERR
decl_stmt|;
specifier|const
name|char
modifier|*
name|userName
decl_stmt|;
name|char
modifier|*
name|userShell
decl_stmt|;
name|char
name|shellFileLine
index|[
literal|256
index|]
decl_stmt|;
name|struct
name|stat
name|sb
decl_stmt|;
name|struct
name|passwd
modifier|*
name|pw
decl_stmt|;
name|FILE
modifier|*
name|shellFile
decl_stmt|;
name|retval
operator|=
name|pam_get_user
argument_list|(
name|pamh
argument_list|,
operator|&
name|userName
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
operator|!=
name|PAM_SUCCESS
condition|)
return|return
name|PAM_SERVICE_ERR
return|;
if|if
condition|(
operator|!
name|userName
operator|||
operator|(
name|strlen
argument_list|(
name|userName
argument_list|)
operator|<=
literal|0
operator|)
condition|)
block|{
comment|/* Don't let them use a NULL username... */
name|pam_get_user
argument_list|(
name|pamh
argument_list|,
operator|&
name|userName
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
operator|!=
name|PAM_SUCCESS
condition|)
return|return
name|PAM_SERVICE_ERR
return|;
block|}
name|pw
operator|=
name|getpwnam
argument_list|(
name|userName
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|pw
condition|)
return|return
name|PAM_AUTH_ERR
return|;
comment|/* user doesn't exist */
name|userShell
operator|=
name|pw
operator|->
name|pw_shell
expr_stmt|;
if|if
condition|(
name|stat
argument_list|(
name|SHELL_FILE
argument_list|,
operator|&
name|sb
argument_list|)
condition|)
block|{
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s cannot be stat'd (it probably does not exist)"
argument_list|,
name|SHELL_FILE
argument_list|)
expr_stmt|;
return|return
name|PAM_AUTH_ERR
return|;
comment|/* must have /etc/shells */
block|}
if|if
condition|(
operator|(
name|sb
operator|.
name|st_mode
operator|&
name|S_IWOTH
operator|)
operator|||
operator|!
name|S_ISREG
argument_list|(
name|sb
operator|.
name|st_mode
argument_list|)
condition|)
block|{
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
literal|"%s is either world writable or not a normal file"
argument_list|,
name|SHELL_FILE
argument_list|)
expr_stmt|;
return|return
name|PAM_AUTH_ERR
return|;
block|}
name|shellFile
operator|=
name|fopen
argument_list|(
name|SHELL_FILE
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|shellFile
operator|==
name|NULL
condition|)
block|{
comment|/* Check that we opened it successfully */
name|_pam_log
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Error opening %s"
argument_list|,
name|SHELL_FILE
argument_list|)
expr_stmt|;
return|return
name|PAM_SERVICE_ERR
return|;
block|}
comment|/* There should be no more errors from here on */
name|retval
operator|=
name|PAM_AUTH_ERR
expr_stmt|;
comment|/* This loop assumes that PAM_SUCCESS == 0         and PAM_AUTH_ERR != 0 */
while|while
condition|(
operator|(
name|fgets
argument_list|(
name|shellFileLine
argument_list|,
literal|255
argument_list|,
name|shellFile
argument_list|)
operator|!=
name|NULL
operator|)
operator|&&
name|retval
condition|)
block|{
if|if
condition|(
name|shellFileLine
index|[
name|strlen
argument_list|(
name|shellFileLine
argument_list|)
operator|-
literal|1
index|]
operator|==
literal|'\n'
condition|)
name|shellFileLine
index|[
name|strlen
argument_list|(
name|shellFileLine
argument_list|)
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|retval
operator|=
name|strcmp
argument_list|(
name|shellFileLine
argument_list|,
name|userShell
argument_list|)
expr_stmt|;
block|}
name|fclose
argument_list|(
name|shellFile
argument_list|)
expr_stmt|;
if|if
condition|(
name|retval
condition|)
name|retval
operator|=
name|PAM_AUTH_ERR
expr_stmt|;
return|return
name|retval
return|;
block|}
end_function

begin_function
name|PAM_EXTERN
name|int
name|pam_sm_setcred
parameter_list|(
name|pam_handle_t
modifier|*
name|pamh
parameter_list|,
name|int
name|flags
parameter_list|,
name|int
name|argc
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|argv
parameter_list|)
block|{
return|return
name|PAM_SUCCESS
return|;
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|PAM_STATIC
end_ifdef

begin_comment
comment|/* static module data */
end_comment

begin_decl_stmt
name|struct
name|pam_module
name|_pam_shells_modstruct
init|=
block|{
literal|"pam_shells"
block|,
name|pam_sm_authenticate
block|,
name|pam_sm_setcred
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|,
name|NULL
block|, }
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* end of module definition */
end_comment

end_unit

