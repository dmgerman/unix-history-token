begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* ntp_refclock.c,v 3.1 1993/07/06 01:11:25 jbj Exp  * ntp_refclock - processing support for reference clocks  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|"ntpd.h"
end_include

begin_include
include|#
directive|include
file|"ntp_refclock.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|REFCLOCK
end_ifdef

begin_comment
comment|/*  * Reference clock support is provided here by maintaining the  * fiction that the clock is actually a peer.  As no packets are  * exchanged with a reference clock, however, we replace the  * transmit, receive and packet procedures with separate code  * to simulate them.  Refclock_transmit and refclock_receive  * maintain the peer variables in a state analogous to an  * actual peer and pass reference clock data on through the  * filters.  Refclock_peer and refclock_unpeer are called to  * initialize and terminate reference clock associations.  */
end_comment

begin_comment
comment|/*  * The refclock configuration table.  Imported from refclock_conf.c  */
end_comment

begin_decl_stmt
specifier|extern
name|struct
name|refclock
modifier|*
name|refclock_conf
index|[]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|u_char
name|num_refclock_conf
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Imported from the I/O module  */
end_comment

begin_decl_stmt
specifier|extern
name|struct
name|interface
modifier|*
name|any_interface
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|struct
name|interface
modifier|*
name|loopback_interface
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Imported from the timer module  */
end_comment

begin_decl_stmt
specifier|extern
name|U_LONG
name|current_time
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|struct
name|event
name|timerqueue
index|[]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Imported from the main and peer modules.  We use the same  * algorithm for spacing out timers at configuration time that  * the peer module does.  */
end_comment

begin_decl_stmt
specifier|extern
name|U_LONG
name|init_peer_starttime
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|int
name|initializing
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|int
name|debug
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|refclock_transmit
name|P
argument_list|(
operator|(
expr|struct
name|peer
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * refclock_newpeer - initialize and start a reference clock  */
end_comment

begin_function
name|int
name|refclock_newpeer
parameter_list|(
name|peer
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
name|u_char
name|clktype
decl_stmt|;
name|int
name|unit
decl_stmt|;
comment|/* 	 * Sanity... 	 */
if|if
condition|(
operator|!
name|ISREFCLOCKADR
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Internal error: attempting to initialize %s as reference clock"
argument_list|,
name|ntoa
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
name|clktype
operator|=
name|REFCLOCKTYPE
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
expr_stmt|;
name|unit
operator|=
name|REFCLOCKUNIT
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
expr_stmt|;
comment|/* 	 * If clktype is invalid, return 	 */
if|if
condition|(
name|clktype
operator|>=
name|num_refclock_conf
operator|||
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_start
operator|==
name|noentry
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Can't initialize %s, no support for clock type %d\n"
argument_list|,
name|ntoa
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
argument_list|,
name|clktype
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* 	 * Complete initialization of peer structure. 	 */
name|peer
operator|->
name|refclktype
operator|=
name|clktype
expr_stmt|;
name|peer
operator|->
name|refclkunit
operator|=
name|unit
expr_stmt|;
name|peer
operator|->
name|flags
operator||=
name|FLAG_REFCLOCK
expr_stmt|;
name|peer
operator|->
name|stratum
operator|=
name|STRATUM_REFCLOCK
expr_stmt|;
name|peer
operator|->
name|ppoll
operator|=
name|peer
operator|->
name|minpoll
expr_stmt|;
name|peer
operator|->
name|hpoll
operator|=
name|peer
operator|->
name|minpoll
expr_stmt|;
name|peer
operator|->
name|event_timer
operator|.
name|peer
operator|=
name|peer
expr_stmt|;
name|peer
operator|->
name|event_timer
operator|.
name|event_handler
operator|=
name|refclock_transmit
expr_stmt|;
comment|/* 	 * Do driver dependent initialization 	 */
if|if
condition|(
operator|!
operator|(
operator|(
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_start
operator|)
operator|(
name|unit
operator|,
name|peer
operator|)
operator|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Clock dependent initialization of %s failed"
argument_list|,
name|ntoa
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
comment|/* 	 * Set up the timeout for reachability determination. 	 */
if|if
condition|(
name|initializing
condition|)
block|{
name|init_peer_starttime
operator|+=
operator|(
literal|1
operator|<<
name|EVENT_TIMEOUT
operator|)
expr_stmt|;
if|if
condition|(
name|init_peer_starttime
operator|>=
operator|(
literal|1
operator|<<
name|peer
operator|->
name|minpoll
operator|)
condition|)
name|init_peer_starttime
operator|=
operator|(
literal|1
operator|<<
name|EVENT_TIMEOUT
operator|)
expr_stmt|;
name|peer
operator|->
name|event_timer
operator|.
name|event_time
operator|=
name|init_peer_starttime
expr_stmt|;
block|}
else|else
block|{
name|peer
operator|->
name|event_timer
operator|.
name|event_time
operator|=
name|current_time
operator|+
operator|(
literal|1
operator|<<
name|peer
operator|->
name|hpoll
operator|)
expr_stmt|;
block|}
name|TIMER_ENQUEUE
argument_list|(
name|timerqueue
argument_list|,
operator|&
name|peer
operator|->
name|event_timer
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_comment
comment|/*  * refclock_unpeer - shut down a clock  */
end_comment

begin_function
name|void
name|refclock_unpeer
parameter_list|(
name|peer
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
comment|/* 	 * Do sanity checks.  I know who programmed the calling routine! 	 */
if|if
condition|(
name|peer
operator|->
name|refclktype
operator|>=
name|num_refclock_conf
operator|||
name|refclock_conf
index|[
name|peer
operator|->
name|refclktype
index|]
operator|->
name|clock_shutdown
operator|==
name|noentry
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Attempting to shutdown %s: no such clock!"
argument_list|,
name|ntoa
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Tell the driver we're finished. 	 */
operator|(
name|refclock_conf
index|[
name|peer
operator|->
name|refclktype
index|]
operator|->
name|clock_shutdown
operator|)
operator|(
name|peer
operator|->
name|refclkunit
operator|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * refclock_transmit - replacement transmit procedure for reference clocks  */
end_comment

begin_function
specifier|static
name|void
name|refclock_transmit
parameter_list|(
name|peer
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
block|{
name|u_char
name|opeer_reach
decl_stmt|;
name|int
name|clktype
decl_stmt|;
name|int
name|unit
decl_stmt|;
name|clktype
operator|=
name|peer
operator|->
name|refclktype
expr_stmt|;
name|unit
operator|=
name|peer
operator|->
name|refclkunit
expr_stmt|;
name|peer
operator|->
name|sent
operator|++
expr_stmt|;
comment|/* 	 * The transmit procedure is supposed to freeze a time stamp. 	 * Get one just for fun, and to tell when we last were here. 	 */
name|get_systime
argument_list|(
operator|&
name|peer
operator|->
name|xmt
argument_list|)
expr_stmt|;
comment|/* 	 * Fiddle reachability. 	 */
name|opeer_reach
operator|=
name|peer
operator|->
name|reach
expr_stmt|;
name|peer
operator|->
name|reach
operator|<<=
literal|1
expr_stmt|;
if|if
condition|(
name|peer
operator|->
name|reach
operator|==
literal|0
condition|)
block|{
comment|/* 		 * Clear this one out.  No need to redo 		 * selection since this fellow will definitely 		 * be suffering from dispersion madness. 		 */
if|if
condition|(
name|opeer_reach
operator|!=
literal|0
condition|)
block|{
name|peer_clear
argument_list|(
name|peer
argument_list|)
expr_stmt|;
name|peer
operator|->
name|timereachable
operator|=
name|current_time
expr_stmt|;
name|report_event
argument_list|(
name|EVNT_UNREACH
argument_list|,
name|peer
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Update reachability and poll variables 	 */
block|}
elseif|else
if|if
condition|(
operator|(
name|opeer_reach
operator|&
literal|3
operator|)
operator|==
literal|0
condition|)
block|{
name|l_fp
name|off
decl_stmt|;
if|if
condition|(
name|peer
operator|->
name|valid
operator|>
literal|0
condition|)
name|peer
operator|->
name|valid
operator|--
expr_stmt|;
name|off
operator|.
name|l_ui
operator|=
name|off
operator|.
name|l_uf
operator|=
literal|0
expr_stmt|;
name|clock_filter
argument_list|(
name|peer
argument_list|,
operator|&
name|off
argument_list|,
literal|0
argument_list|,
name|NTP_MAXDISPERSE
argument_list|)
expr_stmt|;
if|if
condition|(
name|peer
operator|->
name|flags
operator|&
name|FLAG_SYSPEER
condition|)
name|clock_select
argument_list|()
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|peer
operator|->
name|valid
operator|<
name|NTP_SHIFT
condition|)
name|peer
operator|->
name|valid
operator|++
expr_stmt|;
comment|/* 	 * If he wants to be polled, do it. 	 */
if|if
condition|(
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_poll
operator|!=
name|noentry
condition|)
operator|(
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_poll
operator|)
operator|(
name|unit
operator|,
name|peer
operator|)
expr_stmt|;
comment|/* 	 * Finally, reset the timer 	 */
name|peer
operator|->
name|event_timer
operator|.
name|event_time
operator|+=
operator|(
literal|1
operator|<<
name|peer
operator|->
name|hpoll
operator|)
expr_stmt|;
name|TIMER_ENQUEUE
argument_list|(
name|timerqueue
argument_list|,
operator|&
name|peer
operator|->
name|event_timer
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * refclock_receive - simulate the receive and packet procedures for clocks  */
end_comment

begin_function
name|void
name|refclock_receive
parameter_list|(
name|peer
parameter_list|,
name|offset
parameter_list|,
name|delay
parameter_list|,
name|dispersion
parameter_list|,
name|reftime
parameter_list|,
name|rectime
parameter_list|,
name|leap
parameter_list|)
name|struct
name|peer
modifier|*
name|peer
decl_stmt|;
name|l_fp
modifier|*
name|offset
decl_stmt|;
name|s_fp
name|delay
decl_stmt|;
name|u_fp
name|dispersion
decl_stmt|;
name|l_fp
modifier|*
name|reftime
decl_stmt|;
name|l_fp
modifier|*
name|rectime
decl_stmt|;
name|int
name|leap
decl_stmt|;
block|{
name|int
specifier|restrict
expr_stmt|;
name|int
name|trustable
decl_stmt|;
specifier|extern
name|u_char
name|leap_indicator
decl_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|debug
condition|)
name|printf
argument_list|(
literal|"refclock_receive: %s %s %s %s)\n"
argument_list|,
name|ntoa
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
argument_list|,
name|lfptoa
argument_list|(
name|offset
argument_list|,
literal|6
argument_list|)
argument_list|,
name|fptoa
argument_list|(
name|delay
argument_list|,
literal|5
argument_list|)
argument_list|,
name|ufptoa
argument_list|(
name|dispersion
argument_list|,
literal|5
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* 	 * The name of this routine is actually a misnomer since 	 * we mostly simulate the variable setting of the packet 	 * procedure.  We do check the flag values, though, and 	 * set the trust bits based on this. 	 */
specifier|restrict
operator|=
name|restrictions
argument_list|(
operator|&
name|peer
operator|->
name|srcadr
argument_list|)
expr_stmt|;
if|if
condition|(
specifier|restrict
operator|&
operator|(
name|RES_IGNORE
operator||
name|RES_DONTSERVE
operator|)
condition|)
block|{
comment|/* 		 * Ours is not to reason why... 		 */
return|return;
block|}
name|peer
operator|->
name|received
operator|++
expr_stmt|;
name|peer
operator|->
name|processed
operator|++
expr_stmt|;
name|peer
operator|->
name|timereceived
operator|=
name|current_time
expr_stmt|;
if|if
condition|(
specifier|restrict
operator|&
name|RES_DONTTRUST
condition|)
name|trustable
operator|=
literal|0
expr_stmt|;
else|else
name|trustable
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|peer
operator|->
name|flags
operator|&
name|FLAG_AUTHENABLE
condition|)
block|{
if|if
condition|(
name|trustable
condition|)
name|peer
operator|->
name|flags
operator||=
name|FLAG_AUTHENTIC
expr_stmt|;
else|else
name|peer
operator|->
name|flags
operator|&=
operator|~
name|FLAG_AUTHENTIC
expr_stmt|;
block|}
if|if
condition|(
name|leap
operator|==
literal|0
condition|)
name|peer
operator|->
name|leap
operator|=
name|leap_indicator
expr_stmt|;
else|else
name|peer
operator|->
name|leap
operator|=
name|leap
expr_stmt|;
comment|/* 	 * Set the timestamps. rec and org are in local time, 	 * while ref is in timecode time. 	 */
name|peer
operator|->
name|rec
operator|=
name|peer
operator|->
name|org
operator|=
operator|*
name|rectime
expr_stmt|;
name|peer
operator|->
name|reftime
operator|=
operator|*
name|reftime
expr_stmt|;
comment|/* 	 * If the interface has been set to any_interface, set it 	 * to the loop back address if we have one.  This is so 	 * that peers which are unreachable are easy to see in 	 * peer display. 	 */
if|if
condition|(
name|peer
operator|->
name|dstadr
operator|==
name|any_interface
operator|&&
name|loopback_interface
operator|!=
literal|0
condition|)
name|peer
operator|->
name|dstadr
operator|=
name|loopback_interface
expr_stmt|;
comment|/* 	 * Set peer.pmode based on the hmode.  For appearances only. 	 */
switch|switch
condition|(
name|peer
operator|->
name|hmode
condition|)
block|{
case|case
name|MODE_ACTIVE
case|:
name|peer
operator|->
name|pmode
operator|=
name|MODE_PASSIVE
expr_stmt|;
break|break;
case|case
name|MODE_CLIENT
case|:
name|peer
operator|->
name|pmode
operator|=
name|MODE_SERVER
expr_stmt|;
break|break;
default|default:
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"refclock_receive: internal error, mode = %d"
argument_list|,
name|peer
operator|->
name|hmode
argument_list|)
expr_stmt|;
block|}
comment|/* 	 * Abandon ship if the radio came bum. We only got this far 	 * in order to make pretty billboards, even if bum. 	 */
if|if
condition|(
name|leap
operator|==
name|LEAP_NOTINSYNC
condition|)
return|return;
comment|/* 	 * If this guy was previously unreachable, report him 	 * reachable. 	 */
if|if
condition|(
name|peer
operator|->
name|reach
operator|==
literal|0
condition|)
name|report_event
argument_list|(
name|EVNT_REACH
argument_list|,
name|peer
argument_list|)
expr_stmt|;
name|peer
operator|->
name|reach
operator||=
literal|1
expr_stmt|;
comment|/* 	 * Give the data to the clock filter and update the clock. 	 */
name|clock_filter
argument_list|(
name|peer
argument_list|,
name|offset
argument_list|,
name|delay
argument_list|,
name|dispersion
argument_list|)
expr_stmt|;
name|clock_update
argument_list|(
name|peer
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * refclock_control - set and/or return clock values  */
end_comment

begin_function
name|void
name|refclock_control
parameter_list|(
name|srcadr
parameter_list|,
name|in
parameter_list|,
name|out
parameter_list|)
name|struct
name|sockaddr_in
modifier|*
name|srcadr
decl_stmt|;
name|struct
name|refclockstat
modifier|*
name|in
decl_stmt|;
name|struct
name|refclockstat
modifier|*
name|out
decl_stmt|;
block|{
name|u_char
name|clktype
decl_stmt|;
name|int
name|unit
decl_stmt|;
comment|/* 	 * Sanity... 	 */
if|if
condition|(
operator|!
name|ISREFCLOCKADR
argument_list|(
name|srcadr
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Internal error: refclock_control received %s as reference clock"
argument_list|,
name|ntoa
argument_list|(
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
name|clktype
operator|=
name|REFCLOCKTYPE
argument_list|(
name|srcadr
argument_list|)
expr_stmt|;
name|unit
operator|=
name|REFCLOCKUNIT
argument_list|(
name|srcadr
argument_list|)
expr_stmt|;
comment|/* 	 * If clktype is invalid, return 	 */
if|if
condition|(
name|clktype
operator|>=
name|num_refclock_conf
operator|||
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_control
operator|==
name|noentry
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Internal error: refclock_control finds %s as not supported"
argument_list|,
name|ntoa
argument_list|(
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
comment|/* 	 * Give the stuff to the clock. 	 */
operator|(
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_control
operator|)
operator|(
name|unit
operator|,
name|in
operator|,
name|out
operator|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * refclock_buginfo - return debugging info  */
end_comment

begin_function
name|void
name|refclock_buginfo
parameter_list|(
name|srcadr
parameter_list|,
name|bug
parameter_list|)
name|struct
name|sockaddr_in
modifier|*
name|srcadr
decl_stmt|;
name|struct
name|refclockbug
modifier|*
name|bug
decl_stmt|;
block|{
name|u_char
name|clktype
decl_stmt|;
name|int
name|unit
decl_stmt|;
comment|/* 	 * Sanity... 	 */
if|if
condition|(
operator|!
name|ISREFCLOCKADR
argument_list|(
name|srcadr
argument_list|)
condition|)
block|{
name|syslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"Internal error: refclock_buginfo received %s as reference clock"
argument_list|,
name|ntoa
argument_list|(
name|srcadr
argument_list|)
argument_list|)
expr_stmt|;
return|return;
block|}
name|clktype
operator|=
name|REFCLOCKTYPE
argument_list|(
name|srcadr
argument_list|)
expr_stmt|;
name|unit
operator|=
name|REFCLOCKUNIT
argument_list|(
name|srcadr
argument_list|)
expr_stmt|;
comment|/* 	 * If clktype is invalid or call is unsupported, return 	 */
if|if
condition|(
name|clktype
operator|>=
name|num_refclock_conf
operator|||
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_buginfo
operator|==
name|noentry
condition|)
block|{
return|return;
block|}
comment|/* 	 * Give the stuff to the clock. 	 */
operator|(
name|refclock_conf
index|[
name|clktype
index|]
operator|->
name|clock_buginfo
operator|)
operator|(
name|unit
operator|,
name|bug
operator|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * init_refclock - initialize the reference clock drivers  */
end_comment

begin_function
name|void
name|init_refclock
parameter_list|()
block|{
specifier|register
name|u_char
name|i
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_refclock_conf
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|refclock_conf
index|[
name|i
index|]
operator|->
name|clock_init
operator|!=
name|noentry
condition|)
operator|(
name|refclock_conf
index|[
name|i
index|]
operator|->
name|clock_init
operator|)
operator|(
operator|)
expr_stmt|;
block|}
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

end_unit

