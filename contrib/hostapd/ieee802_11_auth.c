begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Host AP (software wireless LAN access point) user space daemon for  * Host AP kernel driver / IEEE 802.11 authentication (ACL)  * Copyright (c) 2003-2005, Jouni Malinen<jkmaline@cc.hut.fi>  *  * This program is free software; you can redistribute it and/or modify  * it under the terms of the GNU General Public License version 2 as  * published by the Free Software Foundation.  *  * Alternatively, this software may be distributed under the terms of BSD  * license.  *  * See README and COPYING for more details.  */
end_comment

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<time.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<arpa/inet.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|"hostapd.h"
end_include

begin_include
include|#
directive|include
file|"ieee802_11.h"
end_include

begin_include
include|#
directive|include
file|"ieee802_11_auth.h"
end_include

begin_include
include|#
directive|include
file|"radius.h"
end_include

begin_include
include|#
directive|include
file|"radius_client.h"
end_include

begin_include
include|#
directive|include
file|"eloop.h"
end_include

begin_include
include|#
directive|include
file|"hostap_common.h"
end_include

begin_define
define|#
directive|define
name|RADIUS_ACL_TIMEOUT
value|30
end_define

begin_struct
struct|struct
name|hostapd_cached_radius_acl
block|{
name|time_t
name|timestamp
decl_stmt|;
name|macaddr
name|addr
decl_stmt|;
name|int
name|accepted
decl_stmt|;
comment|/* HOSTAPD_ACL_* */
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|next
decl_stmt|;
name|u32
name|session_timeout
decl_stmt|;
name|u32
name|acct_interim_interval
decl_stmt|;
block|}
struct|;
end_struct

begin_struct
struct|struct
name|hostapd_acl_query_data
block|{
name|time_t
name|timestamp
decl_stmt|;
name|u8
name|radius_id
decl_stmt|;
name|macaddr
name|addr
decl_stmt|;
name|u8
modifier|*
name|auth_msg
decl_stmt|;
comment|/* IEEE 802.11 authentication frame from station */
name|size_t
name|auth_msg_len
decl_stmt|;
name|struct
name|hostapd_acl_query_data
modifier|*
name|next
decl_stmt|;
block|}
struct|;
end_struct

begin_function
specifier|static
name|void
name|hostapd_acl_cache_free
parameter_list|(
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|acl_cache
parameter_list|)
block|{
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|prev
decl_stmt|;
while|while
condition|(
name|acl_cache
condition|)
block|{
name|prev
operator|=
name|acl_cache
expr_stmt|;
name|acl_cache
operator|=
name|acl_cache
operator|->
name|next
expr_stmt|;
name|free
argument_list|(
name|prev
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_function
specifier|static
name|int
name|hostapd_acl_cache_get
parameter_list|(
name|struct
name|hostapd_data
modifier|*
name|hapd
parameter_list|,
name|u8
modifier|*
name|addr
parameter_list|,
name|u32
modifier|*
name|session_timeout
parameter_list|,
name|u32
modifier|*
name|acct_interim_interval
parameter_list|)
block|{
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|entry
decl_stmt|;
name|time_t
name|now
decl_stmt|;
name|time
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
name|entry
operator|=
name|hapd
operator|->
name|acl_cache
expr_stmt|;
while|while
condition|(
name|entry
condition|)
block|{
if|if
condition|(
name|memcmp
argument_list|(
name|entry
operator|->
name|addr
argument_list|,
name|addr
argument_list|,
name|ETH_ALEN
argument_list|)
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|now
operator|-
name|entry
operator|->
name|timestamp
operator|>
name|RADIUS_ACL_TIMEOUT
condition|)
return|return
operator|-
literal|1
return|;
comment|/* entry has expired */
if|if
condition|(
name|entry
operator|->
name|accepted
operator|==
name|HOSTAPD_ACL_ACCEPT_TIMEOUT
condition|)
operator|*
name|session_timeout
operator|=
name|entry
operator|->
name|session_timeout
expr_stmt|;
operator|*
name|acct_interim_interval
operator|=
name|entry
operator|->
name|acct_interim_interval
expr_stmt|;
return|return
name|entry
operator|->
name|accepted
return|;
block|}
name|entry
operator|=
name|entry
operator|->
name|next
expr_stmt|;
block|}
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|hostapd_acl_query_free
parameter_list|(
name|struct
name|hostapd_acl_query_data
modifier|*
name|query
parameter_list|)
block|{
if|if
condition|(
name|query
operator|==
name|NULL
condition|)
return|return;
name|free
argument_list|(
name|query
operator|->
name|auth_msg
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|query
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|int
name|hostapd_radius_acl_query
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|,
name|u8
modifier|*
name|addr
parameter_list|,
name|struct
name|hostapd_acl_query_data
modifier|*
name|query
parameter_list|)
block|{
name|struct
name|radius_msg
modifier|*
name|msg
decl_stmt|;
name|char
name|buf
index|[
literal|128
index|]
decl_stmt|;
name|query
operator|->
name|radius_id
operator|=
name|radius_client_get_id
argument_list|(
name|hapd
operator|->
name|radius
argument_list|)
expr_stmt|;
name|msg
operator|=
name|radius_msg_new
argument_list|(
name|RADIUS_CODE_ACCESS_REQUEST
argument_list|,
name|query
operator|->
name|radius_id
argument_list|)
expr_stmt|;
if|if
condition|(
name|msg
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|radius_msg_make_authenticator
argument_list|(
name|msg
argument_list|,
name|addr
argument_list|,
name|ETH_ALEN
argument_list|)
expr_stmt|;
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|RADIUS_ADDR_FORMAT
argument_list|,
name|MAC2STR
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_USER_NAME
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add User-Name\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
if|if
condition|(
operator|!
name|radius_msg_add_attr_user_password
argument_list|(
name|msg
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|,
name|hapd
operator|->
name|conf
operator|->
name|radius
operator|->
name|auth_server
operator|->
name|shared_secret
argument_list|,
name|hapd
operator|->
name|conf
operator|->
name|radius
operator|->
name|auth_server
operator|->
name|shared_secret_len
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add User-Password\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|own_ip_addr
operator|.
name|af
operator|==
name|AF_INET
operator|&&
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_NAS_IP_ADDRESS
argument_list|,
operator|(
name|u8
operator|*
operator|)
operator|&
name|hapd
operator|->
name|conf
operator|->
name|own_ip_addr
operator|.
name|u
operator|.
name|v4
argument_list|,
literal|4
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add NAS-IP-Address\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
ifdef|#
directive|ifdef
name|CONFIG_IPV6
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|own_ip_addr
operator|.
name|af
operator|==
name|AF_INET6
operator|&&
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_NAS_IPV6_ADDRESS
argument_list|,
operator|(
name|u8
operator|*
operator|)
operator|&
name|hapd
operator|->
name|conf
operator|->
name|own_ip_addr
operator|.
name|u
operator|.
name|v6
argument_list|,
literal|16
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add NAS-IPv6-Address\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
endif|#
directive|endif
comment|/* CONFIG_IPV6 */
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|nas_identifier
operator|&&
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_NAS_IDENTIFIER
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|hapd
operator|->
name|conf
operator|->
name|nas_identifier
argument_list|,
name|strlen
argument_list|(
name|hapd
operator|->
name|conf
operator|->
name|nas_identifier
argument_list|)
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add NAS-Identifier\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|RADIUS_802_1X_ADDR_FORMAT
literal|":%s"
argument_list|,
name|MAC2STR
argument_list|(
name|hapd
operator|->
name|own_addr
argument_list|)
argument_list|,
name|hapd
operator|->
name|conf
operator|->
name|ssid
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_CALLED_STATION_ID
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add Called-Station-Id\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
name|RADIUS_802_1X_ADDR_FORMAT
argument_list|,
name|MAC2STR
argument_list|(
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_CALLING_STATION_ID
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add Calling-Station-Id\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
if|if
condition|(
operator|!
name|radius_msg_add_attr_int32
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_NAS_PORT_TYPE
argument_list|,
name|RADIUS_NAS_PORT_TYPE_IEEE_802_11
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add NAS-Port-Type\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|snprintf
argument_list|(
name|buf
argument_list|,
sizeof|sizeof
argument_list|(
name|buf
argument_list|)
argument_list|,
literal|"CONNECT 11Mbps 802.11b"
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|radius_msg_add_attr
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_CONNECT_INFO
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|buf
argument_list|,
name|strlen
argument_list|(
name|buf
argument_list|)
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Could not add Connect-Info\n"
argument_list|)
expr_stmt|;
goto|goto
name|fail
goto|;
block|}
name|radius_client_send
argument_list|(
name|hapd
operator|->
name|radius
argument_list|,
name|msg
argument_list|,
name|RADIUS_AUTH
argument_list|,
name|addr
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
name|fail
label|:
name|radius_msg_free
argument_list|(
name|msg
argument_list|)
expr_stmt|;
name|free
argument_list|(
name|msg
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|hostapd_allowed_address
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|,
name|u8
modifier|*
name|addr
parameter_list|,
name|u8
modifier|*
name|msg
parameter_list|,
name|size_t
name|len
parameter_list|,
name|u32
modifier|*
name|session_timeout
parameter_list|,
name|u32
modifier|*
name|acct_interim_interval
parameter_list|)
block|{
operator|*
name|session_timeout
operator|=
literal|0
expr_stmt|;
operator|*
name|acct_interim_interval
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|hostapd_maclist_found
argument_list|(
name|hapd
operator|->
name|conf
operator|->
name|accept_mac
argument_list|,
name|hapd
operator|->
name|conf
operator|->
name|num_accept_mac
argument_list|,
name|addr
argument_list|)
condition|)
return|return
name|HOSTAPD_ACL_ACCEPT
return|;
if|if
condition|(
name|hostapd_maclist_found
argument_list|(
name|hapd
operator|->
name|conf
operator|->
name|deny_mac
argument_list|,
name|hapd
operator|->
name|conf
operator|->
name|num_deny_mac
argument_list|,
name|addr
argument_list|)
condition|)
return|return
name|HOSTAPD_ACL_REJECT
return|;
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|macaddr_acl
operator|==
name|ACCEPT_UNLESS_DENIED
condition|)
return|return
name|HOSTAPD_ACL_ACCEPT
return|;
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|macaddr_acl
operator|==
name|DENY_UNLESS_ACCEPTED
condition|)
return|return
name|HOSTAPD_ACL_REJECT
return|;
if|if
condition|(
name|hapd
operator|->
name|conf
operator|->
name|macaddr_acl
operator|==
name|USE_EXTERNAL_RADIUS_AUTH
condition|)
block|{
name|struct
name|hostapd_acl_query_data
modifier|*
name|query
decl_stmt|;
comment|/* Check whether ACL cache has an entry for this station */
name|int
name|res
init|=
name|hostapd_acl_cache_get
argument_list|(
name|hapd
argument_list|,
name|addr
argument_list|,
name|session_timeout
argument_list|,
name|acct_interim_interval
argument_list|)
decl_stmt|;
if|if
condition|(
name|res
operator|==
name|HOSTAPD_ACL_ACCEPT
operator|||
name|res
operator|==
name|HOSTAPD_ACL_ACCEPT_TIMEOUT
condition|)
return|return
name|res
return|;
if|if
condition|(
name|res
operator|==
name|HOSTAPD_ACL_REJECT
condition|)
return|return
name|HOSTAPD_ACL_REJECT
return|;
name|query
operator|=
name|hapd
operator|->
name|acl_queries
expr_stmt|;
while|while
condition|(
name|query
condition|)
block|{
if|if
condition|(
name|memcmp
argument_list|(
name|query
operator|->
name|addr
argument_list|,
name|addr
argument_list|,
name|ETH_ALEN
argument_list|)
operator|==
literal|0
condition|)
block|{
comment|/* pending query in RADIUS retransmit queue; 				 * do not generate a new one */
return|return
name|HOSTAPD_ACL_PENDING
return|;
block|}
name|query
operator|=
name|query
operator|->
name|next
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|hapd
operator|->
name|conf
operator|->
name|radius
operator|->
name|auth_server
condition|)
return|return
name|HOSTAPD_ACL_REJECT
return|;
comment|/* No entry in the cache - query external RADIUS server */
name|query
operator|=
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|query
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|query
operator|==
name|NULL
condition|)
block|{
name|printf
argument_list|(
literal|"malloc for query data failed\n"
argument_list|)
expr_stmt|;
return|return
name|HOSTAPD_ACL_REJECT
return|;
block|}
name|memset
argument_list|(
name|query
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|query
argument_list|)
argument_list|)
expr_stmt|;
name|time
argument_list|(
operator|&
name|query
operator|->
name|timestamp
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|query
operator|->
name|addr
argument_list|,
name|addr
argument_list|,
name|ETH_ALEN
argument_list|)
expr_stmt|;
if|if
condition|(
name|hostapd_radius_acl_query
argument_list|(
name|hapd
argument_list|,
name|addr
argument_list|,
name|query
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Failed to send Access-Request for ACL "
literal|"query.\n"
argument_list|)
expr_stmt|;
name|hostapd_acl_query_free
argument_list|(
name|query
argument_list|)
expr_stmt|;
return|return
name|HOSTAPD_ACL_REJECT
return|;
block|}
name|query
operator|->
name|auth_msg
operator|=
name|malloc
argument_list|(
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|query
operator|->
name|auth_msg
operator|==
name|NULL
condition|)
block|{
name|printf
argument_list|(
literal|"Failed to allocate memory for auth frame.\n"
argument_list|)
expr_stmt|;
name|hostapd_acl_query_free
argument_list|(
name|query
argument_list|)
expr_stmt|;
return|return
name|HOSTAPD_ACL_REJECT
return|;
block|}
name|memcpy
argument_list|(
name|query
operator|->
name|auth_msg
argument_list|,
name|msg
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|query
operator|->
name|auth_msg_len
operator|=
name|len
expr_stmt|;
name|query
operator|->
name|next
operator|=
name|hapd
operator|->
name|acl_queries
expr_stmt|;
name|hapd
operator|->
name|acl_queries
operator|=
name|query
expr_stmt|;
comment|/* Queued data will be processed in hostapd_acl_recv_radius() 		 * when RADIUS server replies to the sent Access-Request. */
return|return
name|HOSTAPD_ACL_PENDING
return|;
block|}
return|return
name|HOSTAPD_ACL_REJECT
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|hostapd_acl_expire_cache
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|,
name|time_t
name|now
parameter_list|)
block|{
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|prev
decl_stmt|,
modifier|*
name|entry
decl_stmt|,
modifier|*
name|tmp
decl_stmt|;
name|prev
operator|=
name|NULL
expr_stmt|;
name|entry
operator|=
name|hapd
operator|->
name|acl_cache
expr_stmt|;
while|while
condition|(
name|entry
condition|)
block|{
if|if
condition|(
name|now
operator|-
name|entry
operator|->
name|timestamp
operator|>
name|RADIUS_ACL_TIMEOUT
condition|)
block|{
name|HOSTAPD_DEBUG
argument_list|(
name|HOSTAPD_DEBUG_MINIMAL
argument_list|,
literal|"Cached ACL entry for "
name|MACSTR
literal|" has expired.\n"
argument_list|,
name|MAC2STR
argument_list|(
name|entry
operator|->
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|prev
condition|)
name|prev
operator|->
name|next
operator|=
name|entry
operator|->
name|next
expr_stmt|;
else|else
name|hapd
operator|->
name|acl_cache
operator|=
name|entry
operator|->
name|next
expr_stmt|;
name|tmp
operator|=
name|entry
expr_stmt|;
name|entry
operator|=
name|entry
operator|->
name|next
expr_stmt|;
name|free
argument_list|(
name|tmp
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|prev
operator|=
name|entry
expr_stmt|;
name|entry
operator|=
name|entry
operator|->
name|next
expr_stmt|;
block|}
block|}
end_function

begin_function
specifier|static
name|void
name|hostapd_acl_expire_queries
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|,
name|time_t
name|now
parameter_list|)
block|{
name|struct
name|hostapd_acl_query_data
modifier|*
name|prev
decl_stmt|,
modifier|*
name|entry
decl_stmt|,
modifier|*
name|tmp
decl_stmt|;
name|prev
operator|=
name|NULL
expr_stmt|;
name|entry
operator|=
name|hapd
operator|->
name|acl_queries
expr_stmt|;
while|while
condition|(
name|entry
condition|)
block|{
if|if
condition|(
name|now
operator|-
name|entry
operator|->
name|timestamp
operator|>
name|RADIUS_ACL_TIMEOUT
condition|)
block|{
name|HOSTAPD_DEBUG
argument_list|(
name|HOSTAPD_DEBUG_MINIMAL
argument_list|,
literal|"ACL query for "
name|MACSTR
literal|" has expired.\n"
argument_list|,
name|MAC2STR
argument_list|(
name|entry
operator|->
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|prev
condition|)
name|prev
operator|->
name|next
operator|=
name|entry
operator|->
name|next
expr_stmt|;
else|else
name|hapd
operator|->
name|acl_queries
operator|=
name|entry
operator|->
name|next
expr_stmt|;
name|tmp
operator|=
name|entry
expr_stmt|;
name|entry
operator|=
name|entry
operator|->
name|next
expr_stmt|;
name|hostapd_acl_query_free
argument_list|(
name|tmp
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|prev
operator|=
name|entry
expr_stmt|;
name|entry
operator|=
name|entry
operator|->
name|next
expr_stmt|;
block|}
block|}
end_function

begin_function
specifier|static
name|void
name|hostapd_acl_expire
parameter_list|(
name|void
modifier|*
name|eloop_ctx
parameter_list|,
name|void
modifier|*
name|timeout_ctx
parameter_list|)
block|{
name|hostapd
modifier|*
name|hapd
init|=
name|eloop_ctx
decl_stmt|;
name|time_t
name|now
decl_stmt|;
name|time
argument_list|(
operator|&
name|now
argument_list|)
expr_stmt|;
name|hostapd_acl_expire_cache
argument_list|(
name|hapd
argument_list|,
name|now
argument_list|)
expr_stmt|;
name|hostapd_acl_expire_queries
argument_list|(
name|hapd
argument_list|,
name|now
argument_list|)
expr_stmt|;
name|eloop_register_timeout
argument_list|(
literal|10
argument_list|,
literal|0
argument_list|,
name|hostapd_acl_expire
argument_list|,
name|hapd
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/* Return 0 if RADIUS message was a reply to ACL query (and was processed here)  * or -1 if not. */
end_comment

begin_function
specifier|static
name|RadiusRxResult
name|hostapd_acl_recv_radius
parameter_list|(
name|struct
name|radius_msg
modifier|*
name|msg
parameter_list|,
name|struct
name|radius_msg
modifier|*
name|req
parameter_list|,
name|u8
modifier|*
name|shared_secret
parameter_list|,
name|size_t
name|shared_secret_len
parameter_list|,
name|void
modifier|*
name|data
parameter_list|)
block|{
name|struct
name|hostapd_data
modifier|*
name|hapd
init|=
name|data
decl_stmt|;
name|struct
name|hostapd_acl_query_data
modifier|*
name|query
decl_stmt|,
modifier|*
name|prev
decl_stmt|;
name|struct
name|hostapd_cached_radius_acl
modifier|*
name|cache
decl_stmt|;
name|query
operator|=
name|hapd
operator|->
name|acl_queries
expr_stmt|;
name|prev
operator|=
name|NULL
expr_stmt|;
while|while
condition|(
name|query
condition|)
block|{
if|if
condition|(
name|query
operator|->
name|radius_id
operator|==
name|msg
operator|->
name|hdr
operator|->
name|identifier
condition|)
break|break;
name|prev
operator|=
name|query
expr_stmt|;
name|query
operator|=
name|query
operator|->
name|next
expr_stmt|;
block|}
if|if
condition|(
name|query
operator|==
name|NULL
condition|)
return|return
name|RADIUS_RX_UNKNOWN
return|;
name|HOSTAPD_DEBUG
argument_list|(
name|HOSTAPD_DEBUG_MINIMAL
argument_list|,
literal|"Found matching Access-Request "
literal|"for RADIUS message (id=%d)\n"
argument_list|,
name|query
operator|->
name|radius_id
argument_list|)
expr_stmt|;
if|if
condition|(
name|radius_msg_verify
argument_list|(
name|msg
argument_list|,
name|shared_secret
argument_list|,
name|shared_secret_len
argument_list|,
name|req
argument_list|,
literal|0
argument_list|)
condition|)
block|{
name|printf
argument_list|(
literal|"Incoming RADIUS packet did not have correct "
literal|"authenticator - dropped\n"
argument_list|)
expr_stmt|;
return|return
name|RADIUS_RX_INVALID_AUTHENTICATOR
return|;
block|}
if|if
condition|(
name|msg
operator|->
name|hdr
operator|->
name|code
operator|!=
name|RADIUS_CODE_ACCESS_ACCEPT
operator|&&
name|msg
operator|->
name|hdr
operator|->
name|code
operator|!=
name|RADIUS_CODE_ACCESS_REJECT
condition|)
block|{
name|printf
argument_list|(
literal|"Unknown RADIUS message code %d to ACL query\n"
argument_list|,
name|msg
operator|->
name|hdr
operator|->
name|code
argument_list|)
expr_stmt|;
return|return
name|RADIUS_RX_UNKNOWN
return|;
block|}
comment|/* Insert Accept/Reject info into ACL cache */
name|cache
operator|=
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|cache
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|cache
operator|==
name|NULL
condition|)
block|{
name|printf
argument_list|(
literal|"Failed to add ACL cache entry\n"
argument_list|)
expr_stmt|;
goto|goto
name|done
goto|;
block|}
name|memset
argument_list|(
name|cache
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|cache
argument_list|)
argument_list|)
expr_stmt|;
name|time
argument_list|(
operator|&
name|cache
operator|->
name|timestamp
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|cache
operator|->
name|addr
argument_list|,
name|query
operator|->
name|addr
argument_list|,
sizeof|sizeof
argument_list|(
name|cache
operator|->
name|addr
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|msg
operator|->
name|hdr
operator|->
name|code
operator|==
name|RADIUS_CODE_ACCESS_ACCEPT
condition|)
block|{
if|if
condition|(
name|radius_msg_get_attr_int32
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_SESSION_TIMEOUT
argument_list|,
operator|&
name|cache
operator|->
name|session_timeout
argument_list|)
operator|==
literal|0
condition|)
name|cache
operator|->
name|accepted
operator|=
name|HOSTAPD_ACL_ACCEPT_TIMEOUT
expr_stmt|;
else|else
name|cache
operator|->
name|accepted
operator|=
name|HOSTAPD_ACL_ACCEPT
expr_stmt|;
if|if
condition|(
name|radius_msg_get_attr_int32
argument_list|(
name|msg
argument_list|,
name|RADIUS_ATTR_ACCT_INTERIM_INTERVAL
argument_list|,
operator|&
name|cache
operator|->
name|acct_interim_interval
argument_list|)
operator|==
literal|0
operator|&&
name|cache
operator|->
name|acct_interim_interval
operator|<
literal|60
condition|)
block|{
name|HOSTAPD_DEBUG
argument_list|(
name|HOSTAPD_DEBUG_MINIMAL
argument_list|,
literal|"Ignored too "
literal|"small Acct-Interim-Interval %d for "
literal|"STA "
name|MACSTR
literal|"\n"
argument_list|,
name|cache
operator|->
name|acct_interim_interval
argument_list|,
name|MAC2STR
argument_list|(
name|query
operator|->
name|addr
argument_list|)
argument_list|)
expr_stmt|;
name|cache
operator|->
name|acct_interim_interval
operator|=
literal|0
expr_stmt|;
block|}
block|}
else|else
name|cache
operator|->
name|accepted
operator|=
name|HOSTAPD_ACL_REJECT
expr_stmt|;
name|cache
operator|->
name|next
operator|=
name|hapd
operator|->
name|acl_cache
expr_stmt|;
name|hapd
operator|->
name|acl_cache
operator|=
name|cache
expr_stmt|;
comment|/* Re-send original authentication frame for 802.11 processing */
name|HOSTAPD_DEBUG
argument_list|(
name|HOSTAPD_DEBUG_MINIMAL
argument_list|,
literal|"Re-sending authentication frame "
literal|"after successful RADIUS ACL query\n"
argument_list|)
expr_stmt|;
name|ieee802_11_mgmt
argument_list|(
name|hapd
argument_list|,
name|query
operator|->
name|auth_msg
argument_list|,
name|query
operator|->
name|auth_msg_len
argument_list|,
name|WLAN_FC_STYPE_AUTH
argument_list|)
expr_stmt|;
name|done
label|:
if|if
condition|(
name|prev
operator|==
name|NULL
condition|)
name|hapd
operator|->
name|acl_queries
operator|=
name|query
operator|->
name|next
expr_stmt|;
else|else
name|prev
operator|->
name|next
operator|=
name|query
operator|->
name|next
expr_stmt|;
name|hostapd_acl_query_free
argument_list|(
name|query
argument_list|)
expr_stmt|;
return|return
name|RADIUS_RX_PROCESSED
return|;
block|}
end_function

begin_function
name|int
name|hostapd_acl_init
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|)
block|{
if|if
condition|(
name|radius_client_register
argument_list|(
name|hapd
operator|->
name|radius
argument_list|,
name|RADIUS_AUTH
argument_list|,
name|hostapd_acl_recv_radius
argument_list|,
name|hapd
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
name|eloop_register_timeout
argument_list|(
literal|10
argument_list|,
literal|0
argument_list|,
name|hostapd_acl_expire
argument_list|,
name|hapd
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|void
name|hostapd_acl_deinit
parameter_list|(
name|hostapd
modifier|*
name|hapd
parameter_list|)
block|{
name|struct
name|hostapd_acl_query_data
modifier|*
name|query
decl_stmt|,
modifier|*
name|prev
decl_stmt|;
name|hostapd_acl_cache_free
argument_list|(
name|hapd
operator|->
name|acl_cache
argument_list|)
expr_stmt|;
name|query
operator|=
name|hapd
operator|->
name|acl_queries
expr_stmt|;
while|while
condition|(
name|query
condition|)
block|{
name|prev
operator|=
name|query
expr_stmt|;
name|query
operator|=
name|query
operator|->
name|next
expr_stmt|;
name|hostapd_acl_query_free
argument_list|(
name|prev
argument_list|)
expr_stmt|;
block|}
block|}
end_function

end_unit

