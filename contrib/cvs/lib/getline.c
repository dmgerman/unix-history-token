begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* getline.c -- Replacement for GNU C library function getline  Copyright (C) 1993 Free Software Foundation, Inc.  This program is free software; you can redistribute it and/or modify it under the terms of the GNU General Public License as published by the Free Software Foundation; either version 2 of the License, or (at your option) any later version.  This program is distributed in the hope that it will be useful, but WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License for more details.  */
end_comment

begin_comment
comment|/* Written by Jan Brittenson, bson@gnu.ai.mit.edu.  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<assert.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|"getline.h"
end_include

begin_if
if|#
directive|if
name|STDC_HEADERS
end_if

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|char
modifier|*
name|malloc
argument_list|()
decl_stmt|,
modifier|*
name|realloc
argument_list|()
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* Always add at least this many bytes when extending the buffer.  */
end_comment

begin_define
define|#
directive|define
name|MIN_CHUNK
value|64
end_define

begin_comment
comment|/* Read up to (and including) a TERMINATOR from STREAM into *LINEPTR    + OFFSET (and null-terminate it).  If LIMIT is non-negative, then    read no more than LIMIT chars.     *LINEPTR is a pointer returned from malloc (or NULL), pointing to    *N characters of space.  It is realloc'd as necessary.       Return the number of characters read (not including the null    terminator), or -1 on error or EOF.  On a -1 return, the caller    should check feof(), if not then errno has been set to indicate the    error.  */
end_comment

begin_function
name|int
name|getstr
parameter_list|(
name|lineptr
parameter_list|,
name|n
parameter_list|,
name|stream
parameter_list|,
name|terminator
parameter_list|,
name|offset
parameter_list|,
name|limit
parameter_list|)
name|char
modifier|*
modifier|*
name|lineptr
decl_stmt|;
name|size_t
modifier|*
name|n
decl_stmt|;
name|FILE
modifier|*
name|stream
decl_stmt|;
name|char
name|terminator
decl_stmt|;
name|int
name|offset
decl_stmt|;
name|int
name|limit
decl_stmt|;
block|{
name|int
name|nchars_avail
decl_stmt|;
comment|/* Allocated but unused chars in *LINEPTR.  */
name|char
modifier|*
name|read_pos
decl_stmt|;
comment|/* Where we're reading into *LINEPTR. */
name|int
name|ret
decl_stmt|;
if|if
condition|(
operator|!
name|lineptr
operator|||
operator|!
name|n
operator|||
operator|!
name|stream
condition|)
block|{
name|errno
operator|=
name|EINVAL
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
operator|!
operator|*
name|lineptr
condition|)
block|{
operator|*
name|n
operator|=
name|MIN_CHUNK
expr_stmt|;
operator|*
name|lineptr
operator|=
name|malloc
argument_list|(
operator|*
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|*
name|lineptr
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
block|}
name|nchars_avail
operator|=
operator|*
name|n
operator|-
name|offset
expr_stmt|;
name|read_pos
operator|=
operator|*
name|lineptr
operator|+
name|offset
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|int
name|save_errno
decl_stmt|;
specifier|register
name|int
name|c
decl_stmt|;
if|if
condition|(
name|limit
operator|==
literal|0
condition|)
break|break;
else|else
block|{
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
comment|/* If limit is negative, then we shouldn't pay attention to              it, so decrement only if positive. */
if|if
condition|(
name|limit
operator|>
literal|0
condition|)
name|limit
operator|--
expr_stmt|;
block|}
name|save_errno
operator|=
name|errno
expr_stmt|;
comment|/* We always want at least one char left in the buffer, since we 	 always (unless we get an error while reading the first char) 	 NUL-terminate the line buffer.  */
name|assert
argument_list|(
operator|(
operator|*
name|lineptr
operator|+
operator|*
name|n
operator|)
operator|==
operator|(
name|read_pos
operator|+
name|nchars_avail
operator|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|nchars_avail
operator|<
literal|2
condition|)
block|{
if|if
condition|(
operator|*
name|n
operator|>
name|MIN_CHUNK
condition|)
operator|*
name|n
operator|*=
literal|2
expr_stmt|;
else|else
operator|*
name|n
operator|+=
name|MIN_CHUNK
expr_stmt|;
name|nchars_avail
operator|=
operator|*
name|n
operator|+
operator|*
name|lineptr
operator|-
name|read_pos
expr_stmt|;
operator|*
name|lineptr
operator|=
name|realloc
argument_list|(
operator|*
name|lineptr
argument_list|,
operator|*
name|n
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|*
name|lineptr
condition|)
block|{
name|errno
operator|=
name|ENOMEM
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|read_pos
operator|=
operator|*
name|n
operator|-
name|nchars_avail
operator|+
operator|*
name|lineptr
expr_stmt|;
name|assert
argument_list|(
operator|(
operator|*
name|lineptr
operator|+
operator|*
name|n
operator|)
operator|==
operator|(
name|read_pos
operator|+
name|nchars_avail
operator|)
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|ferror
argument_list|(
name|stream
argument_list|)
condition|)
block|{
comment|/* Might like to return partial line, but there is no 	     place for us to store errno.  And we don't want to just 	     lose errno.  */
name|errno
operator|=
name|save_errno
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|c
operator|==
name|EOF
condition|)
block|{
comment|/* Return partial line, if any.  */
if|if
condition|(
name|read_pos
operator|==
operator|*
name|lineptr
condition|)
return|return
operator|-
literal|1
return|;
else|else
break|break;
block|}
operator|*
name|read_pos
operator|++
operator|=
name|c
expr_stmt|;
name|nchars_avail
operator|--
expr_stmt|;
if|if
condition|(
name|c
operator|==
name|terminator
condition|)
comment|/* Return the line.  */
break|break;
block|}
comment|/* Done - NUL terminate and return the number of chars read.  */
operator|*
name|read_pos
operator|=
literal|'\0'
expr_stmt|;
name|ret
operator|=
name|read_pos
operator|-
operator|(
operator|*
name|lineptr
operator|+
name|offset
operator|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_function
name|int
name|getline
parameter_list|(
name|lineptr
parameter_list|,
name|n
parameter_list|,
name|stream
parameter_list|)
name|char
modifier|*
modifier|*
name|lineptr
decl_stmt|;
name|size_t
modifier|*
name|n
decl_stmt|;
name|FILE
modifier|*
name|stream
decl_stmt|;
block|{
return|return
name|getstr
argument_list|(
name|lineptr
argument_list|,
name|n
argument_list|,
name|stream
argument_list|,
literal|'\n'
argument_list|,
literal|0
argument_list|,
name|GETLINE_NO_LIMIT
argument_list|)
return|;
block|}
end_function

begin_function
name|int
name|getline_safe
parameter_list|(
name|lineptr
parameter_list|,
name|n
parameter_list|,
name|stream
parameter_list|,
name|limit
parameter_list|)
name|char
modifier|*
modifier|*
name|lineptr
decl_stmt|;
name|size_t
modifier|*
name|n
decl_stmt|;
name|FILE
modifier|*
name|stream
decl_stmt|;
name|int
name|limit
decl_stmt|;
block|{
return|return
name|getstr
argument_list|(
name|lineptr
argument_list|,
name|n
argument_list|,
name|stream
argument_list|,
literal|'\n'
argument_list|,
literal|0
argument_list|,
name|limit
argument_list|)
return|;
block|}
end_function

end_unit

