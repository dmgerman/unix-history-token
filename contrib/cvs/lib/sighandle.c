begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* sighandle.c -- Library routines for manipulating chains of signal handlers    Copyright (C) 1992 Free Software Foundation, Inc.     This program is free software; you can redistribute it and/or modify    it under the terms of the GNU General Public License as published by    the Free Software Foundation; either version 2, or (at your option)    any later version.     This program is distributed in the hope that it will be useful,    but WITHOUT ANY WARRANTY; without even the implied warranty of    MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the    GNU General Public License for more details.  */
end_comment

begin_comment
comment|/* Written by Paul Sander, HaL Computer Systems, Inc.<paul@hal.com>    Brian Berliner<berliner@Sun.COM> added POSIX support */
end_comment

begin_comment
comment|/*************************************************************************  *  * signal.c -- This file contains code that manipulates chains of signal  *             handlers.  *  *             Facilities are provided to register a signal handler for  *             any specific signal.  When a signal is received, all of the  *             registered signal handlers are invoked in the reverse order  *             in which they are registered.  Note that the signal handlers  *             must not themselves make calls to the signal handling  *             facilities.  *  *************************************************************************/
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"system.h"
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<signal.h>
end_include

begin_comment
comment|/* Add prototype support.  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|PROTO
end_ifndef

begin_if
if|#
directive|if
name|defined
argument_list|(
name|USE_PROTOTYPES
argument_list|)
condition|?
name|USE_PROTOTYPES
expr|:
name|defined
argument_list|(
name|__STDC__
argument_list|)
end_if

begin_define
define|#
directive|define
name|PROTO
parameter_list|(
name|ARGS
parameter_list|)
value|ARGS
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|PROTO
parameter_list|(
name|ARGS
parameter_list|)
value|()
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|STDC_HEADERS
end_ifdef

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_else
else|#
directive|else
end_else

begin_if
if|#
directive|if
name|__STDC__
end_if

begin_function_decl
name|char
modifier|*
name|calloc
parameter_list|(
name|unsigned
name|nelem
parameter_list|,
name|unsigned
name|size
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
name|char
modifier|*
name|malloc
parameter_list|(
name|unsigned
name|size
parameter_list|)
function_decl|;
end_function_decl

begin_else
else|#
directive|else
end_else

begin_function_decl
name|char
modifier|*
name|calloc
parameter_list|()
function_decl|;
end_function_decl

begin_function_decl
name|char
modifier|*
name|malloc
parameter_list|()
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __STDC__ */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* STDC_HEADERS */
end_comment

begin_comment
comment|/* Define the highest signal number (usually) */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|SIGMAX
end_ifndef

begin_define
define|#
directive|define
name|SIGMAX
value|64
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* Define linked list of signal handlers structure */
end_comment

begin_struct
struct|struct
name|SIG_hlist
block|{
name|RETSIGTYPE
function_decl|(
modifier|*
name|handler
function_decl|)
parameter_list|()
function_decl|;
name|struct
name|SIG_hlist
modifier|*
name|next
decl_stmt|;
block|}
struct|;
end_struct

begin_comment
comment|/*  * Define array of lists of signal handlers.  Note that this depends on  * the implementation to initialize each element to a null pointer.  */
end_comment

begin_decl_stmt
specifier|static
name|struct
name|SIG_hlist
modifier|*
modifier|*
name|SIG_handlers
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Define array of default signal vectors */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
end_ifdef

begin_decl_stmt
specifier|static
name|struct
name|sigaction
modifier|*
name|SIG_defaults
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_ifdef
ifdef|#
directive|ifdef
name|BSD_SIGNALS
end_ifdef

begin_decl_stmt
specifier|static
name|struct
name|sigvec
modifier|*
name|SIG_defaults
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_expr_stmt
specifier|static
name|RETSIGTYPE
argument_list|(
argument|**SIG_defaults
argument_list|)
name|PROTO
argument_list|(
operator|(
name|int
operator|)
argument_list|)
expr_stmt|;
end_expr_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* Critical section housekeeping */
end_comment

begin_decl_stmt
specifier|static
name|int
name|SIG_crSectNest
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Nesting level */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
end_ifdef

begin_decl_stmt
specifier|static
name|sigset_t
name|SIG_crSectMask
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Signal mask */
end_comment

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|int
name|SIG_crSectMask
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Signal mask */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_escape
end_escape

begin_comment
comment|/*  * Initialize the signal handler arrays  */
end_comment

begin_function
specifier|static
name|int
name|SIG_init
parameter_list|()
block|{
name|int
name|i
decl_stmt|;
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|sigset_t
name|sigset_test
decl_stmt|;
endif|#
directive|endif
if|if
condition|(
name|SIG_defaults
operator|&&
name|SIG_handlers
condition|)
comment|/* already allocated */
return|return
operator|(
literal|0
operator|)
return|;
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigfillset
argument_list|(
operator|&
name|sigset_test
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|1
init|;
name|i
operator|<
name|SIGMAX
operator|&&
name|sigismember
argument_list|(
operator|&
name|sigset_test
argument_list|,
name|i
argument_list|)
operator|==
literal|1
condition|;
name|i
operator|++
control|)
empty_stmt|;
if|if
condition|(
name|i
operator|<
name|SIGMAX
condition|)
name|i
operator|=
name|SIGMAX
expr_stmt|;
name|i
operator|++
expr_stmt|;
if|if
condition|(
operator|!
name|SIG_defaults
condition|)
name|SIG_defaults
operator|=
operator|(
expr|struct
name|sigaction
operator|*
operator|)
name|calloc
argument_list|(
name|i
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sigaction
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigemptyset
argument_list|(
operator|&
name|SIG_crSectMask
argument_list|)
expr_stmt|;
else|#
directive|else
name|i
operator|=
name|SIGMAX
operator|+
literal|1
expr_stmt|;
ifdef|#
directive|ifdef
name|BSD_SIGNALS
if|if
condition|(
operator|!
name|SIG_defaults
condition|)
name|SIG_defaults
operator|=
operator|(
expr|struct
name|sigvec
operator|*
operator|)
name|calloc
argument_list|(
name|i
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|sigvec
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
operator|!
name|SIG_defaults
condition|)
name|SIG_defaults
operator|=
operator|(
name|RETSIGTYPE
argument_list|(
argument|**
argument_list|)
name|PROTO
argument_list|(
operator|(
name|int
operator|)
argument_list|)
operator|)
name|calloc
argument_list|(
name|i
argument_list|,
sizeof|sizeof
argument_list|(
name|RETSIGTYPE
argument_list|(
argument|**
argument_list|)
name|PROTO
argument_list|(
operator|(
name|int
operator|)
argument_list|)
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|SIG_crSectMask
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|!
name|SIG_handlers
condition|)
name|SIG_handlers
operator|=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|*
operator|)
name|calloc
argument_list|(
name|i
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|SIG_hlist
operator|*
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
operator|!
name|SIG_defaults
operator|||
operator|!
name|SIG_handlers
operator|)
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/*  * The following invokes each signal handler in the reverse order in which  * they were registered.  */
end_comment

begin_decl_stmt
specifier|static
name|RETSIGTYPE
name|SIG_handle
name|PROTO
argument_list|(
operator|(
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|RETSIGTYPE
name|SIG_handle
parameter_list|(
name|sig
parameter_list|)
name|int
name|sig
decl_stmt|;
block|{
name|struct
name|SIG_hlist
modifier|*
name|this
decl_stmt|;
comment|/* Dispatch signal handlers */
name|this
operator|=
name|SIG_handlers
index|[
name|sig
index|]
expr_stmt|;
while|while
condition|(
name|this
operator|!=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
call|(
modifier|*
name|this
operator|->
name|handler
call|)
argument_list|(
name|sig
argument_list|)
expr_stmt|;
name|this
operator|=
name|this
operator|->
name|next
expr_stmt|;
block|}
return|return;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/*  * The following registers a signal handler.  If the handler is already  * registered, it is not registered twice, nor is the order in which signal  * handlers are invoked changed.  If this is the first signal handler  * registered for a given signal, the old sigvec structure is saved for  * restoration later.  */
end_comment

begin_decl_stmt
name|int
name|SIG_register
argument_list|(
name|sig
argument_list|,
name|fn
argument_list|)
name|int
name|sig
decl_stmt|;
end_decl_stmt

begin_function_decl
name|RETSIGTYPE
function_decl|(
modifier|*
name|fn
function_decl|)
parameter_list|()
function_decl|;
end_function_decl

begin_block
block|{
name|int
name|val
decl_stmt|;
name|struct
name|SIG_hlist
modifier|*
name|this
decl_stmt|;
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|struct
name|sigaction
name|act
decl_stmt|;
name|sigset_t
name|sigset_mask
decl_stmt|,
name|sigset_omask
decl_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|struct
name|sigvec
name|vec
decl_stmt|;
name|int
name|mask
decl_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* Initialize */
if|if
condition|(
name|SIG_init
argument_list|()
operator|!=
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|val
operator|=
literal|0
expr_stmt|;
comment|/* Block this signal while we look at handler chain */
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigemptyset
argument_list|(
operator|&
name|sigset_mask
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigaddset
argument_list|(
operator|&
name|sigset_mask
argument_list|,
name|sig
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_BLOCK
argument_list|,
operator|&
name|sigset_mask
argument_list|,
operator|&
name|sigset_omask
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|mask
operator|=
name|sigblock
argument_list|(
name|sigmask
argument_list|(
name|sig
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* See if this handler was already registered */
name|this
operator|=
name|SIG_handlers
index|[
name|sig
index|]
expr_stmt|;
while|while
condition|(
name|this
operator|!=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
if|if
condition|(
name|this
operator|->
name|handler
operator|==
name|fn
condition|)
break|break;
name|this
operator|=
name|this
operator|->
name|next
expr_stmt|;
block|}
comment|/* Register the new handler only if it is not already registered. */
if|if
condition|(
name|this
operator|==
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
comment|/* 		 * If this is the first handler registered for this signal, 		 * set up the signal handler dispatcher 		 */
if|if
condition|(
name|SIG_handlers
index|[
name|sig
index|]
operator|==
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|act
operator|.
name|sa_handler
operator|=
name|SIG_handle
expr_stmt|;
operator|(
name|void
operator|)
name|sigemptyset
argument_list|(
operator|&
name|act
operator|.
name|sa_mask
argument_list|)
expr_stmt|;
name|act
operator|.
name|sa_flags
operator|=
literal|0
expr_stmt|;
name|val
operator|=
name|sigaction
argument_list|(
name|sig
argument_list|,
operator|&
name|act
argument_list|,
operator|&
name|SIG_defaults
index|[
name|sig
index|]
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|memset
argument_list|(
operator|&
name|vec
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
name|vec
argument_list|)
argument_list|)
expr_stmt|;
name|vec
operator|.
name|sv_handler
operator|=
name|SIG_handle
expr_stmt|;
name|val
operator|=
name|sigvec
argument_list|(
name|sig
argument_list|,
operator|&
name|vec
argument_list|,
operator|&
name|SIG_defaults
index|[
name|sig
index|]
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
operator|(
name|SIG_defaults
index|[
name|sig
index|]
operator|=
name|signal
argument_list|(
name|sig
argument_list|,
name|SIG_handle
argument_list|)
operator|)
operator|==
name|SIG_ERR
condition|)
name|val
operator|=
operator|-
literal|1
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
block|}
comment|/* If not, register it */
if|if
condition|(
operator|(
name|val
operator|==
literal|0
operator|)
operator|&&
operator|(
name|this
operator|==
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
operator|)
condition|)
block|{
name|this
operator|=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|malloc
argument_list|(
sizeof|sizeof
argument_list|(
expr|struct
name|SIG_hlist
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|this
operator|==
name|NULL
condition|)
block|{
name|val
operator|=
operator|-
literal|1
expr_stmt|;
block|}
else|else
block|{
name|this
operator|->
name|handler
operator|=
name|fn
expr_stmt|;
name|this
operator|->
name|next
operator|=
name|SIG_handlers
index|[
name|sig
index|]
expr_stmt|;
name|SIG_handlers
index|[
name|sig
index|]
operator|=
name|this
expr_stmt|;
block|}
block|}
block|}
comment|/* Unblock the signal */
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_SETMASK
argument_list|,
operator|&
name|sigset_omask
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
operator|(
name|void
operator|)
name|sigsetmask
argument_list|(
name|mask
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
return|return
name|val
return|;
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/*  * The following deregisters a signal handler.  If the last signal handler for  * a given signal is deregistered, the default sigvec information is restored.  */
end_comment

begin_decl_stmt
name|int
name|SIG_deregister
argument_list|(
name|sig
argument_list|,
name|fn
argument_list|)
name|int
name|sig
decl_stmt|;
end_decl_stmt

begin_function_decl
name|RETSIGTYPE
function_decl|(
modifier|*
name|fn
function_decl|)
parameter_list|()
function_decl|;
end_function_decl

begin_block
block|{
name|int
name|val
decl_stmt|;
name|struct
name|SIG_hlist
modifier|*
name|this
decl_stmt|;
name|struct
name|SIG_hlist
modifier|*
name|last
decl_stmt|;
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|sigset_t
name|sigset_mask
decl_stmt|,
name|sigset_omask
decl_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|int
name|mask
decl_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* Initialize */
if|if
condition|(
name|SIG_init
argument_list|()
operator|!=
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|val
operator|=
literal|0
expr_stmt|;
name|last
operator|=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
expr_stmt|;
comment|/* Block this signal while we look at handler chain */
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigemptyset
argument_list|(
operator|&
name|sigset_mask
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigaddset
argument_list|(
operator|&
name|sigset_mask
argument_list|,
name|sig
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_BLOCK
argument_list|,
operator|&
name|sigset_mask
argument_list|,
operator|&
name|sigset_omask
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|mask
operator|=
name|sigblock
argument_list|(
name|sigmask
argument_list|(
name|sig
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* Search for the signal handler */
name|this
operator|=
name|SIG_handlers
index|[
name|sig
index|]
expr_stmt|;
while|while
condition|(
operator|(
name|this
operator|!=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
operator|)
operator|&&
operator|(
name|this
operator|->
name|handler
operator|!=
name|fn
operator|)
condition|)
block|{
name|last
operator|=
name|this
expr_stmt|;
name|this
operator|=
name|this
operator|->
name|next
expr_stmt|;
block|}
comment|/* If it was registered, remove it */
if|if
condition|(
name|this
operator|!=
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
if|if
condition|(
name|last
operator|==
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
name|SIG_handlers
index|[
name|sig
index|]
operator|=
name|this
operator|->
name|next
expr_stmt|;
block|}
else|else
block|{
name|last
operator|->
name|next
operator|=
name|this
operator|->
name|next
expr_stmt|;
block|}
name|free
argument_list|(
operator|(
name|char
operator|*
operator|)
name|this
argument_list|)
expr_stmt|;
block|}
comment|/* Restore default behavior if there are no registered handlers */
if|if
condition|(
name|SIG_handlers
index|[
name|sig
index|]
operator|==
operator|(
expr|struct
name|SIG_hlist
operator|*
operator|)
name|NULL
condition|)
block|{
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|val
operator|=
name|sigaction
argument_list|(
name|sig
argument_list|,
operator|&
name|SIG_defaults
index|[
name|sig
index|]
argument_list|,
operator|(
expr|struct
name|sigaction
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|val
operator|=
name|sigvec
argument_list|(
name|sig
argument_list|,
operator|&
name|SIG_defaults
index|[
name|sig
index|]
argument_list|,
operator|(
expr|struct
name|sigvec
operator|*
operator|)
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
if|if
condition|(
name|signal
argument_list|(
name|sig
argument_list|,
name|SIG_defaults
index|[
name|sig
index|]
argument_list|)
operator|==
name|SIG_ERR
condition|)
name|val
operator|=
operator|-
literal|1
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
block|}
comment|/* Unblock the signal */
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_SETMASK
argument_list|,
operator|&
name|sigset_omask
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
operator|(
name|void
operator|)
name|sigsetmask
argument_list|(
name|mask
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
return|return
name|val
return|;
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/*  * The following begins a critical section.  */
end_comment

begin_function
name|void
name|SIG_beginCrSect
parameter_list|()
block|{
if|if
condition|(
name|SIG_init
argument_list|()
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|SIG_crSectNest
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
name|sigset_t
name|sigset_mask
decl_stmt|;
operator|(
name|void
operator|)
name|sigfillset
argument_list|(
operator|&
name|sigset_mask
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_SETMASK
argument_list|,
operator|&
name|sigset_mask
argument_list|,
operator|&
name|SIG_crSectMask
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
name|SIG_crSectMask
operator|=
name|sigblock
argument_list|(
operator|~
literal|0
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* TBD */
endif|#
directive|endif
endif|#
directive|endif
block|}
name|SIG_crSectNest
operator|++
expr_stmt|;
block|}
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/*  * Return nonzero if currently in a critical section.  * Otherwise return zero.  */
end_comment

begin_function
name|int
name|SIG_inCrSect
parameter_list|()
block|{
return|return
name|SIG_crSectNest
operator|>
literal|0
return|;
block|}
end_function

begin_escape
end_escape

begin_comment
comment|/*  * The following ends a critical section.  */
end_comment

begin_function
name|void
name|SIG_endCrSect
parameter_list|()
block|{
if|if
condition|(
name|SIG_init
argument_list|()
operator|==
literal|0
condition|)
block|{
name|SIG_crSectNest
operator|--
expr_stmt|;
if|if
condition|(
name|SIG_crSectNest
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|POSIX_SIGNALS
operator|(
name|void
operator|)
name|sigprocmask
argument_list|(
name|SIG_SETMASK
argument_list|,
operator|&
name|SIG_crSectMask
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
else|#
directive|else
ifdef|#
directive|ifdef
name|BSD_SIGNALS
operator|(
name|void
operator|)
name|sigsetmask
argument_list|(
name|SIG_crSectMask
argument_list|)
expr_stmt|;
else|#
directive|else
comment|/* TBD */
endif|#
directive|endif
endif|#
directive|endif
block|}
block|}
block|}
end_function

end_unit

