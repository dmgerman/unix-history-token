begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * EAP peer: EAP-TLS/PEAP/TTLS/FAST common functions  * Copyright (c) 2004-2008, Jouni Malinen<j@w1.fi>  *  * This program is free software; you can redistribute it and/or modify  * it under the terms of the GNU General Public License version 2 as  * published by the Free Software Foundation.  *  * Alternatively, this software may be distributed under the terms of BSD  * license.  *  * See README and COPYING for more details.  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_include
include|#
directive|include
file|"eap_i.h"
end_include

begin_include
include|#
directive|include
file|"eap_tls_common.h"
end_include

begin_include
include|#
directive|include
file|"eap_config.h"
end_include

begin_include
include|#
directive|include
file|"sha1.h"
end_include

begin_include
include|#
directive|include
file|"tls.h"
end_include

begin_function
specifier|static
name|int
name|eap_tls_check_blob
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|name
parameter_list|,
specifier|const
name|u8
modifier|*
modifier|*
name|data
parameter_list|,
name|size_t
modifier|*
name|data_len
parameter_list|)
block|{
specifier|const
name|struct
name|wpa_config_blob
modifier|*
name|blob
decl_stmt|;
if|if
condition|(
operator|*
name|name
operator|==
name|NULL
operator|||
name|os_strncmp
argument_list|(
operator|*
name|name
argument_list|,
literal|"blob://"
argument_list|,
literal|7
argument_list|)
operator|!=
literal|0
condition|)
return|return
literal|0
return|;
name|blob
operator|=
name|eap_get_config_blob
argument_list|(
name|sm
argument_list|,
operator|*
name|name
operator|+
literal|7
argument_list|)
expr_stmt|;
if|if
condition|(
name|blob
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_ERROR
argument_list|,
literal|"%s: Named configuration blob '%s' not "
literal|"found"
argument_list|,
name|__func__
argument_list|,
operator|*
name|name
operator|+
literal|7
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
operator|*
name|name
operator|=
name|NULL
expr_stmt|;
operator|*
name|data
operator|=
name|blob
operator|->
name|data
expr_stmt|;
operator|*
name|data_len
operator|=
name|blob
operator|->
name|len
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
specifier|static
name|void
name|eap_tls_params_from_conf1
parameter_list|(
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|,
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|)
block|{
name|params
operator|->
name|ca_cert
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|ca_cert
expr_stmt|;
name|params
operator|->
name|ca_path
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|ca_path
expr_stmt|;
name|params
operator|->
name|client_cert
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|client_cert
expr_stmt|;
name|params
operator|->
name|private_key
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|private_key
expr_stmt|;
name|params
operator|->
name|private_key_passwd
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|private_key_passwd
expr_stmt|;
name|params
operator|->
name|dh_file
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|dh_file
expr_stmt|;
name|params
operator|->
name|subject_match
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|subject_match
expr_stmt|;
name|params
operator|->
name|altsubject_match
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|altsubject_match
expr_stmt|;
name|params
operator|->
name|engine
operator|=
name|config
operator|->
name|engine
expr_stmt|;
name|params
operator|->
name|engine_id
operator|=
name|config
operator|->
name|engine_id
expr_stmt|;
name|params
operator|->
name|pin
operator|=
name|config
operator|->
name|pin
expr_stmt|;
name|params
operator|->
name|key_id
operator|=
name|config
operator|->
name|key_id
expr_stmt|;
name|params
operator|->
name|cert_id
operator|=
name|config
operator|->
name|cert_id
expr_stmt|;
name|params
operator|->
name|ca_cert_id
operator|=
name|config
operator|->
name|ca_cert_id
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|void
name|eap_tls_params_from_conf2
parameter_list|(
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|,
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|)
block|{
name|params
operator|->
name|ca_cert
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|ca_cert2
expr_stmt|;
name|params
operator|->
name|ca_path
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|ca_path2
expr_stmt|;
name|params
operator|->
name|client_cert
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|client_cert2
expr_stmt|;
name|params
operator|->
name|private_key
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|private_key2
expr_stmt|;
name|params
operator|->
name|private_key_passwd
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|private_key2_passwd
expr_stmt|;
name|params
operator|->
name|dh_file
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|dh_file2
expr_stmt|;
name|params
operator|->
name|subject_match
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|subject_match2
expr_stmt|;
name|params
operator|->
name|altsubject_match
operator|=
operator|(
name|char
operator|*
operator|)
name|config
operator|->
name|altsubject_match2
expr_stmt|;
name|params
operator|->
name|engine
operator|=
name|config
operator|->
name|engine2
expr_stmt|;
name|params
operator|->
name|engine_id
operator|=
name|config
operator|->
name|engine2_id
expr_stmt|;
name|params
operator|->
name|pin
operator|=
name|config
operator|->
name|pin2
expr_stmt|;
name|params
operator|->
name|key_id
operator|=
name|config
operator|->
name|key2_id
expr_stmt|;
name|params
operator|->
name|cert_id
operator|=
name|config
operator|->
name|cert2_id
expr_stmt|;
name|params
operator|->
name|ca_cert_id
operator|=
name|config
operator|->
name|ca_cert2_id
expr_stmt|;
block|}
end_function

begin_function
specifier|static
name|int
name|eap_tls_params_from_conf
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|,
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|,
name|int
name|phase2
parameter_list|)
block|{
name|os_memset
argument_list|(
name|params
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|params
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|phase2
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: using phase2 config options"
argument_list|)
expr_stmt|;
name|eap_tls_params_from_conf2
argument_list|(
name|params
argument_list|,
name|config
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: using phase1 config options"
argument_list|)
expr_stmt|;
name|eap_tls_params_from_conf1
argument_list|(
name|params
argument_list|,
name|config
argument_list|)
expr_stmt|;
block|}
name|params
operator|->
name|tls_ia
operator|=
name|data
operator|->
name|tls_ia
expr_stmt|;
comment|/* 	 * Use blob data, if available. Otherwise, leave reference to external 	 * file as-is. 	 */
if|if
condition|(
name|eap_tls_check_blob
argument_list|(
name|sm
argument_list|,
operator|&
name|params
operator|->
name|ca_cert
argument_list|,
operator|&
name|params
operator|->
name|ca_cert_blob
argument_list|,
operator|&
name|params
operator|->
name|ca_cert_blob_len
argument_list|)
operator|||
name|eap_tls_check_blob
argument_list|(
name|sm
argument_list|,
operator|&
name|params
operator|->
name|client_cert
argument_list|,
operator|&
name|params
operator|->
name|client_cert_blob
argument_list|,
operator|&
name|params
operator|->
name|client_cert_blob_len
argument_list|)
operator|||
name|eap_tls_check_blob
argument_list|(
name|sm
argument_list|,
operator|&
name|params
operator|->
name|private_key
argument_list|,
operator|&
name|params
operator|->
name|private_key_blob
argument_list|,
operator|&
name|params
operator|->
name|private_key_blob_len
argument_list|)
operator|||
name|eap_tls_check_blob
argument_list|(
name|sm
argument_list|,
operator|&
name|params
operator|->
name|dh_file
argument_list|,
operator|&
name|params
operator|->
name|dh_blob
argument_list|,
operator|&
name|params
operator|->
name|dh_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Failed to get configuration blobs"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|eap_tls_init_connection
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|,
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|)
block|{
name|int
name|res
decl_stmt|;
name|data
operator|->
name|conn
operator|=
name|tls_connection_init
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|conn
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Failed to initialize new TLS "
literal|"connection"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|res
operator|=
name|tls_connection_set_params
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|params
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
name|TLS_SET_PARAMS_ENGINE_PRV_INIT_FAILED
condition|)
block|{
comment|/* 		 * At this point with the pkcs11 engine the PIN might be wrong. 		 * We reset the PIN in the configuration to be sure to not use 		 * it again and the calling function must request a new one. 		 */
name|os_free
argument_list|(
name|config
operator|->
name|pin
argument_list|)
expr_stmt|;
name|config
operator|->
name|pin
operator|=
name|NULL
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|res
operator|==
name|TLS_SET_PARAMS_ENGINE_PRV_VERIFY_FAILED
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to load private key"
argument_list|)
expr_stmt|;
comment|/* 		 * We do not know exactly but maybe the PIN was wrong, 		 * so ask for a new one. 		 */
name|os_free
argument_list|(
name|config
operator|->
name|pin
argument_list|)
expr_stmt|;
name|config
operator|->
name|pin
operator|=
name|NULL
expr_stmt|;
name|eap_sm_request_pin
argument_list|(
name|sm
argument_list|)
expr_stmt|;
name|sm
operator|->
name|ignore
operator|=
name|TRUE
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
elseif|else
if|if
condition|(
name|res
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to set TLS connection "
literal|"parameters"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_ssl_init - Initialize shared TLS functionality  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @config: Pointer to the network configuration  * Returns: 0 on success, -1 on failure  *  * This function is used to initialize shared TLS functionality for EAP-TLS,  * EAP-PEAP, EAP-TTLS, and EAP-FAST.  */
end_comment

begin_function
name|int
name|eap_peer_tls_ssl_init
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|)
block|{
name|struct
name|tls_connection_params
name|params
decl_stmt|;
if|if
condition|(
name|config
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|data
operator|->
name|eap
operator|=
name|sm
expr_stmt|;
name|data
operator|->
name|phase2
operator|=
name|sm
operator|->
name|init_phase2
expr_stmt|;
if|if
condition|(
name|eap_tls_params_from_conf
argument_list|(
name|sm
argument_list|,
name|data
argument_list|,
operator|&
name|params
argument_list|,
name|config
argument_list|,
name|data
operator|->
name|phase2
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|eap_tls_init_connection
argument_list|(
name|sm
argument_list|,
name|data
argument_list|,
name|config
argument_list|,
operator|&
name|params
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|-
literal|1
return|;
name|data
operator|->
name|tls_out_limit
operator|=
name|config
operator|->
name|fragment_size
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|phase2
condition|)
block|{
comment|/* Limit the fragment size in the inner TLS authentication 		 * since the outer authentication with EAP-PEAP does not yet 		 * support fragmentation */
if|if
condition|(
name|data
operator|->
name|tls_out_limit
operator|>
literal|100
condition|)
name|data
operator|->
name|tls_out_limit
operator|-=
literal|100
expr_stmt|;
block|}
if|if
condition|(
name|config
operator|->
name|phase1
operator|&&
name|os_strstr
argument_list|(
name|config
operator|->
name|phase1
argument_list|,
literal|"include_tls_length=1"
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Include TLS Message Length in "
literal|"unfragmented packets"
argument_list|)
expr_stmt|;
name|data
operator|->
name|include_tls_length
operator|=
literal|1
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_ssl_deinit - Deinitialize shared TLS functionality  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  *  * This function deinitializes shared TLS functionality that was initialized  * with eap_peer_tls_ssl_init().  */
end_comment

begin_function
name|void
name|eap_peer_tls_ssl_deinit
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|)
block|{
name|tls_connection_deinit
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_derive_key - Derive a key based on TLS session data  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @label: Label string for deriving the keys, e.g., "client EAP encryption"  * @len: Length of the key material to generate (usually 64 for MSK)  * Returns: Pointer to allocated key on success or %NULL on failure  *  * This function uses TLS-PRF to generate pseudo-random data based on the TLS  * session data (client/server random and master key). Each key type may use a  * different label to bind the key usage into the generated material.  *  * The caller is responsible for freeing the returned buffer.  */
end_comment

begin_function
name|u8
modifier|*
name|eap_peer_tls_derive_key
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
specifier|const
name|char
modifier|*
name|label
parameter_list|,
name|size_t
name|len
parameter_list|)
block|{
name|struct
name|tls_keys
name|keys
decl_stmt|;
name|u8
modifier|*
name|rnd
init|=
name|NULL
decl_stmt|,
modifier|*
name|out
decl_stmt|;
name|out
operator|=
name|os_malloc
argument_list|(
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|out
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
comment|/* First, try to use TLS library function for PRF, if available. */
if|if
condition|(
name|tls_connection_prf
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|label
argument_list|,
literal|0
argument_list|,
name|out
argument_list|,
name|len
argument_list|)
operator|==
literal|0
condition|)
return|return
name|out
return|;
comment|/* 	 * TLS library did not support key generation, so get the needed TLS 	 * session parameters and use an internal implementation of TLS PRF to 	 * derive the key. 	 */
if|if
condition|(
name|tls_connection_get_keys
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
operator|&
name|keys
argument_list|)
condition|)
goto|goto
name|fail
goto|;
if|if
condition|(
name|keys
operator|.
name|client_random
operator|==
name|NULL
operator|||
name|keys
operator|.
name|server_random
operator|==
name|NULL
operator|||
name|keys
operator|.
name|master_key
operator|==
name|NULL
condition|)
goto|goto
name|fail
goto|;
name|rnd
operator|=
name|os_malloc
argument_list|(
name|keys
operator|.
name|client_random_len
operator|+
name|keys
operator|.
name|server_random_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|rnd
operator|==
name|NULL
condition|)
goto|goto
name|fail
goto|;
name|os_memcpy
argument_list|(
name|rnd
argument_list|,
name|keys
operator|.
name|client_random
argument_list|,
name|keys
operator|.
name|client_random_len
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|rnd
operator|+
name|keys
operator|.
name|client_random_len
argument_list|,
name|keys
operator|.
name|server_random
argument_list|,
name|keys
operator|.
name|server_random_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|tls_prf
argument_list|(
name|keys
operator|.
name|master_key
argument_list|,
name|keys
operator|.
name|master_key_len
argument_list|,
name|label
argument_list|,
name|rnd
argument_list|,
name|keys
operator|.
name|client_random_len
operator|+
name|keys
operator|.
name|server_random_len
argument_list|,
name|out
argument_list|,
name|len
argument_list|)
condition|)
goto|goto
name|fail
goto|;
name|os_free
argument_list|(
name|rnd
argument_list|)
expr_stmt|;
return|return
name|out
return|;
name|fail
label|:
name|os_free
argument_list|(
name|out
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|rnd
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_reassemble_fragment - Reassemble a received fragment  * @data: Data for TLS processing  * @in_data: Next incoming TLS segment  * @in_len: Length of in_data  * Returns: 0 on success, 1 if more data is needed for the full message, or  * -1 on error  */
end_comment

begin_function
specifier|static
name|int
name|eap_peer_tls_reassemble_fragment
parameter_list|(
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|)
block|{
name|u8
modifier|*
name|buf
decl_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in_len
operator|+
name|in_len
operator|==
literal|0
condition|)
block|{
comment|/* No message data received?! */
name|wpa_printf
argument_list|(
name|MSG_WARNING
argument_list|,
literal|"SSL: Invalid reassembly state: "
literal|"tls_in_left=%lu tls_in_len=%lu in_len=%lu"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|data
operator|->
name|tls_in_left
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|data
operator|->
name|tls_in_len
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|in_len
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|data
operator|->
name|tls_in_len
operator|+
name|in_len
operator|>
literal|65536
condition|)
block|{
comment|/* 		 * Limit length to avoid rogue servers from causing large 		 * memory allocations. 		 */
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Too long TLS fragment (size over "
literal|"64 kB)"
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|in_len
operator|>
name|data
operator|->
name|tls_in_left
condition|)
block|{
comment|/* Sender is doing something odd - reject message */
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: more data than TLS message length "
literal|"indicated"
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|buf
operator|=
name|os_realloc
argument_list|(
name|data
operator|->
name|tls_in
argument_list|,
name|data
operator|->
name|tls_in_len
operator|+
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Could not allocate memory for TLS "
literal|"data"
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|os_memcpy
argument_list|(
name|buf
operator|+
name|data
operator|->
name|tls_in_len
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_in
operator|=
name|buf
expr_stmt|;
name|data
operator|->
name|tls_in_len
operator|+=
name|in_len
expr_stmt|;
name|data
operator|->
name|tls_in_left
operator|-=
name|in_len
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in_left
operator|>
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Need %lu bytes more input "
literal|"data"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|data
operator|->
name|tls_in_left
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_data_reassemble - Reassemble TLS data  * @data: Data for TLS processing  * @in_data: Next incoming TLS segment  * @in_len: Length of in_data  * @out_len: Variable for returning length of the reassembled message  * @need_more_input: Variable for returning whether more input data is needed  * to reassemble this TLS packet  * Returns: Pointer to output data, %NULL on error or when more data is needed  * for the full message (in which case, *need_more_input is also set to 1).  *  * This function reassembles TLS fragments. Caller must not free the returned  * data buffer since an internal pointer to it is maintained.  */
end_comment

begin_function
specifier|const
name|u8
modifier|*
name|eap_peer_tls_data_reassemble
parameter_list|(
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|size_t
modifier|*
name|out_len
parameter_list|,
name|int
modifier|*
name|need_more_input
parameter_list|)
block|{
operator|*
name|need_more_input
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in_left
operator|>
name|in_len
operator|||
name|data
operator|->
name|tls_in
condition|)
block|{
comment|/* Message has fragments */
name|int
name|res
init|=
name|eap_peer_tls_reassemble_fragment
argument_list|(
name|data
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
decl_stmt|;
if|if
condition|(
name|res
condition|)
block|{
if|if
condition|(
name|res
operator|==
literal|1
condition|)
operator|*
name|need_more_input
operator|=
literal|1
expr_stmt|;
return|return
name|NULL
return|;
block|}
comment|/* Message is now fully reassembled. */
block|}
else|else
block|{
comment|/* No fragments in this message, so just make a copy of it. */
name|data
operator|->
name|tls_in_left
operator|=
literal|0
expr_stmt|;
name|data
operator|->
name|tls_in
operator|=
name|os_malloc
argument_list|(
name|in_len
condition|?
name|in_len
else|:
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|os_memcpy
argument_list|(
name|data
operator|->
name|tls_in
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_in_len
operator|=
name|in_len
expr_stmt|;
block|}
operator|*
name|out_len
operator|=
name|data
operator|->
name|tls_in_len
expr_stmt|;
return|return
name|data
operator|->
name|tls_in
return|;
block|}
end_function

begin_comment
comment|/**  * eap_tls_process_input - Process incoming TLS message  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @in_data: Message received from the server  * @in_len: Length of in_data  * @out_data: Buffer for returning a pointer to application data (if available)  * Returns: 0 on success, 1 if more input data is needed, 2 if application data  * is available, -1 on failure  */
end_comment

begin_function
specifier|static
name|int
name|eap_tls_process_input
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|out_data
parameter_list|)
block|{
specifier|const
name|u8
modifier|*
name|msg
decl_stmt|;
name|size_t
name|msg_len
decl_stmt|;
name|int
name|need_more_input
decl_stmt|;
name|u8
modifier|*
name|appl_data
decl_stmt|;
name|size_t
name|appl_data_len
decl_stmt|;
name|msg
operator|=
name|eap_peer_tls_data_reassemble
argument_list|(
name|data
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|,
operator|&
name|msg_len
argument_list|,
operator|&
name|need_more_input
argument_list|)
expr_stmt|;
if|if
condition|(
name|msg
operator|==
name|NULL
condition|)
return|return
name|need_more_input
condition|?
literal|1
else|:
operator|-
literal|1
return|;
comment|/* Full TLS message reassembled - continue handshake processing */
if|if
condition|(
name|data
operator|->
name|tls_out
condition|)
block|{
comment|/* This should not happen.. */
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: eap_tls_process_input - pending "
literal|"tls_out data even though tls_out_len = 0"
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|data
operator|->
name|tls_out
argument_list|)
expr_stmt|;
name|WPA_ASSERT
argument_list|(
name|data
operator|->
name|tls_out
operator|==
name|NULL
argument_list|)
expr_stmt|;
block|}
name|appl_data
operator|=
name|NULL
expr_stmt|;
name|data
operator|->
name|tls_out
operator|=
name|tls_connection_handshake
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|msg
argument_list|,
name|msg_len
argument_list|,
operator|&
name|data
operator|->
name|tls_out_len
argument_list|,
operator|&
name|appl_data
argument_list|,
operator|&
name|appl_data_len
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|appl_data
operator|&&
name|tls_connection_established
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
operator|&&
operator|!
name|tls_connection_get_failed
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
condition|)
block|{
name|wpa_hexdump_key
argument_list|(
name|MSG_MSGDUMP
argument_list|,
literal|"SSL: Application data"
argument_list|,
name|appl_data
argument_list|,
name|appl_data_len
argument_list|)
expr_stmt|;
operator|*
name|out_data
operator|=
name|wpabuf_alloc_ext_data
argument_list|(
name|appl_data
argument_list|,
name|appl_data_len
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|out_data
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|appl_data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
literal|2
return|;
block|}
name|os_free
argument_list|(
name|appl_data
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_tls_process_output - Process outgoing TLS message  * @data: Data for TLS processing  * @eap_type: EAP type (EAP_TYPE_TLS, EAP_TYPE_PEAP, ...)  * @peap_version: Version number for EAP-PEAP/TTLS  * @id: EAP identifier for the response  * @ret: Return value to use on success  * @out_data: Buffer for returning the allocated output buffer  * Returns: ret (0 or 1) on success, -1 on failure  */
end_comment

begin_function
specifier|static
name|int
name|eap_tls_process_output
parameter_list|(
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|EapType
name|eap_type
parameter_list|,
name|int
name|peap_version
parameter_list|,
name|u8
name|id
parameter_list|,
name|int
name|ret
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|out_data
parameter_list|)
block|{
name|size_t
name|len
decl_stmt|;
name|u8
modifier|*
name|flags
decl_stmt|;
name|int
name|more_fragments
decl_stmt|,
name|length_included
decl_stmt|;
name|len
operator|=
name|data
operator|->
name|tls_out_len
operator|-
name|data
operator|->
name|tls_out_pos
expr_stmt|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: %lu bytes left to be sent out (of total "
literal|"%lu bytes)"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|len
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|data
operator|->
name|tls_out_len
argument_list|)
expr_stmt|;
comment|/* 	 * Limit outgoing message to the configured maximum size. Fragment 	 * message if needed. 	 */
if|if
condition|(
name|len
operator|>
name|data
operator|->
name|tls_out_limit
condition|)
block|{
name|more_fragments
operator|=
literal|1
expr_stmt|;
name|len
operator|=
name|data
operator|->
name|tls_out_limit
expr_stmt|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: sending %lu bytes, more fragments "
literal|"will follow"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|len
argument_list|)
expr_stmt|;
block|}
else|else
name|more_fragments
operator|=
literal|0
expr_stmt|;
name|length_included
operator|=
name|data
operator|->
name|tls_out_pos
operator|==
literal|0
operator|&&
operator|(
name|data
operator|->
name|tls_out_len
operator|>
name|data
operator|->
name|tls_out_limit
operator|||
name|data
operator|->
name|include_tls_length
operator|)
expr_stmt|;
if|if
condition|(
operator|!
name|length_included
operator|&&
name|eap_type
operator|==
name|EAP_TYPE_PEAP
operator|&&
name|peap_version
operator|==
literal|0
operator|&&
operator|!
name|tls_connection_established
argument_list|(
name|data
operator|->
name|eap
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
condition|)
block|{
comment|/* 		 * Windows Server 2008 NPS really wants to have the TLS Message 		 * length included in phase 0 even for unfragmented frames or 		 * it will get very confused with Compound MAC calculation and 		 * Outer TLVs. 		 */
name|length_included
operator|=
literal|1
expr_stmt|;
block|}
operator|*
name|out_data
operator|=
name|eap_msg_alloc
argument_list|(
name|EAP_VENDOR_IETF
argument_list|,
name|eap_type
argument_list|,
literal|1
operator|+
name|length_included
operator|*
literal|4
operator|+
name|len
argument_list|,
name|EAP_CODE_RESPONSE
argument_list|,
name|id
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|out_data
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|flags
operator|=
name|wpabuf_put
argument_list|(
operator|*
name|out_data
argument_list|,
literal|1
argument_list|)
expr_stmt|;
operator|*
name|flags
operator|=
name|peap_version
expr_stmt|;
if|if
condition|(
name|more_fragments
condition|)
operator|*
name|flags
operator||=
name|EAP_TLS_FLAGS_MORE_FRAGMENTS
expr_stmt|;
if|if
condition|(
name|length_included
condition|)
block|{
operator|*
name|flags
operator||=
name|EAP_TLS_FLAGS_LENGTH_INCLUDED
expr_stmt|;
name|wpabuf_put_be32
argument_list|(
operator|*
name|out_data
argument_list|,
name|data
operator|->
name|tls_out_len
argument_list|)
expr_stmt|;
block|}
name|wpabuf_put_data
argument_list|(
operator|*
name|out_data
argument_list|,
operator|&
name|data
operator|->
name|tls_out
index|[
name|data
operator|->
name|tls_out_pos
index|]
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_out_pos
operator|+=
name|len
expr_stmt|;
if|if
condition|(
operator|!
name|more_fragments
condition|)
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_process_helper - Process TLS handshake message  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @eap_type: EAP type (EAP_TYPE_TLS, EAP_TYPE_PEAP, ...)  * @peap_version: Version number for EAP-PEAP/TTLS  * @id: EAP identifier for the response  * @in_data: Message received from the server  * @in_len: Length of in_data  * @out_data: Buffer for returning a pointer to the response message  * Returns: 0 on success, 1 if more input data is needed, 2 if application data  * is available, or -1 on failure  *  * This function can be used to process TLS handshake messages. It reassembles  * the received fragments and uses a TLS library to process the messages. The  * response data from the TLS library is fragmented to suitable output messages  * that the caller can send out.  *  * out_data is used to return the response message if the return value of this  * function is 0, 2, or -1. In case of failure, the message is likely a TLS  * alarm message. The caller is responsible for freeing the allocated buffer if  * *out_data is not %NULL.  *  * This function is called for each received TLS message during the TLS  * handshake after eap_peer_tls_process_init() call and possible processing of  * TLS Flags field. Once the handshake has been completed, i.e., when  * tls_connection_established() returns 1, EAP method specific decrypting of  * the tunneled data is used.  */
end_comment

begin_function
name|int
name|eap_peer_tls_process_helper
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|EapType
name|eap_type
parameter_list|,
name|int
name|peap_version
parameter_list|,
name|u8
name|id
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|out_data
parameter_list|)
block|{
name|int
name|ret
init|=
literal|0
decl_stmt|;
operator|*
name|out_data
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_out_len
operator|>
literal|0
operator|&&
name|in_len
operator|>
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Received non-ACK when output "
literal|"fragments are waiting to be sent out"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|data
operator|->
name|tls_out_len
operator|==
literal|0
condition|)
block|{
comment|/* 		 * No more data to send out - expect to receive more data from 		 * the AS. 		 */
name|int
name|res
init|=
name|eap_tls_process_input
argument_list|(
name|sm
argument_list|,
name|data
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|,
name|out_data
argument_list|)
decl_stmt|;
if|if
condition|(
name|res
condition|)
block|{
comment|/* 			 * Input processing failed (res = -1) or more data is 			 * needed (res = 1). 			 */
return|return
name|res
return|;
block|}
comment|/* 		 * The incoming message has been reassembled and processed. The 		 * response was allocated into data->tls_out buffer. 		 */
block|}
if|if
condition|(
name|data
operator|->
name|tls_out
operator|==
name|NULL
condition|)
block|{
comment|/* 		 * No outgoing fragments remaining from the previous message 		 * and no new message generated. This indicates an error in TLS 		 * processing. 		 */
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tls_connection_get_failed
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
condition|)
block|{
comment|/* TLS processing has failed - return error */
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Failed - tls_out available to "
literal|"report error"
argument_list|)
expr_stmt|;
name|ret
operator|=
operator|-
literal|1
expr_stmt|;
comment|/* TODO: clean pin if engine used? */
block|}
if|if
condition|(
name|data
operator|->
name|tls_out_len
operator|==
literal|0
condition|)
block|{
comment|/* 		 * TLS negotiation should now be complete since all other cases 		 * needing more data should have been caught above based on 		 * the TLS Message Length field. 		 */
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: No data to be sent out"
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|data
operator|->
name|tls_out
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_out
operator|=
name|NULL
expr_stmt|;
return|return
literal|1
return|;
block|}
comment|/* Send the pending message (in fragments, if needed). */
return|return
name|eap_tls_process_output
argument_list|(
name|data
argument_list|,
name|eap_type
argument_list|,
name|peap_version
argument_list|,
name|id
argument_list|,
name|ret
argument_list|,
name|out_data
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_build_ack - Build a TLS ACK frame  * @id: EAP identifier for the response  * @eap_type: EAP type (EAP_TYPE_TLS, EAP_TYPE_PEAP, ...)  * @peap_version: Version number for EAP-PEAP/TTLS  * Returns: Pointer to the allocated ACK frame or %NULL on failure  */
end_comment

begin_function
name|struct
name|wpabuf
modifier|*
name|eap_peer_tls_build_ack
parameter_list|(
name|u8
name|id
parameter_list|,
name|EapType
name|eap_type
parameter_list|,
name|int
name|peap_version
parameter_list|)
block|{
name|struct
name|wpabuf
modifier|*
name|resp
decl_stmt|;
name|resp
operator|=
name|eap_msg_alloc
argument_list|(
name|EAP_VENDOR_IETF
argument_list|,
name|eap_type
argument_list|,
literal|1
argument_list|,
name|EAP_CODE_RESPONSE
argument_list|,
name|id
argument_list|)
expr_stmt|;
if|if
condition|(
name|resp
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Building ACK (type=%d id=%d ver=%d)"
argument_list|,
operator|(
name|int
operator|)
name|eap_type
argument_list|,
name|id
argument_list|,
name|peap_version
argument_list|)
expr_stmt|;
name|wpabuf_put_u8
argument_list|(
name|resp
argument_list|,
name|peap_version
argument_list|)
expr_stmt|;
comment|/* Flags */
return|return
name|resp
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_reauth_init - Re-initialize shared TLS for session resumption  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|eap_peer_tls_reauth_init
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|)
block|{
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
name|tls_connection_shutdown
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_status - Get TLS status  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @buf: Buffer for status information  * @buflen: Maximum buffer length  * @verbose: Whether to include verbose status information  * Returns: Number of bytes written to buf.  */
end_comment

begin_function
name|int
name|eap_peer_tls_status
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|buflen
parameter_list|,
name|int
name|verbose
parameter_list|)
block|{
name|char
name|name
index|[
literal|128
index|]
decl_stmt|;
name|int
name|len
init|=
literal|0
decl_stmt|,
name|ret
decl_stmt|;
if|if
condition|(
name|tls_get_cipher
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|name
argument_list|,
sizeof|sizeof
argument_list|(
name|name
argument_list|)
argument_list|)
operator|==
literal|0
condition|)
block|{
name|ret
operator|=
name|os_snprintf
argument_list|(
name|buf
operator|+
name|len
argument_list|,
name|buflen
operator|-
name|len
argument_list|,
literal|"EAP TLS cipher=%s\n"
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|ret
operator|<
literal|0
operator|||
operator|(
name|size_t
operator|)
name|ret
operator|>=
name|buflen
operator|-
name|len
condition|)
return|return
name|len
return|;
name|len
operator|+=
name|ret
expr_stmt|;
block|}
return|return
name|len
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_process_init - Initial validation/processing of EAP requests  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @eap_type: EAP type (EAP_TYPE_TLS, EAP_TYPE_PEAP, ...)  * @ret: Return values from EAP request validation and processing  * @reqData: EAP request to be processed (eapReqData)  * @len: Buffer for returning length of the remaining payload  * @flags: Buffer for returning TLS flags  * Returns: Pointer to payload after TLS flags and length or %NULL on failure  *  * This function validates the EAP header and processes the optional TLS  * Message Length field. If this is the first fragment of a TLS message, the  * TLS reassembly code is initialized to receive the indicated number of bytes.  *  * EAP-TLS, EAP-PEAP, EAP-TTLS, and EAP-FAST methods are expected to use this  * function as the first step in processing received messages. They will need  * to process the flags (apart from Message Length Included) that are returned  * through the flags pointer and the message payload that will be returned (and  * the length is returned through the len pointer). Return values (ret) are set  * for continuation of EAP method processing. The caller is responsible for  * setting these to indicate completion (either success or failure) based on  * the authentication result.  */
end_comment

begin_function
specifier|const
name|u8
modifier|*
name|eap_peer_tls_process_init
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|EapType
name|eap_type
parameter_list|,
name|struct
name|eap_method_ret
modifier|*
name|ret
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|reqData
parameter_list|,
name|size_t
modifier|*
name|len
parameter_list|,
name|u8
modifier|*
name|flags
parameter_list|)
block|{
specifier|const
name|u8
modifier|*
name|pos
decl_stmt|;
name|size_t
name|left
decl_stmt|;
name|unsigned
name|int
name|tls_msg_len
decl_stmt|;
if|if
condition|(
name|tls_get_errors
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: TLS errors detected"
argument_list|)
expr_stmt|;
name|ret
operator|->
name|ignore
operator|=
name|TRUE
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|pos
operator|=
name|eap_hdr_validate
argument_list|(
name|EAP_VENDOR_IETF
argument_list|,
name|eap_type
argument_list|,
name|reqData
argument_list|,
operator|&
name|left
argument_list|)
expr_stmt|;
if|if
condition|(
name|pos
operator|==
name|NULL
condition|)
block|{
name|ret
operator|->
name|ignore
operator|=
name|TRUE
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|left
operator|==
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Invalid TLS message: no Flags "
literal|"octet included"
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|sm
operator|->
name|workaround
condition|)
block|{
name|ret
operator|->
name|ignore
operator|=
name|TRUE
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Workaround - assume no Flags "
literal|"indicates ACK frame"
argument_list|)
expr_stmt|;
operator|*
name|flags
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
operator|*
name|flags
operator|=
operator|*
name|pos
operator|++
expr_stmt|;
name|left
operator|--
expr_stmt|;
block|}
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: Received packet(len=%lu) - "
literal|"Flags 0x%02x"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|wpabuf_len
argument_list|(
name|reqData
argument_list|)
argument_list|,
operator|*
name|flags
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|flags
operator|&
name|EAP_TLS_FLAGS_LENGTH_INCLUDED
condition|)
block|{
if|if
condition|(
name|left
operator|<
literal|4
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Short frame with TLS "
literal|"length"
argument_list|)
expr_stmt|;
name|ret
operator|->
name|ignore
operator|=
name|TRUE
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|tls_msg_len
operator|=
name|WPA_GET_BE32
argument_list|(
name|pos
argument_list|)
expr_stmt|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"SSL: TLS Message Length: %d"
argument_list|,
name|tls_msg_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in_left
operator|==
literal|0
condition|)
block|{
name|data
operator|->
name|tls_in_total
operator|=
name|tls_msg_len
expr_stmt|;
name|data
operator|->
name|tls_in_left
operator|=
name|tls_msg_len
expr_stmt|;
name|os_free
argument_list|(
name|data
operator|->
name|tls_in
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_in
operator|=
name|NULL
expr_stmt|;
name|data
operator|->
name|tls_in_len
operator|=
literal|0
expr_stmt|;
block|}
name|pos
operator|+=
literal|4
expr_stmt|;
name|left
operator|-=
literal|4
expr_stmt|;
block|}
name|ret
operator|->
name|ignore
operator|=
name|FALSE
expr_stmt|;
name|ret
operator|->
name|methodState
operator|=
name|METHOD_MAY_CONT
expr_stmt|;
name|ret
operator|->
name|decision
operator|=
name|DECISION_FAIL
expr_stmt|;
name|ret
operator|->
name|allowNotifications
operator|=
name|TRUE
expr_stmt|;
operator|*
name|len
operator|=
name|left
expr_stmt|;
return|return
name|pos
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_reset_input - Reset input buffers  * @data: Data for TLS processing  *  * This function frees any allocated memory for input buffers and resets input  * state.  */
end_comment

begin_function
name|void
name|eap_peer_tls_reset_input
parameter_list|(
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|)
block|{
name|data
operator|->
name|tls_in_left
operator|=
name|data
operator|->
name|tls_in_total
operator|=
name|data
operator|->
name|tls_in_len
operator|=
literal|0
expr_stmt|;
name|os_free
argument_list|(
name|data
operator|->
name|tls_in
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_in
operator|=
name|NULL
expr_stmt|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_reset_output - Reset output buffers  * @data: Data for TLS processing  *  * This function frees any allocated memory for output buffers and resets  * output state.  */
end_comment

begin_function
name|void
name|eap_peer_tls_reset_output
parameter_list|(
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|)
block|{
name|data
operator|->
name|tls_out_len
operator|=
literal|0
expr_stmt|;
name|data
operator|->
name|tls_out_pos
operator|=
literal|0
expr_stmt|;
name|os_free
argument_list|(
name|data
operator|->
name|tls_out
argument_list|)
expr_stmt|;
name|data
operator|->
name|tls_out
operator|=
name|NULL
expr_stmt|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_decrypt - Decrypt received phase 2 TLS message  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @in_data: Message received from the server  * @in_decrypted: Buffer for returning a pointer to the decrypted message  * Returns: 0 on success, 1 if more input data is needed, or -1 on failure  */
end_comment

begin_function
name|int
name|eap_peer_tls_decrypt
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|in_decrypted
parameter_list|)
block|{
name|int
name|res
decl_stmt|;
specifier|const
name|u8
modifier|*
name|msg
decl_stmt|;
name|size_t
name|msg_len
decl_stmt|,
name|buf_len
decl_stmt|;
name|int
name|need_more_input
decl_stmt|;
name|msg
operator|=
name|eap_peer_tls_data_reassemble
argument_list|(
name|data
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
operator|&
name|msg_len
argument_list|,
operator|&
name|need_more_input
argument_list|)
expr_stmt|;
if|if
condition|(
name|msg
operator|==
name|NULL
condition|)
return|return
name|need_more_input
condition|?
literal|1
else|:
operator|-
literal|1
return|;
name|buf_len
operator|=
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_in_total
operator|>
name|buf_len
condition|)
name|buf_len
operator|=
name|data
operator|->
name|tls_in_total
expr_stmt|;
comment|/* 	 * Even though we try to disable TLS compression, it is possible that 	 * this cannot be done with all TLS libraries. Add extra buffer space 	 * to handle the possibility of the decrypted data being longer than 	 * input data. 	 */
name|buf_len
operator|+=
literal|500
expr_stmt|;
name|buf_len
operator|*=
literal|3
expr_stmt|;
operator|*
name|in_decrypted
operator|=
name|wpabuf_alloc
argument_list|(
name|buf_len
condition|?
name|buf_len
else|:
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|in_decrypted
operator|==
name|NULL
condition|)
block|{
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
name|wpa_printf
argument_list|(
name|MSG_WARNING
argument_list|,
literal|"SSL: Failed to allocate memory for "
literal|"decryption"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|res
operator|=
name|tls_connection_decrypt
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|msg
argument_list|,
name|msg_len
argument_list|,
name|wpabuf_mhead
argument_list|(
operator|*
name|in_decrypted
argument_list|)
argument_list|,
name|buf_len
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_input
argument_list|(
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Failed to decrypt Phase 2 data"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|wpabuf_put
argument_list|(
operator|*
name|in_decrypted
argument_list|,
name|res
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_encrypt - Encrypt phase 2 TLS message  * @sm: Pointer to EAP state machine allocated with eap_peer_sm_init()  * @data: Data for TLS processing  * @eap_type: EAP type (EAP_TYPE_TLS, EAP_TYPE_PEAP, ...)  * @peap_version: Version number for EAP-PEAP/TTLS  * @id: EAP identifier for the response  * @in_data: Plaintext phase 2 data to encrypt or %NULL to continue fragments  * @out_data: Buffer for returning a pointer to the encrypted response message  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|eap_peer_tls_encrypt
parameter_list|(
name|struct
name|eap_sm
modifier|*
name|sm
parameter_list|,
name|struct
name|eap_ssl_data
modifier|*
name|data
parameter_list|,
name|EapType
name|eap_type
parameter_list|,
name|int
name|peap_version
parameter_list|,
name|u8
name|id
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|out_data
parameter_list|)
block|{
name|int
name|res
decl_stmt|;
name|size_t
name|len
decl_stmt|;
if|if
condition|(
name|in_data
condition|)
block|{
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
name|len
operator|=
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
operator|+
literal|300
expr_stmt|;
name|data
operator|->
name|tls_out
operator|=
name|os_malloc
argument_list|(
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|data
operator|->
name|tls_out
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|res
operator|=
name|tls_connection_encrypt
argument_list|(
name|sm
operator|->
name|ssl_ctx
argument_list|,
name|data
operator|->
name|conn
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|data
operator|->
name|tls_out
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"SSL: Failed to encrypt Phase 2 "
literal|"data (in_len=%lu)"
argument_list|,
operator|(
name|unsigned
name|long
operator|)
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|)
expr_stmt|;
name|eap_peer_tls_reset_output
argument_list|(
name|data
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|data
operator|->
name|tls_out_len
operator|=
name|res
expr_stmt|;
block|}
return|return
name|eap_tls_process_output
argument_list|(
name|data
argument_list|,
name|eap_type
argument_list|,
name|peap_version
argument_list|,
name|id
argument_list|,
literal|0
argument_list|,
name|out_data
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_select_phase2_methods - Select phase 2 EAP method  * @config: Pointer to the network configuration  * @prefix: 'phase2' configuration prefix, e.g., "auth="  * @types: Buffer for returning allocated list of allowed EAP methods  * @num_types: Buffer for returning number of allocated EAP methods  * Returns: 0 on success, -1 on failure  *  * This function is used to parse EAP method list and select allowed methods  * for Phase2 authentication.  */
end_comment

begin_function
name|int
name|eap_peer_select_phase2_methods
parameter_list|(
name|struct
name|eap_peer_config
modifier|*
name|config
parameter_list|,
specifier|const
name|char
modifier|*
name|prefix
parameter_list|,
name|struct
name|eap_method_type
modifier|*
modifier|*
name|types
parameter_list|,
name|size_t
modifier|*
name|num_types
parameter_list|)
block|{
name|char
modifier|*
name|start
decl_stmt|,
modifier|*
name|pos
decl_stmt|,
modifier|*
name|buf
decl_stmt|;
name|struct
name|eap_method_type
modifier|*
name|methods
init|=
name|NULL
decl_stmt|,
modifier|*
name|_methods
decl_stmt|;
name|u8
name|method
decl_stmt|;
name|size_t
name|num_methods
init|=
literal|0
decl_stmt|,
name|prefix_len
decl_stmt|;
if|if
condition|(
name|config
operator|==
name|NULL
operator|||
name|config
operator|->
name|phase2
operator|==
name|NULL
condition|)
goto|goto
name|get_defaults
goto|;
name|start
operator|=
name|buf
operator|=
name|os_strdup
argument_list|(
name|config
operator|->
name|phase2
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|prefix_len
operator|=
name|os_strlen
argument_list|(
name|prefix
argument_list|)
expr_stmt|;
while|while
condition|(
name|start
operator|&&
operator|*
name|start
operator|!=
literal|'\0'
condition|)
block|{
name|int
name|vendor
decl_stmt|;
name|pos
operator|=
name|os_strstr
argument_list|(
name|start
argument_list|,
name|prefix
argument_list|)
expr_stmt|;
if|if
condition|(
name|pos
operator|==
name|NULL
condition|)
break|break;
if|if
condition|(
name|start
operator|!=
name|pos
operator|&&
operator|*
operator|(
name|pos
operator|-
literal|1
operator|)
operator|!=
literal|' '
condition|)
block|{
name|start
operator|=
name|pos
operator|+
name|prefix_len
expr_stmt|;
continue|continue;
block|}
name|start
operator|=
name|pos
operator|+
name|prefix_len
expr_stmt|;
name|pos
operator|=
name|os_strchr
argument_list|(
name|start
argument_list|,
literal|' '
argument_list|)
expr_stmt|;
if|if
condition|(
name|pos
condition|)
operator|*
name|pos
operator|++
operator|=
literal|'\0'
expr_stmt|;
name|method
operator|=
name|eap_get_phase2_type
argument_list|(
name|start
argument_list|,
operator|&
name|vendor
argument_list|)
expr_stmt|;
if|if
condition|(
name|vendor
operator|==
name|EAP_VENDOR_IETF
operator|&&
name|method
operator|==
name|EAP_TYPE_NONE
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_ERROR
argument_list|,
literal|"TLS: Unsupported Phase2 EAP "
literal|"method '%s'"
argument_list|,
name|start
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|num_methods
operator|++
expr_stmt|;
name|_methods
operator|=
name|os_realloc
argument_list|(
name|methods
argument_list|,
name|num_methods
operator|*
sizeof|sizeof
argument_list|(
operator|*
name|methods
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|_methods
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|methods
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|methods
operator|=
name|_methods
expr_stmt|;
name|methods
index|[
name|num_methods
operator|-
literal|1
index|]
operator|.
name|vendor
operator|=
name|vendor
expr_stmt|;
name|methods
index|[
name|num_methods
operator|-
literal|1
index|]
operator|.
name|method
operator|=
name|method
expr_stmt|;
block|}
name|start
operator|=
name|pos
expr_stmt|;
block|}
name|os_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
name|get_defaults
label|:
if|if
condition|(
name|methods
operator|==
name|NULL
condition|)
name|methods
operator|=
name|eap_get_phase2_types
argument_list|(
name|config
argument_list|,
operator|&
name|num_methods
argument_list|)
expr_stmt|;
if|if
condition|(
name|methods
operator|==
name|NULL
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_ERROR
argument_list|,
literal|"TLS: No Phase2 EAP methods available"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|wpa_hexdump
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Phase2 EAP types"
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|methods
argument_list|,
name|num_methods
operator|*
sizeof|sizeof
argument_list|(
expr|struct
name|eap_method_type
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|types
operator|=
name|methods
expr_stmt|;
operator|*
name|num_types
operator|=
name|num_methods
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * eap_peer_tls_phase2_nak - Generate EAP-Nak for Phase 2  * @types: Buffer for returning allocated list of allowed EAP methods  * @num_types: Buffer for returning number of allocated EAP methods  * @hdr: EAP-Request header (and the following EAP type octet)  * @resp: Buffer for returning the EAP-Nak message  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|eap_peer_tls_phase2_nak
parameter_list|(
name|struct
name|eap_method_type
modifier|*
name|types
parameter_list|,
name|size_t
name|num_types
parameter_list|,
name|struct
name|eap_hdr
modifier|*
name|hdr
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|resp
parameter_list|)
block|{
name|u8
modifier|*
name|pos
init|=
operator|(
name|u8
operator|*
operator|)
operator|(
name|hdr
operator|+
literal|1
operator|)
decl_stmt|;
name|size_t
name|i
decl_stmt|;
comment|/* TODO: add support for expanded Nak */
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Phase 2 Request: Nak type=%d"
argument_list|,
operator|*
name|pos
argument_list|)
expr_stmt|;
name|wpa_hexdump
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLS: Allowed Phase2 EAP types"
argument_list|,
operator|(
name|u8
operator|*
operator|)
name|types
argument_list|,
name|num_types
operator|*
sizeof|sizeof
argument_list|(
expr|struct
name|eap_method_type
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|resp
operator|=
name|eap_msg_alloc
argument_list|(
name|EAP_VENDOR_IETF
argument_list|,
name|EAP_TYPE_NAK
argument_list|,
name|num_types
argument_list|,
name|EAP_CODE_RESPONSE
argument_list|,
name|hdr
operator|->
name|identifier
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|resp
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|num_types
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|types
index|[
name|i
index|]
operator|.
name|vendor
operator|==
name|EAP_VENDOR_IETF
operator|&&
name|types
index|[
name|i
index|]
operator|.
name|method
operator|<
literal|256
condition|)
name|wpabuf_put_u8
argument_list|(
operator|*
name|resp
argument_list|,
name|types
index|[
name|i
index|]
operator|.
name|method
argument_list|)
expr_stmt|;
block|}
name|eap_update_len
argument_list|(
operator|*
name|resp
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

end_unit

