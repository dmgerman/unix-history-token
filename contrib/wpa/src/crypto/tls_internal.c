begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * TLS interface functions and an internal TLS implementation  * Copyright (c) 2004-2011, Jouni Malinen<j@w1.fi>  *  * This software may be distributed under the terms of the BSD license.  * See README for more details.  *  * This file interface functions for hostapd/wpa_supplicant to use the  * integrated TLSv1 implementation.  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_include
include|#
directive|include
file|"tls.h"
end_include

begin_include
include|#
directive|include
file|"tls/tlsv1_client.h"
end_include

begin_include
include|#
directive|include
file|"tls/tlsv1_server.h"
end_include

begin_decl_stmt
specifier|static
name|int
name|tls_ref_count
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_struct
struct|struct
name|tls_global
block|{
name|int
name|server
decl_stmt|;
name|struct
name|tlsv1_credentials
modifier|*
name|server_cred
decl_stmt|;
name|int
name|check_crl
decl_stmt|;
block|}
struct|;
end_struct

begin_struct
struct|struct
name|tls_connection
block|{
name|struct
name|tlsv1_client
modifier|*
name|client
decl_stmt|;
name|struct
name|tlsv1_server
modifier|*
name|server
decl_stmt|;
block|}
struct|;
end_struct

begin_function
name|void
modifier|*
name|tls_init
parameter_list|(
specifier|const
name|struct
name|tls_config
modifier|*
name|conf
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
decl_stmt|;
if|if
condition|(
name|tls_ref_count
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|tlsv1_client_global_init
argument_list|()
condition|)
return|return
name|NULL
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|tlsv1_server_global_init
argument_list|()
condition|)
return|return
name|NULL
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
block|}
name|tls_ref_count
operator|++
expr_stmt|;
name|global
operator|=
name|os_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|global
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|global
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
return|return
name|global
return|;
block|}
end_function

begin_function
name|void
name|tls_deinit
parameter_list|(
name|void
modifier|*
name|ssl_ctx
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|ssl_ctx
decl_stmt|;
name|tls_ref_count
operator|--
expr_stmt|;
if|if
condition|(
name|tls_ref_count
operator|==
literal|0
condition|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
name|tlsv1_client_global_deinit
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
name|tlsv1_cred_free
argument_list|(
name|global
operator|->
name|server_cred
argument_list|)
expr_stmt|;
name|tlsv1_server_global_deinit
argument_list|()
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
block|}
name|os_free
argument_list|(
name|global
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|int
name|tls_get_errors
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
name|struct
name|tls_connection
modifier|*
name|tls_connection_init
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|)
block|{
name|struct
name|tls_connection
modifier|*
name|conn
decl_stmt|;
name|struct
name|tls_global
modifier|*
name|global
init|=
name|tls_ctx
decl_stmt|;
name|conn
operator|=
name|os_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|conn
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
operator|!
name|global
operator|->
name|server
condition|)
block|{
name|conn
operator|->
name|client
operator|=
name|tlsv1_client_init
argument_list|()
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|client
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|global
operator|->
name|server
condition|)
block|{
name|conn
operator|->
name|server
operator|=
name|tlsv1_server_init
argument_list|(
name|global
operator|->
name|server_cred
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|server
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
name|conn
return|;
block|}
end_function

begin_function
name|void
name|tls_connection_deinit
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return;
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
name|tlsv1_client_deinit
argument_list|(
name|conn
operator|->
name|client
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
name|tlsv1_server_deinit
argument_list|(
name|conn
operator|->
name|server
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
name|int
name|tls_connection_established
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_established
argument_list|(
name|conn
operator|->
name|client
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_established
argument_list|(
name|conn
operator|->
name|server
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_shutdown
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_shutdown
argument_list|(
name|conn
operator|->
name|client
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_shutdown
argument_list|(
name|conn
operator|->
name|server
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_params
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
name|struct
name|tlsv1_credentials
modifier|*
name|cred
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|client
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|cred
operator|=
name|tlsv1_cred_alloc
argument_list|()
expr_stmt|;
if|if
condition|(
name|cred
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|tlsv1_set_ca_cert
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|params
operator|->
name|ca_cert_blob
argument_list|,
name|params
operator|->
name|ca_cert_blob_len
argument_list|,
name|params
operator|->
name|ca_path
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to configure trusted CA "
literal|"certificates"
argument_list|)
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|cred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_cert
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|client_cert_blob
argument_list|,
name|params
operator|->
name|client_cert_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to configure client "
literal|"certificate"
argument_list|)
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|cred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_private_key
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|params
operator|->
name|private_key_passwd
argument_list|,
name|params
operator|->
name|private_key_blob
argument_list|,
name|params
operator|->
name|private_key_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to load private key"
argument_list|)
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|cred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_dhparams
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|dh_file
argument_list|,
name|params
operator|->
name|dh_blob
argument_list|,
name|params
operator|->
name|dh_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to load DH parameters"
argument_list|)
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|cred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_client_set_cred
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|cred
argument_list|)
operator|<
literal|0
condition|)
block|{
name|tlsv1_cred_free
argument_list|(
name|cred
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|tlsv1_client_set_time_checks
argument_list|(
name|conn
operator|->
name|client
argument_list|,
operator|!
operator|(
name|params
operator|->
name|flags
operator|&
name|TLS_CONN_DISABLE_TIME_CHECKS
operator|)
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
else|#
directive|else
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
block|}
end_function

begin_function
name|int
name|tls_global_set_params
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
specifier|const
name|struct
name|tls_connection_params
modifier|*
name|params
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
name|struct
name|tls_global
modifier|*
name|global
init|=
name|tls_ctx
decl_stmt|;
name|struct
name|tlsv1_credentials
modifier|*
name|cred
decl_stmt|;
comment|/* Currently, global parameters are only set when running in server 	 * mode. */
name|global
operator|->
name|server
operator|=
literal|1
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|global
operator|->
name|server_cred
argument_list|)
expr_stmt|;
name|global
operator|->
name|server_cred
operator|=
name|cred
operator|=
name|tlsv1_cred_alloc
argument_list|()
expr_stmt|;
if|if
condition|(
name|cred
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|tlsv1_set_ca_cert
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|ca_cert
argument_list|,
name|params
operator|->
name|ca_cert_blob
argument_list|,
name|params
operator|->
name|ca_cert_blob_len
argument_list|,
name|params
operator|->
name|ca_path
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to configure trusted CA "
literal|"certificates"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_cert
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|client_cert
argument_list|,
name|params
operator|->
name|client_cert_blob
argument_list|,
name|params
operator|->
name|client_cert_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to configure server "
literal|"certificate"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_private_key
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|private_key
argument_list|,
name|params
operator|->
name|private_key_passwd
argument_list|,
name|params
operator|->
name|private_key_blob
argument_list|,
name|params
operator|->
name|private_key_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to load private key"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|tlsv1_set_dhparams
argument_list|(
name|cred
argument_list|,
name|params
operator|->
name|dh_file
argument_list|,
name|params
operator|->
name|dh_blob
argument_list|,
name|params
operator|->
name|dh_blob_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_INFO
argument_list|,
literal|"TLS: Failed to load DH parameters"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
literal|0
return|;
else|#
directive|else
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
block|}
end_function

begin_function
name|int
name|tls_global_set_verify
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|int
name|check_crl
parameter_list|)
block|{
name|struct
name|tls_global
modifier|*
name|global
init|=
name|tls_ctx
decl_stmt|;
name|global
operator|->
name|check_crl
operator|=
name|check_crl
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_verify
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|verify_peer
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_set_verify
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|verify_peer
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_keys
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|struct
name|tls_keys
modifier|*
name|keys
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_get_keys
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|keys
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_get_keys
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|keys
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_prf
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|char
modifier|*
name|label
parameter_list|,
name|int
name|server_random_first
parameter_list|,
name|u8
modifier|*
name|out
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
block|{
return|return
name|tlsv1_client_prf
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|label
argument_list|,
name|server_random_first
argument_list|,
name|out
argument_list|,
name|out_len
argument_list|)
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
block|{
return|return
name|tlsv1_server_prf
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|label
argument_list|,
name|server_random_first
argument_list|,
name|out
argument_list|,
name|out_len
argument_list|)
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_handshake
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|appl_data
parameter_list|)
block|{
return|return
name|tls_connection_handshake2
argument_list|(
name|tls_ctx
argument_list|,
name|conn
argument_list|,
name|in_data
argument_list|,
name|appl_data
argument_list|,
name|NULL
argument_list|)
return|;
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_handshake2
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|appl_data
parameter_list|,
name|int
modifier|*
name|need_more_data
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
name|u8
modifier|*
name|res
decl_stmt|,
modifier|*
name|ad
decl_stmt|;
name|size_t
name|res_len
decl_stmt|,
name|ad_len
decl_stmt|;
name|struct
name|wpabuf
modifier|*
name|out
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|client
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|ad
operator|=
name|NULL
expr_stmt|;
name|res
operator|=
name|tlsv1_client_handshake
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|in_data
condition|?
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
else|:
name|NULL
argument_list|,
name|in_data
condition|?
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
else|:
literal|0
argument_list|,
operator|&
name|res_len
argument_list|,
operator|&
name|ad
argument_list|,
operator|&
name|ad_len
argument_list|,
name|need_more_data
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|out
operator|=
name|wpabuf_alloc_ext_data
argument_list|(
name|res
argument_list|,
name|res_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|out
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|res
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|ad
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
if|if
condition|(
name|appl_data
condition|)
block|{
if|if
condition|(
name|ad
condition|)
block|{
operator|*
name|appl_data
operator|=
name|wpabuf_alloc_ext_data
argument_list|(
name|ad
argument_list|,
name|ad_len
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|appl_data
operator|==
name|NULL
condition|)
name|os_free
argument_list|(
name|ad
argument_list|)
expr_stmt|;
block|}
else|else
operator|*
name|appl_data
operator|=
name|NULL
expr_stmt|;
block|}
else|else
name|os_free
argument_list|(
name|ad
argument_list|)
expr_stmt|;
return|return
name|out
return|;
else|#
directive|else
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
return|return
name|NULL
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_server_handshake
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|struct
name|wpabuf
modifier|*
modifier|*
name|appl_data
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
name|u8
modifier|*
name|res
decl_stmt|;
name|size_t
name|res_len
decl_stmt|;
name|struct
name|wpabuf
modifier|*
name|out
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|server
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
if|if
condition|(
name|appl_data
condition|)
operator|*
name|appl_data
operator|=
name|NULL
expr_stmt|;
name|res
operator|=
name|tlsv1_server_handshake
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
operator|&
name|res_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|==
name|NULL
operator|&&
name|tlsv1_server_established
argument_list|(
name|conn
operator|->
name|server
argument_list|)
condition|)
return|return
name|wpabuf_alloc
argument_list|(
literal|0
argument_list|)
return|;
if|if
condition|(
name|res
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|out
operator|=
name|wpabuf_alloc_ext_data
argument_list|(
name|res
argument_list|,
name|res_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|out
operator|==
name|NULL
condition|)
block|{
name|os_free
argument_list|(
name|res
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
return|return
name|out
return|;
else|#
directive|else
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
name|NULL
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_encrypt
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
block|{
name|struct
name|wpabuf
modifier|*
name|buf
decl_stmt|;
name|int
name|res
decl_stmt|;
name|buf
operator|=
name|wpabuf_alloc
argument_list|(
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
operator|+
literal|300
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|res
operator|=
name|tlsv1_client_encrypt
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_mhead
argument_list|(
name|buf
argument_list|)
argument_list|,
name|wpabuf_size
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpabuf_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|wpabuf_put
argument_list|(
name|buf
argument_list|,
name|res
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
block|{
name|struct
name|wpabuf
modifier|*
name|buf
decl_stmt|;
name|int
name|res
decl_stmt|;
name|buf
operator|=
name|wpabuf_alloc
argument_list|(
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
operator|+
literal|300
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|res
operator|=
name|tlsv1_server_encrypt
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_mhead
argument_list|(
name|buf
argument_list|)
argument_list|,
name|wpabuf_size
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpabuf_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|wpabuf_put
argument_list|(
name|buf
argument_list|,
name|res
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
name|NULL
return|;
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_decrypt
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|)
block|{
return|return
name|tls_connection_decrypt2
argument_list|(
name|tls_ctx
argument_list|,
name|conn
argument_list|,
name|in_data
argument_list|,
name|NULL
argument_list|)
return|;
block|}
end_function

begin_function
name|struct
name|wpabuf
modifier|*
name|tls_connection_decrypt2
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
specifier|const
name|struct
name|wpabuf
modifier|*
name|in_data
parameter_list|,
name|int
modifier|*
name|need_more_data
parameter_list|)
block|{
if|if
condition|(
name|need_more_data
condition|)
operator|*
name|need_more_data
operator|=
literal|0
expr_stmt|;
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
block|{
return|return
name|tlsv1_client_decrypt
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|need_more_data
argument_list|)
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
block|{
name|struct
name|wpabuf
modifier|*
name|buf
decl_stmt|;
name|int
name|res
decl_stmt|;
name|buf
operator|=
name|wpabuf_alloc
argument_list|(
operator|(
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
operator|+
literal|500
operator|)
operator|*
literal|3
argument_list|)
expr_stmt|;
if|if
condition|(
name|buf
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|res
operator|=
name|tlsv1_server_decrypt
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|wpabuf_head
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_len
argument_list|(
name|in_data
argument_list|)
argument_list|,
name|wpabuf_mhead
argument_list|(
name|buf
argument_list|)
argument_list|,
name|wpabuf_size
argument_list|(
name|buf
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpabuf_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|wpabuf_put
argument_list|(
name|buf
argument_list|,
name|res
argument_list|)
expr_stmt|;
return|return
name|buf
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
name|NULL
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_resumed
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_resumed
argument_list|(
name|conn
operator|->
name|client
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_resumed
argument_list|(
name|conn
operator|->
name|server
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_cipher_list
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|u8
modifier|*
name|ciphers
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_set_cipher_list
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|ciphers
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_set_cipher_list
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|ciphers
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_get_cipher
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|buflen
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_get_cipher
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|buf
argument_list|,
name|buflen
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_get_cipher
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|buf
argument_list|,
name|buflen
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_enable_workaround
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_client_hello_ext
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|int
name|ext_type
parameter_list|,
specifier|const
name|u8
modifier|*
name|data
parameter_list|,
name|size_t
name|data_len
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
block|{
return|return
name|tlsv1_client_hello_ext
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|ext_type
argument_list|,
name|data
argument_list|,
name|data_len
argument_list|)
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_failed
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_read_alerts
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_write_alerts
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_get_keyblock_size
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
return|return
name|tlsv1_client_get_keyblock_size
argument_list|(
name|conn
operator|->
name|client
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
return|return
name|tlsv1_server_get_keyblock_size
argument_list|(
name|conn
operator|->
name|server
argument_list|)
return|;
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_function
name|unsigned
name|int
name|tls_capabilities
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|)
block|{
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_connection_set_session_ticket_cb
parameter_list|(
name|void
modifier|*
name|tls_ctx
parameter_list|,
name|struct
name|tls_connection
modifier|*
name|conn
parameter_list|,
name|tls_session_ticket_cb
name|cb
parameter_list|,
name|void
modifier|*
name|ctx
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_CLIENT
if|if
condition|(
name|conn
operator|->
name|client
condition|)
block|{
name|tlsv1_client_set_session_ticket_cb
argument_list|(
name|conn
operator|->
name|client
argument_list|,
name|cb
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_CLIENT */
ifdef|#
directive|ifdef
name|CONFIG_TLS_INTERNAL_SERVER
if|if
condition|(
name|conn
operator|->
name|server
condition|)
block|{
name|tlsv1_server_set_session_ticket_cb
argument_list|(
name|conn
operator|->
name|server
argument_list|,
name|cb
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
endif|#
directive|endif
comment|/* CONFIG_TLS_INTERNAL_SERVER */
return|return
operator|-
literal|1
return|;
block|}
end_function

end_unit

