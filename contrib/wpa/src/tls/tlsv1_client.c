begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * TLSv1 client (RFC 2246)  * Copyright (c) 2006-2007, Jouni Malinen<j@w1.fi>  *  * This program is free software; you can redistribute it and/or modify  * it under the terms of the GNU General Public License version 2 as  * published by the Free Software Foundation.  *  * Alternatively, this software may be distributed under the terms of BSD  * license.  *  * See README and COPYING for more details.  */
end_comment

begin_include
include|#
directive|include
file|"includes.h"
end_include

begin_include
include|#
directive|include
file|"common.h"
end_include

begin_include
include|#
directive|include
file|"sha1.h"
end_include

begin_include
include|#
directive|include
file|"tls.h"
end_include

begin_include
include|#
directive|include
file|"tlsv1_common.h"
end_include

begin_include
include|#
directive|include
file|"tlsv1_record.h"
end_include

begin_include
include|#
directive|include
file|"tlsv1_client.h"
end_include

begin_include
include|#
directive|include
file|"tlsv1_client_i.h"
end_include

begin_comment
comment|/* TODO:  * Support for a message fragmented across several records (RFC 2246, 6.2.1)  */
end_comment

begin_function
name|void
name|tls_alert
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|u8
name|level
parameter_list|,
name|u8
name|description
parameter_list|)
block|{
name|conn
operator|->
name|alert_level
operator|=
name|level
expr_stmt|;
name|conn
operator|->
name|alert_description
operator|=
name|description
expr_stmt|;
block|}
end_function

begin_function
name|void
name|tlsv1_client_free_dh
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
name|os_free
argument_list|(
name|conn
operator|->
name|dh_p
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|dh_g
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|dh_ys
argument_list|)
expr_stmt|;
name|conn
operator|->
name|dh_p
operator|=
name|conn
operator|->
name|dh_g
operator|=
name|conn
operator|->
name|dh_ys
operator|=
name|NULL
expr_stmt|;
block|}
end_function

begin_function
name|int
name|tls_derive_pre_master_secret
parameter_list|(
name|u8
modifier|*
name|pre_master_secret
parameter_list|)
block|{
name|WPA_PUT_BE16
argument_list|(
name|pre_master_secret
argument_list|,
name|TLS_VERSION
argument_list|)
expr_stmt|;
if|if
condition|(
name|os_get_random
argument_list|(
name|pre_master_secret
operator|+
literal|2
argument_list|,
name|TLS_PRE_MASTER_SECRET_LEN
operator|-
literal|2
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|tls_derive_keys
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|pre_master_secret
parameter_list|,
name|size_t
name|pre_master_secret_len
parameter_list|)
block|{
name|u8
name|seed
index|[
literal|2
operator|*
name|TLS_RANDOM_LEN
index|]
decl_stmt|;
name|u8
name|key_block
index|[
name|TLS_MAX_KEY_BLOCK_LEN
index|]
decl_stmt|;
name|u8
modifier|*
name|pos
decl_stmt|;
name|size_t
name|key_block_len
decl_stmt|;
if|if
condition|(
name|pre_master_secret
condition|)
block|{
name|wpa_hexdump_key
argument_list|(
name|MSG_MSGDUMP
argument_list|,
literal|"TLSv1: pre_master_secret"
argument_list|,
name|pre_master_secret
argument_list|,
name|pre_master_secret_len
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|seed
argument_list|,
name|conn
operator|->
name|client_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|seed
operator|+
name|TLS_RANDOM_LEN
argument_list|,
name|conn
operator|->
name|server_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
if|if
condition|(
name|tls_prf
argument_list|(
name|pre_master_secret
argument_list|,
name|pre_master_secret_len
argument_list|,
literal|"master secret"
argument_list|,
name|seed
argument_list|,
literal|2
operator|*
name|TLS_RANDOM_LEN
argument_list|,
name|conn
operator|->
name|master_secret
argument_list|,
name|TLS_MASTER_SECRET_LEN
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Failed to derive "
literal|"master_secret"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|wpa_hexdump_key
argument_list|(
name|MSG_MSGDUMP
argument_list|,
literal|"TLSv1: master_secret"
argument_list|,
name|conn
operator|->
name|master_secret
argument_list|,
name|TLS_MASTER_SECRET_LEN
argument_list|)
expr_stmt|;
block|}
name|os_memcpy
argument_list|(
name|seed
argument_list|,
name|conn
operator|->
name|server_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|seed
operator|+
name|TLS_RANDOM_LEN
argument_list|,
name|conn
operator|->
name|client_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
name|key_block_len
operator|=
literal|2
operator|*
operator|(
name|conn
operator|->
name|rl
operator|.
name|hash_size
operator|+
name|conn
operator|->
name|rl
operator|.
name|key_material_len
operator|+
name|conn
operator|->
name|rl
operator|.
name|iv_size
operator|)
expr_stmt|;
if|if
condition|(
name|tls_prf
argument_list|(
name|conn
operator|->
name|master_secret
argument_list|,
name|TLS_MASTER_SECRET_LEN
argument_list|,
literal|"key expansion"
argument_list|,
name|seed
argument_list|,
literal|2
operator|*
name|TLS_RANDOM_LEN
argument_list|,
name|key_block
argument_list|,
name|key_block_len
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Failed to derive key_block"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|wpa_hexdump_key
argument_list|(
name|MSG_MSGDUMP
argument_list|,
literal|"TLSv1: key_block"
argument_list|,
name|key_block
argument_list|,
name|key_block_len
argument_list|)
expr_stmt|;
name|pos
operator|=
name|key_block
expr_stmt|;
comment|/* client_write_MAC_secret */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|write_mac_secret
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|hash_size
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|hash_size
expr_stmt|;
comment|/* server_write_MAC_secret */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|read_mac_secret
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|hash_size
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|hash_size
expr_stmt|;
comment|/* client_write_key */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|write_key
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|key_material_len
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|key_material_len
expr_stmt|;
comment|/* server_write_key */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|read_key
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|key_material_len
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|key_material_len
expr_stmt|;
comment|/* client_write_IV */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|write_iv
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|iv_size
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|iv_size
expr_stmt|;
comment|/* server_write_IV */
name|os_memcpy
argument_list|(
name|conn
operator|->
name|rl
operator|.
name|read_iv
argument_list|,
name|pos
argument_list|,
name|conn
operator|->
name|rl
operator|.
name|iv_size
argument_list|)
expr_stmt|;
name|pos
operator|+=
name|conn
operator|->
name|rl
operator|.
name|iv_size
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_handshake - Process TLS handshake  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @in_data: Input data from TLS peer  * @in_len: Input data length  * @out_len: Length of the output buffer.  * @appl_data: Pointer to application data pointer, or %NULL if dropped  * @appl_data_len: Pointer to variable that is set to appl_data length  * Returns: Pointer to output data, %NULL on failure  */
end_comment

begin_function
name|u8
modifier|*
name|tlsv1_client_handshake
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|size_t
modifier|*
name|out_len
parameter_list|,
name|u8
modifier|*
modifier|*
name|appl_data
parameter_list|,
name|size_t
modifier|*
name|appl_data_len
parameter_list|)
block|{
specifier|const
name|u8
modifier|*
name|pos
decl_stmt|,
modifier|*
name|end
decl_stmt|;
name|u8
modifier|*
name|msg
init|=
name|NULL
decl_stmt|,
modifier|*
name|in_msg
decl_stmt|,
modifier|*
name|in_pos
decl_stmt|,
modifier|*
name|in_end
decl_stmt|,
name|alert
decl_stmt|,
name|ct
decl_stmt|;
name|size_t
name|in_msg_len
decl_stmt|;
name|int
name|no_appl_data
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|state
operator|==
name|CLIENT_HELLO
condition|)
block|{
if|if
condition|(
name|in_len
condition|)
return|return
name|NULL
return|;
return|return
name|tls_send_client_hello
argument_list|(
name|conn
argument_list|,
name|out_len
argument_list|)
return|;
block|}
if|if
condition|(
name|in_data
operator|==
name|NULL
operator|||
name|in_len
operator|==
literal|0
condition|)
return|return
name|NULL
return|;
name|pos
operator|=
name|in_data
expr_stmt|;
name|end
operator|=
name|in_data
operator|+
name|in_len
expr_stmt|;
name|in_msg
operator|=
name|os_malloc
argument_list|(
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|in_msg
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
comment|/* Each received packet may include multiple records */
while|while
condition|(
name|pos
operator|<
name|end
condition|)
block|{
name|in_msg_len
operator|=
name|in_len
expr_stmt|;
if|if
condition|(
name|tlsv1_record_receive
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|,
name|pos
argument_list|,
name|end
operator|-
name|pos
argument_list|,
name|in_msg
argument_list|,
operator|&
name|in_msg_len
argument_list|,
operator|&
name|alert
argument_list|)
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Processing received "
literal|"record failed"
argument_list|)
expr_stmt|;
name|tls_alert
argument_list|(
name|conn
argument_list|,
name|TLS_ALERT_LEVEL_FATAL
argument_list|,
name|alert
argument_list|)
expr_stmt|;
goto|goto
name|failed
goto|;
block|}
name|ct
operator|=
name|pos
index|[
literal|0
index|]
expr_stmt|;
name|in_pos
operator|=
name|in_msg
expr_stmt|;
name|in_end
operator|=
name|in_msg
operator|+
name|in_msg_len
expr_stmt|;
comment|/* Each received record may include multiple messages of the 		 * same ContentType. */
while|while
condition|(
name|in_pos
operator|<
name|in_end
condition|)
block|{
name|in_msg_len
operator|=
name|in_end
operator|-
name|in_pos
expr_stmt|;
if|if
condition|(
name|tlsv1_client_process_handshake
argument_list|(
name|conn
argument_list|,
name|ct
argument_list|,
name|in_pos
argument_list|,
operator|&
name|in_msg_len
argument_list|,
name|appl_data
argument_list|,
name|appl_data_len
argument_list|)
operator|<
literal|0
condition|)
goto|goto
name|failed
goto|;
name|in_pos
operator|+=
name|in_msg_len
expr_stmt|;
block|}
name|pos
operator|+=
name|TLS_RECORD_HEADER_LEN
operator|+
name|WPA_GET_BE16
argument_list|(
name|pos
operator|+
literal|3
argument_list|)
expr_stmt|;
block|}
name|os_free
argument_list|(
name|in_msg
argument_list|)
expr_stmt|;
name|in_msg
operator|=
name|NULL
expr_stmt|;
name|no_appl_data
operator|=
name|appl_data
operator|==
name|NULL
operator|||
operator|*
name|appl_data
operator|==
name|NULL
expr_stmt|;
name|msg
operator|=
name|tlsv1_client_handshake_write
argument_list|(
name|conn
argument_list|,
name|out_len
argument_list|,
name|no_appl_data
argument_list|)
expr_stmt|;
name|failed
label|:
name|os_free
argument_list|(
name|in_msg
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|alert_level
condition|)
block|{
name|conn
operator|->
name|state
operator|=
name|FAILED
expr_stmt|;
name|os_free
argument_list|(
name|msg
argument_list|)
expr_stmt|;
name|msg
operator|=
name|tlsv1_client_send_alert
argument_list|(
name|conn
argument_list|,
name|conn
operator|->
name|alert_level
argument_list|,
name|conn
operator|->
name|alert_description
argument_list|,
name|out_len
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|msg
operator|==
name|NULL
condition|)
block|{
name|msg
operator|=
name|os_zalloc
argument_list|(
literal|1
argument_list|)
expr_stmt|;
operator|*
name|out_len
operator|=
literal|0
expr_stmt|;
block|}
return|return
name|msg
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_encrypt - Encrypt data into TLS tunnel  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @in_data: Pointer to plaintext data to be encrypted  * @in_len: Input buffer length  * @out_data: Pointer to output buffer (encrypted TLS data)  * @out_len: Maximum out_data length   * Returns: Number of bytes written to out_data, -1 on failure  *  * This function is used after TLS handshake has been completed successfully to  * send data in the encrypted tunnel.  */
end_comment

begin_function
name|int
name|tlsv1_client_encrypt
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|u8
modifier|*
name|out_data
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
name|size_t
name|rlen
decl_stmt|;
name|wpa_hexdump_key
argument_list|(
name|MSG_MSGDUMP
argument_list|,
literal|"TLSv1: Plaintext AppData"
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|out_data
operator|+
name|TLS_RECORD_HEADER_LEN
argument_list|,
name|in_data
argument_list|,
name|in_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|tlsv1_record_send
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|,
name|TLS_CONTENT_TYPE_APPLICATION_DATA
argument_list|,
name|out_data
argument_list|,
name|out_len
argument_list|,
name|in_len
argument_list|,
operator|&
name|rlen
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Failed to create a record"
argument_list|)
expr_stmt|;
name|tls_alert
argument_list|(
name|conn
argument_list|,
name|TLS_ALERT_LEVEL_FATAL
argument_list|,
name|TLS_ALERT_INTERNAL_ERROR
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
return|return
name|rlen
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_decrypt - Decrypt data from TLS tunnel  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @in_data: Pointer to input buffer (encrypted TLS data)  * @in_len: Input buffer length  * @out_data: Pointer to output buffer (decrypted data from TLS tunnel)  * @out_len: Maximum out_data length  * Returns: Number of bytes written to out_data, -1 on failure  *  * This function is used after TLS handshake has been completed successfully to  * receive data from the encrypted tunnel.  */
end_comment

begin_function
name|int
name|tlsv1_client_decrypt
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
specifier|const
name|u8
modifier|*
name|in_data
parameter_list|,
name|size_t
name|in_len
parameter_list|,
name|u8
modifier|*
name|out_data
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
specifier|const
name|u8
modifier|*
name|in_end
decl_stmt|,
modifier|*
name|pos
decl_stmt|;
name|int
name|res
decl_stmt|;
name|u8
name|alert
decl_stmt|,
modifier|*
name|out_end
decl_stmt|,
modifier|*
name|out_pos
decl_stmt|;
name|size_t
name|olen
decl_stmt|;
name|pos
operator|=
name|in_data
expr_stmt|;
name|in_end
operator|=
name|in_data
operator|+
name|in_len
expr_stmt|;
name|out_pos
operator|=
name|out_data
expr_stmt|;
name|out_end
operator|=
name|out_data
operator|+
name|out_len
expr_stmt|;
while|while
condition|(
name|pos
operator|<
name|in_end
condition|)
block|{
if|if
condition|(
name|pos
index|[
literal|0
index|]
operator|!=
name|TLS_CONTENT_TYPE_APPLICATION_DATA
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Unexpected content type "
literal|"0x%x"
argument_list|,
name|pos
index|[
literal|0
index|]
argument_list|)
expr_stmt|;
name|tls_alert
argument_list|(
name|conn
argument_list|,
name|TLS_ALERT_LEVEL_FATAL
argument_list|,
name|TLS_ALERT_UNEXPECTED_MESSAGE
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|olen
operator|=
name|out_end
operator|-
name|out_pos
expr_stmt|;
name|res
operator|=
name|tlsv1_record_receive
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|,
name|pos
argument_list|,
name|in_end
operator|-
name|pos
argument_list|,
name|out_pos
argument_list|,
operator|&
name|olen
argument_list|,
operator|&
name|alert
argument_list|)
expr_stmt|;
if|if
condition|(
name|res
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Record layer processing "
literal|"failed"
argument_list|)
expr_stmt|;
name|tls_alert
argument_list|(
name|conn
argument_list|,
name|TLS_ALERT_LEVEL_FATAL
argument_list|,
name|alert
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|out_pos
operator|+=
name|olen
expr_stmt|;
if|if
condition|(
name|out_pos
operator|>
name|out_end
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Buffer not large enough "
literal|"for processing the received record"
argument_list|)
expr_stmt|;
name|tls_alert
argument_list|(
name|conn
argument_list|,
name|TLS_ALERT_LEVEL_FATAL
argument_list|,
name|TLS_ALERT_INTERNAL_ERROR
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|pos
operator|+=
name|TLS_RECORD_HEADER_LEN
operator|+
name|WPA_GET_BE16
argument_list|(
name|pos
operator|+
literal|3
argument_list|)
expr_stmt|;
block|}
return|return
name|out_pos
operator|-
name|out_data
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_global_init - Initialize TLSv1 client  * Returns: 0 on success, -1 on failure  *  * This function must be called before using any other TLSv1 client functions.  */
end_comment

begin_function
name|int
name|tlsv1_client_global_init
parameter_list|(
name|void
parameter_list|)
block|{
return|return
name|crypto_global_init
argument_list|()
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_global_deinit - Deinitialize TLSv1 client  *  * This function can be used to deinitialize the TLSv1 client that was  * initialized by calling tlsv1_client_global_init(). No TLSv1 client functions  * can be called after this before calling tlsv1_client_global_init() again.  */
end_comment

begin_function
name|void
name|tlsv1_client_global_deinit
parameter_list|(
name|void
parameter_list|)
block|{
name|crypto_global_deinit
argument_list|()
expr_stmt|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_init - Initialize TLSv1 client connection  * Returns: Pointer to TLSv1 client connection data or %NULL on failure  */
end_comment

begin_function
name|struct
name|tlsv1_client
modifier|*
name|tlsv1_client_init
parameter_list|(
name|void
parameter_list|)
block|{
name|struct
name|tlsv1_client
modifier|*
name|conn
decl_stmt|;
name|size_t
name|count
decl_stmt|;
name|u16
modifier|*
name|suites
decl_stmt|;
name|conn
operator|=
name|os_zalloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|conn
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
name|conn
operator|->
name|state
operator|=
name|CLIENT_HELLO
expr_stmt|;
if|if
condition|(
name|tls_verify_hash_init
argument_list|(
operator|&
name|conn
operator|->
name|verify
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Failed to initialize verify "
literal|"hash"
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|count
operator|=
literal|0
expr_stmt|;
name|suites
operator|=
name|conn
operator|->
name|cipher_suites
expr_stmt|;
ifndef|#
directive|ifndef
name|CONFIG_CRYPTO_INTERNAL
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_RSA_WITH_AES_256_CBC_SHA
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_CRYPTO_INTERNAL */
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_RSA_WITH_AES_128_CBC_SHA
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_RSA_WITH_3DES_EDE_CBC_SHA
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_RSA_WITH_RC4_128_SHA
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_RSA_WITH_RC4_128_MD5
expr_stmt|;
name|conn
operator|->
name|num_cipher_suites
operator|=
name|count
expr_stmt|;
return|return
name|conn
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_deinit - Deinitialize TLSv1 client connection  * @conn: TLSv1 client connection data from tlsv1_client_init()  */
end_comment

begin_function
name|void
name|tlsv1_client_deinit
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
name|crypto_public_key_free
argument_list|(
name|conn
operator|->
name|server_rsa_key
argument_list|)
expr_stmt|;
name|tlsv1_record_set_cipher_suite
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|,
name|TLS_NULL_WITH_NULL_NULL
argument_list|)
expr_stmt|;
name|tlsv1_record_change_write_cipher
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|)
expr_stmt|;
name|tlsv1_record_change_read_cipher
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|)
expr_stmt|;
name|tls_verify_hash_free
argument_list|(
operator|&
name|conn
operator|->
name|verify
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|client_hello_ext
argument_list|)
expr_stmt|;
name|tlsv1_client_free_dh
argument_list|(
name|conn
argument_list|)
expr_stmt|;
name|tlsv1_cred_free
argument_list|(
name|conn
operator|->
name|cred
argument_list|)
expr_stmt|;
name|os_free
argument_list|(
name|conn
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_established - Check whether connection has been established  * @conn: TLSv1 client connection data from tlsv1_client_init()  * Returns: 1 if connection is established, 0 if not  */
end_comment

begin_function
name|int
name|tlsv1_client_established
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
return|return
name|conn
operator|->
name|state
operator|==
name|ESTABLISHED
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_prf - Use TLS-PRF to derive keying material  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @label: Label (e.g., description of the key) for PRF  * @server_random_first: seed is 0 = client_random|server_random,  * 1 = server_random|client_random  * @out: Buffer for output data from TLS-PRF  * @out_len: Length of the output buffer  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|tlsv1_client_prf
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
specifier|const
name|char
modifier|*
name|label
parameter_list|,
name|int
name|server_random_first
parameter_list|,
name|u8
modifier|*
name|out
parameter_list|,
name|size_t
name|out_len
parameter_list|)
block|{
name|u8
name|seed
index|[
literal|2
operator|*
name|TLS_RANDOM_LEN
index|]
decl_stmt|;
if|if
condition|(
name|conn
operator|->
name|state
operator|!=
name|ESTABLISHED
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|server_random_first
condition|)
block|{
name|os_memcpy
argument_list|(
name|seed
argument_list|,
name|conn
operator|->
name|server_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|seed
operator|+
name|TLS_RANDOM_LEN
argument_list|,
name|conn
operator|->
name|client_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|os_memcpy
argument_list|(
name|seed
argument_list|,
name|conn
operator|->
name|client_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
name|os_memcpy
argument_list|(
name|seed
operator|+
name|TLS_RANDOM_LEN
argument_list|,
name|conn
operator|->
name|server_random
argument_list|,
name|TLS_RANDOM_LEN
argument_list|)
expr_stmt|;
block|}
return|return
name|tls_prf
argument_list|(
name|conn
operator|->
name|master_secret
argument_list|,
name|TLS_MASTER_SECRET_LEN
argument_list|,
name|label
argument_list|,
name|seed
argument_list|,
literal|2
operator|*
name|TLS_RANDOM_LEN
argument_list|,
name|out
argument_list|,
name|out_len
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_get_cipher - Get current cipher name  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @buf: Buffer for the cipher name  * @buflen: buf size  * Returns: 0 on success, -1 on failure  *  * Get the name of the currently used cipher.  */
end_comment

begin_function
name|int
name|tlsv1_client_get_cipher
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|buflen
parameter_list|)
block|{
name|char
modifier|*
name|cipher
decl_stmt|;
switch|switch
condition|(
name|conn
operator|->
name|rl
operator|.
name|cipher_suite
condition|)
block|{
case|case
name|TLS_RSA_WITH_RC4_128_MD5
case|:
name|cipher
operator|=
literal|"RC4-MD5"
expr_stmt|;
break|break;
case|case
name|TLS_RSA_WITH_RC4_128_SHA
case|:
name|cipher
operator|=
literal|"RC4-SHA"
expr_stmt|;
break|break;
case|case
name|TLS_RSA_WITH_DES_CBC_SHA
case|:
name|cipher
operator|=
literal|"DES-CBC-SHA"
expr_stmt|;
break|break;
case|case
name|TLS_RSA_WITH_3DES_EDE_CBC_SHA
case|:
name|cipher
operator|=
literal|"DES-CBC3-SHA"
expr_stmt|;
break|break;
case|case
name|TLS_DH_anon_WITH_AES_128_CBC_SHA
case|:
name|cipher
operator|=
literal|"ADH-AES-128-SHA"
expr_stmt|;
break|break;
case|case
name|TLS_RSA_WITH_AES_256_CBC_SHA
case|:
name|cipher
operator|=
literal|"AES-256-SHA"
expr_stmt|;
break|break;
case|case
name|TLS_RSA_WITH_AES_128_CBC_SHA
case|:
name|cipher
operator|=
literal|"AES-128-SHA"
expr_stmt|;
break|break;
default|default:
return|return
operator|-
literal|1
return|;
block|}
if|if
condition|(
name|os_strlcpy
argument_list|(
name|buf
argument_list|,
name|cipher
argument_list|,
name|buflen
argument_list|)
operator|>=
name|buflen
condition|)
return|return
operator|-
literal|1
return|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_shutdown - Shutdown TLS connection  * @conn: TLSv1 client connection data from tlsv1_client_init()  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|tlsv1_client_shutdown
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
name|conn
operator|->
name|state
operator|=
name|CLIENT_HELLO
expr_stmt|;
if|if
condition|(
name|tls_verify_hash_init
argument_list|(
operator|&
name|conn
operator|->
name|verify
argument_list|)
operator|<
literal|0
condition|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Failed to re-initialize verify "
literal|"hash"
argument_list|)
expr_stmt|;
return|return
operator|-
literal|1
return|;
block|}
name|tlsv1_record_set_cipher_suite
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|,
name|TLS_NULL_WITH_NULL_NULL
argument_list|)
expr_stmt|;
name|tlsv1_record_change_write_cipher
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|)
expr_stmt|;
name|tlsv1_record_change_read_cipher
argument_list|(
operator|&
name|conn
operator|->
name|rl
argument_list|)
expr_stmt|;
name|conn
operator|->
name|certificate_requested
operator|=
literal|0
expr_stmt|;
name|crypto_public_key_free
argument_list|(
name|conn
operator|->
name|server_rsa_key
argument_list|)
expr_stmt|;
name|conn
operator|->
name|server_rsa_key
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|session_resumed
operator|=
literal|0
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_resumed - Was session resumption used  * @conn: TLSv1 client connection data from tlsv1_client_init()  * Returns: 1 if current session used session resumption, 0 if not  */
end_comment

begin_function
name|int
name|tlsv1_client_resumed
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
return|return
operator|!
operator|!
name|conn
operator|->
name|session_resumed
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_hello_ext - Set TLS extension for ClientHello  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @ext_type: Extension type  * @data: Extension payload (%NULL to remove extension)  * @data_len: Extension payload length  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|tlsv1_client_hello_ext
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|int
name|ext_type
parameter_list|,
specifier|const
name|u8
modifier|*
name|data
parameter_list|,
name|size_t
name|data_len
parameter_list|)
block|{
name|u8
modifier|*
name|pos
decl_stmt|;
name|conn
operator|->
name|session_ticket_included
operator|=
literal|0
expr_stmt|;
name|os_free
argument_list|(
name|conn
operator|->
name|client_hello_ext
argument_list|)
expr_stmt|;
name|conn
operator|->
name|client_hello_ext
operator|=
name|NULL
expr_stmt|;
name|conn
operator|->
name|client_hello_ext_len
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|data
operator|==
name|NULL
operator|||
name|data_len
operator|==
literal|0
condition|)
return|return
literal|0
return|;
name|pos
operator|=
name|conn
operator|->
name|client_hello_ext
operator|=
name|os_malloc
argument_list|(
literal|6
operator|+
name|data_len
argument_list|)
expr_stmt|;
if|if
condition|(
name|pos
operator|==
name|NULL
condition|)
return|return
operator|-
literal|1
return|;
name|WPA_PUT_BE16
argument_list|(
name|pos
argument_list|,
literal|4
operator|+
name|data_len
argument_list|)
expr_stmt|;
name|pos
operator|+=
literal|2
expr_stmt|;
name|WPA_PUT_BE16
argument_list|(
name|pos
argument_list|,
name|ext_type
argument_list|)
expr_stmt|;
name|pos
operator|+=
literal|2
expr_stmt|;
name|WPA_PUT_BE16
argument_list|(
name|pos
argument_list|,
name|data_len
argument_list|)
expr_stmt|;
name|pos
operator|+=
literal|2
expr_stmt|;
name|os_memcpy
argument_list|(
name|pos
argument_list|,
name|data
argument_list|,
name|data_len
argument_list|)
expr_stmt|;
name|conn
operator|->
name|client_hello_ext_len
operator|=
literal|6
operator|+
name|data_len
expr_stmt|;
if|if
condition|(
name|ext_type
operator|==
name|TLS_EXT_PAC_OPAQUE
condition|)
block|{
name|conn
operator|->
name|session_ticket_included
operator|=
literal|1
expr_stmt|;
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: Using session ticket"
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_get_keys - Get master key and random data from TLS connection  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @keys: Structure of key/random data (filled on success)  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|tlsv1_client_get_keys
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|struct
name|tls_keys
modifier|*
name|keys
parameter_list|)
block|{
name|os_memset
argument_list|(
name|keys
argument_list|,
literal|0
argument_list|,
sizeof|sizeof
argument_list|(
operator|*
name|keys
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|state
operator|==
name|CLIENT_HELLO
condition|)
return|return
operator|-
literal|1
return|;
name|keys
operator|->
name|client_random
operator|=
name|conn
operator|->
name|client_random
expr_stmt|;
name|keys
operator|->
name|client_random_len
operator|=
name|TLS_RANDOM_LEN
expr_stmt|;
if|if
condition|(
name|conn
operator|->
name|state
operator|!=
name|SERVER_HELLO
condition|)
block|{
name|keys
operator|->
name|server_random
operator|=
name|conn
operator|->
name|server_random
expr_stmt|;
name|keys
operator|->
name|server_random_len
operator|=
name|TLS_RANDOM_LEN
expr_stmt|;
name|keys
operator|->
name|master_key
operator|=
name|conn
operator|->
name|master_secret
expr_stmt|;
name|keys
operator|->
name|master_key_len
operator|=
name|TLS_MASTER_SECRET_LEN
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_get_keyblock_size - Get TLS key_block size  * @conn: TLSv1 client connection data from tlsv1_client_init()  * Returns: Size of the key_block for the negotiated cipher suite or -1 on  * failure  */
end_comment

begin_function
name|int
name|tlsv1_client_get_keyblock_size
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|)
block|{
if|if
condition|(
name|conn
operator|->
name|state
operator|==
name|CLIENT_HELLO
operator|||
name|conn
operator|->
name|state
operator|==
name|SERVER_HELLO
condition|)
return|return
operator|-
literal|1
return|;
return|return
literal|2
operator|*
operator|(
name|conn
operator|->
name|rl
operator|.
name|hash_size
operator|+
name|conn
operator|->
name|rl
operator|.
name|key_material_len
operator|+
name|conn
operator|->
name|rl
operator|.
name|iv_size
operator|)
return|;
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_set_cipher_list - Configure acceptable cipher suites  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @ciphers: Zero (TLS_CIPHER_NONE) terminated list of allowed ciphers  * (TLS_CIPHER_*).  * Returns: 0 on success, -1 on failure  */
end_comment

begin_function
name|int
name|tlsv1_client_set_cipher_list
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|u8
modifier|*
name|ciphers
parameter_list|)
block|{
ifdef|#
directive|ifdef
name|EAP_FAST
name|size_t
name|count
decl_stmt|;
name|u16
modifier|*
name|suites
decl_stmt|;
comment|/* TODO: implement proper configuration of cipher suites */
if|if
condition|(
name|ciphers
index|[
literal|0
index|]
operator|==
name|TLS_CIPHER_ANON_DH_AES128_SHA
condition|)
block|{
name|count
operator|=
literal|0
expr_stmt|;
name|suites
operator|=
name|conn
operator|->
name|cipher_suites
expr_stmt|;
ifndef|#
directive|ifndef
name|CONFIG_CRYPTO_INTERNAL
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_DH_anon_WITH_AES_256_CBC_SHA
expr_stmt|;
endif|#
directive|endif
comment|/* CONFIG_CRYPTO_INTERNAL */
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_DH_anon_WITH_AES_128_CBC_SHA
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_DH_anon_WITH_3DES_EDE_CBC_SHA
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_DH_anon_WITH_RC4_128_MD5
expr_stmt|;
name|suites
index|[
name|count
operator|++
index|]
operator|=
name|TLS_DH_anon_WITH_DES_CBC_SHA
expr_stmt|;
name|conn
operator|->
name|num_cipher_suites
operator|=
name|count
expr_stmt|;
block|}
return|return
literal|0
return|;
else|#
directive|else
comment|/* EAP_FAST */
return|return
operator|-
literal|1
return|;
endif|#
directive|endif
comment|/* EAP_FAST */
block|}
end_function

begin_comment
comment|/**  * tlsv1_client_set_cred - Set client credentials  * @conn: TLSv1 client connection data from tlsv1_client_init()  * @cred: Credentials from tlsv1_cred_alloc()  * Returns: 0 on success, -1 on failure  *  * On success, the client takes ownership of the credentials block and caller  * must not free it. On failure, caller is responsible for freeing the  * credential block.  */
end_comment

begin_function
name|int
name|tlsv1_client_set_cred
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|struct
name|tlsv1_credentials
modifier|*
name|cred
parameter_list|)
block|{
name|tlsv1_cred_free
argument_list|(
name|conn
operator|->
name|cred
argument_list|)
expr_stmt|;
name|conn
operator|->
name|cred
operator|=
name|cred
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|void
name|tlsv1_client_set_session_ticket_cb
parameter_list|(
name|struct
name|tlsv1_client
modifier|*
name|conn
parameter_list|,
name|tlsv1_client_session_ticket_cb
name|cb
parameter_list|,
name|void
modifier|*
name|ctx
parameter_list|)
block|{
name|wpa_printf
argument_list|(
name|MSG_DEBUG
argument_list|,
literal|"TLSv1: SessionTicket callback set %p (ctx %p)"
argument_list|,
name|cb
argument_list|,
name|ctx
argument_list|)
expr_stmt|;
name|conn
operator|->
name|session_ticket_cb
operator|=
name|cb
expr_stmt|;
name|conn
operator|->
name|session_ticket_cb_ctx
operator|=
name|ctx
expr_stmt|;
block|}
end_function

end_unit

