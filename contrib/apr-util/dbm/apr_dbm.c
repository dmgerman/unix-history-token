begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr.h"
end_include

begin_include
include|#
directive|include
file|"apr_dso.h"
end_include

begin_include
include|#
directive|include
file|"apr_hash.h"
end_include

begin_include
include|#
directive|include
file|"apr_errno.h"
end_include

begin_include
include|#
directive|include
file|"apr_pools.h"
end_include

begin_include
include|#
directive|include
file|"apr_strings.h"
end_include

begin_define
define|#
directive|define
name|APR_WANT_MEMFUNC
end_define

begin_define
define|#
directive|define
name|APR_WANT_STRFUNC
end_define

begin_include
include|#
directive|include
file|"apr_want.h"
end_include

begin_include
include|#
directive|include
file|"apr_general.h"
end_include

begin_include
include|#
directive|include
file|"apr_atomic.h"
end_include

begin_include
include|#
directive|include
file|"apu_config.h"
end_include

begin_include
include|#
directive|include
file|"apu.h"
end_include

begin_include
include|#
directive|include
file|"apu_internal.h"
end_include

begin_include
include|#
directive|include
file|"apu_version.h"
end_include

begin_include
include|#
directive|include
file|"apr_dbm_private.h"
end_include

begin_include
include|#
directive|include
file|"apu_select_dbm.h"
end_include

begin_include
include|#
directive|include
file|"apr_dbm.h"
end_include

begin_include
include|#
directive|include
file|"apr_dbm_private.h"
end_include

begin_comment
comment|/* ### note: the setting of DBM_VTABLE will go away once we have multiple    ### DBMs in here.     ### Well, that day is here.  So, do we remove DBM_VTABLE and the old    ### API entirely?  Oh, what to do.  We need an APU_DEFAULT_DBM #define.    ### Sounds like a job for autoconf. */
end_comment

begin_if
if|#
directive|if
name|APU_USE_DB
end_if

begin_define
define|#
directive|define
name|DBM_VTABLE
value|apr_dbm_type_db
end_define

begin_define
define|#
directive|define
name|DBM_NAME
value|"db"
end_define

begin_elif
elif|#
directive|elif
name|APU_USE_GDBM
end_elif

begin_define
define|#
directive|define
name|DBM_VTABLE
value|apr_dbm_type_gdbm
end_define

begin_define
define|#
directive|define
name|DBM_NAME
value|"gdbm"
end_define

begin_elif
elif|#
directive|elif
name|APU_USE_NDBM
end_elif

begin_define
define|#
directive|define
name|DBM_VTABLE
value|apr_dbm_type_ndbm
end_define

begin_define
define|#
directive|define
name|DBM_NAME
value|"ndbm"
end_define

begin_elif
elif|#
directive|elif
name|APU_USE_SDBM
end_elif

begin_define
define|#
directive|define
name|DBM_VTABLE
value|apr_dbm_type_sdbm
end_define

begin_define
define|#
directive|define
name|DBM_NAME
value|"sdbm"
end_define

begin_else
else|#
directive|else
end_else

begin_comment
comment|/* Not in the USE_xDBM list above */
end_comment

begin_error
error|#
directive|error
error|a DBM implementation was not specified
end_error

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|APU_DSO_BUILD
end_if

begin_decl_stmt
specifier|static
name|apr_hash_t
modifier|*
name|drivers
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|apr_uint32_t
name|initialised
init|=
literal|0
decl_stmt|,
name|in_init
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|apr_status_t
name|dbm_term
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|)
block|{
comment|/* set drivers to NULL so init can work again */
name|drivers
operator|=
name|NULL
expr_stmt|;
comment|/* Everything else we need is handled by cleanups registered      * when we created mutexes and loaded DSOs      */
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* APU_DSO_BUILD */
end_comment

begin_function
specifier|static
name|apr_status_t
name|dbm_open_type
parameter_list|(
name|apr_dbm_type_t
specifier|const
modifier|*
modifier|*
name|vtable
parameter_list|,
specifier|const
name|char
modifier|*
name|type
parameter_list|,
name|apr_pool_t
modifier|*
name|pool
parameter_list|)
block|{
if|#
directive|if
operator|!
name|APU_DSO_BUILD
operator|*
name|vtable
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
name|type
argument_list|,
literal|"default"
argument_list|)
condition|)
operator|*
name|vtable
operator|=
operator|&
name|DBM_VTABLE
expr_stmt|;
if|#
directive|if
name|APU_HAVE_DB
elseif|else
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
name|type
argument_list|,
literal|"db"
argument_list|)
condition|)
operator|*
name|vtable
operator|=
operator|&
name|apr_dbm_type_db
expr_stmt|;
endif|#
directive|endif
elseif|else
if|if
condition|(
operator|*
name|type
operator|&&
operator|!
name|strcasecmp
argument_list|(
name|type
operator|+
literal|1
argument_list|,
literal|"dbm"
argument_list|)
condition|)
block|{
if|#
directive|if
name|APU_HAVE_GDBM
if|if
condition|(
operator|*
name|type
operator|==
literal|'G'
operator|||
operator|*
name|type
operator|==
literal|'g'
condition|)
operator|*
name|vtable
operator|=
operator|&
name|apr_dbm_type_gdbm
expr_stmt|;
endif|#
directive|endif
if|#
directive|if
name|APU_HAVE_NDBM
if|if
condition|(
operator|*
name|type
operator|==
literal|'N'
operator|||
operator|*
name|type
operator|==
literal|'n'
condition|)
operator|*
name|vtable
operator|=
operator|&
name|apr_dbm_type_ndbm
expr_stmt|;
endif|#
directive|endif
if|#
directive|if
name|APU_HAVE_SDBM
if|if
condition|(
operator|*
name|type
operator|==
literal|'S'
operator|||
operator|*
name|type
operator|==
literal|'s'
condition|)
operator|*
name|vtable
operator|=
operator|&
name|apr_dbm_type_sdbm
expr_stmt|;
endif|#
directive|endif
comment|/* avoid empty block */
empty_stmt|;
block|}
if|if
condition|(
operator|*
name|vtable
condition|)
return|return
name|APR_SUCCESS
return|;
return|return
name|APR_ENOTIMPL
return|;
else|#
directive|else
comment|/* APU_DSO_BUILD */
name|char
name|modname
index|[
literal|32
index|]
decl_stmt|;
name|char
name|symname
index|[
literal|34
index|]
decl_stmt|;
name|apr_dso_handle_sym_t
name|symbol
decl_stmt|;
name|apr_status_t
name|rv
decl_stmt|;
name|int
name|usertype
init|=
literal|0
decl_stmt|;
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
name|type
argument_list|,
literal|"default"
argument_list|)
condition|)
name|type
operator|=
name|DBM_NAME
expr_stmt|;
elseif|else
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
name|type
argument_list|,
literal|"db"
argument_list|)
condition|)
name|type
operator|=
literal|"db"
expr_stmt|;
elseif|else
if|if
condition|(
operator|*
name|type
operator|&&
operator|!
name|strcasecmp
argument_list|(
name|type
operator|+
literal|1
argument_list|,
literal|"dbm"
argument_list|)
condition|)
block|{
if|if
condition|(
operator|*
name|type
operator|==
literal|'G'
operator|||
operator|*
name|type
operator|==
literal|'g'
condition|)
name|type
operator|=
literal|"gdbm"
expr_stmt|;
elseif|else
if|if
condition|(
operator|*
name|type
operator|==
literal|'N'
operator|||
operator|*
name|type
operator|==
literal|'n'
condition|)
name|type
operator|=
literal|"ndbm"
expr_stmt|;
elseif|else
if|if
condition|(
operator|*
name|type
operator|==
literal|'S'
operator|||
operator|*
name|type
operator|==
literal|'s'
condition|)
name|type
operator|=
literal|"sdbm"
expr_stmt|;
block|}
else|else
name|usertype
operator|=
literal|1
expr_stmt|;
if|if
condition|(
name|apr_atomic_inc32
argument_list|(
operator|&
name|initialised
argument_list|)
condition|)
block|{
name|apr_atomic_set32
argument_list|(
operator|&
name|initialised
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* prevent wrap-around */
while|while
condition|(
name|apr_atomic_read32
argument_list|(
operator|&
name|in_init
argument_list|)
condition|)
comment|/* wait until we get fully inited */
empty_stmt|;
block|}
else|else
block|{
name|apr_pool_t
modifier|*
name|parent
decl_stmt|;
comment|/* Top level pool scope, need process-scope lifetime */
for|for
control|(
name|parent
operator|=
name|apr_pool_parent_get
argument_list|(
name|pool
argument_list|)
init|;
name|parent
operator|&&
name|parent
operator|!=
name|pool
condition|;
name|parent
operator|=
name|apr_pool_parent_get
argument_list|(
name|pool
argument_list|)
control|)
name|pool
operator|=
name|parent
expr_stmt|;
comment|/* deprecate in 2.0 - permit implicit initialization */
name|apu_dso_init
argument_list|(
name|pool
argument_list|)
expr_stmt|;
name|drivers
operator|=
name|apr_hash_make
argument_list|(
name|pool
argument_list|)
expr_stmt|;
name|apr_hash_set
argument_list|(
name|drivers
argument_list|,
literal|"sdbm"
argument_list|,
name|APR_HASH_KEY_STRING
argument_list|,
operator|&
name|apr_dbm_type_sdbm
argument_list|)
expr_stmt|;
name|apr_pool_cleanup_register
argument_list|(
name|pool
argument_list|,
name|NULL
argument_list|,
name|dbm_term
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
expr_stmt|;
name|apr_atomic_dec32
argument_list|(
operator|&
name|in_init
argument_list|)
expr_stmt|;
block|}
name|rv
operator|=
name|apu_dso_mutex_lock
argument_list|()
expr_stmt|;
if|if
condition|(
name|rv
condition|)
block|{
operator|*
name|vtable
operator|=
name|NULL
expr_stmt|;
return|return
name|rv
return|;
block|}
operator|*
name|vtable
operator|=
name|apr_hash_get
argument_list|(
name|drivers
argument_list|,
name|type
argument_list|,
name|APR_HASH_KEY_STRING
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|vtable
condition|)
block|{
name|apu_dso_mutex_unlock
argument_list|()
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
comment|/* The driver DSO must have exactly the same lifetime as the      * drivers hash table; ignore the passed-in pool */
name|pool
operator|=
name|apr_hash_pool_get
argument_list|(
name|drivers
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|NETWARE
argument_list|)
name|apr_snprintf
argument_list|(
name|modname
argument_list|,
sizeof|sizeof
argument_list|(
name|modname
argument_list|)
argument_list|,
literal|"dbm%s.nlm"
argument_list|,
name|type
argument_list|)
expr_stmt|;
elif|#
directive|elif
name|defined
argument_list|(
name|WIN32
argument_list|)
operator|||
name|defined
argument_list|(
name|__CYGWIN__
argument_list|)
name|apr_snprintf
argument_list|(
name|modname
argument_list|,
sizeof|sizeof
argument_list|(
name|modname
argument_list|)
argument_list|,
literal|"apr_dbm_%s-"
name|APU_STRINGIFY
argument_list|(
name|APU_MAJOR_VERSION
argument_list|)
literal|".dll"
argument_list|,
name|type
argument_list|)
expr_stmt|;
else|#
directive|else
name|apr_snprintf
argument_list|(
name|modname
argument_list|,
sizeof|sizeof
argument_list|(
name|modname
argument_list|)
argument_list|,
literal|"apr_dbm_%s-"
name|APU_STRINGIFY
argument_list|(
name|APU_MAJOR_VERSION
argument_list|)
literal|".so"
argument_list|,
name|type
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|apr_snprintf
argument_list|(
name|symname
argument_list|,
sizeof|sizeof
argument_list|(
name|symname
argument_list|)
argument_list|,
literal|"apr_dbm_type_%s"
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apu_dso_load
argument_list|(
name|NULL
argument_list|,
operator|&
name|symbol
argument_list|,
name|modname
argument_list|,
name|symname
argument_list|,
name|pool
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|==
name|APR_SUCCESS
operator|||
name|rv
operator|==
name|APR_EINIT
condition|)
block|{
comment|/* previously loaded?!? */
operator|*
name|vtable
operator|=
name|symbol
expr_stmt|;
if|if
condition|(
name|usertype
condition|)
name|type
operator|=
name|apr_pstrdup
argument_list|(
name|pool
argument_list|,
name|type
argument_list|)
expr_stmt|;
name|apr_hash_set
argument_list|(
name|drivers
argument_list|,
name|type
argument_list|,
name|APR_HASH_KEY_STRING
argument_list|,
operator|*
name|vtable
argument_list|)
expr_stmt|;
name|rv
operator|=
name|APR_SUCCESS
expr_stmt|;
block|}
else|else
operator|*
name|vtable
operator|=
name|NULL
expr_stmt|;
name|apu_dso_mutex_unlock
argument_list|()
expr_stmt|;
return|return
name|rv
return|;
endif|#
directive|endif
comment|/* APU_DSO_BUILD */
block|}
end_function

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_open_ex
argument_list|(
argument|apr_dbm_t **pdb
argument_list|,
argument|const char *type
argument_list|,
argument|const char *pathname
argument_list|,
argument|apr_int32_t mode
argument_list|,
argument|apr_fileperms_t perm
argument_list|,
argument|apr_pool_t *pool
argument_list|)
end_macro

begin_block
block|{
name|apr_dbm_type_t
specifier|const
modifier|*
name|vtable
init|=
name|NULL
decl_stmt|;
name|apr_status_t
name|rv
init|=
name|dbm_open_type
argument_list|(
operator|&
name|vtable
argument_list|,
name|type
argument_list|,
name|pool
argument_list|)
decl_stmt|;
if|if
condition|(
name|rv
operator|==
name|APR_SUCCESS
condition|)
block|{
name|rv
operator|=
call|(
name|vtable
operator|->
name|open
call|)
argument_list|(
name|pdb
argument_list|,
name|pathname
argument_list|,
name|mode
argument_list|,
name|perm
argument_list|,
name|pool
argument_list|)
expr_stmt|;
block|}
return|return
name|rv
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_open
argument_list|(
argument|apr_dbm_t **pdb
argument_list|,
argument|const char *pathname
argument_list|,
argument|apr_int32_t mode
argument_list|,
argument|apr_fileperms_t perm
argument_list|,
argument|apr_pool_t *pool
argument_list|)
end_macro

begin_block
block|{
return|return
name|apr_dbm_open_ex
argument_list|(
name|pdb
argument_list|,
name|DBM_NAME
argument_list|,
name|pathname
argument_list|,
name|mode
argument_list|,
name|perm
argument_list|,
name|pool
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|void
argument_list|)
end_macro

begin_macro
name|apr_dbm_close
argument_list|(
argument|apr_dbm_t *dbm
argument_list|)
end_macro

begin_block
block|{
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|close
call|)
argument_list|(
name|dbm
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_fetch
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t key
argument_list|,
argument|apr_datum_t *pvalue
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|fetch
call|)
argument_list|(
name|dbm
argument_list|,
name|key
argument_list|,
name|pvalue
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_store
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t key
argument_list|,
argument|apr_datum_t value
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|store
call|)
argument_list|(
name|dbm
argument_list|,
name|key
argument_list|,
name|value
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_delete
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t key
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|del
call|)
argument_list|(
name|dbm
argument_list|,
name|key
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|int
argument_list|)
end_macro

begin_macro
name|apr_dbm_exists
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t key
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|exists
call|)
argument_list|(
name|dbm
argument_list|,
name|key
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_firstkey
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t *pkey
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|firstkey
call|)
argument_list|(
name|dbm
argument_list|,
name|pkey
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_nextkey
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t *pkey
argument_list|)
end_macro

begin_block
block|{
return|return
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|nextkey
call|)
argument_list|(
name|dbm
argument_list|,
name|pkey
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|void
argument_list|)
end_macro

begin_macro
name|apr_dbm_freedatum
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|apr_datum_t data
argument_list|)
end_macro

begin_block
block|{
call|(
modifier|*
name|dbm
operator|->
name|type
operator|->
name|freedatum
call|)
argument_list|(
name|dbm
argument_list|,
name|data
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|char *
argument_list|)
end_macro

begin_macro
name|apr_dbm_geterror
argument_list|(
argument|apr_dbm_t *dbm
argument_list|,
argument|int *errcode
argument_list|,
argument|char *errbuf
argument_list|,
argument|apr_size_t errbufsize
argument_list|)
end_macro

begin_block
block|{
if|if
condition|(
name|errcode
operator|!=
name|NULL
condition|)
operator|*
name|errcode
operator|=
name|dbm
operator|->
name|errcode
expr_stmt|;
comment|/* assert: errbufsize> 0 */
if|if
condition|(
name|dbm
operator|->
name|errmsg
operator|==
name|NULL
condition|)
operator|*
name|errbuf
operator|=
literal|'\0'
expr_stmt|;
else|else
operator|(
name|void
operator|)
name|apr_cpystrn
argument_list|(
name|errbuf
argument_list|,
name|dbm
operator|->
name|errmsg
argument_list|,
name|errbufsize
argument_list|)
expr_stmt|;
return|return
name|errbuf
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_dbm_get_usednames_ex
argument_list|(
argument|apr_pool_t *p
argument_list|,
argument|const char *type
argument_list|,
argument|const char *pathname
argument_list|,
argument|const char **used1
argument_list|,
argument|const char **used2
argument_list|)
end_macro

begin_block
block|{
name|apr_dbm_type_t
specifier|const
modifier|*
name|vtable
decl_stmt|;
name|apr_status_t
name|rv
init|=
name|dbm_open_type
argument_list|(
operator|&
name|vtable
argument_list|,
name|type
argument_list|,
name|p
argument_list|)
decl_stmt|;
if|if
condition|(
name|rv
operator|==
name|APR_SUCCESS
condition|)
block|{
call|(
name|vtable
operator|->
name|getusednames
call|)
argument_list|(
name|p
argument_list|,
name|pathname
argument_list|,
name|used1
argument_list|,
name|used2
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
return|return
name|rv
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|void
argument_list|)
end_macro

begin_macro
name|apr_dbm_get_usednames
argument_list|(
argument|apr_pool_t *p
argument_list|,
argument|const char *pathname
argument_list|,
argument|const char **used1
argument_list|,
argument|const char **used2
argument_list|)
end_macro

begin_block
block|{
name|apr_dbm_get_usednames_ex
argument_list|(
name|p
argument_list|,
name|DBM_NAME
argument_list|,
name|pathname
argument_list|,
name|used1
argument_list|,
name|used2
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/* Most DBM libraries take a POSIX mode for creating files.  Don't trust  * the mode_t type, some platforms may not support it, int is safe.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|int
argument_list|)
end_macro

begin_macro
name|apr_posix_perms2mode
argument_list|(
argument|apr_fileperms_t perm
argument_list|)
end_macro

begin_block
block|{
name|int
name|mode
init|=
literal|0
decl_stmt|;
name|mode
operator||=
literal|0700
operator|&
operator|(
name|perm
operator|>>
literal|2
operator|)
expr_stmt|;
comment|/* User  is off-by-2 bits */
name|mode
operator||=
literal|0070
operator|&
operator|(
name|perm
operator|>>
literal|1
operator|)
expr_stmt|;
comment|/* Group is off-by-1 bit */
name|mode
operator||=
literal|0007
operator|&
operator|(
name|perm
operator|)
expr_stmt|;
comment|/* World maps 1 for 1 */
return|return
name|mode
return|;
block|}
end_block

end_unit

