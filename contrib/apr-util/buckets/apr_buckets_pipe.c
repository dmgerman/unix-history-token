begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr_buckets.h"
end_include

begin_function
specifier|static
name|apr_status_t
name|pipe_bucket_read
parameter_list|(
name|apr_bucket
modifier|*
name|a
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|str
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|,
name|apr_read_type_e
name|block
parameter_list|)
block|{
name|apr_file_t
modifier|*
name|p
init|=
name|a
operator|->
name|data
decl_stmt|;
name|char
modifier|*
name|buf
decl_stmt|;
name|apr_status_t
name|rv
decl_stmt|;
name|apr_interval_time_t
name|timeout
decl_stmt|;
if|if
condition|(
name|block
operator|==
name|APR_NONBLOCK_READ
condition|)
block|{
name|apr_file_pipe_timeout_get
argument_list|(
name|p
argument_list|,
operator|&
name|timeout
argument_list|)
expr_stmt|;
name|apr_file_pipe_timeout_set
argument_list|(
name|p
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
operator|*
name|str
operator|=
name|NULL
expr_stmt|;
operator|*
name|len
operator|=
name|APR_BUCKET_BUFF_SIZE
expr_stmt|;
name|buf
operator|=
name|apr_bucket_alloc
argument_list|(
operator|*
name|len
argument_list|,
name|a
operator|->
name|list
argument_list|)
expr_stmt|;
comment|/* XXX: check for failure? */
name|rv
operator|=
name|apr_file_read
argument_list|(
name|p
argument_list|,
name|buf
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|block
operator|==
name|APR_NONBLOCK_READ
condition|)
block|{
name|apr_file_pipe_timeout_set
argument_list|(
name|p
argument_list|,
name|timeout
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
operator|&&
name|rv
operator|!=
name|APR_EOF
condition|)
block|{
name|apr_bucket_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
comment|/*      * If there's more to read we have to keep the rest of the pipe      * for later.  Otherwise, we'll close the pipe.      * XXX: Note that more complicated bucket types that       * refer to data not in memory and must therefore have a read()      * function similar to this one should be wary of copying this      * code because if they have a destroy function they probably      * want to migrate the bucket's subordinate structure from the      * old bucket to a raw new one and adjust it as appropriate,      * rather than destroying the old one and creating a completely      * new bucket.      */
if|if
condition|(
operator|*
name|len
operator|>
literal|0
condition|)
block|{
name|apr_bucket_heap
modifier|*
name|h
decl_stmt|;
comment|/* Change the current bucket to refer to what we read */
name|a
operator|=
name|apr_bucket_heap_make
argument_list|(
name|a
argument_list|,
name|buf
argument_list|,
operator|*
name|len
argument_list|,
name|apr_bucket_free
argument_list|)
expr_stmt|;
name|h
operator|=
name|a
operator|->
name|data
expr_stmt|;
name|h
operator|->
name|alloc_len
operator|=
name|APR_BUCKET_BUFF_SIZE
expr_stmt|;
comment|/* note the real buffer size */
operator|*
name|str
operator|=
name|buf
expr_stmt|;
name|APR_BUCKET_INSERT_AFTER
argument_list|(
name|a
argument_list|,
name|apr_bucket_pipe_create
argument_list|(
name|p
argument_list|,
name|a
operator|->
name|list
argument_list|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|apr_bucket_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
name|a
operator|=
name|apr_bucket_immortal_make
argument_list|(
name|a
argument_list|,
literal|""
argument_list|,
literal|0
argument_list|)
expr_stmt|;
operator|*
name|str
operator|=
name|a
operator|->
name|data
expr_stmt|;
if|if
condition|(
name|rv
operator|==
name|APR_EOF
condition|)
block|{
name|apr_file_close
argument_list|(
name|p
argument_list|)
expr_stmt|;
block|}
block|}
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_pipe_make
argument_list|(
argument|apr_bucket *b
argument_list|,
argument|apr_file_t *p
argument_list|)
end_macro

begin_block
block|{
comment|/*      * A pipe is closed when the end is reached in pipe_bucket_read().  If      * the pipe isn't read to the end (e.g., error path), the pipe will be      * closed when its pool goes away.      *      * Note that typically the pipe is allocated from the request pool      * so it will disappear when the request is finished. However the      * core filter may decide to set aside the tail end of a CGI      * response if the connection is pipelined. This turns out not to      * be a problem because the core will have read to the end of the      * stream so the bucket(s) that it sets aside will be the heap      * buckets created by pipe_bucket_read() above.      */
name|b
operator|->
name|type
operator|=
operator|&
name|apr_bucket_type_pipe
expr_stmt|;
name|b
operator|->
name|length
operator|=
call|(
name|apr_size_t
call|)
argument_list|(
operator|-
literal|1
argument_list|)
expr_stmt|;
name|b
operator|->
name|start
operator|=
operator|-
literal|1
expr_stmt|;
name|b
operator|->
name|data
operator|=
name|p
expr_stmt|;
return|return
name|b
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_pipe_create
argument_list|(
argument|apr_file_t *p
argument_list|,
argument|apr_bucket_alloc_t *list
argument_list|)
end_macro

begin_block
block|{
name|apr_bucket
modifier|*
name|b
init|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|b
argument_list|)
argument_list|,
name|list
argument_list|)
decl_stmt|;
name|APR_BUCKET_INIT
argument_list|(
name|b
argument_list|)
expr_stmt|;
name|b
operator|->
name|free
operator|=
name|apr_bucket_free
expr_stmt|;
name|b
operator|->
name|list
operator|=
name|list
expr_stmt|;
return|return
name|apr_bucket_pipe_make
argument_list|(
name|b
argument_list|,
name|p
argument_list|)
return|;
block|}
end_block

begin_decl_stmt
name|APU_DECLARE_DATA
specifier|const
name|apr_bucket_type_t
name|apr_bucket_type_pipe
init|=
block|{
literal|"PIPE"
block|,
literal|5
block|,
name|APR_BUCKET_DATA
block|,
name|apr_bucket_destroy_noop
block|,
name|pipe_bucket_read
block|,
name|apr_bucket_setaside_notimpl
block|,
name|apr_bucket_split_notimpl
block|,
name|apr_bucket_copy_notimpl
block|}
decl_stmt|;
end_decl_stmt

end_unit

