begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr.h"
end_include

begin_include
include|#
directive|include
file|"apr_general.h"
end_include

begin_include
include|#
directive|include
file|"apr_file_io.h"
end_include

begin_include
include|#
directive|include
file|"apr_buckets.h"
end_include

begin_if
if|#
directive|if
name|APR_HAS_MMAP
end_if

begin_include
include|#
directive|include
file|"apr_mmap.h"
end_include

begin_comment
comment|/* mmap support for static files based on ideas from John Heidemann's  * patch against 1.0.5.  See  *<http://www.isi.edu/~johnh/SOFTWARE/APACHE/index.html>.  */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* APR_HAS_MMAP */
end_comment

begin_function
specifier|static
name|void
name|file_bucket_destroy
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
block|{
name|apr_bucket_file
modifier|*
name|f
init|=
name|data
decl_stmt|;
if|if
condition|(
name|apr_bucket_shared_destroy
argument_list|(
name|f
argument_list|)
condition|)
block|{
comment|/* no need to close the file here; it will get          * done automatically when the pool gets cleaned up */
name|apr_bucket_free
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
block|}
end_function

begin_if
if|#
directive|if
name|APR_HAS_MMAP
end_if

begin_function
specifier|static
name|int
name|file_make_mmap
parameter_list|(
name|apr_bucket
modifier|*
name|e
parameter_list|,
name|apr_size_t
name|filelength
parameter_list|,
name|apr_off_t
name|fileoffset
parameter_list|,
name|apr_pool_t
modifier|*
name|p
parameter_list|)
block|{
name|apr_bucket_file
modifier|*
name|a
init|=
name|e
operator|->
name|data
decl_stmt|;
name|apr_mmap_t
modifier|*
name|mm
decl_stmt|;
if|if
condition|(
operator|!
name|a
operator|->
name|can_mmap
condition|)
block|{
return|return
literal|0
return|;
block|}
if|if
condition|(
name|filelength
operator|>
name|APR_MMAP_LIMIT
condition|)
block|{
if|if
condition|(
name|apr_mmap_create
argument_list|(
operator|&
name|mm
argument_list|,
name|a
operator|->
name|fd
argument_list|,
name|fileoffset
argument_list|,
name|APR_MMAP_LIMIT
argument_list|,
name|APR_MMAP_READ
argument_list|,
name|p
argument_list|)
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
literal|0
return|;
block|}
name|apr_bucket_split
argument_list|(
name|e
argument_list|,
name|APR_MMAP_LIMIT
argument_list|)
expr_stmt|;
name|filelength
operator|=
name|APR_MMAP_LIMIT
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|(
name|filelength
operator|<
name|APR_MMAP_THRESHOLD
operator|)
operator|||
operator|(
name|apr_mmap_create
argument_list|(
operator|&
name|mm
argument_list|,
name|a
operator|->
name|fd
argument_list|,
name|fileoffset
argument_list|,
name|filelength
argument_list|,
name|APR_MMAP_READ
argument_list|,
name|p
argument_list|)
operator|!=
name|APR_SUCCESS
operator|)
condition|)
block|{
return|return
literal|0
return|;
block|}
name|apr_bucket_mmap_make
argument_list|(
name|e
argument_list|,
name|mm
argument_list|,
literal|0
argument_list|,
name|filelength
argument_list|)
expr_stmt|;
name|file_bucket_destroy
argument_list|(
name|a
argument_list|)
expr_stmt|;
return|return
literal|1
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|apr_status_t
name|file_bucket_read
parameter_list|(
name|apr_bucket
modifier|*
name|e
parameter_list|,
specifier|const
name|char
modifier|*
modifier|*
name|str
parameter_list|,
name|apr_size_t
modifier|*
name|len
parameter_list|,
name|apr_read_type_e
name|block
parameter_list|)
block|{
name|apr_bucket_file
modifier|*
name|a
init|=
name|e
operator|->
name|data
decl_stmt|;
name|apr_file_t
modifier|*
name|f
init|=
name|a
operator|->
name|fd
decl_stmt|;
name|apr_bucket
modifier|*
name|b
init|=
name|NULL
decl_stmt|;
name|char
modifier|*
name|buf
decl_stmt|;
name|apr_status_t
name|rv
decl_stmt|;
name|apr_size_t
name|filelength
init|=
name|e
operator|->
name|length
decl_stmt|;
comment|/* bytes remaining in file past offset */
name|apr_off_t
name|fileoffset
init|=
name|e
operator|->
name|start
decl_stmt|;
if|#
directive|if
name|APR_HAS_THREADS
operator|&&
operator|!
name|APR_HAS_XTHREAD_FILES
name|apr_int32_t
name|flags
decl_stmt|;
endif|#
directive|endif
if|#
directive|if
name|APR_HAS_MMAP
if|if
condition|(
name|file_make_mmap
argument_list|(
name|e
argument_list|,
name|filelength
argument_list|,
name|fileoffset
argument_list|,
name|a
operator|->
name|readpool
argument_list|)
condition|)
block|{
return|return
name|apr_bucket_read
argument_list|(
name|e
argument_list|,
name|str
argument_list|,
name|len
argument_list|,
name|block
argument_list|)
return|;
block|}
endif|#
directive|endif
if|#
directive|if
name|APR_HAS_THREADS
operator|&&
operator|!
name|APR_HAS_XTHREAD_FILES
if|if
condition|(
operator|(
name|flags
operator|=
name|apr_file_flags_get
argument_list|(
name|f
argument_list|)
operator|)
operator|&
name|APR_FOPEN_XTHREAD
condition|)
block|{
comment|/* this file descriptor is shared across multiple threads and          * this OS doesn't support that natively, so as a workaround          * we must reopen the file into a->readpool */
specifier|const
name|char
modifier|*
name|fname
decl_stmt|;
name|apr_file_name_get
argument_list|(
operator|&
name|fname
argument_list|,
name|f
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_file_open
argument_list|(
operator|&
name|f
argument_list|,
name|fname
argument_list|,
operator|(
name|flags
operator|&
operator|~
name|APR_FOPEN_XTHREAD
operator|)
argument_list|,
literal|0
argument_list|,
name|a
operator|->
name|readpool
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
return|return
name|rv
return|;
name|a
operator|->
name|fd
operator|=
name|f
expr_stmt|;
block|}
endif|#
directive|endif
operator|*
name|len
operator|=
operator|(
name|filelength
operator|>
name|APR_BUCKET_BUFF_SIZE
operator|)
condition|?
name|APR_BUCKET_BUFF_SIZE
else|:
name|filelength
expr_stmt|;
operator|*
name|str
operator|=
name|NULL
expr_stmt|;
comment|/* in case we die prematurely */
name|buf
operator|=
name|apr_bucket_alloc
argument_list|(
operator|*
name|len
argument_list|,
name|e
operator|->
name|list
argument_list|)
expr_stmt|;
comment|/* Handle offset ... */
name|rv
operator|=
name|apr_file_seek
argument_list|(
name|f
argument_list|,
name|APR_SET
argument_list|,
operator|&
name|fileoffset
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_bucket_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
name|rv
operator|=
name|apr_file_read
argument_list|(
name|f
argument_list|,
name|buf
argument_list|,
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
operator|&&
name|rv
operator|!=
name|APR_EOF
condition|)
block|{
name|apr_bucket_free
argument_list|(
name|buf
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
name|filelength
operator|-=
operator|*
name|len
expr_stmt|;
comment|/*      * Change the current bucket to refer to what we read,      * even if we read nothing because we hit EOF.      */
name|apr_bucket_heap_make
argument_list|(
name|e
argument_list|,
name|buf
argument_list|,
operator|*
name|len
argument_list|,
name|apr_bucket_free
argument_list|)
expr_stmt|;
comment|/* If we have more to read from the file, then create another bucket */
if|if
condition|(
name|filelength
operator|>
literal|0
operator|&&
name|rv
operator|!=
name|APR_EOF
condition|)
block|{
comment|/* for efficiency, we can just build a new apr_bucket struct          * to wrap around the existing file bucket */
name|b
operator|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|b
argument_list|)
argument_list|,
name|e
operator|->
name|list
argument_list|)
expr_stmt|;
name|b
operator|->
name|start
operator|=
name|fileoffset
operator|+
operator|(
operator|*
name|len
operator|)
expr_stmt|;
name|b
operator|->
name|length
operator|=
name|filelength
expr_stmt|;
name|b
operator|->
name|data
operator|=
name|a
expr_stmt|;
name|b
operator|->
name|type
operator|=
operator|&
name|apr_bucket_type_file
expr_stmt|;
name|b
operator|->
name|free
operator|=
name|apr_bucket_free
expr_stmt|;
name|b
operator|->
name|list
operator|=
name|e
operator|->
name|list
expr_stmt|;
name|APR_BUCKET_INSERT_AFTER
argument_list|(
name|e
argument_list|,
name|b
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|file_bucket_destroy
argument_list|(
name|a
argument_list|)
expr_stmt|;
block|}
operator|*
name|str
operator|=
name|buf
expr_stmt|;
return|return
name|rv
return|;
block|}
end_function

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_file_make
argument_list|(
argument|apr_bucket *b
argument_list|,
argument|apr_file_t *fd
argument_list|,
argument|apr_off_t offset
argument_list|,
argument|apr_size_t len
argument_list|,
argument|apr_pool_t *p
argument_list|)
end_macro

begin_block
block|{
name|apr_bucket_file
modifier|*
name|f
decl_stmt|;
name|f
operator|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|f
argument_list|)
argument_list|,
name|b
operator|->
name|list
argument_list|)
expr_stmt|;
name|f
operator|->
name|fd
operator|=
name|fd
expr_stmt|;
name|f
operator|->
name|readpool
operator|=
name|p
expr_stmt|;
if|#
directive|if
name|APR_HAS_MMAP
name|f
operator|->
name|can_mmap
operator|=
literal|1
expr_stmt|;
endif|#
directive|endif
name|b
operator|=
name|apr_bucket_shared_make
argument_list|(
name|b
argument_list|,
name|f
argument_list|,
name|offset
argument_list|,
name|len
argument_list|)
expr_stmt|;
name|b
operator|->
name|type
operator|=
operator|&
name|apr_bucket_type_file
expr_stmt|;
return|return
name|b
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_bucket *
argument_list|)
end_macro

begin_macro
name|apr_bucket_file_create
argument_list|(
argument|apr_file_t *fd
argument_list|,
argument|apr_off_t offset
argument_list|,
argument|apr_size_t len
argument_list|,
argument|apr_pool_t *p
argument_list|,
argument|apr_bucket_alloc_t *list
argument_list|)
end_macro

begin_block
block|{
name|apr_bucket
modifier|*
name|b
init|=
name|apr_bucket_alloc
argument_list|(
sizeof|sizeof
argument_list|(
operator|*
name|b
argument_list|)
argument_list|,
name|list
argument_list|)
decl_stmt|;
name|APR_BUCKET_INIT
argument_list|(
name|b
argument_list|)
expr_stmt|;
name|b
operator|->
name|free
operator|=
name|apr_bucket_free
expr_stmt|;
name|b
operator|->
name|list
operator|=
name|list
expr_stmt|;
return|return
name|apr_bucket_file_make
argument_list|(
name|b
argument_list|,
name|fd
argument_list|,
name|offset
argument_list|,
name|len
argument_list|,
name|p
argument_list|)
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_bucket_file_enable_mmap
argument_list|(
argument|apr_bucket *e
argument_list|,
argument|int enabled
argument_list|)
end_macro

begin_block
block|{
if|#
directive|if
name|APR_HAS_MMAP
name|apr_bucket_file
modifier|*
name|a
init|=
name|e
operator|->
name|data
decl_stmt|;
name|a
operator|->
name|can_mmap
operator|=
name|enabled
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
else|#
directive|else
return|return
name|APR_ENOTIMPL
return|;
endif|#
directive|endif
comment|/* APR_HAS_MMAP */
block|}
end_block

begin_function
specifier|static
name|apr_status_t
name|file_bucket_setaside
parameter_list|(
name|apr_bucket
modifier|*
name|data
parameter_list|,
name|apr_pool_t
modifier|*
name|reqpool
parameter_list|)
block|{
name|apr_bucket_file
modifier|*
name|a
init|=
name|data
operator|->
name|data
decl_stmt|;
name|apr_file_t
modifier|*
name|fd
init|=
name|NULL
decl_stmt|;
name|apr_file_t
modifier|*
name|f
init|=
name|a
operator|->
name|fd
decl_stmt|;
name|apr_pool_t
modifier|*
name|curpool
init|=
name|apr_file_pool_get
argument_list|(
name|f
argument_list|)
decl_stmt|;
if|if
condition|(
name|apr_pool_is_ancestor
argument_list|(
name|curpool
argument_list|,
name|reqpool
argument_list|)
condition|)
block|{
return|return
name|APR_SUCCESS
return|;
block|}
if|if
condition|(
operator|!
name|apr_pool_is_ancestor
argument_list|(
name|a
operator|->
name|readpool
argument_list|,
name|reqpool
argument_list|)
condition|)
block|{
name|a
operator|->
name|readpool
operator|=
name|reqpool
expr_stmt|;
block|}
name|apr_file_setaside
argument_list|(
operator|&
name|fd
argument_list|,
name|f
argument_list|,
name|reqpool
argument_list|)
expr_stmt|;
name|a
operator|->
name|fd
operator|=
name|fd
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_decl_stmt
name|APU_DECLARE_DATA
specifier|const
name|apr_bucket_type_t
name|apr_bucket_type_file
init|=
block|{
literal|"FILE"
block|,
literal|5
block|,
name|APR_BUCKET_DATA
block|,
name|file_bucket_destroy
block|,
name|file_bucket_read
block|,
name|file_bucket_setaside
block|,
name|apr_bucket_shared_split
block|,
name|apr_bucket_shared_copy
block|}
decl_stmt|;
end_decl_stmt

end_unit

