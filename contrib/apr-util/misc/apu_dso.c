begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"apu_config.h"
end_include

begin_include
include|#
directive|include
file|"apu.h"
end_include

begin_include
include|#
directive|include
file|"apr_pools.h"
end_include

begin_include
include|#
directive|include
file|"apr_tables.h"
end_include

begin_include
include|#
directive|include
file|"apr_dso.h"
end_include

begin_include
include|#
directive|include
file|"apr_strings.h"
end_include

begin_include
include|#
directive|include
file|"apr_hash.h"
end_include

begin_include
include|#
directive|include
file|"apr_file_io.h"
end_include

begin_include
include|#
directive|include
file|"apr_env.h"
end_include

begin_include
include|#
directive|include
file|"apr_atomic.h"
end_include

begin_include
include|#
directive|include
file|"apu_internal.h"
end_include

begin_include
include|#
directive|include
file|"apu_version.h"
end_include

begin_if
if|#
directive|if
name|APU_DSO_BUILD
end_if

begin_if
if|#
directive|if
name|APR_HAS_THREADS
end_if

begin_decl_stmt
specifier|static
name|apr_thread_mutex_t
modifier|*
name|mutex
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
specifier|static
name|apr_hash_t
modifier|*
name|dsos
init|=
name|NULL
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|apr_uint32_t
name|initialised
init|=
literal|0
decl_stmt|,
name|in_init
init|=
literal|1
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
name|APR_HAS_THREADS
end_if

begin_function
name|apr_status_t
name|apu_dso_mutex_lock
parameter_list|()
block|{
return|return
name|apr_thread_mutex_lock
argument_list|(
name|mutex
argument_list|)
return|;
block|}
end_function

begin_function
name|apr_status_t
name|apu_dso_mutex_unlock
parameter_list|()
block|{
return|return
name|apr_thread_mutex_unlock
argument_list|(
name|mutex
argument_list|)
return|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_function
name|apr_status_t
name|apu_dso_mutex_lock
parameter_list|()
block|{
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_function
name|apr_status_t
name|apu_dso_mutex_unlock
parameter_list|()
block|{
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|apr_status_t
name|apu_dso_term
parameter_list|(
name|void
modifier|*
name|ptr
parameter_list|)
block|{
comment|/* set statics to NULL so init can work again */
name|dsos
operator|=
name|NULL
expr_stmt|;
if|#
directive|if
name|APR_HAS_THREADS
name|mutex
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
comment|/* Everything else we need is handled by cleanups registered      * when we created mutexes and loaded DSOs      */
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_function
name|apr_status_t
name|apu_dso_init
parameter_list|(
name|apr_pool_t
modifier|*
name|pool
parameter_list|)
block|{
name|apr_status_t
name|ret
init|=
name|APR_SUCCESS
decl_stmt|;
name|apr_pool_t
modifier|*
name|parent
decl_stmt|;
if|if
condition|(
name|apr_atomic_inc32
argument_list|(
operator|&
name|initialised
argument_list|)
condition|)
block|{
name|apr_atomic_set32
argument_list|(
operator|&
name|initialised
argument_list|,
literal|1
argument_list|)
expr_stmt|;
comment|/* prevent wrap-around */
while|while
condition|(
name|apr_atomic_read32
argument_list|(
operator|&
name|in_init
argument_list|)
condition|)
comment|/* wait until we get fully inited */
empty_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
comment|/* Top level pool scope, need process-scope lifetime */
for|for
control|(
name|parent
operator|=
name|apr_pool_parent_get
argument_list|(
name|pool
argument_list|)
init|;
name|parent
operator|&&
name|parent
operator|!=
name|pool
condition|;
name|parent
operator|=
name|apr_pool_parent_get
argument_list|(
name|pool
argument_list|)
control|)
name|pool
operator|=
name|parent
expr_stmt|;
name|dsos
operator|=
name|apr_hash_make
argument_list|(
name|pool
argument_list|)
expr_stmt|;
if|#
directive|if
name|APR_HAS_THREADS
name|ret
operator|=
name|apr_thread_mutex_create
argument_list|(
operator|&
name|mutex
argument_list|,
name|APR_THREAD_MUTEX_DEFAULT
argument_list|,
name|pool
argument_list|)
expr_stmt|;
comment|/* This already registers a pool cleanup */
endif|#
directive|endif
name|apr_pool_cleanup_register
argument_list|(
name|pool
argument_list|,
name|NULL
argument_list|,
name|apu_dso_term
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
expr_stmt|;
name|apr_atomic_dec32
argument_list|(
operator|&
name|in_init
argument_list|)
expr_stmt|;
return|return
name|ret
return|;
block|}
end_function

begin_function
name|apr_status_t
name|apu_dso_load
parameter_list|(
name|apr_dso_handle_t
modifier|*
modifier|*
name|dlhandleptr
parameter_list|,
name|apr_dso_handle_sym_t
modifier|*
name|dsoptr
parameter_list|,
specifier|const
name|char
modifier|*
name|module
parameter_list|,
specifier|const
name|char
modifier|*
name|modsym
parameter_list|,
name|apr_pool_t
modifier|*
name|pool
parameter_list|)
block|{
name|apr_dso_handle_t
modifier|*
name|dlhandle
init|=
name|NULL
decl_stmt|;
name|char
modifier|*
name|pathlist
decl_stmt|;
name|char
name|path
index|[
name|APR_PATH_MAX
operator|+
literal|1
index|]
decl_stmt|;
name|apr_array_header_t
modifier|*
name|paths
decl_stmt|;
name|apr_pool_t
modifier|*
name|global
decl_stmt|;
name|apr_status_t
name|rv
init|=
name|APR_EDSOOPEN
decl_stmt|;
name|char
modifier|*
name|eos
init|=
name|NULL
decl_stmt|;
name|int
name|i
decl_stmt|;
operator|*
name|dsoptr
operator|=
name|apr_hash_get
argument_list|(
name|dsos
argument_list|,
name|module
argument_list|,
name|APR_HASH_KEY_STRING
argument_list|)
expr_stmt|;
if|if
condition|(
operator|*
name|dsoptr
condition|)
block|{
return|return
name|APR_EINIT
return|;
block|}
comment|/* The driver DSO must have exactly the same lifetime as the      * drivers hash table; ignore the passed-in pool */
name|global
operator|=
name|apr_hash_pool_get
argument_list|(
name|dsos
argument_list|)
expr_stmt|;
comment|/* Retrieve our path search list or prepare for a single search */
if|if
condition|(
operator|(
name|apr_env_get
argument_list|(
operator|&
name|pathlist
argument_list|,
name|APR_DSOPATH
argument_list|,
name|pool
argument_list|)
operator|!=
name|APR_SUCCESS
operator|)
operator|||
operator|(
name|apr_filepath_list_split
argument_list|(
operator|&
name|paths
argument_list|,
name|pathlist
argument_list|,
name|pool
argument_list|)
operator|!=
name|APR_SUCCESS
operator|)
condition|)
name|paths
operator|=
name|apr_array_make
argument_list|(
name|pool
argument_list|,
literal|1
argument_list|,
sizeof|sizeof
argument_list|(
name|char
operator|*
argument_list|)
argument_list|)
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|APU_DSO_LIBDIR
argument_list|)
comment|/* Always search our prefix path, but on some platforms such as      * win32 this may be left undefined      */
operator|(
operator|*
operator|(
operator|(
name|char
operator|*
operator|*
operator|)
name|apr_array_push
argument_list|(
name|paths
argument_list|)
operator|)
operator|)
operator|=
name|APU_DSO_LIBDIR
expr_stmt|;
endif|#
directive|endif
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|paths
operator|->
name|nelts
condition|;
operator|++
name|i
control|)
block|{
if|#
directive|if
name|defined
argument_list|(
name|WIN32
argument_list|)
comment|/* Use win32 dso search semantics and attempt to          * load the relative lib on the first pass.          */
if|if
condition|(
operator|!
name|eos
condition|)
block|{
name|eos
operator|=
name|path
expr_stmt|;
operator|--
name|i
expr_stmt|;
block|}
else|else
endif|#
directive|endif
block|{
name|eos
operator|=
name|apr_cpystrn
argument_list|(
name|path
argument_list|,
operator|(
operator|(
name|char
operator|*
operator|*
operator|)
name|paths
operator|->
name|elts
operator|)
index|[
name|i
index|]
argument_list|,
sizeof|sizeof
argument_list|(
name|path
argument_list|)
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|eos
operator|>
name|path
operator|)
operator|&&
operator|(
name|eos
operator|-
name|path
operator|<
sizeof|sizeof
argument_list|(
name|path
argument_list|)
operator|-
literal|1
operator|)
condition|)
operator|*
operator|(
name|eos
operator|++
operator|)
operator|=
literal|'/'
expr_stmt|;
block|}
name|apr_cpystrn
argument_list|(
name|eos
argument_list|,
name|module
argument_list|,
sizeof|sizeof
argument_list|(
name|path
argument_list|)
operator|-
operator|(
name|eos
operator|-
name|path
operator|)
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_dso_load
argument_list|(
operator|&
name|dlhandle
argument_list|,
name|path
argument_list|,
name|global
argument_list|)
expr_stmt|;
if|if
condition|(
name|dlhandleptr
condition|)
block|{
operator|*
name|dlhandleptr
operator|=
name|dlhandle
expr_stmt|;
block|}
if|if
condition|(
name|rv
operator|==
name|APR_SUCCESS
condition|)
block|{
comment|/* APR_EDSOOPEN */
break|break;
block|}
if|#
directive|if
name|defined
argument_list|(
name|APU_DSO_LIBDIR
argument_list|)
elseif|else
if|if
condition|(
name|i
operator|<
name|paths
operator|->
name|nelts
operator|-
literal|1
condition|)
block|{
else|#
directive|else
else|else
block|{
comment|/* No APU_DSO_LIBDIR to skip */
endif|#
directive|endif
comment|/* try with apr-util-APU_MAJOR_VERSION appended */
name|eos
operator|=
name|apr_cpystrn
argument_list|(
name|eos
argument_list|,
literal|"apr-util-"
name|APU_STRINGIFY
argument_list|(
name|APU_MAJOR_VERSION
argument_list|)
literal|"/"
argument_list|,
sizeof|sizeof
argument_list|(
name|path
argument_list|)
operator|-
operator|(
name|eos
operator|-
name|path
operator|)
argument_list|)
expr_stmt|;
name|apr_cpystrn
argument_list|(
name|eos
argument_list|,
name|module
argument_list|,
sizeof|sizeof
argument_list|(
name|path
argument_list|)
operator|-
operator|(
name|eos
operator|-
name|path
operator|)
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_dso_load
argument_list|(
operator|&
name|dlhandle
argument_list|,
name|path
argument_list|,
name|global
argument_list|)
expr_stmt|;
if|if
condition|(
name|dlhandleptr
condition|)
block|{
operator|*
name|dlhandleptr
operator|=
name|dlhandle
expr_stmt|;
block|}
if|if
condition|(
name|rv
operator|==
name|APR_SUCCESS
condition|)
block|{
comment|/* APR_EDSOOPEN */
break|break;
block|}
block|}
block|}
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
comment|/* APR_ESYMNOTFOUND */
return|return
name|rv
return|;
name|rv
operator|=
name|apr_dso_sym
argument_list|(
name|dsoptr
argument_list|,
name|dlhandle
argument_list|,
name|modsym
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
comment|/* APR_ESYMNOTFOUND */
name|apr_dso_unload
argument_list|(
name|dlhandle
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|module
operator|=
name|apr_pstrdup
argument_list|(
name|global
argument_list|,
name|module
argument_list|)
expr_stmt|;
name|apr_hash_set
argument_list|(
name|dsos
argument_list|,
name|module
argument_list|,
name|APR_HASH_KEY_STRING
argument_list|,
operator|*
name|dsoptr
argument_list|)
expr_stmt|;
block|}
return|return
name|rv
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* APU_DSO_BUILD */
end_comment

end_unit

