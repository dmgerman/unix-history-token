begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* Licensed to the Apache Software Foundation (ASF) under one or more  * contributor license agreements.  See the NOTICE file distributed with  * this work for additional information regarding copyright ownership.  * The ASF licenses this file to You under the Apache License, Version 2.0  * (the "License"); you may not use this file except in compliance with  * the License.  You may obtain a copy of the License at  *  *     http://www.apache.org/licenses/LICENSE-2.0  *  * Unless required by applicable law or agreed to in writing, software  * distributed under the License is distributed on an "AS IS" BASIS,  * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.  * See the License for the specific language governing permissions and  * limitations under the License.  */
end_comment

begin_include
include|#
directive|include
file|"apr.h"
end_include

begin_if
if|#
directive|if
name|APR_HAVE_STDIO_H
end_if

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|APR_HAVE_STDLIB_H
end_if

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
name|APR_HAVE_UNISTD_H
end_if

begin_include
include|#
directive|include
file|<unistd.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"apu.h"
end_include

begin_include
include|#
directive|include
file|"apr_portable.h"
end_include

begin_include
include|#
directive|include
file|"apr_thread_mutex.h"
end_include

begin_include
include|#
directive|include
file|"apr_thread_cond.h"
end_include

begin_include
include|#
directive|include
file|"apr_errno.h"
end_include

begin_include
include|#
directive|include
file|"apr_queue.h"
end_include

begin_if
if|#
directive|if
name|APR_HAS_THREADS
end_if

begin_comment
comment|/*   * define this to get debug messages  * #define QUEUE_DEBUG  */
end_comment

begin_struct
struct|struct
name|apr_queue_t
block|{
name|void
modifier|*
modifier|*
name|data
decl_stmt|;
name|unsigned
name|int
name|nelts
decl_stmt|;
comment|/**< # elements */
name|unsigned
name|int
name|in
decl_stmt|;
comment|/**< next empty location */
name|unsigned
name|int
name|out
decl_stmt|;
comment|/**< next filled location */
name|unsigned
name|int
name|bounds
decl_stmt|;
comment|/**< max size of queue */
name|unsigned
name|int
name|full_waiters
decl_stmt|;
name|unsigned
name|int
name|empty_waiters
decl_stmt|;
name|apr_thread_mutex_t
modifier|*
name|one_big_mutex
decl_stmt|;
name|apr_thread_cond_t
modifier|*
name|not_empty
decl_stmt|;
name|apr_thread_cond_t
modifier|*
name|not_full
decl_stmt|;
name|int
name|terminated
decl_stmt|;
block|}
struct|;
end_struct

begin_ifdef
ifdef|#
directive|ifdef
name|QUEUE_DEBUG
end_ifdef

begin_function
specifier|static
name|void
name|Q_DBG
parameter_list|(
name|char
modifier|*
name|msg
parameter_list|,
name|apr_queue_t
modifier|*
name|q
parameter_list|)
block|{
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%ld\t#%d in %d out %d\t%s\n"
argument_list|,
name|apr_os_thread_current
argument_list|()
argument_list|,
name|q
operator|->
name|nelts
argument_list|,
name|q
operator|->
name|in
argument_list|,
name|q
operator|->
name|out
argument_list|,
name|msg
argument_list|)
expr_stmt|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|Q_DBG
parameter_list|(
name|x
parameter_list|,
name|y
parameter_list|)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/**  * Detects when the apr_queue_t is full. This utility function is expected  * to be called from within critical sections, and is not threadsafe.  */
end_comment

begin_define
define|#
directive|define
name|apr_queue_full
parameter_list|(
name|queue
parameter_list|)
value|((queue)->nelts == (queue)->bounds)
end_define

begin_comment
comment|/**  * Detects when the apr_queue_t is empty. This utility function is expected  * to be called from within critical sections, and is not threadsafe.  */
end_comment

begin_define
define|#
directive|define
name|apr_queue_empty
parameter_list|(
name|queue
parameter_list|)
value|((queue)->nelts == 0)
end_define

begin_comment
comment|/**  * Callback routine that is called to destroy this  * apr_queue_t when its pool is destroyed.  */
end_comment

begin_function
specifier|static
name|apr_status_t
name|queue_destroy
parameter_list|(
name|void
modifier|*
name|data
parameter_list|)
block|{
name|apr_queue_t
modifier|*
name|queue
init|=
name|data
decl_stmt|;
comment|/* Ignore errors here, we can't do anything about them anyway. */
name|apr_thread_cond_destroy
argument_list|(
name|queue
operator|->
name|not_empty
argument_list|)
expr_stmt|;
name|apr_thread_cond_destroy
argument_list|(
name|queue
operator|->
name|not_full
argument_list|)
expr_stmt|;
name|apr_thread_mutex_destroy
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_function

begin_comment
comment|/**  * Initialize the apr_queue_t.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_create
argument_list|(
argument|apr_queue_t **q
argument_list|,
argument|unsigned int queue_capacity
argument_list|,
argument|apr_pool_t *a
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
name|apr_queue_t
modifier|*
name|queue
decl_stmt|;
name|queue
operator|=
name|apr_palloc
argument_list|(
name|a
argument_list|,
sizeof|sizeof
argument_list|(
name|apr_queue_t
argument_list|)
argument_list|)
expr_stmt|;
operator|*
name|q
operator|=
name|queue
expr_stmt|;
comment|/* nested doesn't work ;( */
name|rv
operator|=
name|apr_thread_mutex_create
argument_list|(
operator|&
name|queue
operator|->
name|one_big_mutex
argument_list|,
name|APR_THREAD_MUTEX_UNNESTED
argument_list|,
name|a
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
name|rv
operator|=
name|apr_thread_cond_create
argument_list|(
operator|&
name|queue
operator|->
name|not_empty
argument_list|,
name|a
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
name|rv
operator|=
name|apr_thread_cond_create
argument_list|(
operator|&
name|queue
operator|->
name|not_full
argument_list|,
name|a
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
comment|/* Set all the data in the queue to NULL */
name|queue
operator|->
name|data
operator|=
name|apr_pcalloc
argument_list|(
name|a
argument_list|,
name|queue_capacity
operator|*
sizeof|sizeof
argument_list|(
name|void
operator|*
argument_list|)
argument_list|)
expr_stmt|;
name|queue
operator|->
name|bounds
operator|=
name|queue_capacity
expr_stmt|;
name|queue
operator|->
name|nelts
operator|=
literal|0
expr_stmt|;
name|queue
operator|->
name|in
operator|=
literal|0
expr_stmt|;
name|queue
operator|->
name|out
operator|=
literal|0
expr_stmt|;
name|queue
operator|->
name|terminated
operator|=
literal|0
expr_stmt|;
name|queue
operator|->
name|full_waiters
operator|=
literal|0
expr_stmt|;
name|queue
operator|->
name|empty_waiters
operator|=
literal|0
expr_stmt|;
name|apr_pool_cleanup_register
argument_list|(
name|a
argument_list|,
name|queue
argument_list|,
name|queue_destroy
argument_list|,
name|apr_pool_cleanup_null
argument_list|)
expr_stmt|;
return|return
name|APR_SUCCESS
return|;
block|}
end_block

begin_comment
comment|/**  * Push new data onto the queue. Blocks if the queue is full. Once  * the push operation has completed, it signals other threads waiting  * in apr_queue_pop() that they may continue consuming sockets.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_push
argument_list|(
argument|apr_queue_t *queue
argument_list|,
argument|void *data
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
if|if
condition|(
name|apr_queue_full
argument_list|(
name|queue
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|queue
operator|->
name|terminated
condition|)
block|{
name|queue
operator|->
name|full_waiters
operator|++
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_wait
argument_list|(
name|queue
operator|->
name|not_full
argument_list|,
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
name|queue
operator|->
name|full_waiters
operator|--
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
comment|/* If we wake up and it's still empty, then we were interrupted */
if|if
condition|(
name|apr_queue_full
argument_list|(
name|queue
argument_list|)
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"queue full (intr)"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
else|else
block|{
return|return
name|APR_EINTR
return|;
block|}
block|}
block|}
name|queue
operator|->
name|data
index|[
name|queue
operator|->
name|in
index|]
operator|=
name|data
expr_stmt|;
name|queue
operator|->
name|in
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|in
operator|>=
name|queue
operator|->
name|bounds
condition|)
name|queue
operator|->
name|in
operator|-=
name|queue
operator|->
name|bounds
expr_stmt|;
name|queue
operator|->
name|nelts
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|empty_waiters
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"sig !empty"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_signal
argument_list|(
name|queue
operator|->
name|not_empty
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
end_block

begin_comment
comment|/**  * Push new data onto the queue. If the queue is full, return APR_EAGAIN. If  * the push operation completes successfully, it signals other threads  * waiting in apr_queue_pop() that they may continue consuming sockets.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_trypush
argument_list|(
argument|apr_queue_t *queue
argument_list|,
argument|void *data
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
if|if
condition|(
name|apr_queue_full
argument_list|(
name|queue
argument_list|)
condition|)
block|{
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|APR_EAGAIN
return|;
block|}
name|queue
operator|->
name|data
index|[
name|queue
operator|->
name|in
index|]
operator|=
name|data
expr_stmt|;
name|queue
operator|->
name|in
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|in
operator|>=
name|queue
operator|->
name|bounds
condition|)
name|queue
operator|->
name|in
operator|-=
name|queue
operator|->
name|bounds
expr_stmt|;
name|queue
operator|->
name|nelts
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|empty_waiters
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"sig !empty"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_signal
argument_list|(
name|queue
operator|->
name|not_empty
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
end_block

begin_comment
comment|/**  * not thread safe  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|unsigned int
argument_list|)
end_macro

begin_macro
name|apr_queue_size
argument_list|(
argument|apr_queue_t *queue
argument_list|)
end_macro

begin_block
block|{
return|return
name|queue
operator|->
name|nelts
return|;
block|}
end_block

begin_comment
comment|/**  * Retrieves the next item from the queue. If there are no  * items available, it will block until one becomes available.  * Once retrieved, the item is placed into the address specified by  * 'data'.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_pop
argument_list|(
argument|apr_queue_t *queue
argument_list|,
argument|void **data
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
comment|/* Keep waiting until we wake up and find that the queue is not empty. */
if|if
condition|(
name|apr_queue_empty
argument_list|(
name|queue
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|queue
operator|->
name|terminated
condition|)
block|{
name|queue
operator|->
name|empty_waiters
operator|++
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_wait
argument_list|(
name|queue
operator|->
name|not_empty
argument_list|,
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
name|queue
operator|->
name|empty_waiters
operator|--
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
comment|/* If we wake up and it's still empty, then we were interrupted */
if|if
condition|(
name|apr_queue_empty
argument_list|(
name|queue
argument_list|)
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"queue empty (intr)"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
else|else
block|{
return|return
name|APR_EINTR
return|;
block|}
block|}
block|}
operator|*
name|data
operator|=
name|queue
operator|->
name|data
index|[
name|queue
operator|->
name|out
index|]
expr_stmt|;
name|queue
operator|->
name|nelts
operator|--
expr_stmt|;
name|queue
operator|->
name|out
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|out
operator|>=
name|queue
operator|->
name|bounds
condition|)
name|queue
operator|->
name|out
operator|-=
name|queue
operator|->
name|bounds
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|full_waiters
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"signal !full"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_signal
argument_list|(
name|queue
operator|->
name|not_full
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
end_block

begin_comment
comment|/**  * Retrieves the next item from the queue. If there are no  * items available, return APR_EAGAIN.  Once retrieved,  * the item is placed into the address specified by 'data'.  */
end_comment

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_trypop
argument_list|(
argument|apr_queue_t *queue
argument_list|,
argument|void **data
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
if|if
condition|(
name|queue
operator|->
name|terminated
condition|)
block|{
return|return
name|APR_EOF
return|;
comment|/* no more elements ever again */
block|}
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
if|if
condition|(
name|apr_queue_empty
argument_list|(
name|queue
argument_list|)
condition|)
block|{
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|APR_EAGAIN
return|;
block|}
operator|*
name|data
operator|=
name|queue
operator|->
name|data
index|[
name|queue
operator|->
name|out
index|]
expr_stmt|;
name|queue
operator|->
name|nelts
operator|--
expr_stmt|;
name|queue
operator|->
name|out
operator|++
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|out
operator|>=
name|queue
operator|->
name|bounds
condition|)
name|queue
operator|->
name|out
operator|-=
name|queue
operator|->
name|bounds
expr_stmt|;
if|if
condition|(
name|queue
operator|->
name|full_waiters
condition|)
block|{
name|Q_DBG
argument_list|(
literal|"signal !full"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
name|rv
operator|=
name|apr_thread_cond_signal
argument_list|(
name|queue
operator|->
name|not_full
argument_list|)
expr_stmt|;
if|if
condition|(
name|rv
operator|!=
name|APR_SUCCESS
condition|)
block|{
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
block|}
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
expr_stmt|;
return|return
name|rv
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_interrupt_all
argument_list|(
argument|apr_queue_t *queue
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
name|Q_DBG
argument_list|(
literal|"intr all"
argument_list|,
name|queue
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
operator|)
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
name|apr_thread_cond_broadcast
argument_list|(
name|queue
operator|->
name|not_empty
argument_list|)
expr_stmt|;
name|apr_thread_cond_broadcast
argument_list|(
name|queue
operator|->
name|not_full
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
operator|)
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
return|return
name|APR_SUCCESS
return|;
block|}
end_block

begin_macro
name|APU_DECLARE
argument_list|(
argument|apr_status_t
argument_list|)
end_macro

begin_macro
name|apr_queue_term
argument_list|(
argument|apr_queue_t *queue
argument_list|)
end_macro

begin_block
block|{
name|apr_status_t
name|rv
decl_stmt|;
if|if
condition|(
operator|(
name|rv
operator|=
name|apr_thread_mutex_lock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
operator|)
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
comment|/* we must hold one_big_mutex when setting this... otherwise,      * we could end up setting it and waking everybody up just after a       * would-be popper checks it but right before they block      */
name|queue
operator|->
name|terminated
operator|=
literal|1
expr_stmt|;
if|if
condition|(
operator|(
name|rv
operator|=
name|apr_thread_mutex_unlock
argument_list|(
name|queue
operator|->
name|one_big_mutex
argument_list|)
operator|)
operator|!=
name|APR_SUCCESS
condition|)
block|{
return|return
name|rv
return|;
block|}
return|return
name|apr_queue_interrupt_all
argument_list|(
name|queue
argument_list|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* APR_HAS_THREADS */
end_comment

end_unit

