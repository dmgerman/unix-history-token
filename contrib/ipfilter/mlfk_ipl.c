begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/* $FreeBSD$ */
end_comment

begin_comment
comment|/*  * Copyright (C) 2012 by Darren Reed.  *  * See the IPFILTER.LICENCE file for details on licencing.  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/kernel.h>
end_include

begin_include
include|#
directive|include
file|<sys/module.h>
end_include

begin_include
include|#
directive|include
file|<sys/conf.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<sys/sysctl.h>
end_include

begin_include
include|#
directive|include
file|<sys/select.h>
end_include

begin_if
if|#
directive|if
name|__FreeBSD_version
operator|>=
literal|500000
end_if

begin_include
include|#
directive|include
file|<sys/selinfo.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<net/if.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in_systm.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|"netinet/ipl.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_compat.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_fil.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_state.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_nat.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_auth.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_frag.h"
end_include

begin_include
include|#
directive|include
file|"netinet/ip_sync.h"
end_include

begin_decl_stmt
specifier|extern
name|ipf_main_softc_t
name|ipfmain
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
name|__FreeBSD_version
operator|>=
literal|502116
end_if

begin_decl_stmt
specifier|static
name|struct
name|cdev
modifier|*
name|ipf_devs
index|[
name|IPL_LOGSIZE
index|]
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|dev_t
name|ipf_devs
index|[
name|IPL_LOGSIZE
index|]
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|static int sysctl_ipf_int ( SYSCTL_HANDLER_ARGS );
endif|#
directive|endif
end_endif

begin_function_decl
specifier|static
name|int
name|ipf_modload
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_function_decl
specifier|static
name|int
name|ipf_modunload
parameter_list|(
name|void
parameter_list|)
function_decl|;
end_function_decl

begin_if
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|500024
operator|)
end_if

begin_if
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
end_if

begin_decl_stmt
specifier|static
name|int
name|ipfopen
name|__P
argument_list|(
operator|(
expr|struct
name|cdev
operator|*
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|thread
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|ipfclose
name|__P
argument_list|(
operator|(
expr|struct
name|cdev
operator|*
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|thread
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|int
name|ipfopen
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|thread
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|ipfclose
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|thread
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __FreeBSD_version>= 502116 */
end_comment

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|int
name|ipfopen
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|proc
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|ipfclose
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
name|int
operator|,
name|int
operator|,
expr|struct
name|proc
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
end_if

begin_decl_stmt
specifier|static
name|int
name|ipfread
name|__P
argument_list|(
operator|(
expr|struct
name|cdev
operator|*
operator|,
expr|struct
name|uio
operator|*
operator|,
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|ipfwrite
name|__P
argument_list|(
operator|(
expr|struct
name|cdev
operator|*
operator|,
expr|struct
name|uio
operator|*
operator|,
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
specifier|static
name|int
name|ipfread
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
expr|struct
name|uio
operator|*
operator|,
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|int
name|ipfwrite
name|__P
argument_list|(
operator|(
name|dev_t
operator|,
expr|struct
name|uio
operator|*
operator|,
name|int
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __FreeBSD_version>= 502116 */
end_comment

begin_expr_stmt
name|SYSCTL_DECL
argument_list|(
name|_net_inet
argument_list|)
expr_stmt|;
end_expr_stmt

begin_define
define|#
directive|define
name|SYSCTL_IPF
parameter_list|(
name|parent
parameter_list|,
name|nbr
parameter_list|,
name|name
parameter_list|,
name|access
parameter_list|,
name|ptr
parameter_list|,
name|val
parameter_list|,
name|descr
parameter_list|)
define|\
value|SYSCTL_OID(parent, nbr, name, CTLTYPE_INT|access, \ 		   ptr, val, sysctl_ipf_int, "I", descr);
end_define

begin_define
define|#
directive|define
name|CTLFLAG_OFF
value|0x00800000
end_define

begin_comment
comment|/* IPFilter must be disabled */
end_comment

begin_define
define|#
directive|define
name|CTLFLAG_RWO
value|(CTLFLAG_RW|CTLFLAG_OFF)
end_define

begin_expr_stmt
name|SYSCTL_NODE
argument_list|(
name|_net_inet
argument_list|,
name|OID_AUTO
argument_list|,
name|ipf
argument_list|,
name|CTLFLAG_RW
argument_list|,
literal|0
argument_list|,
literal|"IPF"
argument_list|)
expr_stmt|;
end_expr_stmt

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_flags, CTLFLAG_RW,&ipf_flags, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_pass, CTLFLAG_RW,&ipf_pass, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_active, CTLFLAG_RD,&ipf_active, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcpidletimeout, CTLFLAG_RWO,&ipf_tcpidletimeout, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcphalfclosed, CTLFLAG_RWO,&ipf_tcphalfclosed, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcpclosewait, CTLFLAG_RWO,&ipf_tcpclosewait, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcplastack, CTLFLAG_RWO,&ipf_tcplastack, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcptimeout, CTLFLAG_RWO,&ipf_tcptimeout, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_tcpclosed, CTLFLAG_RWO,&ipf_tcpclosed, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_udptimeout, CTLFLAG_RWO,&ipf_udptimeout, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_udpacktimeout, CTLFLAG_RWO,&ipf_udpacktimeout, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_icmptimeout, CTLFLAG_RWO,&ipf_icmptimeout, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_defnatage, CTLFLAG_RWO,&ipf_nat_defage, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_ipfrttl, CTLFLAG_RW,&ipf_ipfrttl, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_running, CTLFLAG_RD,&ipf_running, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_statesize, CTLFLAG_RWO,&ipf_state_size, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_statemax, CTLFLAG_RWO,&ipf_state_max, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_nattable_sz, CTLFLAG_RWO,&ipf_nat_table_sz, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_natrules_sz, CTLFLAG_RWO,&ipf_nat_maprules_sz, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_rdrrules_sz, CTLFLAG_RWO,&ipf_nat_rdrrules_sz, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, ipf_hostmap_sz, CTLFLAG_RWO,&ipf_nat_hostmap_sz, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_authsize, CTLFLAG_RWO,&ipf_auth_size, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_authused, CTLFLAG_RD,&ipf_auth_used, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_defaultauthage, CTLFLAG_RW,&ipf_auth_defaultage, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_chksrc, CTLFLAG_RW,&ipf_chksrc, 0, ""); SYSCTL_IPF(_net_inet_ipf, OID_AUTO, fr_minttl, CTLFLAG_RW,&ipf_minttl, 0, "");
endif|#
directive|endif
end_endif

begin_define
define|#
directive|define
name|CDEV_MAJOR
value|79
end_define

begin_include
include|#
directive|include
file|<sys/poll.h>
end_include

begin_if
if|#
directive|if
name|__FreeBSD_version
operator|>=
literal|500043
end_if

begin_include
include|#
directive|include
file|<sys/select.h>
end_include

begin_function_decl
specifier|static
name|int
name|ipfpoll
parameter_list|(
name|struct
name|cdev
modifier|*
name|dev
parameter_list|,
name|int
name|events
parameter_list|,
name|struct
name|thread
modifier|*
name|td
parameter_list|)
function_decl|;
end_function_decl

begin_decl_stmt
specifier|static
name|struct
name|cdevsw
name|ipf_cdevsw
init|=
block|{
if|#
directive|if
name|__FreeBSD_version
operator|>=
literal|502103
operator|.
name|d_version
operator|=
name|D_VERSION
block|,
operator|.
name|d_flags
operator|=
literal|0
block|,
comment|/* D_NEEDGIANT - Should be SMP safe */
endif|#
directive|endif
operator|.
name|d_open
operator|=
name|ipfopen
block|,
operator|.
name|d_close
operator|=
name|ipfclose
block|,
operator|.
name|d_read
operator|=
name|ipfread
block|,
operator|.
name|d_write
operator|=
name|ipfwrite
block|,
operator|.
name|d_ioctl
operator|=
name|ipfioctl
block|,
operator|.
name|d_poll
operator|=
name|ipfpoll
block|,
operator|.
name|d_name
operator|=
literal|"ipf"
block|,
if|#
directive|if
name|__FreeBSD_version
operator|<
literal|600000
operator|.
name|d_maj
operator|=
name|CDEV_MAJOR
block|,
endif|#
directive|endif
block|}
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_function_decl
specifier|static
name|int
name|ipfpoll
parameter_list|(
name|dev_t
name|dev
parameter_list|,
name|int
name|events
parameter_list|,
name|struct
name|proc
modifier|*
name|td
parameter_list|)
function_decl|;
end_function_decl

begin_decl_stmt
specifier|static
name|struct
name|cdevsw
name|ipf_cdevsw
init|=
block|{
comment|/* open */
name|ipfopen
block|,
comment|/* close */
name|ipfclose
block|,
comment|/* read */
name|ipfread
block|,
comment|/* write */
name|ipfwrite
block|,
comment|/* ioctl */
name|ipfioctl
block|,
comment|/* poll */
name|ipfpoll
block|,
comment|/* mmap */
name|nommap
block|,
comment|/* strategy */
name|nostrategy
block|,
comment|/* name */
literal|"ipf"
block|,
comment|/* maj */
name|CDEV_MAJOR
block|,
comment|/* dump */
name|nodump
block|,
comment|/* psize */
name|nopsize
block|,
comment|/* flags */
literal|0
block|,
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|<
literal|500043
operator|)
comment|/* bmaj */
operator|-
literal|1
block|,
endif|#
directive|endif
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|430000
operator|)
comment|/* kqfilter */
name|NULL
endif|#
directive|endif
block|}
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
specifier|static
name|char
modifier|*
name|ipf_devfiles
index|[]
init|=
block|{
name|IPL_NAME
block|,
name|IPNAT_NAME
block|,
name|IPSTATE_NAME
block|,
name|IPAUTH_NAME
block|,
name|IPSYNC_NAME
block|,
name|IPSCAN_NAME
block|,
name|IPLOOKUP_NAME
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_function
specifier|static
name|int
name|ipfilter_modevent
parameter_list|(
name|module_t
name|mod
parameter_list|,
name|int
name|type
parameter_list|,
name|void
modifier|*
name|unused
parameter_list|)
block|{
name|int
name|error
init|=
literal|0
decl_stmt|;
switch|switch
condition|(
name|type
condition|)
block|{
case|case
name|MOD_LOAD
case|:
name|error
operator|=
name|ipf_modload
argument_list|()
expr_stmt|;
break|break;
case|case
name|MOD_UNLOAD
case|:
name|error
operator|=
name|ipf_modunload
argument_list|()
expr_stmt|;
break|break;
default|default:
name|error
operator|=
name|EINVAL
expr_stmt|;
break|break;
block|}
return|return
name|error
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|ipf_modload
parameter_list|()
block|{
name|char
modifier|*
name|defpass
decl_stmt|,
modifier|*
name|c
decl_stmt|,
modifier|*
name|str
decl_stmt|;
name|int
name|i
decl_stmt|,
name|j
decl_stmt|,
name|error
decl_stmt|;
if|if
condition|(
name|ipf_load_all
argument_list|()
operator|!=
literal|0
condition|)
return|return
name|EIO
return|;
if|if
condition|(
name|ipf_create_all
argument_list|(
operator|&
name|ipfmain
argument_list|)
operator|==
name|NULL
condition|)
return|return
name|EIO
return|;
name|error
operator|=
name|ipfattach
argument_list|(
operator|&
name|ipfmain
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
condition|)
return|return
name|error
return|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|IPL_LOGSIZE
condition|;
name|i
operator|++
control|)
name|ipf_devs
index|[
name|i
index|]
operator|=
name|NULL
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
operator|(
name|str
operator|=
name|ipf_devfiles
index|[
name|i
index|]
operator|)
condition|;
name|i
operator|++
control|)
block|{
name|c
operator|=
name|NULL
expr_stmt|;
for|for
control|(
name|j
operator|=
name|strlen
argument_list|(
name|str
argument_list|)
init|;
name|j
operator|>
literal|0
condition|;
name|j
operator|--
control|)
if|if
condition|(
name|str
index|[
name|j
index|]
operator|==
literal|'/'
condition|)
block|{
name|c
operator|=
name|str
operator|+
name|j
operator|+
literal|1
expr_stmt|;
break|break;
block|}
if|if
condition|(
operator|!
name|c
condition|)
name|c
operator|=
name|str
expr_stmt|;
name|ipf_devs
index|[
name|i
index|]
operator|=
name|make_dev
argument_list|(
operator|&
name|ipf_cdevsw
argument_list|,
name|i
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
literal|0600
argument_list|,
name|c
argument_list|)
expr_stmt|;
block|}
name|error
operator|=
name|ipf_pfil_hook
argument_list|()
expr_stmt|;
if|if
condition|(
name|error
operator|!=
literal|0
condition|)
return|return
name|error
return|;
name|ipf_event_reg
argument_list|()
expr_stmt|;
if|if
condition|(
name|FR_ISPASS
argument_list|(
name|ipfmain
operator|.
name|ipf_pass
argument_list|)
condition|)
name|defpass
operator|=
literal|"pass"
expr_stmt|;
elseif|else
if|if
condition|(
name|FR_ISBLOCK
argument_list|(
name|ipfmain
operator|.
name|ipf_pass
argument_list|)
condition|)
name|defpass
operator|=
literal|"block"
expr_stmt|;
else|else
name|defpass
operator|=
literal|"no-match -> block"
expr_stmt|;
name|printf
argument_list|(
literal|"%s initialized.  Default = %s all, Logging = %s%s\n"
argument_list|,
name|ipfilter_version
argument_list|,
name|defpass
argument_list|,
ifdef|#
directive|ifdef
name|IPFILTER_LOG
literal|"enabled"
argument_list|,
else|#
directive|else
literal|"disabled"
argument_list|,
endif|#
directive|endif
ifdef|#
directive|ifdef
name|IPFILTER_COMPILED
literal|" (COMPILED)"
else|#
directive|else
literal|""
endif|#
directive|endif
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

begin_function
specifier|static
name|int
name|ipf_modunload
parameter_list|()
block|{
name|int
name|error
decl_stmt|,
name|i
decl_stmt|;
if|if
condition|(
name|ipfmain
operator|.
name|ipf_refcnt
condition|)
return|return
name|EBUSY
return|;
name|error
operator|=
name|ipf_pfil_unhook
argument_list|()
expr_stmt|;
if|if
condition|(
name|error
operator|!=
literal|0
condition|)
return|return
name|error
return|;
if|if
condition|(
name|ipfmain
operator|.
name|ipf_running
operator|>=
literal|0
condition|)
block|{
name|error
operator|=
name|ipfdetach
argument_list|(
operator|&
name|ipfmain
argument_list|)
expr_stmt|;
if|if
condition|(
name|error
operator|!=
literal|0
condition|)
return|return
name|error
return|;
name|ipf_destroy_all
argument_list|(
operator|&
name|ipfmain
argument_list|)
expr_stmt|;
name|ipf_unload_all
argument_list|()
expr_stmt|;
block|}
else|else
name|error
operator|=
literal|0
expr_stmt|;
name|ipfmain
operator|.
name|ipf_running
operator|=
operator|-
literal|2
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|ipf_devfiles
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|ipf_devs
index|[
name|i
index|]
operator|!=
name|NULL
condition|)
name|destroy_dev
argument_list|(
name|ipf_devs
index|[
name|i
index|]
argument_list|)
expr_stmt|;
block|}
name|printf
argument_list|(
literal|"%s unloaded\n"
argument_list|,
name|ipfilter_version
argument_list|)
expr_stmt|;
return|return
name|error
return|;
block|}
end_function

begin_decl_stmt
specifier|static
name|moduledata_t
name|ipfiltermod
init|=
block|{
literal|"ipfilter"
block|,
name|ipfilter_modevent
block|,
literal|0
block|}
decl_stmt|;
end_decl_stmt

begin_expr_stmt
name|DECLARE_MODULE
argument_list|(
name|ipfilter
argument_list|,
name|ipfiltermod
argument_list|,
name|SI_SUB_PROTO_DOMAIN
argument_list|,
name|SI_ORDER_ANY
argument_list|)
expr_stmt|;
end_expr_stmt

begin_ifdef
ifdef|#
directive|ifdef
name|MODULE_VERSION
end_ifdef

begin_expr_stmt
name|MODULE_VERSION
argument_list|(
name|ipfilter
argument_list|,
literal|1
argument_list|)
expr_stmt|;
end_expr_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
literal|0
end_if

begin_ifdef
ifdef|#
directive|ifdef
name|SYSCTL_IPF
end_ifdef

begin_endif
unit|int sysctl_ipf_int ( SYSCTL_HANDLER_ARGS ) { 	int error = 0;  	if (arg1) 		error = SYSCTL_OUT(req, arg1, sizeof(int)); 	else 		error = SYSCTL_OUT(req,&arg2, sizeof(int));  	if (error || !req->newptr) 		return (error);  	if (!arg1) 		error = EPERM; 	else { 		if ((oidp->oid_kind& CTLFLAG_OFF)&& (ipfmain.ipf_running> 0)) 			error = EBUSY; 		else 			error = SYSCTL_IN(req, arg1, sizeof(int)); 	} 	return (error); }
endif|#
directive|endif
end_endif

begin_endif
endif|#
directive|endif
end_endif

begin_function
specifier|static
name|int
if|#
directive|if
name|__FreeBSD_version
operator|>=
literal|500043
name|ipfpoll
parameter_list|(
name|struct
name|cdev
modifier|*
name|dev
parameter_list|,
name|int
name|events
parameter_list|,
name|struct
name|thread
modifier|*
name|td
parameter_list|)
else|#
directive|else
function|ipfpoll
parameter_list|(
name|dev_t
name|dev
parameter_list|,
name|int
name|events
parameter_list|,
name|struct
name|proc
modifier|*
name|td
parameter_list|)
endif|#
directive|endif
block|{
name|u_int
name|unit
init|=
name|GET_MINOR
argument_list|(
name|dev
argument_list|)
decl_stmt|;
name|int
name|revents
decl_stmt|;
if|if
condition|(
name|unit
operator|<
literal|0
operator|||
name|unit
operator|>
name|IPL_LOGMAX
condition|)
return|return
literal|0
return|;
name|revents
operator|=
literal|0
expr_stmt|;
switch|switch
condition|(
name|unit
condition|)
block|{
case|case
name|IPL_LOGIPF
case|:
case|case
name|IPL_LOGNAT
case|:
case|case
name|IPL_LOGSTATE
case|:
ifdef|#
directive|ifdef
name|IPFILTER_LOG
if|if
condition|(
operator|(
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
operator|)
operator|&&
name|ipf_log_canread
argument_list|(
operator|&
name|ipfmain
argument_list|,
name|unit
argument_list|)
condition|)
name|revents
operator||=
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
expr_stmt|;
endif|#
directive|endif
break|break;
case|case
name|IPL_LOGAUTH
case|:
if|if
condition|(
operator|(
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
operator|)
operator|&&
name|ipf_auth_waiting
argument_list|(
operator|&
name|ipfmain
argument_list|)
condition|)
name|revents
operator||=
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
expr_stmt|;
break|break;
case|case
name|IPL_LOGSYNC
case|:
if|if
condition|(
operator|(
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
operator|)
operator|&&
name|ipf_sync_canread
argument_list|(
operator|&
name|ipfmain
argument_list|)
condition|)
name|revents
operator||=
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
expr_stmt|;
if|if
condition|(
operator|(
name|events
operator|&
operator|(
name|POLLOUT
operator||
name|POLLWRNORM
operator|)
operator|)
operator|&&
name|ipf_sync_canwrite
argument_list|(
operator|&
name|ipfmain
argument_list|)
condition|)
name|revents
operator||=
name|events
operator|&
operator|(
name|POLLOUT
operator||
name|POLLWRNORM
operator|)
expr_stmt|;
break|break;
case|case
name|IPL_LOGSCAN
case|:
case|case
name|IPL_LOGLOOKUP
case|:
default|default :
break|break;
block|}
if|if
condition|(
operator|(
name|revents
operator|==
literal|0
operator|)
operator|&&
operator|(
operator|(
name|events
operator|&
operator|(
name|POLLIN
operator||
name|POLLRDNORM
operator|)
operator|)
operator|!=
literal|0
operator|)
condition|)
name|selrecord
argument_list|(
name|td
argument_list|,
operator|&
name|ipfmain
operator|.
name|ipf_selwait
index|[
name|unit
index|]
argument_list|)
expr_stmt|;
return|return
name|revents
return|;
block|}
end_function

begin_comment
comment|/*  * routines below for saving IP headers to buffer  */
end_comment

begin_function
specifier|static
name|int
name|ipfopen
parameter_list|(
name|dev
parameter_list|,
name|flags
if|#
directive|if
operator|(
operator|(
name|BSD
operator|>=
literal|199506
operator|)
operator|||
operator|(
name|__FreeBSD_version
operator|>=
literal|220000
operator|)
operator|)
parameter_list|,
name|devtype
parameter_list|,
name|p
parameter_list|)
name|int
name|devtype
decl_stmt|;
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|500024
operator|)
name|struct
name|thread
modifier|*
name|p
decl_stmt|;
else|#
directive|else
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __FreeBSD_version>= 500024 */
end_comment

begin_else
else|#
directive|else
end_else

begin_endif
unit|)
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
end_if

begin_decl_stmt
name|struct
name|cdev
modifier|*
name|dev
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|int
name|flags
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|u_int
name|unit
init|=
name|GET_MINOR
argument_list|(
name|dev
argument_list|)
decl_stmt|;
name|int
name|error
decl_stmt|;
if|if
condition|(
name|IPL_LOGMAX
operator|<
name|unit
condition|)
name|error
operator|=
name|ENXIO
expr_stmt|;
else|else
block|{
switch|switch
condition|(
name|unit
condition|)
block|{
case|case
name|IPL_LOGIPF
case|:
case|case
name|IPL_LOGNAT
case|:
case|case
name|IPL_LOGSTATE
case|:
case|case
name|IPL_LOGAUTH
case|:
case|case
name|IPL_LOGLOOKUP
case|:
case|case
name|IPL_LOGSYNC
case|:
ifdef|#
directive|ifdef
name|IPFILTER_SCAN
case|case
name|IPL_LOGSCAN
case|:
endif|#
directive|endif
name|error
operator|=
literal|0
expr_stmt|;
break|break;
default|default :
name|error
operator|=
name|ENXIO
expr_stmt|;
break|break;
block|}
block|}
return|return
name|error
return|;
block|}
end_block

begin_function
specifier|static
name|int
name|ipfclose
parameter_list|(
name|dev
parameter_list|,
name|flags
if|#
directive|if
operator|(
operator|(
name|BSD
operator|>=
literal|199506
operator|)
operator|||
operator|(
name|__FreeBSD_version
operator|>=
literal|220000
operator|)
operator|)
parameter_list|,
name|devtype
parameter_list|,
name|p
parameter_list|)
name|int
name|devtype
decl_stmt|;
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|500024
operator|)
name|struct
name|thread
modifier|*
name|p
decl_stmt|;
else|#
directive|else
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* __FreeBSD_version>= 500024 */
end_comment

begin_else
else|#
directive|else
end_else

begin_endif
unit|)
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
end_if

begin_decl_stmt
name|struct
name|cdev
modifier|*
name|dev
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_decl_stmt
name|dev_t
name|dev
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|int
name|flags
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|u_int
name|unit
init|=
name|GET_MINOR
argument_list|(
name|dev
argument_list|)
decl_stmt|;
if|if
condition|(
name|IPL_LOGMAX
operator|<
name|unit
condition|)
name|unit
operator|=
name|ENXIO
expr_stmt|;
else|else
name|unit
operator|=
literal|0
expr_stmt|;
return|return
name|unit
return|;
block|}
end_block

begin_comment
comment|/*  * ipfread/ipflog  * both of these must operate with at least splnet() lest they be  * called during packet processing and cause an inconsistancy to appear in  * the filter lists.  */
end_comment

begin_if
if|#
directive|if
operator|(
name|BSD
operator|>=
literal|199306
operator|)
end_if

begin_decl_stmt
specifier|static
name|int
name|ipfread
argument_list|(
name|dev
argument_list|,
name|uio
argument_list|,
name|ioflag
argument_list|)
name|int
name|ioflag
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_function
specifier|static
name|int
name|ipfread
parameter_list|(
name|dev
parameter_list|,
name|uio
parameter_list|)
endif|#
directive|endif
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
name|struct
name|cdev
modifier|*
name|dev
decl_stmt|;
else|#
directive|else
name|dev_t
name|dev
decl_stmt|;
endif|#
directive|endif
name|struct
name|uio
modifier|*
name|uio
decl_stmt|;
block|{
name|u_int
name|unit
init|=
name|GET_MINOR
argument_list|(
name|dev
argument_list|)
decl_stmt|;
if|if
condition|(
name|unit
operator|<
literal|0
condition|)
return|return
name|ENXIO
return|;
if|if
condition|(
name|ipfmain
operator|.
name|ipf_running
operator|<
literal|1
condition|)
return|return
name|EIO
return|;
if|if
condition|(
name|unit
operator|==
name|IPL_LOGSYNC
condition|)
return|return
name|ipf_sync_read
argument_list|(
operator|&
name|ipfmain
argument_list|,
name|uio
argument_list|)
return|;
ifdef|#
directive|ifdef
name|IPFILTER_LOG
return|return
name|ipf_log_read
argument_list|(
operator|&
name|ipfmain
argument_list|,
name|unit
argument_list|,
name|uio
argument_list|)
return|;
else|#
directive|else
return|return
name|ENXIO
return|;
endif|#
directive|endif
block|}
end_function

begin_comment
comment|/*  * ipfwrite  * both of these must operate with at least splnet() lest they be  * called during packet processing and cause an inconsistancy to appear in  * the filter lists.  */
end_comment

begin_if
if|#
directive|if
operator|(
name|BSD
operator|>=
literal|199306
operator|)
end_if

begin_decl_stmt
specifier|static
name|int
name|ipfwrite
argument_list|(
name|dev
argument_list|,
name|uio
argument_list|,
name|ioflag
argument_list|)
name|int
name|ioflag
decl_stmt|;
end_decl_stmt

begin_else
else|#
directive|else
end_else

begin_function
specifier|static
name|int
name|ipfwrite
parameter_list|(
name|dev
parameter_list|,
name|uio
parameter_list|)
endif|#
directive|endif
if|#
directive|if
operator|(
name|__FreeBSD_version
operator|>=
literal|502116
operator|)
name|struct
name|cdev
modifier|*
name|dev
decl_stmt|;
else|#
directive|else
name|dev_t
name|dev
decl_stmt|;
endif|#
directive|endif
name|struct
name|uio
modifier|*
name|uio
decl_stmt|;
block|{
if|if
condition|(
name|ipfmain
operator|.
name|ipf_running
operator|<
literal|1
condition|)
return|return
name|EIO
return|;
if|if
condition|(
name|GET_MINOR
argument_list|(
name|dev
argument_list|)
operator|==
name|IPL_LOGSYNC
condition|)
return|return
name|ipf_sync_write
argument_list|(
operator|&
name|ipfmain
argument_list|,
name|uio
argument_list|)
return|;
return|return
name|ENXIO
return|;
block|}
end_function

end_unit

