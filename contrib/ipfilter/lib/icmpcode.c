begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	$FreeBSD$	*/
end_comment

begin_comment
comment|/*  * Copyright (C) 1993-2001 by Darren Reed.  *  * See the IPFILTER.LICENCE file for details on licencing.  *  * Id: icmpcode.c,v 1.7.2.1 2004/12/09 19:41:20 darrenr Exp  */
end_comment

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"ipf.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|MIN
end_ifndef

begin_define
define|#
directive|define
name|MIN
parameter_list|(
name|a
parameter_list|,
name|b
parameter_list|)
value|((a)> (b) ? (b) : (a))
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
name|char
modifier|*
name|icmpcodes
index|[
name|MAX_ICMPCODE
operator|+
literal|1
index|]
init|=
block|{
literal|"net-unr"
block|,
literal|"host-unr"
block|,
literal|"proto-unr"
block|,
literal|"port-unr"
block|,
literal|"needfrag"
block|,
literal|"srcfail"
block|,
literal|"net-unk"
block|,
literal|"host-unk"
block|,
literal|"isolate"
block|,
literal|"net-prohib"
block|,
literal|"host-prohib"
block|,
literal|"net-tos"
block|,
literal|"host-tos"
block|,
literal|"filter-prohib"
block|,
literal|"host-preced"
block|,
literal|"preced-cutoff"
block|,
name|NULL
block|}
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Return the number for the associated ICMP unreachable code.  */
end_comment

begin_function
name|int
name|icmpcode
parameter_list|(
name|str
parameter_list|)
name|char
modifier|*
name|str
decl_stmt|;
block|{
name|char
modifier|*
name|s
decl_stmt|;
name|int
name|i
decl_stmt|,
name|len
decl_stmt|;
if|if
condition|(
operator|(
name|s
operator|=
name|strrchr
argument_list|(
name|str
argument_list|,
literal|')'
argument_list|)
operator|)
condition|)
operator|*
name|s
operator|=
literal|'\0'
expr_stmt|;
if|if
condition|(
name|ISDIGIT
argument_list|(
operator|*
name|str
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|ratoi
argument_list|(
name|str
argument_list|,
operator|&
name|i
argument_list|,
literal|0
argument_list|,
literal|255
argument_list|)
condition|)
return|return
operator|-
literal|1
return|;
else|else
return|return
name|i
return|;
block|}
name|len
operator|=
name|strlen
argument_list|(
name|str
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|icmpcodes
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
if|if
condition|(
operator|!
name|strncasecmp
argument_list|(
name|str
argument_list|,
name|icmpcodes
index|[
name|i
index|]
argument_list|,
name|MIN
argument_list|(
name|len
argument_list|,
name|strlen
argument_list|(
name|icmpcodes
index|[
name|i
index|]
argument_list|)
argument_list|)
argument_list|)
condition|)
return|return
name|i
return|;
return|return
operator|-
literal|1
return|;
block|}
end_function

end_unit

