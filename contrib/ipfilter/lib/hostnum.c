begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*	$FreeBSD$	*/
end_comment

begin_comment
comment|/*  * Copyright (C) 1993-2001 by Darren Reed.  *  * See the IPFILTER.LICENCE file for details on licencing.  *  * Id: hostnum.c,v 1.10.2.1 2004/12/09 19:41:20 darrenr Exp  */
end_comment

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|"ipf.h"
end_include

begin_comment
comment|/*  * returns an ip address as a long var as a result of either a DNS lookup or  * straight inet_addr() call  */
end_comment

begin_function
name|int
name|hostnum
parameter_list|(
name|ipa
parameter_list|,
name|host
parameter_list|,
name|linenum
parameter_list|,
name|ifname
parameter_list|)
name|u_32_t
modifier|*
name|ipa
decl_stmt|;
name|char
modifier|*
name|host
decl_stmt|;
name|int
name|linenum
decl_stmt|;
name|char
modifier|*
name|ifname
decl_stmt|;
block|{
name|struct
name|in_addr
name|ip
decl_stmt|;
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
literal|"any"
argument_list|,
name|host
argument_list|)
operator|||
operator|(
name|ifname
operator|&&
operator|*
name|ifname
operator|&&
operator|!
name|strcasecmp
argument_list|(
name|ifname
argument_list|,
name|host
argument_list|)
operator|)
condition|)
return|return
literal|0
return|;
ifdef|#
directive|ifdef
name|USE_INET6
if|if
condition|(
name|use_inet6
condition|)
block|{
if|if
condition|(
name|inet_pton
argument_list|(
name|AF_INET6
argument_list|,
name|host
argument_list|,
name|ipa
argument_list|)
operator|==
literal|1
condition|)
return|return
literal|0
return|;
else|else
return|return
operator|-
literal|1
return|;
block|}
endif|#
directive|endif
if|if
condition|(
name|ISDIGIT
argument_list|(
operator|*
name|host
argument_list|)
operator|&&
name|inet_aton
argument_list|(
name|host
argument_list|,
operator|&
name|ip
argument_list|)
condition|)
block|{
operator|*
name|ipa
operator|=
name|ip
operator|.
name|s_addr
expr_stmt|;
return|return
literal|0
return|;
block|}
if|if
condition|(
operator|!
name|strcasecmp
argument_list|(
literal|"<thishost>"
argument_list|,
name|host
argument_list|)
condition|)
name|host
operator|=
name|thishost
expr_stmt|;
return|return
name|gethost
argument_list|(
name|host
argument_list|,
name|ipa
argument_list|)
return|;
block|}
end_function

end_unit

