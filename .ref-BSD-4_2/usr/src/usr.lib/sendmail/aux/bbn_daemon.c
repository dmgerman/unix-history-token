begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|"sendmail.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|DAEMON
end_ifndef

begin_expr_stmt
name|SCCSID
argument_list|(
argument|@
operator|(
operator|#
operator|)
name|bbn_daemon
operator|.
name|c
literal|3.36
operator|+
literal|12
operator|/
literal|13
operator|/
literal|82
operator|(
name|w
operator|/
name|o
name|daemon
name|mode
operator|)
argument_list|)
expr_stmt|;
end_expr_stmt

begin_else
else|#
directive|else
end_else

begin_expr_stmt
name|SCCSID
argument_list|(
argument|@
operator|(
operator|#
operator|)
name|bbn_daemon
operator|.
name|c
literal|3.36
operator|+
literal|12
operator|/
literal|13
operator|/
literal|82
operator|(
name|w
operator|/
name|daemon
name|mode
operator|)
argument_list|)
expr_stmt|;
end_expr_stmt

begin_comment
comment|/* **  DAEMON.C -- routines to use when running as a daemon. ** **	BB&N Version.  Hack attack!!  This version is not supported, **	and probably has some bugs...... ** **	Basic semantics are: ** **	getrequests() **		Opens a port and initiates a connection. **		Returns in a child.  Must set InChannel and **		OutChannel appropriately. **	makeconnection(host, port, outfile, infile) **		Make a connection to the named host on the given **		port.  Set *outfile and *infile to the files **		appropriate for communication.  Returns zero on **		success, else an exit status describing the **		error. ** **	The semantics of both of these should be clean. */
end_comment

begin_include
include|#
directive|include
file|"netlib.h"
end_include

begin_include
include|#
directive|include
file|"con.h"
end_include

begin_include
include|#
directive|include
file|<wait.h>
end_include

begin_define
define|#
directive|define
name|SMTPSOCK
value|25
end_define

begin_escape
end_escape

begin_comment
comment|/* **  GETREQUESTS -- open mail IPC port and get requests. ** **	Parameters: **		none. ** **	Returns: **		none. ** **	Side Effects: **		Waits until some interesting activity occurs.  When **		it does, a child is created to process it, and the **		parent waits for completion.  Return from this **		routine is always in the child. */
end_comment

begin_macro
name|getrequests
argument_list|()
end_macro

begin_block
block|{
name|int
name|t
decl_stmt|;
name|int
name|iretval
decl_stmt|;
name|union
name|wait
name|status
decl_stmt|;
name|struct
name|con
name|openparams
decl_stmt|;
comment|/* 	**  Set up the address for the mailer. 	*/
name|bzero
argument_list|(
operator|&
name|openparams
argument_list|,
sizeof|sizeof
name|openparams
argument_list|)
expr_stmt|;
comment|/* clear out the structure */
name|openparams
operator|.
name|c_mode
operator|=
name|CONTCP
expr_stmt|;
name|openparams
operator|.
name|c_lport
operator|=
name|SMTPSOCK
expr_stmt|;
name|openparams
operator|.
name|c_rbufs
operator|=
literal|2
expr_stmt|;
comment|/* 2k receive buffer */
comment|/* 	**  Try to actually open the connection. 	*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|1
argument_list|)
condition|)
name|printf
argument_list|(
literal|"getrequests\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DEBUG
for|for
control|(
init|;
condition|;
control|)
block|{
comment|/* wait for a connection */
specifier|register
name|int
name|pid
decl_stmt|;
do|do
block|{
name|errno
operator|=
literal|0
expr_stmt|;
name|t
operator|=
name|open
argument_list|(
literal|"/dev/net/net"
argument_list|,
operator|&
name|openparams
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|t
operator|<
literal|0
operator|&&
name|errno
operator|==
name|EINTR
condition|)
do|;
if|if
condition|(
name|t
operator|<
literal|0
condition|)
block|{
name|syserr
argument_list|(
literal|"getrequests: open"
argument_list|)
expr_stmt|;
name|sleep
argument_list|(
literal|500
argument_list|)
expr_stmt|;
continue|continue;
block|}
comment|/* set 'eol' mode in connection */
name|ioctl
argument_list|(
name|t
argument_list|,
name|NETSETE
argument_list|,
literal|0
argument_list|)
expr_stmt|;
comment|/* 		**  Create a subprocess to process the mail. 		*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
name|printf
argument_list|(
literal|"getrequests: forking (fd = %d)\n"
argument_list|,
name|t
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DEBUG
name|pid
operator|=
name|fork
argument_list|()
expr_stmt|;
if|if
condition|(
name|pid
operator|<
literal|0
condition|)
block|{
name|syserr
argument_list|(
literal|"daemon: cannot fork"
argument_list|)
expr_stmt|;
name|sleep
argument_list|(
literal|10
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|close
argument_list|(
name|t
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|pid
operator|==
literal|0
condition|)
block|{
comment|/* 			**  CHILD -- return to caller. 			**	Verify calling user id if possible here. 			*/
name|InChannel
operator|=
name|fdopen
argument_list|(
name|t
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
name|OutChannel
operator|=
name|fdopen
argument_list|(
name|t
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
name|printf
argument_list|(
literal|"getreq: returning\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DEBUG
ifdef|#
directive|ifdef
name|LOG
if|if
condition|(
name|LogLevel
operator|>
literal|11
condition|)
name|syslog
argument_list|(
name|LOG_DEBUG
argument_list|,
literal|"connected, pid=%d"
argument_list|,
name|getpid
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|LOG
return|return;
block|}
comment|/* 		**  PARENT -- wait for child to terminate. 		**	Perhaps we should allow concurrent processing? 		*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|15
argument_list|,
literal|2
argument_list|)
condition|)
block|{
name|sleep
argument_list|(
literal|2
argument_list|)
expr_stmt|;
name|printf
argument_list|(
literal|"getreq: parent waiting\n"
argument_list|)
expr_stmt|;
block|}
endif|#
directive|endif
endif|DEBUG
comment|/* close the port so that others will hang (for a while) */
if|if
condition|(
operator|(
name|iretval
operator|=
name|close
argument_list|(
name|t
argument_list|)
operator|)
operator|<
literal|0
condition|)
name|syserr
argument_list|(
literal|"getreq:  cannot close port 25:  %d"
argument_list|,
name|iretval
argument_list|)
expr_stmt|;
comment|/* pick up old zombies; implement load limiting */
while|while
condition|(
name|wait3
argument_list|(
operator|&
name|status
argument_list|,
name|WNOHANG
argument_list|,
literal|0
argument_list|)
operator|>
literal|0
condition|)
continue|continue;
block|}
block|}
end_block

begin_escape
end_escape

begin_comment
comment|/* **  MAKECONNECTION -- make a connection to an SMTP socket on another machine. ** **	Parameters: **		host -- the name of the host. **		port -- the port number to connect to. **		outfile -- a pointer to a place to put the outfile **			descriptor. **		infile -- ditto for infile. ** **	Returns: **		An exit code telling whether the connection could be **			made and if not why not. ** **	Side Effects: **		none. */
end_comment

begin_macro
name|makeconnection
argument_list|(
argument|host
argument_list|,
argument|port
argument_list|,
argument|outfile
argument_list|,
argument|infile
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|host
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|u_short
name|port
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|FILE
modifier|*
modifier|*
name|outfile
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|FILE
modifier|*
modifier|*
name|infile
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|s
decl_stmt|;
name|struct
name|con
name|openparams
decl_stmt|;
name|bzero
argument_list|(
operator|&
name|openparams
argument_list|,
sizeof|sizeof
name|openparams
argument_list|)
expr_stmt|;
comment|/* clear structure's memory */
name|openparams
operator|.
name|c_mode
operator|=
name|CONACT
operator||
name|CONTCP
expr_stmt|;
name|openparams
operator|.
name|c_timeo
operator|=
literal|60
expr_stmt|;
comment|/* 	**  Set up the address for the mailer. 	**	Accept "[a.b.c.d]" syntax for host name. 	*/
if|if
condition|(
name|host
index|[
literal|0
index|]
operator|==
literal|'['
condition|)
block|{
name|long
name|hid
init|=
literal|0
decl_stmt|;
name|int
name|i
decl_stmt|,
name|j
decl_stmt|;
specifier|register
name|char
modifier|*
name|p
init|=
name|host
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|3
init|;
name|i
operator|>=
literal|0
operator|&&
operator|*
name|p
operator|!=
literal|']'
operator|&&
operator|*
name|p
operator|!=
literal|'\0'
condition|;
name|i
operator|--
control|)
block|{
name|j
operator|=
literal|0
expr_stmt|;
while|while
condition|(
name|isdigit
argument_list|(
operator|*
operator|++
name|p
argument_list|)
condition|)
name|j
operator|=
name|j
operator|*
literal|10
operator|+
operator|(
operator|*
name|p
operator|-
literal|'0'
operator|)
expr_stmt|;
if|if
condition|(
operator|*
name|p
operator|!=
operator|(
name|i
operator|==
literal|0
condition|?
literal|']'
else|:
literal|'.'
operator|)
operator|||
name|j
operator|>
literal|255
operator|||
name|j
operator|<
literal|0
condition|)
break|break;
name|hid
operator||=
name|j
operator|<<
operator|(
operator|(
literal|3
operator|-
name|i
operator|)
operator|*
literal|8
operator|)
expr_stmt|;
block|}
if|if
condition|(
name|i
operator|>=
literal|0
operator|||
operator|*
name|p
operator|!=
literal|']'
operator|||
operator|*
operator|++
name|p
operator|!=
literal|'\0'
condition|)
block|{
name|usrerr
argument_list|(
literal|"Invalid numeric domain spec \"%s\""
argument_list|,
name|host
argument_list|)
expr_stmt|;
return|return
operator|(
name|EX_NOHOST
operator|)
return|;
block|}
name|openparams
operator|.
name|c_fcon
operator|.
name|_na_l
operator|=
name|hid
expr_stmt|;
block|}
else|else
block|{
name|openparams
operator|.
name|c_fcon
operator|=
name|gethost
argument_list|(
name|host
argument_list|)
expr_stmt|;
if|if
condition|(
name|isbadhost
argument_list|(
name|openparams
operator|.
name|c_fcon
argument_list|)
condition|)
return|return
operator|(
name|EX_NOHOST
operator|)
return|;
block|}
comment|/* 	**  Determine the port number. 	*/
if|if
condition|(
name|port
operator|==
literal|0
condition|)
name|openparams
operator|.
name|c_fport
operator|=
name|SMTPSOCK
expr_stmt|;
else|else
name|openparams
operator|.
name|c_fport
operator|=
name|port
expr_stmt|;
comment|/* 	**  Try to actually open the connection. 	*/
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|printf
argument_list|(
literal|"makeconnection(%s=%X)\n"
argument_list|,
name|host
argument_list|,
name|openparams
operator|.
name|c_fcon
operator|.
name|_na_l
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DEBUG
name|s
operator|=
name|open
argument_list|(
literal|"/dev/net/net"
argument_list|,
operator|&
name|openparams
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|DEBUG
if|if
condition|(
name|tTd
argument_list|(
literal|16
argument_list|,
literal|1
argument_list|)
condition|)
name|printf
argument_list|(
literal|"makeconnection: %d\n"
argument_list|,
name|s
argument_list|)
expr_stmt|;
endif|#
directive|endif
endif|DEBUG
if|if
condition|(
name|s
operator|<
literal|0
condition|)
return|return
operator|(
name|EX_TEMPFAIL
operator|)
return|;
comment|/* connection ok, put it into canonical form */
operator|*
name|outfile
operator|=
name|fdopen
argument_list|(
name|s
argument_list|,
literal|"w"
argument_list|)
expr_stmt|;
operator|*
name|infile
operator|=
name|fdopen
argument_list|(
name|s
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
endif|DAEMON
end_endif

begin_escape
end_escape

begin_comment
comment|/* **  MYHOSTNAME -- return the name of this host. ** **	This is a hideous hack....  It should be fixed to really work **	under the BB&N code.  However, since Brendan's configuration **	tables don't use this feature it won't matter. ** **	Parameters: **		hostbuf -- a place to return the name of this host. **		size -- the size of hostbuf. ** **	Returns: **		A list of aliases for this host. **		NULL if it cannot be found. ** **	Side Effects: **		none. */
end_comment

begin_function
name|char
modifier|*
modifier|*
name|myhostname
parameter_list|(
name|hostbuf
parameter_list|,
name|size
parameter_list|)
name|char
name|hostbuf
index|[]
decl_stmt|;
name|int
name|size
decl_stmt|;
block|{
specifier|register
name|FILE
modifier|*
name|f
decl_stmt|;
name|hostbuf
index|[
literal|0
index|]
operator|=
literal|'\0'
expr_stmt|;
name|f
operator|=
name|fopen
argument_list|(
literal|"/usr/include/whoami"
argument_list|,
literal|"r"
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
operator|!=
name|NULL
condition|)
block|{
operator|(
name|void
operator|)
name|fgets
argument_list|(
name|hostbuf
argument_list|,
name|size
argument_list|,
name|f
argument_list|)
expr_stmt|;
name|fixcrlf
argument_list|(
name|hostbuf
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|f
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|NULL
operator|)
return|;
block|}
end_function

end_unit

