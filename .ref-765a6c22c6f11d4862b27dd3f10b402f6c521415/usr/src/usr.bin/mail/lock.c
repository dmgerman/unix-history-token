begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)lock.c	2.4 (Berkeley) %G%"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * A mailing program.  *  * Stuff to do version 7 style locking.  */
end_comment

begin_include
include|#
directive|include
file|"rcv.h"
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_decl_stmt
name|char
modifier|*
name|maillock
init|=
literal|".mail"
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Lock suffix for mailname */
end_comment

begin_decl_stmt
name|char
modifier|*
name|lockname
init|=
literal|"/usr/spool/mail/tmXXXXXX"
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
name|locktmp
index|[
literal|30
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Usable lock temporary */
end_comment

begin_decl_stmt
specifier|static
name|char
name|curlock
index|[
literal|50
index|]
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* Last used name of lock */
end_comment

begin_decl_stmt
specifier|static
name|int
name|locked
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* To note that we locked it */
end_comment

begin_comment
comment|/*  * Lock the specified mail file by setting the file mailfile.lock.  * We must, of course, be careful to remove the lock file by a call  * to unlock before we stop.  The algorithm used here is to see if  * the lock exists, and if it does, to check its modify time.  If it  * is older than 5 minutes, we assume error and set our own file.  * Otherwise, we wait for 5 seconds and try again.  */
end_comment

begin_macro
name|lock
argument_list|(
argument|file
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|file
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|f
decl_stmt|;
name|struct
name|stat
name|sbuf
decl_stmt|;
name|long
name|curtime
decl_stmt|;
if|if
condition|(
name|file
operator|==
name|NOSTR
condition|)
block|{
name|printf
argument_list|(
literal|"Locked = %d\n"
argument_list|,
name|locked
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|locked
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|strcpy
argument_list|(
name|curlock
argument_list|,
name|file
argument_list|)
expr_stmt|;
name|strcat
argument_list|(
name|curlock
argument_list|,
name|maillock
argument_list|)
expr_stmt|;
name|strcpy
argument_list|(
name|locktmp
argument_list|,
name|lockname
argument_list|)
expr_stmt|;
name|mktemp
argument_list|(
name|locktmp
argument_list|)
expr_stmt|;
name|remove
argument_list|(
name|locktmp
argument_list|)
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
name|f
operator|=
name|lock1
argument_list|(
name|locktmp
argument_list|,
name|curlock
argument_list|)
expr_stmt|;
if|if
condition|(
name|f
operator|==
literal|0
condition|)
block|{
name|locked
operator|=
literal|1
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|stat
argument_list|(
name|curlock
argument_list|,
operator|&
name|sbuf
argument_list|)
operator|<
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|time
argument_list|(
operator|&
name|curtime
argument_list|)
expr_stmt|;
if|if
condition|(
name|curtime
operator|<
name|sbuf
operator|.
name|st_ctime
operator|+
literal|300
condition|)
block|{
name|sleep
argument_list|(
literal|5
argument_list|)
expr_stmt|;
continue|continue;
block|}
name|remove
argument_list|(
name|curlock
argument_list|)
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Remove the mail lock, and note that we no longer  * have it locked.  */
end_comment

begin_macro
name|unlock
argument_list|()
end_macro

begin_block
block|{
name|remove
argument_list|(
name|curlock
argument_list|)
expr_stmt|;
name|locked
operator|=
literal|0
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Attempt to set the lock by creating the temporary file,  * then doing a link/unlink.  If it fails, return -1 else 0  */
end_comment

begin_macro
name|lock1
argument_list|(
argument|tempfile
argument_list|,
argument|name
argument_list|)
end_macro

begin_decl_stmt
name|char
name|tempfile
index|[]
decl_stmt|,
name|name
index|[]
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
name|fd
decl_stmt|;
name|fd
operator|=
name|creat
argument_list|(
name|tempfile
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|<
literal|0
condition|)
return|return
operator|(
operator|-
literal|1
operator|)
return|;
name|close
argument_list|(
name|fd
argument_list|)
expr_stmt|;
if|if
condition|(
name|link
argument_list|(
name|tempfile
argument_list|,
name|name
argument_list|)
operator|<
literal|0
condition|)
block|{
name|remove
argument_list|(
name|tempfile
argument_list|)
expr_stmt|;
return|return
operator|(
operator|-
literal|1
operator|)
return|;
block|}
name|remove
argument_list|(
name|tempfile
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

