begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright 1986, 1987, 1988 by the Massachusetts Institute  * of Technology.  * For copying and distribution information, please see the file  *<Copyright.MIT>.  *  * This routine constructs a Kerberos 'safe msg', i.e. authenticated  * using a private session key to seed a checksum. Msg is NOT  * encrypted.  *  *      Note-- bcopy is used to avoid alignment problems on IBM RT  *  *      Returns either<0 ===> error, or resulting size of message  *  * Steve Miller    Project Athena  MIT/DEC  *  *	from: mk_safe.c,v 4.12 89/03/22 14:50:49 jtkohl Exp $  * $FreeBSD$  */
end_comment

begin_if
if|#
directive|if
literal|0
end_if

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_endif
unit|static char rcsid[] = "$FreeBSD$";
endif|#
directive|endif
end_endif

begin_comment
comment|/* lint */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* system include files */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<netinet/in.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_comment
comment|/* application include files */
end_comment

begin_include
include|#
directive|include
file|<des.h>
end_include

begin_include
include|#
directive|include
file|<krb.h>
end_include

begin_include
include|#
directive|include
file|<prot.h>
end_include

begin_include
include|#
directive|include
file|"lsb_addr_comp.h"
end_include

begin_function_decl
specifier|extern
name|char
modifier|*
name|errmsg
parameter_list|()
function_decl|;
end_function_decl

begin_decl_stmt
specifier|extern
name|int
name|errno
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|extern
name|int
name|krb_debug
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* static storage */
end_comment

begin_decl_stmt
specifier|static
name|u_long
name|cksum
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|C_Block
name|big_cksum
index|[
literal|2
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|struct
name|timeval
name|msg_time
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|u_char
name|msg_time_5ms
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|long
name|msg_time_sec
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * krb_mk_safe() constructs an AUTH_MSG_SAFE message.  It takes some  * user data "in" of "length" bytes and creates a packet in "out"  * consisting of the user data, a timestamp, and the sender's network  * address, followed by a checksum computed on the above, using the  * given "key".  The length of the resulting packet is returned.  *  * The "out" packet consists of:  *  * Size			Variable		Field  * ----			--------		-----  *  * 1 byte		KRB_PROT_VERSION	protocol version number  * 1 byte		AUTH_MSG_SAFE |		message type plus local  *			HOST_BYTE_ORDER		byte order in low bit  *  * ===================== begin checksum ================================  *  * 4 bytes		length			length of user data  * length		in			user data  * 1 byte		msg_time_5ms		timestamp milliseconds  * 4 bytes		sender->sin.addr.s_addr	sender's IP address  *  * 4 bytes		msg_time_sec or		timestamp seconds with  *			-msg_time_sec		direction in sign bit  *  * ======================= end checksum ================================  *  * 16 bytes		big_cksum		quadratic checksum of  *						above using "key"  */
end_comment

begin_function
name|long
name|krb_mk_safe
parameter_list|(
name|in
parameter_list|,
name|out
parameter_list|,
name|length
parameter_list|,
name|key
parameter_list|,
name|sender
parameter_list|,
name|receiver
parameter_list|)
name|u_char
modifier|*
name|in
decl_stmt|;
comment|/* application data */
name|u_char
modifier|*
name|out
decl_stmt|;
comment|/* 				 * put msg here, leave room for header! 				 * breaks if in and out (header stuff) 				 * overlap 				 */
name|u_long
name|length
decl_stmt|;
comment|/* of in data */
name|C_Block
modifier|*
name|key
decl_stmt|;
comment|/* encryption key for seed and ivec */
name|struct
name|sockaddr_in
modifier|*
name|sender
decl_stmt|;
comment|/* sender address */
name|struct
name|sockaddr_in
modifier|*
name|receiver
decl_stmt|;
comment|/* receiver address */
block|{
specifier|register
name|u_char
modifier|*
name|p
decl_stmt|,
modifier|*
name|q
decl_stmt|;
comment|/*      * get the current time to use instead of a sequence #, since      * process lifetime may be shorter than the lifetime of a session      * key.      */
if|if
condition|(
name|gettimeofday
argument_list|(
operator|&
name|msg_time
argument_list|,
operator|(
expr|struct
name|timezone
operator|*
operator|)
literal|0
argument_list|)
condition|)
block|{
return|return
operator|-
literal|1
return|;
block|}
name|msg_time_sec
operator|=
operator|(
name|long
operator|)
name|msg_time
operator|.
name|tv_sec
expr_stmt|;
name|msg_time_5ms
operator|=
name|msg_time
operator|.
name|tv_usec
operator|/
literal|5000
expr_stmt|;
comment|/* 5ms quanta */
name|p
operator|=
name|out
expr_stmt|;
operator|*
name|p
operator|++
operator|=
name|KRB_PROT_VERSION
expr_stmt|;
operator|*
name|p
operator|++
operator|=
name|AUTH_MSG_SAFE
operator||
name|HOST_BYTE_ORDER
expr_stmt|;
name|q
operator|=
name|p
expr_stmt|;
comment|/* start for checksum stuff */
comment|/* stuff input length */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|length
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|length
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|length
argument_list|)
expr_stmt|;
comment|/* make all the stuff contiguous for checksum */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
name|in
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
operator|(
name|int
operator|)
name|length
argument_list|)
expr_stmt|;
name|p
operator|+=
name|length
expr_stmt|;
comment|/* stuff time 5ms */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|msg_time_5ms
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|msg_time_5ms
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|msg_time_5ms
argument_list|)
expr_stmt|;
comment|/* stuff source address */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|sender
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|sender
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|sender
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
expr_stmt|;
comment|/*      * direction bit is the sign bit of the timestamp.  Ok until      * 2038??      */
comment|/* For compatibility with broken old code, compares are done in VAX        byte order (LSBFIRST) */
if|if
condition|(
name|lsb_net_ulong_less
argument_list|(
name|sender
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|,
comment|/* src< recv */
name|receiver
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msg_time_sec
operator|=
operator|-
name|msg_time_sec
expr_stmt|;
elseif|else
if|if
condition|(
name|lsb_net_ulong_less
argument_list|(
name|sender
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|,
name|receiver
operator|->
name|sin_addr
operator|.
name|s_addr
argument_list|)
operator|==
literal|0
condition|)
if|if
condition|(
name|lsb_net_ushort_less
argument_list|(
name|sender
operator|->
name|sin_port
argument_list|,
name|receiver
operator|->
name|sin_port
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|msg_time_sec
operator|=
operator|-
name|msg_time_sec
expr_stmt|;
comment|/*      * all that for one tiny bit!  Heaven help those that talk to      * themselves.      */
comment|/* stuff time sec */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
operator|&
name|msg_time_sec
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|msg_time_sec
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|msg_time_sec
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|NOENCRYPTION
name|cksum
operator|=
literal|0
expr_stmt|;
name|bzero
argument_list|(
name|big_cksum
argument_list|,
sizeof|sizeof
argument_list|(
name|big_cksum
argument_list|)
argument_list|)
expr_stmt|;
else|#
directive|else
name|cksum
operator|=
name|quad_cksum
argument_list|(
operator|(
name|C_Block
operator|*
operator|)
name|q
argument_list|,
name|big_cksum
argument_list|,
name|p
operator|-
name|q
argument_list|,
literal|2
argument_list|,
name|key
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|krb_debug
condition|)
name|printf
argument_list|(
literal|"\ncksum = %lu"
argument_list|,
name|cksum
argument_list|)
expr_stmt|;
comment|/* stuff checksum */
name|bcopy
argument_list|(
operator|(
name|char
operator|*
operator|)
name|big_cksum
argument_list|,
operator|(
name|char
operator|*
operator|)
name|p
argument_list|,
sizeof|sizeof
argument_list|(
name|big_cksum
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|+=
sizeof|sizeof
argument_list|(
name|big_cksum
argument_list|)
expr_stmt|;
return|return
operator|(
call|(
name|long
call|)
argument_list|(
name|p
operator|-
name|out
argument_list|)
operator|)
return|;
comment|/* resulting size */
block|}
end_function

end_unit

