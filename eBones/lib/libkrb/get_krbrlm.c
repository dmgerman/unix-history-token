begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright 1985, 1986, 1987, 1988 by the Massachusetts Institute  * of Technology.  * For copying and distribution information, please see the file  *<Copyright.MIT>.  *  *	from: get_krbrlm.c,v 4.8 89/01/22 20:02:54 rfrench Exp $  * $FreeBSD$  */
end_comment

begin_if
if|#
directive|if
literal|0
end_if

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_endif
unit|static char *rcsid = "$FreeBSD$";
endif|#
directive|endif
end_endif

begin_comment
comment|/* lint */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<krb.h>
end_include

begin_include
include|#
directive|include
file|<strings.h>
end_include

begin_comment
comment|/*  * krb_get_lrealm takes a pointer to a string, and a number, n.  It fills  * in the string, r, with the name of the nth realm specified on the  * first line of the kerberos config file (KRB_CONF, defined in "krb.h").  * It returns 0 (KSUCCESS) on success, and KFAILURE on failure.  If the  * config file does not exist, and if n=1, a successful return will occur  * with r = KRB_REALM (also defined in "krb.h").  *  * NOTE: for archaic& compatibility reasons, this routine will only return  * valid results when n = 1.  *  * For the format of the KRB_CONF file, see comments describing the routine  * krb_get_krbhst().  */
end_comment

begin_function
name|int
name|krb_get_lrealm
parameter_list|(
name|r
parameter_list|,
name|n
parameter_list|)
name|char
modifier|*
name|r
decl_stmt|;
name|int
name|n
decl_stmt|;
block|{
name|FILE
modifier|*
name|cnffile
decl_stmt|,
modifier|*
name|fopen
argument_list|()
decl_stmt|;
if|if
condition|(
name|n
operator|>
literal|1
condition|)
return|return
operator|(
name|KFAILURE
operator|)
return|;
comment|/* Temporary restriction */
if|if
condition|(
operator|(
name|cnffile
operator|=
name|fopen
argument_list|(
name|KRB_CONF
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|n
operator|==
literal|1
condition|)
block|{
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|r
argument_list|,
name|KRB_REALM
argument_list|)
expr_stmt|;
return|return
operator|(
name|KSUCCESS
operator|)
return|;
block|}
else|else
return|return
operator|(
name|KFAILURE
operator|)
return|;
block|}
if|if
condition|(
name|fscanf
argument_list|(
name|cnffile
argument_list|,
literal|"%s"
argument_list|,
name|r
argument_list|)
operator|!=
literal|1
condition|)
block|{
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|cnffile
argument_list|)
expr_stmt|;
return|return
operator|(
name|KFAILURE
operator|)
return|;
block|}
operator|(
name|void
operator|)
name|fclose
argument_list|(
name|cnffile
argument_list|)
expr_stmt|;
return|return
operator|(
name|KSUCCESS
operator|)
return|;
block|}
end_function

end_unit

