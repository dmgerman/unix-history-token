begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright 1987, 1988 by the Massachusetts Institute of Technology.  * For copying and distribution information, please see the file  *<Copyright.MIT>.  *  *	from: get_tf_fullname.c,v 4.3 90/03/10 22:40:20 jon Exp $  * $FreeBSD$  */
end_comment

begin_if
if|#
directive|if
literal|0
end_if

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_endif
unit|static char rcsid[] = "$FreeBSD$";
endif|#
directive|endif
end_endif

begin_comment
comment|/* lint */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|<krb.h>
end_include

begin_include
include|#
directive|include
file|<strings.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_comment
comment|/*  * This file contains a routine to extract the fullname of a user  * from the ticket file.  */
end_comment

begin_comment
comment|/*  * krb_get_tf_fullname() takes four arguments: the name of the  * ticket file, and variables for name, instance, and realm to be  * returned in.  Since the realm of a ticket file is not really fully  * supported, the realm used will be that of the the first ticket in  * the file as this is the one that was obtained with a password by  * krb_get_in_tkt().  */
end_comment

begin_function
name|int
name|krb_get_tf_fullname
parameter_list|(
name|ticket_file
parameter_list|,
name|name
parameter_list|,
name|instance
parameter_list|,
name|realm
parameter_list|)
name|char
modifier|*
name|ticket_file
decl_stmt|;
name|char
modifier|*
name|name
decl_stmt|;
name|char
modifier|*
name|instance
decl_stmt|;
name|char
modifier|*
name|realm
decl_stmt|;
block|{
name|int
name|tf_status
decl_stmt|;
name|CREDENTIALS
name|c
decl_stmt|;
if|if
condition|(
operator|(
name|tf_status
operator|=
name|tf_init
argument_list|(
name|ticket_file
argument_list|,
name|R_TKT_FIL
argument_list|)
operator|)
operator|!=
name|KSUCCESS
condition|)
return|return
operator|(
name|tf_status
operator|)
return|;
if|if
condition|(
operator|(
operator|(
name|tf_status
operator|=
name|tf_get_pname
argument_list|(
name|c
operator|.
name|pname
argument_list|)
operator|)
operator|!=
name|KSUCCESS
operator|)
operator|||
operator|(
operator|(
name|tf_status
operator|=
name|tf_get_pinst
argument_list|(
name|c
operator|.
name|pinst
argument_list|)
operator|)
operator|!=
name|KSUCCESS
operator|)
condition|)
return|return
operator|(
name|tf_status
operator|)
return|;
if|if
condition|(
name|name
condition|)
name|strcpy
argument_list|(
name|name
argument_list|,
name|c
operator|.
name|pname
argument_list|)
expr_stmt|;
if|if
condition|(
name|instance
condition|)
name|strcpy
argument_list|(
name|instance
argument_list|,
name|c
operator|.
name|pinst
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|tf_status
operator|=
name|tf_get_cred
argument_list|(
operator|&
name|c
argument_list|)
operator|)
operator|==
name|KSUCCESS
condition|)
block|{
if|if
condition|(
name|realm
condition|)
name|strcpy
argument_list|(
name|realm
argument_list|,
name|c
operator|.
name|realm
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|tf_status
operator|==
name|EOF
condition|)
return|return
operator|(
name|KFAILURE
operator|)
return|;
else|else
return|return
operator|(
name|tf_status
operator|)
return|;
block|}
operator|(
name|void
operator|)
name|tf_close
argument_list|()
expr_stmt|;
return|return
operator|(
name|tf_status
operator|)
return|;
block|}
end_function

end_unit

