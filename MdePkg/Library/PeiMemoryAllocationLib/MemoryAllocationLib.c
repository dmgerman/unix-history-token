begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/** @file   Support routines for memory allocation routines    based on PeiService for PEI phase drivers.    Copyright (c) 2006 - 2015, Intel Corporation. All rights reserved.<BR>   This program and the accompanying materials                             are licensed and made available under the terms and conditions of the BSD License            which accompanies this distribution.  The full text of the license may be found at           http://opensource.org/licenses/bsd-license.php.                                                THE PROGRAM IS DISTRIBUTED UNDER THE BSD LICENSE ON AN "AS IS" BASIS,                        WITHOUT WARRANTIES OR REPRESENTATIONS OF ANY KIND, EITHER EXPRESS OR IMPLIED.               **/
end_comment

begin_include
include|#
directive|include
file|<PiPei.h>
end_include

begin_include
include|#
directive|include
file|<Library/MemoryAllocationLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/PeiServicesLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/BaseMemoryLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/DebugLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/HobLib.h>
end_include

begin_comment
comment|/**   Allocates one or more 4KB pages of a certain memory type.    Allocates the number of 4KB pages of a certain memory type and returns a pointer to the allocated   buffer.  The buffer returned is aligned on a 4KB boundary.  If Pages is 0, then NULL is returned.   If there is not enough memory remaining to satisfy the request, then NULL is returned.    @param  MemoryType            The type of memory to allocate.   @param  Pages                 The number of 4 KB pages to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalAllocatePages
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|MemoryType
parameter_list|,
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
name|EFI_STATUS
name|Status
decl_stmt|;
name|EFI_PHYSICAL_ADDRESS
name|Memory
decl_stmt|;
if|if
condition|(
name|Pages
operator|==
literal|0
condition|)
block|{
return|return
name|NULL
return|;
block|}
name|Status
operator|=
name|PeiServicesAllocatePages
argument_list|(
name|MemoryType
argument_list|,
name|Pages
argument_list|,
operator|&
name|Memory
argument_list|)
expr_stmt|;
if|if
condition|(
name|EFI_ERROR
argument_list|(
name|Status
argument_list|)
condition|)
block|{
return|return
name|NULL
return|;
block|}
return|return
operator|(
name|VOID
operator|*
operator|)
operator|(
name|UINTN
operator|)
name|Memory
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiBootServicesData.    Allocates the number of 4KB pages of type EfiBootServicesData and returns a pointer to the   allocated buffer.  The buffer returned is aligned on a 4KB boundary.  If Pages is 0, then NULL   is returned.  If there is not enough memory remaining to satisfy the request, then NULL is   returned.    @param  Pages                 The number of 4 KB pages to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocatePages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
return|return
name|InternalAllocatePages
argument_list|(
name|EfiBootServicesData
argument_list|,
name|Pages
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiRuntimeServicesData.    Allocates the number of 4KB pages of type EfiRuntimeServicesData and returns a pointer to the   allocated buffer.  The buffer returned is aligned on a 4KB boundary.  If Pages is 0, then NULL   is returned.  If there is not enough memory remaining to satisfy the request, then NULL is   returned.    @param  Pages                 The number of 4 KB pages to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateRuntimePages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
return|return
name|InternalAllocatePages
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|Pages
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiReservedMemoryType.    Allocates the number of 4KB pages of type EfiReservedMemoryType and returns a pointer to the   allocated buffer.  The buffer returned is aligned on a 4KB boundary.  If Pages is 0, then NULL   is returned.  If there is not enough memory remaining to satisfy the request, then NULL is   returned.    @param  Pages                 The number of 4 KB pages to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateReservedPages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
return|return
name|InternalAllocatePages
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|Pages
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Frees one or more 4KB pages that were previously allocated with one of the page allocation   functions in the Memory Allocation Library.    Frees the number of 4KB pages specified by Pages from the buffer specified by Buffer.  Buffer   must have been allocated on a previous call to the page allocation services of the Memory   Allocation Library.  If it is not possible to free allocated pages, then this function will   perform no actions.      If Buffer was not allocated with a page allocation function in the Memory Allocation Library,   then ASSERT().   If Pages is zero, then ASSERT().     @param  Buffer                The pointer to the buffer of pages to free.   @param  Pages                 The number of 4 KB pages to free.  **/
end_comment

begin_function
name|VOID
name|EFIAPI
name|FreePages
parameter_list|(
name|IN
name|VOID
modifier|*
name|Buffer
parameter_list|,
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
name|ASSERT
argument_list|(
name|Pages
operator|!=
literal|0
argument_list|)
expr_stmt|;
comment|//
comment|// PEI phase does not support to free pages, so leave it as NOP.
comment|//
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of a certain memory type at a specified alignment.    Allocates the number of 4KB pages specified by Pages of a certain memory type with an alignment   specified by Alignment.  The allocated buffer is returned.  If Pages is 0, then NULL is returned.   If there is not enough memory at the specified alignment remaining to satisfy the request, then   NULL is returned.   If Alignment is not a power of two and Alignment is not zero, then ASSERT().   If Pages plus EFI_SIZE_TO_PAGES (Alignment) overflows, then ASSERT().    @param  MemoryType            The type of memory to allocate.   @param  Pages                 The number of 4 KB pages to allocate.   @param  Alignment             The requested alignment of the allocation.                                   Must be a power of two.                                 If Alignment is zero, then byte alignment is used.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalAllocateAlignedPages
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|MemoryType
parameter_list|,
name|IN
name|UINTN
name|Pages
parameter_list|,
name|IN
name|UINTN
name|Alignment
parameter_list|)
block|{
name|EFI_PHYSICAL_ADDRESS
name|Memory
decl_stmt|;
name|EFI_PHYSICAL_ADDRESS
name|AlignedMemory
decl_stmt|;
name|EFI_PEI_HOB_POINTERS
name|Hob
decl_stmt|;
name|BOOLEAN
name|SkipBeforeMemHob
decl_stmt|;
name|BOOLEAN
name|SkipAfterMemHob
decl_stmt|;
name|EFI_PHYSICAL_ADDRESS
name|HobBaseAddress
decl_stmt|;
name|UINT64
name|HobLength
decl_stmt|;
name|EFI_MEMORY_TYPE
name|HobMemoryType
decl_stmt|;
name|UINTN
name|TotalPages
decl_stmt|;
comment|//
comment|// Alignment must be a power of two or zero.
comment|//
name|ASSERT
argument_list|(
operator|(
name|Alignment
operator|&
operator|(
name|Alignment
operator|-
literal|1
operator|)
operator|)
operator|==
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|Pages
operator|==
literal|0
condition|)
block|{
return|return
name|NULL
return|;
block|}
comment|//
comment|// Make sure that Pages plus EFI_SIZE_TO_PAGES (Alignment) does not overflow.
comment|//
name|ASSERT
argument_list|(
name|Pages
operator|<=
operator|(
name|MAX_ADDRESS
operator|-
name|EFI_SIZE_TO_PAGES
argument_list|(
name|Alignment
argument_list|)
operator|)
argument_list|)
expr_stmt|;
comment|//
comment|// We would rather waste some memory to save PEI code size.
comment|// meaning in addition to the requested size for the aligned mem,
comment|// we simply reserve an overhead memory equal to Alignmemt(page-aligned), no matter what.
comment|// The overhead mem size could be reduced later with more involved malloc mechanisms
comment|// (e.g., somthing that can detect the alignment boundary before allocating memory or
comment|//  can request that memory be allocated at a certain address that is aleady aligned).
comment|//
name|TotalPages
operator|=
name|Pages
operator|+
operator|(
name|Alignment
operator|<=
name|EFI_PAGE_SIZE
condition|?
literal|0
else|:
name|EFI_SIZE_TO_PAGES
argument_list|(
name|Alignment
argument_list|)
operator|)
expr_stmt|;
name|Memory
operator|=
operator|(
name|EFI_PHYSICAL_ADDRESS
operator|)
operator|(
name|UINTN
operator|)
name|InternalAllocatePages
argument_list|(
name|MemoryType
argument_list|,
name|TotalPages
argument_list|)
expr_stmt|;
if|if
condition|(
name|Memory
operator|==
literal|0
condition|)
block|{
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Out of memory resource! \n"
operator|)
argument_list|)
expr_stmt|;
return|return
name|NULL
return|;
block|}
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Allocated Memory unaligned: Address = 0x%LX, Pages = 0x%X, Type = %d \n"
operator|,
name|Memory
operator|,
name|TotalPages
operator|,
operator|(
name|UINTN
operator|)
name|MemoryType
operator|)
argument_list|)
expr_stmt|;
comment|//
comment|// Alignment calculation
comment|//
name|AlignedMemory
operator|=
name|Memory
expr_stmt|;
if|if
condition|(
name|Alignment
operator|>
name|EFI_PAGE_SIZE
condition|)
block|{
name|AlignedMemory
operator|=
name|ALIGN_VALUE
argument_list|(
name|Memory
argument_list|,
name|Alignment
argument_list|)
expr_stmt|;
block|}
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"After aligning to 0x%X bytes: Address = 0x%LX, Pages = 0x%X \n"
operator|,
name|Alignment
operator|,
name|AlignedMemory
operator|,
name|Pages
operator|)
argument_list|)
expr_stmt|;
comment|//
comment|// In general three HOBs cover the total allocated space.
comment|// The aligned portion is covered by the aligned mem HOB and
comment|// the unaligned(to be freed) portions before and after the aligned portion are covered by newly created HOBs.
comment|//
comment|// Before mem HOB covers the region between "Memory" and "AlignedMemory"
comment|// Aligned mem HOB covers the region between "AlignedMemory" and "AlignedMemory + EFI_PAGES_TO_SIZE(Pages)"
comment|// After mem HOB covers the region between "AlignedMemory + EFI_PAGES_TO_SIZE(Pages)" and "Memory + EFI_PAGES_TO_SIZE(TotalPages)"
comment|//
comment|// The before or after mem HOBs need to be skipped under special cases where the aligned portion
comment|// touches either the top or bottom of the original allocated space.
comment|//
name|SkipBeforeMemHob
operator|=
name|FALSE
expr_stmt|;
name|SkipAfterMemHob
operator|=
name|FALSE
expr_stmt|;
if|if
condition|(
name|Memory
operator|==
name|AlignedMemory
condition|)
block|{
name|SkipBeforeMemHob
operator|=
name|TRUE
expr_stmt|;
block|}
if|if
condition|(
operator|(
name|Memory
operator|+
name|EFI_PAGES_TO_SIZE
argument_list|(
name|TotalPages
argument_list|)
operator|)
operator|==
operator|(
name|AlignedMemory
operator|+
name|EFI_PAGES_TO_SIZE
argument_list|(
name|Pages
argument_list|)
operator|)
condition|)
block|{
comment|//
comment|// This condition is never met in the current implementation.
comment|// There is always some after-mem since the overhead mem(used in TotalPages)
comment|// is no less than Alignment.
comment|//
name|SkipAfterMemHob
operator|=
name|TRUE
expr_stmt|;
block|}
comment|//
comment|// Search for the mem HOB referring to the original(unaligned) allocation
comment|// and update the size and type if needed.
comment|//
name|Hob
operator|.
name|Raw
operator|=
name|GetFirstHob
argument_list|(
name|EFI_HOB_TYPE_MEMORY_ALLOCATION
argument_list|)
expr_stmt|;
while|while
condition|(
name|Hob
operator|.
name|Raw
operator|!=
name|NULL
condition|)
block|{
if|if
condition|(
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryBaseAddress
operator|==
name|Memory
condition|)
block|{
break|break;
block|}
name|Hob
operator|.
name|Raw
operator|=
name|GET_NEXT_HOB
argument_list|(
name|Hob
argument_list|)
expr_stmt|;
name|Hob
operator|.
name|Raw
operator|=
name|GetNextHob
argument_list|(
name|EFI_HOB_TYPE_MEMORY_ALLOCATION
argument_list|,
name|Hob
operator|.
name|Raw
argument_list|)
expr_stmt|;
block|}
name|ASSERT
argument_list|(
name|Hob
operator|.
name|Raw
operator|!=
name|NULL
argument_list|)
expr_stmt|;
if|if
condition|(
name|SkipBeforeMemHob
condition|)
block|{
comment|//
comment|// Use this HOB as aligned mem HOB as there is no portion before it.
comment|//
name|HobLength
operator|=
name|EFI_PAGES_TO_SIZE
argument_list|(
name|Pages
argument_list|)
expr_stmt|;
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryLength
operator|=
name|HobLength
expr_stmt|;
block|}
else|else
block|{
comment|//
comment|// Use this HOB as before mem HOB and create a new HOB for the aligned portion
comment|//
name|HobLength
operator|=
operator|(
name|AlignedMemory
operator|-
name|Memory
operator|)
expr_stmt|;
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryLength
operator|=
name|HobLength
expr_stmt|;
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryType
operator|=
name|EfiConventionalMemory
expr_stmt|;
block|}
name|HobBaseAddress
operator|=
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryBaseAddress
expr_stmt|;
name|HobMemoryType
operator|=
name|Hob
operator|.
name|MemoryAllocation
operator|->
name|AllocDescriptor
operator|.
name|MemoryType
expr_stmt|;
comment|//
comment|// Build the aligned mem HOB if needed
comment|//
if|if
condition|(
operator|!
name|SkipBeforeMemHob
condition|)
block|{
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Updated before-mem HOB with BaseAddress = %LX, Length = %LX, MemoryType = %d \n"
operator|,
name|HobBaseAddress
operator|,
name|HobLength
operator|,
operator|(
name|UINTN
operator|)
name|HobMemoryType
operator|)
argument_list|)
expr_stmt|;
name|HobBaseAddress
operator|=
name|AlignedMemory
expr_stmt|;
name|HobLength
operator|=
name|EFI_PAGES_TO_SIZE
argument_list|(
name|Pages
argument_list|)
expr_stmt|;
name|HobMemoryType
operator|=
name|MemoryType
expr_stmt|;
name|BuildMemoryAllocationHob
argument_list|(
name|HobBaseAddress
argument_list|,
name|HobLength
argument_list|,
name|HobMemoryType
argument_list|)
expr_stmt|;
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Created aligned-mem HOB with BaseAddress = %LX, Length = %LX, MemoryType = %d \n"
operator|,
name|HobBaseAddress
operator|,
name|HobLength
operator|,
operator|(
name|UINTN
operator|)
name|HobMemoryType
operator|)
argument_list|)
expr_stmt|;
block|}
else|else
block|{
if|if
condition|(
name|HobBaseAddress
operator|!=
literal|0
condition|)
block|{
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Updated aligned-mem HOB with BaseAddress = %LX, Length = %LX, MemoryType = %d \n"
operator|,
name|HobBaseAddress
operator|,
name|HobLength
operator|,
operator|(
name|UINTN
operator|)
name|HobMemoryType
operator|)
argument_list|)
expr_stmt|;
block|}
block|}
comment|//
comment|// Build the after mem HOB if needed
comment|//
if|if
condition|(
operator|!
name|SkipAfterMemHob
condition|)
block|{
name|HobBaseAddress
operator|=
name|AlignedMemory
operator|+
name|EFI_PAGES_TO_SIZE
argument_list|(
name|Pages
argument_list|)
expr_stmt|;
name|HobLength
operator|=
operator|(
name|Memory
operator|+
name|EFI_PAGES_TO_SIZE
argument_list|(
name|TotalPages
argument_list|)
operator|)
operator|-
operator|(
name|AlignedMemory
operator|+
name|EFI_PAGES_TO_SIZE
argument_list|(
name|Pages
argument_list|)
operator|)
expr_stmt|;
name|HobMemoryType
operator|=
name|EfiConventionalMemory
expr_stmt|;
name|BuildMemoryAllocationHob
argument_list|(
name|HobBaseAddress
argument_list|,
name|HobLength
argument_list|,
name|HobMemoryType
argument_list|)
expr_stmt|;
name|DEBUG
argument_list|(
operator|(
name|DEBUG_INFO
operator|,
literal|"Created after-mem HOB with BaseAddress = %LX, Length = %LX, MemoryType = %d \n"
operator|,
name|HobBaseAddress
operator|,
name|HobLength
operator|,
operator|(
name|UINTN
operator|)
name|HobMemoryType
operator|)
argument_list|)
expr_stmt|;
block|}
return|return
operator|(
name|VOID
operator|*
operator|)
operator|(
name|UINTN
operator|)
name|AlignedMemory
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiBootServicesData at a specified alignment.    Allocates the number of 4KB pages specified by Pages of type EfiBootServicesData with an   alignment specified by Alignment.  The allocated buffer is returned.  If Pages is 0, then NULL is   returned.  If there is not enough memory at the specified alignment remaining to satisfy the   request, then NULL is returned.      If Alignment is not a power of two and Alignment is not zero, then ASSERT().   If Pages plus EFI_SIZE_TO_PAGES (Alignment) overflows, then ASSERT().    @param  Pages                 The number of 4 KB pages to allocate.   @param  Alignment             The requested alignment of the allocation.                                   Must be a power of two.                                 If Alignment is zero, then byte alignment is used.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateAlignedPages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|,
name|IN
name|UINTN
name|Alignment
parameter_list|)
block|{
return|return
name|InternalAllocateAlignedPages
argument_list|(
name|EfiBootServicesData
argument_list|,
name|Pages
argument_list|,
name|Alignment
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiRuntimeServicesData at a specified alignment.    Allocates the number of 4KB pages specified by Pages of type EfiRuntimeServicesData with an   alignment specified by Alignment.  The allocated buffer is returned.  If Pages is 0, then NULL is   returned.  If there is not enough memory at the specified alignment remaining to satisfy the   request, then NULL is returned.      If Alignment is not a power of two and Alignment is not zero, then ASSERT().   If Pages plus EFI_SIZE_TO_PAGES (Alignment) overflows, then ASSERT().    @param  Pages                 The number of 4 KB pages to allocate.   @param  Alignment             The requested alignment of the allocation.                                   Must be a power of two.                                 If Alignment is zero, then byte alignment is used.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateAlignedRuntimePages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|,
name|IN
name|UINTN
name|Alignment
parameter_list|)
block|{
return|return
name|InternalAllocateAlignedPages
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|Pages
argument_list|,
name|Alignment
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates one or more 4KB pages of type EfiReservedMemoryType at a specified alignment.    Allocates the number of 4KB pages specified by Pages of type EfiReservedMemoryType with an   alignment specified by Alignment.  The allocated buffer is returned.  If Pages is 0, then NULL is   returned.  If there is not enough memory at the specified alignment remaining to satisfy the   request, then NULL is returned.      If Alignment is not a power of two and Alignment is not zero, then ASSERT().   If Pages plus EFI_SIZE_TO_PAGES (Alignment) overflows, then ASSERT().    @param  Pages                 The number of 4 KB pages to allocate.   @param  Alignment             The requested alignment of the allocation.                                   Must be a power of two.                                 If Alignment is zero, then byte alignment is used.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateAlignedReservedPages
parameter_list|(
name|IN
name|UINTN
name|Pages
parameter_list|,
name|IN
name|UINTN
name|Alignment
parameter_list|)
block|{
return|return
name|InternalAllocateAlignedPages
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|Pages
argument_list|,
name|Alignment
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Frees one or more 4KB pages that were previously allocated with one of the aligned page   allocation functions in the Memory Allocation Library.    Frees the number of 4KB pages specified by Pages from the buffer specified by Buffer.  Buffer   must have been allocated on a previous call to the aligned page allocation services of the Memory   Allocation Library.  If it is not possible to free allocated pages, then this function will    perform no actions.      If Buffer was not allocated with an aligned page allocation function in the Memory Allocation   Library, then ASSERT().   If Pages is zero, then ASSERT().      @param  Buffer                The pointer to the buffer of pages to free.   @param  Pages                 The number of 4 KB pages to free.  **/
end_comment

begin_function
name|VOID
name|EFIAPI
name|FreeAlignedPages
parameter_list|(
name|IN
name|VOID
modifier|*
name|Buffer
parameter_list|,
name|IN
name|UINTN
name|Pages
parameter_list|)
block|{
name|ASSERT
argument_list|(
name|Pages
operator|!=
literal|0
argument_list|)
expr_stmt|;
comment|//
comment|// PEI phase does not support to free pages, so leave it as NOP.
comment|//
block|}
end_function

begin_comment
comment|/**   Allocates a buffer of a certain pool type.    Allocates the number bytes specified by AllocationSize of a certain pool type and returns a   pointer to the allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is   returned.  If there is not enough memory remaining to satisfy the request, then NULL is returned.    @param  MemoryType            The type of memory to allocate.   @param  AllocationSize        The number of bytes to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalAllocatePool
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|MemoryType
parameter_list|,
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
comment|//
comment|// If we need lots of small runtime/reserved memory type from PEI in the future,
comment|// we can consider providing a more complex algorithm that allocates runtime pages and
comment|// provide pool allocations from those pages.
comment|//
return|return
name|InternalAllocatePages
argument_list|(
name|MemoryType
argument_list|,
name|EFI_SIZE_TO_PAGES
argument_list|(
name|AllocationSize
argument_list|)
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates a buffer of type EfiBootServicesData.    Allocates the number bytes specified by AllocationSize of type EfiBootServicesData and returns a   pointer to the allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is   returned.  If there is not enough memory remaining to satisfy the request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocatePool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
name|EFI_STATUS
name|Status
decl_stmt|;
name|VOID
modifier|*
name|Buffer
decl_stmt|;
name|Status
operator|=
name|PeiServicesAllocatePool
argument_list|(
name|AllocationSize
argument_list|,
operator|&
name|Buffer
argument_list|)
expr_stmt|;
if|if
condition|(
name|EFI_ERROR
argument_list|(
name|Status
argument_list|)
condition|)
block|{
name|Buffer
operator|=
name|NULL
expr_stmt|;
block|}
return|return
name|Buffer
return|;
block|}
end_function

begin_comment
comment|/**   Allocates a buffer of type EfiRuntimeServicesData.    Allocates the number bytes specified by AllocationSize of type EfiRuntimeServicesData and returns   a pointer to the allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is   returned.  If there is not enough memory remaining to satisfy the request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateRuntimePool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
return|return
name|InternalAllocatePool
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|AllocationSize
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates a buffer of type EfiReservedMemoryType.    Allocates the number bytes specified by AllocationSize of type EfiReservedMemoryType and returns   a pointer to the allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is   returned.  If there is not enough memory remaining to satisfy the request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateReservedPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
return|return
name|InternalAllocatePool
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|AllocationSize
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates and zeros a buffer of a certain pool type.    Allocates the number bytes specified by AllocationSize of a certain pool type, clears the buffer   with zeros, and returns a pointer to the allocated buffer.  If AllocationSize is 0, then a valid   buffer of 0 size is returned.  If there is not enough memory remaining to satisfy the request,   then NULL is returned.    @param  PoolType              The type of memory to allocate.   @param  AllocationSize        The number of bytes to allocate and zero.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalAllocateZeroPool
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|PoolType
parameter_list|,
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
name|VOID
modifier|*
name|Memory
decl_stmt|;
name|Memory
operator|=
name|InternalAllocatePool
argument_list|(
name|PoolType
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|Memory
operator|!=
name|NULL
condition|)
block|{
name|Memory
operator|=
name|ZeroMem
argument_list|(
name|Memory
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
block|}
return|return
name|Memory
return|;
block|}
end_function

begin_comment
comment|/**   Allocates and zeros a buffer of type EfiBootServicesData.    Allocates the number bytes specified by AllocationSize of type EfiBootServicesData, clears the   buffer with zeros, and returns a pointer to the allocated buffer.  If AllocationSize is 0, then a   valid buffer of 0 size is returned.  If there is not enough memory remaining to satisfy the   request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate and zero.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateZeroPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
name|VOID
modifier|*
name|Memory
decl_stmt|;
name|Memory
operator|=
name|AllocatePool
argument_list|(
name|AllocationSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|Memory
operator|!=
name|NULL
condition|)
block|{
name|Memory
operator|=
name|ZeroMem
argument_list|(
name|Memory
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
block|}
return|return
name|Memory
return|;
block|}
end_function

begin_comment
comment|/**   Allocates and zeros a buffer of type EfiRuntimeServicesData.    Allocates the number bytes specified by AllocationSize of type EfiRuntimeServicesData, clears the   buffer with zeros, and returns a pointer to the allocated buffer.  If AllocationSize is 0, then a   valid buffer of 0 size is returned.  If there is not enough memory remaining to satisfy the   request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate and zero.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateRuntimeZeroPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
return|return
name|InternalAllocateZeroPool
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|AllocationSize
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Allocates and zeros a buffer of type EfiReservedMemoryType.    Allocates the number bytes specified by AllocationSize of type EfiReservedMemoryType, clears the   buffer with zeros, and returns a pointer to the allocated buffer.  If AllocationSize is 0, then a   valid buffer of 0 size is returned.  If there is not enough memory remaining to satisfy the   request, then NULL is returned.    @param  AllocationSize        The number of bytes to allocate and zero.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateReservedZeroPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|)
block|{
return|return
name|InternalAllocateZeroPool
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|AllocationSize
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Copies a buffer to an allocated buffer of a certain pool type.    Allocates the number bytes specified by AllocationSize of a certain pool type, copies   AllocationSize bytes from Buffer to the newly allocated buffer, and returns a pointer to the   allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is returned.  If there   is not enough memory remaining to satisfy the request, then NULL is returned.   If Buffer is NULL, then ASSERT().   If AllocationSize is greater than (MAX_ADDRESS - Buffer + 1), then ASSERT().     @param  PoolType              The type of pool to allocate.   @param  AllocationSize        The number of bytes to allocate and zero.   @param  Buffer                The buffer to copy to the allocated buffer.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalAllocateCopyPool
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|PoolType
parameter_list|,
name|IN
name|UINTN
name|AllocationSize
parameter_list|,
name|IN
name|CONST
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
name|VOID
modifier|*
name|Memory
decl_stmt|;
name|ASSERT
argument_list|(
name|Buffer
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|AllocationSize
operator|<=
operator|(
name|MAX_ADDRESS
operator|-
operator|(
name|UINTN
operator|)
name|Buffer
operator|+
literal|1
operator|)
argument_list|)
expr_stmt|;
name|Memory
operator|=
name|InternalAllocatePool
argument_list|(
name|PoolType
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|Memory
operator|!=
name|NULL
condition|)
block|{
name|Memory
operator|=
name|CopyMem
argument_list|(
name|Memory
argument_list|,
name|Buffer
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
block|}
return|return
name|Memory
return|;
block|}
end_function

begin_comment
comment|/**   Copies a buffer to an allocated buffer of type EfiBootServicesData.    Allocates the number bytes specified by AllocationSize of type EfiBootServicesData, copies   AllocationSize bytes from Buffer to the newly allocated buffer, and returns a pointer to the   allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is returned.  If there   is not enough memory remaining to satisfy the request, then NULL is returned.      If Buffer is NULL, then ASSERT().   If AllocationSize is greater than (MAX_ADDRESS - Buffer + 1), then ASSERT().     @param  AllocationSize        The number of bytes to allocate and zero.   @param  Buffer                The buffer to copy to the allocated buffer.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateCopyPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|,
name|IN
name|CONST
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
name|VOID
modifier|*
name|Memory
decl_stmt|;
name|ASSERT
argument_list|(
name|Buffer
operator|!=
name|NULL
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|AllocationSize
operator|<=
operator|(
name|MAX_ADDRESS
operator|-
operator|(
name|UINTN
operator|)
name|Buffer
operator|+
literal|1
operator|)
argument_list|)
expr_stmt|;
name|Memory
operator|=
name|AllocatePool
argument_list|(
name|AllocationSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|Memory
operator|!=
name|NULL
condition|)
block|{
name|Memory
operator|=
name|CopyMem
argument_list|(
name|Memory
argument_list|,
name|Buffer
argument_list|,
name|AllocationSize
argument_list|)
expr_stmt|;
block|}
return|return
name|Memory
return|;
block|}
end_function

begin_comment
comment|/**   Copies a buffer to an allocated buffer of type EfiRuntimeServicesData.    Allocates the number bytes specified by AllocationSize of type EfiRuntimeServicesData, copies   AllocationSize bytes from Buffer to the newly allocated buffer, and returns a pointer to the   allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is returned.  If there   is not enough memory remaining to satisfy the request, then NULL is returned.      If Buffer is NULL, then ASSERT().   If AllocationSize is greater than (MAX_ADDRESS - Buffer + 1), then ASSERT().     @param  AllocationSize        The number of bytes to allocate and zero.   @param  Buffer                The buffer to copy to the allocated buffer.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateRuntimeCopyPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|,
name|IN
name|CONST
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
return|return
name|InternalAllocateCopyPool
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|AllocationSize
argument_list|,
name|Buffer
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Copies a buffer to an allocated buffer of type EfiReservedMemoryType.    Allocates the number bytes specified by AllocationSize of type EfiReservedMemoryType, copies   AllocationSize bytes from Buffer to the newly allocated buffer, and returns a pointer to the   allocated buffer.  If AllocationSize is 0, then a valid buffer of 0 size is returned.  If there   is not enough memory remaining to satisfy the request, then NULL is returned.      If Buffer is NULL, then ASSERT().   If AllocationSize is greater than (MAX_ADDRESS - Buffer + 1), then ASSERT().     @param  AllocationSize        The number of bytes to allocate and zero.   @param  Buffer                The buffer to copy to the allocated buffer.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|AllocateReservedCopyPool
parameter_list|(
name|IN
name|UINTN
name|AllocationSize
parameter_list|,
name|IN
name|CONST
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
return|return
name|InternalAllocateCopyPool
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|AllocationSize
argument_list|,
name|Buffer
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Reallocates a buffer of a specified memory type.    Allocates and zeros the number bytes specified by NewSize from memory of the type   specified by PoolType.  If OldBuffer is not NULL, then the smaller of OldSize and    NewSize bytes are copied from OldBuffer to the newly allocated buffer, and    OldBuffer is freed.  A pointer to the newly allocated buffer is returned.     If NewSize is 0, then a valid buffer of 0 size is  returned.  If there is not    enough memory remaining to satisfy the request, then NULL is returned.      If the allocation of the new buffer is successful and the smaller of NewSize and OldSize   is greater than (MAX_ADDRESS - OldBuffer + 1), then ASSERT().    @param  PoolType       The type of pool to allocate.   @param  OldSize        The size, in bytes, of OldBuffer.   @param  NewSize        The size, in bytes, of the buffer to reallocate.   @param  OldBuffer      The buffer to copy to the allocated buffer.  This is an                           optional parameter that may be NULL.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|InternalReallocatePool
parameter_list|(
name|IN
name|EFI_MEMORY_TYPE
name|PoolType
parameter_list|,
name|IN
name|UINTN
name|OldSize
parameter_list|,
name|IN
name|UINTN
name|NewSize
parameter_list|,
name|IN
name|VOID
modifier|*
name|OldBuffer
name|OPTIONAL
parameter_list|)
block|{
name|VOID
modifier|*
name|NewBuffer
decl_stmt|;
name|NewBuffer
operator|=
name|InternalAllocateZeroPool
argument_list|(
name|PoolType
argument_list|,
name|NewSize
argument_list|)
expr_stmt|;
if|if
condition|(
name|NewBuffer
operator|!=
name|NULL
operator|&&
name|OldBuffer
operator|!=
name|NULL
condition|)
block|{
name|CopyMem
argument_list|(
name|NewBuffer
argument_list|,
name|OldBuffer
argument_list|,
name|MIN
argument_list|(
name|OldSize
argument_list|,
name|NewSize
argument_list|)
argument_list|)
expr_stmt|;
name|FreePool
argument_list|(
name|OldBuffer
argument_list|)
expr_stmt|;
block|}
return|return
name|NewBuffer
return|;
block|}
end_function

begin_comment
comment|/**   Reallocates a buffer of type EfiBootServicesData.    Allocates and zeros the number bytes specified by NewSize from memory of type   EfiBootServicesData.  If OldBuffer is not NULL, then the smaller of OldSize and    NewSize bytes are copied from OldBuffer to the newly allocated buffer, and    OldBuffer is freed.  A pointer to the newly allocated buffer is returned.     If NewSize is 0, then a valid buffer of 0 size is  returned.  If there is not    enough memory remaining to satisfy the request, then NULL is returned.      If the allocation of the new buffer is successful and the smaller of NewSize and OldSize   is greater than (MAX_ADDRESS - OldBuffer + 1), then ASSERT().    @param  OldSize        The size, in bytes, of OldBuffer.   @param  NewSize        The size, in bytes, of the buffer to reallocate.   @param  OldBuffer      The buffer to copy to the allocated buffer.  This is an optional                           parameter that may be NULL.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|ReallocatePool
parameter_list|(
name|IN
name|UINTN
name|OldSize
parameter_list|,
name|IN
name|UINTN
name|NewSize
parameter_list|,
name|IN
name|VOID
modifier|*
name|OldBuffer
name|OPTIONAL
parameter_list|)
block|{
return|return
name|InternalReallocatePool
argument_list|(
name|EfiBootServicesData
argument_list|,
name|OldSize
argument_list|,
name|NewSize
argument_list|,
name|OldBuffer
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Reallocates a buffer of type EfiRuntimeServicesData.    Allocates and zeros the number bytes specified by NewSize from memory of type   EfiRuntimeServicesData.  If OldBuffer is not NULL, then the smaller of OldSize and    NewSize bytes are copied from OldBuffer to the newly allocated buffer, and    OldBuffer is freed.  A pointer to the newly allocated buffer is returned.     If NewSize is 0, then a valid buffer of 0 size is  returned.  If there is not    enough memory remaining to satisfy the request, then NULL is returned.    If the allocation of the new buffer is successful and the smaller of NewSize and OldSize   is greater than (MAX_ADDRESS - OldBuffer + 1), then ASSERT().    @param  OldSize        The size, in bytes, of OldBuffer.   @param  NewSize        The size, in bytes, of the buffer to reallocate.   @param  OldBuffer      The buffer to copy to the allocated buffer.  This is an optional                           parameter that may be NULL.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|ReallocateRuntimePool
parameter_list|(
name|IN
name|UINTN
name|OldSize
parameter_list|,
name|IN
name|UINTN
name|NewSize
parameter_list|,
name|IN
name|VOID
modifier|*
name|OldBuffer
name|OPTIONAL
parameter_list|)
block|{
return|return
name|InternalReallocatePool
argument_list|(
name|EfiRuntimeServicesData
argument_list|,
name|OldSize
argument_list|,
name|NewSize
argument_list|,
name|OldBuffer
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Reallocates a buffer of type EfiReservedMemoryType.    Allocates and zeros the number bytes specified by NewSize from memory of type   EfiReservedMemoryType.  If OldBuffer is not NULL, then the smaller of OldSize and    NewSize bytes are copied from OldBuffer to the newly allocated buffer, and    OldBuffer is freed.  A pointer to the newly allocated buffer is returned.     If NewSize is 0, then a valid buffer of 0 size is  returned.  If there is not    enough memory remaining to satisfy the request, then NULL is returned.    If the allocation of the new buffer is successful and the smaller of NewSize and OldSize   is greater than (MAX_ADDRESS - OldBuffer + 1), then ASSERT().    @param  OldSize        The size, in bytes, of OldBuffer.   @param  NewSize        The size, in bytes, of the buffer to reallocate.   @param  OldBuffer      The buffer to copy to the allocated buffer.  This is an                           optional parameter that may be NULL.    @return A pointer to the allocated buffer or NULL if allocation fails.  **/
end_comment

begin_function
name|VOID
modifier|*
name|EFIAPI
name|ReallocateReservedPool
parameter_list|(
name|IN
name|UINTN
name|OldSize
parameter_list|,
name|IN
name|UINTN
name|NewSize
parameter_list|,
name|IN
name|VOID
modifier|*
name|OldBuffer
name|OPTIONAL
parameter_list|)
block|{
return|return
name|InternalReallocatePool
argument_list|(
name|EfiReservedMemoryType
argument_list|,
name|OldSize
argument_list|,
name|NewSize
argument_list|,
name|OldBuffer
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/**   Frees a buffer that was previously allocated with one of the pool allocation functions in the   Memory Allocation Library.    Frees the buffer specified by Buffer.  Buffer must have been allocated on a previous call to the   pool allocation services of the Memory Allocation Library.  If it is not possible to free pool   resources, then this function will perform no actions.      If Buffer was not allocated with a pool allocation function in the Memory Allocation Library,   then ASSERT().    @param  Buffer                The pointer to the buffer to free.  **/
end_comment

begin_function
name|VOID
name|EFIAPI
name|FreePool
parameter_list|(
name|IN
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
comment|//
comment|// PEI phase does not support to free pool, so leave it as NOP.
comment|//
block|}
end_function

end_unit

