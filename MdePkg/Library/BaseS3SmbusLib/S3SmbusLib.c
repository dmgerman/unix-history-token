begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/** @file   Smbus Library Services that do SMBus transactions and also enable the operatation   to be replayed during an S3 resume. This library class maps directly on top   of the SmbusLib class.     Copyright (c) 2007, Intel Corporation. All rights reserved.<BR>    This program and the accompanying materials   are licensed and made available under the terms and conditions   of the BSD License which accompanies this distribution.  The   full text of the license may be found at   http://opensource.org/licenses/bsd-license.php    THE PROGRAM IS DISTRIBUTED UNDER THE BSD LICENSE ON AN "AS IS" BASIS,   WITHOUT WARRANTIES OR REPRESENTATIONS OF ANY KIND, EITHER EXPRESS OR IMPLIED.  **/
end_comment

begin_include
include|#
directive|include
file|<Base.h>
end_include

begin_include
include|#
directive|include
file|<Library/DebugLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/S3BootScriptLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/SmbusLib.h>
end_include

begin_include
include|#
directive|include
file|<Library/S3SmbusLib.h>
end_include

begin_comment
comment|/**   Saves an SMBus operation to S3 script to be replayed on S3 resume.     This function provides a standard way to save SMBus operation to S3 boot Script.   The data can either be of the Length byte, word, or a block of data.   If it falis to save S3 boot script, then ASSERT ().    @param  SmbusOperation  Signifies which particular SMBus hardware protocol instance that it will use to                           execute the SMBus transactions.   @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Length          Signifies the number of bytes that this operation will do. The maximum number of                           bytes can be revision specific and operation specific.   @param  Buffer          Contains the value of data to execute to the SMBus slave device. Not all operations                           require this argument. The length of this buffer is identified by Length.  **/
end_comment

begin_function
name|VOID
name|InternalSaveSmBusExecToBootScript
parameter_list|(
name|IN
name|EFI_SMBUS_OPERATION
name|SmbusOperation
parameter_list|,
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|UINTN
name|Length
parameter_list|,
name|IN
name|OUT
name|VOID
modifier|*
name|Buffer
parameter_list|)
block|{
name|RETURN_STATUS
name|Status
decl_stmt|;
name|Status
operator|=
name|S3BootScriptSaveSmbusExecute
argument_list|(
name|SmBusAddress
argument_list|,
name|SmbusOperation
argument_list|,
operator|&
name|Length
argument_list|,
name|Buffer
argument_list|)
expr_stmt|;
name|ASSERT
argument_list|(
name|Status
operator|==
name|RETURN_SUCCESS
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS quick read command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS quick read command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address field of SmBusAddress is required.   If Status is not NULL, then the status of the executed command is returned in Status.   If PEC is set in SmBusAddress, then ASSERT().   If Command in SmBusAddress is not zero, then ASSERT().   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.  **/
end_comment

begin_function
name|VOID
name|EFIAPI
name|S3SmBusQuickRead
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|SmBusQuickRead
argument_list|(
name|SmBusAddress
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusQuickRead
argument_list|,
name|SmBusAddress
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS quick write command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS quick write command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address field of SmBusAddress is required.   If Status is not NULL, then the status of the executed command is returned in Status.   If PEC is set in SmBusAddress, then ASSERT().   If Command in SmBusAddress is not zero, then ASSERT().   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.  **/
end_comment

begin_function
name|VOID
name|EFIAPI
name|S3SmBusQuickWrite
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|SmBusQuickWrite
argument_list|(
name|SmBusAddress
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusQuickWrite
argument_list|,
name|SmBusAddress
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS receive byte command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS receive byte command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address field of SmBusAddress is required.   The byte received from the SMBUS is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Command in SmBusAddress is not zero, then ASSERT().   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The byte received from the SMBUS.  **/
end_comment

begin_function
name|UINT8
name|EFIAPI
name|S3SmBusReceiveByte
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT8
name|Byte
decl_stmt|;
name|Byte
operator|=
name|SmBusReceiveByte
argument_list|(
name|SmBusAddress
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusReceiveByte
argument_list|,
name|SmBusAddress
argument_list|,
literal|1
argument_list|,
operator|&
name|Byte
argument_list|)
expr_stmt|;
return|return
name|Byte
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS send byte command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS send byte command on the SMBUS device specified by SmBusAddress.   The byte specified by Value is sent.   Only the SMBUS slave address field of SmBusAddress is required.  Value is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Command in SmBusAddress is not zero, then ASSERT().   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Value           The 8-bit value to send.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The parameter of Value.  **/
end_comment

begin_function
name|UINT8
name|EFIAPI
name|S3SmBusSendByte
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|UINT8
name|Value
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT8
name|Byte
decl_stmt|;
name|Byte
operator|=
name|SmBusSendByte
argument_list|(
name|SmBusAddress
argument_list|,
name|Value
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusSendByte
argument_list|,
name|SmBusAddress
argument_list|,
literal|1
argument_list|,
operator|&
name|Byte
argument_list|)
expr_stmt|;
return|return
name|Byte
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS read data byte command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS read data byte command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   The 8-bit value read from the SMBUS is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The byte read from the SMBUS.  **/
end_comment

begin_function
name|UINT8
name|EFIAPI
name|S3SmBusReadDataByte
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT8
name|Byte
decl_stmt|;
name|Byte
operator|=
name|SmBusReadDataByte
argument_list|(
name|SmBusAddress
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusReadByte
argument_list|,
name|SmBusAddress
argument_list|,
literal|1
argument_list|,
operator|&
name|Byte
argument_list|)
expr_stmt|;
return|return
name|Byte
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS write data byte command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS write data byte command on the SMBUS device specified by SmBusAddress.   The 8-bit value specified by Value is written.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   Value is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Value           The 8-bit value to write.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The parameter of Value.  **/
end_comment

begin_function
name|UINT8
name|EFIAPI
name|S3SmBusWriteDataByte
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|UINT8
name|Value
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT8
name|Byte
decl_stmt|;
name|Byte
operator|=
name|SmBusWriteDataByte
argument_list|(
name|SmBusAddress
argument_list|,
name|Value
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusWriteByte
argument_list|,
name|SmBusAddress
argument_list|,
literal|1
argument_list|,
operator|&
name|Byte
argument_list|)
expr_stmt|;
return|return
name|Byte
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS read data word command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS read data word command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   The 16-bit value read from the SMBUS is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().      @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The byte read from the SMBUS.  **/
end_comment

begin_function
name|UINT16
name|EFIAPI
name|S3SmBusReadDataWord
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT16
name|Word
decl_stmt|;
name|Word
operator|=
name|SmBusReadDataWord
argument_list|(
name|SmBusAddress
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusReadWord
argument_list|,
name|SmBusAddress
argument_list|,
literal|2
argument_list|,
operator|&
name|Word
argument_list|)
expr_stmt|;
return|return
name|Word
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS write data word command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS write data word command on the SMBUS device specified by SmBusAddress.   The 16-bit value specified by Value is written.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   Value is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Value           The 16-bit value to write.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The parameter of Value.  **/
end_comment

begin_function
name|UINT16
name|EFIAPI
name|S3SmBusWriteDataWord
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|UINT16
name|Value
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT16
name|Word
decl_stmt|;
name|Word
operator|=
name|SmBusWriteDataWord
argument_list|(
name|SmBusAddress
argument_list|,
name|Value
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusWriteWord
argument_list|,
name|SmBusAddress
argument_list|,
literal|2
argument_list|,
operator|&
name|Word
argument_list|)
expr_stmt|;
return|return
name|Word
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS process call command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS process call command on the SMBUS device specified by SmBusAddress.   The 16-bit value specified by Value is written.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   The 16-bit value returned by the process call command is returned.   If Status is not NULL, then the status of the executed command is returned in Status.   If Length in SmBusAddress is not zero, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Value           The 16-bit value to write.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The 16-bit value returned by the process call command.  **/
end_comment

begin_function
name|UINT16
name|EFIAPI
name|S3SmBusProcessCall
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|UINT16
name|Value
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINT16
name|Word
decl_stmt|;
name|Word
operator|=
name|SmBusProcessCall
argument_list|(
name|SmBusAddress
argument_list|,
name|Value
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusProcessCall
argument_list|,
name|SmBusAddress
argument_list|,
literal|2
argument_list|,
operator|&
name|Value
argument_list|)
expr_stmt|;
return|return
name|Word
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS read block command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS read block command on the SMBUS device specified by SmBusAddress.   Only the SMBUS slave address and SMBUS command fields of SmBusAddress are required.   Bytes are read from the SMBUS and stored in Buffer.   The number of bytes read is returned, and will never return a value larger than 32-bytes.   If Status is not NULL, then the status of the executed command is returned in Status.   It is the caller's responsibility to make sure Buffer is large enough for the total number of bytes read.   SMBUS supports a maximum transfer size of 32 bytes, so Buffer does not need to be any larger than 32 bytes.   If Length in SmBusAddress is not zero, then ASSERT().   If Buffer is NULL, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Buffer          Pointer to the buffer to store the bytes read from the SMBUS.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The number of bytes read.  **/
end_comment

begin_function
name|UINTN
name|EFIAPI
name|S3SmBusReadBlock
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|VOID
modifier|*
name|Buffer
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINTN
name|Length
decl_stmt|;
name|Length
operator|=
name|SmBusReadBlock
argument_list|(
name|SmBusAddress
argument_list|,
name|Buffer
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusReadBlock
argument_list|,
name|SmBusAddress
argument_list|,
name|Length
argument_list|,
name|Buffer
argument_list|)
expr_stmt|;
return|return
name|Length
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS write block command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS write block command on the SMBUS device specified by SmBusAddress.   The SMBUS slave address, SMBUS command, and SMBUS length fields of SmBusAddress are required.   Bytes are written to the SMBUS from Buffer.   The number of bytes written is returned, and will never return a value larger than 32-bytes.   If Status is not NULL, then the status of the executed command is returned in Status.     If Length in SmBusAddress is zero or greater than 32, then ASSERT().   If Buffer is NULL, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  Buffer          Pointer to the buffer to store the bytes read from the SMBUS.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The number of bytes written.  **/
end_comment

begin_function
name|UINTN
name|EFIAPI
name|S3SmBusWriteBlock
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|OUT
name|VOID
modifier|*
name|Buffer
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINTN
name|Length
decl_stmt|;
name|Length
operator|=
name|SmBusWriteBlock
argument_list|(
name|SmBusAddress
argument_list|,
name|Buffer
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusWriteBlock
argument_list|,
name|SmBusAddress
argument_list|,
name|SMBUS_LIB_LENGTH
argument_list|(
name|SmBusAddress
argument_list|)
argument_list|,
name|Buffer
argument_list|)
expr_stmt|;
return|return
name|Length
return|;
block|}
end_function

begin_comment
comment|/**   Executes an SMBUS block process call command and saves the value in the S3 script to be replayed   on S3 resume.    Executes an SMBUS block process call command on the SMBUS device specified by SmBusAddress.   The SMBUS slave address, SMBUS command, and SMBUS length fields of SmBusAddress are required.   Bytes are written to the SMBUS from WriteBuffer.  Bytes are then read from the SMBUS into ReadBuffer.   If Status is not NULL, then the status of the executed command is returned in Status.   It is the caller's responsibility to make sure ReadBuffer is large enough for the total number of bytes read.   SMBUS supports a maximum transfer size of 32 bytes, so Buffer does not need to be any larger than 32 bytes.   If Length in SmBusAddress is zero or greater than 32, then ASSERT().   If WriteBuffer is NULL, then ASSERT().   If ReadBuffer is NULL, then ASSERT().   If any reserved bits of SmBusAddress are set, then ASSERT().    @param  SmBusAddress    Address that encodes the SMBUS Slave Address,                           SMBUS Command, SMBUS Data Length, and PEC.   @param  WriteBuffer     Pointer to the buffer of bytes to write to the SMBUS.   @param  ReadBuffer      Pointer to the buffer of bytes to read from the SMBUS.   @param  Status          Return status for the executed command.                           This is an optional parameter and may be NULL.    @return The number of bytes written.  **/
end_comment

begin_function
name|UINTN
name|EFIAPI
name|S3SmBusBlockProcessCall
parameter_list|(
name|IN
name|UINTN
name|SmBusAddress
parameter_list|,
name|IN
name|VOID
modifier|*
name|WriteBuffer
parameter_list|,
name|OUT
name|VOID
modifier|*
name|ReadBuffer
parameter_list|,
name|OUT
name|RETURN_STATUS
modifier|*
name|Status
name|OPTIONAL
parameter_list|)
block|{
name|UINTN
name|Length
decl_stmt|;
name|Length
operator|=
name|SmBusBlockProcessCall
argument_list|(
name|SmBusAddress
argument_list|,
name|WriteBuffer
argument_list|,
name|ReadBuffer
argument_list|,
name|Status
argument_list|)
expr_stmt|;
name|InternalSaveSmBusExecToBootScript
argument_list|(
name|EfiSmbusBWBRProcessCall
argument_list|,
name|SmBusAddress
argument_list|,
name|SMBUS_LIB_LENGTH
argument_list|(
name|SmBusAddress
argument_list|)
argument_list|,
name|ReadBuffer
argument_list|)
expr_stmt|;
return|return
name|Length
return|;
block|}
end_function

end_unit

