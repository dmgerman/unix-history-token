begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * $Source: /usr/src/kerberosIV/krb/RCS/kuserok.c,v $  * $Author: kfall $  *  * Copyright 1987, 1988 by the Massachusetts Institute of Technology.  *  * For copying and distribution information, please see the file  *<mit-copyright.h>.  *  * kuserok: check if a kerberos principal has  * access to a local account  */
end_comment

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|rcsid_kuserok_c
index|[]
init|=
literal|"$Header: /usr/src/kerberosIV/krb/RCS/kuserok.c,v 4.7 90/06/23 03:11:11 kfall Exp $"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|lint
end_endif

begin_include
include|#
directive|include
file|<mit-copyright.h>
end_include

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/socket.h>
end_include

begin_include
include|#
directive|include
file|<sys/stat.h>
end_include

begin_include
include|#
directive|include
file|<sys/file.h>
end_include

begin_include
include|#
directive|include
file|<des.h>
end_include

begin_include
include|#
directive|include
file|<krb.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<pwd.h>
end_include

begin_include
include|#
directive|include
file|<strings.h>
end_include

begin_define
define|#
directive|define
name|OK
value|0
end_define

begin_define
define|#
directive|define
name|NOTOK
value|1
end_define

begin_define
define|#
directive|define
name|MAX_USERNAME
value|10
end_define

begin_comment
comment|/*  * Given a Kerberos principal "kdata", and a local username "luser",  * determine whether user is authorized to login according to the  * authorization file ("~luser/.klogin" by default).  Returns OK  * if authorized, NOTOK if not authorized.  *  * If there is no account for "luser" on the local machine, returns  * NOTOK.  If there is no authorization file, and the given Kerberos  * name "kdata" translates to the same name as "luser" (using  * krb_kntoln()), returns OK.  Otherwise, if the authorization file  * can't be accessed, returns NOTOK.  Otherwise, the file is read for  * a matching principal name, instance, and realm.  If one is found,  * returns OK, if none is found, returns NOTOK.  *  * The file entries are in the format:  *  *	name.instance@realm  *  * one entry per line.  *  * The ATHENA_COMPAT code supports old-style Athena ~luser/.klogin  * file entries.  See the file "kparse.c".  */
end_comment

begin_function
name|int
name|kuserok
parameter_list|(
name|kdata
parameter_list|,
name|luser
parameter_list|)
name|AUTH_DAT
modifier|*
name|kdata
decl_stmt|;
name|char
modifier|*
name|luser
decl_stmt|;
block|{
name|struct
name|stat
name|sbuf
decl_stmt|;
name|struct
name|passwd
modifier|*
name|pwd
decl_stmt|;
name|char
name|pbuf
index|[
name|MAXPATHLEN
index|]
decl_stmt|;
name|int
name|isok
init|=
name|NOTOK
decl_stmt|,
name|rc
decl_stmt|;
name|FILE
modifier|*
name|fp
decl_stmt|;
name|char
name|kuser
index|[
name|MAX_USERNAME
index|]
decl_stmt|;
name|char
name|principal
index|[
name|ANAME_SZ
index|]
decl_stmt|,
name|inst
index|[
name|INST_SZ
index|]
decl_stmt|,
name|realm
index|[
name|REALM_SZ
index|]
decl_stmt|;
name|char
name|linebuf
index|[
name|BUFSIZ
index|]
decl_stmt|;
name|char
modifier|*
name|newline
decl_stmt|;
name|int
name|gobble
decl_stmt|;
comment|/* no account => no access */
if|if
condition|(
operator|(
name|pwd
operator|=
name|getpwnam
argument_list|(
name|luser
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
return|return
operator|(
name|NOTOK
operator|)
return|;
block|}
operator|(
name|void
operator|)
name|strcpy
argument_list|(
name|pbuf
argument_list|,
name|pwd
operator|->
name|pw_dir
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|strcat
argument_list|(
name|pbuf
argument_list|,
literal|"/.klogin"
argument_list|)
expr_stmt|;
if|if
condition|(
name|access
argument_list|(
name|pbuf
argument_list|,
name|F_OK
argument_list|)
condition|)
block|{
comment|/* not accessible */
comment|/* 	 * if he's trying to log in as himself, and there is no .klogin file, 	 * let him.  To find out, call 	 * krb_kntoln to convert the triple in kdata to a name which we can 	 * string compare.  	 */
if|if
condition|(
operator|!
name|krb_kntoln
argument_list|(
name|kdata
argument_list|,
name|kuser
argument_list|)
operator|&&
operator|(
name|strcmp
argument_list|(
name|kuser
argument_list|,
name|luser
argument_list|)
operator|==
literal|0
operator|)
condition|)
block|{
return|return
operator|(
name|OK
operator|)
return|;
block|}
block|}
comment|/* open ~/.klogin */
if|if
condition|(
operator|(
name|fp
operator|=
name|fopen
argument_list|(
name|pbuf
argument_list|,
literal|"r"
argument_list|)
operator|)
operator|==
name|NULL
condition|)
block|{
return|return
operator|(
name|NOTOK
operator|)
return|;
block|}
comment|/*      * security:  if the user does not own his own .klogin file,      * do not grant access      */
if|if
condition|(
name|fstat
argument_list|(
name|fileno
argument_list|(
name|fp
argument_list|)
argument_list|,
operator|&
name|sbuf
argument_list|)
condition|)
block|{
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
return|return
operator|(
name|NOTOK
operator|)
return|;
block|}
if|if
condition|(
name|sbuf
operator|.
name|st_uid
operator|!=
name|pwd
operator|->
name|pw_uid
condition|)
block|{
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
return|return
operator|(
name|NOTOK
operator|)
return|;
block|}
comment|/* check each line */
while|while
condition|(
operator|(
name|isok
operator|!=
name|OK
operator|)
operator|&&
operator|(
name|fgets
argument_list|(
name|linebuf
argument_list|,
name|BUFSIZ
argument_list|,
name|fp
argument_list|)
operator|!=
name|NULL
operator|)
condition|)
block|{
comment|/* null-terminate the input string */
name|linebuf
index|[
name|BUFSIZ
operator|-
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
name|newline
operator|=
name|NULL
expr_stmt|;
comment|/* nuke the newline if it exists */
if|if
condition|(
name|newline
operator|=
name|index
argument_list|(
name|linebuf
argument_list|,
literal|'\n'
argument_list|)
condition|)
operator|*
name|newline
operator|=
literal|'\0'
expr_stmt|;
name|rc
operator|=
name|kname_parse
argument_list|(
name|principal
argument_list|,
name|inst
argument_list|,
name|realm
argument_list|,
name|linebuf
argument_list|)
expr_stmt|;
if|if
condition|(
name|rc
operator|==
name|KSUCCESS
condition|)
block|{
name|isok
operator|=
operator|(
name|strncmp
argument_list|(
name|kdata
operator|->
name|pname
argument_list|,
name|principal
argument_list|,
name|ANAME_SZ
argument_list|)
operator|||
name|strncmp
argument_list|(
name|kdata
operator|->
name|pinst
argument_list|,
name|inst
argument_list|,
name|INST_SZ
argument_list|)
operator|||
name|strncasecmp
argument_list|(
name|kdata
operator|->
name|prealm
argument_list|,
name|realm
argument_list|,
name|REALM_SZ
argument_list|)
operator|)
expr_stmt|;
block|}
comment|/* clean up the rest of the line if necessary */
if|if
condition|(
operator|!
name|newline
condition|)
while|while
condition|(
operator|(
operator|(
name|gobble
operator|=
name|getc
argument_list|(
name|fp
argument_list|)
operator|)
operator|!=
name|EOF
operator|)
operator|&&
name|gobble
operator|!=
literal|'\n'
condition|)
empty_stmt|;
block|}
name|fclose
argument_list|(
name|fp
argument_list|)
expr_stmt|;
return|return
operator|(
name|isok
operator|)
return|;
block|}
end_function

end_unit

