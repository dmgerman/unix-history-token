begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  *	digest support for NTP, MD5 and with OpenSSL more  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_CONFIG_H
end_ifdef

begin_include
include|#
directive|include
file|<config.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_include
include|#
directive|include
file|"ntp_fp.h"
end_include

begin_include
include|#
directive|include
file|"ntp_string.h"
end_include

begin_include
include|#
directive|include
file|"ntp_stdlib.h"
end_include

begin_include
include|#
directive|include
file|"ntp.h"
end_include

begin_include
include|#
directive|include
file|"ntp_md5.h"
end_include

begin_comment
comment|/* provides OpenSSL digest API */
end_comment

begin_comment
comment|/*  * MD5authencrypt - generate message digest  *  * Returns length of MAC including key ID and digest.  */
end_comment

begin_function
name|int
name|MD5authencrypt
parameter_list|(
name|int
name|type
parameter_list|,
comment|/* hash algorithm */
name|u_char
modifier|*
name|key
parameter_list|,
comment|/* key pointer */
name|u_int32
modifier|*
name|pkt
parameter_list|,
comment|/* packet pointer */
name|int
name|length
comment|/* packet length */
parameter_list|)
block|{
name|u_char
name|digest
index|[
name|EVP_MAX_MD_SIZE
index|]
decl_stmt|;
name|u_int
name|len
decl_stmt|;
name|EVP_MD_CTX
name|ctx
decl_stmt|;
comment|/* 	 * Compute digest of key concatenated with packet. Note: the 	 * key type and digest type have been verified when the key 	 * was creaded. 	 */
name|INIT_SSL
argument_list|()
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|OPENSSL
argument_list|)
operator|&&
name|OPENSSL_VERSION_NUMBER
operator|>=
literal|0x0090700fL
if|if
condition|(
operator|!
name|EVP_DigestInit
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_get_digestbynid
argument_list|(
name|type
argument_list|)
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"MAC encrypt: digest init failed"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
else|#
directive|else
name|EVP_DigestInit
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_get_digestbynid
argument_list|(
name|type
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|EVP_DigestUpdate
argument_list|(
operator|&
name|ctx
argument_list|,
name|key
argument_list|,
name|cache_secretsize
argument_list|)
expr_stmt|;
name|EVP_DigestUpdate
argument_list|(
operator|&
name|ctx
argument_list|,
operator|(
name|u_char
operator|*
operator|)
name|pkt
argument_list|,
operator|(
name|u_int
operator|)
name|length
argument_list|)
expr_stmt|;
name|EVP_DigestFinal
argument_list|(
operator|&
name|ctx
argument_list|,
name|digest
argument_list|,
operator|&
name|len
argument_list|)
expr_stmt|;
name|memmove
argument_list|(
operator|(
name|u_char
operator|*
operator|)
name|pkt
operator|+
name|length
operator|+
literal|4
argument_list|,
name|digest
argument_list|,
name|len
argument_list|)
expr_stmt|;
return|return
operator|(
name|len
operator|+
literal|4
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * MD5authdecrypt - verify MD5 message authenticator  *  * Returns one if digest valid, zero if invalid.  */
end_comment

begin_function
name|int
name|MD5authdecrypt
parameter_list|(
name|int
name|type
parameter_list|,
comment|/* hash algorithm */
name|u_char
modifier|*
name|key
parameter_list|,
comment|/* key pointer */
name|u_int32
modifier|*
name|pkt
parameter_list|,
comment|/* packet pointer */
name|int
name|length
parameter_list|,
comment|/* packet length */
name|int
name|size
comment|/* MAC size */
parameter_list|)
block|{
name|u_char
name|digest
index|[
name|EVP_MAX_MD_SIZE
index|]
decl_stmt|;
name|u_int
name|len
decl_stmt|;
name|EVP_MD_CTX
name|ctx
decl_stmt|;
comment|/* 	 * Compute digest of key concatenated with packet. Note: the 	 * key type and digest type have been verified when the key 	 * was created. 	 */
name|INIT_SSL
argument_list|()
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|OPENSSL
argument_list|)
operator|&&
name|OPENSSL_VERSION_NUMBER
operator|>=
literal|0x0090700fL
if|if
condition|(
operator|!
name|EVP_DigestInit
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_get_digestbynid
argument_list|(
name|type
argument_list|)
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"MAC decrypt: digest init failed"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
else|#
directive|else
name|EVP_DigestInit
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_get_digestbynid
argument_list|(
name|type
argument_list|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|EVP_DigestUpdate
argument_list|(
operator|&
name|ctx
argument_list|,
name|key
argument_list|,
name|cache_secretsize
argument_list|)
expr_stmt|;
name|EVP_DigestUpdate
argument_list|(
operator|&
name|ctx
argument_list|,
operator|(
name|u_char
operator|*
operator|)
name|pkt
argument_list|,
operator|(
name|u_int
operator|)
name|length
argument_list|)
expr_stmt|;
name|EVP_DigestFinal
argument_list|(
operator|&
name|ctx
argument_list|,
name|digest
argument_list|,
operator|&
name|len
argument_list|)
expr_stmt|;
if|if
condition|(
operator|(
name|u_int
operator|)
name|size
operator|!=
name|len
operator|+
literal|4
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"MAC decrypt: MAC length error"
argument_list|)
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
return|return
operator|!
name|memcmp
argument_list|(
name|digest
argument_list|,
operator|(
name|char
operator|*
operator|)
name|pkt
operator|+
name|length
operator|+
literal|4
argument_list|,
name|len
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  * Calculate the reference id from the address. If it is an IPv4  * address, use it as is. If it is an IPv6 address, do a md5 on  * it and use the bottom 4 bytes.  * The result is in network byte order.  */
end_comment

begin_function
name|u_int32
name|addr2refid
parameter_list|(
name|sockaddr_u
modifier|*
name|addr
parameter_list|)
block|{
name|u_char
name|digest
index|[
literal|20
index|]
decl_stmt|;
name|u_int32
name|addr_refid
decl_stmt|;
name|EVP_MD_CTX
name|ctx
decl_stmt|;
name|u_int
name|len
decl_stmt|;
if|if
condition|(
name|IS_IPV4
argument_list|(
name|addr
argument_list|)
condition|)
return|return
operator|(
name|NSRCADR
argument_list|(
name|addr
argument_list|)
operator|)
return|;
name|INIT_SSL
argument_list|()
expr_stmt|;
if|#
directive|if
name|defined
argument_list|(
name|OPENSSL
argument_list|)
operator|&&
name|OPENSSL_VERSION_NUMBER
operator|>=
literal|0x0090700fL
name|EVP_MD_CTX_init
argument_list|(
operator|&
name|ctx
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|EVP_MD_CTX_FLAG_NON_FIPS_ALLOW
comment|/* MD5 is not used as a crypto hash here. */
name|EVP_MD_CTX_set_flags
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_MD_CTX_FLAG_NON_FIPS_ALLOW
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
operator|!
name|EVP_DigestInit_ex
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_md5
argument_list|()
argument_list|,
name|NULL
argument_list|)
condition|)
block|{
name|msyslog
argument_list|(
name|LOG_ERR
argument_list|,
literal|"MD5 init failed"
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
else|#
directive|else
name|EVP_DigestInit
argument_list|(
operator|&
name|ctx
argument_list|,
name|EVP_md5
argument_list|()
argument_list|)
expr_stmt|;
endif|#
directive|endif
name|EVP_DigestUpdate
argument_list|(
operator|&
name|ctx
argument_list|,
operator|(
name|u_char
operator|*
operator|)
name|PSOCK_ADDR6
argument_list|(
name|addr
argument_list|)
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|in6_addr
argument_list|)
argument_list|)
expr_stmt|;
name|EVP_DigestFinal
argument_list|(
operator|&
name|ctx
argument_list|,
name|digest
argument_list|,
operator|&
name|len
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
operator|&
name|addr_refid
argument_list|,
name|digest
argument_list|,
sizeof|sizeof
argument_list|(
name|addr_refid
argument_list|)
argument_list|)
expr_stmt|;
return|return
operator|(
name|addr_refid
operator|)
return|;
block|}
end_function

end_unit

