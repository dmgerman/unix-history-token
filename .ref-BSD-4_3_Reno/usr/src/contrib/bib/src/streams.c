begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
name|char
name|sccsid
index|[]
init|=
literal|"@(#)streams.c	2.4	3/5/87"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
endif|not lint
end_endif

begin_empty
empty|#
end_empty

begin_include
include|#
directive|include
file|"stdio.h"
end_include

begin_include
include|#
directive|include
file|"streams.h"
end_include

begin_include
include|#
directive|include
file|"ctype.h"
end_include

begin_comment
comment|/*  getword(stream,p,ignore):         read next sequence of nonspaces on current line into *p.     null if no more words on current line.     %x (x in ignore) terminates line and any following non-blank lines that        don't begin with '%'     all words of the form %a are returned as null.     *p is a null terminated string (char p[maxstr]). */
end_comment

begin_macro
name|getword
argument_list|(
argument|stream
argument_list|,
argument|p
argument_list|,
argument|ignore
argument_list|)
end_macro

begin_decl_stmt
name|FILE
modifier|*
name|stream
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|p
decl_stmt|,
modifier|*
name|ignore
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|c
decl_stmt|;
name|char
modifier|*
name|oldp
decl_stmt|,
modifier|*
name|stop
decl_stmt|;
name|long
name|save
decl_stmt|;
name|oldp
operator|=
name|p
expr_stmt|;
name|stop
operator|=
name|p
operator|+
name|maxstr
operator|-
literal|1
expr_stmt|;
do|do
block|{
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|isspace
argument_list|(
name|c
argument_list|)
operator|&&
name|c
operator|!=
literal|'\n'
condition|)
do|;
while|while
condition|(
operator|!
name|isspace
argument_list|(
name|c
argument_list|)
condition|)
block|{
operator|*
name|p
operator|=
name|c
expr_stmt|;
if|if
condition|(
name|p
operator|<
name|stop
condition|)
name|p
operator|++
expr_stmt|;
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
operator|*
name|p
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|oldp
index|[
literal|0
index|]
operator|==
literal|'%'
condition|)
block|{
name|oldp
index|[
literal|0
index|]
operator|=
name|NULL
expr_stmt|;
if|if
condition|(
name|index
argument_list|(
name|ignore
argument_list|,
name|oldp
index|[
literal|1
index|]
argument_list|)
operator|!=
name|NULL
condition|)
block|{
do|do
block|{
while|while
condition|(
name|c
operator|!=
literal|'\n'
condition|)
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
name|save
operator|=
name|ftell
argument_list|(
name|stream
argument_list|)
expr_stmt|;
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
do|while
condition|(
name|c
operator|!=
name|EOF
operator|&&
operator|!
name|isspace
argument_list|(
name|c
argument_list|)
operator|&&
name|c
operator|!=
literal|'%'
condition|)
do|;
name|pos
argument_list|(
name|save
argument_list|)
expr_stmt|;
block|}
block|}
block|}
end_block

begin_comment
comment|/*  recsize(stream,start):     returns length of record beginning at start     (record ends at blank line or eof)     assumes and retains stream positioned at start */
end_comment

begin_function
name|long
name|int
name|recsize
parameter_list|(
name|stream
parameter_list|,
name|start
parameter_list|)
name|FILE
modifier|*
name|stream
decl_stmt|;
name|long
name|int
name|start
decl_stmt|;
block|{
name|char
name|c
decl_stmt|;
comment|/*  length = # of chars from start to beginning */
name|long
name|int
name|length
decl_stmt|;
comment|/*  of current line.  c in current line.        */
name|int
name|nonspaces
decl_stmt|;
comment|/*  nonspaces = # of nonspaces in current line. */
name|nonspaces
operator|=
literal|0
expr_stmt|;
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
name|length
operator|=
literal|0L
expr_stmt|;
while|while
condition|(
operator|(
name|c
operator|!=
literal|'\n'
operator|||
name|nonspaces
operator|!=
literal|0
operator|)
operator|&&
name|c
operator|!=
name|EOF
condition|)
block|{
if|if
condition|(
name|c
operator|==
literal|'\n'
condition|)
block|{
name|length
operator|=
name|ftell
argument_list|(
name|stream
argument_list|)
operator|-
name|start
expr_stmt|;
name|nonspaces
operator|=
literal|0
expr_stmt|;
block|}
elseif|else
if|if
condition|(
operator|!
name|isspace
argument_list|(
name|c
argument_list|)
condition|)
name|nonspaces
operator|++
expr_stmt|;
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
name|pos
argument_list|(
name|start
argument_list|)
expr_stmt|;
return|return
operator|(
name|length
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  nextrecord(stream,x): seeks in stream for first non-blank line         at or after char x in stream. seeks to eof if x is past last record.         x is the index of a character in the file (not eof).     returns position in stream.  (returns EOF, if seeks to EOF) */
end_comment

begin_function
name|long
name|int
name|nextrecord
parameter_list|(
name|stream
parameter_list|,
name|x
parameter_list|)
name|FILE
modifier|*
name|stream
decl_stmt|;
name|long
name|int
name|x
decl_stmt|;
block|{
name|long
name|int
name|start
decl_stmt|;
comment|/*  position of the beginning of the line  */
name|char
name|c
decl_stmt|;
comment|/*      containing c                       */
name|pos
argument_list|(
name|x
argument_list|)
expr_stmt|;
name|start
operator|=
name|x
expr_stmt|;
comment|/*  find start of first non-blank record        */
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|c
operator|==
literal|'\n'
condition|)
block|{
name|start
operator|=
name|ftell
argument_list|(
name|stream
argument_list|)
expr_stmt|;
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|c
operator|==
literal|'#'
condition|)
while|while
condition|(
name|c
operator|!=
literal|'\n'
condition|)
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
elseif|else
if|if
condition|(
operator|!
name|isspace
argument_list|(
name|c
argument_list|)
condition|)
break|break;
else|else
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|feof
argument_list|(
name|stream
argument_list|)
condition|)
block|{
name|pos
argument_list|(
name|start
argument_list|)
expr_stmt|;
name|start
operator|=
name|EOF
expr_stmt|;
block|}
else|else
name|pos
argument_list|(
name|start
argument_list|)
expr_stmt|;
return|return
operator|(
name|start
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  nextline(stream,x): seeks in stream after first newline at or after         char x in stream. seeks to eof if x is in last line.         x is the index of a character in the file (not eof).     returns position in stream */
end_comment

begin_function
name|long
name|int
name|nextline
parameter_list|(
name|stream
parameter_list|,
name|x
parameter_list|)
name|FILE
modifier|*
name|stream
decl_stmt|;
name|long
name|int
name|x
decl_stmt|;
block|{
name|pos
argument_list|(
name|x
argument_list|)
expr_stmt|;
while|while
condition|(
name|getc
argument_list|(
name|stream
argument_list|)
operator|!=
literal|'\n'
condition|)
empty_stmt|;
return|return
operator|(
name|ftell
argument_list|(
name|stream
argument_list|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  printline(stream): copies stream up to a newline */
end_comment

begin_macro
name|printline
argument_list|(
argument|stream
argument_list|)
end_macro

begin_decl_stmt
name|FILE
modifier|*
name|stream
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|c
decl_stmt|;
while|while
condition|(
operator|(
name|c
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
operator|)
operator|!=
literal|'\n'
operator|&&
name|c
operator|!=
name|EOF
condition|)
name|putchar
argument_list|(
name|c
argument_list|)
expr_stmt|;
name|putchar
argument_list|(
literal|'\n'
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  getline(stream,p):  store in *p next chars in stream up to \n         advance stream past \n.     limit of  maxstr-1 chars may be stored at p. */
end_comment

begin_macro
name|getline
argument_list|(
argument|stream
argument_list|,
argument|p
argument_list|)
end_macro

begin_decl_stmt
name|FILE
modifier|*
name|stream
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|char
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
modifier|*
name|stop
decl_stmt|;
name|stop
operator|=
name|p
operator|+
name|maxstr
operator|-
literal|1
expr_stmt|;
while|while
condition|(
operator|(
operator|*
name|p
operator|=
name|getc
argument_list|(
name|stream
argument_list|)
operator|)
operator|!=
literal|'\n'
operator|&&
operator|*
name|p
operator|!=
name|EOF
condition|)
if|if
condition|(
name|p
operator|<
name|stop
condition|)
name|p
operator|++
expr_stmt|;
operator|*
name|p
operator|=
name|NULL
expr_stmt|;
block|}
end_block

begin_comment
comment|/* replace string old at the head of subj by new */
end_comment

begin_macro
name|strreplace
argument_list|(
argument|subj
argument_list|,
argument|old
argument_list|,
argument|new
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|subj
decl_stmt|,
modifier|*
name|old
decl_stmt|,
modifier|*
name|new
decl_stmt|;
end_decl_stmt

begin_block
block|{
name|char
name|buf
index|[
literal|128
index|]
decl_stmt|;
name|int
name|lg
decl_stmt|;
name|strcpy
argument_list|(
name|buf
argument_list|,
operator|&
name|subj
index|[
name|strlen
argument_list|(
name|old
argument_list|)
index|]
argument_list|)
expr_stmt|;
name|strcpy
argument_list|(
name|subj
argument_list|,
name|new
argument_list|)
expr_stmt|;
name|strcat
argument_list|(
name|subj
argument_list|,
name|buf
argument_list|)
expr_stmt|;
block|}
end_block

end_unit

