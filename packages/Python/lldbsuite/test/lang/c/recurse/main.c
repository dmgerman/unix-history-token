begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdint.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_function
name|uint32_t
name|recurse_crash
parameter_list|(
name|uint32_t
name|depth
parameter_list|)
block|{
if|if
condition|(
name|depth
operator|>
literal|0
condition|)
return|return
name|recurse_crash
argument_list|(
name|depth
operator|-
literal|1
argument_list|)
return|;
return|return
literal|0
return|;
block|}
end_function

begin_function
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
specifier|const
modifier|*
name|argv
index|[]
parameter_list|)
block|{
comment|// If we have more than one argument, then it should a depth to recurse to.
comment|// If we have just the program name as an argument, use UINT32_MAX so we
comment|// eventually crash the program by overflowing the stack
name|uint32_t
name|depth
init|=
name|UINT32_MAX
decl_stmt|;
if|if
condition|(
name|argc
operator|>
literal|1
condition|)
block|{
name|char
modifier|*
name|end
init|=
name|NULL
decl_stmt|;
name|depth
operator|=
name|strtoul
argument_list|(
name|argv
index|[
literal|1
index|]
argument_list|,
operator|&
name|end
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|end
operator|==
name|NULL
operator|||
operator|*
name|end
operator|!=
literal|'\0'
condition|)
name|depth
operator|=
name|UINT32_MAX
expr_stmt|;
block|}
name|recurse_crash
argument_list|(
name|depth
argument_list|)
expr_stmt|;
return|return
literal|0
return|;
block|}
end_function

end_unit

