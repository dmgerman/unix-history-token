begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1982, 1986, 1993  *	The Regents of the University of California.  All rights reserved.  *  * This code is derived from software contributed to Berkeley by  * the Systems Programming Group of the University of Utah Computer  * Science Department, and William Jolitz.  *  * %sccs.include.redist.c%  *  *	@(#)vm_machdep.c	8.3 (Berkeley) %G%  *	Utah $Hdr: vm_machdep.c 1.16.1.1 89/06/23$  */
end_comment

begin_include
include|#
directive|include
file|<sys/param.h>
end_include

begin_include
include|#
directive|include
file|<sys/systm.h>
end_include

begin_include
include|#
directive|include
file|<sys/proc.h>
end_include

begin_include
include|#
directive|include
file|<sys/malloc.h>
end_include

begin_include
include|#
directive|include
file|<sys/buf.h>
end_include

begin_include
include|#
directive|include
file|<sys/vnode.h>
end_include

begin_include
include|#
directive|include
file|<sys/user.h>
end_include

begin_include
include|#
directive|include
file|<machine/cpu.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm.h>
end_include

begin_include
include|#
directive|include
file|<vm/vm_kern.h>
end_include

begin_comment
comment|/*  * Finish a fork operation, with process p2 nearly set up.  * Copy and update the kernel stack and pcb, making the child  * ready to run, and marking it so that it can return differently  * than the parent.  Returns 1 in the child process, 0 in the parent.  * We currently double-map the user area so that the stack is at the same  * address in each process; in the future we will probably relocate  * the frame pointers on the stack after copying.  */
end_comment

begin_expr_stmt
name|cpu_fork
argument_list|(
name|p1
argument_list|,
name|p2
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p1
operator|,
operator|*
name|p2
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|struct
name|user
modifier|*
name|up
init|=
name|p2
operator|->
name|p_addr
decl_stmt|;
name|int
name|foo
decl_stmt|,
name|offset
decl_stmt|,
name|addr
decl_stmt|,
name|i
decl_stmt|;
specifier|extern
name|char
name|kstack
index|[]
decl_stmt|;
specifier|extern
name|int
name|mvesp
parameter_list|()
function_decl|;
comment|/* 	 * Copy pcb and stack from proc p1 to p2.  	 * We do this as cheaply as possible, copying only the active 	 * part of the stack.  The stack and pcb need to agree; 	 * this is tricky, as the final pcb is constructed by savectx, 	 * but its frame isn't yet on the stack when the stack is copied. 	 * mi_switch compensates for this when the child eventually runs. 	 * This should be done differently, with a single call 	 * that copies and updates the pcb+stack, 	 * replacing the bcopy and savectx. 	 */
name|p2
operator|->
name|p_addr
operator|->
name|u_pcb
operator|=
name|p1
operator|->
name|p_addr
operator|->
name|u_pcb
expr_stmt|;
name|offset
operator|=
name|mvesp
argument_list|()
operator|-
operator|(
name|int
operator|)
name|kstack
expr_stmt|;
name|bcopy
argument_list|(
operator|(
name|caddr_t
operator|)
name|kstack
operator|+
name|offset
argument_list|,
operator|(
name|caddr_t
operator|)
name|p2
operator|->
name|p_addr
operator|+
name|offset
argument_list|,
operator|(
name|unsigned
operator|)
name|ctob
argument_list|(
name|UPAGES
argument_list|)
operator|-
name|offset
argument_list|)
expr_stmt|;
name|p2
operator|->
name|p_md
operator|.
name|md_regs
operator|=
name|p1
operator|->
name|p_md
operator|.
name|md_regs
expr_stmt|;
comment|/* 	 * Wire top of address space of child to it's u. 	 * First, fault in a page of pte's to map it. 	 */
name|addr
operator|=
name|trunc_page
argument_list|(
operator|(
name|u_int
operator|)
name|vtopte
argument_list|(
name|kstack
argument_list|)
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|vm_map_pageable
argument_list|(
operator|&
name|p2
operator|->
name|p_vmspace
operator|->
name|vm_map
argument_list|,
name|addr
argument_list|,
name|addr
operator|+
name|NBPG
argument_list|,
name|FALSE
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|UPAGES
condition|;
name|i
operator|++
control|)
name|pmap_enter
argument_list|(
operator|&
name|p2
operator|->
name|p_vmspace
operator|->
name|vm_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|kstack
operator|+
name|i
operator|*
name|NBPG
argument_list|,
name|pmap_extract
argument_list|(
name|kernel_pmap
argument_list|,
operator|(
operator|(
name|int
operator|)
name|p2
operator|->
name|p_addr
operator|)
operator|+
name|i
operator|*
name|NBPG
argument_list|)
argument_list|,
name|VM_PROT_READ
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|pmap_activate
argument_list|(
operator|&
name|p2
operator|->
name|p_vmspace
operator|->
name|vm_pmap
argument_list|,
operator|&
name|up
operator|->
name|u_pcb
argument_list|)
expr_stmt|;
comment|/* 	 *  	 * Arrange for a non-local goto when the new process 	 * is started, to resume here, returning nonzero from setjmp. 	 */
if|if
condition|(
name|savectx
argument_list|(
name|up
argument_list|,
literal|1
argument_list|)
condition|)
block|{
comment|/* 		 * Return 1 in child. 		 */
return|return
operator|(
literal|1
operator|)
return|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_include
include|#
directive|include
file|"npx.h"
end_include

begin_if
if|#
directive|if
name|NNPX
operator|>
literal|0
end_if

begin_decl_stmt
specifier|extern
name|struct
name|proc
modifier|*
name|npxproc
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_ifdef
ifdef|#
directive|ifdef
name|notyet
end_ifdef

begin_comment
comment|/*  * cpu_exit is called as the last action during exit.  *  * We change to an inactive address space and a "safe" stack,  * passing thru an argument to the new stack. Now, safely isolated  * from the resources we're shedding, we release the address space  * and any remaining machine-dependent resources, including the  * memory for the user structure and kernel stack.  *  * Next, we assign a dummy context to be written over by mi_switch,  * calling it to send this process off to oblivion.  * [The nullpcb allows us to minimize cost in mi_switch() by not having  * a special case].  */
end_comment

begin_function_decl
name|struct
name|proc
modifier|*
name|switch_to_inactive
parameter_list|()
function_decl|;
end_function_decl

begin_expr_stmt
name|cpu_exit
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|static
name|struct
name|pcb
name|nullpcb
decl_stmt|;
comment|/* pcb to overwrite on last switch */
if|#
directive|if
name|NNPX
operator|>
literal|0
comment|/* free cporcessor (if we have it) */
if|if
condition|(
name|p
operator|==
name|npxproc
condition|)
name|npxproc
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
comment|/* move to inactive space and stack, passing arg accross */
name|p
operator|=
name|switch_to_inactive
argument_list|(
name|p
argument_list|)
expr_stmt|;
comment|/* drop per-process resources */
name|vmspace_free
argument_list|(
name|p
operator|->
name|p_vmspace
argument_list|)
expr_stmt|;
name|kmem_free
argument_list|(
name|kernel_map
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|p
operator|->
name|p_addr
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|)
expr_stmt|;
name|p
operator|->
name|p_addr
operator|=
operator|(
expr|struct
name|user
operator|*
operator|)
operator|&
name|nullpcb
expr_stmt|;
name|mi_switch
argument_list|()
expr_stmt|;
comment|/* NOTREACHED */
block|}
end_block

begin_else
else|#
directive|else
end_else

begin_expr_stmt
name|cpu_exit
argument_list|(
name|p
argument_list|)
specifier|register
expr|struct
name|proc
operator|*
name|p
expr_stmt|;
end_expr_stmt

begin_block
block|{
comment|/* free coprocessor (if we have it) */
if|#
directive|if
name|NNPX
operator|>
literal|0
if|if
condition|(
name|p
operator|==
name|npxproc
condition|)
name|npxproc
operator|=
literal|0
expr_stmt|;
endif|#
directive|endif
name|curproc
operator|=
name|p
expr_stmt|;
name|mi_switch
argument_list|()
expr_stmt|;
block|}
end_block

begin_macro
name|cpu_wait
argument_list|(
argument|p
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* drop per-process resources */
name|vmspace_free
argument_list|(
name|p
operator|->
name|p_vmspace
argument_list|)
expr_stmt|;
name|kmem_free
argument_list|(
name|kernel_map
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|p
operator|->
name|p_addr
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|)
expr_stmt|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Dump the machine specific header information at the start of a core dump.  */
end_comment

begin_macro
name|cpu_coredump
argument_list|(
argument|p
argument_list|,
argument|vp
argument_list|,
argument|cred
argument_list|)
end_macro

begin_decl_stmt
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|vnode
modifier|*
name|vp
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|struct
name|ucred
modifier|*
name|cred
decl_stmt|;
end_decl_stmt

begin_block
block|{
return|return
operator|(
name|vn_rdwr
argument_list|(
name|UIO_WRITE
argument_list|,
name|vp
argument_list|,
operator|(
name|caddr_t
operator|)
name|p
operator|->
name|p_addr
argument_list|,
name|ctob
argument_list|(
name|UPAGES
argument_list|)
argument_list|,
operator|(
name|off_t
operator|)
literal|0
argument_list|,
name|UIO_SYSSPACE
argument_list|,
name|IO_NODELOCKED
operator||
name|IO_UNIT
argument_list|,
name|cred
argument_list|,
operator|(
name|int
operator|*
operator|)
name|NULL
argument_list|,
name|p
argument_list|)
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * Set a red zone in the kernel stack after the u. area.  */
end_comment

begin_macro
name|setredzone
argument_list|(
argument|pte
argument_list|,
argument|vaddr
argument_list|)
end_macro

begin_decl_stmt
name|u_short
modifier|*
name|pte
decl_stmt|;
end_decl_stmt

begin_decl_stmt
name|caddr_t
name|vaddr
decl_stmt|;
end_decl_stmt

begin_block
block|{
comment|/* eventually do this by setting up an expand-down stack segment    for ss0: selector, allowing stack access down to top of u.    this means though that protection violations need to be handled    thru a double fault exception that must do an integral task    switch to a known good context, within which a dump can be    taken. a sensible scheme might be to save the initial context    used by sched (that has physical memory mapped 1:1 at bottom)    and take the dump while still in mapped mode */
block|}
end_block

begin_comment
comment|/*  * Move pages from one kernel virtual address to another.  * Both addresses are assumed to reside in the Sysmap,  * and size must be a multiple of CLSIZE.  */
end_comment

begin_expr_stmt
name|pagemove
argument_list|(
name|from
argument_list|,
name|to
argument_list|,
name|size
argument_list|)
specifier|register
name|caddr_t
name|from
operator|,
name|to
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|size
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pte
modifier|*
name|fpte
decl_stmt|,
modifier|*
name|tpte
decl_stmt|;
if|if
condition|(
name|size
operator|%
name|CLBYTES
condition|)
name|panic
argument_list|(
literal|"pagemove"
argument_list|)
expr_stmt|;
name|fpte
operator|=
name|kvtopte
argument_list|(
name|from
argument_list|)
expr_stmt|;
name|tpte
operator|=
name|kvtopte
argument_list|(
name|to
argument_list|)
expr_stmt|;
while|while
condition|(
name|size
operator|>
literal|0
condition|)
block|{
operator|*
name|tpte
operator|++
operator|=
operator|*
name|fpte
expr_stmt|;
operator|*
operator|(
name|int
operator|*
operator|)
name|fpte
operator|++
operator|=
literal|0
expr_stmt|;
name|from
operator|+=
name|NBPG
expr_stmt|;
name|to
operator|+=
name|NBPG
expr_stmt|;
name|size
operator|-=
name|NBPG
expr_stmt|;
block|}
name|tlbflush
argument_list|()
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * Convert kernel VA to physical address  */
end_comment

begin_expr_stmt
name|kvtop
argument_list|(
name|addr
argument_list|)
specifier|register
name|caddr_t
name|addr
expr_stmt|;
end_expr_stmt

begin_block
block|{
name|vm_offset_t
name|va
decl_stmt|;
name|va
operator|=
name|pmap_extract
argument_list|(
name|kernel_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|va
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"kvtop: zero page frame"
argument_list|)
expr_stmt|;
return|return
operator|(
operator|(
name|int
operator|)
name|va
operator|)
return|;
block|}
end_block

begin_ifdef
ifdef|#
directive|ifdef
name|notdef
end_ifdef

begin_comment
comment|/*  * The probe[rw] routines should probably be redone in assembler  * for efficiency.  */
end_comment

begin_expr_stmt
name|prober
argument_list|(
name|addr
argument_list|)
specifier|register
name|u_int
name|addr
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|page
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|addr
operator|>=
name|USRSTACK
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|p
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
name|page
operator|=
name|btop
argument_list|(
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|page
operator|<
name|dptov
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_dsize
argument_list|)
operator|||
name|page
operator|>
name|sptov
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_ssize
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|probew
argument_list|(
name|addr
argument_list|)
specifier|register
name|u_int
name|addr
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|page
decl_stmt|;
specifier|register
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
if|if
condition|(
name|addr
operator|>=
name|USRSTACK
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|p
operator|=
name|u
operator|.
name|u_procp
expr_stmt|;
name|page
operator|=
name|btop
argument_list|(
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|page
operator|<
name|dptov
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_dsize
argument_list|)
operator|||
name|page
operator|>
name|sptov
argument_list|(
name|p
argument_list|,
name|p
operator|->
name|p_ssize
argument_list|)
condition|)
return|return
operator|(
operator|(
operator|*
operator|(
name|int
operator|*
operator|)
name|vtopte
argument_list|(
name|p
argument_list|,
name|page
argument_list|)
operator|&
name|PG_PROT
operator|)
operator|==
name|PG_UW
operator|)
return|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

begin_comment
comment|/*  * NB: assumes a physically contiguous kernel page table  *     (makes life a LOT simpler).  */
end_comment

begin_expr_stmt
name|kernacc
argument_list|(
name|addr
argument_list|,
name|count
argument_list|,
name|rw
argument_list|)
specifier|register
name|u_int
name|addr
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|count
decl_stmt|,
name|rw
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|struct
name|pde
modifier|*
name|pde
decl_stmt|;
specifier|register
name|struct
name|pte
modifier|*
name|pte
decl_stmt|;
specifier|register
name|int
name|ix
decl_stmt|,
name|cnt
decl_stmt|;
specifier|extern
name|long
name|Syssize
decl_stmt|;
if|if
condition|(
name|count
operator|<=
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|pde
operator|=
operator|(
expr|struct
name|pde
operator|*
operator|)
operator|(
operator|(
name|u_int
operator|)
name|u
operator|.
name|u_procp
operator|->
name|p_p0br
operator|+
name|u
operator|.
name|u_procp
operator|->
name|p_szpt
operator|*
name|NBPG
operator|)
expr_stmt|;
name|ix
operator|=
operator|(
name|addr
operator|&
name|PD_MASK
operator|)
operator|>>
name|PD_SHIFT
expr_stmt|;
name|cnt
operator|=
operator|(
operator|(
name|addr
operator|+
name|count
operator|+
operator|(
literal|1
operator|<<
name|PD_SHIFT
operator|)
operator|-
literal|1
operator|)
operator|&
name|PD_MASK
operator|)
operator|>>
name|PD_SHIFT
expr_stmt|;
name|cnt
operator|-=
name|ix
expr_stmt|;
for|for
control|(
name|pde
operator|+=
name|ix
init|;
name|cnt
condition|;
name|cnt
operator|--
operator|,
name|pde
operator|++
control|)
if|if
condition|(
name|pde
operator|->
name|pd_v
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|ix
operator|=
name|btop
argument_list|(
name|addr
operator|-
literal|0xfe000000
argument_list|)
expr_stmt|;
name|cnt
operator|=
name|btop
argument_list|(
name|addr
operator|-
literal|0xfe000000
operator|+
name|count
operator|+
name|NBPG
operator|-
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|cnt
operator|>
operator|(
name|int
operator|)
operator|&
name|Syssize
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|cnt
operator|-=
name|ix
expr_stmt|;
for|for
control|(
name|pte
operator|=
operator|&
name|Sysmap
index|[
name|ix
index|]
init|;
name|cnt
condition|;
name|cnt
operator|--
operator|,
name|pte
operator|++
control|)
if|if
condition|(
name|pte
operator|->
name|pg_v
operator|==
literal|0
comment|/*|| (rw == B_WRITE&& pte->pg_prot == 1)*/
condition|)
return|return
operator|(
literal|0
operator|)
return|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_expr_stmt
name|useracc
argument_list|(
name|addr
argument_list|,
name|count
argument_list|,
name|rw
argument_list|)
specifier|register
name|u_int
name|addr
expr_stmt|;
end_expr_stmt

begin_decl_stmt
name|int
name|count
decl_stmt|,
name|rw
decl_stmt|;
end_decl_stmt

begin_block
block|{
specifier|register
name|int
function_decl|(
modifier|*
name|func
function_decl|)
parameter_list|()
function_decl|;
specifier|register
name|u_int
name|addr2
decl_stmt|;
specifier|extern
name|int
name|prober
argument_list|()
decl_stmt|,
name|probew
argument_list|()
decl_stmt|;
if|if
condition|(
name|count
operator|<=
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|addr2
operator|=
name|addr
expr_stmt|;
name|addr
operator|+=
name|count
expr_stmt|;
name|func
operator|=
operator|(
name|rw
operator|==
name|B_READ
operator|)
condition|?
name|prober
else|:
name|probew
expr_stmt|;
do|do
block|{
if|if
condition|(
call|(
modifier|*
name|func
call|)
argument_list|(
name|addr2
argument_list|)
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|addr2
operator|=
operator|(
name|addr2
operator|+
name|NBPG
operator|)
operator|&
operator|~
name|PGOFSET
expr_stmt|;
block|}
do|while
condition|(
name|addr2
operator|<
name|addr
condition|)
do|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_block

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
specifier|extern
name|vm_map_t
name|phys_map
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * Map an IO request into kernel virtual address space.  Requests fall into  * one of five catagories:  *  *	B_PHYS|B_UAREA:	User u-area swap.  *			Address is relative to start of u-area (p_addr).  *	B_PHYS|B_PAGET:	User page table swap.  *			Address is a kernel VA in usrpt (Usrptmap).  *	B_PHYS|B_DIRTY:	Dirty page push.  *			Address is a VA in proc2's address space.  *	B_PHYS|B_PGIN:	Kernel pagein of user pages.  *			Address is VA in user's address space.  *	B_PHYS:		User "raw" IO request.  *			Address is VA in user's address space.  *  * All requests are (re)mapped into kernel VA space via the useriomap  * (a name with only slightly more meaning than "kernelmap")  */
end_comment

begin_expr_stmt
name|vmapbuf
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|npf
decl_stmt|;
specifier|register
name|caddr_t
name|addr
decl_stmt|;
specifier|register
name|long
name|flags
init|=
name|bp
operator|->
name|b_flags
decl_stmt|;
name|struct
name|proc
modifier|*
name|p
decl_stmt|;
name|int
name|off
decl_stmt|;
name|vm_offset_t
name|kva
decl_stmt|;
specifier|register
name|vm_offset_t
name|pa
decl_stmt|;
if|if
condition|(
operator|(
name|flags
operator|&
name|B_PHYS
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vmapbuf"
argument_list|)
expr_stmt|;
name|addr
operator|=
name|bp
operator|->
name|b_saveaddr
operator|=
name|bp
operator|->
name|b_un
operator|.
name|b_addr
expr_stmt|;
name|off
operator|=
operator|(
name|int
operator|)
name|addr
operator|&
name|PGOFSET
expr_stmt|;
name|p
operator|=
name|bp
operator|->
name|b_proc
expr_stmt|;
name|npf
operator|=
name|btoc
argument_list|(
name|round_page
argument_list|(
name|bp
operator|->
name|b_bcount
operator|+
name|off
argument_list|)
argument_list|)
expr_stmt|;
name|kva
operator|=
name|kmem_alloc_wait
argument_list|(
name|phys_map
argument_list|,
name|ctob
argument_list|(
name|npf
argument_list|)
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_un
operator|.
name|b_addr
operator|=
call|(
name|caddr_t
call|)
argument_list|(
name|kva
operator|+
name|off
argument_list|)
expr_stmt|;
while|while
condition|(
name|npf
operator|--
condition|)
block|{
name|pa
operator|=
name|pmap_extract
argument_list|(
operator|&
name|p
operator|->
name|p_vmspace
operator|->
name|vm_pmap
argument_list|,
operator|(
name|vm_offset_t
operator|)
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|pa
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vmapbuf: null page frame"
argument_list|)
expr_stmt|;
name|pmap_enter
argument_list|(
name|vm_map_pmap
argument_list|(
name|phys_map
argument_list|)
argument_list|,
name|kva
argument_list|,
name|trunc_page
argument_list|(
name|pa
argument_list|)
argument_list|,
name|VM_PROT_READ
operator||
name|VM_PROT_WRITE
argument_list|,
name|TRUE
argument_list|)
expr_stmt|;
name|addr
operator|+=
name|PAGE_SIZE
expr_stmt|;
name|kva
operator|+=
name|PAGE_SIZE
expr_stmt|;
block|}
block|}
end_block

begin_comment
comment|/*  * Free the io map PTEs associated with this IO operation.  * We also invalidate the TLB entries and restore the original b_addr.  */
end_comment

begin_expr_stmt
name|vunmapbuf
argument_list|(
name|bp
argument_list|)
specifier|register
expr|struct
name|buf
operator|*
name|bp
expr_stmt|;
end_expr_stmt

begin_block
block|{
specifier|register
name|int
name|npf
decl_stmt|;
specifier|register
name|caddr_t
name|addr
init|=
name|bp
operator|->
name|b_un
operator|.
name|b_addr
decl_stmt|;
name|vm_offset_t
name|kva
decl_stmt|;
if|if
condition|(
operator|(
name|bp
operator|->
name|b_flags
operator|&
name|B_PHYS
operator|)
operator|==
literal|0
condition|)
name|panic
argument_list|(
literal|"vunmapbuf"
argument_list|)
expr_stmt|;
name|npf
operator|=
name|btoc
argument_list|(
name|round_page
argument_list|(
name|bp
operator|->
name|b_bcount
operator|+
operator|(
operator|(
name|int
operator|)
name|addr
operator|&
name|PGOFSET
operator|)
argument_list|)
argument_list|)
expr_stmt|;
name|kva
operator|=
call|(
name|vm_offset_t
call|)
argument_list|(
operator|(
name|int
operator|)
name|addr
operator|&
operator|~
name|PGOFSET
argument_list|)
expr_stmt|;
name|kmem_free_wakeup
argument_list|(
name|phys_map
argument_list|,
name|kva
argument_list|,
name|ctob
argument_list|(
name|npf
argument_list|)
argument_list|)
expr_stmt|;
name|bp
operator|->
name|b_un
operator|.
name|b_addr
operator|=
name|bp
operator|->
name|b_saveaddr
expr_stmt|;
name|bp
operator|->
name|b_saveaddr
operator|=
name|NULL
expr_stmt|;
block|}
end_block

end_unit

