begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  $Revision: 1.5 $ ** */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|"configdata.h"
end_include

begin_include
include|#
directive|include
file|"clibrary.h"
end_include

begin_include
include|#
directive|include
file|"libinn.h"
end_include

begin_include
include|#
directive|include
file|"nntp.h"
end_include

begin_comment
comment|/* **  Send a string of one or more lines down a stdio FILE using RFC977 **  conventions.  Return -1 on error. */
end_comment

begin_function
name|int
name|NNTPsendarticle
parameter_list|(
name|p
parameter_list|,
name|F
parameter_list|,
name|Terminate
parameter_list|)
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|FILE
modifier|*
name|F
decl_stmt|;
name|BOOL
name|Terminate
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|next
decl_stmt|;
for|for
control|(
init|;
name|p
operator|&&
operator|*
name|p
condition|;
name|next
index|[
operator|-
literal|1
index|]
operator|=
literal|'\n'
operator|,
name|p
operator|=
name|next
control|)
block|{
comment|/* Get pointer to next line.  Truncate long lines. */
if|if
condition|(
operator|(
name|next
operator|=
name|strchr
argument_list|(
name|p
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|next
operator|++
operator|=
literal|'\0'
expr_stmt|;
comment|/* Write line. */
if|if
condition|(
operator|*
name|p
operator|==
literal|'.'
operator|&&
name|putc
argument_list|(
literal|'.'
argument_list|,
name|F
argument_list|)
operator|==
name|EOF
condition|)
return|return
operator|-
literal|1
return|;
if|if
condition|(
name|fprintf
argument_list|(
name|F
argument_list|,
literal|"%s\r\n"
argument_list|,
name|p
argument_list|)
operator|==
name|EOF
condition|)
return|return
operator|-
literal|1
return|;
comment|/* Done? */
if|if
condition|(
name|next
operator|==
name|NULL
condition|)
break|break;
block|}
if|if
condition|(
name|Terminate
operator|&&
name|fprintf
argument_list|(
name|F
argument_list|,
literal|".\r\n"
argument_list|)
operator|==
name|EOF
condition|)
return|return
operator|-
literal|1
return|;
return|return
name|fflush
argument_list|(
name|F
argument_list|)
operator|==
name|EOF
operator|||
name|ferror
argument_list|(
name|F
argument_list|)
condition|?
operator|-
literal|1
else|:
literal|0
return|;
block|}
end_function

end_unit

