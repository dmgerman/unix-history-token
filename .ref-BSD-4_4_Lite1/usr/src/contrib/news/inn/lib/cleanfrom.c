begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  $Revision: 1.4 $ ** */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|"configdata.h"
end_include

begin_include
include|#
directive|include
file|"clibrary.h"
end_include

begin_include
include|#
directive|include
file|"macros.h"
end_include

begin_define
define|#
directive|define
name|LPAREN
value|'('
end_define

begin_define
define|#
directive|define
name|RPAREN
value|')'
end_define

begin_comment
comment|/* **  Clean up a from line, making the following transformations: **	address			address **	address (stuff)		address **	stuff<address>		address */
end_comment

begin_function
name|void
name|HeaderCleanFrom
parameter_list|(
name|from
parameter_list|)
name|char
modifier|*
name|from
decl_stmt|;
block|{
specifier|register
name|char
modifier|*
name|p
decl_stmt|;
specifier|register
name|char
modifier|*
name|end
decl_stmt|;
comment|/* Do the equivalent of sed's "1q" */
if|if
condition|(
operator|(
name|p
operator|=
name|strchr
argument_list|(
name|from
argument_list|,
literal|'\n'
argument_list|)
operator|)
operator|!=
name|NULL
condition|)
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
comment|/* Do pretty much the equivalent of sed's "s/ (.*)//"; doesn't      * work for "(save (delete this)" but that's okay. */
if|if
condition|(
operator|(
name|p
operator|=
name|strchr
argument_list|(
name|from
argument_list|,
name|LPAREN
argument_list|)
operator|)
operator|&&
name|p
operator|>
name|from
operator|&&
operator|*
operator|--
name|p
operator|==
literal|' '
operator|&&
operator|(
name|end
operator|=
name|strrchr
argument_list|(
name|p
argument_list|,
name|RPAREN
argument_list|)
operator|)
condition|)
block|{
while|while
condition|(
operator|*
operator|++
name|end
condition|)
operator|*
name|p
operator|++
operator|=
operator|*
name|end
expr_stmt|;
operator|*
name|p
operator|=
literal|'\0'
expr_stmt|;
block|}
comment|/* Do the equivalent of sed's "s/.*<\(.*\)>/\1/" */
if|if
condition|(
operator|(
name|p
operator|=
name|strrchr
argument_list|(
name|from
argument_list|,
literal|'<'
argument_list|)
operator|)
operator|&&
operator|(
name|end
operator|=
name|strrchr
argument_list|(
name|p
argument_list|,
literal|'>'
argument_list|)
operator|)
condition|)
block|{
while|while
condition|(
operator|++
name|p
operator|<
name|end
condition|)
operator|*
name|from
operator|++
operator|=
operator|*
name|p
expr_stmt|;
operator|*
name|from
operator|=
literal|'\0'
expr_stmt|;
block|}
block|}
end_function

end_unit

