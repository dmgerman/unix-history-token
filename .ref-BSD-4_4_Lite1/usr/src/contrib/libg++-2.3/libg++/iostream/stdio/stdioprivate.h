begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<ioprivate.h>
end_include

begin_define
define|#
directive|define
name|__validfp
parameter_list|(
name|fp
parameter_list|)
define|\
value|(((fp->_flags& _IO_MAGIC_MASK) == _OLD_STDIO_MAGIC&& \     (fp = *(FILE**)&((int*)fp)[1])), \    __checkfp(fp))
end_define

begin_define
define|#
directive|define
name|__checkfp
parameter_list|(
name|fp
parameter_list|)
value|(fp->_flags& _IO_MAGIC_MASK) == _IO_MAGIC
end_define

begin_function
specifier|inline
name|streambuf
modifier|*
name|FILE_to_streambuf
parameter_list|(
name|FILE
modifier|*
name|fp
parameter_list|)
block|{
if|if
condition|(
operator|(
name|fp
operator|->
name|_flags
operator|&
name|_IO_MAGIC_MASK
operator|)
operator|==
name|_OLD_STDIO_MAGIC
condition|)
return|return
operator|*
operator|(
name|streambuf
operator|*
operator|*
operator|)
operator|&
operator|(
operator|(
name|int
operator|*
operator|)
name|fp
operator|)
index|[
literal|1
index|]
return|;
if|if
condition|(
operator|(
name|fp
operator|->
name|_flags
operator|&
name|_IO_MAGIC_MASK
operator|)
operator|==
name|_IO_MAGIC
condition|)
return|return
operator|(
name|streambuf
operator|*
operator|)
name|fp
return|;
return|return
name|NULL
return|;
block|}
end_function

end_unit

