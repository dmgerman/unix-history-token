begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (c) 1994 David I. Bell  * Permission is granted to use, distribute, or modify this source,  * provided that this copyright notice remains intact.  *  * Description:  *	This is a very fast storage allocator. It allocates blocks of a small  *	number of different sizes, and keeps free lists of each size.  Blocks  *	that don't exactly fit are passed up to the next larger size.  In this  *	implementation, the available sizes are 2^n-4 (or 2^n-12) bytes long.  *	This is designed for use in a program that uses vast quantities of  *	memory, but bombs when it runs out.  *  * Abnormal Conditions  *	This is a public domain storage allocator.  *  * Modifications:  *	Date		Programmer		Description of modification  *	27-FEB-90	Landon Curt Noll	most systems can ignore alloc.c  *	2-OCT-89	David I. Bell		Add free list. Sbrk now optional  *	30-JUN-87	Peter Miller		Made it work on Slimos.  *	21-FEB-82	Chris Kingsley		Initial Coding  *			kingsley@cit-20		Caltech  */
end_comment

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|"alloc.h"
end_include

begin_include
include|#
directive|include
file|"have_stdlib.h"
end_include

begin_ifdef
ifdef|#
directive|ifdef
name|HAVE_STDLIB_H
end_ifdef

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_if
if|#
directive|if
literal|0
end_if

begin_define
define|#
directive|define
name|DEBUG
value|1
end_define

begin_comment
comment|/* defined if debugging code enabled */
end_comment

begin_define
define|#
directive|define
name|MSTATS
value|1
end_define

begin_comment
comment|/* defined if memory statistics kept */
end_comment

begin_endif
endif|#
directive|endif
end_endif

begin_define
define|#
directive|define
name|NO_SBRK
value|1
end_define

begin_comment
comment|/* defined if cannot use sbrk */
end_comment

begin_if
if|#
directive|if
name|defined
argument_list|(
name|CALC_MALLOC
argument_list|)
end_if

begin_comment
comment|/*  * Make these functions really accessible here.  */
end_comment

begin_undef
undef|#
directive|undef
name|malloc
end_undef

begin_undef
undef|#
directive|undef
name|realloc
end_undef

begin_undef
undef|#
directive|undef
name|free
end_undef

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_define
define|#
directive|define
name|assert
parameter_list|(
name|x
parameter_list|,
name|v
parameter_list|)
value|if ((x)==0) assertfailed(v)
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|assert
parameter_list|(
name|x
parameter_list|,
name|v
parameter_list|)
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_typedef
typedef|typedef
name|unsigned
name|char
name|u_char
typedef|;
end_typedef

begin_typedef
typedef|typedef
name|unsigned
name|short
name|u_short
typedef|;
end_typedef

begin_typedef
typedef|typedef
name|unsigned
name|int
name|u_int
typedef|;
end_typedef

begin_typedef
typedef|typedef
name|char
modifier|*
name|caddr_t
typedef|;
end_typedef

begin_ifdef
ifdef|#
directive|ifdef
name|NO_SBRK
end_ifdef

begin_function_decl
specifier|extern
name|char
modifier|*
name|malloc
parameter_list|()
function_decl|;
end_function_decl

begin_function_decl
specifier|extern
name|char
modifier|*
name|realloc
parameter_list|()
function_decl|;
end_function_decl

begin_else
else|#
directive|else
end_else

begin_function_decl
specifier|extern
name|char
modifier|*
name|sbrk
parameter_list|()
function_decl|;
end_function_decl

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * The overhead on a block is at least 4 bytes.  When free, this space  * contains a pointer to the next free block, and the bottom two bits must  * be zero.  When in use, the first byte is set to MAGIC, and the second  * byte is the size index.  The remaining bytes are for alignment.  * If range checking (RCHECK) is enabled and the size of the block fits  * in two bytes, then the top two bytes hold the size of the requested block  * plus the range checking words, and the header word MINUS ONE.  */
end_comment

begin_union
union|union
name|overhead
block|{
name|union
name|overhead
modifier|*
name|ov_next
decl_stmt|;
comment|/* when free */
struct|struct
block|{
name|u_char
name|ovu_magic
decl_stmt|;
comment|/* magic number */
name|u_char
name|ovu_index
decl_stmt|;
comment|/* bucket # */
define|#
directive|define
name|ov_magic
value|ovu.ovu_magic
define|#
directive|define
name|ov_index
value|ovu.ovu_index
ifdef|#
directive|ifdef
name|RCHECK
name|u_short
name|ovu_size
decl_stmt|;
comment|/* actual block size */
name|u_int
name|ovu_rmagic
decl_stmt|;
comment|/* range magic number */
define|#
directive|define
name|ov_size
value|ovu.ovu_size
define|#
directive|define
name|ov_rmagic
value|ovu.ovu_rmagic
endif|#
directive|endif
block|}
name|ovu
struct|;
block|}
union|;
end_union

begin_define
define|#
directive|define
name|QUANTUM_NBITS
value|4
end_define

begin_define
define|#
directive|define
name|QUANTUM
value|(1<<QUANTUM_NBITS)
end_define

begin_define
define|#
directive|define
name|MAGIC
value|0xff
end_define

begin_comment
comment|/* magic # on accounting info */
end_comment

begin_define
define|#
directive|define
name|RMAGIC
value|0x55555555
end_define

begin_comment
comment|/* magic # on range info */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|RCHECK
end_ifdef

begin_define
define|#
directive|define
name|RSLOP
value|sizeof(u_int)
end_define

begin_else
else|#
directive|else
end_else

begin_define
define|#
directive|define
name|RSLOP
value|0
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * nextf[i] is the pointer to the next free block of size 2^(i+3).  The  * smallest allocatable block is 8 bytes.  The overhead information  * precedes the data area returned to the user.  */
end_comment

begin_define
define|#
directive|define
name|NBUCKETS
value|32
end_define

begin_comment
comment|/* we can't run out on a 32 bit machine! */
end_comment

begin_decl_stmt
specifier|static
name|union
name|overhead
modifier|*
name|nextf
index|[
name|NBUCKETS
index|]
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|union
name|overhead
modifier|*
name|watchloc
init|=
literal|0
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* location to be watched */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|MSTATS
end_ifdef

begin_comment
comment|/*  * nmalloc[i] is the difference between the number of mallocs and frees  * for a given block size.  */
end_comment

begin_decl_stmt
specifier|static
name|u_int
name|nmalloc
index|[
name|NBUCKETS
index|]
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*  * Watch some allocated memory to see if it gets blasted.  */
end_comment

begin_macro
name|allocwatch
argument_list|(
argument|cp
argument_list|)
end_macro

begin_decl_stmt
name|char
modifier|*
name|cp
decl_stmt|;
end_decl_stmt

begin_block
block|{
if|if
condition|(
name|cp
operator|==
name|NULL
condition|)
block|{
name|watchloc
operator|=
name|NULL
expr_stmt|;
return|return;
block|}
name|watchloc
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
name|cp
operator|-
literal|1
expr_stmt|;
name|assert
argument_list|(
name|watchloc
operator|->
name|ov_magic
operator|==
name|MAGIC
argument_list|,
literal|10
argument_list|)
expr_stmt|;
block|}
end_block

begin_macro
name|alloccheck
argument_list|()
end_macro

begin_block
block|{
name|assert
argument_list|(
operator|(
name|watchloc
operator|==
name|NULL
operator|)
operator|||
operator|(
name|watchloc
operator|->
name|ov_magic
operator|==
name|MAGIC
operator|)
argument_list|,
literal|11
argument_list|)
expr_stmt|;
block|}
end_block

begin_comment
comment|/*  * NAME  *	morecore - get more memory  *  * SYNOPSIS  *	void  *	morecore(bucket)  *	int bucket;  *  * DESCRIPTION  *	Morecore is used to allocate more memory to the indicated bucket.  *  * RETURNS  *	void  */
end_comment

begin_function
specifier|static
name|void
name|morecore
parameter_list|(
name|bucket
parameter_list|)
specifier|register
name|u_int
name|bucket
decl_stmt|;
block|{
specifier|register
name|union
name|overhead
modifier|*
name|op
decl_stmt|;
specifier|register
name|u_int
name|rnu
decl_stmt|;
comment|/* 2^rnu bytes will be requested */
specifier|register
name|u_int
name|nblks
decl_stmt|;
comment|/* become nblks blocks of the desired size */
specifier|register
name|u_int
name|siz
decl_stmt|;
name|assert
argument_list|(
name|bucket
operator|>=
name|QUANTUM_NBITS
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|bucket
operator|<
name|NBUCKETS
argument_list|,
literal|2
argument_list|)
expr_stmt|;
name|assert
argument_list|(
operator|!
name|nextf
index|[
name|bucket
index|]
argument_list|,
literal|3
argument_list|)
expr_stmt|;
ifndef|#
directive|ifndef
name|NO_SBRK
comment|/* 	 * Insure memory is allocated on a page boundary. 	 * Should make getpageize() call? 	 */
define|#
directive|define
name|PAGE_SIZE
value|(1<<10)
name|siz
operator|=
operator|(
name|u_int
operator|)
name|sbrk
argument_list|(
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|siz
operator|&
operator|(
name|PAGE_SIZE
operator|-
literal|1
operator|)
condition|)
name|sbrk
argument_list|(
name|PAGE_SIZE
operator|-
operator|(
name|siz
operator|&
operator|(
name|PAGE_SIZE
operator|-
literal|1
operator|)
operator|)
argument_list|)
expr_stmt|;
endif|#
directive|endif
comment|/* take 2k unless the block is bigger than that */
name|rnu
operator|=
operator|(
name|bucket
operator|<=
literal|11
operator|)
condition|?
literal|11
else|:
name|bucket
expr_stmt|;
name|assert
argument_list|(
name|rnu
operator|>=
name|bucket
argument_list|,
literal|4
argument_list|)
expr_stmt|;
name|nblks
operator|=
literal|1L
operator|<<
operator|(
name|rnu
operator|-
name|bucket
operator|)
expr_stmt|;
comment|/* how many blocks to get */
name|siz
operator|=
literal|1L
operator|<<
name|rnu
expr_stmt|;
ifndef|#
directive|ifndef
name|NO_SBRK
name|op
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
name|sbrk
argument_list|(
name|siz
argument_list|)
expr_stmt|;
comment|/* no more room! */
if|if
condition|(
operator|(
name|int
operator|)
name|op
operator|==
operator|-
literal|1
condition|)
return|return;
comment|/* 	 * Round up to minimum allocation size boundary 	 * and deduct from block count to reflect. 	 */
if|if
condition|(
operator|(
name|int
operator|)
name|op
operator|&
operator|(
name|QUANTUM
operator|-
literal|1
operator|)
condition|)
block|{
name|op
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
operator|(
operator|(
operator|(
name|int
operator|)
name|op
operator|+
name|QUANTUM
operator|)
operator|&
operator|~
operator|(
name|QUANTUM
operator|-
literal|1
operator|)
operator|)
expr_stmt|;
name|nblks
operator|--
expr_stmt|;
block|}
else|#
directive|else
name|op
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
name|malloc
argument_list|(
name|siz
argument_list|)
expr_stmt|;
comment|/* no more room! */
if|if
condition|(
operator|!
name|op
condition|)
return|return;
endif|#
directive|endif
comment|/* 	 * Add new memory allocated to the 	 * free list for this hash bucket. 	 */
name|nextf
index|[
name|bucket
index|]
operator|=
name|op
expr_stmt|;
name|siz
operator|=
literal|1L
operator|<<
name|bucket
expr_stmt|;
while|while
condition|(
operator|--
name|nblks
condition|)
block|{
name|op
operator|->
name|ov_next
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
operator|(
operator|(
name|caddr_t
operator|)
name|op
operator|+
name|siz
operator|)
expr_stmt|;
name|op
operator|=
name|op
operator|->
name|ov_next
expr_stmt|;
block|}
block|}
end_function

begin_comment
comment|/*  * NAME  *	mem_alloc - memory allocator  *  * SYNOPSIS  *	char *  *	mem_alloc()  *  * DESCRIPTION  *	Mem_alloc is used to allocate memory large enought to fit the requested  *	size, and on a boundary suitable for placing any value.  *  * RETURNS  *	char *, pointer to base of dynamic memory allocated  *  * CAVEAT  *	Use mem_free() when you are finished with the space.  */
end_comment

begin_function
name|char
modifier|*
name|mem_alloc
parameter_list|(
name|nbytes
parameter_list|)
specifier|register
name|unsigned
name|long
name|int
name|nbytes
decl_stmt|;
block|{
specifier|register
name|union
name|overhead
modifier|*
name|p
decl_stmt|;
specifier|register
name|int
name|bucket
decl_stmt|;
specifier|register
name|unsigned
name|long
name|int
name|shiftr
decl_stmt|;
if|if
condition|(
name|nbytes
operator|>
operator|(
operator|(
name|unsigned
name|int
operator|)
operator|-
literal|1
operator|)
condition|)
return|return
name|NULL
return|;
name|assert
argument_list|(
operator|(
name|watchloc
operator|==
name|NULL
operator|)
operator|||
operator|(
name|watchloc
operator|->
name|ov_magic
operator|==
name|MAGIC
operator|)
argument_list|,
literal|12
argument_list|)
expr_stmt|;
comment|/* 	 * Convert amount of memory requested into 	 * closest block size stored in hash buckets 	 * which satisfies request.  Account for 	 * space used per block for accounting. 	 */
name|nbytes
operator|=
operator|(
name|nbytes
operator|+
sizeof|sizeof
argument_list|(
expr|union
name|overhead
argument_list|)
operator|+
name|RSLOP
operator|+
operator|(
name|QUANTUM
operator|-
literal|1
operator|)
operator|)
operator|&
operator|~
operator|(
name|QUANTUM
operator|-
literal|1
operator|)
expr_stmt|;
name|shiftr
operator|=
operator|(
name|nbytes
operator|-
literal|1
operator|)
operator|>>
name|QUANTUM_NBITS
expr_stmt|;
comment|/* apart from this loop, this is O(1) */
name|bucket
operator|=
name|QUANTUM_NBITS
expr_stmt|;
while|while
condition|(
name|shiftr
condition|)
block|{
name|shiftr
operator|>>=
literal|1
expr_stmt|;
name|bucket
operator|++
expr_stmt|;
block|}
comment|/* 	 * If nothing in hash bucket right now, 	 * request more memory from the system. 	 */
if|if
condition|(
operator|!
name|nextf
index|[
name|bucket
index|]
condition|)
name|morecore
argument_list|(
name|bucket
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|p
operator|=
name|nextf
index|[
name|bucket
index|]
operator|)
condition|)
return|return
operator|(
name|char
operator|*
operator|)
literal|0
return|;
comment|/* remove from linked list */
name|nextf
index|[
name|bucket
index|]
operator|=
name|p
operator|->
name|ov_next
expr_stmt|;
name|p
operator|->
name|ov_magic
operator|=
name|MAGIC
expr_stmt|;
name|p
operator|->
name|ov_index
operator|=
name|bucket
expr_stmt|;
ifdef|#
directive|ifdef
name|MSTATS
name|nmalloc
index|[
name|bucket
index|]
operator|++
expr_stmt|;
endif|#
directive|endif
ifdef|#
directive|ifdef
name|RCHECK
comment|/* 	 * Record allocated size of block and 	 * bound space with magic numbers 	 */
if|if
condition|(
name|nbytes
operator|<=
operator|(
literal|1L
operator|<<
literal|16
operator|)
condition|)
name|p
operator|->
name|ov_size
operator|=
name|nbytes
operator|-
literal|1
expr_stmt|;
name|p
operator|->
name|ov_rmagic
operator|=
name|RMAGIC
expr_stmt|;
operator|*
operator|(
operator|(
name|u_int
operator|*
operator|)
operator|(
operator|(
name|caddr_t
operator|)
name|p
operator|+
name|nbytes
operator|-
name|RSLOP
operator|)
operator|)
operator|=
name|RMAGIC
expr_stmt|;
endif|#
directive|endif
return|return
operator|(
operator|(
name|char
operator|*
operator|)
operator|(
name|p
operator|+
literal|1
operator|)
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * NAME  *	mem_free - free memory  *  * SYNOPSIS  *	int  *	mem_free(cp)  *	char * cp;  *  * DESCRIPTION  *	Mem_free is used to release space allocated by mem_alloc  *	or mem_realloc.  *  * RETURNS  *	int  *  * CAVEAT  *	do not pass mem_free() an argument that was returned by mem_alloc()  *	or mem_realloc().  */
end_comment

begin_function
name|int
name|mem_free
parameter_list|(
name|cp
parameter_list|)
name|char
modifier|*
name|cp
decl_stmt|;
block|{
specifier|register
name|u_int
name|bucket
decl_stmt|;
specifier|register
name|union
name|overhead
modifier|*
name|op
decl_stmt|;
name|assert
argument_list|(
operator|(
name|watchloc
operator|==
name|NULL
operator|)
operator|||
operator|(
name|watchloc
operator|->
name|ov_magic
operator|==
name|MAGIC
operator|)
argument_list|,
literal|13
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|cp
condition|)
return|return;
name|op
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
name|cp
operator|-
literal|1
expr_stmt|;
name|assert
argument_list|(
name|op
operator|->
name|ov_magic
operator|==
name|MAGIC
argument_list|,
literal|5
argument_list|)
expr_stmt|;
comment|/* make sure it was in use */
name|assert
argument_list|(
name|op
operator|->
name|ov_index
operator|<
name|NBUCKETS
argument_list|,
literal|6
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|op
operator|->
name|ov_index
operator|>=
name|QUANTUM_NBITS
argument_list|,
literal|7
argument_list|)
expr_stmt|;
ifdef|#
directive|ifdef
name|RCHECK
name|assert
argument_list|(
name|op
operator|->
name|ov_index
operator|>
literal|16
operator|||
name|op
operator|->
name|ov_size
operator|==
operator|(
literal|1L
operator|<<
name|op
operator|->
name|ov_index
operator|)
operator|-
literal|1
argument_list|,
literal|8
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|op
operator|->
name|ov_rmagic
operator|==
name|RMAGIC
argument_list|,
literal|9
argument_list|)
expr_stmt|;
name|assert
argument_list|(
name|op
operator|->
name|ov_index
operator|>
literal|16
operator|||
operator|*
operator|(
name|u_int
operator|*
operator|)
operator|(
operator|(
name|caddr_t
operator|)
name|op
operator|+
name|op
operator|->
name|ov_size
operator|+
literal|1
operator|-
name|RSLOP
operator|)
operator|==
name|RMAGIC
argument_list|,
literal|10
argument_list|)
expr_stmt|;
endif|#
directive|endif
ifndef|#
directive|ifndef
name|DEBUG
if|if
condition|(
name|op
operator|->
name|ov_magic
operator|!=
name|MAGIC
condition|)
return|return;
comment|/* sanity */
endif|#
directive|endif
name|bucket
operator|=
name|op
operator|->
name|ov_index
expr_stmt|;
name|op
operator|->
name|ov_next
operator|=
name|nextf
index|[
name|bucket
index|]
expr_stmt|;
name|nextf
index|[
name|bucket
index|]
operator|=
name|op
expr_stmt|;
ifdef|#
directive|ifdef
name|MSTATS
name|nmalloc
index|[
name|bucket
index|]
operator|--
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_comment
comment|/*  * NAME  *	findbucket - find a bucket  *  * SYNOPSIS  *	int  *	findbucket(freep, srchlen)  *	union overhead * freep;  *	int srchlen;  *  * DESCRIPTION  *	Findbucket is used to find the bucket a free block is in.  *	Search ``srchlen'' elements of each free list for a block whose  *	header starts at ``freep''.  If srchlen is -1 search the whole list.  *  * RETURNS  *	bucket number, or -1 if not found.  */
end_comment

begin_function
specifier|static
name|int
name|findbucket
parameter_list|(
name|freep
parameter_list|,
name|srchlen
parameter_list|)
name|union
name|overhead
modifier|*
name|freep
decl_stmt|;
name|int
name|srchlen
decl_stmt|;
block|{
specifier|register
name|union
name|overhead
modifier|*
name|p
decl_stmt|;
specifier|register
name|int
name|i
decl_stmt|,
name|j
decl_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NBUCKETS
condition|;
name|i
operator|++
control|)
block|{
name|j
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|p
operator|=
name|nextf
index|[
name|i
index|]
init|;
name|p
operator|&&
name|j
operator|!=
name|srchlen
condition|;
name|p
operator|=
name|p
operator|->
name|ov_next
control|)
block|{
if|if
condition|(
name|p
operator|==
name|freep
condition|)
return|return
name|i
return|;
name|j
operator|++
expr_stmt|;
block|}
block|}
return|return
operator|-
literal|1
return|;
block|}
end_function

begin_comment
comment|/*  * When a program attempts "storage compaction" as mentioned in the  * old malloc man page, it realloc's an already freed block.  Usually  * this is the last block it freed; occasionally it might be farther  * back.  We have to search all the free lists for the block in order  * to determine its bucket: first we make one pass thru the lists  * checking only the first block in each; if that fails we search  * ``realloc_srchlen'' blocks in each list for a match (the variable  * is extern so the caller can modify it).  If that fails we just copy  * however many bytes was given to realloc() and hope it's not huge.  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|realloc_srchlen
init|=
literal|4
decl_stmt|;
end_decl_stmt

begin_comment
comment|/* 4 should be plenty, -1 =>'s whole list */
end_comment

begin_comment
comment|/*  * NAME  *	mem_realloc - change size  *  * SYNOPSIS  *	char  *	mem_realloc(cp, nbytes)  *	char * cp;  *	u_int nbytes;  *  * DESCRIPTION  *	Mem_realloc is used to enlarge a chunk of memory  *	returned by mem_alloc() or mem_realloc().  *  * RETURNS  *	char *, pointer to base of dynamic memory allocated  *  * CAVEAT  *	Use mem_free() when you are finished with the space.  */
end_comment

begin_function
name|char
modifier|*
name|mem_realloc
parameter_list|(
name|cp
parameter_list|,
name|nbytes
parameter_list|)
name|char
modifier|*
name|cp
decl_stmt|;
name|unsigned
name|long
name|nbytes
decl_stmt|;
block|{
specifier|register
name|u_int
name|old_nbytes
decl_stmt|;
specifier|register
name|union
name|overhead
modifier|*
name|op
decl_stmt|;
name|char
modifier|*
name|res
decl_stmt|;
specifier|register
name|u_int
name|old_bucket
decl_stmt|;
name|short
name|was_alloced
init|=
literal|0
decl_stmt|;
if|if
condition|(
name|nbytes
operator|>
operator|(
operator|(
name|unsigned
name|int
operator|)
operator|-
literal|1
operator|)
condition|)
return|return
name|NULL
return|;
name|assert
argument_list|(
operator|(
name|watchloc
operator|==
name|NULL
operator|)
operator|||
operator|(
name|watchloc
operator|->
name|ov_magic
operator|==
name|MAGIC
operator|)
argument_list|,
literal|14
argument_list|)
expr_stmt|;
if|if
condition|(
operator|!
name|cp
condition|)
return|return
name|mem_alloc
argument_list|(
name|nbytes
argument_list|)
return|;
name|op
operator|=
operator|(
expr|union
name|overhead
operator|*
operator|)
operator|(
operator|(
name|caddr_t
operator|)
name|cp
operator|-
sizeof|sizeof
argument_list|(
expr|union
name|overhead
argument_list|)
operator|)
expr_stmt|;
if|if
condition|(
name|op
operator|->
name|ov_magic
operator|==
name|MAGIC
condition|)
block|{
name|was_alloced
operator|++
expr_stmt|;
name|old_bucket
operator|=
name|op
operator|->
name|ov_index
expr_stmt|;
block|}
else|else
block|{
comment|/* 		 * Already free, doing "compaction". 		 * 		 * Search for the old block of memory on the 		 * free list. First, check the most common 		 * case (last element free'd), then (this failing) 		 * the last ``realloc_srchlen'' items free'd. 		 * If all lookups fail, then assume the size of 		 * the memory block being realloc'd is the 		 * smallest possible. 		 */
if|if
condition|(
operator|(
name|old_bucket
operator|=
name|findbucket
argument_list|(
name|op
argument_list|,
literal|1
argument_list|)
operator|)
operator|==
operator|-
literal|1
operator|&&
operator|(
name|old_bucket
operator|=
name|findbucket
argument_list|(
name|op
argument_list|,
name|realloc_srchlen
argument_list|)
operator|)
operator|==
operator|-
literal|1
condition|)
name|old_bucket
operator|=
name|QUANTUM_NBITS
expr_stmt|;
block|}
name|old_nbytes
operator|=
operator|(
literal|1L
operator|<<
name|old_bucket
operator|)
operator|-
sizeof|sizeof
argument_list|(
expr|union
name|overhead
argument_list|)
operator|-
name|RSLOP
expr_stmt|;
comment|/* 	 * avoid the copy if same size block 	 */
if|if
condition|(
name|was_alloced
operator|&&
name|nbytes
operator|<=
name|old_nbytes
operator|&&
name|nbytes
operator|>
operator|(
name|old_nbytes
operator|>>
literal|1
operator|)
operator|-
sizeof|sizeof
argument_list|(
expr|union
name|overhead
argument_list|)
operator|-
name|RSLOP
condition|)
return|return
name|cp
return|;
comment|/* 	 * grab another chunk 	 */
if|if
condition|(
operator|!
operator|(
name|res
operator|=
name|mem_alloc
argument_list|(
name|nbytes
argument_list|)
operator|)
condition|)
return|return
operator|(
name|char
operator|*
operator|)
literal|0
return|;
name|assert
argument_list|(
name|cp
operator|!=
name|res
argument_list|,
literal|11
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|res
argument_list|,
name|cp
argument_list|,
operator|(
name|nbytes
operator|<
name|old_nbytes
operator|)
condition|?
name|nbytes
else|:
name|old_nbytes
argument_list|)
expr_stmt|;
if|if
condition|(
name|was_alloced
condition|)
name|mem_free
argument_list|(
name|cp
argument_list|)
expr_stmt|;
return|return
name|res
return|;
block|}
end_function

begin_else
else|#
directive|else
end_else

begin_comment
comment|/*CALC_MALLOC*/
end_comment

begin_undef
undef|#
directive|undef
name|MSTATS
end_undef

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*CALC_MALLOC*/
end_comment

begin_comment
comment|/*  * Allocate a new item from the specified free list.  * Returns NULL if no item can be allocated.  */
end_comment

begin_function
name|ALLOCITEM
modifier|*
name|allocitem
parameter_list|(
name|fp
parameter_list|)
name|FREELIST
modifier|*
name|fp
decl_stmt|;
comment|/* free list header */
block|{
name|FREEITEM
modifier|*
name|ip
decl_stmt|;
comment|/* allocated item */
if|if
condition|(
name|fp
operator|->
name|curfree
operator|>
literal|0
condition|)
block|{
name|fp
operator|->
name|curfree
operator|--
expr_stmt|;
name|ip
operator|=
name|fp
operator|->
name|freelist
expr_stmt|;
name|fp
operator|->
name|freelist
operator|=
name|ip
operator|->
name|next
expr_stmt|;
return|return
operator|(
name|ALLOCITEM
operator|*
operator|)
name|ip
return|;
block|}
name|ip
operator|=
operator|(
name|FREEITEM
operator|*
operator|)
name|malloc
argument_list|(
name|fp
operator|->
name|itemsize
argument_list|)
expr_stmt|;
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
return|return
name|NULL
return|;
return|return
operator|(
name|ALLOCITEM
operator|*
operator|)
name|ip
return|;
block|}
end_function

begin_comment
comment|/*  * Free an item by placing it back on a free list.  * If too many items are on the list, it is really freed.  */
end_comment

begin_function
name|void
name|freeitem
parameter_list|(
name|fp
parameter_list|,
name|ip
parameter_list|)
name|FREELIST
modifier|*
name|fp
decl_stmt|;
comment|/* freelist header */
name|FREEITEM
modifier|*
name|ip
decl_stmt|;
comment|/* item to be freed */
block|{
if|if
condition|(
name|ip
operator|==
name|NULL
condition|)
return|return;
if|if
condition|(
name|fp
operator|->
name|curfree
operator|>=
name|fp
operator|->
name|maxfree
condition|)
block|{
name|free
argument_list|(
operator|(
name|char
operator|*
operator|)
name|ip
argument_list|)
expr_stmt|;
return|return;
block|}
name|ip
operator|->
name|next
operator|=
name|fp
operator|->
name|freelist
expr_stmt|;
name|fp
operator|->
name|freelist
operator|=
name|ip
expr_stmt|;
name|fp
operator|->
name|curfree
operator|++
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * NAME  *	mem_stats - print memory statistics  *  * SYNOPSIS  *	void  *	mem_stats(s)  *	char * s;  *  * DESCRIPTION  *	Mem_stats is used to print out statistics about current memory usage.  *	``s'' is the title string  *  *	Prints two lines of numbers, one showing the length of the free list  *	for each size category, the second showing the number of mallocs -  *	frees for each size category.  *  * RETURNS  *	void  */
end_comment

begin_comment
comment|/*ARGSUSED*/
end_comment

begin_function
name|void
name|mem_stats
parameter_list|(
name|s
parameter_list|)
name|char
modifier|*
name|s
decl_stmt|;
block|{
ifdef|#
directive|ifdef
name|MSTATS
specifier|register
name|int
name|i
decl_stmt|,
name|j
decl_stmt|;
specifier|register
name|union
name|overhead
modifier|*
name|p
decl_stmt|;
name|int
name|totfree
init|=
literal|0
decl_stmt|;
name|int
name|totused
init|=
literal|0
decl_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Memory allocation statistics %s\n"
argument_list|,
name|s
argument_list|)
expr_stmt|;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%11s:%12s%12s%12s\n"
argument_list|,
literal|"Bucket"
argument_list|,
literal|"In Use"
argument_list|,
literal|"Free"
argument_list|,
literal|"Sum"
argument_list|)
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|NBUCKETS
condition|;
name|i
operator|++
control|)
block|{
for|for
control|(
name|j
operator|=
literal|0
operator|,
name|p
operator|=
name|nextf
index|[
name|i
index|]
init|;
name|p
condition|;
name|p
operator|=
name|p
operator|->
name|ov_next
operator|,
name|j
operator|++
control|)
empty_stmt|;
if|if
condition|(
operator|!
name|j
operator|&&
operator|!
name|nmalloc
index|[
name|i
index|]
condition|)
continue|continue;
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%11d:%12d%12d%12d\n"
argument_list|,
operator|(
literal|1L
operator|<<
name|i
operator|)
argument_list|,
name|nmalloc
index|[
name|i
index|]
argument_list|,
name|j
argument_list|,
name|j
operator|+
name|nmalloc
index|[
name|i
index|]
argument_list|)
expr_stmt|;
name|totfree
operator|+=
name|j
operator|*
operator|(
literal|1L
operator|<<
name|i
operator|)
expr_stmt|;
name|totused
operator|+=
name|nmalloc
index|[
name|i
index|]
operator|*
operator|(
literal|1L
operator|<<
name|i
operator|)
expr_stmt|;
block|}
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"%11s:%12d%12d%12d\n"
argument_list|,
literal|"Totals"
argument_list|,
name|totused
argument_list|,
name|totfree
argument_list|,
name|totused
operator|+
name|totfree
argument_list|)
expr_stmt|;
else|#
directive|else
name|fprintf
argument_list|(
name|stderr
argument_list|,
literal|"Memory allocation stats were not compiled into calc\n"
argument_list|)
expr_stmt|;
endif|#
directive|endif
block|}
end_function

begin_ifdef
ifdef|#
directive|ifdef
name|DEBUG
end_ifdef

begin_function
name|void
name|assertfailed
parameter_list|(
name|n
parameter_list|)
block|{
name|printf
argument_list|(
literal|"Assertion %d failed\n"
argument_list|,
name|n
argument_list|)
expr_stmt|;
name|exit
argument_list|(
literal|1
argument_list|)
expr_stmt|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* END CODE */
end_comment

end_unit

