begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)v_paragraph.c	10.7 (Berkeley) 3/6/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<sys/time.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<errno.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_include
include|#
directive|include
file|"vi.h"
end_include

begin_define
define|#
directive|define
name|INTEXT_CHECK
value|{							\ 	if (len == 0 || v_isempty(p, len)) {				\ 		if (!--cnt)						\ 			goto found;					\ 		pstate = P_INBLANK;					\ 	}								\
comment|/*								\ 	 * !!!								\ 	 * Historic documentation (USD:15-11, 4.2) said that formfeed	\ 	 * characters (^L) in the first column delimited paragraphs.	\ 	 * The historic vi code mentions formfeed characters, but never	\ 	 * implements them.  It seems reasonable, do it.		\ 	 */
value|\ 	if (p[0] == '\014') {						\ 		if (!--cnt)						\ 			goto found;					\ 		continue;						\ 	}								\ 	if (p[0] != '.' || len< 2)					\ 		continue;						\ 	for (lp = VIP(sp)->ps; *lp != '\0'; lp += 2)			\ 		if (lp[0] == p[1]&&					\ 		    (lp[1] == ' '&& len == 2 || lp[1] == p[2])&&	\ 		    !--cnt)						\ 			goto found;					\ }
end_define

begin_comment
comment|/*  * v_paragraphf -- [count]}  *	Move forward count paragraphs.  *  * Paragraphs are empty lines after text, formfeed characters, or values  * from the paragraph or section options.  *  * PUBLIC: int v_paragraphf __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_paragraphf
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
enum|enum
block|{
name|P_INTEXT
block|,
name|P_INBLANK
block|}
name|pstate
enum|;
name|size_t
name|lastlen
decl_stmt|,
name|len
decl_stmt|;
name|recno_t
name|cnt
decl_stmt|,
name|lastlno
decl_stmt|,
name|lno
decl_stmt|;
name|int
name|isempty
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|,
modifier|*
name|lp
decl_stmt|;
comment|/* 	 * !!! 	 * If the starting cursor position is at or before any non-blank 	 * characters in the line, i.e. the movement is cutting all of the 	 * line's text, the buffer is in line mode.  It's a lot easier to 	 * check here, because we know that the end is going to be the start 	 * or end of a line. 	 * 	 * This was historical practice in vi, with a single exception.  If 	 * the paragraph movement was from the start of the last line to EOF, 	 * then all the characters were deleted from the last line, but the 	 * line itself remained.  If somebody complains, don't pause, don't 	 * hesitate, just hit them. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
literal|0
condition|)
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_LMODE
argument_list|)
expr_stmt|;
else|else
block|{
name|vp
operator|->
name|m_stop
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|nonblank
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_stop
operator|.
name|lno
argument_list|,
operator|&
name|vp
operator|->
name|m_stop
operator|.
name|cno
argument_list|)
condition|)
return|return
operator|(
literal|1
operator|)
return|;
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|<=
name|vp
operator|->
name|m_stop
operator|.
name|cno
condition|)
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_LMODE
argument_list|)
expr_stmt|;
block|}
comment|/* Figure out what state we're currently in. */
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
literal|0
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
goto|goto
name|eof
goto|;
comment|/* 	 * If we start in text, we want to switch states 	 * (2 * N - 1) times, in non-text, (2 * N) times. 	 */
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
name|cnt
operator|*=
literal|2
expr_stmt|;
if|if
condition|(
name|len
operator|==
literal|0
operator|||
name|v_isempty
argument_list|(
name|p
argument_list|,
name|len
argument_list|)
condition|)
name|pstate
operator|=
name|P_INBLANK
expr_stmt|;
else|else
block|{
operator|--
name|cnt
expr_stmt|;
name|pstate
operator|=
name|P_INTEXT
expr_stmt|;
block|}
for|for
control|(
init|;
condition|;
control|)
block|{
name|lastlno
operator|=
name|lno
expr_stmt|;
name|lastlen
operator|=
name|len
expr_stmt|;
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
operator|++
name|lno
argument_list|,
literal|0
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
goto|goto
name|eof
goto|;
switch|switch
condition|(
name|pstate
condition|)
block|{
case|case
name|P_INTEXT
case|:
name|INTEXT_CHECK
expr_stmt|;
break|break;
case|case
name|P_INBLANK
case|:
if|if
condition|(
name|len
operator|==
literal|0
operator|||
name|v_isempty
argument_list|(
name|p
argument_list|,
name|len
argument_list|)
condition|)
break|break;
if|if
condition|(
operator|--
name|cnt
condition|)
block|{
name|pstate
operator|=
name|P_INTEXT
expr_stmt|;
break|break;
block|}
comment|/* 			 * !!! 			 * Non-motion commands move to the end of the range, 			 * delete and yank stay at the start.  Ignore others. 			 * Adjust the end of the range for motion commands; 			 * historically, a motion component was to the end of 			 * the previous line, whereas the movement command was 			 * to the start of the new "paragraph". 			 */
name|found
label|:
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
block|{
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|lastlno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|lastlen
condition|?
name|lastlen
operator|-
literal|1
else|:
literal|0
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_start
expr_stmt|;
block|}
else|else
block|{
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
block|}
return|return
operator|(
literal|0
operator|)
return|;
default|default:
name|abort
argument_list|()
expr_stmt|;
block|}
block|}
comment|/* 	 * !!! 	 * Adjust end of the range for motion commands; EOF is a movement 	 * sink.  The } command historically moved to the end of the last 	 * line, not the beginning, from any position before the end of the 	 * last line.  It also historically worked on empty files, so we 	 * have to make it okay. 	 */
name|eof
label|:
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|==
name|lno
operator|||
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|==
name|lno
operator|-
literal|1
condition|)
block|{
if|if
condition|(
name|db_eget
argument_list|(
name|sp
argument_list|,
name|vp
operator|->
name|m_start
operator|.
name|lno
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|,
operator|&
name|isempty
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
name|isempty
condition|)
return|return
operator|(
literal|1
operator|)
return|;
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
operator|(
name|len
condition|?
name|len
operator|-
literal|1
else|:
literal|0
operator|)
condition|)
block|{
name|v_eof
argument_list|(
name|sp
argument_list|,
name|NULL
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
block|}
comment|/* 	 * !!! 	 * Non-motion commands move to the end of the range, delete 	 * and yank stay at the start.  Ignore others. 	 * 	 * If deleting the line (which happens if deleting to EOF), then 	 * cursor movement is to the first nonblank. 	 */
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
operator|&&
name|ISCMD
argument_list|(
name|vp
operator|->
name|rkp
argument_list|,
literal|'d'
argument_list|)
condition|)
block|{
name|F_CLR
argument_list|(
name|vp
argument_list|,
name|VM_RCM_MASK
argument_list|)
expr_stmt|;
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_RCM_SETFNB
argument_list|)
expr_stmt|;
block|}
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|lno
operator|-
literal|1
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
name|len
condition|?
name|len
operator|-
literal|1
else|:
literal|0
expr_stmt|;
name|vp
operator|->
name|m_final
operator|=
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|?
name|vp
operator|->
name|m_start
else|:
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_paragraphb -- [count]{  *	Move backward count paragraphs.  *  * PUBLIC: int v_paragraphb __P((SCR *, VICMD *));  */
end_comment

begin_function
name|int
name|v_paragraphb
parameter_list|(
name|sp
parameter_list|,
name|vp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|VICMD
modifier|*
name|vp
decl_stmt|;
block|{
enum|enum
block|{
name|P_INTEXT
block|,
name|P_INBLANK
block|}
name|pstate
enum|;
name|size_t
name|len
decl_stmt|;
name|recno_t
name|cnt
decl_stmt|,
name|lno
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|,
modifier|*
name|lp
decl_stmt|;
comment|/* 	 * !!! 	 * Check for SOF.  The historic vi didn't complain if users hit SOF 	 * repeatedly, unless it was part of a motion command.  There is no 	 * question but that Emerson's editor of choice was vi. 	 * 	 * The { command historically moved to the beginning of the first 	 * line if invoked on the first line. 	 * 	 * !!! 	 * If the starting cursor position is in the first column (backward 	 * paragraph movements did NOT historically pay attention to non-blank 	 * characters) i.e. the movement is cutting the entire line, the buffer 	 * is in line mode.  Cuts from the beginning of the line also did not 	 * cut the current line, but started at the previous EOL. 	 * 	 * Correct for a left motion component while we're thinking about it. 	 */
name|lno
operator|=
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
if|if
condition|(
name|ISMOTION
argument_list|(
name|vp
argument_list|)
condition|)
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|==
literal|1
condition|)
block|{
name|v_sof
argument_list|(
name|sp
argument_list|,
operator|&
name|vp
operator|->
name|m_start
argument_list|)
expr_stmt|;
return|return
operator|(
literal|1
operator|)
return|;
block|}
else|else
operator|--
name|vp
operator|->
name|m_start
operator|.
name|lno
expr_stmt|;
name|F_SET
argument_list|(
name|vp
argument_list|,
name|VM_LMODE
argument_list|)
expr_stmt|;
block|}
else|else
operator|--
name|vp
operator|->
name|m_start
operator|.
name|cno
expr_stmt|;
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|lno
operator|<=
literal|1
condition|)
goto|goto
name|sof
goto|;
comment|/* Figure out what state we're currently in. */
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
name|lno
argument_list|,
literal|0
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
goto|goto
name|sof
goto|;
comment|/* 	 * If we start in text, we want to switch states 	 * (2 * N - 1) times, in non-text, (2 * N) times. 	 */
name|cnt
operator|=
name|F_ISSET
argument_list|(
name|vp
argument_list|,
name|VC_C1SET
argument_list|)
condition|?
name|vp
operator|->
name|count
else|:
literal|1
expr_stmt|;
name|cnt
operator|*=
literal|2
expr_stmt|;
if|if
condition|(
name|len
operator|==
literal|0
operator|||
name|v_isempty
argument_list|(
name|p
argument_list|,
name|len
argument_list|)
condition|)
name|pstate
operator|=
name|P_INBLANK
expr_stmt|;
else|else
block|{
operator|--
name|cnt
expr_stmt|;
name|pstate
operator|=
name|P_INTEXT
expr_stmt|;
comment|/* 		 * !!! 		 * If the starting cursor is past the first column, 		 * the current line is checked for a paragraph. 		 */
if|if
condition|(
name|vp
operator|->
name|m_start
operator|.
name|cno
operator|>
literal|0
condition|)
operator|++
name|lno
expr_stmt|;
block|}
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|db_get
argument_list|(
name|sp
argument_list|,
operator|--
name|lno
argument_list|,
literal|0
argument_list|,
operator|&
name|p
argument_list|,
operator|&
name|len
argument_list|)
condition|)
goto|goto
name|sof
goto|;
switch|switch
condition|(
name|pstate
condition|)
block|{
case|case
name|P_INTEXT
case|:
name|INTEXT_CHECK
expr_stmt|;
break|break;
case|case
name|P_INBLANK
case|:
if|if
condition|(
name|len
operator|!=
literal|0
operator|&&
operator|!
name|v_isempty
argument_list|(
name|p
argument_list|,
name|len
argument_list|)
condition|)
block|{
if|if
condition|(
operator|!
operator|--
name|cnt
condition|)
goto|goto
name|found
goto|;
name|pstate
operator|=
name|P_INTEXT
expr_stmt|;
block|}
break|break;
default|default:
name|abort
argument_list|()
expr_stmt|;
block|}
block|}
comment|/* SOF is a movement sink. */
name|sof
label|:
name|lno
operator|=
literal|1
expr_stmt|;
name|found
label|:
name|vp
operator|->
name|m_stop
operator|.
name|lno
operator|=
name|lno
expr_stmt|;
name|vp
operator|->
name|m_stop
operator|.
name|cno
operator|=
literal|0
expr_stmt|;
comment|/* 	 * All commands move to the end of the range.  (We already 	 * adjusted the start of the range for motion commands). 	 */
name|vp
operator|->
name|m_final
operator|=
name|vp
operator|->
name|m_stop
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * v_buildps --  *	Build the paragraph command search pattern.  *  * PUBLIC: int v_buildps __P((SCR *, char *, char *));  */
end_comment

begin_function
name|int
name|v_buildps
parameter_list|(
name|sp
parameter_list|,
name|p_p
parameter_list|,
name|s_p
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|char
modifier|*
name|p_p
decl_stmt|,
decl|*
name|s_p
decl_stmt|;
end_function

begin_block
block|{
name|VI_PRIVATE
modifier|*
name|vip
decl_stmt|;
name|size_t
name|p_len
decl_stmt|,
name|s_len
decl_stmt|;
name|char
modifier|*
name|p
decl_stmt|;
comment|/* 	 * The vi paragraph command searches for either a paragraph or 	 * section option macro. 	 */
name|p_len
operator|=
name|p_p
operator|==
name|NULL
condition|?
literal|0
else|:
name|strlen
argument_list|(
name|p_p
argument_list|)
expr_stmt|;
name|s_len
operator|=
name|s_p
operator|==
name|NULL
condition|?
literal|0
else|:
name|strlen
argument_list|(
name|s_p
argument_list|)
expr_stmt|;
if|if
condition|(
name|p_len
operator|==
literal|0
operator|&&
name|s_len
operator|==
literal|0
condition|)
return|return
operator|(
literal|0
operator|)
return|;
name|MALLOC_RET
argument_list|(
name|sp
argument_list|,
name|p
argument_list|,
name|char
operator|*
argument_list|,
name|p_len
operator|+
name|s_len
operator|+
literal|1
argument_list|)
expr_stmt|;
name|vip
operator|=
name|VIP
argument_list|(
name|sp
argument_list|)
expr_stmt|;
if|if
condition|(
name|vip
operator|->
name|ps
operator|!=
name|NULL
condition|)
name|free
argument_list|(
name|vip
operator|->
name|ps
argument_list|)
expr_stmt|;
if|if
condition|(
name|p_p
operator|!=
name|NULL
condition|)
name|memmove
argument_list|(
name|p
argument_list|,
name|p_p
argument_list|,
name|p_len
operator|+
literal|1
argument_list|)
expr_stmt|;
if|if
condition|(
name|s_p
operator|!=
name|NULL
condition|)
name|memmove
argument_list|(
name|p
operator|+
name|p_len
argument_list|,
name|s_p
argument_list|,
name|s_len
operator|+
literal|1
argument_list|)
expr_stmt|;
name|vip
operator|->
name|ps
operator|=
name|p
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_block

end_unit

