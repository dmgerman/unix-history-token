begin_unit|revision:0.9.5;language:C;cregit-version:0.0.1
begin_comment
comment|/*-  * Copyright (c) 1992, 1993, 1994  *	The Regents of the University of California.  All rights reserved.  * Copyright (c) 1992, 1993, 1994, 1995, 1996  *	Keith Bostic.  All rights reserved.  *  * See the LICENSE file for redistribution information.  */
end_comment

begin_include
include|#
directive|include
file|"config.h"
end_include

begin_ifndef
ifndef|#
directive|ifndef
name|lint
end_ifndef

begin_decl_stmt
specifier|static
specifier|const
name|char
name|sccsid
index|[]
init|=
literal|"@(#)ex_txt.c	10.17 (Berkeley) 10/10/96"
decl_stmt|;
end_decl_stmt

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/* not lint */
end_comment

begin_include
include|#
directive|include
file|<sys/types.h>
end_include

begin_include
include|#
directive|include
file|<sys/queue.h>
end_include

begin_include
include|#
directive|include
file|<bitstring.h>
end_include

begin_include
include|#
directive|include
file|<ctype.h>
end_include

begin_include
include|#
directive|include
file|<limits.h>
end_include

begin_include
include|#
directive|include
file|<stdio.h>
end_include

begin_include
include|#
directive|include
file|<stdlib.h>
end_include

begin_include
include|#
directive|include
file|<string.h>
end_include

begin_include
include|#
directive|include
file|"../common/common.h"
end_include

begin_comment
comment|/*  * !!!  * The backslash characters was special when it preceded a newline as part of  * a substitution replacement pattern.  For example, the input ":a\<cr>" would  * failed immediately with an error, as the<cr> wasn't part of a substitution  * replacement pattern.  This implies a frightening integration of the editor  * and the parser and/or the RE engine.  There's no way I'm going to reproduce  * those semantics.  *  * So, if backslashes are special, this code inserts the backslash and the next  * character into the string, without regard for the character or the command  * being entered.  Since "\<cr>" was illegal historically (except for the one  * special case), and the command will fail eventually, no historical scripts  * should break (presuming they didn't depend on the failure mode itself or the  * characters remaining when failure occurred.  */
end_comment

begin_decl_stmt
specifier|static
name|int
name|txt_dent
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|TEXT
operator|*
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_decl_stmt
specifier|static
name|void
name|txt_prompt
name|__P
argument_list|(
operator|(
name|SCR
operator|*
operator|,
name|TEXT
operator|*
operator|,
name|ARG_CHAR_T
operator|,
name|u_int32_t
operator|)
argument_list|)
decl_stmt|;
end_decl_stmt

begin_comment
comment|/*  * ex_txt --  *	Get lines from the terminal for ex.  *  * PUBLIC: int ex_txt __P((SCR *, TEXTH *, ARG_CHAR_T, u_int32_t));  */
end_comment

begin_function
name|int
name|ex_txt
parameter_list|(
name|sp
parameter_list|,
name|tiqh
parameter_list|,
name|prompt
parameter_list|,
name|flags
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|TEXTH
modifier|*
name|tiqh
decl_stmt|;
name|ARG_CHAR_T
name|prompt
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
block|{
name|EVENT
name|ev
decl_stmt|;
name|GS
modifier|*
name|gp
decl_stmt|;
name|TEXT
name|ait
decl_stmt|,
modifier|*
name|ntp
decl_stmt|,
modifier|*
name|tp
decl_stmt|;
name|carat_t
name|carat_st
decl_stmt|;
name|size_t
name|cnt
decl_stmt|;
name|int
name|rval
decl_stmt|;
name|rval
operator|=
literal|0
expr_stmt|;
comment|/* 	 * Get a TEXT structure with some initial buffer space, reusing the 	 * last one if it's big enough.  (All TEXT bookkeeping fields default 	 * to 0 -- text_init() handles this.) 	 */
if|if
condition|(
name|tiqh
operator|->
name|cqh_first
operator|!=
operator|(
name|void
operator|*
operator|)
name|tiqh
condition|)
block|{
name|tp
operator|=
name|tiqh
operator|->
name|cqh_first
expr_stmt|;
if|if
condition|(
name|tp
operator|->
name|q
operator|.
name|cqe_next
operator|!=
operator|(
name|void
operator|*
operator|)
name|tiqh
operator|||
name|tp
operator|->
name|lb_len
operator|<
literal|32
condition|)
block|{
name|text_lfree
argument_list|(
name|tiqh
argument_list|)
expr_stmt|;
goto|goto
name|newtp
goto|;
block|}
name|tp
operator|->
name|len
operator|=
literal|0
expr_stmt|;
block|}
else|else
block|{
name|newtp
label|:
if|if
condition|(
operator|(
name|tp
operator|=
name|text_init
argument_list|(
name|sp
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
literal|32
argument_list|)
operator|)
operator|==
name|NULL
condition|)
goto|goto
name|err
goto|;
name|CIRCLEQ_INSERT_HEAD
argument_list|(
name|tiqh
argument_list|,
name|tp
argument_list|,
name|q
argument_list|)
expr_stmt|;
block|}
comment|/* Set the starting line number. */
name|tp
operator|->
name|lno
operator|=
name|sp
operator|->
name|lno
operator|+
literal|1
expr_stmt|;
comment|/* 	 * If it's a terminal, set up autoindent, put out the prompt, and 	 * set it up so we know we were suspended.  Otherwise, turn off 	 * the autoindent flag, as that requires less special casing below. 	 * 	 * XXX 	 * Historic practice is that ^Z suspended command mode (but, because 	 * it ran in cooked mode, it was unaffected by the autowrite option.) 	 * On restart, any "current" input was discarded, whether in insert 	 * mode or not, and ex was in command mode.  This code matches historic 	 * practice, but not 'cause it's easier. 	 */
name|gp
operator|=
name|sp
operator|->
name|gp
expr_stmt|;
if|if
condition|(
name|F_ISSET
argument_list|(
name|gp
argument_list|,
name|G_SCRIPTED
argument_list|)
condition|)
name|LF_CLR
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
expr_stmt|;
else|else
block|{
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
condition|)
block|{
name|LF_SET
argument_list|(
name|TXT_EOFCHAR
argument_list|)
expr_stmt|;
if|if
condition|(
name|v_txt_auto
argument_list|(
name|sp
argument_list|,
name|sp
operator|->
name|lno
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
name|tp
argument_list|)
condition|)
goto|goto
name|err
goto|;
block|}
name|txt_prompt
argument_list|(
name|sp
argument_list|,
name|tp
argument_list|,
name|prompt
argument_list|,
name|flags
argument_list|)
expr_stmt|;
block|}
for|for
control|(
name|carat_st
operator|=
name|C_NOTSET
init|;
condition|;
control|)
block|{
if|if
condition|(
name|v_event_get
argument_list|(
name|sp
argument_list|,
operator|&
name|ev
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
condition|)
goto|goto
name|err
goto|;
comment|/* Deal with all non-character events. */
switch|switch
condition|(
name|ev
operator|.
name|e_event
condition|)
block|{
case|case
name|E_CHARACTER
case|:
break|break;
case|case
name|E_ERR
case|:
goto|goto
name|err
goto|;
case|case
name|E_REPAINT
case|:
case|case
name|E_WRESIZE
case|:
continue|continue;
case|case
name|E_EOF
case|:
name|rval
operator|=
literal|1
expr_stmt|;
comment|/* FALLTHROUGH */
case|case
name|E_INTERRUPT
case|:
comment|/* 			 * Handle EOF/SIGINT events by discarding partially 			 * entered text and returning.  EOF returns failure, 			 * E_INTERRUPT returns success. 			 */
goto|goto
name|notlast
goto|;
default|default:
name|v_event_err
argument_list|(
name|sp
argument_list|,
operator|&
name|ev
argument_list|)
expr_stmt|;
goto|goto
name|notlast
goto|;
block|}
comment|/* 		 * Deal with character events. 		 * 		 * Check to see if the character fits into the input buffer. 		 * (Use tp->len, ignore overwrite and non-printable chars.) 		 */
name|BINC_GOTO
argument_list|(
name|sp
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|lb_len
argument_list|,
name|tp
operator|->
name|len
operator|+
literal|1
argument_list|)
expr_stmt|;
switch|switch
condition|(
name|ev
operator|.
name|e_value
condition|)
block|{
case|case
name|K_CR
case|:
comment|/* 			 * !!! 			 * Historically,<carriage-return>'s in the command 			 * weren't special, so the ex parser would return an 			 * unknown command error message.  However, if they 			 * terminated the command if they were in a map.  I'm 			 * pretty sure this still isn't right, but it handles 			 * what I've seen so far. 			 */
if|if
condition|(
operator|!
name|F_ISSET
argument_list|(
operator|&
name|ev
operator|.
name|e_ch
argument_list|,
name|CH_MAPPED
argument_list|)
condition|)
goto|goto
name|ins_ch
goto|;
comment|/* FALLTHROUGH */
case|case
name|K_NL
case|:
comment|/* 			 * '\' can escape<carriage-return>/<newline>.  We 			 * don't discard the backslash because we need it 			 * to get the<newline> through the ex parser. 			 */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_BACKSLASH
argument_list|)
operator|&&
name|tp
operator|->
name|len
operator|!=
literal|0
operator|&&
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
operator|-
literal|1
index|]
operator|==
literal|'\\'
condition|)
goto|goto
name|ins_ch
goto|;
comment|/* 			 * CR returns from the ex command line. 			 * 			 * XXX 			 * Terminate with a nul, needed by filter. 			 */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_CR
argument_list|)
condition|)
block|{
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
index|]
operator|=
literal|'\0'
expr_stmt|;
goto|goto
name|done
goto|;
block|}
comment|/* 			 * '.' may terminate text input mode; free the current 			 * TEXT. 			 */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_DOTTERM
argument_list|)
operator|&&
name|tp
operator|->
name|len
operator|==
name|tp
operator|->
name|ai
operator|+
literal|1
operator|&&
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
operator|-
literal|1
index|]
operator|==
literal|'.'
condition|)
block|{
name|notlast
label|:
name|CIRCLEQ_REMOVE
argument_list|(
name|tiqh
argument_list|,
name|tp
argument_list|,
name|q
argument_list|)
expr_stmt|;
name|text_free
argument_list|(
name|tp
argument_list|)
expr_stmt|;
goto|goto
name|done
goto|;
block|}
comment|/* Set up bookkeeping for the new line. */
if|if
condition|(
operator|(
name|ntp
operator|=
name|text_init
argument_list|(
name|sp
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|,
literal|32
argument_list|)
operator|)
operator|==
name|NULL
condition|)
goto|goto
name|err
goto|;
name|ntp
operator|->
name|lno
operator|=
name|tp
operator|->
name|lno
operator|+
literal|1
expr_stmt|;
comment|/* 			 * Reset the autoindent line value.  0^D keeps the ai 			 * line from changing, ^D changes the level, even if 			 * there were no characters in the old line.  Note, if 			 * using the current tp structure, use the cursor as 			 * the length, the autoindent characters may have been 			 * erased. 			 */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
condition|)
block|{
if|if
condition|(
name|carat_st
operator|==
name|C_NOCHANGE
condition|)
block|{
if|if
condition|(
name|v_txt_auto
argument_list|(
name|sp
argument_list|,
name|OOBLNO
argument_list|,
operator|&
name|ait
argument_list|,
name|ait
operator|.
name|ai
argument_list|,
name|ntp
argument_list|)
condition|)
goto|goto
name|err
goto|;
name|free
argument_list|(
name|ait
operator|.
name|lb
argument_list|)
expr_stmt|;
block|}
elseif|else
if|if
condition|(
name|v_txt_auto
argument_list|(
name|sp
argument_list|,
name|OOBLNO
argument_list|,
name|tp
argument_list|,
name|tp
operator|->
name|len
argument_list|,
name|ntp
argument_list|)
condition|)
goto|goto
name|err
goto|;
name|carat_st
operator|=
name|C_NOTSET
expr_stmt|;
block|}
name|txt_prompt
argument_list|(
name|sp
argument_list|,
name|ntp
argument_list|,
name|prompt
argument_list|,
name|flags
argument_list|)
expr_stmt|;
comment|/* 			 * Swap old and new TEXT's, and insert the new TEXT 			 * into the queue. 			 */
name|tp
operator|=
name|ntp
expr_stmt|;
name|CIRCLEQ_INSERT_TAIL
argument_list|(
name|tiqh
argument_list|,
name|tp
argument_list|,
name|q
argument_list|)
expr_stmt|;
break|break;
case|case
name|K_CARAT
case|:
comment|/* Delete autoindent chars. */
if|if
condition|(
name|tp
operator|->
name|len
operator|<=
name|tp
operator|->
name|ai
operator|&&
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
condition|)
name|carat_st
operator|=
name|C_CARATSET
expr_stmt|;
goto|goto
name|ins_ch
goto|;
case|case
name|K_ZERO
case|:
comment|/* Delete autoindent chars. */
if|if
condition|(
name|tp
operator|->
name|len
operator|<=
name|tp
operator|->
name|ai
operator|&&
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
condition|)
name|carat_st
operator|=
name|C_ZEROSET
expr_stmt|;
goto|goto
name|ins_ch
goto|;
case|case
name|K_CNTRLD
case|:
comment|/* Delete autoindent char. */
comment|/* 			 * !!! 			 * Historically, the ^D command took (but then ignored) 			 * a count.  For simplicity, we don't return it unless 			 * it's the first character entered.  The check for len 			 * equal to 0 is okay, TXT_AUTOINDENT won't be set. 			 */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_CNTRLD
argument_list|)
condition|)
block|{
for|for
control|(
name|cnt
operator|=
literal|0
init|;
name|cnt
operator|<
name|tp
operator|->
name|len
condition|;
operator|++
name|cnt
control|)
if|if
condition|(
operator|!
name|isblank
argument_list|(
name|tp
operator|->
name|lb
index|[
name|cnt
index|]
argument_list|)
condition|)
break|break;
if|if
condition|(
name|cnt
operator|==
name|tp
operator|->
name|len
condition|)
block|{
name|tp
operator|->
name|len
operator|=
literal|1
expr_stmt|;
name|tp
operator|->
name|lb
index|[
literal|0
index|]
operator|=
name|ev
operator|.
name|e_c
expr_stmt|;
name|tp
operator|->
name|lb
index|[
literal|1
index|]
operator|=
literal|'\0'
expr_stmt|;
comment|/* 					 * Put out a line separator, in case 					 * the command fails. 					 */
operator|(
name|void
operator|)
name|putchar
argument_list|(
literal|'\n'
argument_list|)
expr_stmt|;
goto|goto
name|done
goto|;
block|}
block|}
comment|/* 			 * POSIX 1003.1b-1993, paragraph 7.1.1.9, states that 			 * the EOF characters are discarded if there are other 			 * characters to process in the line, i.e. if the EOF 			 * is not the first character in the line.  For this 			 * reason, historic ex discarded the EOF characters, 			 * even if occurring in the middle of the input line. 			 * We match that historic practice. 			 * 			 * !!! 			 * The test for discarding in the middle of the line is 			 * done in the switch, because the CARAT forms are N+1, 			 * not N. 			 * 			 * !!! 			 * There's considerable magic to make the terminal code 			 * return the EOF character at all.  See that code for 			 * details. 			 */
if|if
condition|(
operator|!
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
operator|||
name|tp
operator|->
name|len
operator|==
literal|0
condition|)
continue|continue;
switch|switch
condition|(
name|carat_st
condition|)
block|{
case|case
name|C_CARATSET
case|:
comment|/* ^^D */
if|if
condition|(
name|tp
operator|->
name|len
operator|>
name|tp
operator|->
name|ai
operator|+
literal|1
condition|)
continue|continue;
comment|/* Save the ai string for later. */
name|ait
operator|.
name|lb
operator|=
name|NULL
expr_stmt|;
name|ait
operator|.
name|lb_len
operator|=
literal|0
expr_stmt|;
name|BINC_GOTO
argument_list|(
name|sp
argument_list|,
name|ait
operator|.
name|lb
argument_list|,
name|ait
operator|.
name|lb_len
argument_list|,
name|tp
operator|->
name|ai
argument_list|)
expr_stmt|;
name|memcpy
argument_list|(
name|ait
operator|.
name|lb
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|ai
argument_list|)
expr_stmt|;
name|ait
operator|.
name|ai
operator|=
name|ait
operator|.
name|len
operator|=
name|tp
operator|->
name|ai
expr_stmt|;
name|carat_st
operator|=
name|C_NOCHANGE
expr_stmt|;
goto|goto
name|leftmargin
goto|;
case|case
name|C_ZEROSET
case|:
comment|/* 0^D */
if|if
condition|(
name|tp
operator|->
name|len
operator|>
name|tp
operator|->
name|ai
operator|+
literal|1
condition|)
continue|continue;
name|carat_st
operator|=
name|C_NOTSET
expr_stmt|;
name|leftmargin
label|:
operator|(
name|void
operator|)
name|gp
operator|->
name|scr_ex_adjust
argument_list|(
name|sp
argument_list|,
name|EX_TERM_CE
argument_list|)
expr_stmt|;
name|tp
operator|->
name|ai
operator|=
name|tp
operator|->
name|len
operator|=
literal|0
expr_stmt|;
break|break;
case|case
name|C_NOTSET
case|:
comment|/* ^D */
if|if
condition|(
name|tp
operator|->
name|len
operator|>
name|tp
operator|->
name|ai
condition|)
continue|continue;
if|if
condition|(
name|txt_dent
argument_list|(
name|sp
argument_list|,
name|tp
argument_list|)
condition|)
goto|goto
name|err
goto|;
break|break;
default|default:
name|abort
argument_list|()
expr_stmt|;
block|}
comment|/* Clear and redisplay the line. */
operator|(
name|void
operator|)
name|gp
operator|->
name|scr_ex_adjust
argument_list|(
name|sp
argument_list|,
name|EX_TERM_CE
argument_list|)
expr_stmt|;
name|txt_prompt
argument_list|(
name|sp
argument_list|,
name|tp
argument_list|,
name|prompt
argument_list|,
name|flags
argument_list|)
expr_stmt|;
break|break;
default|default:
comment|/* 			 * See the TXT_BEAUTIFY comment in vi/v_txt_ev.c. 			 * 			 * Silently eliminate any iscntrl() character that was 			 * not already handled specially, except for<tab> and 			 *<ff>. 			 */
name|ins_ch
label|:
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_BEAUTIFY
argument_list|)
operator|&&
name|iscntrl
argument_list|(
name|ev
operator|.
name|e_c
argument_list|)
operator|&&
name|ev
operator|.
name|e_value
operator|!=
name|K_FORMFEED
operator|&&
name|ev
operator|.
name|e_value
operator|!=
name|K_TAB
condition|)
break|break;
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
operator|++
index|]
operator|=
name|ev
operator|.
name|e_c
expr_stmt|;
break|break;
block|}
block|}
comment|/* NOTREACHED */
name|done
label|:
return|return
operator|(
name|rval
operator|)
return|;
name|err
label|:
name|alloc_err
label|:
return|return
operator|(
literal|1
operator|)
return|;
block|}
end_function

begin_comment
comment|/*  * txt_prompt --  *	Display the ex prompt, line number, ai characters.  Characters had  *	better be printable by the terminal driver, but that's its problem,  *	not ours.  */
end_comment

begin_function
specifier|static
name|void
name|txt_prompt
parameter_list|(
name|sp
parameter_list|,
name|tp
parameter_list|,
name|prompt
parameter_list|,
name|flags
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|TEXT
modifier|*
name|tp
decl_stmt|;
name|ARG_CHAR_T
name|prompt
decl_stmt|;
name|u_int32_t
name|flags
decl_stmt|;
block|{
comment|/* Display the prompt. */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_PROMPT
argument_list|)
condition|)
operator|(
name|void
operator|)
name|printf
argument_list|(
literal|"%c"
argument_list|,
name|prompt
argument_list|)
expr_stmt|;
comment|/* Display the line number. */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_NUMBER
argument_list|)
operator|&&
name|O_ISSET
argument_list|(
name|sp
argument_list|,
name|O_NUMBER
argument_list|)
condition|)
operator|(
name|void
operator|)
name|printf
argument_list|(
literal|"%6lu  "
argument_list|,
operator|(
name|u_long
operator|)
name|tp
operator|->
name|lno
argument_list|)
expr_stmt|;
comment|/* Print out autoindent string. */
if|if
condition|(
name|LF_ISSET
argument_list|(
name|TXT_AUTOINDENT
argument_list|)
condition|)
operator|(
name|void
operator|)
name|printf
argument_list|(
literal|"%.*s"
argument_list|,
operator|(
name|int
operator|)
name|tp
operator|->
name|ai
argument_list|,
name|tp
operator|->
name|lb
argument_list|)
expr_stmt|;
operator|(
name|void
operator|)
name|fflush
argument_list|(
name|stdout
argument_list|)
expr_stmt|;
block|}
end_function

begin_comment
comment|/*  * txt_dent --  *	Handle ^D outdents.  *  * Ex version of vi/v_ntext.c:txt_dent().  See that code for the (usual)  * ranting and raving.  This is a fair bit simpler as ^T isn't special.  */
end_comment

begin_function
specifier|static
name|int
name|txt_dent
parameter_list|(
name|sp
parameter_list|,
name|tp
parameter_list|)
name|SCR
modifier|*
name|sp
decl_stmt|;
name|TEXT
modifier|*
name|tp
decl_stmt|;
block|{
name|u_long
name|sw
decl_stmt|,
name|ts
decl_stmt|;
name|size_t
name|cno
decl_stmt|,
name|off
decl_stmt|,
name|scno
decl_stmt|,
name|spaces
decl_stmt|,
name|tabs
decl_stmt|;
name|ts
operator|=
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_TABSTOP
argument_list|)
expr_stmt|;
name|sw
operator|=
name|O_VAL
argument_list|(
name|sp
argument_list|,
name|O_SHIFTWIDTH
argument_list|)
expr_stmt|;
comment|/* Get the current screen column. */
for|for
control|(
name|off
operator|=
name|scno
operator|=
literal|0
init|;
name|off
operator|<
name|tp
operator|->
name|len
condition|;
operator|++
name|off
control|)
if|if
condition|(
name|tp
operator|->
name|lb
index|[
name|off
index|]
operator|==
literal|'\t'
condition|)
name|scno
operator|+=
name|COL_OFF
argument_list|(
name|scno
argument_list|,
name|ts
argument_list|)
expr_stmt|;
else|else
operator|++
name|scno
expr_stmt|;
comment|/* Get the previous shiftwidth column. */
name|cno
operator|=
name|scno
expr_stmt|;
name|scno
operator|-=
operator|--
name|scno
operator|%
name|sw
expr_stmt|;
comment|/* 	 * Since we don't know what comes before the character(s) being 	 * deleted, we have to resolve the autoindent characters .  The 	 * example is a<tab>, which doesn't take up a full shiftwidth 	 * number of columns because it's preceded by<space>s.  This is 	 * easy to get if the user sets shiftwidth to a value less than 	 * tabstop, and then uses ^T to indent, and ^D to outdent. 	 * 	 * Count up spaces/tabs needed to get to the target. 	 */
for|for
control|(
name|cno
operator|=
literal|0
operator|,
name|tabs
operator|=
literal|0
init|;
name|cno
operator|+
name|COL_OFF
argument_list|(
name|cno
argument_list|,
name|ts
argument_list|)
operator|<=
name|scno
condition|;
operator|++
name|tabs
control|)
name|cno
operator|+=
name|COL_OFF
argument_list|(
name|cno
argument_list|,
name|ts
argument_list|)
expr_stmt|;
name|spaces
operator|=
name|scno
operator|-
name|cno
expr_stmt|;
comment|/* Make sure there's enough room. */
name|BINC_RET
argument_list|(
name|sp
argument_list|,
name|tp
operator|->
name|lb
argument_list|,
name|tp
operator|->
name|lb_len
argument_list|,
name|tabs
operator|+
name|spaces
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* Adjust the final ai character count. */
name|tp
operator|->
name|ai
operator|=
name|tabs
operator|+
name|spaces
expr_stmt|;
comment|/* Enter the replacement characters. */
for|for
control|(
name|tp
operator|->
name|len
operator|=
literal|0
init|;
name|tabs
operator|>
literal|0
condition|;
operator|--
name|tabs
control|)
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
operator|++
index|]
operator|=
literal|'\t'
expr_stmt|;
for|for
control|(
init|;
name|spaces
operator|>
literal|0
condition|;
operator|--
name|spaces
control|)
name|tp
operator|->
name|lb
index|[
name|tp
operator|->
name|len
operator|++
index|]
operator|=
literal|' '
expr_stmt|;
return|return
operator|(
literal|0
operator|)
return|;
block|}
end_function

end_unit

